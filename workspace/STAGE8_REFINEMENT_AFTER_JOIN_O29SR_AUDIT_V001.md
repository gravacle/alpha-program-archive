# AUDIT — STAGE 8 REFINEMENT AFTER JOINING (O29SR). DEFAULT-REFUTE, RE-DERIVED AT BYTES.

Commission O29SR, REFINEMENT-AUDIT, 2026-08-15. DETERMINATION ONLY.
Testimony given zero weight. Every quotation re-read at bytes; every homological
computation re-run independently in a fresh venv.
Nothing here is proposed, authored, adopted, constructed, or repaired.

## VERDICT — CONFIRMED-WITH-CORRECTIONS

```text
OVERALL:  CONFIRMED-WITH-CORRECTIONS

THE TARGET'S BOTTOM LINE SURVIVES DEFAULT-REFUTE.  The three generators are
inter-cell inert at bytes; the composition "refine after join" has no object of
record to consume; the joined network's cycle rank is 0 and no sequence of the
three moves it.  I re-derived all of it independently and every load-bearing
number reproduced EXACTLY.

FOUR CORRECTIONS ARE REQUIRED, NONE OF WHICH OVERTURNS THE VERDICT:

X-1  A DIMENSION IS MIXED INSIDE ONE DISPLAY.  "Refinement multiplies the cells
     (1 -> 16 / 24 / 1232)" takes 16 and 24 from the TOP-CELL column and 1232
     from the 2-FACE column.  1232 is a count of TRIANGLES, not of cells of the
     kind 16 and 24 are.  The consistent top-cell figure is 384, which I compute
     below.  (§A.3)  Hazard (A), dimensional form.  MATERIAL, NOT FATAL.

X-2  AN UNMARKED ELISION INSIDE A QUOTED SPAN, ON THE SIDE THAT HELPS THE
     ARGUMENT.  The certificate's A2-scope sentence is quoted to
     "no unexhibited class member is claimed" and stops there; the sentence
     continues ", though the telescoping mechanism above is instance-independent
     for any member whose child edges chain each parent edge."  (§A.4)
     Hazard (F).  The restored clause does NOT reopen the escape.

X-3  A SECOND UNMARKED ELISION, AND A MISATTRIBUTED ANTECEDENT.  NET :892-893 is
     quoted from mid-sentence, dropping "This does not make the commissioned maps
     schematic:".  In context "It limits what the tower proves" refers to the
     TYPE-U autonomy gap of §9.6, not to the two-node scope the target enlists it
     for.  (§A.5)  Hazard (F).

X-4  THE CORPUS'S OWN ANCHOR H^1 DISPLAY ON THE VERY OBJECTS COMPUTED IS NOT
     REPORTED.  Hunt G-10 (:269-271) records dim H^1 = 0 on all five sealed
     single-cell complexes, "the anchor result".  The target's headline "THE
     GENERATORS ARE NOT INERT — AT THE CELL LEVEL" is true only for the
     1-skeleton invariant and is FALSE for the corpus's anchor invariant.  The
     target flags the invariant choice twice but never against G-10, which it had
     open and quoted from.  (§A.6)  Hazard (A).  Correction STRENGTHENS the
     verdict.

PLUS a class of line-citation slippage (§A.10): several ranges overshoot or
undershoot the text they label, including one that lands on a blank line and a
horizontal rule.  Quotations themselves are accurate; the addresses are not.

NOT FOUND, AFTER LOOKING HARD FOR EACH:
  no cell-rank reported as a network rank or vice versa (hazard A, primary form);
  no escape resting on cell count rising (hazard B);
  no no-escape asserted from silence (hazard C);
  no form argument resting on the two-cell case (hazard D) — the target REFUSES
    that verdict and its refusal is correct at bytes;
  no fourth cell-yielding rule beyond the one the target already flags (hazard E);
  no proposal, adoption, or constructed composite (hazard G);
  no lens token, no program quantity, no measured constant (hazard H).
```

```text
FENCES HELD.  alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false.
No value, no number as a program quantity, no measured-constant comparison.
Every integer below is a cell count, a face count, an incidence-matrix rank, a
component count, or a cycle rank.  No git action.  No register/tracker/road/plan/
continuation file read.  Nothing repaired.
```

---

## 0. PREFLIGHT

Output name probed ABSENT before any write:
`STAGE8_REFINEMENT_AFTER_JOIN_O29SR_AUDIT_V001.md` — ABSENT, and its
`.seal.sha256` — ABSENT.

### 0.1 TARGET SEAL — verified FROM THE ARTIFACT'S OWN DIRECTORY

```text
cd "/Users/bgm/MB Work/alpha-program-archive/workspace"
shasum -a 256 -c STAGE8_REFINEMENT_AFTER_JOIN_O29SR_V001.md.seal.sha256
  STAGE8_REFINEMENT_AFTER_JOIN_O29SR_V001.md: OK
  c28d5c2d684aca20634051c36ab7db532136cc0df846348f936a1ca42d17f6ec
```

### 0.2 CONSUMED SEALS — all verified the same way, same directory

```text
STAGE8_INGREDIENT_CENSUS_O17SR_AUDIT_V001.md            OK
STAGE8_INGREDIENT_CENSUS_O17SR_V001.md                  OK
STAGE8_GLUING_CANDIDATE_O23SR_V001.md                   OK
STAGE8_GLUING_CANDIDATE_O23SR_AUDIT_V001.md             OK
STAGE8_JOIN_ADMISSIBILITY_O28SR_V001.md                 OK
STAGE8_JOIN_ADMISSIBILITY_O28SR_AUDIT_V001.md           OK
STAGE8_D0_SQUARE_CERTIFICATE_V001.md                    OK
STAGE8_D0_SQUARE_CHECK_V001.md                          OK
STAGE8_B1A_REFINEMENT_CARRIER_DARIO_V001.md             OK
STAGE8_GLUED_TOPOLOGY_HUNT_V001.md                      OK
```

### 0.3 THE TARGET'S HASH-RESOLUTION OF NET — INDEPENDENTLY REPRODUCED

```text
my own shasum -a 256 over the four candidates:
  V001  87f696261651567e04242abc1a54d5a2b457a19e07926e9e9856b02dc1719eb1
  V002  9b2e42f8c1cf2791ce859ca8be64864dbce84d87a357426989db083b0db8641f
  V003  51724fae30f6985d4dbf0b4df72a47d0c4ede8dfecdabfd8266d9a60e18296ec
  V004  69f4d93b9f84075a3112fb011b9838f380fb8c0341610572170cf7a13d5aed08

The pin is real and is where the target says it is.  O23SR :68-70 verbatim:
  "The hunt's import line :56 pins NET by full hash `87f696261651567e...`;
   `shasum -a 256` over the four candidates returns `87f69626` for **V001**
   only (V002 `9b2e42f8`, V003 `51724fae`, V004 `69f4d93b`)."
EVERY ONE OF THE FOUR DIGESTS MATCHES MINE.  NET = V001 CONFIRMED.
V002/V003/V004 hashed only; NOT read for content by me either.
```

### 0.4 Declared scoped sweep, and the CUTOFF

```text
SWEEP CUTOFF (UTC):  2026-08-15T23:40:00Z
R1  workspace/                                    top-level *.md
R2  ...alpha_fundamental_record_action_cleanroom_v003/   *.md
Sweeps declared in §A.9.  Scoped reads only.
NO register / tracker / road / plan / continuation / ledger / lens file was
opened, listed, or grepped.
"Q-..." items: EXPECTED-UNLOCATABLE per commission; none sought, none read.
```

---
## A. THE HUNT, HARDEST FIRST

### A.1 HAZARD (A) — THE LEVEL CONFLATED. *** NOT FOUND IN ITS PRIMARY FORM. ***

This is the standing hazard, so I checked every computation in the target for
which object it is computed on.

```text
TARGET §1.5   labelled CELL LEVEL     17 / 136 / 50 / 464
TARGET §3.2   labelled NETWORK LEVEL  0
TARGET §3.3   labelled NETWORK LEVEL  [0] over 1600 sequences
TARGET §3.4   labelled COUNTERFACTUAL 0 / 0 / 0
TARGET §4.2   labelled FORM, symbolic (N-1)(N-2)/2

NO CELL-LEVEL NUMBER IS EVER REPORTED AS A NETWORK NUMBER, AND NO NETWORK NUMBER
IS EVER REPORTED AS A CELL NUMBER.  The two sets are never combined, compared, or
substituted.  I checked each of the five displays against its own construction.
```

The target's identification of the parent 4-cube with ONE CELL — which is what
lets it call 17/136/50/464 "cell level" — is not its own reading. O28SR :865-866
does exactly this with a `K`-named object:

```text
  the family's index   b_1(C_j)   counts the 0-CELLS INSIDE ONE CELL
                                  (K_square: four vertices, b_1 = 1)
```

`K_square` is a `K`-named complex treated as ONE CELL whose internal vertices are
counted. The target's usage is the corpus's usage. **NOT A CONFLATION.**

Two *dimensional* level defects do exist, and they are X-1 and X-4 below.

### A.2 HAZARD (B) — AN ESCAPE CLAIMED WHERE THE SUBDIVISION IS INTERNAL. *** NOT FOUND. ***

The target claims NO escape, so the hazard can only bite as a *failure to display*
the incidence pattern. It does display it, and I re-derived the display at bytes.

I rebuilt each generator's `sd*_1` from the certificate's own script and read,
for every one of the 32 parent-edge rows, which child cells the row touches:

```text
GENERATOR  ROW FOR PARENT EDGE e = (s,t)     CHILDREN TOUCHED     ALL INSIDE e?
A1         a=2s, m=a+e_i, b=2t;
           deposits (a,m) and (m,b)          the 2 child edges    YES  (cert :390-391)
A2-F       deposits fei[e]                   e itself             YES  (cert :407)
A2-B       face := e as a face;
           deposits +(s,face), -(t,face)     the 2 half-edges     YES  (cert :421-422)

NOT ONE ROW OF ANY OF THE THREE sd*_1 MATRICES TOUCHES A CELL OUTSIDE ITS OWN
PARENT EDGE.  Verified by reconstruction, not by reading the target's summary.
```

And the incidence pattern before and after, which the commission demands be
displayed:

```text
                       NODES (cells)   INTER-CELL EDGES   NETWORK CYCLE RANK
BEFORE any refinement        2                1                   0
AFTER any word over
  {A1, A2-F, A2-B} on
  either or both cells       2                1                   0
UNCHANGED.  No generator emits an edge with ends in two different parents,
because every deposit above is indexed by a single parent edge.
```

**AND THE TARGET KILLS THE CELL-COUNT ESCAPE ITSELF**, which is what the
commission asks be killed. Its §3.4 grants the node-multiplication reading and
still gets 0; its :496-499 states the reason flatly — *"Multiplying the cells does
not by itself multiply the CYCLES; it multiplies the nodes, and nodes and edges
grow together along a tree."* I reproduced every counterfactual rank as 0 (§B.6).
**NO ESCAPE RESTS ON THE CELL COUNT RISING.**

### A.3 X-1 — A DIMENSION IS MIXED INSIDE ONE DISPLAY. *** CORRECTION. ***

The target's §4.1 and its VERDICT line :891 both display:

```text
parent K   ONE oriented unit 4-cube.
A1         16 subcubes          (certificate :157 "81 / 216 / 16 subcubes")
A2-F       24 simplices         (certificate :158)
A2-B       1232 triangles       (crosscheck [4056,5082), F'_2 column)
```
and restate it as `1 -> 16 / 24 / 1232`.

**16 AND 24 ARE TOP-DIMENSIONAL CELLS. 1232 IS A COUNT OF 2-FACES.** The
certificate's crosscheck table, quoted at cert :119-122, has a dedicated `F'_2`
column, and 1232 is the A2-B entry in it — the same column in which A1 reads
**216**, not 16, and A2-F reads **110**, not 24. The target takes two rows from
the top-cell column and one from the 2-face column and prints them as one series
of "cells".

I computed all six figures independently and every one reproduces:

```text
                    TOP CELLS      F'_2 (2-faces)
A1                        16                 216     both reproduced exactly
A2-F                      24                 110     both reproduced exactly
A2-B                     384                1232     both reproduced exactly

THE CONSISTENT TOP-CELL SERIES IS   1 -> 16 / 24 / 384.
THE CONSISTENT 2-FACE SERIES IS          216 / 110 / 1232.
THE TARGET'S SERIES 16 / 24 / 1232 IS NEITHER.
```

A2-B's 384 is the number of maximal flags
`vertex < edge < square < 3-cube < 4-cube` in the face poset of the 4-cube; I
enumerated them. The target's own §3.4 then carries 1232 forward as A2-B's
"children" count, alongside A1's 16 and A2-F's 24.

```text
DOES THIS OVERTURN ANYTHING?  NO.
The counterfactual's rank is 0 for EVERY N — a connected graph on N nodes with
N-1 edges is a tree whatever N is.  I ran it at both 1232 and the correct 384
and both give CYCLE RANK = 0.
SO THE CONCLUSION IS UNDISTURBED AND THE DISPLAY IS WRONG.
It is recorded because this is the commission's standing hazard wearing a
dimensional coat, and because "refinement multiplies the cells by 1232-fold" is
a sentence a later reader would carry off as a cell count.
```

### A.4 X-2 — HAZARD (F). AN UNMARKED ELISION, TAIL OF A QUOTED SENTENCE.

Eight builds today have elided material from inside quoted spans, so I read every
quoted span to its own sentence end. **One tail elision and one head elision
survive.** This is the tail one.

The target's §2.3 presents, as the certificate's own voice:

```text
**Scope of the A2 class.** V011 licenses `A2` as a class ("oriented simplicial/
barycentric subdivision"). Both sealed instances of record (`A2-F`, `A2-B` ...)
are certified by explicit algebra. The certificate binds to these exhibited
instances; no unexhibited class member is claimed
```

The certificate at :231-236 reads, and the closing sentence does not stop there:

```text
**Scope of the A2 class.** V011 licenses `A2` as a class ("oriented simplicial/
barycentric subdivision"). Both sealed instances of record (`A2-F`, `A2-B` — the two the
crosscheck constructed and the posing consumed) are certified by explicit algebra. The
certificate binds to these exhibited instances; no unexhibited class member is claimed,
though the telescoping mechanism above is instance-independent for any member whose
child edges chain each parent edge.
```

```text
WHAT WAS DROPPED, AND WHY IT MATTERS.
DROPPED:  ", though the telescoping mechanism above is instance-independent for
           any member whose child edges chain each parent edge."
The parenthetical elision earlier in the span IS marked with "...".  THIS ONE IS
NOT MARKED AT ALL — the quotation simply ends, mid-sentence, at the comma.
The target then argues from it: "the certificate expressly declines to claim one."
The restored clause shows the certificate does NOT decline across the board — it
declines to claim unexhibited INSTANCES while asserting the MECHANISM is
instance-independent for a described family.  The elision falls on the side that
helps the target's leg two.
```

**BUT THE RESTORED CLAUSE DOES NOT REOPEN THE ESCAPE, AND I SAY SO PLAINLY.** The
instance-independence it grants is restricted to members *"whose child edges chain
each parent edge"* — which is the parent-local condition itself. Any member the
clause reaches is therefore inter-cell inert by the same summation-index argument.
**The elided text, restored, cuts the target's way on the merits and against it on
method.** It is recorded because unmarked elision inside a quoted span is the
defect under standing watch, not because the conclusion moves.

### A.5 X-3 — HAZARD (F). A HEAD ELISION, AND AN ANTECEDENT THAT DOES NOT REFER.

The target's §4.3 offers three NET quotations in support of "the corpus itself says
the scope is a scope". The third:

```text
  NET V001 :892-893, verbatim:
    "every member of the matched two-node class is instantiated. It limits what
     the tower proves."
```

NET :892-893 at bytes:

```text
This does not make the commissioned maps schematic: every member of the
matched two-node class is instantiated. It limits what the tower proves.
```

```text
TWO DEFECTS IN ONE SPAN.

(1) HEAD ELISION, UNMARKED.  The quotation begins mid-sentence, after a colon,
    dropping "This does not make the commissioned maps schematic:".  There is no
    ellipsis.  The dropped clause is NET's own mitigation — it is the half that
    says the instantiation is COMPLETE within its class.  Removing it converts a
    reassurance-plus-caveat into a bare caveat.

(2) THE ANTECEDENT OF "It" IS NOT THE TWO-NODE SCOPE.  The sentence sits at the
    close of NET §9.6, "History-update attack", immediately after this block
    (NET :887-890):
        V001_IS_A_COMPLETE_EMISSION_LAW = true [PROPOSAL STATUS]
        V001_IS_A_COMPLETE_AUTONOMOUS_NETWORK_DYNAMICS = false | TYPE-U
    "It limits what the tower proves" refers to the TYPE-U autonomy gap — that
    d_J/d_R are emission maps and not a complete autonomous network evolution
    (NET :883-885).  It is not a sentence about node count.
    The target enlists it under a heading about the TWO-NODE SCOPE.
```

**DOES THE POINT IT IS ENLISTED FOR SURVIVE WITHOUT IT? YES.** The other two
quotations in the same list carry the claim on their own, and both verify exactly
at bytes: NET :347-348 *"V001 instantiates only the matched two-node class, so no
permutation or intertwiner is selected."* and NET :340-344's subjunctive NS-8. The
target's conclusion — that two is an instantiation scope and not a structural
ceiling — is **correct at bytes and independently confirmed by me**. Only the third
support is misused.

### A.6 X-4 — THE CORPUS'S ANCHOR H^1 ON THE VERY OBJECTS COMPUTED, UNREPORTED.

The target's §1.5 is headed **"THE GENERATORS ARE NOT INERT — AT THE CELL LEVEL"**
and displays `17 -> 136, 50, 464`, calling that *"the whole point of the level
distinction."*

Those are 1-skeleton cycle ranks. The target flags this twice — §1.5's closing
STATED LIMITATION and F-4 — and both flags are honest. **But the corpus carries an
H^1 display on precisely these five objects, in an artifact the target consumed
and quoted from, and it is nowhere in the target.** Hunt :269-271, verbatim:

```text
G-10 THE WORKING CLASS (AND 38bbb9fc, carried).  CONN.  The five sealed single-cell
     complexes (parent K, A1, A2-F, A2-B, Z): dim H^1 = 0 exactly on every one —
     the anchor result, not recomputed here.
```

I attached the parent's 24 square faces and computed H^1 exactly, in the fresh
venv, checking `d_1 d_0 = 0` first:

```text
parent K:  d_1 d_0 == 0 ?                                 True
           E = 32   rank(d_0) = 15   rank(d_1) = 17
           1-skeleton cycle rank  E - rank(d_0)         = 17
           H^1 with the 24 faces attached                = 0
           hunt G-10 anchor "dim H^1 = 0 on every one"   MATCH = True
```

```text
THE CORRECTION, STATED EXACTLY.
On the corpus's ANCHOR invariant the generators are INERT at the cell level too:
0 -> 0 -> 0 -> 0.  The target's headline is invariant-relative and, on the
invariant the hunt calls "the anchor result", FALSE.
The target's own C-1 defends its choice by saying "the corpus's own displays
compute the graph invariant (O28SR AUDIT :189-196, on a complex with NO 2-cell)".
That display is on K_square, which HAS no 2-cells — so the two invariants coincide
there trivially and it is no evidence about objects that DO have 2-cells.  The
parent 4-cube has 24.
THIS CORRECTION STRENGTHENS THE VERDICT.  If the generators are inert at the cell
level as well as the network level, the escape is narrower still, not wider.
I therefore record it as a correction the target should have made against itself,
not as a defect in its answer.
```

### A.7 HAZARD (C) — NO-ESCAPE FROM SILENCE. *** NOT FOUND. THE DISTINCTION IS DRAWN FROM CLAUSES. ***

The commission requires that NO-ESCAPE-BY-COMPOSITION be shown from a clause and
not from the corpus merely being quiet. **Both of the target's legs are affirmative
and I verified each at its own bytes.**

```text
LEG ONE rests on an affirmative existential denial, not on silence:
  O28SR :894-895, verbatim, exact to the period —
    "NOTHING SEALED EXISTS: a physical-stratum glued multi-cell record
     complex."
  The same clause stands at O28SR :506 (quoting Hunt :446) and heads a longer
  enumeration at O28SR :621-623.  Hunt :274-281 states it in its own voice:
  "At the PHYSICAL write-carrier stratum ... NO glued multi-cell complex exists".
  THIS IS A CLAUSE THAT SAYS THE OBJECT IS ABSENT.  NOT SILENCE.

LEG TWO rests on constructions I re-read at bytes, not on absence of text:
  every one of the three generators reads ONLY product((0,1,2),repeat=4) or the
  4-cube's own 16 Boolean vertices (cert :372, :394-396, :410).  I rebuilt all
  three and confirmed no other input is consumed.
  THIS IS A POSITIVE READING OF A DOMAIN.  NOT SILENCE.
```

And the target explicitly refuses to convert its own N>=3 arithmetic into an
escape, on the correct ground. Its :624-627 cites O28SR AUDIT E-3, which I verified
at :507 verbatim: *"NEITHER FORECLOSED NOR TAKEN IS NOT AN ESCAPE."* The target
records the arithmetic and stops. **That is the distinction the commission asks
for, drawn in the right direction.**

### A.8 HAZARD (D) — THE FORM ARGUMENT. *** THE TARGET DECLINES IT, AND THE DECLINE IS CORRECT. ***

The commission warns that a verdict resting on "the joining rule's form forces a
tree at any number of cells" must be re-derived independently and must not rest on
the two-cell case. **The target does not select that verdict. It refuses it, and
gives as its reason that the wording is false at bytes.** I re-derived the
arithmetic from the rule's own statement, symbolically, without consulting the
target's expression:

```text
From row N's own selected and rejected columns (NET :242, both verified verbatim):
  SIMPLE     "weighted edge" REJECTED, Adj_2 is 0/1        -> no parallel edges
  LOOPLESS   "no self-edge" SELECTED                       -> no loops
  SYMMETRIC  "reciprocal"; "Directed graph" REJECTED       -> one undirected edge

max edges on n nodes, simple loopless      =  n*(n-1)/2
max cycle rank at connectivity 1           =  n**2/2 - 3*n/2 + 1
sympy factor                               =  (n-1)*(n-2)/2
simplify( max - (n-1)(n-2)/2 ) == 0        ->  True

  N=1: max edges= 0   MAX CYCLE RANK = 0
  N=2: max edges= 1   MAX CYCLE RANK = 0
  N=3: max edges= 3   MAX CYCLE RANK = 1
  N=4: max edges= 6   MAX CYCLE RANK = 3
  N=5: max edges=10   MAX CYCLE RANK = 6
  N=6: max edges=15   MAX CYCLE RANK = 10
```

**MY EXPRESSION IS IDENTICAL TO THE TARGET'S**, checked symbolically rather than
by eye: `simplify(mine - (n**2/2 - 3*n/2 + 1)) == 0` returns True.

```text
THE VERDICT OPTION "FORCES A TREE AT ANY NUMBER OF CELLS" IS FALSE, AND THE TARGET
IS RIGHT TO REFUSE IT.  At N >= 3 the maximum is strictly positive.  Reciprocity,
no-self-edge and simplicity do not force a tree; the TWO-NODE SCOPE does, and the
scope is a scope — NET :347-348 says so in the flat indicative and I verified it.
The tree is forced AT N=2 and nowhere else.
HAD THE TARGET SELECTED NO-ESCAPE-BY-FORM IT WOULD HAVE CARRIED A FALSE UNIVERSAL
QUANTIFIER INTO THE RECORD.  IT DID NOT.  This is the strongest thing in the
artifact and it survives default-refute intact.
```

### A.9 HAZARD (E) — A GENERATOR MISSED. *** NONE FOUND BEYOND THE ONE ALREADY FLAGGED. ***

I swept independently rather than trusting the target's §0.2.

```text
S-1  R1 top-level *.md, uncapped:
       "edgewise" | "stellar" | "midpoint subdivision" | "sd^n" | "order.complex"
       -> 15 files.  Each hit inspected for a CELL-YIELDING RULE.
S-2  The commissioned ground's own enumeration is decisive and I take it at bytes.
     O17SR AUDIT :122-140 types the cell-yielding rules EXHAUSTIVELY, and there
     are exactly three:
       A1     "TYPE  RULE.  CELLS STAND ON ITS OUTPUT SIDE."   (:129)
       A2-F   "TYPE  RULE.  CELLS STAND ON ITS OUTPUT SIDE."   (:134)
       A2-B   "TYPE  RULE.  CELLS STAND ON ITS OUTPUT SIDE."   (:139)
     and :98-99 states the count: "three of the five are cell-yielding rules."
     A0 is excluded by the certificate itself — "A0 carries no refinement by
     type (795)" (cert :145); composites are the fifth and are not a new rule.
THE TARGET SURVEYED EXACTLY THE THREE THE GROUND NAMES.  NO FOURTH CELL-YIELDING
RULE EXISTS IN THE LICENSED INVENTORY.
```

The one family outside that inventory is the one the target itself flags as F-2,
and I confirm both its existence and its exclusion:

```text
Ref_PL / edgewise subdivision.  RA27_3 FRONTIER :271 verbatim:
  "Ref_PL objects = order-complex/edgewise subdivisions sd_n(G);"
and :277-278 verbatim: "Ref_0 != full Ref_PL/P4 generator category", "inside a
proposal", "A pointer, not a supply."  DIFFERENT INDEX, rooted at Q-408, which
this commission declares EXPECTED-UNLOCATABLE.  I did not survey it either, and
my answer does not cover it.
NOTE ON ITS CITATION: the target's F-2 attributes "CommonRef(n,m) = lcm(n,m)" to
RA27_3 FRONTIER :271-279.  IT IS NOT THERE.  It is of record, at
STAGE8_7A_COMPLETENESS_PROOF_DARIO_V001.md :161 (row L9) and :326.  The fragment
is real; the address is wrong.  Filed under §A.10.
```

### A.10 CITATION SLIPPAGE — THE QUOTATIONS ARE SOUND, THE ADDRESSES ARE NOT

Every quoted string in the target was checked character-for-character against its
source. **No quotation was found to be fabricated, altered, or paraphrased-as-
verbatim.** The two elisions are X-2 and X-3. What remains is address drift, which
I record in full because a later reader following these pointers will not land on
the text:

```text
#   TARGET'S ADDRESS              WHERE THE TEXT ACTUALLY IS        SEVERITY
--  ----------------------------  --------------------------------  --------
1   O28SR AUDIT :215-216          :212-213.  :215 IS A BLANK LINE   HIGH
    (cited twice: verdict block   AND :216 IS A HORIZONTAL RULE.    lands on
    and §2.2)                     The quoted sentence is exact.     no text
2   F-2 "CommonRef(n,m)=lcm(n,m)" COMPLETENESS_PROOF :161, :326.    HIGH
    -> RA27_3 FRONTIER :271-279   Not in RA27_3 at all.             wrong file
3   cert :382-389 for "depositing  the deposits are :390-391;       MEDIUM
    the TWO CHILD EDGES"           :382-389 is setup + loop head
4   cert :419-421 for "one along   the "against" deposit is :422;   MEDIUM
    and one against"               range holds only the "along" one
5   cert :155-158 census check     rows run :155-159; A2-B at :159  LOW
                                   falls outside the cited range
6   cert :55-58 version hazard     :56-58                           LOW
7   cert :237-241 composites       :238-242                         LOW
8   O28SR :861-871 level quote     quoted text ends :868 (overshoot) LOW
9   O23SR :218-222 output side     quoted text ends :221 (overshoot) LOW
10  hunt :266 reciprocal display   sentence wraps to :267           LOW
11  COMPLETENESS :479-488          :480-487 (overshoot both ends)   LOW
12  "the same sentence also        at :621 it HEADS A LONGER         LOW
    stands at :506 and :621"       ENUMERATION, not the same sentence
13  §1.5 "sealed (16, 32)"         the sealed row is "16 / 32 / 24" LOW
                                   — the 24 squares are dropped
```

```text
NONE OF THE THIRTEEN CHANGES A NUMBER OR A CONCLUSION.  Items 1 and 2 are
recorded as HIGH because they point a reader at text that is not there — item 1
at whitespace, item 2 at the wrong artifact — and both are attached to load-
bearing claims (that no glued complex was constructed; that a fourth family is
out of scope).  Both underlying facts are TRUE and I verified both at bytes.
```

---

## B. THE HOMOLOGICAL RE-DERIVATION, INDEPENDENT AND EXACT

Fresh virtual environment, created for this audit and used for nothing else.
Python 3.9.6, sympy 1.14.0, exact `Integer` only, **no floats anywhere**. Every
complex rebuilt from the sealed constructive spans — I did not copy a single
matrix, count, or rank out of the target or any other artifact. Cycle rank taken
as `E - rank(d_0)` with `(d_0 lambda)_e = lambda_t - lambda_s` per V011
`[44595,44690)`, cross-checked against `E - V + components` with components from
an independent exact union-find, and additionally against `rank(d_0) == V - c`.

### B.1 CELL LEVEL — every target number reproduced EXACTLY

```text
                V      E   rank(d0)  comp  E-rank(d0)  E-V+c  agree  rank==V-c
parent K       16     32        15      1          17     17   True       True
A1             81    216        80      1         136    136   True       True
A2-F           16     65        15      1          50     50   True       True
A2-B           81    544        80      1         464    464   True       True

TARGET'S §1.5:  17 / 136 / 50 / 464.   MINE: 17 / 136 / 50 / 464.  IDENTICAL.
```

### B.2 SEALED CENSUS — reproduced from construction, not from the target

```text
parent K   V/E/F2        = 16 / 32 / 24    sealed 16 / 32 / 24 squares    MATCH
A1         V/E/subcubes  = 81 / 216 / 16   sealed 81 / 216 / 16           MATCH
A2-F       V/E/simplices = 16 / 65 / 24    sealed 16 / 65 / 24            MATCH
A2-B       V/E           = 81 / 544        sealed 81 / 544                MATCH
```

### B.3 THE DIMENSION AUDIT BEHIND X-1

```text
                TOP CELLS   F'_2 (2-faces)   crosscheck F'_2   MATCH
A1                     16              216               216   True
A2-F                   24              110               110   True
A2-B                  384             1232              1232   True
A2-B top cells = maximal flags vertex<edge<square<3-cube<4-cube, enumerated = 384
```

### B.4 NETWORK LEVEL

```text
Adj_2 reproduced = [[0, 1], [1, 0]]    sealed NS-14 [[0,1],[1,0]]    MATCH=True
  diagonal zero (no self-edge) = True
  symmetric (reciprocal)       = True
NETWORK  V=2  E=1  rank(d_0)=1  components=1   CYCLE RANK = 0
  E-V+c = 0   agree=True
  corpus display "H^1 = 1 - 2 + 1 = 0" reproduced: 0   agree=True
```

### B.5 EXHAUSTIVE SEQUENCE SWEEP — reproduced

```text
words per cell (len<=3) over {A1, A2-F, A2-B} = 40    ordered pairs = 1600
DISTINCT NETWORK CYCLE RANKS OVER ALL 1600 SEQUENCES = [0]
MAX = 0    ANY SEQUENCE RAISES IT ABOVE ZERO? False
TARGET'S §3.3 DISPLAY REPRODUCED EXACTLY, INCLUDING THE COUNTS 40 AND 1600.
```

**And I agree with the target that the sweep is not the reason.** The reason is
the summation index, which I read at bytes in §A.2: no `sd*_1` row of any of the
three touches a cell outside its own parent, therefore no generator can emit an
inter-cell edge, therefore the network's node set and edge set are refinement-
invariant at every depth — not merely to depth 3.

### B.6 THE COUNTERFACTUAL, AT BOTH THE TARGET'S FIGURE AND THE CORRECT ONE

```text
                  children     V      E   c   CYCLE RANK
A1                      16    16     15   1            0
A2-F                    24    24     23   1            0
A2-B (F'_2 col, 1232) 1232  1232   1231   1            0   <- the target's row
A2-B (top cells, 384)  384   384    383   1            0   <- the correct row
BOTH GIVE ZERO.  X-1 IS A DISPLAY DEFECT, NOT AN ARITHMETIC ONE.
```

### B.7 H^1 WITH 2-CELLS ATTACHED — the check the target did not run

```text
parent K:  d_1 d_0 == 0 ?  True        (chain-complex sanity, run first)
           E=32  rank(d_0)=15  rank(d_1)=17
           1-skeleton cycle rank  = 17
           H^1 (24 faces attached) = 0     hunt G-10 anchor MATCH=True
```

---

## C. HAZARDS (G) AND (H)

```text
(G) ANY PROPOSAL, ADOPTION, OR CONSTRUCTED COMPOSITE.   *** NONE FOUND. ***
    Scanned the target for constructive verbs.  The only hit is a NEGATION, at
    its :856: "NAMED AS FALSIFIERS, NOT AS THINGS TO BUILD.  I propose neither."
    Its §5 DISPLAYS the composites clause as record (cert :238-242, "Not
    recomputed here, per the posing") and constructs nothing.  Its F-6 leaves the
    refuted premise and the false universal STANDING, unrepaired, which is what
    a determination is required to do.
    THE TARGET PROPOSES NOTHING, ADOPTS NOTHING, AND BUILDS NO COMPOSITE.
    NEITHER DO I.

(H) ANY LENS TOKEN; ANY NUMBER OF PROGRAM IMPORT.        *** NONE FOUND. ***
    Scanned for alpha / kappa / coupling / fine-structure / 137 / Planck /
    Newton / lens / hbar / measured-constant tokens.  Every hit in the target is
    inside its own fence declaration or its own negation ("No alpha, kappa,
    coupling, norm, scale, or measured constant is computed, bounded, compared,
    or mentioned as a target").
    EVERY INTEGER IN THE TARGET IS A CELL COUNT, A FACE COUNT, A MATRIX RANK, A
    COMPONENT COUNT, OR A CYCLE RANK.  I checked each one.  SO IS EVERY INTEGER
    IN THIS AUDIT.
```

---
## D. VERDICT PER DELIVERABLE

```text
D-1  THE THREE GENERATORS AT THEIR OWN BYTES        *** CONFIRMED-WITH-CORRECTIONS ***
     All three located, seals OK, constructions rebuilt independently.  All three
     SUBDIVIDE WITHIN ONE CELL; NONE alters inter-cell incidence — verified row by
     row across all 32 parent-edge rows of all three sd*_1 matrices, not accepted
     on the target's summary.  Every sealed census reproduced exactly.
     CORRECTIONS: X-4 (the anchor H^1 display on these same objects is
     unreported, and on it the generators are inert too); address items 3, 4, 5,
     13 in §A.10.

D-2  THE COMPOSITION QUESTION                       *** CONFIRMED-WITH-CORRECTIONS ***
     NOT AVAILABLE is right, and both legs are affirmative clauses rather than
     silence — I verified each at bytes (§A.7).
     CORRECTIONS: X-2 (the tail elision sits inside leg two's supporting quote);
     address item 1 (the "no glued complex was constructed" citation lands on a
     blank line and a horizontal rule).

D-3  THE COMPOSITION GRANTED ARGUENDO               *** CONFIRMED ***
     Joined network V=2 E=1 rank(d_0)=1 c=1 CYCLE RANK 0, reproducing 1-2+1=0.
     All 1600 sequences: DISTINCT RANKS = [0], MAX = 0.  Reproduced exactly,
     including the counts 40 and 1600.  The structural invariance argument is
     sound and I re-derived its premise at bytes.  The arguendo framing is
     labelled everywhere it appears.  NO CORRECTION.

D-4  THE ORDER QUESTION                             *** CONFIRMED-WITH-CORRECTIONS ***
     The orders differ in AVAILABILITY, not result — correct.  The refusal of
     NO-ESCAPE-BY-FORM is correct and is the strongest thing in the artifact;
     I re-derived (N-1)(N-2)/2 symbolically from the rule's own three properties
     and my expression is identical to the target's.
     CORRECTIONS: X-1 (16 / 24 / 1232 mixes top cells with 2-faces; the
     consistent series is 16 / 24 / 384); X-3 (the third NET support is a head
     elision whose "It" refers to the TYPE-U autonomy gap, not to node count).

D-5  STATUS OF EACH GENERATOR                       *** CONFIRMED ***
     A1 / A2-F / A2-B: licensed by a law that self-describes as PROPOSED,
     d_0-square CERTIFIED and independently CONFIRMED (CHECK :9 verbatim,
     "CERT_VERDICT = CONFIRMED."), instance-bound, NOT ADOPTED.  The joiner:
     PROPOSED_NOT_ADOPTED (NET :3, verified).  No recommendation is made, and
     the target says so.  NO CORRECTION beyond address item 7.

OVERALL                                             *** CONFIRMED-WITH-CORRECTIONS ***
```

---

## E. CHOICE LEDGER

```text
C-1  I RE-DERIVED RATHER THAN RE-READ.  Every complex was rebuilt from the sealed
     constructive spans and every rank recomputed.  ALTERNATIVE REJECTED: checking
     the target's arithmetic against the certificate's stored numbers.  REASON:
     testimony carries zero weight under this commission, and the certificate
     discloses at :147-148 that no sealed artifact stores a dense matrix dump — so
     reconstruction is the only route that is not testimony.

C-2  I COMPUTED IN THE TARGET'S INVARIANT FOR COMPARISON — ADOPTING NOTHING, THE
     VERB IS BARRED AND NOT MEANT — AND THEN COMPUTED THE OTHER ONE ANYWAY.
     ALTERNATIVE REJECTED: computing only E - rank(d_0), which is
     what the target displays.  REASON: X-4 is only visible if the second
     invariant is actually computed.  Attaching the 24 faces and getting H^1 = 0
     is what turns "the target chose an invariant" into "the corpus's anchor
     invariant says something the target did not report."

C-3  I TREATED THE RECIPROCAL PAIR AS ONE EDGE.  Same convention as the target,
     and for the same reason: it is the corpus's, displayed at hunt :266-267 and
     requoted at O23SR :143.  ALTERNATIVE REJECTED: counting a->b and b->a
     separately, which would give 2-2+1 = 1.  REASON: that would manufacture a
     cycle out of a counting convention and would contradict the source's own
     displayed value.  I record that THE WHOLE NETWORK-LEVEL RESULT TURNS ON THIS
     CONVENTION, and that it is not the target's invention.

C-4  I RAN THE COUNTERFACTUAL AT BOTH 1232 AND 384.  ALTERNATIVE REJECTED:
     reporting only that the target's figure is the wrong dimension.  REASON: a
     correction that does not show whether the conclusion moves is half a
     correction.  It does not move.

C-5  I DID NOT CONSTRUCT A GLUED COMPLEX WITH CELL INTERIORS, AND DID NOT REPAIR
     ANY DEFECT I FOUND.  ALTERNATIVE REJECTED: building the object so the
     composition could be run literally, and fixing the 1232.  REASON: both are
     authoring, barred to a determination.  X-1 through X-4 are DISPLAYED and
     LEFT STANDING.

C-6  I TOOK THE THREE-GENERATOR SCOPE FROM THE GROUND'S OWN EXHAUSTIVE TYPING
     (O17SR AUDIT :122-140, :98-99) RATHER THAN FROM MY OWN LEXICAL SWEEP ALONE.
     ALTERNATIVE REJECTED: resting hazard (E) on grep results.  REASON: a lexical
     sweep can only ever be evidence of absence; the audit's typed enumeration is
     a clause.  The sweep was run as corroboration and is declared, not relied on.

C-7  I RESOLVED NET BY CONTENT HASH MYSELF.  ALTERNATIVE REJECTED: accepting the
     target's §0.1 hash table.  REASON: zero weight to testimony.  All four
     digests recomputed; all four match; V002/V003/V004 not read for content.
```

---

## F. TOY_SEPARATION

```text
WHAT IS ACTUAL SURFACE IN THIS AUDIT.
 - The three sealed generator constructions.  I rebuilt all three and reproduced
   every sealed census exactly.  REAL, and re-derived rather than accepted.
 - The 32 sd*_1 parent-edge rows.  I read which children each row touches.  This
   is the load-bearing fact of the whole question and it is REAL at bytes.
 - Adj_2 = [[0,1],[1,0]], "no self-edge", the two-node scope.  REAL at NET's bytes.
 - The corpus's anchor H^1 = 0 display (hunt G-10).  REAL, and I recomputed it
   for parent K rather than citing it.
 - (N-1)(N-2)/2.  REAL, re-derived symbolically from row N's own three properties.

WHAT IS NOT SURFACE, AND IS LABELLED AS SUCH EVERYWHERE IT APPEARS HERE.
 - The joined complex with cell interiors.  DOES NOT EXIST.  I did not build one.
 - The node-multiplication reading (§B.6).  NOT OF RECORD.  Computed only to show
   it does not help, at both the target's figure and the correct one.
 - The N >= 3 reciprocal edge family (§A.8).  NOT SELECTED; intertwiners NAMED,
   NOT BUILT; REJECTED in row N's own alternatives column.  Arithmetic displayed,
   family not asserted available.
 - The counterfactual's tree configuration.  The target's §3.4 CHOOSES N-1 pairs;
   that choice is what makes it a tree.  The target says so itself at its :496
   and addresses the alternative in §4.  DISPLAYED PREMISE, NOT A HIDDEN ONE.

NO TOY WAS SUBSTITUTED FOR THE SURFACE, BY THE TARGET OR BY ME.  The question was
whether three specific sealed generators can move a specific sealed number.  I
rebuilt the generators, recomputed the number, and got the same answer the target
got — and separately confirmed that the composition it would require has no object
of record to consume.

NO PHYSICAL QUANTITY APPEARS.  No alpha, kappa, coupling, norm, scale, or measured
constant is computed, bounded, compared, or mentioned as a target.
```

---

## G. IMPORT AUDIT

```text
IMPORTED AND USED — all seals verified from their own directory
  O29SR TARGET     the artifact under audit.                        §A, §B, §D
  O17SR AUDIT      :83 heading; :98-99 the three-of-five count; :122-140 the
                   exhaustive typing; :153-156; :570.               §A.1, §A.9
  D0_SQUARE CERT   :106-127 sd*_1 and the crosscheck table; :134-145 inventory;
                   :155-162 sealed census; :231-242 A2 scope + composites;
                   :365-423 the three constructions.                §A.2-A.4, §B
  D0_SQUARE CHECK  :9 "CERT_VERDICT = CONFIRMED."                   §D
  B1A CARRIER      :93 sd*_1; :111-113 interior cancellation; :187-195 and
                   :423-428 the coframe STOP.                       §A.2, §A.7
  O23SR            :68-70 the hash pin; :143 the reciprocal display;
                   :218-221 the output side.                        §0.3, §E
  NET V001         :3 status; :242 row N; :340-348 NS-8 and the scope;
                   :883-893 §9.6.  Hash-resolved by me.             §A.5, §A.8
  O28SR            :243-250 the level distinction ground; :506; :621-623;
                   :861-871; :894-895.                              §A.1, §A.7
  O28SR AUDIT      :186-197 the K_square computation; :212-213; :507 E-3. §A.6-A.7
  GLUED TOPOLOGY HUNT  :262-267 G-9; :269-271 G-10 the anchor; :274-281. §A.6
  RA27_3 FRONTIER  :268-278, for F-2 only.  Seal OK.                §A.9
  COMPLETENESS PROOF   :161, :326 (CommonRef); :480-487 (F-3).  Seal OK. §A.9

IMPORTED AND NOT USED AS EVIDENCE
  O17SR CENSUS     seal verified; its §2.1 is REFUTED of record, so no finding
                   here rests on it.
  NET V002/V003/V004 — HASHED ONLY, NOT READ FOR CONTENT.

NOT IMPORTED
  Any register, tracker, road, plan, continuation, ledger, or lens file — none
  read, none opened, none listed, none grepped.
  Any "Q-..." file — none sought.  EXPECTED-UNLOCATABLE per commission.
  The TOP-LEVEL V011 copy — the two-byte-version hazard is carried from the
  certificate's own :56-58; the top-level copy was NOT read by me.

MACHINERY  reads; shasum -a 256 -c from each artifact's own directory; scoped
  greps; exact integer linear algebra in a fresh venv built for this audit.  No
  member binding, no fixed-point execution, no end test, no git action, no
  network, no write outside my own two output files.
```

---
## H. FLAG BLOCK

```text
F-1  THE TARGET'S OWN STRONGEST ACT IS ITS REFUSAL, AND THE REFUSAL SURVIVES.
     Declining NO-ESCAPE-BY-FORM because "at any number of cells" is false at
     bytes is the correct call, and it is the call the commission's hazard (D)
     exists to test.  I re-derived (N-1)(N-2)/2 independently and symbolically and
     confirm the refusal.  A build that had taken the stronger-sounding verdict
     would have put a false universal quantifier into the record.  RECORDED AS
     SOUND, WITHOUT INFLATION.

F-2  ONE QUOTED SPAN IN THE TARGET CANNOT BE VERIFIED BY ANYONE BUT ITS PRINCIPAL.
     Its §3.3 quotes "the commission's own words" — "A subdivision that merely
     refines within cells while leaving the inter-cell incidence a tree leaves the
     network rank at zero."  That text is its commission, not an artifact on disk.
     I DID NOT VERIFY IT AND DO NOT TREAT IT AS VERIFIED.  Every other quoted span
     in the target was checked at bytes.  Named so no reader counts it among them.

F-3  THE FOURTH FAMILY REMAINS UNSURVEYED, BY THE TARGET AND BY ME.
     Ref_PL / edgewise subdivision is of record, is a DIFFERENT INDEX, sits inside
     a proposal, and is rooted at Q-408 which this commission declares
     EXPECTED-UNLOCATABLE.  NEITHER OF US SURVEYED IT.  Neither answer covers it.
     The target flagged this itself; I confirm the flag and extend it to my own
     scope rather than let this audit's confirmation read as wider than it is.

F-4  A SECOND-ORDER DOUBT OF RECORD, CARRIED NOT RESOLVED.  COMPLETENESS PROOF
     :480-487 reports zero corpus-wide occurrences of any text equating
     barycentric with order-simplex/Freudenthal/edgewise subdivision, and that
     A2's instantiation may rest on "an unstated identification".  I verified the
     clause.  It does not affect either determination — A2-F and A2-B were built
     and computed SEPARATELY here, from their own byte-spans, and both give
     network rank 0 — but I carry it forward rather than let separate treatment
     paper over it.

F-5  THE WHOLE NETWORK-LEVEL RESULT TURNS ON ONE COUNTING CONVENTION.  If a
     reciprocal pair were counted as TWO edges, the joined pair would read
     2-2+1 = 1 rather than 0.  The convention is the corpus's, displayed at hunt
     :266-267 and requoted at O23SR :143, and both the target and I take it from
     there rather than choosing it.  NAMED BECAUSE IT IS THE SINGLE POINT AT WHICH
     A DIFFERENT READING OF THE RECORD WOULD CHANGE THE ANSWER.

F-6  WHAT WOULD OVERTURN THIS AUDIT.  A generator of record whose sd*_1 has a row
     touching a cell outside its own parent — I checked all 96 rows across the
     three matrices and none does.  Or a selected edge family on N >= 3 with at
     least N pairs — none is of record, and NET :242 rejects the intertwiners such
     a family would need.  NAMED AS FALSIFIERS, NOT AS THINGS TO BUILD.  I propose
     neither and construct neither.

F-7  NO REPAIR PERFORMED.  X-1 through X-4 and all thirteen address items are
     DISPLAYED and LEFT STANDING in the target.  I renumber nothing, correct no
     artifact, author no replacement, and adopt nothing.  The target's own F-6
     likewise left the refuted O17SR premise standing, and that was right.
```

---

## I. VERDICT, RESTATED WITH ITS GROUND

```text
*** CONFIRMED-WITH-CORRECTIONS ***

THE QUESTION AUDITED.  Do the corpus's cell-yielding refinement generators,
applied after joining, produce cycle content at the network level?

THE TARGET ANSWERED NO, ON TWO AFFIRMATIVE LEGS PLUS AN ARGUENDO COMPUTATION.
UNDER DEFAULT-REFUTE, RE-DERIVED AT BYTES, THAT ANSWER HOLDS.

WHAT I REPRODUCED EXACTLY, FROM MY OWN CONSTRUCTIONS:
  cell-level cycle ranks           17 / 136 / 50 / 464
  sealed censuses                  16/32/24 ; 81/216/16 ; 16/65/24 ; 81/544
  joined network                   V=2 E=1 rank(d_0)=1 c=1 CYCLE RANK 0
  the sweep                        40 words, 1600 pairs, DISTINCT RANKS = [0]
  the form arithmetic              (N-1)(N-2)/2, symbolically identical
  the hash resolution of NET       all four digests
EVERY LOAD-BEARING NUMBER IN THE TARGET IS CORRECT.

WHAT I FOUND WRONG, AND LEFT STANDING:
  X-1  16 / 24 / 1232 mixes top cells with 2-faces.  Consistent series: 16/24/384.
  X-2  unmarked tail elision in the certificate's A2-scope quote.
  X-3  unmarked head elision in NET :892-893, plus an antecedent that refers to
       the TYPE-U autonomy gap and not to node count.
  X-4  the corpus's anchor H^1 = 0 on these same objects is unreported; on that
       invariant the generators are inert at the cell level too.
  and thirteen address items, two of which point at no text at all.

NONE OF THESE MOVES THE ANSWER.  X-1's arithmetic gives 0 at either figure; X-2's
restored clause is confined to parent-local members; X-3's point is carried by the
two other quotations, which verify; X-4 narrows the escape rather than widening it.

THE REASON THE ANSWER IS NO IS NOT THE SWEEP AND NOT THE TARGET'S SAY-SO.  It is
that not one of the 96 parent-edge rows across the three sd*_1 matrices touches a
cell outside its own parent.  I read all 96.  A generator that cannot emit an
inter-cell edge cannot change a network whose cycle rank is a function of the node
set and the inter-cell edge set alone.

NO ESCAPE IS ASSERTED AND NONE IS INVENTED.  NO ESCAPE IS FORECLOSED BEYOND WHAT
THE BYTES FORECLOSE.  The Ref_PL family remains unsurveyed by both artifacts and
is named as such.

FENCES AT CLOSE  alpha_computed = false ; proof_authorized = false ;
  kappa_record_computed = false.
  NOTHING PROPOSED.  NOTHING AUTHORED.  NOTHING ADOPTED.  NO RULE AND NO COMPOSITE
  CONSTRUCTED.  NOTHING REPAIRED.  NO VALUE.  NO PROGRAM QUANTITY.  NO
  MEASURED-CONSTANT COMPARISON.  NO GIT.
ALL_RESULTS = CLAIMED until checked.
```

---

## APPENDIX — ENVIRONMENT AND REPRODUCIBILITY

```text
Fresh virtual environment created for this audit; nothing reused, nothing shared
with the target's environment.
  python 3.9.6
  sympy 1.14.0
  exact arithmetic only: sympy Integer.  NO FLOATS ANYWHERE.
  every rank is Matrix.rank() over Q
  every component count is cross-checked by an independent exact union-find
  every cycle rank is cross-checked as E - rank(d_0) against E - V + components,
    and additionally against rank(d_0) == V - components; all agree
  the 2-cell computation checks d_1 d_0 == 0 before reporting H^1

CONSTRUCTIONS were rebuilt from the sealed byte-spans:
  parent K   {0,1}^4 vertices; edges differing in one coordinate, oriented by
             coordinate sum; faces by choice of two varying directions
  A1         cert :372-380
  A2-F       cert :394-396, :398-403
  A2-B       cert :410-414
No matrix, count, or rank was copied from the target or from any other artifact.
The certificate discloses at :147-148 that no sealed artifact stores a dense
matrix dump, so reconstruction from the constructive displays is the only route
that is not testimony, and it is the one taken.
```
