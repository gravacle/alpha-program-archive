# STAGE 8 — AUDIT OF THE THREE-PIN DETERMINATION — V001

## INDEPENDENT ADVERSARIAL AUDITOR — CODENAME PIN-AUDIT — [SEALED]

Date: 2026-08-14 (session UTC 2026-08-15). Target:
`STAGE8_PIN_DETERMINATION_S9AD_V001.md`, seal `09047c2a…` VERIFIED AT PATH
before opening. Commission: not told the outcomes; default REFUTE; work all
three pins independently at bytes, fix per-pin verdicts in a hashed note
BEFORE opening the build; then read once in full and attack.

Fences held throughout: `alpha_computed = false` ; `proof_authorized =
false` ; `kappa_record_computed = false`. DETERMINATION/AUDIT ONLY: nothing
here adopts, seals, or fixes any pin; no member bound, no candidate
authored, no route/branch/address/representative selected, no gate moved.
No file matching register|road_|ledger|lens|plan|tracker|THE_HANDOFF
opened; "Q-…" tokens EXPECTED-UNLOCATABLE. No git; no existing file edited;
this artifact + its seal sidecar are the ONE output pair, at
commission-distinct paths probed ABSENT at session start. Bytes-level and
exact-symbolic only; no CAS; no float; no value frozen.

**SWEEP CUTOFF (this audit's own re-run sweeps): 2026-08-15T03:32:58Z.**
Exhaustiveness is sweep-displayed only.

---

## 0. VERDICT

```text
OVERALL = CONFIRMED-WITH-CORRECTIONS

PIN-1  FORCED-READING(ACTUAL) at the operative sites, residue displayed
       — CONFIRMED. Every byte-site of the forcing chain re-walked and
       found verbatim; my own pre-committed breakers dissolve at sealed
       bytes the prior displays had not walked (§3).
PIN-2-HOL  FORCED-READING(NOT-A-MEMBERSHIP-CLAUSE) — CONFIRMED (§4).
PIN-2-H    FREE, sheet staged PROPOSED-NOT-ADOPTED — CONFIRMED; matches
       my independently pre-fixed verdict exactly; sheet honesty PASS.
PIN-3  FORCED-READING(MANDATORY) — CONFIRMED. The §8.2 anti-tuning row
       and the r_N^D explicit-conditionality contrast are verbatim; my
       pre-committed G2 breakers (rows 20/25 "conditional",
       "failure-capable") dissolve at their own status-column bytes (§5).

CORRECTIONS (all verdict-neutral): O-1..O-4 (§6). None changes any pin
verdict, the payoff display, or any flag.
```

## 1. INDEPENDENCE PROTOCOL, EXECUTED

Per commission, I worked all three pins at bytes FIRST and fixed verdicts
in a scratchpad note, hashed BEFORE the build was opened:

```text
PRE-FIX NOTE sha256 = dbdc94eb3d88467d7cf5b120a30501478db4c377e4ca8ab7b6
                      cbad09aac0e023
  (scratchpad/PIN_AUDIT_PREFIX_VERDICTS.md, session-local)
PRE-FIXED: PIN-1 FREE; PIN-2 FREE (both halves; HOL counter = the DoR
  "AS LAW" summary); PIN-3 FREE at D3 grade with displayed weight toward
  MANDATORY (breakers named: §1.5 "carrying", rows 20/25 "H-only
  conditional", SPEC-12d "failure-capable").
```

DISCLOSURE OF THE FLIPS, per the audit discipline (the right answer, not a
procedurally clean exercise): on walking the build's cited bytes — every
one verified below — my committed breakers for PIN-1 and PIN-3 and for the
HOL half of PIN-2 each FAIL at sealed bytes my pre-work had not walked.
The commission's own breaking rule ("a single sealed consumer coherent
under the other reading breaks the force") was applied to every candidate
breaker; none survives. PIN-2-H (my FREE) agrees with the build. The
flips are displayed with their defeating bytes named, not absorbed.

## 2. SEALS AND BYTE-SITES RE-VERIFIED (this session, independently)

```text
09047c2a  the build                                MATCH (read once, full)
c8053f6c  OB-2 determination                       MATCH (read in full)
d9f4aad0  OB-2 audit                               MATCH (rows/F-A/CL at bytes)
9a0c2045  D3 freeze (precedent)                    MATCH (Part 1 G1-G4 at bytes)
fdf20bd4  JII obstruction                          MATCH (all cited spans walked)
ba9430c1  JII review                               MATCH (K4.2/K5 at bytes)
b53d9e93  LOC build                                MATCH (§1.1 S1-S8, §2.3 (C1),
                                                   §2.5 voids at bytes)
ccb94dfa  KER build                                MATCH ((B2-5/6/8/13/15) at bytes)
3fd4b924  PJ2                                      MATCH (carrier naming :178-180)
9c74ed3d  J7 face/J2 display                       MATCH (seal)
e0b66180  DoR-020-A9                               MATCH (read in full, 21 lines)
ac9335dc  Xi_N V004                                MATCH ((Z1)-(Z3),(D8) at bytes)
d662889b  ExtSrc round 2                           MATCH (seal)
12/12 MATCH ; MISMATCHES = 0. Held construction 506bfe2a untouched.
```

Every load-bearing quotation in the build was located verbatim at its
cited carrier, including: LOC (S2b) intro ("containing the image of
(S2a)… natural embedding"); LOC (S4) intro ("separately typed
SCHWARTZ-KERNEL REALIZATION, with the bundle density and dual-density
conventions fixed"); LOC (S5) intro ("old Q-408 branch REPRODUCED
EXACTLY"); LOC post-(S8) actuality + anti-formal sentence; LOC §2.3 (C1)
"Op_N^(loc,2) is the declared properly supported classical
pseudodifferential operator class of order <=2" (:883); LOC §2.5 void
conditions 1 and 3 verbatim (:950-957, incl. the sealed equivalence
"outside the symbol domain <=> fails the wavefront transversality test");
KER (B2-5)/(B2-6) actual bilocal distributions on M x M, (B2-8) support,
(B2-15) D'_tau seminorms on the actual kernels (:485-491); fdf20bd4 Joint
B gap-typing + machinery appeal "actual wavefront/diagonal/refinement
realization theorem" (:1098-1101); fdf20bd4 §7.5 alternative-1 row
(:1629); fdf20bd4 §8.2 anti-tuning row (:1676, verbatim); fdf20bd4
authority row binding LOC as "complete candidate specification
(S1)–(S25)" (:80); r_N^D explicit conditionality (:192/:954/:1323);
ac9335dc (Z1)/(Z3)/(D8) (:84-124, :707-712); DoR-020-A9 basis line +
"ADOPTION LICENSES; IT INHABITS NOTHING"; OB-2 audit F-A repair sentence
("PROVABLE-shape UNDER PIN B-2(i) (or sealed Sch-linearity), antecedent
undecided", :180-184) and its VERBATIM rows 72-82.

## 3. PIN-1 ATTACKED — FORCED(ACTUAL) SURVIVES

My pre-committed breakers, each re-worked at bytes:

```text
BRK-1 "Joint B holds the abstract door open" (my pre-fix ground; also
  OB-2's displayed ground). DISSOLVES: at Joint B's OWN bytes the joint
  types (JB-2) as microlocal eligibility ("requires the appropriate
  conormal/wavefront, order, density, and principal-symbol structure"),
  names its gap PHYSICAL_KERNEL_TO_LOCAL_OPERATOR_ELIGIBILITY, and
  appeals to an "ACTUAL wavefront/diagonal/refinement realization
  theorem"; §7.5's typed alternative demands "a theorem placing the
  actual Q-408 image in the properly-supported classical order-at-most-
  two domain". Under squares-only every one of these sealed displays is
  wrong-shaped/dead text. The door-open sentence speaks to derivability
  without external imports ("by itself"), not to slot semantics — the
  build's d3 disposal is correct at the bytes.
BRK-2 "D_N^ker is member-supplied, so no tautological inclusion".
  DISSOLVES against the verdict AS STATED: the build forces containment/
  actual-realization at the OPERATIVE sites only ((i)/(ii)) and displays
  the unrepresented-complement residue as unforced (§3.5) — exactly what
  (S2b) "containing", (S5) "reproduced exactly", post-(S8) same-named-
  topology, and §2.5 void 1 carry.
BRK-3 "(SPEC-12a) displays only continuous+injective". DISSOLVES: display
  economy at a consumer vs the authority-table binding of LOC as the
  complete (S1)-(S25) specification; the venue argument is the D3
  precedent's own G4 (T13 pattern). Verified: OB-2 itself consumed the
  LOC (S4) SENTENCE as a sealed Class-1 clause, so prose-is-clause is
  the record's demonstrated practice, and OB-2's contrary name-
  minimalism was an unforced PREMISE (its CL-2), not sealed law.
G2 RE-RUN (my own sweeps, cutoff above): "iota_N^Q408" -> 12 files = the
  build's 11 + the build itself (post-dates its sweep); consistent.
  "structure-only|squares-only|abstract embedding" -> 5 files, every hit
  an unrelated usage (probe/inspection prose); NO sealed byte defines
  either slot as structure-only. G2 stands.
DEAD-TEXT CHECK (the decisive shape): under squares-only, LOC §2.5 void
  condition 1 can never fire (an abstract continuous injection cannot
  "fail to be realized with the fixed bundle/density convention") and
  void 3's sealed equivalence is unstatable — sealed displays with no
  work. Same G3 shape the D3 freeze used. FORCE HOLDS.
```

CONFIRMED, including §3.5's residue display, the anti-convenience
statement (the forcing arms kills AGAINST the D-1-side family), the
reversal mechanics, and the consequence display (checked against OB-2
§4.1/§4.2 and the audit's F-A/C-1: "NG-3 PROVABLE-shape … satisfied on
its first disjunct" is exactly the audit's repair sentence; NG-1 armed at
B-3(iii) alone; nothing fires).

## 4. PIN-2 ATTACKED — SPLIT SURVIVES

HOL half (FORCED, not-membership): my pre-committed counter was the DoR
summary "the topological-charge typing AS LAW". DISSOLVES: the summary's
own parenthetical cashes the typing as exactly the (Z3)/(Z4) clause
content; the DoR's basis line adopts the candidate's "14 clauses"; and
under the membership reading the sealed (Z3) status sentence and the (D8)
[PROVABLE] display ("continuity … is not refuted for the zero map by this
antecedent", verified :710-712) are analyses of a non-member — dead text.
My encirclement sweep re-run matches SW-D: outside ac9335dc/the DoR, every
"encircl*" hit is review/adjudication prose describing candidate content
(GAMMA_H_REVIEW :195; LOC_REVIEW :277,:299 spot-checked at bytes); no
sealed byte defines an encirclement predicate on members. Two sealed
determinations already state the zero-map admission as fact at clause
bytes (OB-2 flag; audit row 79 VERBATIM). CONFIRMED.

H half (FREE): matches my pre-fixed verdict on the same grounds the build
displays (the kappa-only anti-formal asymmetry; the §1.3 refusal of the
nonvanishing exclusion with (STRUCK) not restored; the sealed "ACTUAL
incidence/integration/evaluation rule" lead phrase keeping R-A
admissible; consumers all at the derivability quantifier). SHEET HONESTY
PASS: every consequence row is quoted or directly paraphrased from OB-2
§5.1/§5.3/§4/§6.3 (each checked); both readings carried with FOR-displays
of comparable strength; reversal mechanics displayed in BOTH directions
plus the supersession clause; STATUS PROPOSED-NOT-ADOPTED; no steering
found under adversarial reading. CONFIRMED.

## 5. PIN-3 ATTACKED — FORCED(MANDATORY) SURVIVES

My pre-committed G2 breakers, each re-worked at bytes:

```text
BRK-4 "§1.5 'An H member carrying …'". The build's stage-pair disposal is
  correct at the bytes: the sentence names BOTH stage-indexed instances
  over an arrow f:N->M — the square's endpoint data — and is exact under
  the mandatory reading; my own pre-fix note independently flagged the
  stage-pair reading before the build was opened. A law-section
  participle amending the definition-site tuple from a distance is the
  venue conflation the D3 precedent's G4 rejects.
BRK-5 "rows 20/25 'H-only conditional'". DISSOLVES at the rows' own
  bytes: row 20's status column reads "DECLARED/REQUIRED", row 25's
  "REQUIRED"; and the table's own usage of "conditional" is formation-
  conditionality (rows 23/24: "conditional coordinate" for Riesz
  reparameterizations) — the composites are conditional on the
  FullKerCert PROOF, not on optional carriage. Coherent under mandatory;
  breaks nothing.
BRK-6 "SPEC-12d 'failure-capable … TYPE-U seam'". Failure-capable = the
  certificate can fail to be DERIVED (TYPE-U grade; a member whose cert
  fails is no member) — coherent under mandatory; SPEC-12d's own next
  sentence supplies this reading.
G3 VERIFIED: the §8.2 anti-tuning row is verbatim at :1676 — "replace
  J7's full Q-408 domain by Ker_N^rep | H-only FullJ7_N/FullKerCert_N
  remains a separate required clause; Ker_N^rep is used only for named
  surface-base tests | clean" — the optionality reading's exact
  operative effect, sealed as a blocked move. G1 contrast VERIFIED: the
  spec marks genuine conditionality explicitly where intended (r_N^D "on
  each admitted sign-odd sector", "required only on each admitted G5
  sign-odd sector" :1323); no marker attaches to FullJ7_N in SPEC-2.
G2 RE-RUN: "FullJ7" -> fdf20bd4, OB-2 + audit, the build, and one
  supervision continuation state file (not a defining carrier; not
  opened as such); no FullJ7-free H membership tuple anywhere.
```

CONFIRMED. NG-2 binds every H member; the D-1 tree's NG-2/NG-3
obligations are unconditional, exactly as the payoff display states.

## 6. CORRECTIONS AND OBSERVATIONS (all verdict-neutral)

```text
O-1 (PIN-3, counter-evidence display gap): FC3-G4 disposes only the §1.5
    participle. fdf20bd4's rows 20/25 ("H-only conditional …") and the
    H0 ledger row ("H-only TYPE-U") are qualifying-flavored sealed text
    left undisplayed. They break nothing (BRK-5/BRK-6 above — their own
    status columns and grade semantics dispose them), but the D3 method
    disposes counter-evidence at its own venue. REPAIR: append the two
    rows + H0 to FC3-G4 with the status-column disposal. Verdict-neutral.
O-2 (PIN-1 §3.4, precision): "the (S2b)/(S4)/(S5) INTRO SENTENCES …
    ABSENT from both prior displays" slightly overstates for (S5): the
    OB-2 audit's LOC row cites "(S5)" (equation-grade). The intro PHRASE
    ("reproduced exactly") is genuinely unwalked there; the display
    should distinguish phrase from equation. Verdict-neutral.
O-3 (PIN-2-HOL, scope): OB-2 §5.2 also names R_N^rec ("record-support
    carrier") and supp_D-as-actual-support among W-HOL's semantic names;
    §4.1's chain forces only the commissioned "encirclement" half, and
    "W-HOL's degenerate completion stands admitted" inherits OB-2's own
    B-2(ii) framing (that the two named word-sites decide W-0/W-HOL).
    Within the commission this is licensed; one displayed sentence
    noting the two residual §5.2 names would close the scope exactly.
    Verdict-neutral (the residual names are gate-conditional and were
    never elevated to B-2).
O-4 (PIN-1 FC1-G1(ii)): "containing the image of (S2a)" admits, in
    isolation, a containment-via-embedding reading. Non-breaking: FC1
    does not rest on (ii) alone, and the build's CL-2 honestly displays
    the fallback (verdict retained on c1-c5 + G3 without the prose-is-
    clause premise). Verdict-neutral.
```

## 7. PAYOFF DISPLAY AND PROCESS CHECKS

```text
PAYOFF FIDELITY = PASS: combination space 2 (three forced + one binary
  free) is exact; every consequence line traces to OB-2 §4/§5/§6 or its
  audit's C-1 (each checked); the "flip" lean mirrors OB-2's own NET
  sentence with the census item licensed by PIN-1 + OB-2 §4.1; both
  directions displayed; OB2_STATUS unchanged under both combinations —
  correct: the deciders (B-1, B-3(i)/(ii)/(iii)) are outside the pins.
PRIOR-VERDICT RELATION = SOUND: both prior ledgers' CL-1 clauses named
  "a sealed fixing of a B-2 pin" via wider sweep as the licensed change
  event (verified at both CL blocks); the build is that exhibit; OB-2's
  contrary CL-2/CL-3 readings were PREMISE-tagged (unforced), and OB-2's
  own Class-1 practice (consuming the (S4) sentence) supports the
  build's CL-2. No sealed verdict contradicted at its walked bytes.
PROCESS = CLEAN: one output + sidecar at commission-distinct paths;
  absence-probe displayed; sweeps with declared cutoffs; choice ledger
  (zero OPEN), toy separation, flag block present; fences held; no
  float; no adoption; supersession clause carried on every FORCED
  finding; the FREE sheet returns to the principal alone.
```

## 8. CHOICE LEDGER (terminal; per the guard)

```text
ACL-1 CORPUS BOUNDARY: both roots swept minus fenced filename classes
      (register|road_|ledger|lens|plan|tracker|THE_HANDOFF — none
      opened); a32_holdout not content-swept (holdout discipline); my
      re-run sweeps at cutoff 2026-08-15T03:32:58Z. A wider sweep could
      change this audit only by exhibiting a sealed byte defeating a
      chain verified here. PREMISE(named).
ACL-2 INDEPENDENCE: verdicts pre-fixed and hashed (dbdc94eb…) before the
      build was opened; flips disclosed with defeating bytes named (§1,
      §3-§5). PREMISE(named).
ACL-3 BREAKING RULE: "one sealed carrier coherent under the other
      reading breaks the force" applied symmetrically; dead-text
      displays counted as G3-grade evidence per the D3 shape.
      PREMISE(named).
ACL-4 d662889b and 9c74ed3d seal-verified but consumed only through
      OB-2/fdf20bd4 displays, as the build did. PREMISE(named).
ACL-5 DISCLOSURE: directory listings and grep output displayed fenced-
      class BASENAMES; no fenced file's content opened, read, or used.
      The supervision continuation state file surfaced in the FullJ7
      sweep was not opened. PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  No toy object constructed or used. The zero functional/zero map appear
  only as quoted from sealed displays inside reading-audits; nothing is
  proposed, preferred, promoted, or bound. All work is byte-verification
  plus exact reading logic; no model, stage, or number enters any
  conclusion.
```

## FLAG BLOCK

```text
AUDIT = CONFIRMED-WITH-CORRECTIONS (O-1..O-4, all verdict-neutral)
PIN1 = CONFIRMED FORCED-READING(ACTUAL) at operative sites, residue
  displayed; all three of my pre-committed breakers dissolve at bytes
PIN2_HOL = CONFIRMED FORCED-READING(NOT-A-MEMBERSHIP-CLAUSE); zero map
  admitted at clause bytes; scope note O-3
PIN2_H = CONFIRMED FREE; sheet honesty PASS; matches my pre-fixed verdict
PIN3 = CONFIRMED FORCED-READING(MANDATORY); §8.2 G3 row verbatim; my
  rows-20/25 and failure-capable breakers dissolve at their own bytes
SHEET = PROPOSED-NOT-ADOPTED, honest two-reading menu, no steering found,
  reversal mechanics both directions, returns to the principal alone
PAYOFF_FIDELITY = PASS ; OB2_STATUS = UNCHANGED (UNDECIDABLE-TODAY, both
  combinations; deciders outside the pins)
INDEPENDENCE = pre-fixed hashed verdicts dbdc94eb… BEFORE opening; flips
  disclosed with defeating bytes named
SEALS = 13/13 MATCH including the build (12 carriers + 09047c2a) ;
  MISMATCHES = 0 ; HELD_CONSTRUCTION untouched
SWEEPS = re-run independently ; CUTOFF 2026-08-15T03:32:58Z ;
  exhaustiveness sweep-displayed only
DETERMINATION_ONLY = held (no adoption, no member, no candidate, no
  selection, no common cell, nothing numeric, no CAS, no float, no value
  frozen) ; MEMBER_BOUND = false ; CHAIN_INVOKED = false
NUMERIC_EVALUATION = false ; MEASURED_CONSTANT_COMPARISON = none
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = the build survives an audit that was committed against it: two
  forced readings and a split stood because their chains rest on sealed
  sentences with no work under the defeated alternative — the same shape
  that froze D3 — and the one genuinely free reading is staged, not
  taken. The FORCED findings return to the registrar for consumption;
  the PIN-2-H sheet awaits the principal; four verdict-neutral repairs
  are listed at §6.
```
