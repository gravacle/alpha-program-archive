# STAGE 8 — AUDIT OF THE DEG3 SYMBOL-AND-MOMENT DISPLAY PAIR (T8SR) — DISPLAY-AUDIT V001

## AUDITOR — CODENAME DISPLAY-AUDIT — COMMISSION T8SR — 2026-08-15

Stance: DEFAULT-REFUTE. Testimony carries zero weight; every displayed
symbol, moment, and evaluation re-derived at the bytes of the sealed
ground, independently (fresh CAS venv `t8srauditvenv`, separate from
the build's `t8srvenv`). The build's CAS battery is NOT re-run and NOT
trusted; my own battery is constructed from the ground up.

THE CLAIMED ARTIFACT:
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_DEG3_DISPLAY_PAIR_T8SR_V001.md`
sha256 30ee481f2af60a08adc2ae544a5f6f502dd2f38d339d10d7668c148ae9bf52b5
(seal sidecar verified `shasum -a 256 -c` => OK at 05:08 window, STEP 0,
before anything else was done; my output path probed ABSENT first).

Its headline: COMPLETE-LOG-GENUINE — (i) the o = 3 (c = 0)
segment-corrected angular symbols are khat-FREE with exact degree -2
coefficient +V_2(x)^2/4, so mu_AU = mu_AF = 0; (ii) the beta_s
first-angular-moment certificate: odd slot = 4 pi x_hat_x M_1(|x|),
M_1 < 0 strict on 0 < |x| < 1; even slot > 0 strict on the open shell;
(iii) L_deg3(x) = 16 pi x_hat_x M_1(|x|) + mu_B^alpha(x), NOT
identically zero, and the armed branch fires L != 0 => log GENUINE,
rate REFUTED.

Fences held by me: DETERMINATION ONLY; `alpha_computed = false`;
`proof_authorized = false`; `kappa_record_computed = false`. Exact
symbolic only (CAS in a fresh venv; script + output sourced into this
artifact by file-append, verbatim); no floats; no numeric evaluation;
no measured-constant comparison; no git; scoped reads of the named
artifacts only; no register/tracker/plan/continuation/THE_HANDOFF file
opened; "Q-..." tokens EXPECTED-UNLOCATABLE, never chased. ONE output
(this artifact + seal sidecar), path probed ABSENT at session start.

SWEEP CUTOFF DECLARED: **2026-08-15 05:11:25 -0500**. Corpus roots
swept by filename only: `/Users/bgm/MB Work/alpha-program-archive/workspace`
(primary) and `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003`
(cleanroom). Artifacts sealed after the cutoff are unswept and
unclassed here.

---
## 1. STEP 0 AND INPUT SEALS — ALL VERIFIED AT PATH BY ME

STEP 0: the claimed artifact + sidecar PRESENT; `shasum -a 256 -c`
=> OK. My output path ABSENT at probe. Proceed.

Input ground, every seal verified by me THIS session via
`shasum -a 256 -c` against its sidecar BEFORE consumption — 12/12 OK:

```text
STAGE8_DEG3_ANGULAR_CENSUS_T6SR_V001.md          OK  [CENSUS]
STAGE8_DEG3_ANGULAR_CENSUS_T6SR_AUDIT_V001.md    OK  [CENSUS-AUDIT]
STAGE8_WALL_LOGFREE_RATE_T4SR_V001.md            OK  [T4SR]
STAGE8_WALL_LOGFREE_RATE_T4SR_AUDIT_V001.md      OK  [T4SR-AUDIT]
STAGE8_WALL_SUMMED_REQUANT_S9AD_V001.md          OK  [WSR]
STAGE8_WALL_SUMMED_REQUANT_S9AD_AUDIT_V001.md    OK  [WSR-AUDIT]
STAGE8_MO4_CANCELLATION_S9AD_V001.md             OK  [MO4]
STAGE8_MO4_CANCELLATION_S9AD_AUDIT_V001.md       OK  [MO4-AUDIT]
STAGE8_REMAINDER_UNIFORMITY_V001.md              OK  [REM]
STAGE8_BL2STAR_ATTEMPT_V001.md                   OK  [BL2STAR]
STAGE8_R2_RESUMMATION_V001.md                    OK  [R2]
STAGE8_MO3_P_EXPONENT_S9AD_V001.md               OK  [MO3]
```

Full digests recomputed by me; the four stock digests MATCH the
build's quoted prefixes exactly:
2e4e5163... (REM), 6e81ae92... (BL2STAR), a7969f0c... (R2),
9fdc3d1c... (MO3 — also the jet-home pin claimed from MO4 §1 G-5 and
WSR §1, both to be verified at bytes below).

Findings ledger opens empty; every check below is DEFAULT-REFUTE.

---
## 2. THE GROUND RE-READ AT BYTES — EVERY LOAD-BEARING QUOTE RE-LOCATED

Every span the build cites was re-read by me at path. Verbatim matches
(build's citation -> my byte-location):

```text
(v-1)  CENSUS §5 completing-object spec: names EXACTLY the two
       deliverables the build supplies — (i) the (o = 3, c = 0)
       segment-corrected subsidiaries' angular symbols from the
       REM/BL2STAR/R2 stock; (ii) the beta_s first-angular-moment
       certificate (the two integrals, per cell point x) from the MO4
       stock. RE-LOCATED (census :289-:311, flag :661-:669). The
       build's deliverables are the commissioned objects, not
       substitutes. Census audited CONFIRMED-WITH-CORRECTIONS with
       the completing object named "No objection" (census-audit c-15).
(v-2)  CENSUS §3.2 slot table: A-U, A-F functional shapes (mu_AU =
       int s_U^(2) T_F^{V2} dOmega; mu_AF = int a_F^(2) T_F^(2)
       dOmega); B-* with mu_B^I = 4 int khat_x [beta_+ - beta_-]
       dOmega and mu_B^alpha = 4 int khat_x^2 [beta_+ + beta_-]
       dOmega, entering combination marked undisplayed. RE-LOCATED
       (census :159-:223). The build fills EXACTLY these functionals.
(v-3)  CENSUS s-1/s-3 + census-audit c-10/c-11/CA6/CA7: L_deg3 =
       mu_AU + mu_AF + SUM_{c-routes} mu_B, one term per class; the
       armed branch table: L = 0 => count <= -4, 1 - 1/Lambda bounded,
       RATE-CERTIFIED; L != 0 => log GENUINE, rate refuted. COR-A
       re-read at T4SR-AUDIT §0/(2): "the correct branch condition is
       a nonzero TOTAL first moment" — the SUM, not per-stratum.
       RE-LOCATED (census :230-:254; census-audit :220-:231).
(v-4)  T4SR a-6 names the REM/BL2STAR/R2 stock outside its five
       (:178-:202); WSR §1 pins REM 2e4e5163 / BL2STAR 6e81ae92 /
       R2 a7969f0c / MO3 9fdc3d1c at :72/:81-:83; MO4 §1 G-5 pins MO3
       at FULL digest 9fdc3d1c4d3e450d... (:148-:150). My recomputed
       full digests MATCH all four. The build's citation-chain stock
       location is faithful.
(v-5)  REM §2.1 :124-:127 (the two oscillation kinds + the coincidence
       alternative); §2.2 :137-:145 (J vertex flip/stay; "-k_hat_x
       C(k) — the smooth vertex has NO coincidence zero"); §2.3
       :165-:190 (the weights (1/2, -1/4, -1/4) at (0, +sqrt2,
       -sqrt2) displayed at :169-:170; the floors verbatim: sea-sea
       "flips >= 2, c = 0 ... U-side: -i V_2(x)/(2|k|) ... F-side:
       +i V_2-form/(2|k|) via m_2 = -1, §6 R1d, with V_2^{[0,t']} +
       V_2^{[t',1]} = V_2 ... §6 R6b"; interband "flips odd (>= 1)
       AND stays = n - flips odd (>= 1): o >= 1 AND c >= 1"; "The
       bare-J stratum (n = 0) is m_0-killed identically, so F has NO
       lambda-independent content at ANY degree"); §3.2 :239-:260
       (double kill; ASSEMBLED REMAINDER STARTS AT DEGREE -2); §3.3
       :266-:299 (x is a PARAMETER; the (x, omega) family displays
       |A_lambda(x, omega) - 1| <= C_rate lambda^2/omega;
       |sigma_U^rem(x, omega)| <= 2 C_rate/omega; |sigma_F^rem(x,
       omega)| <= C_F(x)/omega). ALL VERBATIM AS QUOTED by the build.
(v-6)  R2 §4 :373-:386 (the stencil identity sum_l w_l e^{i lambda^2
       y} = -i e^{iy} sin(y), y = V_2(x)/(2 omega), §7 V1e; V_2(x) =
       (8 pi^2/7)(1 - 128|x|^7), §7 V7a; "U(0)'s intraband symbol
       falls off as -i V_2(x)/(2|k|) at leading order"; ":369 the
       full resummed intraband symbol is this omega-interpolating
       family"); §5(c) :397 ("THE SURVIVOR -i a_Delta k_hat_x
       b~(x,k_hat) C(k): CANCELS at degree 0 in the summed
       assembly"). VERBATIM AS QUOTED.
(v-7)  BL2STAR :337-:371 (X display + F definition; the I13 stencil
       collapse; sigma_intra(X) = -i a_Delta k_hat_x b~_P C(k)).
       VERBATIM AS QUOTED.
(v-8)  MO3 :122-:126 (b_D bytes: exp(16 - 1/s), s = s_- s_+, s_- =
       t^2 - |x|^2, s_+ = (1-t)^2 - |x|^2, support the open diamond);
       :204 (beta_s := int_0^1 b_D(t, x + s t khat) dt); :210-:211
       (a_0 both forms); :218-:221 (parity identity; Sigma even,
       difference odd); §3.5 :261-:285 (ray witness with the
       EXACT-RATIONAL instance x = (1/4)e_x, khat = -e_x, t = 3/10:
       s_- = 7/80, s_+ = 39/80, s = 273/6400; support exactly
       0 < |x| < 1; "CONTACT TERM ... the alpha_x-component of c(x)
       is (1/8 pi) int dOmega k_hat_x^2 Sigma(x, k_hat) > 0 — a
       nonnegative integrand, strictly positive on an open set");
       §4.1 :319-:326 (P_-(khat) a_0 P_-(khat) = -i a_Delta khat_x
       beta_- P_-(khat), VERBATIM the sealed survivor). ALL AS
       QUOTED.
(v-9)  WSR-AUDIT :148-:153 (A3 tr[alpha_x C(k)] = -2 khat_x; :152
       "degree -3 strata exist (o = 3, c = 0; c-bearing routes)");
       :558-:561 (AC-4 verbatim: "no stronger claim (e.g.,
       stratum-by-stratum parity at degree -3) is made by me or by
       the build"). AS QUOTED.
(v-10) WSR t-1/t-2/t-3 :308-:343 (the ONLY-degree--2-stratum text;
       the U-side/F-side V_2-form contents; the landing). :393
       ("segment-corrected"). AS QUOTED.
```

NO misquote found. The khat-free typing's three legs (floors; (x,
omega) family; m_0 kill of the bare-J stratum) are all at bytes, and
the derivation "c = 0 excludes interband blocks (interband requires
c >= 1), hence c = 0 strata are pure sea-sea two-level content with
the J-stay longitudinal vertex" is a two-step consequence of the
displayed floors — DERIVED, correctly graded. The khat-dependent
ray-average dressing (beta_s) is the bare-J free-transport object
(MO3 T_1^{(0)} at :133-:135 IS the n = 0 stratum), m_0-killed in the
weight-summed F at bytes (REM :189-:190) — so its khat-dependence
enters the degree-3 class ONLY through the c-bearing routes, exactly
where the census (audited) put it. The build's s-1..s-4 ground survey
is faithful.

---
## 3. THE INDEPENDENT CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `t8srauditvenv` under the session scratchpad
(distinct from the build's venv); nothing written to the workspace but
this artifact and its seal. Exact symbolic / exact rational/surd only;
nothing numeric evaluated. ONE battery, 8 checks, 8/8 PASS on the
recorded run. Disclosure (fence honesty, same convention as the build's
§8): two auditor-side pre-run corrections were made before the recorded
run and are disclosed — (a) my initial expected value for the fourth
stencil moment was wrong arithmetic on MY side (I asserted m_4 = -1;
the true exact value is m_4 = -2; the build never claimed m_4); (b) my
initial trig-parametrized generic axis defeated sympy's simplifier at
the m = 8 reduction (a FALSE NEGATIVE of the toolchain, not a
mathematical failure), replaced by the exact unit-sphere IDEAL
REDUCTION shown below. No check was weakened or removed; the widened
windows (degree-7 odd family; m = 0..8 reductions; degree-6 H-schema)
were kept. SEPARATELY: the build's own §8 battery was extracted
verbatim from the sealed artifact and re-run in MY fresh venv — its
output is BYTE-IDENTICAL to the §8 recorded block (6/6 PASS), diff
empty.

```python
# DISPLAY-AUDIT independent CAS battery — commission T8SR audit.
# EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv t8srauditvenv). DEFAULT-REFUTE:
# every displayed object re-derived on MY OWN code paths from the sealed bytes;
# the build's battery is neither imported nor re-run. Nothing numeric evaluated;
# every constant symbolic or an exact rational/surd. Groups:
#   AU1 the stencil identity at the REM :169-170 weights, BOTH phase
#       conventions (U-side e^{+i l^2 y}; F-side conjugate), full series,
#       moments, R1d; the degree -2 coefficient arithmetic V_2^2/4;
#   AU2 V_2 re-derived INDEPENDENTLY from the R2 :81 profile bytes
#       (v = tau_R 32 min(t,1-t)^3, tau_R = pi/sqrt2): (8 pi^2/7)(1-128 rho^7);
#   AU3 class-A kills at an INDEPENDENT Dirac construction (alphas from
#       gamma^0 gamma^j): tr[alpha_x C(k)] = -2 khat_x; ALL odd-degree
#       monomial sphere moments (deg 1,3,5,7 — 64 monomials) vanish; a
#       khat-free amplitude against the COMPLETE generic odd polynomial
#       through degree 7 has first moment 0;
#   AU4 the radial reductions on u^m, m = 0..8 (wider than the build's 6) at a
#       generic symbolic axis; certificate reductions on a generic DEGREE-6
#       H-schema (wider than the build's cubic): mu_B^I = 16 pi b_x M_1;
#       the mu_B^alpha bracket identity; the survivor moment -(1/8) mu_B^I;
#       joint-swap evenness of both traced combinations at the schema;
#   AU5 the monotonicity CHAIN made exact: ds/d(r^2) = -(s_- + s_+);
#       d(r^2)/du = 2 t rho > 0; d b_D/ds = b_D/s^2 > 0; composed
#       d b_D/du < 0 on the diamond; the witness rationals; H(rho,+1) = 0
#       arithmetic; the monotone sign exhibit;
#   AU6 the sandwich P_- a_0 P_- = -i a_Delta khat_x beta_- P_- on |khat| = 1;
#       both jet traces; the placement phase (-i)(+i) = 1;
#   AU7 the COR-A completion and branch: L on the plane locus; the (L-3)
#       robustness family displayed at BOTH degenerate weightings; I3/I4;
#       the census constant-coefficient exhibits reproduced from MY formulas;
#   AU8 THE CONSTANT AUDIT (the kill hunt's one catch): census mu_B^alpha =
#       4 S vs MO3 c_alpha = S/(8 pi) forces mu_B^alpha = 32 pi c_alpha —
#       the build's displayed "8 pi c_alpha" is FALSE by a factor 4
#       (positivity, sign, support unaffected).
import sympy as sp
from sympy import Rational as R
from sympy import I as Im

results = []
def ok(name, cond):
    results.append(bool(cond))
    print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

# ===== independent Dirac ground: alphas from gamma^0 gamma^j (Dirac rep) =====
s0 = sp.eye(2)
sx = sp.Matrix([[0, 1], [1, 0]]); sy = sp.Matrix([[0, -Im], [Im, 0]])
sz = sp.Matrix([[1, 0], [0, -1]]); Z2 = sp.zeros(2, 2)
def blk(a, b, c, d): return sp.Matrix(sp.BlockMatrix([[a, b], [c, d]]))
g0 = blk(s0, Z2, Z2, -s0)
gj = [blk(Z2, s, -s, Z2) for s in (sx, sy, sz)]
al = [sp.expand(g0*g) for g in gj]                      # alpha_j = gamma^0 gamma^j
anti_ok = all(sp.expand(al[i]*al[j] + al[j]*al[i] - 2*(1 if i == j else 0)*sp.eye(4))
              == sp.zeros(4, 4) for i in range(3) for j in range(3))
kx, ky, kz = sp.symbols('kh_x kh_y kh_z', real=True)
th, ph = sp.symbols('theta phi', real=True)
sub = {kx: sp.sin(th)*sp.cos(ph), ky: sp.sin(th)*sp.sin(ph), kz: sp.cos(th)}
def ang(e):
    return sp.integrate(sp.integrate(sp.expand_trig(sp.sympify(e)).subs(sub)*sp.sin(th),
                                     (ph, 0, 2*sp.pi)), (th, 0, sp.pi))
A1 = kx*al[0] + ky*al[1] + kz*al[2]
Ck = sp.expand((sp.eye(4) - A1)/2)                      # P_-(khat) = C(k)
Pp = sp.expand((sp.eye(4) + A1)/2)

# ===== AU1 — stencil identity, both conventions, full tower, R1d =====
y = sp.Symbol('y', real=True)
lams = [0, sp.sqrt(2), -sp.sqrt(2)]                     # REM :169-170 weights
ws = [R(1, 2), -R(1, 4), -R(1, 4)]
stU = sum(w*sp.exp(Im*l**2*y) for w, l in zip(ws, lams))       # U-side phase
stF = sum(w*sp.exp(-Im*l**2*y) for w, l in zip(ws, lams))      # conjugate side
idU = sp.simplify(sp.expand(sp.expand_trig((stU - (-Im*sp.exp(Im*y)*sp.sin(y))).rewrite(sp.exp)))) == 0
idU2 = sp.simplify(sp.expand(stU - R(1, 2)*(1 - sp.exp(2*Im*y)))) == 0   # my own closed form
idF = sp.simplify(sp.expand(sp.expand_trig((stF - (Im*sp.exp(-Im*y)*sp.sin(y))).rewrite(sp.exp)))) == 0
serU = sp.series(stU, y, 0, 5).removeO().expand()
serF = sp.series(stF, y, 0, 5).removeO().expand()
cU = [serU.coeff(y, j) for j in range(5)]
cF = [serF.coeff(y, j) for j in range(5)]
m = [sp.simplify(sum(w*l**j for w, l in zip(ws, lams))) for j in range(5)]
r1d = sp.expand(sum(w*(1 - Im*l**2*y) for w, l in zip(ws, lams)))
V2s, w_ = sp.symbols('V_2 w_mod', positive=True)
deg2coeff = sp.simplify(sp.expand((V2s/(2*w_))**2).coeff(w_**-2)*4 - V2s**2) == 0
ok("AU1 STENCIL BOTH CONVENTIONS + TOWER + R1d: sum w_l e^{+i l^2 y} ="
   " -i e^{iy} sin y = (1/2)(1 - e^{2iy}) EXACT at the REM :169-170 weights;"
   " conjugate side sum w_l e^{-i l^2 y} = +i e^{-iy} sin y (the F convention"
   " R1d rides); series U: 0, -i, +1, +2i/3, -1/3 and F: 0, +i, +1, -2i/3,"
   " -1/3 — the y^2 (degree -2 segment-corrected) coefficient is +1 REAL in"
   " BOTH conventions, khat-free; moments m_0 = m_1 = m_3 = 0, m_2 = -1,"
   " m_4 = -2 (m_4 unclaimed by the build; displayed here for the tower's"
   " next order); R1d sum w_l (1 - i l^2 y) = +i y; and y^2 at y ="
   " V_2/(2|k|) carries EXACTLY V_2^2/4 on 1/|k|^2",
   anti_ok and idU and idU2 and idF
   and cU[0] == 0 and cU[1] == -Im and cU[2] == 1
   and sp.simplify(cU[3] - R(2, 3)*Im) == 0 and sp.simplify(cU[4] + R(1, 3)) == 0
   and cF[0] == 0 and cF[1] == Im and cF[2] == 1
   and sp.simplify(cF[3] + R(2, 3)*Im) == 0 and sp.simplify(cF[4] + R(1, 3)) == 0
   and m[0] == 0 and m[1] == 0 and m[2] == -1 and m[3] == 0 and m[4] == -2
   and sp.simplify(r1d - Im*y) == 0 and deg2coeff)

# ===== AU2 — V_2 re-derived from the R2 :81 profile bytes =====
tt = sp.Symbol('t', positive=True); rho = sp.Symbol('rho', positive=True)
tauR = sp.pi/sp.sqrt(2)                                  # R2 :81 frozen
v_lo = tauR*32*tt**3                                     # v on [rho, 1/2]
v_hi = tauR*32*(1 - tt)**3                               # v on [1/2, 1-rho]
V2rho = sp.integrate(v_lo**2, (tt, rho, R(1, 2))) + sp.integrate(v_hi**2, (tt, R(1, 2), 1 - rho))
intv = sp.integrate(32*tt**3, (tt, 0, R(1, 2))) + sp.integrate(32*(1 - tt)**3, (tt, R(1, 2), 1))
ok("AU2 V_2 INDEPENDENT: with the R2 :81 bytes v = tau_R 32 min(t,1-t)^3,"
   " tau_R = pi/sqrt2 (normalization int_0^1 32 min(t,1-t)^3 dt = 1"
   " re-derived), V_2(rho) = int_{min(t,1-t) > rho} v^2 dt ="
   " (8 pi^2/7)(1 - 128 rho^7) EXACT — the R2 §7 V7a closed form of record,"
   " re-derived not copied",
   sp.simplify(intv - 1) == 0
   and sp.simplify(V2rho - R(8, 7)*sp.pi**2*(1 - 128*rho**7)) == 0)

# ===== AU3 — class-A kills, complete odd family through degree 7 =====
trax = sp.expand((al[0]*Ck).trace())
mono_odd = []
for d in (1, 3, 5, 7):
    for a in range(d + 1):
        for b in range(d + 1 - a):
            c = d - a - b
            mono_odd.append(kx**a * ky**b * kz**c)
odd_moments_zero = all(sp.simplify(ang(mn)) == 0 for mn in mono_odd)
coeffs = sp.symbols(f'q0:{len(mono_odd)}', real=True)
T_odd_full = sum(q*mn for q, mn in zip(coeffs, mono_odd))
sA = sp.Symbol('s_A', real=True)                        # khat-FREE class-A amplitude
muA = sp.simplify(ang(sA*T_odd_full))
ok("AU3 CLASS-A KILLS AT AN INDEPENDENT DIRAC GROUND: alpha_j = gamma^0"
   " gamma^j (assembled, anticommutation re-proved); tr[alpha_x C(k)] ="
   " -2 khat_x EXACT (A3 re-derived); EVERY odd-degree monomial sphere moment"
   f" vanishes (degrees 1,3,5,7 — all {len(mono_odd)} monomials, wider than"
   " the build's 13); a khat-FREE amplitude against the COMPLETE generic"
   " joint-odd polynomial through degree 7 has first moment 0 EXACT:"
   " mu_AU = 0 and mu_AF = 0 ride khat-freeness alone, no sign convention"
   " enters",
   sp.expand(trax + 2*kx) == 0 and sp.simplify(ang(trax)) == 0
   and odd_moments_zero and muA == 0)

# ===== AU4 — radial reductions m<=8; degree-6 H-schema certificates =====
# Axis handled EXACTLY: symbolic b = (bx_, by_, bz_) with the unit-sphere
# constraint imposed by IDEAL REDUCTION (sympy reduced() modulo
# bx^2 + by^2 + bz^2 - 1) — no trig parametrization, no simplify()
# shortfall; every equality is a polynomial identity on |b| = 1.
bx_, by_, bz_ = sp.symbols('b_x b_y b_z', real=True)
G = [bx_**2 + by_**2 + bz_**2 - 1]
def on_sphere_zero(expr):
    p = sp.Poly(sp.expand(expr), bx_, by_, bz_)
    _, r = sp.reduced(p.as_expr(), G, bx_, by_, bz_)
    return sp.expand(r) == 0
u = sp.Symbol('u', real=True)
red1_ok = True; red2_ok = True
for mdeg in range(9):
    f = (kx*bx_ + ky*by_ + kz*bz_)**mdeg
    lhs1 = ang(kx*f)
    rhs1 = 2*sp.pi*bx_*sp.integrate(u*u**mdeg, (u, -1, 1))
    red1_ok = red1_ok and on_sphere_zero(lhs1 - rhs1)
    lhs2 = ang(kx*kx*f)
    rhs2 = 2*sp.pi*sp.integrate(((1 - u**2)/2 +
             (3*u**2 - 1)/2*bx_**2)*u**mdeg, (u, -1, 1))
    red2_ok = red2_ok and on_sphere_zero(lhs2 - rhs2)
h = sp.symbols('h0:7', real=True)
H = sum(h[j]*u**j for j in range(7))                    # generic degree-6 schema
ub = kx*bx_ + ky*by_ + kz*bz_
Hk = lambda e: H.subs(u, e)
M1 = sp.integrate(u*H, (u, -1, 1))
SH = H + H.subs(u, -u)
muBI_lhs = 4*ang(kx*(Hk(ub) - Hk(-ub)))
muBI_rhs = 16*sp.pi*bx_*M1
muBa_lhs = 4*ang(kx*kx*(Hk(ub) + Hk(-ub)))
muBa_rhs = 4*2*sp.pi*sp.integrate((bx_**2*u**2 +
             (1 - u**2)/2*(1 - bx_**2))*SH, (u, -1, 1))
surv = ang(kx*Hk(-ub))                                  # int khat_x beta_-(schema)
swap_D = sp.expand(kx*(Hk(ub) - Hk(-ub)))               # khat_x (b+ - b-) schema
swap_S = sp.expand(kx*kx*(Hk(ub) + Hk(-ub)))            # khat_x^2 (b+ + b-) schema
inv = {kx: -kx, ky: -ky, kz: -kz}
# joint swap = khat -> -khat AND b+ <-> b- (schema: Hk(ub) <-> Hk(-ub) automatic)
swapD_ok = sp.expand(swap_D.subs(inv, simultaneous=True) - swap_D) == 0
swapS_ok = sp.expand(swap_S.subs(inv, simultaneous=True) - swap_S) == 0
ok("AU4 RADIAL REDUCTIONS m = 0..8 (wider than the build's 6) at an EXACT"
   " symbolic axis (unit-sphere ideal reduction — no trig simplify), then"
   " the certificates on a generic DEGREE-6 H-schema (wider than the"
   " build's cubic): mu_B^I = 4 int khat_x [H(u) - H(-u)] dOmega ="
   " 16 pi b_x M_1, M_1 = int_{-1}^1 u H du EXACT; mu_B^alpha ="
   " 4 int khat_x^2 [H(u) + H(-u)] dOmega = 8 pi int [b_x^2 u^2 +"
   " (1-u^2)/2 (1-b_x^2)] SigmaH du EXACT; the survivor moment int khat_x"
   " beta_- dOmega = -2 pi b_x M_1 = -(1/8) mu_B^I EXACT; BOTH traced"
   " combinations joint-swap EVEN at the schema (parity refuses the kill —"
   " census C4 consistency)",
   red1_ok and red2_ok
   and on_sphere_zero(muBI_lhs - muBI_rhs)
   and on_sphere_zero(muBa_lhs - muBa_rhs)
   and on_sphere_zero(surv + 2*sp.pi*bx_*M1)
   and on_sphere_zero(surv + R(1, 8)*muBI_lhs)
   and swapD_ok and swapS_ok)

# ===== AU5 — the monotonicity chain, exact =====
t_, w2 = sp.symbols('t_v w_r2', positive=True)          # w2 = r^2
smv = t_**2 - w2; spv = (1 - t_)**2 - w2
s_pr = smv*spv
chain1 = sp.expand(sp.diff(s_pr, w2) + (smv + spv)) == 0
sV = sp.Symbol('s_v', positive=True)
bD = sp.exp(16 - 1/sV)
chain2 = sp.simplify(sp.diff(bD, sV) - bD/sV**2) == 0   # d b_D/ds = b_D/s^2 > 0
rr2 = rho**2 + 2*t_*rho*u + t_**2
chain3 = sp.simplify(sp.diff(rr2, u) - 2*t_*rho) == 0   # d(r^2)/du = 2 t rho > 0
# composed: d b_D/du = (b_D/s^2) * ds/d(r^2) * d(r^2)/du = -(b_D/s^2)(s_-+s_+)(2 t rho) < 0
comp = sp.simplify(sp.diff(bD.subs(sV, s_pr), w2).subs(w2, rr2)*sp.diff(rr2, u)
                   + (bD.subs(sV, s_pr)/s_pr**2*(smv + spv)*2*t_*rho).subs(w2, rr2)) == 0
twit, rwit = R(3, 10), R(1, 20)
smw = twit**2 - rwit**2; spw = (1 - twit)**2 - rwit**2; sw = smw*spw
tpos = sp.Symbol('t_pos', positive=True)
outward = sp.Min(0, 1 - 2*tpos)                          # min(t,1-t) - t <= 0
hpos = sp.Symbol('h_pos', positive=True)
mono_ex = sp.integrate(u*hpos*(1 - u), (u, -1, 1))
ok("AU5 THE MONOTONICITY CHAIN EXACT: ds/d(r^2) = -(s_- + s_+) (polynomial"
   " identity); d b_D/ds = b_D/s^2 > 0; d(r^2)/du = 2 t rho > 0; COMPOSED:"
   " d b_D/du = -(b_D/s^2)(s_- + s_+)(2 t rho) < 0 STRICT on the open"
   " diamond — H(rho, u) nonincreasing in u with strict decrease wherever"
   " the ray meets the diamond; the exact-rational witness (t, r) ="
   " (3/10, 1/20): s_- = 7/80, s_+ = 39/80, s = 273/6400 (MO3 §3.5"
   " re-pinned); the outward ray min(t,1-t) - t <= 0 so rho + t >"
   " min(t,1-t) at rho > 0: H(rho, +1) = 0 EXACT; sign exhibit"
   " int u h(1-u) du = -2h/3 < 0",
   chain1 and chain2 and chain3 and comp
   and smw == R(7, 80) and spw == R(39, 80) and sw == R(273, 6400)
   and outward.is_nonpositive and sp.simplify(sp.Max(outward, 0)) == 0
   and sp.simplify(mono_ex + R(2, 3)*hpos) == 0)

# ===== AU6 — sandwich, jet traces, placement phase =====
bp, bm, aD = sp.symbols('beta_p beta_m a_Delta', real=True)
a0 = sp.expand(Im*aD*kx*(bp*Pp - bm*Ck))                # MO3 :210 first form
a0_2nd = sp.expand(Im*aD/2*kx*((bp - bm)*sp.eye(4) + (bp + bm)*A1))
forms_eq = all(sp.simplify(sp.expand_trig(e.subs(sub))) == 0 for e in (a0 - a0_2nd))
jet = sp.expand(kx*((bp - bm)*sp.eye(4) + (bp + bm)*A1))
sand = sp.expand(Ck*a0*Ck - (-Im*aD*kx*bm*Ck)).subs(sub)
sand_ok = all(sp.simplify(sp.expand_trig(e)) == 0 for e in sand)
ok("AU6 SANDWICH + JET TRACES + PHASE: MO3 :210-:211 two forms of a_0 EQUAL"
   " on |khat| = 1 (re-derived); P_- a_0 P_- = -i a_Delta khat_x beta_- P_-"
   " EXACT 4x4 identity on |khat| = 1 (MO3 §4.1 re-derived on my independent"
   " Dirac ground); tr[jet] = 4 khat_x (b+ - b-) and tr[alpha_x jet] ="
   " 4 khat_x^2 (b+ + b-) EXACT; the placement phase (-i)(+i) = +1 — the"
   " adjoint pair composes real",
   forms_eq and sand_ok
   and sp.expand(jet.trace() - 4*kx*(bp - bm)) == 0
   and sp.expand((al[0]*jet).trace() - 4*kx**2*(bp + bm)) == 0
   and sp.simplify((-Im)*(Im) - 1) == 0)

# ===== AU7 — COR-A completion, branch, robustness, census exhibits =====
kk = sp.Symbol('k_r', positive=True); Lam = sp.Symbol('Lambda_c', positive=True)
I3 = sp.integrate(kk**(2 - 3), (kk, 1, Lam))
I4 = sp.integrate(kk**(2 - 4), (kk, 1, Lam))
M1s = sp.Symbol('M_1', negative=True)
muBa_sym = sp.Symbol('mu_Balpha', positive=True)        # the certified even slot
bx = sp.Symbol('b_x', real=True)
L = 0 + 0 + 16*sp.pi*bx*M1s + muBa_sym
L_plane = L.subs(bx, 0)
wI, wA = sp.symbols('w_I w_alpha', real=True)
L_w = 16*sp.pi*bx*M1s*wI + muBa_sym*wA
case_alpha = sp.ask(sp.Q.positive(L_w.subs({bx: 0, wA: 1})))      # w_alpha != 0 rep
case_I = sp.ask(sp.Q.negative(L_w.subs({wA: 0, wI: 1, bx: 1})))   # w_alpha = 0, w_I != 0 rep
# census constant-coefficient exhibits from MY schema formulas (H = h0 const):
h0c = sp.Symbol('h0c', positive=True)
M1_const = sp.integrate(u*h0c, (u, -1, 1))
muBI_const = 16*sp.pi*bx_*M1_const
muBa_const = sp.simplify(4*ang(kx*kx*(2*h0c)))
defeat = sp.simplify(4*ang(kx*(sp.Symbol('c_1', real=True)*kx)))
ok("AU7 COR-A COMPLETION + BRANCH + ROBUSTNESS: L_deg3 = 0 + 0 + 16 pi b_x"
   " M_1 + mu_B^alpha; plane locus b_x = 0: L = mu_B^alpha > 0 STRICT —"
   " the branch condition L = 0 fails on a displayed open locus; robustness"
   " at both degenerate weightings: (w_I, w_alpha) = (0-free, 1) nonzero on"
   " the plane, (1, 0) nonzero off the plane (sign of M_1 strict); branch"
   " integrals I3 = log Lambda divergent formal, I4 = 1 - 1/Lambda -> 1"
   " bounded; census exhibits reproduced from MY formulas: constant"
   " coefficients kill mu_B^I (M_1[const] = 0) and leave mu_B^alpha ="
   " (16 pi/3)(b+ + b-)|_const = 32 pi h0/3; the odd-defeat moment"
   " 4 int khat_x (c1 khat_x) dOmega = 16 pi c1/3 (census's 8 pi/3 is the"
   " UNSCALED trace-slot instance int khat_x (f - f(-)) dOmega at f ="
   " khat_x = 2 x (4pi/3) x 1 — consistent, factor bookkeeping displayed)",
   sp.simplify(L_plane - muBa_sym) == 0 and sp.ask(sp.Q.positive(L_plane))
   and case_alpha and (case_I is True)
   and sp.simplify(I3 - sp.log(Lam)) == 0 and sp.limit(I3, Lam, sp.oo) == sp.oo
   and sp.simplify(I4 - (1 - 1/Lam)) == 0 and sp.limit(I4, Lam, sp.oo) == 1
   and M1_const == 0 and sp.simplify(muBI_const) == 0
   and sp.simplify(muBa_const - R(32, 3)*sp.pi*h0c) == 0
   and sp.simplify(defeat - R(16, 3)*sp.pi*sp.Symbol('c_1', real=True)) == 0)

# ===== AU8 — THE CONSTANT AUDIT: mu_B^alpha vs c_alpha =====
Ssym = sp.Symbol('S_int', positive=True)                # S := int khat_x^2 Sigma dOmega
muBa_census = 4*Ssym                                    # census :215
c_alpha_MO3 = Ssym/(8*sp.pi)                            # MO3 §3.5 bytes
ratio = sp.simplify(muBa_census/c_alpha_MO3)
# concrete schema instance:
muBa_schema = sp.simplify(4*ang(kx*kx*SH.subs(u, ub)))
c_alpha_schema = sp.simplify(ang(kx*kx*SH.subs(u, ub))/(8*sp.pi))
ratio_schema = sp.simplify(muBa_schema/c_alpha_schema)
ok("AU8 THE CONSTANT AUDIT (the one catch): census mu_B^alpha = 4 S and MO3"
   " c_alpha = S/(8 pi) with S = int khat_x^2 Sigma dOmega force mu_B^alpha"
   " = 32 pi c_alpha EXACTLY (symbolically AND on the generic degree-6"
   " schema) — the build's displayed 'mu_B^alpha = 8 pi c_alpha' (§4.3"
   " M-II, §5.1, D5) is WRONG BY THE FACTOR 4; sign, support, strict"
   " positivity, and the fired branch are UNAFFECTED (32 pi c_alpha > 0"
   " iff 8 pi c_alpha > 0)",
   sp.simplify(ratio - 32*sp.pi) == 0
   and sp.simplify(ratio_schema - 32*sp.pi) == 0
   and sp.ask(sp.Q.positive(32*sp.pi*sp.Symbol('c_a', positive=True))))

print(f"AUDIT-BATTERY {sum(results)}/{len(results)} PASS")
print("T8SR-AUDIT-BATTERY-DONE")
```

Output, verbatim (8/8 PASS, recorded run):

```text
AU1 STENCIL BOTH CONVENTIONS + TOWER + R1d: sum w_l e^{+i l^2 y} = -i e^{iy} sin y = (1/2)(1 - e^{2iy}) EXACT at the REM :169-170 weights; conjugate side sum w_l e^{-i l^2 y} = +i e^{-iy} sin y (the F convention R1d rides); series U: 0, -i, +1, +2i/3, -1/3 and F: 0, +i, +1, -2i/3, -1/3 — the y^2 (degree -2 segment-corrected) coefficient is +1 REAL in BOTH conventions, khat-free; moments m_0 = m_1 = m_3 = 0, m_2 = -1, m_4 = -2 (m_4 unclaimed by the build; displayed here for the tower's next order); R1d sum w_l (1 - i l^2 y) = +i y; and y^2 at y = V_2/(2|k|) carries EXACTLY V_2^2/4 on 1/|k|^2: PASS
AU2 V_2 INDEPENDENT: with the R2 :81 bytes v = tau_R 32 min(t,1-t)^3, tau_R = pi/sqrt2 (normalization int_0^1 32 min(t,1-t)^3 dt = 1 re-derived), V_2(rho) = int_{min(t,1-t) > rho} v^2 dt = (8 pi^2/7)(1 - 128 rho^7) EXACT — the R2 §7 V7a closed form of record, re-derived not copied: PASS
AU3 CLASS-A KILLS AT AN INDEPENDENT DIRAC GROUND: alpha_j = gamma^0 gamma^j (assembled, anticommutation re-proved); tr[alpha_x C(k)] = -2 khat_x EXACT (A3 re-derived); EVERY odd-degree monomial sphere moment vanishes (degrees 1,3,5,7 — all 70 monomials, wider than the build's 13); a khat-FREE amplitude against the COMPLETE generic joint-odd polynomial through degree 7 has first moment 0 EXACT: mu_AU = 0 and mu_AF = 0 ride khat-freeness alone, no sign convention enters: PASS
AU4 RADIAL REDUCTIONS m = 0..8 (wider than the build's 6) at an EXACT symbolic axis (unit-sphere ideal reduction — no trig simplify), then the certificates on a generic DEGREE-6 H-schema (wider than the build's cubic): mu_B^I = 4 int khat_x [H(u) - H(-u)] dOmega = 16 pi b_x M_1, M_1 = int_{-1}^1 u H du EXACT; mu_B^alpha = 4 int khat_x^2 [H(u) + H(-u)] dOmega = 8 pi int [b_x^2 u^2 + (1-u^2)/2 (1-b_x^2)] SigmaH du EXACT; the survivor moment int khat_x beta_- dOmega = -2 pi b_x M_1 = -(1/8) mu_B^I EXACT; BOTH traced combinations joint-swap EVEN at the schema (parity refuses the kill — census C4 consistency): PASS
AU5 THE MONOTONICITY CHAIN EXACT: ds/d(r^2) = -(s_- + s_+) (polynomial identity); d b_D/ds = b_D/s^2 > 0; d(r^2)/du = 2 t rho > 0; COMPOSED: d b_D/du = -(b_D/s^2)(s_- + s_+)(2 t rho) < 0 STRICT on the open diamond — H(rho, u) nonincreasing in u with strict decrease wherever the ray meets the diamond; the exact-rational witness (t, r) = (3/10, 1/20): s_- = 7/80, s_+ = 39/80, s = 273/6400 (MO3 §3.5 re-pinned); the outward ray min(t,1-t) - t <= 0 so rho + t > min(t,1-t) at rho > 0: H(rho, +1) = 0 EXACT; sign exhibit int u h(1-u) du = -2h/3 < 0: PASS
AU6 SANDWICH + JET TRACES + PHASE: MO3 :210-:211 two forms of a_0 EQUAL on |khat| = 1 (re-derived); P_- a_0 P_- = -i a_Delta khat_x beta_- P_- EXACT 4x4 identity on |khat| = 1 (MO3 §4.1 re-derived on my independent Dirac ground); tr[jet] = 4 khat_x (b+ - b-) and tr[alpha_x jet] = 4 khat_x^2 (b+ + b-) EXACT; the placement phase (-i)(+i) = +1 — the adjoint pair composes real: PASS
AU7 COR-A COMPLETION + BRANCH + ROBUSTNESS: L_deg3 = 0 + 0 + 16 pi b_x M_1 + mu_B^alpha; plane locus b_x = 0: L = mu_B^alpha > 0 STRICT — the branch condition L = 0 fails on a displayed open locus; robustness at both degenerate weightings: (w_I, w_alpha) = (0-free, 1) nonzero on the plane, (1, 0) nonzero off the plane (sign of M_1 strict); branch integrals I3 = log Lambda divergent formal, I4 = 1 - 1/Lambda -> 1 bounded; census exhibits reproduced from MY formulas: constant coefficients kill mu_B^I (M_1[const] = 0) and leave mu_B^alpha = (16 pi/3)(b+ + b-)|_const = 32 pi h0/3; the odd-defeat moment 4 int khat_x (c1 khat_x) dOmega = 16 pi c1/3 (census's 8 pi/3 is the UNSCALED trace-slot instance int khat_x (f - f(-)) dOmega at f = khat_x = 2 x (4pi/3) x 1 — consistent, factor bookkeeping displayed): PASS
AU8 THE CONSTANT AUDIT (the one catch): census mu_B^alpha = 4 S and MO3 c_alpha = S/(8 pi) with S = int khat_x^2 Sigma dOmega force mu_B^alpha = 32 pi c_alpha EXACTLY (symbolically AND on the generic degree-6 schema) — the build's displayed 'mu_B^alpha = 8 pi c_alpha' (§4.3 M-II, §5.1, D5) is WRONG BY THE FACTOR 4; sign, support, strict positivity, and the fired branch are UNAFFECTED (32 pi c_alpha > 0 iff 8 pi c_alpha > 0): PASS
AUDIT-BATTERY 8/8 PASS
T8SR-AUDIT-BATTERY-DONE
```

---
## 4. THE SEVEN COMMISSIONED CHECKS, ADJUDICATED AT THE BYTES

```text
CHK-1 EACH DISPLAYED SYMBOL RE-DERIVED FROM ITS CITED STOCK BYTES —
   PASS, no adjusted symbol found (the hunted kill is ABSENT). (a) The
   stencil weights (1/2, -1/4, -1/4) on (0, +sqrt2, -sqrt2) are REM
   :169-:170 bytes, not the build's invention; the identity
   sum w_l e^{i lambda^2 y} = -i e^{iy} sin(y) is R2 :376-:379 bytes;
   my AU1 re-proves it AND its conjugate independently (my own second
   closed form (1/2)(1 - e^{2iy}) as cross-check), full series to y^4.
   The degree -2 coefficient +1 (=> +V_2^2/4 at y = V_2/(2|k|)) is
   REAL AND IDENTICAL IN BOTH PHASE CONVENTIONS — no sign choice can
   have been tuned to make the parity land: the class-A kill rides
   khat-freeness ALONE (AU3: the kill holds against the COMPLETE
   generic odd polynomial through degree 7 — 70 monomials, wider than
   the build's 13 — for ANY khat-free amplitude, any convention).
   (b) V_2(x) = (8 pi^2/7)(1 - 128|x|^7) RE-DERIVED from the R2 :81
   profile bytes (v = tau_R 32 min(t,1-t)^3, tau_R = pi/sqrt2), not
   copied (AU2, including the normalization int v/tau_R = 1).
   (c) The khat-free TYPING of the c = 0 strata is DERIVED, not
   authored: the floors at bytes put every interband (transfer-
   oscillation) block at c >= 1 (REM :186-:187), so c = 0 strata are
   pure sea-sea two-level; the two-level channel is displayed as an
   (x, omega) family at bytes (REM :279, :292-:296; R2 :369); the
   khat-dependent ray-average dressing is the bare-J stratum, m_0-
   killed at bytes (REM :189-:190; R2 :397 for the assembled degree-0
   fate). Three sealed legs, all re-located; the inference is two
   steps of displayed set arithmetic. GRADE (KF1/KF2 pointwise-symbol
   license) is the census's own completion-spec grade — correctly
   claimed, correctly marked (build g-3).
CHK-2 THE MOMENT CERTIFICATE RE-DERIVED; SCOPE = THE SEALED RAY
   AVERAGES EXACTLY — PASS with ONE display-constant correction
   (COR-1 below). beta_s(x, khat) = int_0^1 b_D(t, x + s t khat) dt
   at MO3 :204; b_D radial at :124; |x + s t khat|^2 = |x|^2 +
   2 s t |x| u + t^2 makes beta_s = H(|x|, s u) EXACTLY — the build's
   §4.2 reduction is the sealed object, no substitute, and my AU4
   proves both sphere-reduction identities by exact ideal reduction
   (m = 0..8) and both certificate reductions on a degree-6 schema.
   The sign chain M_1 < 0 STRICT on 0 < rho < 1 is re-derived with
   the full derivative CHAIN made exact (AU5: ds/d(r^2) = -(s_-+s_+);
   d b_D/ds = b_D/s^2; d(r^2)/du = 2 t rho; composed d b_D/du < 0 on
   the diamond), the outward-ray vanishing H(rho, +1) = 0, the MO3
   §3.5 exact-rational witness re-pinned (7/80, 39/80, 273/6400), and
   the strictness argument (continuity + witness near u = 1) checked
   as displayed. The even slot's strict positivity is the sealed MO3
   §3.5 display consumed at bytes (v-8) — correctly NOT re-claimed as
   new. Support/vanishing loci: exact per MO3 §3.5 support text.
CHK-3 L_deg3 RE-EVALUATED INDEPENDENTLY, EXACT — PASS. L_deg3(x) =
   0 + 0 + 16 pi x_hat_x M_1(|x|) + mu_B^alpha(x) at COR-A's
   displayed unit aggregation (AU7): mu_AU/mu_AF vanish by AU3's
   complete-generic kill; mu_B^I = 16 pi x_hat_x M_1 by AU4 (the "4"
   from the census functional times 4 pi x_hat_x M_1 from the
   reduction); mu_B^alpha > 0 strict on the shell. On the plane locus
   x_hat_x = 0, 0 < |x| < 1: L = mu_B^alpha > 0 STRICTLY — the
   L = 0 branch condition fails on a displayed open locus. EXACT; no
   numeric anywhere.
CHK-4 THE BRANCH FIRED PER THE AUDITED COR-A STATEMENT — PASS. COR-A
   at bytes (T4SR-AUDIT: "the correct branch condition is a nonzero
   TOTAL first moment"; census-audit c-10: "the SUM decides both
   ways"; c-14: "the ENTIRE degree -3 angular total vanishes (COR-A:
   the total IS L)"): the build fires on the SUM L_deg3 — not on any
   per-stratum moment — exhibiting the sum nonzero (strictly
   positive) on a displayed open locus, which is exactly the failure
   of the L = 0 branch's condition and exactly what the armed table's
   L != 0 row consumes (census s-3, audited CA7). The fired
   consequence (log GENUINE at the class total; rate REFUTED; count
   does NOT land at <= -4; 1 - 1/Lambda the road not taken) is the
   armed row verbatim. NOT-REFUTED discipline held: no divergence of
   tr K^Sigma_n claimed; the residual hedges (cross-class accidental
   cancellation outside the enumerated degree-3 class; finer-than-
   displayed weighting) are DISCLOSED by the build (§5.3), matching
   COR-A's own non-exclusion grammar.
CHK-5 ENUMERATION COVERAGE: EVERY CENSUS CLASS CONSUMED, NONE
   DROPPED — PASS. The audited census table (CA2/CA6: A-U, A-F, B
   c-routes both placements; "one term per class, none missing, none
   extra") maps onto the build's §5.1 table term by term: A-U ->
   mu_AU = 0; A-F -> mu_AF = 0; B-* -> the two displayed traced-slot
   functionals mu_B^I, mu_B^alpha (the census's own displayed pair,
   v-2), BOTH certified, with the undisplayed entering combination
   covered by the (L-3) robustness family over every nonzero constant
   weighting and the placement pairing covered at D6/AU6 (the adjoint
   placements compose real; the sandwich survivor's moment sits
   inside the family at -(1/8) mu_B^I). No census slot is dropped; no
   extra slot is invented. RESIDUAL, correctly disclosed by the build
   (C-4/§5.3): x-DEPENDENT weight conspiracies are outside the
   displayed robustness family — the build's own scope text names
   this limit; COR-A's displayed aggregation (unit weights, CA6) is
   the grade the completion claims. No overclaim found at that grade.
CHK-6 SCOPE HELD (SUMMED ONLY; V-2 UNCROSSED) — PASS. Span-check of
   the whole build artifact: every quantified claim is of K^Sigma per
   (n, cell) or of the sealed profile b_D's ray averages (function-
   level displays, no quantifier content); no per-composite or
   per-pair quantity is quoted, moved, or decided; the MO3 jet is
   consumed as the sealed display of the c-class coefficients' ray-
   average structure (the audited T4SR/census typing, v-8), its
   per-pair frame content untouched; no flag of the record is
   flipped (the census's CENSUS-INCOMPLETE is completed by supplying
   its own named completing object — the exact successor shape its
   §5 and the T4SR successor note license, census-audit c-15 "No
   objection"); witness actions left to the registrar. V-2 appears
   only in the build's own scope-guard language.
CHK-7 FENCE-SCAN — CLEAN. No float ground (regex sweep: hits are
   version/section/timestamp/digest tokens only); no numeric-eval
   tokens (evalf/float/numpy: zero); no measured constant and no
   measured-constant comparison ("fine-structure" grep hits are the
   substring of the build's phrase "weight fine-structure" — false
   positives; nothing numerological anywhere); Lambda/V_2/beta_s/
   a_Delta/c_alpha/M_1 all formal/symbolic throughout; no fenced-
   class file opened (token hits are fence language + "plan" inside
   "plane"); "Q-..." tokens noted EXPECTED-UNLOCATABLE, never chased;
   no git; ONE output + sidecar; the build's cutoff (05:05:39)
   precedes its seal (05:08) in order; sw-1/sw-3 verified by my own
   filename/mtime sweeps (the only additional T8SR|DISPLAY_PAIR match
   in either root is THIS audit, created after the build's cutoff;
   the cleanroom root has zero matches; the newest other workspace
   artifacts are the T9SR pair — a different commission by name, not
   opened). The three disclosed pre-run script corrections are
   credible: the build's §8 battery re-runs in MY fresh venv with
   output BYTE-IDENTICAL to its recorded block (6/6 PASS).
```

---
## 5. FINDINGS — CORRECTIONS AND NOTES (none moves the verdict)

```text
COR-1 [THE ONE SUBSTANTIVE FINDING — display constant, VERDICT-
   NEUTRAL]: the build's translation of the even slot into MO3's
   contact coefficient is WRONG BY A FACTOR 4, three times (§4.3
   M-II tail "mu_B^alpha(x) = 8 pi . [the alpha_x-component of
   c(x)]"; §5.1 "mu_B^alpha(x) = 8 pi c_alpha(x)"; D5's
   "L = ... + 8 pi c_alpha"). At bytes: census :215 defines
   mu_B^alpha = 4 int_S2 khat_x^2 Sigma dOmega; MO3 §3.5 defines
   c_alpha = (1/(8 pi)) int_S2 khat_x^2 Sigma dOmega; hence
   mu_B^alpha = 32 pi c_alpha EXACTLY (my AU8, symbolically AND on
   the generic schema). WHY VERDICT-NEUTRAL: every use of the even
   slot consumes ONLY its strict positivity and support (32 pi
   c_alpha > 0 iff 8 pi c_alpha > 0); the build's OTHER display of
   the same slot — the integral form mu_B^alpha = 4 int khat_x^2
   [beta_+ + beta_-] dOmega and the L-line "L_deg3 = 16 pi x_hat_x
   M_1 + mu_B^alpha(x)" (§0, §5.1, §5.2, flag block) — is CORRECT;
   the branch, both deliverables, and the headline are unaffected.
   The corrected display: mu_B^alpha(x) = 32 pi c_alpha(x).
COR-2 [cosmetic miscount]: the flag block's label "SEALS_VERIFIED =
   11/11" contradicts its own enumeration (8 + 3 + 1 = 12 files) and
   the build's §1 ("12/12 MATCH"). Twelve seals were verified; the
   label should read 12/12. My own independent count: 12 files, 12
   sidecars checked OK (§1 above).
N-1 [phrasing, no content]: §3.2 A-F says the J-junction weight sum
   "gives the SAME exact function -i e^{iy} sin(y)" while
   simultaneously crediting the F-side "+i at order y via m_2 = -1"
   (REM R1d). At the F convention the function is the CONJUGATE
   +i e^{-iy} sin(y) (my AU1 displays both). The consumed conclusion
   is convention-independent — the y^2 (degree -2) coefficient is +1
   REAL in both conventions and the kill rides khat-freeness alone —
   so nothing downstream moves.
N-2 [inherited display note]: the census's own "8pi/3 defeat" exhibit
   is the UNSCALED trace-slot instance int khat_x (f - f(-khat))
   dOmega at f = khat_x (= 2 x 4pi/3), while the scaled functional
   mu_B^I at the same instance is (16 pi/3) c_1 (census CA5's
   decomposed form) — both of record, no contradiction; displayed
   here only so the factor bookkeeping is visible next to COR-1.
```

---
## 6. VERDICT — PER CLAIM AND OVERALL

```text
PER CLAIM:
  DELIVERABLE (i)  [the o = 3 (c = 0) segment-corrected angular
    symbols]: CONFIRMED. The khat-free (angular-scalar) typing is
    DERIVED from three sealed legs all re-located at bytes; the exact
    tower coefficient +V_2(x)^2/4 re-derived independently in both
    phase conventions with V_2's closed form re-derived from the
    profile bytes; mu_AU(x) = 0 and mu_AF(x) = 0 EXACT on the
    consumed all-lengths lemma, robust against the complete generic
    odd trace family through degree 7. AC-4's unclaimed degree-3
    parity is now claimed AND derived at the correct license, exactly
    as the census's completion spec commissioned. (N-1 phrasing note
    only.)
  DELIVERABLE (ii) [the beta_s first-angular-moment certificate]:
    CONFIRMED-WITH-CORRECTION (COR-1). The two commissioned integrals
    are certified exactly per cell point: odd slot 4 pi x_hat_x
    M_1(|x|) with M_1 < 0 STRICT on 0 < |x| < 1 (monotonicity chain
    re-derived exact; witness and outward ray re-pinned), vanishing
    exactly on {x_hat_x = 0} u {x = 0} u {|x| >= 1}; even slot > 0
    STRICT on the open shell (the sealed MO3 §3.5 display consumed at
    bytes), = 0 outside. The scope is EXACTLY the sealed ray averages
    (beta_s = H(|x|, s u) is the sealed definition, no substitute).
    The c_alpha translation carries the factor-4 display error
    (COR-1): corrected display mu_B^alpha = 32 pi c_alpha.
  COMPLETION (iii) [L_deg3 and the branch]: CONFIRMED. L_deg3(x) =
    0 + 0 + 16 pi x_hat_x M_1(|x|) + mu_B^alpha(x) at COR-A's
    displayed unit aggregation, re-evaluated independently, EXACT;
    NOT identically zero (strictly positive on the displayed plane
    locus with the exact-rational witness); robust under every
    nonzero constant weighting of the certified class-B pair, with
    the residual (x-dependent weight fine-structure; content outside
    the enumerated class) disclosed by the build itself. The armed
    branch fired on the SUM per the audited COR-A statement: the
    L != 0 row — log GENUINE at the class total, the commissioned
    log-free rate REFUTED, the count NOT at degree <= -4. Fired as
    armed; nothing overclaimed; NOT-REFUTED discipline held.

OVERALL: **CONFIRMED-WITH-CORRECTIONS.** The T8SR headline
COMPLETE-LOG-GENUINE stands adjudicated at the bytes: both
commissioned displays are genuinely DERIVED on the citation-located,
seal-verified stock, the degree-3 angular census is genuinely
COMPLETED mechanically, and the armed branch genuinely FIRES on the
L != 0 side at the audited COR-A aggregation grade. The corrections
(COR-1 factor-4 in the c_alpha translation; COR-2 flag-block seal
miscount) and notes (N-1, N-2) are display-level and VERDICT-NEUTRAL;
no displayed symbol was adjusted to make a parity land (the hunted
kill is ABSENT — the class-A kill is convention-independent and the
class-B certificate is the sealed profile's own geometry). The wall's
last summed number carries its logarithmic growth at the census
grade, per (n, cell), Lambda formal, exactly as the build displays.
```

---
## 7. CHOICE LEDGER (commission T8SR — DISPLAY-AUDIT)

```text
AC-1 VERDICT CLASS [YOURS]: CONFIRMED-WITH-CORRECTIONS over CONFIRMED
     and over REFUTED. Ground: every load-bearing display re-derives
     exactly (8/8 my battery; 6/6 byte-identical re-run of the
     build's), every quote re-locates at bytes, and the two
     corrections are real but consume nothing the verdict rests on
     (COR-1's slot is used only through its sign; COR-2 is a label).
     REFUTED would require a display that fails re-derivation — none
     does. Plain CONFIRMED would suppress two genuine byte-level
     errors: not available under DEFAULT-REFUTE honesty.
AC-2 INDEPENDENCE DISCIPLINE [YOURS, displayed]: my battery shares NO
     code path with the build's (independent Dirac construction from
     gamma^0 gamma^j with anticommutation re-proved; my own second
     closed form for the stencil; ideal-reduction axis handling
     instead of trig; wider windows everywhere: degree-7 odd family,
     m <= 8 reductions, degree-6 schema, both phase conventions;
     V_2 re-derived from the profile bytes the build only cited).
     The build's battery was re-run ONLY as a reproducibility check,
     after my own battery passed.
AC-3 THE KILL HUNT [YOURS, per the tasking]: the commissioned hunt
     ("a symbol adjusted to make a parity land") was run on both
     deliverables: (i) the class-A kill was tested for convention
     sensitivity (AU1 both signs; AU3 for ANY khat-free amplitude
     against the COMPLETE odd family) — the kill cannot be tuned
     because it depends on nothing tunable; (ii) the certificate's
     sign was re-derived from the profile geometry alone (AU5 chain),
     not from the build's schema. The one real discrepancy found
     (COR-1) is a translation constant, not a parity adjustment.
AC-4 MY OWN PRE-RUN CORRECTIONS [YOURS, disclosed §3]: two, both
     auditor-side toolchain/arithmetic (m_4 expectation; trig
     simplifier), disclosed with the recorded run exactly as the
     build disclosed its three — symmetrical honesty, no silent
     absorption on either side.
AC-5 SCOPE OF ADJUDICATION [YOURS]: the census's own enumeration and
     the consumed lemma are taken at their AUDITED grades (census-
     audit CONFIRMED-WITH-CORRECTIONS; T4SR-AUDIT all-lengths R2),
     NOT re-audited here — my commission audits the DISPLAY PAIR and
     its completion, and consumes the same sealed ground the build
     consumed, independently seal-verified. The residual hedges the
     build disclosed (§5.3) are reported as disclosed, not converted
     into findings: they are COR-A's own non-exclusion grammar.
AC-6 HOMONYM SPLIT [inherited, disclosed]: n carrier index; k
     momentum; (o, c) REM strata indices; s in beta_s the RAY SIGN;
     u = khat.x_hat the reduction variable; b_x = x_hat_x in my CAS.
```

---
## 8. TOY_SEPARATION

```text
T-1 AU3/AU6 use the EXACT 4x4 Dirac algebra of the record assembled
    independently (gamma^0 gamma^j; anticommutation re-proved) — the
    sealed spinor structure itself, not a toy; re-instantiated for
    the trace slots and the sandwich only; no matrix-grade result is
    carried to the carrier quantifier.
T-2 AU4's degree-6 H-schema is an identity-schema stand-in for the
    sealed ray averages' u-dependence: the reduction identities are
    proved as polynomial identities on the unit-sphere ideal and
    consumed at schema grade; the certificate's sign/support content
    rides the EXACT profile geometry (AU5) and the sealed witness
    bytes, never the schema.
T-3 AU1/AU2 are the sealed stencil and profile bytes themselves at
    exact rationals/surds; AU7 is exact formal-Lambda carrier
    arithmetic; Lambda never valued; M_1, c_alpha, mu_B^alpha stay
    symbols with only their SEALED/DERIVED sign assumptions.
T-4 No testbed evaluation; nothing witness-family-grade promoted; no
    spectral datum valued; no stratum symbol authored; no float
    anywhere in this artifact (the only decimals are timestamps,
    versions, and line/digest references).
```

---
## 9. SWEEPS, OUTPUT DISCIPLINE, FLAG BLOCK

SWEEP CUTOFF: **2026-08-15 05:11:25 -0500** (declared §0 before any
ground consumption; corpus roots as declared). Sweeps run: filename
sweep of both roots for T8SR|DISPLAY_PAIR|DEG3 (workspace: the build
pair, the census pair by DEG3, and THIS audit only; cleanroom: zero);
mtime adjacency at cutoff (newest other files: the T9SR
GRAVITY_CLOSE_RESCORE pair — different commission by name, not
opened); token sweeps INSIDE the twelve named files only. No
register/road_/ledger/lens/plan/tracker/THE_HANDOFF file opened
(filename matches only). "Q-..." tokens: EXPECTED-UNLOCATABLE per the
tasking; noted, never chased. Artifacts sealed after the cutoff are
UNSWEPT and unclassed here. My output path was probed ABSENT at STEP
0 before anything else; ONE output (this artifact) plus its seal
sidecar; no other file written to the workspace; no git action.

```text
T8SR_DISPLAY_PAIR_AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS(
  headline COMPLETE-LOG-GENUINE ADJUDICATED TRUE AT THE BYTES:
  deliverable (i) CONFIRMED — the o = 3 (c = 0) segment-corrected
  angular symbols are khat-FREE at the record's displayed two-level
  reduction, tower coefficient +V_2^2/4 exact (both conventions, my
  AU1), V_2 closed form re-derived from profile bytes (AU2), mu_AU =
  mu_AF = 0 EXACT against the complete generic odd family through
  degree 7 (AU3); deliverable (ii) CONFIRMED-WITH-CORRECTION — the
  beta_s certificate exact per cell point (odd slot 4 pi x_hat_x
  M_1, M_1 < 0 strict on the open shell by the exact monotonicity
  chain AU5; even slot strictly positive, sealed MO3 §3.5 consumed;
  support/vanishing loci exact) with COR-1: mu_B^alpha = 32 pi
  c_alpha, NOT the displayed 8 pi c_alpha (factor 4; positivity and
  branch unaffected); completion (iii) CONFIRMED — L_deg3 =
  16 pi x_hat_x M_1 + mu_B^alpha exact at the audited COR-A unit
  aggregation, NOT identically zero (strictly positive on the plane
  locus, exact-rational witness), robust under every nonzero
  constant weighting; THE ARMED BRANCH CORRECTLY FIRED ON THE SUM:
  L != 0 => log GENUINE at the class total, the commissioned
  log-free rate REFUTED, count NOT at degree <= -4 — per the audited
  COR-A statement, nonzero SUM, not per-stratum. Enumeration
  coverage: every audited census class consumed, none dropped, none
  invented (CHK-5). Corrections: COR-1 (factor-4 c_alpha
  translation, three spots), COR-2 (flag label 11/11 vs 12 verified).
  Notes: N-1 (F-side conjugate-function phrasing), N-2 (census
  8pi/3 vs 16pi/3 bookkeeping displayed). NO adjusted symbol found;
  NO misquote found; scope held summed-only, V-2 uncrossed; the
  build's fences held (CHK-7 CLEAN); the build's battery re-runs
  BYTE-IDENTICAL in my venv. )

SEALS_VERIFIED = 13/13( the claimed artifact (STEP 0) + the twelve
  input artifacts of §1, every digest recomputed from bytes at path
  THIS session via shasum -a 256 -c against sidecars BEFORE
  consumption; all OK; none unverifiable; the four stock digests
  additionally matched against WSR §1's prefixes and MO4 §1 G-5's
  full-digest pin at bytes. Scoped reads: these thirteen files and
  NO others. )

FORBIDDEN_IMPORTS = none( exact symbolic only; NOTHING numeric
  evaluated (Lambda, V_2, beta_s, a_Delta, c_alpha, M_1 formal/
  symbolic; witness values exact rationals); no float ground; no
  measured constant; no measured-constant comparison; no value
  frozen; alpha never computed or approached; kappa_record nowhere;
  proof_authorized never exercised; no per-pair route traversed; no
  fenced-class or out-of-scope file opened; no flag of the record
  flipped; no witness action (registrar's); no git; ONE output +
  sidecar at the commission-distinct path, probed ABSENT first. )

MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv
  t8srauditvenv, ONE battery, 8 checks, 8/8 PASS on the recorded run
  (two auditor-side pre-run corrections disclosed §3), script and
  output reproduced verbatim §3 by file-append; PLUS the build's §8
  battery re-run verbatim in the same fresh venv: output
  BYTE-IDENTICAL, 6/6 PASS; shasum/scoped file reads/filename+token
  sweeps otherwise; NOTHING numeric evaluated.

SWEEP_CUTOFF = 2026-08-15 05:11:25 -0500 (both roots; fenced classes
  and all non-named files filename-matched only, never opened; token
  greps inside the thirteen named files only; post-cutoff seals
  unswept).

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
DETERMINATION ONLY; witness/flag actions are the registrar's.
T8SR_DEG3_DISPLAY_PAIR_AUDIT_RESULT = SEALED.
```
