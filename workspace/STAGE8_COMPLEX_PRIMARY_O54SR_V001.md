# STAGE 8 — O54SR: IS THE RECORD'S COMPLEX PRIMARY, OR A DECOMPOSITION OF SOMETHING?

Lane: BUILD (read-and-classify at bytes)
Date: 2026-08-16
Artifact: `STAGE8_COMPLEX_PRIMARY_O54SR_V001.md`

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

No authoring. No advocacy. No adoption. Nothing computed; no magnitude approached.

---

## 0. HEADLINE

```text
Q1  PRIMARY-OR-DECOMPOSITION-OF   = BOTH-AT-DIFFERENT-SITES
                                    (record's own complex PRIMARY;
                                     two decomposition-of sites, both
                                     expressly NOT the record)
Q2  WHAT IS ENTERED               = "THERE IS A COMPLEX" (the class),
                                     NOT "WHICH COMPLEX"
Q3  SOURCE (x) RECORD (x) ENV     = CHOSEN — stipulated alongside the complex,
                                     not derived from it
Q4  DYNAMICS                      = UNMODIFIED (unitary; every non-unitary
                                     object is typed as a reduction)
Q5  DISCRIMINATOR                 = OBSERVATIONALLY-EQUIVALENT-AT-BYTES,
                                     with one near-miss reported and refused
```

The clean result of this commission is that the corpus **does** separate the two readings
for its own record complex — and separates them in the direction of PRIMARY — but does so
only *negatively*, by refusing every identification with the decomposition-bearing objects
it also carries. It never states the positive claim. Q5 is therefore a genuine null.

---

## 1. Q1 — PRIMARY OR DECOMPOSITION-OF?

### GRADE: **BOTH-AT-DIFFERENT-SITES** — itemized below.

The corpus contains three complex-valued sites. The first is the record's complex and is
PRIMARY. The second and third are decompositions of named prior objects, and at both the
corpus itself states they are **not** the record and are **not** identified with it.

---

### SITE 1 — `K` in `BareRec_2`. **PRIMARY.**

`BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md :287-292`, quoted whole:

> "where `K` is a finite oriented regular CW complex of dimension at most two,
> each connected component carries zero or one distinguished root `r`, `L`
> assigns a one-dimensional Hermitian fiber to every vertex, and `U` is the
> discrete unitary connection below. The first-opening object has exactly one
> root. Rootless objects are retained so every elementary cell has a test
> inclusion."

This is the definitional introduction, and it is a **`where` clause of a category-object
declaration** — `:281-285`: *"An object of `BareRec_2` is"* `(K,r,L,U)`. There is no
genitive. Nothing is named that `K` decomposes. No continuum, manifold, region, atom, or
larger structure appears in the sentence or in the object tuple. The tuple's other three
slots (`r`, `L`, `U`) are all structures carried **on** `K`, not structures `K` is carried
on.

The absence is corroborated by the corpus's own base census. `STAGE8_BASE_GEOMETRY_O48SR_V001.md :629`
types this object and states what it carries:

> "| **BASE-3** | `K` in `BareRec_2 = (K,r,L,U)` | *"a finite oriented regular CW complex of
> dimension at most two"*, roots, incidence, orientation — **and nothing else** | cell weights
> `M_p = I` are a **hypothesis**, Gate 3 open |"

and `:632-635`, quoted whole:

> "**BASE-3 is bare incidence plus orientation and a root.** If the question is asked of the
> record's own cellular object, the answer is: cells, faces, adjacency, ordering, orientation,
> one root — and a cell inner product that is hypothesised to be the identity and therefore
> distinguishes nothing."

**"and nothing else"** is the operative clause. A decomposition-of reading requires a
decomposed thing in the object's data; there is none.

---

### SITE 2 — `K_L` in the filtration audit. **DECOMPOSITION-OF.** Named: the four-torus.

`BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md :826-835`, quoted whole including its
adverse clause:

> "For the filtration audit, use the finite translation-complete test object
>
> ```text
> K_L = the oriented 2-skeleton of (Z/LZ)^4,    L>=3,
> ```
>
> with one Hermitian line at each vertex and arbitrary periodic unitary link
> transports. `K_L` is a finite two-dimensional CW complex with four edge
> directions and every elementary plaquette; the higher-dimensional cells of
> the four-torus are not included."

**This is a decomposition-of, unambiguously.** The named prior object is `(Z/LZ)^4`, the
four-torus. `K_L` is its **2-skeleton** — a dimensional truncation of a larger structure,
with the corpus stating exactly what was cut: *"the higher-dimensional cells of the
four-torus are not included."*

**But the corpus expressly denies that `K_L` is the record.**
`STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_AND_EXTENSION_DETERMINATION_V001.md :17-23`, whole:

> "**No derived physical filled two-cell was found in the bounded whole-program
> sweep.  The closest object is nevertheless much stronger than "nothing":  V011
> already specifies a concrete, curvature-capable filled carrier, the periodic
> two-skeleton `K_L`, and defines its face cochains, face boundary, local
> holonomy/curvature, and generated face-response form.  `K_L` is a frozen test
> object selected by the specification, not a face forced by record composition.**"

and its booking `:355-357`:

> "```text
> K_L_forced_by_record_composition = false | TYPE-R |
> ```"

Its own table entry `:281` types it: **"Selected, not forced."**

So: the corpus's one true skeleton-of-a-prior-object is a **test fixture**, booked
`= false` against record composition, and V011 itself adds at `:1397-1412` (as quoted by
the census at `:26-28`) that *"One hypercubic sequence is a **regression fixture, not proof
of universality**."*

---

### SITE 3 — the PL refinement category's graphs. **DECOMPOSITION-OF.** Named: the adopted physical background `X_o`.

`STAGE8_TASK5_EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V001.md :226-245`, quoted whole
across the heading and the map:

> "### 1.2 Every stage is a cellulation of the same actual carrier
>
> For `G` in component `o`, let
>
> ```text
> r_G:|G| -> X_o                                      (T1-2)
> ```
>
> be its PL realization homeomorphism. […]
>
> Thus a new edge is not renamed combinatorics: `(T1-3)` is a genuine path in
> the adopted physical background."

and its own symbol table `:123`:

> "same-region    = two cellulations of the same declared physical carrier;"

**This is the decomposition-of reading in full.** The prior object is named twice — `X_o`,
"the same actual carrier", "the adopted physical background", "the same declared physical
carrier" — and the complexes are its cellulations, related by a realization homeomorphism.

**But this is BASE-2, not BASE-3, and the corpus states the bridge is open.**
`STAGE8_BASE_GEOMETRY_O48SR_V001.md :620-623`, whole:

> "## §C — THE DECISIVE STRUCTURAL FACT: FOUR BASES, NOT ONE
>
> This is why the grade is PARTIAL and not BASE-CARRIES-GEOMETRY. The corpus carries **four
> distinct base-like objects**, and does not identify them with one another."

and `:637`: *"**The bridges between these are open of record, in both directions.**"*

Its flag `FL-1` at `:934`, quoted whole:

> "| **FL-1** | **THE GEOMETRY AND THE RECORD SIT ON DIFFERENT BASES.** | Every substantive
> base-side geometric structure (metric, geodesic, parallel transport, extrinsic curvature,
> coframe, density, `Vol_4`) lives on a smooth carrier — `M_G`, `M_K`, `Sigma`. The record's
> own cellular object `K` is *"a finite oriented regular CW complex of dimension at most two"*
> with a root, and carries incidence, faces, orientation, and cell weights hypothesised to be
> the identity. **The corpus does not identify these objects.** Forward bridge `f_g` =
> **UNDECIDABLE**, `SUPPLIED = 0`. Reverse bridge =
> `reverse_discrete_to_smooth_bridge_canonical = false`. Registered here as the load-bearing
> structural fact of this audit. |"

---

### 1.1 THE REFINEMENT STRUCTURE, WEIGHED — as the commission requires

The commission asks whether the corpus carries refinement **between complexes** (compatible
with PRIMARY) or refinement **of a continuum into** a complex (not compatible). The answer
is in three parts, and the third is an indeterminate.

**(a) The one refinement law that is BUILT is complex-to-complex.**

`BID_FIRST_OPENING_GRAPH_REFINEMENT_QUOTIENT_V001.md :30-41`, quoted whole:

> "The elementary reduction
>
> ```text
> r --e_1--> v --e_2--> p
>   -> r --e--> p
> ```
>
> uses
>
> ```text
> U_e=U_(e_2) U_(e_1).
> ```"

Source: an oriented source-record **graph**. Target: an oriented source-record **graph**.
The declared domain at `:12-19` is *"a connected oriented source-record graph with"* one
root, one public endpoint, no other public vertex labels, unitary edge transports. **No
continuum appears at either end.** Its inverse is named at `:43`: *"Its inverse is an
invisible bivalent subdivision."* This is refinement **between complexes**, and is
therefore compatible with PRIMARY.

Its own boundary clause, `:74-79`, quoted whole with the adverse sentence:

> "## Earned result and boundary
>
> Within the declared one-root/one-public-endpoint acyclic first-opening class,
> the one-arrow graph is the unique reduced representative. This is a graph
> refinement theorem, not a proof that nature permits only that class. Enlarged
> objects remain valid later branches."

**(b) The refinement category `Ref` — whose objects would carry the geometry — is UNBUILT,
and its objects are complexes, not continua.**

`STAGE8_TASK5_JREF_AND_LOCAL_KERNEL_ESTIMATES_LANE2_V001.md :128-134`, quoted whole:

> "```text
> Ref_0       = the derived identity/isomorphism finite realization skeleton
> Ref         = the unbuilt geometric refinement category
> J_0         = the built finite current/kernel realization on Ref_0
> J_ref       = the unbuilt target-independent geometric realization functor
> zero_ext    = append identity coordinates; no mesh shrinkage
> refinement  = a certified map of the same represented physical region
> ```"

and `:246-257`, the contract, quoted whole:

> "```text
> J_ref : Ref -> CompletedCurrentKernelClass,      (D2-1)
> ```
>
> where an object of `Ref` carries at least:
>
> ```text
> finite oriented record complex;
> Gate-4 boundary/incidence and degree;
> connection and source incidence;
> algebra and representation;
> coframe/volume and current-density normalization;
> ```"

Sources and targets, read at bytes: `J_ref`'s **source** is `Ref`, whose objects are
*"finite oriented record complex"* plus decorations; its **target** is
`CompletedCurrentKernelClass`, an analysis object. **The functor runs complex → analysis,
never continuum → complex.** Both `Ref` and `J_ref` are labelled **"unbuilt"** in the
corpus's own symbol table. Note also `zero_ext = append identity coordinates; no mesh
shrinkage` — the corpus's built extension operation explicitly does **not** shrink a mesh,
which is the operation a continuum-discretization reading would require.

The one decomposition-flavoured phrase in that table is
`refinement = a certified map of the same represented physical region`. It is a **gloss on
an unbuilt category**, and "represented physical region" is not tied to `K` anywhere.

**(c) The corpus's own cell refinement/coarsening law has its form WITHHELD.**

`BARE_SURFACE_I2_DETERMINATION_V001.md :36`, the relevant clause quoted whole:

> "the exact refinement/coarsening law of the cell (§I.5 — named as exact and derived; its
> explicit form is not displayed on the sealed ground)."

and `:110`, whole:

> "The ground names four content-withheld items: the exact refinement law's form (§I.5), the
> balanced-geodesic identities' form (§II.9), the onset bound's form (§II.9), the thirteen
> interface junctions' identities (§II.8)."

and `:112`, whole:

> "- **Refinement law:** it is structure OF the cell (§I.5), acting within the unit-modulus
> write algebra (§II.6 is stage-unqualified: "the records provably do not carry" amplitude).
> Composition of unit-modulus factors is unit-modulus (CAS B4.1; induction — base B2.1, step
> B4.1): **no refinement or coarsening stage can generate an amplitude slot.** The vacancy is
> refinement-stable whatever the law's detailed form."

**This is the honest limit of Q1's refinement audit.** The corpus's central
refinement/coarsening law is typed as *"structure OF the cell"* — which reads
complex-internal, hence PRIMARY-compatible — but its explicit form is **not on the sealed
ground**. Its sources and targets therefore cannot be read at bytes.

```text
REFINEMENT_BETWEEN_COMPLEXES_FOUND_AND_BUILT = true
  (BID_FIRST_OPENING_GRAPH_REFINEMENT_QUOTIENT_V001 :30-41)
REFINEMENT_OF_A_CONTINUUM_INTO_A_COMPLEX_FOUND = false | TYPE-S |
  patterns: 'triangulation of', 'discretization of', 'discretizing the/a'
  hits: 0 across both roots
CELL_REFINEMENT_COARSENING_LAW_SOURCE_AND_TARGET = INDETERMINATE-AT-BYTES
  reason: form content-withheld on the sealed ground (BARE_SURFACE_I2 :36,:110)
```

### 1.2 THE THREE CHOICE SPACES ARE EXPRESSLY NOT IDENTIFIED

The single most load-bearing text for Q1 is
`STAGE8_TASK4D_CELLULATION_INDEPENDENCE_OD3_VERDICT_INVARIANCE_THEOREM_V001.md :19-26`,
quoted whole including its adverse clauses:

> "The five public continuum consumers do **not** factor through `T_cyl` or that
> unitary-orbit quotient. Their required maps are not merely unproved: their
> domains require geometric/refinement and physical-response fields absent from
> `T_cyl`. Moreover, the 1,088 objects are linear-extension **support
> filtrations of one fixed unfilled `K_square`**, not full `J_ref` realizations,
> not cellulations of a continuum region, and not the charter's decompositions
> of an admitted atom into causal diamonds. No sealed map identifies these
> three choice spaces."

Read exactly: the record-side objects are **"not cellulations of a continuum region"**, in
the corpus's own words, and **"No sealed map identifies these three choice spaces."** The
decomposition-bearing space (causal-diamond decompositions of an admitted atom) is a third,
separate space, and it decomposes an *atom*, not the record complex. Its bookings, `:53-55`
and `:63-69`, quoted whole:

> "```text
> THE_1088_ARE_CHARTER_OD3_DIAMOND_DECOMPOSITIONS = false | TYPE-R |
>   test: fixed-square linear extensions versus disjoint causal-diamond covers
>         of an admitted atom with exact four-volume sum
> ```"

> "```text
> CELLULATION_INDEPENDENCE_PROVED = false | TYPE-U |
>   would-build: instantiated Ref/J_ref/tau_ref plus response naturality,
>                BoundaryCert and public-interface diagrams
>
> CHARTER_OD3_DISCHARGED = false | TYPE-U |
>   would-build: completed-response independence across every admissible
>                causal-diamond decomposition of the same atom
> ```"

### 1.3 Q1 VERDICT

## **BOTH-AT-DIFFERENT-SITES**

```text
SITE 1  K in BareRec_2 (the record's complex)        = PRIMARY
        ground: BID V011 :287-292 — a `where` clause of a category-object
        declaration, no genitive, nothing named that K decomposes;
        corroborated O48SR :629 "and nothing else"

SITE 2  K_L = the oriented 2-skeleton of (Z/LZ)^4    = DECOMPOSITION-OF
        decomposes: the four-torus (Z/LZ)^4
        ground: BID V011 :826-835 "the higher-dimensional cells of the
        four-torus are not included"
        STATUS: test object; K_L_forced_by_record_composition = false

SITE 3  PL refinement graphs, r_G:|G| -> X_o          = DECOMPOSITION-OF
        decomposes: X_o, "the same actual carrier" / "the adopted physical
        background" / "the same declared physical carrier"
        ground: Q408 EXHIBIT V001 :226-245, :123
        STATUS: BASE-2, not BASE-3; bridge to K open both directions (O48SR
        FL-1); "No sealed map identifies these three choice spaces" (TASK4D :26)

NET FOR THE RECORD'S OWN COMPLEX = PRIMARY, established negatively
```

**The qualification that keeps this honest.** The record's complex is graded PRIMARY
because every decomposition-of site in the corpus is *disowned* — booked `false`, typed as
a test fixture, or placed on a different base with an open bridge. The corpus nowhere makes
the positive assertion "the complex is the record and decomposes nothing." PRIMARY here is
the residue of refusals, not a claim. That is why Q5 comes out null.

---

## 2. Q2 — WHAT IS ENTERED, EXACTLY?

The commission's dichotomy: entered = **"which complex"** (a stipulated decomposition) or
entered = **"there is a complex"** (a posit of the object).

### **ANSWER: "THERE IS A COMPLEX." The class is entered; the member is not.**

The decisive text is `STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_AND_EXTENSION_DETERMINATION_V001.md
:340-342`, quoted whole:

> "2. The general `BareRec_2` category permits any finite oriented regular CW
>    complex of dimension at most two.  A permissive object class does not select
>    a member."

**"A permissive object class does not select a member."** That is the corpus separating
exactly the two things this commission asked it to separate, in one sentence. What is
entered is the admissible class. Which member is not entered — because nothing selects one.

### 2.1 ITEMIZATION, each with its own provenance grade

| # | Entered item | Grade | What decides it |
|---|---|---|---|
| E-1 | **That there is a complex at all** (the object slot in `(K,r,L,U)`) | **ENTERED** — premise | `BID V011 :281-287`: *"An object of `BareRec_2` is"* `(K,r,L,U)`, *"where `K` is a finite oriented regular CW complex of dimension at most two"*. Declared, not derived. |
| E-2 | **Finiteness** | **ENTERED** — in the same `where` clause | `BID V011 :287`, the word "finite". No derivation of finiteness found; the corpus separately books `BID_FINITE_RECORD_DURABILITY_NO_GO_V001.md` as a no-go, i.e. finiteness is a premise with a *negative* consequence, not a result. |
| E-3 | **Dimension bound ≤ 2** | **ENTERED** — same clause | `BID V011 :287`. Its consequence is booked open, not derived: `any_physical_filled_two_cell_forced_in_bounded_roots = false \| TYPE-S` (CENSUS `:352`), i.e. the corpus cannot even force the *existence* of the degree-2 cell the bound admits. A bound that admits a cell nothing forces is a premise. |
| E-4 | **Orientation** | **ENTERED (definitional)** | `BID V011 :287-292`; graded by the prior commission at `STAGE8_BASE_GEOMETRY_O48SR_V001.md :732`: *"\| **Orientation of `K`; root `r`** \| **ENTERED (definitional)** \| `BID V011 :287-292`, quoted in Q3 §A \|"* |
| E-5 | **The root `r`** | **ENTERED (definitional)** | same row, `O48SR :732`. Note `BID V011 :288`: *"each connected component carries zero or one distinguished root `r`"* — zero-or-one, so even the root is not universally imposed. |
| E-6 | **Cell weights `M_p = I`** | **NOT ENTERED — HYPOTHESISED, and the source says so** | `BID V011 :502-505`, whole: *"in the elementary cell bases. This is a new physical hypothesis, not a result inherited from the action character. Gate 3 must classify coherent Hilbert functors starting from all positive-definite forms and prove whether one unitary equivalence class survives."* The corpus admits every positive-definite competitor at `:463-472` and leaves Gate 3 open. |
| E-7 | **A choice among admissible complexes ("which complex")** | **NOT ENTERED** | CENSUS `:341-342`: *"A permissive object class does not select a member."* And `K_L_forced_by_record_composition = false \| TYPE-R` (CENSUS `:355-356`) — the one concrete complex the corpus writes down is booked as unforced and is a test object. |

### 2.2 The one place a member IS fixed — and it is fixed by a premise, disclosed

`BID V011 :262-274`, quoted whole including the adverse paragraph:

> "This premise fixes the minimal first-opening `1`-complex to the rooted star
> `K_(1,r)` with `r=3`. Consequently,
>
> ```text
> dim C_0 = 4,
> dim C_1 = 3,
> dim(C_0 direct-sum C_1) = 7.
> ```
>
> Equivalently, the first-opening inventory contains three public endpoint
> vertices plus one unresolved root vertex. Those dimensions are a disclosed
> target-aware postulate consequence. They are not a new BID prediction, a
> holdout, or evidence for alpha."

This is the only site at which a specific complex is pinned, it is pinned at **dimension
one** (a `1`-complex, not the 2-complex under audit), and the corpus types the result as *"a
disclosed target-aware postulate consequence"* — a consequence of an entered premise, not an
entered choice of complex, and expressly not evidence.

### 2.3 Q2 VERDICT

```text
WHAT_IS_ENTERED = "THERE IS A COMPLEX" (the admissible class)
WHICH_COMPLEX_IS_ENTERED = false
  ground: CENSUS :341-342 "A permissive object class does not select a member."
STIPULATED_DECOMPOSITION_READING_OF_ENTERED = NOT SUPPORTED AT BYTES
POSIT_OF_THE_OBJECT_READING_OF_ENTERED = SUPPORTED AT BYTES
CELL_WEIGHTS = hypothesised, not entered; Gate 3 open (BID V011 :502-505)
```

Because "which complex" is not entered, **the entered thing is not a stipulated
decomposition.** This is an independent confirmation of Q1's net grade, reached from a
different text.

---

## 3. Q3 — THE FORMATION CONDITION, AUDITED FOR HIDDEN CHOICE

### 3.1 The gate, opened

`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` (sealed, OK). Its firing condition,
`:74-86`, quoted whole:

> "For orthogonal conditional global states,
>
> ```text
> <R_0|R_1>=0,
> ```
>
> so the analytic consequence is
>
> ```text
> J_FS,rel
>   := integral_cell d tau Delta H_W(tau)
>   >= pi hbar/2.
> ```
>
> This is an energy-uncertainty/path-length budget. It is not automatically
> either branch's dynamical action, the action difference, or a coefficient in
> the microscopic Lagrangian."

The moment solves an equation, `:115-116`: `epsilon_rel tau_*/hbar = pi/2`. Confirmed.

### 3.2 THE TENSOR SPLIT, EXAMINED DIRECTLY — as the commission requires

The commission reports the gate as acting on a Hilbert space written `source ⊗ record ⊗
environment`. **A quotation-integrity correction is owed first.**

That tensor notation is **not in the gate's bytes.** It appears in the prior commission's
type signature, `STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md :275`:

> "```text
> (U_0, U_1 : R -> U(H_S ⊗ H_R ⊗ H_E)) × |r> ∈ H  ->  overlap <r|W(tau_*)|r> ∈ C
> ```"

The gate's own text, `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md :21-29`, quoted
whole, uses no product symbol at all:

> "## Common closed dilation
>
> Let the source, designated record subsystem, and required environment form
> one closed Hilbert space. From one common ready state `|r>`, the two
> source-conditioned closed-dilation unitaries are
>
> ```text
> U_0(tau), U_1(tau).
> ```"

So the split at bytes is carried by three words — **"source"**, **"designated record
subsystem"**, **"required environment"** — joined by the imperative **"Let … form one closed
Hilbert space."** The `⊗` is O47SR's typing of that sentence. The finding below holds under
either reading, and is if anything sharper at bytes: a three-way split asserted in prose is
even less derived than one asserted in symbols.

### 3.3 GRADING EVERY SPLIT THE GATE'S DEFINITION INVOKES

| # | Split / factorization invoked | Grade | Ground |
|---|---|---|---|
| **X-1** | **source / record / environment**, the three-way carrier split | **CHOSEN** | `ONSET GATE V003 :23-24`: *"Let the source, designated record subsystem, and required environment form one closed Hilbert space."* The verb is **"Let"** — a stipulation. **"designated"** is the corpus's own word for the record factor, and *designated* is selection vocabulary. Nothing in `(K,r,L,U)` contains a source, an environment, or a subsystem boundary; `K` carries cells, incidence, orientation, a root and fibers, *"and nothing else"* (`O48SR :629`). **Sweep result: the strings "designated record subsystem" and "required environment" occur in the corpus at exactly one file — this gate — and nowhere is either defined, derived, or tied to a cell of `K`.** The gate's own status block books the completion as absent, `:164`: *"the complete source-record-environment action"* under **Not established**; and `:165` of the write gate books `complete_source_record_environment_operator_derived = false`. |
| **X-2** | the **`U(1)`-vertex-gauge split** on fibers | **CONSTITUTIVE** | `BID V011 :328-335` — gauge maps `h_v in U(1)` are indexed by the vertices **of `K`**; the split is the complex's own vertex set. |
| **X-3** | the **degree grading** `C_0 / C_1 / C_2` | **CONSTITUTIVE** | `BID V011 :309-315` — the carriers are direct sums **over `K_0`, `K_1^+`, `K_2^+`**, i.e. read off the complex's own cells. |
| **X-4** | `Q_Sigma tensor Y_R`, the **source ⊗ record** factorization in the write gate | **CHOSEN** | `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md :71`, `:83-86`. Its `Q_Sigma` is declared inherited, `:32-33`, whole: *"This is a local branch input, not a derivation of every current-flux spectrum."* And `:35-40` opens with the verb **"adopt"**. |
| **X-5** | the **preferred basis / axis** `Y_R` | **CHOSEN** | `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md :67-68`, whole: *"Endpoint rephasing rotates the equatorial pair `(v_x,v_y)`. **Choosing** `Y_R` as representative, the conditional integrated record-changing holonomy is"*. Its status block `:153` books `chosen_Y_representative_verified = true` — the corpus calls it chosen in its own token name. |
| **X-6** | the **reduced/partial-trace split** used for readout | **CHOSEN**, and booked underived | `ONSET GATE V003 :92-95`, whole: *"Global orthogonality is not yet a durable public record. Perfect readout from the designated record subsystem requires orthogonal supports of its reduced conditional states. Durability additionally requires persistence and recoverability or redundancy."* Status `:188`: `orthogonal_reduced_record_supports_derived = false`. |
| **X-7** | the **saturation point** on the derived admissible set | **CHOSEN**, and the corpus says so | `ONSET GATE V003 :97-108`, whole: *"## Adopted Gravacle onset rule // The allow/require boundary is adopted to select first admissible record onset through a shortest **relative** projective path on the unique physical record cell. Conditional on this target-value-free, target-aware Level-1 rule, // `J_FS,rel = pi hbar/2.` // The lower bound is derived from the imported theorem. Saturation is adopted, not derived. Historical target blindness is not claimed."* Status `:182-183`: `relative_onset_saturation_derived = false`, `relative_onset_saturation_adopted_Level_1 = true`. |
| **X-8** | a **fragment partition / redundancy family** (Darwinism-style) | **ABSENT** | Swept; the only adjacent text is `:95` *"recoverability or redundancy"* as an unmet requirement. No fragment family is defined. |

### 3.4 Q3 VERDICT ON THE SOURCE–RECORD–ENVIRONMENT SPLIT SPECIFICALLY

## **CHOSEN — stipulated alongside the complex, not derived from it.**

Three independent bytes-level grounds:

1. **The verb is imperative.** *"**Let** the source, designated record subsystem, and
   required environment form one closed Hilbert space."* (`:23-24`) A `Let` clause posits;
   it does not derive.
2. **The record factor is "designated".** The corpus's own adjective for the record
   subsystem is a selection word, and it repeats it at `:93` (*"the designated record
   subsystem"*).
3. **The complex cannot supply the split.** `K` is *"cells, faces, adjacency, ordering,
   orientation, one root"* and *"nothing else"* (`O48SR :629,:632-635`). There is no source
   cell, no environment cell, no subsystem boundary in `(K,r,L,U)`. A dedicated sweep for an
   environment carried by the complex returned **zero** hits.

**Adverse note carried forward, in the corpus's favour:** the gate does not hide this. It
books the completion as not established at `:164` and the write gate books
`complete_source_record_environment_operator_derived = false` at `:165`. The split is
stipulated *openly*. The finding is that it is stipulated, not that it is concealed.

**Consequence for Q1, stated and then bounded.** X-1 is a chosen split that the complex does
not supply. That is a *stipulation adjacent to* the complex — it does not make the complex a
decomposition of anything, because the thing being split (the closed Hilbert space) is
introduced by the same sentence and is not a prior object the complex is cut from. **The
hidden choice in the formation condition is real, and it is not a decomposition of the
record.**

---

## 4. Q4 — IS THE DYNAMICS UNMODIFIED?

### GRADE: **UNMODIFIED.** Every evolution object is unitary or an explicitly typed reduction.

| # | Evolution object | Grade | Defining text |
|---|---|---|---|
| **D-1** | `U_0(tau), U_1(tau)` — the source-conditioned closed-dilation unitaries | **UNITARY** | `ONSET GATE V003 :21-29`, whole, quoted at §3.2 above: *"Let the source, designated record subsystem, and required environment form one closed Hilbert space. From one common ready state `\|r>`, the two source-conditioned closed-dilation unitaries are `U_0(tau), U_1(tau)`."* A **closed dilation** is unitary by construction. |
| **D-2** | `W(tau) = U_0(tau)^dagger U_1(tau)` — the relative unitary | **UNITARY** | `ONSET GATE V003 :41-45`: *"`<R_0\|R_1> = <r\|W(tau_*)\|r>,` `W(tau) = U_0(tau)^dagger U_1(tau).`"* A product of a unitary adjoint and a unitary. |
| **D-3** | `H_W = i hbar [partial_tau W] W^dagger = U_0^dagger (H_1-H_0) U_0` | **UNITARY** (self-adjoint generator) | `ONSET GATE V003 :57-63`. A Hamiltonian generator; no imaginary part, no jump operator. |
| **D-4** | `U_0 = exp(+i pi Y/4)`, `U_1 = exp(-i pi Y/4)` — exact two-state realization | **UNITARY** | `ONSET GATE V003 :119-124`, whole: *"The exact symmetric branch representatives are `U_0 = (I+iY)/sqrt(2) = exp(+i pi Y/4), U_1 = (I-iY)/sqrt(2) = exp(-i pi Y/4).`"* Exponentials of `i` times self-adjoint. |
| **D-5** | `U_write = (I-Q_Sigma) tensor I_R + Q_Sigma tensor (-iY_R)` | **UNITARY** | `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md :80-86`, whole: *"The ready-subspace unitary is `U_write = (I-Q_Sigma) tensor I_R + Q_Sigma tensor (-iY_R).`"* Named "the ready-subspace **unitary**"; status `:155` books `candidate_write_preserves_declared_flux_sector = true`. |
| **D-6** | `U_chi` — the algebraic identity-phase family | **UNITARY** | `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md :93-99`. Same form with a `exp(-i chi)` phase; a phase does not break unitarity. |
| **D-7** | `U_e : L_s -> L_t`, the edge transports of the complex | **UNITARY** | `BID V011 :294-299`, whole: *"Each vertex `v` carries a one-dimensional Hermitian fiber `L_v`. Each oriented edge `e:s->t` carries a unitary transport `U_e:L_s->L_t,  U_(bar e)=U_e^(-1).`"* |
| **D-8** | The complete microscopic generator | **UNITARY — and the corpus states the rule** | `PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md :39-41`, quoted whole: *"The complete microscopic source-plus-record generator must be a unitary, phase-complete dilation. Open-system channels and durable record instruments are obtained only after tracing or conditioning downstream degrees."* |
| **D-9** | The record-comparator evolution `H_R = (pi hbar/tau_R) P_1` | **UNITARY — stated in terms** | `PRIMITIVE_BINARY_CLOSURE_GENERATOR_GATE_V001.md :80-82`, quoted whole: *"The fundamental evolution remains unitary. A dephasing or Lindblad rate is not a primitive coefficient; decoherence and durable amplification must be computed from downstream degrees under the transport-only principle."* |
| **D-10** | Reduced conditional record states / partial trace | **NON-UNITARY-AS-REDUCTION** | `ONSET GATE V003 :92-95`, whole, quoted at X-6 above: readout is defined through *"orthogonal supports of its **reduced** conditional states"*. Reduction of a closed unitary dilation. Corroborated `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md :103-107`, whole: *"In a fixed source basis, a coherent zero/one-flux superposition produces different joint source-record density matrices for different `chi`, while the reduced record density is unchanged. The audit verifies this using unnormalized density numerators of trace `2`; division by `2` gives the normalized density matrices and preserves both statements."* |
| **D-11** | Open-system channels / decoherence | **NON-UNITARY-AS-REDUCTION**, and expressly so | `PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md :40-41` (D-8's second sentence): *"Open-system channels and durable record instruments are obtained only after tracing or conditioning downstream degrees."* |
| **D-12** | Lindblad / dephasing rates | **EXCLUDED AS PRIMITIVE** | `PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md :43-58`, quoted whole: *"## Excluded primitive mutations // The following are not independent terms in the primitive generator: // `c[A] I` gauge-history-dependent scalar phases; `c_F integral F^2` a bare Maxwell action; `ell_* bar(psi) sigma.F psi` a primitive Pauli insertion; higher-covariant-derivative charged vertices; **independent Lindblad/dephasing rates**; an independently normalized record-curvature term. // Such structures may arise as calculated composite terms of the exact closed transport dynamics. They may not be inserted as microscopic inputs or chosen after inspecting a response."* |

### 4.1 SWEEP FOR MODIFIED DYNAMICS — all patterns, all counts

```text
pattern                                       hits (both roots, .proof_deps excluded)
GRW                                            0
"continuous spontaneous localization"          0
stochastic Schrodinger / Schrodinger stochastic 0
nonlinear Schrodinger / modified Schrodinger   0
"collapse term"                                0
"spontaneous collapse"                         0
"objective collapse"                           0
Lindblad                                       6  (3 files; ALL opened; ALL exclusionary)
non-unitary / nonunitary                      86  (reduction-typing, not fundamental)
CPTP / completely positive                   102  (channel language, downstream)
```

All six Lindblad lines were opened. Every one is an **exclusion**:
`PRIMITIVE_TRANSPORT_ONLY_… :52` lists it among *"Excluded primitive mutations"*;
`PRIMITIVE_BINARY_CLOSURE_… :80` says *"A dephasing or Lindblad rate is not a primitive
coefficient"*; `STAGE8_ROUTE4_NATIVE_FINITE_ALGEBRA_THEOREM_AUDIT_V001.md :198` reads
*"Lindblad/dephasing rates, and independent record-curvature terms may not be"* [added].

### 4.2 Q4 VERDICT

```text
UNITARY                      = D-1..D-9   (9 objects)
NON-UNITARY-AS-REDUCTION     = D-10..D-11 (2 objects)
MODIFIED-DYNAMICS            = 0 objects
INDETERMINATE                = 0 objects
MODIFIED_DYNAMICS_FOUND = false | TYPE-S | patterns and counts at §4.1
```

**Nothing in the formation machinery modifies ordinary unitary evolution.** The corpus is
not merely free of a collapse term — it has a *stated principle* excluding one
(`PRIMITIVE_TRANSPORT_ONLY_… :39-58`), and it books the exclusion as substantive at `:62-75`:
*"Gauge and Lorentz covariance alone do not imply this rule. The rule chooses a physical
hierarchy"*. That the exclusion is itself a **choice** ("The rule **chooses**") is recorded in
the CHOICE LEDGER as C-9; it is a choice *for* unmodified dynamics, not a modification.

---

## 5. Q5 — THE DISCRIMINATOR

### 5.1 THE FOUR SPECIFIED SHAPES, HUNTED

| Shape | Result |
|---|---|
| **(i)** a statement that the complex **approximates** something | **NONE.** Patterns `the complex approximat`, `K approximat`, `approximates the (continuum\|manifold\|smooth)` → **0** record hits. The one "approximation" hit in the corpus, `STAGE8_GAMMA_K_QUASIFREE_ROOT_ROUTE_ATTEMPT_V001.md :149`, runs the other way: *"This is not an invented finite-rank approximation. It is the continuum"* [clause continues]. |
| **(ii)** a **continuum limit** in which the complex is recovered | **NONE IN THAT DIRECTION.** Every continuum-limit hit is a limit *of* the discrete, or a booking that no limit was taken. See §5.2 — the near-miss. |
| **(iii)** a claim that a **different complex would describe the same record** | **ONE NEAR-MISS**, reported and refused in §5.3. |
| **(iv)** a claim the record has **no description finer** than the complex | **NONE.** Patterns `no description finer`, `finest (record\|complex)`, `nothing finer` → 1 hit, `STAGE8_TRANSPORT_LAW_POSED_V001.md :330`, opened; it concerns a case-analysis pattern, not the complex's fineness. |

### 5.2 NEAR-MISS A — DoR-007's "base-smoothness exists only as a refinement limit"

This is the strongest candidate in the corpus and it must be handled carefully, because on a
fast reading it looks decisive.

`DECISION_OF_RECORD_007_SMOOTH_FORK_DERIVE_THE_LIMIT_2026-08-01_V001.md :21-24`, quoted whole:

> "3. Grounding: the record surface is continuous fibers over a discrete base (OBS-12);
>    base-smoothness exists only as a refinement limit; the limit's cellulation-independence
>    is the theorem's content. Everything discrete-sufficient (comparison, transport,
>    holonomy, charge/flux access, onset, writes, formal face curvature, finite Ward)
>    proceeds without waiting."

**Why it looks like a discriminator.** *"base-smoothness exists **only** as a refinement
limit"* asserts the continuum is downstream of the discrete. Under DECOMPOSITION-OF the
continuum is prior and the complex is cut from it; this sentence says the reverse.

**Why it fails as one, on three counts.**

1. **It is a governance ruling, not a corpus theorem about the complex.** DoR-007's own head
   is a *decision*: `:8-10`, whole: *"The smooth-required subset (Q-199: the alpha-facing
   chain — public EM connection, local Maxwell tensor, continuum Ward/transversality, Thomson
   matching) is to be met by a DERIVED DISCRETE-TO-CONTINUUM EQUIVALENCE THEOREM — the
   stitching rule as a theorem over refinements."* **"is to be met by"** — the theorem is
   assigned, not held. `:19-20`: *"The DISCRETE-TO-CONTINUUM EQUIVALENCE THEOREM becomes a
   NAMED OBLIGATION"*. An obligation is not a statement with a truth value at bytes.
2. **Its own decisive word presupposes the other reading.** *"the limit's
   **cellulation-independence** is the theorem's content"*. Cellulation-independence is only
   a meaningful demand if there are alternative cellulations *of something* — the
   decomposition-of grammar. The sentence therefore contains both readings at once and
   discriminates neither.
3. **The subject is the "record surface"/"base", not `K`.** The corpus keeps these
   distinct — four bases, unidentified (`O48SR :620-623`), bridges open both directions
   (`:637`), and `CELLULATION_INDEPENDENCE_PROVED = false | TYPE-U` (`TASK4D :63`). A
   sentence about the base does not settle a question about BASE-3 while the bridge between
   them is booked open.

**Grade: NOT A DISCRIMINATOR.** It is an unmet obligation whose statement is
reading-ambiguous in its own vocabulary.

### 5.3 NEAR-MISS B — "the same physical presentation"

`BID_FIRST_OPENING_GRAPH_REFINEMENT_QUOTIENT_V001.md :71-72`, quoted whole:

> "Thus an unlabeled bivalent subdivision is the same physical presentation,
> while these competitors are enlarged record objects."

with its supporting definition `:21-28`, quoted whole:

> "An internal vertex is presentation-only when it is:
>
> ```text
> unlabeled;
> not durable;
> of directed indegree one and outdegree one;
> and carries no independent source, handle, or environment datum.
> ```"

**Why it looks like a discriminator.** Two *different* complexes — one subdivided, one not —
are called the same physical thing. Under a strict identity reading of PRIMARY (the complex
*is* the record), two distinct complexes should be two distinct records. So this looks like
it refutes PRIMARY and supports DECOMPOSITION-OF.

**Why it fails as one.** DECOMPOSITION-OF requires a **prior object that is cut**. Here
there is no prior object anywhere in the text: the equivalence class is constructed *from*
the complexes, by a quotient generated by moves between them (`:43-44`: *"Equivalence is
generated by these moves, edge reparametrization, and label-preserving graph
isomorphism."*). A quotient of complexes is downstream of complexes. The sentence is
therefore **true under both readings**: under DECOMPOSITION-OF, two decompositions of one
thing; under PRIMARY, two presentations in one equivalence class with the class as the
record. It does refute the *strictest* form of PRIMARY (bare identity of complex and
record), but the commission's PRIMARY is *"the complex **is** the record … its cells are
constitutive"*, which survives a presentation quotient intact.

**Grade: NOT A DISCRIMINATOR.** Its word "presentation-only" is suggestive and is recorded
in the FLAG BLOCK as the sharpest surviving tension, but it does not take opposite truth
values under the two readings.

### 5.4 THE PRIOR NEGATIVE — TWICE BOOKED, AND WHETHER IT GOVERNS HERE

The commission notes the corpus has twice booked a neighbouring binary as refuted for want
of a sealed operational definition. **Both bookings located, both opened, both trace to one
source.**

**The source.** `STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md` (sealed, OK),
`:71-80`, quoted whole:

> "```text
> P5_supplied_by_unambiguously_means_temporal_production = NO_VERDICT |
>   reason: no sealed operational definition selects temporal creation over
>   derivational/common-framework provision
>
> rank1_class_requires_source_action_to_create_record_carrier = NO_VERDICT |
>   reason: the definition says "derived from" a joint operator, while P3
>   preserves the antecedent carrier and no temporal carrier-generation map is
>   stated
> ```"

and its lead finding, quoted by O36SR at `:1069-1078` from that artifact's `:10-18`, whole:

> "The forced binary is refuted. The class is **GENERATIVE in relay 184's
>  derivational-provenance sense**: its package is `produced by` and `all
>  derived from` one joint operator. The theory is simultaneously
>  **FORMATION in physical order**: an antecedent carrier exists, then a
>  durable record forms only when the stated conditions succeed. One joint
>  source-record-field operator can derive state/effects/domains/dynamics on
>  a carrier that exists before a particular write. Therefore relay 184's
>  assertion that its GENERATIVE category `Contradicts formation` does not
>  survive the class-plus-Q-43 witness."

**Booking 1** — `STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md :1044-1060`, whose own gloss reads:
*"A corpus artifact posed exactly "does this word mean making, or does it mean constraining?"
and booked the answer NO_VERDICT for lack of a sealed operational definition.  THAT IS A
CORPUS-NATIVE STATEMENT OF INDETERMINACY AT THE SAME AXIS THIS COMMISSION IS ASKING ABOUT."*

**Booking 2** — `STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md :1782-1787`, quoted whole:

> "The prior commission found the two **not mutually exclusive** and booked
> `relay_GENERATIVE_and_FORMATION_are_mutually_exclusive = false | TYPE-R`. It then found
> that the *one* place the two readings could have been separated — whether `supplied by`
> means temporal production or derivational provision — could not be decided, **for exactly
> the reason this commission would need: `no sealed operational definition selects temporal
> creation over derivational/common-framework provision`.**"

O47SR's own Q5 landed in the same place, `:1805` and `:1825-1830`, quoted whole:

> "## **OBSERVATIONALLY-EQUIVALENT-AT-BYTES.**"

> "```text
> DISCRIMINATOR_FOUND = false
> Q5_VERDICT = OBSERVATIONALLY-EQUIVALENT-AT-BYTES
> PRIOR_NEGATIVE_GOVERNS = true  (with the three qualifications of 8.6)
> DISCRIMINATOR_MANUFACTURED = false
> ```"

**DOES IT GOVERN HERE? — YES, and by the same mechanism, with one qualification.**

The mechanism transfers exactly. In all three cases a single word carries the whole binary
and is never operationally sealed:

```text
prior 1:  "supplied by"   — temporal production   vs  derivational provision
prior 2:  "performs"      — operated              vs  spontaneous
present:  "entered"       — which complex (stipulated decomposition)
                          vs  there is a complex (posit of the object)
```

**The qualification, and it is a real one.** The present commission is **better off than its
two predecessors on Q2**, and worse off on Q5. Where the prior lanes had *no* text separating
their readings, this one has `CENSUS :341-342` — *"A permissive object class does not select
a member"* — which does decide what "entered" ranges over. That is why Q2 returns a
determinate answer where the prior commissions returned NO_VERDICT. What the present
commission lacks is the *further* text that would make PRIMARY and DECOMPOSITION-OF differ in
their consequences once "there is a complex" is fixed. **The prior negative governs the
consequence question, not the entry question.**

### 5.5 Q5 VERDICT

## **OBSERVATIONALLY-EQUIVALENT-AT-BYTES.**

```text
DISCRIMINATOR_FOUND = false
Q5_VERDICT = OBSERVATIONALLY-EQUIVALENT-AT-BYTES
NEAR_MISSES_REPORTED = 2   (DoR-007 :21-24; REFINEMENT QUOTIENT :71-72)
NEAR_MISSES_ACCEPTED = 0
PRIOR_NEGATIVE_GOVERNS = true, for the consequence question
PRIOR_NEGATIVE_GOVERNS_Q2 = false — CENSUS :341-342 decides the entry question
DISCRIMINATOR_MANUFACTURED = false
```

**What would decide it, stated so a future lane is not tempted to invent one:** a sealed
statement, about `K` in `BareRec_2` specifically and not about "the base" or "the record
surface", that either (a) names an object `K` is a decomposition of and exhibits the
decomposing map, or (b) asserts that no object stands behind `K`. Neither exists in either
root. The corpus's refusals (§1.2) get to (b)'s *neighbourhood* without ever arriving: they
say `K` is not identified with the decomposition-bearing spaces, which is weaker than saying
nothing stands behind it.

---

## 6. CHOICE LEDGER

Every place this audit could have gone another way, with what forced the call.

| # | The choice | Taken | Alternative | What forced it |
|---|---|---|---|---|
| C-1 | Which text counts as "the definition of the record's complex" | `BID V011 :281-292`, the `BareRec_2` object declaration | Treat `K_L` (`:826-835`) as the definition | `K_L` is introduced by the imperative "use" for a "filtration audit"; the census books `K_L_forced_by_record_composition = false \| TYPE-R`. The object declaration is where the category's objects are defined. |
| C-2 | Whether SITE 2 and SITE 3 make the grade DECOMPOSITION-OF outright | No — BOTH-AT-DIFFERENT-SITES, with the record's own complex PRIMARY | Grade the whole DECOMPOSITION-OF | `TASK4D :26` *"No sealed map identifies these three choice spaces"*; `O48SR` FL-1 *"The corpus does not identify these objects."* Grading the record's complex from a disowned test object would import an identification the corpus refuses. |
| C-3 | Whether to grade Q1 **PRIMARY** flat rather than BOTH-AT-DIFFERENT-SITES | BOTH-AT-DIFFERENT-SITES | PRIMARY | The commission's own grade menu offers BOTH-AT-DIFFERENT-SITES for exactly this shape, and two genuine decomposition-of sites exist at bytes. Suppressing them to report a cleaner PRIMARY would be advocacy. |
| C-4 | Whether the refinement structure counts against PRIMARY | No | Yes | The built law's source and target are both graphs (`REFINEMENT QUOTIENT :12-19,:30-41`); `J_ref`'s source is `Ref` (objects: *"finite oriented record complex"*) and target is an analysis class; both `Ref` and `J_ref` are labelled "unbuilt". |
| C-5 | Whether to treat the withheld §I.5 law as PRIMARY-compatible | No — graded **INDETERMINATE-AT-BYTES** | Read "structure OF the cell" as settling it | *"its explicit form is not displayed on the sealed ground"* (`BARE_SURFACE_I2 :36`). A law whose form is withheld has no readable source or target. |
| C-6 | Whether O47SR's `H_S ⊗ H_R ⊗ H_E` may be quoted as the gate's own text | No — corrected in §3.2 | Carry the commission's framing through | The gate's bytes at `:23-24` contain no product symbol. Quotation integrity outranks convenience, and the correction strengthens rather than weakens the finding. |
| C-7 | Whether the S/R/E split's being CHOSEN makes the complex a decomposition | No | Yes | The split's object (a closed Hilbert space) is introduced by the same sentence; it is not a prior object `K` is cut from. Reported as an adjacent stipulation. |
| C-8 | Whether `M_p = I` counts as "entered" for Q2 | No — **hypothesised**, listed separately as E-6 | Count it among the entered items | The source distinguishes them itself: *"This is a new physical hypothesis, not a result"* with Gate 3 open (`BID V011 :502-505`). |
| C-9 | Whether "The rule chooses a physical hierarchy" makes Q4 MODIFIED | No | Yes | The choice is *for* unitarity and *against* inserted Lindblad terms (`PRIMITIVE_TRANSPORT_ONLY_… :43-58,:62-75`). A choice to exclude a modification is not a modification. |
| C-10 | Whether either Q5 near-miss should be accepted | No, both refused | Accept DoR-007 :21-24 | §5.2 and §5.3. Accepting either would manufacture a discriminator the commission expressly barred. |
| C-11 | Whether `SURFACE_DEFINITION_OF_RECORD_V002/V003.md` may be used | No — **not opened as authority** | Open and cite | It sits in `supervision/` and is not among the 28 permitted rulings; the discipline says "Nothing else in that directory." It is **not** in either corpus root (verified by listing both). Declared here rather than silently used. |

---

## 7. IMPORT AUDIT

Every notion used that the corpus does not itself define, with the survival test.

| # | Import | Where it touched this audit | Survives without it? |
|---|---|---|---|
| I-1 | **CW-complex theory** (skeleta, attaching maps, regularity, cellular approximation) | **NOT USED AS A PREMISE.** The commission's specific hazard. I read `regular CW complex` as a **name the corpus applies**, and took no theorem from it. In particular I did **not** infer from "CW complex" that `K` must be a decomposition of a topological space, nor that a 2-complex has a geometric realization with any property. | **YES — the finding requires it.** Every Q1 ground is a quoted corpus sentence about what `K`'s tuple carries, not a fact about complexes. Had I imported the standard reading (a CW complex is a *filtration of a space*), Q1 would have flipped to DECOMPOSITION-OF **on an import**, which is precisely the error the commission named. |
| I-2 | **Tensor-product factorization** of a composite Hilbert space | §3.2, to interpret "form one closed Hilbert space" | **YES.** The Q3 grade rests on the verbs *"Let"* and *"designated"* and on `K`'s inventory — not on the algebra of `⊗`. Indeed §3.2 *removes* the tensor framing. |
| I-3 | **Unitarity / self-adjointness / dilation** | Q4 grading of D-1..D-9 | **YES.** The corpus states the grades in its own words: *"the two source-conditioned closed-dilation unitaries"*, *"The ready-subspace unitary"*, *"The fundamental evolution remains unitary."* |
| I-4 | **Partial trace / reduced state** | Q4 D-10, D-11 | **YES.** The corpus uses "reduced conditional states", "reduced record density", and "after tracing or conditioning downstream degrees" itself. |
| I-5 | **Fubini-Study / Mandelstam-Tamm** | Context for the onset bound | **YES, and the corpus flags it as an import.** `ONSET GATE V003 :67`: *"Import the standard Fubini-Study/Mandelstam-Tamm theorem"*, booked `relative_orthogonalization_bound_imported = true` at `:180`. Not load-bearing for any grade here. |
| I-6 | **PL topology / geometric realization** | §1 SITE 3, reading `r_G:\|G\| -> X_o` | **YES.** I used only that the corpus *calls* it a realization homeomorphism into a named carrier — the genitive is on the page. |
| I-7 | **Quotient/equivalence-class reasoning** | §5.3 | **PARTIALLY.** The refusal of near-miss B leans on "a quotient of complexes is downstream of complexes". This is a general notion. Without it, near-miss B would be graded INDETERMINATE rather than NOT-A-DISCRIMINATOR — **and Q5's verdict would not change**, since an indeterminate near-miss is still not a discriminator. |
| I-8 | **Decoherence / einselection / quantum Darwinism** | Q3 X-8 only, as a *search template* | **YES.** The search returned nothing; no einselection notion entered any grade. |

**Net:** no import is load-bearing for any grade. I-1 is the one that would have changed a
verdict, and it was refused.

---

## 8. SWEEP CUTOFFS

Exclusion globs held as an **ARRAY** and passed as `--exclude` per element:

```text
EXCL = ( '*REGISTER*' '*TRACKER*' 'THE_PLAN*' 'ROAD_REMAINING*' 'THE_HANDOFF*'
         'OBSERVATIONS_REGISTER*' '*DECISION_SHEET*' '*FINISH_B*'
         'STAGE8_COMPLEX_PRIMARY_O54SR_V001.md' )
```

The last element is the **self-exclusion** of this artifact.

| Pattern | Hits (files) | Leak counter | Disposition |
|---|---|---|---|
| `regular CW complex` | 53 | 0 | Deduplicated to 2 distinct contents; both opened |
| `new physical hypothesis` | 34 | 0 | Traced to `BID V011 :502`; opened |
| `cellulation\|triangulat\|discretiz\|coarse.grain` | 656 | 0 | Narrowed by genitive (§below) |
| `cellulation of` (genitive) | 16 lines | 0 | **ALL 16 OPENED** |
| `triangulation of` / `discretization of` / `discretizing the\|a` | **0** | 0 | Nil return is itself the finding |
| `the complex approximat\|K approximat\|approximates the (continuum\|manifold\|smooth)` | 1 | 0 | Opened; runs the other direction |
| `refinement\|coarsening` | 1123 | 0 | Narrowed to functor signatures |
| `J_ref :\|Ref_phys :\|refinement functor` | 30 lines | 0 | Opened the 4 carrying signatures |
| `GRW`, `continuous spontaneous localization`, `stochastic Schrod`, `nonlinear Schrod`, `collapse term`, `spontaneous collapse`, `objective collapse` | **0 each** | 0 | Q4 nil returns |
| `Lindblad` | 6 lines / 3 files | 0 | **ALL 6 OPENED**; all exclusionary |
| `non-unitary\|nonunitary` | 86 | 0 | Sampled; reduction-typing |
| `CPTP\|completely positive` | 102 | 0 | Sampled; downstream channels |
| `designated record subsystem\|required environment` | **1 file** | 0 | Opened whole (172→199 lines) |
| `operational definition` | 12 | 0 | All opened; the twice-booked material |
| `base-smoothness\|exists only as a refinement limit` | 12 | 0 | All opened |
| `no description finer\|finest (record\|complex)\|nothing finer` | 1 | 0 | Opened; not on point |

**Leak counter total across all patterns: 0.** No barred filename appeared in any result set.

### OPEN WHAT YOU COUNT — declared

**Opened whole:** `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` (199 lines),
`SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md` (172),
`BID_FIRST_OPENING_GRAPH_REFINEMENT_QUOTIENT_V001.md` (95),
`DECISION_OF_RECORD_007_…` (26), `DOR_020_CONTINUUM_PACKAGE_…` (45).

**Opened at the cited spans and re-verified line-by-line:** `BID V011` (:250-430, :455-575,
:818-840), `STAGE8_BASE_GEOMETRY_O48SR_V001.md` (:528-645, :715-740, :930-940),
`STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md` (:215-365, :1735-1845),
`STAGE8_TASK4D_…_V001.md` (:1-75), `STAGE8_TASK5_EQ6_Q408_…_V001.md` (:110-150, :218-275),
`STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_…_V001.md` (:1-40, :275-360),
`STAGE8_TASK5_JREF_…_V001.md` (:125-140, :240-262),
`PRIMITIVE_TRANSPORT_ONLY_…_V001.md` (:30-80), `PRIMITIVE_BINARY_CLOSURE_…_V001.md` (:55-111),
`STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md` (:1040-1075),
`STAGE8_COMMON_ORIGIN_…_TYPING_V001.md` (:62-85), `BARE_SURFACE_I2_DETERMINATION_V001.md`
(:36, :110-112).

**Counted but NOT opened, declared:** the 86 `non-unitary` and 102 `CPTP` hits were sampled,
not exhausted — they are corroborative only, and Q4's grade rests on D-1..D-12's own defining
text. The 1,123 `refinement|coarsening` hits were narrowed by signature pattern rather than
read exhaustively; the four files carrying an actual functor signature were opened.

**The 28 rulings.** Both prefixes enumerated and counted: `DOR_NNN_…` = **14**,
`DECISION_OF_RECORD_NNN_…` = **14**, total **28**. **`DECISION_OF_RECORD_012` verified
absent** by enumeration (present: 003, 004, 005, 006, 007, 008, 009, 010, 011, 013,
014×3, 015) — reported absent, and confirmed absent rather than assumed. Titles of all 28
read; **DoR-007 and DoR-020 opened whole** as the two bearing on continuum/refinement.

---

## 9. SEALS

`shasum -a 256 -c` run from each artifact's own directory. **Both sidecar conventions
probed** for every file before any unsealed call.

| Artifact | Convention found | Result |
|---|---|---|
| `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` | bare `<stem>.seal.sha256` | **OK** |
| `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md` | bare | **OK** |
| `PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md` | bare | **OK** |
| `STAGE8_BASE_GEOMETRY_O48SR_V001.md` | `<file>.md.seal.sha256` | **OK** |
| `STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md` | `.md.seal` | **OK** |
| `STAGE8_TASK4D_CELLULATION_INDEPENDENCE_OD3_VERDICT_INVARIANCE_THEOREM_V001.md` | `.md.seal` | **OK** |
| `STAGE8_TASK5_EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V001.md` | `.md.seal` | **OK** |
| `STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md` | `.md.seal` | **OK** |
| `STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md` | `.md.seal` | **OK** |
| `STAGE8_TASK5_JREF_AND_LOCAL_KERNEL_ESTIMATES_LANE2_V001.md` | `.md.seal` | **OK** |

## **SEALS: 10/10 OK.**

### 9.1 UNSEALED SOURCES — declared, not concealed

Three cited artifacts carry **no sidecar under either convention**, in either root:

```text
BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md          UNSEALED (both roots)
BID_FIRST_OPENING_GRAPH_REFINEMENT_QUOTIENT_V001.md    UNSEALED (both roots)
PRIMITIVE_BINARY_CLOSURE_GENERATOR_GATE_V001.md        UNSEALED (both roots)
```

`BARE_SURFACE_I2_DETERMINATION_V001.md` and
`STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_AND_EXTENSION_DETERMINATION_V001.md` were likewise
probed under both conventions and are unsealed.

**This matters most for `BID V011`, the primary definitional source of the whole question.**
Two independent integrity anchors were therefore established in its place:

1. **Content-addressed corroboration.** The corpus stores a copy at
   `workspace/mirrored_sources/corpus_members/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md`
   and at `…/rd22_run_033/evidence/aa7c6d49…--BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md`.
   Both hash to `aa7c6d4904706276514728819df20f48e8fdca0ff83f97ad5f1724c5f81f108a`, which
   **equals the filename hash of the evidence copy**. The evidence copy is self-verifying.
2. **Cross-root identity of the working copy.** The workspace and cleanroom top-level copies
   both hash to `20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48` — identical
   across the two roots.

**Two variants of V011 exist** (`20a3a17d…`, 2348 lines; `aa7c6d49…`, 2251 lines). They were
diffed. **The divergence begins at line 1953** and is confined to an A32 freeze/registry
section. **Every line this audit quotes from V011 is byte-identical in both variants and at
the same line number** — verified directly for `:287` and `:502`. The Q1/Q2 grounds are
therefore variant-independent.

---

## 10. FLAG BLOCK

| # | Flag | Content |
|---|---|---|
| **FL-1** | **THE CORPUS SEPARATES THE TWO READINGS ONLY BY REFUSAL.** Every ground for PRIMARY in §1 is a *negative*: `K` is not identified with the four-torus skeleton, not with the PL cellulations, not with the causal-diamond decompositions; *"No sealed map identifies these three choice spaces."* The corpus never asserts that nothing stands behind `K`. **PRIMARY here is a residue, not a claim** — and that is exactly why Q5 is null. This is the load-bearing structural fact of this audit. |
| **FL-2** | **THE DEFINITIONAL SOURCE OF THE ENTIRE QUESTION IS UNSEALED.** `BID V011` carries no sidecar under either convention in either root, and exists in **two variants**. The quoted spans are variant-identical and the evidence copy is content-addressed and self-verifying (§9.1), so no finding is at risk — but the object whose provenance this commission audits rests on an unsealed file. |
| **FL-3** | **THE CENTRAL REFINEMENT/COARSENING LAW HAS ITS FORM WITHHELD.** *"named as exact and derived; its explicit form is not displayed on the sealed ground"* (`BARE_SURFACE_I2 :36`). The commission asked which refinement the corpus has, quoting sources and targets. For the law the corpus calls its own, **the sources and targets cannot be read at bytes.** Graded INDETERMINATE-AT-BYTES rather than assimilated to either reading. |
| **FL-4** | **THE FORMATION GATE'S THREE-WAY SPLIT IS DEFINED IN ONE SENTENCE, IN ONE FILE, AND NOWHERE ELSE.** The strings *"designated record subsystem"* and *"required environment"* occur at exactly one location in both roots — `ONSET GATE V003 :23`, `:93`. There is no second definition, no derivation, and no tie to any cell of `K`. A load-bearing factorization with a single unelaborated occurrence. |
| **FL-5** | **"PRESENTATION-ONLY" IS THE SHARPEST SURVIVING TENSION.** `REFINEMENT QUOTIENT :21` types an internal vertex as *"presentation-only"*, and `:71` calls a subdivision *"the same physical presentation"*. If the complex were the record without remainder there would be no presentation/record gap to name. This does not discriminate the two readings (§5.3) but it is the one place the corpus's own vocabulary admits that the complex carries structure the record does not. |
| **FL-6** | **Q4 IS THE CLEANEST RESULT IN THIS AUDIT.** Zero hits on seven modified-dynamics patterns; all six Lindblad lines exclusionary; a stated principle requiring unitarity (`PRIMITIVE_TRANSPORT_ONLY_… :39-41`) and a stated exclusion list (`:43-58`). No grade was INDETERMINATE. |
| **FL-7** | **THE PRIOR NEGATIVE GOVERNS ASYMMETRICALLY.** Unlike its two predecessors, this commission's *entry* question (Q2) **is** decided at bytes, by `CENSUS :341-342`. Only the *consequence* question (Q5) falls to the prior negative. A future lane should not read O36SR/O47SR's precedent as making this whole commission indeterminate — Q1, Q2, Q3 and Q4 all returned determinate grades. |
| **FL-8** | **SCOPE DECLARATION.** `SURFACE_DEFINITION_OF_RECORD_V002.md` / `V003.md` sit in `supervision/`, are **not** among the 28 permitted rulings, and are **not** present in either corpus root (both listed and verified). They appear to bear directly on this question (`V003 :28` matched the base-smoothness pattern). **They were not opened as authority.** Declared so the next lane knows what was left closed and why. |

---

## 11. GATES

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
authored = false
advocated = false
adopted = false
discriminator_manufactured = false
imports_load_bearing = false
```
