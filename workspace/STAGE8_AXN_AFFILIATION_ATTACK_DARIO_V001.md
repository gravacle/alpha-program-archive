# STAGE 8 — THE AFFILIATION ATTACK: DOES THE RESOLVENT ROUTE CLOSE ITEM 14?
## DARIO LANE (Builder B) — RELAY 884 — [PLAN:AXN-BUILD-B12]

```text
alpha_computed         = false
proof_authorized       = false
kappa_record_computed  = false
```

All charter fences live. No smooth import; no numeric evaluation of any physical quantity; no member
binding; no fixed-point execution; no end test; no comparison to measured constants; no common cell
formed; no junction map evaluated. PE-1..PE-13 pointer-only, none opened or consulted. Builder-A code
not opened. `~/.codex` untouched; memory-bank never searched. No register, plan, tracker, git action.

CLAIM STATUS: **all headline items CLAIMED.**

Subjects seal-verified BEFORE reading: my 875 `0eeb2a7db620f6c9`; 877 `d184ca49…` (located by digest:
`STAGE8_AXN_BUILD_INFINITE_INCIDENCE_CROSSCHECK_CODEX2_V001.md`); the scaling bundle at its **law-8
group seal**; the quasi-local span at its pin.

**DISCLOSED.** This attacks the gap **I** narrowed, along the route **I** suggested, and the relay
predicts the shape of its own answer. **That is the strongest scaffolding I have been handed.** The
guard I set at pickup was to **check the index sets before applying the resolvent criterion, not
after** — and that check is what decided the relay.

---

## 0. LEAD

**`ITEM14 = NARROWED` — the resolvent route does not close it, and it fails for a reason more basic
than a missing membership.**

```text
THE SEALED STRONG-RESOLVENT STATEMENT IS OVER LATTICE SPACING  a -> 0 .
alpha_infinity IS OVER RECORD FACTORS  A -> A (x) I .
THESE ARE DIFFERENT DIRECTED SYSTEMS.
```

**Verified at bytes, and the asymmetry is total:** the scaling result opens *"For lattice spacing
`a`"* and contains **zero** occurrences of `record factor`; the parent SPEC's quasi-local section is
indexed by *"completed record factor `R_j`"* and contains **one** occurrence of `lattice`.

**So the sealed convergence does not speak about membership in the record-factor limit algebra at
all.** Applying the resolvent criterion across the two systems would manufacture an affiliation out of
notation — precisely the failure I named at pickup and the same class as the glyph collision I
confirmed one relay ago at 883.

| stop | fires? |
|---|---|
| `AFFILIATED` | **only at the finite anchor**, where it is trivial (§1) |
| `RESOLVENT-GAP` | **FIRES** — membership unsupplied, and the sealed convergence is in the wrong directed system (§2) |
| `TOPOLOGY-GAP` | **FIRES, and for a sharper reason than "no completion exists"** (§2.3) |
| `SHARED-WITH-S4` | **FIRES at the generic case**, with 877's correction carried (§3) |

**AND I CONCEDE A CORRECTION TO MY OWN 875.** I wrote there that the infinite-incidence gap and the S4
gap *"meet on one missing object; they are not two independent debts."* **877 sharpens that to
`SAME-FAMILY-DISTINCT-RECEIVERS`, and 877 is right.** Its receiver test shows neither closes the
other; my phrasing over-merged them.

---

## 1. THE FINITE ANCHOR — displayed, and trivial for the right reason

At finite incidence `N_p`, `D_p` is a **finite** operator — at the sealed anchor, dimension 108 —
hence **bounded**. Therefore:

```text
D_p^2  IS AN ELEMENT of A_{N_p} .     Affiliation is trivial: a bounded operator in a matrix
                                       algebra needs no resolvent criterion at all.
```

Under the inclusions, its image is `D_p² ⊗ I ∈ A_{N_p+1}`, and it is **grading-compatible** by the
identity I verified at 875:

```text
alpha_{N+1}( D_p^2 (x) I ) = alpha_N( D_p^2 ) (x) I        residual 0.000e+00 at N = 1,2,3
```

**The finite anchor is affiliated and compatible — and it is trivial precisely because boundedness
makes the resolvent question empty.** That is worth stating: **the anchor supplies no evidence for the
unbounded case**, because the only thing it tests is a criterion that unboundedness alone makes
non-trivial. `FINITE = displayed.`

---

## 2. THE FREE-TAIL CASE — the route does not close

### 2.1 The index-set mismatch — the decisive finding

The sealed free-tail material is the scaling result's continuum limit:

```text
For lattice spacing a :   d_a,j(k) = [exp(i a k_j) - 1]/a ,   h_a(k) -> h(k)
||(h_a - z)^{-1}|| <= 1/|Im z|  +  pointwise convergence  =>  STRONG RESOLVENT CONVERGENCE
```

**Its directed system is lattice spacing `a`.** My `α_∞` is the limit of `α_N = Ad_{Γ_N}` along
`A_N → A_N ⊗ I`, **indexed by record factors.**

**A convergence statement in the `a`-system is not a membership statement in the `N`-system's limit
algebra.** No sealed sentence relates the two systems — and the byte evidence is that they do not even
share vocabulary (`record factor` = 0 in the scaling result).

**This is not a missing lemma. It is a not-yet-posed question**: the resolvent criterion needs the
resolvent to be a limit *of elements of the algebra one is affiliating to*, and the sealed limit is
taken elsewhere.

### 2.2 `RESOLVENT-GAP` — the membership, named

Even granting a common system, the required membership is:

```text
(h - z)^{-1}  in  A_infinity        for  Im z != 0
```

**Unsupplied.** Law 9b, **closure declared first**: `C_AFF :=` {the scaling bundle's seven sealed
members; the parent SPEC's quasi-local span and its fifteen pinned authorities; my 875; 877's `C_ALG`
and `C_S4`}. *Within that closure*, **no member asserts resolvent membership in the record-factor
limit algebra**, and none supplies a map carrying `a`-system resolvents into the `N`-system.

### 2.3 `TOPOLOGY-GAP` — and I tested my own prior framing rather than inheriting it

At pickup I said I must **not** simply inherit "the completion is unsealed" from my own 875 framing.
Testing it:

- **A C\*-completion is available and needs no state.** The inclusions `A_N → A_N ⊗ I` are isometric
  `*`-homomorphisms, so the algebraic inductive limit carries a natural C\*-norm and completes without
  any state. **So "no completion exists" would be the wrong stop, and I do not report it.**
- **But the sealed convergence is STRONG, not norm.** A strong limit of elements of `∪A_N` need not
  lie in the **norm** closure. To make strong convergence a closure operation one needs a
  **representation** — and the representation needs the **quasi-local outgoing record state**, which
  the parent SPEC lists among the items *"the gate must prove"* (item 3), **an obligation, not an
  established fact**.

**So `TOPOLOGY-GAP` fires in a sharpened form:** *the topology in which the sealed convergence holds
is not the topology in which the algebra is closed, and the topology that would reconcile them
requires an unsealed state.* **That is a stronger and more useful statement than "the completion is
unsealed" — which is false.**

`FREE_TAIL = RESOLVENT-GAP and TOPOLOGY-GAP.` **Not `AFFILIATED-AT-FREE-SCOPE`.**

---

## 3. THE GENERIC CASE — `SHARED-WITH-S4`, with 877's correction carried

Beyond the free tail, a generic member's affiliation additionally requires the **write/defect part's
resolvent behaviour**. That is exactly S4's open physical half: a compact write-region defect can
produce bound modes or point spectrum even when the free measure is absolutely continuous — the
obstruction I confirmed at 869 and which `complete_write_defect_spectrum_closed = false` seals.

**So the route lands on S4's physical half, and I say so, as the relay directs.** `S4_LINK = EXACT`,
receiver named: **the write/defect resolvent behaviour**, at S4's `named unbounded operators and
common invariant closure domain` receiver.

**But the merger is exactly as 877 types it, not as I typed it at 875.** 877's receiver test shows: no
S4 source names `D_p²` as its endpoint/write/response operator (zero occurrences); `R_INF` does not
name the complete endpoint/write/response/inverse/Schur family; **neither implies the other**; and
shared enabling material exists. **`SAME-FAMILY-DISTINCT-RECEIVERS`, and the frontier does not
contract.**

**My 875 said the two gaps "meet on one missing object." That over-merged them, and I withdraw it.**
The correct statement is that they draw on the same enabling family — unbounded operator realization,
domain, provenance, same-parent descent — while remaining distinct receivers, so **closing S4 would
not by itself close item 14.**

---

## 4. WHAT ITEM 14 NOW IS

```text
875 :  "no infinite/incomplete TENSOR construction, so Gamma_p is undefined at infinite incidence"
       -> narrowed to: AFFILIATION of a generic D_p^2 with the graded quasi-local limit algebra

884 :  affiliation additionally requires, and the record supplies neither:
       (i)  A RELATION BETWEEN TWO DIRECTED SYSTEMS -- the lattice-spacing system in which the
            sealed convergence is stated, and the record-factor system whose limit algebra is
            the affiliation target; and
       (ii) A REPRESENTATION making strong convergence a closure operation, which needs the
            quasi-local state that the gate lists as an obligation.
       The generic case then adds the write/defect resolvent behaviour = S4's physical half.
```

`ITEM14 = NARROWED (to the two-system relation plus the representation, with the generic case landing
on S4's physical half).` **UNMOVED as a discharge; sharper as a debt.**

---

## 5. FREEDOMS-CONSUMED (law 2, law 2a)

```text
CARRIED UNCHANGED: the scaling result's FREE and FLAT qualifiers travelling on every use; its
  strong-resolvent statement AT ITS OWN INDEX SET; the parent SPEC's gate items 1-7 AS OBLIGATIONS,
  including item 3 (existence of the quasi-local outgoing record state); my 875's alpha_infinity as an
  AUTOMORPHISM on the ALGEBRAIC limit with no spatial Gamma_infinity and no state consumed; S4's
  physical half OPEN; 877's SAME-FAMILY-DISTINCT-RECEIVERS typing ADOPTED OVER MY OWN 875 PHRASING.

DERIVED HERE: (a) the index-set mismatch, verified at bytes by vocabulary asymmetry (record factor = 0
  in the scaling result; lattice = 1 in the quasi-local SPEC); (b) the finite anchor's triviality and
  WHY it supplies no evidence for the unbounded case; (c) the correction to my own pickup framing —
  a C*-completion IS available without a state, so "no completion exists" is the WRONG stop; (d) the
  sharpened TOPOLOGY-GAP: strong-vs-norm, with the reconciling topology needing an unsealed state;
  (e) the two-part restatement of item 14.

SELECTED HERE: NOTHING.  No affiliation, resolvent membership, topology, representation, state,
  completion, domain, operator, or system-relating map is constructed, chosen, or assumed.  NO FLAG
  MOVES.  Item 14 is not closed and the frontier does not contract.

NOT DONE AND DISCLOSED: I did not attempt to relate the two directed systems — that is the named
  missing object and constructing it would exceed a bounded attack.  My C*-completion observation is a
  standard structural fact about isometric directed systems, used only NEGATIVELY (to reject a wrong
  stop), not to build anything.  I did not re-verify 877's receiver sweep counts; I adopted its typing
  because its test is displayed and its correction runs AGAINST my own prior phrasing, which is the
  direction that needs least suspicion.
```

**FLATTENING CHECK — 37/37 walked, clean.**
**S03 and THE VOID CONDITION — live at §2.1, which is where this relay could have gone wrong
invisibly.** The available cheat was to take a sealed *"strong resolvent convergence"* and a sealed
*"quasi-local limit algebra"* and let the resolvent criterion bridge them. **They are limits over
different index sets, and nothing sealed relates them.** No bridge is drawn.
**S12** — the gate's items carried as **obligations**; `NO_VERDICT`-class statuses never converted to
gaps-awaiting-work.
**S26 / S08 / S19 / S24** untouched — no completion, state, topology, or regularity class imported;
the free/flat qualifiers travel.
**T1 / T5** untouched.
**BR-1 HELD IN BOTH DIRECTIONS.** 877 got no exculpatory weight — but its correction runs **against**
my own prior artifact, which is the direction a producer-declared object may lawfully push. **And my
own 875 got no confirming weight toward its own route**: the route it suggested is the one this relay
found does not close.

---

## 6. FINAL LINES

```text
FINITE = displayed.  At finite incidence D_p is a FINITE operator (dimension 108 at the sealed anchor)
  hence BOUNDED, so D_p^2 IS AN ELEMENT of A_{N_p} and affiliation is TRIVIAL — a bounded operator in
  a matrix algebra needs no resolvent criterion.  Its image under the inclusions is D_p^2 (x) I, and it
  is grading-compatible by the identity I verified at 875, alpha_{N+1}(D_p^2 (x) I) = alpha_N(D_p^2)
  (x) I at residual 0.000e+00 for N = 1,2,3.  STATED BECAUSE IT MATTERS: THE ANCHOR SUPPLIES NO
  EVIDENCE FOR THE UNBOUNDED CASE, since the only thing it tests is a criterion that unboundedness
  alone makes non-trivial.
FREE_TAIL = RESOLVENT-GAP and TOPOLOGY-GAP.  NOT AFFILIATED-AT-FREE-SCOPE.  THE DECISIVE FINDING IS AN
  INDEX-SET MISMATCH, checked BEFORE the criterion was applied rather than after: the sealed
  strong-resolvent statement is over LATTICE SPACING a -> 0, while alpha_infinity is over RECORD
  FACTORS A -> A (x) I.  VERIFIED AT BYTES BY A TOTAL VOCABULARY ASYMMETRY — the scaling result opens
  "For lattice spacing `a`" and contains ZERO occurrences of "record factor"; the parent SPEC's
  quasi-local section is indexed by "completed record factor R_j" and contains ONE occurrence of
  "lattice".  A CONVERGENCE STATEMENT IN ONE DIRECTED SYSTEM IS NOT A MEMBERSHIP STATEMENT IN THE
  OTHER'S LIMIT ALGEBRA; this is NOT A MISSING LEMMA BUT A NOT-YET-POSED QUESTION, and bridging them
  would MANUFACTURE AN AFFILIATION OUT OF NOTATION — the same class as the glyph collision I confirmed
  at 883.  RESOLVENT-GAP: the required membership (h - z)^{-1} in A_infinity is UNSUPPLIED; law 9b
  closure C_AFF declared first, and within it no member asserts resolvent membership in the
  record-factor limit algebra nor supplies a map carrying a-system resolvents into the N-system.
  TOPOLOGY-GAP, IN A SHARPENED FORM, AND I TESTED MY OWN PRIOR FRAMING RATHER THAN INHERITING IT: a
  C*-COMPLETION IS AVAILABLE AND NEEDS NO STATE, since the inclusions are isometric *-homomorphisms —
  SO "NO COMPLETION EXISTS" IS THE WRONG STOP AND I DO NOT REPORT IT.  The real gap is that the sealed
  convergence is STRONG, NOT NORM: a strong limit of elements of the union need not lie in the NORM
  closure, and making strong convergence a closure operation needs a REPRESENTATION, which needs the
  QUASI-LOCAL OUTGOING RECORD STATE — item 3 of what the gate "MUST PROVE", an OBLIGATION, not an
  established fact.
GENERIC = SHARED-WITH-S4 (requirements displayed).  Beyond the free tail, a generic member's
  affiliation requires the WRITE/DEFECT PART'S RESOLVENT BEHAVIOUR — exactly S4's open physical half,
  where a compact write-region defect can produce bound modes or point spectrum even when the free
  measure is absolutely continuous (the obstruction I confirmed at 869, sealed by
  complete_write_defect_spectrum_closed = false).  RECEIVER NAMED: the write/defect resolvent
  behaviour, at S4's "named unbounded operators and common invariant closure domain".
S4_LINK = EXACT (receiver named) — BUT NOT A MERGER, AND HERE I CONCEDE A CORRECTION TO MY OWN 875.
  I wrote there that the infinite-incidence gap and the S4 gap "MEET ON ONE MISSING OBJECT; they are
  not two independent debts."  877 SHARPENS THAT TO SAME-FAMILY-DISTINCT-RECEIVERS AND 877 IS RIGHT:
  no S4 source names D_p^2 as its endpoint/write/response operator (zero occurrences); R_INF does not
  name the complete endpoint/write/response/inverse/Schur family; NEITHER IMPLIES THE OTHER; shared
  enabling material exists.  MY PHRASING OVER-MERGED THEM AND I WITHDRAW IT — the correct statement is
  that they draw on the same enabling family while remaining DISTINCT RECEIVERS, so CLOSING S4 WOULD
  NOT BY ITSELF CLOSE ITEM 14, AND THE FRONTIER DOES NOT CONTRACT.
ITEM14 = NARROWED (to the two-system relation plus the representation; generic case landing on S4's
  physical half).  UNMOVED AS A DISCHARGE, SHARPER AS A DEBT.  875 moved it from "no infinite tensor
  construction" to "affiliation"; 884 shows affiliation additionally requires (i) A RELATION BETWEEN
  TWO DIRECTED SYSTEMS — the lattice-spacing system in which the sealed convergence is stated and the
  record-factor system whose limit algebra is the target — and (ii) A REPRESENTATION making strong
  convergence a closure operation, which needs the unsealed quasi-local state.  The record supplies
  NEITHER, and I construct NEITHER.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+3):
  (1) THIS RELAY HANDED ME MY OWN GAP, MY OWN ROUTE, AND A PREDICTION OF ITS OWN ANSWER — the most
      scaffolded task I have been given — AND THE ANSWER IS THAT THE ROUTE DOES NOT CLOSE.  The single
      guard that produced that was set at pickup and honoured: CHECK THE INDEX SETS BEFORE APPLYING
      THE CRITERION, NOT AFTER.  Had I applied the resolvent criterion first and looked for
      obstructions second, the vocabulary asymmetry would have read as a technicality rather than as
      the finding.
  (2) I CORRECTED MY OWN PICKUP FRAMING MID-ARTIFACT, AGAINST MY OWN CONVENIENCE.  I inherited from
      875 the sense that the completion is unsealed; testing it showed a C*-completion needs no state,
      so "no completion exists" is FALSE and TOPOLOGY-GAP had to be re-argued on strong-vs-norm
      instead.  Reporting the wrong stop would have been easier and would have looked identical to a
      reader.
  (3) I WITHDRAW A SENTENCE FROM MY OWN 875 ON THE OPPOSITE LANE'S CORRECTION.  "Meet on one missing
      object" over-merged two receivers, and 877's test is displayed while my phrasing was an
      impression.  I note that 877's correction runs AGAINST my prior artifact, which is the direction
      a producer-declared object may lawfully push — so adopting it needs less suspicion than adopting
      a correction that flattered me would.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

---

**GATES HELD.** All charter fences live; nothing selected; no smooth import; no EM identification; no
member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities;
no comparison to measured constants; no common cell formed; no junction map evaluated. PE-1..PE-13
pointer-only, none opened or consulted. Builder-B independence held. `~/.codex` untouched;
memory-bank never searched. No register, plan, tracker, or git action.
