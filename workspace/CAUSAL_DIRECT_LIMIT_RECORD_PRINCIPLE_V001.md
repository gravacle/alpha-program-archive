# Causal Direct-Limit Record Principle v001

Date: 2026-07-24

## Provenance

This is the append-only promoted successor to
`CAUSAL_DIRECT_LIMIT_RECORD_HYPOTHESIS_V001.md`. Its scope and promotion are
established by:

```text
FORK_8_CAUSAL_DIRECT_LIMIT_PROMOTION_RESULT_V001.md.
```

It adds no coupling, normalization, or numerical selector.

## Principle

In the ordinary `3+1` flat-asymptotic branch, a durable public record is the
compatible outgoing sector of a future-directed Lorentz-covariant causal
exhaustion governed by the same finite source-record parent.

The physical content has three inseparable parts:

1. the finite parent performs the local reversible write;
2. its absolutely-continuous source tail gives thresholded source-root
   nonreturn for every admitted finite-energy `L2` excitation; and
3. one-use causal support leaves completed record factors exactly invariant,
   producing a compatible quasi-local output state, its GNS
   representation, and recoverable central record sequences.

Finite half-line, tree, and spatial-lattice models are regulators or
reductions. An effective environment is downstream. None supplies an
independent damping law or spectral density.

## Falsifiers

The principle fails in its declared branch if:

```text
the Lorentz-covariant continuum class is not the physical architecture;
the intrinsic primitive-cell measure is not the classified uniform measure;
the parent requires a separately selected outgoing state or tail;
an admitted finite-energy source root has a point component or fails
thresholded nonreturn;
a later primitive cell changes a completed record restriction;
the outgoing record states fail inductive compatibility;
the public labels cannot be separated by recoverable central sequences;
or two cofinal physical exhaustions give different stabilized local record
states or dressed maps.
```

## Scope

```text
ordinary_3_plus_1_flat_asymptotic_branch = true
completed_record_quasilocal_sector = true
finite_energy_L2_source_tail = true

global_source_inclusive_state_limit_derived = false
global_infinite_future_source_Moller_unitary_derived = false
curved_nonstationary_extension_derived = false
absolute_charged_response_normalization_derived = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
