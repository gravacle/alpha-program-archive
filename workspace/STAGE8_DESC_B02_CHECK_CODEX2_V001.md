CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 14 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
CLEANROOM_ROOT = /Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003
ARCHIVE_ROOT = /Users/bgm/MB Work/alpha-program-archive
PATH_RULE = cleanroom paths resolve from CLEANROOM_ROOT; archive paths resolve from ARCHIVE_ROOT
LANE = CODEX 2
ROLE = OPPOSITE-LANE B02 CHECK AGAINST THE PRE-BUILD FROZEN EXPECTATION
DIGEST_RULE = digests only; no process certificate
CLOSURE_END_BYTE = 00003511
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | cleanroom `relay_inbox/RELAY_PASTE_1010_B02_CHECK_CODEX2_V001.md` | `519274a614d1f0ac7c33797dd3db7b75386604b5ad61336ee4afb5641213e6ef` | assignment |
| 02 | cleanroom `STAGE8_DESC_B02_DARIO_V001.md` | `80fc23ed9687b4ccde1a51e4455e0c1878837c535cd4c5594e839c43f0743d1a` | build under check |
| 03 | cleanroom `build_b02_pins_v001.py` | `7daa1b841d5aa32ad1675b732b1a7de3f7664c297581adda8a3d1bf50b3fa345` | build pin generator |
| 04 | cleanroom `STAGE8_DESC_B02_EXPECTATION_CODEX2_V001.md` | `45ae77bd7a162783535a08c14312613247604dad02fb77571ed5de81db0971c3` | frozen expectation |
| 05 | archive `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | family-level ratification and no-member clause |
| 06 | archive `workspace/STAGE8_DESC_AUDIT_CODEX2_V002.md` | `078d6d54a2590f1caed4cea0245508981bd98e07adcef7f5b5df62d8419f838d` | B02 row, stop rule, classification |
| 07 | archive `workspace/STAGE8_DESC_DEMAND_DARIO_V006.md` | `33d8c0d4abf16b2ba3dd75ff77776a27b1bb82968aa24e753ca4ddb163932aba` | conditions 19–23 and process bars |
| 08 | archive `workspace/STAGE8_DESC_B01_DARIO_V002.md` | `19bf16f7ec1dda3e4c039e57b99a2896db4e9074ff1904e0e52096e2e90cf01b` | 32-row comparison inventory |
| 09 | archive `workspace/STAGE8_AXN_B0_ACCEPTANCE_INVERSE_CODEX2_V001.md` | `ae93720f5f5534a8d0d9915ab84368639f3fd96927b0d51a206a35c6b40b6019` | A01–A05 |
| 10 | archive `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md` | `ec848d293d0d824150c21b252452ae46732705cf972c857c2dc02671af53878c` | candidate/signature schema and T1 |
| 11 | archive `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | ratified-by-digest family bytes and displayed tuple |
| 12 | archive `workspace/STAGE8_AXN_ORIGIN_PRODUCER_CODEX2_V001.md` | `4574b5a1339acd317030df9de2bd9da3edf766dd5f829dd8b966f9d8599c77ab` | joint-object/codomain audit |
| 13 | archive `workspace/STAGE8_AXN_BUILD_B0_ROOT_CENSUS_CODEX2_V001.md` | `a614a69409b40ab839e036d9439ac0591df64854d4275d721a624454b7213141` | candidate/signature census |
| 14 | archive `workspace/STAGE8_TASK2D_MULTIAXIAL_STATE_TRANSITION_ENVELOPE_SPECIFICATION_V001.md` | `bda00e99e964bd75d60429549e5f6c70762bf1607270b9a6294dc4c97cf2c635` | PathCert schema and uninstantiated join |

```text
BLIND HELD. THE BUILD IS COMPARED ONLY TO THE ALREADY-SEALED EXPECTATION.
EVERY SCALE SYMBOLIC. NO NUMERIC EVALUATION OF PHYSICAL QUANTITIES.
NO MEMBER BOUND. NO SELECTOR EXERCISED. omega_phys UNTOUCHED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESCENT B02 — OPPOSITE-LANE CHECK — CODEX 2 — V001

Date: 2026-08-11  
Status: **CORRECTED — three stops lawful; `Params_B0` and its Q-914 consequence corrected.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. Generator custody and pin replay

Static inspection of the sealed generator establishes:

```text
FILES entries                         = 17
B02 closure members 01–17             = 17
ordered (member-id,path) equality      = true
filesystem-reading call sites         = 1
call                                  = open(path,"rb") at line 128
directory walks / globs / env / network = none
```

The generator therefore opens exactly closure members 01–17 and nothing else. It was rerun from the
archive root and returned zero with all 25 span locators resolved. The live register at member 05 has
advanced since Dario's seal, so that one current full-file digest differs under the standing
live-append tolerance; no cited span is taken from member 05. This is not an undeclared input.

The five spans required by this relay were also rehashed independently of the generator:

| Ground | Span | Bytes | Recomputed SHA-256 | Result |
|---|---:|---:|---|---|
| DoR-013 | `[355,992)` | 637 | `bc83e53db7e46f27b570b1492ebd9ea53f168c344e1b06d1b82b45bb36a00750` | match |
| A01–A05 | `[7262,10112)` | 2,850 | `843c25b5f88255919b20587fe25fecc8b5d908974af50b5aa4af8b0094088b70` | match |
| B0 census candidate row | `[11160,11437)` | 277 | `be13ed252882549c1efd20dbea49912727def653789e555da73faec746d9f54f` | match |
| origin producer audit | `[8902,10649)` | 1,747 | `20b0b65035f4dbf75ae72f2c62983f881a71cb8bca69fe568e9d9eaa83c7e77f` | match |
| supply-map B02 row | `[32292,32547)` | 255 | `05d5c2ef695729973c1c81eac754acdc7fa666ffac291a94dec976cfe9b58509` | match |

## 2. Stop-form conformance

The frozen expectation permits an absent component only through a named fail-closed stop. The build
uses no quiet value to bridge any stop.

| Stop object | Build evidence | Frozen expectation receiver | Verdict |
|---|---|---|---|
| candidate | DoR-013 keeps the ratified origin at family level with no member selected; A01 rejects an indeterminate family/empty slot; the census reports the five-field candidate MATERIAL-ABSENT. The build attacks its own reason and narrows it to construction from the ratified origin | `STOP_B02_OBJ_ABSENT_OR_UNINHABITED` and the component stops F01–F05 | **LAWFUL, in the build's narrowed scope** |
| joint trace at `omega_phys` | the producer audit says `omega_phys` UNBOUND; stop-rule clause 3 requires a record-native derivation or prospective principal freeze; the build does neither | `STOP_B02_TRACE_ABSENT_OR_UNBOUND` / F07 | **LAWFUL** |
| subtrace join | the source-sector `d_state` lands in `Dens_1^+(H_A)` while the receiving square requires joint `d_state_Omega`/`d_joint`; the build records WRONG CODOMAIN and performs no composition | `STOP_B02_TRACE_ABSENT_OR_UNBOUND`, G01/G04, and condition-16 no-ad-hoc-assembly guard | **LAWFUL** |

The per-field signature disposition is also a lawful stop form **except for `Params_B0`**, adjudicated
next. `Type_B0`, `Arity_B0`, `Dom_B0`, and `Cod_B0` remain non-values; Carrier/Core conditionals remain
unreachable; provenance remains partial. No filled signature, constructor, trace, oracle, or
certificate is quietly asserted.

## 3. Adversarial point — `Params_B0`

### 3.1 Receiver direction

The sealed receiver defines:

```text
Sig_B0 := (Type_B0,Arity_B0,Dom_B0,Cod_B0,Params_B0)
Obj_B0 conforms_to Sig_B0
```

Thus `Params_B0` is the parameter record **of the candidate object**. A04 requires a filled signature
and expressly supplies a no-verdict route when the candidate, declared signature, or independent
oracle is missing. The census says there are “no candidate values or complete signature instance.”

The Gen_Omega span instead enumerates fields of `Omega_prim,N^v003`, the ratified origin family. It
also says the tuple contains no downstream state/response/target outputs. Those bytes can constrain a
future candidate constructed from that origin, but they are not a `Params_B0` value while `Obj_B0`
is absent. Equating them would flatten a source-family parameter tuple into a stopped object's
signature—the exact vacuity the build otherwise avoids.

```text
PARAMS_B0 = OPEN-RULED
Gen_Omega tuple = sealed constraint stock, not a filled candidate-signature field
T1 = NO_VERDICT by its own route
```

The build is wrong at §5.3's `FORCED` typing. The frozen expectation is right: F02 remains stopped and
G01/G06 remain unresolved.

### 3.2 Other four signature fields

`Type_B0`, `Arity_B0`, `Dom_B0`, and `Cod_B0` are not runnable for T1's stated reason. There is no
independently frozen `Obj_B0`, no filled `Sig_B0`, and no independent signature/domain/codomain
oracle. T1 therefore returns `NO_VERDICT`; it does not return FAIL and none of the four fields may be
inferred from that result.

## 4. Q-914 comparison

Because `Params_B0` is OPEN, Q-914 is not dischargeable now. To test Dario's stronger claim anyway,
the displayed Gen_Omega tuple was decomposed into its 16 atomic requirement rows and compared against
the 32 B01 row identities under the frozen expectation's exact key: primitive identity, mathematical
type, domain, authority/content digest, and exact status. No fuzzy thematic match was permitted.

| # | Displayed tuple requirement | B01 exact row | Classification |
|---:|---|---|---|
| 01 | `A0` finite scalar source realization | none | MISSING |
| 02 | `P_0` | none | MISSING |
| 03 | `P_ch` | none | MISSING |
| 04 | `E_ch` | none | MISSING |
| 05 | `Tr_A` | none | MISSING |
| 06 | `I_src` | none | MISSING |
| 07 | `P_src` | none | MISSING |
| 08 | anchor tag | none | MISSING |
| 09 | `Cert_anchor_IF(P_src)` | none | MISSING |
| 10 | `K_cell` | none | MISSING |
| 11 | `iota_cell` | none | MISSING |
| 12 | `plus_root` | none | MISSING |
| 13 | `chi_n` with `n in {+1,-1}` | none | MISSING |
| 14 | `E_post` | none | MISSING |
| 15 | tensor/zero-extension grammar | none | MISSING |
| 16 | declared finite domains | none | MISSING |

```text
EXACT-COVERED = 0
STATUS-GAP   = 0
MISSING      = 16
AMBIGUOUS    = 0
```

The absence is row-granular: B01 contains broader principles with adjacent themes, but none supplies
the displayed primitive/type/domain/authority-digest identity. The comparison is also provisional,
not a Q-914 discharge, because it was parsed from family constraint stock rather than a filled
`Sig_B0` through a sealed `REQUIRED_PRIMITIVES` extractor.

If a later filled signature adopts these sixteen requirements, Q-914 requires a **further sealed B01
act** adding each requirement with type, domain, authority, byte digest, source status, and revised
coverage boundary. B02 must then pin the new B01 version and restart from its first constructor step.
No in-place extension or post-output supplement is lawful. Until a filled signature and extractor
exist, Q-914 is carried whole.

## 5. Member 12 stale header

The full proposal file independently rehashes to:

```text
1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0
```

DoR-013 names that same file by digest prefix `1be5f6a7…` and ratifies the anchored generative family
and three maps. The file's `[0,147)` header still says `PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL
RATIFICATION`; it predates and is superseded in status by DoR-013. The header is stale, not governing.

The repair is documentary and must preserve the ratified bytes:

1. keep the `1be5...` file sealed and unchanged, because DoR-013 ratifies that digest;
2. seal a status-overlay/supersession record binding the old digest to
   `RATIFIED_FAMILY_LEVEL_BY_DOR_013`, while retaining the no-member clause;
3. update later citations to read status through that overlay plus DoR-013, never through the stale
   header alone.

Editing the old header in place would create a new digest not ratified by DoR-013 and is therefore not
the repair. No repair is executed in this relay.

## 6. Independent residue grep

The pattern set was fixed before hits were inspected:

```text
CARRIED\s*=\s*VERBATIM | nothing removed | METHOD\s*=\s*REPRODUC
REPRODUC | set[- ]deltas? | diff[- ]triples? | thirteen[- ]hunk
build_v004\.py | RETAINED\s+[0-9] | ADDED\s+[0-9] | DROPPED\s+[0-9]
```

Case-insensitive replay returns one hit: V001 line 259, `reproducing`, inside the fenced, source-bound
DoR-013 `[355,992)` quotation describing `d_law` as reproducing the ratified DoR-009 law.

**Ruling:** the fenced quotation is a lawful home. The token describes semantic reproduction by a
map, not the retired process claim that an artifact is reproducible from a build procedure. Editing
the quote would corrupt evidence; moving the fence would add ceremony without changing claim scope.
The correct result distinguishes lexical hits from operative claim residue:

```text
LEXICAL_HITS = 1 (fenced governing quotation)
OPERATIVE_REMOVED_CLASS_CLAIMS = 0
FENCE_MOVE_REQUIRED = false
```

## 7. Stop 2 and B03

The `omega_phys` stop is reading-independent: both A01 family readings eventually require the same
joint StatePort/PathCert squares, and the record says no origin map binds `omega_phys`. It is also
scope-independent across the displayed finite and role scopes: the selector is absent at both, while
the finite subtrace cannot substitute for the joint receiver. Consequently B03 remains the sole
lawful protocol step that could release this stop—a sealed record-native derivation or a prospective
principal freeze made before output inspection. The stop does not authorize either choice, does not
supply a value, and does not permit B02/B04+ to proceed first. PE-17 remains the protocol pointer with
zero verdict weight; `omega_phys` stays untouched here.

## 8. Frozen-expectation delta ledger

| Delta | Build statement | Frozen expectation | Ruling |
|---|---|---|---|
| D1 | `Params_B0 = FORCED` from the ratified origin tuple | a filled F02 belongs to `Obj_B0`; G01 and G06 stop candidate-field filling/extraction | **BUILD WRONG:** Params is OPEN constraint stock |
| D2 | Q-914 is runnable/dischargeable against Params alone | Q-914 runs from a filled signature through a sealed extractor and exact row/status comparison | **BUILD WRONG:** comparison is provisional 0/0/16/0; obligation carried whole |

The other apparent divergences conform to the expectation rather than amend it:

- three stops instantiate named fail-closed forms;
- the per-field signature record is a stopped diagnostic, not a ten-component substitute;
- the newly cited candidate/signature schema and T1 route lawfully narrow G01/G05 exactly as the
  expectation allowed—by sealed citation, not invention;
- the build's eleven binding conditions strengthen context, while this relay's frozen minimum was
  deliberately conditions 19–23.

`EXPECTATION_DELTAS = 2-NAMED`; both corrections are to the build, not the frozen expectation.

## 9. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the frozen expectation; B02 build and generator; B02/B01 supply rows; conditions 19-23; A01-A05;
  candidate/signature/T1 receiver; DoR-013 and the byte-identical ratified proposal; producer and
  census stops; PathCert; the 32-row B01 inventory; all standing fences.

DERIVED HERE:
  Q-920 input-set equality and open-call census; five independent span hashes; three stop-form
  dispositions; Params_B0 receiver-direction ruling; four T1 non-runnable confirmations; the
  16-requirement Q-914 comparison; stale-header custody ruling and documentary repair shape; residue
  lexical/operative split; B03 consequence; two expectation deltas.

SELECTED HERE:
  nothing. No candidate, constructor, signature value, parameter record, carrier, core, provenance
  object, trace, map, certificate, anchor member, primitive extension, or omega_phys is selected,
  supplied, adopted, constructed, or proposed for a freeze.

SCALING WEIGHTS:
  none consumed, fixed, formed, compared, or substituted.
```

## 10. Flattening, blind, and verb audit

- **S01-S37 FLATTENING CHECK:** clean after D1/D2. Family parameters were not flattened into a
  candidate signature; constraint stock was not flattened into a field value; a source-sector map
  was not flattened into a joint-codomain map; a fenced lexical hit was not flattened into an
  operative process claim; non-forcing was not flattened into global impossibility.
- **BLIND HELD:** the build is scored against the earlier sealed expectation. No criterion was
  weakened or added to rescue or kill an implementation detail.
- **PE-1..PE-17:** pointer-only; zero verdict weight.
- **BUILDER-NEVER-VERIFIES:** this is the assigned opposite-lane check.
- **FENCES:** no member binding, physical fixed-point execution, end test, numeric evaluation of a
  physical quantity, or comparison to a measured constant. `omega_phys` untouched.
- **VERB AUDIT:** `lawful` receives an exact named stop form and pinned bytes; `missing` receives the
  frozen exact match key; `open` is a receiver-typing result, not a selected value; `ratified` is
  carried only from DoR-013.

```text
CLOSURE = declared-first (byte 0; closure end 3511; scan 0 hits)
STOPS = CANDIDATE:LAWFUL-NARROWED / TRACE-SELECTOR:LAWFUL / JOIN-CODOMAIN:LAWFUL
PARAMS_B0 = OPEN-RULED
Q914 = EXACT:0 / STATUS-GAP:0 / MISSING:16 / AMBIGUOUS:0 (carried whole; conditional further-act + full restart)
MEMBER12 = STALE-CONFIRMED
RESIDUE_GREP = 1-FENCED-LEXICAL / 0-OPERATIVE (own patterns; lawful quotation)
EXPECTATION_DELTAS = 2-NAMED
VERDICT = CORRECTED (Params_B0 and Q-914; three stops stand)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
