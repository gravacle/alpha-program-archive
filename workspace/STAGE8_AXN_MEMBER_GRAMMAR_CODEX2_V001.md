# STAGE 8 / [PLAN:AXN-BOUND-2] — THE MEMBER GRAMMAR ASSEMBLED FROM THE ADMITTING RECORD

Lane: CODEX 2. Relay 814. All headline determinations are **CLAIMED** pending registrar cross-check.

## Lead determination — CLAIMED

The record's member grammar is now assembled as far as sealed stock permits, but the census is **OPEN OF
RECORD**.

The grammar has three different kinds of entries which must not be flattened:

1. **concrete exhibited forms:** the positive public-stiffness ray and the two parent-action controls `S_0,S_1`;
2. **named audit/mutation forms without concrete member manifests:** independent finite `F^2`,
   gauge-covariant higher-derivative source terms, different finite causal updates, record-curvature terms,
   and dissipative terms; and
3. **one cross-axis omitted-member schema:** `CTP_PRESTATE_EFFECT_BRANCH_MUTATION`, varying the common-origin
   P5 state/effect/domain package while holding the listed action/evolution channels fixed.

Only the first category contains exhibited forms. The second category names directions an exhaustive audit
must cover, but sealed text does not supply their complete operators, domains, coefficients, or member lists.
The third is a constructible schema, not a built producer. An earlier census-build wrote candidate formulae for
several open classes, but marked every one `derived=false` and identified the displayed forms as choices. Those
bytes are negative evidence against promoting those formulae into the record grammar; they are not generators
licensed by the corpus.

The record does supply a live base physical equivalence and a conditional pre-root response-equivalence
`~_R`. It also supplies a finite transport-carrier common-refinement relation. The latter identifies connection
carrier presentations on the displayed finite common refinement modulo vertex gauge; it is not a theorem that
two complete microscopic actions are equivalent. No general reparameterization/equal-action relation is live:
S25 bars it absolutely, and the public `K_R(mu)>0` ray is not collapsed by connection rescaling.

The decisive open sentences are direct. The admission audit says the named higher-derivative, finite-update,
and record-curvature/dissipative classes “are not enumerated as concrete members” and therefore “total
cardinality is not fixed.” The P5 audit says its typed role constraints “do not give a finite enumeration, a
generator, a coverage proof, or an equivalence relation that collapses P5 variants.” The action-form closure
specification says present sealed text does not supply the complete action universe. Thus no exhaustive
`G_AF`, no complete equivalence/exclusion decision, and no no-outside proof can be assembled.

Consequently 812 remains **PARTIAL**. S03 release condition (a) remains partial, and F0 absence #2 is narrowed
but not discharged: the missing object is now the executable response-complete presentation of the admitted
action/P5 family, including concrete generator output, relations, coefficient descent, and a no-outside proof.

No selector, member binding, equal-action declaration, reparameterization, minimality rule, response/root
evaluation, or physical quantity evaluation is made.

---

## 0. Preflight and custody — CLAIMED

`relay_outbox/814_ACK.md` was written before the relay body was read. The relay inbox SHA-256 is
`2313655b448fd5085f72efb6943605c76e2376f26d3ad9567efe1f1bb7115df1`, matching its adjacent seal. This
output and its sidecar were absent before writing.

The pinned state brief was verified and read first:

```text
PROGRAM_STATE_BRIEF_V005.md
e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c
```

The governing 812 report was verified at its adjacent seal before use:

```text
STAGE8_AXN_FAMILY_BOUNDING_CODEX2_V001.md
a449cfb31b4c96569c8a2845bc4b018affa157b1d95b87f8dcddca646197537c
```

`QUESTIONS_SETTLED_REGISTER_V001.md` was verified at current live digest
`5f761712e5a385a07f591999e72fc5efeefef581073218dc2f235bc51050f2b3`. Q-696, Q-719, and Q-720 were
re-read before classification; the earlier Q-120/Q-124/Q-131/Q-146–Q-153 sequence was then used as the
register index for the widened probe. Live append after Q-720 was tolerated and did not change those entries.

The ten widened-probe authorities below, the current decline register, and the corrected common-refinement
solve all matched their adjacent seals. The action inventory's sidecar names the archive-relative
`workspace/` copy; the cleanroom copy itself hashes to the same pinned digest `cafad33e…`.

`PE-1` through `PE-9` were pointer-known only. Their contents were not opened or consulted and carry zero
weight.

---

## 1. Three passes — CLAIMED

### Pass 1 — register narrowing

The register sequence fixes the search target rather than deciding it by slogan:

```text
Q-120  two exhibited parent controls admitted; larger action class unresolved
Q-124  three action-form classes named but schematic
Q-131  no concrete identities for those three classes
Q-146  candidate census written, explicitly not frozen and not derived
Q-147  omitted P5 state/effect/domain schema defeats five-slot coverage
Q-148  common-origin is a filter, not a P5 closure theorem
Q-149  fixed derivative order conditionally finite; all-orders closure absent
Q-151  exact missing interface = response-complete finite presentation
Q-153  native finite-algebra route does not yet supply that presentation
Q-696  F0 absence #2 = mutation-family enumeration
Q-719  surviving action node = family with no selector
Q-720  812 PARTIAL waits on grammar/census/equivalence/no-outside
```

This pass produced no ruling and bound no member.

### Pass 2 — widened sealed-corpus probe

The Q-719 probe-width lesson adds 17 sealed spans to 812's 17 spans. They recover the earlier admission audit,
member manifest, attempted census, adversarial P5 axis, higher-derivative equivalence theorem, closure-theorem
specification, response-complete-presentation requirement, and the exact finite common-refinement carrier.

The widened probe changes 812 in one material way: the action-form equivalence is no longer simply “absent.”
A conditional `~_R` is specified and proved under H1–H8, but its applicability to the completed corpus remains
underived and it supplies no all-orders closure. It does not change the open census result.

### Pass 3 — forcing / no-outside attack

Each possible promotion was attacked in both directions:

| candidate promotion | forcing result |
|---|---|
| `S_0,S_1` are the complete census | **REFUTED BY RECORD**: they are controls/witnesses, not the universe |
| the three named action classes are a generator set | **REFUTED BY RECORD**: names lack concrete schemas, fields, and coverage |
| the 146 candidate formulae are the grammar | **BARRED**: the artifact calls them choices, `derived=false`, and not a freeze |
| “finite causal update” implies a finite roster | **REFUTED BY RECORD**: “finite” types an update; it does not prove class cardinality |
| fixed-order higher-derivative finiteness closes all orders | **REFUTED CONDITIONALLY**: the union needs a cutoff, resummation, UV generator, or native finite algebra; none is sealed |
| common origin removes P5 | **REFUTED BY RECORD**: it removes ad hoc packages, not alternative complete target-independent common-origin producers |
| base gauge equivalence closes all action mutations | **REFUTED BY RECORD**: the action-form null extension is missing |
| common-refinement equality is complete-action equivalence | **TYPE ERROR**: its proved receiver is a finite connection carrier modulo vertex gauge |
| a public connection rescaling collapses `K_R(mu)>0` | **REFUTED**: compact periods/unit character keep the points physically distinct; S25 also bars the route |
| failure to find a finite generator proves non-enumerability | **REFUTED BY SCOPE**: no impossibility theorem against future exhaustive generation is sealed |

Only after these three passes is the principal-question test applied in §6.4.

---

## 2. The 34 admitting/constraining spans — CLAIMED

### 2.1 The 17 governing spans carried from 812

The exact half-open pins and span digests are those displayed in governing 812 §1.1; they were re-read here.

| ID | sealed source / half-open span | grammar contribution |
|---|---|---|
| A01 | assembly V005 `76589e94…` `[20230,22055)` | S03 admits a family, not a selector; pair and non-enumeration |
| A02 | assembly V005 `76589e94…` `[26636,27760)` | S03 release branches; family must first be bounded/classified |
| A03 | public charged-action gate `1fae8b74…` `[446,2034)` | public forms `S_K`, `K_R(mu)>0`; rescaling does not identify them |
| A04 | same `[2061,3738)` | zero-bare is not finite-coefficient selection; complete producer obligations |
| A05 | complete-parent gate `67816cfe…` `[202,1701)` | shared premises; exact `S_0,S_1`; HD and finite-update directions |
| A06 | same `[2164,2796)` | complete generator must exclude Pauli/equivalent mutations; no declared minimality |
| A07 | R3 `345d447e…` `[77,774)` | finite first-order parent form and generated descendants; continuum open |
| A08 | same `[3687,5310)` | source-inclusive/global/parameter-free closures remain false |
| A09 | source-closure gate `17245fdd…` `[662,2201)` | local `V_1,V_2` are nonselection witnesses, not complete members |
| A10 | same `[2773,3320)` | durable record, normalization, isolated background, stability, causal/gauge/stitching predicates |
| A11 | boundary-onset gate `cc113a16…` `[1952,2636)` | geometric orthogonality is not automatically an action |
| A12 | same `[3371,4269)` | complete action/durability/causal interval still required |
| A13 | S28 source `03db8d3d…` `[12531,13213)` | five free data; only boundary closure plus induced action may constrain them |
| A14 | PRIN `ae1d0492…` `[1484,2361)` | singular target-independent selection conjunction; no retroactive choice |
| A15 | S34 source `39f7ea6d…` `[3765,4145)` | complete parent must supply isolated positive stable solution equation |
| A16 | T1 source `9be3f55f…` `[5062,5962)` | spectral measure/outgoing algebra must share the parent action |
| A17 | governing 809 `5f802c10…` `[8522,9217)` | derive/vary the complete coupled action; patching is barred |

### 2.2 The 17 widened-probe spans

Every span SHA below was recomputed directly from the sealed bytes.

| ID | sealed source SHA / half-open span / span SHA-256 | added grammar or constraint |
|---|---|---|
| W01 | admission audit `fd688838…` `[148,1377)` / `50478c1bbe354d2bccd88a137e5fc927e785c46c60b1c47a5f265ef62caa749e` | exact admission predicate and the two exhibited controls |
| W02 | same `[1378,3478)` / `5e284be5000826a5af8b3403ae6e295b67d7c08eb7884f423b2f0c1646575973` | exact open-census sentence, required ledger fields, coverage/freeze absence |
| W03 | member manifest `e042f0aa…` `[717,3479)` / `253450b997b0e477737d307454825caaa9e2a059f0512d479a518f0d258a820d` | concrete controls versus three schematic classes; cross-axis coverage needed |
| W04 | concrete identities `04ca81a1…` `[659,3541)` / `e67bc6b51a98001661031234ae724b7199c729f837f163f100153d2f94ddd37c` | no concrete HD/update/curvature/dissipative members; adoption/premise exclusions typed |
| W05 | census build `db966e2b…` `[141,1876)` / `050c84ad3768ef4f5f8c34515b21f0efd15b1446e25c66b14b87a279c96605e9` | candidate HD/update formulae are choices and `derived=false`; not licensed |
| W06 | same `[1877,3607)` / `93c5a4e4c90d80a11d34516086832a955668965af9ff4f59538637ee615cee37` | candidate curvature/dissipative forms also hypothetical; manifest not complete/frozen |
| W07 | omitted-member audit `9faa88e8…` `[662,2249)` / `60ce7b3ec62cdfeb1958020d88800664e2aac33552c6f854ee771faeb7a8f5d8` | `CTP_PRESTATE_EFFECT_BRANCH_MUTATION`; concrete member absent; five-slot coverage defeated |
| W08 | P5 audit `1274b1b7…` `[6941,7838)` / `d3b9d55e12d2696a4c472a62bdb66e2fbbcfe2687dfe586503fdc374ba2bccdf` | common origin excludes ad hoc packages, not alternative complete producers |
| W09 | same `[9105,14476)` / `834a09fd3520a2a8b1f9daef32241898928a66bda81419b12738937944dde9d8` | P5a–P5d, ledger roles, and exact absence of enumeration/generator/coverage/equivalence |
| W10 | HD theorem `c6ad988a…` `[3020,4470)` / `4d0070c750512000b54989e159defbc662e480f6451b08db0b19e0b8c8221409` | conditional pre-root response relation `~_R` and its five preservation clauses |
| W11 | same `[6621,8622)` / `836b7918968353eb20fe11fa153d0edb5f809370921d3ffd49d7a0b8d8417bf7` | H1–H8 applicability gaps; fixed-order basis versus unclosed all-orders union |
| W12 | closure spec `ca2ab3b5…` `[7607,9098)` / `8d2d46dbea116341ffe68d3c8cc8ac3ee855be767dbc2f1e06cc0069043b6dd5` | `F_AF`, base `~_phys`, exhaustive `G_AF`, no-outside, mandatory named classes |
| W13 | same `[9345,10688)` / `91aa39e6d99dd448b1e54cdd2d71135cc6b0fca2fe452d78e2b12b8b5f767bcb` | generation or executable bound must carry a no-outside decision |
| W14 | same `[10755,12717)` / `610ed7c0e6f6e6f03a3faaeff03f0b2163cc1e475719149a5a33e180c849331b` | complete action universe absent; base equivalence named; action-null extension absent |
| W15 | tower-fixing property `b78d3f7a…` `[2041,3941)` / `aad057fe13f6514f852cac557271df33f1133a01944c62fdf79bc26d269ac07b` | four-part response-complete finite-presentation certificate |
| W16 | same `[6288,7623)` / `268f0ee71f6631e820a3e5734a6551d7076135d7e4599d4946d2e9b77494c87d` | candidate generators exist, but coefficient descent and no-outside are absent |
| W17 | corrected joint solve `9cf9b329…` `[6043,7614)` / `5a4aafa14bc36bbae66e99fc361f291aaa6dbb06461d232d3236eb63d513125b` | exact common-refinement carrier: one cochain on `Z`, bijective modulo vertex gauge |

`SPAN_CENSUS = 17 governing + 17 widened = 34.`

---

## 3. AS1 — grammar of record — CLAIMED

### 3.1 Admission predicate

The record licenses classification through this intensional predicate, not through a closed roster:

```text
ADMIT(m) only if
  microscopic(m)
  AND target_independent(m)
  AND m belongs to the completed source-record-field / BR-CTP producer
      or its admitted mutation family
  AND response/root_facing(m) is typed before any value is inspected
  AND no derived equivalence already identifies m
  AND no upstream target-independent theorem excludes m before evaluation
  AND all common-origin, boundary, durability, causal, gauge, quotient,
      source, stability, and stitching predicates applicable to m hold.
```

This is an assembled paraphrase of W01, W07–W09, W12–W14, and A03–A17. It is not a new admission rule.

### 3.2 Concrete displayed forms

`G0`, `G1`, and the `M_*` tokens below are report-local cross-reference aliases for sealed forms/sentences.
They add no member, operation, coefficient, or admission rule to the corpus.

#### G0 — public compact-connection ray

```text
G0 = { S_K | K_R(mu) > 0, compact periods and unit character fixed }.
```

Every displayed point of this public sector is distinct under the public gate. This sector is not promoted to
a statement that every `S_K` extends to one complete microscopic parent.

#### G1 — the two parent-action controls

The exact displayed forms in A05 are:

```text
S_0
  = integral d^4x sqrt(-g)
      i hbar bar(psi) gamma^mu D_mu psi
    + S_record[R,a,g].

S_1
  = S_0
    + hbar ell_*
      integral d^4x sqrt(-g)
        bar(psi) sigma^(mu nu) psi F_(mu nu).
```

The shared declared premises are Lorentz covariance, CPT compatibility, compact `U(1)_rel`, one unit-character
vectorlike Dirac source, one primitive record carrier, one supplied record-cell scale, zero independent bare
Maxwell stiffness, and no use of alpha. The second form adds the fixed-coefficient Pauli source vertex. These
are response-inequivalent admission witnesses of record. Neither is selected or certified as the unique parent.

### 3.3 Named moves and sector schemas

Index the following **names already present in the audit grammar**, with the status column controlling whether
they may be instantiated:

| move / sector | record-licensed shape | status and exclusions |
|---|---|---|
| `M_F2` | independent finite `F^2` action-partition mutation | mandatory closure-test class in W12; the shared `S_0/S_1` sector has zero independent bare stiffness, so this is not silently admitted there |
| `M_Pauli` | `S_0 -> S_1` by the displayed Pauli insertion | one concrete exhibited move; not EOM-exact on the sealed evidence; no control selected |
| `M_HD` | add gauge-covariant higher-derivative source terms | named open schema only; no sealed derivative-order/coefficient grammar, domains, or concrete leading member |
| `M_UPDATE` | replace by a different finite causal update law | named open schema only; “finite” describes the update, not a finite class; no closed list or closure theorem |
| `M_RCURV` | record-curvature action-partition mutation | named proposed class; no typed concrete operator; held out by adopted branch exclusion, not derived no-go |
| `M_DISS` | dissipative action-partition mutation | named proposed class; no typed concrete operator; outside the unitary premise absent a dilation/carrier, not derived no-go |
| `M_P5` | `CTP_PRESTATE_EFFECT_BRANCH_MUTATION` with action/evolution and named five channels fixed, but common-origin `rho_pre`, admitted `E_r`, or domains varied | constructible schema, no concrete producer; arbitrary external choices fail common origin; alternative complete target-independent common-origin producers are not excluded |

The earlier candidate strings `S_HD[n,c]`, `U_Delta^ret`, `U_Delta^loc`, `O_curv`, and `O_diss` are **not**
copied into this grammar. W05–W06 say they were newly written choices, `derived=false`, with missing fields.

### 3.4 P5 typed subgrammar

W09 supplies a type grammar but not members:

```text
P5a  rho_pre: positive normalized trace-class pre-record state on the completed object
P5b  E_r: admitted record effects / POVM effects / instrument effects
P5c  compatible effect and operator domains on one completed object
P5d  branch/state/contour data insofar as rho_pre is a CTP state/contour input

COMMON_ORIGIN:
  P5a-P5d and the candidate dynamics must be supplied by the same microscopic source.
```

This excludes packages supplied externally, by preference, or after seeing a response/root. It does not
enumerate the common-origin packages or prove them all equivalent.

### 3.5 Constraining exclusions

| constraint | excluded without selecting a survivor |
|---|---|
| target-independence and pre-response freeze | target-fitted members, tolerances, exclusions, or census narrowing |
| complete producer/common origin | detached action snippets and ad hoc P5 packages |
| public compact unit character | connection rescaling as a collapse of the `K_R` ray |
| zero independent bare stiffness in the exhibited parent pair | promotion of an arbitrary bare `F^2` coefficient inside that pair |
| boundary/durability/causal requirements | local symmetry-level `V_1,V_2` or geometric orthogonality promoted as complete actions |
| S28/PRIN | action-only, boundary-only, or postcomparison selection of the five free data |
| S34 | complete actions lacking the required isolated-positive-stable equation/solution carrier |
| T1/T5 | spectral-density substitution, patched `x`, or an unvaried surrogate action |
| no declared minimality | choosing `S_0`, first order, shortest grammar, or one update by name |
| provenance / derivation discipline | promoting W05–W06's authored candidates or adopted filtration into derived generators |

### 3.6 Grammar normal form

The strongest record-faithful normal form is therefore:

```text
F_record = PredicateClass(
  concrete displays = { public ray G0, controls S_0 and S_1 },
  audit directions  = { M_F2, M_Pauli, M_HD, M_UPDATE, M_RCURV, M_DISS, M_P5 },
  admission filters = { target-independent, pre-response, complete/common-origin,
                        boundary/durable/causal/gauge/quotient/stability/stitching },
  equivalences      = E_record below
).
```

`PredicateClass` is deliberate. It does not assert that the displayed directions are exhaustive, mutually
disjoint, instantiated, or countable.

---

## 4. AS2 — census and equivalence — CLAIMED

### 4.1 Census determination

```text
CENSUS = OPEN OF RECORD.
```

The record contains a finite list of **known labels**, not a finite census of admitted members. Three exact
closure failures decide the question:

1. W02: “Additional named classes—higher-derivative source terms, different finite causal updates, and
   record-curvature/dissipative mutations—are not enumerated as concrete members. Therefore total cardinality
   is not fixed.”
2. W09: P5 role statements “do not give a finite enumeration, a generator, a coverage proof, or an equivalence
   relation that collapses P5 variants.”
3. W14: “Current sealed text does not supply” the complete action universe; W13 requires an executable cover
   plus a decision/proof that no outside mutation is admitted.

The higher-derivative result sharpens but does not reverse that answer. Under its stated H1–H8/finite-field
local expansion conditions, the quotient basis is finite at each fixed derivative order, while the union over
all orders remains unclosed absent one of four added results: a finite cutoff, all-orders
convergence/resummation, a UV completion/microscopic generator, or a stronger native finite algebra. None is
sealed. Applicability of H1–H8 to the completed corpus is itself underived. This is not promoted to an
unconditional theorem that the full physical family has infinite cardinality.

### 4.2 Named move set is not an exhaustive generator

For audit bookkeeping only:

```text
G_named = {
  M_F2, M_Pauli, M_HD, M_UPDATE, M_RCURV, M_DISS, M_P5
}.
```

`G_named` fails generator status because:

```text
concrete output grammar              = absent for M_HD/M_UPDATE/M_RCURV/M_DISS/M_P5
complete fields/domains/provenance   = absent
cross-axis coverage                  = failed by M_P5
coefficient descent                  = absent
decidable equivalence/exclusion      = absent for the full class
no-outside proof                     = absent
```

The exact sufficient replacement is W15's response-complete finite presentation:

```text
(finite or executable generators,
 decidable relations/kernel,
 exact coefficient descent from finite primitive data,
 proof of response completeness and no outside admitted response class).
```

The existing boundary-superconnection/quasi-free candidate structure is adopted, historically target-aware,
scoped to a finite stationary one-complex, and lacks the last two load-bearing outputs. It is not activated.

### 4.3 Equivalence relation assembled with typed domains

The record-equivalence package is the disjointly typed union below, not one flattened relation:

```text
E_record = {
  E_phys,
  E_R_conditionally,
  E_ref_transport
}.
```

#### `E_phys` — live base physical relation

For complete action mutations, W12/W14 name:

```text
gauge
+ public isometry
+ charge-conjugate orientation
+ Boundary-Resolved equivalence
+ any additional null transformation actually derived by the completed problem.
```

The final term is not a license to invent a null direction. W14 explicitly says the action-form null
extension is absent.

#### `E_R_conditionally` — pre-root response equivalence

W10 defines `Delta S_1 ~_R Delta S_2` when a local invertible target-independent field change:

```text
preserves physical quotient, causal/boundary domains, measure, contacts, Ward identities;
acts identically on discrete incidence except derived gauge;
preserves compact-U(1) holonomy and introduces no incidence magnitude;
covariantly transforms sources/observables;
and yields the same retarded-response functional modulo fixed contacts.
```

Under H1–H8, EOM-exact plus fixed total-boundary operators are removable order by order. W11 also records that
H1–H8 are not discharged for the completed corpus. Therefore `~_R` is assembled as a conditional relation and
cannot be used here to erase `S_1-S_0`, the public stiffness ray, or an unspecified open-class member.

#### `E_ref_transport` — finite carrier refinement equivalence

W17 supplies a sealed finite common refinement `Z`. A coherent A1/A2 carrier pair determines one cochain `S`
on `Z`, and conversely the shared cochain determines the path carriers modulo the same 80-dimensional vertex
gauge. On that receiver:

```text
(carrier_1 ~_ref carrier_2)
iff their pullbacks are the same content-addressed cochain S on Z,
modulo vertex gauge,
with the exact response pullback equality.
```

This relation is exact for the displayed transport carrier. It does not say the underlying complete actions
are equivalent, does not enumerate cellulations globally, and does not select a gauge representative.

### 4.4 Reparameterization and S25

No new reparameterization relation is introduced. The conditional local field-redefinition theorem is carried
at its sealed scope; it is not generalized into “equal action” or used to flatten a continuous modulus. S25
states:

```text
No reparameterization or equal-action principle may be introduced;
the principal refused that route.
```

Accordingly:

```text
K_R rescaling collapse                    = false
S_0/S_1 equal-action identification       = false
open mutation sectors identified by name  = false
transport representative gauge fixed      = false
```

---

## 5. AS3 — consequences — CLAIMED

### 5.1 Effect on 812 bounding

812's partial classification is sharpened, not completed:

```text
concrete public direction       = positive K_R ray, not collapsed
concrete parent controls        = S_0 and S_1
finite transport projection     = 1887-dimensional full-rank identity stratum
named action/P5 directions      = seven audit moves, several only schematic
fixed-order HD quotient         = conditionally finite
global action/P5 census         = OPEN OF RECORD
global action equivalence       = base + conditional relation; null extension incomplete
no-outside decision             = FAILS OF RECORD
```

The `1887` remains only the finite transport projection's dimension. It is not the family dimension. The named
seven audit moves are not seven members or an exhaustive sector partition.

`BOUNDING = still partial.` What remains is not “find more examples”; it is supply an executable exhaustive
presentation or an equivalent upstream uniqueness theorem, complete the action/P5 equivalence/exclusion
decision, and prove no outside admitted response-changing mutation exists.

### 5.2 S03 release condition (a)

```text
S03_RELEASE_A = PARTIAL.
```

The family is classified in additional named directions and its equivalence structure is better typed. It is
not bounded/classified exhaustively because the action/P5 member grammar is intensional, the all-orders tower
is not closed, P5 is not covered, and no no-outside result exists. The release condition is advanced but not
discharged.

### 5.3 F0 absence #2

```text
F0_ABSENCE_2 = REMAINING GAP STATED.
```

The absent object is now exact:

```text
an executable response-complete presentation of all admitted pre-response
action-form, update, and common-origin P5 mutations, carrying
  - concrete member generation,
  - complete typed fields and provenance,
  - decidable physical/response equivalence or upstream exclusion,
  - all-orders coefficient descent where a tower is generated,
  - cross-axis coverage,
  - and a no-outside proof.
```

The displayed concrete controls and named schemas do not discharge that object.

### 5.4 Other construction ends

```text
T2 / S28  five free D_BR data remain unselected.
T4 / S34  no isolated positive stable solution is produced.
T1        same-parent spectral construction fence remains live.
T5        complete-action construction/variation fence remains live.
P5        common-origin filter remains live; P0/P5 producer remains unbuilt.
```

### 5.5 No-outside verdict

The no-outside decision **FAILS OF RECORD**, meaning the required proof is absent. This is not a positive
counterexample theorem against every future generator and not a claim of physical infinite cardinality.

---

## 6. Freedoms, flattening, and ruling-last — CLAIMED

### 6.1 `FREEDOMS_CONSUMED`

| datum | treatment |
|---|---|
| family member | **NONE BOUND OR SELECTED** |
| public `K_R(mu)>0` ray | **CARRIED WHOLE**; no point chosen, no rescaling quotient |
| `S_0,S_1` | **EXHIBITED CONTROLS ONLY**; neither adopted |
| `G_named` | **CARRIED AS NON-EXHAUSTIVE AUDIT NAMES**; not promoted to a generator |
| W05–W06 candidate formulae | **EXCLUDED FROM GRAMMAR** as authored `derived=false` choices |
| higher-derivative order/coefficient | **UNBOUND**; no cutoff or coefficient selected |
| finite causal update | **UNBOUND**; no update rule selected |
| record-curvature/dissipation | **UNBOUND**; premise/adoption exclusions not strengthened |
| P5 state/effect/domain/branch | **UNBOUND**; common-origin constraint retained |
| action-form equivalence | **CARRIED AT SEALED SCOPE**; no new null direction |
| transport common-refinement carrier | **QUOTIENTED ONLY BY VERTEX GAUGE**; no representative selected |
| response-complete finite presentation | **CARRIED AS ABSENT**; no adopted candidate substituted |
| selector/minimality principle | **NONE CONSUMED** |
| smooth constituent / EM reading | **NOT CONSUMED; BARRED** |

`SUBSTITUTED = none.`

### 6.2 Flattening check against all 37 decline rows

`DECLINE_REGISTER_V002.md` was verified and walked from S01 through S37. The live intersections are:

```text
S03  comparison group, local gauge action, and projective quotient remain typed separately.
S08  finite incidence/transport carriers are not identified with EM, Maxwell, smooth field, or response.
S10  local diagnostic potentials remain nonselection witnesses, not physical backgrounds.
S11  CTP histories are not independently transformed; common transformation requirements remain.
S12  status flags are never substituted for member manifests, generators, or proofs.
S14/S15/S23  no flux or bundle selector is authored.
S19/S24  no decay/clustering axiom is imported to close a class.
S22  the positive covector scale remains free.
S25  BARRED-ABSOLUTE: no reparameterization/equal-action principle; no K_R-ray collapse.
S26  C_ref is not used as a smooth source; the finite common refinement is only its sealed carrier.
S28  the five free data remain unselected; boundary closure plus induced action stays conjunctive.
S29-S35  no conditional grant is activated; S34 is a filter, not a produced solution.
S36/S37  absent flux/monopole objects are not supplied.
```

`FLATTENING_CHECK = clean (37/37 rows walked; S25 live and explicit).`

### 6.3 Fence audit

```text
selector proposed                         = false
equal-action principle introduced        = false
reparameterization introduced            = false
minimality declaration used              = false
member bound                              = false
fixed-point execution                     = false
end test                                  = false
numeric evaluation of physical quantity  = false
comparison to measured constant           = false
smooth import                             = false
electromagnetic identification            = false
alpha_computed                            = false
proof_authorized                          = false
kappa_record_computed                     = false
```

### 6.4 Principal question after the three passes

```text
PRINCIPAL_QUESTION = none.
```

The three passes end at a missing construction/theorem, not a discretionary ambiguity. A principal adoption
of one candidate generator, a finite cutoff, one P5 branch, or a minimal action would narrow or select the
family; it would not derive the response-complete presentation or no-outside property demanded by current law.
S03 already reserves principal re-ruling as a distinct release branch, but this classification relay produces
no new forced fork requiring such a ruling. The next lawful object is the presentation certificate itself (or
an equivalent upstream uniqueness theorem), owned by the later construction road.

---

## 7. Self verb audit — CLAIMED

**CLEAN, with four scope disclosures.**

1. “Grammar” means an assembled intensional grammar plus concrete controls and named audit directions, not a
   complete generative grammar.
2. “OPEN OF RECORD” means no exhaustive sealed census/generator/no-outside decision; it is not an impossibility
   theorem against future enumeration and not an unconditional infinite-cardinality claim.
3. The conditional `~_R` is not activated beyond its H1–H8 scope and does not override S25.
4. Common-refinement equivalence is only a transport-carrier relation; it is not complete-action equivalence.

All headline items are **CLAIMED**. No register, plan, tracker, git, commit, push, chain invocation, member
binding, fixed-point, end-test, response/root evaluation, physical evaluation, measured-constant comparison,
smooth import, or electromagnetic identification action was performed.

GRAMMAR = assembled (34 licensing/constraint spans; concrete forms and named moves displayed)
CENSUS = OPEN OF RECORD (admission-audit, P5, and complete-universe sentences cited; named moves are not exhaustive generators)
EQUIVALENCE = assembled (base physical relation + conditional pre-root response relation + finite transport common-refinement relation; domains displayed; global action-null extension absent)
BOUNDING = still partial (complete executable member generation, all-orders coefficient descent, cross-axis coverage, full equivalence/exclusion, and no-outside proof remain)
S03_RELEASE_A = PARTIAL
F0_ABSENCE_2 = remaining gap stated (response-complete action/update/P5 presentation with no-outside proof)
PRINCIPAL_QUESTION = none (three passes end at a construction/theorem obligation, not a forced discretionary fork)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+4 scope disclosures: intensional grammar is not exhaustive; open-of-record is not non-enumerability; conditional equivalence is not activated; transport refinement is not action equivalence)
