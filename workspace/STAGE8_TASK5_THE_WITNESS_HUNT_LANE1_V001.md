# STAGE 8 TASK 5 — THE WITNESS HUNT — LANE 1 V001

Date: 2026-08-04  
Task: PASTE 500 / Task 5  
Lane: Codex Lane 1  
Custody: construction inside the conditionally ratified continuum theory  
Standing: DoR-020 in force; `[EQ6]` not discharged

## Lead result

```text
B_Q408_REFINEMENT =
  OBSTRUCTED_ON_FULL_SCOPE |
  CURRENT_KERNEL_SUBPACKAGE_BUILT_ON_Ref_path;
  exact stop = Ref_path_COFRAME_DENSITY_CONNECTION_CURVATURE_DATA
               + ACTUAL_NEW_EDGE_PATH_TESTFORM_COFRAME_DENSITY_GENERATOR.

B_R1_NATURAL =
  OBSTRUCTED_ON_FULL_SCOPE |
  BUILT_AS_THE_RETAINED_COVARIANT_Adm_base_FAMILY_ON_THE_RATIFIED_STAGE_SCOPE;
  exact stop = ACTION_REPRESENTATIVE_NATURALITY_ON_Ref_path_AND_NEW_REFINEMENT_ARROWS.

B_C1_COMPLETION =
  OBSTRUCTED_ON_FULL_SCOPE |
  FIXED_STAGE_AND_EACH_RANK_PRESERVING_Ref_path_ORBIT_BUILT;
  exact stop = FULL_PHYSICAL_KERNEL_TOPOLOGY
               + FAMILY_UNIFORM_ESTIMATES
               + HILBERTIZABLE_W5.

B_FAITHFULNESS =
  OBSTRUCTED_ON_FULL_SCOPE |
  FINITE_AND_EACH_RANK_PRESERVING_Ref_path_ORBIT_PROVED_FAITHFUL;
  exact stop = COMPLETED_LOWER_SEPARATION_OR_SAFE_QUOTIENT_CERTIFICATE.

B_C2_RESPONSE_BOUNDARY =
  OBSTRUCTED_ON_FULL_SCOPE |
  ZERO_DISCREPANCY_COCYCLE_AND_EXACT_KERNEL_TRANSPORT_BUILT_ON_Ref_path;
  exact stop = NEW_EDGE_COMMON_REFINEMENT_RESPONSE_SOLUTION
               + ACTUAL_BOUNDARY_COCYCLE_AND_SUBEXTENSIVITY.

B_C3_MAXWELL_HODGE =
  OBSTRUCTED_ON_FULL_SCOPE |
  FINITE_WARD_ANNIHILATION_BUILT;
  exact stop = COMPLETED_LOCAL_SYMBOL
               + CLOSED_RANGE_HODGE_MAXWELL_PACKAGE
               + NONZERO_L_T_NORMALIZATION.

PHYSICAL_P2_READER =
  OBSTRUCTED |
  ALGEBRAIC_FAMILY_RETAINED;
  J2_PHYSICAL_PULLBACK_NOT_DEFINED.

JOINT_EQ6 = PARTIAL |
  a genuine finite/Ref_path compatibility subdiagram is built, but the
  full J1--J15 equalizer has no term.  The frontier is the six exact
  full-scope obstructions above, jointly, not merely their separate
  nonemptiness.

MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
```

No fiber is called inhabited by a conditional theorem.  The new positive
content is narrower and exact: on every pure rank-preserving subdivision
orbit, the Q-408 kernel is transported by isomorphisms, so its finite bound
and faithfulness persist without degradation, and the induced discrepancy
cocycle is zero.  Cycle-creating/new-edge refinement and every genuinely
completed analytic certificate remain open.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
numeric_evaluation = false
registered_verdict_written = false
measured_constant_compared = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 0. Preflight and authorities

### 0.1 Three-line preflight

```text
DOES_THE_OBJECT_EXIST = yes | DoR-020 and [EQ6] exist
IS_THE_VERSION_CURRENT = yes | register head Q-422
ARE_ITS_INPUTS_PRESENT = yes_for_the_hunt |
  the six witness objects themselves are the construction target
PREFLIGHT = PASS
```

The locked process was read in full.  The DoR-020 decision and its sidecar
were verified before reading.

| Authority | Verified SHA-256 | Use |
|---|---|---|
| locked process | `e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2` | custody and fences |
| DoR-020 decision | `bead32b7a12dd70b48a5ddd729395f23914dce06047783226a33d3ccb1eafa31` | conditional adoption and prohibitions |
| package V005 | `09883a0d7a5d1329033a09c59ff8d7c843a0098cba569d2a2b97f64f45b2dd95` | adopted clauses and typed partial rules |
| six-bedrock theorem | `288a3d4147cb10f2e51180b21f7c6b2b4749503504c57b8a1121e3925c70c94c` | construction order and reduction proofs |
| J1--J15 constraint system | `d7dcbc3ad7c470c8aaf0d8407db625d3e910cf180cd7b035fbc96bdfe58471b4` | joint equalizer |
| hostile regressions | `510ebda9c05220b3eb229e865a0a41868f5ce166ff2a5805762d540309a6ab1b` | abstract-kernel, circular-reader, and nonemptiness attacks |
| Q-408 finite physical kernel | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | actual finite current/kernel |

### 0.2 Register sweep

```text
Q-396  Adm_base is nonempty and has nontrivial completed flat directions;
Q-407  algebraic reader family classified, physical family unbuilt;
Q-408  finite physical current/kernel and finite Ward annihilation built;
Q-409  C1/C2 conditional, C3 partial, faithfulness separately open;
Q-410  J_ref, density, uniform estimates, and completed symbol absent;
Q-411  J1--J15 joint equalizer defined, inhabitance not proved;
Q-415  abstract-kernel/circular-reader witness killed;
Q-418  rank-preserving Ref_path square derived;
Q-419  objectwise minimum and certificate assertion killed;
Q-420  V005 rules typed; member stopped at the first P4 generator;
Q-421  minimal set of six proved, with joint-equalizer regression;
Q-422  continuum package conditionally ratified under [EQ6].
```

### 0.3 Bearing symbol distinctions

```text
Ref_path != full Ref_PL;
finite injectivity != completed faithfulness;
orbitwise uniformity != family-wide uniformity;
zero discrepancy on an exact subdivision != a global boundary theorem;
finite Ward annihilation != completed Hodge/Maxwell structure;
R2_alg != R2_phys;
six partial witnesses != one EQ6 witness.
```

---

## 1. Q1 — build order

The dependency graph is used as an execution order, with the geometric
carrier first because it supplies the actual directed core on which the
other full-scope certificates are posed:

```text
1  B_Q408_REFINEMENT
2  B_R1_NATURAL
3  B_C1_COMPLETION
4  B_FAITHFULNESS
5  B_C2_RESPONSE_BOUNDARY
6  B_C3_MAXWELL_HODGE
7  derive/test the physical P2 reader
8  test the J1--J15 joint equalizer
```

The first two are parallel roots in the abstract dependency graph.  The
order above maximizes reuse: the P4 attempt fixes the largest physical
subcategory actually available; P1 naturality, completion, response, and
symbol claims are then tested on exactly that category rather than on a
formal refinement category with no physical arrows.

Failure at one step does not terminate later independent attempts.  Each
later step is run first on the maximal built subcategory and then attacked
for a full-scope extension.

```text
Q1_BUILD_ORDER = PASS
```

---

## 2. Q2.1 — `B_Q408_REFINEMENT`

### 2.1.1 Genuine current/kernel subpackage on `Ref_path`

For every subdivision of an already realized path,

```text
gamma_(e,j)(t)=gamma_e((j-1+t)/q),
P_nm=id,
S_nm(c_e)=(c_e,...,c_e).                          (Q2-1)
```

Line-integral additivity gives

```text
A_n=S_nm^* A_m P_nm.                              (Q2-2)
```

Pure subdivision introduces only degree-two vertices, so conservation
makes `S_nm` an isomorphism of the complete cycle carriers.  W3 and
DoR-019 give

```text
S_nm^* R_(K,m) S_nm=R_(K,n).                     (Q2-3)
```

Transporting a profile by

```text
(j_nm^H H)(S_nm x,S_nm y)=H(x,y)                 (Q2-4)
```

then gives the actual Q-408 square

```text
Kern_m^fin(j_nm^H H)[a,b]=Kern_n^fin(H)[a,b].    (Q2-5)
```

These are actual physical paths and actual Q-408 analysis maps, not an
operator stand-in.  Composition, reality, orientation covariance,
batching, quotient compatibility, and identity zero-extension follow from
the same equations.

They do **not** provide `Cof_R`, `Dens_R`, `eta_conn,R`, or
`eta_curv,R`.  Therefore they inhabit the path/current/kernel projection of
`X4(R)`, not the full P4 generator fiber even on `Ref_path`.

### 2.1.2 Full-scope attack

On `Ref_path` itself, no sealed clause upgrades the W3 current isometry to
a physical coframe/density or connection/curvature transport.  Those four
coordinates of `(Q2-6)` remain absent.

Let `R:G->G'` be the first primitive generator with an edge not equal to a
subinterval of any existing physical `gamma_e`.  A witness needs

```text
Gamma_R, P_R, S_R, Cof_R, Dens_R,
eta_conn,R, eta_curv,R                            (Q2-6)
```

and the exact Q-408 squares.  The adopted clauses type these terms but do
not supply any of them.

The available attacks fail as follows:

| Candidate | Failure |
|---|---|
| formal PL edge as path | no map into the adopted physical field background; repeats coefficient/physical relabeling |
| arbitrary bounded `P_R,S_R` | abstract-kernel substitution; no `Gamma_R` provenance |
| zero on the new edge | can delete a record-visible cycle; fails faithfulness/J13/J15 |
| boundary-path concatenation | depends on an unselected route and need not represent the interior edge or compose under refinement |
| reader/response-defined path | circular `pi_Mx` and target tuning |
| imported smooth geodesic | adds undeclared target geometry and a physical fork |

No lawful term remains.

```text
B_Q408_REFINEMENT = OBSTRUCTED_ON_FULL_SCOPE / TYPE-U
BUILT_SUBSCOPE = Ref_path_current_and_kernel_projection
EXACT_OBSTRUCTION =
  Ref_path_COFRAME_DENSITY_CONNECTION_CURVATURE_DATA
  + ACTUAL_NEW_EDGE_PATH_TESTFORM_COFRAME_DENSITY_GENERATOR
```

---

## 3. Q2.2 — `B_R1_NATURAL`

### 3.1 Retained-family witness on the ratified stage scope

DoR-017 supplies a nonempty admissible completed action family
`Adm_base`.  Q-396 proves its covariance, reality, batching, restriction,
unit, and finite-shadow conditions on its ratified stage category and
exhibits nontrivial admissible flat directions.  DoR-020 forbids member
binding, so the lawful positive object is the whole retained family, not a
selected representative.  Thus

```text
B_R1^stage=Adm_base.                                  (Q2-7)
```

is a genuine nonempty retained-family object on the already proved scope.
No least-norm member is chosen.

### 3.2 Full-scope attack

Even for a newly derived `Ref_path` arrow, the current/kernel square
`(Q2-2)`--`(Q2-5)` does not supply an action pullback or a
Hessian/reducing-domain cube.  No sealed certificate identifies that P4
arrow with an existing `rho_Gamma` action arrow.  Promoting the adjacent
kernel square would repeat the category error behind the killed abstract
witness.

For a genuinely new P4 primitive `R`, the same gap is larger: no ratified
map sends the unbuilt geometric tuple `(Q2-6)` to an action restriction.
Q-396 proves there are multiple completed flat directions with the same
finite shadows, so finite authority cannot select an extension.

Even a future P4 term would not by itself prove this action square; this is
the type-independence established in the six-bedrock theorem.

```text
B_R1_NATURAL = OBSTRUCTED_ON_FULL_SCOPE / TYPE-U
BUILT_SUBSCOPE = retained_Adm_base_family_on_ratified_stage_arrows
EXACT_OBSTRUCTION =
  ACTION_REPRESENTATIVE_AND_HESSIAN_NATURALITY_ON_Ref_path
  + ACTION_REPRESENTATIVE_AND_HESSIAN_NATURALITY_ON_NEW_REFINEMENT_ARROWS
```

---

## 4. Q2.3 — `B_C1_COMPLETION`

### 4.1 Fixed-stage construction

At every finite stage, the Q-408 profile carrier is finite/trace-class and
the physical bilocal map is bounded into its declared distribution class.
Let `C_G,Q` be its actual operator bound for each admitted finite target
seminorm `p_Q`:

```text
p_Q(Kern_G^fin H)<=C_G,Q ||H||_prof.              (Q2-8)
```

This is an attained finite-stage bound, not a response graph norm.

### 4.2 New orbitwise construction on `Ref_path`

Equations `(Q2-3)`--`(Q2-5)` identify the profile carriers and kernels by
isometries.  Transport the target seminorm by the exact physical pullback:

```text
p_(Q,m)(K)=p_(Q,n)(Res_nm K).                     (Q2-9)
```

Then

```text
p_(Q,m)(Kern_m^fin(j_nm^H H))
 =p_(Q,n)(Kern_n^fin H)
 <=C_G,Q ||H||_prof
 =C_G,Q ||j_nm^H H||_prof.                       (Q2-10)
```

So every rank-preserving subdivision orbit has one transported bound, with
no growth under repeated subdivision.  Completing an orbit in these
transported seminorms reproduces the same represented finite-dimensional
profile/kernel object.

```text
C1_Ref_path_ORBIT_UNIFORMITY = BUILT
```

### 4.3 Full-scope attack

`(Q2-10)` compares only isomorphic cycle carriers.  It gives no bound for a
cycle-creating generator or for a new local-symbol seminorm.  The full
family still requires:

```text
one physical topology on all actual kernels;
uniform constants over all generator orbits;
an attained global bound family;
a positive covariant W5 generating that topology. (Q2-11)
```

The Q-410 length/refinement attack still applies: finite boundedness places
no bound on growth across new cycle ranks.  Choosing the image/graph norm
would make continuity tautological and fails the permanent graph-norm
regression.

```text
B_C1_COMPLETION = OBSTRUCTED_ON_FULL_SCOPE / TYPE-U
BUILT_SUBSCOPE = fixed_stages_and_each_Ref_path_orbit
EXACT_OBSTRUCTION =
  FULL_PHYSICAL_KERNEL_TOPOLOGY
  + FAMILY_UNIFORM_ESTIMATES
  + HILBERTIZABLE_W5
```

---

## 5. Q2.4 — `B_FAITHFULNESS`

### 5.1 Finite and orbitwise proof

Q-408 proves

```text
Kern_G^fin(H)=0 -> H=0.                           (Q2-12)
```

On `Ref_path`, `j_nm^H` is an isomorphism and `(Q2-5)` commutes.  Therefore

```text
Kern_m^fin(j_nm^H H)=0
 -> Kern_n^fin(H)=0
 -> H=0
 -> j_nm^H H=0.                                  (Q2-13)
```

Repeated subdivision adds no new profile direction.  The completion of a
single rank-preserving orbit under the transported isometric profile norm
is therefore the same faithful represented object.

```text
FAITHFULNESS_FINITE = BUILT
FAITHFULNESS_Ref_path_ORBIT = BUILT
```

### 5.2 Full-scope attack

The completed full category may contain a nonzero infinite-support class
with every finite restriction zero.  Neither `(Q2-12)` nor upper estimates
exclude it.  The exact standing model is the quotient map

```text
q:ell^2 -> ell^2/span{v},
v has infinite support.                            (Q2-14)
```

Its restriction to `c_00` is injective while its completed kernel is
`span{v}`.  Thus a full witness needs either:

```text
a uniform lower/separation estimate proving ker(Kernbar)=0;
or
a disclosed covariant quotient, finite-visible intersection theorem,
and reader/contact annihilator certificates.       (Q2-15)
```

No such object is in the adopted finite stock.

```text
B_FAITHFULNESS = OBSTRUCTED_ON_FULL_SCOPE / TYPE-U
BUILT_SUBSCOPE = finite_stages_and_each_Ref_path_orbit
EXACT_OBSTRUCTION = COMPLETED_LOWER_SEPARATION_OR_SAFE_QUOTIENT
```

---

## 6. Q2.5 — `B_C2_RESPONSE_BOUNDARY`

### 6.1 Exact zero-cocycle witness on `Ref_path`

For a pure path subdivision, define the kernel transport by the exact
commuting square `(Q2-5)`.  Its discrepancy is

```text
beta_nm:=Kern_m^fin(j_nm^H H)-Pull_nm(Kern_n^fin H)=0. (Q2-16)
```

For composable subdivisions,

```text
beta_nl=0=beta_ml+eta_ml(beta_nm).                (Q2-17)
```

Thus the cocycle law holds exactly.  The generated contact ideal is `{0}`;
it deletes no visible bulk cycle.  Reality, orientation, batching, and
restriction are inherited from the physical square.  The algebraic
discrepancy is identically zero, but the physical volume-normalized
subextensive statement is only conditional until the missing P4 density is
supplied; this subpackage does not call `0/Vol_R` defined before `Vol_R`
exists.

This is not a blanket declaration that every future boundary discrepancy
vanishes.  It is a computed statement on the exact subdivision subcategory.

```text
C2_Ref_path_KERNEL_TRANSPORT = BUILT
C2_Ref_path_BETA = 0 / BUILT
C2_Ref_path_SUBEXTENSIVITY = CONDITIONAL_ON_POSITIVE_P4_DENSITY
```

### 6.2 Full-scope attack

A common refinement involving a new edge can have two paths through the
refinement category.  No actual response transport or boundary discrepancy
exists before the new physical maps exist.  Even after P4/P5, cocycle
compatibility is independent: a diamond with path sums `0` and `b!=0` has
no response-natural solution.

The full witness therefore still needs:

```text
nonempty response naturality solution;
actual beta_R on every primitive;
path-independent cocycle proof;
safe closed contact ideal;
subextensive boundary theorem in the same tau_ker. (Q2-18)
```

```text
B_C2_RESPONSE_BOUNDARY = OBSTRUCTED_ON_FULL_SCOPE / TYPE-U
BUILT_SUBSCOPE = exact_Ref_path_kernel_transport_and_zero_cocycle
EXACT_OBSTRUCTION =
  NEW_EDGE_COMMON_REFINEMENT_RESPONSE_SOLUTION
  + ACTUAL_BOUNDARY_COCYCLE_AND_SUBEXTENSIVITY
```

---

## 7. Q2.6 — `B_C3_MAXWELL_HODGE`

### 7.1 Genuine finite positive content

Q-408 proves at every finite stage that the actual kernel annihilates exact
Ward directions in both slots:

```text
Kern_G^fin(H)(d alpha,v)=0,
Kern_G^fin(H)(u,d beta)=0.                        (Q2-19)
```

This supplies the finite Ward kernel and its covariance/reality/restriction
certificates.  By `(Q2-10)`, the equality transports unchanged on every
`Ref_path` orbit.

```text
FINITE_WARD = BUILT
Ref_path_WARD_TRANSPORT = BUILT
```

### 7.2 Hodge/local-symbol attack

Finite Ward annihilation does not construct:

```text
a completed local cotangent/principal-symbol map;
closed range of d and delta;
the contact/harmonic quotient;
a physical Maxwell summand and magnetic partner;
a nonzero normalized L_T coefficient;
refinement-commuting Hodge projections.            (Q2-20)
```

The permanent one-edge witness prevents promotion from current isometry to
Hodge naturality:

```text
Delta_2 S(1)=(1,1) != (2,2)=S Delta_1(1).         (Q2-21)
```

Independently, on `ell^2` the operator `d e_n=(1/n)e_n` has dense nonclosed
range, proving that boundedness and faithfulness do not force the closed
range required by P8.

No functional calculus can be run before the operator/domain and local
symbol in `(Q2-20)` exist.

```text
B_C3_MAXWELL_HODGE = OBSTRUCTED_ON_FULL_SCOPE / TYPE-U
BUILT_SUBSCOPE = finite_Ward_and_Ref_path_transport
EXACT_OBSTRUCTION =
  COMPLETED_LOCAL_SYMBOL
  + CLOSED_RANGE_HODGE_MAXWELL_PACKAGE
  + NONZERO_L_T_NORMALIZATION
```

---

## 8. Derived physical-reader attempt

Q-407 supplies the algebraic normalized-reader carrier

```text
ell_(chi,T)(Q(a,b,C))=a+chi b+Tr(TC),             (Q2-22)
```

subject to exact relation annihilation, covariance, reality, restriction,
and batching.  This remains a lawful retained algebraic family; no
`(chi,T)` member is selected.

The physical reader must be

```text
ell_phys=pi_Mx compose Loc compose Kernbar compose Q. (Q2-23)
```

The current hunt has no full `Kernbar`, no full `Loc`, no `pi_Mx`, and no
completed kernel horn.  Defining `pi_Mx` from `(Q2-22)` would repeat the
circular-reader kill.  Finite Ward annihilation alone does not identify a
Maxwell coefficient.

```text
R2_ALGEBRAIC = RETAINED_FAMILY / BUILT
R2_PHYSICAL = OBSTRUCTED / TYPE-U
J2_MAP = NOT_DEFINED
```

---

## 9. Q3 — joint equalizer step

### 9.1 Built compatibility subdiagram

On the common finite/`Ref_path` scope, the following rows are genuinely
checked:

| J-family | Result on built subdiagram |
|---|---|
| J3 | reader-family covariance/reality/restriction holds algebraically; no physical reader term |
| J4 | retained `Adm_base` family obeys its ratified stage cubes; no `Ref_path` action cube is promoted |
| J5 | `h=0` anchors every retained P1 family member |
| J6 | zero added action preserves Shape-K scope |
| J8 | all displayed maps use the R4-only unit seam |
| J9 | ordered batching stays componentwise |
| J10 | `Ref_path` functoriality follows from nested path restriction |
| J11 | line-integral/current normalization is exact on `Ref_path`; no new-cell density claim |
| J12 | finite physical kernel square commutes on `Ref_path` |
| J13 | `beta=0` obeys the cocycle and safe-ideal requirements on `Ref_path` |
| J14 | finite and orbitwise upper bounds/faithfulness hold; no full completion claim |
| J15 | every finite/Ref_path bottom square is exact |

J1 and J7 remain algebraic formulas rather than physical-member equations.
J2 is absent because `(Q2-23)` is absent.  J10--J15 do not extend to new
primitive arrows.

### 9.2 Joint-equalizer regression

No inference is made from the separate positive subobjects to an EQ6
witness.  The permanent counterexample remains:

```text
X={x:x=0},
Y={y:y=1},
compatibility x=y,
X and Y nonempty but Eq(X,Y)=empty.               (Q3-1)
```

Here the concrete unresolved equalities include:

```text
the future physical reader equals the future Maxwell pullback (J2);
the future P1 action restricts along the future P4 arrows (J4/J5);
the future response transport and boundary cocycle share the future P5
topology (J12--J14);
the completed diagrams reproduce every finite square jointly (J15).
```

None can be checked by combining separate schemas.

### 9.3 Joint status

```text
JOINT_EQ6 = PARTIAL

PARTIAL_POSITIVE_CONTENT =
  retained_Adm_base_family
  + actual_Q408_finite_and_Ref_path_square
  + Ref_path_orbit_upper_bounds
  + finite_and_Ref_path_orbit_faithfulness
  + Ref_path_zero_boundary_cocycle
  + finite_Ward_annihilation
  + algebraic_reader_family.

FULL_EQ6_TERM = absent
MEMBER_BINDING = forbidden_and_not_attempted
```

---

## 10. Q4 — honest frontier, falsifiers, and anti-tuning

### 10.1 Per-fiber frontier ledger

| Fiber | Full status | Maximal built content | Exact remaining object |
|---|---|---|---|
| `B_Q408_REFINEMENT` | **OBSTRUCTED / TYPE-U** | finite and `Ref_path` path/current/kernel projection | `Ref_path` coframe/density/connection/curvature plus new-edge full generator |
| `B_R1_NATURAL` | **OBSTRUCTED / TYPE-U** | retained covariant `Adm_base` family on ratified stage scope | action/Hessian naturality on `Ref_path` and new arrows |
| `B_C1_COMPLETION` | **OBSTRUCTED / TYPE-U** | fixed-stage and orbitwise transported bounds | full topology, family-uniform estimates, W5 |
| `B_FAITHFULNESS` | **OBSTRUCTED / TYPE-U** | finite and orbitwise faithfulness | completed lower bound or safe quotient |
| `B_C2_RESPONSE_BOUNDARY` | **OBSTRUCTED / TYPE-U** | exact `Ref_path` kernel transport and `beta=0` | common-refinement response/cocycle/subextensivity |
| `B_C3_MAXWELL_HODGE` | **OBSTRUCTED / TYPE-U** | finite Ward and orbit transport | local symbol, closed range, Hodge/Maxwell, normalization |
| joint EQ6 | **PARTIAL** | finite/`Ref_path` compatibility subdiagram | one full proof-carrying tuple satisfying every J row |

### 10.2 What `[EQ6]` now needs

The condition is discharged only by one tuple carrying all of:

```text
1  full P4 realization data on `Ref_path` and an actual new-edge Q-408
   refinement realization;
2  a compatible natural completed action family/section on those arrows;
3  one full physical kernel topology with uniform estimates and W5;
4  completed faithfulness or a safe quotient;
5  one response/boundary cocycle solution in that same topology;
6  one closed-range local-symbol/Hodge/Maxwell package;
7  the induced normalized physical reader;
8  one proof of J1--J15 on the same tuple.          (Q4-1)
```

Items 7--8 are not additional bedrock generators; they are the required
joint consequences/certificates.

### 10.3 Falsifiers

| Falsifier | Run | Result |
|---|---|---|
| one-edge/tree | cycle carrier zero; no scalar cycle reader or response introduced | **PASS** |
| pendant/tree quotient | no quotient taken and no visible cycle deleted | **PASS** |
| reciprocal/S8-A finite shadows | Q-408 maps carried unchanged; no completed value substituted | **PASS** |
| abstract kernel | every positive map is the actual Q-408 map | **PASS** |
| circular `pi_Mx` | physical reader remains absent with P8 | **PASS** |
| false nonemptiness | every full fiber marked obstructed; no certificate assertion | **PASS** |
| Hodge from isometry | `(Q2-21)` retained; no Hodge lift | **PASS** |
| minimum/restriction | no minimum or member chosen | **PASS** |
| joint equalizer | separate partial witnesses not promoted | **PASS** |
| DoR-008 | every claimed finite/`Ref_path` square is exact | **PASS** |

### 10.4 Anti-tuning ledger

```text
1  Freeze DoR-020, the six-fiber order, Q-408, and J1--J15.
2  Build the actual Ref_path square before examining any later coefficient.
3  Attempt the first new-edge generator and record the stop.
4  Continue each independent fiber on the maximal actual subcategory.
5  Derive orbitwise bounds, faithfulness, and beta=0 only from exact
   isometric transport.
6  Keep finite Ward distinct from a physical Maxwell symbol.
7  Attempt the physical reader only after the symbol chain; leave it absent.
8  Run the joint equalizer after all six attempts.
9  Bind no member, run no fixed point, and perform no end test.
10 Inspect no protected or measured value.
```

No fence blocked a structural result.  The obstructions are absent physical
or analytic data, not process restrictions.

---

## 11. Final board

```text
B_Q408_REFINEMENT = OBSTRUCTED / Ref_path_current_kernel_projection_built
B_R1_NATURAL = OBSTRUCTED / retained_family_on_ratified_stage_scope
B_C1_COMPLETION = OBSTRUCTED / fixed_and_Ref_path_orbit_bounds_built
B_FAITHFULNESS = OBSTRUCTED / finite_and_Ref_path_orbit_faithfulness_built
B_C2_RESPONSE_BOUNDARY = OBSTRUCTED / Ref_path_beta_zero_built
B_C3_MAXWELL_HODGE = OBSTRUCTED / finite_Ward_built

R2_ALGEBRAIC = BUILT_RETAINED_FAMILY
R2_PHYSICAL = OBSTRUCTED

JOINT_EQ6 = PARTIAL /
  finite_and_Ref_path_compatibility_subdiagram_only;
  full_joint_witness_absent.

EQ6_DISCHARGED = false
MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```
