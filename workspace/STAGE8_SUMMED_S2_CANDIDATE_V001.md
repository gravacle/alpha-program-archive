# STAGE 8 — THE SUMMED-S2' CANDIDATE: AN E1-SUCCESSOR SPEC AMENDMENT, AUTHORED AS PROPOSED SPEC CONTENT — NOT ADOPTED

## BLIND AUTHOR — CODENAME S2PRIME — [SEALED]

Date: 2026-08-14
Role: BLIND author (S2PRIME). This artifact AUTHORS the one spec act the
sealed record itself established no lane can perform: the summed-quantifier
S2' certifying clause and the F'-14-compliant re-derivation of the R.3
colour-sum closure around it — the E1-successor amendment named in so many
words at AQ §2.3(iii) (0a10c030) and ZF §5 q-1..q-3 (26f9314b), and carried
as DEBT 3 in REM (2e4e5163 :509-512).

**ON ITS FACE: THIS IS AUTHORED PROPOSED SPEC CONTENT UNDER THE MANDATE
PATTERN (ad9fc14e). AUTHORED, NEVER ENTERED. STATUS =
AUTHORED-PROPOSED-SPEC-NOT-ADOPTED. Adoption is the PRINCIPAL'S act, taken
with the E1-successor spec-author role, by a SECOND, SEPARATE entry addendum
per §5.1 below. No construction may consume any clause of this artifact as
premise until that addendum exists. The registrar may not enter it; the
lanes may not enter it; conversational summaries may not substitute for the
addendum. This artifact performs NO derivation, NO estimate, NO computation,
NO flag flip, and adopts nothing.**

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC CONTENT ONLY — clause text, quantifiers, and
sealed identities cited at spans; NO physical quantity computed, bounded, or
evaluated; no CAS, nothing executed beyond shasum/file reads/greps. No
scale, imported GR, or faithfulness authority. No
register/tracker/plan/road/ledger/lens file read. Output name probed before
write: ABSENT. No git action (not registered, not committed, not pushed).
Every clause below is marked [CARRIED :span] (sealed E1 text carried
unchanged), [GROUNDED :source] (statement of sealed structure at spans), or
[AUTHORED] (new proposed spec content — the amendment's own act).

---

## 0. WHAT THIS AMENDMENT IS, IN ONE PARAGRAPH

The sealed record splits the quantifier by object (AQ Det. 1, CONFIRMED
331035d3): every certifying estimate (S2 / R-L2b / X_n) is PER-PAIR SUP;
every defined object (R_record,L / R_pointer / Z_comp) is the WEIGHT-SUMMED
ASSEMBLY; the one sealed bridge (R.3) runs per-pair -> summed only. The
per-pair certifying estimate is REFUTED of record (52f2490b: no admissible M
for sup_n ||X_n||_2 per pair), while the SUMMED assembled response is
HS-controlled carrier-uniformly of record (REM 2e4e5163, B-L2* DISCHARGED at
the summed level, CONFIRMED 89641f76). No sealed clause lets the summed
control certify anything: the certifying quantifier is sealed per-pair, and
a sealed spec's quantifier cannot be derived — only amended. This amendment
writes the summed-quantifier certifying clause (S2'), moves the colour sum
inside the Schatten-2 norm with the C4 kill operating BEFORE the norm, and
re-derives the R.3 closure around it compatibly with F'-14 — consuming
assembled-rho displays, never per-pair logs of sums. It creates a sealed
TARGET; it certifies nothing at that target.

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/` unless
noted. Every digest recomputed from bytes at path THIS session; every one
matches its sidecar `.seal.sha256` and, where a tasked digest exists, the
tasked prefix. NONE UNVERIFIABLE.

```text
26f9314bdbbf1d7638ecbdf398c891cb3ba54251d4b40314df39c03ab48c08b7  STAGE8_ZERO_FREE_DERIVATION_V001.md          MATCH (ZF; tasked 26f9314b)
aed551e3dba40a0846e540709f0608398d2f5d28764e01033efbd9b20ed1d90f  STAGE8_ZERO_FREE_CHECK_V001.md               MATCH (ZF-CHK; tasked aed551e3)
0a10c0305d4cde8b226d844ed0fb9289f393b670e02b144bf762dd49c853c9c4  STAGE8_ASSEMBLY_QUANTIFIER_V001.md           MATCH (AQ; tasked 0a10c030)
331035d393695519eaa061a343abb67129a8ea48fcc47a765daaef1883b0aa22  STAGE8_ASSEMBLY_QUANTIFIER_CHECK_V001.md     MATCH (AQ-CHK; tasked 331035d3)
2e4e5163bb5b9c06078890fc573dd149905975b55860dcabcc7050fb1aa02666  STAGE8_REMAINDER_UNIFORMITY_V001.md          MATCH (REM; tasked 2e4e5163)
89641f762d35c0e6d3c8fb0475e2de742663f9dcf9f08a44621b059c2bd927ec  STAGE8_REMAINDER_UNIFORMITY_CHECK_V001.md    MATCH (REM-CHK; tasked 89641f76)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (E1; tasked 46846730)
ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213  ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md  MATCH (ATTACH-DoR; tasked ad9fc14e)
df4514a5b807cbc903465505ad896ffb6e72c7ab9f891a4541226d18398a7034  STAGE8_G1_KERNEL_CERTIFICATE_V001.md         MATCH (G1; consulted for the licensed sentence and gate list)
52f2490b187fd4b307c2af45f6238ea02f1d6839b23466fefee1dbba47ed6241  STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md  MATCH (RL2B-REFUTED; the per-pair refutation, cited)
```

Sealed spans relied on below, read at path this session: E1 :644-648
(cancellation-first organizing principle), :652-666 (R.0 per-pair object
and Carleman display), :669-674 (F'-14 / SCAD_COLOR_SUM_LOG_MISUSE),
:679-689 (R.1 quantifier preamble; sup over CELLS deferred to R-L0),
:691-708 (S1, S2, S2b), :720-736 (S4 H-R; S5 H-B0), :739-762 (R.2 sector
algebra), :766-790 (R-L4a/b/R-L4), :794-800 (kappa_bal = 1 from surviving
weights), :803-848 (R.3, R-L2b, colour-sum closure), :851-894 (R.4:
R-L0/R-L0b), :339-352 (C4), :353-364 (C6 + volume-diagonal locus),
:1131-1144 (C-L1/C-L2), :1714-1719 (F'-14 fence text); AQ §2 (Q-1..Q-11),
§2.3, §4, §5; AQ-CHK §2 (the hostile hunt); REM §2.1, §3.5-3.6, §5; REM-CHK
verdict (CONFIRMED; findings f1/f2 non-load-bearing); ZF §3-§6 (the
scaffolding; o-1/o-2/o-3; q-1..q-3); ZF-CHK :37-61 (CONFIRMED with the
R-L0 landing-set correction); G1 §5 (the licensed sentence :327-348), §4.1
(OBL-D); ATTACH-DoR (the mandate pattern, entry mechanics, honesty-of-record
and falsifier structure).

---

## 2. NECESSITY, ESTABLISHED OF RECORD (cited, not re-argued)

```text
N-1  THE SUMMED CERTIFYING CLAUSE EXISTS NOWHERE SEALED — HOSTILE-SEARCHED
     TWICE. AQ §2.1: "No weight-summed variant of S2/R-L2b exists anywhere
     in the sealed text (searched: no sealed display places
     `sum w_mu^* w_lambda` inside a Schatten-2 norm)." AQ-CHK §2
     independently re-hunted with enumerated patterns over all ten sealed
     sources and every weight-sum site in E1 (:671, :729-731, :839-840,
     :877-885, :1010, :1868): "NONE places the sum inside a Schatten-2 (or
     any) norm; NONE states a summed certifying estimate. The build's
     negative claim survives hostile search."
N-2  THE QUANTIFIER OF A SEALED SPEC CANNOT BE DERIVED, ONLY AMENDED.
     AQ §2.3(iii): "WHAT KIND OF ACT WOULD SETTLE THE FATE: not a reading —
     a spec act. ... an E1-successor spec amendment — an act of the spec's
     author, not of any lane." ZF §5: the quantifier content is "the spec
     author's act, irreducibly" (q-1, q-2, q-3). REM :509-512 (DEBT 3):
     "a SPEC-AUTHOR'S act; this artifact analyzes the summed object
     structurally and cannot create the sealed target."
N-3  THE ABSENCE IS LOAD-BEARING, NOT DECORATIVE. E1 :644-648's
     cancellation-first list ("the a-difference and the Moebius difference
     are taken before any norm is applied") conspicuously omits the colour
     sum (AQ §2.3(i), reinforced at the bytes by AQ-CHK §2). The C4 kill
     (|Σ_λ w_λ|² = |m_0|² = 0, E1 :339-345) therefore operates only where
     nothing certifies (the defined objects), and the certifying estimate
     stands only where it is refuted (per-pair, 52f2490b).
```

This artifact performs exactly the act N-2 names, in the only lawful shape:
AUTHORED PROPOSED SPEC CONTENT under the mandate pattern (ad9fc14e), for
the principal's adoption or rejection.

---

## 3. q-1 — THE OBJECT: THE SUMMED CERTIFYING OBJECT, CHOSEN AND JUSTIFIED

### 3.1 The decision [AUTHORED]

**The amendment quantifies over the FIXED-n FAMILY WITH n-UNIFORM
CONSTANTS — the discharged trilemma form — and defines NO summed limit
object.**

### 3.2 The justification [GROUNDED]

```text
J-1  WHAT THE DISCHARGED CHAIN ACTUALLY DELIVERS is exactly this form. REM
     :131-134: "carrier-uniformity is EXACTLY HS-finiteness of the
     uncompressed X: the trilemma form sup_n ||X_n||_2 <= M"; delivered at
     REM :380-382 and NET :448-452 ("sup over the D3/Hermite-Galerkin
     refinement family of ||X_n||_2 <= ||X||_2 <= M, M symbolic and
     carrier-free: the trilemma form of the sealed require-clause
     delivered"). An amendment quantified any other way could not be
     discharged by the only summed control that exists sealed.
J-2  THE PER-PAIR CARRIER LIMIT DOES NOT EXIST OF RECORD. ZF §4.1(c):
     per-pair sup_n ||Delta_n(a)||_2 = +infinity (52f2490b), "so the
     per-pair determinants have NO uncompressed limit object on the sealed
     basis; only the n-indexed family exists. Any Route B statement is
     therefore about the FIXED-n family with n-uniform bounds." A
     spec-DEFINED summed limit object would have to freeze sum-then-limit
     vs limit-then-sum with zero sealed limit theory behind either — a
     CONSTRUCTION act, which the record demands separately (G1 §6,
     CONSUMER 2: break [1] "demands a sealed CONSTRUCTION of C as an
     object; the discharged chain is a BOUND, not a construction") and
     which this amendment must not and does not smuggle.
J-3  WHAT THE DEFINED OBJECTS CONSUME is served by n-uniform constants.
     R_record,L consumes the log of the complete summed amplitude,
     POST-LIMIT (AQ Q-8, FORM :68-82); V011's zero-free demand is
     volume-uniformity of a neighborhood — a constant uniform over the
     family and over cells (ZF D-1; Gamma additivity V011 :1253-1258 makes
     any family-shrinking radius fail the many-cell object). Both consume
     CONSTANTS that survive the limit, not a new limit object. n-uniform
     fixed-n statements supply exactly those constants; the post-limit
     definitional objects of the sealed architecture are left untouched.
J-4  QUANTIFIER CONTINUITY WITH THE SEALED LINEAGE. The sealed R-L2b
     notation of record is already the family form ("Let X_n = C_n(V_n(a) -
     V_n(0))C_n. R-L2b requires sup_n ||X_n||_2 <= M" — TRILEMMA :156-157
     via AQ Q-7; the identical clause is §0 of 52f2490b). The amendment
     changes ONE thing about this shape: the colour sum moves inside. The
     refuted clause and the amended clause are therefore comparable at
     sight — which is what makes falsifier V-1 (§5.3) sharp.
```

### 3.3 The clause text — proposed §R.0' addition to E1 [AUTHORED]

```text
S2'-O  THE SUMMED CERTIFYING OBJECT. Per admitted state (M-3), per admitted
    cell C of D3, per CTP pair on the closed pair polydisc (M-2), per
    carrier index n of the D3/Hermite-Galerkin refinement family, define

      Delta^Sigma_n(a) := sum_{(mu,lambda) in SECT} w_mu^* w_lambda ·
                          C_n ( V_{mu lambda, n}(a) - V_{mu lambda, n}(0) ) C_n

    — the colour sum taken INSIDE, at the operator level, BEFORE any norm.
    SECT is the pair index set of record, administered by the R-L4 family:
    the ALL-PAIRS instance Delta^{Sigma,all} (on which the C4 kill runs by
    the m_0 factorization) and the SURVIVING-sector instance
    Delta^{Sigma,surv} (which the closure of §4.3 consumes; opposite-phase
    pairs EXCLUDED per R-L4b [CARRIED E1 :759-762], never assigned a
    value) are BOTH defined; their relation is obligation-administered
    (§4.4 D-ii), never assumed.
    THE CANCELLATION-FIRST LIST IS HEREBY EXTENDED [AUTHORED amendment to
    E1 :644-648]: the a-difference, the Moebius difference, AND THE COLOUR
    SUM are taken before any norm is applied. The C4 kill (m_0 = m_1 = 0,
    [CARRIED E1 :339-345]) thereby operates INSIDE the certifying object:
    in Delta^{Sigma,all}_n every Dyson stratum with fewer than two record
    insertions per CTP branch is annihilated identically before the norm
    exists to see it.
    THE CERTIFYING QUANTIFIER: nonvanishing/estimate claims under this
    amendment are claims about the n-INDEXED FAMILY WITH n-UNIFORM
    CONSTANTS. No summed limit object is defined by this amendment; no
    limit is taken in any certifying clause; the post-limit consumption is
    the sealed architecture's own (FORM/V011), inheriting exactly the
    n-free constants and nothing else.
    The per-pair object of R.0 [CARRIED E1 :652-666] is NOT deleted: it
    remains defined, and every per-pair sealed result (including the
    refutation 52f2490b) stands. The amendment ADDS the summed certifying
    quantifier; it does not touch the per-pair record.
```

---

## 4. q-2 — THE ESTIMATE CLAUSE: SUMMED-S2', AND R.3 RE-DERIVED AROUND IT

### 4.1 The certifying estimate — proposed §R.1 addition [AUTHORED]

```text
S2'. G_hs^Sigma(C, eps) := |C|_4^{-alpha'} · || Delta^Sigma_n(a) ||_2

    [SUMMED SUBTRACTED HILBERT-SCHMIDT DENSITY. The colour sum is INSIDE
     the Schatten-2 norm; the C4 kill has operated BEFORE the norm (S2'-O).
     MUST be a TWO-TIME (cell-S-matrix) object; the equal-time version is
     FALSE by C6 [CARRIED E1 :353-364, :696-698]. *** THE EXPONENT alpha'
     IS NOT ASSERTED *** — symbol only, R-L2b discipline carried [CARRIED
     E1 :698-700, :826-832]; deriving it is R-L2b^Sigma below. The sups
     over states and the closed pair polydisc at radius eps are taken as in
     R.1 [CARRIED E1 :687-689]; the sup over COLOUR PAIRS is REPLACED by
     the sum inside; the sup over CELLS remains deferred to R-L0 [CARRIED
     E1 :689]; the sup over the CARRIER index n is taken as in the trilemma
     form.]

R-L2b^Sigma  SUMMED UNIFORMITY REQUIRE-CLAUSE (the amendment's certifying
    statement). Certify

      sup_n || Delta^Sigma_n(a) ||_2 <= |C|_4^{alpha'} · G_hs^Sigma(C,eps)

    with G_hs^Sigma finite UNIFORMLY over the D3 refinement quantifier, a
    functional of (||b_D||, tau_R, sea-kernel decay data, |w_lambda|) ONLY
    — no carrier index, no cellulation datum [CARRIED discipline, E1
    :684-686, :873-875] — exhibiting the mechanism that supplies the
    powers. alpha' may turn out to equal the alpha of R-L2b; it may not be
    assumed to. Witness (new): SCAD_SUMMED_HS_UNIFORMITY_UNCERTIFIED.
```

### 4.2 What discharges S2'/R-L2b^Sigma NOW, at the bytes [GROUNDED]

```text
D-NOW-1  THE ALL-PAIRS OPERATOR COMPONENT IS DISCHARGED OF RECORD. REM
    (2e4e5163, CONFIRMED 89641f76): the assembled response X — which IS
    the all-pairs colour sum inside the object (X = a_+ C U(0)^dag F C +
    a_- C F~^dag U(0) C + O(a^2), U(0) and F each weight-summed, so the
    branch product carries sum_{mu,lambda} w_mu^* w_lambda per pair) — is
    HS-controlled CARRIER-UNIFORMLY: sup_n ||X_n||_2 <= ||X||_2 <= M in
    the trilemma form, M symbolic and carrier-free, with the C4 double
    degree-0 kill BEFORE the norm as the deciding step, the a-series
    dominated on the closed polydisc at all orders, and reversal inside
    the coincidence strata EXCLUDED (REM §3.5, §4, NET). Grade: exact
    where identity-grade, estimate-grade e-i/e-ii with NAMED overturn
    conditions (REM §5) — carried at that grade, not upgraded here.
D-NOW-2  THE F'-14-COMPLIANT NONVANISHING DISPLAY EXISTS OF RECORD. ZF
    §4.2 (26f9314b, CONFIRMED-with-correction aed551e3): normalization
    derived exactly (all-pairs weight total = |m_0|^2 = 0, so
    normalization is MANDATORY; N_surv(0) = 1/2 at the C-L1 collapse
    level conditional on R-L4a/b; c = (1/2, 1/8, 1/8, 1/8, 1/8);
    sum c = 1; kappa_bal = sum|c| = 1); entirety at fixed n; and the
    assembled-rho display |Z_hat(a) - 1| <= kappa_bal · x e^x,
    |Z_hat| >= 1 - kappa_bal x e^x > 0 on {kappa_bal x e^x < 1} —
    per-pair-INPUT but sum-level-OUTPUT, no per-pair log applied to any
    sum: F'-14-compliant by construction. This is the mechanism the
    closure of §4.3 adopts.
```

### 4.3 The re-derived colour-sum closure — proposed §R.3' [AUTHORED, with every consumed identity CARRIED at span]

```text
R.3'  SUMMED-QUANTIFIER COLOUR-SUM CLOSURE (F'-14-COMPLIANT: the assembly
    is of assembled-rho displays; NO per-pair log of any sum appears; the
    per-pair Carleman display of R.0 is consumed ONLY pairwise on the
    surviving sector where it is sealed VALID [CARRIED E1 :663-666]).

    (a) THE ASSEMBLED OBJECT AND ITS NORMALIZATION [adopting ZF §4.2]:
        Z_hat_n(a) := Z^{surv}_comp,n(a) / N_surv,n(0),  Z_hat_n(0) = 1,
        with N_surv, c_{mu lambda}, kappa_bal as in R.3 [CARRIED E1
        :838-841] and, at the C-L1 collapse level conditional on R-L4a/b
        and the C-L2 error input: N_surv(0) = 1/2, c = (1/2, 1/8 x 4),
        kappa_bal = 1 [GROUNDED ZF §4.2, CAS Z10c-Z10g].
    (b) THE EXACT LINEAR PASSAGE — where the colour sum moves inside by
        LINEARITY, not by any log: since sum_surv c = 1,
        Z_hat_n(a) - 1 = sum_surv c_{mu lambda} (rho_{mu lambda,n}(a) - 1),
        and the first-order (trace) content of the right side is
        tr[ sum_surv c_{mu lambda} Delta_{mu lambda,n}(a) ] =
        tr[ Delta^{Sigma,surv,c}_n(a) ] — the c-weighted colour sum INSIDE
        the trace, exactly. [AUTHORED display; the trace linearity is
        elementary and the per-pair first-order content is the sealed
        Carleman display's own leading term, E1 :663-666.]
    (c) THE SEGMENT REMAINDER — named, not hidden: the balance of
        (b) beyond the linear trace term (the per-pair Carleman segment
        integrals and exponential higher orders) must be certified AT THE
        SUMMED-COMPATIBLE QUANTIFIER (n-uniformly). The per-pair route to
        it is REFUTED of record (52f2490b) and is hereby BARRED as a
        discharge route for this clause. Require-clause R.3'-c: certify
        sup_n | Z_hat_n(a) - 1 - tr[Delta^{Sigma,surv,c}_n(a)] | <=
        Q^Sigma(C,eps) with Q^Sigma admissible (same functional discipline
        as G_hs^Sigma), consuming H-R [CARRIED E1 :720-727], the C-L2
        error input [CARRIED E1 :1137-1144], and the R-L4-administered
        sector definition. Witness (new):
        SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED.
    (d) THE CLOSURE OUTPUT [adopting ZF §4.2's mechanism]: wherever
        (b) + (c) certify |Z_hat_n(a) - 1| <= B(C,eps) < 1 n-uniformly,
        then n-uniformly |Z_hat_n(a)| >= 1 - B > 0, Log Z_hat_n is defined
        and analytic there (Z_hat_n entire at fixed n [GROUNDED ZF
        §4.1(b)]), and |Phi_C| obeys the R.3.b form with B in place of
        kappa_bal x e^x [CARRIED form, E1 :844-845]. The zero-free
        neighborhood consumed by V011's demand is then {B < 1} with B
        n-free and, upon R-L0, cell-uniform [GROUNDED ZF §3.5(e),
        ZF-CHK's R-L0 landing-set correction].
    (e) SECTOR HYGIENE [CARRIED]: no term of the excluded sector appears
        in any sum of this closure (SCAD_VANISHING_SECTOR_DIVIDED stands,
        E1 :846-848); rho is never formed on a pair with
        det(1+A(0)) = 0.
```

### 4.4 What GATES the discharge — stated exactly, nothing papered over [GROUNDED]

The amendment's clauses are written to be dischargeable by D-NOW-1 +
D-NOW-2 PLUS the following, which gate its DISCHARGE, not its adoption:

```text
D-i    R-L4a / R-L4b [CARRIED E1 :766-790; witnesses
       E1_BASELINE_DETERMINANT_EXISTENCE_UNCERTIFIED,
       E1_OPPOSITE_PHASE_SECTOR_VANISHING_UNCERTIFIED]. They DEFINE the
       surviving sum (ZF o-3) and, at determinant level, identify the
       all-pairs and surviving-sector amplitude sums (opposite-phase
       determinants vanishing identically). UNDER THIS AMENDMENT their
       discharge burden EXPLICITLY INCLUDES the sector bookkeeping at the
       amended quantifier: relating Delta^{Sigma,surv(,c)} (consumed by
       R.3'(b)-(c)) to Delta^{Sigma,all} (where D-NOW-1 discharges). THE
       STRUCTURAL FACT IS SEALED AND IS RESTATED HERE SO NOTHING IS
       HIDDEN: the surviving-sector restriction BREAKS the m_0 product
       factorization the discharged kill runs on, and no sealed identity
       converts control of || sum w^* w Delta ||_2 into control of the
       per-pair-first determinant sum without per-pair inputs (ZF o-2(i),
       o-2(ii), verbatim in substance). R.3'(b) supplies the ONE exact
       per-pair-free passage (trace linearity); R.3'(c) and this clause
       carry the rest as named obligations.
D-ii   R-L0 (or R-L0b) [CARRIED E1 :851-894; witness
       E1_CELL_SCALE_NORMALIZATION_UNCERTIFIED /
       SCAD_COLOR_SUM_REQUIRES_ABSOLUTE_X_BOUND]. The cell sup. Per
       ZF-CHK's correction (:49-61), the summed-S2' axis is the
       carrier/colour-sum axis and DOES NOT deliver the cell sup; R-L0
       MAY FAIL (the tau_R scale-invariance hazard stands as sealed).
       Carried unchanged; re-used threshold 1/2; NO new number.
D-iii  THE C-L2 INPUT [CARRIED E1 :1137-1144; witness
       E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED]. Supplies the baseline
       error object (A(0) on the surviving sector) that R.3'(a)'s
       collapse-level normalization and R.3'(c)'s remainder consume.
D-iv   NAMED HYPOTHESES CARRIED: H-R (S4) and H-B0 (S5) retain their
       sealed undischarged status [CARRIED E1 :720-736]; kappa_bal = 1 is
       consumed ONLY as the surviving-weight arithmetic [CARRIED E1
       :794-800], never from the stricken display.
```

The amendment does NOT paper over D-i..D-iv. None of them is discharged,
weakened, re-derived, or re-quantified here beyond D-i's explicit burden
statement; each gates the DISCHARGE of S2'/R.3'; NONE gates adoption.

---

## 5. q-3 — ADOPTION MECHANICS (mandate-shaped throughout, per ad9fc14e)

### 5.1 Entry conditions [AUTHORED, on the DoR-013 / member-12 pattern]

```text
E-1  This artifact is AUTHORED PROPOSED SPEC CONTENT, carried
     AUTHORED-PROPOSED-SPEC-NOT-ADOPTED. No lane, no registrar, no
     construction may consume any clause of §3-§4 as premise before entry.
E-2  OPPOSITE-LANE CHECK: a hostile check of this artifact (seals
     re-verified at path; every CARRIED span re-read at bytes; the
     AUTHORED/CARRIED/GROUNDED marking audited; the anti-outcome-shopping
     declaration §6 audited against the motivation chain) must be sealed
     BEFORE any entry act.
E-3  THE ENTRY ADDENDUM IS A SECOND, SEPARATE PRINCIPAL ACT: an addendum
     naming (i) this artifact's digest, (ii) the check's digest, and
     (iii) the principal's explicit confirmation, taken with the
     E1-successor spec-author role. Until it exists, nothing is entered.
     Conversational summaries may not substitute.
E-4  HONESTY OF RECORD: every authored ingredient is marked AUTHORED in
     place (§3.3, §4.1, §4.3, §5, and the two new witness names). An
     authored ingredient later shown to have been presented as forced —
     or a CARRIED span shown not to match the sealed bytes — VOIDS the
     candidate (mandate pattern, ad9fc14e Honesty-of-Record clause).
```

### 5.2 Witness retirement [AUTHORED]

```text
W-1  SCAD_COLOR_SUM_LOG_MISUSE (F'-14) IS RETIRED ON DISCHARGE, NOT ON
     ADOPTION. F'-14 is a validity bar — passed or violated, never
     discharged by proof ABOUT it (AQ §4.3). Adoption of this amendment
     creates the sealed Route B target and adopts R.3' as THE compliant
     passage SHAPE; the witness STANDS at adoption and stands until
     S2'/R-L2b^Sigma AND R.3'(b)-(d) are DISCHARGED (with D-i..D-iv), at
     which point it is retired BY DERIVATION, not by convenience.
W-2  The new witnesses SCAD_SUMMED_HS_UNIFORMITY_UNCERTIFIED and
     SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED are LIVE from adoption
     until their clauses discharge. All existing witnesses of the E1
     lineage stand unmodified; no witness is retired by this artifact.
```

### 5.3 Falsifiers and void conditions [AUTHORED, mandate-shaped]

```text
V-1  REFUTATION AT ITS OWN QUANTIFIER VOIDS. If the summed estimate is
     refuted at the amendment's own quantifier — an exhibited admissible
     refutation of sup_n ||Delta^Sigma_n(a)||_2 <= |C|_4^{alpha'}
     G_hs^Sigma at the amended quantifier, the exact analogue of 52f2490b
     — the amendment is VOID and the summed horn dies with it. This
     falsifier is FIRST-CLASS: the amendment creates a target at which
     failure is as reportable as success.
V-2  THE ONE-WAY BRIDGE SHOWN TWO-WAY VOIDS. If a sealed derivation is
     exhibited running summed -> per-pair control (the reverse of R.3's
     one-way bridge, AQ §2.3), then the sealed per-pair refutation
     (52f2490b) transfers to the summed object and the amendment's
     premise set collapses: VOID without awaiting anything downstream.
V-3  ANCHOR OVERTURN VOIDS. If REM's named overturn conditions (REM §5)
     obtain — a failure of [F]_0 = 0 or [U(0)]_0 = 0 under re-derivation;
     a stratum class outside the §2.3 enumeration; a composition route
     evading both Plancherel and the capped-mass Cauchy-Schwarz — or if
     the ZF scaffolding (normalization, nonvanishing mechanism) is
     overturned at check-grade, the corresponding clause of this
     amendment is VOID (D-NOW-1/D-NOW-2 are its only discharge anchors).
V-4  BYTES-CONTRADICTION VOIDS (structural falsifier, ATTACH-DoR
     pattern): if any sealed forced result is shown, at bytes, to
     contradict any clause of §3-§4, the candidate is void.
V-5  KILL-FAILURE VOIDS: if the C4 kill is shown NOT to operate on
     Delta^{Sigma,all}_n as defined (a stratum with fewer than two record
     insertions per CTP branch surviving m_0 = m_1 = 0 inside the
     object), S2'-O's kill-before-the-norm clause is false as written:
     VOID.
V-6  MARKING-FRAUD VOIDS: per E-4.
```

### 5.4 No-freeze clauses [AUTHORED]

```text
NF-1  NO VALUE. No number is frozen or introduced anywhere in this
      amendment. alpha' is a SYMBOL (not 1/2, not anything); the only
      threshold appearing (1/2 in R-L0) is the parent's frozen threshold
      RE-USED [CARRIED E1 :857-858]. No physical quantity is evaluated.
NF-2  NO CARRIER / NO CELLULATION DATUM. Every functional in S2'/R.3' is
      of (||b_D||, tau_R, sea-kernel decay data, |w_lambda|) only;
      Scoping Clause 1 carried (witness
      SCOPING_CLAUSE_1_VIOLATION_CELLULATION_DATUM stands); the D3
      quantifier is not pinned to any finite list under any outcome.
NF-3  NO FLAG FLIPS — GOV-F RESERVED. Every sealed flag stands exactly as
      sealed (including V011 :2184 volume_uniform_zero_free_neighborhood_
      proved = false and E1 :2191 record_vertex_two_time_HS_certified =
      false, which REM's discharge leaves for the evaluator of record).
      Flag flips belong to the Gate-6 evaluator, never to this amendment,
      never to its adoption.
NF-4  NO FROZEN DEFINITION TOUCHED. M(t) stays SHARP (softening is a
      sealed Phase-A A1 definition, not touched [CARRIED E1 :1123-1124]);
      equal-time stays FALSE by C6; C4, C6, D3, the weights, tau_R, and
      the per-pair R.0 objects are all carried unchanged.
NF-5  NO LIMIT OBJECT FROZEN (per §3): the amendment defines no summed
      limit object and takes no limit.
```

---

## 6. ANTI-OUTCOME-SHOPPING DECLARATION [AUTHORED, required]

```text
THE MOTIVATION CHAIN, STATED IN FULL AND IN ORDER:
  M-1  AQ (0a10c030, 2026-08-13) determined the quantifier SPLIT-BY-OBJECT
       from sealed bytes and named the settling act (§2.3(iii)) — a spec
       amendment placing the colour sum inside the norm — as a
       DETERMINATION about what kind of act would settle the fate, made
       BEFORE any summed-level estimate existed.
  M-2  REM (2e4e5163, 2026-08-13) discharged B-L2* at the summed level,
       independently, and carried the summed-S2' as DEBT 3 — an act it
       explicitly could not perform.
  M-3  ZF (26f9314b, 2026-08-14) derived the analytic scaffolding
       (normalization, nonvanishing mechanism, F'-14-compliant display)
       and separated the residue into q-1/q-2/q-3 — the spec author's
       acts, irreducibly.
  M-4  This artifact authors exactly q-1/q-2/q-3, from M-1..M-3 and the
       sealed E1 text alone.
THE CHAIN CONSULTS: (i) the quantifier split of record; (ii) what the
defined objects consume; (iii) what the discharged chain delivers. THE
CHAIN NOWHERE CONSULTS THE FIXED/RUNS OUTCOME: no clause of §3-§5 depends
on which horn any discharge would favor; the amendment's target is one at
which REFUTATION is first-class (V-1) and at which certification is NOT
asserted, predicted, or presumed. The per-pair NET lean ("RUNNING as a
lean", AQ §5) is neither cited as motivation nor moved by this amendment.
No output was inspected; no physical quantity appears anywhere in this
artifact; no verdict-bearing computation exists for any clause to have
been shaped by. The amendment is motivated by the sealed architecture —
the split, the kill's location, the discharged delivery — not by which
verdict it enables.
```

---

## 7. HONEST CONSEQUENCE MAP [GROUNDED; neutral]

```text
WHAT ADOPTION ENABLES (and nothing more):
  C-1  THE F'-14 PASSAGE BECOMES TRAVERSABLE: Route B acquires its sealed
       target (S2'/R-L2b^Sigma + R.3') — a road that exists to be walked.
       Walking it is DISCHARGE (a lane's derivation against a now-sealed
       clause), and only discharge retires the witness (W-1).
  C-2  THE "C EXISTS" SENTENCE'S GATE LIST REDUCES. G1's licensed sentence
       (:343-348) gates "C exists as an intensive object at the summed
       quantifier" on {F'-14 + summed-S2' + OBL-D}. On adoption the
       unsealed-spec-act gate is GONE (that is adoption's whole effect)
       and the F'-14 gate becomes this amendment's own discharge
       conditions. The named-gate list of record becomes:
       R-L4a/b + R-L0(/R-L0b) + C-L2 + OBL-D
       — with H-R/H-B0 carried inside as sealed, and the amendment's two
       new witnesses (§5.2 W-2) LIVE as the spec-internal discharge
       ledger of S2'/R.3' itself (§4.4 D-i carrying the sector
       bookkeeping inside the R-L4 family's burden).
  C-3  THE V011 ZERO-FREE REDUCTION BECOMES CONSUMABLE at the amended
       quantifier: ZF's {kappa_bal x e^x < 1} reduction plugs into
       R.3'(d)'s {B < 1} with the cell axis still R-L0's (ZF-CHK
       correction honored).
WHAT ADOPTION DOES NOT DO:
  C-4  NO VERDICT: FIXED is not certified; RUNNING is not refuted or
       advanced; the per-pair refutation (52f2490b) stands untouched; the
       per-pair FIXED horn stays dead as architected; the summed horn
       stays LIVE-AND-GATED, now at a sealed target.
  C-5  NO COMPUTATION: nothing is evaluated; no estimate is performed; no
       constant acquires a value; alpha' remains a symbol.
  C-6  NO FLAG: GOV-F reserved (NF-3); every sealed flag stands.
  C-7  NO DISCHARGE: R-L4a/b, R-L0, C-L2, OBL-D, H-R, H-B0 — all stand
       exactly as sealed; B-L2*'s discharge is not reopened or altered;
       OBL-D continues to ride the transport package unmoved.
  C-8  NO CONSTRUCTION: F0 break [1]'s demand for a sealed construction
       of C is not met, advanced, or restated by this amendment.
```

---

## 8. FLAG BLOCK

```text
Q1_OBJECT = FIXED-n-FAMILY-WITH-n-UNIFORM-CONSTANTS(
  clause S2'-O authored §3.3: Delta^Sigma_n(a) = sum w_mu^* w_lambda
  C_n(V_{mu lambda,n}(a) - V_{mu lambda,n}(0))C_n, colour sum inside at
  the operator level, C4 kill before any norm, cancellation-first list
  extended to the colour sum; certifying claims quantified over the
  n-indexed D3/Hermite-Galerkin family with n-uniform constants — the
  discharged trilemma form — and NO summed limit object defined.
  JUSTIFICATION: the discharged chain delivers exactly sup_n (REM
  :131-134, :380-382, NET); the per-pair carrier limit does not exist of
  record (ZF §4.1(c), 52f2490b) and a spec-defined limit object would be
  an unsupplied construction act (G1 break [1]); the defined objects
  (R_record,L's log of the summed amplitude, post-limit) and V011's
  volume-uniform demand consume n-free CONSTANTS, not a new limit object;
  quantifier continuity with the sealed R-L2b/X_n form makes refutation
  at the new quantifier (V-1) comparable at sight. Per-pair R.0 record
  untouched.)

Q2_ESTIMATE = AUTHORED-WITH-HONEST-DISCHARGE-MAP(
  clause S2'/R-L2b^Sigma authored §4.1: G_hs^Sigma = |C|_4^{-alpha'}
  ||Delta^Sigma_n(a)||_2, colour sum INSIDE the Schatten-2 norm, C4 kill
  BEFORE the norm, two-time object, alpha' NOT asserted (symbol; new
  witness SCAD_SUMMED_HS_UNIFORMITY_UNCERTIFIED); certify sup_n
  ||Delta^Sigma_n||_2 <= |C|_4^{alpha'} G_hs^Sigma uniformly over D3,
  functionals of the frozen tuple only. R.3' closure authored §4.3,
  F'-14-COMPLIANT: assembled-rho displays only, never per-pair logs of
  sums — (a) normalization adopted from ZF §4.2 (N_surv(0)=1/2,
  c=(1/2,1/8x4), kappa_bal=1, conditional on R-L4a/b + C-L2); (b) the
  exact linear passage by trace LINEARITY (colour sum inside the trace);
  (c) the segment remainder as a NAMED require-clause at the summed
  quantifier (new witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED),
  per-pair discharge route BARRED (52f2490b); (d) the perturbation-of-1
  output {B < 1} => nonvanishing + R.3.b-form, per ZF's mechanism.
  DISCHARGES IT NOW: REM 2e4e5163 (CONFIRMED 89641f76) — the all-pairs
  assembled trilemma-form bound, C4 kill before the norm, grade e-i/e-ii
  carried with overturn conditions; ZF 26f9314b (CONFIRMED-with-correction
  aed551e3) — the F'-14-compliant nonvanishing display and exact
  normalization. GATES ITS DISCHARGE (not its adoption; nothing papered
  over): R-L4a/b (sector definition INCLUDING the all-pairs/surviving
  bookkeeping at the amended quantifier — the m_0 factorization break
  o-2(ii) and the per-pair-first obstruction o-2(i) restated verbatim in
  substance at §4.4 D-i); R-L0(/R-L0b) (the cell sup — ZF-CHK's
  landing-set correction honored; summed-S2' does not deliver it); the
  C-L2 input (G_cm/baseline error); H-R and H-B0 carried as sealed.)

Q3_MECHANICS = MANDATE-SHAPED(
  ENTRY: authored-proposed-not-adopted on the ad9fc14e pattern —
  opposite-lane hostile check sealed first; then a SECOND, SEPARATE
  principal act: an entry addendum naming this artifact's digest + the
  check's digest with the principal's explicit confirmation under the
  E1-successor spec-author role; no lane/registrar entry; summaries never
  substitute; authored-as-forced VOIDS. WITNESS RETIREMENT:
  SCAD_COLOR_SUM_LOG_MISUSE retired ON DISCHARGE of S2' + R.3' (with
  D-i..D-iv), NEVER on adoption — a validity bar is passed by derivation,
  not convenience; two new witnesses LIVE from adoption. FALSIFIERS:
  V-1 refutation at the amendment's own quantifier VOIDS (first-class,
  the 52f2490b analogue); V-2 the one-way bridge shown two-way VOIDS
  (the per-pair refutation would transfer); V-3 anchor overturn (REM §5
  conditions / ZF scaffolding) VOIDS the leaning clause; V-4 sealed-bytes
  contradiction VOIDS; V-5 C4-kill failure on the defined object VOIDS;
  V-6 marking fraud VOIDS. NO-FREEZE: no value (alpha' a symbol; only
  the re-used 1/2), no carrier/cellulation datum, no flag flips (GOV-F
  reserved), no frozen definition touched (M(t) sharp; equal-time FALSE;
  C4/C6/D3/weights unchanged), no limit object frozen.)

ANTI_OUTCOME_SHOPPING = DECLARED(
  motivation chain M-1..M-4 stated in order: AQ's settling-act
  determination (pre-dating any summed estimate) -> REM's independent
  discharge carrying DEBT 3 -> ZF's q-1..q-3 separation -> this authored
  act; the chain consults ONLY the quantifier split, what the defined
  objects consume, and what the discharged chain delivers; it NOWHERE
  consults the FIXED/RUNS outcome; refutation at the new quantifier is a
  first-class falsifier (V-1); certification is not asserted, predicted,
  or presumed; the per-pair RUNNING lean is neither cited nor moved; no
  output inspected; no physical quantity appears in this artifact.)

CONSEQUENCE_MAP = NEUTRAL(
  ENABLES: F'-14 Route B acquires its sealed target (traversable, not
  traversed); the "C exists" gate list reduces from {F'-14 + summed-S2' +
  OBL-D} to R-L4a/b + R-L0(/R-L0b) + C-L2 + OBL-D (the spec-act gate gone
  by adoption; the F'-14 gate becoming the amendment's own discharge
  conditions, ledgered by its two live witnesses); ZF's zero-free
  reduction becomes consumable at the amended quantifier. DOES NOT: no
  verdict (FIXED uncertified, RUNNING unadvanced, 52f2490b untouched);
  no computation (nothing evaluated, alpha' a symbol); no flag (GOV-F);
  no discharge of R-L4a/b / R-L0 / C-L2 / OBL-D / H-R / H-B0; no
  construction of C (F0 break [1] unmet); B-L2*'s discharge unaltered.)

STATUS = AUTHORED-PROPOSED-SPEC-NOT-ADOPTED

SEALS_VERIFIED = 10/10(
  26f9314b ZF; aed551e3 ZF-CHK; 0a10c030 AQ; 331035d3 AQ-CHK; 2e4e5163
  REM; 89641f76 REM-CHK; 46846730 E1; ad9fc14e ATTACH-DoR; df4514a5 G1;
  52f2490b RL2B-REFUTED — all recomputed from bytes at path this session,
  all matching sidecars and tasked prefixes; none unverifiable.)

FORBIDDEN_IMPORTS = none(
  no scale, no imported GR, no faithfulness authority; no physical
  quantity computed, bounded, or evaluated — exact symbolic clause
  content only; no register/tracker/plan/road/ledger/lens file read;
  no number introduced anywhere.)

MACHINERY_INVOKED = no(
  no CAS, nothing executed beyond shasum -a 256, file reads, and greps
  for seal and span verification.)

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
SUMMED_S2_CANDIDATE = SEALED (as AUTHORED PROPOSED SPEC CONTENT ONLY).
```
