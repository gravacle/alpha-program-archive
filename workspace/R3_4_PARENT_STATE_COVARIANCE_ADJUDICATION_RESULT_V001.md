# R3.4 Parent-State Covariance Adjudication Result v001

## Verdict

```text
PARENT_STATE_COVARIANCE_CURRENT_PARENT_BLOCKED
```

The Parent-State Covariance Principle is adopted and sealed. The current
parent lineage does not yet pass it.

## Exact static-parent check

For the one-cell incidence generator and completed public-label projector,

```text
||[c_partial,P_p]||_F=sqrt(2).
```

The executable reproduces this with zero displayed error. The completed
label is therefore not invariant under the existing static parent.

## Authority correction

The existing redundant-record result says it uses the "already adopted"
Primitive Reversible Record-Write Principle. That statement is stale:

```text
PRIMITIVE_REVERSIBLE_RECORD_WRITE_PRINCIPLE_V001.md
  is superseded;

PRIMITIVE_REVERSIBLE_RECORD_WRITE_PRINCIPLE_V002.md
  has candidate_is_active_Level_1_postulate = false.
```

The live source-flux result instead has:

```text
complete_physical_write_operator_derived = false;
complete_source_record_environment_operator_derived = false.
```

Accordingly, the prior redundancy lane remains a correct conditional
calculation for its declared write map, but it is not an unconditional
parent-derived durability result.

## What the new principle does and does not select

An exact negative control constructs two target-free finite local parent
nets. Both have:

```text
zero interior-derivation embedding error;
zero compatible tracial-state restriction error;
and one covariant inductive-system realization.
```

Their local responses nevertheless differ, with Frobenius difference

```text
8 sqrt(2) = 11.313708498984761.
```

Thus Parent-State Covariance is a real consistency constraint, but it is not
a microscopic-action uniqueness theorem.

The primitive Pauli completion is excluded inside the separately adopted
transport-only/single-operator branch. That exclusion is not derived from
Parent-State Covariance and is not relabeled as such.

## Reopen route

The strongest live replacement candidate is the later derived charged
controlled incidence operator

```text
B_ch=P_ch tensor B_Q.
```

Its target-free audit passes while retaining

```text
complete_connected_source_record_action_derived = false.
```

The next gate must determine whether its exact first-opening unitary,
charge-superselected finite states, and causal cell support produce one
compatible outgoing state and dynamics. It must not reuse the retired
controlled-X premise.

## Status

```text
parent_state_covariance_principle_adopted = true
static_parent_preserves_completed_label = false
superseded_write_rule_used_by_old_redundancy_lane = true
parent_state_covariance_selects_unique_parent = false
pauli_exclusion_derived_from_parent_state_covariance = false
live_complete_scattering_parent_derived = false
parent_to_outgoing_limit_derived = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
