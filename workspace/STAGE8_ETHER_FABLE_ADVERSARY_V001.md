# STAGE 8 — ETHER ADVERSARY: BREAK THE BARE-SURFACE COUPLING DETERMINATION ON BEDROCK
## FABLE-ADVERSARY-ETHER — ADVERSARIAL REVIEW OF FABLE-ETHER-1114 — [CLAIMED]

Date: 2026-08-13
Role: ADVERSARIAL DETERMINATION / TYPING ONLY. No value of n_ch, kappa, alpha, or any
constant is computed, bounded, estimated, or compared. Every quantity symbolic.
Machinery (carrier, K_KK, fiber radius, metric, ell_P, Thomson, KK action,
"alpha rides a scale") is invoked neither to attack nor to defend.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false` ; `coupling_evaluation_authorized = false`

---

## 0. VERDICT IN ONE LINE

**The three slot-closure legs HOLD on bedrock — I strengthen them below — but the
generator step does not: nothing on bedrock forces |n_ch| = 1. Every sealed
failure-capable test the bare surface owns passes verbatim under n -> m·n for any
nonzero integer m; the sealed |n| = 1 result is conditional on a faithfulness
hypothesis that lives on the premise side (DoR-008's ratified faithful carrier;
the Stage-1 PREMISE disposition); and the subject's attempt to re-derive
faithfulness as "the defining content of a record" is a definitional upgrade of
the surface's own variation-based access test into an injectivity requirement no
bedrock structure imposes. The bedrock-determined content is n_ch in Z \ {0}
up to orientation — a discrete free dimensionless datum (the winding magnitude)
remains. DETERMINED is over-read. OVER-CLOSURE-CONFIRMED.**

---

## 1. SOURCES READ, SEALS RECOMPUTED AT PATH

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every hash below
was recomputed at path with `shasum -a 256` immediately before reading and again
immediately before this artifact was written; every one matches its expected
value byte-for-byte (comparison made programmatically, not by eye).

| # | Source | SHA-256 (recomputed at path) | Match |
|---:|---|---|---|
| SUBJECT | `STAGE8_BARE_SURFACE_ALPHA_DETERMINATION_FABLE_V001.md` | `82e81f6e659108c05872d30d4c5ad00bf66cad3c272c4aa8240aa89fc5a62010` | YES |
| S4 | `STAGE8_TASK2D_FINITE_COUPLING_FAMILY_FORCING_PROTOCOL_RESULT_V001.md` | `92c821001268a57b638fa42639dbed3926ecfc439ba5f3479182bcab9b152351` | YES |
| S3 | `BID_UNIQUE_CHARGED_CONTROLLED_COUPLING_DERIVATION_V001.md` | `b786db3adec8cc335967d49ec13b59923d67f424644f72c535b27b579dd1489f` | YES |
| S1 | `STAGE8_SADDLE_FOUNDATION_PARENT_ACTION_DARIO_V001.md` | `1d11f15040f8b85b7e081fccfeddb995c41941c55464d759a2fa91a8feffc775` | YES |
| S2 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | YES (matches S1 member-03 row and the subject's S2 row) |

S2 was read in addition to the four assigned sources because the subject's legs
(b) and (c) and its R1/R3 residuals rest on S2's displayed identities; attacking
on the subject's paraphrase alone would not have been a bedrock attack. S2 is a
workspace artifact, not a barred file.

No register, tracker, plan, road, or ledger file was read. No machinery object
was read for content. S3's interval crosscheck (`tau_R`) was not used.

---

## 2. THE CLAIM, AS THE SUBJECT MAKES IT

The bare surface determines its native coupling as a pure number: n_ch, the
charged-sector holonomy winding index, forced to the character-lattice
GENERATOR (unit winding) up to orientation, by (a) the no-multiplier theorem
(S3, zero-dimensional multiplier space), (b) character-lattice quantization
(C7 + C4), (c) unit-modulus exactness (S2). Standing TYPE-P on DoR-008,
DoR-009. Honest residual (S4): the attachment-placement census and the
orientation (non-)identification.

---

## 3. WHAT HOLDS — AND I MAKE IT STRONGER THAN THE SUBJECT DID

I attacked the continuous slot first, hunting the F6b pattern (an over-general
universal with an untested surface-native slot). I constructed the strongest
bedrock counterexample candidate I could build, and it DIES on bedrock.
Recording it, because it is the exhaustiveness proof the subject's leg (b) only
sketched:

**The leak-write candidate.** Nothing in S4's open census forbids a record write
that rotates the ready state partially into the third record configuration with
a history-dependent angle: symbolically, a unitary per-cell write `W(h)` with
`W(identity) = S` (so the standing one-cell falsifier still passes at zero
history) whose doubled kernel is a real-amplitude function `k(h_+, h_-)` with
modulus at most one, continuously deformable — a surface-native continuous
"form factor" that is NOT a multiplier in S3's sense (it sits inside the write,
not in front of the compression) and is NOT a non-integer character power (it
is not of the form `z^lambda` at all). It even passes the equal-history
identity automatically, by unitarity alone. If admissible, this would refute
leg (b)'s exhaustiveness exactly the way Codex broke F6b.

**It is killed by bedrock, in three forced steps.**

1. Product structure of the sandwich (tensor writes, product ready state)
   factorizes the doubled kernel per cell; Cauchy-Schwarz on the sandwich
   bounds each per-cell factor by unit modulus. (Incidence + two-branch
   structure only.)
2. C4, the sealed common-gauge rule (arbitrary common endpoint elements acting
   on both branches), forces each per-cell kernel to depend on the RELATIVE
   holonomy alone: `K = k(delta)`. For the leak-write, invariance of the
   rotation-angle difference under all common endpoint actions forces the angle
   function to be an additive homomorphism from a COMPACT group into the reals
   — which is trivial. The leak-write collapses to the history-independent
   baseline and is then eliminated by the surface's own charge/flux-access
   requirement (S4 Section 4.1). The continuous form-factor slot does not
   exist.
3. C7 in its genuine composition sense (refining one cell into two composes
   holonomies and multiplies responses — the reading the subject's own leg (b)
   requires, and which the bedrock fence grants) forces
   `k(delta_a · delta_b) = k(delta_a) · k(delta_b)`: `k` is a homomorphism into
   the nonzero complex numbers with bounded image, hence into the unit circle
   by compactness — i.e., exactly a character `chi_n`, `n` an integer. This
   re-derives legs (b) and (c) together.

Additionally, a constant unimodular prefactor common to both branches cancels
identically in the doubled relative kernel (it is branch-common slack, not a
coupling), and a constant modulus different from one violates unitarity of the
write. So leg (a) is confirmed twice over, independently of S3's adopted
projection-module axioms.

```text
NO_CONTINUOUS_SURFACE_SLOT = CONFIRMED, and now exhaustively: the COMPLETE
  admissible per-cell response set on bedrock is { chi_n : n in Z }, derived
  from incidence + C4 + C7 + compactness alone. Legs (a), (b), (c) all hold.
  My strongest continuous counterexample candidate is refuted by the surface's
  own structure. This half of the subject is not over-closure; it is under-
  claimed if anything.
```

---

## 4. THE HOLE — THE GENERATOR STEP IS NOT BEDROCK

Section 3 leaves the admissible set `{ chi_n : n in Z }`. The surface's own
sealed eliminative requirement — the charge/flux-access test, S4 Section 4.1 —
is VARIATION-BASED: it eliminates the baseline because it "has zero variation
under every external history." That test kills exactly `n = 0` and nothing
else: every response with `|n| >= 2` varies under generic histories and PASSES
it.

**4.1 The n-blindness of the entire sealed battery.** Substitute `chi_m` for
`chi_1` (m any nonzero integer) throughout S2 and check every sealed
failure-capable test, line by line:

```text
C1 zero-source exact write      D_n at zero history = identity for EVERY n
                                                          -> PASS, n-blind
Equal-history identity          unit modulus, any n       -> PASS, n-blind
C3 dephasing                    <R_N|W_N|R_N> = 0 from the off-diagonal
                                write, any n              -> PASS, n-blind
C4 gauge covariance             the endpoint action t -> chi_n(t) is a
                                representation for every n; the relative
                                kernel cancels common gauge for every n
                                                          -> PASS, n-blind
C7 zero-extension               chi_n(identity) = 1 for every n
                                                          -> PASS, n-blind
One-cell standing falsifier     evaluated AT zero history, where all n agree
                                                          -> PASS, n-blind
CTP Hermiticity                 conjugation symmetry, any n
                                                          -> PASS, n-blind
Charge/flux access              nonzero variation, any nonzero n
                                                          -> PASS for all n != 0
```

The bare surface, interrogated by every failure-capable test it owns, CANNOT
DISTINGUISH the generator from any other nonzero lattice point. This is
checkable by anyone against S2 Sections 3 through 5: no displayed identity uses
|n| = 1; each uses only unit modulus and the homomorphism property.

**4.2 Where |n| = 1 actually comes from.** The subject's Section 5(a) cites
S4 Section 3.1, which cites `STAGE1_PREMISE_DISPOSITION_V001.md:15-47` as
proving "that a faithful one-dimensional character has |n| = 1." That sealed
result is a CONDITIONAL — elementary character theory: IF faithful THEN
|n| = 1. The hypothesis, faithfulness, is carried entirely on the premise side:

- DoR-008 is, in S2's own authority table, the "Ratified CTP/character
  carrier" — and S2 constructs `F_N` ONLY "for each faithful character
  n in {+1,-1}" (S2 Section 0). The faithful pair is the RATIFIED CARRIER'S
  content, consumed as premise. S2 Section 9: "n = +1,-1 are faithful character
  orientations."
- The conditional's source file is literally named PREMISE_DISPOSITION, and per
  S4 3.1 it "chooses positive orientation as the convention" — disposition and
  choice language, not forcing language.

**4.3 The subject's bedrock substitute fails.** Section 5(a) asserts:
"Faithfulness is not an extra axiom here; it is the defining content of a
RECORD of the connection: a non-faithful response is constant on kernel cosets
— precisely those source histories the record would fail to record." Two
defects, both on bedrock:

1. It is not what the surface's own eliminative test says. The sealed test is
   access/variation, and it kills n = 0 only. Upgrading "must vary" to "must
   separate every pair of holonomies" (injectivity) is a NEW requirement. No
   sealed source states it; the subject cites none; the surface's structure
   nowhere imposes it. A winding-two record does not "fail to record" — it
   records every history, ambiguously up to the finite kernel pair. Whether
   ambiguity-up-to-a-finite-subgroup disqualifies a record is a normative
   reading of the word "record" — a definition adopted, not a theorem proved.
   That is precisely the shape of an over-closure: the desired conclusion
   smuggled in as the meaning of a word.
2. The one escape — record-native operationalism ("the connection IS whatever
   the record registers, so the effective holonomy group is the quotient, and
   any response is tautologically the faithful generator OF ITS OWN quotient")
   — does not rescue DETERMINED; it dissolves it. The corpus fixes a PRIOR
   lattice with a named centered generator (the sealed
   `PRIMITIVE_RELATIVE_PHASE_CONNECTION` source, cited by S4 3.1 and by the
   subject's own Section 3). Relative to that prior lattice, the operationalist
   move makes "unit winding" true by definition and empty of content, while the
   physical freedom reappears one level down as the unrecorded index of the
   quotient embedding — the same free integer. Either the lattice is prior and
   |n_ch| is undetermined, or the lattice is operational and "n_ch = generator"
   determines nothing.

**4.4 Consequence.** On bedrock the determination terminates at:

```text
n_ch in Z \ {0}, up to orientation conjugation.
```

Existence: YES. Discreteness: YES. No continuous dial: YES (Section 3).
Uniqueness: NO. The winding MAGNITUDE is a free discrete dimensionless datum of
the bare surface. A free integer is a free dimensionless coupling datum — the
one thing the claim says the surface does not leave. Distinct magnitudes are
physically distinct on the surface's own terms (their influence kernels differ
at generic histories, symbolically, no evaluation needed), so this is not a
labeling redundancy like the sign.

---

## 5. THE RESIDUAL (S4) — HALF CONVENTION, HALF THE SAME HOLE COMPOUNDED

**5.1 Orientation half: NOT a coupling freedom.** S4 Section 3 shows U1 reality
and CPT are intertwiners exchanging branch/orientation/charge — the pair is
symmetry-related, not identified, and the invariant magnitude is unaffected.
The subject carries this correctly and does not quotient it. No attack lands
here; the sign is convention at the invariant level.

**5.2 Census half: a genuine freedom, and the subject's closure of it is the
exact move S4 bars.** S4 seals: `SURVIVOR_COUNT = NO_VERDICT`;
`TOTAL_ATTACHMENT_FAMILY_CARDINALITY = NO_VERDICT`; "no exhaustive attachment
grammar"; and — decisive — "Counting only the relay's labels would turn the
missing coverage proof into a physical selection rule" (S4 Section 6). The
subject's R1(ii) does exactly that: it counts the response classes of the three
INSTANTIATED record-write fixtures (`K_post = K_conj =` unit-winding character,
`K_pre =` trivial) and concludes "every instantiated placement that interacts
at all carries the SAME dimensionless invariant." Over the fixture family,
true. As the universal the verdict needs — over an open census S4 explicitly
refuses to bound — unproved. By Section 3 above, every admissible attachment
carries SOME `chi_n` (same TYPE); nothing bounds it to the SAME VALUE
(unit magnitude), and nothing sealed bounds the number of attachments per cell,
so the per-cell TOTAL winding — which is what the influence kernel actually
carries — is an uncounted integer. S4's further remark that even "a continuous
distribution of a connection term among those locations is not excluded by any
entered source" is over-cautious on bedrock (Section 3 closes the continuous
direction), but the DISCRETE freedom it gestures at is real, and it is the same
free integer as Section 4. Two independent routes, one hole.

The subject's R1 sentence "the placement freedom is DISCRETE and carries no
strength dial" is therefore half right: discrete, yes; but a free integer IS
the strength freedom the surface admits — calling it "no strength dial" because
it fails to be continuous is the flattening the subject's own Section 9
promised not to perform.

---

## 6. WHAT THE HONEST VERDICT WOULD HAVE BEEN

```text
SURFACE_DETERMINES_COUPLING = DETERMINED-UP-TO-A-FREE-POSITIVE-INTEGER:
  the continuous slot is refuted (theorem-grade, three legs, independently
  re-derivable from C4 + C7 + compactness); the trivial character is refuted
  (access test); the sign is orientation convention; and the remaining
  magnitude — |n_ch| a positive integer — is fixed on the PREMISE side only,
  by DoR-008's ratified faithful carrier, not by the bare surface.
```

That verdict would have been strong, honest, and new — and the ether reading
survives in it almost intact: no continuous dial, no scale, nothing for a scale
to ride. What does not survive is "forced to the generator": the surface
refutes the DIAL but does not pick the TOOTH. The subject crossed that gap with
a ratified premise re-labeled as bedrock ("Faithfulness is not an extra axiom
here") plus a survivor count over a family its own residual source refuses to
close. That is the lineage failure mode, landed on the most-hoped-for answer:
the claim's headline words — DETERMINED, "a pure number," "forced to the
generator" — are carried precisely and only by the two non-bedrock steps.

---

## 7. WHAT THIS ARTIFACT DOES NOT SAY

```text
IT DOES NOT say the subject's slot-closure is wrong — Section 3 strengthens it.
IT DOES NOT say unit winding is false, or assign |n_ch| any value; it says the
   BARE SURFACE does not force it — the forcing lives in DoR-008 and in an
   adopted definition of "record."
IT DOES NOT evaluate any coupling, kappa, alpha, response, or constant.
IT DOES NOT invoke any machinery object to attack or defend; every objection
   stands on characters, incidence, C4, C7, compactness, and the surface's own
   sealed access test.
IT DOES NOT quotient the orientation pair, and it CONFIRMS the sign residual
   as convention, against my own brief's suspicion.
IT DOES NOT touch S1's import-block or anything metric-side.
IT DOES NOT register, commit, or push anything.
```

---

## 8. FINAL FLAG BLOCK

```text
RESIDUAL_IS_A_COUPLING_FREEDOM = YES(the per-cell total winding magnitude — a
  free positive integer left open by the uncounted attachment census; the
  orientation half of the residual is convention, NOT a freedom)

FAITHFULNESS_BEDROCK_FORCED = REFUTED(faithfulness enters as DoR-008's ratified
  faithful character carrier plus the Stage-1 premise disposition's hypothesis;
  the surface's own sealed eliminative test is variation-based and passes every
  nonzero winding; the subject's "defining content of a RECORD" is an adopted
  injectivity definition, not a structural forcing)

NO_MULTIPLIER_EXHAUSTIVE = CONFIRMED (for its stated scope — continuous
  multipliers/strength dials; independently re-derived here from C4 + C7 +
  compactness, which also kills the strongest surface-native continuous
  counterexample candidate, the leak-write form factor; the freedom the
  determination misses is discrete and lies outside the multiplier theorem's
  scope, which S3 itself never overstated)

DETERMINATION_SURVIVES_WITHOUT_PREMISES = NO(DoR-008 — the ratified FAITHFUL
  character carrier is what restricts the winding to the unit pair; strip it
  and bedrock leaves n_ch in Z \ {0}: discreteness, no-dial, and unit modulus
  survive; uniqueness does not)

BEDROCK_HOLE = FOUND(the generator step: unit winding magnitude is not
  bedrock-forced; every sealed failure-capable test of the bare surface is
  n-blind — passes verbatim under n -> m·n for any nonzero integer m — so the
  winding magnitude is a free discrete dimensionless datum of the surface,
  fixed only by premise; compounded independently by the open attachment
  census, where the subject's same-invariant universal is a survivor count over
  a family S4 seals as non-exhaustive and explicitly bars from being counted)

MY_OBJECTION_IS_MACHINERY_BASED = no

VERDICT = OVER-CLOSURE-CONFIRMED (a bedrock hole exists; DETERMINED is
  over-read: the surface refutes the continuous dial but does not select the
  lattice point; "forced to the generator" is premise re-labeled as bedrock
  plus an unlicensed universal over an open census)

alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false ; coupling_evaluation_authorized = false
NOT_REGISTERED_NOT_COMMITTED_NOT_PUSHED = true
ALL_RESULTS = CLAIMED until the external-lane check.
```
