# STAGE 8 — EXTSRC ROUND 3: ADVERSARIAL AUDIT — V001

## ADVERSARIAL AUDITOR — CODENAME EX3-AUDIT — [SEALED]

Date: 2026-08-15 (session UTC). Target: `STAGE8_EXTSRC_ROUND3_S9AD_V001.md`
(`824eb72f…`, sidecar MATCH, verified at path before opening). Default
posture: REFUTE. Stakes symmetric: a false INHABITED grounds an authoring
act on a phantom; a false EMPTY kills two families and steals ACT-1.

Fences held throughout: `alpha_computed = false` ; `proof_authorized =
false` ; `kappa_record_computed = false`. DETERMINATION-AUDIT ONLY: no
member bound, no adoption, no authored physics, no route/branch/address/
representative selected, no score/gate moved; the principal's acts
untouched. The held construction `506bfe2a…` untouched, unopened,
unpresupposed. No fenced-class file opened (register|road_|ledger|lens|
plan|tracker|THE_HANDOFF); "Q-…" tokens EXPECTED-UNLOCATABLE (cited only
as carried inside sealed artifacts). No git; ONE output file plus seal
sidecar; output path probed ABSENT at session start and re-probed ABSENT
at 2026-08-15T05:17:42Z immediately before this write. Bytes-level +
exact symbolic only; own battery in a FRESH venv (sympy 1.14.0; 26/26
PASS; exact integers/rationals/symbols; no float; no value frozen).

**SWEEP CUTOFF: 2026-08-15T05:17Z** (S-1..S-5 below). Exhaustiveness is
sweep-displayed only.

---

## 0. VERDICT

```text
VERDICT = CONFIRMED-WITH-CORRECTIONS

The round-3 determination STANDS: B-1 = UNDECIDABLE-STILL is correct,
honestly reasoned, and correctly scoped; neither INHABITED nor EMPTY is
reachable at today's bytes; the reduction (INHABITED <=> no mixing
relation, at dim M_N >= 2 + TOP-1/TOP-2, at the displayed census) is
sound as scoped; W-EX3 is a lawful conditional schema, not a smuggled
member; NG-3' is sound at its stated grade; rounds 1-2 are respected at
their own walked bytes. Four corrections, C-1..C-4 (§4), all
verdict-neutral; none moves the menu outcome, the reduction, or any gate.
```

## 1. INDEPENDENCE PROTOCOL (executed before the build was opened)

```text
The fiber question was worked independently under the fixed pins (PIN-1
ACTUAL, PIN-2-HOL zero-admitted, PIN-3 MANDATORY, PIN-2-H free/both) from
rounds 1-2 + the OB-2/PIN/B-3 chain, WITHOUT opening the round-3 build.
Pre-verdict FIXED and hashed BEFORE the build's seal was even checked:

  SHA-256(EX3_AUDIT_PREVERDICT.md)
   = d42cf82b2f39c4be6b7a454fd9c21055d7a270179ec31015bb9e4d52a6ed6b17
  fixed at 2026-08-15T05:09:18Z (build had been written 05:04:00Z;
  unopened and unread until 05:10Z+).

PRE-VERDICT CONTENT (summary): UNDECIDABLE-today at the named bits —
(i) the mixing-relation existence/value bit (B-3(ii)); (ii) dim M_N
(B-3(iii)); (iii) the (SA-1) closure — WITH the pins' yield accounted:
at the finite-rank census every linear map is continuous, subspaces are
closed, quotient = subspace topology, so rounds 1-2's topology-
certificate stops DISSOLVE at the existence quantifier; no-mixing =>
stage-local data exists; mixing-with-zero-value => EMPTY (NG-3, premises
met via PIN-1); mixing-with-nonzero-value => kappa_T,N forced equal to a
nonzero path-bilocal kernel, killing at dim >= 2 (the NG-1-grade
import). Audit-failing patterns pre-declared for each menu branch.

MATCH WITH THE BUILD: verdict identical (UNDECIDABLE); yield accounting
identical in every load-bearing step, including the independently
pre-derived NG-3' widening. DIVERGENCE, in the BUILD'S favor: my
pre-verdict PRESUMED O_R5,N Hausdorff; the build refused the presumption
and surfaced TOP-1/TOP-2 as new named finite displays — verified genuine
(§3.5). The build is STRICTER than my independent run, not looser.
```

## 2. SEALS, BYTES, PROBES (this session, at path)

```text
824eb72f  STAGE8_EXTSRC_ROUND3_S9AD_V001.md      MATCH (sidecar verbatim)
Re-hashed ALL 12 artifacts the build relies on: c8053f6c, d9f4aad0,
09047c2a, abe71688, 98ee31ba, 68202c7b, 075a2b16, d662889b, fdf20bd4,
5de94e16, ccb94dfa, feb84216 — 12/12 MATCH, zero mismatches.
Output-path uniqueness: no competing round-3 artifact (S-4); the build's
write time (05:04:00Z) postdates its own final ABSENT probe (05:00:07Z)
— probe discipline consistent at file mtimes.
Prior audits' verdicts verified at bytes: OB-2 audit and PIN audit and
B-3 audit all CONFIRMED-WITH-CORRECTIONS as the build states them.
```

## 3. THE ATTACKS RUN (commissioned battery)

### 3.1 Attack on an INHABITED reading — the joint-fiber bar

Not applicable as a kill: the build asserts NO inhabitance. W-EX3 was
nevertheless attacked as if promoted: (a) it is ONE tuple — arms 2-4 are
built from arm 1's S_N and share D_N^ker/kappa_T,N/D_N^Loc, so no
separate-satisfaction promotion exists to void; (b) every free choice is
a ledgered existential coordinate (CL-3), none bound — no smuggled
member binding found; (c) its conditionality is displayed AT the claim
sites (no-mixing; TOP-1/TOP-2; overlap; dim where used), and §4.6
withholds closure carriage explicitly. PIN-2-H both-readings validity:
verified structurally — no arm consumes (JD-3)'s words. CLEAN.

### 3.2 Attack on an EMPTY reading — the quantifier distinction

Not applicable as a kill: no emptiness is asserted. The build's §5
respects the obstruction's own distinction: NG-3 held at its undecided
antecedent; NG-3' correctly gated on dim >= 2 with the import tagged at
OB-2 NG-1's own grade; §5.4's clause-shape consistency proof (which
BLOCKS shape-only emptiness routes) independently rebuilt and PASSED
(battery D1-D12: one joint two-stage tuple satisfying every displayed
B-1 clause shape — one value on a pure-cycle-only ideal, nonzero
non-overlapping kappa, (A1-13) equality, full addressed chain with
non-vacuous PhysAgree and the zero-cheat refuted, both (S23) squares,
S22a). No TYPE-U stop is anywhere promoted to emptiness. CLEAN.

### 3.3 The UNDECIDABLE verdict — is the pins' yield honestly accounted?

This is where the build had to survive, and it does:

```text
u1 (common carrier dissolved by PIN-1)      VERIFIED — (S2b)/(S4)/post-
   (S8) forced semantics land both (SPEC-11b) branches in the actual
   bilocal carrier; the round-1 gap was a derivability gap.
u2 (topology certificates at the census)    VERIFIED — E_N Hausdorff
   finite-dim (dim <= 5, battery B2) => qtilde continuity, Rel_N =
   ker(qtilde) (T.3), kappa_N continuity, (T7)/(A1-12) equality all
   follow from tagged standard TVS facts; the EMBEDDING is correctly NOT
   claimed and correctly gated on TOP-1. My own sweep confirms TOP-1
   undisplayed (SEAM1 :489 names "the physical O_R5,N topology"; no
   separation byte anywhere; S-3).
u3 (addressed chain mod TOP-2)              VERIFIED — (A1-15m)/(A1-18m)/
   (A1-20)/(A1-21a) walked at V001 bytes; (A1-21b) verified CONDITIONAL
   at its own bytes ("Where a future G5/source-reversal member is
   admitted…") so vacuous satisfaction is lawful; the finite-dim target
   turns round-2's vector-valued Hahn-Banach objection into finitely
   many scalar extensions, correctly gated on TOP-2 (BANACH feb84216
   :365 verified a requirement row, final column NO — not a
   certificate); the zero-cheat refutation is non-vacuous (battery D9).
u4 (transport bases mod overlap)            VERIFIED — (JE-0)/(JE-1a)
   verified verbatim at fdf20bd4 :1266/:1289; defining-square
   construction well-posed given the overlap bit; overlap bit correctly
   isolated (V001 :326 "not asserted to be direct" verified); S22a
   composable-pair emptiness correct on the four anchor stages.
u5 (one value <=> no-mixing)               VERIFIED INDEPENDENTLY —
   battery C1/C2: a mixing relation forces kappa = -v/a uniquely;
   multi-mixing consistency is exactly the pure-cycle discharge; the
   pure-cycle branch vanishes exactly (battery A1-A3, symbolic t, rank-1
   + generic SPD + q=2 image). The biconditional's dim-scoping is
   correctly stated at §4.2.
d1-d4 (the deciders)                        VERIFIED all genuinely
   undisplayed by my own sweeps S-1/S-2/S-3 (cutoff 05:17Z): no mixing
   display, no dim M_N byte, no O_R5,N separation/convexity byte, no
   competing artifact; the (SA-1) closure openness carried unchanged.
BOUNDARY CALL — the one place a wrong scope flips the verdict: charging
   (A1-14)/(JB-2) psiDO eligibility to B-1 would force EMPTY at dim>=2
   regardless of mixing. VERIFIED AT BYTES: V001 §1.2 (:331-:336)
   "recorded here to preserve the package boundary but is NOT charged to
   Joint A or to the HOL composite (A1-1)"; :594 repeats it. The build's
   CL-7 scope reading is the sealed boundary, not a convenience. And the
   build SURFACES the void-3 scope residue (§5.3) rather than deciding
   it. CLEAN.
```

### 3.4 Rounds 1-2 respected

No round-1/2 proven display is contradicted at its own walked bytes: the
metric-section failure (M19), the quotient-inverse maximality, the (B3)
open line, and every per-attack gap stand; the round-3 discharges run at
the EXISTENCE quantifier on new ground rounds 1-2 did not have (the
fixed pins; the B-3 finite census) — the re-scoping OB-2 §3.2 itself
licensed. The V002 checkpoint was honored (principal's commissioning).

### 3.5 My own battery and sweeps

```text
BATTERY (fresh venv, sympy 1.14.0): 26/26 PASS — (R4)/(R6)-(R7) exact
  vanishing (symbolic t; rank-1, generic SPD, q=2 image); census dims
  and the full NG-2 table rows (real sharp 4<=4 at rank 2; thresholds 8
  and 6 re-derived); arm-1 forcing and consistency algebra; my OWN joint
  two-stage clause-shape model (independent of the build's B5, same
  conclusion); NG-3' forcing shape (CAS half).
SWEEPS S-1..S-5 (roots = workspace, supervision, cleanroom_output,
  relay_inbox, relay_outbox, minus fenced classes; the historical
  cleanroom path is absent on disk per the PIN CL-1 display): mixing —
  known lineage only; dim M_N — S9AD set only; O_R5 topology — no
  certificate; round-3 — target only; post-cutoff landings — see C-4.
```

## 4. CORRECTIONS (all verdict-neutral; none fires)

```text
C-1  THE O-2 OMISSION (the one substantive miss). The B-3 AUDIT
     (68202c7b, which the build consumed "at bytes") carries observation
     O-2: KER :120's sealed typing "globally hyperbolic oriented
     Lorentzian U(1) backgrounds" + the standard signature convention
     (1, n-1), n >= 2, pins dim M_N >= 2 as an external CONVENTION
     import — explicitly "the same grade as NG-1's microlocal import"
     and explicitly flagged "worth a line when NG-1 is next touched."
     The round-3 build touches the dim M_N cell CENTRALLY (NG-3', the
     overlap bit, the reduction's operative branch, the void-3 note) and
     freely uses the same-grade wavefront import (§4.5/§5.2) — yet
     nowhere carries or even cites O-2. The import standard is thereby
     asymmetric: one same-grade import consumed, the other silently
     dropped. UNDER O-2's import the dim>=2 branch of the reduction is
     the operative one, which STRENGTHENS the build's own payoff
     (INHABITED <=> no-mixing, mod TOP-1/TOP-2, without the dim fork);
     the verdict is unchanged either way (the decider is still the
     mixing bit + TOP-1/TOP-2). REPAIR: registrar to carry O-2's line
     alongside the round-3 reduction; a sealed convention pin remains
     the clean discharge (it is a pin determination, not a lane call).
C-2  CAS NOT DISPLAYED VERBATIM. "27/27 checks PASS" is asserted with
     no battery code inline or in a sidecar; the commissioned verbatim
     re-execution is impossible. Mitigated: my independent battery
     reproduces every named exact-symbolic claim (26/26). REPAIR: future
     S9AD builds sidecar the battery source (the B-3 build has the same
     defect; pattern-level repair).
C-3  FLAG-BLOCK SCOPE DROP. §0 states the reduction "at the displayed
     census, under the fixed readings, modulo the named premises and
     tagged imports"; the §6 NET REDUCTION line and the flag-block
     REDUCTION line drop the census/B-1-scope qualifier ("decides B-1
     essentially outright"). A flag-block-only consumer could over-read
     past the (SA-1) boundary the build itself names at §4.6/d4.
     REPAIR: read the flag line WITH §0's qualifier; registrar note.
C-4  CUTOFF-WINDOW STALENESS (note-grade; discipline held). The
     principal's PIN-2-H fixing (READING R-A, supervision, sealed
     05:02:33Z per mtime) landed AFTER the build's declared cutoff
     (05:00Z) and BEFORE its write (05:04:00Z): the §0 "PIN-2-H FREE"
     line was two minutes stale at write. VERDICT-NEUTRAL BY THE BUILD'S
     OWN STRUCTURE: every arm is displayed identical under R-A/R-B (no
     arm touches (JD-3) — verified), so the fixing changes nothing in
     B-1; the fixing's own scope line ("fixes the H-tail membership
     quantifier only") concurs. The two other post-cutoff landings
     (MO4_CANCELLATION, WALL_SUMMED_REQUANT) touch no deciding bit
     (swept). REPAIR: registrar records that round 3 is R-A-compatible
     as built; no rebuild needed.
```

## 5. WHAT WOULD HAVE REFUTED (and did not fire)

```text
R-a  (A1-14) charged to B-1, or its exclusion unsupported at V001 bytes
     — DID NOT FIRE (boundary verified verbatim, §3.3).
R-b  Arm-1 biconditional wrong (e.g. multi-mixing inconsistency missed,
     or pure-cycle discharge assumed rather than derived) — DID NOT
     FIRE (battery C1/C2/A1-A3).
R-c  Finite-dimensionality misapplied (e.g. Euclidean-domain continuity
     used where the DOMAIN topology is the unproved one; TOP-1 needed
     but silently assumed) — DID NOT FIRE: the build gates exactly the
     steps that need TOP-1/TOP-2 and frees exactly the steps that do
     not (checked step by step, §3.3 u2/u3).
R-d  W-EX3 promoted past its conditionality, past the census, or into a
     member — DID NOT FIRE (§3.1).
R-e  NG-3' claimed PROVABLE outright or at dim-1, or its import
     untagged — DID NOT FIRE (§5.2 gates and tags correctly).
R-f  A sealed byte deciding a "d" item existing before the cutoff and
     missed — DID NOT FIRE (my own sweeps at 05:17Z concur with SW-1..6).
```

## 6. CHOICE LEDGER (terminal; per the guard)

```text
CL-1  CORPUS BOUNDARY: the five archive roots minus fenced filename
      classes; a32_holdout not content-swept (holdout discipline); the
      historical cleanroom path absent on disk (PIN CL-1 display
      consumed). Sweeps S-1..S-5, cutoff 2026-08-15T05:17Z. A wider
      sweep could change this audit only by exhibiting a pre-cutoff
      sealed display of a d1-d4 item or a competing round-3 artifact.
      PREMISE(named).
CL-2  INDEPENDENCE INSTRUMENT: the pre-verdict hash (§1) is the
      commitment; the pre-verdict file lives in the session scratchpad
      (commission-distinct, outside the corpus), content summarized
      verbatim-faithfully in §1. PREMISE(named).
CL-3  AUDITED-GRADE CONSUMPTION: OB-2/PIN/B-3 pairs consumed at their
      audited grades; LOC/PJ2/LOC-NAT/KER clause bytes consumed as
      quoted inside those artifacts except where re-walked directly
      (V001 §1.2-1.4, fdf20bd4 :406-:415/:1255-:1295, SEAM1 :489/:541,
      BANACH :151/:365/:462) — each re-walk hash-verified first.
      PREMISE(named).
CL-4  EXTERNAL-MATH IMPORTS accepted at the build's tagged sites at the
      same grade OB-2's audit accepted NG-1's import; my battery uses
      only exact CAS, no imports. PREMISE(named).
CL-5  PIN2H_READING_RA_FIXED_2026-08-15.md (supervision; not a fenced
      filename class) was OPENED post-verdict-fixing to adjudicate C-4;
      its register row cited as carried ("Q-1096") — EXPECTED-
      UNLOCATABLE, not chased. PREMISE(named).
CL-6  DISCLOSURE: sweep listings displayed fenced-class BASENAMES
      (register-class among them); no fenced content opened, read, or
      used. PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  My battery's model objects (rank-1/2x2 SPD matrices, the two-stage
  clause-shape model D1-D12, symbolic t/a_i/v_i never evaluated) are
  verification instruments only: they test the build's exact claims and
  the clause-set satisfiability SHAPE, decide nothing about the sealed
  carriers, and are promoted into no verdict about inhabitance. No toy
  value, stage, address, dimension, or number enters any conclusion; no
  float anywhere; no value frozen.
```

## FLAG BLOCK

```text
EX3_AUDIT = CONFIRMED-WITH-CORRECTIONS (C-1 O-2/dim-convention omission;
  C-2 CAS not verbatim-displayed; C-3 flag-scope drop; C-4 cutoff-window
  staleness on PIN-2-H — all verdict-neutral, none fires)
TARGET_VERDICT_STANDS = B-1 UNDECIDABLE-STILL; INHABITED not reached
  (conditional schema only); EMPTY not reached (no displayed sealed-
  carrier kill); the reduction sound AS SCOPED (census; dim>=2 +
  TOP-1/TOP-2); NG-3' sound at PART-PROVABLE + tagged import
INDEPENDENCE = pre-verdict fixed + hashed BEFORE opening the build
  (d42cf82b…, 05:09:18Z); independent verdict = UNDECIDABLE at the same
  named bits; divergence (TOP-1/TOP-2) resolves IN THE BUILD'S FAVOR
JOINT_FIBER_BAR = respected by the build (one tuple, verified); no
  smuggled member binding found; PIN-2-H both-readings validity verified
  per arm; the 05:02Z R-A fixing is verdict-neutral for B-1
ROUNDS_1_2 = respected at their own walked bytes; existence-vs-
  derivability re-scoping is OB-2-licensed; V002 checkpoint honored
BOUNDARY_CALL = (A1-14) H-tail exclusion VERIFIED VERBATIM at V001
  §1.2 — the one scope call that could have flipped the verdict holds
SEALS = target 1/1 + relied-upon 12/12 MATCH ; MISMATCHES = 0 ;
  output path first-use, probed ABSENT twice
BATTERY = own, fresh venv, sympy 1.14.0, 26/26 PASS, exact only
SWEEPS = S-1..S-5 ; CUTOFF 2026-08-15T05:17Z ; post-cutoff landings
  adjudicated (C-4), none touches a deciding bit
KILL_SURFACE_UNCHANGED = the deciders remain d1-d4 exactly as the build
  names them; under B-3-audit O-2's convention import the dim fork
  closes toward the STRONGER form of the build's own reduction
SK2 = NOT FIRED ; ACT3 = NOT FORCED ; D1_MENU = UNCHANGED
DETERMINATION_ONLY = held (no member bound, no candidate authored, no
  adoption, no selection, no common cell, nothing numeric beyond exact
  integers/symbols) ; MEMBER_BOUND = false ; CHAIN_INVOKED = false
HELD_CONSTRUCTION = untouched, unopened, unpresupposed (506bfe2a by bar)
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = the third round survives an adversarial audit that was built to
  kill it: the verdict was independently re-derived blind and matched;
  the one scope call that could have flipped it is sealed text; the one
  substantive miss (O-2) points the SAME direction the build already
  leans. B-1 now genuinely sits on the mixing bit + TOP-1/TOP-2 (+ the
  closure), each a finite display, exactly as the build printed.
```
