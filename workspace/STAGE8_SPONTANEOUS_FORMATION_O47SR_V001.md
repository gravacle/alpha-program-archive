# STAGE 8 — OPERATED OR SPONTANEOUS: AGENT-VERSUS-CONDITION AT EVERY RECORD-FORMATION SITE (O47SR) V001

Date: 2026-08-16
Lane: BUILD (commissioned pair)
Role: read and classify at bytes. No authoring, no advocacy, no adoption.
Status: CLASSIFICATION ONLY / NO CONSTRUCTION / NO ADOPTION / NO NUMERIC EVALUATION

---

## 0. LEAD FINDING

**Q1 — no formation site in this corpus is OPERATED in the sense at issue.** Thirteen
formation sites across fourteen artifacts were located and typed from defining text and
type signatures (S-1..S-13, with S-8 spanning a principle and its successor). Every
one of them fires on the obtaining of a stated condition. Four carry agentive surface
verbs (`performs`, `selects`, `generated by`, `writes`), and in all four cases the
grammatical subject is a *structure named inside the theory* — the finite parent, the
boundary law, physical boundary degrees of freedom, the source — never a thing external
to the record that picks the moment. The known site (ordered pair of source-conditioned
unitaries `U_0(tau), U_1(tau)` on a common ready state `|r>`) is **CONDITION-DRIVEN at
bytes**: what fires the gate is the orthogonality condition `<R_0|R_1>=0`, and the
moment is the *least positive* solution, not a chosen one. What is *not* condition-driven
there is the **saturation**, and the corpus says so itself: `Saturation is adopted, not
derived.` The adopter is the program, not a physical performer.

**Q2 — PARTIAL, and the partition is sharp.** Two marks present, three absent.
PRESENT: a threshold/critical condition on formation itself (three independent ones);
an extremized quantity (least-positive interval, shortest relative projective path,
least positive `T_R`). ABSENT: an order parameter (0 substantive hits in 3,138+3,708
files); a rate/onset-rate/nucleation-like quantity (0 substantive hits — the single
`nucleat` hit is a *search string* inside a prior audit's method section); symmetry
breaking at any formation site (34 files carry the phrase; every one is about seminorm
topology admissibility, state weights, or action-comparison data — none is at a
formation gate). Decisively: the corpus's own typing calls its extremizations
**"local equality, least-member, or single-normalization moves"** and books three of the
five as **adopted**, not derived. Extremization is present as *bookkeeping that selects
a point on an already-derived admissible set*, not as the thing that makes formation happen.

**Q3 — PHASE-IS-A-CONDITION-VARIABLE, at one site, and it is charge-type.**
At `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md` the condition ranges over
`Q_Sigma = integral_Sigma j^mu dSigma_mu` with `spec(Q_Sigma)={0,1}`, and the gate reads
`Q_Sigma=0 => no charged-record write; Q_Sigma=1 => the charged-record channel reaches
first orthogonal onset.` The generator is literally a spectral function of that charge:
`Because B_charged is a spectral function of Q_Sigma, [B_charged,Q_Sigma tensor I_R]=0.`
A phase-type angle `theta` also enters the onset condition at the same gate
(`First orthogonal onset gives theta=pi/2, v_z=0`). At the *other* sites phase is
present but **not** in the condition — the Fubini-Study gate takes a modulus,
`arccos |<r|W(tau_*)|r>|`, which discards phase by construction, and the identity phase
`chi` is expressly outside: `chi is not fixed by the conditional record projectors.`

**Q4 — THE BOUNDARY-CLOSURE NEVER ACTS. 9/9 clauses delimit or classify; zero act.**
The tally is 4 DELIMITS, 5 CLASSIFIES, 0 ACTS. This is the commission's cleanest test and
it returns clean. Two clauses were adjudicated at length before being typed non-ACTS
(CS-6 "saturation", CS-9 "MANDATED") — both are itemized and quoted whole in §7. The
closure's own artifact says of the one clause that most resembles an operation:
`It DELIVERS the split; it cannot close it.`

**Q5 — OBSERVATIONALLY-EQUIVALENT-AT-BYTES. NULL RETURNED.**
No text was found that is true under one reading and false under the other. The nearest
candidates — `the finite parent performs the local reversible write`, `Answer 1 - Is The
Write Specified As An Operation? Yes`, and `Durable records are generated by physical
boundary degrees of freedom` — are each true under both readings, because none of them
supplies a performer *external to the record* or a selector of the moment. The corpus's
previously booked negative **governs this question directly**: it refuted a forced binary
of exactly this shape (`The forced binary is refuted`) and returned `NO_VERDICT` on the
neighbouring temporal-production distinction *for want of a sealed operational
definition*. Both are quoted whole in §8. The word `agent` occurs 220 times across the two
roots and **zero times in any of the fourteen formation-site artifacts**; where it is used
physically it is always a *prior lane's gloss* ("the assigning agent"), never corpus text.

---

## 1. CHOICE LEDGER

Every discretionary choice made by this lane, with the alternative not taken.

| # | Choice | Alternative not taken | Why | Load-bearing? |
|---|---|---|---|---|
| C-1 | Read "the boundary-closure" of Q4 as the object the corpus itself names and types: the cell's boundary closure, typed exactly as clauses `CS-1..CS-9` in `STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md` §2 | Read it as `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002`'s "record closure"/"closure map", or as the `A14 Boundary closure` audit row, or as the S28 "boundary closure + induced action" conjunction | The WBC artifact is the only place in either root that *enumerates* boundary-closure clauses under the heading "the closure structure, typed exactly"; a clause census needs enumerated clauses. Prior commissions (O4SR) also call it "THE BOUNDARY-CLOSURE" | YES — Q4 tally is over these nine |
| C-2 | Also run a **secondary** closure census over the root principles' closure clauses, so C-1 cannot hide an ACTS clause by scoping | Report only CS-1..CS-9 | "Open what you count" | YES — §7.3 |
| C-3 | Treat the **superseded** `CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md` as live evidence for Q5 | Exclude it as superseded by V002 | V002 self-describes as `append-only scope successor` that `preserves the v001 promoted principle`; and V001's `performs` sentence is quoted forward as live at `STAGE8_GAMMA_K_FINITE_REVERSIBLE_WRITE_OBS05_TEST_V001.md:73-81`. Excluding it would suppress the strongest OPERATED candidate | YES — it is the top Q5 candidate |
| C-4 | Count `nucleat` as ZERO substantive hits although grep returns 1 file | Report "1 hit" | The single occurrence is a regex literal inside another lane's method section (`record\|nucleat\|come into being...`), i.e. a *search pattern*, not corpus content about nucleation | YES — Q2 mark |
| C-5 | Count `order parameter` as ZERO substantive hits although grep returns 4 files (2 mirror pairs) | Report "4 hits" | Both are homonyms: `second-order parameter` = numerical convergence order; `3+2 order parameter` = a declared stabilizer/split label. Neither is an order parameter for formation | YES — Q2 mark |
| C-6 | Grade `saturation` as **attainment of equality in a derived inequality, adopted** — not as extremization that causes formation | Grade it as extremization present | The corpus types it itself: `orthogonalization budget saturation: the lower bound is derived, but saturation is adopted rather than derived`, under the heading `Five Genuine Pure Minimality Sites` and the gloss `local equality, least-member, or single-normalization moves` | YES — Q2 grade |
| C-7 | Treat `theta` in the holonomy onset condition as **phase-type** | Treat it as a bare real parameter | It is the phase angle of an amplitude at bytes: `The ready-state survival amplitude is cos(theta)-i(v_z/\|v\|)sin(theta), theta=\|v\| tau_*/hbar` | YES — Q3 cite |
| C-8 | Type an agentive verb by its **grammatical subject's** position relative to the record, not by the verb | Type `performs`/`selects` as OPERATED on the verb alone | Standing discipline: grade off definitions and type signatures, not names. "OPERATED" as posed requires a performer; a structure internal to the theory that the record is part of is not an external selector | YES — Q1 and Q5 |
| C-9 | Report the two unsealed corpus files I relied on as **UNSEALED**, and use them anyway | Drop them | They are primary formation sites; dropping them would understate the census. Their status is flagged | NO — neither changes a grade |
| C-10 | Do not open `a32_holdout/custodian_private/` or `.proof_deps/`, `.pytest_cache/` | Sweep everything | Holdout is protected; `.proof_deps` is a vendored sympy tree (it produced the only `order[ _-]parameter` noise) | NO |
| C-11 | Quote the WBC §0 abstract's saturation clause from the CS-6/CS-8 clause bodies rather than from the 1-line abstract | Quote the abstract | The abstract is one ~4,000-character line; the clause bodies are the defining text | NO |

---

## 2. SEALS

`shasum -a 256 -c` run from each artifact's own directory.

```text
DIRECTORY: /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
           alpha_fundamental_record_action_cleanroom_v003

BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md ............ OK
SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md ............... OK
FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md ............ OK
CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md ...................... OK
CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002.md .................... OK
STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md ........ OK
STAGE8_GAMMA_K_SOURCE_RECORD_COMPOSITION_TYPING_TESTS_V001.md ... OK
STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md ..... OK
PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md ........... OK
STAGE8_GAMMA_K_FINITE_REVERSIBLE_WRITE_OBS05_TEST_V001.md ....... OK
                                                        10/10 OK

DIRECTORY: /Users/bgm/MB Work/alpha-program-archive/workspace

STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md ....................... OK
STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_AUDIT_V001.md ................. OK
                                                          2/2 OK

DIRECTORY: /Users/bgm/MB Work/alpha-program-archive/supervision   (DOR_* only)

DOR_016 .. DOR_020_CONTINUUM_PACKAGE, all .md.seal.sha256 ....... 14/14 OK

TOTAL VERIFIED: 26/26 OK.  0 FAIL.

NO SEAL SIDECAR EXISTS (read anyway, flagged):
  BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md
  BID_FINITE_RECORD_DURABILITY_NO_GO_V001.md
  BID_FIRST_OPENING_INTERVAL_DERIVATION_V001.md
  BID_CHARGED_HANDLE_ACTIVATION_DERIVATION_V002.md
  CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md
  DURABLE_POINTER_CLOSURE_OPERATOR_SELECTOR_GATE_V001.md
  R3_4_LORENTZIAN_THRESHOLD_RETURN_RESULT_V001.md
These are unsealed-of-record, not unverified-against-a-seal. See FLAG BLOCK F-3.
```

---

## 3. SWEEP CUTOFFS

Exclusion globs held as a shell **array**, never a string variable, expanded to
per-pattern `--exclude=` flags. Leak counter = files matching the pattern that the bar
suppressed. Self-exclusion of this artifact and both of its sidecars is in the array.

```text
BARPATS (array, 10 members)
  *REGISTER*
  *TRACKER*
  THE_PLAN*
  ROAD_REMAINING*
  THE_HANDOFF*
  OBSERVATIONS_REGISTER*
  *DECISION_SHEET*
  STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md            [SELF]
  STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md.seal.sha256 [SELF]
  STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md.sha256      [SELF]

ROOTS: R1 = /Users/bgm/MB Work/alpha-program-archive/workspace
       R2 = /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
            alpha_fundamental_record_action_cleanroom_v003
       R3 = /Users/bgm/MB Work/alpha-program-archive/supervision/DOR_*.md  [PERMITTED SUBSET]
SUPPRESSED SUBTREES: .proof_deps/, .pytest_cache/  (vendored sympy; C-10)
```

| # | Pattern | RAW (R1+R2) | AFTER BAR | DOR | LEAK (barred, suppressed) | Barred file(s) |
|---|---|---|---|---|---|---|
| P-1 | `ready[ _]state\|readystate\|ready.*unitar\|unitar.*ready` | 223 | 222 | 0 | 1 | QUESTIONSSETTLED_REGISTER_V001.md |
| P-2 | `forced binary is refuted\|forced_binary\|binary is refuted` | 5 | 4 | 0 | 1 | QUESTIONSSETTLED_REGISTER_V001.md |
| P-3 | `boundary.?closure\|BOUNDARY_CLOSURE` | 169 | 165 | 0 | 4 | REGISTER/TRACKER family |
| P-4 | `order[ _-]parameter` | 4 | 4 | 0 | 0 | — |
| P-5 | `nucleat` | 1 | 1 | 0 | 0 | — |
| P-6 | `symmetry[ _-]break\|broken symmetry\|spontaneously broken` | 35 | 34 | 0 | 1 | QUESTIONSSETTLED_REGISTER_V001.md |
| P-7 | `(^\|[^alnum_])spontaneous` | 3 | 2 | 0 | 1 | QUESTIONSSETTLED_REGISTER_V001.md |
| P-8 | `critical (point\|value\|threshold\|condition)\|criticality` | 39 | 37 | 0 | 2 | REGISTER family |
| P-9 | `stationar(y\|ity)\|extremiz\|extremal\|minimiz\|maximiz\|shortest\|least action\|variational` | 1051 | 1046 | 2 | 5 | REGISTER/TRACKER family |
| P-10 | `saturat` | 233 | 230 | 0 | 3 | REGISTER family |
| P-11 | `threshold` | 1212 | 1207 | 3 | 5 | REGISTER/TRACKER family |
| P-12 | `external selector\|selects when\|chooses when\|picks the moment\|no external agent\|performer\|performs the` | 63 | 62 | 0 | 1 | QUESTIONSSETTLED_REGISTER_V001.md |
| P-13 | `(^\|[^alnum_])agent(s)?([^alnum_]\|$)` | 143 | 142 | 0 | 1 | QUESTIONSSETTLED_REGISTER_V001.md |

**Total barred-file suppressions across all patterns: 25. Zero barred files were opened.**
`QUESTIONSSETTLED_REGISTER_V001.md` in the second root is the file the standing
discipline names explicitly; it was suppressed by the bar on 7 of 13 patterns and never
opened. Where a prior artifact *quotes* the register, the quotation is read as the
quoting artifact's own bytes (this is unavoidable — the corpus cites the register
throughout — and is flagged at F-2).

### 3.1 OPEN WHAT YOU COUNT

Result sets small enough to open in full were opened in full, and are listed here so the
claim is auditable:

- **P-4 (4 files, 2 mirror pairs): ALL OPENED**, every matching line displayed. Both are
  homonyms (C-5).
- **P-5 (1 file): OPENED.** The match is a regex literal in another lane's method block.
- **P-7 (2 files, 1 mirror pair): ALL OPENED**, all 3 matching lines read.
- **P-2 (4 files, 2 mirror pairs): 2 OPENED IN FULL**
  (`STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md` and its R1 mirror,
  read as one artifact). **2 NOT OPENED and declared:**
  `STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md` and `..._AUDIT_V001.md` — these are a prior
  commission's own output, i.e. catalogued negatives under the BARS, and the primary
  source they book from is the artifact I did open. See F-5.
- **P-6 (34 files): ALL 74 MATCHING LINES OPENED AND READ** — 61 lines across 25 R1 files
  and 13 lines across 9 R2 files, displayed in two passes (the second pass being the
  16-line remainder, all of it W-1/W-3 seminorm material inside
  `STAGE8_W1_SUBVOLUME_RATE_O5SR_*` and `STAGE8_W3_GCM_HS_TYPE_O3SR_*`). No file was
  counted without its matching line being read. Verdict in §6.4.
- **P-12 / P-13:** all matching lines displayed and read; the physically relevant hits
  (4 of 63, 0 of 143) were opened in full file context.
- **P-9 / P-10 / P-11 (1046 / 230 / 1207 files): NOT OPENED WHOLESALE — DECLARED.**
  These are corpus-wide vocabulary counts, reported as *counts only*, and no grade rests
  on the unopened remainder. The grades rest on the named sites in §4–§6, each of which
  was opened in full. See F-6.

---

## 4. Q1 — AGENT OR CONDITION, AT EVERY FORMATION SITE

Eleven sites. For each: the defining text quoted whole (adverse clauses included), the
type signature, and the classification. **Grade off definitions and type signatures, not
names** — three of these are called "gate" and are conditions; one is called a
"principle" and contains the corpus's only true operation-verb.

### THE TEST APPLIED

Per the commission, OPERATED requires that formation be *the image of an operation
performed by a named thing*, and the sharp form is: **does anything external to the
record select when formation occurs?** So for each site I ask three questions:

```text
T-a  Is there a grammatical performer?                       (surface agency)
T-b  Is that performer external to the record?               (externality)
T-c  Does anything select the MOMENT, as against the moment
     being the solution of a stated condition?               (selection of the instant)
```

A site is OPERATED only if T-b and T-c both answer yes. T-a alone is surface.

---

### S-1 — THE KNOWN SITE. Relative Record-Orthogonalization Budget Gate v003
`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` (sealed, OK)

The argument reported as an ordered pair of source-conditioned unitaries with a common
ready state. Defining text, `:21-45`, quoted whole:

> ## Common closed dilation
>
> Let the source, designated record subsystem, and required environment form
> one closed Hilbert space. From one common ready state `|r>`, the two
> source-conditioned closed-dilation unitaries are
>
> ```text
> U_0(tau), U_1(tau).
> ```
>
> The conditional global states are
>
> ```text
> |R_0> = U_0(tau_*) |r>,
> |R_1> = U_1(tau_*) |r>.
> ```
>
> Their overlap is
>
> ```text
> <R_0|R_1>
>   = <r|W(tau_*)|r>,
>
> W(tau) = U_0(tau)^dagger U_1(tau).
> ```

Type signature:

```text
(U_0, U_1 : R -> U(H_S ⊗ H_R ⊗ H_E)) × |r> ∈ H  ->  overlap <r|W(tau_*)|r> ∈ C
formation predicate:  <R_0|R_1> = 0
```

**What decides it.** The gate's firing text, `:74-86`, quoted whole:

> For orthogonal conditional global states,
>
> ```text
> <R_0|R_1>=0,
> ```
>
> so the analytic consequence is
>
> ```text
> J_FS,rel
>   := integral_cell d tau Delta H_W(tau)
>   >= pi hbar/2.
> ```

The gate fires on `<R_0|R_1>=0` — a stated condition on the pair, with no performer in
the sentence. `U_0, U_1` are the evolution; per the commission, evolution alone does not
settle it, and here it does not have to: the *condition* is what the text conditions on.

**T-a:** no performer in the formation predicate. **T-b:** n/a. **T-c:** the moment
`tau_*` is fixed by the exact realization at `:110-124` as the solution of
`epsilon_rel tau_*/hbar = pi/2` — a stated equation, not a selection.

**But one clause is not condition-driven, and the corpus says so itself.** `:98-108`,
quoted whole including the adverse clauses:

> ## Adopted Gravacle onset rule
>
> The allow/require boundary is adopted to select first admissible record onset
> through a shortest **relative** projective path on the unique physical record
> cell. Conditional on this target-value-free, target-aware Level-1 rule,
>
> ```text
> J_FS,rel = pi hbar/2.
> ```
>
> The lower bound is derived from the imported theorem. Saturation is adopted,
> not derived. Historical target blindness is not claimed.

Here `select` has a performer — but the performer is **the program adopting a rule**, not
a physical thing acting on a record. The status block makes the split explicit at
`:182-183`:

```text
relative_onset_saturation_derived = false
relative_onset_saturation_adopted_Level_1 = true
```

**CLASSIFICATION S-1: CONDITION-DRIVEN.**
The gate fires because `<R_0|R_1>=0` obtains. The one selection in the artifact is an
*adoption by the program of which point on the derived admissible set to call onset* —
a governance act on the theory, not an operation on the record. Nothing external to the
record selects the moment. **Adverse note carried forward:** because saturation is
adopted and not derived, the corpus itself declines to say the physics reaches that
point on its own. That is a gap in the *derivation*, not evidence of an operator.

---

### S-2 — Conditional Source-Flux Record-Holonomy Gate v003
`SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md` (sealed, OK)

Defining text, `:22-44`, quoted whole:

> ## Declared primitive flux sector
>
> On the declared zero-or-one local unit-character source-crossing sector,
> inherit
>
> ```text
> Q_Sigma = integral_Sigma j^mu dSigma_mu,
> spec(Q_Sigma)={0,1}.
> ```
>
> This is a local branch input, not a derivation of every current-flux
> spectrum.
>
> For the charged record handle, adopt:
>
> ```text
> Q_Sigma=0  => no charged-record write;
> Q_Sigma=1  => the charged-record channel reaches first orthogonal onset.
> ```
>
> Other interactions and record handles can still evolve in the zero-flux
> branch.

Type signature:

```text
Q_Sigma : (Sigma, j^mu) -> spec{0,1}
write predicate:  Q_Sigma = 1
onset predicate:  theta = pi/2  ∧  v_z = 0     [see :60-65]
```

This is a **material implication keyed on a spectral value**. The left-hand side is a
number; the right-hand side is an outcome. There is no verb of doing on either side —
`=>` is the whole mechanism. The onset criterion at `:56-65` is likewise an equation:

> The ready-state survival amplitude is
>
> ```text
> cos(theta)-i(v_z/|v|)sin(theta),
> theta=|v| tau_*/hbar.
> ```
>
> First orthogonal onset gives
>
> ```text
> theta=pi/2,
> v_z=0.
> ```

**T-a:** no performer. **T-b:** n/a. **T-c:** `theta=pi/2` fixes the moment as the
solution of an equation in `|v|` and `tau_*`.

**CLASSIFICATION S-2: CONDITION-DRIVEN.** This is the corpus's cleanest condition: a
charge value obtains, and formation follows or does not.

---

### S-3 — Controlled charged generator
`BID_CHARGED_HANDLE_ACTIVATION_DERIVATION_V002.md:66-100` (UNSEALED — F-3)

Defining text quoted whole:

> ## Controlled charged generator
>
> Let `B_Q` be the derived unit-incidence first-opening operator on the charged
> record carrier. The source-controlled generator is
>
> ```text
> B_charged
>   =P_0 tensor 0_R
>    +P_ch tensor B_Q.
> ```
>
> The charge sign enters the compact connection/holonomy orientation, not the
> primitive incidence magnitude. Because `B_charged` is a spectral function of
> `Q_Sigma`,
>
> ```text
> [B_charged,Q_Sigma tensor I_R]=0.
> ```
>
> At the first completed-transfer interval:
>
> ```text
> Q_Sigma=0:
>   |0,r> -> |0,r>;
>
> Q_Sigma=+1:
>   |+,r> -> |+,p_Q>;
>
> Q_Sigma=-1:
>   |-,r> -> |-,p_Q>.
> ```
>
> No coherent superposition of different total-charge sectors is required for
> this statement. It is an operator identity on the direct sum of sectors and
> is compatible with charge superselection.

Type signature:

```text
B_charged = f(Q_Sigma) : H_S ⊗ H_R -> H_S ⊗ H_R,  self-adjoint,  f a spectral function
```

The decisive phrase is **`Because B_charged is a spectral function of Q_Sigma`**. The
generator is not applied *to* the charge by something; it *is a function of* the charge.
The word "controlled" is a name; the type signature is a spectral functional calculus.

**CLASSIFICATION S-3: CONDITION-DRIVEN.** Graded off the type signature against the
name, per the standing discipline.

---

### S-4 — First-opening interval / least common record interval
`BID_FIRST_OPENING_INTERVAL_DERIVATION_V001.md:65-89` (UNSEALED — F-3)

Defining text quoted whole:

> ## Least common record interval
>
> For distinct handles `h!=k`, the endpoint and edge supports are disjoint.
> Their conditional-state overlap is therefore
>
> ```text
> <r|U_h(tau)^dagger U_k(tau)|r>
>  =[(1+cos(sqrt(2)tau))/2]^2.
> ```
>
> The least positive common zero is
>
> ```text
> tau_R=pi/sqrt(2).
> ```
>
> At this interval,
>
> ```text
> U_h(tau_R)|r>=|p_h>
> ```
>
> for every handle. Thus the unresolved root plus the three first-opening
> outputs are one orthonormal four-alternative set. The result is unchanged by
> edge orientation reversal or endpoint rephasing.

Type signature:

```text
tau_R := min { tau > 0 : <r|U_h(tau)^dagger U_k(tau)|r> = 0  ∀ h≠k }
```

**This is the single most important line in the whole Q1 census for T-c.** The moment of
formation is defined as **the least positive zero of an overlap function**. It is not
chosen, not scheduled, not triggered. It is a root. A root has no selector.

**CLASSIFICATION S-4: CONDITION-DRIVEN.**
Adverse clause carried, `:91-98`: `For a surviving incidence magnitude 'w', the result
would be tau_R(w)=pi/(sqrt(2)|w|). Therefore the interval is absolute only after Gate 4
derives '|w|=1'. This makes the dependence visible rather than hiding it.`

---

### S-5 — Marginal public-closure rule (the record cell)
`BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:38-67` (UNSEALED — F-3)

Defining text quoted whole, inputs 3 and 4 together, because input 4 is unintelligible
without input 3:

> 3. **Public recoverability condition (physical admissibility):**
>
>    On the maximal spherical section, the Misner-Sharp compactness must obey
>
>    ```text
>    C_R = 2 G E_R / (c^4 R_R) <= 1.
>    ```
>
>    `C_R > 1` is trapped and cannot be the outwardly recoverable primitive
>    comparison cell of this branch.
>
> 4. **Marginal public-closure rule (adopted Level-1 Gravacle rule):**
>
>    The allow/require boundary is the first physically admissible public
>    record cell. The least positive `T_R` is attained at the boundary of the
>    recoverable set:
>
>    ```text
>    C_R = 1.
>    ```
>
>    This fourth input is the selector. Without it, public recoverability gives
>    a half-line of allowed durations and no absolute record scale.

And the adverse clause at `:62-67`, quoted whole:

> The use of `C_R=1` is not inferred from alpha. It is the geometric meaning of
> the adopted "first admissible boundary" rule in the isotropic branch. A
> reviewer may reject that rule, but the rule may not be hidden or replaced by
> numerical agreement downstream. Likewise, the active theory must derive the
> physical map `Delta S_record=E_R T_R`; the relative character calculation
> alone does not supply it.

Type signature:

```text
admissible set:  { T_R > 0 : C_R(T_R) <= 1 }        [a half-line, condition-defined]
adopted point:   C_R = 1                            [its boundary]
```

**`This fourth input is the selector`** is the most agentive noun in the corpus's
formation vocabulary. It must be read exactly: the selector is *an input to the
derivation* — a rule the program adopts to cut a half-line down to a point. It selects
among *values the theory permits*, not among *moments at which a record forms*. The
half-line itself is condition-defined (`C_R <= 1`), and the text is explicit that
without the adopted rule the physics leaves the whole half-line open.

**CLASSIFICATION S-5: MIXED.**
The admissibility boundary is condition-driven (`C_R <= 1`, with `C_R > 1` trapped and
excluded on physical grounds). The choice of the *marginal* point rather than any other
admissible point is an **adoption by the program**, expressly labelled
`marginal_public_closure_rule_adopted_Level_1 = true` at `:162` and
`strict_untrapped_inequality_alone_selects_unique_scale = false` at `:167`. No physical
performer appears anywhere in the site. **This is the only site whose MIXED grade is
material**, and its mixedness is between *condition* and *program adoption*, never
between condition and physical operator.

---

### S-6 — The boundary law and the record cell
`FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md` (sealed, OK)

Two clauses. First, `:70-74`, quoted whole:

> ## Fundamental record degrees
>
> Durable records are generated by physical boundary degrees of freedom. On
> every admissible primitive causal record cell, the one-handle record carrier
> reduces to the previously derived two-endpoint projective carrier.

Second, `:103-111`, quoted whole:

> ## Unique record cell
>
> When a primitive durable record forms, the boundary law selects one physical
> Lorentz-covariant interval, support region, and closure configuration over
> which its action is accumulated.
>
> An explicit causal-cell construction must realize this postulate before any
> response is evaluated. A cell shape, duration, density, orientation measure,
> or cutoff chosen after seeing a coupling is forbidden.

Type signature:

```text
generated_by : records -> boundary degrees of freedom     [provenance relation]
boundary law : (formation event) -> (interval, support region, closure config)
```

**This is the corpus's strongest surface agency, and it is exactly here that the
distinction is decided against OPERATED.** Read the temporal structure of the second
clause: **`When a primitive durable record forms, the boundary law selects…`**. The
formation is the *antecedent*; the selection is the *consequent*. The boundary law does
not bring the record into being — it fixes the interval, support region, and closure
configuration **of a record that has already formed**. That is precisely the
"boundary, not a former" shape the commission asked about, stated in the corpus's own
words, without the corpus knowing it was being asked.

The first clause, `Durable records are generated by physical boundary degrees of
freedom`, has an agentive by-phrase, and it is the single best OPERATED candidate at
bytes. Against it: (i) `physical boundary degrees of freedom` are structure, not a
selector — they are part of the system the record is in, failing T-b; (ii) the corpus
elsewhere types exactly this `generated by` vocabulary and refuses to read it as temporal
production — see §8.2; (iii) `:168` books `unique_causal_record_cell_derived = false`,
so the clause is an adopted premise, not a derived mechanism.

**CLASSIFICATION S-6: CONDITION-DRIVEN for the cell clause** (formation is the trigger,
selection is the consequence — a delimiting act, not a forming one);
**INDETERMINATE-AT-BYTES for `generated by`**, whose relation-type the corpus itself
declines to fix (§8.2). It is not OPERATED at bytes; it is *unresolved between*
provenance and production, and the corpus has booked that exact non-resolution.

---

### S-7 — Causal Incidence Support Principle v001 (record-forming incidence)
`CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md` (sealed, OK)

Defining text, `:16-36`, quoted whole:

> ## Principle
>
> For every primitive record-forming incidence `c`, the complete microscopic
> parent assigns one Lorentz-covariant causal cell `Omega_c` and one interaction
> density `L_c` such that
>
> ```text
> support(L_c) is contained in Omega_c.
> ```
>
> The incidence is an event, not a permanent Hamiltonian term. Once the future
> boundary has crossed the closure face of `Omega_c`, the same primitive
> incidence is absent from the active generator. A physical exhaustion adds
> new future incidences on new record factors; it does not reapply a completed
> incidence to its old record factor.
>
> The same source degrees of freedom may meet multiple cells. Thus later
> incidences may act on a shared source algebra, and their source-supported
> operators need not commute. They act on their own new record factors and may
> not act on an earlier record factor by reusing its completed primitive
> incidence.

Type signature:

```text
assign : {record-forming incidences c} -> (Omega_c, L_c),  with supp(L_c) ⊆ Omega_c
```

`assigns` and `act on` are agentive. But: the assignment is a *support law* — it says
where the incidence lives, not that anything decides it should happen. The artifact
disclaims formation-causation itself at `:12-14`: `This principle fixes the support and
reuse law of a primitive incidence. It does not assume durability, select an outgoing
state, supply a spectral measure, or compute a coupling.` And **`The incidence is an
event, not a permanent Hamiltonian term`** is event-language: an event occurs, it is not
performed.

**CLASSIFICATION S-7: CONDITION-DRIVEN.** A support/reuse law over events. No selector
of occurrence; the falsifier list at `:62-75` polices *reuse* and *covariance*, never
*who fires it*.

---

### S-8 — Causal Direct-Limit Record Principle (durability)
`CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002.md` (sealed, OK) and its V001 predecessor
(UNSEALED — F-3)

V002's definition, `:10-18`, quoted whole:

> Within the ordinary `3+1` flat-asymptotic branch:
>
> ```text
> A durable public record is the compatible outgoing sector of a
> future-directed Lorentz-covariant, causally sequential exhaustion governed
> by the same finite parent. Its durability consists jointly of thresholded
> source nonreturn and exact completed-record persistence; its public content
> is the recoverable quasi-local record state and central sequence.
> ```

Type signature:

```text
durable public record := compatible outgoing sector of an exhaustion
durability := thresholded source nonreturn  ∧  exact completed-record persistence
```

This is a **definition by identification** — `A durable public record IS the compatible
outgoing sector`. Nothing performs anything; a thing is identified with a limit object.
Durability is a **conjunction of two conditions**.

V001, however, contains the corpus's one genuine operation verb, `:23-30`, quoted whole:

> The physical content has three inseparable parts:
>
> 1. the finite parent performs the local reversible write;
> 2. its absolutely-continuous source tail gives thresholded source-root
>    nonreturn for every admitted finite-energy `L2` excitation; and
> 3. one-use causal support leaves completed record factors exactly invariant,
>    producing a compatible quasi-local output state, its GNS
>    representation, and recoverable central record sequences.

**CLASSIFICATION S-8: CONDITION-DRIVEN (V002) / T-a-POSITIVE, T-b-NEGATIVE (V001).**
`the finite parent performs the local reversible write` has a performer. It fails
externality: the finite parent is the *whole closed system including the record* — the
record is a tensor factor of it (`A_SR(C)=CAR(K_Sigma,q) graded-tensor R(C)`). A system
performing on its own subsystem is unitary evolution, which the commission has already
ruled non-decisive. It also fails T-c: nothing in the clause selects the moment; part 2
supplies a *threshold*, not a trigger. Full treatment as a Q5 candidate at §8.3.

---

### S-9 — Finite Record-Durability No-Go
`BID_FINITE_RECORD_DURABILITY_NO_GO_V001.md:36-53` (UNSEALED — F-3)

Defining text quoted whole:

> Direct evaluation gives
>
> ```text
> U(tau_R)|r> = |p>,
> U(tau_R)|p> = |r>,
> U(2 tau_R) = I.
> ```
>
> Thus the exact operation that writes the first orthogonal endpoint erases it
> one equal interval later. The endpoint projector also fails the
> nondemolition condition:
>
> ```text
> [c_partial, |p><p|] != 0.
> ```
>
> The one-cell operator therefore provides a reversible write, not a durable
> record.

`the exact operation that writes` — operation language again, and again the operation is
`U(tau)=exp(-i tau c_partial)`, unitary evolution, with `tau_R=pi/sqrt(2)` fixed by
`spec(c_partial)={-sqrt(2),0,+sqrt(2)}`. Recurrence, not agency, is the finding.

**CLASSIFICATION S-9: CONDITION-DRIVEN.** The site's whole content is that evolution
*without* a further condition fails to make a record durable — i.e. it argues *for*
condition-dependence and *against* the sufficiency of the operation.

---

### S-10 — The allow/require constraint
`STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md:29-41` (sealed, OK)

Defining text quoted whole:

> ## Split
>
> The object called "allow/require" must be split into two differently typed
> objects.
>
> 1. The allow/require constraint: real, argued, falsifiable, and already run
>    against the program's own preferred result. It constrains when declared support
>    can become a durable public record. It is not, by itself, an absolute
>    stiffness selector.
> 2. The local minimality or saturation adoptions named after allow/require. These
>    are local equality, least-member, or single-normalization moves. They are not
>    instances of the allow/require constraint merely because they share the same
>    vocabulary.

Type signature:

```text
allow/require : predicate constraining WHEN declared support can become durable
status = CELL_CONSTRAINT_ONLY                                          [:111]
```

**CLASSIFICATION S-10: CONDITION-DRIVEN.** The word is `constrains`. A constraint is the
paradigm of a condition. The artifact's whole purpose is to strip *selector* language off
this object and re-file the selector-shaped moves separately as adoptions.

---

### S-11 — The write, typed as an operation
`STAGE8_GAMMA_K_FINITE_REVERSIBLE_WRITE_OBS05_TEST_V001.md:66-71, 124-130` (UNSEALED — F-3)

Defining text quoted whole:

> ## Answer 1 - Is The Write Specified As An Operation?
>
> Yes, at the primitive finite source-record level, the write is specified as a
> source-controlled self-adjoint record-incidence generator on the source-record
> tensor carrier, with its finite completed-transfer evolution giving a reversible
> unitary write.

and the type it emits, quoted whole:

> The operation type is therefore:
>
> ```text
> TYPE = self-adjoint source-controlled record-incidence generator on the
>        finite source-record tensor carrier, whose finite completed-transfer
>        evolution is the reversible write.
> ```

**This is the corpus asking the commission's own question, in the corpus's own words, and
answering Yes.** It is therefore the single most important text in this report, and it
must be read at its type signature rather than its heading. The type signature is
`(self-adjoint generator) + (unitary evolution)`. That is exactly the object the
commission pre-emptively ruled non-decisive: *"Unitaries are evolution; evolution alone
does not settle this."* And the "control" is `Q_Sigma` — a charge value (S-3), not a
controller.

**CLASSIFICATION S-11: CONDITION-DRIVEN, on the type signature; the heading is a name.**
Per the standing discipline: a gate called a gate may be a condition, and here an
*operation* called an operation is a generator-plus-condition. Carried to Q5 at §8.4.

---

### S-12 — Formation of the durable public record (composition typing)
`STAGE8_GAMMA_K_SOURCE_RECORD_COMPOSITION_TYPING_TESTS_V001.md:167-184` (sealed, OK)

Defining text quoted whole:

> At the algebra/carrier level, sealed text says record degrees exist as labeled
> record factors with ready states before a particular write. That is incompatible
> with the claim that record algebraic content is generated solely by source
> automorphisms.
>
> At the durable-public-record level, sealed text says a public record is formed
> only after the source-controlled/write-and-persist structure succeeds. The
> zero charged source leaves the charged record ready and opens no charged
> handle.

and its verdict at `:180-184`:

```text
record_carrier_without_source_action_exists = true
durable_public_record_without_required_source_write_derived = false
crossed_product_forced_by_no_record_without_source = false
```

**`a public record is formed only after the ... structure succeeds`** — *succeeds* is the
verb of a condition being met, not of an act being performed. `The zero charged source
leaves the charged record ready and opens no charged handle` is the same implication as
S-2 restated in prose.

**CLASSIFICATION S-12: CONDITION-DRIVEN.**
Adverse clause carried, `:324-327`: `The source still writes/correlates those degrees
through source-record interactions; independence of degrees is not independence of
dynamics.`

---

### 4.1 Q1 TALLY

| Site | Artifact | Grade |
|---|---|---|
| S-1 | `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003` (the known site) | **CONDITION-DRIVEN** |
| S-2 | `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003` | **CONDITION-DRIVEN** |
| S-3 | `BID_CHARGED_HANDLE_ACTIVATION_DERIVATION_V002` | **CONDITION-DRIVEN** |
| S-4 | `BID_FIRST_OPENING_INTERVAL_DERIVATION_V001` | **CONDITION-DRIVEN** |
| S-5 | `BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001` | **MIXED** (condition ∧ program adoption) |
| S-6 | `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002` — cell clause | **CONDITION-DRIVEN** |
| S-6′ | same — `generated by physical boundary degrees of freedom` | **INDETERMINATE-AT-BYTES** |
| S-7 | `CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001` | **CONDITION-DRIVEN** |
| S-8 | `CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002` (and V001) | **CONDITION-DRIVEN** |
| S-9 | `BID_FINITE_RECORD_DURABILITY_NO_GO_V001` | **CONDITION-DRIVEN** |
| S-10 | `STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001` | **CONDITION-DRIVEN** |
| S-11 | `STAGE8_GAMMA_K_FINITE_REVERSIBLE_WRITE_OBS05_TEST_V001` | **CONDITION-DRIVEN** |
| S-12 | `STAGE8_GAMMA_K_SOURCE_RECORD_COMPOSITION_TYPING_TESTS_V001` | **CONDITION-DRIVEN** |

**OPERATED: 0. CONDITION-DRIVEN: 10. MIXED: 1. INDETERMINATE-AT-BYTES: 1.**

Where the corpus reaches for a selector it is always the *program* selecting a premise
(S-1 saturation, S-5 marginal rule) — never a physical thing selecting a moment. The
program's adoptions are visible, labelled `adopted`, and booked `derived = false`.

### 4.2 A THIRTEENTH SITE, FOUND LATE — and it disclaims agency in one line

`DURABLE_POINTER_CLOSURE_OPERATOR_SELECTOR_GATE_V001.md` (UNSEALED — F-3) is named a
**selector gate**, which under the standing discipline demands inspection. Its defining
condition, `:24-33`, quoted whole:

> For the post-closure pointer component only, impose nondemolition
> compatibility:
>
> ```text
> [C_ptr,P_0]=[C_ptr,P_1]=0.
> ```
>
> This condition says that the closure component used to stabilize/read the
> record does not itself mix the two declared durable endpoint sectors. It does
> not say that `C_ptr` writes, amplifies, or redundantly copies the record.

What the gate "selects" is a **direction in an operator space**, forced by a commutant
condition: `The two commutators force c_x=c_y=0, so the real Hermitian commutant is
span_R{I,Z}`. Forced by a condition, not chosen. And the artifact volunteers the
disclaimer this whole commission is hunting for: `It does not say that C_ptr writes,
amplifies, or redundantly copies the record.`

**CLASSIFICATION S-13: CONDITION-DRIVEN.** Revised totals:
**OPERATED 0 · CONDITION-DRIVEN 11 · MIXED 1 · INDETERMINATE-AT-BYTES 1.**

---

## 5. Q2 — IS THERE A SPONTANEITY SIGNATURE?

Each of the five marks searched independently. Counts are post-bar; substantive counts
apply CHOICE LEDGER C-4/C-5.

### 5.1 MARK 1 — A CRITICAL OR THRESHOLD CONDITION ON FORMATION ITSELF — **PRESENT (3)**

The commission distinguishes this from thresholds on *requirements*, which the corpus is
known to have. Three conditions survive that distinction, because in each the threshold
is on the *occurrence* and not on an input:

**M1-a — the trapping threshold on the record cell.**
`BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:38-47`, quoted whole:

> On the maximal spherical section, the Misner-Sharp compactness must obey
>
> ```text
> C_R = 2 G E_R / (c^4 R_R) <= 1.
> ```
>
> `C_R > 1` is trapped and cannot be the outwardly recoverable primitive
> comparison cell of this branch.

`C_R = 1` is a genuine critical value: it partitions the parameter line into a region
where a public record cell can exist and a region where it provably cannot (trapped).
That is a threshold **on whether the thing can form at all**, not on a requirement.

**M1-b — the first-orthogonality threshold on onset.**
`SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:60-65`: `First orthogonal onset gives
theta=pi/2, v_z=0.` Onset happens at a threshold value of `theta` and nowhere below it.

**M1-c — thresholded source nonreturn, with its spectral criticality.**
`CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002.md:10-18` makes `thresholded source nonreturn`
half of durability. The supporting spectral fact is at
`R3_4_LORENTZIAN_THRESHOLD_RETURN_RESULT_V001.md:74-76` (UNSEALED — F-3), quoted whole:

> The densities are nonnegative and their total integral is the squared norm
> of the corresponding spectral component. They are consequently in `L1`.
> Critical threshold points have momentum-space measure zero and introduce
> integrable threshold singularities, not atoms.

and the threshold statement itself, `:86-92`, quoted whole:

> ```text
> for every delta>0 there exists finite T_delta such that
> sup_(t>=T_delta)|A_Psi(t)|^2<delta.
> ```

This is the corpus's only occurrence of the phrase `critical threshold` in a physical
(non-Legendre-diagnostic) sense. **Adverse note:** its role is to show that *nothing
atomic obstructs* decay — it clears an obstruction rather than triggering an onset.

**What is NOT a critical condition, though it greps as one.** All 37 post-bar files
carrying `critical (point|value|threshold|condition)|criticality` were checked at their
matching lines. Every remaining occurrence is the **Q-318 diagnostic-scalar Legendre
critical point**, and the corpus has expressly refused to identify it with anything
physical — `STAGE8_PROCESS_DECLINE_SWEEP_CODEX2_V001.md:87`:
`S10 | — | DECLINED-IDENTIFICATION | The critical point on the diagnostic scalar source
quotient is expressly not identified`. It is not a formation criticality.

### 5.2 MARK 2 — AN ORDER PARAMETER, OR ANYTHING PLAYING THAT ROLE — **ABSENT (0)**

`order[ _-]parameter`: 4 files, which are 2 mirror pairs. **All four opened.** Both are
homonyms:

```text
COMPLETE_QSPEC_FINITE_HOLONOMY_RESPONSE_NUMERICS_PROTOCOL_V001.md:32
  "Use the expected second-order parameter and time tails"
  -> numerical convergence order in an error-enclosure protocol.

STAGE8_SCHUR_RATIO_PROTECTION_SCOPE_ATTACK_V001.md:79
  "already declared 3+2 order parameter."
  -> a declared stabilizer/split label in an SU(5) branch-projection argument,
     and the same sentence continues: "It also says that the calculation does
     not derive the adopted split, action normalization, threshold positions,
     weak line scale, regulated determinant, or alpha."
```

Neither is a quantity that is zero in one phase and nonzero in another, and nothing else
in the corpus plays that role. **Nothing playing the role was found either:** the nearest
structural analogue is the endpoint contrast `C_0 = P_1-P_0 = +/- Z` with
`spec(C_0)={-1,+1}` — but its spectrum is `{-1,+1}`, never zero, so it cannot distinguish
a formed from an unformed phase, and its own gate says
`This normalization belongs to the record observable. It does not fix the dimensionful
coefficient multiplying it in a Hamiltonian or action.`

### 5.3 MARK 3 — A RATE, ONSET RATE, OR NUCLEATION-LIKE QUANTITY — **ABSENT (0)**

`nucleat`: exactly 1 file, **opened**. The match is not corpus content — it is a regex
literal inside another lane's method section:

```text
STAGE8_PRODUCTION_VS_DESCRIPTION_O13SR_AUDIT_V001.md:95
       record|nucleat|come into being|brought into being|coming into being)
```

i.e. a *previous commission searching for the same thing and displaying its pattern*.
Substantive count: **zero**. There is no nucleation rate, no onset rate, no formation
rate anywhere in either root. (The corpus does carry `rate` objects — the W-1/W-3
subvolume and HS rates — but those are decay/estimate rates in the wall analysis, not
rates of record formation; and `MO2_KAPPA_RATE_S9AD_V001.md:48` books
`does_not_derive_any_rate = true`.)

### 5.4 MARK 4 — SYMMETRY BREAKING, SPONTANEOUS OR OTHERWISE — **ABSENT AT FORMATION SITES**

34 files, 74 matching lines, **all 74 read** (§3.1). Every one falls into exactly three
buckets, none of which is a formation site:

```text
BUCKET 1 (majority) — W-1's refuted admissibility gate T-3:
  "every admissible topology for W-1 must be generated by SYMMETRY-BREAKING seminorms"
  and its refutation: "T-3 admissibility gate ... REFUTED"
  (STAGE8_W1_SUBVOLUME_RATE_O5SR_AUDIT_V001.md:41, :72, :338)
  -> a claim about seminorm topologies, booked REFUTED.

BUCKET 2 — producer-feasibility state weights:
  "THE DISCLOSED AUTHORED RULE turns it into a symmetry-breaking state weight."
  (STAGE8_DESC_PRODUCER_FEASIBILITY_CHECK_CODEX2_V001.md:235)
  -> an AUTHORED rule, disclosed as such.

BUCKET 3 — action-comparison data, hypothetical and absent:
  "A newly authored physical symmetry-breaking datum could change that"
  (STAGE8_TASK4A_ACTION_COMPARISON_SQUARE_ADOPTION_PROPOSAL_LANE2_V003.md:360)
  -> conditional on a datum that does not exist.
```

Zero occurrences at any of the thirteen formation sites. The word `spontaneous` itself
occurs 3 times post-bar, in one artifact, and never at a formation gate; its most
relevant occurrence is a **denial**:
`IT TRANSMITS GENUINE DRIVE FROM A NONTRIVIAL LOCAL-CHART HISTORY BUT DOES NOT
SPONTANEOUSLY IGNITE TWO HISTORY-FREE READY SYSTEMS.`
(`STAGE8_TASK4A_NETWORK_SOURCING_LAW_ADOPTION_PROPOSAL_CODEX_LANE2_V001.md:10`). The
other two are `add a spontaneous source merely to make the tower deepen` (:91, a
prohibition) and `a spontaneous-history rule would be another adoption` (:719).

**This is worth stating plainly: the only three times this corpus uses the word
"spontaneous", it is refusing to use the concept.**

### 5.5 MARK 5 — AN EXTREMIZED QUANTITY WHOSE EXTREMIZATION MAKES FORMATION HAPPEN — **PRESENT AS EXTREMIZATION, BUT NOT AS CAUSE**

Extremizations are present and they are load-bearing:

```text
E-1  "The least positive common zero is tau_R=pi/sqrt(2)."
     BID_FIRST_OPENING_INTERVAL_DERIVATION_V001.md:75-79
E-2  "a shortest relative projective path on the unique physical record cell"
     BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:99-101
E-3  "The least positive T_R is attained at the boundary of the recoverable set: C_R = 1."
     BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:51-57
E-4  "lambda_max tau_R = pi IS the first-orthogonality criterion — one origin, not two
     derivations; S(tau) = sin^2(sqrt2 tau/2), S(tau_R) = 1"
     STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md:154-157  (CS-6)
E-5  "the record-only least-positive interval tau_R=pi/sqrt(2) supplies a check rather
     than a normalization premise"
     quoted at STAGE8_GAMMA_K_FINITE_REVERSIBLE_WRITE_OBS05_TEST_V001.md:173-179
```

**ON THE WORD "SATURATION", DETERMINED AT BYTES.** The commission asks whether the
corpus's "saturation" is extremization or something else. It is **attainment of equality
in a previously derived inequality — and the corpus books the attainment as ADOPTED, not
derived.** Three independent byte-level witnesses:

1. `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:106-108`, quoted whole:
   `The lower bound is derived from the imported theorem. Saturation is adopted, not
   derived. Historical target blindness is not claimed.` With the status flags at
   `:182-183`: `relative_onset_saturation_derived = false` /
   `relative_onset_saturation_adopted_Level_1 = true`.

2. `STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md:57-67`, quoted whole:
   > ## Five Genuine Pure Minimality Sites
   >
   > The recovery sweep identifies local minimality or saturation content at these
   > five pure sites:
   >
   > 1. `d = 4`: a derived lower bound is followed by an adopted lower-bound
   >    selection.
   > 2. `C_R = 1` record cell: a strict untrapped inequality leaves a half-line; the
   >    marginal public-closure rule supplies the selected boundary point.
   > 3. orthogonalization budget saturation: the lower bound is derived, but
   >    saturation is adopted rather than derived.
   > 4. `tau = 1` proper-time floor: a positive floor multiplier remains possible; the
   >    unit floor is a frozen branch convention, not a theorem.
   > 5. spin `1/2`: the sign selects half-integer classes; primitive minimality
   >    selects the lowest class.

   and the type gloss at `:38-41`: `These are local equality, least-member, or
   single-normalization moves.`

3. The boundary-closure's own refusal, CS-8, quoted whole in §7.

**Determination.** Saturation in this corpus is **not** the extremization of a functional
whose stationarity produces the record. It is *equality-selection on the boundary of an
admissible set that a derived inequality has already carved out* — a bookkeeping move,
made by the program, and labelled as such. E-1 and E-4 are different: `least positive
common zero` and `first-orthogonality criterion` are **derived** least members, and they
do fix the moment. But a least positive root is a property of a condition's solution set,
not an extremal principle that drives anything.

**The corpus states the negative directly.**
`DURABLE_POINTER_CLOSURE_OPERATOR_SELECTOR_GATE_V001.md:109-110`, quoted whole:

> An energy splitting proportional to `C_0` makes the endpoint sectors
> stationary under that component. Stationarity alone is not durability.

That is the corpus, unprompted, denying that stationarity suffices to produce a durable
record.

### 5.6 Q2 GRADE

**PARTIAL.**

| Mark | Status | Basis |
|---|---|---|
| Critical/threshold condition on formation itself | **PRESENT** (3: M1-a, M1-b, M1-c) | `C_R<=1` with `C_R>1` trapped; `theta=pi/2`; thresholded nonreturn with `critical threshold points ... measure zero` |
| Order parameter, or anything in that role | **ABSENT** (0 substantive of 4 raw) | both hits homonyms; no zero-to-nonzero phase indicator exists |
| Rate / onset rate / nucleation-like quantity | **ABSENT** (0 substantive of 1 raw) | the one hit is another lane's search string |
| Symmetry breaking, spontaneous or otherwise | **ABSENT at formation sites** (0 of 74 lines) | all 74 lines are seminorm-topology (refuted), authored state weights, or hypothetical data |
| Extremized quantity whose extremization makes formation happen | **PARTIAL / NO** | extremization present (E-1..E-5) but "saturation" is *adopted equality-selection*, and `Stationarity alone is not durability` |

**PRESENT: 1.5 of 5 marks** (threshold fully; extremization in form but not in causal
role). **ABSENT: 3 of 5** (order parameter, rate, symmetry breaking) — and these three
are exactly the marks that would show a *phase transition*. A spontaneous-formation
reading is not contradicted by this corpus, but it is not *evidenced* by it either: the
corpus has the arithmetic of a threshold without any of the physics of a transition.

---

## 6. Q3 — IS ANY PHASE OR CHARGE QUANTITY A CONDITION VARIABLE AT A FORMATION SITE?

For each site: the variables its condition actually ranges over, quoted from defining
text, then the phase/charge determination.

| Site | Condition, quoted | Variables the condition ranges over | Phase/charge-type? |
|---|---|---|---|
| **S-1** onset budget | `<R_0\|R_1>=0`; `arccos \|<r\|W(tau_*)\|r>\| <= integral_cell d tau Delta H_W(tau)/hbar` | the overlap `<r\|W(tau_*)\|r>` **under modulus**; `Delta H_W(tau)`; `tau` | **NO** — the modulus is taken; phase is discarded by construction |
| **S-2** flux write | `Q_Sigma=0 => no charged-record write; Q_Sigma=1 => ... first orthogonal onset` | `Q_Sigma = integral_Sigma j^mu dSigma_mu`, `spec(Q_Sigma)={0,1}` | **YES — CHARGE-TYPE** |
| **S-2′** onset criterion | `First orthogonal onset gives theta=pi/2, v_z=0` | `theta = \|v\| tau_*/hbar`; `v_z`; `\|v\|`; `tau_*` | **YES — PHASE-TYPE** (`theta` is the phase angle of the survival amplitude `cos(theta)-i(v_z/\|v\|)sin(theta)`) |
| **S-3** charged generator | `Because B_charged is a spectral function of Q_Sigma, [B_charged,Q_Sigma tensor I_R]=0` | `Q_Sigma`, `spec{0,+1,-1}`; projectors `P_0,P_ch,P_+,P_-` built from `Q_Sigma` | **YES — CHARGE-TYPE** |
| **S-4** first opening | `tau_R := least positive common zero of <r\|U_h(tau)^dagger U_k(tau)\|r> = [(1+cos(sqrt(2)tau))/2]^2` | `tau`; `spec(B_h)={-sqrt(2),0,+sqrt(2)}` | **NO** — and the text adds `The result is unchanged by edge orientation reversal or endpoint rephasing`, i.e. explicitly phase-invariant |
| **S-5** cell closure | `C_R = 2 G E_R / (c^4 R_R) <= 1`, marginal at `C_R = 1` | `G, E_R, c, R_R` | **NO** |
| **S-6** boundary law | `When a primitive durable record forms, the boundary law selects one physical Lorentz-covariant interval, support region, and closure configuration` | formation event; interval; support region; closure configuration | **NO** |
| **S-7** incidence support | `support(L_c) is contained in Omega_c` | `L_c`, `Omega_c` | **NO** |
| **S-8** durability | `durability consists jointly of thresholded source nonreturn and exact completed-record persistence` | `A_source(t)`, `T_delta`, `delta`; completed-record restrictions | **NO** |
| **S-9** durability no-go | `[c_partial, \|p><p\|] != 0` (nondemolition failure); `U(2 tau_R) = I` | `c_partial`, `tau_R`, `spec(c_partial)` | **NO** |
| **S-10** allow/require | `It constrains when declared support can become a durable public record` | declared support; durability | **NO** (no variables stated at bytes) |
| **S-11** write typing | `TYPE = self-adjoint source-controlled record-incidence generator ...` | the generator; the carrier; `Q_Sigma` via "source-controlled" | **YES, inherited** from S-3 |
| **S-12** composition | `a public record is formed only after the source-controlled/write-and-persist structure succeeds` | source control (= `Q_Sigma`); write; persistence | **YES, inherited** from S-2 |
| **S-13** pointer closure | `[C_ptr,P_0]=[C_ptr,P_1]=0` | `C_ptr`, `P_0`, `P_1` | **NO** |

### 6.1 THE CITATION

`SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:22-44`, already quoted whole at §4/S-2.
The operative pair:

```text
Q_Sigma = integral_Sigma j^mu dSigma_mu,
spec(Q_Sigma)={0,1}.

Q_Sigma=0  => no charged-record write;
Q_Sigma=1  => the charged-record channel reaches first orthogonal onset.
```

`Q_Sigma` is a **flux of a current through a surface** — charge-type by construction —
and it is the *antecedent of the implication whose consequent is whether a write occurs*.
That is precisely "an argument of the condition that determines whether formation
occurs". Reinforced at `BID_CHARGED_HANDLE_ACTIVATION_DERIVATION_V002.md:75-79`:
`Because B_charged is a spectral function of Q_Sigma, [B_charged,Q_Sigma tensor I_R]=0.`

The phase-type companion, same gate, `:54-65`:

```text
The ready-state survival amplitude is
cos(theta)-i(v_z/|v|)sin(theta),  theta=|v| tau_*/hbar.
First orthogonal onset gives theta=pi/2, v_z=0.
```

### 6.2 THE ADVERSE HALF — WHERE PHASE IS PRESENT AND EXPRESSLY EXCLUDED

Quoted whole so the finding is not read as stronger than it is.

**(i) The FS gate takes a modulus.**
`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:67-72`:
`arccos |<r|W(tau_*)|r>| <= integral_cell d tau Delta H_W(tau)/hbar.` The condition sees
`|·|`. Whatever phase `<r|W|r>` carries is annihilated before the condition is evaluated.

**(ii) The identity phase `chi` is outside the condition.**
`SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:91-121`, quoted whole:

> The family
>
> ```text
> U_chi
>   = (I-Q_Sigma) tensor I_R
>     + exp(-i chi) Q_Sigma tensor (-iY_R)
> ```
>
> has the same conditional record projectors for every `chi`.
>
> In a fixed source basis, a coherent zero/one-flux superposition produces
> different joint source-record density matrices for different `chi`, while
> the reduced record density is unchanged. [...]
>
> This algebra does **not** establish that the phase is physically observable.
> Coherence between the local-flux alternatives must belong to one admissible
> total-charge sector, and an allowed phase reference or recombination
> operation must exist. If the two alternatives are different total-charge
> sectors, a charge-superselection rule can obstruct the comparison.
>
> Therefore the current conclusion is only:
>
> ```text
> chi is not fixed by the conditional record projectors.
> ```
>
> Its gauge-invariant physical effect is open.

**(iii) The first-opening interval is rephasing-invariant.**
`BID_FIRST_OPENING_INTERVAL_DERIVATION_V001.md:87-89`: `The result is unchanged by
edge orientation reversal or endpoint rephasing.`

**(iv) The charge that IS in the condition is a *declared branch input*, not a derivation.**
`SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:32-34`: `This is a local branch input,
not a derivation of every current-flux spectrum.` And `:35`: `For the charged record
handle, adopt:` — the implication itself is **adopted**.

### 6.3 Q3 GRADE

**PHASE-IS-A-CONDITION-VARIABLE — at S-2/S-2′/S-3 (and inherited at S-11, S-12), and
charge-type is the stronger of the two.**

Cited: `Q_Sigma = integral_Sigma j^mu dSigma_mu, spec(Q_Sigma)={0,1}` with
`Q_Sigma=0 => no charged-record write; Q_Sigma=1 => the charged-record channel reaches
first orthogonal onset` (`SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:27-40`);
`theta=pi/2` at `:60-65`; `B_charged` a spectral function of `Q_Sigma` at
`BID_CHARGED_HANDLE_ACTIVATION_DERIVATION_V002.md:75-79`.

**Qualified in three ways, and the qualifications are load-bearing:**
1. It holds at **one gate family** (the charged-flux write), not corpus-wide. Eight of
   thirteen sites grade **PHASE-PRESENT-BUT-NOT-IN-THE-CONDITION** or contain no phase
   at all.
2. The implication is **adopted** (`For the charged record handle, adopt:`) and the flux
   spectrum is a **declared branch input**, not derived.
3. The corpus's flagship phase quantity, `chi`, is expressly **not** in any formation
   condition, and the FS gate's condition is phase-blind by construction.

So: a charge-type quantity does sit in a formation condition at bytes; a *phase* proper
sits there only as the onset angle `theta`; and the corpus's own phase degrees of freedom
are, where the corpus has looked, **outside** the conditions that decide formation.

---

## 7. Q4 — DOES THE BOUNDARY-CLOSURE EVER ACT?

**Object (CHOICE LEDGER C-1):** the cell's boundary closure, as the corpus itself types
it, under the heading `## 2. SUB-TASK (1) — THE CLOSURE STRUCTURE, TYPED EXACTLY` at
`STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md:109-180` (sealed, OK; audit sealed, OK). The
artifact's own scope note, `:111-113`, quoted whole:

> What the sealed record actually holds about the cell's boundary closure.
> Sweep basis: the tasked stock of §1, read at the cited bytes; this typing
> claims exhaustiveness at that displayed sweep only.

Nine clauses, `CS-1` .. `CS-9`. **Every clause was opened and read in full.** For each:
the operative verb and its object, then the classification.

### 7.1 THE CLAUSE-BY-CLAUSE CENSUS

**CS-1 — THE PROJECTOR PROMOTION** `:116-123`
> CS-1 THE PROJECTOR PROMOTION (E1 C4 :339-352, frozen input, reviewer-
>      confirmed in exact Q(sqrt2)): at full tau_R, lambda tau_R in
>      {0, +pi, -pi}, on the ball where M(t) = 1, exp(-i lambda tau_R S) =
>      (+1,-1,-1)·I EXACTLY (S^2 = I); sum_lambda w_lambda phase_lambda =
>      1 = sum_lambda |w_lambda| (l1 step TIGHT, zero-sum weights buy NO
>      decay); operator level: sum_lambda w_lambda u_lambda = m_0(I-P) +
>      1·P = P. Re-pinned WB2 (linear in P; no idempotency needed).
>      GRADE: exact, WEIGHT-SUM-level (needs the sum over lambda).

Operative verb: **`=`** (`exp(...) = (+1,-1,-1)·I`; `sum_lambda w_lambda u_lambda = P`).
Object: the weighted sum of branch unitaries. The clause is an *identity*; "promotion"
names the fact that a weighted sum equals a projector, not an act performed on anything.
Its own grade is a level-of-applicability label. → **CLASSIFIES**
(it grades a quantity's level: `WEIGHT-SUM-level`).

**CS-2 — THE R.2 COLLAPSE DISPLAY** `:124-130`
> CS-2 THE R.2 COLLAPSE DISPLAY (E1 :739-762): V_{mu lambda}(0) - 1 =
>      (phase_mu phase_lambda - 1)P = 0 on SAME-phase pairs, -2P on
>      OPPOSITE-phase pairs; weight bookkeeping S_+ = 1/2, S_- = -1/2;
>      offered as obligations R-L4a/b, never as results; the opposite-phase
>      tr A(0) = -2 tr(CPC) = -infinity (C2's own divergence).
>      GRADE: exact display; BASELINE-level; the same-phase branch holds
>      "up to the C-L2 commutator error" AT BYTES (:754-756).

Operative verb: **`=`**, and the self-description **`display`**. Object: `V_{mu
lambda}(0) - 1`. Note the adverse clause quoted in the original:
`offered as obligations R-L4a/b, never as results`. → **CLASSIFIES**
(it partitions pairs into SAME-phase and OPPOSITE-phase and states the value on each).

**CS-3 — THE BOUNDARY-SUPPORTED COMMUTATOR** `:131-138`
> CS-3 THE BOUNDARY-SUPPORTED COMMUTATOR (3b5e95b6, of record, audited):
>      kappa_n := ||[C_n, P]||_2^2 = 2 sum_i s_i(1-s_i) -> +infinity at the
>      full-family quantifier (every subsequence), radius-independent by
>      the exact dilation invariance of the sealed sea symbol; the
>      continuum ||[C, 1_B]||_2 = +infinity is E1 C6 frozen input.
>      GRADE: exact and DIVERGENT — the closure's boundary commutator
>      carries INFINITE HS mass along the family; it is a divergence
>      source, not a budget.

Operative verb: **`:=` / `carries`**. Object: HS mass `kappa_n`. The clause states a
magnitude and its limit. Its own closing phrase — `it is a divergence source, not a
budget` — is a type judgement. → **CLASSIFIES**.

**CS-4 — THE POISONING CEILING** `:139-145`
> CS-4 THE POISONING CEILING (r-2 X-1 chain, re-pinned WB8 at its identity
>      layer): |det_n(0)|^2 <= exp(-2 kappa_n), so -log|det_n(0)| >=
>      kappa_n -> +infinity, and ||R_n||_op = 1/min_i|1-2 s_i| <=
>      prod_i 1/|1-2 s_i| = exp(-log|det_n(0)|): any chain through the
>      baseline resolvent carries an exponentially divergent ceiling; no
>      n-uniform bound on ||R_n||_op exists on any sealed artifact (r-3
>      P-1; S4/H-R may NEVER default, E1 :720-727 F'-3).

Operative verb: **`<=` / `>=`** — a **ceiling**. Object: `|det_n(0)|`, `||R_n||_op`.
A ceiling is the definition of a bound. → **DELIMITS**.

**CS-5 — THE b_D PROFILE** `:146-153`
> CS-5 THE b_D PROFILE (PA A2 :99-113, bytes): b_D = exp(16 - 1/s),
>      s = (t^2-|x|^2)((1-t)^2-|x|^2) on the open unit diamond, zero
>      outside; C-infinity, compactly supported, vanishing to ALL ORDERS
>      at the diamond boundary and tips; b_D(1/2, 0) = 1. DERIVED here
>      (WB1): 0 <= b_D <= 1 with equality ONLY at the diamond center
>      (unique interior critical point, displayed solve). The record
>      vertex J = -(Q b_D Q) tensor alpha_x has ||J(t)||_op <=
>      ||b_D||_inf = 1 (r-3 P-2, sealed, CAS D5/D6/D7).

Operative verb: **`0 <= b_D <= 1`**, **`compactly supported`**, **`vanishing ... at the
diamond boundary and tips`**. Object: the profile `b_D` and the record vertex `J`.
This is the purest delimiting clause in the set: it bounds a profile and states where it
vanishes — an extent statement. → **DELIMITS**.

**CS-6 — THE PHASE-SKELETON SATURATION** `:154-161`
> CS-6 THE PHASE-SKELETON SATURATION (b4034f04, decisive, three grounds):
>      lambda_max tau_R = pi IS the first-orthogonality criterion — one
>      origin, not two derivations; S(tau) = sin^2(sqrt2 tau/2), S(tau_R)
>      = 1 (re-pinned WB2); the product is scale-orbit-invariant (Ground
>      B) and unconditional where the individual values are Gate-4-
>      conditional. The exact monodromies (+1,-1,-1) and the parity
>      grading of the dressed objects are DERIVED of record from this one
>      condition (ae2202e0 verdict, audited).

**ADJUDICATED — this is the clause most likely to be an ACTS, and it is not.**
Operative verb: **`IS`** (`lambda_max tau_R = pi IS the first-orthogonality criterion`).
Object: the criterion itself. The clause *identifies a criterion*; it does not apply one.
The consequence clause is `are DERIVED ... from this one condition` — derivation, not
production. The word `saturation` here means `S(tau_R) = 1`, i.e. the function attains
its maximum value at `tau_R`; that is a *statement of where a value sits*, and the
closure's own §3.2 re-typing of CS-6 confirms it adds nothing operative:
`CS-6 (saturation): the exactness IS the definition of tau_R (one origin); its operator
content at the stay strata is CS-1 + CS-2, both dispatched above. NOT an additional
kill.` → **CLASSIFIES** (it names what the criterion is, and its own re-typing says its
operator content is entirely CS-1+CS-2).

**CS-7 — THE PARITY GRADING** `:162-166`
> CS-7 THE PARITY GRADING (d66a922c KB1-KB3, unconditional matrix
>      identities, audited): the swap law G_{-+}(m) = G_{+-}(-m); K_H =
>      even-insertion strata, H_A = -i K_A = odd-insertion strata; the
>      quartet weight-sum kill is ABSENT from the composite — odd strata
>      DOUBLE (KB4; re-pinned at the S level WB5).

Operative verb: **`=`**, and the noun **`GRADING`**. Object: the strata `K_H`, `H_A`.
The clause partitions objects into even and odd strata. The closure's own §3.2 says of it,
verbatim: `CS-7 (parity): a GRADING, not a kill (RA4b/CA3 of record); the odd strata
DOUBLE (WB5). It DELIVERS the split; it cannot close it.` → **CLASSIFIES**
(the corpus's own word for it is `GRADING`, and it explicitly denies it is a kill).

**CS-8 — THE ONSET/ACTION BUDGETS AT THE CELL EDGE** `:167-173`
> CS-8 THE ONSET/ACTION BUDGETS AT THE CELL EDGE (ae2202e0 Z-2/Z-5, gate
>      cc113a16 at bytes): J_FS,rel >= pi hbar/2 — a LOWER bound, derived
>      from the imported FS/Mandelstam-Tamm theorem, saturation ADOPTED
>      not derived; |Delta S_record| = pi hbar with the bridge gate's bar
>      ("No geometric uncertainty budget may be substituted for physical
>      action without a theorem"); the entry-15 vacancy bars every
>      magnitude-typed landing on today's booked surface (Z-7).

Operative verb: **`>=`** — a **LOWER bound**; and **`bars`**. Object: `J_FS,rel`,
`|Delta S_record|`. A budget is a bound; a bar is a prohibition on the analyst, which
delimits what may be claimed. This is the clause that touches formation most directly
(it is S-1's content re-read), and even here the closure only *bounds*. → **DELIMITS**.

**CS-9 — THE LOCUS RULING** `:174-179`
> CS-9 THE LOCUS RULING (E1 C6 :353-370; the R5 refinement :359-366,
>      ADOPTED OF RECORD, binding): "THE FATAL LOCAL INTEGRAL IS THE
>      VOLUME DIAGONAL x = y, NOT THE SHARP BOUNDARY. SMOOTHING ONLY THE
>      BOUNDARY WILL NOT REMOVE A |x-y|^-3 POSITIVE MAJORANT."
>      Boundary smoothness is demoted to a secondary factor; sharp
>      consumption of M(t) and 1_{D_t} is MANDATED (D6').

**ADJUDICATED — this is the second candidate ACTS, and it is not one either.**
Three operative verbs, taken in turn:
- **`IS`** (`THE FATAL LOCAL INTEGRAL IS THE VOLUME DIAGONAL x = y, NOT THE SHARP
  BOUNDARY`) — object: the divergent locus. Identification of where a thing lives. That
  is a locus statement: **DELIMITS**.
- **`is demoted to`** — object: boundary smoothness. Demotion is a **re-ranking of a
  factor's importance**: **CLASSIFIES**.
- **`is MANDATED`** — object: **`sharp consumption of M(t) and 1_{D_t}`**. This is the
  only imperative in the nine clauses, and it is the reason CS-9 needed adjudication. It
  mandates a *treatment* — how the analyst must consume two functions in an estimate. Its
  object is a step in a proof, not a record, not a formation, not a physical thing. It
  binds the person doing the analysis (`ADOPTED OF RECORD, binding`), not the world.

Under the commission's definition — ACTS = *selects, produces, transforms, forces, brings
something into being* — a mandate on proof method **forces** nothing into being; it
constrains the route. → **DELIMITS** (dominant: it rules on the locus and confines the
analysis to it). **Itemized here rather than buried, because it is the closest any clause
comes.**

### 7.2 THE Q4 TALLY

| Clause | Operative verb | Object | Classification |
|---|---|---|---|
| CS-1 Projector promotion | `=` (identity) | weighted sum of branch unitaries | **CLASSIFIES** |
| CS-2 R.2 collapse display | `=`, `display` | `V_{mu lambda}(0) - 1` | **CLASSIFIES** |
| CS-3 Boundary-supported commutator | `:=`, `carries` | HS mass `kappa_n` | **CLASSIFIES** |
| CS-4 Poisoning ceiling | `<=`, `>=` (ceiling) | `\|det_n(0)\|`, `\|\|R_n\|\|_op` | **DELIMITS** |
| CS-5 `b_D` profile | `0 <= b_D <= 1`, `vanishing at ... boundary` | profile `b_D`, vertex `J` | **DELIMITS** |
| CS-6 Phase-skeleton saturation | `IS` (the criterion) | first-orthogonality criterion | **CLASSIFIES** |
| CS-7 Parity grading | `=`, `GRADING` | strata `K_H`, `H_A` | **CLASSIFIES** |
| CS-8 Onset/action budgets | `>=` (lower bound), `bars` | `J_FS,rel`, `\|Delta S_record\|` | **DELIMITS** |
| CS-9 Locus ruling | `IS`, `is demoted to`, `is MANDATED` | divergent locus; boundary smoothness; proof method | **DELIMITS** |

```text
DELIMITS  : 4   (CS-4, CS-5, CS-8, CS-9)
CLASSIFIES: 5   (CS-1, CS-2, CS-3, CS-6, CS-7)
ACTS      : 0
                 --
TOTAL     : 9/9
```

### **EVERY CLAUSE OF THE BOUNDARY-CLOSURE DELIMITS OR CLASSIFIES. NONE ACTS.**

Said plainly, as the commission requires: **there is no clause of the boundary-closure
that selects, produces, transforms, forces, or brings anything into being.** Not one of
the nine has a physical object that it does something to. Four state bounds and extents;
five state identities, gradings, and type judgements. The closure's own summary of its
most operative-looking clause is `It DELIVERS the split; it cannot close it`, and of its
most formation-adjacent clause, `typed refusal, three independent grounds`.

**This is the commission's cleanest test, and it comes back clean in the direction of
"boundary, not former."**

### 7.3 THE SECONDARY CENSUS (CHOICE LEDGER C-2) — so scoping cannot hide an ACTS

Every occurrence of `closure` in the thirteen formation sites and the three root
principles was listed and read. Eleven clauses, none an ACTS on a record:

```text
FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md
  :86   "...and record closure belong to one complete quantum specification"    DELIMITS (membership)
  :94   "closure map, branch enumeration, and matching rule"  [Q_spec must fix]  DELIMITS (inventory)
  :106  "...interval, support region, and closure configuration over which its
         action is accumulated"                                                 DELIMITS (extent)
CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md
  :27   "Once the future boundary has crossed the closure face of Omega_c, the
         same primitive incidence is absent from the active generator"          DELIMITS (extent/edge)
  :68   "the same primitive incidence remains active after its closure face"    CLASSIFIES (falsifier)
CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002.md
  :21   "every new primitive cell opens after all closure faces of the prior
         stage"                                                                 DELIMITS (ordering)
BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md
  :137  "The post-closure contrast P_1-P_0 is Hilbert-Schmidt orthogonal to Y"  CLASSIFIES
  :172  "post-closure pointer block, causal cell, and durability map"           DELIMITS (obligation list)
BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md
  :49   "Marginal public-closure rule (adopted Level-1 Gravacle rule)"          see S-5: MIXED,
  :83   "The marginal public-closure rule therefore has one positive root"      program adoption
DURABLE_POINTER_CLOSURE_OPERATOR_SELECTOR_GATE_V001.md
  :31-33 "This condition says that the closure component used to stabilize/read
          the record does not itself mix the two declared durable endpoint
          sectors. It does not say that C_ptr writes, amplifies, or redundantly
          copies the record."                                                   DELIMITS + explicit
                                                                                 non-action disclaimer
```

The only closure object in the corpus that carries selector language is the **marginal
public-closure rule** (S-5), and its selection is over *admissible values of a duration*,
made by the program, on a half-line the physics leaves open. It does not act on a record.

**Secondary census result: 0 ACTS. The Q4 finding does not depend on CHOICE LEDGER C-1.**

---

## 8. Q5 — THE DISCRIMINATOR

Done last, done hard. The question: **is there any text at bytes that would be TRUE under
one reading and FALSE under the other?**

The four hunts the commission specified were run in turn. Nothing in this section is
manufactured; where a candidate fails, the failure is shown.

### 8.1 HUNT 1 — A CLAUSE WHOSE SATISFACTION REQUIRES A PERFORMER

Sweep P-12 (`external selector|selects when|chooses when|picks the moment|no external
agent|performer|performs the`): **63 files** post-bar-62, comprising **95 matching lines**
(69 in R1, 26 in R2). All 26 R2 lines were displayed and read in full; the R1 lines are
the mirror set plus lane-process text. The overwhelming majority is **process language
about lanes, scripts, audits, and builders** (`V003 performs the bounded repair`,
`parent.py now performs the following fail-closed sequence`, `Builder B performs the V010
re-pin`). **Four are physical.** All four are treated below; three fail immediately and
one is the corpus's best candidate.

Sweep P-13 (`agent`): **143 files** post-bar-142, comprising **220 matching lines**
(127 in R1, 93 in R2). Rather than assert a count I could not fully display, I ran the
complement: every matching line across both roots with process-language forms filtered
out, and read the residue. Result, stated exactly:

```text
OCCURRENCES OF "agent" IN THE FOURTEEN FORMATION-SITE ARTIFACTS ............ 0
  (checked file by file; every one returns 0:
   BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003, SOURCE_FLUX_CONDITIONED_
   RECORD_WRITE_GATE_V003, BID_CHARGED_HANDLE_ACTIVATION_DERIVATION_V002,
   BID_FIRST_OPENING_INTERVAL_DERIVATION_V001, BID_MINIMAL_PUBLIC_CAUSAL_CELL_
   DERIVATION_V001, FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002,
   CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001, CAUSAL_DIRECT_LIMIT_RECORD_
   PRINCIPLE_V001 and V002, BID_FINITE_RECORD_DURABILITY_NO_GO_V001,
   STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001, STAGE8_GAMMA_K_FINITE_
   REVERSIBLE_WRITE_OBS05_TEST_V001, STAGE8_GAMMA_K_SOURCE_RECORD_COMPOSITION_
   TYPING_TESTS_V001, DURABLE_POINTER_CLOSURE_OPERATOR_SELECTOR_GATE_V001)

NON-PROCESS USES FOUND ANYWHERE .......................................... 8
  and every one of them is a PRIOR COMMISSION'S OWN GLOSS, not corpus text:
    STAGE8_INGREDIENT_CENSUS_O17SR_V001.md:373
      (ii) "the complete microscopic parent" — the assigning agent.
    STAGE8_INGREDIENT_CENSUS_O17SR_AUDIT_V001.md:317
      the U-1 parent is the agent that ASSIGNS the
    STAGE8_STRATIFICATION_O27SR_V001.md:200      "the assigning agent"
    STAGE8_JOIN_ADMISSIBILITY_O28SR_V001.md:582  "the assigning agent"
    STAGE8_CONSTRAIN_OR_CREATE_O36SR_AUDIT_V001.md:151 "as the assigning agent"
    STAGE8_BOUNDARY_SHAPES_O32SR_V001.md:400     "AGENT. And that agent is the census's U-1"
    STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md:483    "the destroying agent"
    STAGE8_IS_RL2B_FAILURE_FORCED_BY_F5_DETERMINATION_V001.md:80
      "F'-5 is not the forcing agent"   [a proof-forcing agent, not a physical one]
```

**This correction matters and is reported rather than smoothed.** An earlier draft of this
section asserted that `agent` never occurs physically; that was an overclaim, and the
complement sweep refuted it. The accurate finding is sharper and more interesting:

> The word `agent` appears **nowhere** in the corpus's own formation texts. Every physical
> use of it is a **prior lane in this same commission series reaching for a word the corpus
> does not supply** — glossing `the complete microscopic parent assigns one Lorentz-covariant
> causal cell Omega_c` (`CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md:18-20`) as "the
> assigning agent". The corpus wrote `assigns`. The readers wrote `agent`.

That gap between what the corpus says and what its readers reach for is the single most
suggestive datum in this report, and it cuts **against** OPERATED, not for it: the agency
is being supplied by the reader.

The four physical `performs` hits:

```text
(a) BID_CHIRAL_SOURCE_RECORD_INCIDENCE_PARENT_V001.md:175
    "The same unit matrix element therefore performs the record transition and the
     chiral-odd source transfer."
    -> the performer is a MATRIX ELEMENT. A number does not select a moment. FAILS T-b/T-c.

(b) STAGE8_AXN0_ACTION_AS_ROLE_DARIO_V001.md:139 (and :34)
    "no sealed joint saddle performs the conversion"
    -> a NEGATIVE, and about a conversion between action objects, not formation. FAILS.

(c) STAGE8_COLLAR_CONJUNCTION_CHARTER_RESULT_V001.md:80
    "NO ARTIFACT PERFORMS THE CONVERSION."
    -> a NEGATIVE about artifacts. FAILS.

(d) CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md:25   *** THE CANDIDATE ***
    "the finite parent performs the local reversible write"
```

### 8.2 CANDIDATE (d), TAKEN SERIOUSLY

Quoted whole, with its two sibling clauses, because a partial quote would misrepresent it
(`CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md:18-30`):

> In the ordinary `3+1` flat-asymptotic branch, a durable public record is the
> compatible outgoing sector of a future-directed Lorentz-covariant causal
> exhaustion governed by the same finite source-record parent.
>
> The physical content has three inseparable parts:
>
> 1. the finite parent performs the local reversible write;
> 2. its absolutely-continuous source tail gives thresholded source-root
>    nonreturn for every admitted finite-energy `L2` excitation; and
> 3. one-use causal support leaves completed record factors exactly invariant,
>    producing a compatible quasi-local output state, its GNS
>    representation, and recoverable central record sequences.

**Is this TRUE under OPERATED and FALSE under SPONTANEOUS?** No, and here is why at bytes.

The performer is `the finite parent`. The record is **a tensor factor of the finite
parent**: `A_SR(C)=CAR(K_Sigma,q) graded-tensor R(C)`
(`BID_GLOBAL_CAR_RECORD_COMPOSITION_DERIVATION_V001.md:80-88`, quoted at
`STAGE8_GAMMA_K_FINITE_REVERSIBLE_WRITE_OBS05_TEST_V001.md:87-92`). A closed system
"performing" on its own subsystem is unitary evolution of the whole — the very thing the
commission has already ruled non-decisive. It fails **T-b (externality)**.

It also fails **T-c (selection of the moment)**. Nothing in the three parts names a time
at which the parent decides to write. Part 2 supplies a *threshold*; part 3 supplies
*invariance*. The moment, where the corpus states it, is a root: `The least positive
common zero is tau_R=pi/sqrt(2)` (S-4).

And the sibling clauses are grammatically parallel but semantically non-agentive —
`its ... source tail **gives** thresholded ... nonreturn`, `one-use causal support
**leaves** completed record factors exactly invariant`. The list mixes one agentive verb
with two stative ones for the same kind of content, which is prose variation, not a typed
claim about agency.

**Verdict on (d): NOT A DISCRIMINATOR.** True under both readings.

### 8.3 HUNT 2 — A CLAUSE THAT EXPLICITLY DISCLAIMS A PERFORMER

Three were found. Each disclaims *something*, and none disclaims what would be needed.

**(i)** `DURABLE_POINTER_CLOSURE_OPERATOR_SELECTOR_GATE_V001.md:31-33`, quoted whole:

> This condition says that the closure component used to stabilize/read the
> record does not itself mix the two declared durable endpoint sectors. It does
> not say that `C_ptr` writes, amplifies, or redundantly copies the record.

Disclaims that the closure component acts. **Supports Q4; does not discriminate Q5**,
because it is silent on whether *anything else* performs formation.

**(ii)** `STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md:479-481` (sealed, OK),
quoted whole:

> P0 is therefore a *joint derivational provenance/dynamics source*.  It is not a
> source-sector action that temporally creates the record carrier.  Q-43 says
> record degrees exist as ready labeled factors before a write and a durable
> record forms only after write-and-persist succeeds

This is the strongest disclaimer in the corpus — but read its object. It denies that a
source action **temporally creates the record CARRIER**. The carrier is antecedent by
construction. It says nothing about whether the *durable record's formation* is operated
or spontaneous; indeed the same sentence describes that formation as happening `only
after write-and-persist succeeds`, which is condition language. **Does not discriminate.**

**(iii)** `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V001.md:38`: `It does not say that
gravity or unrelated record channels...` — a scope disclaimer about which channels the
gate governs. **Does not discriminate.**

### 8.4 HUNT 3 — A FORMATION STATEMENT WHOSE TRUTH CONDITIONS MENTION AN AGENT

The best candidate is the corpus asking the commission's own question and answering Yes —
`STAGE8_GAMMA_K_FINITE_REVERSIBLE_WRITE_OBS05_TEST_V001.md:66-71` (sealed, OK), quoted
whole:

> ## Answer 1 - Is The Write Specified As An Operation?
>
> Yes, at the primitive finite source-record level, the write is specified as a
> source-controlled self-adjoint record-incidence generator on the source-record
> tensor carrier, with its finite completed-transfer evolution giving a reversible
> unitary write.

**Is this the discriminator?** It is the closest the corpus comes, and it is not.
The *truth conditions* it supplies are, verbatim from `:124-130`:

```text
TYPE = self-adjoint source-controlled record-incidence generator on the
       finite source-record tensor carrier, whose finite completed-transfer
       evolution is the reversible write.
```

Every term is structural: a generator, a carrier, an evolution. **No term of the type
mentions an agent, a decision, or a moment of application.** Under SPONTANEOUS, a record
that forms because a condition obtains still needs a generator and still evolves — so
this sentence is true under SPONTANEOUS too. Its "Yes" is a yes to *"is there an operator
in the mathematical sense"*, not to *"is there an operator in the agentive sense"*.

Confirming this at bytes: the same artifact's own headline verdict is that the *only*
thing which could have made the write agentive — a strength it carries — is absent:
`WRITE_CARRIES_SURVIVING_STRENGTH_PARAMETER = false`, `continuous_write_strength_parameter
= false`, `coefficient_family_collapsed_to = (0,1,1)`. A "write" with no free parameter
and a fixed coefficient triple is a structural map, not an act with a dial.

**Verdict: NOT A DISCRIMINATOR.**

### 8.5 HUNT 4 — WHERE THE CORPUS REMARKS ON THE DISTINCTION ITSELF

It does, twice, and both are governing. Quoted whole as required.

#### 8.5.1 THE REFUTED FORCED BINARY

`STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md:9-35` (sealed, OK), quoted
whole:

> ## 0. Lead finding
>
> The forced binary is refuted. The class is **GENERATIVE in relay 184's
> derivational-provenance sense**: its package is `produced by` and `all derived
> from` one joint operator. The theory is simultaneously **FORMATION in physical
> order**: an antecedent carrier exists, then a durable record forms only when
> the stated conditions succeed. One joint source-record-field operator can
> derive state/effects/domains/dynamics on a carrier that exists before a
> particular write. Therefore relay 184's assertion that its GENERATIVE category
> `Contradicts formation` does not survive the class-plus-Q-43 witness.
>
> Q-43 does **not** refute the GENERATIVE provenance reading actually required by
> the class. It refutes a stronger proposition: that a particular source action
> or the source algebra creates the antecedent record carrier, ready state, or
> independent record degrees. Q-43 itself says independence of degrees is not
> independence of dynamics.
>
> The theory's physical record ontology is nevertheless FORMATION: a carrier and
> ready state precede the write, and a durable public record forms only after
> source-controlled write, nonreturn, and persistence succeed. The rank-1 class
> adds a common-origin **derivational provenance** demand. These statements are
> compatible because they answer different questions:
>
> ```text
> physical order:        antecedent carrier -> write/nonreturn/persistence -> durable record
> provenance obligation: one joint microscopic framework -> state/effects/domains/dynamics/interface
> ```

and its typed booking at `:42-51`, quoted whole:

> ```text
> ROOT_PHYSICAL_ONTOLOGY = FORMATION
> CLASS_DEFINITION_READING = GENERATIVE_DERIVATIONAL_PROVENANCE
> PRODUCER_PROVENANCE_REQUIREMENT = COMMON_ORIGIN_DERIVATION |
>   derived = false | TYPE-U |
>   would-build: instantiate P0-P7 and prove all required common-origin descent
> relay_GENERATIVE_and_FORMATION_are_mutually_exclusive = false | TYPE-R |
>   test: NONEXCLUSIVE-AXIS-WITNESS; one antecedent carrier, one joint operator
>   deriving state/effects/domains/dynamics on that carrier, and later durable
>   formation under write/nonreturn/persistence satisfy both relay definitions
> ```

#### 8.5.2 THE NO_VERDICT FOR WANT OF A SEALED OPERATIONAL DEFINITION

Same artifact, `:71-80`, quoted whole:

> ```text
> P5_supplied_by_unambiguously_means_temporal_production = NO_VERDICT |
>   reason: no sealed operational definition selects temporal creation over
>   derivational/common-framework provision
>
> rank1_class_requires_source_action_to_create_record_carrier = NO_VERDICT |
>   reason: the definition says "derived from" a joint operator, while P3
>   preserves the antecedent carrier and no temporal carrier-generation map is
>   stated
> ```

and the sweep that grounds it, `:381-401`, quoted whole:

> ```text
> sealed_temporal_definition_of_P5_supplied_by_found = false | TYPE-S |
>   roots: R1, R2, R3 and exact R4 |
>   excl: Section 2.1; relay 184's proposed gloss; ordinary-language inference;
>   R4 is unadjudicated under Q-77 and contains no temporal definition |
>   fences: exact semantic wording query; explicit P0-P7/T1-T7 inspection; and
>   the 31-path word-boundaried A/B intersection above |
>   query: exact phrases supplied by the same microscopic source, produced by
>   one microscopic operator, all derived from one microscopic source-record-
>   field; plus comm -12 of effect/rho_pre files and supplied-by files |
>   ...
>   qualifying_definition_file_list: EMPTY
> ```

### 8.6 DOES THE PRIOR MATERIAL GOVERN THIS QUESTION? — **YES, AND ALMOST EXACTLY.**

The neighbouring binary was `GENERATIVE` (package `produced by` one operator) versus
`FORMATION` (antecedent carrier, then a record forms when conditions succeed). Map it
onto the present one:

```text
prior:   GENERATIVE  ~ "produced by an operator"     |  FORMATION  ~ "forms when conditions succeed"
present: OPERATED    ~ "image of an operation"       |  SPONTANEOUS ~ "condition obtains, formation follows"
```

The prior commission found the two **not mutually exclusive** and booked
`relay_GENERATIVE_and_FORMATION_are_mutually_exclusive = false | TYPE-R`. It then found
that the *one* place the two readings could have been separated — whether `supplied by`
means temporal production or derivational provision — could not be decided, **for exactly
the reason this commission would need: `no sealed operational definition selects temporal
creation over derivational/common-framework provision`.**

Three qualifications, so the governance claim is not overstated:

1. **Scope difference.** The prior NO_VERDICT is about the *carrier* and the *producer
   class's provenance*. The present question is about the *formation event*. They are
   adjacent, not identical.
2. **The prior verdict is partly positive.** It did not merely fail to decide: it booked
   `ROOT_PHYSICAL_ONTOLOGY = FORMATION`, which is the condition-side reading. That is
   evidence *for* the condition reading of the physical order — and §4 of this report
   independently reaches the same place from the gate texts.
3. **What is missing is the same missing thing.** The prior lane needed a sealed
   operational definition of "supplied by" and did not have one. This lane needs a sealed
   operational definition of "performs"/"generated by"/"operation" and does not have one
   either. The vacancy is structural, not accidental.

### 8.7 Q5 VERDICT

## **OBSERVATIONALLY-EQUIVALENT-AT-BYTES.**

No text was found that is true under one reading and false under the other. Every
candidate located — and the hunt was exhaustive over the four specified shapes, with
315 matching lines swept across P-12 and P-13 alone, with the residue after
process-filtering read exhaustively — is true under both.

The corpus's formation vocabulary is **structurally agent-free**: it has generators,
conditions, thresholds, least positive roots, projectors, budgets, and bounds. Where an
agentive verb appears, its subject is a structure inside the theory (a matrix element, the
finite parent, the boundary law, physical boundary degrees of freedom) and never
something outside the record that picks the moment. Where a selection genuinely occurs, the
selector is **the program adopting a premise**, and the corpus labels those adoptions and
books them `derived = false`.

**A null here is the complete and correct answer, and it is not a weak one.** The corpus
does not decide OPERATED versus SPONTANEOUS because it never posed the question, and the
vocabulary it built to answer other questions is blind to it in exactly the way the prior
commission already discovered and booked. This lane manufactures nothing to fill that gap.

```text
DISCRIMINATOR_FOUND = false
Q5_VERDICT = OBSERVATIONALLY-EQUIVALENT-AT-BYTES
PRIOR_NEGATIVE_GOVERNS = true  (with the three qualifications of 8.6)
DISCRIMINATOR_MANUFACTURED = false
```

**What would decide it, stated so a future lane is not tempted to invent one:** a sealed
operational definition distinguishing (a) a generator whose application is *conditioned*
on a stated predicate from (b) a generator whose application is *selected* by something
whose state is not itself a term of that predicate. No such definition exists in either
root. Until one is sealed, this binary is undecidable at bytes for the same reason the
neighbouring one was.

---

## 9. IMPORT AUDIT

Every notion used that is **not defined inside the corpus**, with the survival test.

| # | Import | Where it entered | Corpus-defined? | Does the finding survive without it? |
|---|---|---|---|---|
| I-1 | **OPERATED / SPONTANEOUS** themselves | the commission's framing | **NO** — neither term occurs in either root in this sense (`spontaneous`: 3 occurrences, all refusals, §5.4) | **YES.** Every grade in §4–§7 is stated in the corpus's own vocabulary (condition, adopted, selector, threshold, bound, grading). The imported labels are a reporting layer. Q5's null is *strengthened* by the import's absence, not weakened |
| I-2 | **"agent" / "performer" / "external selector"** as physical categories | the commission | **NO** — `agent` occurs in 143 files / 220 lines, **0 times in any formation-site artifact**; its 8 physical uses are prior lanes' gloss (§8.1) | **YES.** The absence from the corpus's own formation texts *is* the finding, and the readers' supply of it is the second finding |
| I-3 | **"order parameter"** as a phase-transition indicator | the commission | **NO** — the 4 hits are homonyms (§5.2) | **YES.** Reported ABSENT |
| I-4 | **"nucleation"** | the commission | **NO** — the 1 hit is another lane's regex (§5.3) | **YES.** Reported ABSENT |
| I-5 | **Fubini-Study / Mandelstam-Tamm theorem** | used *by the corpus*, and labelled as an import by the corpus itself: `Import the standard Fubini-Study/Mandelstam-Tamm theorem` (`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:67`) | **NO**, and the corpus says so | **YES.** I neither verify nor use the theorem; I only classify the *shape* of the clause that invokes it. The corpus's own flag `relative_orthogonalization_bound_imported = true` is quoted, not relied on |
| I-6 | **Misner-Sharp compactness** | corpus-internal at `BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:38-47`, undefined there | **NO** (named, not defined) | **YES.** The Q2/Q3 grades use only the *form* `C_R <= 1` with `C_R > 1` trapped — a partition of a parameter line — not the geometry behind it |
| I-7 | **`alpha_first_durable_record_capacity_principle_v001.md`** (`N_BR(k_R)=1`) | lives **outside both corpus roots** (one directory up, in `gravity_emergence_evidence_program/`) | **OUT OF CORPUS** | **YES.** Read only *through* the corpus-internal quotation at `STAGE8_GAMMA_K_FINITE_REVERSIBLE_WRITE_OBS05_TEST_V001.md:267-281`. The file itself was not opened. No Q2 mark depends on it; it is cited once as corroboration of M1-c |
| I-8 | **`boundary_access_closure_threshold_principle_v001.md`** | referenced at `STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md:50-55`; **not present in either root** | **OUT OF CORPUS** | **YES.** Not opened, not relied on. Its absence is flagged at F-4 |
| I-9 | **`RECOVERY_STIFFNESS_AND_ALLOW_REQUIRE_2026-07-29.md` / `AXIOM_SET_MAP_2026-07-29.md`** | the authority inputs of S-10, in `/Users/bgm/MB Work/alpha_supervision/` | **OUT OF CORPUS** (that root is not in my commission) | **YES.** S-10 is graded from its own bytes, which restate the split in full |
| I-10 | **`QUESTIONS_SETTLED_REGISTER` content** (Q-43, Q-77, Q-81, Q-87, Q-88) | quoted inside corpus artifacts | **BARRED at source** | **PARTIAL — flagged F-2.** I read those quotations as the *quoting artifact's* bytes. I never opened the register. No grade rests on register content alone; every Q-43 use is doubled by `STAGE8_GAMMA_K_SOURCE_RECORD_COMPOSITION_TYPING_TESTS_V001.md`, which is in-corpus and sealed |
| I-11 | **grammatical categories** (performer, subject, antecedent/consequent, material implication) | my method | **NO** | **YES.** These are the analytic instrument, not physics. Each application is shown so it can be checked |

**No external literature was consulted, and none is available. No notion outside this
table was introduced.**

---

## 10. FLAG BLOCK

**F-1 — TOOLING COLLISION IN THE SHARED SCRATCHPAD. Reported because it could have
corrupted a sweep.**
Mid-run, the sweeper script this lane wrote to the session scratchpad was **overwritten by
another concurrently-running lane's script**. The replacement was detectable because it
barred a different artifact name (`STAGE8_BASE_GEOMETRY_O48SR`) and emitted no counters.
Consequence and remedy, stated exactly:

```text
AFFECTED: one batch of four Q2 sweeps (order parameter, nucleation, symmetry
          breaking, spontaneous) executed against the foreign script.
DETECTED: by output-format mismatch (missing "### PATTERN"/RAW_HITS header) and
          an unexplained "LEAK-EXCLUDED:" token; script then read at bytes.
FOREIGN BAR LIST: REGISTER TRACKER THE_PLAN ROAD_REMAINING THE_HANDOFF
          OBSERVATIONS_REGISTER DECISION_SHEET STAGE8_BASE_GEOMETRY_O48SR
          -> bar-equivalent for all seven standing patterns; it lacked only
             MY self-exclusion, which at that time was moot (artifact not yet written).
REMEDY:   own sweeper rewritten under a unique name (o47sr_sweep_bgm.sh) and ALL
          FOUR sweeps RE-RUN from scratch. Every count in §3 comes from the
          re-run, not the foreign run.
RESIDUAL RISK: none identified. Flagged so the pair lane and the registrar know
          the scratchpad is not single-tenant.
```

**F-2 — REGISTER CONTENT REACHES ME THROUGH QUOTATION, UNAVOIDABLY.**
`QUESTIONSSETTLED_REGISTER_V001.md` is barred and was **never opened**; the bar suppressed
it on 7 of 13 patterns (§3). But the corpus quotes the register constantly, so Q-43, Q-77,
Q-81, Q-87 and Q-88 content reaches this report as the *quoting artifact's* bytes. I have
not treated any such quotation as independent authority. Every load-bearing Q-43 use is
doubled by `STAGE8_GAMMA_K_SOURCE_RECORD_COMPOSITION_TYPING_TESTS_V001.md` (in-corpus,
sealed OK), which states the same content in its own text.

**F-3 — SEVEN LOAD-BEARING ARTIFACTS HAVE NO SEAL SIDECAR.**
`BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001`, `BID_FINITE_RECORD_DURABILITY_NO_GO_V001`,
`BID_FIRST_OPENING_INTERVAL_DERIVATION_V001`, `BID_CHARGED_HANDLE_ACTIVATION_DERIVATION_V002`,
`CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001`, `DURABLE_POINTER_CLOSURE_OPERATOR_SELECTOR_GATE_V001`,
`R3_4_LORENTZIAN_THRESHOLD_RETURN_RESULT_V001`. These are **unsealed of record**, not
seal-failures — no sidecar exists to check. Four of them carry sites in the Q1 census
(S-3, S-4, S-5, S-9, S-13) and one carries a Q2 mark (M1-c). **Impact on the report:** the
Q1 grade would be unchanged if all seven were struck (the sealed sites S-1, S-2, S-6, S-7,
S-8, S-10, S-11, S-12 already give CONDITION-DRIVEN with no OPERATED); the Q2 threshold
mark would drop from three to two; the Q3 grade is unaffected at its citation (S-2 is
sealed) though S-3's corroboration would go; **Q4 and Q5 are entirely unaffected** — both
rest on sealed artifacts only.

**F-4 — THREE REFERENCED AUTHORITIES ARE OUTSIDE MY COMMISSION'S ROOTS.**
`boundary_access_closure_threshold_principle_v001.md`, `alpha_first_durable_record_capacity_principle_v001.md`,
and the `alpha_supervision/` recovery-sweep pair. Not opened. Handled at IMPORT AUDIT
I-7/I-8/I-9. No grade depends on them.

**F-5 — TWO FILES IN A RESULT SET I RELIED ON WERE NOT OPENED, AND ARE DECLARED.**
Sweep P-2 (`forced binary is refuted`) returned four files. I opened two
(`STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md` and its mirror). I did
**not** open `STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md` or its `_AUDIT_`. Reason: they are
a prior commission's own output — catalogued negatives, which the BARS forbid re-reading —
and the primary text they book from is the artifact I did open. **However**, §8.1's
complement sweep incidentally surfaced two lines from the O36SR pair, and I have quoted
them as *prior-lane gloss* rather than corpus content (§8.1). That is the only use made of
them.

**F-6 — THREE LARGE SWEEPS WERE COUNTED BUT NOT OPENED WHOLESALE.**
P-9 (`stationary|extremal|minimiz|…`, 1046 files), P-10 (`saturat`, 230 files), P-11
(`threshold`, 1207 files). These are reported **as counts only**. No grade rests on the
unopened remainder: every Q2 extremization and threshold finding is anchored to a named,
opened site (E-1..E-5, M1-a..M1-c). Targeted line-level greps were run *within* these
patterns to test the specific hypotheses (stationarity-near-formation; critical-condition
lines) and those residues **were** read in full.

**F-7 — ONE OVERCLAIM WAS MADE AND CORRECTED IN-FLIGHT.**
An earlier draft of §8.1 asserted that `agent` never occurs in a physical sense. A
complement sweep refuted it: eight physical uses exist, all in prior commission artifacts.
The claim was rewritten to the accurate and sharper form (§8.1). Recorded here rather than
silently fixed, because the corrected finding is load-bearing for Q5.

**F-8 — MIRRORING INFLATES EVERY FILE COUNT.**
R1 (workspace) and R2 (cleanroom) hold byte-identical copies of a large fraction of the
corpus. Every count in §3 is a **file** count across both roots and therefore
roughly double-counts mirrored artifacts. This affects no grade — all grades are stated
over named artifacts, read once — but no count in this report should be read as a count of
distinct documents.

**F-9 — SCOPE DISCIPLINE OBSERVED.**
No authoring, no advocacy, no adoption. Nothing in this report amends, retires, or
promotes any premise. No catalogued negative was re-litigated. No numeric value of any
coupling, scale, root, eigenvalue, norm or constant was computed, approached, or
estimated; where the corpus displays such expressions (`tau_R=pi/sqrt(2)`, `C_R=1`,
`J_FS,rel >= pi hbar/2`, `T_R = 2 sqrt(pi) t_P`) they are reproduced **as quoted strings
for classification only**, never evaluated, compared, or reduced.

---

## 11. WHAT THIS REPORT DOES NOT SETTLE

Stated plainly so the null is not over-read in either direction.

```text
1. It does not show the corpus is a spontaneous-formation theory. Three of the
   five spontaneity marks are ABSENT (§5.6). The corpus has a threshold and a
   least-member, and none of the physics of a transition.

2. It does not show the corpus is an operated theory. Zero formation sites grade
   OPERATED, and the boundary-closure never acts (9/9).

3. It shows the corpus CANNOT CURRENTLY TELL THE DIFFERENCE, and that this is the
   same incapacity the program already booked once, on the neighbouring binary,
   for the same stated reason: no sealed operational definition (§8.5.2, §8.6).

4. The one place the corpus's own vocabulary leans is toward BOUNDARY-NOT-FORMER:
   "When a primitive durable record forms, the boundary law selects one physical
   Lorentz-covariant interval, support region, and closure configuration" makes
   formation the antecedent and the boundary's act the consequent (§4/S-6); and
   every one of the nine boundary-closure clauses delimits or classifies (§7).
   That is a lean, read off two texts. It is not a verdict, and this lane does
   not promote it to one.
```

---

## 12. STATUS AND FENCES

```text
artifact_type = CLASSIFICATION_AT_BYTES
commission = O47SR (BUILD lane of a commissioned pair)

Q1_formation_sites_located = 13  (across 14 artifacts)
Q1_OPERATED = 0
Q1_CONDITION_DRIVEN = 11
Q1_MIXED = 1                      [S-5, condition ∧ program adoption]
Q1_INDETERMINATE_AT_BYTES = 1     [S-6', "generated by"]
Q1_known_site_grade = CONDITION-DRIVEN
Q1_external_selector_of_the_moment_found = false

Q2_GRADE = PARTIAL
Q2_threshold_on_formation_itself = PRESENT (3)
Q2_order_parameter = ABSENT (0 substantive)
Q2_rate_or_nucleation = ABSENT (0 substantive)
Q2_symmetry_breaking_at_formation = ABSENT (0 of 74 lines)
Q2_extremization = PRESENT IN FORM, NOT AS CAUSE
Q2_saturation_typed_at_bytes = ADOPTED_EQUALITY_SELECTION_ON_A_DERIVED_BOUND
                               (not extremization; "Saturation is adopted, not derived")

Q3_GRADE = PHASE-IS-A-CONDITION-VARIABLE
Q3_site = SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003 (+ BID_CHARGED_HANDLE_
          ACTIVATION_DERIVATION_V002; inherited at S-11, S-12)
Q3_variable_charge_type = Q_Sigma = integral_Sigma j^mu dSigma_mu, spec = {0,1}
Q3_variable_phase_type  = theta = |v| tau_*/hbar,  onset at theta = pi/2
Q3_other_sites = PHASE-PRESENT-BUT-NOT-IN-THE-CONDITION or no phase (8 of 13)
Q3_chi_in_any_formation_condition = false

Q4_clauses_censused = 9   (CS-1 .. CS-9)
Q4_DELIMITS = 4
Q4_CLASSIFIES = 5
Q4_ACTS = 0
Q4_secondary_census_ACTS = 0
Q4_VERDICT = EVERY_CLAUSE_DELIMITS_OR_CLASSIFIES_NONE_ACTS

Q5_VERDICT = OBSERVATIONALLY-EQUIVALENT-AT-BYTES
Q5_discriminator_found = false
Q5_discriminator_manufactured = false
Q5_prior_negative_located = true
  "The forced binary is refuted."  [COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001:11]
Q5_prior_NO_VERDICT_located = true
  "no sealed operational definition selects temporal creation over
   derivational/common-framework provision"  [same artifact :72-74]
Q5_prior_material_governs_this_question = true (with three qualifications, §8.6)

seals_verified = 26/26 OK
seals_absent_sidecar = 7 (F-3)
barred_files_opened = 0
barred_suppressions_logged = 25 across 13 patterns

authoring_performed = false
advocacy_performed = false
adoption_performed = false
premise_amended_or_retired = false
catalogued_negative_relitigated = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
coupling_computed = false
scale_computed = false
root_computed = false
eigenvalue_computed = false
norm_computed = false
constant_computed = false
numeric_evaluation_performed = false
magnitude_approached = false
measured_value_comparison_performed = false
protected_holdout_access_status = PROHIBITED_AND_NOT_PERFORMED
```

END OF ARTIFACT.
