# STAGE 8 / [PLAN:AXN-BUILD-A7] — THE FIRST CLOSURE ROUND
## DARIO LANE (Builder B) — V001 — STEP 2 of relay 840

RELAY 840. Lane guard PASS (DARIO). Inbox = `c233921c1d0469e7…`, seal verified BEFORE reading.
State brief `e26f0d16055f3e83…` verified and read. Charter `c0ad6decf156ef06`, seal OK, binds whole.
Gate opened: `STAGE8_AXN_BUILD_SKELETON_V2_CROSSCHECK_CODEX2_V001.md` = `24001c12ce7f12a1`, seal OK.

**CONSUMES:** `STAGE8_AXN_BUILD_PACKAGE_V3_DARIO_V001.md` — **Part 1 of this same relay, CLAIMED and
un-cross-checked.** Disclosed rather than hidden: I stopped at 839 precisely to avoid consuming an
un-upgraded package, and I am now consuming one I wrote minutes ago. The difference is that V3 is
**constructed against the audit that defeated V2**, row by row, and its provenance is internal to
this relay rather than inherited across a gate. **A reader who thinks that distinction insufficient
should treat this round as CLAIMED-on-CLAIMED and weight it accordingly.**

GATES: `alpha_computed = false`; `proof_authorized = false`; `kappa_record_computed = false`.
**DERIVE-NEVER-SELECT ABSOLUTE. The KK re-import barred.** No smooth import; no EM identification.
PE-1..PE-11 pointer-only — **including PE-9, which this relay's AS2 names; not opened, not
consulted, and §4 below declines the direction it points.**
**ALL HEADLINE ITEMS CLAIMED.**

---

## 1. AS1 — THE STRATUM, BY BINDING

`BIND_PACKET_PARENT` (829 §3.5, 833-verified), applied to `E_4 = {S_0, S_1, S_rot, S_BF}`. Its seven
clauses: (1) finite restriction yields the sealed first-order parent; (2) cell profile is the
intrinsic `w(s)`, no fitted profile; (3) one shared charged source with distinct record factors;
(4) overlap and closure-face descendants generated by the parent square, no independent coefficient;
(5) common self-adjoint domain and unique finite propagator; (6) finite compact-support Møller maps;
(7) exact completed-record reduced-state persistence.

### 1.1 Per-member computation

**`S_0` = `∫d⁴x √-g [iℏ ψ̄ γ^μ D_μ ψ] + S_record[R,a,g]`.**
Clause 1 requires computing the **finite restriction of the whole action**. The action has two
summands. The first is fully displayed. **The second is the symbol `S_record[R,a,g]`, whose
integrand and domain are absent — so the restriction is not a computable object.** Clauses 2, 3, 4
and 7 each route through the record sector specifically (cell profile, record factors, record
descendants, completed-record persistence), and clauses 5 and 6 require the restriction from clause 1.
**VERDICT: NOT EVALUABLE. All seven clauses are blocked, six of them by `BOX_record` directly.**

**`S_1` = `S_0` + Pauli.** Contains `S_record` identically. **VERDICT: NOT EVALUABLE, same blocker.**
*(The Pauli term is itself fully displayed and is not the obstruction — see §3.2.)*

**`S_rot`, `S_BF`.** Their action terms are exhibited only in a cleanroom v001 file **outside this
archive**; `ARCHIVE-SIDE ONLY` bars me. The single archive-side datum is 836's finding that both
**omit `S_D`**. Omitting the displayed source term is not by itself a clause-3 failure — clause 3
demands *one shared charged source with distinct record factors*, and I cannot read whether either
carries a charged source by another route. **VERDICT: NOT EVALUABLE ON ARCHIVE-SIDE STOCK.** I
decline to convert 836's omission fact into a binding failure I cannot compute.

### 1.2 The stratum, and the ordering result — CLAIMED

```text
STRATUM = predicate-selected: NONE.  The predicate selects no member, because it cannot be
          EVALUATED on any of the four.  Two are blocked by BOX_record; two by custody.
```

**This is a derivation, not a shrug, and it inverts the round's own instruction.** 836 and this relay
both direct: *"closure starts by deriving the packet-binding stratum before hole pressure."* That
ordering presupposes the predicate is evaluable. **It is not: clause 1 requires the finite
restriction of a candidate action, and every archive-side candidate contains `S_record` as an
undefined symbol. `BOX_record` is therefore UPSTREAM OF THE BINDING PREDICATE ITSELF.**

```text
AS1 CANNOT PRECEDE AS2.  The stratum derivation is gated on one of the holes it was meant to
precede.  The lawful order is: press BOX_record first, then derive the stratum.
```

I report this as the round's first result rather than proceeding as if the order held.

---

## 2. AS2 — THE ELEVEN CENSUS ROWS UNDER PRESSURE

Pressure instruments: `BIND_PACKET_PARENT` (all seven clauses) + the composed interface
(F1–F7 falsifiers, F8–F10 promotion requirements, F11–F12 conditionals **gated**, F13 mandatory
not-established, F14 meta-gate) + generation rules G2–G5 at their 832-confirmed scopes.

### 2.1 H1 `BOX_record` — **NARROWED**, and this is the round's main output

Six independent constraints bear on it, each read off a clause or a sealed rule:

```text
from CIS G2   support(L_c) ⊆ Omega_c, one Lorentz-covariant cell per primitive incidence
              => BOX_record is a SUM OVER PRIMITIVE RECORD-FORMING INCIDENCES of per-cell terms.
                 IT CANNOT BE A NONLOCAL RECORD FUNCTIONAL.
from CIS G3   one-use: absent from the active generator past its closure face
              => each term is an EVENT, not a permanent Hamiltonian term.
from CIS G4   writes land on NEW record factors; shared source support permitted
from clause 2 the cell profile is the intrinsic w(s) = 32 min(s,1-s)^3, "without a fitted profile"
              => THE CELL PROFILE IS FIXED, NOT FREE.  This removes an entire functional freedom.
from clause 1 the finite restriction must supply the packet's RECORD-INCIDENCE BLOCK D_rec/inc,
              whose Hermiticity and square identities are exact to machine zero
              => BOX_record's restriction is FIRST-ORDER and its target block is NAMED.
from clause 4 its square must generate the overlap and closure-face descendants with NO independent
              coefficient
from clause 7 completed-record reduced-state persistence must be EXACT
```

**New exact shape:**

> `BOX_record` = a sum over primitive record-forming incidences `c` of terms `L_c` with
> `support(L_c) ⊆ Omega_c`, one-use, writing on new record factors, **carrying the intrinsic profile
> `w(s)` with no fitted parameter**, whose finite restriction is first-order and supplies
> `D_rec/inc`, whose square generates the overlap and closure-face descendants with no independent
> coefficient, and which yields exact completed-record persistence.

**What moved:** V3's shape was *"integrand, domain, normalization, cell coupling, descendant
inventory, durability map all absent."* Of those six, **domain, cell coupling and descendant
inventory are now fixed** (per-cell `Omega_c`; the intrinsic `w(s)`; the square-generated pair), and
**normalization is constrained** by clause 7's exactness. **Integrand and durability map remain
open.** This is pressure, not authorship: every line is read off a clause or a sealed rule, and no
functional form is written.

### 2.2 H3 `chi_P` — **NARROWED** from a free fork to a decidable question

Clause 1 makes the fork **determinable**: the Pauli insertion changes the charged current vertex to
`Γ^μ(p+q,p) = γ^μ + 2i ell_* σ^{μν} q_ν` (A05), so the packet parent's vertex decides `chi_P`. If the
parent's charged vertex is minimal, clause 1 forces `chi_P = 0`; if it carries the Pauli structure,
`chi_P = 1`. **The fork is no longer free — it is read off the packet.**

**I stop short of reading it off.** The packet's verdict block names *"one shared charged source"* and
the exact Clifford/incidence identities, and displays no Pauli insertion — but whether the displayed
operator content is exhaustive of the parent's vertex structure is a builder-level determination
requiring spans I should not over-read. **NARROWED to: `chi_P` is determined by the packet's charged
vertex; the determination is LEG A's, not mine.**

### 2.3 H4 `BOX_HD` — **NARROWED**

Clause 1 requires the finite restriction to yield a **first-order** parent. A higher-derivative
summand whose restriction contributed a second- or higher-order piece would break it. **So the
admissible content of `BOX_HD` is restricted to terms whose FINITE RESTRICTION preserves
first-orderness** — the term may be higher-derivative in its continuum form, but its restriction to
the finite causal complex may not raise the order.

**This is the first bite anything has taken on the HD tower**, and it is a constraint on the
*restriction*, not on the continuum form — stated precisely so it is not over-read. Order, coefficient
descent, provenance and leading member remain absent.

### 2.4 H5 `BOX_UPDATE` — **NARROWED**

The update is not an action, so the predicate reaches it only through its outputs. Clauses 5 and 6 do
that: the update must reproduce the **unique finite propagator** `U_fin` and the **finite
compact-support Møller maps** `Omega_fin`. **NARROWED from "an unbuilt update slot" to "an update law
whose induced propagation reproduces `U_fin` and `Omega_fin` exactly."** A named target replaces an
open slot; no roster and no update rule is supplied.

### 2.5 H2 `K_R` — **UNMOVED**, and expressly NOT ruling-shaped

829 separates two layers that may not be flattened: the microscopic displayed-pair stratum and the
**induced public stratum**, where `S_K` and `K_R` live. **The binding predicate constrains the
microscopic finite restriction; not one of its seven clauses mentions the public layer.** So it does
not reach `K_R`, and positivity still forces only `K_R > 0`.

**Why I do NOT type this RULING-SHAPED**, though it is a genuine freedom surviving all pressure: a
ruling that selected a `K_R` point would be an authored physics input directly shaping the derived
number — precisely what S03's ruling bars from the construction end and C7 bars from the response
end, and what S25 bars via the rescaling route. **A freedom the instrument cannot reach is not
thereby a freedom a ruling should fill.** Typed UNMOVED with the reason displayed.

### 2.6 H6 `BOX_gravity` — **UNMOVED**, and I decline the direction the relay points

**Does the binding predicate reach the gravity sector? No, and the reason is structural.** The
predicate is a *parent-reproduction* condition, and its receiver is the sealed finite first-order
operator on a finite causal complex. Its seven clauses name: a first-order restriction, the cell
profile, a charged source with record factors, square-generated descendants, a self-adjoint domain
and finite propagator, Møller maps, and record persistence. **None mentions a metric or gravitational
dynamics. The parent has no gravitational sector to reproduce, so reproducing it constrains
`BOX_gravity` not at all.**

Nor do the falsifiers reach it: F1–F7 are CIS incidence/record-factor conditions; F8–F12 are
outgoing-sector; F13 tests descendants' absolute continuity, and a gravitational descendant would
have to be square-generated by a parent that has no gravity sector.

**The relay asks: *"if the pressure forces it toward the conversion/obstruction structure, DISPLAY
THAT DERIVATION."* IT DOES NOT, AND I DECLINE.** The antecedent fails. Displaying a derivation toward
a structure the pressure does not force would be authorship dressed as pressure — the void condition
names exactly that, and the KK bar is already in force against the nearest available import. **PE-9
is pointer-known, zero-weight, unopened, and the direction the relay's parenthetical gestures at is
one the spans do not support.**

`BOX_GRAVITY = UNMOVED. The predicate's receiver has no gravitational sector, so the build's sole
forcing instrument does not reach the charter's gravity sector at all.`

### 2.7 I1–I5 — all **UNMOVED**, each for a stated reason

| interface | why the pressure does not reach it |
|---|---|
| I1 `ENVIRONMENT_SECTOR_CLOSURE` | no clause names an environment sector. The disjunction's second branch (does `BOX_record`/P5 **exhaust** it?) is adjacent to H1's narrowing — H1 is now typed as a per-cell record-incidence sum — but **whether that sum exhausts the charter's environment sector is a question about sector naming, not one the predicate answers.** Neither branch taken. |
| I2 `GRAVITATIONAL_QUANTUM_MEASURE` | same structural reason as H6, twice over: no gravitational sector in the receiver, and a **measure** is not an action functional the predicate could bind |
| I3 `ACTION_P5_NO_OUTSIDE` | the predicate binds **one candidate**; it says nothing about the **family's** closure. A per-candidate instrument cannot deliver a package-census certificate |
| I4 `O1_SOURCE_TAIL` | **clause 6 is explicitly FINITE compact-support Møller maps.** O1 lives in the infinite-future source-inclusive bracket. The predicate is a finite-restriction condition by construction — structurally the same boundary my 824 found between the record side and the source-inclusive side |
| I5 `O3_CROSS_REGULATOR` | behind I4, plus its own cross-presentation comparison. **The E4a template is not its discharge** (838's warning, carried) |

### 2.8 P1–P4 carry unchanged

`U2` and `U3` still have no supplier — the predicate supplies neither a canonical decomposition law
nor a same-parent exhaustiveness theorem. `D-1` remains displayed, not selected. `E4c`/O2 remains the
deliberately uncovered disjunct.

---

## 3. AS3 — THE ROUND'S LEDGER

```text
ROUND 1 TALLY over the eleven pressed rows (6 holes + 5 interfaces):
  CLOSED-FORCED   0
  NARROWED        4    H1 BOX_record (substantially), H3 chi_P, H4 BOX_HD, H5 BOX_UPDATE
  UNMOVED         7    H2 K_R, H6 BOX_gravity, I1, I2, I3, I4, I5
  RULING-SHAPED   0    (H2 is the only candidate and is expressly declined — §2.5)
  P1-P4 carry unchanged.
```

**U1's status after the round.** U1 is still build-supplied — but the round establishes a
**dependency U1 did not previously carry**: the action-to-operator map cannot be applied to any
archive-side candidate until `BOX_record` has content, because the map's input is an action whose
restriction must be computable. **U1 is gated on H1, and H1 is now the round's chokepoint.**

**What round two needs.**

1. **`BOX_record`'s integrand, on the narrowed shape** (§2.1). It gates the stratum derivation (§1.2),
   U1, and through them the whole of AS1. **It is the single highest-leverage object in the census.**
2. **The packet's charged vertex read at builder level**, which decides `chi_P` (§2.2). LEG A's.
3. **A decision on the ordering**, since AS1-before-AS2 is not executable: round two should be
   explicitly ordered *press H1 → derive the stratum → re-press*.
4. **Nothing round two can do about H6, I1–I5** with this instrument. **Seven of eleven rows are
   untouched by the build's sole forcing instrument**, and that is the round's second structural
   result: the binding predicate is a *parent-reproduction* condition, so it reaches only what the
   parent contains — the source and record sectors — and is silent on gravity, environment, the
   family's closure, and the source-inclusive tail.

---

## 4. FREEDOMS-CONSUMED · FLATTENING CHECK

```text
CARRIED, NOT CONSUMED: the four exhibited completions (none selected, none bound, none declared a
  packet-parent inhabitant); S_rot/S_BF's exhibit NOT read (ARCHIVE-SIDE ONLY) and 836's omission
  fact NOT converted into a binding failure; the intrinsic w(s) CARRIED AS THE PACKET FIXES IT, not
  chosen; the K_R ray whole; chi_P's fork whole; D-1's two routes; the KK candidate as a
  DISQUALIFICATION only.
NARROWED HERE (4 rows): by READING OFF predicate clauses and sealed rules.  NO functional form, no
  integrand, no coefficient, no profile, no update rule, and no decomposition is written.
DECLINED HERE: the relay's BOX_gravity direction, because the antecedent ("if the pressure forces
  it") FAILS; and a RULING-SHAPED typing for K_R, because a ruling there would author the number.
SCALING WEIGHTS (law 2a): NONE CONSUMED.  SUBSTITUTED: NONE.
```

**FLATTENING CHECK — 37 rows walked.**
**S03 / the void condition — LIVE AND LOAD-BEARING TWICE.** At H6, where the relay's own parenthetical
points toward a structure the pressure does not force; **declined**. At H1, where the narrowed shape
is close enough to a form that writing one would be tempting; **the shape is displayed and the
integrand is not written.**
**S12** — every narrowing cites a *clause or rule*, never a status flag. **S26/S08** — KK material
used only to disqualify. **S19/S24** — F8 remains a requirement to be run, not an assumed decay law;
no clustering imported at I4. **S25** — no rescaling route to `K_R`. **S28/S34** — untouched.
`FLATTENING_CHECK = clean (37/37; S03 and the void condition live at H6 and H1).`

## 5. SELF-AUDIT

**VERB AUDIT: NOT CLEAN (+4).**

**(1) I CONSUMED A PACKAGE I WROTE MINUTES AGO, ONE RELAY AFTER STOPPING TO AVOID EXACTLY THAT.**
Disclosed in the header rather than buried. The distinction I claim — V3 is built against the audit
that defeated V2, internal to this relay — is real but is **my own** distinction, and a reviewer may
reject it. Treat this round as CLAIMED-on-CLAIMED.

**(2) THE ROUND'S HEADLINE IS A NEGATIVE ABOUT THE INSTRUMENT I HELPED CERTIFY.** At 833 I verified
`BIND_PACKET_PARENT` as *exactly* the parent-reproduction condition and called the interlock clean.
The round now shows that same predicate reaches **four of eleven rows**. Both are true and they sit
together: it is exactly the parent-reproduction condition, **and that is a narrow instrument.** I did
not say so at 833, when I had the information to.

**(3) THE ORDERING INVERSION CONTRADICTS AN INSTRUCTION FROM TWO SOURCES.** 836 and this relay both
say derive the stratum first. I report that it cannot be done in that order. **I may be wrong about
the blocker** — if a lane can compute a finite restriction of `S_0` without `S_record`'s integrand,
§1.2 collapses and AS1 runs as instructed. I could not, and I state the failure rather than
producing a stratum by weakening clause 1.

**(4) H1'S NARROWING IS THE MOST SUBSTANTIVE THING I HAVE PRODUCED IN THIS BUILD, AND IT IS THE MOST
EXPOSED.** Six constraints, each read off a clause or rule; if any single reading over-reaches, the
shape is wider than I state. The `w(s)` line is the one I would attack first: clause 2 says the cell
profile *is* the intrinsic `w(s)* "without a fitted profile"*, and I have read that as fixing
`BOX_record`'s profile — but it could instead be read as constraining only the *packet's* profile,
with `BOX_record` free to carry another that restricts correctly. **I take the stronger reading and
flag it as the round's weakest joint.**

---

```
V3 = census complete (rows: 6 holes + 5 interfaces + 4 unresolved = 15)   [Part 1, sealed separately]

STRATUM = predicate-selected: NONE — and the reason is a derivation, not a shrug.
  S_0 and S_1: NOT EVALUABLE.  Clause 1 requires the finite restriction of the WHOLE action; both
    contain S_record[R,a,g] as an undefined symbol, so the restriction is not a computable object.
    Clauses 2/3/4/7 route through the record sector directly; 5/6 depend on 1.
  S_rot and S_BF: NOT EVALUABLE ON ARCHIVE-SIDE STOCK.  Their exhibit is outside the archive; 836's
    S_D-omission fact is the only archive-side datum and I DECLINE to convert it into a binding
    failure I cannot compute.
  *** THE ORDERING INVERTS: BOX_record IS UPSTREAM OF THE BINDING PREDICATE ITSELF, so "derive the
  packet-binding stratum before hole pressure" IS NOT EXECUTABLE IN THAT ORDER.  The lawful order is
  press H1 first, then derive the stratum. ***

ROUND = 0 CLOSED-FORCED / 4 NARROWED / 7 UNMOVED / 0 RULING-SHAPED
  NARROWED: H1 BOX_record — SUBSTANTIALLY.  Six constraints read off clauses and sealed rules give:
    a SUM OVER PRIMITIVE INCIDENCES of per-cell terms with support(L_c) ⊆ Omega_c (so NOT a nonlocal
    functional), one-use, on new record factors, CARRYING THE INTRINSIC w(s) = 32 min(s,1-s)^3 WITH
    NO FITTED PARAMETER, whose finite restriction is first-order and supplies D_rec/inc, whose square
    generates the overlap and closure-face descendants with no independent coefficient, and which
    yields exact completed-record persistence.  Of V3's six absences, DOMAIN, CELL COUPLING and
    DESCENDANT INVENTORY are now FIXED and NORMALIZATION is CONSTRAINED; integrand and durability map
    remain open.
  NARROWED: H3 chi_P — from a free binary fork to A QUESTION DECIDED BY THE PACKET'S CHARGED VERTEX
    (clause 1 + the Pauli vertex change).  The reading-off is LEG A's, not mine.
  NARROWED: H4 BOX_HD — clause 1 restricts admissible content to terms WHOSE FINITE RESTRICTION
    PRESERVES FIRST-ORDERNESS.  The first bite anything has taken on the HD tower, and it constrains
    the RESTRICTION, not the continuum form.
  NARROWED: H5 BOX_UPDATE — from an unbuilt slot to AN UPDATE WHOSE INDUCED PROPAGATION REPRODUCES
    U_fin AND Omega_fin exactly (clauses 5 and 6).
  UNMOVED (7): H2 K_R — the predicate constrains the MICROSCOPIC restriction and K_R lives in the
    INDUCED PUBLIC stratum, which no clause mentions.  EXPRESSLY NOT TYPED RULING-SHAPED: a ruling
    selecting a K_R point would author the number, which S03 bars from the construction end, C7 from
    the response end, and S25 via rescaling.  A FREEDOM THE INSTRUMENT CANNOT REACH IS NOT THEREBY A
    FREEDOM A RULING SHOULD FILL.  H6 and I1-I5: each unreached for a stated structural reason.

BOX_GRAVITY = UNMOVED, and the relay's suggested direction DECLINED.
  The predicate is a PARENT-REPRODUCTION condition whose receiver is the sealed finite first-order
  operator; not one of its seven clauses mentions a metric or gravitational dynamics.  THE PARENT HAS
  NO GRAVITATIONAL SECTOR TO REPRODUCE, so reproducing it constrains BOX_gravity NOT AT ALL.  The
  falsifiers do not reach it either.  The relay's AS2 says "IF the pressure forces it toward the
  conversion/obstruction structure, DISPLAY THAT DERIVATION" — THE ANTECEDENT FAILS AND I DECLINE.
  Displaying a derivation toward a structure the pressure does not force would be authorship dressed
  as pressure; the void condition names exactly that, and the KK bar is already in force against the
  nearest import.  PE-9 pointer-known, zero-weight, UNOPENED.

U1 = still build-supplied, NOW GATED ON H1.  The action-to-operator map cannot be applied to any
  archive-side candidate until BOX_record has content, because its input is an action whose
  restriction must be computable.  H1 IS THE ROUND'S CHOKEPOINT.

ROUND2 = (1) BOX_record's integrand on the narrowed shape — it gates the stratum, U1 and all of AS1,
  and is the highest-leverage object in the census; (2) the packet's charged vertex read at builder
  level, deciding chi_P; (3) an explicit re-ordering — press H1, derive the stratum, re-press;
  (4) NOTHING for H6 and I1-I5 with this instrument.  SEVEN OF ELEVEN ROWS ARE UNTOUCHED BY THE
  BUILD'S SOLE FORCING INSTRUMENT — the second structural result: the binding predicate reaches only
  what the parent contains (source and record), and is SILENT on gravity, environment, the family's
  closure, and the source-inclusive tail.

CHAIN_INVOKED = false

VERB_AUDIT_SELF = NOT CLEAN (+4)
  (1) I CONSUMED A PACKAGE I WROTE MINUTES AGO, ONE RELAY AFTER STOPPING TO AVOID EXACTLY THAT.
      Disclosed in the header, not buried; the distinction I claim is MY OWN and a reviewer may
      reject it.  Treat this round as CLAIMED-on-CLAIMED.
  (2) THE HEADLINE IS A NEGATIVE ABOUT AN INSTRUMENT I HELPED CERTIFY.  At 833 I verified
      BIND_PACKET_PARENT as EXACTLY the parent-reproduction condition and called the interlock clean;
      the round shows it reaches FOUR OF ELEVEN ROWS.  Both are true — it is exactly that condition,
      AND THAT IS A NARROW INSTRUMENT — and I did not say so at 833 when I had the information to.
  (3) THE ORDERING INVERSION CONTRADICTS AN INSTRUCTION FROM TWO SOURCES (836 and this relay).  I MAY
      BE WRONG ABOUT THE BLOCKER: if a lane can compute a finite restriction of S_0 without
      S_record's integrand, §1.2 collapses and AS1 runs as instructed.  I could not, and I state the
      failure rather than producing a stratum by weakening clause 1.
  (4) H1'S NARROWING IS THE MOST SUBSTANTIVE THING I HAVE PRODUCED IN THIS BUILD AND THE MOST
      EXPOSED.  THE w(s) LINE IS ITS WEAKEST JOINT: clause 2 says the cell profile is the intrinsic
      w(s) "without a fitted profile", and I read that as fixing BOX_record's profile — but it could
      constrain only the PACKET's profile, leaving BOX_record free to carry another that restricts
      correctly.  I TAKE THE STRONGER READING AND FLAG IT FOR ATTACK FIRST.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
