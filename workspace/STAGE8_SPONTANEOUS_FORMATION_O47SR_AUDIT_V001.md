# STAGE 8 — DEFAULT-REFUTE AUDIT OF O47SR (OPERATED OR SPONTANEOUS) V001

Date: 2026-08-16
Lane: DEFAULT-REFUTE AUDIT (commissioned pair). Default verdict REFUTED.
Role: adversarial re-derivation at bytes. No authoring, no advocacy, no adoption.
Status: AUDIT ONLY / NO CONSTRUCTION / NO ADOPTION / NO NUMERIC EVALUATION

TARGET: `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md`
TARGET sha256: `1b0e035b85499d990e7bc0a334490c50842c52c68ada7cbb2b0e2d9bf6fc8024` (verified at path,
matches the build's reported value and both of its sidecars).

---

## 0. LEAD FINDING

**Overall: CONFIRMED-WITH-CORRECTIONS.** The build's two headline findings survive
adversarial re-derivation at bytes: **no formation site grades OPERATED**, and **no clause
of the boundary-closure ACTS (0 of 9)**. I hunted independently for both and found neither
an OPERATED site nor an ACTS clause. Q5's null also survives: I found no discriminator
either, and the three candidates the build adjudicated are genuinely true under both
readings, not merely less natural under one.

**Six corrections, three of them material to a stated grade or tally.**

1. **The known site is misgraded, and it is misgraded against the build's own criterion**
   (COR-A). S-1 and S-5 are the *same move* — a derived inequality leaves a half-line, and
   an adopted program rule supplies the boundary point — and the corpus itself files them
   as two entries in one list. The build graded S-5 **MIXED** and S-1 **CONDITION-DRIVEN**.
   At the known site the adopted rule's own words are `adopted to select first admissible
   record onset` — it selects an *onset*, i.e. the moment, which is the build's own test
   T-c. Corrected: **S-1 = MIXED**.

2. **A rate object exists and the build's universal negative is false at bytes** (COR-B).
   The build searched Mark 3 with the single token `nucleat` and then asserted
   `There is no nucleation rate, no onset rate, no formation rate anywhere in either root.`
   At bytes the corpus carries a **write-rate** and a **write envelope**
   (`ER-A (write envelope tau_R w(t) M(t), w(t) = 32 r(t)^3)`), and names deriving the
   write-rate as the route that would discharge the premise. Mark 3 is **not ABSENT**; it is
   **NAMED-BUT-NOT-DERIVED**. The build's supporting citation for the negative is also
   misattributed (COR-C).

3. **The Q1 tally is arithmetically wrong at both stages** (COR-D), independently of COR-A.

4. **The Q3 phase claim is the presence-near/presence-in inflation** (COR-E). The build's
   stated ground for typing `theta` PHASE-TYPE — `It is the phase angle of an amplitude at
   bytes` — is false at bytes: the amplitude is `cos(theta)-i(v_z/|v|)sin(theta)`, and the
   onset condition the build cites *sets* `v_z=0`, which leaves the amplitude real. The
   charge-type half of Q3 (`Q_Sigma`) is solid and is confirmed.

5. **Three exhaustiveness claims fail** (COR-F): the secondary closure census lists 11 of 21
   occurrences while claiming *every*; the symmetry-breaking set is 75 lines, not the 74 the
   build says it read in full; and the P-12/P-13 line counts are not reproducible at bytes
   (P-12 is 69 lines, not 95). None of these overturns a finding; all three violate OPEN
   WHAT YOU COUNT.

6. **The only occurrence of the commission's own phrase `external selector` in either root
   was caught by the build's own sweep pattern and never surfaced.** It is adjudicated here
   (§6.4) and it is not an OPERATED site — but it should have been itemized, not passed over.

Seals: **26/26 OK**, re-verified independently from each artifact's own directory. The seven
unsealed-of-record artifacts are confirmed to have no sidecar. **0 barred files opened.**

---

## 1. CHOICE LEDGER

| # | Choice | Alternative not taken | Why | Load-bearing? |
|---|---|---|---|---|
| A-1 | Re-derive every Q1 site from the source artifact's own bytes rather than checking the build's quotations against each other | Spot-check the quotes | The commission says CLAIMED only | YES — COR-A, COR-D |
| A-2 | Test the build's grade *consistency* across sites as hard as its grade *correctness* at each site | Grade each site in isolation | An inconsistent criterion applied twice to one shape is the classic way a headline is protected | YES — COR-A |
| A-3 | Re-run every Q2 sweep with patterns the build did not run (`phase transition`, `condensat`, `vacuum expectation`/`vev`, `Landau|Ginzburg|Goldstone`, `latent heat|critical exponent|universality class`, formation/onset-scoped `rate`) | Re-run only the build's own patterns | D2: a claimed ABSENT on a narrow sweep is the defect to hunt | YES — COR-B |
| A-4 | Re-run the build's **printed** patterns verbatim to test whether its own numbers reproduce | Accept the reported counts | A count that cannot be reproduced from its own printed pattern is not a count | YES — COR-F |
| A-5 | Add the ER-A write envelope as an audit-found site (S-14) and report the tally **both with and without it**, rather than folding it in silently | Fold it in; or omit it | Transparency about an audit-added site; the reader can take either tally | YES — §4.3 |
| A-6 | Accept the build's C-1 scoping of "the boundary-closure" as `CS-1..CS-9` | Re-scope | Verified at bytes: the WBC artifact's title names `THE SEALED BOUNDARY-CLOSURE STRUCTURE OF THE RECORD CELL`, and `CS-` labels run 1..9 with no CS-10 in either the WBC or its audit | NO — I reach the same 0 ACTS |
| A-7 | Grade M1-a (`C_R <= 1`) as CONTESTED rather than striking it | Strike it; or accept PRESENT | The corpus's own heading is `Public recoverability condition (physical admissibility)`; admissibility falls on the requirement side of the commission's own distinction, but the partition is real | YES — Q2 count |
| A-8 | Do not open `a32_holdout/`, `.proof_deps/`, `.pytest_cache/`, `.git/` | Sweep everything | Holdout protected; `.proof_deps` is a vendored tree | NO |
| A-9 | Self-exclude this audit artifact and both sidecars from every sweep; exclude the build's artifact from corpus counts | Count them | Otherwise the build's own text inflates every corpus count | YES — all counts |

---

## 2. SEALS — RE-VERIFIED INDEPENDENTLY

`shasum -a 256 -c` run from each artifact's own directory.

```text
DIRECTORY: /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
           alpha_fundamental_record_action_cleanroom_v003

BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md ............ OK
SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md ............... OK
FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md ............ OK
CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md ...................... OK
CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002.md .................... OK
STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md ........ OK
STAGE8_GAMMA_K_SOURCE_RECORD_COMPOSITION_TYPING_TESTS_V001.md ... OK
STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md ..... OK
PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md ........... OK
STAGE8_GAMMA_K_FINITE_REVERSIBLE_WRITE_OBS05_TEST_V001.md ....... OK
                                                        10/10 OK

DIRECTORY: /Users/bgm/MB Work/alpha-program-archive/workspace

STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md ....................... OK
STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_AUDIT_V001.md ................. OK
                                                          2/2 OK

DIRECTORY: /Users/bgm/MB Work/alpha-program-archive/supervision   (DOR_* only, PERMITTED)

DOR_016_NETWORK_SOURCING_LAW_RATIFICATION_2026-08-03 ............ OK
DOR_017_ACTION_COMPARISON_SQUARE_N_MEMBER_RATIFICATION_2026-08-03  OK
DOR_018_N_MEMBER_JETS_SHAPE_K_RATIFICATION_2026-08-03 ........... OK
DOR_019_CARRIER_METRIC_AND_UNITS_RATIFICATION_2026-08-03 ........ OK
DOR_020_A1_WHERE_CLAUSES_AMENDMENT_2026-08-04 ................... OK
DOR_020_A2_COMPLETED_EXISTENCE_ADOPTED_2026-08-06 ............... OK
DOR_020_A3_J4_RELATIONAL_INCREMENT_GROUPOID_2026-08-04 .......... OK
DOR_020_A4_J12_CONTACT_GLUE_STRICT_PUSHOUT_2026-08-04 ........... OK
DOR_020_A5_CONTACT_LAPLACIAN_REDUCING_2026-08-04 ................ OK
DOR_020_A6_J2_SCOPED_PROJECTED_LAW_2026-08-05 ................... OK
DOR_020_A7_EC_BRANCH_CARRIED_2026-08-05 ......................... OK
DOR_020_A8_GAMMA_BOTH_ROUTES_2026-08-05 ......................... OK
DOR_020_A9_XI_N_ADOPTED_2026-08-05 .............................. OK
DOR_020_CONTINUUM_PACKAGE_CONDITIONAL_RATIFICATION_2026-08-04 ... OK
                                                        14/14 OK

TOTAL VERIFIED: 26/26 OK.  0 FAIL.  The build's seal report is accurate.

NO SEAL SIDECAR EXISTS — independently confirmed by directory listing, all seven:
  BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md
  BID_FINITE_RECORD_DURABILITY_NO_GO_V001.md
  BID_FIRST_OPENING_INTERVAL_DERIVATION_V001.md
  BID_CHARGED_HANDLE_ACTIVATION_DERIVATION_V002.md
  CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md
  DURABLE_POINTER_CLOSURE_OPERATOR_SELECTOR_GATE_V001.md
  R3_4_LORENTZIAN_THRESHOLD_RETURN_RESULT_V001.md
The build's F-3 is accurate, including its per-question impact statement.
```

---

## 3. SWEEP CUTOFFS

Exclusion globs held as a shell **array** (`BARPATS`, 10 members), expanded to per-pattern
`--exclude=` flags. Self-exclusion of this artifact and both sidecars is in the array. The
build's artifact is excluded from all corpus counts so its own text cannot inflate them.
Sweeper written under a unique name (`audit_sweep_o47sr_v1.sh`) in an isolated subdirectory,
in view of the build's F-1 report that the session scratchpad is not single-tenant.

```text
BARPATS (array, 10 members)
  *REGISTER*   *TRACKER*   THE_PLAN*   ROAD_REMAINING*   THE_HANDOFF*
  OBSERVATIONS_REGISTER*   *DECISION_SHEET*
  STAGE8_SPONTANEOUS_FORMATION_O47SR_AUDIT_V001.md              [SELF]
  STAGE8_SPONTANEOUS_FORMATION_O47SR_AUDIT_V001.md.seal.sha256  [SELF]
  STAGE8_SPONTANEOUS_FORMATION_O47SR_AUDIT_V001.md.sha256       [SELF]

ROOTS: R1 = /Users/bgm/MB Work/alpha-program-archive/workspace
       R2 = .../alpha_fundamental_record_action_cleanroom_v003
       R3 = .../supervision/DOR_*.md   [PERMITTED SUBSET]
SUPPRESSED SUBTREES: .proof_deps/, .pytest_cache/, .git/, custodian_private/
```

| # | Pattern | FILES (R1+R2) | LINES | LEAK (barred, suppressed) | Barred file(s) |
|---|---|---|---|---|---|
| A-P1 | `order[ _-]parameter` | 4 | 4 | 0 | — |
| A-P2 | `nucleat` | 1 | 1 | 0 | — |
| A-P3 | `phase transition\|phase-transition` | 0 | 0 | 0 | — |
| A-P4 | `condensat` | 0 | 0 | 0 | — |
| A-P5 | `vacuum expectation\|(^\|[^a-z])vev([^a-z]\|$)` | 0 | 0 | 0 | — |
| A-P6 | `Landau\|Ginzburg\|Goldstone` | 3 | 13 | 0 | — |
| A-P7 | `latent heat\|critical exponent\|universality class` | 6 | 12 | 2 | STAGE8_DEMAND_REGISTER_O25SR_V001 + _AUDIT_ |
| A-P8 | `(formation\|onset\|nucleation\|writing\|write\|record-form)[ _-]rate\|rate of (formation\|onset\|record\|writing)` | 10 | 12 | 0 | — |
| A-P9 | `(^\|[^a-z])spontaneous` | 2 md (1 mirror pair) + 2 sidecars | 6 md (**3 distinct**) + 2 sidecar | 1 | QUESTIONSSETTLED_REGISTER_V001.md |
| A-P10 | `symmetry[ _-]break\|broken symmetry\|spontaneously broken` | 34 | **75** | 1 | QUESTIONSSETTLED_REGISTER_V001.md |
| A-P11 | `external selector\|selects when\|chooses when\|picks the moment\|no external agent\|performer\|performs the` (the build's P-12, verbatim) | 62 | **69** (R1 41, R2 28) | 1 | QUESTIONSSETTLED_REGISTER_V001.md |
| A-P12 | `(^\|[^a-z_])agents?([^a-z_]\|$)` (word-boundaried) | 142 | **245** | 1 | QUESTIONSSETTLED_REGISTER_V001.md |
| A-P13 | `(^\|[^alnum_])agent(s)?([^alnum_]\|$)` (the build's P-13 **as printed**) | **184** | **319** | 1 | QUESTIONSSETTLED_REGISTER_V001.md |
| A-P14 | `external selector` alone | 2 (1 mirror pair) | 2 | 0 | — |
| A-P15 | `write[ _-]rate` | 4 (2 mirror pairs) | 6 (**3 distinct**) | 0 | — |
| A-P16 | `closure` in the 3 root principles + 13 site artifacts | 7 | **21** | 0 | — |

**Total barred-file suppressions: 6 across 16 patterns. Zero barred files were opened.**
`QUESTIONSSETTLED_REGISTER_V001.md` was suppressed on every pattern that reached it and
never opened. Where a permitted artifact quotes the register, I read the quotation as the
quoting artifact's bytes and rest no grade on it — the same position the build took at F-2,
which I confirm is unavoidable and correctly disclosed.

### 3.1 OPEN WHAT YOU COUNT

- **A-P1 (4 files): ALL OPENED**, all 4 matching lines displayed. Both are homonyms exactly
  as the build's C-5 states — confirmed verbatim, see §5.2.
- **A-P2 (1 file): OPENED.** Regex literal in another lane's method block. C-4 confirmed.
- **A-P3..A-P8, A-P14, A-P15: ALL LINES OPENED AND READ** (12 or fewer each).
- **A-P9 (3 lines): ALL OPENED**, including the full single ~600-character line 10.
- **A-P11 (69 lines): ALL 69 OPENED AND DISPLAYED**, both roots. This is the set the build
  reported as 95.
- **A-P16 (21 occurrences): ALL OPENED.**
- **A-P10 (75 lines) and A-P12/A-P13: counts only, plus the targeted residues.** For A-P10 I
  additionally ran a *per-file* check across all fourteen formation-site artifacts (§5.4);
  for A-P12 I ran and read the non-process residue (§6.3). Declared: I did not display all
  75 / 245 lines. No grade below rests on the undisplayed remainder.

---

## 4. D1 — AGENT OR CONDITION (Q1)

### 4.1 WHAT SURVIVES

Every one of the build's thirteen site citations was re-read at the source artifact's own
bytes. **All quoted spans are verbatim accurate**, including the adverse clauses, and the
line citations are accurate to within a header line in three cases (S-1 `:98-108` where the
heading sits at `:97`; S-2 `:56-65` where the amplitude block opens at `:53`; S-3 `:75-79`
where the `Because` sentence opens at `:78`). None of the three drifts changes a reading.

**No OPERATED site exists, and I hunted for one.** I ran the whole of the build's P-12
pattern (69 lines, all displayed) and a word-boundaried `agent` sweep, and read the
non-process residue. Every agentive subject in the corpus's formation texts is a structure
named inside the theory — `the finite parent`, `the boundary law`, `physical boundary
degrees of freedom`, `the same unit matrix element`, `the complete microscopic parent`. I
found **no performer external to the record and no selector of the moment**, exactly as the
build reports. The build's C-8 (type by the grammatical subject's position relative to the
record, not by the verb) is the correct instrument and is applied consistently.

I also confirm the build's F-7 self-correction at bytes: `agent` returns **0** in every one
of the fourteen formation-site artifacts, checked file by file. That finding is sound.

### 4.2 COR-A — THE KNOWN SITE IS MISGRADED, AGAINST THE BUILD'S OWN CRITERION

This is the heaviest correction and it is exactly where D1 said to look.

The build defines MIXED at S-5 as **condition ∧ program adoption**. It then grades S-5 MIXED
and S-1 CONDITION-DRIVEN. At bytes those two sites are the same move, and **the corpus files
them as two entries in one list.**

`STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md:57-67` (sealed, OK), quoted whole:

> ## Five Genuine Pure Minimality Sites
>
> The recovery sweep identifies local minimality or saturation content at these
> five pure sites:
>
> 1. `d = 4`: a derived lower bound is followed by an adopted lower-bound
>    selection.
> 2. `C_R = 1` record cell: a strict untrapped inequality leaves a half-line; the
>    marginal public-closure rule supplies the selected boundary point.
> 3. orthogonalization budget saturation: the lower bound is derived, but
>    saturation is adopted rather than derived.
> 4. `tau = 1` proper-time floor: a positive floor multiplier remains possible; the
>    unit floor is a frozen branch convention, not a theorem.
> 5. spin `1/2`: the sign selects half-integer classes; primitive minimality
>    selects the lowest class.

Item 2 **is** S-5. Item 3 **is** S-1. One list, one type, one gloss at `:38-41`:
`These are local equality, least-member, or single-normalization moves.` The build quotes
this whole passage at its own §5.5 and uses it to type *saturation* — and then does not
apply it to the grade.

**And the known site's adopted rule selects the moment in its own words.**
`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:97-108`, quoted whole:

> ## Adopted Gravacle onset rule
>
> The allow/require boundary is adopted to select first admissible record onset
> through a shortest **relative** projective path on the unique physical record
> cell. Conditional on this target-value-free, target-aware Level-1 rule,
>
> ```text
> J_FS,rel = pi hbar/2.
> ```
>
> The lower bound is derived from the imported theorem. Saturation is adopted,
> not derived. Historical target blindness is not claimed.

`adopted to select first admissible record **onset**`. An onset is a moment. The build's own
test T-c asks `Does anything select the MOMENT, as against the moment being the solution of a
stated condition?` — and at the known site the answer, in the corpus's own sentence, is that
an adopted rule selects the first admissible onset. The build answered T-c by pointing
elsewhere in the artifact, to `epsilon_rel tau_*/hbar = pi/2` — but that equation is not a
solved condition either. At bytes it is **taken**, inside a declared example,
`:112-117`, quoted whole:

> On the primitive record factor, take
>
> ```text
> H_W = epsilon_rel Y,
> epsilon_rel tau_*/hbar = pi/2.
> ```

The verb is `take`, and `:148` calls it `the declared two-state symmetric realization`. The
artifact's own status block books `unique_causal_record_interval_numerically_derived = false`
at `:190` — a line adverse to the build's reading of T-c which the build did not quote,
although it quoted `:182-183` from the same block.

**Sub-finding: the LEAD FINDING's characterization of the known site is unsupported at that
site's bytes.** The build's §0 says the known site's `moment is the *least positive*
solution, not a chosen one` (`:20-22`). I grepped `least|first|shortest|minimal|select`
across the whole of `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md`: it returns
**exactly two lines, 99 and 100**, and both sit *inside the adopted rule*
(`select first admissible record onset`, `a shortest relative projective path`). The word
`least` never occurs in that artifact. "Least positive" is imported from S-4
(`BID_FIRST_OPENING_INTERVAL_DERIVATION_V001.md:75-79`), a different artifact.

**CORRECTED GRADE S-1: MIXED (condition ∧ program adoption).** The condition half is real —
`<R_0|R_1>=0` is a stated condition with no performer, and I confirm that. The adoption half
is equally real, is about the *onset*, and the build itself names it: at `:876-878` the build
writes `Where the corpus reaches for a selector it is always the *program* selecting a
premise (S-1 saturation, S-5 marginal rule)` — naming S-1 as a selector site in the same
breath as the site it grades MIXED.

**This does not create an OPERATED site.** The selector is the program, not a physical
performer, in both cases. The direction of the headline is unchanged; the grade and the tally
are not.

Deciding bytes: `STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md:64-67` and
`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:99`.

### 4.3 COR-D — THE Q1 TALLY IS WRONG AT BOTH STAGES

Independent of COR-A, the build's arithmetic does not match its own table.

`STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md:858-874` displays thirteen graded rows
(S-1..S-12 plus S-6′). Counting CONDITION-DRIVEN rows in that table: S-1, S-2, S-3, S-4,
S-6, S-7, S-8, S-9, S-10, S-11, S-12 = **11**. The line immediately beneath, `:874`, states
`OPERATED: 0. CONDITION-DRIVEN: 10. MIXED: 1. INDETERMINATE-AT-BYTES: 1.` — **10, from a
table showing 11.**

`:903-904` then adds S-13 (CONDITION-DRIVEN) and reports
`OPERATED 0 · CONDITION-DRIVEN 11 · MIXED 1 · INDETERMINATE-AT-BYTES 1`. Adding one to a
base of 11 gives 12, not 11. The reported final number is right only because it inherits the
§4.1 undercount.

There is a second bookkeeping fault: the totals sum to 13, which is the **site** count, while
the graded rows number 14 (S-6 and S-6′ are two grades on one artifact). Site count and
graded-row count are conflated throughout §12.

**CORRECTED Q1 TALLY (13 sites, 14 graded rows), with COR-A applied:**

```text
OPERATED                 :  0
CONDITION-DRIVEN         : 11   (S-2, S-3, S-4, S-6, S-7, S-8, S-9, S-10, S-11, S-12, S-13)
MIXED                    :  2   (S-1, S-5 — both condition ∧ program adoption)
INDETERMINATE-AT-BYTES   :  1   (S-6′, "generated by")
                           --
GRADED ROWS              : 14   over 13 SITES across 14 ARTIFACTS
```

The build's headline number `CONDITION-DRIVEN 11` happens to coincide with the corrected
value, by two errors cancelling. **MIXED is 2, not 1**, and the row total is 14, not 13.

### 4.4 A SITE THE BUILD MISSED — THE WRITE ENVELOPE (S-14, audit-added)

The census has no entry for the object that fixes **the shape of the write in time**.

`STAGE8_T7_BETA_ER_A_DISCLOSED_PREMISE_ADOPTION_V001.md:17-27`, quoted whole:

> ## The premise
>
> ```text
> ER-A is carried as a DISCLOSED PREMISE:
>   the intrinsic-action envelope realization used throughout is
>   ER-A (write envelope tau_R w(t) M(t), w(t) = 32 r(t)^3),
>   NOT derived and NOT selected — assumed, and disclosed as assumed.
> ER-B (tau_R rho M(t), rho = 24/pi) remains UNEXCLUDED but
>   BATTERY-UNDEFINED: the sealed battery pins ER-A and never computes
>   ER-B; the absence of ER-B obligations is NOT an exclusion of ER-B,
>   and no later reader may read it as one.
> ```

and `:88-98`, quoted whole including the adverse closing clauses:

> action, not on any endpoint datum — at equal total action the shape
> coefficients differ (1/3 flat, 5/9 for the sealed r(t)^3 bump, 1
> concentrated). THE RECORD RETAINS HOW ACTION ACCUMULATED, NOT MERELY
> HOW MUCH. Consequently the ER fork is a PHYSICAL question about how
> action accumulates on a causal diamond, not a representational one, and
> the eventual route to discharging this premise is to DERIVE the
> write-rate from the record principle itself — not to show the fork does
> not matter. That route is explicitly NOT opened here; it is a
> principal-level scoping question for later.

`ER_A_selected = false` and `ER_B_selected = false` are booked in the same file's protected
status block.

**CLASSIFICATION S-14: MIXED (condition ∧ program adoption), audit-added.** It is neither
OPERATED — nothing external performs the envelope — nor purely condition-driven: the
envelope is `NOT derived and NOT selected — assumed`. It matters because it is the corpus's
own statement that the *temporal profile of the write* is currently a premise, and that the
corpus knows what would discharge it.

**TALLY INCLUDING S-14 (14 sites, 15 graded rows):**

```text
OPERATED  0  ·  CONDITION-DRIVEN 11  ·  MIXED 3  ·  INDETERMINATE-AT-BYTES 1
```

Both tallies are reported so the reader may take either scoping. The OPERATED count is 0
under both.

### 4.5 A LESSER OMISSION AT S-13

The build grades S-13 CONDITION-DRIVEN on the ground that the pointer direction is
`Forced by a condition, not chosen`. That is right about the *commutant* step. But the
condition itself is imposed, not obtained: `DURABLE_POINTER_CLOSURE_OPERATOR_SELECTOR_GATE_V001.md:24`
reads `For the post-closure pointer component only, **impose** nondemolition compatibility`,
and the file's own status block books `nondemolition_pointer_condition_adopted_for_selector = true`
at `:124`. Neither byte appears in the build's S-13 treatment. This does not move the grade —
an adopted condition is still not a physical performer — but the adverse bytes should have
been carried, and the site is a further instance of the condition∧adoption shape.

### 4.6 D1 GRADE

**CONFIRMED-WITH-CORRECTIONS.** The finding `OPERATED = 0` is confirmed at bytes and
survives an independent hunt. The known site was **not** resolved honestly: it was graded
CONDITION-DRIVEN under a criterion that the build applied as MIXED to the identical shape
one site later, and the lead finding's supporting characterization of its moment is imported
from another artifact. The tally is wrong at both stages and a site is missing.
**S-1 sub-grade: REFUTED.**

---

## 5. D2 — SPONTANEITY SIGNATURE (Q2)

### 5.1 MARK 1 — THRESHOLD/CRITICAL CONDITION ON FORMATION — **PRESENT (1 firm, 2 contested)**

The build reports PRESENT (3). One survives cleanly; two do not.

**M1-b survives.** `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:60-65`, quoted whole:

> First orthogonal onset gives
>
> ```text
> theta=pi/2,
> v_z=0.
> ```

This is a threshold on the onset itself. Confirmed.

**M1-a is CONTESTED.** The build calls `C_R <= 1` a threshold `on whether the thing can form
at all, not on a requirement`. But the corpus's own heading for that input, at
`BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:38`, is:

> 3. **Public recoverability condition (physical admissibility):**

and the excluded region is described as one that `cannot be the outwardly recoverable
primitive comparison cell of this branch` (`:46-47`). The object is the *admissibility of a
comparison cell*, and the commission expressly distinguishes thresholds on formation from
thresholds on **requirements**. Admissibility is a requirement. The partition of the
parameter line is real, so I do not strike M1-a; I grade it **CONTESTED**, and I note that
the build's own §5.1 asserts the opposite of the corpus's heading without quoting it.

**M1-c is CONTESTED by the build's own admission.** Its §5.1 closes: `**Adverse note:** its
role is to show that *nothing atomic obstructs* decay — it clears an obstruction rather than
triggering an onset.` A mark whose own treatment says it does not trigger an onset should not
be counted in a tally of thresholds on formation. Counted here as CONTESTED.

Corrected Mark 1: **PRESENT (1 firm, 2 contested)** rather than PRESENT (3).

### 5.2 MARK 2 — ORDER PARAMETER — **ABSENT (0). CONFIRMED, AND STRENGTHENED.**

All four `order[ _-]parameter` files opened at their matching lines; both are homonyms
exactly as the build's C-5 states, verbatim:

```text
COMPLETE_QSPEC_FINITE_HOLONOMY_RESPONSE_NUMERICS_PROTOCOL_V001.md:32
  "Use the expected second-order parameter and time tails:"
STAGE8_SCHUR_RATIO_PROTECTION_SCOPE_ATTACK_V001.md:79
  "already declared 3+2 order parameter.  It also says that the calculation does"
```

**I ran five patterns the build did not run, precisely to test this ABSENT.** All corroborate:

```text
phase transition | phase-transition ........................ 0 files, 0 lines
condensat .................................................. 0 files, 0 lines
vacuum expectation | vev (word-boundaried) ................. 0 files, 0 lines
Landau | Ginzburg | Goldstone .............................. 3 files, 13 lines — ALL OPENED;
   every one is a FILENAME inside an artifact-fingerprint manifest
   ("alpha_br_exact_charged_root_goldstone_superhessian_v001.md"), not physics content
latent heat | critical exponent | universality class ....... 6 files, 12 lines — ALL OPENED;
   "critical exponent" is the HS-rate/singular-value decay exponent of the wall analysis
   (STAGE8_COUNTING_BOUND_O24SR_*), and "universality class" appears only inside negatives
   ("nor Thomson matching is currently derived")
```

**Mark 2 ABSENT: CONFIRMED on a materially wider sweep than the build ran.**

### 5.3 MARK 3 — RATE / NUCLEATION — **COR-B: THE BUILD'S UNIVERSAL IS FALSE AT BYTES**

The build searched this mark with the single token `nucleat`, graded it `ABSENT (0)`, and
then asserted a universal at `:1005-1006`:

> Substantive count: **zero**. There is no nucleation rate, no onset rate, no formation
> rate anywhere in either root.

That universal is refuted at bytes. Sweeping `(formation|onset|nucleation|writing|write|
record-form)[ _-]rate|rate of (formation|onset|record|writing)` returns a **write-rate**:

`STAGE8_T7_BETA_ER_A_DISCLOSED_PREMISE_ADOPTION_V001.md:93-96`, quoted whole:

> the eventual route to discharging this premise is to DERIVE the
> write-rate from the record principle itself — not to show the fork does
> not matter.

with the object it belongs to at `:18-22`:

> ER-A (write envelope tau_R w(t) M(t), w(t) = 32 r(t)^3),
>   NOT derived and NOT selected — assumed, and disclosed as assumed.

and its re-statement at `STAGE8_STANDING_PREMISE_TEST_CLASSIFICATION_V001.md:425-434`, which
quotes the same span and books `ER_A_EXPOSING_ROUTE_IDENTIFIED = true`.

A **write envelope** is a rate-like time profile of the formation event, and the corpus
names deriving the **write-rate** as the route that would discharge the premise. This is
precisely a formation-rate object. It is not derived — but "not derived" is not "absent",
and the build's own §5.6 table reports `ABSENT (0 substantive of 1 raw)`.

**CORRECTED MARK 3: NAMED-BUT-NOT-DERIVED, not ABSENT.**

For completeness, the two adjacent hits I opened and judged **not** to be formation rates:
`STAGE8_ETHER_COLD_CONFIRM_OPUS_V001.md:185-187` — `the rate of record-phase response per
unit source holonomy` — is a per-holonomy winding, not a rate in time; and the W-1/W-3
subvolume and HS rates are decay/estimate rates in the wall analysis, as the build says.

### 5.4 COR-C — THE CITATION SUPPORTING THE RATE NEGATIVE IS MISATTRIBUTED

The build closes §5.3 with:

> and `MO2_KAPPA_RATE_S9AD_V001.md:48` books `does_not_derive_any_rate = true`

Two faults at bytes. The filename is `STAGE8_MO2_KAPPA_RATE_S9AD_V001.md`. And line 48 does
not book that flag for that artifact. The whole sentence, `:44-49`, quoted whole:

> Parallel S9AD lanes were checked at the cutoff for
> MO-2 occupation: NONE claims it (REFUTING_BRANCH flag
> `does_not_derive_any_rate = true`; WALL_BOUNDARY_CLOSURE: "MO-1/MO-2 never
> enter"; the MO3 lane occupies MO-3 only).

The flag belongs to **REFUTING_BRANCH**, a different lane, and is cited as evidence that no
parallel lane occupies MO-2. Meanwhile the artifact's own one-line verdict, `:51-57`, is the
opposite of what the citation is used for:

> ## 0. VERDICT IN ONE LINE
>
> **DERIVED — a certified LINEAR lower growth law (gamma = 1, constants
> symbolic): for the carrier compression family of record, per admitted state
> and cell, at every collapse-bearing cell time (r = r(t) > 0), for each
> frozen ell in {1, sqrt2}, uniformly for all four opposite-phase pairs at
> once,**

A booking pulled out of a clause whose subject is another lane, used to support a universal
negative about rates, on the facing page of a verdict reading `DERIVED — a certified LINEAR
lower growth law`. Deciding bytes:
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_MO2_KAPPA_RATE_S9AD_V001.md:44-49`
against `:51-57`.

### 5.5 MARK 4 — SYMMETRY BREAKING — **ABSENT AT FORMATION SITES. CONFIRMED.**

I ran the per-file test the build did not display: `symmetry[ _-]break|broken symmetry|
order[ _-]parameter|agent` against **each of the fourteen formation-site artifacts
individually**. Every one returns **0**. The build's finding is confirmed by a method
independent of reading 75 lines.

**Count correction:** the sweep returns **75** matching lines across 34 files, not 74. The
build's §3.1 says `**ALL 74 MATCHING LINES OPENED AND READ**` and breaks them down as
`61 lines across 25 R1 files and 13 lines across 9 R2 files`. The set is one line larger than
the set it reports having exhausted. Included in COR-F.

`spontaneous` itself: **3 lines in one artifact (mirrored)**, exactly as the build reports —
`STAGE8_TASK4A_NETWORK_SOURCING_LAW_ADOPTION_PROPOSAL_CODEX_LANE2_V001.md` at `:10`, `:91`,
`:719`. Confirmed. One quotation-integrity note: `:10` is a single ~600-character line and
the build's quote of it stops at `READY SYSTEMS.`; the line continues
`A FULLY AUTONOMOUS FEEDBACK LOOP STILL NEEDS A PORT-TO-WRITE-HISTORY UPDATE, WHICH THIS
COMMISSION DID NOT AUTHOR.` The omitted clause is not adverse — it reinforces the build's
reading — but under the mid-sentence line-wrap discipline the span was called complete when
it was not.

### 5.6 MARK 5 — EXTREMIZATION — **PRESENT IN FORM, NOT AS CAUSE. CONFIRMED.**

Every byte-level witness re-read and verbatim confirmed: the five-site list and its type
gloss `These are local equality, least-member, or single-normalization moves`
(`STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md:38-41, 57-71`);
`Saturation is adopted, not derived.` with `relative_onset_saturation_derived = false`
(`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:107, :182`); and

> An energy splitting proportional to `C_0` makes the endpoint sectors
> stationary under that component. Stationarity alone is not durability.

(`DURABLE_POINTER_CLOSURE_OPERATOR_SELECTOR_GATE_V001.md:109-110`). The build's determination
on "saturation" is correct and is the strongest reasoning in its report.

### 5.7 CORRECTED Q2

| Mark | Build | **Audit** |
|---|---|---|
| Threshold/critical condition on formation itself | PRESENT (3) | **PRESENT (1 firm, 2 contested)** |
| Order parameter, or anything in that role | ABSENT (0) | **ABSENT (0) — CONFIRMED, wider sweep** |
| Rate / onset rate / nucleation-like quantity | ABSENT (0) | **NAMED-BUT-NOT-DERIVED — REFUTED** |
| Symmetry breaking at formation sites | ABSENT (0 of 74) | **ABSENT — CONFIRMED (0 of 75)** |
| Extremization that makes formation happen | PARTIAL / NO | **PARTIAL / NO — CONFIRMED** |

**Q2 GRADE: PARTIAL — sustained.** But the build's headline claim at `:1128-1129`, that
`ABSENT: 3 of 5 ... and these three are exactly the marks that would show a *phase
transition*`, is **corrected to ABSENT: 2 of 5**. The rate leg is gone.

### 5.8 D2 GRADE

**CONFIRMED-WITH-CORRECTIONS**, with **Mark 3 sub-grade REFUTED.** Two of five marks are
confirmed on wider sweeps than the build ran; one is refuted at bytes by a pattern the build
never ran; one is overcounted; one carries a count that is one line short of the set it
claims to have exhausted, and one supporting citation is misattributed.

---

## 6. D3 — PHASE AS A CONDITION VARIABLE (Q3)

### 6.1 THE CHARGE HALF — CONFIRMED

`SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:22-43`, re-read whole:

> ## Declared primitive flux sector
>
> On the declared zero-or-one local unit-character source-crossing sector,
> inherit
>
> ```text
> Q_Sigma = integral_Sigma j^mu dSigma_mu,
> spec(Q_Sigma)={0,1}.
> ```
>
> This is a local branch input, not a derivation of every current-flux
> spectrum.
>
> For the charged record handle, adopt:
>
> ```text
> Q_Sigma=0  => no charged-record write;
> Q_Sigma=1  => the charged-record channel reaches first orthogonal onset.
> ```
>
> Other interactions and record handles can still evolve in the zero-flux
> branch.

`Q_Sigma` is the antecedent of the implication whose consequent is whether a write occurs.
That is presence **in** the condition, not near it. **CONFIRMED.** The build's three
qualifications — the implication is `adopt`ed, the spectrum is `a local branch input`, and
`chi is not fixed by the conditional record projectors` — are all verbatim accurate and all
adverse to its own finding. Carrying them was correct.

### 6.2 COR-E — THE PHASE HALF IS THE PREDICTED INFLATION

The build's C-7 states the ground for typing `theta` PHASE-TYPE:

> It is the phase angle of an amplitude at bytes: `The ready-state survival amplitude is
> cos(theta)-i(v_z/\|v\|)sin(theta), theta=\|v\| tau_*/hbar`

At bytes that ground is false. `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:53-65`,
quoted whole:

> The ready-state survival amplitude is
>
> ```text
> cos(theta)-i(v_z/|v|)sin(theta),
> theta=|v| tau_*/hbar.
> ```
>
> First orthogonal onset gives
>
> ```text
> theta=pi/2,
> v_z=0.
> ```

`theta` is not the phase angle of that amplitude. The amplitude's phase is set by the
`v_z/|v|` component; `theta` is the rotation angle of the holonomy. And the onset condition
the build cites as the phase-type site **sets `v_z=0` in the same breath** — which leaves the
amplitude `cos(theta)`, a real quantity with no phase to be a variable of. The corpus removes
the phase-carrying component at exactly the point where the build locates phase in the
condition.

This is the presence-near/presence-in inflation D3 named, and it is located precisely.

**CORRECTED S-2′ GRADE: INDETERMINATE-AT-BYTES on the ground stated.** `theta` is an angle
in a holonomy (`theta = |v| tau_*/hbar`, in an artifact titled `Record-Holonomy Gate`), and a
holonomy angle is phase-like in the corpus's own `Hol_gamma(a) = exp(i integral_gamma a)`
sense — so a PHASE-TYPE reading may be reachable. But it is not reachable on the ground the
build gives, and the build gave no other. At bytes the claim is not decided.

Deciding bytes: `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:56-57` against `:63-64`.

### 6.3 WHAT ELSE SURVIVES

The build's three adverse quotations are verbatim accurate and were the right ones to carry:
the FS gate takes a modulus (`arccos |<r|W(tau_*)|r>|`, `:70-71`); the first-opening interval
is `unchanged by edge orientation reversal or endpoint rephasing` (`:87-89`); and
`chi is not fixed by the conditional record projectors` with `Its gauge-invariant physical
effect is open.` (`:118-121`). The eight NO rows in the Q3 table were each checked against
their source artifact and each is correct.

### 6.4 D3 GRADE

**CONFIRMED-WITH-CORRECTIONS.** The Q3 verdict label stands because the charge-type finding
carries it. The phase-type companion is corrected to INDETERMINATE-AT-BYTES, so the build's
§0 sentence `A phase-type angle theta also enters the onset condition at the same gate`
should read as undecided at bytes, not established.

---

## 7. D4 — DOES THE CLOSURE ACT (Q4)

### 7.1 RECOUNT — THE TALLY STANDS

All nine clauses re-read at `STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md:109-180` (sealed, OK).
Every quoted span is verbatim accurate and every line citation is exact. I re-graded each
clause off its own operative verb and object, without reference to the build's grade:

| Clause | Operative verb at bytes | Object | Audit grade | Build |
|---|---|---|---|---|
| CS-1 | `=` (`sum_lambda w_lambda u_lambda = ... = P`); `GRADE: exact, WEIGHT-SUM-level` | weighted sum of branch unitaries | CLASSIFIES | same |
| CS-2 | `=`, self-named `display`; `offered as obligations ..., never as results` | `V_{mu lambda}(0) - 1` | CLASSIFIES | same |
| CS-3 | `:=`, `carries`; closes `it is a divergence source, not a budget` | HS mass `kappa_n` | CLASSIFIES | same |
| CS-4 | `<=`, `>=`, `ceiling`; `may NEVER default` | `\|det_n(0)\|`, `\|\|R_n\|\|_op` | DELIMITS | same |
| CS-5 | `0 <= b_D <= 1`, `compactly supported`, `vanishing to ALL ORDERS` | profile `b_D`, vertex `J` | DELIMITS | same |
| CS-6 | `IS` (the criterion); `are DERIVED ... from this one condition` | first-orthogonality criterion | CLASSIFIES | same |
| CS-7 | `=`, `GRADING`; `the quartet weight-sum kill is ABSENT` | strata `K_H`, `H_A` | CLASSIFIES | same |
| CS-8 | `>=` (LOWER bound), `bars` | `J_FS,rel`, `\|Delta S_record\|` | DELIMITS | same |
| CS-9 | `IS`, `is demoted to`, `is MANDATED` | locus; smoothness; proof method | DELIMITS | same |

```text
DELIMITS  : 4   (CS-4, CS-5, CS-8, CS-9)
CLASSIFIES: 5   (CS-1, CS-2, CS-3, CS-6, CS-7)
ACTS      : 0
TOTAL     : 9/9      — TALLY CONFIRMED UNCHANGED
```

I pressed the two clauses that could flip it, and both hold. **CS-7** contains the verb
`DOUBLE` (`odd strata DOUBLE`) — a verb of change; its object is a count of strata, a fact
about multiplicity, and the closure's own re-typing calls it `a GRADING, not a kill`.
**CS-9's `is MANDATED`** is the only imperative in the set; its object is
`sharp consumption of M(t) and 1_{D_t}`, a step in an estimate, and it binds the analyst
(`ADOPTED OF RECORD, binding`), not a record. The build's adjudication of both is sound and
was itemized rather than buried, which is the right handling.

**No clause was omitted from the primary census.** I enumerated every `CS-[0-9]+` label in
both the WBC artifact and its audit: the labels run `CS-1 .. CS-9` and no CS-10 exists in
either file. The build's C-1 scoping is also confirmed against the artifact's own title,
`THE SEALED BOUNDARY-CLOSURE STRUCTURE OF THE RECORD CELL`.

### 7.2 COR-F(i) — THE SECONDARY CENSUS IS NOT EXHAUSTIVE, THOUGH IT CLAIMS TO BE

The build's §7.3 opens:

> Every occurrence of `closure` in the thirteen formation sites and the three root
> principles was listed and read. Eleven clauses, none an ACTS on a record

Enumerated at bytes, that scope contains **21** occurrences, not 11:

```text
FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002 ........ 3   (all 3 listed)
CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001 .................. 2   (all 2 listed)
CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002 ................ 1   (listed)
CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001 ................ 0
BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003 ........ 2   (all 2 listed)
SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003 ........... 0
BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001 ........... 3   (2 listed; :162 OMITTED)
DURABLE_POINTER_CLOSURE_OPERATOR_SELECTOR_GATE_V001 ...... 9   (1 listed; 8 OMITTED)
BID_CHARGED_HANDLE_ACTIVATION_DERIVATION_V002 ............ 0
BID_FIRST_OPENING_INTERVAL_DERIVATION_V001 ............... 1   (OMITTED)
BID_FINITE_RECORD_DURABILITY_NO_GO_V001 .................. 0
                                                          --
TOTAL 21 ; LISTED 11 ; OMITTED 10
```

The census was run expressly `so C-1 cannot hide an ACTS clause by scoping` (C-2), and under
`Open what you count`. Ten of twenty-one is not every.

**I opened all ten omissions.** None ACTS on a record, so **the Q4 finding survives**:

- `BID_MINIMAL...:162` — `marginal_public_closure_rule_adopted_Level_1 = true` (status flag).
- `DURABLE_POINTER...:7-9` — `This gate asks whether the primitive two-endpoint record
  algebra **selects** a dimensionless durable closure-operator direction` — the only omitted
  occurrence with a selection verb. Its object is an operator direction, not a record; the
  artifact's own answer is that the direction is *forced* by the commutant condition, and
  `:129` books `complete_closure_operator_selected = false`. **Not an ACTS.**
- `DURABLE_POINTER...:24` (`impose`), `:57` (`leaves`), `:78` (`can enter`), `:91-92`
  (`cannot be`), `:129` (negative booking), `:1`, `:8` (titles) — none acts.
- `BID_FIRST_OPENING...:141` — `the complete record/environment action to preserve or
  redundantly encode the endpoint label after closure` — an obligation on the action.

### 7.3 D4 GRADE

**CONFIRMED-WITH-CORRECTIONS.** The tally is confirmed unchanged at **4 DELIMITS · 5
CLASSIFIES · 0 ACTS, 9/9**, the primary clause set is confirmed complete, and there is no
ACTS clause to quote. The secondary census's exhaustiveness claim is false and is corrected;
the finding it was meant to protect survives the correction.

---

## 8. D5 — THE DISCRIMINATOR (Q5)

### 8.1 THE NULL SURVIVES

I hunted independently and found no discriminator either. The build's three candidates were
each re-tested against the standard D5 sets: is it genuinely **false** under one reading, or
merely less natural?

- **`the finite parent performs the local reversible write`**
  (`CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md:25`). Genuinely true under both. The
  performer is the parent; the record is a factor of the parent
  (`A_SR(C)=CAR(K_Sigma,q) graded-tensor R(C)`, quoted in-corpus at
  `STAGE8_GAMMA_K_FINITE_REVERSIBLE_WRITE_OBS05_TEST_V001.md:87-91`); under a
  condition-driven reading a closed system still evolves its own subsystem. Not a
  discriminator. The build's observation that the two sibling clauses use stative verbs
  (`gives`, `leaves`) for the same kind of content is a real and fair point.
- **`Answer 1 - Is The Write Specified As An Operation? Yes`** — re-read at
  `:66-71` with its type at `:123-130`. Every term of the type is structural (generator,
  carrier, evolution); none names an agent, a decision, or a moment of application. True
  under both. The build's corroboration is verified: `continuous_write_strength_parameter =
  false` at `:191`, `coefficient_family_collapsed_to = (0,1,1)` at `:193`,
  `WRITE_CARRIES_SURVIVING_STRENGTH_PARAMETER = false` at `:335`.
- **`Durable records are generated by physical boundary degrees of freedom`**
  (`FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:72`). True under both; the corpus
  declines to fix the relation type. INDETERMINATE-AT-BYTES is the right grade.

I tested two candidates of my own that the build did not raise, and both fail:

- **The falsifier keyed on external selection.**
  `CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md:43` — `the parent requires a separately
  selected outgoing state or tail;` — and `CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md:73` —
  `the parent requires a separately selected post-write switch-off rule;`. These come closest
  in shape to a clause about external selection, and they are refuted as discriminators:
  their subject is the *completeness of the parent*, not agency at formation. An operator
  internal to the parent is still not "separately selected". True under both.
- **The ER-A write envelope** (§4.4). Its being an assumed premise is compatible with both
  readings. Not a discriminator — but see §8.3.

**Q5 VERDICT SUSTAINED: OBSERVATIONALLY-EQUIVALENT-AT-BYTES. No discriminator was
manufactured, by the build or by me.**

### 8.2 THE PRIOR NEGATIVE — QUOTED WHOLE, GOVERNANCE CORRECTLY SCOPED

Both spans were re-read at source and are **verbatim accurate and quoted whole**, including
the adverse paragraphs:
`STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md:9-35` (`The forced binary is
refuted.` through the physical-order/provenance-obligation display and the typed booking at
`:42-51`), and `:71-80` (`P5_supplied_by_unambiguously_means_temporal_production =
NO_VERDICT | reason: no sealed operational definition selects temporal creation over
derivational/common-framework provision`).

The build's three scope qualifications at §8.6 are each correct at bytes, and the second —
that the prior verdict is **partly positive**, booking `ROOT_PHYSICAL_ONTOLOGY = FORMATION` —
is the one a weaker report would have dropped. Carrying it was right.

One framing sentence the build did not quote sits immediately above the NO_VERDICT block, at
`:68-69`, and it reinforces rather than undercuts: `\`supplied by\` is not operationally
defined as either temporal production or common-framework provision.`

### 8.3 THE CLOSING "WHAT WOULD DECIDE IT" IS INCOMPLETE AT BYTES

The build's §8.7 closes:

> **What would decide it** ... a sealed operational definition distinguishing (a) a generator
> whose application is *conditioned* on a stated predicate from (b) a generator whose
> application is *selected* by something whose state is not itself a term of that predicate.
> No such definition exists in either root.

The definitional vacancy is real and correctly stated. But it is not the only route the
corpus names. `STAGE8_T7_BETA_ER_A_DISCLOSED_PREMISE_ADOPTION_V001.md:93-96` names a
**derivational** route in the corpus's own words: `the eventual route to discharging this
premise is to DERIVE the write-rate from the record principle itself`. If the write's time
profile were derived from the record principle rather than assumed, part of what the binary
turns on would move. The build's closing should carry that alongside the definitional
vacancy.

### 8.4 COR-F(ii) — THE SWEEP COUNTS BEHIND Q5 ARE NOT REPRODUCIBLE

I re-ran the build's **printed** patterns verbatim.

```text
P-12, pattern exactly as printed at :177 and :1506-1507
  build reports : 63 files raw / 62 after bar / 95 matching lines (69 R1, 26 R2)
  at bytes      : 62 files after bar /  69 matching lines (41 R1, 28 R2)
  -> the file count reproduces; the line total does NOT. 69 is the TOTAL, and the build
     reports it as the R1 subtotal and then adds 26 to it. Also: the build states
     "All 26 R2 lines were displayed and read in full" — R2 carries 28.

P-13, pattern exactly as printed at :178
  build reports : 143 files raw / 142 after bar / 220 matching lines (127 R1, 93 R2)
  as printed    : 184 files / 319 lines   [the class [^alnum_] is the six literal
                                           characters a,l,n,u,m,_ — not [^[:alnum:]_]]
  word-bounded  : 142 files / 245 lines
  -> the file count reproduces only under a correctly word-boundaried pattern; the line
     count reproduces under neither.

Consequence for :1809-1810, "315 matching lines swept across P-12 and P-13 alone":
  at bytes 69 + 245 = 314 (word-boundaried) or 69 + 319 = 388 (as printed).
  The reported 315 is the sum of two numbers neither of which reproduces.
```

**I opened all 69 P-12 lines and displayed them.** The build's four physical `performs` hits
are correctly identified and correctly adjudicated. Two further non-lane subjects were not
listed: `STAGE8_WALL_SUMMED_REQUANT_S9AD_V001.md:561` — `" weight sum ITSELF performs the
dressed-only subtraction"` — and its `:744` sibling; the subject is a mathematical object,
same class as the matrix element at (a), and neither is a formation site.

On the `agent` census: the build's load-bearing finding — **0 occurrences in all fourteen
formation-site artifacts** — is confirmed by me file by file, and its F-7 self-correction is
sound and well-reported. But its count of **8** non-process physical uses is short. My
residue additionally carries `STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md:123` and `:1421`
(`ASSIGNING-AGENT LOCATOR`) and `STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md:552` (a second
`destroying agent`). All are prior-lane glosses, so **the finding survives** — the corpus
wrote `assigns`, its readers wrote `agent` — but the build quantified over "anywhere" while
declaring at F-5 that it had not opened two files containing such uses.

### 8.5 COR-F(iii) — THE ONE `external selector` IN EITHER ROOT WAS NEVER SURFACED

The build's own P-12 pattern leads with `external selector`. That token matches exactly
twice in the corpus (one mirror pair), and neither hit appears anywhere in the build's
report. `STAGE8_GRAVITY_FORCES_COUPLING_HUNT_DARIO_V001.md:64-66`, quoted whole:

> ```text
>   member 04 [4442,8155)    847efdb85c7beceb5ad183df76f9a6ac189a07c9f242f1055f16b1a459dd2bdb
>                            the junction inventory; the external selector ADOPTED; the sealed failure
>                            of the edge action as a selector for the radius
> ```

**Adjudicated here.** This is a byte-span inventory in the gravity/junction coupling route.
The selector at issue is a selector **for the radius** — a scale — and the same line books
`the sealed failure of the edge action as a selector`. It is not a selector of a formation
moment, and it is **not an OPERATED site**; the file's own closure declaration reads
`NOTHING ENTERED OR ADOPTED.` So it does not change Q1 or Q5. But it is the only place in
either root where the commission's own phrase appears, the build's own sweep caught it, and
a report answering "does anything external select" should have itemized it rather than
passed over it.

### 8.6 D5 GRADE

**CONFIRMED-WITH-CORRECTIONS.** The null is correct, is not manufactured, and survives an
independent hunt including two candidates the build never raised. The prior negative is
quoted whole and its governing scope is stated correctly, with the qualifications that matter
carried. The counts underpinning the hunt are not reproducible at bytes, one whole-set claim
is two lines short, and the single most on-point token match in the corpus was never
surfaced.

---

## 9. D6 — BARS, FENCES, IMPORTS

**Bars.** `QUESTIONSSETTLED_REGISTER_V001.md` in the second root was suppressed by my array
on every pattern that reached it and **was never opened**. I opened **0 barred files**. The
build's claim of 0 barred files opened is consistent with its own leak table and with mine.
The build's F-2 disclosure — that register content reaches it through quotation in permitted
artifacts, unavoidably — is correct and correctly handled; it rests no grade on register
content alone. `DOR_*` under `supervision/` were read as permitted; nothing else there was
touched.

**Catalogued negatives.** The build declared at F-5 that it did not open
`STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md` or its `_AUDIT_`, then disclosed that its
complement sweep surfaced two lines from the pair and that it quoted them as prior-lane
gloss. That is honest handling of an unavoidable collision, disclosed rather than smoothed.
No re-litigation of a catalogued negative occurred, in the build or here.

**Fences.** `alpha_computed = false`, `proof_authorized = false`,
`kappa_record_computed = false` hold in the build and here. I checked every place the build
reproduces an expression (`tau_R=pi/sqrt(2)`, `C_R=1`, `T_R = 2 sqrt(pi) t_P`,
`J_FS,rel >= pi hbar/2`, `epsilon_rel tau_*/hbar = pi/2`, `S(tau_R) = 1`): each is
reproduced as a quoted string for classification and **none is evaluated, compared, reduced,
or approached in magnitude**. Same discipline observed in this audit.

**Authoring / advocacy / adoption.** No authored object, no adoption, and no premise amended
or retired, in the build or here. The closest the build comes to advocacy is `:1453-1454` —
`This is the commission's cleanest test, and it comes back clean in the direction of
"boundary, not former."` — which is emphatic. It does not amount to an adoption, and the
build immediately fences it at `:1976-1978`: `That is a lean, read off two texts. It is not a
verdict, and this lane does not promote it to one.` **No bar violation found.**

**Imports.** The build's IMPORT AUDIT was checked item by item. Three existence claims were
tested at the filesystem and all three are accurate: `alpha_first_durable_record_capacity_
principle_v001.md` and `boundary_access_closure_threshold_principle_v001.md` both exist one
directory up in `gravity_emergence_evidence_program/` — **outside both commission roots** —
and neither was opened by the build. I-1 through I-11 each state the survival test correctly.
This audit introduces no import beyond the same grammatical categories (I-11) and the
commission's own OPERATED/SPONTANEOUS labels (I-1). **Every finding above survives without
them**: the corrections are stated in the corpus's own vocabulary — `adopted`, `selector`,
`onset`, `write-rate`, `write envelope`, `admissibility`, `holonomy`, `DERIVED`.

**No external literature was consulted, and none is available.**

### 9.1 D6 GRADE

**CONFIRMED-WITH-CORRECTIONS.** Seals, bars, fences, and the import audit are clean and
independently verified. The corrections in this dimension are the count-reporting defects
already itemized at COR-F, which are a discipline failure (OPEN WHAT YOU COUNT) rather than
a bar or fence violation.

---

## 10. CORRECTIONS, IN SEVERITY ORDER

| # | Correction | Deciding file:line | Corrected value |
|---|---|---|---|
| **COR-A** | The known site S-1 is graded CONDITION-DRIVEN under a criterion the build applies as MIXED to the identical shape at S-5; the corpus files both as entries in one list; and the site's adopted rule selects the **onset** in its own words. The lead finding's "least positive solution" at that site is imported from another artifact — `least` never occurs there | `STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md:64-67`; `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:99` | **S-1 = MIXED** (was CONDITION-DRIVEN) |
| **COR-B** | Mark 3 graded ABSENT on a one-token sweep, with the universal `There is no nucleation rate, no onset rate, no formation rate anywhere in either root`. A **write-rate** and a **write envelope** exist and are named | `STAGE8_T7_BETA_ER_A_DISCLOSED_PREMISE_ADOPTION_V001.md:93-96` and `:18-22` | **Mark 3 = NAMED-BUT-NOT-DERIVED** (was ABSENT 0). Q2 ABSENT count **2 of 5**, not 3 |
| **COR-C** | The citation supporting the rate negative is misattributed: the flag belongs to another lane (REFUTING_BRANCH), the filename is wrong, and the cited artifact's own verdict is `DERIVED — a certified LINEAR lower growth law` | `STAGE8_MO2_KAPPA_RATE_S9AD_V001.md:44-49` against `:51-57` | Citation withdrawn as authority |
| **COR-D** | Q1 tally arithmetic: the §4.1 table shows 11 CONDITION-DRIVEN rows and the line beneath states 10; §4.2 then adds S-13 to that base and reports 11 where 12 follows. Site count and graded-row count are conflated | `STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md:874` and `:903-904` | **0 OPERATED · 11 CONDITION-DRIVEN · 2 MIXED · 1 INDETERMINATE = 14 rows over 13 sites** |
| **COR-E** | Q3's phase-type ground is false at bytes: `theta` is not the phase angle of the amplitude, and the cited onset condition sets `v_z=0`, leaving the amplitude real | `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:56-57` against `:63-64` | **S-2′ = INDETERMINATE-AT-BYTES** on the ground stated (charge-type finding unaffected) |
| **COR-F** | Three exhaustiveness/count failures: (i) secondary closure census lists 11 of 21 while claiming *every*; (ii) symmetry-breaking set is 75 lines, not the 74 claimed read in full; (iii) P-12 is 69 lines not 95, P-13's line count reproduces under no pattern, `external selector` never surfaced | (i) `BID_MINIMAL...:162`, `DURABLE_POINTER...:7-9`, `BID_FIRST_OPENING...:141`; (ii) sweep A-P10; (iii) `STAGE8_GRAVITY_FORCES_COUPLING_HUNT_DARIO_V001.md:64-66` | Counts corrected; **no finding overturned** |

**Lesser items, no grade moved:** S-13's condition is `impose`d and booked
`nondemolition_pointer_condition_adopted_for_selector = true` (`DURABLE_POINTER...:24`,
`:124`) — adverse bytes not carried; the known site's `unique_causal_record_interval_
numerically_derived = false` (`BOUNDARY_RECORD_ONSET...:190`) not quoted although `:182-183`
from the same block was; the `spontaneous` denial at `TASK4A...:10` quoted to a mid-line stop;
the `agent` non-process count of 8 is at least 11.

---

## 11. GRADES

```text
D1  AGENT OR CONDITION (Q1) .................. CONFIRMED-WITH-CORRECTIONS
      OPERATED = 0 .................................. CONFIRMED (independently hunted)
      known-site grade S-1 ........................... REFUTED  -> MIXED
      Q1 tally ....................................... CORRECTED (COR-D)
      census completeness ............................ CORRECTED (S-14 added)

D2  SPONTANEITY SIGNATURE (Q2) ............... CONFIRMED-WITH-CORRECTIONS
      Mark 1 threshold ............................... CORRECTED (1 firm, 2 contested)
      Mark 2 order parameter ......................... CONFIRMED (wider sweep, 5 new patterns)
      Mark 3 rate/nucleation ......................... REFUTED  -> NAMED-BUT-NOT-DERIVED
      Mark 4 symmetry breaking ....................... CONFIRMED (0 of 75; per-file test)
      Mark 5 extremization ........................... CONFIRMED
      Q2 grade PARTIAL ............................... SUSTAINED

D3  PHASE AS CONDITION VARIABLE (Q3) ......... CONFIRMED-WITH-CORRECTIONS
      charge-type (Q_Sigma) .......................... CONFIRMED
      phase-type (theta) ............................. REFUTED on the stated ground
                                                       -> INDETERMINATE-AT-BYTES

D4  DOES THE CLOSURE ACT (Q4) ................ CONFIRMED-WITH-CORRECTIONS
      tally 4 DELIMITS / 5 CLASSIFIES / 0 ACTS ....... CONFIRMED UNCHANGED
      primary clause set CS-1..CS-9 complete ......... CONFIRMED (no CS-10 exists)
      secondary census exhaustiveness ................ REFUTED (11 of 21) — finding survives

D5  THE DISCRIMINATOR (Q5) ................... CONFIRMED-WITH-CORRECTIONS
      OBSERVATIONALLY-EQUIVALENT-AT-BYTES ............ CONFIRMED (2 new candidates tested)
      prior negative quoted whole / scope ............ CONFIRMED
      sweep counts / external-selector omission ...... CORRECTED

D6  BARS, FENCES, IMPORTS .................... CONFIRMED-WITH-CORRECTIONS
      seals 26/26 OK ................................. CONFIRMED independently
      0 barred files opened .......................... CONFIRMED
      fences held .................................... CONFIRMED
      import audit ................................... CONFIRMED (3 existence claims tested)
      count reporting ................................ CORRECTED (COR-F)

OVERALL ...................................... CONFIRMED-WITH-CORRECTIONS
```

---

## 12. IMPORT AUDIT (THIS AUDIT)

| # | Import | Corpus-defined? | Survives without it? |
|---|---|---|---|
| AI-1 | OPERATED / SPONTANEOUS | NO — commission framing | **YES.** Every correction is stated in corpus vocabulary: `adopted`, `selector`, `onset`, `write-rate`, `write envelope`, `admissibility`, `DERIVED` |
| AI-2 | order parameter / nucleation / phase transition / condensate / VEV / Landau-Ginzburg / critical exponent | NO — my Mark-2 and Mark-3 test patterns | **YES.** Their absence at bytes is the result; introducing them as search tokens imports no claim |
| AI-3 | "phase angle of an amplitude" as a type | NO — the build's C-7 category | **YES.** COR-E is decided by the corpus's own `v_z=0` at `:64`, not by the imported category |
| AI-4 | grammatical categories (performer, antecedent/consequent, material implication, imperative) | NO — analytic instrument | **YES.** Each application is shown at bytes so it can be checked |
| AI-5 | Fubini-Study / Mandelstam-Tamm; Misner-Sharp | NO — corpus labels both as imports itself | **YES.** Neither is verified or used here; only the shape of the clause invoking them is classified |

No external literature was consulted. No notion outside this table was introduced.

---

## 13. FLAG BLOCK

**AF-1 — THE BUILD'S SCRATCHPAD-COLLISION REPORT (F-1) IS PLAUSIBLE AND WAS TAKEN
SERIOUSLY.** I could not verify the collision itself — the scratchpad state is transient and
I did not attempt to inspect another lane's files. I proceeded as if the warning were true:
this audit's sweeper was written under a unique name in an isolated subdirectory, and every
count reported here comes from it. The build's remedy (re-run all four affected sweeps under
a uniquely-named tool) is the right one, and reporting the incident rather than smoothing it
was correct. Note that the four sweeps it re-ran are the four where I found the Mark-3 defect
— but the defect is one of **pattern choice**, not of tooling: `nucleat` alone cannot find
`write-rate`.

**AF-2 — THE COUNT DEFECTS ARE SYSTEMATIC, NOT INCIDENTAL.** COR-F gathers five separate
count/exhaustiveness failures (secondary census 11-of-21; 74-vs-75 lines; P-12 69-vs-95;
P-13 unreproducible; agent non-process 8-vs-≥11). Individually each is small. Together they
mean the report's numeric claims should not be relied on without re-derivation, even where —
as in every case here — the finding they support survives.

**AF-3 — TWO OF THE BUILD'S SELF-CORRECTIONS ARE SOUND AND SHOULD BE PRESERVED.** F-7 (the
`agent` overclaim, caught and rewritten in flight) and the §5.5 determination on `saturation`
are the strongest work in the report. F-7's corrected form — that every physical use of
`agent` is a prior lane's gloss on the corpus's `assigns` — is confirmed at bytes and is more
informative than the claim it replaced.

**AF-4 — MY OWN DECLARED NON-OPENINGS.** I did not display all 75 symmetry-breaking lines or
all 245 `agent` lines; for both I ran a targeted test instead (per-file check across the
fourteen site artifacts; process-filtered residue) and say so. I did not open
`a32_holdout/custodian_private/`, `.proof_deps/`, `.pytest_cache/`, or any barred file. I did
not open the two out-of-root principle files at I-7/I-8, only confirmed their paths.

**AF-5 — WHAT THIS AUDIT DOES NOT DO.** It does not adopt, promote, retire, or amend
anything. It does not decide OPERATED versus SPONTANEOUS — that binary remains undecided at
bytes, and the corrections above do not move it. Correcting S-1 to MIXED moves a grade toward
*program adoption*, not toward an operator: no physical performer was found at any site by
either lane.

---

## 14. STATUS AND FENCES

```text
artifact_type = DEFAULT_REFUTE_AUDIT_AT_BYTES
commission = O47SR (AUDIT lane of a commissioned pair)
target_sha256_verified = true

OVERALL_GRADE = CONFIRMED-WITH-CORRECTIONS
D1 = CONFIRMED-WITH-CORRECTIONS   (S-1 sub-grade REFUTED)
D2 = CONFIRMED-WITH-CORRECTIONS   (Mark 3 sub-grade REFUTED)
D3 = CONFIRMED-WITH-CORRECTIONS   (phase-type sub-grade REFUTED)
D4 = CONFIRMED-WITH-CORRECTIONS   (tally CONFIRMED UNCHANGED)
D5 = CONFIRMED-WITH-CORRECTIONS   (null CONFIRMED)
D6 = CONFIRMED-WITH-CORRECTIONS

CORRECTED_Q1_sites = 13  (14 audit-added: S-14 ER-A write envelope)
CORRECTED_Q1_graded_rows = 14  (15 with S-14)
CORRECTED_Q1_OPERATED = 0
CORRECTED_Q1_CONDITION_DRIVEN = 11
CORRECTED_Q1_MIXED = 2           (S-1, S-5)   [3 with S-14]
CORRECTED_Q1_INDETERMINATE_AT_BYTES = 1       (S-6')
CORRECTED_Q1_known_site_grade = MIXED         (was CONDITION-DRIVEN)
Q1_external_selector_of_the_moment_found = false   [CONFIRMED]

CORRECTED_Q2_GRADE = PARTIAL (sustained)
CORRECTED_Q2_threshold_on_formation = PRESENT (1 firm, 2 contested)
CORRECTED_Q2_order_parameter = ABSENT (0)     [CONFIRMED on 5 additional patterns]
CORRECTED_Q2_rate_or_nucleation = NAMED-BUT-NOT-DERIVED   (was ABSENT)
CORRECTED_Q2_symmetry_breaking_at_formation = ABSENT (0 of 75 lines; 0 in all 14 site files)
CORRECTED_Q2_extremization = PRESENT IN FORM, NOT AS CAUSE   [CONFIRMED]
CORRECTED_Q2_marks_absent = 2 of 5            (was 3 of 5)

CORRECTED_Q3_GRADE = PHASE-IS-A-CONDITION-VARIABLE, CHARGE-TYPE ONLY
CORRECTED_Q3_charge_type = CONFIRMED   (Q_Sigma, antecedent of the write implication)
CORRECTED_Q3_phase_type = INDETERMINATE-AT-BYTES on the ground stated

CORRECTED_Q4_DELIMITS = 4
CORRECTED_Q4_CLASSIFIES = 5
CORRECTED_Q4_ACTS = 0
CORRECTED_Q4_TOTAL = 9/9                      [TALLY CONFIRMED UNCHANGED]
Q4_primary_clause_set_complete = true         (CS-1..CS-9; no CS-10 in either WBC file)
Q4_secondary_census_exhaustive_as_claimed = false   (11 of 21 listed; 0 ACTS among the 10)

CORRECTED_Q5_VERDICT = OBSERVATIONALLY-EQUIVALENT-AT-BYTES   [CONFIRMED]
Q5_discriminator_found = false
Q5_discriminator_manufactured = false
Q5_audit_candidates_tested_and_failed = 2
Q5_prior_negative_quoted_whole = true         [VERIFIED VERBATIM]
Q5_prior_material_governs = true (with the build's three qualifications, each verified)

seals_verified = 26/26 OK
seals_absent_sidecar = 7   [independently confirmed]
barred_files_opened = 0
patterns_run = 16

authoring_performed = false
advocacy_performed = false
adoption_performed = false
premise_amended_or_retired = false
catalogued_negative_relitigated = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
coupling_computed = false
scale_computed = false
root_computed = false
eigenvalue_computed = false
norm_computed = false
constant_computed = false
numeric_evaluation_performed = false
magnitude_approached = false
measured_value_comparison_performed = false
protected_holdout_access_status = PROHIBITED_AND_NOT_PERFORMED
```

END OF ARTIFACT.
