# STAGE 8 TASK 5 / EQ6 — REVIEW OF THE THREE REPAIRS + THE J7-CONTRACT ADJUDICATION — DARIO V001

Date: 2026-08-05
Lane: Dario (Claude Opus 5), cross-family reviewer and adjudicator of record
Task: PASTE 565 / Task 5 / EQ6
Custody: review of record on Lane 3's build of my §7 items 1–3, and adjudication of J7's contract.
Basis: my membership review (`9caef0f7…`) and FC6 determination (`d032309d…`), both verified.

## Lead result

```text
REGISTER_HEAD = Q-491

F1(a) coefficient face      = DEFECTIVE (+scope not shown inhabited; +tau_r sourceless)
F1(b) displayed J2 on A_RP+ = KILL      (+(F3) and (F1) asserted as sealed, unsupplied)
F1(c) pi_Mx/Loc typing      = PASS      (+the double-Loc defect is genuinely retired)

O1 = GENUINE (+stronger than the build states: forced, not merely unproven, on the
     acyclic branch; and it has TWO branches, only one of which is a forcing)
O2 = GENUINE (+no route; the one scalar bottom coordinate is the ALGEBRAIC reader's
     shadow, so using it is the F_PLDEC circularity class, not merely absent)

J7_AMENDMENT = candidate stated, full gate, NOT ADOPTED
SIX_AS_ONE_PRINCIPLE = yes as mathematical content; no as one authority
  -- and the count is wrong: only THREE of the six are amendments of a demand of record

VERB_AUDIT_SELF = CLEAN (+2 disclosed corrections to my own prior artifacts)

MEMBER_BOUND = false ; FIXED_POINT_EXECUTED = false ; END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false ; MEASURED_CONSTANT_COMPARISON = none
MACHINERY_APPEAL = false (the census's appeal at Q-490 stands; I add none)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

**The headline.** The typing repair is real and I confirm it: the double-`Loc` defect I found is
genuinely retired. The coefficient face is the first `chi^Mx`/`T^Mx` extraction the program has ever
had, and its fixed-stage algebra is correct — I recomputed it. But **the displayed J2 reconstruction
does not discharge the debt I recorded; it relocates it.** Where the J2 build said "can be
reconstructed" of the composite, this build says "the actual Q-408 and local-symbol stock supplies"
of the premises — and for two of those premises, `(F1)` and `(F3)`, it does not. The `(F3)` seam is
exactly where the chain crosses from the Q-408 kernel side onto the Hodge cochain carrier, and that
crossing is unsupplied in the entire corpus.

## 0. Preflight

| Check | Result |
|---|---|
| Register head Q-491 | verified (my own FC6 row) |
| Build under review `9c74ed3d…` | verified before reading |
| My membership review `9caef0f7…` | verified |
| My FC6 determination `d032309d…` | verified against its workspace sidecar |
| Census `676863b5…` | verified |
| Output name absent before construction | verified — no clobber |

## 1. F1 — the three repairs

### 1.1 (a) The `A_RP^+` coefficient face — DEFECTIVE, with real content

[PROVABLE] **What I recomputed and confirm.** The fixed-stage extraction is correct:

- `(H3)` the Laplacian intertwiner — (H1) into the `d` slot, (H2) into the `δ` slot, both directions
  checked;
- `(H4)` the spectral-polynomial step, taken correctly **on the union of the two finite spectra**
  (`p(0)=1`, `p=0` on each nonzero spectral value gives `p(Δ)=P_H`);
- `(H5)` by adjoint, using `ρ_f^C=(j_f^C)^*` and `P_H` self-adjoint;
- `(F6)` the normalization-line transport — each of the six steps consumes exactly one of
  `(F5),(F1),(F2),(F3),(F4)`, in order, landing `m_T,M = j_f^C m_T,N`;
- `(C2)` via `Tr(T·(x^♭⊗x^♭)) = <x,Tx>`; `(C6)` by trace cyclicity; `(C8)` by composition.

[PROVABLE] **A result the build understates.** Since `j_f^C` is injective, `(F6)` makes
`m_T ≠ 0` propagate in **both** directions along a rank-preserving arrow. Hence `A_RP^+` is a union
of **connected components** of `A_RP`, and the two conjuncts of `(F10)` are redundant. The build
states the forward half only ("constant along every rank-preserving connected component once it
holds at one object"). The component-wise character matters: it is what makes the support
*intensional* rather than a pointwise side condition, and §3 uses it.

[PROVABLE] **DEFECT 1 — the scope is not shown inhabited, and the disclosure is at the wrong
strength.** Build line 323 says the stock "does not establish that **every** such component lies in
`(F10)`". The truth is stronger and is sealed in the census:

```text
(MT-4)  m_T,X != 0  iff  <s_T,X, c>_X != 0
census: "The sources prove the existence of harmonic cycle vectors at cyclic stages,
         but do not prove (MT-4)'s nonzero pairing for the particular L_T symbol.
         They expressly retain both normalization branches."
census (CONS-2): "cycle-carrying vertex -> m_T UNDETERMINED absent a seed theorem"
census line 612: "inference `cycle present => m_T != 0` | expressly rejected by (MT-3)-(MT-5)"
```

**No stage anywhere is established to have `m_T ≠ 0`.** So `A_RP^+` is not established nonempty and
`J7_COEFFICIENT_FACE_ON_A_RP_PLUS = BUILT` is a theorem on a possibly-empty subcategory. The
mathematics is not wrong — it is conditional, and the condition is unmet. This is the vacuity
failure mode, and it is the same one the census expressly refuses to accept as a discharge. The
correct board line is `BUILT ON A_RP^+, INHABITANCE OF A_RP^+ NOT ESTABLISHED (+no seed theorem)`.

[PROVABLE] **DEFECT 2 — `tau_r` is sourceless, and `(D6)`/`(D7)` need more than it.** Build line 704
introduces `tau_r` as "the sealed action-unit/reality transport on the symbolic response tuple" with
no equation, no source file, and no entry in either preflight table. `(D1a)` then writes
`q_D := tau_g(q_B) = tau_h(q_C)` — the equality of the two response-transport routes is carried by
the notation, not by a proof. Independently, the four-vertex chains `(D6)`/`(D7)` additionally need
`mu_B = mu_C = mu_D` and the corresponding equalities on `dot_omega` and `mu_i`, which are nowhere
displayed. `(D1a)`'s `s_B = s_C = s_D = s_N` is fine — it follows from `j^K` isometry — but the
response-tuple half of the diamond is asserted.

[PROVABLE] The consequence is bounded and worth stating precisely: the **fixed-stage** face `(C1)`–
`(C8)` stands; the **four-position diamond** `(D2)`–`(D7)` does not, pending `tau`'s source and its
diamond functoriality.

### 1.2 (b) The displayed J2 reconstruction — KILL

[PROVABLE] My standard, which this item was commissioned to meet, was: no "can be", every
intermediate written. The **composite** now meets it — `(J2-9)`, `(RL4)`, `(RF6)` and `(IND4)` are
written out factor by factor, and I verified each substitution. But the standard applies to the
premises too, and two of the four factor squares the display consumes are asserted as sealed supply
and are not sealed anywhere.

[PROVABLE] Build lines 255–262: "On `A_RP`, the actual Q-408 and local-symbol stock supplies"
`(F1)`–`(F4)`. Checking each:

| Square | Status |
|---|---|
| `(F4)` `P_H,M j_f^C = j_f^C P_H,N` | **SUPPLIED** — it is `(H5)`, derived in-artifact from the sealed `(H1)`,`(H2)` |
| `(F2)` `Kernbar_M j_f^prof = eta_f^K Kernbar_N` | supplied by the Q-408 stock |
| `(F1)` `Q_M j_f^E = j_f^prof Q_N` | **NOT SUPPLIED** — the stock gives `j_prof,f` *on the quotient*; no prequotient map descending through the relations is displayed. It requires `j_f^E(Rel_N) ⊆ Rel_M`, written nowhere. |
| `(F3)` `Loc_M eta_f^K = j_f^C Loc_N` | **NOT SUPPLIED — and this is the kill** |

[PROVABLE] `(F3)` is the single step that carries the whole chain off the Q-408 kernel side and onto
the Hodge cochain carrier. What the projector cert actually derives is narrower:

```text
projector cert V003, line 126:
| rank-preserving Hodge naturality | DERIVED | rank-preserving W3 scope | `ρ_f d`, `ρ_f δ` equations |
```

`d` and `δ` — **not `Loc`.** From those two, `P_H` naturality follows (that is `(H3)`–`(H5)`, and it
is correct). `Loc`'s naturality is a different square about a different map, and a workspace-wide
search returns no sealed source for it. The program has already noticed the gap in this exact place:
Q-468 records "the fixed-stage theorem does not itself give refinement naturality (needed for J2 on
all arrows)."

[PROVABLE] So the debt has moved one factor deeper, not gone. The J2 build said the composite "can
be reconstructed" from three sealed intertwiners; this build reconstructs it from **five** squares,
of which two are new and unsupplied. **`J2_RP_RECONSTRUCTION = DISPLAYED` is not assertible.** The
correct verb is `DISPLAYED MODULO (F1),(F3)`, with both named as open.

[PROVABLE] Two lesser items, recorded because the artifact claims completeness:

- **`(RL4)` is not an independent display.** It is `(J2-9)` with `z := z_r` substituted — the same
  six equalities with a different letter. The reciprocal-loop generator was the one case I named at
  minimum, and a substitution instance does not exercise it. `(RL1)`–`(RL2)` *are* a genuine
  computation (I verified: the two `ε_u` factors multiply to 1), but they establish the projector
  square, which `(H5)` already gives.
- **`Loc_N` linearity is unestablished.** `(J2-7)` decomposes `reader_N^E(a,b,C)` by linearity;
  `Q_N` and `Kernbar_N` are fine, but `Loc_N = sigma_N^fin` has no stated linear structure — indeed
  no stated domain or codomain — in sealed stock. This is a facet of the `(F3)` defect: the build
  adopts one of two conflicting sealed readings of what `Loc` is.

### 1.3 (c) The `pi_Mx`/`Loc` typing — PASS

[PROVABLE] This is the repair that works, and I confirm it against the artifacts it classifies.

The double-`Loc` defect I recorded was real, and I can now state its origin exactly. Projector cert
V003 lines 155–158 define

```text
pi_Mx := π_M^resp ∘ P_H,M ∘ Loc_M
and compare via   r_f^Bot ∘ pi_Mx ∘ Eta_f = pi_Nx
```

— a **bottom-valued** `pi_Mx` that already contains `Loc`, used in a square with **no** further
`Loc`. A6's square then writes `r_f^Bot ∘ pi_Mx,M ∘ Loc_M ∘ eta_f = pi_Mx,N ∘ Loc_N`, adding a second
`Loc`. Read with V003's `pi_Mx`, that is the double-`Loc` expression. The build's resolution — a
scalar `(T5) pi_Mx,N := lambda_N ∘ P_H,N`, containing `P_H` and **no** `Loc`, kept type-distinct from
the bundled bottom leg `(T9) J_(N|f)^bot : D_f^old -> Bot_N^resp` — is correct and it dissolves the
expression without redefining anything away.

[PROVABLE] I checked the row I most suspected. Row 10 assigns J2-build line 69's string
`π_Mx,N := π_M^resp ∘ P_H,N ∘ Loc_N` to the bundled leg `(T9)`, even though the J2 build placed that
string under a bullet labelled "rank-preserving". Tracing the string to its origin settles it: it is
a mis-subscripted copy of V003 line 156, which is manifestly bottom-valued (it contains `π_M^resp`)
and is consumed by V003 in the bottom comparison. **The build's referent-identification is right and
the J2 build's placement was the error.** Row 10 stands.

[PART-PROVABLE] Two registrar notes, not defects. (i) Row 1 merges two sources with opposite status
verdicts — Maxwell/Hodge builds the scalar candidate on the `m_T≠0` branch while physical J2 records
`pi_Mx,N = NOT_BUILT / TYPE-U` — and the row carries no note of the split. (ii) Under the retyping,
A6's residue is strictly larger than A6's own text states: A6 retains "FULL J2 on the new-cycle
factor" as post-scope, but the whole scalar J2 on cycle-creating arrows including the old-image part
is now post-scope. The build's own board line 900 says the larger thing
(`FULL_CYCLE_CREATING_SCALAR_J2 = POST_SCOPE_NOT_FORMED`); the table does not connect it to A6. Both
belong in the amendment's recital, not in a defect list.

## 2. F2 — are the obstructions genuine?

### 2.1 O1 — GENUINE, and stronger than stated, but two-branched

[PROVABLE] **No route exists.** I searched the sealed stock for any theorem, nondegeneracy
condition, Ward anchoring, or where-clause admissibility rule forcing `m_T,N ≠ 0`. There is none, and
the census records the inference as **expressly rejected**: "`cycle present => m_T != 0` — expressly
rejected by (MT-3)–(MT-5)".

[PROVABLE] **The build understates it on one branch.** Build line 813 offers only an absence of proof
("No all-stage nonvanishing theorem occurs in the sealed stock"). At an **acyclic** stage the
statement is a forcing, not an absence: the cycle space `K_N = ker(B_N^T)` is trivial, so there is no
harmonic sector, `H_N = {0}`, hence `P_H,N = 0`, hence `m_T,N = P_H,N(…) = 0` **necessarily**. The
census records the same at its stage tables ("connected tree; `H={0}`"). At those positions the
address is *principled* — there is no Maxwell sector there to read — which is exactly what an
amendment must say instead of "the denominator vanishes".

[PROVABLE] **But O1 has two branches and only one is a forcing.** By `(MT-3)`, `H_X ≠ {0}` does not
imply `P_H,X s_T,X ≠ 0`: at a cycle-carrying stage with vanishing transverse pairing, `m_T,X = 0`
while a Maxwell sector *does* exist. The census types those as **UNDETERMINED**, not zero. Any
amendment carrying only the acyclic reason would under-describe O1 and would commit, on the second
branch, precisely the vacuousness slip the census refuses. Both branches must be carried separately.

[PART-PROVABLE] Minor: build line 805 attributes the zero branch to projector cert V003; the
substantive zero-branch content is in the Maxwell/Hodge cert (`m_T,N=0` voids (2-5); "no denominator
is …"). A citation slip, not a defect of the obstruction.

### 2.2 O2 — GENUINE, and non-closure is stronger than absence

[PROVABLE] **The scalar bottom coordinates do not close it.** `Bot_N` does contain scalar entries —
`ReaderBottom_N` and `Q243_N` — and a hostile reader reaches for them first. `ReaderBottom_N` is the
bottom shadow of the `R2_alg^fin` coordinate, i.e. of the sealed **algebraic** reader family. Routing
the Maxwell coefficient face through it would define the physical comparison from the algebraic
reader — the F_PLDEC circularity class, which DoR-020-A6 and the J2 build's K5 expressly closed. So
O2 is not merely "no arrow exists"; **the one available arrow is forbidden.** That is a stronger and
more durable statement of the obstruction than the build gives, and the amendment should carry it.

[PROVABLE] **No placement `H_N -> P_N` exists** in the assembly, the projector certs, or the scoped
mate cert.

[PROVABLE] **The tempting repair is the D3 defect returning.** One could restate J7 with codomain
`Bot^resp` instead of `R`, making it transportable by `r_f^Bot`. That is exactly the move I killed:
it collapses J7 into the A6 bottom square and discharges two distinct hypotheses of
`COMPLETION_THEOREM_V2` with one equation. The build declines it explicitly (line 826) and is right
to. **My D3 finding is correctly stated and still stands.**

[PROVABLE] O1 and O2 are **independent**: the census confirms "even a future proof that all cyclic
vertices have nonzero `m_T` would not close O2. Conversely, retaining A6 on the old-image bottom does
not close the scalar coefficient face because its codomain remains `Bot^resp`, not `R`."

## 3. F3 — THE ADJUDICATION: J7's true contract

[YOURS] Offered as a full-gate candidate for the principal. **Not adopted here.** No lane adopts it.

### 3.1 The demand of record, and why it cannot be met as written

[PROVABLE] The contract, from the diamonds artifact D2 / J7-OPEN slot, as the census renders it:

```text
per square admitted to FC11:
  four scalar stage readers at N, B, C, D;
  four coefficient-transport faces on a, f, g, h;
  one equality of the two N-to-D routes.                    (CEN-2)
```

with "no acyclic-vertex exemption and no cycle-creating new-factor exemption." The census executed it
on the three actual prototypes and returned `CENSUS = DEMANDED_UNFORMABLE`.

### 3.2 The intensional support — stated by property

[YOURS] The support is **two-sorted**, and it needs a third truth value.

```text
(S-STAGE)  A stage-reader position at X is IN SUPPORT iff  m_T,X != 0,
           equivalently  <s_T,X, h>_X != 0 for some h in H_X.
           Out of support in exactly two ways:
             (O1-Z) H_X = {0}  -- acyclic; m_T,X = 0 is FORCED; no Maxwell sector exists
                    at X. The address is principled.
             (O1-U) H_X != {0} but the transverse pairing vanishes -- UNDETERMINED,
                    not proved absent; awaits a seed theorem.

(S-FACE)   A transport-face position on a leg r : X -> Y is IN SUPPORT iff
             both X and Y satisfy (S-STAGE), AND r lies in A_RP.
           Out of support in exactly two ways:
             (O2-old) r in A_CC: only a Bot^resp-valued comparison exists (the A6 square);
                      no R-valued face.
             (O2-new) the position is a target-only new-cycle factor: no comparison exists
                      at all.

(S-UNCL)   An arrow not yet classified into A_iso/A_RP/A_CC/A_CR carries NO verdict.
           Formability is UNDETERMINED, not absent. This value exists because the corpus
           exhibits at least one such consumer witness.
```

[PROVABLE] **Why this is genuinely intensional and not a lane's convenience.** By `(F6)` plus
injectivity of `j_f^C`, `(S-STAGE)` is **constant on connected components of `A_RP`**. The support is
therefore a union of components determined by a structural property of the record, checkable by the
census's own deterministic classifier `(CONS-2)`, not a list a lane may edit. And the address
predicate `m_T` is fixed at step five of the build's dependency order `(B1)`, strictly upstream of
`lambda`, `pi_Mx`, `ell^Mx`, `reader^E` and the `chi`/`T` extraction — so the scoping predicate is not
derived from the thing being scoped. That is the line between a lawful address and the F_PLDEC class.

### 3.3 What out-of-support positions carry instead — the decision, with reasons

[YOURS] **Not "vacuous."** The census's refusal is correct and I uphold it: "Calling either position
vacuous would change D2 rather than satisfy it." A vacuous reading would make the row true at exactly
the positions that motivated it. Beyond that, a vacuous pass would be indistinguishable from the
hollow-pass failure mode I killed at D3, and the program has already paid for that once.

[YOURS] The lawful disposition is **split by branch**, because the four out-of-support cases are not
the same kind of absence:

| Position | Carries | Why this and not something else |
|---|---|---|
| `(O1-Z)` acyclic | **NOT FORMED**, with the structural reason displayed: `H_X={0} ⟹ P_H,X=0 ⟹ m_T,X=0`; no Maxwell sector exists at `X` | A typed absence with a principled address. Not vacuous — the demand is recorded as unmet, and the reason is geometric, not evidential. This mirrors the FC6 precedent: "vacuous by empty source domain, not by metric behaviour, and any scoped row must say so." |
| `(O1-U)` cyclic, vanishing pairing | **UNDETERMINED**, naming the seed theorem as the object that would settle it | Absence of proof, not proof of absence. Typing it as NOT FORMED would be the vacuousness slip on the second branch. |
| `(O2-old)` `A_CC` old-image | the **A6 bottom square `(T10)`** as an explicitly labelled **weaker comparison** — plus the `R`-valued face retained as a **post-scope condition** | Lawful and precedented (A6 itself). Critical rider: the weaker comparison must be labelled as *not* the coefficient face. Consuming it *as* J7 is the D3 defect. |
| `(O2-new)` target-only new cycle | **post-scope condition only** | No comparison of any strength exists there. |

### 3.4 Effect on `COMPLETION_THEOREM_V2`'s hypothesis

[PROVABLE] The theorem takes J2 and J7 as **two** faces ("Given J2 and J7, the only missing equations
are precisely the two inserted faces"). Under the amendment its J7 hypothesis is discharged **only on
squares lying wholly in `A_RP^+`**, and FC11 becomes support-scoped in the same way FC6 is.

[PROVABLE] **The amendment does not rescue FC11 on the actual tower.** Of the three executed
prototypes: `D-sub` lies wholly in `A_RP`, so its status turns only on whether its vertices satisfy
`(S-STAGE)` — currently `UNDETERMINED`; `D-disj` and `D-contact` each contain an acyclic source
`(O1-Z)` and target-only new-cycle positions `(O2-new)`, so both are out. **No actual cycle-creating
diamond discharges the J7 hypothesis under the amendment.** This is a truth-in-labelling amendment,
not a repair. My D3 kill of the membership theorem stands whether or not the principal adopts.

### 3.5 Genuine alternatives, kept of record

| Candidate | Content | Assessment |
|---|---|---|
| (a) **The support-scoped row above** | state the address; type the four residues | **minimal** — see below |
| (b) Prove the seed theorem and close O2 | the unconditional row | The real repair. The seed theorem is a bounded target; O2 may be genuinely obstructed, so (b) is not known to be reachable. |
| (c) Restate J7 with codomain `Bot^resp` | transportable by `r_f^Bot` | **REJECT** — collapses J7 into A6/J2; the D3 defect. |
| (d) Strike J7 from `COMPLETION_THEOREM_V2`'s hypotheses | fewer hypotheses | **REJECT** — the theorem then proves strictly less, and D2 says both inserted faces are needed; FC11 would not follow. |
| (e) Reject / leave open | no row | Lawful. FC11 stays open on cycle-creating diamonds indefinitely; the all-rank cascade stays PARTIAL. |

**Minimality of (a).** It adds no mathematical content whatever. It states the address at which an
already-ratified demand is met, and types the residue. Zero new coefficients, no member selected, no
map constructed; it reduces to the existing D2 contract on the support. That is the same minimality
standard A4, A5 and A6 were adopted under.

### 3.6 Void conditions — all falsifiable

```text
1  any position INSIDE the declared support where the face fails to form, or where the
   two N-to-D routes disagree -- DIRECTLY FALSIFIABLE;
2  any use of the address to EXEMPT a position that is in fact formable -- support-shrink
   to escape a demand; this is the clause-(vi)/Q-451 hazard and is the critical one;
3  any substitution of the A6 bottom square for the R-valued coefficient face (the D3 defect),
   including consuming the weaker comparison of (O2-old) as if it were J7;
4  any reading of an out-of-support position as "vacuous", or as satisfying (CEN-2);
5  a later seed theorem proving m_T != 0 at a stage where the face still fails to form;
6  any redefinition of the address predicate m_T downstream of lambda/pi_Mx/reader --
   i.e. any breach of the (B1) dependency order that made the address lawful.
```

### 3.7 The clause awaiting the manifests — stated exactly

[PROVABLE] **Manifest-pending: the global cardinality clause only** — i.e. the quantifier "for every
square admitted to FC11". The census's classifier `(CONS-2)` is deterministic and will type any
instance that arrives, so the *intension* of the support is complete now; what is missing is the
**enumeration** of the family it ranges over. Until the diamond-family manifest and the clause-(vi)
consumer-arrow manifest exist, the amendment's extent is undetermined even though its content is not.

[PROVABLE] **Not manifest-pending, and must not be conflated with it: the inhabitance clause.**
Whether `A_RP^+` is nonempty at all awaits a **seed theorem** on `(MT-4)`, a different object from
the manifests. An amendment that flagged only the manifest dependency would leave the reader believing
the support is inhabited and merely uncounted. It is neither.

## 4. F4 — the pattern audit

### 4.1 The count is wrong before the question can be asked

[PROVABLE] The six are not six of a kind, and three of them are not instances at all:

| # | Instance | What the record shows | Is it a support-scoping of a demand of record? |
|---|---|---|---|
| 1 | **J4** | functional J4 **refuted** on cycle creation; replaced by the relation/increment groupoid (A3, principal ruling) | **No — a RETYPING** (function → relation). A genuine amendment, different species. |
| 2 | **J15** | "the ratified J15 text is ALREADY CONTRAVARIANT … the builds attempted the wrong direction; `J15_AMENDMENT_REQUIRED = false`" | **No — a builder direction error.** The ratified row never changed. Not an instance. |
| 3 | **J2** | full → scoped old-image (A6, principal ruling) | **Yes** |
| 4 | **within-cycle locality** | "the demand came from the Q-466 **spec**"; "within-cycle point locality **NOT RATIFIED** and geometrically false" | **No — spec overreach**, never ratified, and refuted geometrically. Not an instance. |
| 5 | **FC6** | the qualifier "on their proved finite scopes" is **authored into the ratified FC6 text**; the guard's own exhibition graded it "PASS ON PROVED SCOPES" | **Reading, not amendment.** No demand of record was narrowed. |
| 6 | **J7** | support-scoping **proposed**, adjudication pending | **Yes, prospectively** |

[PROVABLE] So: **three genuine amendments of ratified demands (J4, J2, J7-pending), of which two are
support-scopings; one reading (FC6); two corrections of builder or spec overreach (J15, within-cycle
locality).** This matters for how the principal should read the pattern. "The sixth time we have had
to weaken a ratified demand" would be alarming and would suggest the law is eroding. The record does
not say that. It says the ratified law has mostly held, and that most of what looked like erosion was
builds and specs over-reaching past it.

### 4.2 Is there one principle? Yes — as mathematical content

[YOURS] All six do share one content, and it is worth carrying explicitly:

> **A demanded law carries an intensional address — the structural property of the record at which
> its ingredients are formed — and at positions outside that address the demand is *unformable*
> rather than false; every such position must carry a typed status naming what is absent and what
> would supply it, never a vacuous pass.**

[YOURS] Three riders, normative rather than commentary, without which the principle is a licence:

1. **The address must be intensional and upstream.** It is stated by a structural property, and the
   predicate defining it must be fixed strictly before the objects it scopes. (This is what
   distinguishes J7's `m_T` address from the F_PLDEC circularity class.)
2. **The address is of the statement, not of the admitted family.** It may never narrow `Obj(I_F)` or
   `Mor(I_F)`. Scoping a law's internal quantifier and shrinking the index category are different
   acts, and the anti-shrink clause (vi) governs the second absolutely.
3. **Every address carries an extensional obligation.** It must be tested against the actual manifest,
   and where the manifest does not exist that is a recorded open obligation, not a discharge.

### 4.3 But not one authority — and this is the load-bearing half

[PROVABLE] The six differ in **who may declare the address**, and a principle silent on that is the
self-service escape:

- **ratified-row re-address** (J4, J2, J7-pending) — principal ruling, full gate;
- **already-qualified guard row** (FC6) — no ruling needed; the qualifier is in the ratified text;
- **strike / no-op** (J15, within-cycle locality) — nothing of record was ever demanded; a lane may
  record this without a ruling, but may not call it an amendment.

[YOURS] Written as one undifferentiated principle, tier two becomes a doorway: any lane could declare
its row "already qualified" and self-authorize. **The DoR-020-A2 text should carry the principle
together with the three-tier authority rule and the three riders, or not carry it at all.**

### 4.4 What the principle forbids

[YOURS] Each already witnessed in this corpus, so none is decorative:

1. Declaring an address to escape a demand the record does form (the clause-(vi)/Q-451 scope error).
2. Reading an out-of-support position as vacuously satisfied (the census's refusal).
3. Substituting a weaker in-support comparison for the demanded one and calling the demand met (my
   D3 defect — J7 discharged by J2's equation).
4. Defining the address predicate from the object being addressed (the F_PLDEC circularity class).
5. Reporting a status verb stronger than the chain carries (the systematic optimism my D6 audit
   found).

## 5. F5 — verb audit on my own board

| My board line | Strongest verb my sources carry | Honest? |
|---|---|---|
| `F1(a) = DEFECTIVE` | census `(MT-3)`–`(MT-5)`, `(CONS-2)`, line 612 for inhabitance; build line 704 and the absence of any `tau` source | **CLEAN** — and I credit the confirmed fixed-stage algebra rather than reporting a bare defect |
| `F1(b) = KILL` | projector V003 line 126 (`ρ_f d`, `ρ_f δ` only); my own corpus-wide search returning no `(F3)` source; Q-468 | **CLEAN** |
| `F1(c) = PASS` | V003 lines 155–158 traced as the string's origin | **CLEAN** — I record it as a pass despite having been the one who raised the defect |
| `O1 = GENUINE` | census `(MT-4)`; my own `H_X={0}` derivation, independently recorded by the census | **CLEAN**, with the two branches distinguished rather than merged |
| `O2 = GENUINE` | `ReaderBottom_N` as the algebraic reader's shadow; the F_PLDEC closure | **CLEAN** |
| `J7_AMENDMENT = candidate stated` | full gate supplied; explicitly **not adopted** | **CLEAN** |
| `SIX_AS_ONE_PRINCIPLE` | the six instances traced individually to primary text | **CLEAN** — I answer "yes as content, no as authority" rather than the flat yes the framing invited |

[PROVABLE] **Two corrections to my own prior artifacts, disclosed rather than buried:**

1. **My FC6 determination (§3.2) flagged a caveat that FC6 "is silent" on its arrow quantifier and
   that a ruling from the axiom's author arm was needed.** That was over-cautious. FC6's ratified text
   reads "…hold **on their proved finite scopes**; disjoint new-cycle sectors are not mixed", and the
   axiom repeats it corpus-wide at §1.2 ("The current finite stock supplies the kinds in FC1–FC13 on
   its proved finite/`Ref_path`/flip scopes"). The qualifier is authored into the ratified row. FC6's
   arrow quantifier was never narrowed by any lane, so there is nothing to rule on. This **strengthens**
   `GUARD_ROW = scope_qualified`; it does not change it.
2. **In this task I initially read J2-build line 69's placement under a "rank-preserving" bullet as a
   misattribution by the build.** Tracing the string to its origin at projector V003 line 156 refutes
   that: the string is bottom-valued at its source, and the build's row 10 is correct. The J2 build's
   placement was the error.

```text
VERB_AUDIT_SELF = CLEAN (+2 disclosed corrections, both to my own work)
```

## 6. Fence and stopping board

```text
COEFFICIENT_FACE_FIXED_STAGE = CONFIRMED
COEFFICIENT_FACE_DIAMOND_ROUTES = NOT ESTABLISHED (+tau_r sourceless; +mu equalities undisplayed)
A_RP_PLUS_INHABITANCE = NOT ESTABLISHED (+no seed theorem on (MT-4))
J2_RP_RECONSTRUCTION = DISPLAYED MODULO (F1),(F3) -- both asserted as sealed, neither supplied
PI_MX_LOC_TYPING = DEFINITIVE (confirmed; the double-Loc defect is retired)
O1 = GENUINE (two branches: forced at acyclic; undetermined at cyclic-vanishing-pairing)
O2 = GENUINE (the one scalar bottom route is forbidden, not merely absent)
COMPLETION_THEOREM_V2_J7_HYPOTHESIS = undischarged on every actual cycle-creating diamond
MEMBERSHIP_THEOREM = still DEFECTIVE; D3 stands
AMENDMENT = candidate stated, full gate, NOT ADOPTED
MACHINERY_APPEAL = false
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

No register, plan, tracker, git, commit, push, member-binding, fixed-point, end-test, or evaluation
action was performed by this lane. No law was adopted.

REPAIRS = DEFECTIVE (+F1(a) coefficient face: scope `A_RP^+` not shown inhabited, `tau_r` sourceless
and the four-position diamond routes unestablished; +F1(b) displayed J2: `(F3)` `Loc_M eta_f^K =
j_f^C Loc_N` and `(F1)` `Q_M j_f^E = j_f^prof Q_N` asserted as sealed supply and unsupplied anywhere
in the corpus — the debt was relocated, not discharged; F1(c) typing CONFIRMED)
O1_O2 = GENUINE (+no route to either; +O1 is a forcing, not an absence, on the acyclic branch, and
carries a second undetermined branch; +O2's only scalar bottom coordinate is the algebraic reader's
shadow, so the route is forbidden by the F_PLDEC closure rather than merely missing)
J7_AMENDMENT = candidate_stated (+intensional support: two-sorted — `(S-STAGE)` `m_T,X != 0`,
constant on connected components of `A_RP`; `(S-FACE)` both ends in `(S-STAGE)` and the leg in
`A_RP`; plus a third value `(S-UNCL)` for unclassified consumer arrows; residues typed NOT FORMED /
UNDETERMINED / weaker-labelled-comparison-plus-post-scope / post-scope, never vacuous; global
cardinality clause awaits the manifests, inhabitance awaits a seed theorem)
SIX_AS_ONE_PRINCIPLE = yes/no (+yes as mathematical content — a demanded law carries an intensional
address, and outside it the demand is unformable rather than false, every such position carrying a
typed status; +no as one authority — only three of the six are amendments of a demand of record, J15
and within-cycle locality are builder and spec overreach and FC6 is a reading, so the axiom text must
carry the three-tier authority rule and the three riders or not carry the principle at all)
VERB_AUDIT_SELF = CLEAN (+2 disclosed corrections, both to my own work)
