CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
CLOSURE_END_BYTE = XXXXXXXX   (computed on bytes as a fixed point at seal time)
VERDICT_BEARING_SET = exactly the 22 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member carries its FULL path from the alpha-program-archive root and is rehashed
  at that path before sealing. EVERY PINNED DIGEST AND EVERY PINNED SPAN IN THIS ARTIFACT WAS
  COMPUTED AT ITS PATH FROM ITS OWN BYTES, never completed from a display and never copied from
  another artifact's table — including the four spans this artifact shares with member 04.
ANCHOR_RULE = CLOSURE_MEMBER_CITATION_RULE_V001; positional member numbers resolve or the seal
  refuses (generator R5). No flag name is an anchor.
LANE = DARIO   ROLE_THIS_RELAY = MEASURE CANDIDATE V002 — THE DESCENT (relay 1079)
AUTHORITY = member 02, the MEASURE SUPPLY MANDATE, verified LIVE at seal time.
SUPERSESSION = APPEND-ONLY. Member 03 (V001) is BYTE-UNTOUCHED; its digest is checked before and
  after this relay and the generator refuses on any drift. V002 does not edit V001; it withdraws
  V001's ledger claim in its own words and supplies what V001 lacked.
SELF_CITATION_BAR = DARIO's own outputs are NOT record witnesses. Members 03, 05, 11 are SUBJECTS;
  member 16 is my own demand map and is used for certificate GRAMMAR only, never as a forcing
  ground. The generator refuses if any FORCED row cites a DARIO-authored member.
BLIND EXPOSURE = MAXIMAL. kappa_record, alpha and the battery are NAMED AS POINTERS at section 8.3
  and were NOT opened, consulted, imagined, or optimized toward at any point.
NO_NUMERIC_PATH = this artifact and its generator contain no numeric evaluation of any physical
  quantity, no sampling, and no tolerance. Member 04 ruled V001's Monte-Carlo a fence defect; the
  ruling is accepted, the path is deleted rather than tightened, and the generator scans ITS OWN
  source to enforce the deletion.
ALL_RESULTS = CLAIMED until the opposite-lane check.
LIVE_MEMBERS = NONE.
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1079_MEASURE_CANDIDATE_V002_DARIO_V001.md` | `13cae6cbf9a190dd8aeb1774a04bf573d3fb3a92a25aebf044f78a857bf01bdd` | assignment |
| 02 | `workspace/MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `b9716661d1a03a16ec7fd06c836de2f74dbae0acafa0564c9241f96e791b420c` | **THE AUTHORITY — verified live** |
| 03 | `workspace/STAGE8_MEASURE_CANDIDATE_PROPOSAL_DARIO_V001.md` | `c64c44fbc07248038d6005280dfc128e9faf60e985fc9a0c7cc7817438d0870f` | **V001 — SUBJECT, byte-untouched, superseded append-only** |
| 04 | `workspace/STAGE8_MEASURE_CANDIDATE_CHECK_CODEX2_V001.md` | `b65cd05f98687f1c9c52ad84e353d2f488f273bbe9c862c09f899cd839ebb5c1` | **THE CHECK — the void grounds this relay answers** |
| 05 | `workspace/STAGE8_DESC_MEASURE_HUNT_DARIO_V001.md` | `03dcaa17c78922f90bb905cd1ea7d9e9673514b74a229f0e6808a7a1dad1edd8` | the STILL-CONTINUUM result (SUBJECT) |
| 06 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | **`Lambda`, `X`, `Y`, `B iso C(Y)`, `State(B) <-> Prob(Y)`, the licensed standard theorems** |
| 07 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U1_DOR008_ASSEMBLY_DETERMINATION_V001.md` | `1b0e928c452c10a8be72be22ff81fd7677f5045d2ad8d398a9f7f7f57b9ab3b0` | `Theta_F` at its own source |
| 08 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | `i_R`, `i_B`, `res_B`, **SM-1..SM-8 — including SM-6** |
| 09 | `workspace/STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md` | `32725f12a119bbb456dbe80e2c304e9dfb839acc9a05fda3d9f64e7317158842` | the S1 receiver typing |
| 10 | `workspace/ATTACH_ENTRY_ADDENDUM_V001.md` | `420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73` | the Attach entry of record |
| 11 | `workspace/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md` | `ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc` | the entered Attach candidate (SUBJECT) |
| 12 | `workspace/STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | **THE RATIFIED LAW — `U_N`, the cell action, `Z_N`** |
| 13 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | **THE FROZEN TRACE — the tuple and the three maps. READ ONLY WITH 14 AND 15.** |
| 14 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | the read rule governing 13 |
| 15 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | the family-level ratification; **the no-member clause** |
| 16 | `workspace/STAGE8_DESC_DEMAND_DARIO_V008.md` | `968aad4fb387f8887959bd3f40888233b08b96bcd42d9ac8281556470afa0d54` | certificate GRAMMAR only (mine — never a forcing ground) |
| 17 | `workspace/R3_4_OUTGOING_RECORD_GNS_COMPLETION_RESULT_V001.md` | `10909b5c21e73ecf655462339a27bd645b8d35e3ad11fb6f8cb204c601992995` | **`omega_out` — the outgoing-record state, and how it is produced** |
| 18 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U3_DOR008_DOR009_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `b0118d89e0b4b321fbf9caab5bfb811a0b5fd572e808d02615ee8503db2bc1ac` | `q_N`, the relative-history coordinate |
| 19 | `relay_inbox/RELAY_PASTE_1049_FRESH_SESSION_BOOTSTRAP_DARIO_V007.md` | `8c6cd83fac688dbdb3398b2c7abe3b044149a1a7ba6a6e102da7355381742084` | governing bootstrap |
| 20 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 21 | `workspace/ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213` | the Attach mandate — the precedent structure |
| 22 | `workspace/STAGE8_DESC_NECESSITY_PROBE_DARIO_V001.md` | `cc3ce8ca29736ede2b5d47e76bca34955c2da31c86147e8cd279fde4a0530577` | the required-side result (SUBJECT) |

```text
PINNED SPANS — each recomputed in this relay from the member's own bytes at its own path.
  member 12 [4243,5977)    ed170b5ff7d6dcf236db7ca39fffc239091a2377a02e5aaf66413b75072a60bf
                           the cell action, the controlled law, and equations (1)(2)(3)
  member 12 [9282,9619)    1e28b852b5cc47c728da171578c4e0d1c849c73304d565f131dec0baaf909946
                           the equal-history reduced source channel
  member 13 [22959,24536)  3e0b43706ef6e3297fa099f53702e658a2d41f73c33a425ac29ed2a23aef3d43
                           the primitive tuple, the three maps, and the frozen construction order
  member 08 [17865,20008)  aa04e0ea2c901d0a33312a23d6ade023f8c1e92f2e90d1ff6b447f4e98cea171
                           SM-1..SM-8 — REPLAYED INDEPENDENTLY, matching member 04's own pin
  member 02 [1470,2433)    758023b00d6bfebf0d518036370dd625a26d206acd1c17813b8e0eac576c4c97
                           the object to be supplied: measure WITH descent map and certificate
  member 17 [698,1630)     2a99a925eb1288a8b0aa074963b2beeb4a082cb65d109e6821765a44a4f6c383
                           omega_out, produced as a pullback through the frozen evolution
  member 15 [355,992)      bc83e53db7e46f27b570b1492ebd9ea53f168c344e1b06d1b82b45bb36a00750
                           the family-level adoption and the no-member clause
  member 16 [12644,13333)  a3e76adea4f095c33af5f5afb67d49021b65ed1ffd907833da96e1436c829c6c
                           the compositional/provenance bar (grammar only)

ESTABLISHED IN THIS RELAY — ALGEBRAICALLY, WITH NOTHING EVALUATED:
  the PHASE-ONLY LEMMA and the DESCENT DICHOTOMY of section 3, derived from the pinned spans above
  by exact algebra. No number was computed, no sample drawn, no tolerance accepted.

BLIND HELD. EVERY SCALE SYMBOLIC. NO NUMERIC EVALUATION. NO MEMBER BOUND.
NO FIXED-POINT EXECUTION. NO END TEST. NO MEASURED-CONSTANT COMPARISON.
NO FREEZE BEYOND THE AUTHORED MEASURE AND THE AUTHORED GENERATION RULE, BOTH DISCLOSED AT SECTION 7.
PE-1..17 POINTER-ONLY, LEDGER SHUT. CASCADE-TERRITORY STOP FORM LIVE.
alpha_computed = false   proof_authorized = false   kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

CLOSURE_DECLARATION_END

# PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL ENTRY (MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001, b9716661d1a0…)

# STAGE 8 — THE MEASURE CANDIDATE, V002
## RELAY 1079 — `[PLAN:DESC-37]` — DARIO LANE — THE DESCENT — [CLAIMED]

Date: 2026-08-12

**WHAT THIS VERSION DOES.** Member 04 voided V001 for a package defect, not a measure defect: the
measure is lawful, the declared descent is not complete. V002 carries the measure untouched and
answers the descent. It reaches a **STOP**, and the stop is the result:

```text
A LAWFUL NON-PRODUCT COMMON-ORIGIN DESCENT DOES NOT EXIST ON SEALED GROUNDS.
Not "was not found" — cannot exist, for a reason that is proved from the ratified primitives
and priced exactly at section 5.
```

**AND THE PRODUCT IS NO LONGER A CHOICE.** Member 04's ground for the void was that
`Omega_SR tensor mu` chooses the product over correlated joint extensions and that the choice went
unledgered. Section 3 shows the correlated extensions **are not there to be chosen against**: the
ratified law couples the history into the record only through a phase on the source sector
coherence, and the ratified source state is the one state that has none. The product is what the
frozen trace produces. That repairs the omission by derivation rather than by relabelling — and it
costs a different, real authored row, declared at section 7.

---

## 1. WHAT MEMBER 04 SETTLED, CARRIED AS ITS FINDINGS

Member 04 is the opposite lane. Its findings are carried here as **its** rulings; none is
re-adjudicated in my favour, and the two that hurt are carried first.

```text
CARRIED AGAINST ME
  1  LEDGER 9F/1A is REFUTED.  The product coupling was a second authored choice, carried in the
     FORCED column by omission.  ** I WITHDRAW THE 9F/1A LEDGER. **
  2  The closed claim had an INGREDIENT ESCAPE.  My extractor's identifier class never reaches
     punctuation, and the load-bearing product operation WAS punctuation.  11/5/0 was correct
     code-versus-code and could not support the claim I made with it.
  3  The generator's Monte-Carlo sampled and accepted a tolerance.  It conflicts with the
     no-numeric-evaluation fence and must not be represented as an exact-algebra receipt.
  4  "Three self-caught executions" is NOT_OF_CLOSED_RECORD — a run history is not a sealed object.
     V002 therefore claims nothing about its own runs; the refusal paths are in the generator's
     bytes, and that is all that is claimed.

CARRIED IN MY FAVOUR — and it is the one I attacked hardest in V001
  5  mu(Z_N) = 0 is CONFIRMED-CONSISTENT.  Member 04 established it by exact character
     orthogonality without executing my sampler, found no sealed demand that the expectation be
     nonzero, and ruled the charge/flux condition an operator/history-variation condition rather
     than a state-expectation constraint.  ** The worry is CLEARED, and I do not re-open it. **
  6  SM-6 is MUST-BUY: disclosure is honest, omission is not lawful.
  7  Selection disclosure is COMPLETE; K4 shape MEETS at receiver/type only, with no K4 verdict.
```

**Point 3 is accepted by deletion, not by tightening.** V002 computes nothing. The lemma and the
theorem of section 3 are exact algebra over the sealed operators, and the generator scans its own
source for a numeric path and refuses if it finds one.

---

## 2. THE MEASURE, CARRIED UNCHANGED

```text
mu := Haar_Y  on  Y = X_+ x X_-,  the translation-invariant probability measure of the compact
abelian group Y.  In the record's own notation this is the state member 06 :216-233 names,
omega_H := tau_0,+ tensor tau_0,-.

A1  mu is invariant under the FULL translation group of Y, not merely under Theta_F.
```

Given A1, `mu` is fixed by uniqueness of Haar measure: no distinguished point, no free parameter.
`Theta_F`-invariance is **delivered rather than asserted** — `Theta_F` acts on `Y` by branch
exchange composed with inversion, both group **automorphisms**, so the pushforward of Haar is
normalized and translation-invariant and is Haar again by **uniqueness**. Member 04 confirmed this
argument independently and added that product Haar on `X_+ x X_-` is the unique Haar measure of the
product, so no second measure parameter enters.

**THE SELECTION STAYS DISCLOSED.** Member 06 `:520-524` forecloses exactly this move: *"Mere
preference for Haar, trivial-character evaluation, a product state, or any convex mixture does not
satisfy that condition."* **A1 is such a preference.** It is lawful only because member 02 mandates
the supply. `SELECTION_DISCLOSED = YES`.

---

## 3. THE DESCENT DICHOTOMY

This section is the relay. It is exact algebra over sealed operators; every premise is pinned.

### 3.1 The frozen trace (member 13 `[22959,24536)`, read with members 14 and 15)

For every finite `N` the primitive tuple `Omega_prim,N` is frozen before output, and three maps
issue from it: `d_state` is the unique normalized fixed state of `P_src`, which the anchor theorem
then **proves** equal to `I_src/Tr_A(I_src)`; `d_ready` is the ready record ray; `d_law` is the
ratified transition. The construction order `T0..T8` fixes the tuple and the certificate before any
output is exposed. **This is the "one frozen construction trace" SM-6 names**, and member 15
`[355,992)` ratifies it AT FAMILY LEVEL with the no-member clause carried.

### 3.2 The law and its action on the ready record (member 12 `[4243,5977)`)

```text
S = [[0,1,0],[1,0,0],[0,0,-1]]  on the one-cell basis (|r>,|p_Q>,|e_Q>)
D_(n,j)[a_j] = diag(1, z_j^(n)[a_j], 1)          each z of unit modulus
W_(1,j)^(n)[a_j] = D_(n,j)[a_j] S_j
W_N^(n)[a] = tensor_j W_(1,j)^(n)[a_j]
U_N^(n)[a] = P_0 tensor I_(3^N) + P_ch tensor W_N^(n)[a]       the ratified controlled transition
|R_N> = |r>^(tensor N)         |P_N> = |p_Q>^(tensor N)
```

and, at the same span, equation (2):

```text
W_N^(n)[a] |R_N> = (product_j z_j^(n)[a_j]) |P_N>.
```

**PHASE-ONLY LEMMA.** *The history enters the record factor only as a unit-modulus scalar.* Equation
(2) is the case that matters, and it extends to every word in the law's unitaries: `S` exchanges
`|r>` and `|p_Q>`, and `D` is diagonal on that pair, so any product of the `U_N[a]` and their
adjoints carries `|R_N>` to a unit-modulus phase times `|R_N>` or `|P_N>`, with the whole history
dependence in the phase. **Nothing in this paragraph is mine except the extension of a sealed
identity to words in the same operators; the identity is the record's.**

### 3.3 The one state that cannot record a phase (member 13 `[22959,24536)`)

`d_state = I_src/Tr_A(I_src)` is the normalized identity, so for the sector projectors of member 12
`[4243,5977)`,

```text
P_0 d_state P_ch = P_0 P_ch / Tr_A(I_src) = 0.
```

**The ratified source state has no coherence between the neutral and charged sectors.** Member 12
`[9282,9619)` states the same fact from the channel side: the equal-history reduced source channel
is `Phi_N(rho) = P_0 rho P_0 + P_ch rho P_ch`, the sector-diagonal map.

### 3.4 THEOREM — the frozen trace generates no history dependence

Let `rho_prim = d_state tensor |R_N><R_N|` and let `V` be any word in the law's unitaries. Writing
`Phi` for the unit-modulus phase of the lemma,

```text
V rho_prim V^dagger
  = (P_0 d_state P_0) tensor |R><R|
  + conjugate(Phi) (P_0 d_state P_ch) tensor |R><P|
  + Phi (P_ch d_state P_0) tensor |P><R|
  + (P_ch d_state P_ch) tensor |P><P|
```

with `|R>,|P>` the two record vectors the word selects. **The history dependence is confined to the
two sector-coherence blocks**, and by section 3.3 those blocks are zero. Hence

```text
V rho_prim V^dagger  is INDEPENDENT of the history.
```

**COROLLARY 1 — no correlated common-origin descent.** Elementary tensors are total in the minimal
tensor product (a licensed standard import, member 06 `:81-96`), so a state on
`A_C0 = A_SR tensor_min B` is determined by its values on `A tensor b`. If the joint state's
history dependence is generated by the law acting on the frozen input — the rule declared as **A2**
at section 7 — then by the theorem the source-record content is the same at every history, and

```text
Omega_C0 = omega_SR tensor mu.
```

**The product is not a choice among correlated extensions. Under the ratified primitives there are
no correlated extensions in this family to choose against.**

**COROLLARY 2 — the exact price of a correlation.** The theorem fails on exactly one hypothesis: a
source state with nonzero `P_0 . P_ch` coherence would record the phase and correlate. So a
non-product common-origin descent requires **replacing `d_state`** — and `d_state` is not a free
slot: the anchor theorem proves it, and member 15 `[355,992)` ratifies the family with
*** NO MEMBER IS SELECTED, EVER ***. Buying the correlation means unseating a ratified theorem, not
authoring an ingredient.

### 3.5 The other route, and why it is not a state

The two-branch object is the natural correlator: weight history pairs by the influence functional.

```text
Omega_Keldysh(A tensor f) "=" integral f . (the two-branch pairing of A between branch evolutions)
```

**It fails twice, and the first failure is generic.**

1. **POSITIVITY.** `B` is commutative and member 06 `:174-207` types its states as `Prob(Y)` —
   probability measures, positive. The influence functional is unit-modulus and complex; it is an
   **element of `B`**, an observable, not a density. Weighting by it does not produce a positive
   functional, for **any** measure. This is not a defect of Haar.
2. **NORMALIZATION, under Haar specifically.** The total mass of that weighting is the expectation
   member 04 computed exactly: it is zero. A functional of total mass zero is not a state.

**I flag failure 1 as the point most favourable to my own candidate and therefore the one to
distrust.** It says Haar is not specially at fault for the death of this route. If the opposite lane
can construct a positive two-branch weighting, Corollary 1's hypothesis narrows and this section
weakens — that is the cleanest single attack on V002 and I name it as such.

### 3.6 What the dichotomy says

```text
THE CORRELATED ROUTE IS NOT POSITIVE.        (3.5, generic)
THE POSITIVE ROUTE IS NOT CORRELATED.        (3.4, from the ratified primitives)
=> NO LAWFUL NON-PRODUCT COMMON-ORIGIN DESCENT EXISTS ON SEALED GROUNDS.
```

**And an observation about SM-6 that belongs to the record rather than to my candidate, offered for
attack.** SM-6 exists, at member 08 `[17865,20008)`, to distinguish a lawful common-origin producer
from *the always-available product extension*. On these grounds the distinction does not bite in the
direction it was written for: **the product is what the lawful common-origin producer produces.**
That is a finding about the sealed corpus, it is CLAIMED, and it is exactly the kind of finding an
opposite lane should try hardest to break. It does **not** excuse V001: V001 asserted the product
with no derivation, and an unsupported claim that later turns out to be derivable was still
unsupported when it was made.

---

## 4. SM-6, AND WHY NO AUTHORED MEASURE CAN MEET IT

Member 08 `[17865,20008)` states SM-6 as a conjunction: **carrier, law, record state, history state,
domains, and required effects descend from one frozen construction trace.**

```text
carrier          descends       member 13 tuple, member 12 carriers
law              descends       d_law, the ratified DoR-009 transition
record state     descends       member 17 [698,1630): omega_out IS the pullback of the incoming
                                state through the frozen evolution — the record marginal of the
                                post-law common-origin state, not an independently imported datum
domains/effects  descend        the tuple's declared finite domains
history state    DOES NOT       ** mu is authored under member 02 **
```

**That last row cannot be repaired by choosing a better measure.** The mandate exists because the
sealed corpus does not determine the history state; member 22 established the same object required;
so any measure supplied under member 02 is authored by construction, and an authored state does not
descend from a frozen trace. Member 16 `[12644,13333)` states the bar in the form that settles it:
*"unrelated artifacts do not become one derivational origin merely because they were assembled
before any output"* — assembly discipline is not provenance.

```text
SM-6 IS SATISFIABLE ONLY IN THE WORLD WHERE THE MEASURE IS DERIVED,
AND IN THAT WORLD THE MANDATE IS UNNECESSARY.
```

So the honest disposition is not "SM-6 failed and I disclosed it" — V001's move, correctly rejected
— but: **SM-6 is unmeetable by this instrument, and here is what meeting it costs.**

---

## 5. THE COST, DISPLAYED

```text
COST-1  A NON-PRODUCT DESCENT
        Requires a source state with P_0/P_ch coherence (section 3.4, Corollary 2).
        d_state is fixed by the anchor theorem and ratified at family level with the no-member
        clause (member 15 [355,992)).
        PRICE: a new decision of record replacing a ratified theorem's output.
        NOT PURCHASABLE BY AUTHORING. Not purchasable by this lane at all.

COST-2  SM-6's HISTORY-STATE CONJUNCT
        Requires a fourth generative map on the history factor -- a history-side channel with an
        anchor certificate of the ratified form (unitality and trace preservation, or detailed
        balance, or symmetry covariance) and a uniqueness theorem for its fixed state, i.e. an
        EXTENSION of the primitive tuple of member 13 [22959,24536).
        PRICE: new physics, plus principal ratification, since the tuple and the three maps are
        ratified objects and a fourth map extends them.
        CONSEQUENCE IF PAID: the history state becomes DERIVED, A1 is dissolved, and the measure
        mandate is spent on nothing. That is the outcome member 02 :47-49 carries open as
        future_derivation_can_select_the_measure = NO_VERDICT, and I record again that I would
        regard it as the best available outcome for this artifact.

COST-3  THE TWO PURCHASES I REFUSE
        (a) Author a source state with sector coherence so the correlation appears.
            REFUSED: it manufactures the descent by contradicting a ratified theorem, and it would
            be authorship aimed at satisfying a constraint rather than at describing the object.
        (b) Weight the joint state by the influence functional and call the result a state.
            REFUSED: it is not positive (section 3.5), and under Haar it has no mass to normalize.
```

**A note on COST-2 that cuts both ways, and I state the side that flatters me second.** If a
history-side anchor theorem of the ratified form existed, its fixed state would be the canonical
trace state of the history algebra — which is what member 06 `:216-233` names, and what A1 selects.
That is a **reason the authored clause has the shape a derivation might take**; it is **not**
evidence that the derivation holds, and it must not be read as support for the candidate. The
existence of the analogy is exactly what a derivation would have to establish rather than assume, and
I have not established it.

---

## 6. THE DESCENT AS IT STANDS

```text
Omega_C0 := omega_SR tensor mu          DERIVED under A2 (section 3.4), not declared
res_B(Omega_C0) = mu = omega_phys       SM-4, member 08 :332
Omega_C0 compose i_R = omega_out        SM-3, member 08 :331; member 17 [698,1630) supplies
                                        omega_out as the pullback of the incoming state through
                                        the frozen evolution, so the record marginal is the frozen
                                        trace's own output rather than an imported datum
Theta_hist := res_B                     lands in State(C(Y)) as a MEASURE, member 09 [5968,6080)
```

```text
SM-1 typed source/codomain      MET at type
SM-2 positivity/normalization   MET      mu in Prob(Y); omega_SR a state; the product of states
SM-3 record marginal            MET      member 04 confirmed the marginal identities for this object
SM-4 history binding            MET      by construction of the descent, not by later preference
SM-5 finite/quotient            MET at shape; the finite cylinder receivers and the relative
                                coordinate q_N of member 18 are unchanged by V002
SM-6 common provenance          ** NOT MET ** -- history-state conjunct only (section 4)
SM-7 target independence        MET      section 8.3; nothing downstream opened
SM-8 coverage                   STATED   the working class is the law-generated family of A2, with
                                its reopening trigger named at section 3.5
```

**One row of that table is different from every other and I will not let it read as routine.**
SM-6 is not "open pending work". It is closed against every instrument this lane has, and section 5
prices the two ways it could be opened, neither of which is mine to take.

### 6.1 The certificate, in the grammar V001 and the Attach candidate used

```text
MC1  OUTPUT-FREE PRIMITIVES        FILLED   A1 and A2 name only the group's own symmetry and the
                                            record's own law.  No downstream object appears in
                                            either, and section 8.3 certifies none was opened.
MC2  ENTRY BEFORE OUTPUT           FILLED   both authored clauses frozen at this artifact's seal.
MC3  EXECUTABLE DESCENT            FILLED   section 6: the map is stated, and section 3.4 derives
                                            its form instead of declaring it.
MC4  SEALED-INTERFACE REPRODUCTION FILLED   mu lies inside the admissible set member 05 censuses;
                                            section 8 walks V1-V8 and contradicts nothing sealed.
MC5  SAME-SOURCE SUPPLY            OPEN     ** and now for a proved reason rather than a disclosed
                                            one. **  The history state is authored and cannot
                                            descend from the frozen trace (section 4); member 16
                                            [12644,13333) supplies the bar that settles it.  The
                                            slot does not close by better assembly.
MC6  K4 COHERENCE                  SHAPE    omega_hist := res_B(Omega_C0) = mu, an element of
                                            State(C(Y)).  Receiver and type only -- member 04's
                                            ruling, carried: this is not a K4 verdict.
```

---

## 7. THE LEDGER, RESTATED

### 7.1 FORCED — with spans

| # | Ingredient | Span |
|---:|---|---|
| F1 | the carrier `Lambda`, `X`, `Y`, `B iso C(Y)`, `Y` compact | member 06 `:141-172` |
| F2 | `State(B) <-> Prob(Y)`, positivity and normalization | member 06 `:174-207`, `:450-458` |
| F3 | U1-reality, `Theta_F` and the compatibility condition | member 07; member 06 `:259-276` |
| F4 | `Y` compact abelian; the standard duality/measure/tensor theorems are LICENSED IMPORTS | member 06 `:81-96` |
| F5 | uniqueness of Haar on a compact group (licensed under F4) | member 06 `:81-96` |
| F6 | the influence functional's exact form, equation (3) | member 12 `[4243,5977)` |
| F7 | `res_B(Omega) := Omega compose i_B`; `A_C0 = A_SR tensor_min B`; `i_R` | member 08 `:126-128`, `:213-218` |
| F8 | SM-3 and SM-4's marginal conditions | member 08 `[17865,20008)` |
| F9 | the S1 receiver typing | member 09 `[5968,6080)` |
| F10 | **the frozen construction trace**: the primitive tuple, `d_state`, `d_ready`, `d_law`, `T0..T8` | member 13 `[22959,24536)`, under member 15 `[355,992)` |
| F11 | **the ratified controlled law and its phase-only action on the ready record**, equations (1)(2) | member 12 `[4243,5977)` |
| F12 | **`d_state` is the normalized identity, hence has no sector coherence**; the sector-diagonal reduced channel corroborates | member 13 `[22959,24536)`; member 12 `[9282,9619)` |
| F13 | SM-6's exact text and its receiver | member 08 `[17865,20008)` |
| F14 | `omega_out` produced as the pullback of the incoming state through the frozen evolution | member 17 `[698,1630)` |
| F15 | elementary tensors are total in the minimal tensor product (licensed under F4) | member 06 `:81-96` |

**15 FORCED.** No row cites a DARIO-authored member; the generator refuses if one does.

### 7.2 AUTHORED — the core, now two clauses

| # | Ingredient | Justification (internal coherence ONLY) |
|---:|---|---|
| **A1** | **`mu` is invariant under the full translation group of `Y`** | The selection principle that introduces no distinguished point and no free parameter, and that implies F3's `Theta_F`-invariance rather than merely coexisting with it. Given A1, F5 fixes `mu`. Justified by parameter-freeness and by nothing downstream. |
| **A2** | **the joint state's history dependence, if any, is generated by the ratified law acting on the frozen input — and by nothing else in the tuple** | The tuple of F10 has exactly one history-indexed primitive, the characters that enter `d_law`; so the law is the only ingredient that could carry a history into the joint state. That NARROWS the rule to near-uniqueness. **Narrowed is not forced, so it is AUTHORED**, per member 02's when-in-doubt clause. Under A2, section 3.4 makes the product a theorem. |

**2 AUTHORED. LEDGER = 15-FORCED / 2-AUTHORED.**

### 7.3 THE RELOCATION, DECLARED RATHER THAN SLIPPED

Member 04's second authored row was *"choose the product coupling as `Omega_C0`"*. **I do not carry
that row, and the reason must be on the record because it is the one place V002 could be reading its
own findings too kindly.**

```text
member 04's A2   the product coupling is chosen
my A2            the generation rule is chosen; THE PRODUCT THEN FOLLOWS (section 3.4)
```

Carrying member 04's row verbatim would misdescribe the object — after section 3.4 the product is
not a choice made beside the rule, it is the rule's consequence. But the relocation is mine, it is
contestable, and it is load-bearing for a count. **So the fallback is stated in advance: if the
opposite lane rules the relocation illegitimate, the ledger is `15-FORCED / 3-AUTHORED` with both
rows carried, and nothing else in this artifact changes.** I would rather publish the fallback than
have the count depend on my own framing surviving review.

### 7.4 THE CLOSED CLAIM, WITH THE ESCAPE SHUT

Member 04 `[12234,13090)` located the escape exactly: the identifier class
`[A-Za-z][A-Za-z0-9_]*` never extracts an operator, and the load-bearing product operation was an
operator. Mapping the operands to a forced row therefore classified everything except the thing that
needed classifying.

**The repair is a property of the construction block, not a promise about the regex.** The block
below contains no character outside `[A-Za-z0-9_]` and whitespace. Every non-whitespace character
therefore lies inside an identifier token, so the extractor is **provably total** — and the generator
refuses if a single character violates that class.

**And the three products are now three tokens.** V001 wrote one tensor sign doing three different
jobs with three different provenances. Each now carries its own ledger row, and the descent's product
carries the AUTHORED one.

```text
CONSTRUCTION_BEGIN
mu Haar_Y
Omega_prim d_state tensor_prim d_ready
d_state normalized_identity
coherence_P0_Pch zero_coherence
d_law_a P_0 tensor_law Id plus P_ch tensor_law W_N_a
W_N_a_R_N phase_a times P_N
omega_law_a Omega_prim compose Ad_d_law_a
omega_law_a history_independent
omega_SR omega_law_a
Omega_C0 omega_SR tensor_C0 mu
res_B_Omega_C0 mu
Theta_hist res_B
keldysh_density Z_N times mu
keldysh_density not_positive
mu_Z_N zero_expectation
CONSTRUCTION_END
```

```text
SYMBOL_TABLE_MAP_BEGIN
mu A1
Haar_Y F5
Omega_prim F10
d_state F12
d_ready F10
tensor_prim F10
normalized_identity F12
coherence_P0_Pch F12
zero_coherence F12
history_independent F12
d_law_a F11
P_0 F11
P_ch F11
Id F11
plus F11
tensor_law F11
W_N_a F11
W_N_a_R_N F11
phase_a F11
times F11
P_N F11
omega_law_a A2
Ad_d_law_a A2
tensor_C0 A2
compose F7
omega_SR F14
Omega_C0 F7
res_B_Omega_C0 F7
res_B F7
Theta_hist F9
keldysh_density F6
Z_N F6
mu_Z_N F6
zero_expectation F6
not_positive F2
SYMBOL_TABLE_MAP_END
```

```text
CLOSED_VOCABULARY_BEGIN
NONE
CLOSED_VOCABULARY_END
```

The vocabulary is the sentinel, read by the generator as the empty set: **coverage rests entirely on
the map**, with no second table able to absorb an unmapped token, and no alias set anywhere — the
mechanism that broke relay 1061 is absent rather than disciplined.

---

## 8. FALSIFIER HOOKS

### 8.1 Void conditions V1-V8, carried with member 04's dispositions

| # | Condition | Disposition |
|---:|---|---|
| V1 | U1 reality | satisfied — automorphism pushforward plus Haar uniqueness |
| V2 | positivity and normalization | satisfied — Haar is a probability measure |
| V3 | one-cell restriction | satisfied — consumed only through `mu(1) = 1` |
| V4 | the influence expectation is real | satisfied exactly, at the value member 04 established |
| V5 | ratified algebra | satisfied — the state is on the existing `B iso C(Y)` |
| V6 | no origin-family member bound | satisfied — **and V002 touches this row harder than V001 did**: section 3 READS the ratified family's outputs and section 5 declines to alter them. No member is selected, ever. |
| V7 | record marginal unchanged | satisfied, and strengthened: member 17 `[698,1630)` shows the marginal is the frozen trace's own output |
| V8 | inside the admissible set | satisfied — one invariant probability measure in the continuum |

**V6 is the row to watch in V002.** COST-1 names replacing `d_state` as the price of a correlation,
and naming a price is not paying it. **Nothing here selects, replaces, or binds a member of the
origin family**, and the generator refuses if the artifact ever pairs the naming with an act.

### 8.2 The attacks I would run first, on my own artifact

```text
1  THE POSITIVITY ARGUMENT OF 3.5 IS THE LOAD-BEARING GENERIC CLAIM.  If a positive two-branch
   weighting exists, Corollary 1's family narrows and the dichotomy weakens.  This is the single
   best attack and it is the reason section 3.5 flags the argument as favourable to me.
2  A2 IS A QUANTIFIER.  The theorem covers conditionals generated by the law's unitaries.  A
   producer that uses the tuple differently -- through the channels rather than the unitaries,
   or through a purification -- is not covered, and I do not claim it is.  A2 is exactly the
   boundary of what section 3.4 proves, which is why it is AUTHORED and not asserted as forced.
3  THE RELOCATION AT 7.3 could be judged self-serving.  The fallback count is published so the
   judgement costs nothing to apply.
4  SM-6's OBSERVATION AT 3.6 is a claim about the sealed corpus made by the lane that benefits
   from it.  It should be tested by someone who does not.
```

### 8.3 Blind exposure — pointers, never opened

`kappa_record`, alpha and the battery are named here as pointers and **were not opened, consulted,
imagined, or optimized toward** at any point in this relay. The descent was shaped by SM-6 and the
common-origin bar only. **Nothing in section 3 was run**: there is no computation in this artifact
and none in its generator, so there was not even a number to be tempted by. Member 02's maximal
strength is the standard applied.

---

## 9. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the 22 closed members; the mandate and its entry law; member 04's seven carried findings; the
  carrier, state space and U1 structure; the ratified law and its equations; the frozen trace and
  its three maps under DoR-013's family-level ratification and no-member clause; SM-1..SM-8;
  omega_out's production; the certificate grammar; all charter fences.

DERIVED HERE:
  the phase-only lemma extended from equation (2) to words in the law's unitaries;
  the theorem that the frozen trace generates no history dependence;
  Corollary 1 (the unique common-origin descent in the law-generated family is the product) and
    Corollary 2 (a correlation requires replacing d_state);
  the dichotomy of section 3.6 and the SM-6 observation attached to it;
  the proof that SM-6's history-state conjunct is unmeetable by any authored measure;
  the exact cost display of section 5;
  the character-class totality property that closes the extractor escape.

AUTHORED HERE:
  A1, the measure principle (carried unchanged from V001).
  A2, the generation rule (new in V002, and the row member 04's finding is answered with).
  NOTHING ELSE.  No member of the origin family, no source state, no anchor, no channel, no
  certificate content, no scale, no selector, no entry.

NOT CONSUMED:
  kappa_record, alpha, any battery output, any downstream score, any measured datum, any member
  selection, any fixed point, any end test, any freeze, any entry addendum.
```

## 10. FLATTENING CHECK

- **A theorem's hypothesis was not flattened into its conclusion.** A2 is stated as the quantifier it
  is (section 8.2, item 2), and the theorem is not claimed for producers it does not cover.
- **"Derived under an authored rule" was not flattened into "forced".** The product is a theorem
  *given A2*, and A2 is in the AUTHORED column where the mandate puts a doubt.
- **A stop was not flattened into a failure, and not flattened into a success either.** Section 5
  prices what would open it; naming a price is not paying it.
- **Naming `d_state`'s replacement as a price was not flattened into selecting a member** (V6).
- **An opposite-lane confirmation was not flattened into my own result**: section 1 carries member
  04's findings as its rulings, including the two that hurt.
- **A relocation of an authored row was not flattened into its deletion** (section 7.3), and the
  fallback count is published rather than held.
- **A code-versus-code token count was not flattened into ingredient completeness** — that was member
  04's finding about V001, and the repair is a property of the character class, not a bigger regex.
- **A structural analogy was not flattened into evidence** (section 5, the note on COST-2).
- **An exact algebraic identity was not flattened into a computed result**: nothing was computed.

## 11. CUSTODY

- **BUILDER NEVER VERIFIES OWN WORK.** Everything above is CLAIMED until the Codex 2 check.
- **V001 IS BYTE-UNTOUCHED**, digest checked before and after; supersession is append-only.
- **No register, plan, tracker, or git action of any kind, read-only included.**
- PE-1..17 pointer-only; no member bound; **nothing entered**.
- Archive-side only: one artifact and one generator in `workspace/`, both sealed.

---

## 12. FINAL LINES

```text
CLOSURE = declared-first (byte 0; end byte XXXXXXXX computed as a fixed point on bytes; 22 members;
     0 live; 8 pinned spans recomputed from their own files; residue scan 0 hits from its own run)

MEASURE = HAAR-CARRIED (lawful, confirmed).  mu := Haar_Y, unchanged from V001, carried with member
     04's confirmations: full-translation invariance lawfully selects Haar; the Theta_F pushforward
     returns Haar by uniqueness; product Haar is the unique Haar measure of the product; and the
     zero expectation of the influence functional is CONFIRMED-CONSISTENT with every sealed
     constraint.  The worry V001 raised hardest against itself is CLEARED and is not reopened.

DESCENT = STOP (cost displayed).  A lawful non-product common-origin descent DOES NOT EXIST on
     sealed grounds, and the stop is proved rather than reported:
       THE CORRELATED ROUTE IS NOT POSITIVE -- the influence functional is a unit-modulus element
       of a commutative algebra, an observable and not a density, so weighting by it is not a state
       for ANY measure (and under Haar it also has no mass to normalize);
       THE POSITIVE ROUTE IS NOT CORRELATED -- the ratified law carries the history into the record
       only as a unit-modulus phase (member 12 [4243,5977), equation (2)), and the ratified
       d_state is the normalized identity, which has no sector coherence to record it (member 13
       [22959,24536)), so every law-generated conditional is history-independent.
     COROLLARY: Omega_C0 = omega_SR tensor mu is DERIVED under A2, not declared.  Member 04's void
     ground -- an unledgered choice of the product over correlated extensions -- is answered by
     showing there are no correlated extensions in that family to choose against.
     COST-1 a correlation requires replacing d_state, which the anchor theorem proves and member 15
     [355,992) ratifies with the no-member clause: a new decision of record, not an authored row.
     COST-2 SM-6's history-state conjunct requires a FOURTH generative map on the history factor
     with an anchor certificate and a uniqueness theorem -- new physics plus principal ratification
     -- and if it is ever paid the measure becomes DERIVED and this mandate is spent on nothing.
     COST-3 the two purchases I refuse, each named with its reason at section 5.

MC5 = OPEN (why, at span).  The history state is authored under member 02 and therefore cannot
     descend from the frozen trace; member 16 [12644,13333) supplies the bar -- assembly before
     output is not common derivational provenance.  The slot does not close by better assembly, and
     that is a proof rather than V001's disclosure.

LEDGER = 15-FORCED / 2-AUTHORED (both authored ingredients itemized at section 7.2).
     A1 the measure principle -- full translation invariance, carried unchanged.
     A2 the generation rule -- the joint state's history dependence is generated by the ratified law
        acting on the frozen input and by nothing else in the tuple; narrowed to near-uniqueness by
        the observation that the tuple has one history-indexed primitive, and AUTHORED anyway
        because narrowed is not forced.
     ** THE 9F/1A LEDGER OF V001 IS WITHDRAWN. **  Member 04 was right that a choice was carried in
     the FORCED column by omission.  V002 does not answer that by relabelling the same object: it
     supplies the derivation V001 lacked, and pays with a different authored row at the place where
     the authorship actually sits.  The relocation is declared at section 7.3 WITH ITS FALLBACK --
     if the opposite lane rules it illegitimate, the count is 15-FORCED / 3-AUTHORED and nothing
     else changes.

EXTRACTOR = PRODUCT-OP-COVERED.  The escape member 04 found at [12234,13090) is closed by a property
     rather than a promise: the construction block admits no character outside [A-Za-z0-9_] and
     whitespace, so every non-whitespace character lies in an identifier token and the extractor is
     provably total; the generator refuses on a single violating character.  The three products that
     V001 wrote with one sign now carry three tokens and three rows -- tensor_prim to F10 (the
     record's own input product), tensor_law to F11 (the law's controlled sum), and tensor_C0 to A2
     (the descent coupling, the one that was unledgered).

CLOSED_CLAIM = 35 symbols / 8 rows / 0 unmapped / vocabulary EMPTY (sentinel NONE)
     -- machine-checked before seal; the generator refuses if this published line and the figures it
     computes disagree, so the line is a receipt rather than a narration.

K4_SHAPE = MEETS.  omega_hist := res_B(Omega_C0) = mu, an element of State(C(Y)) -- receiver and
     type only, carrying member 04's ruling that this is not a K4 verdict.  Unchanged in object from
     V001; changed in provenance, since the descent producing it is now derived.

OUTPUT_INSPECTION = NONE-CERTIFIED, at the maximal strength member 02 :50-52 demands.  Nothing
     downstream was opened, and this relay computed nothing at all: NO_NUMERIC_PATH is enforced by
     the generator against its own source, which is the repair member 04's Monte-Carlo finding asks
     for -- deletion, not a tighter tolerance.

PROSE_DIGESTS = 22/22, STRICT==STABLE; 8 pinned spans recomputed at their paths, one of which
     REPLAYS member 04's own SM span digest independently.
CHAIN_INVOKED = true (chained pickup proceeds to RELAY_PASTE_1074_DIAGB_CHECK_V002_DARIO_V001.md,
     still the one outstanding DARIO relay)
VERB_AUDIT_SELF = CLEAN.  "Forces" is used only of Haar within the measure factor and of the theorem
     under its stated hypothesis; "derived" is used only where section 3 derives; "cannot exist" is
     restricted to the law-generated family named in A2; no physical result, member, correlation, or
     K4 comparison is claimed produced here.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

**THIS ARTIFACT IS PROPOSED_NOT_ADOPTED.** Per member 02 `:63-68`, ratification of content is a
second, separate principal act, and until an entry addendum names this artifact's digest and its
check's digest, **no construction may consume it as premise** — not the registrar, not a lane, not a
conversational summary.

All findings **CLAIMED** until the opposite-lane check.

**The honest shape of this relay.** V001 supplied a measure and declared a descent. Member 04 let the
measure stand and voided the declaration. V002 finds that the declaration was, as it happens,
derivable — and that the derivation closes the door it was asked to open: the product is forced by
the ratified primitives, a correlation would cost a ratified theorem, and SM-6's remaining conjunct
cannot be met by any authored measure at all. **That is a stop, and a stop is a full result.** What
it leaves standing is the same open slot member 02 carries: a target-independent derivation of the
history state would dissolve the authored core, satisfy SM-6, and make this artifact unnecessary. I
have said before that I would regard that as the best available outcome, and the work of this relay
has made it the only one that reaches SM-6 at all.
