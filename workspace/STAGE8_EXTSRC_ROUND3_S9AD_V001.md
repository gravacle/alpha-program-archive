# STAGE 8 — THE COMMON EXTSRC FIBER, ROUND 3: FIRST RUN UNDER FIXED READINGS — V001

## BLIND BUILDER — CODENAME EX3-BUILD — [SEALED]

Date: 2026-08-14 (session UTC 2026-08-15). Commission: OB-2's B-1 — the
abstract inhabitance of the COMMON ExtSrc fiber, the one decider both route
families share (`c8053f6c…` §6.2 B-1, read at bytes) — third round of the
ExtSrc timebox and the FIRST ever run with the semantic pins determined of
record: PIN-1 = FORCED(ACTUAL), PIN-2-HOL = FORCED(zero admitted), PIN-3 =
FORCED(MANDATORY) (`09047c2a…` + audit `abe71688…`, consumed at audited
grade); PIN-2-H FREE — every display below is checked under BOTH R-A and
R-B. New ground since rounds 1-2 (`075a2b16…`, `d662889b…`, both timeboxed
OPEN at their own (B3) lines): the B-3 displays (`98ee31ba…` + audit
`68202c7b…` CONFIRMED-WITH-CORRECTIONS) — exact stage census, max cycle
rank 2, both displayed relation classes pure-cycle, NG-2 holding with room.

Fences held throughout: `alpha_computed = false` ; `proof_authorized =
false` ; `kappa_record_computed = false`. DETERMINATION ONLY — abstract
inhabitance at the existence quantifier: no member bound, no adoption, no
authored physics, no route/branch/address/representative selected, no
score/gate moved; the principal's acts untouched. The held construction
`506bfe2a…` untouched, unopened, unpresupposed. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened; "Q-…" tokens
EXPECTED-UNLOCATABLE (cited only as carried inside sealed artifacts). No
git; no existing file edited; output + sidecar probed ABSENT at session
start and re-probed ABSENT at 2026-08-15T05:00:07Z immediately before this
write; ONE output file plus its seal sidecar. Bytes-level + exact symbolic
only: the battery ran in a FRESH venv (sympy 1.14.0; 27/27 checks PASS;
exact integers/rationals/symbols; no float anywhere; no value frozen).

**SWEEP CUTOFF: 2026-08-15T05:00Z** (SW-1..SW-6 below; anything sealed
after this cutoff is outside this artifact). Exhaustiveness is
sweep-displayed only; nothing absolute is claimed.

---

## 0. VERDICT (the honest menu)

```text
B-1 = UNDECIDABLE-STILL — but no longer for rounds 1-2's reason.

Rounds 1-2 stopped at a seven-gap TYPE-U board (common carrier; mixed
relations; topology certificates; addressed factor; transport bases).
Under the FIXED readings plus the exact B-3 census, this round DISCHARGES
or COLLAPSES every one of those gaps except ONE, in both directions:

  INHABITED — NOT reached. An explicit abstract candidate tuple (W-EX3,
    §4) is displayed for the first time, arm by arm, completing all four
    B-1 arms at every displayed carried stage — CONDITIONAL on exactly
    one sealed-but-undisplayed fact (the extent of the relation ideal
    ker(qtilde_N): the NO-MIXING bit, = B-3(ii)'s missing object) plus
    two newly named topology bits on the sealed carrier O_R5,N (TOP-1/
    TOP-2, §4.3-4.4) and one overlap bit (§4.5; PROVABLE if dim M_N >= 2,
    the import tagged). No sealed byte fixes the ideal's extent (SW-2,
    re-swept); so no witness is asserted on the sealed carriers.
  EMPTY — NOT reached. No joint incompatibility closes: the clause SET
    itself is jointly satisfiable (a complete two-stage model of every
    displayed B-1 clause shape was verified exactly, §5.4 — so EVERY
    emptiness route must consume a sealed-carrier fact, none of which is
    displayed). NEW SHARPENING NG-3': under forced PIN-1, a mixing
    relation with NONZERO cycle-side value ALSO kills both families at
    any carried stage with dim M_N >= 2 (§5.2) — K-b's kill surface
    WIDENS from "mixing with zero value" to "any mixing relation" on the
    dim>=2 cell. NG-3 (zero-value case) stands unchanged, caveat-free.

THE REDUCTION (the round-3 payoff, §6): at the displayed census, under
the fixed readings, modulo the named premises and tagged imports,

  dim M_N >= 2 at the anchor stages:  B-1 inhabited <=> NO mixing
    relation exists in the sealed ideal there (exact biconditional);
  dim M_N undisplayed (today):        mixing-with-zero-value => EMPTY;
    any-mixing at dim>=2 => EMPTY; no-mixing + no-overlap => INHABITED.

One finite future display — the basis/extent of ker(qtilde_N) at the four
anchor stages (the SAME missing object B-3(ii) already names) — now
decides B-1 essentially outright, where before it decided only NG-3.

SK-2 = NOT FIRED ; ACT-3 = NOT FORCED ; D-1 MENU = UNCHANGED. The member
proposal's target is still NOT grounded non-vacuous. EXTSRC TIMEBOX =
ROUND 3 OF 4 COMPLETE (the V002 checkpoint was honored: this round ran on
the principal's own commissioning, under fixed readings, not on lane
initiative). PIN-2-H: every display below is IDENTICAL under R-A and R-B
— B-1's four arms nowhere touch (JD-3)'s words (ROW-3 pattern; stated
per arm).
```

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), THIS SESSION, BEFORE RELIANCE

```text
COMMISSION STOCK (each read at the cited spans or in full)
c8053f6c  STAGE8_OB2_EMPTINESS_S9AD_V001.md              MATCH (read in full;
          §6.2 B-1 typing, §3.2 Class 1, §3.3 joint-fiber bar at bytes)
09047c2a  STAGE8_PIN_DETERMINATION_S9AD_V001.md          MATCH (read in full)
abe71688  STAGE8_PIN_DETERMINATION_S9AD_AUDIT_V001.md    MATCH (hash verified;
          consumed through the pair's displays)
98ee31ba  STAGE8_B3_DISPLAYS_S9AD_V001.md                MATCH (read in full)
68202c7b  STAGE8_B3_DISPLAYS_S9AD_AUDIT_V001.md          MATCH (verdict + flag
          block at bytes: CONFIRMED-WITH-CORRECTIONS; K-d noted)
075a2b16  STAGE8_TASK5_EXTSRC_BUILD_LANE3_V001.md        MATCH (read in full)
d662889b  STAGE8_TASK5_EXTSRC_BUILD_LANE3_V002.md        MATCH (read in full)
CHASED CARRIERS (verified before their bytes were used)
fdf20bd4  STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md     MATCH ((SPEC-11a/b/c),
          (S22)-(S25)/(S23-full-J7), (JE-0)/(JE-1a) two-base display at bytes)
5de94e16  STAGE8_TASK5_COMMON_SEAMS_LANE3_V001.md        MATCH (§3.1-3.2
          topology-boundary + (Q8)-(Q9) at bytes)
ccb94dfa  STAGE8_TASK5_R4_KERNEL_REALIZATION_...V001.md  MATCH (hash verified;
          (B2-x) content consumed at audited grade via OB-2/PIN/B-3)
d9f4aad0  STAGE8_OB2_EMPTINESS_S9AD_AUDIT_V001.md        MATCH (hash verified;
          consumed via its consumers' displays)
feb84216  STAGE8_TASK5_BANACH_PREMISE_DISCHARGE_LANE2_V001.md  SIDECAR MATCH
          (sweep-grade only: "named normed O_R5 class" is a REQUIREMENT row,
          not a sealed topology certificate — feeds TOP-1/TOP-2's openness)
MISMATCHES = 0. LOC (b53d9e93), PJ2 (3fd4b924), LOC-NAT (474bf721),
MANIFESTS, and the Xi/A9 stock were NOT re-walked; their clause bytes are
consumed at audited grade exactly as quoted inside the commission stock.
```

## 2. SWEEPS RUN (cutoff 2026-08-15T05:00Z; roots = workspace, supervision, cleanroom_output, relay_inbox, relay_outbox; fenced filename classes excluded)

```text
SW-1  "EXTSRC_ROUND3|ExtSrc round 3" -> zero hits. NO competing round-3
      artifact exists; the output path is first-use.
SW-2  "mixing relation|mixed relation" -> exactly the known lineage
      (LOC-NAT, ExtSrc V001/V002, MEMBERSHIP_THEOREM V002 [different
      family, disposed at OB-2 SW-4], LOC, JII, SEAM1, the OB-2/PIN/B-3
      S9AD set) + one supervision paste (RELAY_PASTE_174: dependency-graph
      cycle artefacts, a DIFFERENT object — disposed). NO new mixing-
      relation display landed since B-3's cutoff. B-3(ii) stands open.
SW-3  "dim M_N" -> only the S9AD four (OB-2, PIN, B-3 + audit). No sealed
      byte fixes any background dimension. B-3(iii)'s dim-M cell stands.
SW-4  "Hausdorff" -> 25 files; none is a definitional site for O_R5,N's
      topology. The one O_R5-topology byte found (SEAM1 §3.1, at bytes):
      "the physical O_R5,N topology" EXISTS of record; no sealed byte
      certifies it Hausdorff. TOP-1 is genuinely undisplayed.
SW-5  "locally convex" -> 15 files; the definitional hits are KER's
      D'-side displays and the continuum-package lineage; NONE certifies
      O_R5,N locally convex or dual-separating. TOP-2 undisplayed. The
      BANACH file's "named normed O_R5 class" row is a requirement table
      entry (final column NO), not a certificate.
SW-6  "O_R5,(N|w) :=" -> no sealed line fixes O_R5,N's topological type
      beyond SEAM1's naming. (Concurs with rounds 1-2, which typed the
      inherited topology as unproved rather than absent.)
DISCLOSURE: sweep listings displayed fenced-class BASENAMES among grep
hits; no fenced file's content was opened, read, or used.
```

---

## 3. THE QUESTION AT BYTES, AND WHAT CHANGED

### 3.1 B-1's four arms (OB-2 §6.2, verbatim scope)

```text
B-1: abstract inhabitance of ExtSrc =
  arm 1  S_N with ONE VALUE on the sealed relation ideal
         ((SPEC-11b): "Mixed relations in O_R5,N must receive one value");
  arm 2  the topology certificates (kappa_N continuity/embedding; D_N^Loc
         image/subspace topology equality; (A1-12)/(T2)/(T3)/(T7));
  arm 3  the addressed Res/Ker chain ((A1-15m)/(A1-18m)/(A1-20) with
         PhysAgree — the anti-degeneracy that rejects the zero cheat);
  arm 4  the two named transport bases = the two (S23) squares at u and
         at r_nm ((JE-0)/(JE-1a) at fdf20bd4 bytes) + S22a identity/
         composition. ((S24)/(S25) are H-tail; (A1-14)/(JB-2) psiDO
         eligibility is H-tail Joint B — NOT charged to B-1, per V001
         §1.2's own boundary. The seam is displayed at §5.3.)
AT THE JOINT QUANTIFIER: one compatible tuple, never componentwise
existence (288a3d41 bar, consumed via OB-2 §3.3 at audited grade).
```

### 3.2 What the fixed readings change about the QUESTION itself

Rounds 1-2 ran the DERIVABILITY quantifier (does sealed stock produce the
member) and stopped TYPE-U. OB-2 separated the EXISTENCE quantifier and
localized it here. Two consequences of the pins, used throughout:

```text
(a) PIN-1 FORCED(ACTUAL) cuts BOTH ways. It removes the formal escape
    (squares-only tuples are INADMISSIBLE: the (S4) anti-formal sentence
    and void conditions 1/3 are law) — AND it names the common carrier
    the formal rounds could not lawfully choose: D_N^ker must CONTAIN the
    actual path-bilocal kernels ((S2b) intro), Sch_N IS the actual
    Schwartz-kernel realization, kappa_T,N shares "the same named
    topology as the Q-408 image" (post-(S8)). The actual bilocal
    distribution carrier over B_N is therefore not an organizing rail
    any more: it is the forced landing site. Round 1's gap A-I.1
    (COMMON_DKER/SCH/IOTA_MEMBER) DISSOLVES as a gap — the two terms of
    (SPEC-11b) land in one actual Hausdorff carrier because the forced
    semantics already puts both there (§4.1).
(b) Derivability-era objections of the form "choosing X would select
    unratified structure" (round 2 at (A8): complement selection) do not
    bind the EXISTENCE quantifier: an abstract witness is one tuple, and
    its components are existential coordinates, not adoptions. Each such
    choice below is ledgered (CL-3), none is bound.
```

PIN-2-HOL and PIN-3 do not touch B-1's arms (HOL-gate side; H-tuple side);
their consequences pass through unchanged. PIN-2-H: no arm consumes
(JD-3)'s words — every display below is identical under R-A and R-B.

---

## 4. TOWARD INHABITANCE — THE CANDIDATE TUPLE W-EX3, ARM BY ARM

Displayed fiberwise over the sealed carried stages of the B-3 census
(four anchor stages N_RL, M_RL, N_n, N_m — all cycle rank 1 — plus the
diamond slots, max rank 2; every stage finite: dim S_(1,sa)(K_N) =
r(r+1)/2 real, so dim E_N <= 2 + r(r+1)/2 <= 5, battery B3, exact). No
component is bound; W-EX3 is an existential schema whose free choices are
ledgered at CL-3.

### 4.1 The forced common carrier and the actual landings [PART-PROVABLE; imports tagged]

```text
D_N^ker   := D'_biloc,N = D'(M_N x M_N; T*M_N boxtimes T*M_N), the actual
             bilocal distribution space over the sealed background B_N —
             Hausdorff locally convex [external standard]. It CONTAINS
             the image of (S2a) as the actual kernels ((B2-5)/(B2-6) at
             audited grade) — the (S2b) containment is tautological.
iota_N^Q408 := the actual inclusion. Ker_N^Q408 is finite-dimensional
             ((B2-6); OB-2 NG-2 p6); a linear injection of a finite-dim
             space into a Hausdorff TVS is continuous [external
             standard]. Continuous + injective + ACTUAL. PIN-1-compliant.
Sch_N     := the actual Schwartz-kernel map on Op_N^(loc,2) under a fixed
             density convention (witness coordinate: e.g. the metric
             density of g_N). Continuous and injective on the declared
             properly supported classical order<=2 class [IMPORT: the
             Schwartz kernel theorem package — standard external
             mathematics, exact, imported for this display only, the
             same grade as OB-2's NG-1 import].
M_N^loc   := the sealed local Maxwell operator per its sealed "actual
             local Maxwell operator" typing; M_N^loc != 0 consumed
             exactly as OB-2 CL-5 consumed it. kappa_T,N := Sch_N(M_N^loc)
             — an actual diagonal-supported distribution in the same
             named topology as the Q-408 image: the post-(S8) sentence is
             satisfied non-formally, and the (S4) anti-formal clause is
             met BY CONSTRUCTION (nothing here is a parse tag).
```

Both branches of (SPEC-11b) now land in ONE constructed Hausdorff carrier
— the round-1 stop A-I.1 is discharged at the existence quantifier by the
forced reading itself. [Identical under R-A/R-B.]

### 4.2 Arm 1 — S_N with one value on the sealed ideal [PART-PROVABLE; the one conditional bit isolated]

```text
O_R5,N^src := I_N^R5 (the sealed lower bound; lawful at the displayed
              bytes "I_N^R5 subset O_R5,N^src"). Witness coordinate.
S_N(qtilde_N(a,b,C)) := a kappa_T,N + iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)).

WELL-DEFINEDNESS (the "one value" clause), exactly:
  E_N is finite-dimensional at every displayed carried stage (battery
  B3), so ker(qtilde_N) is closed and Rel_N = closure(ker qtilde_N) =
  ker(qtilde_N) [PROVABLE — this also discharges round-2 gap T.3, the
  beta_N premise, at the census]. S_N is single-valued on I_N iff
  ker(qtilde_N) subset ker(F_N):
   - every PURE-CYCLE relation (a=0) is killed: (0,b,C) in ker(qtilde)
     => Q_N^quot(0,b,C) = 0 => both branches vanish [PROVABLE; the two
     displayed classes (R4),(R6)-(R7) re-derived exactly, battery B1/B2];
   - a MIXING relation (a != 0) forces kappa_T,N = -(1/a) x (its
     cycle-side value) (battery B4a); consistency across several mixing
     relations is exactly the pure-cycle condition the sealed Kernbar
     already decides (battery B4b/B4c — the OB-2 §4.3 consistency half,
     now displayed exactly);
   - under PIN-1 the forced identification is IMPOSSIBLE for either
     value of the cycle side when dim M_N >= 2 (§5.1-5.2), and
     impossible for the zero value at ANY dimension.
  THEREFORE: S_N exists on the sealed carriers  <=>  the ideal contains
  NO mixing relation (at dim M_N >= 2; at undisplayed dim, no-mixing
  still suffices). THE BIT IS UNDISPLAYED: B-3(ii)'s missing object,
  re-swept open (SW-2). This is the ONE conditional of arm 1.

CONTINUITY: S_N is a linear map from the finite-dimensional I_N^R5 into
  a Hausdorff TVS. If O_R5,N is Hausdorff (TOP-1, §4.3) the domain is
  Euclidean and continuity is automatic [external standard]. The round-1
  "extension question" (SPEC-11 block) is MOOT at O_src = I_N^R5: no
  extension is demanded beyond the image. Kraw_N = S_N qtilde_N
  continuous [PROVABLE: finite-dim domain]; Sbar_N by automatic descent
  ((A1-7)-(A1-8), premise now inhabited); Kernbarhat_N by corestriction.
D_N^Loc := im(Kraw_N) = span{kappa_T,N} + iota Kernbar(O_N^cyc) — the
  (A1-13)/(SPEC-12c first half) equality holds BY CONSTRUCTION; the plus
  not asserted direct (the overlap bit is §4.5's).
[Identical under R-A/R-B.]
```

### 4.3 Arm 2 — the topology certificates [PROVABLE at the census, modulo ONE named bit on a sealed carrier]

```text
At every displayed carried stage (all finite-dimensional):
  qtilde_N : E_N -> O_R5,N is continuous UNCONDITIONALLY [external
    standard: any linear map from a finite-dim Euclidean space into any
    TVS is continuous] — round-2 gap T.1's demanded certificate;
  kappa_N : I_N^q -> I_N^R5 is continuous UNCONDITIONALLY (quotient
    universal property + corestriction) — gap A-II.1 discharged;
  I_N^q is Euclidean-Hausdorff (finite-dim quotient by a closed kernel);
  TOP-1 (the ONE remaining bit): O_R5,N Hausdorff. Sealed of record:
    "the physical O_R5,N topology" exists (SEAM1 §3.1); NO sealed byte
    certifies separation (SW-4). UNDER TOP-1: the finite-dim subspace
    I_N^R5 is Euclidean, kappa_N is a homeomorphism, and (T2)/(T3) —
    round-2 gaps T.1/T.2 — are PROVABLE. Without TOP-1 the embedding
    certificate genuinely fails to follow: TOP-1 is a new named finite
    display (one sealed separation byte decides it).
  D_N^Loc image topology (T7)/(A1-12): PROVABLE UNCONDITIONALLY —
    D_N^Loc is finite-dimensional inside Hausdorff D'_biloc,N, so the
    E_N/ker(Kraw_N) quotient topology and the subspace topology are both
    Euclidean and agree; the embedding into D_N^ker is automatic. Gaps
    A-II.3 / T.5 discharged.
[Identical under R-A/R-B.]
```

### 4.4 Arm 3 — the addressed Res/Ker chain [PART-PROVABLE; one named bit + one import]

```text
Witness coordinates: w := N; T_(w->N,K)^D := id; O_a^resp := O_w^resp
  (= O_R5,w, the sealed unaddressed relabelling (Q8)-(Q9)); Res_(w,a) :=
  id with QuotResCert trivial; the whole orbit [a] = {a, a^-} carried, no
  representative selected ((A1-21a) respected).
S_(w->N,K) := S_N ∘ P, where P : O_R5,N -> I_N^R5 is a continuous linear
  projection onto the finite-dimensional image.
  TOP-2 (the named bit): such P exists iff continuous coordinate
  functionals exist on I_N^R5 — guaranteed if O_R5,N is locally convex
  Hausdorff [IMPORT: Hahn-Banach + finite-dim complementation, standard
  external mathematics]; NO sealed byte certifies local convexity (SW-5).
  Round 2's (A8) objection ("choosing a complement adds unratified
  structure") governed derivability; at the existence quantifier the
  complement is an existential coordinate (CL-3), and — decisively — the
  target is no longer "an unbuilt target": D_N^Loc is the constructed
  finite-dimensional carrier of §4.2, so the extension IS a finite tuple
  of scalar Hahn-Banach extensions, exactly the case the theorem covers.
THEN: im(S_(w->N,K)) = im(S_N) = D_N^Loc; Ker_(a,K) := corestr(S_(w->N,K));
  PhysAgree (A1-18m): Ker ∘ Res = corestr(S) holds BY CONSTRUCTION, and
  the agreement is GENUINE — S_(w->N,K) restricted to iota_w(O_src) is
  S_N itself ((A1-20) with T^D = id), and S_N != 0 (kappa_T,N in its
  image). The zero cheat is REFUTED inside the schema: Res = 0 forces
  S = 0 on the image against S_N != 0 (battery B5j — the anti-degeneracy
  chain closes, not merely parses).
Sign-odd square (A1-21b): conditional of record on an admitted G5/
  source-reversal member; none is admitted; the witness's odd domains are
  empty at the displayed quantifier (OB-2 §5.2 pattern). Vacuously met.
[Identical under R-A/R-B.]
```

### 4.5 Arm 4 — the two (S23) bases at u and r_nm [PART-PROVABLE; the overlap bit isolated]

```text
The demanded content (fdf20bd4 (JE-0)/(JE-1a), at bytes): for f in
{u : N_RL -> M_RL, r_nm : N_n -> N_m},
   etahat_f iota_N^rep      = iota_M^rep eta_f^(K,rep),
   etahat_f Kernbarhat_N    = Kernbarhat_M j_f^prof,
plus S22a identity/composition.

CONSTRUCTION: define etahat_f BY the second square —
   etahat_f(Kernbarhat_N(p)) := Kernbarhat_M(j_f^prof(p)),
with j_f^prof the sealed profile transport on the cycle branch ((G1) for
u; LOC-NAT J_nm^form for r_nm, consumed at audited grade on their proved
scopes — all four anchor stages are rank 1, where the sealed transports
are exactly the displayed ones) extended slot-wise on the L_T class
(a |-> a; a witness coordinate; well-posed iff the L_T class is
independent of the cycle classes = the NO-MIXING bit again).
WELL-DEFINEDNESS of etahat_f, exactly:
   ker(Kernbarhat_N) = Q_N^quot(ker F_N);
   the a=0 part is carried by the SEALED square (G1) (eta_f^K(0) = 0)
     plus iota injectivity [PROVABLE on the sealed transports' scope];
   the a!=0 part is empty  <=>  kappa_T,N NOT in iota Kernbar(O_N^cyc)
     — THE OVERLAP BIT. Undisplayed of record ("the plus sign is not a
     direct-sum assertion"). PROVABLE if dim M_N >= 2 [IMPORT: the NG-1
     wavefront separation — a nonzero path-bilocal kernel has
     off-diagonal wavefront; kappa_T,N has conormal-diagonal wavefront];
     OPEN at undisplayed dim M_N (= B-3(iii)'s cell, K-c's object).
THEN the first square follows from the second + (G1) [PROVABLE: the
  represented image is inside Kernbarhat's image and the two
  prescriptions agree by the sealed square — battery B5k/B5l exhibit the
  shape]; etahat_f is continuous (finite-dim domain); S22a: identity
  holds; the two named generators admit NO composable nonidentity pair
  (distinct stage pairs), so the composition law at the minimum display
  is exact and the functor-extension debt beyond it is exactly the
  (SA-1) closure — open of record, unchanged, NOT discharged here.
[Identical under R-A/R-B.]
```

### 4.6 The joint check

W-EX3 is ONE tuple: arms 2-4 are built from arm 1's S_N and share every
component (same D_N^ker, same kappa_T,N, same D_N^Loc). Nothing is
promoted from componentwise to joint — the joint quantifier is met by
construction wherever the named bits resolve. The six-fiber/joint-fiber
bar is consumed, not re-walked. What W-EX3 is NOT: not a member, not
bound, not adopted; its carriage across the generated (SA-1) closure is
NOT claimed (the closure's rank reach and induction are open of record —
at a hypothetical carried stage of unbounded rank the finite-dimension
arguments of §4.2-4.3 DO NOT APPLY, stated plainly); it grounds no
family-membership claim for Pkg_N^H or Pkg_N^Hol (the tails and gates
are outside B-1).

---

## 5. TOWARD EMPTINESS — UNDER THE FIXED READINGS

### 5.1 NG-3 unchanged [PROVABLE-shape; antecedent undecided; no caveat]

One mixing relation with ZERO cycle-side value forces kappa_T,N = 0
against Sch_N injective + M_N^loc != 0 — at any dimension, both families
die on registrar consumption. Antecedent: undisplayed (SW-2). Unchanged
from OB-2/B-3.

### 5.2 NG-3' — NEW: any mixing relation kills on the dim>=2 cell [PART-PROVABLE + import]

```text
A mixing relation with NONZERO cycle-side value v forces
   kappa_T,N = -(1/a) v  (exact; battery B4a),
i.e. the actual Schwartz kernel of the actual nonzero order<=2 operator
EQUALS a nonzero path-bilocal kernel. At any carried stage with
dim M_N >= 2, the two have disjoint wavefront character (conormal-
diagonal vs off-diagonal points over the named paths) [IMPORT: wavefront
calculus, the same external step OB-2's NG-1 tagged] — contradiction.
   => under PIN-1, EITHER value of a mixing relation's cycle side kills
      both families at a dim>=2 carried stage.
K-b THEREFORE WIDENS on that cell: the kill no longer needs the zero
value. At dim M_N = 1 (or undisplayed) the nonzero-value case does NOT
close (a smooth compactly supported kernel can be a properly supported
smoothing operator's kernel — the identification is not refutable there);
the zero-value case (§5.1) needs no dimension at all.
```

### 5.3 The void-3 scope note [displayed, not decided]

LOC §2.5 void condition 3 ("A Q-408 kernel … lies outside the symbol
domain") was consumed by the PIN determination as PIN-1 forcing evidence.
WHETHER it additionally binds the common fiber (voiding the LOC package
itself) or only the H-tail Joint B is a READING this artifact does not
decide: under either scope it cannot fire today (its microlocal content
needs dim M_N >= 2 — displayed nowhere), and under the common-scope
reading K-c's kill would widen from H-only to BOTH families. Surfaced for
the registrar; no forcing chain is attempted here.

### 5.4 No formal joint incompatibility exists [PROVABLE; toy quarantined]

A complete two-stage model of every displayed B-1 clause SHAPE — source
agreement (SPEC-11b/c), one value on the ideal, kappa nonzero and
non-overlapping, (A1-13), the full addressed chain with genuine
PhysAgree and the zero-cheat refutation, both (S23) bases, S22a — was
built and verified exactly (battery B5a-B5n, 14 checks). CONSEQUENCE (the
only one drawn): the B-1 clause set is jointly satisfiable as a clause
set, so NO emptiness proof can come from clause shape alone — every
honest no-go must consume a sealed-carrier fact (the ideal's extent; dim
M_N; the closure). The toy decides NOTHING about the sealed carriers and
is quarantined below.

---

## 6. THE REDUCTION — WHAT THE FIXED PINS DID AND DID NOT UNBLOCK, EXACTLY

```text
DID UNBLOCK (this round, at the displayed census):
  u1  the common-carrier gap A-I.1: DISSOLVED by PIN-1's own semantics
      (§4.1) — the actual bilocal carrier is forced, not chosen;
  u2  the topology-certificate gaps T.1/T.2/T.5/A-II.1/A-II.3 and the
      Rel_N = ker premise (T.3): PROVABLE at the census via finite-
      dimensionality, modulo the single new bit TOP-1 (§4.3);
  u3  the addressed-factor gaps A-III.0..3: constructible at the
      existence quantifier modulo TOP-2, with the anti-degeneracy chain
      closing non-vacuously (§4.4);
  u4  the transport-base gaps A-IV.0..2 at the minimum display:
      constructible from the SEALED squares modulo the overlap bit
      (§4.5);
  u5  arm 1's "one value" clause: EQUIVALENT, exactly, to the no-mixing
      bit (§4.2; battery B4) — and the kill surface on the other side
      widens (NG-3', §5.2).
DID NOT UNBLOCK (the deciders, all outside the pins, all named before):
  d1  the extent/basis of ker(qtilde_N) — B-3(ii)'s missing object; THE
      decider. One future sealed display of it at the four anchor stages
      decides B-1: a mixing relation => EMPTY (zero value: any dim;
      either value: dim>=2); no mixing => W-EX3 completes (modulo
      d2/d3);
  d2  dim M_N — B-3(iii)/K-c's cell (feeds NG-3', the overlap bit, and
      the void-3 note);
  d3  TOP-1/TOP-2 — two NEW named finite displays on the sealed O_R5,N
      topology (separation; dual-separation/local convexity), surfaced
      by this round for the first time;
  d4  the (SA-1) closure induction — carriage beyond the displayed
      census remains open exactly as before (rank reach unbounded of
      record; the finite-dimension arguments do not extend).
NET REDUCTION: at dim M_N >= 2 with TOP-1/TOP-2 —
      B-1 INHABITED <=> NO MIXING RELATION AT THE ANCHOR STAGES.
The whole of OB-2 (both families, at the displayed quantifiers, per its
own §5.3 localization) then sits on: the no-mixing bit (B-1 + NG-3/
NG-3'), the NG-2 extent bit (H side), and the census/closure cells —
every one finite, bytes-checkable, threshold printed.
```

---

## 7. CONSISTENCY

Nothing here binds a member, adopts anything, authors physics, selects a
route/branch/address/representative, or touches the held construction.
W-EX3 is a DISPLAY inside a determination — an existential schema whose
every free choice is ledgered; it is not proposed for adoption, not
preferred, and grounds no family claim (§4.6). The joint-fiber bar is
consumed as a bar. R9 remains UNEXECUTABLE/PENDING with an empty
quantifier domain; no common cell is formed or presupposed; A8 remains
UNEXECUTABLE/PENDING. The V002 checkpoint discipline is respected: round
3 ran on the principal's commissioning. No score, flag, criterion, or
gate moves. SK-2 does not fire; ACT-3 is not forced; the D-1 menu is
unchanged — the member proposal's target is still not grounded
non-vacuous, and any commissioned H member still owes the NG-2 proof and
the NG-3 (now NG-3'-widened) consistency display.

## 8. CHOICE LEDGER (terminal; per the guard)

```text
CL-1  CORPUS BOUNDARY (the named unforced choice): roots = workspace,
      supervision, cleanroom_output, relay_inbox, relay_outbox minus
      fenced filename classes (none opened); a32_holdout not content-
      swept (holdout discipline). Sweeps SW-1..SW-6, cutoff
      2026-08-15T05:00Z. A wider sweep changes this determination only
      by exhibiting a sealed display of: the ideal's extent, a mixing
      relation, dim M_N, an O_R5,N separation/convexity certificate, or
      a competing round-3 artifact. PREMISE(named).
CL-2  AUDITED-GRADE CONSUMPTION: the OB-2 pair, the PIN pair (PIN-1
      ACTUAL / PIN-2-HOL zero-admitted / PIN-3 MANDATORY of record;
      PIN-2-H carried both ways), and the B-3 pair (census, ranks,
      relation classes; audit corrections C-1..C-3 respected, K-d noted)
      consumed as commissioned. LOC/PJ2/LOC-NAT/KER clause bytes
      consumed as quoted inside those artifacts at their audited grades;
      fdf20bd4 and SEAM1 re-walked directly at the spans cited in §1.
      PREMISE(named).
CL-3  WITNESS COORDINATES (existential, not adopted, none bound): the
      density convention in Sch_N; O_R5,N^src := I_N^R5; w := N and
      T^D := id in the addressed chain; the projection P (equivalently
      the finite complement); the L_T-slot extension of j_f^prof; the
      empty sign-odd domains. Each appears only inside the schema; no
      representative, scale, or unit is selected; the V001/V002
      cross-sector unit obligations (U1)-(U7) bind any future candidate
      unchanged and are not discharged here. PREMISE(named).
CL-4  EXTERNAL-MATH IMPORTS (tagged at use; exact; no numeric content):
      the Schwartz kernel theorem package (§4.1); finite-dimensional
      TVS facts — unique Euclidean topology on finite-dim Hausdorff
      TVS, automatic continuity from finite-dim domains, closedness of
      finite-dim subspaces (§4.2-4.3); Hahn-Banach + finite-dim
      complementation (§4.4, gated on TOP-2); wavefront calculus (§4.5,
      §5.2 — the same import OB-2's NG-1 tagged). PREMISE(named).
CL-5  SEALED-TYPING CONSUMPTION: M_N^loc != 0 and its "actual local
      Maxwell operator" typing consumed exactly as OB-2 CL-5; the
      L_T,N != 0 question is NOT separately premised — it is subsumed
      by the no-mixing bit (L_T,N = 0 would BE a zero-value mixing
      relation (1,0,0), firing §5.1). PREMISE(named).
CL-6  FINITE-DIMENSIONALITY AT THE CENSUS consumed from the B-3
      displays at audited grade (dim S_(1,sa)(K_N) = r(r+1)/2 real, max
      displayed rank 2, exact); inherited caveat: if a kernel/carrier
      extension or a rank>=3 carried stage is ever sealed, §4.2-4.3's
      arguments must be re-proved on the extended objects (stated at
      §4.6/d4). PREMISE(named).
CL-7  B-1 SCOPE read from OB-2 §6.2's own parenthesis (four arms); the
      H-tail eligibility inclusion (A1-14)/(JB-2) NOT charged to B-1
      per V001 §1.2's sealed boundary; the void-3 scope question
      surfaced (§5.3), not decided. PREMISE(named).
CL-8  FIELD READING: the battery's dimension rows use the real-carrier
      bytes ((B1-3)/(B1-5)/(B2-5) at audited grade); no field pin is
      adopted; no conclusion above depends on real-vs-complex (the
      finite-dimensionality arguments hold under both; the NG-2 table
      is B-3's, untouched). PREMISE(named).
CL-9  DISCLOSURE: fenced-class BASENAMES appeared in sweep listings; no
      fenced content opened, read, or used. The BANACH file was read at
      three lines, sweep-grade, seal-verified (feb84216). One zsh
      word-splitting defect in an early sweep invocation produced empty
      results; detected (implausible against B-3's SW-2), diagnosed,
      and re-run correctly before any reliance. PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The two-stage model of §5.4 (battery B5) is a TOY: it verifies that
  the displayed B-1 clause SHAPES are jointly satisfiable, and it enters
  exactly one conclusion — that emptiness cannot follow from clause
  shape alone — which is a statement about the clause set, not about
  the sealed carriers. No toy value, stage, address, dimension, or
  number enters W-EX3, the reduction (§6), or any verdict. W-EX3 itself
  is NOT a toy: it is a conditional schema over the sealed carriers,
  displayed WITH the exact undisplayed bits it depends on (no-mixing;
  TOP-1/TOP-2; the overlap bit; dim M_N where used), and it is not
  promoted past the existence quantifier, past its census scope, or
  into any candidate, member, or adoption. The battery's symbols (t,
  a_i, v_i, generic 2x2 R) are never evaluated; 27/27 checks are exact.
```

## FLAG BLOCK

```text
EXTSRC_ROUND3 = UNDECIDABLE-STILL (menu §0); ROUND 3 OF 4 SPENT; honest
  outcome; ABSTRACT_EXTSRC_FAMILY_EMPTY = still not proved; ABSTRACT
  WITNESS = still not asserted on the sealed carriers
WHAT_THE_PINS_UNBLOCKED = §6 u1-u5: common carrier forced (A-I.1
  dissolved); topology certificates PROVABLE at the census mod TOP-1;
  addressed chain constructible mod TOP-2; the two (S23) bases
  constructible mod the overlap bit; "one value" <=> NO-MIXING exactly
WHAT_THEY_DID_NOT = §6 d1-d4: the ideal's extent (THE decider, B-3(ii));
  dim M_N (B-3(iii)/K-c); TOP-1/TOP-2 (NEW named finite displays on
  O_R5,N's sealed topology); the (SA-1) closure induction
REDUCTION = at dim M_N >= 2 + TOP-1/TOP-2: B-1 INHABITED <=> NO MIXING
  RELATION at the anchor stages (exact biconditional, §6); one finite
  future display decides B-1 essentially outright
W_EX3 = explicit candidate tuple displayed arm-by-arm (§4), all four
  arms, joint by construction; existential schema only; NOT bound, NOT
  adopted, NOT carried past the displayed census; closure reach open
NG3_PRIME = NEW kill widening (§5.2): under PIN-1, ANY mixing relation
  (either cycle-side value) kills BOTH families at a dim>=2 carried
  stage; zero-value case remains caveat-free (NG-3 unchanged); K-b
  widens on the dim>=2 cell; wavefront import tagged
CLAUSE_SHAPE_CONSISTENCY = PROVED (toy exhibit, quarantined): no formal
  joint incompatibility exists in the B-1 clause set; every emptiness
  route must consume a sealed-carrier fact
VOID3_SCOPE = surfaced not decided (§5.3): common-fiber vs H-tail
  scope of LOC §2.5.3; cannot fire today under either reading
PIN2H_HANDLING = every display identical under R-A and R-B (no arm
  touches (JD-3); stated per arm; ROW-3 pattern)
SK2 = NOT FIRED ; ACT3 = NOT FORCED ; D1_MENU = UNCHANGED ; the member
  proposal's target NOT grounded non-vacuous
JOINT_FIBER_BAR = respected (one tuple; nothing promoted; bar consumed)
SEALS = commission 7/7 + chased 5/5 MATCH ; MISMATCHES = 0
SWEEPS = SW-1..SW-6 ; CUTOFF 2026-08-15T05:00Z ; exhaustiveness
  sweep-displayed only ; no competing round-3 artifact exists
BATTERY = fresh venv, sympy 1.14.0, 27/27 PASS, exact symbolic only, no
  float, no value frozen ; NUMERIC_EVALUATION = exact symbols/integers
  only ; MEASURED_CONSTANT_COMPARISON = none
DETERMINATION_ONLY = held (no member bound, no candidate authored, no
  adoption, no selection, no common cell, no reader, no fixed point, no
  end test) ; MEMBER_BOUND = false ; CHAIN_INVOKED = false
HELD_CONSTRUCTION = untouched, unopened, unpresupposed (506bfe2a by bar)
R9 = UNEXECUTABLE/PENDING ; A8 = UNEXECUTABLE/PENDING
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = the first fixed-readings round turned the common fiber from a
  seven-gap stop into a two-sided finite question: the same one sealed
  display (the relation ideal's extent) that used to arm only NG-3 now
  decides the fiber itself — toward EMPTY if any mixing relation
  appears, toward the displayed candidate W-EX3 if none does — with
  the residual conditions (TOP-1/TOP-2, dim M_N, the closure) each
  named as its own finite display. Nothing moved; the gates are where
  the principal left them.
```
