# Stage 8 Task 4a P7 Finite-Core / Separation / T5 Commuting-Square Certificate v001

Date: 2026-08-02
Lane: CODEX LANE 1
Task: 4a, P7
Authority: DoR-011, construction and proof only
Register head consulted at start and before sealing: Q-255

## 0. Lead determination

**THE GERM-INDEPENDENT MATHEMATICAL CORE IS COMPLETE ON EVERY RATIFIED
CLASS, BUT P7 IS NOT PHYSICALLY DISCHARGED.** The existing proofs and the
proofs assembled here give a complete six-part finite core:

```text
1. canonical sequential-cylinder retractions and their N<=M naturality;
2. density of the finite cylinder spectra in the ratified compact spectrum;
3. separation of the full norm C-star coefficient algebra;
4. separation of the full standard Hilbert C-star module;
5. separation of represented norm-continuous left multipliers; and
6. exact N<=M compatibility of the finite PathCert dynamics subtrace.
```

Thus:

```text
P7_GERM_INDEPENDENT_CORE = 6_of_6_proved
P7_PHYSICAL_DISCHARGE_ROUTES = 0_of_2_completed | TYPE-U |
  would-build: either the object-side C1-C5 package or one selected
               output-side quotient-factorization certificate
```

The difference is not missing mathematics inside the finite core. It is an
interface boundary. The canonical maps

```text
r_N^B, q_N, Pi_N
```

are sequential-cylinder retractions on the ratified kinematic classes. They
are not the unbuilt physical maps

```text
rho_G,N, rho_H,N.
```

The exact conditional composition theorem proved below is:

> If P2-P6 instantiate a physical source core, a norm/module-class retarded
> Hessian embedding, physical restrictions intertwined with the canonical
> `Pi_N`, preservation of the state/measure/contour/contact/boundary/domain
> package, and naturality of inversion and retarded extraction, then
> `Tail_R={0}` and the upper T5 square commutes. If the independently fixed
> finite raw-correlator/extraction/contraction leg is also supplied, the full
> T5 test is executable without any new separation proof.

This theorem is maximally germ-independent: it authors no `SOURCE_GERM_PHYS`
field and is valid for any later germ satisfying the named interface
predicates. But **ratification of P1 alone cannot discharge P7**. The governing
dependency order is expressly

```text
P1 -> P2 -> P3 -> P4 -> P5 -> P6 -> P7.
```

P1 ratification opens that chain; it does not instantiate its downstream
maps, classes, or preservation certificates.

P7 is not moot. Q-254 makes output-side optionality conditional on one selected
consumer independently factoring through `R_phys_class/Tail_R`. Q-250 supplies
the quotient theorem but no factorization, and Q-255 finds even `p_loc`'s
consumption untyped. No selected consumer currently triggers the optionality
clause.

```text
P7_GERM_INDEPENDENT_CORE_COMPLETE = true | TYPE-P |
  premises: DoR-008, DoR-009, DoR-011 and the cited imported mathematics

P7_OPTIONALITY_TRIGGER_MET = false | TYPE-R |
  test: Q-250 supplies only quotient determinacy; Q-255 supplies no selected
        consumer factorization through that quotient

P1_RATIFICATION_ALONE_DISCHARGES_P7 = false | TYPE-R |
  test: Q-254:453-489 places P2-P6 strictly between P1 and P7 and states that
        P6 enables Tail_R and comparison but not Tail_R={0}

P7_OBJECT_SIDE_PHYSICAL_CERTIFICATE_EXISTS = false | TYPE-U |
  would-build: the P2-P6 interface predicates C1-C5 in Section 6

P7_FULL_T5_CERTIFICATE_EXISTS = false | TYPE-U |
  would-build: C1-C6 in Section 6, including the independently fixed finite
               raw-G, E_R^fin, and contraction leg
```

## 1. Preflight, premises, currency, and scope

### 1.1 Preflight

```text
DOES_P7_EXIST = true
  Q-254 specifies it at
  STAGE8_TASK4A_MISSING_PHYSICAL_LAYER_CONSOLIDATED_SPEC_AND_CONSTRAINT_BATTERY_V001.md:318-344.

IS_VERSION_CURRENT = true_through_Q_255
  Q-255 bears on P7 optionality because it leaves p_loc consumption untyped;
  it does not alter the finite-core or separation theorems.

ARE_GERM_INDEPENDENT_INPUTS_PRESENT = true
  Q-247, Q-245, Q-238, Q-250, Q-243 and the ratified DoR-008 classes exist.

ARE_PHYSICAL_P7_INPUTS_PRESENT = false | TYPE-U |
  would-build: Q-254 P2-P6; those packages remain unbuilt.

IS_P7_MOOT_FOR_A_SELECTED_CHANNEL = false | TYPE-R
  test: no selected P9-P11 consumer has a proved quotient factorization.
```

### 1.2 Declared premises and imported mathematics

No new physical premise is introduced. Every positive statement using the
ratified carrier or finite source-coupled law is typed:

```text
TYPE-P | premises: DoR-008, DoR-009, DoR-011.
```

The mathematical proofs consume the following declared imported results:

1. Gelfand duality for the discrete abelian group C-star algebra;
2. a continuous function vanishing on a dense subset is zero;
3. functoriality of group C-star algebras and minimal tensor products in the
   ratified commutative/nuclear setting;
4. definiteness of Hilbert C-star module inner products;
5. faithfulness of left multiplication on the standard module; and
6. the elementary commuting-composition lemma proved in Section 6.

These imported mathematical facts do not supply a physical state, source germ,
measure, contour, boundary condition, response class, or consumer.

### 1.3 Authorities

| Authority | SHA-256 | Load-bearing content |
|---|---|---|
| `DECISION_OF_RECORD_008_FIELD_CTP_ADOPTION_RATIFIED_WITH_FALSIFIER_2026-08-01_V001.md` | `d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19` | sequential labels, zero-extension, norm C-star/CTP completion, standard module, finite falsifier |
| `DECISION_OF_RECORD_009_THE_TRANSITION_LAW_RATIFIED_E_POST_2026-08-02_V001.md` | `11632fa30dfe9225c228f07a3be4717c73bdfc9e38b0c76c46d905d3fe193bc5` | finite source-coupled law used only in the PathCert lower row |
| `DECISION_OF_RECORD_011_TASK4_TRANSPORT_CONSTRUCTION_AUTHORIZED_2026-08-01_V001.md` | `1a54d50a53da30efe5af39b17d81a908eec75daa6ff450563edffed1d616a660` | structural construction/proof authorization |
| `STAGE8_TASK4A_MISSING_PHYSICAL_LAYER_CONSOLIDATED_SPEC_AND_CONSTRAINT_BATTERY_V001.md` | `241bf43f04aed7f215bad6ea80170a9c7733000896df839ac80974769d0a46c3` | current P1-P11 ledger, P7 alternatives, dependency order, battery |
| `STAGE8_TASK4A_FINITE_RESTRICTION_SEPARATION_AND_BIDUAL_TAIL_DETERMINATION_V001.md` | `60b5b4c5788eca2be2d9f11d67983b2e7a5823066cdabf9a734f7a59aae0ecd1` | canonical retractions and separation on ratified classes; bidual counterexample; physical transport conditions |
| `STAGE8_TASK4A_CORRELATOR_COMPLETION_ARROW_AND_RESTRICTION_PINNING_DETERMINATION_V001.md` | `5b9a4a8b000c313049caa71aff4235cc9eb4b0f98bb2af9931fd8820930ed856` | physical separation criterion and four sufficient conditions |
| `STAGE8_ENVELOPE_FINITE_PATHCERT_DYNAMICS_SUBTRACE_CONSTRUCTION_V001.md` | `74a1f903ce79fc76a0f32a036a872b2d59b8944aa9e4211135d65ba8e9800db0` | exact finite lower-row path and N<=M compatibility; explicit scope ceiling |
| `STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md` | `57c06fcbedf4a486b87b42c02e6d13a92096ac6a56751d3ae5fd55f4b11deb57` | T5 square, matching requirements, and applicability bar |
| `STAGE8_TASK4A_RESPONSE_CLASS_FORCING_AND_TAIL_OUTPUT_INERTNESS_DETERMINATION_V001.md` | `fccd16a74269386a2fdb7bac122f907cd659c8eb09ae5f45eabf39e5e9180d79` | exact quotient determinacy and output-side factorization criterion |
| `STAGE8_TASK4A_FINITE_HESSIAN_TO_DELTAPHI_TRANSPORT_MAP_CONSTRUCTION_AND_P_CH_TRACE_V001.md` | `70185aa842bc500724719c65bd66b5f07005e2214b97be0d35e07fd029d5c68c` | exact finite Keldysh rotation and zero ordered retarded block |

### 1.4 Scope

Roots entered:

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003
/Users/bgm/MB Work/alpha_supervision
```

Excluded:

```text
.git; binary/media payloads; archive mirrors as independent authority;
superseded variants except where lineage was needed; germ draft in the other
lane; and a32_holdout/custodian_private (not entered, listed, searched, opened,
or read).
```

Bounded, case-insensitive searches used the terms:

```text
P7 | finite-core | finite restriction | rho_G,N | rho_H,N | Tail_R |
commuting square | T5 | PathCert | zero-extension | left multiplier |
output tail | quotient factorization
```

## 2. P7's complete content and alternatives

Q-254:318-344 states that P7 combines:

1. a finite-core/density theorem;
2. continuity or closability of the admitted response class;
3. separation transport from finite restrictions to the complete response;
4. preservation of state, measure, contour, contacts, boundary data, and
   endpoint domains;
5. commutation of inversion and retarded extraction with restriction; and
6. the T5 square and its independently fixed finite diagnostic leg.

Q-245:491-540 gives the exact physical criterion. For future maps

```text
rho_H,N : RetHess_phys -> RetHess_N,
Tail_R := intersection_N ker(rho_H,N),
```

all finite restrictions determine the complete retarded operator exactly iff

```text
Tail_R = {0}.
```

Its sufficient route is, verbatim in content:

```text
1. finite source images are dense or a core in the physical source topology;
2. admitted retarded bilinear operators are continuous/closable there;
3. restrictions preserve state, measure, contour, contacts, boundary data,
   and domains;
4. inversion and retarded extraction commute with restriction.
```

Raw-map T5 at `:960-996` fixes the comparison:

```text
complete raw G --E_R--> complete retarded Hessian
     | rho_G                         | rho_H
     v                               v
 finite raw G --E_R^fin--> finite retarded Hessian
                                 |
                                 | predeclared contraction
                                 v
                        finite mixed diagnostic
```

The independent finite `Z/W -> G`, finite extraction, and matching carrier,
state, history direction, ordering, contacts, boundary data, and domains are
applicability conditions, not hidden arrows.

P7 has exactly two lawful discharge routes:

```text
OBJECT SIDE: Tail_R={0};
OUTPUT SIDE: the selected consumer factors through
             R_phys_class/Tail_R.
```

Q-250:381-436 proves only that finite restrictions determine `[H]` in the
quotient. It does not prove either discharge route.

## 3. Claim partition

| Claim | Partition | Present standing | Reason |
|---|---|---|---|
| Canonical sequential retractions `r_N^B`, `q_N`, `Pi_N` and N<=M naturality | GERM-INDEPENDENT | proved | fixed ordered labels and zero-extension determine them |
| Finite-cylinder density in the ratified compact spectrum | GERM-INDEPENDENT | proved | every basic product neighborhood fixes finitely many coordinates |
| Norm C-star separation | GERM-INDEPENDENT | proved | continuous coefficients vanish on a dense set only if zero |
| Standard-module vector and inner-product-null separation | GERM-INDEPENDENT | proved | reduces to coefficient separation |
| Represented left-multiplier separation | GERM-INDEPENDENT | proved | faithful left multiplication reduces to coefficient separation |
| Finite PathCert N<=M compatibility | GERM-INDEPENDENT | proved | `omega_M o iota_NM=omega_N` and `J_ML J_NM=J_NL` |
| Physical source core transported from finite source images | GERM-INTERFACE | conditional | needs P2 topology/calculus and P6 comparison maps |
| `RetHess_phys` embedded in the separated norm/module class | GERM-INTERFACE | conditional | needs P5 class typing and an injective representation |
| Physical `rho_H,N` intertwined with canonical `Pi_N` | GERM-INTERFACE | conditional | needs P6, not P1 content |
| Preservation of state/measure/contour/contacts/boundary/domains | GERM-BODY | unbuilt | needs actual P3-P6 objects and maps |
| Inversion and retarded extraction naturality | GERM-BODY | unbuilt | needs actual P4-P6 chain and endpoint data |
| Independently fixed finite raw-G / `E_R^fin` / diagnostic contraction | GERM-BODY | partial only | Q-243 supplies an exact finite relative-phase block, not the full T5 bottom leg on matched physical data |

The first six claims are 6/6 proved in their germ-independent domain. The last
six are not six new mathematical guesses: Sections 5-6 reduce them to fixed
interface predicates and a composition proof. Actual producers must still
instantiate and verify those predicates.

## 4. Germ-independent finite-core certificate

### 4.1 Canonical maps and naturality

The ratified labels are

```text
Lambda_N=Z^N,
j_NM(n_1,...,n_N)=(n_1,...,n_N,0,...,0),
Lambda=direct-sum_(j>=1) Z e_j.
```

The ordered coordinates canonically define

```text
p_N:Lambda->Lambda_N,
p_N(sum_j n_j e_j)=(n_1,...,n_N),
p_N=p_NM compose p_M  for N<=M.
```

For `A=C*(Lambda)` and the CTP coefficient algebra

```text
B=A_+ tensor_min A_-^op,
```

functoriality gives

```text
r_N^B:B->B_N,
r_N^B=r_NM^B compose r_M^B.
```

On the standard module `E_F=B_B` and represented left multipliers:

```text
q_N(x)=r_N^B(x),
Pi_N(L_b)=L_(r_N^B(b)).
```

Therefore for `N<=M`:

```text
q_N=q_NM compose q_M,
Pi_N=Pi_NM compose Pi_M.
```

Every equality follows from the one coordinate truncation family. No germ,
state, measure, contour, response, or consumer appears.

```text
P7_CANONICAL_RETRACTION_NATURALITY = true | TYPE-P |
  premises: DoR-008
```

### 4.2 Density and separation

Under Gelfand duality:

```text
X=Hom(Lambda,U(1))=product_(j>=1) U(1),
Y=X_+ x X_-,
B=C(Y).
```

The finite cylinder spectra `Y_N` set all coordinates beyond `N` to the
identity. Every product-topology basic neighborhood constrains only finitely
many coordinates, so

```text
S_cyl=union_N Y_N
```

is dense in `Y`.

If `b in B` obeys `r_N^B(b)=0` for all `N`, then the continuous function `b`
vanishes on dense `S_cyl`, hence `b=0`. Thus

```text
intersection_N ker(r_N^B)={0}.
```

If `q_N(x)=0` for every `N`, then every `r_N^B(x* x)=0`; coefficient separation
gives `x* x=0`, hence `x=0`. Thus vector-null and inner-product-null kernels
coincide and separate `E_F`.

If `Pi_N(L_b)=0` for every `N`, faithful finite left multiplication gives
`r_N^B(b)=0` for every `N`; hence `b=0` and `L_b=0`. Therefore

```text
intersection_N ker(Pi_N)={0}.
```

These are the full ratified norm classes, not dense-subspace surrogates.

```text
P7_FINITE_CYLINDER_DENSITY = true | TYPE-P | premises: DoR-008
P7_NORM_CSTAR_SEPARATION = true | TYPE-P | premises: DoR-008
P7_STANDARD_MODULE_SEPARATION = true | TYPE-P | premises: DoR-008
P7_LEFT_MULTIPLIER_SEPARATION = true | TYPE-P | premises: DoR-008
```

### 4.3 Finite PathCert compatibility

Q-238 proves for the finite outgoing record system:

```text
iota_NM(A)=A tensor I_(M-N),
omega_M o iota_NM=omega_N,
J_ML J_NM=J_NL.
```

Its frozen certificate carries the exact finite dynamics, record restriction,
and isometric GNS embeddings for all `N`. Hence the finite lower row commutes
under the same sequential order.

```text
P7_FINITE_PATHCERT_SEQUENTIAL_COMPATIBILITY = true | TYPE-P |
  premises: DoR-008, DoR-009
```

This result does not construct a complete physical `PathCert`, physical
`rho_G,N/rho_H,N`, or T5. Q-238 expressly lists those fields as absent.

## 5. The class-transport lemma

### 5.1 Interface statement

Let a future P5-P6 producer instantiate:

```text
R := RetHess_phys,
R_N := RetHess_N,
rho_H,N:R->R_N,

iota_R:R->L_B(E_F),
iota_R,N:R_N->L(B_N),
```

such that:

```text
(T1) iota_R and every iota_R,N are injective;
(T2) iota_R,N compose rho_H,N = Pi_N compose iota_R for every N.
```

No germ body is used in the proof once T1-T2 are certified.

### 5.2 Proof

Let `H in intersection_N ker(rho_H,N)`. Then for every `N`:

```text
Pi_N(iota_R(H))
  =iota_R,N(rho_H,N(H))
  =iota_R,N(0)
  =0.
```

Section 4 proves `intersection_N ker(Pi_N)={0}`, so `iota_R(H)=0`. Injectivity
of `iota_R` gives `H=0`. Therefore

```text
Tail_R=intersection_N ker(rho_H,N)={0}.
```

This is the exact object-side P7 proof, isolated from the content that must
instantiate T1-T2.

```text
P7_CLASS_TRANSPORT_LEMMA_PROVED = true | TYPE-P |
  premises: DoR-008 and interface predicates T1-T2

P7_CLASS_TRANSPORT_PREMISES_INSTANTIATED = false | TYPE-U |
  would-build: P5's RetHess_phys class/injective representation and P6's
               physical rho_H,N with the T2 intertwiner certificate
```

### 5.3 Category boundary

The lemma cannot be transported to the bidual by vocabulary. Q-247 exhibits

```text
Tail_B**=intersection_N ker((r_N^B)**)=z_tail B** != {0}.
```

Thus class membership and the T2 intertwiner are load-bearing. A future
producer admitting weak-star/bidual content must instead prove an output-side
tail theorem or a different separation theorem.

## 6. T5 conditional composition theorem

### 6.1 Fixed interface predicates

Define the following predicates without supplying any germ field:

```text
C1 SOURCE-CORE
   P2 supplies a physical source topology/domain in which the union of finite
   source images is dense or a core, with the admitted derivatives closable.

C2 RESPONSE-CLASS
   P5 supplies RetHess_phys and the injective class embeddings T1.

C3 PHYSICAL-RESTRICTIONS
   P6 supplies rho_G,N and rho_H,N, with T2 and agreement with the sequential
   maps wherever both are defined.

C4 PACKAGE-PRESERVATION
   P3-P6 restrictions preserve the same state, measure, contour,
   i-epsilon/analytic prescription, contacts, boundary data, endpoint domains,
   history direction, and ordering.

C5 EXTRACTION-CHAIN NATURALITY
   P4-P6 supply restrictions on the intermediate inverse/contour-Hessian
   classes for which all of the following independently testable identities
   hold on their admitted domains:

   (a) rho_I,N(I_C[G]) = I_C^fin[rho_G,N(G)];
   (b) rho_C,N(i hbar I) = i hbar rho_I,N(I), with the same compound-index,
       branch-metric, measure, and Keldysh conventions;
   (c) rho_H,N(P_R(H_C)) = P_R^fin(rho_C,N(H_C));
   (d) when the stationary 2PI Schur reduction is used, restriction preserves
       its R=0 surface, tangent blocks, inverse block, and Schur composition.

   Here E_R=P_R compose (i hbar I_C), with the stationary Schur relation as
   its independently checked consistency condition; the finite maps use the
   same ordered construction.

C6 FINITE-BOTTOM-LEG
   Restricted finite Z/W yields finite raw G with the same source convention;
   E_R^fin is independently fixed; and the predeclared contraction is taken
   in the same state and history direction as the independently derived finite
   Duhamel diagnostic.
```

These are restatements of Q-245:509-520 and raw-map T5:960-996, not authored
choices.

### 6.2 Commuting-square proof conditional on C3-C5

For any complete raw correlator `G` in the domain of `E_R`, C5(a) gives the
restricted physical inverse, C5(b) transports the `i hbar` contour Hessian
with its index/branch conventions, and C5(c) transports the ordered retarded
projection. Composing those three independently testable equalities gives:

```text
rho_H,N(E_R(G)) = E_R^fin(rho_G,N(G)).
```

Therefore the upper T5 square commutes for every `N`. If the stationary 2PI
route is used, C5(d) proves that the inverse-Hessian and Schur-complement
descriptions remain the same after restriction. C4 makes the equality a
comparison of the same physical data rather than two representationally
similar but physically different problems.

This is not circular normalization and does not assume the T5 square. Each C5
subidentity can fail separately: inversion can fail to restrict, index/measure
conversion can drift, the ordered boundary-value projection can fail to
commute, or the stationary Schur data can differ. A future producer must
derive all applicable subidentities arrow-by-arrow; it may not declare them
true merely because T5 requests their composite.

```text
P7_T5_UPPER_SQUARE_THEOREM_PROVED = true | TYPE-P |
  premises: interface predicates C3-C5

P7_T5_UPPER_SQUARE_PREMISES_INSTANTIATED = false | TYPE-U |
  would-build: P3-P6 and the arrow-by-arrow C5 certificate
```

### 6.3 Full T5 theorem conditional on C1-C6

Assume C1-C6. Then:

1. C1 makes the complete derivatives and their finite restrictions
   domain-comparable.
2. C2-C3 and the Section 5 lemma prove `Tail_R={0}`.
3. C4 ensures the upper and lower rows use identical physical data.
4. C5 proves the upper square commutes.
5. C6 supplies the independently fixed lower row and diagnostic contraction.

Consequently T5 is executable. Equality is a passing certificate; a mismatch
on the matched data is a `TYPE-R` failure against the proposed physical
completion or extraction package.

```text
P7_CONDITIONAL_COMPOSITION_THEOREM_PROVED = true | TYPE-P |
  premises: DoR-008, DoR-009, DoR-011 and C1-C6

P7_CONDITIONAL_COMPOSITION_THEOREM_EXECUTED_ON_PHYSICAL_OBJECTS = false |
  TYPE-U |
  would-build: instantiated C1-C6
```

### 6.4 What germ ratification would and would not do

A ratified P1 germ could supply the common-origin scalar functional and source
ports needed to begin P2-P6. It would not, by itself, supply:

```text
topology_src or Diff_src (P2);
physical quotient/measure/contour/boundary/domain data (P3);
PhysicalLogGerm differentiability (P4);
RetHess_phys and E_R (P5);
rho_G,N/rho_H,N and both comparison directions (P6);
the independent finite T5 bottom leg (C6).
```

Therefore the lawful instant-discharge statement is narrower:

> Once a ratified germ has been propagated through certified P2-P6 producers
> satisfying C1-C6, this artifact supplies the remaining separation and
> composition proof; no new P7 theorem is needed.

```text
GERM_PLUS_CERTIFIED_P2_TO_P6_DISCHARGES_P7_BY_COMPOSITION = true | TYPE-P |
  premises: C1-C6

GERM_RATIFICATION_WITHOUT_P2_TO_P6_DISCHARGES_P7 = false | TYPE-R |
  test: Q-254 dependency table and the uninstantiated C1-C6 fields
```

## 7. Optionality adjudication

Q-254:487-489 states:

> P7 becomes moot for one chosen output only when that output independently
> proves factorization through `R_phys_class/Tail_R`.

Q-250 proves the quotient criterion but records no complete consumer
factorization. Q-255 then checks the most promising local consumer and finds
that `p_loc`'s consumption is untyped across the sealed sources. Its two
candidate authored forms are downstream P9 material, not current facts.

Therefore:

```text
P7_OUTPUT_SIDE_ROUTE_DISCHARGED = false | TYPE-U |
  would-build: one selected P9, P10, or P11 consumer with an independently
               proved factorization through R_phys_class/Tail_R

P7_OBJECT_SIDE_ROUTE_DISCHARGED = false | TYPE-U |
  would-build: instantiated C1-C5 and the Section 5 transport premises

P7_IS_CURRENTLY_MOOT = false | TYPE-R |
  test: neither discharge route is complete
```

## 8. Germ-body residue and exact next interfaces

The germ-independent work leaves the following relay-sized producer receipts:

| Producer | Required P7 receipt | Failure-capable test |
|---|---|---|
| P2 | `SOURCE_CORE_CERT`: finite source images are dense/core; derivatives closable | exhibit a physical source direction outside the closure/core or a nonclosable admitted derivative |
| P3 | `PACKAGE_PRESERVATION_CERT`: quotient, measure, contour, contacts, boundary and domains restrict identically | matched finite and complete data disagree under restriction |
| P5 | `RESPONSE_CLASS_CERT`: injective embedding of `RetHess_phys` into a separated class | nonzero physical operator maps to zero or class closure exits the stated category |
| P6 | `PHYSICAL_RESTRICTION_CERT`: T2 intertwiner for every N and agreement of comparison directions | named square fails on one admitted object |
| P4-P6 | `EXTRACTION_NATURALITY_CERT`: inverse and retarded extraction commute with restriction | same raw G gives different restricted Hessians |
| finite producer | `T5_BOTTOM_CERT`: finite Z/W -> finite G, independent E_R^fin, matched contraction | finite contraction differs on identical carrier/state/history/order/boundary data |

This list is the germ-interface contract. It does not choose the future germ or
define any response object to make a later test pass.

## 9. Kill-passes

### 9.1 No proxy restriction promoted

`r_N^B`, `q_N`, and `Pi_N` remain canonical sequential-cylinder maps. Q-238's
`PathCert_N^dyn` remains a finite lower-row dynamics subtrace. None is called
physical `rho_G,N/rho_H,N`.

```text
PROXY_RESTRICTION_PROMOTED = false | TYPE-R |
  test: every theorem states its exact domain and every physical transport is
        conditional on T1-T2/C1-C6
```

### 9.2 No dense-subspace weakening

Separation is proved on the full ratified norm C-star algebra, full standard
module, and full represented left-multiplier class. Density is used to prove
equality of continuous elements, not to replace the theorem's domain.

### 9.3 No topology switch

The norm/module theorems and Q-247 bidual counterexample remain separate. No
weak-star tail is treated as a norm element or physical response.

### 9.4 No germ authoring

No source functional, state, measure, contour, background, response class,
consumer, or value-path term is proposed. C1-C6 are interface predicates
quoted from the governing P7/T5 specifications.

### 9.5 No circular square

The conditional equality in Section 6 is not entered as a certificate. A
future producer must derive C5 arrow-by-arrow from the actual inversion and
retarded extraction. Assuming T5 to prove T5 is expressly rejected.

### 9.6 No alternative route claimed

No output-side consumer factorization is executed. Q-255 is carried at its
actual untyped-consumption standing.

```text
FENCE_BLOCKED_STRUCTURAL_RESULT = false | TYPE-R |
  test: every structural proof and conditional composition permitted by
        DoR-011 was executed without a fence stop
MACHINERY_APPEAL_TRIGGERED = false | TYPE-R |
  test: no requested structural result was withheld by a process rule
```

## 10. Typed result ledger

| Result | Type | Standing |
|---|---|---|
| Sequential retractions/naturality | `TYPE-P` | proved under DoR-008 |
| Finite-cylinder density | `TYPE-P` | proved under DoR-008 |
| Norm C-star separation | `TYPE-P` | proved under DoR-008 |
| Standard-module separation | `TYPE-P` | proved under DoR-008 |
| Left-multiplier separation | `TYPE-P` | proved under DoR-008 |
| Finite PathCert N<=M compatibility | `TYPE-P` | proved under DoR-008/009 |
| Physical object-side P7 | `TYPE-U` | C1-C5 not instantiated |
| Full T5 execution | `TYPE-U` | C1-C6 not instantiated |
| Output-side P7 | `TYPE-U` | no selected consumer factorization |
| P7 optionality now applies | `TYPE-R` | neither lawful trigger exists |
| P1 alone discharges P7 | `TYPE-R` | contradicted by Q-254 dependency order |
| Physical `Tail_R={0}` | `NO_VERDICT` | physical class/maps absent |
| Physical `Tail_R!=0` | `NO_VERDICT` | physical class/maps absent |

Only `TYPE-R` rows carry refutational content. `TYPE-U` rows are construction
debts, not physical no-go results.

## 11. Answers in relay order

1. **Full P7 content.** P7 is the finite-core/density, class
   continuity/closability, separation transport, preservation, and T5
   commuting-square certificate. T5 compares complete and independently
   derived finite raw correlator/retarded-Hessian paths and a predeclared
   finite diagnostic contraction. P7 discharges object-side by `Tail_R={0}` or
   output-side by consumer factorization through the tail quotient.
2. **Partition.** Six finite/core claims are germ-independent and proved;
   physical core transport, class typing, and restriction identification are
   germ-interface conditionals; package preservation, extraction naturality,
   and the matched finite bottom leg need actual germ-body/downstream content.
3. **Proof now.** The canonical maps are natural, finite cylinder spectra are
   dense, and the full ratified norm algebra/module/left-multiplier classes are
   separated. Q-238 supplies compatible finite lower-row paths. No physical
   map is promoted from those results.
4. **Conditional composition.** T1-T2 transport separation to
   `Tail_R={0}`. C1-C6 then make T5 executable and prove the square by
   naturality. Once a ratified germ has propagated through certified P2-P6,
   no new P7 theorem remains.
5. **Optionality and kill-passes.** The optionality trigger does not apply;
   no selected output factors through the quotient. The proof authors no germ,
   assumes no physical class, changes no topology, and executes no alternative
   discharge route.

## 12. Custody and terminal fences

This lane writes and seals this append-only artifact, verifies its sidecar,
mirrors only the artifact and sidecar, reports, and stops. It edits no register,
Decision of Record, prior artifact, baseline, or task list, and runs no git,
commit, push, corpus gate, response evaluation, or measured comparison.

```text
alpha_computed = false [TERMINAL_FENCE_DECLARATION]
proof_authorized = false [TERMINAL_FENCE_DECLARATION]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION]
coupling_evaluation_authorized = false [TERMINAL_FENCE_DECLARATION]
production_authorized = false [TERMINAL_FENCE_DECLARATION]

PHYSICAL_RESPONSE_EVALUATED = false | TYPE-S | scope: this artifact
ROOT_SOLVED = false | TYPE-S | scope: this artifact
NUMERICAL_VALUE_PRODUCED = false | TYPE-S | scope: this artifact
MEASURED_CONSTANT_COMPARED = false | TYPE-S | scope: this artifact
GERM_AUTHORED = false | TYPE-S | scope: this artifact
REGISTER_EDITED = false | TYPE-S | scope: custody
GIT_COMMAND_RUN = false | TYPE-S | scope: custody
```
