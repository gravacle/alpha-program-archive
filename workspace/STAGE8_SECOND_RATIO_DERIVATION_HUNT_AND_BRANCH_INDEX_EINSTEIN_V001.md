# STAGE8_SECOND_RATIO_DERIVATION_HUNT_AND_BRANCH_INDEX_EINSTEIN_V001

LANE: EINSTEIN
RELAY: 233 ("THE THIRD EQUATION: WHAT DERIVES `ell_P/(c Delta tau)`?")
DATE: 2026-07-31
ROAD: BEARS ON STEP 2 — AND MOVES AN OBSTRUCTION UPSTREAM OF IT.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.
**NOTHING WAS SOLVED. No numerical value for `rho`, `beta`, `ell_P`, `kappa5`, `R0` or the ratio.**
Every relation below is symbolic. No measured-constant comparison. **The Misner-Sharp / Brown-York
fork is declared where a candidate consumes it and resolved nowhere.** `a32_holdout/custodian_private/`
untouched. Refutations reported, never repaired. Read-only.

METHOD: four establishing tasks (candidate survey · the `R0`/branch-freeness structural core ·
parameter count · obstruction fates), each adversarially attacked with both failure modes policed
(manufacture *and* withholding). Run `wf_9373db38-4d7`, eight agents. One attack CONFIRMED, three
WEAKENED. *** THE ATTACKS CORRECTED THIS LANE IN BOTH DIRECTIONS. Two of my own sealed findings are
refuted below, one of my own pre-workflow hazards is refuted, and one correction runs against the
relay brief's own starting point. *** Every decisive line was re-read by me at source before use.

---

## 0. LEAD

*** NOTHING IN ANY ROOT DERIVES `ell_P/(c Delta tau)`. AND THE STRONGER ANSWER, WHICH IS THE ONE THAT
MATTERS: IT CANNOT BE BRANCH-FREE. `ell_P` IS BRANCH-INDEXED BY ITS OWN DEFINITION, SO THE RATIO
INHERITS THE INDEX, AND OBSTRUCTION 1 IS NOT A DEFECT STANDING BESIDE THE THIRD EQUATION — IT IS
INSIDE THE THIRD EQUATION'S OWN VARIABLE. ***

So the relay's third symmetric lead fires, and I take it plainly: **`rho^2 = 16 pi K_*` is a true
relation the program cannot use**, and knowing that before more is built on it is the deliverable.

**THE GROUND IS ONE SEALED LINE, NOT AN INFERENCE.**
`STAGE8_KAPPA5_ELL_P_FUNDAMENTAL_FORK_DETERMINATION_V001.md:199-201`:

> "If `kappa5` is fixed under `R -> beta R`, then `ell_P` is not constant. It is defined from
> `kappa5` and the fiber extent, so changing the reference fiber extent changes the reduced
> four-dimensional coefficient."

On the other law `ell_P` is fixed by construction. `c Delta tau` is inert under the degeneracy map by
seal (`STAGE8_JOINT_SADDLE_SYSTEM_POSING_EINSTEIN_V001.md:46`). **Two held-fixed laws, two different
transport weights for one ratio**, and `:290`: *"There is no theorem that chooses between the two
held-fixed laws."* No `R0` adjudication is required to reach this — see §4, where I report that my own
pre-workflow hazard was both unnecessary and partly wrong.

**AND THE CORPUS HAD ALREADY WRITTEN THE ANSWER DOWN, IN A LIST OF CASUALTIES.**
`STAGE8_HELD_FIXED_BRANCH_DERIVABILITY_AND_AXIOM_PRICE_V001.md:270-278`, under *"What must be retyped
branch-conditional"* if the `kappa5`-fixed law is adopted, lists as its last item, verbatim:
*"inside/outside consistency conditions consuming `ell_P/(c Delta tau)`"*. The object this relay asks
for a branch-free equation in **is already typed branch-conditional**.

**THE ONE POSITIVE RESULT, AND IT IS A MERGER, NOT A CLOSURE.** Any candidate third relation is one of
exactly two kinds, and there is no third kind:

```text
(i)  it is written in branch-free variables and does not mention ell_P
       -> it does not convert, and the rho-form still cannot be evaluated;
(ii) it determines ell_P/(c Delta tau) across the family
       -> it determines the parent normalization against geometric data,
          which IS the parent-normalization/frame law.
```

*** THEREFORE THE RELAY'S "SECOND DIMENSIONLESS RATIO THAT NOTHING DERIVES" AND THE CORPUS'S
"INDEPENDENT PARENT-NORMALIZATION/FRAME LAW" WOULD-BUILD ARE THE SAME MISSING OBJECT. *** Two entries
on the structural open list are one entry. And it retargets the search: the law is **parent-side**.
All seven candidates the brief names are record-side or inside-side, which is why none of them reach.

---

## 1. TASK 2.1 — WHAT COULD DERIVE IT. EACH SCORED AGAINST *THIS RATIO*, EACH TYPED

**(a) THE RECORD INTERVAL'S OWN CONSTRUCTION — DOES NOT BEAR. TYPE-U.**
`c Delta tau` is fixed by nothing: the modulus gate opens its degeneracy with *"Even if one unique base
record interval `Delta tau` is granted"* (`COUPLED_RECORD_BUNDLE_MODULUS_GATE_V002.md:54-57`) — granted,
not derived. **Relative to what?** Only up to the scale orbit: *"The allow/require rule fixes the first
completed action interval, not an absolute clock duration"*
(`BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md:55`), with
`absolute_SI_record_duration_derived = false` (`:88`). And that machinery's object is `T_R`, not
`Delta tau`: the identification `Delta tau === T_R` *"was explicitly refused with a bounded negative
behind the refusal"* (`STAGE8_JOINT_SADDLE_SYSTEM_POSING_EINSTEIN_V001.md:199`).

**(b) DURABILITY / THRESHOLDED DURABILITY — DOES NOT BEAR. TYPE-U, AND SELF-DECLARED SCALE-FREE.**
The closure criterion imports no magnitude by construction — *"no absolute value is imported here"*
(`alpha-program-archive/cleanroom_output/11_DURABILITY_CLOSURE_PRINCIPLE_V001.md:44-45`), and it *"does
not by itself fix the magnitude"* (`:59`). The thresholded form was swept for `R`-scaling and returned
empty: `thresholded_durability_potential_derived = false | TYPE-U`
(`STAGE8_RADION_POSITIVE_POWER_SURFACE_SPECTRUM_TERM_V001.md:96`). No length, no ratio, no weight.

**(c) THE MARGINAL CLOSURE CONDITION `C_R = 1` — DOES NOT BEAR ON *THIS* RATIO. TYPE-C + TYPE-U.**
*** SILENT FORK DECLARED. *** It is genuinely the program's only absolute-scale selector
(`STAGE8_BOUNDARY_LEAF_TYPING_AXIOMS_VS_DEBTS_EINSTEIN_V001.md:76-79`), and it does produce a
Planck-to-interval ratio — **but the wrong one**: its object is `T_R/t_P`
(`BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:86, :123`), and `Delta tau === T_R` is refused
(above). It also cannot select alone: *"`C_R = 1` is a CONDITIONAL selector — selects only after an
unbuilt source-record-gravity"* Hamilton-Jacobi bridge
(`STAGE8_OVERDETERMINATION_SPECIFICATION_AND_PAIR_SURVEY_EINSTEIN_V001.md:247`).
*** DECLARATION: this candidate is ENERGY-CONSUMING BY NECESSITY
(`STAGE8_MARGINALITY_ENERGY_PHRASING_ADJUDICATION_EINSTEIN_V001.md:131-134`), and the two quasilocal
energies split its root by `sqrt(2)` (`:47-48`). ANY ROUTE THROUGH `C_R = 1` IS A MISNER-SHARP /
BROWN-YORK FORK CHOICE. I DECLARE IT AND DO NOT RESOLVE IT. ***
**AND A WORSE FACT ABOUT IT, WHICH BELONGS IN §5:** reaching a *Planck* ratio at all requires
`ell_P === l_P` — which **is** the reduced-4d-gravity matching, i.e. obstruction 3. The best available
candidate does not merely leave obstruction 3 standing; it **depends on** it.

**(d) THE SPECTRAL FLOOR `N_BR(k_R) = 1` AND `k_R` — CIRCULARITY CHECKED EXPLICITLY, AS INSTRUCTED.**
**The check returns: NOT CIRCULAR BY TYPE — a simultaneous system, not a loop.** `k_R`'s own pin
consumes nothing downstream: *"an intrinsic spectral-counting condition on `D_BR` containing no tau"*,
*"a physical constraint, not a definition of units"*
(`STAGE8_ROUTE_TYPING_AND_TAU_RESOLUTION_EINSTEIN_V001.md:61-64`). Nothing in it consumes `K_*`, alpha,
or a coupling. **But the route is unavailable on two independent grounds:** the pin's landing is itself
unbuilt (`:69-73`), and the bridge from `k_R` to any record interval is already **TYPE-R refuted** —
*"DIFFERENT OBJECTS, NO BRIDGE"* (`alpha_supervision/BOHM_GR_EM_RELATION_REFUTED_2026-07-29.md:52`),
and separately barred by the live non-collapse binding on `x`/`rho`/`T_R`
(`STAGE8_GRAVITY_EVIDENCE_ADMISSION_AND_FIVE_STRATA_BINDING_V001.md:138-144`).
**Q-69 held:** `T_R_and_k_R_have_derived_bridge = false` is kept distinct from the object that would
discharge it. **And the identity question stays open, not closed:** `condition_is_an_identity =
NO_VERDICT | TYPE-U`, blocker the `k_R` bridge — a `k_R` route is *neither* demonstrably circular *nor*
demonstrably clear.

**(e) THE `A_4` LOGARITHM — DOES NOT BEAR; ITS OBLIGATION IS DISCHARGED INTERNALLY. TYPE-U.**
The dependence is real — *"the closure residual is built from an action carrying the `A_4` log, so
`k_R` -- the floor -- appears in the equation that fixes `K_*`"*
(`STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md:66-67`). **But the reference scale it needs is its own
floor**, absorbed by definition through the dimensionless proper time `tau = s k_R^2`. It is not
`ell_P` and not `c Delta tau`. *The corpus does carry an obligation of this shape under another name* —
`F(T_R/t_P, dimensionless branch data) = 0`
(`BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md:66-79`) — but it belongs to a different
interval, it is an obligation rather than a derivation
(`coupled_gravity_record_stationarity_equation_derived = false`, `:90`), and **it is itself
branch-indexed: the corpus wrote "dimensionless branch data" *inside* its own closure condition.**

**(f) `tau_R = pi/sqrt(2)` AND THE PROPER-TIME FLOOR `s = 1/k_R^2` — CANNOT BEAR, STRUCTURALLY. TYPE-U.**
Both are **invariant along** the scale orbit (`first_opening_data_break_scale_orbit = false`,
`BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md:87`). A quantity constant along the orbit
cannot fix a ratio that moves along it; any equation it supplies is automatically satisfied there —
rank-deficient, not merely unhelpful.

**(g) A PARENT-SIDE LENGTH INDEPENDENT OF `R0` — ONE EXISTS, AND IT RENAMES THE GAP RATHER THAN FILLING
IT.** Under the units convention that makes `ell_P` a length (see §4.3), `kappa5^(2/3)` is a length
built from the parent normalization alone. **But nothing ties it to `c Delta tau`**, and the single
dimensionless combination it forms with the fiber radius is exactly the unknown that would *replace*
`u := (c Delta tau)/ell_P` — a change of letters, not an added equation. The parent action's whole
dimensionful content is `kappa5`, whose *"numerical value and relation to any microscopic theory are
ABSENT"* (`STAGE8_PARENT_KK_ACTION_ELL_P_DEFINITION_AND_REDUCTION_V001.md:26-27`). `TYPE-S` on any
sealed relation between a parent-side length and the record interval.

**SCOPE OF THE SURVEY'S NEGATIVE (checked before asserting).** Four roots: the archive workspace;
`alpha_supervision`; the cleanroom v003; and `alpha-program-archive/cleanroom_output` — **87 files,
outside the `workspace/` subtree, sidecars `NN_NAME_V001.seal.sha256` with no `.md` before `.seal`**.
Exclusions: `a32_holdout/custodian_private/` (never entered), `.git`, `.proof_deps`. Word-boundaried,
case-insensitive. **I re-ran the `cleanroom_output` sweep myself: zero hits for `ell_P`, `t_P`,
`Planck`, `kappa5`, `Kaluza`, `radion` across all 87 files — that root bears on this question not at
all.** Every occurrence of the ratio anywhere is an artifact **naming it as absent**
(`STAGE8_INSIDE_OUTSIDE_CONSISTENCY_CONDITION_EINSTEIN_V001.md:43, :115, :209`;
`STAGE8_HELD_FIXED_BRANCH_DERIVABILITY_AND_AXIOM_PRICE_V001.md:278`;
`alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:5922`).

---

## 2. TASK 2.2 — THE RELATION. **NONE IS DERIVED.** WHAT IS WRITTEN, AND NOT SOLVED

```text
K_* = K_KK   ->   rho^2 = 16 pi K_* ,   rho = R_*/ell_P     [fork-free; NOT branch-free — see §6]
beta = rho * ell_P/(c Delta tau)                            [SECOND RATIO: ABSENT]
ell_P^2 := kappa5^2/(2 pi R0)                               [DEFINITION/CONVENTION, PARENT_KK:35]
```

**THE ONE CORPUS OBJECT OF THE RIGHT SHAPE IS A RESTATEMENT, NOT A SUPPLEMENT.** Register Q-11's
`beta^2 = 16 pi ell_P^2 K_*/(c^2 Delta_tau^2)` (`alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:346-353`)
is the **composition of the two equations already counted**. The register itself types it PART-PROVABLE,
needing `Delta_tau = T_K` — which is the cross-sector spec's own falsifier F2 — and yielding a curve
rather than a point. It adds no information. *Reported because it is the nearest object and would
otherwise be mistaken for the third equation.*

**NO SUBSTITUTED FORM IS OFFERED HERE.** A workflow agent produced one by substituting the parent
definition into the gate's `rho`; **its own attacker showed the resulting numeral was unlicensed, and
§6.1 shows why the substitution is not licensed at all.** I write no such form. **No square root is
taken; no factor is evaluated; no value is produced.**

---

## 3. TASK 2.3 — THE PARAMETER COUNT, RUN EXPLICITLY

**BASELINE (sealed):** three dimensionless unknowns — `beta`, `u := (c Delta tau)/ell_P`, `K_*` —
against two equations, the condition itself plus `C_record(K_*) = 0`
(`STAGE8_INSIDE_OUTSIDE_CONSISTENCY_CONDITION_EINSTEIN_V001.md:141-142`). **One short.**

**WITH A HYPOTHETICAL THIRD RELATION FIXING `u`: three unknowns, three equations. *THE COUNT CLOSES.***
**I say so rather than manufacture a count failure I do not have.** The brief's test — *"a relation that
adds an unknown along with an equation has bought nothing"* — is passed: a relation fixing `u` adds no
unknown.

**BUT THE SOLUTION IS NOT A POINT.** `u` carries a branch-dependent transport weight (§0, §4), so the
solution set is a **branch-indexed family** with no theorem selecting between its members. The bar
(Q-65/Q-76 as corrected by `PRINCIPAL_AMENDMENT_002`) requires **existence and uniqueness both
derived**; **uniqueness fails on the branch index.** *** PERMISSIONS NEVER FORCE. The condition TOUCHES
and does not CLOSE — the `boundary_access_closure_threshold_principle_v001` failure mode, reproduced a
third time. I report that as the precedent repeated, not as progress. ***

**AND EACH OF OBSTRUCTIONS 2 AND 3 IS SUFFICIENT ALONE REGARDLESS OF THE COUNT.** No candidate in §1
reduces the count: (a),(b),(e),(g) supply no equation; (c),(d) each supply one equation in a *new*
unknown in a disjoint namespace; (f) supplies only orbit-invariant relations, which are rank-deficient.

**THE COUNT IS ROBUST AT THREE — AND THAT REFUTES MY OWN PRE-WORKFLOW HAZARD.** I entered this relay
suspecting the count could **rise to four** if the reference radius `R0` were independent of the saddle
radius `R_*`. Sealed text excludes that on both live branches: on the `kappa5`-fixed law the denominator
is *"itself tied to the numerator through the parent reduction"*
(`STAGE8_KAPPA5_ELL_P_FUNDAMENTAL_FORK_DETERMINATION_V001.md:231-232`); on the `ell_P`-fixed law
*"`rho = R_*/ell_P` remains a ratio to a fixed denominator"*
(`STAGE8_HELD_FIXED_BRANCH_DERIVABILITY_AND_AXIOM_PRICE_V001.md:293`). **`R0` is not a free fourth
datum either way. My hazard is refuted; the sealed "one short" stands.** What *is* branch-indexed is
**which** three — the second variable is `u` on one branch and a parent-side ratio on the other.

---

## 4. THE STRUCTURAL CORE

### 4.1 `R0` VS `R_*` — NOT SCOPE-EMPTY. **ANSWERED ON EACH BRANCH, IN OPPOSITE DIRECTIONS**

`R_*` is **never defined in any root**; it is introduced undefined at
`COUPLED_RECORD_BUNDLE_MODULUS_GATE_V002.md:95` inside a reopen condition the same gate records as
unmet, and every other occurrence quotes that clause. `R0` is defined at exactly one site,
`STAGE8_PARENT_KK_ACTION_ELL_P_DEFINITION_AND_REDUCTION_V001.md:38`. **It would be easy, and wrong, to
call the relation between them scope-empty.** Two sealed lines are directly on point, each attached to
a branch — `FORK:231-232` and `AXIOM_PRICE:293`, quoted in §3.

*** RULING: `R0_identical_to_R_star = NO_VERDICT | BRANCH-CONDITIONAL` — **not** TYPE-S; the qualifying
list is not empty. THE CONSEQUENCE IS THE POINT: `R0`-VS-`R_*` IS NOT AN INDEPENDENT QUESTION THAT
COULD BE SETTLED FIRST AND USED TO SETTLE THE BRANCH. ITS ANSWER IS *FIXED BY* THE BRANCH. BOTH HAZARDS
COLLAPSE ONTO THE ONE UNDETERMINED FORK. ***

I do not affirm the identity (no unproved-identity transport), **and I do not read its absence as a
separation theorem** — the same error in reverse.

### 4.2 BRANCH-FREENESS — **NO**, AND THE ESCAPE HATCH IS NAMED RATHER THAN HIDDEN

The executed comparison is in §0 and rests on `FORK:199-201` plus the seal that `Delta tau` is inert
under the degeneracy map. `ell_P_held_fixed_under_R_scaling = NO_VERDICT | TYPE-U`
(`PARENT_KK:72`), and the parent artifact states the trap in its own voice: *"Defining `ell_P` cannot
itself answer this without circularity; the definition precedes the scaling test"* (`:69-70`). **That
same sentence applies one level down to anything built on `ell_P`.**

**THE NEGATIVE IS SCOPED, NOT OVER-SOLD.** A branch-free relation is not logically impossible. It exists
**iff** the relation *derives* the held-fixed law as a by-product — which is option (ii) of the §0
dichotomy, i.e. the discharge of obstruction 1. **Two sealed facts make that would-build strictly
larger than "a third equation":**

1. *** `STAGE8_JOINT_SADDLE_SYSTEM_POSING_EINSTEIN_V001.md:49-51` — MY OWN RELAY-221 LINE, READ AGAINST
   THIS QUESTION FOR THE FIRST TIME AND ADVERSE TO MY OWN CONSTRUCTION: "EVERY CONDITION OF THE POSED
   SYSTEM IS INVARIANT ON BOTH BRANCHES. THE ONE-PARAMETER FAMILY SURVIVES AT FIXED BASE METRIC AND
   FIXED `Delta tau`." Therefore **any relation derived from the posed system is invariant under the
   very map it would have to break.** THE THIRD RELATION MUST ORIGINATE OUTSIDE THE POSED SYSTEM
   ENTIRELY. ***
2. The corpus types the branch choice terminal: *"axiom-shaped: a node with no discharge route is
   terminal unless and until a derivation is supplied"* (`FORK:304-305`), with
   `held_fixed_branch_derived = false | TYPE-U | would-build: independent parent-normalization/frame
   law` (`FORK:292-295`).

**THE RULING'S EXACT FAILURE SURFACE, STATED RATHER THAN HIDDEN.** Branch-indexing is a statement about
transport along the family. A relation stated at **one pinned configuration** would never invoke the
family. **The only configuration-pinning device the corpus names is the parameter-free saddle of reopen
condition 3, and a saddle in `R` is a `delta/delta R` variation — which needs the branch.** If someone
produces a configuration pin that is *not* a variation in `R`, this ruling weakens to "branch-indexed on
the family, possibly branch-free at a point." I have not found one. *(A second, independent reason the
saddle is unavailable — it reduces to a propagation equation rather than a root condition,
`JOINT_SADDLE:147-152` — is already sealed, and it does not need the branch.)*

### 4.3 A DIMENSIONAL GAP INSIDE THE DEFINITION ITSELF — TYPE-C

`ell_P^2 := kappa5^2/(2 pi R0)` is dimensionally closed **only under an imported convention**. Reading
the parent action as displayed (`PARENT_KK:15`), `kappa5^2` carries a length dimension of three **iff
the parent action is measured in units of `hbar`**. *** THE PARENT ARTIFACT NEVER SAYS SO: I checked —
`hbar` occurs **zero** times in all 78 lines of it. *** The convention lives in the *other* artifact
(`COUPLED_RECORD_BUNDLE_MODULUS_GATE_V002.md:38` writes `S_4/hbar`), and the only corpus-wide natural-
units declaration is explicitly scoped elsewhere: *"Natural units `hbar=c=1` are used within the
displayed operator algebra"* (`PRIMITIVE_SOURCE_RECORD_CHIRAL_OPERATOR_GATE_V002.md:21`).
**Consequence:** the claim that `ell_P/(c Delta tau)` is a *dimensionless* ratio rests on transporting a
units convention between two artifacts. `ell_P_dimensionally_closed_within_its_own_artifact = false |
TYPE-C`, release: a statement of the parent action's units. *Reported, not repaired.*

---

## 5. THE THREE OBSTRUCTIONS — SCORED. **3 OF 3 SURVIVE**

**OBSTRUCTION 1 — THE HELD-FIXED BRANCH.** *** SURVIVES, AND IS PROMOTED FROM PARALLEL TO PRIOR. *** It
is not a defect sitting beside the count; it is a precondition on the *variable* the third equation
would be written in (§0 dichotomy). `held_fixed_branch_derived = false | TYPE-U` (`FORK:292-295`).
**New this relay:** the branch also silently indexes (a) *which* three unknowns the sealed count
contains, (b) the `delta/delta R` row of the joint saddle system (§6.2), and (c) the object `rho` that
the program has been calling fork-free (§6.3).

**OBSTRUCTION 2 — `k_R` UNBRIDGED INSIDE THE EQUATION FIXING `K_*`.** **SURVIVES.** The circularity
check the brief demanded returns *not circular by type* — a simultaneous system (§1(d)) — but the route
cannot supply the ratio for a different and stronger reason: the bridge is TYPE-R refuted and barred by
a live non-collapse binding. And on the §0 finding it would face obstruction 1 regardless: whatever it
supplied would still be a statement about `ell_P`. `condition_is_an_identity = NO_VERDICT | TYPE-U`
carried unchanged; Q-69 respected.

**OBSTRUCTION 3 — BOTH MATCHING MAPS UNBUILT.** **SURVIVES — AND WORSE THAN "UNTOUCHED".**
`matching_map_derived = false`; `quantum_threshold_map_derived = false` (`INSIDE_OUTSIDE:152-153`). A
relation among dimensionless ratios builds neither map. *** AND THE ONE CANDIDATE THAT REACHES AN
ABSOLUTE SCALE **DEPENDS ON** OBSTRUCTION 3: `C_R = 1` reaches a Planck ratio only through
`ell_P === l_P`, which is the reduced-4d-gravity matching itself. ***

**SCORECARD: THREE OF THREE REMAIN. AND THE COUNT OBSTRUCTION, THE ONE A THIRD EQUATION WAS SUPPOSED TO
REMOVE, IS NOT CLEANLY REMOVED EITHER — IT CLOSES ONTO A BRANCH-INDEXED SET.**

---

## 6. REFUTATIONS — REPORTED, NEVER REPAIRED. **THREE ARE AGAINST MY OWN SEALED WORK**

### 6.1 THE TWO ARTIFACTS DO NOT AGREE ON WHAT `ell_P` NAMES — **TYPE-R, EXECUTED BY ME AT SOURCE**

```text
STAGE8_PARENT_KK_ACTION_ELL_P_DEFINITION_AND_REDUCTION_V001.md:56
    S4 ⊃ (1/(2 ell_P^2)) ∫ d^4x sqrt(-g) R4[g],

COUPLED_RECORD_BUNDLE_MODULUS_GATE_V002.md:38-39
    S_4/hbar = [1/(16 pi ell_P^2)] integral sqrt(-g) [R_4 - (R^2/4) F F],
```

Two displayed four-dimensional Einstein-Hilbert coefficients, **one symbol**, differing by a factor of
`8 pi`. `rho` and `K_KK` are written in the gate's `ell_P`; the definition `ell_P^2 := kappa5^2/(2 pi
R0)` is written in the parent's. *** SO THE PARENT DEFINITION DOES NOT, AS WRITTEN, SUPPLY THE `ell_P`
THAT APPEARS IN `rho`. SUBSTITUTING ONE INTO THE OTHER IS UNPROVED-IDENTITY TRANSPORT. ***
**SCOPED HONESTLY:** this is a *transport* defect, not a physics refutation — conventions differing by a
fixed factor are not a disagreement about the world. **It does not affect §0**, because a constant
dimensionless rescaling commutes with both transport laws and cannot turn a nonzero weight into zero.
It does bear on §4.1, since the substitution H1 asks about runs exactly across this seam.
`parent_ell_P_and_gate_ell_P_are_the_same_symbol = false | TYPE-R`.

### 6.2 MY OWN RELAY-221 CLAIM IS BRANCH-CONDITIONAL — **TYPE-R, AGAINST MYSELF**

`STAGE8_JOINT_SADDLE_SYSTEM_POSING_EINSTEIN_V001.md:113` asserts the breathing mode *"enters the action
**only** through `K_KK`, the sole `R`-dependence in the sealed reduced action."* **That is true on the
`ell_P`-fixed law and false on the other**, where `PARENT_KK:49` puts the fiber radius in the
Einstein-Hilbert coefficient as well. **My saddle system's `delta/delta R` row therefore silently adopts
a branch.** `K_KK_is_the_sole_R_dependence = false as a branch-free statement | TYPE-R`. Reported, not
repaired. *(The general silent adoption is already sealed —
`ell_P_fixed_branch_adopted_by_reduced_route = true | TYPE-C`, `FORK:297-298`; what is new is its
specific bite on the saddle posing.)*

### 6.3 A CORRECTION AGAINST THE RELAY BRIEF'S OWN STARTING POINT — **FORK-FREE IS NOT BRANCH-FREE**

The brief opens with *"THE `rho`-FORM: FORK-FREE"*, which is **correct as scoped**: the `rho`-form does
not consume `Delta tau` and so does not import the Misner-Sharp / Brown-York `sqrt(2)`
(`INSIDE_OUTSIDE:186-188`). **It says nothing about the held-fixed branch.** `rho = R_*/ell_P` is listed
among the objects to be retyped branch-conditional (`AXIOM_PRICE:275`) and *"is no longer a ratio to a
fixed denominator"* on the `kappa5`-fixed law (`:267`). *** `rho` IS FORK-FREE AND BRANCH-INDEXED
SIMULTANEOUSLY. READING "the only fork-free condition the program has" AS "the only clean condition the
program has" IS THE ERROR THIS FINDING FORECLOSES. *** `rho_is_branch_free = false | TYPE-R`.

### 6.4 TWO NAMESPACE HAZARDS CARRIED FORWARD FOR WHOEVER BUILDS NEXT

- *** THE BRANCH LETTERS ARE INVERTED BETWEEN TWO SEALED ARTIFACTS. ***
  `STAGE8_HELD_FIXED_AND_FRAME_DETERMINATION_EINSTEIN_V001.md:139-146` (mine, relay 224) letters
  **"BRANCH A — `ell_P` fundamental" / "BRANCH B — parent normalization fundamental"**;
  `STAGE8_KAPPA5_ELL_P_FUNDAMENTAL_FORK_DETERMINATION_V001.md:196` letters **"Branch A: `kappa5`
  fixed"**. Any lane quoting across both files with one lettering will read backwards. **I have written
  no branch letters anywhere in this artifact — only the named laws — and I recommend that convention.**
- The `C_R` symbol collision (compactness ratio vs induced Einstein coefficient) already caused one
  refuted route (`BOHM_GR_EM_RELATION_REFUTED_2026-07-29.md`) and is still live.

---

## 7. TYPED NEGATIVES

```text
N1  second_ratio_derived_anywhere = false | TYPE-S
      scope: four roots incl. cleanroom_output (87 files, re-swept by me: zero hits on ell_P/t_P/
      Planck/kappa5/Kaluza/radion); exclusions a32_holdout/custodian_private, .git, .proof_deps;
      word-boundaried, case-insensitive. Every occurrence NAMES the ratio as absent.
N2  ell_P_over_c_Delta_tau_can_be_branch_free = false | TYPE-R, EXECUTED
      ground: FORK:199-201 + JOINT_SADDLE:46 + FORK:290.  *** ONLY TYPE-R IS PHYSICAL CONTENT;
      THIS IS THE LANE'S ONE PHYSICAL RESULT AND IT IS A NEGATIVE. ***
N3  rho_is_branch_free = false | TYPE-R  (fork-free =/= branch-free; AXIOM_PRICE:267, :275)
N4  parent_ell_P_and_gate_ell_P_are_the_same_symbol = false | TYPE-R  (PARENT_KK:56 vs V002:38-39)
N5  K_KK_is_the_sole_R_dependence = false as a branch-free statement | TYPE-R  (my JOINT_SADDLE:113)
N6  R0_identical_to_R_star = NO_VERDICT | BRANCH-CONDITIONAL  (NOT TYPE-S: FORK:231-232; AXIOM_PRICE:293)
N7  parameter_count_rises_above_three = false | TYPE-R  (refutes THIS LANE'S OWN entering hazard)
N8  ell_P_dimensionally_closed_within_its_own_artifact = false | TYPE-C
      release: a statement of the parent action's units.  hbar occurs 0x in PARENT_KK (checked).
N9  held_fixed_branch_derived = false | TYPE-U  (FORK:292-295; would-build: independent parent-
      normalization/frame law derived before any beta/rho/coupling evaluation)  *** TYPE-U, NOT
      TYPE-C — a workflow agent typed this TYPE-C and its attacker corrected it against source. ***
N10 third_relation_derivable_from_the_posed_system = false | TYPE-R  (JOINT_SADDLE:49-51: every
      condition of the posed system is invariant on both branches)
N11 T_R_and_k_R_have_derived_bridge = false | TYPE-R, carried  (BOHM_GR_EM_RELATION_REFUTED:52)
N12 k_R_pin_lands_on_the_complete_operator = false | TYPE-U  (ROUTE_TYPING:69-73)
N13 condition_is_an_identity = NO_VERDICT | TYPE-U, carried — blocker: the k_R bridge
N14 absolute_SI_record_duration_derived = false ; first_opening_data_break_scale_orbit = false | TYPE-U
N15 thresholded_durability_potential_derived = false | TYPE-U  (RADION_POSITIVE_POWER:96)
N16 C_R_1_selects_alone = false | TYPE-C  (OVERDETERMINATION:247) — *** AND ANY ROUTE THROUGH IT IS A
      DECLARED, UNRESOLVED MISNER-SHARP / BROWN-YORK FORK CHOICE ***
N17 the struck overdetermination bar still lives in STAGE8_CROSS_SECTOR_METRIC_RULE_ADMISSIBILITY_
      SPEC_V001.md at :265-267 (F3) and :292 (minimal-pass item 3) | TYPE-R, reported not repaired.
      *** NEITHER WAS APPLIED ANYWHERE ABOVE. ***
N18 step-5 route-level identity: UNDEFEATED BUT UNAFFIRMED | NO_VERDICT | TYPE-U, carried.
      The same-object ground was NOT extended to the fiber. Nothing above rests on it.
N19 new_class_required = false — existing vocabulary held throughout.
```

**HYPOTHETICAL FLAGS, MARKED INLINE WHERE USED:** `R0 = R_*` (never affirmed; §4.1); the star subscript
read as "value selected by a stationarity condition" (notational analogy, not a sealed definition);
parent-`R0` and gate-`R` as one object (transport, undefeated but unaffirmed, and damaged by §6.1); the
units convention imported to close §4.3. **Every "passes = true" downstream of any of these sits under a
would-build.**

---

## 8. WHAT WOULD ACTUALLY DISCHARGE THIS (Q-92)

**ONE OBJECT, NOT TWO.** An independent **parent-normalization / frame law**, derived before any
`beta`/`rho`/coupling evaluation, stating whether `kappa5` or `ell_P` is held fixed under `R -> beta R`
(`FORK:292-295`). By the §0 dichotomy it is **the same object** as the missing converter. Three
constraints on where it can live, all established above:

1. **It must be parent-side.** Every candidate in §1 is record-side or inside-side, and by the dichotomy
   no record-side candidate can reach `ell_P` without *becoming* this law.
2. **It must originate outside the posed saddle system**, which is invariant on both branches (N10).
3. **It cannot be read off the reduced expression.** That route is already sealed as circular:
   *"it identifies the branch currently used by the reduced expression and then treats that expression
   as proving the branch"* (`AXIOM_PRICE:236`), echoing `PARENT_KK:69-70`.

*** THE SHARPEST CONSEQUENCE, AND IT REORDERS THE WORK: THE PROGRAM HAS BEEN TREATING THE HELD-FIXED
BRANCH AND THE MISSING CONVERTER AS TWO INDEPENDENT GAPS CLOSABLE IN EITHER ORDER. THEY HAVE AN ORDER.
ANYONE WHO DERIVES `ell_P/(c Delta tau)` BEFORE THE HELD-FIXED LAW WILL HAVE DERIVED A BRANCH-INDEXED
QUANTITY AND WILL NOT KNOW IT. ***

---

## 9. DISCIPLINE

- **Q-92:** nothing bears, and what is missing is named one level deeper than the relay named it — not
  "a second ratio" but the parent-normalization law that the second ratio *is*. A complete answer.
- **Q-54:** every negative typed; `NO_VERDICT` used where the corpus is genuinely split (N6, N13, N18).
  Only TYPE-R claimed as physical content.
- **Q-69:** no flag identified with its discharge object — checked on the `k_R` bridge, the held-fixed
  branch, and the matching maps.
- **No unproved-identity transport:** `R0`/`R_*` not identified **and** their non-identity not read as a
  separation theorem; `Delta tau === T_R` and `ell_P === l_P` refused; the same-object ground not
  extended to the fiber; **and §6.1 catches a transport the program was one step from making.**
- **The bar:** corrected Q-65/Q-76 with `PRINCIPAL_AMENDMENT_002` — existence *and* uniqueness both
  derived. **Overdetermination never treated as an admission condition. F3 and minimal-pass item 3 NOT
  APPLIED** (N17).
- **Anti-manufacture, both directions.** No relation was manufactured: I declined to substitute across
  the `8 pi` seam and wrote no numeral from it, and I reported that **the count closes** rather than
  inventing a count failure that would have flattered the negative. **And none was withheld:** the
  parent-side length `kappa5^(2/3)` was surfaced at full strength before being shown to rename rather
  than fill the gap, the `C_R = 1` candidate was led at full strength before being killed on sealed
  ground, and the one genuine positive — the merger of two open items — is reported up, not talked down.
- **Fork:** declared on `C_R = 1` and on the `T_R/t_P` construction; **resolved on neither.** The §0
  ruling itself consumes no quasilocal energy — it is transport of a definition under a sealed map.
- **Q-91 custody:** no `git`; baseline untouched; no gate or deploy command; no publication
  authorization requested. Written in the cleanroom, sealed there, mirrored with sidecar to the archive
  workspace, both seals verified; report; stop.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
