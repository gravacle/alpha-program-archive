# STAGE 8 — AUDIT OF THE SUMMED MECHANISM DETERMINATION — O7SR AUDIT V001

## MECHANISM AUDITOR — CODENAME SUMMED-MECH-AUDIT — COMMISSION O7SR — [SEALED]

```text
COMMISSION      O7SR, 2026-08-15. DEFAULT-REFUTE. Testimony ZERO WEIGHT.
TARGET          STAGE8_SUMMED_MECHANISM_O7SR_V001.md (+ .seal.sha256)
STEP 0          Probe + shasum -a 256 -c FROM THE ARTIFACT'S OWN DIRECTORY:
                STAGE8_SUMMED_MECHANISM_O7SR_V001.md: OK. Output path probed
                ABSENT before any write. Proceeded.
METHOD          The same ground consumed INDEPENDENTLY. Every load-bearing
                symbolic step RE-DERIVED FROM SCRATCH in a fresh venv — the
                target's battery was NOT imported and NOT re-run as its own
                witness. Where the target's assertion EXPRESSION was in
                question, that expression was transcribed verbatim and probed
                for vacuity separately from the claim it labels.
FENCES          alpha_computed = false ; proof_authorized = false ;
                kappa_record_computed = false — ALL HELD, none approached.
MODE            DETERMINATION ONLY. No authored physics, no value, no number as
                the value of anything, no measured-constant comparison. No git.
CAS             AUDIT battery 46/46 PASS, fresh venv o7sraudit, sympy 1.14.0.
```

---

## 0. VERDICT

```text
CONFIRMED-WITH-CORRECTIONS.

  THE VERDICT ITSELF STANDS, AND ITS HARDEST PART SURVIVES THE HARDEST TEST.
  The target's MIXED determination — three of four summed decided quantities
  generic at mechanism grade on both axes; the fourth grading-dependent for its
  being a log and Clifford-dependent for its certified nonvanishing; NO gauge
  dependence anywhere — is re-derived here independently and holds at every
  load-bearing step. Seven corrections follow. NOT ONE MOVES A VERDICT.

  THE PIVOTAL STEP, TYPED AT ITS OWN BYTES AND STRENGTHENED BEYOND THE TARGET.
  The commission named joint inversion = S-conjugation as the step to break.
  It does not break. It is a GRADED-ALGEBRA fact — not a Clifford fact, not a
  group fact, not a generic algebraic identity — and this audit establishes it
  one grade harder than the target did (A5, §3).

  THE STRUCTURE-DEPENDENT CALL IS NOT INFLATED. mu_B^alpha > 0 genuinely FAILS
  without g_ab ~ delta_ab. The target ASSERTS the counterfactual bracket; this
  audit DERIVES it from the azimuthal average and adds the sharpness check the
  target omitted (A7, §4). The call is earned.

  THE ONE GENERIC CALL WHOSE COUNTERFACTUAL IS NAMED BUT NOT DISPLAYED is the
  enclosure's (C-5). It is a CORRECTION and not a kill, for a reason displayed
  in §5: the missing dial is the target's own declared NON-content datum, and
  every CONTENT substitution for that mechanism IS displayed. This audit
  supplies the missing display; the claim survives it.

  THREE OF THE THIRTY-NINE CAS CHECKS ARE VACUOUS (C-1, C-2, C-3) — one of them
  a literal tautology standing under the target's self-described "strongest
  generic step in the whole chain". Every conclusion they label is nonetheless
  TRUE, re-derived here properly or located at sealed bytes. "39/39 PASS" should
  read 36 checks doing work and 3 doing none.

  THE LOG'S NON-VANISHING IS NOT MOVED, and this audit confirms the target did
  not move it. DISPLAY's COMPLETE-LOG-GENUINE and the refuted rate stand
  untouched at bytes; the target confines itself to WARRANT and TRANSPORT.
AUDIT CAS: 46/46 PASS, single run, fresh venv.
```

---

## 1. SEALS — VERIFIED INDEPENDENTLY, FROM EACH ARTIFACT'S OWN DIRECTORY

`shasum -a 256 -c <name>.md.seal.sha256`, run from
`/Users/bgm/MB Work/alpha-program-archive/workspace`. ALL: **OK**.

```text
  TARGET
  39c6bf0f9cf061e7  STAGE8_SUMMED_MECHANISM_O7SR_V001.md              OK

  THE ELEVEN GROUNDS THE TARGET CONSUMED — every one re-verified, and every
  16-hex prefix the target DISPLAYS re-computed and MATCHED:
  6e5762a8b785f4b7  STAGE8_WALL_SUMMED_REQUANT_S9AD_V001.md          OK  match
  549f14238ec42496  STAGE8_WALL_SUMMED_REQUANT_S9AD_AUDIT_V001.md    OK  match
  172e8bbff1c9c17a  STAGE8_WALL_LOGFREE_RATE_T4SR_V001.md            OK  match
  8241cb31accb96ba  STAGE8_WALL_LOGFREE_RATE_T4SR_AUDIT_V001.md      OK  match
  11dd8ac5cc519061  STAGE8_DEG3_ANGULAR_CENSUS_T6SR_V001.md          OK  match
  8a82864adb57cb4f  STAGE8_DEG3_ANGULAR_CENSUS_T6SR_AUDIT_V001.md    OK  match
  30ee481f2af60a08  STAGE8_DEG3_DISPLAY_PAIR_T8SR_V001.md            OK  match
  3f26fbca841f8a3f  STAGE8_DEG3_DISPLAY_PAIR_T8SR_AUDIT_V001.md      OK  match
  4ae292319f9f4b60  STAGE8_EM_PARTICIPATION_O4SR_V001.md             OK  match
  f5f2467e8cbe781a  STAGE8_EM_PARTICIPATION_O4SR_AUDIT_V001.md       OK  match
  afc3a79c646c044d  STAGE8_COMPLETION_MAP_T17SR_V001.md              OK  match
```

11/11 verified, 11/11 prefixes concur. The target's §1 seal display is EXACT.
Write-order check: target `.md` mtime 13:09, sidecar 13:10, its declared cutoff
13:03:29 — cutoff precedes write, in order.

---

## 2. THE COMMISSION'S PREMISE, RE-CHECKED AT O4SR'S OWN BYTES

The target's whole warrant is that O4SR discharged FP-S on a token tally. That is
CONFIRMED at bytes, and so is the sharper claim that makes the finding possible.

```text
(v-1) THE TALLY IS THERE. O4SR :424-425 verbatim: "A full-file token tally of
      `STAGE8_DEG3_DISPLAY_PAIR_T8SR_V001.md` (seal OK) returns ZERO occurrences
      of maxwell, hodge, electromagnetic, photon, gauge, U(1), or Ward." And
      :428-429: "Under all three substitutions the branch-exchange symmetry, the
      insertion-parity grading, and a radial profile's strict monotonicity read
      identically." The target's quotation is EXACT, ellipsis honest.

(v-2) THE THREE SUBSTITUTIONS ARE THERE AND THEY ARE WHAT THE TARGET SAYS.
      O4SR :321-322 verbatim: "(s1) a different gauge group;  (s2) non-gauge
      content;  (s3) a generic symbol class." CONFIRMED.

(v-3) THE TARGET'S (p-2) IS THE LOAD-BEARING OBSERVATION AND IT IS CORRECT.
      I read O4SR's FP-S paragraph whole (:418-429). It names the swap law, the
      coupling-parity grading, "two independent exact mechanisms", b_D, beta_s
      and M_1. THE ONE-VERTEX TRACE-PARITY LEMMA IS ABSENT FROM IT, and so is
      any trace form. The three items O4SR does assert invariance for are
      exactly the three this audit independently types GENERIC. The two
      structure-carrying steps are the two O4SR never names.

(v-4) O4SR-AUDIT A-6 AT BYTES, :294: "not 9, and every one of the ten is the
      substring `outward` — not `toward` and not `forward`." The target's
      "corrects 9 to 10 and identifies every hit as the substring `outward`" is
      EXACT. O4SR-AUDIT :625 "it sits allow-side on a fact about" [vocabularies]
      — the target's quotation of its NET is accurate.
```

---

## 3. THE PIVOTAL STEP — RE-TYPED AT ITS OWN BYTES, AND STRENGTHENED

The commission ordered this step checked hardest: **is joint inversion =
S-conjugation a Clifford fact, a group fact, or a generic algebraic identity?**

**THE RECORD'S OWN BYTES, both loci.** RATE-AUDIT :44 verbatim: "joint momentum
inversion on the letter span IS conjugation by S (`S(u.alpha)S = -u.alpha`
generic, `S S S = S`, `S 1 S = 1`), so `W(-khat,-khat') = S W S` for EVERY word
of EVERY length and `tr[alpha_x W(-)] = -tr[alpha_x W]` by cyclicity +
`S alpha_x S = -alpha_x`." The target's transcription is EXACT.

**WHAT THE TARGET DID NOT DO, AND THIS AUDIT DOES.** The target typed the step on
an abstract dim-6 class. It never went back and instantiated the RECORD's own S.
WSR :58 names it at bytes: `S = -i gamma^0 gamma^5`, "anticommuting with beta".
I built that S from the Dirac basis and tested the three premises on it directly
(AUDIT A5.RECORD-S-EXISTS + `sph.py`):

```text
  g5^2 = I                          TRUE
  S^2 = I                           TRUE
  S = S^dag                         TRUE
  S alpha_a S = -alpha_a  (all a)   TRUE   <-- momentum letters AND vertex ODD
  S S S = S                         TRUE
  S 1 S = 1                         TRUE
  beta S = -S beta                  TRUE   (concurs with WSR's own clause)
```

So the lemma's three premises — (i) an inner Z2-grading, (ii) momentum odd,
(iii) vertex odd — hold ON THE RECORD'S OWN CLASS with the Clifford relation
never invoked. The target's typing is not merely defensible; it is exhibited on
the record's own involution.

**THE TYPING, DECIDED.**

```text
(t-1) NOT A GENERIC ALGEBRAIC IDENTITY. Removing the grading BREAKS it, and the
      break is displayed at the first length where parity can be tested
      nontrivially. AUDIT A5.FAIL-ungraded, built independently of the target:
        GRADED class    tr[V_x A A'] = 0 IDENTICALLY;
        UNGRADED class  tr[W_x A A'] NONZERO and JOINT-EVEN.
      A5.FAIL-even-vertex isolates premise (iii) separately: graded content,
      EVEN vertex — nonzero and joint-even again. All three premises bite.

(t-2) NOT A CLIFFORD FACT. AUDIT A5.LEMMA-holds-on-NONCLIFFORD: on a dim-6
      inner-graded class with Hermitian block-off-diagonal odd letters and the
      Clifford relation verified FALSE, all 40 words over {V.khat, V.khat', S}
      to length 3 give tr[V_x W] ODD-or-zero under the joint inversion, 16 of
      them nonzero. The target's M4 result reproduces exactly, on my own build.

(t-3) NOT A GROUP FACT EITHER, and the general half is provable rather than
      sampled. The "or-zero" half — the half that does the work at even length —
      is: tr X = tr(S X S) = tr(-X) = -tr X = 0 for any odd X. AUDIT
      A5.ODD-TRACELESS runs this on a GENERIC 6x6 matrix's odd part, not on a
      word list: it consumes S^2 = I and cyclicity and NOTHING ELSE. No group
      representation, no dimension, no anticommutation.

(t-4) THE ALL-LENGTHS HALF. A5.CONJ: S L S = L(-momentum) for a generic graded
      letter (odd part + even part, symbolic coefficients), whence W(-) = S W S
      at every length by homomorphism + S^2 = I.

TYPING CONFIRMED: GRADED-ALGEBRA FACT. The target's attribution is CORRECT, and
      it is here established on the record's own S as well as on a witness class.
      NO MIS-ATTRIBUTION FOUND IN THE PARITY CHAIN.
```

---

## 4. THE STRUCTURE-DEPENDENT CALL — DEMANDED TO FAIL, AND IT DOES

The commission's standard: a structure-dependent call inflated from a structure
merely APPEARING in a derivation is a defect; the display that the step FAILS
without it is required. Both of the target's structure-dependent calls meet it.

### 4.1 The Clifford half — the target ASSERTS its counterfactual bracket; this audit DERIVES it

The target writes down, without deriving it, that for a trace form g the record's
bracket becomes `((1-u^2)/2) g_xx + ((3u^2-1)/2) x_hat_x (g x_hat)_x`. An
asserted generalization is exactly where an inflated call would hide. It does
not hide there — the form is correct, and here is the derivation the target owed:

```text
(d-1) THE AZIMUTHAL AVERAGE AT FIXED u = khat.x_hat, exact:
        <khat_a khat_b> = u^2 x_a x_b + ((1-u^2)/2)(delta_ab - x_a x_b).
      Contracting with a SYMBOLIC trace form g (AUDIT A7.BRACKET-GENERIC-DERIVED,
      g and x_hat both fully symbolic):
        <khat_x (g khat)_x> = ((1-u^2)/2) g_xx + ((3u^2-1)/2) x_hat_x (g x_hat)_x
      IDENTICALLY. The target's counterfactual bracket is CORRECT — and it is
      now derived rather than asserted.
(d-2) THE RECORD'S CASE IS THE SAME OBJECT AT g = 4 delta (A7.BRACKET-RECORD-CASE):
      the bracket is the azimuthal average of 4 khat_x^2 — a manifest square,
      hence nonnegative. This is what "the square is manufactured by the trace
      form" means, made explicit.
(d-3) THE FAILURE, RE-DERIVED EXACTLY (A7.CF-PSD-GRAM). At the target's PSD Gram
      instance g = [[1,-7/5,0],[-7/5,5/2,0],[0,0,1]]: leading minors 1, 27/50,
      27/50 — all strictly positive, so g is genuinely positive DEFINITE, which
      is what Hermitian independent odd letters always give. At x_hat ~ (1/10,1,0)
      the cross term x_hat_x (g x_hat)_x = -13/101 STRICTLY NEGATIVE and the
      bracket at u = 1 equals -13/101 < 0. Exact rationals throughout; nothing
      evaluated. REPRODUCED EXACTLY, both numbers.
(d-4) THE SHARPNESS CHECK THE TARGET OMITTED (A7.CF-IS-SHARP): at g = 4 delta the
      SAME instance (same u, same x_hat) is STRICTLY POSITIVE. So the sign flip
      is caused by g and by nothing else in the substitution — the counterfactual
      isolates the dial it claims to isolate. This is the check that separates an
      earned structure-dependent call from an inflated one, and it passes.
(d-5) THE CALL IS CALIBRATED, NOT OVERSTATED. The target claims the integrand
      "LOSES SIGN DEFINITENESS" and that the class "loses the certified
      NONVANISHING" — NOT that the moment is negative. That distinction is
      correct and load-bearing: a non-definite integrand may still integrate
      positive. The target does not overreach, and §5.3(c-4)'s "the census would
      have to be re-run there" is the right consequence.
(d-6) THE CLIFFORD STEPS THEMSELVES, RE-DERIVED (A7.PROJ / A7.SANDWICH /
      A7.TRFORM): (alpha.khat)^2 = I on the unit sphere by exact spherical
      parametrization, FALSE on the graded non-Clifford class; P_- alpha_x P_- =
      -khat_x P_- exactly, consuming BOTH {alpha_x, alpha.khat} = 2 khat_x AND
      the square; tr[alpha_a alpha_b] = 4 delta_ab. The sandwich is at DISPLAY
      (b-7) bytes with khat_x out front, verbatim. So the khat_x carrier IS
      Clifford-manufactured, not merely Clifford-adjacent. CALL EARNED.
```

### 4.2 The grading half — the log's SHAPE

```text
(d-7) The chain is: ungraded => the traced factor acquires an even part
      (A5.FAIL-ungraded) => its first angular moment is generically nonzero
      (int_S2 1 dOmega = 4 pi against int_S2 khat_x dOmega = 0, AUDIT
      A9.MOMENTS-S2) => the degree -2 stratum survives => the carrier integral
      at degree -2 is Lambda - 1, A POWER (A4.d3, Lambda FORMAL).
      Each link displayed. The stratum's identity and its "EXACTLY ONE khat_x
      ... angular average VANISHES EXACTLY — the S1 odd-spinor-trace leg" is at
      WSR :330-333 bytes, and "the ONLY degree -2 assembled stratum" at WSR :58.
      CONFIRMED. The target's (L1-e) — "THE ADJECTIVE IN 'GENUINE LOG' IS
      GENERIC; THE NOUN IS NOT" — is sustained.
(d-8) AND THE ATTRIBUTION WITHIN IT IS RIGHT. What kills the degree -2 stratum is
      PARITY of the traced factor, which needs only the grading; that the odd
      coefficient happens to be -khat_x C(k) is the Clifford-supplied SHAPE, not
      the killing mechanism. Assigning the kill to link 1 and the carrier to
      link 3 is the correct split, not a convenient one.
```

---

## 5. THE HUNT FOR A VOCABULARY-GROUNDED GENERIC CALL

The defect this commission exists to repair. Every generic call in the target
was put to the standard: **display the counterfactual, or be killed.**

```text
GENERIC CALL                          COUNTERFACTUAL DISPLAYED?      DISPOSITION
--------------------------------------------------------------------------------
M1  tr H_A = 0, ||H_A||^2 = 0         YES — asymmetric pairing       CLEAN
      weight breaks Hermiticity (M1.CF; my A2.CF-asym-weight concurs)
M2  the per-factor odd-kill           YES — TWO: branch-dependent    CLEAN
      slot operators (m^3 revives) and asymmetric branch values
      (0,s,-2s) (my A3.CF1/A3.CF2 concur; m_1 = sqrt(2)/4, m_3 = 7 sqrt(2)/2)
M3  the carrier-free enclosure        NAMED, NOT DISPLAYED           C-5
LINK 2  class-A khat-freeness         YES — anisotropy defeats it    CLEAN
      with first moment (4 pi/3) c (my A9.ANISOTROPY-DEFEAT concurs)
LINK 3(A)  the strict sign M_1 < 0    VACUOUS BY CONSTRUCTION        CLEAN (see below)
M8  the gauge axis                    YES — U(N) as multiplicity     CLEAN
      passes the sandwich and the trace form (4N delta) unchanged
```

**Why LINK 3(A) is clean though it displays no counterfactual.** The target
argues the strict sign is content-free "because none of those objects occurs in
it". That is an argument from ABSENCE — the very shape of the defect under hunt.
It survives, and the distinction is worth stating precisely: a token tally reads
the TEXT and infers about the derivation; here the DERIVATION IS DISPLAYED WHOLE
(a polynomial identity in (t, r) and a monotone-schema integral) and one can read
off that no matrix, index or representation enters it at any step. Absence
established by exhibiting the whole derivation is mechanism-grade. Absence
established by counting words in a file is not. The target is on the right side
of that line — though see C-1 for the check that was supposed to certify it.

**C-5 — THE ONE GENERIC CALL WHOSE COUNTERFACTUAL IS ASSERTED RATHER THAN SHOWN.**

```text
THE DEFECT. §3.3 VERDICT M3 states "the only non-generic inputs are the weight
stencil's m_0 = 0 and the CARRIER DIMENSION d = 3" and "the one substitution that
WOULD move it is a change of CARRIER DIMENSION". The battery never varies it:
`d_carrier = 3` is HARDCODED and M3.THRESH runs three degrees at that one
dimension. The §7 group table compounds it, describing the check as "the
threshold arithmetic at carrier dimension d (Lambda FORMAL)" — which reads as d
FORMAL when d is a literal. A load-bearing input is declared load-bearing
without the display that removing it breaks anything.

THE MISSING DISPLAY, SUPPLIED (AUDIT A4.GENERAL-d / A4.CF-CARRIER-DIM, d SYMBOLIC):
  int_1^Lambda k^{d-1+p} dk = (Lambda^{d+p} - 1)/(d + p)   for d + p != 0,
  convergent as Lambda -> oo iff d + p < 0. At the HS degree p = -4:
    d = 3  ->  1 - 1/Lambda   BOUNDED   (the record's case; threshold crossed)
    d = 4  ->  log Lambda     THE THRESHOLD IS LOST
    d = 5  ->  Lambda - 1     a POWER
So d = 3 IS load-bearing, the counterfactual EXISTS, and it is displayable in one
line. THE TARGET'S CLAIM SURVIVES ITS OWN MISSING DISPLAY.

WHY THIS IS A CORRECTION AND NOT A KILL. The commission's kill condition is a
GENERIC call resting on VOCABULARY. M3's does not: every CONTENT substitution is
displayed and does work — HS Pythagoras at dims 1, 2, 5 on generic complex
operators (my A4.PYTH.dim1/2/5), the Cauchy slot extraction as an exact contour
identity with no content datum in it (my A4.CAUCHY), the internal factor via M8.
The undisplayed dial is CARRIER DIMENSION, which the target itself declares a
GEOMETRY datum, non-content, and outside the commission's axes — it is disclosed
as out of scope, not passed off as tested. The gap is in the exhibit, not in the
warrant. CORRECTION.

A NOTE ATTACHED TO THE SAME ROW. §5.1's verdict table files "DIMENSION ONLY"
under the column REP-THEORETIC DEPENDENCE. The body is unambiguous that this
means the CARRIER dimension, a geometry datum — but a geometry datum sitting in
the representation column invites exactly the conflation this commission is
about. The cell should read CARRIER-DIMENSION ONLY (GEOMETRY, NOT CONTENT).
```

**NO KILL FOUND.** No generic classification in the target rests on vocabulary.

---

## 6. CORRECTIONS

### C-1 — A VACUOUS CHECK UNDER THE CHAIN'S SELF-DECLARED STRONGEST STEP

```text
M7.MONO's assertion expression, transcribed VERBATIM from the sealed battery and
probed independently (AUDIT A6.MONO-IS-A-TAUTOLOGY), reduces to X == X:

  LHS = diff(s_prod_r2, r2) + (s_m.subs(r_r**2, r2) + s_p.subs(r_r**2, r2))
                              .subs(r2, r2).subs(r_r, 0) + 0
  RHS = diff(s_prod_r2, r2) + ((t_r**2 - r2) + ((1 - t_r)**2 - r2))

The two `.subs` calls after the first are no-ops (no r2-for-r2 change; no r_r
survives the first substitution), so LHS and RHS are the SAME EXPRESSION. The
check would PASS IDENTICALLY EVEN IF ds/d(r^2) = -(s_- + s_+) WERE FALSE. It
tests nothing.

WHAT IT WAS SUPPOSED TO TEST IS TRUE. Re-derived properly
(A6.MONO-CLAIM-IS-TRUE): diff((t^2 - r2)((1-t)^2 - r2), r2) + ((t^2 - r2) +
((1-t)^2 - r2)) == 0 EXACTLY. So ds/d(r^2) = -(s_- + s_+) HOLDS. It is also at
DISPLAY :54 bytes verbatim, of record. NOTHING MOVES — but the target's §0 calls
the M_1 sign "the strongest generic step in the whole chain" and §4.3 rests it on
M7.MONO. The strongest step's certificate was a tautology.
```

### C-2 — A NON-SEQUITUR CHECK

```text
M6.KHATFREE is labelled "THE khat-FREENESS IS AN ISOTROPY FACT: y = V_2(x)/(2|k|)
depends on k only through |k| ...". Its assertion expression is
`sp.diff(sten_closed, y_) != 0` — the derivative of the stencil tower's closed
form is nonzero. TRUE, and UNRELATED (AUDIT A6.KHATFREE-IS-A-NONSEQUITUR).
Nothing about khat, |k| or isotropy is tested.

THE CLAIM ITSELF IS SOUND but on a different footing than the battery implies: it
rests on DISPLAY's byte-quotation (:165, "NO khat appears in any ...") at the
second-hand-at-verified-seal grade the target declares in CH-6 — legitimate, and
correctly disclosed in prose. It should not also be counted as a CAS pass.
The companion check M6.TOWER IS real and reproduces exactly (my A6.TOWER):
sum_l w_l e^{i lambda^2 y} = -i e^{iy} sin(y) with series -i y + y^2 + (2i/3) y^3.
```

### C-3 — A TRIVIAL CHECK

```text
M7.ISLOT is labelled "THE I-SLOT'S SIGN MECHANISM IS GENERIC, ITS CARRIER IS NOT".
Its assertion is `sp.simplify(d_rep * 4 * sp.pi * xh) != 0` — a product of a
positive symbol, a constant and a real symbol is nonzero. It tests neither the
genericity of the sign nor the Clifford origin of the carrier
(AUDIT A6.ISLOT-IS-TRIVIAL). The split it labels is argued correctly IN PROSE at
(L3-h) and is independently supported by A7.SANDWICH; the CAS line adds nothing.
mu_B^I = 4 . 4 pi x_hat_x M_1 is at DISPLAY :355 bytes as quoted.

CONSEQUENCE OF C-1 + C-2 + C-3, stated once. The headline "39/39 PASS" is
arithmetically right and epistemically loose: 36 checks do work, 3 do none. No
conclusion of the target depends on the three — one is re-derived properly here,
one is at sealed bytes, one is carried by prose plus a different check. The
target's determination is UNAFFECTED. Its battery count is overstated by three.
```

### C-4 — A MIS-CITATION ON THE GAUGE AXIS

```text
§4.1 (L1-d) claims: "an internal U(N) factor commutes with S and leaves all
three premises intact (CAS M8.TENSOR)". M8.TENSOR tests the sandwich identity
P_- alpha_x P_- = -khat_x P_- and the trace form 4N delta_ab. IT NEVER
INSTANTIATES S AND NEVER TOUCHES A GRADING PREMISE. The citation does not
support the claim it is attached to.

THE CLAIM IS TRUE. AUDIT A8.S-COMMUTES supplies the missing check: with
S -> S (x) I_N, (S (x) I_N)^2 = I and (S (x) I_N)(alpha_a (x) I_N)(S (x) I_N) =
-(alpha_a (x) I_N) for every a — the grading premises DO survive the internal
factor. The gauge-axis verdict is unaffected; the pointer was wrong.
```

### C-5 — see §5 above (the enclosure's counterfactual, named but not displayed).

### C-6 — A NUMBER: THE §7 GROUP TABLE'S M2 ROW IS WRONG

```text
The §7 table records M2 as "5/5 PASS". The battery makes SIX M2 checks and §7.2
prints SIX M2 PASS lines: M2.1, M2.2, M2.5, M2.CF1, M2.CF2, M2.ABS. Counted
independently at the target's own bytes (ok() calls and PASS lines both).
The table's own rows then sum to 7+5+5+3+4+4+8+2 = 38, against the stated and
independently verified total of 39. The M2 row should read 6/6. The row's own
prose lists six items, so the description and the figure disagree internally.
THE TOTAL 39 IS CORRECT — I counted 39 ok() calls and 39 distinct PASS lines
(the raw grep returns 42 because §7 quotes three PASS lines verbatim before
§7.2 reprints them).
```

### C-7 — A NUMBER: "SIX SEALED GROUNDS" WHERE FIVE ARE DISPLAYED

```text
§0 states "the six sealed grounds carry ZERO EM/gauge tokens and 17-61
spinor/Clifford tokens each"; §6 SW-2 says the nets were run "over the six
sealed grounds and the target" (which would be seven files). The SW-2 table
displays SIX ROWS = FIVE grounds + the target, and SW-2's own following prose
says "The five artifacts that CARRY the summed mechanisms are EM-token-free".
FIVE is right; "six" appears twice. The finding is unaffected — see below.

THE SWEEP ITSELF REPRODUCES EXACTLY. Re-run independently at case-insensitive
strict word boundaries on both ends:
                                                 NET-SPINOR   NET-EM
  STAGE8_WALL_SUMMED_REQUANT_S9AD_V001.md            23          0
  STAGE8_WALL_LOGFREE_RATE_T4SR_V001.md              57          0
  STAGE8_WALL_LOGFREE_RATE_T4SR_AUDIT_V001.md        61          0
  STAGE8_DEG3_ANGULAR_CENSUS_T6SR_V001.md            32          0
  STAGE8_DEG3_DISPLAY_PAIR_T8SR_V001.md              17          0
  STAGE8_EM_PARTICIPATION_O4SR_V001.md                0        126
ALL TWELVE FIGURES MATCH THE TARGET'S TABLE EXACTLY. (A one-sided word boundary
returns 138 for O4SR; the target's stated "strict word boundaries" is the
two-sided net and is the one that reproduces. Its methodology statement is
accurate.) The disjointness of the two vocabularies in this corpus — the
structural claim §5.3(c-5) rests on — is CONFIRMED.
```

---

## 7. WHAT THE COMMISSION FORBADE MOVING — CHECKED

```text
(n-1) THE LOG'S NON-VANISHING IS DECIDED OF RECORD AND IS NOT MOVED. DISPLAY :54
      at bytes: "COMPLETE-LOG-GENUINE ... THE ARMED BRANCH TABLE FIRES:
      L_deg3 != 0 ==> the degree -3 carrier integral log Lambda is GENUINE at the
      class total — the commissioned log-free rate is REFUTED". The target
      preserves this verbatim in §0, §5.3(c-1) and its FLAG BLOCK, and confines
      itself to (a) the WARRANT for calling the quantity content-generic and
      (b) TRANSPORT to other content classes. NO SENTENCE OF THE TARGET PUTS THE
      RECORD'S OWN NON-VANISHING IN DOUBT. Swept for it specifically. CLEAN.
(n-2) THE TARGET USES THE CORRECT BRANCH CONDITION. RATE-AUDIT's COR-A (:44)
      corrects the refutation branch to require a nonzero TOTAL first moment, not
      a nonzero moment at one stratum. The target's LINK 4 works with the CLASS
      TOTAL L_deg3 at COR-A's displayed aggregation. It does not re-import the
      overstated converse. CLEAN.
(n-3) THE AGGREGATION-ROBUSTNESS IS READ CORRECTLY. DISPLAY :54: "the
      nonvanishing is INVARIANT under every nonzero weighting of the certified
      class-B moments". The target's (L4-b) — that this is robustness across
      UNDISPLAYED WEIGHTS and not across CONTENT, both branches running through
      class-B slots — is a correct reading and a fair one.
(n-4) LENS TOKENS: swept, ZERO. No lens, lensing, lensed, "through the lens".
(n-5) NUMBERS: no float anywhere in the target (swept, zero matches for any
      decimal with 3+ places). No measured constant, no CODATA, no 1/137, no
      fine-structure token, no comparison to any measured quantity. Every
      rational in the artifact is either an exact counterexample instance
      (declared as such in TOY_SEPARATION), an identity coefficient, or a
      quotation of a sealed record constant. THE RECORD'S OWN QUOTED NUMBERS
      RE-DERIVED: the ray witness (t, r) = (3/10, 1/20) gives s_- s_+ =
      (7/80)(39/80) = 273/6400 > 0 EXACTLY, matching DISPLAY :54 and the
      target's (L3-b) (AUDIT A9.RAY-WITNESS). The M_1 schema exhibit
      int_{-1}^1 u h(1-u) du = -2h/3 < 0 re-derived (A9.M1-SCHEMA).
      The "16" in b_D = e^{16 - 1/(s_- s_+)} is the record's own sealed profile
      constant at DISPLAY :54 bytes, quoted and not authored.
(n-6) FENCES: `alpha_computed`, `proof_authorized`, `kappa_record_computed`
      appear in the target ONLY inside its two declaration blocks (:17-18 and
      :1472-1473), all three = false. No alpha value, no kappa, no proof
      authorization, no EM identification. HELD, NONE APPROACHED.
(n-7) The two apparent "alpha = 0" hits at :525/:530 are the substring inside
      `mu_B^alpha = 0 + 0 + ...` — the Dirac alpha of the class-B slot, not a
      coupling. FALSE POSITIVE, cleared at bytes.
```

---

## 8. AUDIT CAS BATTERY

Battery `audit.py`, fresh venv `o7sraudit`, sympy 1.14.0, single run,
**46/46 PASS**. Written from scratch; the target's battery was NOT imported.
Every symbol formal (Lambda, d, p, g, N, M_1, d_rep); exact rationals and surds
only; nothing numeric evaluated.

```text
GROUP  WHAT IT DECIDES INDEPENDENTLY                                    RESULT
A1     the sealed weights/moments re-pinned (m_0 = m_1 = m_3 = 0,        1/1
       m_2 = -1)                                                        PASS
A2     Hermiticity at content dims 1,2,3,5 + the asymmetric-weight       6/6
       counterfactual + the abstract *-identity                         PASS
A3     the odd-kill at dims 1,2,5 + branch-dependence and asymmetric-    5/5
       branch counterfactuals                                           PASS
A4     THE THRESHOLD ARITHMETIC AT SYMBOLIC d (the display the target    7/7
       owed, C-5); d = 3 case; the exact Cauchy slot identity; HS        PASS
       Pythagoras at dims 1,2,5
A5     THE PIVOTAL STEP: odd-traceless proved on a GENERIC matrix (not   7/7
       sampled); the lemma on the non-Clifford graded class (40 words,   PASS
       16 nonzero); the all-lengths conjugation; THE RECORD'S OWN
       S = -i gamma^0 gamma^5 verified to carry all three premises;
       the ungraded and even-vertex failures
A6     VACUITY PROBES on the target's own assertion expressions:         5/5
       M7.MONO IS A TAUTOLOGY; the claim re-derived properly; the        PASS
       tower identity (real); M6.KHATFREE is a non-sequitur;
       M7.ISLOT is trivial
A7     the band-projector Clifford identity by exact spherical           7/7
       parametrization; P_- alpha_x P_- = -khat_x P_-; tr[a_a a_b] =     PASS
       4 delta; THE GENERIC-g BRACKET DERIVED from the azimuthal
       average; the record's case as a manifest square; the PSD Gram
       counterfactual (-13/101); THE SHARPNESS CHECK the target omitted
A8     the U(N) factor through the sandwich and the trace form; THE      3/3
       S-COMMUTATION the target cites M8.TENSOR for (C-4); the two       PASS
       summed kills at an internally doubled class
A9     the ray witness 273/6400; the M_1 monotone schema; the S^2        5/5
       moments; the class-A kill on the non-Clifford class; the          PASS
       anisotropy defeat
```

**ONE FAILURE OCCURRED AND WAS MINE, DISCLOSED.** My first run returned
FAIL on A3.CF1. Cause: I declared the coupling symbol without `real=True`, so
`.H` produced `conjugate(m)` terms and the coefficient extraction split wrongly.
I transcribed the target's `summed_assembly` VERBATIM into a separate probe
(`dbg.py`) and it passed on its own terms — `[m^1]` zero, `[m^3]` nonzero, and
the target's M2.CF1 assertion TRUE. The defect was in my transcription, not in
the target. Corrected (`real=True`, which the target had right) and re-run:
46/46. Recorded because a DEFAULT-REFUTE audit that hides its own false starts
is worth less than one that shows them.

---

## 9. SWEEP CUTOFF AND SCOPE

```text
SWEEP CUTOFF: 2026-08-15 13:14:34 CDT. Nothing created, modified or discovered
in the corpus after this instant is claimed, relied on, or reported here.
CORPUS ROOTS: /Users/bgm/MB Work/alpha-program-archive/workspace (primary) and
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
alpha_fundamental_record_action_cleanroom_v003 (not entered; no file opened).
```

**AW-1 — SCOPED READS.** The target in full; and, of the eleven grounds, scoped
reads at named loci: O4SR :321-322 and :414-435 (the FP-S paragraph whole);
O4SR-AUDIT A-6 :289-294 and its NET :625; WSR :58, :215-220, :330-333, :745,
:752; RATE :52, :154; RATE-AUDIT :44, :131, :419, :480, :520, :667; DISPLAY :54,
:165, :305-308, :355-425, :640. No artifact outside the target and its eleven
grounds was opened.

**AW-2 — DECLARED SCOPED SWEEPS.** (i) The SW-2 token re-sweep, both nets, six
files, reported in full at C-7. (ii) A fence/lens/float/number sweep over the
target only, reported at §7 (n-4)/(n-5)/(n-6)/(n-7). (iii) A count of `ok(` calls
and `^PASS M` lines in the target, reported at C-6. Nothing else was swept.

**AW-3 — WHAT I DID NOT DO.** No register, tracker, road, plan or continuation
file was read, listed, or opened at any point. No "Q-..." token was chased
(EXPECTED-UNLOCATABLE per commission). The MO3/MO4/REM/BL2STAR stock was NOT
opened — where this audit relies on it, it does so exactly as the target does,
through byte-quotations carried by the sealed grounds at the second-hand-at-
verified-seal grade, and every Clifford identity involved is re-derived here from
scratch rather than inherited. The cleanroom root was not entered. No file in the
corpus was edited; one file plus its sidecar written at the commissioned path.
No git. My scratch venv and scripts live outside the corpus, in the session
scratchpad.

---

## 10. VERDICT PER DELIVERABLE

```text
DELIVERABLE 1+2 — the four mechanisms re-derived with counterfactuals
  CONFIRMED-WITH-CORRECTIONS. M1 and M2 clean, both counterfactuals displayed
  and reproduced independently. M3's content substitutions are displayed and do
  work, but the one dial it declares load-bearing (carrier dimension) is named
  and not displayed — C-5, corrected here by supplying the display at symbolic
  d, which the claim survives. C-1 (the M7.MONO tautology) and C-3 also land in
  this deliverable's supporting battery.

DELIVERABLE 3 — the genuine log, chain walked and typed step by step
  CONFIRMED, and STRENGTHENED. The pivotal parity step is correctly typed as a
  GRADED-ALGEBRA fact — established here on the record's own S = -i gamma^0
  gamma^5 and on a generic-matrix proof of the odd-traceless half, both beyond
  what the target displayed. The grading-dependence and the Clifford-dependence
  each display a genuine failure without the structure; neither call is
  inflated, and the Clifford call's asserted bracket is DERIVED here and passes
  a sharpness check the target omitted. Corrections C-2 and C-4 sit in this
  deliverable and move nothing.

DELIVERABLE 4 — verdict and consequence
  CONFIRMED. The MIXED determination, the two-tiered agnosticism, the
  generalization boundary at §5.3(c-4) and the methodological consequence at
  (c-5) all follow from what is displayed, and (c-5)'s premise — the lexical
  disjointness of the two vocabularies in this corpus — reproduces exactly.
  C-6 and C-7 are figure errors in the supporting apparatus, not in the verdict.

OVERALL: CONFIRMED-WITH-CORRECTIONS.
  Seven corrections. Three vacuous CAS checks (C-1, C-2, C-3), one
  mis-citation (C-4), one generic call whose counterfactual is named but not
  displayed (C-5, supplied here), and two figure errors (C-6, C-7). NOT ONE
  MOVES A VERDICT, A TYPING, A GATE, A FLAG OR A STATUS. The target's central
  finding — that the closure's summed state is gauge-agnostic at mechanism
  grade and NOT agnostic about the representation-theoretic type of its content
  for its last summed number — is INDEPENDENTLY RE-DERIVED AND SUSTAINED.
```

---

## 11. CHOICE LEDGER (commission O7SR AUDIT — every unforced choice)

```text
AC-1  READING THE KILL CONDITION ("a generic call that does not display its
      counterfactual is a kill") AS SCOPED TO THE AXIS THE GENERIC CALL IS MADE
      ON. UNFORCED, and it is the choice that decides C-5's severity. M3's call
      is a CONTENT-genericity call; its content counterfactuals are displayed;
      the undisplayed dial is one the target declares non-content and out of
      scope. Read the other way — every named dial must be displayed regardless
      of axis — C-5 becomes a kill and the verdict becomes REFUTED for
      deliverable 1+2. DISCLOSED as the single most consequential judgement in
      this audit. I chose the scoped reading because the commission names the
      defect as genericity resting on VOCABULARY, and M3's rests on displayed
      substitution; and because the missing display is one line, which I supply,
      and the claim survives it unchanged. A reader who rejects the scoping gets
      REFUTED on deliverable 1+2 and CONFIRMED elsewhere.

AC-2  PROBING THE TARGET'S ASSERTION EXPRESSIONS FOR VACUITY SEPARATELY FROM THE
      CLAIMS THEY LABEL. UNFORCED — an audit could re-derive every claim and
      never look at whether the target's own check tested it. Justification:
      DEFAULT-REFUTE with testimony at zero weight means a PASS line is
      testimony until its expression is read. Three of thirty-nine failed that
      reading (C-1, C-2, C-3) while their claims held. Without this choice the
      battery would have been reported as 39/39 doing work.

AC-3  INSTANTIATING THE RECORD'S OWN S = -i gamma^0 gamma^5 rather than stopping
      at the target's abstract dim-6 witness. UNFORCED. It converts the pivotal
      typing from "holds on a class I built" to "holds on the record's own
      involution", which is the difference between a plausible attribution and a
      decided one — and the commission named this step as the one to break.

AC-4  DERIVING THE GENERIC-g BRACKET FROM THE AZIMUTHAL AVERAGE. FORCED once the
      target was found to assert it. An asserted generalization is where an
      inflated structure-dependent call would hide; it had to be derived or the
      call could not be confirmed. It derived, exactly.

AC-5  ADDING THE SHARPNESS CHECK (A7.CF-IS-SHARP) THE TARGET DID NOT RUN.
      UNFORCED. Without it, the PSD-Gram counterexample shows a negative bracket
      but not that g CAUSED it. With it, the same u and x_hat at g = 4 delta are
      strictly positive, so the dial is isolated. This is what separates an
      earned structure-dependent call from an inflated one.

AC-6  DISCLOSING MY OWN FAILED FIRST RUN (§8). FORCED by the audit's own
      standard: I hold the target to displaying what its checks actually do, so
      I display what mine actually did, including the one that was wrong and
      why. The failure was a missing `real=True` in my transcription; the target
      had it right.

AC-7  NOT RE-COUNTING THE ELEVEN SEALS' CONTENTS BEYOND THE NAMED LOCI. FORCED
      by scope (scoped reads of named artifacts). Consequence: my confirmations
      of the target's byte-quotations are confirmations that the quoted text IS
      at the cited bytes and says what the target says it says — not independent
      audits of the artifacts that carry them, which are sealed and audited
      elsewhere.

AC-8  NOT TREATING C-6 AND C-7 AS ANYTHING MORE THAN FIGURE ERRORS. UNFORCED at
      the margin — a stricter reading would call a wrong count in a battery
      table a defect in the battery's report. I report them as figure errors
      because the total (39) is correct and independently verified, the twelve
      sweep figures reproduce exactly, and no claim rests on either slip.
```

---

## 12. TOY_SEPARATION

```text
WHAT IS ACTUAL SURFACE HERE:
  - the target and the eleven grounds, at verified seals: ACTUAL, of record;
  - the record's own S = -i gamma^0 gamma^5, built from the Dirac basis and
    tested: ACTUAL — the record's sealed spinor bytes (WSR :58), re-derived;
  - the Clifford identities of A7 (band projectors, the sandwich, the trace
    form): ACTUAL, the record's own algebra re-derived from scratch;
  - the threshold arithmetic at symbolic d (A4): ACTUAL — it is the record's own
    integral, generalized in the one variable the target named and did not vary;
  - the sealed weights, moments, stencil tower, ray witness and M_1 schema:
    ACTUAL, re-pinned;
  - THE VERDICT PER DELIVERABLE (§10) and the corrections (§6): ACTUAL —
    determinations about the target, which is what this lane produces.

WHAT IS AN INSTRUMENT AND IS NOT CLAIMED AS SURFACE:
  - the generic content classes of A2/A3/A4 (dims 1,2,3,5, generic complex slot
    operators) — probes for mechanism survival, not physics, and no claim is
    made that any is or could be the record's content;
  - the dim-6 inner-graded non-Clifford class and the ungraded class of A5 —
    counterfactual witnesses, named as such at every appearance;
  - the symbolic trace form g and the PSD Gram instance
    [[1,-7/5,0],[-7/5,5/2,0],[0,0,1]] of A7 — an exact-rational counterexample
    and a formal dial, not a proposed trace form for anything;
  - the carrier dimensions d = 4 and d = 5 of A4.CF-CARRIER-DIM — a probe
    establishing that d = 3 is load-bearing. NO claim is made or implied about
    any carrier of any dimension other than the record's;
  - the internal multiplicity N = 2 of A8 — a probe for the gauge axis, not a
    claim about any gauge group.

THE SEPARATION IS CLEAN because every probe appears only inside a conditional of
the form "if X, then the mechanism would/would not survive", and no verdict in
§10 is stated about any probe. The probes decide TYPING and VACUITY; the typings
are about the target's own reasoning and the record's own mechanisms.

NOT A TOY: this lane touches the actual surface at exactly the point the
commission named — the target's four typings and the sealed steps under them —
and its output is an adjudication of those typings, not a model of them.
```

---

## 13. FLAG BLOCK

```text
ARTIFACT      STAGE8_SUMMED_MECHANISM_O7SR_AUDIT_V001.md (+ .seal.sha256)
COMMISSION    O7SR — SUMMED-MECH-AUDIT — 2026-08-15 — DEFAULT-REFUTE
TARGET        STAGE8_SUMMED_MECHANISM_O7SR_V001.md — seal verified OK from the
              artifact's own directory BEFORE any reliance; output path probed
              ABSENT before any write.
VERDICT       CONFIRMED-WITH-CORRECTIONS (overall).
              D1+D2 CONFIRMED-WITH-CORRECTIONS ; D3 CONFIRMED (strengthened) ;
              D4 CONFIRMED.
CORRECTIONS   SEVEN. C-1 M7.MONO is a TAUTOLOGY (claim true, re-derived here);
              C-2 M6.KHATFREE is a non-sequitur; C-3 M7.ISLOT is trivial —
              so 36 of 39 checks do work, not 39; C-4 (L1-d) mis-cites
              M8.TENSOR for an S-commutation it never tests (claim true,
              supplied here); C-5 the enclosure's counterfactual is named and
              not displayed (supplied here at symbolic d; claim survives);
              C-6 the §7 group table's M2 row reads 5/5 and is 6/6, leaving the
              rows summing to 38 against a correct total of 39; C-7 "six sealed
              grounds" where five are displayed and five is right.
              NOT ONE CORRECTION MOVES A VERDICT, TYPING, GATE, FLAG OR STATUS.
KILLS         NONE. No generic classification in the target rests on vocabulary.
              No structure-dependent call is inflated. No step of the parity
              chain is mis-attributed.
SEALS         11/11 grounds + the target re-verified with shasum -a 256 -c from
              the artifacts' own directory; 11/11 displayed prefixes re-computed
              and MATCHED.
CAS           AUDIT battery 46/46 PASS, single run, fresh venv o7sraudit,
              sympy 1.14.0, exact symbolic, nothing numeric evaluated. One
              earlier FAIL was mine and is disclosed in full at §8.
SWEEP CUTOFF  2026-08-15 13:14:34 CDT. Declared scoped sweeps only (AW-2).
FENCES        alpha_computed = false ; proof_authorized = false ;
              kappa_record_computed = false — ALL REMAIN, none approached.
DETERMINATION_ONLY = HELD. No authored physics. No construction. No value. No
              number as the value of anything. No measured-constant comparison.
              No EM identification made, licensed, or recommended. No lens token
              anywhere. No float anywhere in the target or in this audit. No
              gate, flag, witness, absence or status of any artifact moved. No
              file edited; one file plus its sidecar written at the commissioned
              path. No git.
NOT MOVED     The log's NON-VANISHING is decided of record and is untouched:
              DISPLAY's COMPLETE-LOG-GENUINE and the refuted log-free rate stand
              verbatim at bytes, and the target was swept specifically for any
              sentence putting them in doubt. None exists. The two exact
              vanishings stay exact; the enclosure stays carrier-free; FP-S
              stays DECIDED.
SUSTAINED     The target's central finding is independently re-derived and
              SUSTAINED: the closure's summed state is gauge-agnostic AT
              MECHANISM GRADE (U(N) enters every mechanism only as a tensor
              multiplicity), and its last summed number is NOT agnostic about
              the representation-theoretic type of its content — grading-carried
              for the shortfall's being a LOG, Clifford-carried for that log's
              certified NONVANISHING. The pivotal parity step is a
              GRADED-ALGEBRA fact, confirmed here on the record's own involution.
ALL_RESULTS = CLAIMED until the registrar routes; this lane routes nothing.
O7SR_SUMMED_MECHANISM_AUDIT_RESULT = SEALED.
```
