# STAGE8_HELD_FIXED_AND_FRAME_DETERMINATION_EINSTEIN_V001

LANE: EINSTEIN
RELAY: 224 ("WHAT IS HELD FIXED UNDER `R -> beta R`, AND IN WHICH FRAME")
DATE: 2026-07-31
ROAD: ADVANCES STEP 2 / ROUTE A. This lane's own relay-221 result named these two gaps as *"prior
to everything and unbuilt."*

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.
**NO NUMERICAL VALUE FOR `beta` OR `rho`. Nothing evaluated; symbolic weights only.** No
measured-constant comparison. **NO FRAME IS DECLARED AND NO HELD-FIXED RULE IS CHOSEN — both are
principal acts.** The Misner-Sharp / Brown-York fork is **not resolved**. `a32_holdout/
custodian_private/` untouched. Refutations reported, never repaired. Read-only.

`frame_declared = false; held_fixed_rule_derived = false.`

METHOD: four establishing tasks, each adversarially attacked with both failure modes policed. Run
wf_ecc5f5bd-c0b, eight agents. One attack **REFUTED** its contribution outright, three WEAKENED.
*** THE REFUTATION LANDED ON THE RELAY'S OWN HEADLINE CANDIDATE AND ON THIS LANE'S PRIOR R7. IT IS
FOLDED IN FULL AND LEADS §2. ***

---

## 0. LEAD

*** THE FRAME LEAD IS **REFUTED**, AND IT IS REFUTED AGAINST MY OWN PRIOR RELAY. `beta` IS
**FRAME-INVARIANT**. A FRAME CHOICE DOES **NOT** SUPPLY THE CONVERSION, AND NO `beta` DERIVATION IS
CIRCULAR FOR WANT OF A FRAME DECLARATION. ***

The decisive fact is one line of sealed text, and it settles the question because it displays both
quantities at once:

```text
ds_5^2 = g_mu_nu dx^mu dx^nu + R^2 (d theta + A_mu dx^mu)^2          (V002 :26-28)
```

`R` is a proper radius in the **fiber block**; `c Delta tau` is a proper interval in the **base
block** — **of the same `ds_5^2`**. There is no second metric on the sealed line. So
`beta = R/(c Delta tau)` is a **dimensionless invariant of one five-geometry**: invariant under
every fiber-preserving 5d diffeomorphism and under `ds_5^2 -> lambda^2 ds_5^2`. An external-only
rescaling does move `beta`, but by producing a **different five-geometry**, not by reframing one —
and something that moves a dimensionless invariant is not a relabeling. **My relay-221 R7 claimed a
frame declaration was required for `beta` to be well-defined. That claim is withdrawn.**

**THREE QUALIFICATIONS THAT SURVIVE, AND THEY MATTER:** (a) the pin rests on V002 `:35`, *"For
constant `R`"* — at constant `R` every `Omega(R)` is a constant, hence pure units, hence there is no
frame to declare; (b) *** A FRAME AMBIGUITY IS REAL AND CORPUS-NAMED IN THE **DYNAMICAL** SECTOR —
my own relay 221 lists it among the three things `R -> R(x)` generates — SO R7's DEMOTION IS
**SECTOR-LIMITED TO CONSTANT `R`**, NOT GLOBAL; *** (c) `beta` is **fork-relative** even though
frame-invariant, because its denominator `Delta tau` is `sqrt(2)`-fork-conditional — a standing
declaration my own prior artifact made and which any "beta is determined" must carry.

**SO THE CORRECT LEAD IS THE SECOND ONE — GENUINELY UNSTATED — AND IT SPLITS:**

```text
AT THE PARENT (5D) LEVEL   UNSTATED, and more completely than N4 recorded.
  G_5 / kappa_5 / M_5 / ell_5 as a five-dimensional Newton constant: ZERO corpus-wide
    (every hit is a G1-G5 guardrail, a gamma-5 matrix, an M5 milestone, an m_5 Ward index,
     or a GUT gauge coupling — the 5d gravitational normalization HAS NO NAME HERE)
  "circumference": ZERO hits corpus-wide.   ell_P: NEVER DEFINED anywhere in any root.
  "dimensional reduction": asserted ONCE, at V002 :35.
  *** THE REDUCED ACTION IS A WRITTEN-DOWN RESULT WITH NO WRITTEN-DOWN DERIVATION AND NO
  WRITTEN-DOWN PARENT NORMALIZATION. Nothing connects the granted parent to 1/(16 pi ell_P^2). ***

AT THE REDUCED (4D) LEVEL    STATED, AND UNIFORM — ell_P HELD FIXED.
  V002 :39-43 displays an R-FREE coefficient on R_4; my relay 221 says it outright
  ("ell_P held fixed"); phi := ln(R/ell_P) shifts by exactly ln beta only if ell_P is fixed.
  *** MY RELAY-221 N4 SAID "UNSTATED" FLATLY. THAT WAS HALF WRONG AND IS CORRECTED HERE. ***
```

*** AND THE FINDING THAT IS WORSE FOR THE PROGRAM THAN THE ONE I REPORTED LAST TIME: THE CORPUS HAS
**ALREADY MADE THE CHOICE, SILENTLY**. *** The over-determination is structural and I confirm it
without evaluating any prefactor: both displayed terms descend from **one** five-dimensional
density integrated over **one** fiber whose extent is set by `R`, so an `R`-independent 4d
Einstein-Hilbert coefficient is possible **only if `ell_P` absorbs the fiber extent**. Holding both
the parent normalization and `ell_P` fixed while `R` varies is **over-determined — the reduction
relates them.** The corpus resolves this by *displaying* the reduced form with `ell_P` constant and
never saying so at parent level. **That is an underived adoption sitting beneath V002 `:61`'s
`beta^2` and every weight downstream of it.** Under the corrected Q-65/Q-76 bar, existence and
uniqueness must both be **derived** — *** AN ADOPTED HELD-FIXED RULE CANNOT FORCE A NUMBER. ***

---

## 1. THE SUPERSESSION THAT HARDENED THE GAP (new at source, in neither prior relay)

The predecessor **made the gap explicit and the supersession removed the hedge without adding
content**:

> V001 `:22-23`: *"the two-derivative Einstein-Hilbert action on the total space reduces
> **schematically** to"* — with `S_P = [1/(16 pi G_4)] integral_M sqrt(-g)[R_4 - (r_Q^2/4)
> F_mu_nu F^mu_nu]`.

*** V001 USED `G_4` — A **FOUR**-DIMENSIONAL NEWTON CONSTANT — AND SAID **"SCHEMATICALLY."** V002
REPLACED `G_4` WITH `ell_P^2` OF THE SAME SHAPE, **DELETED THE WORD "SCHEMATICALLY," AND WROTE
"DIMENSIONAL REDUCTION GIVES"** (`:35`). A STRENGTHENING OF EPISTEMIC STATUS WITH ZERO ADDED
DERIVATION. *** And in **both** versions the fixed external datum is a **four**-dimensional
gravitational normalization while the five-dimensional one is never written — which is the
reduced-level reading, present from the start and never derived.

---

## 2. THE CIRCULARITY CHARGE, STATED AT FULL STRENGTH AND THEN REFUTED

**The charge:** `beta` is a ratio of an internal to an external length; a modulus-dependent Weyl
rescaling of the external metric changes the external length; therefore the frame declaration
supplies part of the conversion, and any `beta` derivation without one is circular.

**The refutation, executed:** the charge presupposes `R` and `Delta tau` are measured in
**different** metrics. They are not — the sealed ansatz displays both as proper lengths in blocks of
the **same** `ds_5^2` (§0). The standard reply ("a ratio of two lengths in one metric is
frame-independent") **is available here**, and it is available *because the ansatz is a single
line*. `W_ext(Omega)` — rescale the base block, leave the fiber block — does send
`beta -> beta/Omega`, but it is not a change of frame on one geometry; it is a map to a **different
five-geometry**.

*** I REPORT THIS AS A REFUTATION OF MY OWN PRIOR WORK. Relay 221 introduced R7 as "the one
remaining door through which a conversion could enter without ever appearing as a number." That
door is closed at constant `R`, and I say so as plainly as I opened it. What survives is the
sector-limited version: **the frame ambiguity is real once `R` is dynamical**, which is precisely
where relay 221's own posing lives. ***

---

## 3. N4 AND THE FRAME ARE **SEPARATE** QUESTIONS — THE RELAY'S 2.3 PREMISE IS REFUTED

The relay proposed they are "the same question in different clothes." They are not, and the reason
is sharp: *** THE SINGLE WRITTEN LINE `:38-40` IS **DEGENERATE BETWEEN TWO DIFFERENT UNSTATED
STIPULATIONS.** *** Its 4d Einstein-Hilbert coefficient carries no `R` while its `F^2` coefficient
carries `R^2`. That asymmetry is produced **either** (i) by `ell_P` being the `R`-independent
fundamental datum, **or** (ii) by an `R`-dependent Weyl rescaling having been silently applied to
reach an Einstein-frame form from a Jordan-frame reduction. **Two unknowns, one equation — the
written form cannot discriminate.** So they are two **independent** declarations jointly
under-determined by one line, not one question in two costumes.

---

## 4. THE TWO BRANCHES, PRICED (neither chosen)

- **BRANCH A — `ell_P` fundamental.** The sealed `beta^2` survives; the family is genuinely
  distinct theories; a derived charged current **could** in principle break it. **Price:** the
  granted parent is then a *one-parameter family* of parents, not one parent — the word *"complete"*
  at V002 `:31` does no work, and reopen condition 2's uniqueness gets **harder**.
- **BRANCH B — parent normalization fundamental.** `ell_P` inherits `R`-dependence and the sealed
  *"changing `K_KK` by `beta^2`"* **does not follow and must be withdrawn**. **Price:** `rho =
  R_*/ell_P` becomes ill-posed as a modulus — a ratio whose denominator is a function of its
  numerator.

*** BUT ONE COST THE RELAY BRIEFING ASSERTED IS **REFUTED**: Branch B does **not** make the orbit
unphysical. `beta` is a dimensionless invariant and it **moves** along the orbit at fixed base
metric and fixed `Delta tau`, so the points are distinct geometries whatever the action
normalization does. And V002 `:38` displays `S_4/`**`hbar`** — so a Jordan-branch overall constant
rescales an action-*phase* and is a symmetry of the source-free **classical** equations only.
**N4 governs the WEIGHTS along the orbit, not whether the orbit exists.** The briefing's
"nothing to break" branch was carried untested by my first pass and is corrected here. ***

---

## 5. THE MISSING SENTENCES (Q-92: naming what is missing is the complete answer)

**For N4 —** a sealed statement at parent level of the form: *which* normalization of the
five-dimensional action is held fixed under `R -> beta R`, together with the reduction step that
relates it to `1/(16 pi ell_P^2)`. Absent that step, the prefactor's provenance is unstated and the
`beta^2` weight rests on a stipulation the corpus never makes.
**For the frame —** a declaration is **not** required for `beta`'s well-definedness at constant
`R`; it **is** required once `R` is promoted to a field, which is exactly the regime relay 221's
posing occupies.

---

## 6. TYPED NEGATIVES (Q-54)

- **N-A [TYPE-R, executed]** `beta_is_frame_dependent = false` — which-metric test on V002 `:26-28`;
  both lengths are blocks of one `ds_5^2`. **Refutes this lane's relay-221 R7 as stated.**
- **N-B [TYPE-R, executed]** `frame_declaration_supplies_the_conversion = false` (§2).
- **N-C [TYPE-R, executed]** `N4_and_frame_are_the_same_question = false` (§3).
- **N-D [TYPE-R, executed]** `branch_B_makes_the_orbit_unphysical = false` (§4).
- **N-E [TYPE-S]** parent-level held-fixed rule **unstated**. Roots: all four incl.
  `cleanroom_output`; exclusions: holdout, `.git`; queries: `G_5|kappa_5|M_5|ell_5|"five-dimensional
  Newton"|"5D Newton"|"parent Newton"|"Newton constant"|circumference|"fiber volume"|"internal
  volume"|"2 pi R"|"fiber integration"`. **Every hit a false positive for the target sense; the 5d
  gravitational normalization has no name in this corpus. `ell_P` never defined.**
- **N-F [CORRECTION, against my own prior artifact]** `held_fixed_unstated_at_all_levels = false` —
  it is **stated and uniform at the reduced level** (`ell_P` fixed). My relay-221 N4's flat
  "unstated" is corrected to "unstated **at parent level**."
- **N-G [TYPE-C]** `held_fixed_rule_derived = false` — release: the parent-level statement plus its
  reduction step (§5). **Adopted, not derived; an adopted rule cannot force a number.**
- **N-H [TYPE-R, executed]** over-determination confirmed structurally, no prefactor evaluated (§0).
- **N-I [TYPE-S, scope-corrected]** frame vocabulary absent in the relevant sense: `Jordan`/
  `Einstein frame`/`conformal frame` hits are this lane's own artifacts, the register quoting them,
  the relay paste, and unrelated uses (Pauli-Jordan, Jordan-Wigner, Jordan's inequality); `Weyl`
  collocations are Weyl carrier, BCC Weyl walk, Weyl determinant, Solomyak-Weyl, Weyl sequence —
  **none a conformal rescaling of the spacetime metric.** *** A PRIOR RELAY REPORTED THIS AS "ZERO
  HITS CORPUS-WIDE" HAVING SCOPED IT TO **TWO FILES**. The corrected corpus-wide scope is reported
  here and **the negative survives it** — stronger than the original, and the under-scoping is this
  lane's own and is disclosed. ***
- **N-J [TYPE-S, carried]** `beta` is **fork-relative** via `Delta tau` — standing declaration,
  restated because §0's frame-invariance finding must not be read as "beta is uncontaminated."
- **N-K [TYPE-R, reported not repaired]** the struck overdetermination bar still lives in the
  cross-sector spec at `:265-267` and `:292`. **Not applied.**
- **N-L** `new_class_required = false`. Imports named: Kaluza-Klein reduction (**licensed by
  grant** — the sealed ansatz *is* a circle reduction), Jordan/Einstein frame and Weyl rescaling
  (named as **external vocabulary for posing the question**; **no conformal transformation law was
  applied to any sealed expression**), radion (native corpus vocabulary, not an import). **No
  convention-dependent prefactor was imported or evaluated** — which is why N-H is structural and
  the exponent question is typed TYPE-C, not TYPE-R.

---

## 7. CORRECTIONS FOLDED, UNDER THIS LANE'S NAME

1. **R7 withdrawn as stated** (§0, §2) — `beta` is frame-invariant; the demotion is sector-limited
   to constant `R`.
2. **Relay-221 N4's flat "unstated" corrected** to parent-level-unstated / reduced-level-stated
   (N-F).
3. **The briefing's "nothing to break" branch refuted** (§4) — carried untested by my first pass.
4. **A prior "zero hits" scope corrected** from two files to corpus-wide; the negative survives
   (N-I).
5. **The 2.3 unification premise refuted** (§3), with the degeneracy argument replacing it.

---

## 8. DISCIPLINE

- **Q-92 / Q-69 / Q-54** observed; only TYPE-R treated as physical content; hypothetical flags
  marked inline; every constructed map (`W_ext`, the overall rescaling, the composition) marked as
  this relay's construction on granted data.
- **The bar:** corrected Q-65/Q-76 applied; **F3 and minimal-pass item 3 not applied** (N-K).
- **Fork:** no quasilocal-energy-consuming requirement introduced — the which-metric and
  over-determination tests are purely kinematic on granted metric data. The adopted `C_R = 1`
  selector remains declared as fork-consuming; the prospective flag on reopen condition 4 remains
  prospective and is not fired.
- **Q-91 custody:** no `git`; baseline untouched; `deploy_status.sh` not run; no publication
  authorization requested. Written in the cleanroom, sealed there, mirrored with sidecar to the
  archive workspace, both seals verified; report; stop.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
