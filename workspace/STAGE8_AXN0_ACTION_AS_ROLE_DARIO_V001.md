# STAGE 8 / [PLAN:AXN-0] — THE ACTION: MIXED, AND THE SPLIT IS STRUCTURED

Lane: DARIO (Builder B, independent verifier). Relay 809.
State brief pinned: `PROGRAM_STATE_BRIEF_V005.md` = `e26f0d16055f3e83…`, seal OK. Law 8 throughout.
**TYPING ONLY** — no action authored, no mechanism constructed, no role discharged.
**PE-1..PE-8: not opened, not searched, not consulted. PE-8's subject is this relay's subject and
the isolation was held absolutely.** All headline items **CLAIMED**.

## Lead determination — CLAIMED

```text
VERDICT = MIXED, with the split stated.
   Neither "OBJECT-PRIMITIVE stands" nor "ROLE-DOMINANT" is honest.
```

**And the split is not noise — it sorts by where in the program the sentence sits.**

```text
BOTH OBJECT threads sit at the CONSTRUCTION end, and BOTH carry an explicit ANTI-SUBSTITUTION
   clause: the record anticipated role-discharge by a cheaper mechanism and BANNED IT BY NAME.
BOTH ROLE threads sit at the SELECTION end, and in BOTH the sentence never names an
   action-functional at all.
ONE thread (T3) is the hinge, with an object conjunct inside a role demand.
ONE thread (T6) IS MIS-BOUND and must be struck: zero "parent action" occurrences in its entire
   78,368 bytes; its terminus is a typed carrier map, not an action.
```

**So there are five action-threads, not six.**

| | thread | typing | the sentence's own words |
|---|---|---|---|
| T1 | ARCH §5 | **OBJECT** | *"This is not an invitation to add a spectral-density principle"*; both objects from the **same** action |
| T2 | S28 / PRIN | **ROLE** | *"boundary closure plus the induced action **selects** them"* — **singular verb on the conjunction** |
| T3 | M05 / B3 | **MIXED** | ROLE: *"no sealed joint saddle performs the conversion"* / OBJECT: the action as input #1 of 5 |
| T4 | S34 | **ROLE** | *"the complete parameter-free **parent supplies** a Lorentz-scalar equation with one isolated positive stable solution"* — no action-functional present |
| T5 | S1/S2 | **OBJECT** | *"do not patch or choose `x`; derive and **vary** …"* |
| T6 | S16 | **MIS-BOUND, STRUCK** | contains no action |

**Two facts I verified myself change the picture, and they cut in opposite directions.**

**Against the object reading —** the principal's own S03 ruling has already typed the
construction-end deficit, and it typed it as a missing *selector*:

> *"the sealed stock admits an **admissible action family with no sealed selector**; the family is
> not bounded or finitely enumerated by sealed stock … **"declaring one minimal is not a
> derivation"** … C7 bars selection from the response end; this ruling bars selection from the
> construction end until the record itself forces one."*
> — `DECISION_S03_EXPLICIT_INCOMPLETENESS_2026-08-06.md`, seal **OK**, verified by me

**The action-object layer is not empty. It is a FAMILY. What is missing is a SELECTOR.**

**For the object reading —** T1 demands *"the … action required by R3"*, and R3's own sealed parent
result **never uses the word**:

```text
R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_RESULT_V001.md   sha 345d447eaf6d730c   5,310 B
   occurrences of "action", case-insensitive:  ZERO      [counted by me]
   verdict: FINITE_CAUSAL_PARENT_DERIVED_CONTINUUM_COMPLETION_OPEN
```

**The record's actual parent is operator-native. "Action" enters exactly where the program needs a
name for what would perform a selection** — which is the role-typing, in the corpus's own usage.

**And the finding that keeps this from being a dissolution.** I inventoried the record's native
forcers and typed their **outputs**:

```text
refinement forcing (R33)      -> FORCES BY ELIMINATION      (naturality forces the uniform measure)
the intrinsic quadratic (795) -> ELIMINATES                 (killed form-inheritance)
incidence / direction thm     -> FORCES ON A SUBSPACE       (and leaves a residual)
cofinality (E4a)              -> GIVES INDEPENDENCE         (limit is exhaustion-free)
boundary closure              -> named, but PRIN pairs it WITH the induced action, as ONE selector

*** EVERY NATIVE FORCER ELIMINATES, CONSTRAINS, OR GIVES INDEPENDENCE.
*** NOT ONE HAS EVER OUTPUT A UNIQUE POSITIVE VALUE.
```

T4 asks for *"one isolated positive stable solution."* **That is a selection, and it is precisely
the output type the record has never produced.**

```text
TYPE_MATCHES = 0 clean.   TYPE_GAPS = 2 (T2, T4), both of the same shape:
   the role is real, and the record's native mechanisms are of the WRONG OUTPUT TYPE to discharge
   it.  Role-typing RELOCATES the demand; it does not dissolve it.
```

---

## 1. AS1 — THE SIX THREADS, TYPED

Every span and digest below was re-derived from bytes. Seal mode stated per law 8.

### T1 — ARCH §5 (E4c's route) — **OBJECT**

`CAUSAL_DIRECT_LIMIT_ARCHITECTURE_ADJUDICATION_RESULT_V001.md` `9be3f55fd527b9a8…`, packet-sealed
(mode 3), span `[5062,5962)` sha `27291707f7b00908…`:

> *"Fork 8 remains open at a narrower and now explicit point: derive the covariant root spectral
> measure and the recoverable outgoing record algebra from the complete microscopic parent action.
> **This is not an invitation to add a spectral-density principle.** The next valid construction
> must obtain both objects from the **same** target-independent source-record-gravity-gauge action
> required by R3."*

**Probe — would a different lawful mechanism satisfy this sentence? NO, and the sentence answers
unprompted.** *"Not an invitation to add a spectral-density principle"* is an explicit bar on
role-discharge by a cheaper mechanism. **Strengthened, not weakened, by the R3 fact above:** the
sentence demands a functional the record's own parent layer does not possess.

### T2 — S28 / the free-data ledger — **ROLE**

`STAGE8_7A_TOWER_CONTEST_DARIO_V001.md` `03db8d3da273f42c…` (mode 1), span `[12531,13213)` sha
`a451238549a62fdc…`: the ledger's column heading is **"What would constrain it lawfully"**, and all
five rows read *"boundary closure + the induced action, before any comparison (`PRIN`)"*.

**`PRIN` settles it in its own grammar**, verified by me (`ae1d04922cb37f8b…`):

> *"Those are outputs only if boundary closure plus the induced action **selects** them before any
> comparison with alpha…"*

**Singular verb on a conjunction. "Boundary closure plus the induced action" is ONE SELECTOR.** The
action is demoted twice over — *"induced"*, and subordinated under a constraint-column header.

**Probe — YES.** The demand is a lawful constraint arriving before comparison; the action is the
vehicle.

### T3 — M05 / B3 metric conversion — **MIXED**

`STAGE8_PLAN_AUDIT_AND_FORCING_MAP_CODEX2_V001.md` `653bf8679aeab6f6…` (mode 1), span
`[14196,14327)` sha `17dab9d97b4f206c…`:

> *"Inputs needed for force: the complete parent action, radion stabilization, spectrum, matching
> rule, and the declared energy branch."*

**Object-side:** an enumerative list; the very functions a role-reading would fold into the action
are listed as its **peers**, which would be redundant if "action" were a vehicle word.
**Role-side:** the head words are *"Inputs needed for **force**"*, and the immediately preceding
clause in the same cell reads *"no sealed joint saddle **performs** the conversion."*

**Split, per the 801 row-7 discipline:** ROLE = *"no sealed joint saddle performs the conversion"*;
OBJECT = the action as input #1 of five. **MIXED.**

[YOURS] **A method hazard, named because it is this relay's own:** the two typings sit **82 bytes
apart** inside one table cell. A thread typed on a span that excludes its own diagnosis is typed on
a cut, not on a meaning. I widened to the cell before typing.

### T4 — S34 / absolute scale — **ROLE**

`STAGE8_HELD_FIXED_BRANCH_DERIVABILITY_AND_AXIOM_PRICE_V001.md` `39f7ea6d95dcdad3…` (mode 1), span
`[3765,4145)` sha `80ef473adc99fea9…`:

> *"…absolute scale closes only if the complete parameter-free **parent supplies** a Lorentz-scalar
> equation with one isolated positive stable solution."*

**The sentence never names an action-functional.** Its only use of the word is *"a dimensionless
action interval"* — action-as-quantity, a different sense. The deliverable is **an equation with a
unique stable solution**: a selection condition.

**Probe — YES, decisively.** **"Action" entered this thread through the program's reading of
"parent" as "parent action." That reading is ours, not the sentence's** — exactly the
declared-scopes hazard the relay warned of.

### T5 — the S1/S2 derive-and-vary no-go — **OBJECT**

Span `[8513,8641)` sha `53e3bcd73000d7ab…` (mode 1):

> *"do not patch or choose `x`; **derive and vary** the complete compact nonlocal coupled action on
> a physically provenanced carrier"*

**Probe — NO, on one word: `vary`.** Variation has exactly one argument type. No alternative
mechanism admits `δ`. And the paired requirement in the same cell orders the two explicitly —
*"evaluate the full compact proper-time action and public superdeterminant **to select or reject**
a stable `x`"* — the role is grammatically **downstream** of the object.

### T6 — S16 Thomson-limit slot — **MIS-BOUND; STRUCK FROM THE TALLY**

`STAGE8_TASK6_LP_QSPEC_ASSEMBLY_DARIO_V005.md` `76589e94bb4af318…` (mode 1). **`"parent action"`
occurs ZERO times in all 78,368 bytes**, and the chain terminates at *"a lawful typed map /
`LOCAL-SHADOW` carrier → physical alpha-adjacent carrier"* — **a typed carrier map, not an action.**

```text
THREADS = 6 typed: OBJECT 2 (T1, T5) / ROLE 2 (T2, T4) / MIXED 1 (T3) / MIS-BOUND 1 (T6, struck).
   Tally over the five that instantiate the question: OBJECT 2 · ROLE 2 · MIXED 1.
```

---

## 2. AS2 — THE RECORD'S NATIVE FORCERS, AND THE TYPE-MATCH

### 2.1 Inventory, typed by OUTPUT

| native forcer | where it actually forced something | **output type** |
|---|---|---|
| refinement forcing | `INHERITED_REFINEMENT_NATURALITY_FORCES_UNIFORM_MEASURE` (R33) | forces **by elimination** — naturality leaves one measure |
| the intrinsic quadratic | 795: form-inheritance eliminated, O(1) `F`-dependent failure | **elimination** of candidates |
| incidence / the direction theorem | 788: `sd*_1` injective iff `g ∈ A0`; `J_1` forced on `ker d_1` | forces **on a subspace**, leaves a residual |
| cofinality | E4a (799): the colimit is exhaustion-independent | **independence**, not selection |
| boundary closure | PRIN, and it is S28's own first term | named — but PRIN binds it **with** the induced action as **one** selector |

### 2.2 Type-match against the ROLE threads

```text
T2 (constrain the five free data before comparison)
   candidate: boundary closure — the sentence's own first term.
   TYPE-GAP.  PRIN's singular verb makes "boundary closure plus the induced action" ONE selector;
   the native half alone is not the sealed demand.  And no native forcer has ever selected a
   positive value for a radius, a spin structure, or a bundle class — they eliminate.

T4 (one isolated positive stable solution)
   candidates: cofinality (independence), the quadratic (elimination).
   TYPE-GAP.  Neither outputs an isolated stable solution.  This is the sharpest gap in the set:
   the demand is for a SELECTION and the record's mechanisms produce ELIMINATIONS.

TYPE_MATCHES = 0.   TYPE_GAPS = 2.   UNDECIDABLE = 0.
```

[YOURS] **This is why the role-typing does not dissolve the demand.** The B0 precedent dissolved a
role because a native mechanism of the right output type existed. Here none does. **Role-typing
relocates the question from "build a functional" to "supply a selector" — and the record has no
native selector.** That is a real narrowing and an honest one; it is not a dissolution.

---

## 3. AS3 — THE VERDICT FOR THE PRINCIPAL

```text
VERDICT = MIXED (split stated).  "Derive the complete parent action" is NOT, as a whole, the
   program's true remaining primitive — and it is NOT merely classical surface either.

THE ROLE HALF (T2, T4, and T3's role conjunct) — and the record already says so itself.
   The S03 ruling of record types the construction-end deficit as "an admissible action family
   with no sealed selector", "not bounded or finitely enumerated", with "declaring one minimal is
   not a derivation" and a sealed file named COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE.
   RECORD-NATIVE RESTATEMENT, in the record's own mechanism vocabulary and taken from S03's own
   release condition (a):  "BOUND OR CLASSIFY THE ADMISSIBLE ACTION FAMILY FROM RECORD STRUCTURE
   ALONE."  That is a bounding/elimination demand — the output type the record's forcers DO have.

THE OBJECT RESIDUE (T1, T5) — irreducible, and a selector will not discharge it.
   T1 demands a functional R3's own sealed parent result does not contain (zero "action" hits,
   verdict FINITE_CAUSAL_PARENT_DERIVED_CONTINUUM_COMPLETION_OPEN), under a named sector type,
   with role-discharge BARRED BY NAME.
   T5 demands VARIATION, which admits one argument type.
   Both are fenced by explicit anti-substitution clauses.  THE RECORD PUT THOSE FENCES THERE
   BECAUSE IT ANTICIPATED EXACTLY THIS QUESTION.

SO: the campaign is not one campaign.  It is a BOUNDING campaign on the family (role half, native
   output type available) and a CONSTRUCTION obligation at two fenced nodes (object half, no
   substitute admitted).  Reporting either half alone would misdescribe the node.
```

---

## 4. AS4 — FREEDOMS CONSUMED, FLATTENING CHECK

### 4.1 `FREEDOMS_CONSUMED` (law 2a)

| datum | tag |
|---|---|
| the parent action / any member of the family | **NOT AUTHORED, NOT SELECTED** — this relay types sentences and constructs nothing |
| any role-discharging mechanism | **NOT CONSTRUCTED** — §2 types output-compatibility only |
| the five free `D_BR` data | **CARRIED AS UNSELECTED** |
| `x`, `rho`, the energy branch, the spectral measure | **CARRIED AS UNSELECTED** |
| the object/role typing itself | **MINE, and declared as a judgment** — the sentences are quoted; the typing is a reading |
| scaling weights (law 2a) | **NONE CONSUMED** |
| smooth constituent | **NOT CONSUMED; BARRED (S26)** |

**SUBSTITUTED: none.**

### 4.2 `FENCE_CHECK` — the four live fence-posts

```text
S19  decay not inferred from masslessness/gaplessness (external physics)
     RESPECTED — no decay inference appears; nothing is imported to bridge a role.
S21  the gamma construction refuses Misner-Sharp / Brown-York; the HJ conjugate energy must be
     DERIVED
     RESPECTED — and noted as CORROBORATING: S21 is itself a role-shaped decline ("derive, do not
     choose"), the same grammar as T2/T4.  I derive nothing and choose nothing.
S24  any lane reach for a CLUSTERING AXIOM blocks immediately; principal-only, may not rescue a
     route
     RESPECTED — no role is discharged by reaching for an axiom.  §2 reports TYPE-GAPS rather than
     bridging them.
S25  NO REPARAMETERIZATION OR EQUAL-ACTION PRINCIPLE may be introduced; the principal refused it
     RESPECTED — and this is the LIVE HAZARD OF THE ROLE READING, flagged principal-facing:
     the most obvious cheap discharge of an action-role IS an equal-action principle, and S25
     already bars it.  A ROLE verdict must not become a licence to re-enter that route.
FLATTENING_CHECK = clean (37 rows walked; S19/S21/S24/S25 live and each discharged above;
     S26/S08 clean; the rest untouched).
```

---

## 5. JURISDICTION AND VERB AUDIT

**Jurisdiction.** *What was the rule written to protect?* The declared-scopes discipline — "the
action as classical object may itself be OUR reading." The risk was live and it landed: T4's action
**is** our reading, and T6 is not an action thread at all. *Does the outcome space distinguish?*
Yes — the verdict is MIXED with two threads typed each way, one hinge, and one struck. *Would
evidence look different if OBJECT dominated?* Yes: the anti-substitution clauses would appear at
the selection nodes too. They appear only at the construction nodes.

**VOID CONDITION.** Nothing selected. The verdict does not favour the tidier answer: dissolving the
action node would have been the more satisfying result, and §2's type-gap analysis is what stops it.

**PE ISOLATION.** The ledger was never opened, searched, or quoted. **One disclosure:** a
commissioned reader reported encountering an incidental two-line PE reference inside
`STAGE8_PLAN_AUDIT_AND_FORCING_MAP_CODEX2_V001.md` while reading T3's row; it stopped, did not
follow it, and quoted nothing. **My 777 custody finding therefore stands and recurs: PE isolation
cannot be discharged by non-reading while working documents carry PE references.** Reported, not
remedied.

### 5.1 Self verb audit — **NOT CLEAN: three disclosures**

1. **My probe nearly produced a false negative against a correct citation.** Checking the
   "admissible action family" claim, my first `grep -o` pattern returned **zero** and I was about
   to record the citation as unverified. Widening the probe found it in **10 files**, and the
   source is a sealed **principal ruling** that is now the single most load-bearing fact in this
   artifact. **Third time this session my first probe was wrong and only a second pass saved it**
   (792 sidecar spelling; 804 basis extraction; here). The pattern is stable and it is instrumental,
   not inferential.
2. **The typing is a judgment and I mark it as one.** Sentences are quoted verbatim with spans; the
   OBJECT/ROLE assignment is a reading. Two adversarial lenses were commissioned precisely because
   a lane typing its own program's deepest node is the case where a preferred answer is most
   likely — and they **disagreed on two of six**, which is the honest measure of how contestable
   this is.
3. **T3's typing depended on where the span was cut** — the two readings sit 82 bytes apart in one
   table cell. I widened to the cell and typed MIXED, but I record the hazard generally: **this
   relay's whole method is span-typing, and a span boundary can manufacture a typing.**

*Direction check:* all three bound my own result. The verdict is MIXED rather than the cleaner
ROLE-DOMINANT that the relay's framing and the B0 precedent both invited.

---

```text
THREADS = 6 typed: OBJECT 2 (T1 ARCH section 5; T5 S1/S2 derive-and-vary) / ROLE 2 (T2 S28-PRIN;
   T4 S34) / MIXED 1 (T3 M05-B3) / MIS-BOUND 1 (T6 S16, STRUCK — zero "parent action" occurrences
   in 78,368 bytes; terminus is a typed carrier map).  All spans and digests re-derived from bytes;
   seal mode stated per law 8 (T1 mode 3 packet-manifest; the rest mode 1).
   Tally over the five that instantiate the question: OBJECT 2 . ROLE 2 . MIXED 1.
   THE SPLIT IS STRUCTURED: both OBJECT threads sit at the CONSTRUCTION end and both carry an
   explicit ANTI-SUBSTITUTION clause ("not an invitation to add a spectral-density principle";
   "do not patch or choose x") — the record anticipated role-discharge and BANNED IT BY NAME.
   Both ROLE threads sit at the SELECTION end and in both the sentence never names an
   action-functional: T2's is "the INDUCED action" under a column headed "What would constrain it
   lawfully", with PRIN's SINGULAR VERB settling it — "boundary closure plus the induced action
   SELECTS them" — one selector; T4's span contains no action-functional at all, and "action"
   entered that thread through OUR reading of "parent" as "parent action".
NATIVE_FORCERS = inventoried (refinement forcing; the intrinsic quadratic; incidence / the
   direction theorem; cofinality; boundary closure) AND TYPED BY OUTPUT: every one forces by
   ELIMINATION, by CONSTRAINT ON A SUBSPACE, or by INDEPENDENCE.  NOT ONE HAS EVER OUTPUT A UNIQUE
   POSITIVE VALUE.
TYPE_MATCHES = 0 clean / TYPE_GAPS = 2 (T2, T4), both the same shape: the role is real and the
   record's native mechanisms are of the WRONG OUTPUT TYPE to discharge it.  T4 asks for "one
   isolated positive stable solution" — a SELECTION — which is exactly what the record has never
   produced.  Role-typing RELOCATES the demand; it does not dissolve it.  This is where AXN-0
   differs from the B0 precedent, in which a native mechanism of the right type existed.
VERDICT = MIXED (split stated).  THE ROLE HALF: the principal's own S03 ruling of record already
   types the construction-end deficit — "the sealed stock admits an ADMISSIBLE ACTION FAMILY WITH
   NO SEALED SELECTOR; the family is not bounded or finitely enumerated", "declaring one minimal is
   not a derivation", with a sealed COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE and the ruling
   barring "selection from the construction end until the record itself forces one" (verified by
   me, seal OK).  So the action-object layer is NOT EMPTY — it is a FAMILY — and what is missing is
   a SELECTOR.  RECORD-NATIVE RESTATEMENT, taken from S03's own release condition (a): "BOUND OR
   CLASSIFY THE ADMISSIBLE ACTION FAMILY FROM RECORD STRUCTURE ALONE" — a bounding/elimination
   demand, which IS an output type the record's forcers possess.
   THE OBJECT RESIDUE: T1 and T5 are irreducible and no selector discharges them.  T1 demands a
   functional that R3's OWN sealed parent result does not contain — R3_4_COMPLETE_CAUSAL_
   SUPERCONNECTION_PARENT_RESULT_V001.md (sha 345d447eaf6d730c, 5,310 B) has ZERO occurrences of
   "action", counted by me, verdict FINITE_CAUSAL_PARENT_DERIVED_CONTINUUM_COMPLETION_OPEN — so
   the record's actual parent is OPERATOR-NATIVE and "action" enters exactly where the program
   needs a name for what would perform a selection.  T5 demands VARIATION, which admits one
   argument type.  Both are fenced by anti-substitution clauses the record placed deliberately.
   THE CAMPAIGN IS THEREFORE NOT ONE CAMPAIGN: a BOUNDING campaign on the family (role half,
   native output type available) plus a CONSTRUCTION obligation at two fenced nodes (object half,
   no substitute admitted).  Reporting either half alone would misdescribe the node.
FENCE_CHECK = S19/S21/S24/S25 respected.  S19: no decay inference, nothing imported to bridge a
   role.  S21: respected, and noted as CORROBORATING — it is itself a role-shaped decline ("derive,
   do not choose"), the same grammar as T2/T4.  S24: no role discharged by reaching for a
   clustering axiom; section 2 reports TYPE-GAPS rather than bridging them.  S25: respected, AND
   FLAGGED PRINCIPAL-FACING AS THE LIVE HAZARD OF THE ROLE READING — the most obvious cheap
   discharge of an action-role IS an equal-action principle, and S25 already bars it; a ROLE
   verdict must not become a licence to re-enter that refused route.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+3): (1) MY PROBE NEARLY PRODUCED A FALSE NEGATIVE AGAINST A CORRECT
   CITATION — my first grep for "admissible action family" returned ZERO and I was about to record
   the claim unverified; widening found it in 10 files, and its source is a sealed PRINCIPAL RULING
   that is now the most load-bearing fact in this artifact.  THIRD time this session my first probe
   was wrong and only a second pass saved it (792 sidecar spelling; 804 basis extraction; here) —
   the pattern is instrumental, not inferential; (2) THE TYPING IS A JUDGMENT and marked as one —
   sentences quoted verbatim with spans, the OBJECT/ROLE assignment a reading; two adversarial
   lenses were commissioned because a lane typing its own program's deepest node is exactly where
   a preferred answer is most likely, and they DISAGREED ON TWO OF SIX; (3) T3's typing DEPENDED ON
   WHERE THE SPAN WAS CUT — the two readings sit 82 bytes apart in one table cell; I widened to the
   cell, but record the hazard generally: this relay's whole method is span-typing, and a span
   boundary can manufacture a typing.
   PE DISCLOSURE: the ledger was never opened, searched, or quoted.  A commissioned reader reported
   encountering an incidental two-line PE reference inside the forcing-map artifact while reading
   T3's row; it stopped, did not follow, quoted nothing.  MY 777 CUSTODY FINDING THEREFORE STANDS
   AND RECURS: PE isolation cannot be discharged by non-reading while working documents carry PE
   references.  Reported, not remedied.
   All three disclosures bound my own result; the verdict is MIXED rather than the cleaner
   ROLE-DOMINANT that both the relay's framing and the B0 precedent invited.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
