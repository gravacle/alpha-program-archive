# STAGE 8 TASK 5 / R4+R12 - MAXWELL-SYMBOL CERTIFICATES - LANE 1 V001

Date: 2026-08-04  
Task: PASTE 487 / Task 5 / R4+R12  
Lane: Codex Lane 1  
Custody: derivation-first construction and exact typing

## Lead result

```text
C1_COMPLETION_CONTINUITY = TYPED_CONSTRUCTIBLE / CURRENTLY_OBSTRUCTED |
  BUILT: the unique-extension theorem and its exact uniform-seminorm
  hypotheses.
  OPEN: no ratified estimate controls a named local-kernel topology by the
  A4/profile norms.  Continuity would not by itself preserve finite
  faithfulness; COMPLETED_KERNEL_FAITHFULNESS is a separate required
  lower-bound/separation certificate.

C2_GEOMETRIC_REFINEMENT_LOCALITY = TYPED_CONSTRUCTIBLE / OBSTRUCTED_AT_J_REF |
  BUILT: the no-selection kinematic diagram retaining all 1,088 support
  filtrations as inert family labels, plus the conditional kernel-stitching
  theorem.  No tower is falsely assigned to a filtration.
  OPEN: the thin N<=M maps are identity zero-extensions, not geometric
  refinements.  They provide no mesh, cell volume/coframe, density map,
  common geometric refinement, boundary/contact estimate, or diagonal-local
  limit.  The first missing datum remains the target-independent geometric
  realization J_ref.

C3_WARD_CONTACT_HODGE = PARTIAL |
  BUILT CONDITIONALLY: once C1 supplies a continuous completed kernel map,
  the exact finite Ward identities extend by density and closedness.
  OPEN: the contact ideal, local order-two symbol, Hodge/magnetic partner,
  direct-sum theorem, domains, and normalization all depend on C2 plus named
  boundary/operator data.

COMPLETED_SYMBOL = PARTIAL_FINITE_CYLINDRICAL_ONLY |
  the finite/cylindrical conserved-current bilocal kernel calculus remains
  built and exact; a completed physical local Maxwell symbol is NOT_BUILT.

FIBER_CONSTRAINT_COMPLETED = NO_NEW_RELATION_PROVED |
  finite faithfulness and Ward transversality still impose no relation on
  (chi_K,T).  It is not yet lawful to strengthen this to a completed-level
  no-relation theorem: a continuous completion can acquire a kernel unless
  COMPLETED_KERNEL_FAITHFULNESS is proved.  If that certificate later holds,
  completion adds no relation; if it fails, the reader must annihilate the
  completed kernel and the resulting relation is determined by that kernel.

R12_STATUS = PARTIALLY_FACTORED_NOT_DISCHARGED |
  discharged already: T_cyl finite-core density/separation and the
  conditional composition theorem.
  still open: R12 fields 1-6 (Ref, J_ref, tau_ref, transport, response
  naturality, BoundaryCert); Task 6 additionally retains fields 7-8
  (PhysInterface and PublicEquiv).  No geometric/cellulation-independence
  obligation is discharged by renaming the thin tower.
```

The stitching lever is therefore real and load-bearing.  Any nonzero
continuum component created only by an unflagged bidual or weak-* step and
invisible to all finite restrictions would be tail-typed, colliding with
finite authority at the geometry level.  No such class formation is
performed here.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
numeric_evaluation = false
registered_verdict_written = false
member_selected = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 0. Preflight and authority sweep

### 0.1 Locked process and register

The locked process was read in full before work began.  The register head is
Q-408.

| Object | Verified SHA-256 / result |
|---|---|
| `alpha_supervision/LOCKED_PROCESS.md` | `e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2` |
| `QUESTIONS_SETTLED_REGISTER_V001.md` | `93762871b5214559d7c46ad6998d1e21aaa19b9d5590f81edc60e8d730dbdb3d`; sidecar OK |
| DoR-007 derive-the-limit ruling | `17dc023e297cfd6960a36623af2aa8efd57210574076590ef10b579b72c1808b`; sidecar OK |
| DoR-008 ratified falsifier | `d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19`; sidecar OK |
| Q-408 kernel build | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c`; sidecar OK |
| Q-407 partial R4 build | `bae34116c4d6792b5e39b913addeeff1650989660d89ba01bf5de62ec2d9aa50`; sidecar OK |
| Q-406 residue ledger | `c35ef84636ec1501bd99858538988711d0c717ac98671e6162abb0a0e5978c74`; sidecar OK |
| metric V005 / DoR-019 object | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961`; sidecar OK |
| Task-4d stitching half | `430f09715146cc03dabb0e349c422ae2499cff893d4e46b490fc0870954d1cb4`; sidecar OK |
| finite-incidence forcing protocol | `a4d8b9c44fd0705ba97fd49d1e0c8373c28e12e2c3acea9409b60217b274a0f8` |

```text
DOES_THE_OBJECT_EXIST = partially |
  finite/cylindrical kernel calculus yes;
  three completed certificates no
IS_THE_VERSION_CURRENT = yes | through Q-408
ARE_ITS_INPUTS_PRESENT = yes_for_conditional_theorems_and_exact_typing |
  no_for_instantiating_the_geometric_and_local_kernel_interfaces
PREFLIGHT = PASS_WITH_TYPED_BOUNDARY
```

### 0.2 Questions-settled sweep

The following entries and their cited artifacts were checked before any new
construction was attempted.

| Entry | Binding result |
|---|---|
| Q-217/Q-246 | a realization must carry algebra, representation, source incidence, degree, and intertwiners; a support ordering is not enough |
| Q-257/Q-258 | `T_cyl`, finite-core density, retractions, separation, and moving-tail exclusion are built; geometric `T_ref/T_phys` are not |
| Q-261 | the 1,088 survivors are support filtrations, not a family of geometric realizations; O-D3 remains open |
| Q-288 | every topology, completion, and class-formation step must be named; weak-* or bidual formation cannot be silent |
| Q-384 | the A4/DoR-019 Hilbert carrier, Riesz maps, units, W3 isometries, and adjoint restrictions exist; this is not a spacetime-symbol topology |
| Q-396 | completed content can be invisible on all active finite shadows; finite equality alone does not identify completed classes |
| Q-400 | the local Maxwell symbol is blocked on the covariant completed local-symbol map |
| Q-406 | R4 is live and R12 is constructible; R12 had remained decoupled only until the local-symbol route reached it |
| Q-407 | profile Banach topology and algebraic reader fiber `(chi_K,T)` are built; local field realization is not |
| Q-408 | finite/cylindrical Ward kernels are built; C1-C3 are the exact completed-symbol obstructions |

### 0.3 DoR-007 reading

DoR-007 requires the smooth-facing objects to arise from a **derived
discrete-to-continuum equivalence theorem over refinements**.  It expressly
rejects importing a smooth base into the alpha-facing chain.  Its content is:

```text
base smoothness exists only as a refinement limit;
cellulation independence is part of the theorem, not an input;
the public EM connection, local Maxwell tensor, Ward/transversality, and
Thomson-facing objects consume that derived equivalence.
```

Accordingly, the DoR-015 external-background family cannot be reused as an
unproved refinement category.  It is valid field-side input after the seam;
it is not the seam's derivation.

---

## 1. Common notation and the already-built finite calculus

At a finite admitted stage `G`, let

```text
K_G^fin = ker(B_G^T),
I_K,G(c) = u_c,
J_c = the distributional path-current realization of u_c,
H in Prof_G = R Rhat_K + S_1^sa(K_G),
Kern_G^fin(H) = the finite bilocal distribution kernel.
```

Metric V005 gives the A4 current norm and profile norm

```text
||c||_K = ||I_K,G c||_A4,
||b Rhat_K + C||_prof = |b| + ||C||_1.          (C0-1)
```

The Q-408 construction proves at every finite/cylindrical stage:

```text
c -> J_c is faithful;
H -> Kern_G^fin(H) is faithful;
Kern_G^fin(H)(d alpha,v) = 0;
Kern_G^fin(H)(u,d beta) = 0;                    (C0-2)
```

with exact covariance, reality, quotient, W3 restriction, identity
zero-extension, and ordered batching.  Equations `(C0-1)`-`(C0-2)` are the
stock extended or bounded below; they are not re-authored here.

---

## 2. C1 - completion-continuity

### 2.1 Exact certificate type

Fix a named Hausdorff complete locally convex kernel space

```text
Ker_tau = (Ker, {p_Q}_{Q in Q_tau}),             (C1-1)
```

whose finite restrictions contain all `Kern_G^fin(H)`.  A valid C1
certificate consists of:

```text
C1.a  p_q(J_c) <= C_q ||c||_K;
C1.b  p_Q(Kern_G^fin(H)) <= C_Q ||H||_prof;
C1.c  constants are uniform over the directed finite core and the complete
      no-selection realization family;
C1.d  restriction, reality, covariance, and batching maps are continuous in
      tau and commute on the finite core;
C1.e  the target is complete (or the image closure is declared as the target);
C1.f  completed faithfulness is separately proved by a separating lower
      bound or equivalent zero-kernel theorem.                 (C1-2)
```

The first five clauses are continuity/closability.  Clause `C1.f` is not
logically contained in them.

### 2.2 Unique-extension theorem - built

**Theorem C1-Ext.**  If `C1.a`-`C1.e` hold, the finite current and bilocal
kernel maps extend uniquely to continuous maps

```text
Jbar: K_G -> Cur_tau,
Kernbar: Profbar_G -> Ker_tau.                  (C1-3)
```

All continuous finite-core commuting squares continue to commute.

**Proof.**  Let `(H_n)` be profile-finite and Cauchy in `||.||_prof`.  For
every defining seminorm,

```text
p_Q(Kern_fin(H_n)-Kern_fin(H_m))
  <= C_Q ||H_n-H_m||_prof -> 0.                 (C1-4)
```

Completeness of `Ker_tau` gives a limit.  The same estimate makes the limit
independent of the approximating sequence and continuous.  Density gives
uniqueness.  If two continuous maps agree on the dense finite core, their
difference is zero.  Applying that observation to both routes around every
finite-core square proves covariance, reality, W3 restriction, and batching
after completion.  The current map is identical with `H` replaced by `c`.
QED.

```text
C1_UNIQUE_EXTENSION_THEOREM = BUILT / TYPE-P |
  premises: C1.a-C1.e
```

### 2.3 Why the premises do not currently instantiate

The A4/current and trace-class/profile norms control coefficients.  They do
not control any of the following:

```text
path length;
mesh scale or cell volume;
distribution order or derivatives;
wavefront/conormal class;
local concentration;
boundary concentration;
off-diagonal support.                            (C1-5)
```

The complete retained family has no bounded-geometry axiom from which a
uniform `C_Q` could be derived.  DoR-019's completion convention therefore
builds an abstract Hilbert/profile completion, not continuity into a local
distribution-kernel topology.

```text
UNIFORM_A4_TO_LOCAL_KERNEL_ESTIMATE = NOT_BUILT / TYPE-U
NAMED_LOCAL_KERNEL_TOPOLOGY = NOT_BUILT / TYPE-U
C1_INSTANTIATED = false
```

This is an obstruction-by-missing-estimate, not a theorem that no future
geometric package can supply one.

### 2.4 The completed-faithfulness control

Finite injectivity plus a continuous extension does not imply completed
injectivity.  The abstract control is exact.  Let `D` be a dense linear core
in a Hilbert space `H`, choose a nonzero `v` with `D intersect span{v}={0}`,
and let `P` be orthogonal projection onto `v^perp`.  Then

```text
P|_D is injective,
P:H -> H is continuous,
ker(P)=span{v} != {0}.                           (C1-6)
```

Thus Q-408's finite faithfulness cannot be promoted by density alone.  The
needed certificate can be stated either as

```text
||H||_prof <= C s(Kern_fin(H))                   (C1-7)
```

for a separating continuous target seminorm/family `s`, uniformly on the
finite core, or directly as

```text
ker(Kernbar) = {0}.                              (C1-8)
```

```text
COMPLETED_KERNEL_FAITHFULNESS = NOT_BUILT / TYPE-U
C1_CONTINUITY_IMPLIES_COMPLETED_FAITHFULNESS = false | TYPE-R |
  witness: (C1-6)
```

This clause is failure-capable: a nonzero completed profile in
`ker(Kernbar)` refutes it.

---

## 3. C2 - geometric refinement/locality, the stitching lever

### 3.1 What can be built without selection

Let `F_1088` be the complete retained set of `K_square` support filtrations.
Separately, the ratified thin category has objects `N` and a unique order
arrow `N<=M`, represented by identity zero-extension and its W3 adjoint
restriction.  The corpus does **not** supply a tower for each filtration.
Without choosing an `f`, one may only form the constant family object

```text
CylFam_N := F_1088 x T_cyl,N,
j_NM^fam(f,x_N) := (f,j_NM x_N).                 (C2-1)
```

The support-filtration coordinate is inert.  Permutations/relabelings of the
filtration family act on `(C2-1)` and no member is distinguished.  No map
from a filtration into the tower has been asserted.

The finite core remains dense in each component; retractions separate the
norm/module class; the moving-tail sequence has no limit in the ratified
source topology.  Therefore:

```text
NO_SELECTION_FAMILYWISE_KINEMATIC_DIAGRAM = BUILT / TYPE-P
```

This is the maximal lawful construction from the existing thin data.

### 3.2 Why `(C2-1)` is not geometric refinement

The maps in `(C2-1)` append identity cells.  They do not specify:

```text
a physical region represented by a cell;
a mesh parameter h -> 0;
cell volume, coframe, edge length, or face area;
a current-to-density normalization;
a connected many-cell response;
a common geometric refinement of two cellulations;
a boundary/contact ideal or subextensive estimate.            (C2-2)
```

The 1,088 elements are linear-extension/support filtrations.  Q-261 proves
they were never a completed realization family.  Consequently, the product
in `(C2-1)` compares bookkeeping components, not two discretizations of the
same geometry.

The underdetermination can be seen without a response calculation.  Attach
two different positive stagewise cell-volume systems to the same thin
incidence/zero-extension data.  Every currently sealed incidence, support,
restriction, and coefficient-norm fact remains unchanged, while the
normalized current density and any order-two local symbol scale differently.
Because no volume/coframe datum is in the thin system, it cannot distinguish
the two.  Selecting one after inspecting a Maxwell coefficient would be
target tuning.

```text
THIN_ZERO_EXTENSION_IS_GEOMETRIC_REFINEMENT = false | TYPE-R
FILTRATION_FAMILY_FORCES_DENSITY_NORMALIZATION = false | TYPE-R
```

### 3.3 The exact live R12 slice

The R4 local-symbol route needs the following specialization of Task-4d's
eight-field package:

```text
R12_Maxwell := (
  1. Ref, a target-independent category of finite record-complex
     refinements with common refinements;

  2. J_ref^cur, a realization natural transformation carrying incidence,
     degree, source data, connection variation, and conserved currents;

  3. tau_ker, one frozen current/kernel topology, with no weak-* or bidual
     moving-tail class formation;

  4. eta_conn, eta_curv, and density normalization from coframe/volume data,
     coherent on common refinements;

  5. eta_resp, the connected response natural transformation and its W3
     pullback square;

  6. BoundaryContactCert, a failure-capable boundary-subextensivity and
     off-diagonal-to-contact theorem in tau_ker
).                                                (C2-3)
```

Task 6 additionally consumes Task-4d fields 7-8, `PhysInterface` and
`PublicEquiv`.  They are not silently pulled into R4.

The first absent field in `(C2-3)` is `J_ref^cur`.  It cannot be a support
ordering: Q-217/Q-246 require algebra, representation, source incidence,
degree, and intertwiners.  No current authority constructs it.

### 3.4 Conditional kernel-stitching theorem - built

**Theorem C2-Stitch.**  Assume `(C2-3)` and the C1 estimates hold, and assume:

```text
(i)   every pair of cellulations representing the same finite region has a
      common refinement in Ref;
(ii)  J_ref^cur and eta_resp commute with pullback to that refinement;
(iii) the density-normalized off-diagonal remainder converges into the named
      contact/boundary ideal in tau_ker;
(iv)  BoundaryContactCert sends the intensive boundary difference to zero;
(v)   the finite kernel Ward identities and DoR-008 restriction squares hold
      on every refinement;
(vi)  the P7 class-transport predicates hold for tau_ker and the response
      maps.                                      (C2-4)
```

Then the completed response kernel has a unique cellulation-independent
class modulo the named contact/boundary ideal.  Its finite restrictions are
the Q-408 kernels, and its Ward identities are independent of the chosen
cofinal refinement family.

**Proof.**  Pull two finite representatives to a common refinement by `(i)`.
Naturality `(ii)` makes their current and response images agree there up to
the boundary/contact remainder.  Clauses `(iii)`-`(iv)` make that remainder
zero in the quotient limit in the one topology `tau_ker`.  C1 continuity
gives representative-independent completion.  P7 separation excludes a
nonzero norm/module-class difference invisible on every finite restriction.
Finally `(v)` and continuity carry the Ward and DoR-008 squares to the limit.
No cellulation member is selected.  QED.

```text
C2_CONDITIONAL_KERNEL_STITCHING_THEOREM = BUILT / TYPE-P |
  premises: C2-3, C2-4, and C1
C2_PREMISES_INSTANTIATED = false
C2 = OBSTRUCTED_AT_J_REF / TYPE-U
```

### 3.5 DoR-007 consequence

DoR-007 is consumed, not bypassed.  The adopted external background may be
the target of a future `PublicEquiv`; it cannot be used to define `Ref` or
`J_ref^cur`, because that would assume the smooth object whose derivation and
cellulation independence DoR-007 requires.

```text
SMOOTH_BACKGROUND_USED_AS_REFINEMENT_INPUT = false
GEOMETRIC_REFINEMENT_MEMBER_SELECTED = false
```

---

## 4. C3 - Ward/contact/Hodge on the completed carrier

### 4.1 Ward extension - conditional theorem built

For fixed exact one-form `d alpha`, define on the finite kernel class

```text
W_alpha(K) := K(d alpha, .).                     (C3-1)
```

and analogously on the second leg.  If C1 makes these contractions continuous
in `tau_ker`, then their kernels are closed.  Q-408 gives

```text
W_alpha(Kern_fin(H)) = 0                         (C3-2)
```

on the dense finite core.  Therefore the continuous extension vanishes on
its closure.

```text
COMPLETED_WARD_TRANSVERSALITY = TYPE-P_CONDITIONAL |
  premises: C1 plus continuous exact-form contraction
```

This proof is family-wide and does not choose a frame, orientation member,
cycle basis, or reader.

### 4.2 Contact split - not built

A physical contact split requires a closed invariant ideal `I_contact` and a
failure-capable direct sum or quotient statement such as

```text
Ker_tau = Ker_phys direct-sum I_contact
or
Ker_phys = Ker_tau / I_contact.                  (C3-3)
```

The finite path-bilocal kernel is supported on `gamma_e x gamma_f`, including
off-diagonal pairs.  Algebraic Ward transversality does not make that support
diagonal and does not identify which boundary-supported terms are contact.
`BoundaryContactCert` from C2 is therefore a strict prerequisite.

```text
COMPLETED_CONTACT_IDEAL = NOT_BUILT / TYPE-U
WARD_IMPLIES_DIAGONAL_LOCALITY = false | TYPE-R
```

### 4.3 Hodge/magnetic partner - not built

The adopted Lorentzian background family can carry a formal memberwise Hodge
star, but the required completed response theorem additionally needs:

```text
a proved local order-two operator class;
a domain and boundary condition;
a covariant electric/magnetic decomposition or family-wide substitute;
closed range and harmonic-sector control;
a direct-sum theorem compatible with reality, W3, batching, and units;
normalization p_loc(L_T)=1 in the same local-symbol topology. (C3-4)
```

No Cauchy slice, positive spectral generator, self-adjoint domain, or
boundary condition is selected by the ratified family.  A microlocal route
could avoid a spectral choice, but it still needs C2's conormal-to-diagonal
kernel class.  Thus neither route is presently instantiated.

```text
COMPLETED_HODGE_MAGNETIC_PARTNER = NOT_BUILT / TYPE-U
PHYSICAL_WARD_CONTACT_HODGE_SPLIT = PARTIAL
```

### 4.4 C3 composition rule

If C1 and C2 instantiate, a future C3 package must prove all of:

```text
closed Ward kernel;
closed contact/boundary ideal;
covariant quotient/direct sum;
local order-two symbol and magnetic partner;
domain and harmonic-sector statement;
p_loc(L_T)=1 in that topology;
kernel intersections with every rank-one profile;
DoR-008, reality, W3, batching, and R4-unit squares. (C3-5)
```

Failure of any row leaves the physical reader family unenumerated.

---

## 5. C4 - completed-symbol and fiber determination

### 5.1 Status of the Maxwell symbol

The lawful chain is now exactly:

```text
finite/cylindrical Ward bilocal kernels              BUILT
  -> C1 continuous completed local-kernel map        OPEN
  -> C2 cellulation-independent diagonal-local class OPEN
  -> C3 Ward/contact/Hodge symbol class               OPEN
  -> normalized physical local reader p_loc          OPEN.   (C4-1)
```

Therefore:

```text
COMPLETED_MAXWELL_SYMBOL = PARTIAL / NOT_BUILT
```

The word `completed` cannot mean merely the A4 Hilbert completion.  It must
name the kernel topology and locality theorem in `(C4-1)`.

### 5.2 Finite fiber result retained

For the built algebraic reader family,

```text
p_(chi,T)[Rhat_K] = chi_K,
p_(chi,T)[H_x]
  = f(s) chi_K + 2 f_1(s) <x,T x>_K,
s = ||x||_K^2.                                  (C4-2)
```

The reciprocal profile retains one coordinate; the S8-A exchange profile
retains `(chi_K,tau_cross)` before additional stabilizer relations.  Every
finite current kernel is Ward-transverse for every `(chi_K,T)`, so C3's Ward
half adds no relation.

### 5.3 Does completion add a relation?

Let `N_comp=ker(Kernbar)` if C1 is eventually instantiated.  A physical
reader on the represented completed kernel must factor through the quotient,
so its pullback must satisfy

```text
p_(chi,T)(N_comp) = 0.                           (C4-3)
```

There are two exact horns:

```text
COMPLETED_KERNEL_FAITHFULNESS true:
  N_comp={0}; completion adds no relation to (chi_K,T).

COMPLETED_KERNEL_FAITHFULNESS false:
  (C4-3) is a genuine additional relation, determined by N_comp; it may
  reduce the reader fiber but may not be selected by outcome.             (C4-4)
```

Current authority proves neither horn.  DoR-008 fixes all finite shadows but
cannot by itself exclude a completed class with zero finite shadow; Q-396 is
the standing warning.  Consequently the strongest honest finding is:

```text
FIBER_CONSTRAINT_FINITE = NO_NEW_RELATION / TYPE-P
FIBER_CONSTRAINT_COMPLETED = UNDECIDED_ON_COMPLETED_KERNEL / TYPE-U
NO_NEW_COMPLETED_RELATION_ASSERTED = false
```

No constraint is invented, and no existing coordinate is deleted.

---

## 6. Restriction, topology, and six-account ledger

| Account | What is built | What remains / door status |
|---|---|---|
| measure | no measure introduced; A4 coefficient norm and formal paths only | cell volumes/coframes and current-density normalization are C2 / OPEN |
| contour | finite DoR-009/016 CTP ordering and reality carried componentwise | no continuum contour formed; not needed until the local response interface |
| boundary/contact | finite exact Ward annihilation | `BoundaryContactCert`, contact ideal, and subextensivity are C2/C3 / OPEN |
| domain closure | DoR-019 A4/profile Hilbert/Banach completions with W3 adjoints | local distribution/operator topology, domains, closed range, and completed faithfulness are C1/C3 / OPEN |
| stationary Schur | no Schur or inverse is formed in this construction | future local symbol must route units through R4 and preserve the ratified response domain |
| class formation | norm/Banach completions and the family product `(C2-1)` are named; no weak-* or bidual step | `tau_ker` and geometric cofinal limit are OPEN and must be door-flagged |

```text
WEAK_STAR_COMPLETION_USED = false
BIDUAL_EMBEDDING_USED = false
UNFLAGGED_CLASS_FORMATION = false
```

Every restriction statement made here is one of:

```text
finite exact restriction (already proved);
W3 adjoint restriction on rank-preserving maps;
conditional continuity/naturality after C1-C2.
```

No cycle-creating upward map or geometric common-refinement map is inferred
from a W3 adjoint.

---

## 7. Choice table and provenance

No authored structure is installed by this artifact.  The still-open
alternatives are disclosed so a later proposal cannot hide a selection.

| Field | Retained alternatives | Void conditions | Standing |
|---|---|---|---|
| local kernel topology `tau_ker` | distribution seminorm family; Sobolev-type family; conormal/microlocal family; reject | chosen for a desired `chi_K`, modulus, fixed point, or response; topology switch; moving-tail admission | OPEN / no member selected |
| geometric realization | target-independent finite-complex functor family; independently justified equivalent presentation; reject | smooth target assumed; support filtration relabeled as geometry; missing source/degree/intertwiner data | OPEN |
| density normalization | coframe/volume-derived natural density; equivalent family with proved public equivalence; reject | desired Maxwell coefficient fixes scale; no common-refinement law | OPEN |
| locality/contact | boundary quotient; direct-sum ideal; independently proved equivalent class; reject | record-visible off-diagonal content deleted; no boundary theorem | OPEN |
| Hodge/symbol route | covariant microlocal principal symbol; family-wide spectral route with domains; reject | selected slice/frame/domain; missing conormal local class; target-tuned normalization | OPEN |
| completed faithfulness | lower-bound/separation theorem; direct zero-kernel theorem; retain a disclosed quotient kernel | finite injectivity asserted to imply completed injectivity | OPEN |

```text
AUTHORED_FIELDS_INSTALLED = 0
RANK_SELECTED = false
RATIO_EVALUATED = false
ORIENTATION_SELECTED = false
FRAME_SELECTED = false
FILTRATION_SELECTED = false
REALIZATION_SELECTED = false
READER_SELECTED = false
```

---

## 8. C5 - falsifiers and regressions

### F1. Finite restriction reproduction

The constructed statements leave the Q-408 finite kernel unchanged.  On
every finite stage, `(C0-2)` remains exact.  C1-Ext and C2-Stitch expressly
require the completed restriction square to return that kernel.

```text
FINITE_KERNEL_REPRODUCTION = PASS
```

### F2. Reciprocal-loop and S8-A profiles

No relation is added to `(C4-2)`.  The reciprocal profile remains
one-coordinate and the S8-A exchange profile remains two-coordinate before
extra stabilizers.  The finite Ward identity vanishes for both.

```text
RECIPROCAL_PROFILE_REGRESSION = PASS
S8A_PROFILE_REGRESSION = PASS
```

### F3. Identity zero-extension and batching

The family object `(C2-1)` uses the existing componentwise identity
zero-extension and ordered-pair batching only.  It does not multiply network
components or interpret the new cell as a mesh refinement.

```text
IDENTITY_ZERO_EXTENSION = PASS
ORDERED_BATCHING = PASS
```

### F4. Reality, relabeling, and no-selection

All finite equations are equivariant under signed relabeling; reality acts by
conjugation.  The full filtration product is retained.  No frame, orientation,
cycle basis, or filtration member is fixed.

```text
REALITY_COVARIANCE = PASS
FAMILY_RELABELING_COVARIANCE = PASS
NO_SELECTION = PASS
```

### F5. Moving-tail test

No weak-* or bidual completion is used.  `tau_ker` remains open rather than
being replaced by a topology in which a moving tail converges.

```text
MOVING_TAIL_REINTRODUCED = false | PASS
```

### F6. Cellulation/packing falsifier

A future C2 certificate fails if two cofinal cellulations of the same finite
region yield different intensive kernel classes after common-refinement
pullback, or if the boundary/contact difference does not vanish in the one
declared topology.  This is executable once `Ref`, `J_ref^cur`, and
`tau_ker` exist.

```text
CELLULATION_INDEPENDENCE_TEST = DECLARED / NOT_YET_EXECUTABLE
```

### F7. Completed-faithfulness falsifier

A future C1 package fails if there is an A4/profile-Cauchy sequence `H_n`
with nonzero completion class but

```text
Kern_fin(H_n) -> 0 in tau_ker.                   (F7-1)
```

The control `(C1-6)` proves this check is logically independent of finite
injectivity.

```text
COMPLETED_FAITHFULNESS_TEST = DECLARED / NOT_YET_EXECUTABLE
```

### F8. Ward/contact anti-conflation

The existing finite Ward identity passes for kernels with off-diagonal path
support.  Hence any package claiming that Ward alone proves diagonal
locality is refuted by the existing Q-408 kernel family.

```text
WARD_IMPLIES_LOCALITY_ATTACK = REFUTED / PASS
```

### F9. Anti-tuning ledger

The derivation order was:

```text
finite kernel -> completion certificate -> refinement certificate ->
contact/Hodge certificate -> possible reader constraints.    (F9-1)
```

No `chi_K`, `T`, modulus, fixed point, alpha consequence, rank, or measured
quantity was inspected to choose a topology, density, contact ideal, or
Hodge route.

```text
ANTI_TUNING = PASS
```

---

## 9. Final board

| Certificate | Result | Exact next object |
|---|---|---|
| C1 completion-continuity | `TYPED_CONSTRUCTIBLE / OBSTRUCTED` | named `tau_ker`, uniform `(C1-2)` estimates, and completed-faithfulness theorem |
| C2 geometric refinement/locality | `TYPED_CONSTRUCTIBLE / OBSTRUCTED_AT_J_REF` | `R12_Maxwell` fields `(C2-3)`, beginning with `J_ref^cur` |
| C3 Ward/contact/Hodge | `PARTIAL` | instantiate C1/C2, then build `(C3-5)` |
| completed Maxwell symbol | `PARTIAL_FINITE_CYLINDRICAL_ONLY` | compose C1-C3; no shortcut |
| completed fiber relation | `NO_NEW_RELATION_PROVED; COMPLETED_KERNEL_OPEN` | decide `ker(Kernbar)` before quotienting the reader fiber |
| R12 | `PARTIALLY_FACTORED_NOT_DISCHARGED` | fields 1-6 for R4; fields 7-8 remain Task-6 public equivalence |

The primary construction result is the conditional stitching theorem
`C2-Stitch`; the primary obstruction is still the absence of a
target-independent geometric realization `J_ref^cur`.  The primary new
precision is that completed faithfulness must be certified separately from
continuity.  Nothing in the existing finite record authorizes deleting a
reader direction before that question is answered.

```text
COMPLETED_SYMBOL = PARTIAL_FINITE_CYLINDRICAL_ONLY
FIBER_CONSTRAINT_COMPLETED = UNDECIDED_ON_COMPLETED_KERNEL
R12_STATUS = PARTIALLY_FACTORED_NOT_DISCHARGED

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
