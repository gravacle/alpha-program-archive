# Stage 8 `Gen_Omega` Generative-Origin-Rule Adoption Proposal v002

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

Date: 2026-08-02  
Lane: CODEX LANE 2  
Task: 4a  
Register head checked at completion: Q-261

## 0. Lead determination

**The repaired generativity condition is: an admitted preparation channel must
be uniformly input-faithful on the equal-trace charge-superselected difference
space, and its nonzero transient action must be carried by an explicit finite
operational certificate.** This condition is stated entirely on the channel's
input/output action before its invariant state is solved. It mentions no
countermodel state and no downstream result.

The repair has all three required properties:

1. every replacement channel is excluded because it annihilates every
   equal-trace difference;
2. the family is nonempty: strict affine mixing channels with nonzero transient
   coefficient satisfy the condition, have one invariant state, and provide an
   exact lower-bound certificate; and
3. Q-242's old bag/replacement countermodel no longer kills a fixed or family
   member: distinct state outputs now require operationally distinct channels
   whose transient action is finitely testable independently of the fixed
   point. The state family remains open, but its members are no longer terminal
   fields renamed as information-free channels.

This is not a selection of `rho_S` or `p_ch`. The repaired map from channels to
the record-visible parameter is explicitly many-to-one. The physical parameter
is the invariant

```text
kappa_ch(P_src):=Tr(rho_P P_ch),
```

or, equivalently, the fiber quotient of preparation channels with the same
charged invariant marginal. Q-236's finite amplitude sees exactly this
quotient and no transient-channel coordinate.

The ready/pointer wound is also repaired honestly. V002 carries the exchanged
root orientations as an explicit binary family. At zero history the exchange
operator commutes with the ratified write. Away from zero history, the
ratified `E_post` endpoint orientation supplies the licensed time-oriented
breaking in the law; it does not retroactively derive one root orientation.

```text
GEN_OMEGA_V002_STATUS = PROPOSED_NOT_ADOPTED
INPUT_FAITHFUL_GENERATIVITY_CONDITION_STATED = true
REPLACEMENT_CHANNELS_ADMITTED = false | TYPE-R |
  test: every replacement channel has zero lower modulus on the declared
        equal-trace difference space
REPAIRED_FAMILY_NONEMPTY = true
  [PROPOSAL EXISTENCE THEOREM; NOT ADOPTION]

Q242_REPLACEMENT_COUNTERMODEL_KILLS_V002 = false | TYPE-R |
  test: replacement maps fail membership, while distinct surviving origins
        have independently observable transient action

Q242_STATE_FAMILY_SELECTED_BY_V002 = false | TYPE-U |
  missing: a rule selecting/deriving one channel fiber or one invariant marginal

P_CH_CHANNEL_MAP_IS_MANY_TO_ONE = true
PHYSICAL_PARAMETER = kappa_ch(P_src)_FIBER_QUOTIENT
  [PROPOSAL ACCOUNTING; NOT A VALUE]

Q254_BATTERY_PASS_COUNT = 11 [PROPOSAL TEST COUNT; NOT RATIFICATION]
Q254_BATTERY_CONDITIONAL_COUNT = 3 [PROPOSAL TEST COUNT; NOT RATIFICATION]
Q254_BATTERY_FAIL_COUNT = 0 [PROPOSAL TEST COUNT; NOT RATIFICATION]

DOR_013_ELIGIBILITY_VERDICT = ELIGIBLE_FOR_CROSS_LANE_ADVERSARIAL_REVIEW
```

Nothing in this proposal has `TYPE-P` standing. Ratification is reserved to
the principal after independent review.

No response, second variation, kernel, coupling, scale, physical residual,
root, or measured-constant comparison is evaluated.

## 1. Currency, authorities, scope, and V001 preservation ledger

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

### 1.1 Preflight

```text
DOES_THE_OBJECT_EXIST = YES_AS_V001_AND_KILL_RECORD; V002_IS_THIS_PROPOSAL
IS_THE_VERSION_CURRENT = true_through_Q261
ARE_THE_INPUTS_PRESENT = true
  V001, Q-260 kill, Q-242 certificates, Q-254 battery, DoR-009 law
```

Q-261 concerns cellulation independence and proves that the proposed local
incidence primitive supplies neither `J_ref` nor charter O-D3. V002 preserves
that scope ceiling and makes no Task-4d claim.

### 1.2 Controlling authorities

| Authority | Use |
|---|---|
| `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V001.md` (`f2043a2a...`) | superseded proposal; A1/A4/A7 limbs preserved only after fresh V002 checks |
| `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADVERSARIAL_KILL_DETERMINATION_V001.md` (`a340f007...`) | Q-260 attacks A1-A7, replacement counterexample, circular B14, exchange wound and fiber correction |
| `STAGE8_MINIMAL_OMEGA_P5_COMMON_ORIGIN_ROLE_REALIZATION_ATTEMPT_V001.md` | Q-242 root-not-bag criterion and original two-state countermodel |
| `STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md` | executable maps, common carrier, covariance/domain/provenance and target-independence certificates |
| `STAGE8_LAW_READY_STATE_SEALED_COMMON_ORIGIN_DETERMINATION_V001.md` | Q-240 zero-history exchange and failure of law-only ready selection |
| `STAGE8_SOURCE_COUPLED_RECORD_TRANSITION_LAW_ADOPTION_PROPOSAL_V002.md` plus DoR-009 | exact `E_post` law and C1-C8 |
| `STAGE8_SHARED_FINITE_STATE_EFFECT_CONDITIONED_AMPLITUDE_SHARP_STOP_V001.md` | Q-236 central quotient and exact record-visible role of `p_ch` |
| `STAGE8_TASK4A_MISSING_PHYSICAL_LAYER_CONSOLIDATED_SPEC_AND_CONSTRAINT_BATTERY_V001.md` | B1-B14 |
| Q-261 | local incidence is not a geometric realization or O-D3 discharge |

### 1.3 Search scope and queries

Roots entered:

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003
/Users/bgm/MB Work/alpha-program-archive/workspace
/Users/bgm/MB Work/alpha-program-archive/cleanroom_output
/Users/bgm/MB Work/alpha-program-archive/supervision
/Users/bgm/MB Work/alpha_supervision
```

Excluded:

```text
a32_holdout/custodian_private/ [not entered]
.git/
binary/media payloads
archive mirrors as independent authorities
superseded material except the V001->kill lineage
```

Case-insensitive query families actually run:

```text
Q-260 | Q-261 | replacement channel | R_rho | P_src
input faithful | trace class | mixing | transient | fixed point
rho_S | p_ch | P_0 | P_ch | CAR(H_src) | scalarization
ready | pointer | exchange | E_post | C1-C8
B1 ... B14 | common origin | root not bag | no supplementation
```

`rg` was unavailable; `find`, `grep`, `sed`, and `nl` were used.

### 1.4 V001 limb preservation

| Kill row | Required V002 treatment | Fresh result |
|---|---|---|
| A1 incidence/filtration selection | preserve local one-cell type and Task-4d ceiling | **SURVIVES**; no global filtration/refinement field added |
| A2 family-level replacement attack | strengthen G1 structurally | **REPAIRED** by Sections 3-4 |
| A3 conditionals/B14 | replace circular acceptance premise with executable tests | **REPAIRED** by Sections 7-8 |
| A4 law fidelity | reproduce DoR-009 exactly | **SURVIVES** after fresh C1-C8 audit |
| A5 ready/pointer exchange | expose exchanged-root family and consistency square | **REPAIRED WITH AN OPEN BINARY** |
| A6 channel-to-`p_ch` accounting | replace one-to-one claim with fiber quotient | **REPAIRED** |
| A7 finite authority | retain finite transient witness | **SURVIVES** and becomes membership data |

### 1.5 Symbol distinctions

```text
T_1^ss          trace-class charge-superselected inputs, not a state family
Delta_0^ss      equal-trace difference space, not a response perturbation
m_in(P)         input-faithfulness lower modulus, not a spectral gap
rho_P           unique invariant density generated by P
kappa_ch(P)     charged invariant marginal, the record-visible quotient
Fib_p           channel fiber at fixed kappa_ch, not an equivalence of laws
X_rp            ready/pointer exchange, not CTP branch exchange
epsilon_root    authored root-orientation member, not E_post itself
```

## 2. Primitive stratum v002

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

For every finite `N>=1`, freeze before any output:

```text
Omega_prim,N^v002 := (
  A_src, H_src, P_0, P_ch,
  P_src, Cert_IF(P_src),
  K_cell, iota_cell, epsilon_root,
  chi_n with n in {+1,-1},
  E_post,
  tensor/zero-extension grammar,
  declared bounded finite domains
).
```

Terminal objects `rho_S`, `|R_N>`, and `U_N[a]` are absent from this tuple.

### 2.1 Choice table

| Row | Field | V002 content | Alternatives | Void condition |
|---|---|---|---|---|
| G1-v2 | input-faithful mixing preparation channel | normal CPTP, charge-covariant, unique mixing invariant state, positive input modulus on `Delta_0^ss` | replacement/reset; nonmixing channel; state coordinate | void if any membership or lower-modulus test fails |
| G1b-v2 | finite transient operational certificate | declared probe difference `Delta` and nonzero `P_src(Delta)` with the uniform lower-bound proof | mere availability without a witness; a downstream response target | void if witness depends on the solved state or on downstream output |
| G2-v2 | complete invariant-density descent | all source-density blocks are the unique invariant state of G1-v2 | central marginal only; separately supplied blocks | void if any block is post-supplemented |
| G3-v2 | rooted signed-incidence realization | V001's local one-cell object and exact one-cell representation | BID-root transport; K-square filtration; global refinement object | void if the exact carrier/write certificate fails |
| G3b-v2 | root-orientation binary `epsilon_root` | both exchanged root/successor presentations are admitted and priced | silently choose one; claim the law selects one | void if the zero-history exchange square fails |
| G4-v2 | signed-incidence write | exact local `S_inc` | alternate complement sign or terminal `S` field | void on C1/C6 disagreement |
| G5-v2 | faithful character plus ratified `E_post` | exact written-endpoint attachment | `E_conj`, pre-only, nonfaithful | void on C4/C5/C8 failure |
| G6-v2 | finite tensor/zero extension | exact `N`-cell DoR-009 grammar | cross-cell interaction | void on C7 failure |

New authored content relative to sealed/ratified data is G1-v2/G1b-v2 and the
already-disclosed G3-v2/G3b-v2 carrier orientation. No topology, measure,
contour, state coordinate, response, or target is hidden in a definition.

## 3. The repaired non-circular generativity condition

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

Let

```text
T_1^ss := {tau trace-class : tau is charge-superselected},

Delta_0^ss
  := {Delta in T_1^ss : Tr(Delta)=0}.
```

For a normal CPTP charge-covariant channel `P`, define its input modulus

```text
m_in(P)
  := inf { ||P(Delta)||_1 :
           Delta in Delta_0^ss, ||Delta||_1=1 }.
```

The V002 family is

```text
G1_v002 := {
  P :
    P is normal, CPTP and charge-covariant;
    P commutes with the charge-superselection conditional expectation;
    P has one normalized positive trace-class invariant state rho_P;
    P^m(tau) -> rho_P on the declared normalized basin;
    m_in(P)>0;
    Cert_IF(P) supplies the lower bound and one finite-rank
    Delta_test in Delta_0^ss with P(Delta_test)!=0
}.
```

Every clause is stated on channel inputs, outputs, iteration and norm. The
membership definition contains no `rho_P` value, no `p_ch` value, no named
countermodel state, no response, and no desired downstream verdict. Unique
fixed-point existence is a failure-capable property, not a supplied coordinate.

```text
INPUT_FAITHFUL_CONDITION_IS_ANSWER_DEFINED = false | TYPE-S |
  scope: the displayed G1_v002 membership clauses

INPUT_FAITHFUL_CONDITION_CAN_FAIL = true
  [STRUCTURAL FACT: zero-modulus channels fail]
```

The lower modulus is not called a physical spectral gap and no eigenvalue is
computed.

## 4. R1 proofs

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

### 4.1 Every replacement channel fails

For any normalized charge-superselected density `rho`, the replacement map is

```text
R_rho(tau)=Tr(tau)rho.
```

For every nonzero `Delta in Delta_0^ss`,

```text
R_rho(Delta)=Tr(Delta)rho=0.
```

Hence

```text
m_in(R_rho)=0.
```

The result is independent of `rho`; no countermodel state appears in the
condition or proof.

```text
ALL_REPLACEMENT_CHANNELS_FAIL_G1_V002 = true
REPLACEMENT_FAILURE_REASON = ZERO_INPUT_MODULUS
```

### 4.2 The repaired family is nonempty

Let `sigma` be any normalized charge-superselected trace-class state whose
existence is already instantiated by the finite source-state family. Let
`lambda` satisfy the structural membership interval `0<lambda<1`. Define

```text
P_(sigma,lambda)(tau)
  := lambda tau +(1-lambda)Tr(tau)sigma.
```

Then:

1. it is normal, CPTP, trace preserving and charge covariant;
2. it commutes with charge superselection;
3. its unique normalized fixed point is `sigma`;
4. its iterates satisfy

   ```text
   P_(sigma,lambda)^m(tau)
     =lambda^m tau +(1-lambda^m)Tr(tau)sigma,
   ```

   hence mix to `sigma` on the normalized basin; and
5. for `Delta in Delta_0^ss`,

   ```text
   P_(sigma,lambda)(Delta)=lambda Delta,
   m_in(P_(sigma,lambda))=lambda>0.
   ```

Any nonzero finite-rank normalized difference supplies `Cert_IF`. Thus the
family is nonempty on the actual trace-class source type without importing a
normalized identity, Haar state, Gibbs rule, finite cutoff, or scalar measure.

```text
G1_V002_NONEMPTY = true
NONEMPTY_WITNESS_CLASS = STRICT_AFFINE_MIXING_CHANNELS
NORMALIZED_IDENTITY_OR_HAAR_STATE_IMPORTED = false | TYPE-S |
  scope: this construction
```

### 4.3 The Q-242 countermodel rerun at family level

Hold the incidence carrier, root-orientation member, character, `E_post`,
history and generated law fixed. The literal Q-260 witnesses
`R_(rho^(0))` and `R_(rho^(ch))` are not members because their input modulus is
zero.

The broader state family still has surviving origins such as
`P_(rho,lambda)`. This is intentional and does not violate Q-194: distinct
microscopic origins may descend to distinct state packages. The family-level
anti-relabeling question is whether the source state can be replaced while
**all nonterminal origin behavior remains unchanged**.

It cannot. If `sigma!=sigma'`, then for normalized input `tau`,

```text
P_(sigma,lambda)(tau)-P_(sigma',lambda)(tau)
  =(1-lambda)(sigma-sigma'),
```

and each map also carries the certified trace-zero action
`Delta -> lambda Delta`. The two source choices are therefore different
preparation operations with finite operational outputs, not identical weak
tuples whose only difference is a terminal state coordinate.

For a general G1-v2 member, `Cert_IF` is part of the frozen construction trace.
Replacing `rho_P` without changing `P` is impossible by fixed-point uniqueness;
replacing `P` changes a certified antecedent operation whose input/output
action is independently testable before the state is handed to consumers.

```text
Q260_LITERAL_REPLACEMENT_SUBFAMILY_SURVIVES = false | TYPE-R
Q242_STATE_CAN_BE_REPLACED_AT_FIXED_P_SRC = false | TYPE-R
Q242_STATE_CAN_BE_REPLACED_WITH_ALL_OPERATIONAL_ORIGIN_DATA_FIXED = false | TYPE-R

DISTINCT_G1_V002_MEMBERS_MAY_GENERATE_DISTINCT_STATES = true
  [PROPOSAL FAMILY FACT; Q-194 PERMITS NONUNIQUE ORIGINS]

Q242_COUNTERMODEL_ELIMINATES_STATE_FAMILY = false | TYPE-R |
  test: Q-242 tests provenance, not uniqueness of every microscopic origin

Q242_COUNTERMODEL_KILLS_V002 = false | TYPE-R |
  test: the information-free replacement witness is excluded and every
        surviving member carries nonzero transient operational content
```

This is the precise scope of the repair. It establishes a generative origin
family; it does not force one physical member.

## 5. The three executable maps

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

### 5.1 State map

```text
d_state(Omega_prim,N^v002):=rho_P,
```

where `rho_P` is the unique normalized fixed point of `P_src`. All source
blocks come from that solution. No block or scalar state is appended.

### 5.2 Ready-ray map and exchange certificate

Let `X_rp` exchange `v_0` and `v_1` and fix `v_perp`. Let
`epsilon_root in {+,-}` choose one of the two authored rooted presentations:

```text
root_+ = v_0, successor_+ = v_1,
root_- = v_1, successor_- = v_0,

|R_N^epsilon>:=iota_cell(root_epsilon)^(tensor N),
d_ready(Omega_prim,N^v002):=C|R_N^epsilon>.
```

At zero history,

```text
X_rp S_inc X_rp=S_inc,
S_inc root_epsilon=successor_epsilon.
```

Therefore exchanging the authored root commutes with the zero-history law as
a covariance square; neither ray is falsely declared invariant. At nonzero
history the ratified `E_post` attachment distinguishes incoming and written
endpoints. That is licensed DoR-009 law content, not a derivation of
`epsilon_root`.

```text
ZERO_HISTORY_ROOT_EXCHANGE_SQUARE_COMMUTES = true
READY_RAY_IS_ZERO_HISTORY_FIXED_POINT = false | TYPE-R
ROOT_ORIENTATION_SELECTED_BY_SEALED_TEXT = false | TYPE-S
ROOT_ORIENTATION_FAMILY_SIZE = 2 [PROPOSAL FAMILY COUNT]
```

### 5.3 Law map

For the chosen carrier presentation,

```text
D_n[a_j]=diag(1,chi_n(a_j),1),
W_1^(n)[a_j]=D_n[a_j]S_inc,
W_N^(n)[a]=tensor_(j=1)^N W_1^(n)[a_j],

d_law(Omega_prim,N^v002;a)
  :=P_0 tensor I_(3^N)+P_ch tensor W_N^(n)[a].
```

This is the exact DoR-009 `E_post` law. The generated ready-ray orientation
does not change the operator formula.

### 5.4 Ordered construction record

```text
T0 freeze Omega_prim,N^v002 and the target-independent admission record;
T1 verify Cert_IF(P_src) without solving or inspecting downstream outputs;
T2 build the signed one-cell carrier and exchange pair;
T3 solve the unique fixed-point problem -> rho_P;
T4 apply the selected root inclusion -> ready ray;
T5 attach chi_n with E_post, tensor and control -> U_N[a];
T6 issue common-input, covariance, domain, exchange and finite certificates;
T7 expose outputs only after T0-T6 are frozen.
```

## 6. Fresh law and finite-authority checks

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

### 6.1 C1-C8 rerun

| Certificate | V002 result |
|---|---|
| C1 zero-source reduction | PASS AS PROPOSAL: `D_n[0]=I`, exact sealed write |
| C2 equal-history baseline | PASS AS PROPOSAL: unitary branch cancellation |
| C3 ready/pointer dephasing | PASS AS PROPOSAL in each declared root presentation |
| C4 gauge/CTP covariance | PASS AS PROPOSAL with `E_post` endpoint representations |
| C5 charge/flux access | PASS AS PROPOSAL; character reaches written charged branch |
| C6 one-cell authority | PASS AS PROPOSAL; exact `S_inc` and `D_nS_inc` |
| C7 zero extension | PASS AS PROPOSAL; appended `S_inc` factors |
| C8 faithful character/reality | PASS AS PROPOSAL for the inherited conjugate pair |

No law field was changed to repair the state origin.

### 6.2 Finite authority

`K_cell`, `iota_cell`, `epsilon_root`, `S_inc` and the law all have one-cell
finite matrices. The preparation channel's state content is visible in the
finite generated source density. Its transient content is visible on the
finite-rank trace-zero probe carried by `Cert_IF`.

```text
ORIGIN_REQUIRES_RESTRICTION_INVISIBLE_CONTENT = false | TYPE-R |
  test: explicit one-cell and finite-rank channel witnesses

P_SRC_TRANSIENT_CERTIFICATE_HAS_FINITE_SHADOW = true
```

The certificate is an origin-level operational port. It is not promoted to a
physical response, measure, or alpha-facing consumer.

## 7. R2 — non-circular certificate definitions

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

### 7.1 B5, B6 and B9

These three rows remain legitimate downstream conditionals and are no longer
counted as partial Gen-Omega passes:

```text
B5 condition:
  after P2-P6 exist, prove RetHess_phys lies in a named ratified separation
  class; failure is possible by class mismatch.

B6 condition:
  instantiate the physical restrictions and Tail_R; failure is possible by a
  nonzero restriction-invisible element.

B9 condition:
  for the selected consumer, prove tail factorization or exhibit a lawful
  tail-visible witness; failure is possible in either direction.
```

They do not presuppose their conclusions and they are not required to ratify
the origin proposal itself.

### 7.2 Repaired B14

B14-v2 does **not** say “accept G1 as origin physics.” It is the conjunction of
four independently failure-capable tests:

```text
B14-v2.1 OUTPUT-FREE PRIMITIVES:
  rho_S, ready ray and U_N[a] are absent from Omega_prim.

B14-v2.2 OPERATIONAL GENERATIVITY:
  m_in(P_src)>0 with Cert_IF, and each descent output is obtained by the
  displayed fixed-point/root-inclusion/incidence-character operations.

B14-v2.3 FROZEN COMMON TRACE:
  T0-T6 are fixed before any output is exposed; changing an output requires
  changing an antecedent primitive or failing a construction equation.

B14-v2.4 TARGET INDEPENDENCE:
  no membership clause or primitive choice mentions response, kernel,
  coupling, physical residual, measured datum, cancellation or survival.
```

Each test can fail without assuming common origin. Passing their conjunction
is the proposal's common-origin certificate. The Q-260 replacement family
fails B14-v2.2 before a state output is inspected.

```text
B14_V002_IS_NONCIRCULAR = true
  [PROPOSAL LOGICAL CERTIFICATE; NOT RATIFICATION]
B14_V002_REPLACEMENT_CHANNEL_RESULT = FAIL_MEMBERSHIP
B14_V002_RESULT_FOR_DECLARED_V002_FAMILY = PASS_AS_PROPOSAL
```

## 8. R4 — channel fibers and the physical parameter

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

Define on G1-v2

```text
kappa_ch(P):=Tr(rho_P P_ch),

P~_rec Q  iff  kappa_ch(P)=kappa_ch(Q),

Fib_p:={P in G1_v002 : kappa_ch(P)=p}.
```

The map is many-to-one. For fixed `sigma`, the strict affine family
`P_(sigma,lambda)` has the same invariant state and same `kappa_ch` for every
admitted nonzero transient coefficient `lambda`, while its input modulus and
transient action differ.

The finite record contraction is exactly

```text
A_N^P[a_+,a_-]
  =(1-kappa_ch(P))+kappa_ch(P) Z_N[a_+,a_-].
```

Therefore all channels in one `Fib_p` are indistinguishable to this finite
record-visible quotient, and channels in fibers with different `p` give
different values whenever the admitted finite holonomy factor distinguishes
the two coefficients. This proves that `kappa_ch`, not the channel, is the
Q-236 record-visible state parameter.

```text
P_SRC_TO_KAPPA_CH_IS_INJECTIVE = false | TYPE-R |
  test: fixed-sigma varying-lambda strict affine family

P_SRC_TO_KAPPA_CH_IS_MANY_TO_ONE = true
KAPPA_CH_IS_RECORD_VISIBLE = true
  [STRUCTURAL FACT FROM THE EXACT Q-236 AMPLITUDE]

TRANSIENT_CHANNEL_COORDINATES_ARE_RECORD_VISIBLE_IN_A_N = false | TYPE-R |
  test: exact amplitude factors through kappa_ch alone

PHYSICAL_STATE_PARAMETER_SELECTED = false | TYPE-U |
  missing: a rule selecting/deriving one kappa_ch fiber
```

No value of `kappa_ch` is evaluated.

## 9. Full Q-254 battery rerun

**PROPOSED_NOT_ADOPTED — BATTERY RESULTS DO NOT RATIFY THE DRAFT**

No row inherits credit from V001.

| Battery | V002 verdict | Fresh certificate / residual |
|---|---|---|
| B1 finite-restriction reproduction | PASS AS PROPOSAL | exact DoR-009 law, generated state, one-cell carrier and zero extension reproduce the finite interfaces |
| B2 finite retarded baseline | PASS AS PROPOSAL | unchanged exact finite law/source block preserves Q-243's zero, state-parameter-free retarded block |
| B3 finite restrictions remain state-parameter-free in retarded block | PASS AS PROPOSAL | `kappa_ch` occurs in the finite state quotient but is not injected into the retarded block |
| B4 no naive continuous extension | PASS BY NONCLAIM | V002 builds compatible finite origin data only; no physical completion is inferred |
| B5 separation only on named class | CONDITIONAL/DOWNSTREAM | P2-P6 must type `RetHess_phys`; V002 claims no membership |
| B6 tail explicit | CONDITIONAL/DOWNSTREAM | V002 introduces no physical tail or tail identity |
| B7 modulo-tail determinacy | PASS BY COMPATIBILITY | no finite agreement is promoted to completed identity |
| B8 visible quotients finite-domain | PASS AS PROPOSAL | `kappa_ch` is explicitly the finite source-state quotient, not a response output |
| B9 consumer tail certificate | CONDITIONAL/DOWNSTREAM | no alpha-facing consumer is constructed here |
| B10 no finite interior stationary point | PASS | no stationary point is asserted or used |
| B11 C1 is not an evaluation rule | PASS | C1 is only the zero-source operator certificate |
| B12 three zero surfaces distinct | PASS | zero history, physical source zero and retarded evaluation remain different domains |
| B13 finite authority | PASS | G3 finite matrices and G1b finite transient witness; no hidden tail |
| B14 target independence/no supplementation/common origin | PASS AS PROPOSAL | B14-v2.1-v2.4 all pass; replacement channels fail membership non-circularly |

```text
Q254_V002_PASS_COUNT = 11 [PROPOSAL TEST COUNT; NOT RATIFICATION]
Q254_V002_CONDITIONAL_COUNT = 3 [PROPOSAL TEST COUNT; NOT RATIFICATION]
Q254_V002_FAIL_COUNT = 0 [PROPOSAL TEST COUNT; NOT RATIFICATION]
BATTERY_ACCOUNTING_SUM = 14 [PROPOSAL COUNT]
```

## 10. Mandatory replacement and Q-242 tests

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

| Test | Result | Reason |
|---|---|---|
| every `R_rho` | **REJECTED** | zero input modulus |
| nonempty family | **PASS AS PROPOSAL** | strict affine mixing witness class |
| fixed-member state replacement | **REJECTED** | fixed-point uniqueness |
| family-level information-free state embedding | **REJECTED FOR THE DECLARED FAMILY** | every admitted member carries nonzero input-faithful transient behavior and a frozen operational certificate |
| distinct physical origins may yield distinct states | **ALLOWED** | Q-194 noninjective role standard does not require one unique microscopic origin |
| one physical invariant marginal selected | **NO** | channel quotient remains a family |
| root-not-bag | **PASS AS PROPOSAL** | outputs absent from primitive tuple; maps are fixed-point/root/incidence operations, not projections |

The family does contain the strict affine map `sigma -> P_(sigma,lambda)` for
fixed nonzero `lambda`. This map encodes the fixed point in its preparation
operation. That is not hidden. The reason it is not the Q-260 replacement
counterexample is precisely the independently certified action on
`Delta_0^ss`. If an adversarial reviewer rules that **any** preparation
generator whose invariant state can be recovered from its coefficients is a
bag, no preparation dynamics could ever pass: solving for a unique invariant
state necessarily makes it recoverable. V002 instead uses the operational bar:
the generator must contain and certify input-dependent preparation physics
beyond the terminal state.

```text
V002_ANTI_RELABELING_BAR = OPERATIONAL_INPUT_FAITHFULNESS_PLUS_EXECUTABLE_DESCENT
V002_ANTI_RELABELING_BAR_IS_STRONGER_THAN_FIXED_POINT_UNIQUENESS = true
```

## 11. Self-kill audit

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

### 11.1 Attack-tuned condition

The condition was tested for answer dependence. It is defined on the full
equal-trace difference space, not on the two known states or on replacement
maps by name. It has independent operational meaning: no two equal-trace
inputs are erased into the same one-step output, with a quantitative lower
modulus.

```text
KNOWN_ATTACK_MEMBER_NAMED_IN_G1_V002_DEFINITION = false | TYPE-S
DOWNSTREAM_OUTCOME_NAMED_IN_G1_V002_DEFINITION = false | TYPE-S
```

### 11.2 Hidden new adoptions

The transient operational certificate is new authored content. It is disclosed
as G1b-v2, not called a theorem of V001. The root-orientation binary is also
made explicit rather than silently fixed. No other hidden adoption was found.

```text
NEWLY_DISCLOSED_V002_ADOPTION_ROWS = {G1b_v2,G3b_v2}
HIDDEN_ADOPTION_COUNT_AFTER_V002_AUDIT = 0 [DRAFT AUDIT COUNT]
```

### 11.3 Circularity moved rather than removed

The admission lower bound is calculated without `rho_P`; B14-v2 does not say
“the channel is an origin because it was accepted as one.” The fixed point is
solved only after membership and the transient certificate are frozen.

The residual physical freedom is not concealed: `kappa_ch` fibers remain open.
Thus V002 repairs generativity provenance without claiming state selection.

### 11.4 Stronger counterexample hunt

Strict affine channels show the strongest surviving concern: a channel can
carry a freely variable invariant state **and** nontrivial transient action.
This does not refute the operational definition, but it proves:

```text
INPUT_FAITHFULNESS_FORCES_ONE_STATE_ACROSS_THE_FAMILY = false | TYPE-R
INPUT_FAITHFULNESS_SELECTS_ONE_KAPPA_CH_FIBER = false | TYPE-R
```

Accordingly V002 must not be described as deriving the state parameter. Its
claim is only that each state is generated by a genuine preparation operation
rather than supplied as an output coordinate.

### 11.5 No source symmetry or measure imported

The ratified `CAR(H_src)` representation exports no canonical scalar state,
and the field module's scalarization firewall is respected. V002 does not use
unitality to invent a normalized identity on an infinite carrier, does not
twirl over a group, and does not introduce Haar, Gibbs or trace weights.

## 12. Release, scope ceiling, and final typed board

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**

If independently approved and ratified, V002 would supply a generative
three-output family at every finite stage. It would not:

```text
select one physical P_src member or kappa_ch fiber;
complete exclusive effect/domain descent;
ratify SOURCE_GERM_PHYS;
build P2-P6, a physical response, a tail certificate, or a background;
supply J_ref, cellulation independence, or charter O-D3;
authorize any downstream evaluation.
```

```text
GEN_OMEGA_V002_IS_A_PROPOSAL = true [PROPOSAL STATUS]
GEN_OMEGA_V001_SUPERSEDED_IF_V002_RATIFIED = true
  [HYPOTHETICAL RATCHET; NOT CURRENT]

G1_V002_INPUT_FAITHFUL = true [PROPOSAL DEFINITION]
G1_V002_REPLACEMENT_FREE = true [PROPOSAL THEOREM]
G1_V002_NONEMPTY = true [PROPOSAL THEOREM]
G1_V002_STATE_SELECTING = false | TYPE-R

D_STATE_EXECUTABLE = true [PROPOSAL MAP]
D_READY_EXECUTABLE = true [PROPOSAL MAP]
D_LAW_EXECUTABLE = true [PROPOSAL MAP]
D_LAW_EQUALS_DOR009 = true [PROPOSAL CERTIFICATE]

ZERO_HISTORY_EXCHANGE_CONSISTENCY = PASS_AS_PROPOSAL
ROOT_ORIENTATION_PHYSICALLY_SELECTED = false | TYPE-U
P_SRC_TO_KAPPA_CH_MANY_TO_ONE = true
KAPPA_CH_RECORD_VISIBLE = true [STRUCTURAL Q-236 FACT]
KAPPA_CH_SELECTED = false | TYPE-U

REPLACEMENT_CHANNEL_TEST = PASS_AS_PROPOSAL
Q242_COUNTERMODEL_TEST = PASS_AS_PROPOSAL
Q254_BATTERY = 11_PASS__3_CONDITIONAL__0_FAIL

V002_SURVIVES_OWN_KILL_PASS = true [PROPOSAL AUDIT; INDEPENDENT REVIEW PENDING]
INDEPENDENT_ADVERSARIAL_REVIEW_COMPLETED = false | TYPE-C |
  constraint: cross-lane review of this exact V002/hash has not run |
  release: independent adversarial determination

DOR_013_ISSUED = false | TYPE-C |
  constraint: proposal not independently reviewed or ratified |
  release: principal decision after review

GEN_OMEGA_CONCEPT_DERIVED_FROM_SEALED_RECORD_STRUCTURE = false | TYPE-U
PHYSICAL_P5_INSTANCE_COMPLETED = false | TYPE-U
PHYSICAL_VERDICT = NO_VERDICT

TARGET_OUTPUT_USED_TO_NARROW_V002 = false | TYPE-S |
  scope: family, maps and tests in this artifact
FENCE_BLOCKED_STRUCTURAL_RESULT = false | TYPE-S |
  scope: this artifact

alpha_computed = false [TERMINAL_FENCE_DECLARATION]
proof_authorized = false [TERMINAL_FENCE_DECLARATION]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION]
```

No register, gate, decision, plan, tracker, or sealed authority was edited.
