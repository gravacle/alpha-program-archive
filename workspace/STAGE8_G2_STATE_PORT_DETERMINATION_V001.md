# STAGE 8 — G2 STATE-PORT DETERMINATION: WHAT FIXES THE SECTOR WEIGHTS, OF RECORD
## BLIND BUILDER (G2-SELECT) — DETERMINATION FROM SEALED CONTENT — [CLAIMED]

Date: 2026-08-13
Role: DETERMINE what, if anything, of sealed record content fixes node G2 — the
scalarization/state-port pair that produces the sector weights `(w_0, w_ch)` of the
finite-N scalarized anchor `A_N = w_0 + w_ch chi_n(h_rel)` and hence the coherence
prefactor `mu` (`mu = 0 iff w_0 w_ch = 0`). Constraint inventory, selector hunt,
residual-freedom typing, and the symbolic mu bearing. No value of `n`, `kappa`,
`alpha`, `mu`, any weight, dimension, rank, or coefficient is computed, bounded, or
evaluated. Every numeric-looking string below is a sealed-text quotation or an exact
structural integer. Symbolic forms are carried, never classified into values.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false` ; `coupling_evaluation_authorized = false`

Fences held throughout: CONNECTION-ONLY, SCALE-FREE, symbolic. NO scale, NO imported
GR, NO faithfulness as authority; scale-bearing sealed text read SUSPECT-ONLY. No
register/tracker/plan/road/ledger/lens file read. No git action. Output name probed
before write: ABSENT.

---

## 0. VERDICT IN ONE LINE

**PARTIAL SELECTION OF RECORD, and the port splits into two faces that the tasking's
"omega_t family" merges. On the SOURCE face, the record now carries a selector: the
ratified family-level generative origin (DoR-013 Gen_Omega V003 + DoR-014
SOURCE_GERM_PHYS V004) forces, for every anchor-family member at once, the
normalized-identity invariant source state `rho_P = omega_tr` and the symbolic
sector-dimension-ratio form `kappa_ch := Tr_A(rho_P P_ch) = Tr_A(P_ch)/Tr_A(I_src)`
— pinning, under the sealed inclusive effect, `(w_0, w_ch) = (1 - kappa_ch,
kappa_ch)` with BOTH sector ranks strictly positive on the durable branch, hence
`mu != 0` PINNED SYMBOLICALLY, magnitude and sign left open (the ordered rank pair
`(r_0, r_ch)` is unpinned of record, NO_VERDICT, and evaluation is fenced). That
selector is sealed as AUTHORED PHYSICS adopted by principal act — a disclosed-input
decision-of-record, confirmed non-derivable from the transition structure — and it
is conditional: TYPE-P on DoR-008/009/013/014, A0, the Attach entry's three open
conditions, and two standing falsifiers that VOID it if fired. On the FIELD face,
NOTHING selects: the U1-invariant probability-measure continuum on `Y` survives
every executable constraint exactly as M06 sealed it; the post-ratification descent
delivery is sealed `THETA_HIST = STOPPED` (missing object "a MEASURE on Y", both
routes closed, opposite-lane CONFIRMED); the completed-object `rho_pre` and the full
`StatePort_U2_008` remain uninstantiated (TYPE-U). The field-face freedom does NOT
move `mu` between zero and nonzero — the weights are source-face pairings; it gates
the completed-object licensing of the scalar amplitude, not the `w_0 w_ch = 0`
dichotomy. No sealed outcome pins `mu = 0`.**

---

## 1. SOURCES READ, SEALS VERIFIED AT PATH

All recomputed by `shasum -a 256` at path this session BEFORE reading; sidecars
compared where present (matched). Workspace root
`/Users/bgm/MB Work/alpha-program-archive/workspace/` unless shown; DoRs at
`/Users/bgm/MB Work/alpha-program-archive/supervision/`.

| Tag | Source | SHA-256 | Status |
|---|---|---|---|
| M06 | `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | MATCHES-TASKED (digest begins d13920e2; carries the tasked flags and the omega_t family) |
| FORM | `STAGE8_R_RECORD_L_FORM_FABLE_V001.md` | `5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37` | MATCHES-TASKED |
| M03 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | MATCHES-TASKED |
| G3B | `STAGE8_G3_REALIZATION_BUILD_V001.md` | `3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70` | MATCHES-TASKED |
| E1 | `STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md` | `468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5` | MATCHES-TASKED (begins 46846730) |
| PA | `STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md` | `789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3` | LOCATED-FROM-E1 (E1 :82), SEALED-OK |
| U2 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab` | MATCHES M06's cited digest |
| P5A | `STAGE8_SECTION53_P5_AXIS_COVERAGE_AND_EXCLUSION_AUDIT_V001.md` | `1274b1b71b46e6a34b641c0053d61ce1ed16c94e8d570317a7831f558bcfef58` | MATCHES U2's cited digest |
| P5D | `STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md` | `a33be83c1ee7cbfbda2cc3857425cb9e7e90a23bbe3d61c9ec89432e50b77874` | MATCHES U2's cited digest |
| MO | `STAGE8_MINIMAL_OMEGA_P5_COMMON_ORIGIN_ROLE_REALIZATION_ATTEMPT_V001.md` | `576da30f300a0362469d6b4f447423a2298efc0dd28a60f38057e7f4cc8bd888` | SEALED-OK |
| NG | `STAGE8_GEN_OMEGA_NONCIRCULAR_GENERATIVITY_NO_GO_ATTEMPT_V001.md` | `7000963e769362d87e17e36f06903e63aaa8caaeb284ee9c9f3676febe4f4163` | SEALED-OK |
| GOV3 | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | MATCHES DoR-013's cited prefix `1be5f6a7…` |
| SGV4 | `STAGE8_SOURCE_GERM_PHYS_ADOPTION_PROPOSAL_V004.md` | `d4cdbb6623797df6accb7dc9b24134d179bfc8e8d039c585d5c91ae23255869e` | MATCHES DoR-014's cited prefix `d4cdbb66…` |
| D13 | `DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | SEALED-OK (principal act) |
| D14 | `DECISION_OF_RECORD_014_SOURCE_GERM_PHYS_RATIFIED_2026-08-02_V001.md` | `b6e4116df63403478d28be8cdb6589b091cc1aa8b6ad5a40776a28b135cd138f` | SEALED-OK (principal act) |
| AXS1 | `STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md` | `32725f12a119bbb456dbe80e2c304e9dfb839acc9a05fda3d9f64e7317158842` | SEALED-OK |
| TH | `STAGE8_DESC_THETA_HIST_DARIO_V001.md` | `47f7c1248bcb46f631584fe647feb5e614a32ca8a894557d245d06ed343a64ba` | SEALED-OK; digest MATCHES its opposite-lane check's citation |
| THC | `STAGE8_DESC_THETA_HIST_CHECK_CODEX2_V001.md` | `7eaf5a748574add634a76c37de3abc2e81da2b861ed767dd24c9fd7c2279e4ed` | SEALED-OK (check: ROUTE-CONFIRMED; delivery CORRECTED on leg tally) |
| SF | `STAGE8_DESC_SURFACE_FIRST_STEP12_PREFORM_DARIO_V001.md` | `73ef21bdbfef5fc9a3e602aedbf3a54e32a0274a4dd2fcc7214750b5b59ffe31` | SEALED-OK (the kappa_ch record-marginal display) |
| TA | `STAGE8_TASK4A_ALPHA_FACING_OUTPUT_TAIL_ANNIHILATION_THEOREM_DETERMINATION_V001.md` | `a71d4e59fcde1a7df10e8051e46befb9b4b6653a0917bb03a0c0403179717fef` | SEALED-OK (`Sigma_src` = charge-superselected, :386) |

Suspect classification carried from the sources themselves: PA/E1's carrier is a
sealed fixed-gauge, scale-bearing diagnostic (PA :130-135, :50-70) — read
SUSPECT-ONLY, used to classify, never to certify. `alpha_x` in PA/E1 is the Dirac
velocity matrix, not the fine-structure constant. M03's `a_+, a_-` are connection
histories, `sigma_+, sigma_-` discrete source sectors (M03 :159-166) — honored.

---

## 2. THE PORT, POSED EXACTLY — ONE PORT, TWO FACES

The sealed state-port contract (U2 :254-269) is one tuple:

```text
StatePort_U2_008 := ( omega_phys : B -> C, H_omega, pi_omega,
                      rho_pre in trace-class(H_omega), rho_pre >= 0,
                      Tr_omega(rho_pre) = 1,
                      state_domain, state_descent_map,
                      state_provenance_certificate ).
```

Its consumers split it into two faces that the G2 question must keep separate:

```text
FIELD FACE   omega_phys on the branch algebra B iso C(Y), Y = X_+ x X_-
             (M06 §2.1) — the omega_t continuum lives HERE.
SOURCE FACE  the state/effect pairing on the neutral/charged source span —
             the sector weights live HERE: F_N = P_0 + Z_N P_ch is an operator
             on that span (M03 :52-66, :560-567), and any scalarization produces
             w_0 = <E, P_0 rho>, w_ch = <E, P_ch rho> (FORM D4 :203-231).
```

The connection histories `a_+, a_-` are classical parameters of the ratified
finite-N law, not operators in it; the weights `(w_0, w_ch)` are pairings on the
SOURCE face alone. This face split is load-bearing for every verdict below.

---

## 3. DELIVERABLE 1 — THE CONSTRAINT INVENTORY, AT SPANS

What each executable constraint pins, and the exact surviving family.

```text
FIELD FACE (M06, all TYPE-P premises DoR-008; re-confirmed post-entry by TH/THC):
  positivity            pins: omega in F_raw <-> finite positive regular Borel
                        measures on Y (Riesz-Markov; M06 :177-206). Survivor:
                        continuum.
  inclusive-identity    pins: omega(1_B) = 1 — removes the zero functional and one
    normalization       positive scaling degree, nothing else (M06 :421-444).
                        Survivor: probability measures on Y — continuum.
  U1 reality            pins: Theta_F-invariance, omega(Theta_F(b)) =
                        conjugate(omega(b)) (M06 :260-297). Survivor: the
                        U1-invariant probability-measure simplex — continuum;
                        every omega_t = (1-t)omega_H + t omega_epsilon is a fixed
                        point and b_lambda separates distinct t (M06 :243-257,
                        :293-297).
  one-cell C0           pins: nothing beyond omega(1) = 1 — the restriction
                        consumes omega only through the unit (M06 :364-406).
  rho_pre TYPE-ONLY     pins: nothing — every normalized scalar realization
                        admits unit-trace positive trace-class densities
                        (M06 :335-360).
  EXACT SURVIVING FAMILY: U1-invariant probability measures on Y (the full
  invariant simplex, of which the omega_t segment is the displayed witness);
  cardinality CONTINUUM at every executable stage (M06 :478-506). The physical
  subfamily: NO_VERDICT — the placement test is unexecutable (M06 :47-56).
  POST-RATIFICATION STATUS: UNCHANGED — "The entry adds no row to that table"
  (TH :212-216); THETA_HIST = STOPPED (TH :226-231); ROUTE-CONFIRMED (THC :252).

SOURCE FACE:
  admitted family       Sigma_src = { rho_S >= 0, Tr(rho_S) = 1,
                        rho_S = P_0 rho_S P_0 + P_ch rho_S P_ch } (MO :123-128);
                        sealed elsewhere as "charge-superselected" (TA :386).
  positivity+norm       pin: rho_S a state; do NOT pin the sector masses.
  superselection        pins: no neutral-charged coherence — under the inclusive
                        effect the weights carry no relative phase; the
                        cross-history entries I_N(0,ch) = I_N(ch,0) = 0 are the
                        law's own identity, <R_N|P_N> = 0 (M03 :205-213).
  effect port           exactly ONE instantiated effect: the inclusive module
                        identity, descending to the identity under any lawful
                        scalar realization (U2 :288-310); the sealed primitive
                        pins the inclusive functional's final effect to the
                        identity (P5A :207-211 quoting the primitive :17-25).
                        F4-F6 conditioned-effect schemas: typed, uninstantiated
                        (U2 §3.4; P5A ledger rows).
  EXECUTABLE-LEVEL SURVIVOR (pre-DoR-013): all of Sigma_src — under the
  inclusive effect, (w_0, w_ch) = (Tr(P_0 rho_S), Tr(P_ch rho_S)) free on the
  closed simplex { w_0, w_ch >= 0, w_0 + w_ch = 1 }; the outgoing-visible scalar
  is exactly p_ch = Tr(rho_S P_ch) (AXS1 :229-231).
```

---

## 4. DELIVERABLE 2 — THE SELECTOR HUNT, CANDIDATE BY CANDIDATE

**C1 — the ratified law's ready vector `|R_N> = |r>^(tensor N)` (M03 :154-157).**
CONSTRAINS-PARTIALLY. It is the record-side input of the sandwich and is already
consumed in `F_N`'s definition; it produces the zero cross-history entries
(`<R_N|P_N> = 0`, M03 :205-213) and hence the sector-diagonality of the source
operator. It supplies no functional on the source span and no measure on `Y`.
Remains: everything G2 asks.

**C2 — the pointer weights and the eraser slot (M03 :127-133, :235-248; PA
:161-179).** SILENT. `w_lambda = <pointer|P_lambda|ready>` are record-carrier
spectral data of the write vertex — Kraus weights, not sector weights; the eraser
slot `|e_Q>` carries the connection-independent entry `-1` and no state datum. In
E1 they act structurally (they promote the sharp localizer to the lead,
`sum_lambda w_lambda u_lambda = P`, E1 :346-351) — vertex structure, not state
selection. No pairing on the source span, no measure on `Y`.

**C3 — the two-time vertex's state content (E1/PA): the ready injection, the sea
schemes `C_mix`, `C_pure`, the Gaussian-sum representation (PA :184-229; E1
:402-404).** SILENT-AS-AUTHORITY, three independent ways: (i) the carrier is
sealed fixed-gauge and scale-bearing — SUSPECT-ONLY here (PA :130-135, :50-70);
(ii) the sources bar their own promotion — "Neither may be selected by its
output" (PA :72-74), M-3 "per-state, reported separately, never promoted" (E1
:402-404); (iii) no sealed transport carries the diamond-indexed realization to
the record complex or the branch algebra (G3B S2: "No transport law exists",
FR of record). Even granted in full, it supplies dynamics and Kraus structure,
not a state on `B` and not a source-sector density.

**C4 — purity/pointer/decoherence structure (M03 §5.4 :364-382).** CONSTRAINS-
PARTIALLY. The equal-history reduced channel `Phi_N(rho) = P_0 rho P_0 +
P_ch rho P_ch` and the superselection identity type the admitted source state
sector-diagonal and kill the weights' relative phase under the inclusive effect —
for EVERY `rho_S` at once. A channel identity quantified over all states selects
none. No sealed purity requirement on `rho_pre` was found (P5A rows F0-F6 supply
type constraints only).

**C5 — CTP-branch consistency (M06 §3, §4.3; M03 §5.7).** CONSTRAINS-PARTIALLY.
U1/Theta_F compatibility restricts the field face to the invariant simplex;
`F_N[a_+,a_-]^dagger = F_N[a_-,a_+]` holds identically of the law. Every
`omega_t` passes (M06 :410-419); nothing further is cut.

**C6 — THE RATIFIED GENERATIVE ORIGIN (D13 + D14, with GOV3/SGV4 as the adopted
texts).** SELECTS — on the source face, at family level, symbolically.
DoR-013 ratifies Gen_Omega V003 as a FAMILY (three convergent anchor classes;
"NO MEMBER IS SELECTED, EVER"), licensed by the proven neutrality certificate:
"every member forces the same normalized-identity invariant state and the same
symbolic sector-dimension-ratio form for p_ch" (D13 :9-13). The sealed
consequence display (GOV3 §10, :810-830):

```text
rho_P = omega_tr ;   kappa_ch := Tr_A(rho_P P_ch)
                              = Tr_A(P_ch)/Tr_A(I_src)
                              = dim(P_ch H_src^A) /
                                [ dim(P_0 H_src^A) + dim(P_ch H_src^A) ] ;
KAPPA_CH_STATE_FIBER_COUNT = 1  [proposal consequence].
```

DoR-014 ratifies the germ whose ONE parameter is the ordered integer rank pair
`(r_0, r_ch) := (rank P_0^A, rank P_ch^A)` (SGV4 :130-139), with "`r_0 > 0`,
`r_ch > 0` on the durable branch"; "p_ch's symbolic form: FORCED to the sector
ratio of the realized pair. NO evaluation"; "Pinning of the pair: NO_VERDICT of
record" (D14 :10-17). The post-ratification descent delivery records the forced
marginals of the surface-first joint state on the SR face: "source marginal =
omega_tr; record marginal = (1-kappa_ch)|r><r|^N + kappa_ch|p_Q><p_Q|^N — BOTH
STILL FORCED" (TH :128-138; display SF :194-195).

WHAT IT PINS, span: under the sealed inclusive effect, `(w_0, w_ch) =
(1 - kappa_ch, kappa_ch)` — the symbolic sector-dimension-ratio pair, identical
across every anchor-family member (fiber count 1), with both weights STRICTLY
POSITIVE on the durable branch. Hence `w_0 w_ch != 0` and `mu != 0`, symbolically.
WHAT IT LEAVES: the value of the pair `(r_0, r_ch)` (NO_VERDICT; any step that
requires selecting a value VOIDS the step, D14 :25-27); the field-face measure
(next item); the completed-object port.
ITS TYPE, sealed in the act itself: "A0 and the anchor are AUTHORED PHYSICS —
confirmed non-derivable from the existing transition structure … The program
adopts them knowingly" (D13 :34-38) — a PRINCIPAL DECISION-OF-RECORD carrying a
disclosed physical input, NOT a record-derivation. CONDITIONALITY: TYPE-P on
DoR-008/009/013(/014), A0, and the Attach entry's three open conditions (TH
:135-137); VOIDED if the neutrality falsifier or the DoR-008 restriction
falsifier fires (D13 :26-32; D14 :24-27).

**C7 — the record's prepared state at the completed object / the field-face
measure.** NOT SELECTED, sealed at three independent posts: (i) "no sealed
`Theta_hist(rho_S) = omega` exists. Thus the lawful pre-acceptance surface is a
typed relation over `Sigma_src x F_1`, not a graph of a function" (AXS1
:229-232); (ii) `THETA_HIST = STOPPED` — route (a) restriction untyped
(`omega_record | B_N = UNDEFINED`), route (b) direct `State(B)` uncut; "MISSING
OBJECT, named and typed: a MEASURE on Y — omega_phys — together with its descent
map and provenance certificate. NOT an arrow." (TH :199-231); a Dirac measure at
one classical history is expressly refused as selection-not-derivation (TH
:218-223); opposite-lane check: ROUTE-CONFIRMED (THC :139, :252); (iii) legs
L1/L2/L3/L5 OPEN at the completed object, K4 NOT-MET, B03 OPEN (TH :260-296).
`U2_STATE_PORT_INSTANTIATED = false | TYPE-U` stands (M06 :504-506; U2
:282-286).

**C8 — a structural condition that would do the field-face work.** NOT ADOPTED,
NOT EXCLUDED. The universal no-go on non-circular generativity is REFUTED by the
named benchmark condition family `C_BI` (bistochastic + input-faithful, nonempty,
non-circular, `C_BI_ADOPTED = false`, NG :5-41) — proof that structural
pre-output conditions CAN collapse a state orbit without circularity. Its
ratified successor did exactly that work on the SOURCE face (C6). No sealed
analogue exists for the FIELD face.

---

## 5. DELIVERABLE 3 — THE RESIDUAL FREEDOM AND WHAT KIND OF OBJECT SELECTS

```text
RESIDUAL FREEDOM, exact, after everything sealed:
  (F1) the field-face measure omega_phys — one U1-invariant probability measure
       on Y, plus its state_descent_map and state_provenance_certificate
       (AC6 OPEN, TH :233-251; K4 NOT-MET, TH :273-281).
  (F2) the completed-object placement — H_omega, pi_omega, concrete rho_pre,
       state_domain (legs L1/L2/L3 and the d_U2 witness L5, TH :260-296;
       U2 :254-286).
  (F3) the germ's ordered rank pair (r_0, r_ch) — unpinned, NO_VERDICT
       (D14 :14-17); enters (w_0, w_ch) only through the symbolic ratio.
  (F4) the conditioned-effect port F4-F6 — typed, no instance (U2 §3.4;
       P5A ledger); the inclusive baseline is pinned to the identity and is the
       effect the ratified Z_inc chain consumes (P5A :207-224; D14 :8-9).

WHAT KIND OF OBJECT SELECTS EACH, typed:
  (F1) DERIVABLE-NEW-WORK, construction named of record: the SM-1..SM-8-
       conformant joint state with omega_hist = Omega_C0 o i_B, then the K4
       comparison (the sealed release condition, TH :275-277) — equivalently a
       Theta_hist-conformant descent landing in State(C(Y)) with provenance.
       Whether such a construction EXISTS from the ratified origin is
       undetermined of record; failing it, the selector type falls to
       PHYSICAL-INPUT or PRINCIPAL-DECISION (a disclosed adoption, the C6
       pattern). Nothing sealed pre-assigns which.
  (F2) DERIVABLE-NEW-WORK conditional on (F1): H_omega and pi_omega are
       canonical once omega_phys exists (null-quotient completion, U2 :259-263);
       the concrete rho_pre and domain then remain construction, not choice,
       under the no-post-output-supplementation bar.
  (F3) DERIVABLE-NEW-WORK ONLY: the named future detector is the K_square arm
       (capped, C29) plus the missing rank-pinning package (sealed scalar source
       representation + rank-preserving intertwiner) (D14 :14-17). PRINCIPAL
       SELECTION IS BARRED HERE of record: "any construction step that requires
       selecting a specific (r_0, r_ch) value VOIDS the step, not the germ"
       (D14 :25-27).
  (F4) DERIVABLE-NEW-WORK: F4-F6 instances with common-origin certificates
       (U2 :311-345). Not needed for the inclusive-chain weights.

NON-SELECTORS, to close false doors: the E1/PA vertex state content (SUSPECT +
self-barred + untransported, §4 C3); a gauge choice (the field-face family is
already gauge-quotient data; the source freedom is rank data); any response VALUE
(selection by output — barred by the frozen-trace discipline, D13 :32, and PA
:144-145); a Dirac measure at a classical history (TH :218-223).
```

---

## 6. DELIVERABLE 4 — THE MU BEARING, SYMBOLIC, PER OUTCOME

Carried, not classified. The one structural property used: `mu(w) ≡ 0 iff
w_0 w_ch = 0` (FORM D4 :217-231).

```text
OUTCOME A — THE RATIFIED CHAIN CARRIED (DoR-013/014 + inclusive effect + the
  entry's conditions, all TYPE-P):
    (w_0, w_ch) = (1 - kappa_ch, kappa_ch), kappa_ch the symbolic
    charge-sector-dimension ratio; r_0 > 0 and r_ch > 0 on the durable branch
    force BOTH weights nonzero
    =>  mu != 0  PINNED SYMBOLICALLY.
    mu's magnitude and sign: NOT pinned — the pair (r_0, r_ch) is NO_VERDICT,
    evaluation is fenced (D13 :22-24), and FORM D5 already records that the
    finite-N mu's sign is not structurally fixed pending the Gate-5 limit.
    Consequence carried to the derived form: the finite-N interference anchor
    n^2 mu Phi_tot (x) Phi_tot is NON-DEGENERATE on this chain — the
    neutral x charged cross term that carries the entire connection response
    (FORM D4(ii)) does not vanish by state placement.

OUTCOME B — THE CHAIN VOIDED OR REFUSED (a falsifier fires, or the TYPE-P
  premises are not granted):
    (w_0, w_ch) reverts to the free simplex over Sigma_src
    =>  mu UNDETERMINED; mu = 0 exactly on the simplex boundary (one-sector
    states), mu != 0 in its interior; no executable constraint forces either.

OUTCOME C — mu = 0: PINNED BY NO SEALED OUTCOME. Reaching mu = 0 requires
  w_0 w_ch = 0, i.e. a one-sector source state; the only sealed selector forces
  the opposite (both sectors inhabited); asserting mu = 0 from the unbuilt
  field-face port would convert missing construction into a physical refutation,
  which DoR-006 forbids (M06 :464-469).

FIELD-FACE INDEPENDENCE, stated exactly: the residual continuum in State(C(Y))
  does not move mu between zero and nonzero — (w_0, w_ch) are SOURCE-face
  pairings and no sealed object routes the Y-measure into them. What the missing
  measure gates is the LICENSING: the completed-object scalar amplitude, the
  inclusive normalization certificate, and hence the standing of the finite-N
  anchor as a completed-object statement (M06 :440-444; TH :285-296). On the
  sealed record, G2's weight question and G2's completion question have come
  apart: the first is answered conditionally, the second is open.

RECONCILIATION FLAG, for the panel, not resolved here: the D4 anchor is the
  ready-sandwich scalarization; the forced SR-face joint state carries the mixed
  record marginal (1-kappa_ch)|r><r|^N + kappa_ch|p_Q><p_Q|^N (TH :134;
  SF :194-195). Composing the anchor against the forced JOINT state rather than
  the ready sandwich is a sealed-adjacent step no artifact has executed; nothing
  here depends on its outcome.
```

---

## 7. WHAT THIS DOES NOT SAY / OVERCLAIM AUDIT

```text
IT DOES NOT say the state port is instantiated: U2_STATE_PORT_INSTANTIATED =
   false TYPE-U stands; five of six legs turn on the missing measure and the
   completed object (TH :298-301).
IT DOES NOT promote the source-face selection to a record-derivation: the
   selector is sealed AUTHORED PHYSICS adopted by principal act, and every
   downstream positive here carries its TYPE-P premise chain and falsifiers.
IT DOES NOT select an anchor-family member, a rank pair, an omega on Y, or any
   effect instance; it binds no member on any candidate's behalf.
IT DOES NOT evaluate kappa_ch, mu, any weight, dimension, rank, or ratio; the
   kappa_ch display is a sealed-text quotation; "nonzero" is the only property
   carried, and only on the stated conditional chain.
IT DOES NOT consume the E1/PA carrier as authority — SUSPECT-ONLY throughout;
   no scale, no imported GR, no faithfulness anywhere.
IT DOES NOT treat the tasking's sealed-state description as final: M06's
   continuum verdict is REPRODUCED at its spans AND the later sealed principal
   acts are reported — both, dated, neither suppressed. M06's own reopen
   condition (§7: a failure-capable physical condition fixed before response
   output) is exactly the shape DoR-013 supplied on the source face; its
   field-face analogue remains unsupplied.
YOURS assemblies, marked: the two-face split of the port (§2, from the sealed
   codomain displays); the identification w_ch = p_ch = kappa_ch under the
   inclusive effect (§4 C6, exact pairing algebra on sealed displays: D4's
   weights + AXS1's p_ch + GOV3's kappa_ch, all three sealed, the identification
   one line); the simplex coordinatization of the pre-DoR-013 freedom (§3); the
   outcome partition of §6. Each runs on quoted sealed identities; none consumes
   an unsealed value.
FLATTENING CHECK: PARTIAL is not flattened into FOUND or NONE; "mu != 0 pinned"
   is never detached from its conditional chain; NO_VERDICT (the pair) is not
   flattened into "unknown but harmless" — it is the exact open magnitude;
   STOPPED (Theta_hist) is not flattened into "impossible".
```

---

## 8. FINAL FLAG BLOCK

```text
CONSTRAINT_INVENTORY = FIELD FACE: positivity pins finite positive measures on Y
  (M06 :177-206); inclusive normalization pins omega(1)=1 only (M06 :421-444);
  U1 reality pins Theta_F-invariance (M06 :260-297); one-cell C0 consumes only
  omega(1)=1 (M06 :364-406); rho_pre type-only cuts nothing (M06 :335-360);
  EXACT SURVIVOR: the U1-invariant probability-measure simplex on Y, CONTINUUM,
  omega_t = (1-t)omega_H + t omega_epsilon the displayed inequivalent segment
  (M06 :243-257, :478-506), unchanged post-entry (TH :212-216). SOURCE FACE:
  the admitted family Sigma_src = {rho_S >= 0, Tr rho_S = 1, charge-
  superselected} (MO :123-128; TA :386); superselection + the law's own zero
  cross-entries kill sector coherence (M03 :205-213); exactly one instantiated
  effect, the inclusive identity (U2 :288-310; primitive via P5A :207-211);
  EXECUTABLE-LEVEL SURVIVOR: (w_0, w_ch) = (Tr P_0 rho_S, Tr P_ch rho_S) free
  on the closed simplex w_0 + w_ch = 1, w_0, w_ch >= 0 (AXS1 :229-231).

SELECTOR = PARTIAL( FOUND on the source face: the ratified family-level
  generative origin — DoR-013 (Gen_Omega V003, digest 1be5f6a7…) + DoR-014
  (SOURCE_GERM_PHYS V004, digest d4cdbb66…) — every anchor-family member forces
  rho_P = omega_tr and the one symbolic form kappa_ch := Tr_A(rho_P P_ch) =
  Tr_A(P_ch)/Tr_A(I_src) (GOV3 :810-830, fiber count 1); under the sealed
  inclusive effect this pins (w_0, w_ch) = (1 - kappa_ch, kappa_ch), both
  strictly positive on the durable branch (r_0 > 0, r_ch > 0, D14 :10-13);
  forced marginals confirmed at the SR face (TH :128-138). TYPE-P premises
  DoR-008/009/013/014 + A0 + the entry's three open conditions; VOIDS on the
  neutrality or restriction falsifier. THE SELECTOR IS AUTHORED PHYSICS BY
  PRINCIPAL ACT, sealed as non-derivable (D13 :34-38) — a disclosed input, not
  a record-derivation. WHAT REMAINS: (F1) the field-face measure omega_phys on
  Y with descent + provenance — THETA_HIST = STOPPED, both routes, check-
  confirmed (TH :226-231; THC :252); (F2) the completed-object placement
  H_omega/pi_omega/rho_pre/domain — legs L1/L2/L3/L5 OPEN, K4 NOT-MET
  (TH :260-296); U2_STATE_PORT_INSTANTIATED = false TYPE-U stands; (F3) the
  ordered rank pair (r_0, r_ch) — NO_VERDICT (D14 :14-17); (F4) conditioned
  effects F4-F6 — typed, no instance (U2 §3.4). )

SELECTOR_TYPE_IF_NONE = for the REMAINING freedom: (F1) derivable-new-work,
  construction named: the SM-1..SM-8-conformant joint state with omega_hist =
  Omega_C0 o i_B then the K4 comparison (TH :275-277) / equivalently a
  Theta_hist-conformant descent into State(C(Y)); existence undetermined of
  record; fallback type physical-input OR principal-decision (the C6 pattern),
  neither pre-assigned. (F2) derivable-new-work conditional on F1 (canonical
  null-quotient completion, U2 :259-263). (F3) derivable-new-work ONLY — the
  K_square arm (capped, C29) + the rank-pinning package (D14 :14-17); principal
  selection of a value is BARRED (voids the step, D14 :25-27). (F4)
  derivable-new-work (U2 :311-345).

MU_BEARING = symbolic, per outcome, mu ≡ 0 iff w_0 w_ch = 0 carried (FORM D4):
  OUTCOME A (ratified chain carried): mu != 0 PINNED SYMBOLICALLY — both sector
  weights strictly positive; magnitude and sign NOT pinned (pair NO_VERDICT,
  evaluation fenced, Gate-5 sign burden stands); the finite-N interference
  anchor n^2 mu Phi_tot (x) Phi_tot is non-degenerate on this chain.
  OUTCOME B (chain voided/refused): mu UNDETERMINED — free simplex; mu = 0
  exactly at the one-sector boundary, nothing executable forces boundary or
  interior. OUTCOME C: NO sealed outcome pins mu = 0; asserting it from the
  unbuilt field face is barred (DoR-006 via M06 :464-469). FIELD-FACE
  INDEPENDENCE: the State(C(Y)) continuum does not move mu between zero and
  nonzero — it gates completed-object licensing of the scalar amplitude, not
  the w_0 w_ch dichotomy. RECONCILIATION FLAG (unresolved, flagged): the
  ready-sandwich anchor vs the forced mixed record marginal (TH :134).

SEALS_VERIFIED = M06 d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae2
  49ae98 ; FORM 5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37 ;
  M03 2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f ;
  G3B 3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70 ;
  E1 468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5 ;
  PA 789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3 ;
  U2 2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab ;
  P5A 1274b1b71b46e6a34b641c0053d61ce1ed16c94e8d570317a7831f558bcfef58 ;
  P5D a33be83c1ee7cbfbda2cc3857425cb9e7e90a23bbe3d61c9ec89432e50b77874 ;
  MO 576da30f300a0362469d6b4f447423a2298efc0dd28a60f38057e7f4cc8bd888 ;
  NG 7000963e769362d87e17e36f06903e63aaa8caaeb284ee9c9f3676febe4f4163 ;
  GOV3 1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0 ;
  SGV4 d4cdbb6623797df6accb7dc9b24134d179bfc8e8d039c585d5c91ae23255869e ;
  D13 f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec ;
  D14 b6e4116df63403478d28be8cdb6589b091cc1aa8b6ad5a40776a28b135cd138f ;
  AXS1 32725f12a119bbb456dbe80e2c304e9dfb839acc9a05fda3d9f64e7317158842 ;
  TH 47f7c1248bcb46f631584fe647feb5e614a32ca8a894557d245d06ed343a64ba ;
  THC 7eaf5a748574add634a76c37de3abc2e81da2b861ed767dd24c9fd7c2279e4ed ;
  SF 73ef21bdbfef5fc9a3e602aedbf3a54e32a0274a4dd2fcc7214750b5b59ffe31 ;
  TA a71d4e59fcde1a7df10e8051e46befb9b4b6653a0917bb03a0c0403179717fef —
  all recomputed at path before reading; no seal failed.

FORBIDDEN_IMPORTS = none consumed. SUSPECT-classified only, never certifying:
  the PA/E1 fixed-gauge scale-bearing carrier (PA :50-70, :130-135) — classified
  and refused as a selector on its own sealed bars; alpha_x is a Dirac matrix.
  No scale, no imported GR, no faithfulness/injectivity premise anywhere; no
  numeric value of any weight, rank, dimension, ratio, or coefficient computed,
  bounded, or evaluated.

MACHINERY_INVOKED = no (determination and typing only; exact symbolic algebra on
  sealed displays; no numeric evaluation; no register/tracker/plan/road/ledger/
  lens read; no git action; output name probed ABSENT before write).

alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false ;
coupling_evaluation_authorized = false
ALL_RESULTS = CLAIMED until the adversarial panel.
```
