# Stage-1 Premise Disposition v001

Date: 2026-07-24

## Purpose

This file resolves the hidden-premise findings in the independent Stage-1
ledger before any physical cell, source threshold, response, or coupling is
computed. It does not make a numerical alpha claim.

The historical construction is target-aware. No later audit may relabel it
historically target-blind. A genuinely unused prediction remains mandatory
before comparison with measured alpha.

## Disposition 1: primitive unit winding

The ordered two-endpoint record carrier has the active projective stabilizer

```text
U(1)_rel.
```

Every continuous one-dimensional unitary character of `U(1)` is

```text
chi_n(exp(i theta)) = exp(i n theta),  n in Z.
```

Its kernel is all of `U(1)` for `n=0`, the finite subgroup of `|n|`th roots
of unity for `|n|>1`, and the identity alone for `n=+1` or `n=-1`.
Consequently a primitive faithful character has `|n|=1`; the two signs are
related by orientation reversal. Choosing the positive orientation gives
`n=+1`.

Thus unit winding is not a fitted charge magnitude. It follows from:

```text
the derived relative U(1) stabilizer;
continuity;
one-dimensional unitary action;
faithfulness after the response-null quotient;
and an orientation convention.
```

This fixes the unit-character convention. It does not derive the existence
of a physical local electromagnetic connection, which remains adopted
Level-1 field content.

## Disposition 2: primitive source inventory

The current ordinary source branch discloses:

```text
3+1 Lorentzian spacetime;
local Lorentz covariance and spin-statistics;
CPT compatibility;
one massless-bare vectorlike Dirac source;
the primitive faithful U(1)_rel character.
```

Given those inputs, the four-component Dirac representation and the
left-handed anomaly pair `(+1,-1)` follow. The number of primitive
vectorlike pairs does not. It is therefore classified as:

```text
one_pair = DISCLOSED_ORDINARY_PRIMITIVE_BRANCH_INPUT
```

and not as a Gravacle prediction.

The unit charge label is the primitive-character convention just derived;
it is not a numerical electromagnetic coupling. Additional charged species
cannot be silently discarded in a Thomson calculation. Stage 9 must derive
the complete charged spectrum and threshold map, or the final claim must
remain explicitly conditional on the one-source branch.

## Disposition 3: fidelity-action multiplicity

The candidate rule

```text
S_R,E = -hbar log F_R
```

contains a unit information multiplicity by adoption. The complete one-cell
CTP audit subsequently established that this postselected fidelity cost is
not the complete parent action of a closed unitary record theory.

Accordingly it is retired from the load-bearing alpha path:

```text
fidelity_action_is_complete_parent = false
information_multiplicity_enters_Q_spec = false
```

The physical normalization must instead be obtained from the complete
normalized amplitude `Z_Q[A]/Z_Q[0]`. Raising that amplitude to an arbitrary
power would describe multiple copies or a different theory and is forbidden
after `Q_spec` is frozen.

## Disposition 4: premise-set availability

The pre-alpha V156 single-handle theorem and its declared provenance inputs
are present at the hash-pinned paths in `provenance_inputs_v003.json`.
The previously reported missing source-mass, parent-underdetermination, and
chiral-operator audit files are also present in the full working corpus.
Their presence resolves package availability only; it does not promote any
conditional result.

## Stage-1 exit

The hidden premises are now explicit and classified:

```text
unit winding: DERIVED from faithful U(1) character theory;
one primitive Dirac pair: DISCLOSED BRANCH INPUT;
unit fidelity multiplicity: RETIRED from the physical alpha path;
premise files: PRESENT and provenance-checkable.
```

No measured alpha, endpoint value, residual, or desired response coefficient
is used in these dispositions. Historical target blindness remains false,
so the forward-sealed unused prediction in Stage 11 is non-negotiable.

This closes premise *classification*, not the physical theory:

```text
stage1_hidden_premise_ledger_closed = true
historical_target_blindness_established = false
unused_prediction_required = true
complete_source_spectrum_derived = false
complete_parameter_free_Q_spec_frozen = false
unique_causal_record_cell_derived = false
physical_Thomson_stiffness_computed = false
alpha_computed = false
proof_authorized = false
```
