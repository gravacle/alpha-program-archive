# STAGE 8 — TYPING-CANDIDATE-CHECK: BLIND ENTRY-WORTHINESS VERIFICATION OF STAGE8_TYPING_RULE_CANDIDATE_V001
## BLIND VERIFIER (TYPING-CANDIDATE-CHECK) — CROSS-LINEAGE, DEFAULT-REFUTE — [SEALED]

Date: 2026-08-13
Role: BLIND, cross-lineage, DEFAULT = REFUTE (the mandate lineage's prior candidates were
VOIDED by checks — the norm). Under test: one authored proposed-physics candidate for the
multi-cell write typing (R-CLW). I attack outcome-shopping, the FORCED/AUTHORED boundary,
consistency with sealed content, mandate shape, and the consequence map's exactness.
Exact structural algebra only; no physical quantity computed, bounded, or evaluated; no
scale / imported GR / faithfulness authority; no register/tracker/plan/road/ledger/lens
read; no git action; no register/commit/push.

---

## 0. PREFLIGHT AND SEALS

Output name `STAGE8_TYPING_RULE_CANDIDATE_CHECK_V001.md` probed ABSENT (artifact and
sidecar) before write. Every digest below recomputed from bytes at path by
`shasum -a 256` THIS session, BEFORE reliance.

```text
TARGET (matches the tasked digest):
74f9470189a72d0d214ce72153559e17f1dfb65abeb159afb30538f6774b954a  STAGE8_TYPING_RULE_CANDIDATE_V001.md   OK
  (sidecar present and matching; the tasked tc_flag_block is byte-content-identical to
   the artifact's own flag block — compared line by line)

SOURCES (all 13 recomputed at path; ALL match the candidate's §1 table and the tasked list):
ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213  ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md   OK
420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73  ATTACH_ENTRY_ADDENDUM_V001.md                      OK
ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md     OK
92dba8d33b09c5cd2a9ff5dab122f2cf2d3420d521cdf422d210d988f1ab6ee9  STAGE8_BOTH_BLOCKS_DETERMINATION_V001.md           OK
22f69dd3a87a1f71cc79d47606de2f547348a5619ba330cf31d53d013c67350e  STAGE8_BOTH_BLOCKS_CHECK_V001.md                   OK
f1881511c8a93cca3d8eb45f71b3be901cf337c9ce9321da0004901bfded6647  STAGE8_MIN_CARRIER_CANDIDATE_V001.md               OK
bb2e6570e17bb5d7734d306d94ac672df73bb0377b71e252b48e4751b8dc28f5  STAGE8_MIN_CARRIER_CHECK_V001.md                   OK
ca7fa4572a8b7037b40dd280700827081fec0e369ac0a8f9be1607113d719763  STAGE8_ANCHORING_DERIVATION_V001.md                OK
4a048901d4d2869352b5daf668bc341c0a5a715ef9e6ef232ba5772a6c911625  STAGE8_ANCHORING_CHECK_V001.md                     OK
8ed95b5a1ebd64d6899b5c6feabdfb17196095226f26ea0b885a0bf6ee10cab3  STAGE8_REQUIRE_G3_CHECK_V001.md                    OK
2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f  STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md  OK
3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70  STAGE8_G3_REALIZATION_BUILD_V001.md                OK
20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48  BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md      OK
UNVERIFIABLE: none.
```

Face check: the artifact says AUTHORED PROPOSED PHYSICS / PROPOSED_NOT_ADOPTED on line 1
and in §0, states that entry is the principal's act alone by a separate addendum, and
carries all gates false. The mandate's on-its-face requirement is MET.

---

## 1. ATTACK 1 — OUTCOME-SHOPPING (the highest-value attack). FINDING: NONE.

The refutation hypothesis: R-CLW was reverse-engineered from the desired verdict
(T2 passes the AND) and the "motivation chain" is a costume. Every motivating clause was
traced to its claimed sealed ground; every consequence channel through which shopping
could operate was checked for covert selection pressure.

```text
CLAUSE (a) <- (1) M03's ratified per-cell tensor factorization.  VERIFIED AT BYTES:
  M03 :127-133 (D_(n,j)[a_j] := diag(1, z_j^(n)[a_j], 1) reads a_j only), :135-140
  (W_N^(n)[a] := tensor_(j=1)^N W_(1,j)^(n)[a_j]), :387-399 (C7 zero-extension appends
  identity holonomies that drop out).  The G3 build's own sealed R1 (:146-151) already
  states cell-locality is "REALIZED by the ratified finite-locality tensor row, not
  imposed."  No verdict consulted; the row predates every typing analysis.
CLAUSE (b) <- (2) GC's containment-only ruling + the sealed bar.  VERIFIED AT BYTES:
  GC :153 and flag :253-255 (cell-locality of record = containment only), :203-206
  (containment the only content used), :122-148 (the one collapse line — "cell-local
  ⟹ contractible ⟹ boundary ⟹ phi_H = 0" — REQUIRES importing Omega_c's continuum-diamond
  contractibility, unsealed on connection-only bedrock and a forbidden GR import);
  22f69dd3 §3.4 :233-240 (the record "does NOT seal" that a cell-local cycle bounds).
  Grounded in a sealed NEGATIVE, not in any outcome.
CELL CONCEPT + CLOSED READ <- (3) the entered Attach's own operative construction.
  VERIFIED AT BYTES: V007 :633-649 (theta_j := contour-integral over gamma_j of A;
  D_n = diag(1, z^n, 1) — the cell's read IS its closed circuit), :162-172 (operative
  domain connected admitted b_1 = 1), :168-169 (trees INERT, "a DETERMINATION made on
  purpose"), :159-160 and :171-172 (the no-selector bar verbatim); addendum :33-34.
  The entered premise is OF RECORD; following its shape is consistency, not shopping.
(4) the anchoring equation consulted for CONSISTENCY only: certificate K-3 checks
  agreement (satisfied degenerately), selects nothing — verified below (§3).

THE THREE PINS, EACH VERIFIED AT BYTES:
(i)  T0 vs R-CLW: SAME AND outcome on BOTH exhibited carriers.  92dba8d3 :298-303
     (K_3 T0 SATISFIABLE) and :312-321 (T2 SATISFIABLE); f1881511 §5 (K_dd T0
     SATISFIABLE, T2 SATISFIABLE).  EXACT: the AND hinge cannot discriminate T0 from
     R-CLW, so it cannot have selected between them.  The T0/R-CLW discrimination is
     carried by L5, which is marked AUTHORED with a structural case only (§2 below).
(ii) T1's exclusion rests on verdict-independent sealed grounds: GC :122-148 +
     22f69dd3 §3.4 (the bounding inference needs a barred import); V011 :1840 and
     :1881-1882 ("No filled 2-cell is included in the trace carrier" — the entered
     cell's F = 0 census, scope TRACE-CARRIER-SPECIFIC confirmed at 22f69dd3 §1
     :69-132); 92dba8d3 §3.3 :279-281 (the plaquette shape is the working-class
     carrier's census, satisfied trivially there).  Under T1 the STANDING entered
     read theta_j (four edges on no filled face) would be typed NOT cell-local —
     a genuine structural contradiction with the entry, independent of every verdict.
     Note the direction: excluding T1 is where a shopper would push (T1 is the one
     UNSATISFIABLE typing), and it is exactly the exclusion with the strongest sealed
     grounds.  The costume hypothesis fails where it matters most.
(iii) the consequence map REPORTS pre-existing sealed analyses (both checked SOUND
     before this candidate existed: 22f69dd3, bb2e6570); every §9 entry re-derives
     from those seals with no residue (§5 below).  Nothing in the map is new
     computation the clauses could have been tuned against.

REMAINING SHOPPING CHANNELS PROBED: (α) the fork resolution (C2) — the loop horn is
  forced by the entered premise's own closed read + Lemma 1, and its content (ports
  inert, no gluing license) LICENSES NOTHING downstream; a shopper gains nothing.
  (β) the G3 supply (C3) — supplying the typing is the MANDATED object itself
  (the ad9fc14e pattern: supply what the record leaves free, disclosed as authored);
  wanting the freedom closed is the mandate, not shopping.  The split value that
  results is reported as the computed projection, not chosen.  (γ) the anchoring
  value — the DEGENERATE family, reported as-is, not repaired (candidate §5.4).
OUTCOME_SHOPPING = NONE.
```

---

## 2. ATTACK 2 — THE GROUNDING AUDIT (the FORCED/AUTHORED boundary). FINDING: CONFIRMED.

Every ledger row re-opened at its span. The mandate's honesty clause (ad9fc14e :37-43:
"An authored ingredient presented as forced voids the candidate") applied as the void test.

```text
L1  FORCED — CONFIRMED.  M03 :127-140 + :387-399 force that the j-th write factor
    reads cell j's own history carrier and nothing else; GC :153/:203-206/:253-255
    seal cell-locality of record as containment-only.  The row claims containment at
    the ONE-CELL stratum only, and separately states what is NOT forced (which chains
    within the cell; any bounding property).  Honest.
L2  AUTHORED — CONFIRMED HONEST.  The absence it fills is sealed: 92dba8d3 §3.3
    :278-279 ("The sealed text supplies NO definition of 'cell' or 'cell-local' on
    K_3") and N-3 :368-372 ("'cell-local' is undefined of record on any multi-cell
    carrier"), both CONFIRMED at 22f69dd3 §3.3 :220-231.  The extension to C_j^(1)
    is marked authored; minimality is argued (§5), never claimed forced.
L3  FORCED (as an exclusion) — SURVIVES THE BOUNDARY ATTACK, with the reading pinned.
    This is the closest point on the FORCED/AUTHORED boundary and I pressed it:
    what the cited spans force is the NEGATIVE fact only — the record does not seal
    that a cell-local cycle bounds, and the sole bounding-inference route requires an
    unsealed, program-forbidden GR import (GC :122-148; 22f69dd3 §3.4).  A candidate
    COULD still author a bounding clause as disclosed authored content (T1-shaped);
    "no bounding clause" as a decision is therefore not exhausted by the forced
    negative.  THE CANDIDATE'S STRUCTURE ABSORBS THIS EXACTLY: clause (b) carries no
    decision content beyond clause (c) (the absence of a clause is decided by
    exclusivity), and clause (c)/L5 IS marked AUTHORED.  So the residual choice is
    disclosed under L5, the forced negative is what L3 claims, and no authored
    ingredient is presented as forced.  NOT a void.  (Noted as minor finding M2.)
L4  AUTHORED (TYPE-P-grounded) — CONFIRMED HONEST.  addendum :33-34 and V007 :162-172
    type the ATTACH map's domain; the row says exactly that and marks the lift to a
    general write-typing cell concept as authored, TYPE-P on the Attach entry chain.
L5  AUTHORED — CONFIRMED HONEST.  The structural case (a typing that constrains
    nothing on multi-cell carriers types the sealed word away) has genuine sealed
    support — G3 Q6 :130-135 lists cell-local as a REAL fixed property of ell_j;
    GC flag :253-255 lists it among the of-record constraints — and is still marked
    "not forced."  Correct: the support motivates, it does not force.
L6  DERIVED-EXACT — RE-DERIVED INDEPENDENTLY, all pass (script §7, exit 0):
    LEMMA 1: for support(ell) ⊆ C_j^(1) with C_j a subcomplex, every edge's endpoints
      lie in C_j, so bdry_K(ell) = bdry_{C_j}(ell); conserved of record is the ONE
      condition ⊥ im(d_0) = ker(d_0^dagger) = ker(bdry) (GC :77-91), hence the
      admissible on-cell set is Z_1(C_j;Q), dimension b_1(C_j) = E - V + 1 for
      connected C_j.  EXACT; instance verified on the trace cell embedded in K_dd.
    LEMMA 2: dim 1 at b_1 = 1 (line Q·gamma_j), dim 0 at b_1 = 0 (EMPTY with
      total-nonzero), dim b_1 at b_1 > 1 (no selection).  Verified exactly on
      K_square (dim 1), the K_3 3-circuit (dim 1), a 3-edge tree (dim 0), and a
      theta graph (dim 2).
    K-1: gamma_j closed and spanning on K_square; the entered read theta_j = <A,
      gamma_j> is cell-local under R-CLW at coefficient 1.  PASS.
    K-2: tree admissible set empty — reproduces V007 :168-169 as a theorem.  PASS.
    K-3: closed ⟹ ker(d_0^dagger); on K_square the kernel = Z·gamma exhaustively
      over the integer box (my own enumeration), matching ca7fa457 :347-356 (B3) and
      the anchoring check's I-B3; the anchoring equation (*) (ca7fa457 §2.1 :195)
      is satisfied degenerately per §2.3a — never violated.  PASS.
GROUNDING = CONFIRMED.  The authored core is L2/L4/L5, completely disclosed; no
derived costume found on any authored content.
```

---

## 3. ATTACK 3 — CONSISTENCY WITH SEALED CONTENT. FINDING: CLEAN.

```text
vs M03's locality:   the rule's one-cell degenerate case reduces to the sealed
  containment law (candidate D1); M03's factorized row is the one-cell instance of
  clause (a).  No amendment, no re-reading.
vs the entered Attach's own reading:  K-1 verified — the entered read is well-typed;
  K-2 verified — the tree disposition reproduced; the no-selector bar respected
  verbatim (N1 quotes V007 :171-172; the rule types the b_1 > 1 SPACE and selects
  no class — typing was never barred, selecting was).
vs the anchoring law:  admissible writes are closed (Lemma 1) ⟹ (*) holds with
  e_out = e_in, the degenerate family of ca7fa457 §2.3a, verified by the anchoring
  check's X2 (exactly the 4 diagonal pairs).  The LAW is untouched; no sealed
  identity violated.
vs the entered domain restrictions:  R-CLW does not extend the Attach map's domain
  (D3); N-2 of 92dba8d3 (the b_1 > 1 selector supply) is carried OPEN in C1.
vs the F = 0 scope:  untouched; the candidate consumes V011 :1840/:1881-1882 only at
  the spans the verified checks confirmed, at their TRACE-CARRIER-SPECIFIC scope.
vs GC's split-freedom verdict:  no contradiction — the candidate never claims the
  record forces the split; the split becomes computed ONLY as a consequence of an
  ENTERED premise (C3, conditional on entry + carrier; C4 reverts on refutation),
  exactly the N6 "premise alongside the record, never a correction" typing.
CONSISTENCY = CLEAN.  No sealed sentence contradicted, amended, or re-read.
```

---

## 4. ATTACK 4 — MANDATE SHAPE (Attach-lineage conformance). FINDING: CONFORMANT.

Compared clause-by-clause against ad9fc14e and the addendum's entered shape:

```text
Honesty of record (ad9fc14e :37-43): ingredient-by-ingredient FORCED/AUTHORED ledger
  present (§4), authored core named and minimal, the void-on-costume clause adopted
  as binding on the artifact's face (§0).  CONFORMANT.
Falsifiers (ad9fc14e :45-50): F1 = the blind-test / Q-212 blast-radius falsifier
  verbatim in shape; F2 = the structural-contradiction immediate void, with three
  named concrete triggers.  CONFORMANT, with two well-formed additions (F3 the
  transport channel — matching 3e35ffe2 S2/:338-344's named unsupplied slots; F4 the
  dependency void with an exact survival split: circuit clauses die with an Attach
  void, containment rides M03's chain — matching E3's dual TYPE-P chains).
Open forcing slot (ad9fc14e :51-54): F5 mirrors DoR-008's slot in shape
  (future_derivation_can_select_the_write_typing = NO_VERDICT; agree = dissolve,
  disagree = F2).  CONFORMANT.
Battery overdetermination (ad9fc14e :55-58): not carried as a NAMED clause; its
  content is carried — F1 scores every output the typing feeds, §5.4 declares no
  shaping by any downstream target, N5 holds every gate false and evaluates nothing.
  Substance CONFORMANT (noted as minor finding M3).
Entry mechanics (ad9fc14e :60-66): E4 verbatim in shape — separate principal act,
  addendum naming candidate + check digests, explicit confirmation, registrar and
  lanes barred, no conversational substitute; E1 requires this DEFAULT-REFUTE
  opposite-lane check first.  CONFORMANT.
Domain / no-freeze: D1-D4 explicit; N1-N6 cover selector / admission / gluing /
  promotion / value / amendment — each verified against its cited seal (V007
  :171-172; ca7fa457 §4 L-2; §2.3a ports inert; NONE_SEALED; gates; N6).  COMPLETE.
MANDATE_SHAPE = CONFORMANT.
```

---

## 5. ATTACK 5 — THE CONSEQUENCE MAP, RE-DERIVED. FINDING: EXACT.

Every §9 entry re-derived against the sealed analyses at bytes:

```text
C1  K_3: the T2 analysis (92dba8d3 :312-321, witness lam = the 3-circuit cochain,
    phi_H = c_0/27 != 0, phi_f = lam - c_0/27 != 0, re-verified by 22f69dd3 §3.2
    against independent matrices) IS the R-CLW-typed verdict: the 3-circuit is a
    connected b_1 = 1 subcomplex (3 edges, 3 vertices — dim Z_1 = 1 verified) and
    lam ∈ Q·gamma.  K_dd: f1881511 §5 (chi_j = h/4 + (3/4)w, both nonzero, checked
    SOUND bb2e6570 :11) — chi_j is supported on the trace cell's four edges and
    closed (verified by my script inside K_dd), i.e. exactly the Lemma-2 line.
    T0 also SATISFIABLE on both (pin (i)).  CARRIED-OPEN LINES VERIFIED GENUINE:
    N-1 and N-2 quoted from 92dba8d3 §4 :355-367; no physical carrier of record
    (NONE_SEALED stands, f1881511 §6(iv)).  EXACT.
C2  Lemma 1 ⟹ every in-domain admissible write is closed ⟹ ca7fa457 §2.3a binds:
    degenerate common-0-cell family, four equivalent members (anchoring check X2),
    ports INERT, open horn excluded in-domain (mutually exclusive realizations,
    §2.3c), law (*) untouched.  The fork's NOT-DERIVABLE gap (§2.3c :308-316,
    AN_VERDICT :514-522: HOLDER = authored-physics entry or principal decision)
    is discharged by ENTRY — the holder-shaped act, principal-gated per E4.  EXACT.
C3  Under the rule + the ENTERED normalization (coefficient 1 at n = +1 — supplied
    by the entry, correctly NOT attributed to the rule), ell_j = gamma_j(C_j) per
    admitted b_1 = 1 cell; the incidence display is the entered display at ENTERED
    strength with S1 of 3e35ffe2 (:225-230) explicitly NOT discharged at law
    strength — matching ca7fa457 §3's own careful handling; the block-split becomes
    the computed projection of gamma_j(C_j) in K (instances: C1's exact splits).
    UNTOUCHED LIST VERIFIED: the diamond transport (3e35ffe2 S2, :338-344 —
    now F3), the FR 32-dim refinement freedom, mu/G2 (GC §3: CONFIRMED_UNBUILT),
    the post-limit C (f1881511 §6: cross vanishing "can come ONLY from mu" once
    both projections are nonzero — quoted, not evaluated).  EXACT.
C4  Reversion verified against the standing sealed statuses: AND → TYPING-DEPENDENT
    (92dba8d3 :324-326 headline), fork → NOT-DERIVABLE (ca7fa457 §2.3c), G3 → the
    free space (3e35ffe2 §6 :327-336), TYPE-P dependents void (F4 scope), sealed
    record untouched.  EXACT.
CONSEQUENCE_MAP = EXACT.  Nothing flattened: the §11 flattening audit's four
non-flattenings each verified present in the map's own wording.
```

---

## 6. ATTACK 6 — PROVENANCE AND INJECTION. FINDING: CLEAN / none.

```text
PROVENANCE: 13/13 source seals + the target recomputed at path this session by me
  (full digests, not substrings; the corpus's known substring-seal trap avoided);
  all match the candidate's §1 table exactly; the candidate's V011 usage stays
  inside the spans the verified checks confirmed at bytes.  The candidate's
  FORBIDDEN_IMPORTS = none claim VERIFIED: the one scale/GR-adjacent object
  (diamond contractibility) appears only as the sealed BAR grounding clause (b) —
  never consumed as authority; no continuum object consumed; no value evaluated
  (every displayed number re-checked as a sealed quotation or an exact structural
  dimension).  I read no register/tracker/plan/road/ledger/lens file.
INJECTION: all sources technical; no embedded directive encountered in any file
  read; the candidate's E1 spec matches my tasking and directs nothing else.
```

---

## 7. MY SCRIPT, VERBATIM (`tc_check_lemmas.py`, Python 3 stdlib only), AND ITS OUTPUT

Independent re-derivation of Lemmas 1-2 and K-1..K-3 instances by exact Fraction
elimination and exhaustive integer-box enumeration — written from the sealed censuses
directly, not from the candidate's text. Exit code 0.

```python
#!/usr/bin/env python3
# tc_check_lemmas.py — TYPING-CANDIDATE-CHECK independent re-derivation.
# Exact integer algebra, stdlib only. Verifies Lemmas 1-2 and K-1..K-3 instances
# on the sealed cells: K_square (entered cell, inside K_dd), the K_3 coordinate
# 3-circuit, a tree, and a b_1=2 theta graph. No physical quantity computed.
from fractions import Fraction

def bdry(edges, chain, nverts):
    out = [Fraction(0)] * nverts
    for (s, t), c in zip(edges, chain):
        out[t] += c
        out[s] -= c
    return out

def cycle_dim(edges, nverts):
    E = len(edges)
    rows = [[Fraction(0)] * E for _ in range(nverts)]
    for i, (s, t) in enumerate(edges):
        rows[t][i] += 1
        rows[s][i] -= 1
    rank = 0
    r = 0
    for c in range(E):
        piv = next((i for i in range(r, nverts) if rows[i][c] != 0), None)
        if piv is None:
            continue
        rows[r], rows[piv] = rows[piv], rows[r]
        pv = rows[r][c]
        rows[r] = [x / pv for x in rows[r]]
        for i in range(nverts):
            if i != r and rows[i][c] != 0:
                f = rows[i][c]
                rows[i] = [a - f * b for a, b in zip(rows[i], rows[r])]
        r += 1
        rank += 1
    return E - rank

kdd_edges = [(0, 1), (0, 2), (1, 3), (2, 3), (0, 2)]
ksq_edges = kdd_edges[:4]
chi = [1, -1, 1, -1, 0]
gamma = [1, -1, 1, -1]

assert bdry(kdd_edges, chi, 4) == [0, 0, 0, 0], "chi not closed in K_dd"
assert bdry(ksq_edges, gamma, 4) == [0, 0, 0, 0], "gamma not closed in K_square"
assert chi[4] == 0, "chi reads the outside edge"

assert cycle_dim(ksq_edges, 4) == 1, "K_square Z_1 dim != 1"
k3_circ = [(0, 1), (1, 2), (2, 0)]
assert cycle_dim(k3_circ, 3) == 1, "3-circuit Z_1 dim != 1"
assert bdry(k3_circ, [1, 1, 1], 3) == [0, 0, 0], "lam not closed"
tree = [(0, 1), (1, 2), (2, 3)]
assert cycle_dim(tree, 4) == 0, "tree Z_1 dim != 0"
theta = [(0, 1), (0, 1), (0, 1)]
assert cycle_dim(theta, 2) == 2, "theta Z_1 dim != 2"

from itertools import product
members = []
for a, b, c, d in product(range(-3, 4), repeat=4):
    if bdry(ksq_edges, [a, b, c, d], 4) == [0, 0, 0, 0]:
        members.append((a, b, c, d))
assert all((a, b, c, d) == (a, -a, a, -a) for (a, b, c, d) in members), "kernel not Q*gamma"
assert len(members) == 7, "kernel box count wrong"

print("LEMMA1_INSTANCE = PASS (bdry_K == bdry_Cj == 0 on the embedded trace cell)")
print("LEMMA2_DIMS     = PASS (b_1=1 line; b_1=0 empty; b_1=2 dim-2 space)")
print("K1_ENTERED_READ = PASS (gamma_j in Z_1(K_square), coefficient 1)")
print("K3_KERNEL       = PASS (closed chains on K_square = Z*gamma exhaustively in box)")
print("ALL_EXACT_CHECKS_PASS")
```

```text
LEMMA1_INSTANCE = PASS (bdry_K == bdry_Cj == 0 on the embedded trace cell)
LEMMA2_DIMS     = PASS (b_1=1 line; b_1=0 empty; b_1=2 dim-2 space)
K1_ENTERED_READ = PASS (gamma_j in Z_1(K_square), coefficient 1)
K3_KERNEL       = PASS (closed chains on K_square = Z*gamma exhaustively in box)
ALL_EXACT_CHECKS_PASS
```

---

## 8. MINOR FINDINGS (four; none overturning, none requiring repair before entry)

```text
M1  CITATION-PRECISION NIT: the candidate's S5 renders ca7fa457 §2.3c's "not a lane
    act" as "no lane may select the horn" inside quotation marks (with an ellipsized
    parenthetical).  Content-faithful paraphrase presented as quotation; both cited
    spans (§2.3c :308-316; AN_VERDICT :514-522) verified to carry exactly that
    content.  Non-load-bearing.
M2  THE L3 BOUNDARY READING (from §2): "FORCED (as an exclusion)" is correct only
    because the residual decision content of clause (b) is swept into L5, which is
    marked AUTHORED.  The candidate's structure does this exactly; any future
    restatement of R-CLW that drops clause (c) while keeping L3's FORCED tag would
    re-open the boundary attack.
M3  The DoR's battery-overdetermination clause (ad9fc14e :55-58) is carried in
    substance (F1 + §5.4 + N5) but not as a named clause.  Substance conformant.
M4  Span offsets of one line: 92dba8d3 N-2 cites V007 :170-171 where the candidate
    cites :171-172 for the same verbatim bar — the sentence spans both; verified at
    bytes both ways.  Non-defect.
```

---

## FLAG BLOCK

```text
OUTCOME_SHOPPING = NONE (every motivating clause traced to sealed structure at
  verified spans: M03 :127-140/:387-399 → (a); GC :122-148/:153/:203-206/:253-255 +
  22f69dd3 §3.4 → (b); V007 :633-649/:162-172/:168-169/:171-172 + addendum :33-34 →
  the cell concept and closed read; anchoring consulted for consistency only.  The
  three pins verified at bytes: T0 and R-CLW SAME AND outcome on both carriers so
  the hinge cannot have selected; T1's exclusion rests on the sealed bounding-import
  bar + the entered cell's F = 0 census + the working-class-census placement, all
  verdict-independent — and T1 is the one place a shopper would push, which is
  exactly where the sealed grounds are strongest; the map reports pre-existing
  checked-SOUND analyses with no new computation to tune against.  Residual
  channels — fork resolution, G3 supply, anchoring value — probed and clean.)
GROUNDING = CONFIRMED (L1 FORCED verified at M03/GC spans; L2/L4/L5 authored and
  honestly marked, the absences they fill sealed at 92dba8d3 §3.3/N-3 + 22f69dd3
  §3.3; L3's FORCED-as-exclusion survives the boundary attack because the forced
  content is the sealed negative and all residual decision content is carried by
  L5 marked AUTHORED; Lemmas 1-2 and K-1..K-3 re-derived independently, exact,
  script exit 0.  No derived costume on authored content; authored core = L2/L4/L5,
  complete.)
CONSISTENCY = CLEAN (one-cell case reduces to the sealed containment law; the
  entered read is well-typed under the rule with coefficient 1; tree disposition
  reproduced as a theorem; anchoring law satisfied degenerately, X2-verified, never
  violated; no-selector bar respected — the rule types the b_1 > 1 space and
  selects nothing; Attach domain not extended; F = 0 scope untouched; GC's
  split-freedom verdict uncontradicted — the split closes only by ENTERED premise,
  reverting on refutation; no sealed sentence amended or re-read.)
MANDATE_SHAPE = CONFORMANT (honesty ledger ingredient-by-ingredient; F1/F2 verbatim
  in shape from ad9fc14e :45-50 + addendum :49-56; F5 mirrors the DoR-008 slot;
  F3/F4 well-formed additions with exact scopes; E4 = the ad9fc14e :60-66 entry
  mechanics; E1 is this check; D1-D4 and N1-N6 complete and seal-anchored;
  AUTHORED-PROPOSED status on the artifact's face.  Battery-overdetermination
  carried in substance (F1 + §5.4 + N5) rather than as a named clause — noted M3.)
CONSEQUENCE_MAP = EXACT (C1 re-derived against 92dba8d3 :312-321 / 22f69dd3 §3.2
  and f1881511 §5 / bb2e6570 — both witnesses lie in the Lemma-2 line of their
  b_1 = 1 cells, verified by my own algebra; N-1/N-2/no-physical-carrier genuinely
  open of record; C2 forced by Lemma 1 + ca7fa457 §2.3a with X2; C3's conditional
  statuses and untouched list verified at 3e35ffe2 :225-230/:327-336/:338-344,
  GC §3, f1881511 §6; C4's reversion matches every standing sealed status; no
  flattening found.)
ENTRY_WORTHINESS = READY-FOR-PRINCIPAL (VERDICT = SURVIVES-FOR-ENTRY; SCOPE = the
  typing rule R-CLW as stated, an ENTERED-AUTHORED PREMISE candidate only — entry
  decides a typing, no value, no carrier, no admission, no selector, no gluing;
  four minor findings M1-M4, none requiring repair; the principal ratifies against
  E2's list knowing the map, FOR the structural grounds.)
PROVENANCE = CLEAN (13/13 source seals + target recomputed at path this session,
  full digests, all match; candidate's V011 consumption stays at check-confirmed
  spans; FORBIDDEN_IMPORTS = none verified — diamond contractibility appears only
  as the sealed BAR; every displayed number a sealed quotation or exact structural
  dimension; no register/tracker/plan/road/ledger/lens read by me or by it.)
INJECTION = none (all sources technical; no embedded directive encountered).
MACHINERY_USED_BY_ME = no (exact Fraction/integer structural algebra and exhaustive
  integer-box enumeration, stdlib only; no numeric evaluation of any physical
  quantity, no fixed-point execution, no end test, no member binding; output name
  probed ABSENT before write; no git action; no register/commit/push.)
alpha_computed = false, kappa_record_computed = false, proof_authorized = false.
TC_CHECK_RESULT = SEALED.
```
