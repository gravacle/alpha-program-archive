# STAGE 8 — THE LOCALIZATION ASSUMPTIONS UNDER THE CLOSURE'S OBSTRUCTIONS — O9SR V001

## BUILDER — CODENAME LOCALIZATION-BUILD — COMMISSION O9SR — 2026-08-15

**THE QUESTION, adversarial:** are the localization assumptions underlying the
closure's obstructions SOUND, RECORD-NATIVE, and CORRECTLY QUANTIFIED — or are they
machinery-imported notions and/or artifacts of an idealization the record does not
require?

---

## 0. VERDICT IN ONE LINE

```text
MIXED — and the mixture is load-bearing in both directions.

  QUANTIFIER-MISMATCH, CONFIRMED AT BYTES, at exactly ONE of the three
    obstructions: the W-3 SEA-SANDWICH obstruction is proved at the AMBIENT /
    INFINITE-RANK quantifier over the CLASS SUPREMUM, and it is displayed BY ITS
    OWN TEXT as ABSENT at every finite stage the closure actually instantiates.
    The closure's FP-3 requirement is per-composite and n-free. The record DOES
    own the instrument that would bridge "absent at each finite n" to
    "obstructed uniformly in n" — strong convergence plus weak lower
    semicontinuity of the HS norm — and has run it successfully once. But that
    transport needs the AMBIENT DIVERGENCE OF THE RECORD'S OWN COMPOSITE, and
    the sea-sandwich supplies divergence only for h_0 and the class supremum.
    The one successful run of record used a KERNEL-HOMOGENEITY divergence
    instead, never the sea-sandwich. The missing premise is exactly the one
    W-1 §5.3 declares unsealed and unassumed. Therefore the
    uniform-in-n blocking at FP-3 is NOT carried by the sea-sandwich obstruction
    at all; it is carried by a DIFFERENT and independently sealed object (FHB's
    FB9 non-implication). ONE SENTENCE OF RECORD OVERSTATES THIS — W-1 :343
    ("The obstruction bites at the uniformity") — and that sentence is
    inconsistent with W-1's own :379-385 honest boundary. FLAGGED, not refuted:
    the blocker survives on FB9; only its ATTRIBUTION is wrong.

  MACHINERY-IMPORTED, CONFIRMED AT BYTES, for TRANSLATION INVARIANCE as used in
    the seminorm gate: the W-1 audit already proved hypothesis (a) INERT in
    T-3's own proof. Re-derived independently here. Translation invariance is
    not the operative notion at that site; CLASS WIDTH is. The record-native
    form differs from the machinery-shaped form that was displayed.

  ASSUMPTIONS-SOUND for the sharp-cutoff site (S-2), with one cost displayed:
    SHARP localization is NOT a choice among admissible localizations. D6'
    (E1 :465-470) is an EXPLICIT EXHAUSTIVE PROHIBITION — "No obligation,
    lemma, control, repair, or enclosure may replace, mollify, smooth, or
    regularize M(t)" — verified at source by the W-3 audit. So the divergence
    is not an artifact of an unforced choice. It IS, however, shape-sensitive:
    the commutator diverges through the derivative of the indicator, and a
    smooth profile would not diverge. Both halves together give the TYPE: the
    FP-3 operator-norm exclusion is a RECORD-IMPOSED obstruction that inherits
    D6''s grade exactly, not a shape-independent analytic one. That conditional
    dependence is nowhere displayed in the diagnosis chain. FLAGGED, not
    refuted. No smoothing is proposed; D6' is respected as written.

  NOT-WELL-POSED for the cross-obstruction degree comparison (S-4): the three
    obstructions are graded in variables of DIFFERENT KIND — a DISCRETE STAGE
    COUNT at n -> infinity (FP-1), a CONTINUOUS SYMBOL ORDER in dimension 3
    (FP-2), and a CONTINUOUS MOMENTUM-TRANSFER exponent at q -> 0 (FP-3). Three
    predicates wear the same numeral. No record-native functor identifying them
    is sealed anywhere at the declared key sets. The audit already refuted the
    comparison on its own FP-2 row; this run finds the defect is not arithmetic
    but TYPE-LEVEL — even a corrected FP-2 row would have made the universal a
    coincidence of notation. The record's own CAS carries the incoherence
    visibly, in a SPLIT SIGN CONVENTION (need_fp1 > 0 against need_fp2,3 < 0)
    that no single convention can repair. The shared origin survives ONLY in
    the origin run's own §5.2 type-level form, which compares no degrees.

  FENCES HELD: alpha_computed = false; proof_authorized = false;
  kappa_record_computed = false. DETERMINATION ONLY. No physics authored, no
  value, no number of the closure, no measured-constant comparison. NOTHING IS
  REFUTED that was not already refuted of record by its own audit.
```

---

## 1. SEALS VERIFIED AT PATH BEFORE ANY RELIANCE; SWEEP CUTOFF DECLARED

### 1.1 Seals — `shasum -a 256 -c` run FROM EACH ARTIFACT'S OWN DIRECTORY

```text
DIRECTORY: /Users/bgm/MB Work/alpha-program-archive/workspace
  STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md .................... OK
  STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001.md .............. OK
  STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md ................ OK
  STAGE8_OBSTRUCTION_ORIGIN_O6SR_AUDIT_V001.md .......... OK
  STAGE8_W1_SUBVOLUME_RATE_O5SR_V001.md ................. OK
  STAGE8_W1_SUBVOLUME_RATE_O5SR_AUDIT_V001.md ........... OK
  STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md ............... OK
  STAGE8_W2_PREQUOTIENT_RULE_O2SR_AUDIT_V001.md ......... OK
  STAGE8_COMPLETION_MAP_T17SR_V001.md ................... OK
  STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md ............. OK
ALL TEN VERIFIED. No artifact was relied on before its seal was checked.
Every audited target is consumed AT AUDITED GRADE — i.e. with its audit's
CORRECTIONS APPLIED, never at the build's headline. Where an audit REFUTED a
clause, the refuted clause is treated as absent, not as weakened.
```

### 1.2 SWEEP CUTOFF — DECLARED

```text
CUTOFF: 2026-08-15, this session, at the two commissioned corpus roots.
ROOT 1: /Users/bgm/MB Work/alpha-program-archive/workspace     (1743 .md files)
ROOT 2: .../alpha_fundamental_record_action_cleanroom_v003     (2308 .md files)

SCOPED READS: the ten named artifacts, in full at the named sections.

DECLARED SCOPED SWEEPS, key sets stated exactly (exhaustiveness claimed at
these key sets only, at this cutoff):
  K-1  the finite-to-limit transport keys:
       "translation-invariant limit" | "lower semicontinu" |
       "strong operator limit" | "strongly to C"
       ROOT 1 -> 13 files; ROOT 2 -> 4 files. Both roots swept.
  K-2  the localization-shape keys:
       "[h_0, 1_B]" | "sharp cutoff" | "sharp-cutoff" | "mollif" | "smooth" |
       "commutator" | "D6'" | ":465-470"
       Run across the named ground and both roots' file lists.

EXCLUDED BY COMMISSION, NOT READ, NOT LISTED, NOT COUNTED: every register,
tracker, road, plan, continuation, ledger, and lens file. Sweep results were
filtered against those tokens before use.
"Q-..." register tokens: EXPECTED-UNLOCATABLE — not chased.

SWEEP HITS OUTSIDE THE NAMED GROUND THAT PROVED LOAD-BEARING (declared, and
consumed only at the two line-ranges quoted in SS2.3):
  STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md            :20-26, :99-121
  STAGE8_RL2B_CONVERGENCE_HYPOTHESIS_RESOLUTION_AND_F5_TRILEMMA_V001.md :162
These are LANE RESULTS, not registers. They are consumed HERE ONLY as evidence
that a transport instrument EXISTS of record — never for any verdict of theirs.
```

---

## 2. (S-1) THE FINITE-RANK ESCAPE — **QUANTIFIER-MISMATCH, CONFIRMED** [DERIVED]

### 2.1 At which quantifier does each obstruction BITE — at bytes, verbatim

```text
OBSTRUCTION A — THE SEA-SANDWICH (W-3 SS4.1, the FP-3 / W-3 obstruction).
  STATEMENT'S OWN QUANTIFIER, verbatim (W3 :317-321):
    "Let {U_a} be the translation group ON L^2(R^3) (x) C^4 ... If C T C != 0,
     then C T C is NOT COMPACT."
  The object quantified over is the AMBIENT operator on the FULL space. The
  proof's engine is U_a psi -> 0 WEAKLY as |a| -> infinity — an INFINITE-VOLUME
  statement. There is no stage index anywhere in the theorem or its proof.
  ITS OWN ABSENCE CLAUSE, verbatim (W3 :401-402):
    "At finite rank the obstruction is absent (a finite-rank operator is
     compact), and FHB's compression calculus applies."
  ==> BITES AT: LIMIT ONLY (ambient / infinite-rank).
  ==> AND, second coordinate: it bites on the CLASS SUPREMUM, not on a member
      the closure names. W-3 SS4.2's corollary is
        sup_{t in H(sqrt3)} ||C T C||_2 = +infinity, "witnessed EXACTLY by the
        class member h_0 itself" (W3 :361-363),
      and W-3's own DOES NOT ADD clause (:390-393) says the +infinity attaches
      "NOT to the actual C-L2 error layer, which is a DIFFERENT OPERATOR and
      remains UNDECIDED." Restated at :716-718: "objects of the argument, not
      objects of the closure."

OBSTRUCTION B — THE VOLUME-DIAGONAL LOCUS (FP-2).
  MAP :93-98: the fatal |x-y|^{-3} integral is placed "AT THE VOLUME DIAGONAL
  x = y", a per-composite statement about a symbol on the diagonal.
  ==> BITES AT: FINITE STAGE (it is a statement about the composite's own
      kernel/symbol, requiring no limit and no uniformity).

OBSTRUCTION C — THE rank x op CEILING (FP-1).
  MAP :86-87 QUANTIFIER, verbatim: "per-composite, ALL n; the delivered op-grade
  enclosure (||K_H,n||_op <= 2, ||H_A,n||_op <= 2 exact, n-uniform) stands
  DELIVERED MEANWHILE; the conversion refuses." MAP :83-84: the conversion
  "carries the full carrier volume 4n^3 AGAINST AN n-FREE TARGET".
  ==> BITES AT: UNIFORM-IN-STAGE (finite at every n; the refusal is the
      n-freeness).
```

### 2.2 At which quantifier does the CLOSURE REQUIRE the discharge

```text
FP-1  n-FREE — the target is stated "n-free" at MAP :83-84.        UNIFORM-IN-n
FP-2  per-composite — the diagonal statement at each composite.    FINITE STAGE
FP-3  per-composite; and the consuming site R.3 (E1 :810-813) is
      ||A_s||_2 <= ||A(0)||_2 + ||Delta||_2 <= |C|_4^beta G_cm + ...
      G_cm is a FUNCTIONAL OF (C, eps) — carrying NO stage index at
      all. An n-indexed G_cm would not be a G_cm.                  UNIFORM-IN-n

THEREFORE THE CLOSURE NEEDS, AT FP-3: a finite bound on the SPECIFIC C-L2 error
layer A(0), holding UNIFORMLY IN THE STAGE INDEX. It does NOT need — and cannot
use — any statement about an ambient infinite-rank operator, nor about the
supremum over the whole class H(sqrt3).
```

### 2.3 THE MISMATCH, AND WHY IT IS NOT REPAIRED BY THE RECORD'S OWN TRANSPORT

The gap between "absent at every finite stage" and "obstructed uniformly in the
stage" is not automatic. The record OWNS an instrument that closes exactly such
a gap, and has RUN it successfully once. Consumed here only as evidence that the
instrument exists:

```text
THE TRANSPORT INSTRUMENT, of record (RL2B_UNIFORMITY_REFUTED :20-26):
    "X_n -> X strongly, hence weakly;  ||X||_2 <= liminf ||X_n||_2 <= M;
     but ||X||_2 = infinity.   => NO SUCH M EXISTS."
  Its links (:99-121): LINK 3 strong convergence of products under uniform
  boundedness; LINK 4 C_n -> C strongly (two independent derivations);
  LINK 7 weak lower semicontinuity of ||.||_2; and LINK 6 — the AMBIENT
  DIVERGENCE — supplied by kernel homogeneity: "C1's off-diagonal modulus
  EXACTLY 1/(2 pi^2 |r|^3), homogeneous degree -3 ... A degree -3 kernel on R^3
  is NOT Hilbert-Schmidt."

SO THE TRANSPORT REQUIRES TWO PREMISES:
  (T-i)  the record's own stage objects converge strongly to an ambient object;
  (T-ii) THAT AMBIENT OBJECT — the limit of the record's own composites — has
         infinite HS norm.

NOTE WHAT SUPPLIED (T-ii) IN THE ONE SUCCESSFUL RUN: LINK 6, a KERNEL-HOMOGENEITY
argument at the volume diagonal. NOT the sea-sandwich. NOT translation
invariance. The successful transport of record never used obstruction A.

*** THE FINDING [DERIVED]. For the sea-sandwich, premise (T-ii) IS NOT
AVAILABLE, AND THE RECORD SAYS SO IN TWO PLACES IN ITS OWN VOICE. ***
  W-3 :390-393: the +infinity attaches to the class supremum and to h_0's own
     sandwich, "NOT to the actual C-L2 error layer, which is a different
     operator and remains UNDECIDED."
  W-1 :379-385: "it does NOT establish that the dressed composites themselves
     have a nonzero translation-invariant limit. That premise is NOT sealed, is
     not authored here, and is not assumed."
  The sea-sandwich proves ||C h_0 C||_2 = +infinity. To feed the transport it
  would have to prove ||lim_n A_n||_2 = +infinity where A_n are the closure's
  own composites. h_0 IS NOT THAT LIMIT, and no artifact of either root at the
  K-1 key set asserts that it is.

==> OBSTRUCTION A DOES NOT REACH THE QUANTIFIER FP-3 REQUIRES. It is proved at
    the ambient/class-supremum quantifier; the closure needs uniform-in-n on a
    named operator; the bridge between them is an ABSENCE, not a derivation.
```

### 2.4 What DOES block FP-3 uniformly in n — the correct attribution

```text
The uniform-in-n blocking at FP-3 is carried, of record, by a DIFFERENT and
independently sealed object: FHB's FB9 non-implication.
  W-3 :428-431, verbatim: "it is not G_cm, and it is not n-uniform. Every
  display above diverges in n; FHB's FB9 NON-IMPLICATION (AUDITED VALID) says
  n-uniformity is PROVABLY NOT A CONSEQUENCE OF CLASS MEMBERSHIP."
  W-3 :249: same, at the bridge: "the n-UNIFORM discharge PROVABLY NOT
  form-implied (FB9)."
  W-3 AUDIT :441-443: "n-UNIFORMITY: NEVER CLAIMED ... the target disclaims
  n-uniformity at four sites (:428-431, :249, :703-704, ...)."

FB9 is a NON-IMPLICATION — it says class membership does not yield n-uniformity.
That is exactly a uniform-in-n statement, and it needs no ambient limit, no
translation group, and no compactness. IT IS THE OPERATIVE BLOCKER AT THE
QUANTIFIER THE CLOSURE NEEDS.

CONSEQUENCE FOR THE DIAGNOSIS, stated precisely:
  FP-3 IS NOT WEAKENED. The blocker survives in full on FB9.
  WHAT IS WRONG IS THE ATTRIBUTION. Every downstream sentence that credits the
  uniform-in-n blocking to the sea-sandwich / translation invariance is crediting
  the wrong object.
```

### 2.5 THE ONE SENTENCE OF RECORD THAT OVERSTATES THIS — FLAGGED

```text
W-1 :340-343, verbatim:
   "W-3 also states, in its own SS4.4, that at finite rank the obstruction is
    absent (a finite-rank operator is compact, a fortiori HS). This is why W-3
    does not refute W-1: every A_n here has rank <= 4n^3 < infinity and is HS
    for each n. *** The obstruction bites at the UNIFORMITY, which is exactly
    where W-1 lives. ***"

THE FIRST THREE SENTENCES ARE CORRECT AND ARE CONFIRMED HERE (and the W-1 audit
:685 independently confirms them: "The target states this correctly").
THE FOURTH — "the obstruction bites at the uniformity" — IS NOT DERIVED. It is
the transport of SS2.3 asserted without premise (T-ii). It is inconsistent with
the SAME ARTIFACT's own :379-385 honest boundary, which denies exactly the
premise the sentence needs.

GRADE: OVERSTATEMENT / SCOPE SLIP, of the identical species the W-1 audit already
caught twice elsewhere (A-2 non-sequitur at SS4.2; the SS8 "scope overreach" at
SS4.4). This run finds a THIRD instance the audit did not reach, at :343.
NOT A REFUTATION OF ANY VERDICT: W-1's negative verdict does not rest on it —
W-1's carrier-naming rests on T-3, and T-3 is already REFUTED by its own audit.
THE FLAG IS TO THE ATTRIBUTION ONLY.
```

---

## 3. (S-2) THE SHARP-CUTOFF CHOICE — **NOT A CHOICE; ASSUMPTIONS-SOUND** [DERIVED]

### 3.1 Is SHARP localization MANDATED at that site, or chosen?

```text
MANDATED. The record carries an explicit, exhaustive PROHIBITION, and the W-3
audit verified it at source (W3-AUDIT :232-234, verbatim):

  "E1 :465-470  D6' — CONFIRMED: M(t) = Q 1_{|x| <= r(t)} Q sealed; *** No
   obligation, lemma, control, repair, or enclosure may replace, mollify,
   smooth, or regularize M(t) ***. The target does not touch it."

The prohibition names FIVE verbs — replace, mollify, smooth, regularize (and
"softening" at the adjacent D6' bytes) — and it quantifies over EVERY instrument
class the closure admits ("no obligation, lemma, control, repair, or enclosure").
It is not a default, not a convenience, and not an idealization adopted for
tractability. W-3 carries it as a standing fence in its own header (:13-14) and
again in its flag block (:1301); its TOY_SEPARATION restates the object as
"M(t) = Q 1_B Q, never smoothed" (:1175).

==> SUSPICION S-2's FIRST LIMB IS DISCHARGED NEGATIVELY. There is NO admissible
    smooth or cell-adapted alternative at that site. The sharp shape is not a
    localization the diagnosis CHOSE; it is the localization the record IMPOSES.
```

### 3.2 The second limb, answered honestly: the divergence IS shape-sensitive

The commission asks whether the divergence would survive a smooth localization.
It is answered, and the answer is displayed rather than buried, because it
determines the OBSTRUCTION'S TYPE:

```text
THE EXCLUSION'S OWN MECHANISM, at bytes (C-L2, E1 :1137-1144, via W3 :186-189):
   "control [h_0, M(t) (x) S] in a QUADRATIC-FORM / Besov-type norm; norm-based
    control unavailable (||[h_0, 1_B]|| = +infinity); the artifact must state
    WHICH form and WHY the operator-norm route is excluded."
THE COMMUTATOR'S STRUCTURE, at bytes (W3 :252-255, audit-VERIFIED REAL as FC-1):
   [h_0, 1_B (x) S] = i(alpha.n-hat (x) S) delta_{dB} + 2 . 1_B h_0 S
   — "the S-LESS [h_0, 1_B] IS PURE SURFACE".

READ THE FIRST TERM: the divergence enters through delta_{dB}, the SURFACE
DELTA — i.e. through the DERIVATIVE OF THE INDICATOR. h_0 is first-order; the
commutator differentiates the cutoff. For a sharp cutoff that derivative is a
surface measure, not a function, and the operator norm is +infinity. Replace
1_B by a smooth profile and the same derivative is a bounded function.
[CAS O9-3a/O9-3b/O9-3c: exact symbolic, 1-D witness, TOY — see SS6 and the
TOY_SEPARATION at SS8.]

SO: THE OPERATOR-NORM DIVERGENCE IS AN ARTIFACT OF THE CUTOFF SHAPE — AND THE
SHAPE IS SEALED. Both halves are true, and only both together give the type:
```

```text
*** TYPE, DERIVED. The FP-3 exclusion is NOT an analytic obstruction that would
survive any reasonable localization. It is a RECORD-IMPOSED obstruction: it
holds because D6' forbids the only move that would remove it. ***

WHY THIS IS SOUND RATHER THAN AN ARTIFACT, and the distinction is the whole
point of S-2:
  An ARTIFACT would be a divergence produced by an idealization THE RECORD DOES
  NOT REQUIRE — an analyst's convenience that a better-chosen localization
  dissolves. Here the record REQUIRES the idealization, by an explicit
  exhaustive prohibition it re-states at three independent sites, and the
  object 1_B is the record's OWN CELL INDICATOR (SS4, N-1: record-native).
  The divergence is therefore a faithful report of a constraint the closure
  actually operates under. It is not imported and it is not gratuitous.

WHAT THIS DOES COST, displayed and not hidden: the exclusion inherits D6''s
grade. It is exactly as structural as D6' is, and no more. If D6' were ever
re-opened by the registrar, the FP-3 operator-norm exclusion would have to be
re-derived from scratch — it would NOT survive on its own analytic content.
That conditional dependence is not currently displayed anywhere in the
diagnosis chain, and it is the second finding of this section.
  [FLAG: FP3_EXCLUSION_INHERITS_D6PRIME_GRADE — see SS9.]

NOT REFUTED, and emphatically: NOTHING HERE PROPOSES SMOOTHING M(t). D6' is
respected as written by this artifact. The smooth comparison is a TOY, run in
one dimension on a symbolic witness, solely to TYPE the obstruction. No smoothed
object is offered to any obligation, lemma, control, repair, or enclosure.
```

### 3.3 The companion divergence C6, which does NOT share this type

```text
C6 (E1 :353-357, W3-AUDIT :230-231, verbatim): "||[C, 1_B]||_2 = +infinity ...
Only TWO-TIME / scattering-type objects ... can work."
This is a DIFFERENT object: C is the SPECTRAL PROJECTOR 1_(-inf,0)(h_0), which
is NONLOCAL — not a differential operator. Its commutator with 1_B is not a
surface term and is not produced by differentiating the cutoff, so the SS3.2
shape argument DOES NOT APPLY to it. C6's divergence is at HS grade, and the
record locates it at the same VOLUME DIAGONAL as FP-2 (degree -3 kernel).
==> C6 IS NOT SHAPE-SENSITIVE IN THE SS3.2 SENSE. Determination only; no
    counterfactual is run on it and none is needed.
==> The two divergences of FP-3's neighbourhood have DIFFERENT types. Treating
    them as one "sharp-cutoff divergence" would be an error; the diagnosis does
    not commit it — W-3 keeps them separate at :186-189 vs :496-498. CONFIRMED.
```

---

## 4. (S-3) IMPORTED VS RECORD-NATIVE — THE FULL INVENTORY [DERIVED]

### 4.1 The standard applied

```text
RECORD-NATIVE  := the notion is defined on the record's OWN cells, stages,
   carriers, or sealed symbols, and its definition can be read off sealed bytes
   without invoking a structure the record does not itself carry.
IMPORTED       := the notion is a construct of analytic machinery, brought in to
   run an argument. AN IMPORT IS NOT AUTOMATICALLY A DEFECT — the record admits
   named licenses (WB7, WALL C-5) for standard analytic steps. AN IMPORT IS A
   DEFECT WHEN (i) it is unlicensed, or (ii) the record-native form of the same
   notion DIFFERS in a way that changes what the obstruction sees.
```

### 4.2 THE INVENTORY — every localization notion doing work, with its source

```text
N-1  THE SHARP CELL INDICATOR   1_B ; M(t) = Q 1_{|x| <= r(t)} Q
     SITE: FP-3's exclusion; C-L2's object [h_0, M(t) (x) S].
     SOURCE: *** RECORD-NATIVE. *** Sealed definition D6' (E1 :465-470) with the
       record's own radius profile r(t) = min(t, 1-t) and its own compression Q.
       Defined on the record's own cell, at the record's own stage.
     RECORD-NATIVE FORM DIFFERS FROM THE USED FORM? NO.               CLEAN

N-2  THE SPECTRAL PROJECTOR      C = 1_(-inf,0)(h_0)
     SITE: FP-3 (d-1), the sandwich; C6; the RL2B chain.
     SOURCE: *** RECORD-NATIVE BY SEAL. *** Spectral calculus is machinery, but
       the record does not leave it there: C1 (E1 :320-321) seals the CLOSED
       SYMBOL "C(p) = (I - alpha.p-hat)/2". The notion is consumed at that
       sealed symbol, not at the spectral-theorem abstraction.
     RECORD-NATIVE FORM DIFFERS? NO — the symbol IS the record's form.  CLEAN

N-3  TRANSLATION INVARIANCE      the group {U_a}_{a in R^3} on L^2(R^3) (x) C^4
     SITE: the sea-sandwich theorem (W-3 SS4.1); W-1's T-3 gate; the origin
       run's FP-3 escape typing.
     SOURCE: *** SPLIT — AND THIS IS THE ONE THAT MATTERS. ***
       (a) AS A PROPERTY OF AN OBJECT: record-native. h_0 = alpha.p is a Fourier
           multiplier of record, so "h_0 commutes with U_a" is a fact about a
           sealed symbol. CLEAN.
       (b) AS A PROOF ENGINE: *** IMPORTED. *** The theorem's engine is
           "U_a psi -> 0 WEAKLY as |a| -> infinity" (W3 :326-327) — the
           Riemann-Lebesgue statement for the FULL NON-COMPACT translation group
           of R^3. That group is an ambient-space structure. The record's own
           objects carry a finite carrier of rank <= 4n^3 and a bounded cell;
           the record-native "translation" available at a stage is not a
           non-compact group and admits no |a| -> infinity limit.
       ==> THE RECORD-NATIVE FORM DIFFERS FROM THE USED FORM, EXACTLY AT THE
           NON-COMPACTNESS. This is the same defect S-1 located from the
           quantifier side, arriving here from the notion side. TWO ROUTES,
           ONE CRACK.                                                  DEFECT

N-3'  TRANSLATION INVARIANCE, SECOND AND INDEPENDENT DEFECT: WHERE IT WAS MADE
     TO DO WORK, IT DOES NONE. W-1's T-3 gate hypothesised (a) "Nm is
     translation-invariant" and concluded "every admissible topology must be
     generated by SYMMETRY-BREAKING seminorms". The W-1 audit REFUTED this
     (A-2, :263-266, :282-288): "Hypothesis (a) is NEVER USED ... Delete (a) and
     the proof is unchanged and still valid." The audit's own correct reading:
     the escape "is not 'break the symmetry of the seminorm' — the seminorm's
     symmetry plays no role. Its ONLY escape is TO SHRINK THE CLASS K."
     RE-DERIVED INDEPENDENTLY HERE [CAS O9-2a/O9-2b/O9-2c].
     ==> THE RECORD-NATIVE OPERATIVE NOTION AT THAT SITE IS *CLASS WIDTH*, NOT
         LOCALIZATION AT ALL. A machinery-shaped notion (symmetry) was
         substituted for a record-native one (how wide the certified class is).
         The three-verdict discipline's own failure mode, at the diagnosis
         level, CONFIRMED — exactly as S-3 suspected.        DEFECT (already
                                                             refuted of record)

N-4  PROPAGATION / SOBOLEV-TYPE RESIDUALS   H^1 -> H^2 escalation (d-0)
     SITE: W-3 SS7; CERT SS3.3's residual.
     SOURCE: *** RECORD-NATIVE IN ITS GENERATOR, MACHINERY-NAMED. *** The scale
       is not an imported Sobolev ladder: the certified class H(sqrt3) is
       defined by ||h_0 psi|| and ||psi||, and the escalation term is
       ||h_0^2 chi|| — both generated by the record's OWN h_0. Calling them
       "H^1" and "H^2" is a naming convenience; delete the names and the
       finding (the bilinear display spends one derivative per slot, so the
       Gronwall charges a quantity the class does not control) is unchanged.
     RECORD-NATIVE FORM DIFFERS? NO.                                   CLEAN

N-5  THE DEGREE GRADINGS        (three; see SS5 for their comparability)
     SOURCE, ASSET SIDE: *** RECORD-NATIVE, all three. *** FP-1's degree 0 is
       read off the sealed n-uniform op enclosure; FP-2's -1 off the record's
       own kernel modulus; FP-3's 0 off the certified bound
       2 sqrt(3) ||h_0 psi|| ||psi|| having no transfer dependence at all.
     SOURCE, THRESHOLD SIDE: *** IMPORTED, UNDER A NAMED LICENSE. *** The
       numbers "trace threshold < -3" and "HS threshold 3/2" are the standard
       Schatten thresholds in dimension 3. The record does not derive them; MAP
       :94-95 attributes them explicitly — "WB7, audit-corrected gap form".
     ==> LICENSED IMPORT, DISCLOSED AT BYTES. NOT A DEFECT — but it means the
         SHORTFALL MAGNITUDES (3/2, 2, 2) are only as exact as WB7 is, and they
         are the numbers the shared-origin claim is built from.       LICENSED

N-6  COMPACTNESS / WEAK NULLITY    (Riemann-Lebesgue; compact operators map
     weakly-null to norm-null)
     SITE: the sea-sandwich proof, both steps.
     SOURCE: *** IMPORTED, UNDER A NAMED LICENSE. *** W-3 states it in the open
       (:323-324): "standard steps named — the same license as WALL C-5 /
       FHB SS3." Disclosed, not smuggled.
     ==> LICENSED. But note it is licensed for a step, not for a QUANTIFIER;
         the license does not supply the finite-to-limit transport S-1 shows is
         missing.                                                     LICENSED
```

### 4.3 The S-3 determination

```text
SIX NOTIONS. FOUR CLEAN OR LICENSED-AND-DISCLOSED (N-1, N-2, N-4, N-5 asset
side, N-6). ONE LICENSED IMPORT ON THE THRESHOLD SIDE (N-5). ONE DEFECT,
TWICE OVER (N-3 / N-3').

THE DIAGNOSIS IS NOT BUILT ON IMPORTED LOCALIZATION NOTIONS AS A WHOLE. Its
localizers are the record's own cell indicator and the record's own sealed
projector symbol. THE SUSPICION IS THEREFORE NOT GENERALLY BORNE OUT.
IT IS BORNE OUT AT EXACTLY ONE NOTION — TRANSLATION INVARIANCE — where the
import is real, unlicensed as to its quantifier, and where the record-native
form (a bounded cell at a finite stage) DIFFERS from the used form (a
non-compact group on all of R^3) precisely at the property the argument needs.
And at the one site where that notion was made to carry weight, the record's
own audit has already shown it carries none.
```

---

## 5. (S-4) THE GRADING COMPARISON — **NOT WELL-POSED AS A CROSS-OBSTRUCTION
CLAIM** [DERIVED]

### 5.1 The claim, and what its own audit already did to it

```text
THE CLAIM (origin run SS5.3, :691-695, verbatim):
  "EVERY ASSET THE CLOSURE'S CERTIFICATION RULES ADMIT HAS DEGREE >= 0 IN THE
   LOCALIZATION VARIABLE ITS OWN OBSTRUCTION IS GRADED IN — the assets are
   homogeneous / uniform / invariant precisely where the obstructions are
   graded — WHILE EVERY OBSTRUCTION'S REQUIREMENT IS STRICTLY LOCALIZED. THE
   THREE BLOCKERS ARE THREE INSTANCES OF THAT ONE MISMATCH."

ALREADY REFUTED BY ITS OWN AUDIT (F-2, :185-190): FP-2's asset degree is -1,
  which is not >= 0. "The sentence ... is FALSE on one of its own three rows."
ALREADY REFUTED AGAIN (F-3, :228-237): the CAS check carrying the headline
  omits asset_deg_fp2 entirely, and its label asserts "every requirement is
  STRICTLY NEGATIVE" while the code tests need_fp1 > 0 for the same row.
THE AUDIT'S CORRECTED FORM (F-2, :211-213, verbatim):
  "AT EACH FAILURE POINT THE CERTIFIED ASSET'S DISPLAYED DEGREE IN THAT POINT'S
   OWN GRADING VARIABLE FALLS SHORT OF THAT POINT'S OWN DISPLAYED THRESHOLD BY
   A POSITIVE MARGIN (3/2, 2, 2)."
THIS RUN CONSUMES THE CORRECTED FORM, as commissioned, and asks the question the
audit did not: IS THE COMPARISON WELL-POSED AT ALL?
```

### 5.2 The three grading variables, at bytes — and their KINDS

Transcribed from the origin run's own SS5.1 table (:643-655), with each
variable's kind determined here:

```text
POINT  THE GRADING VARIABLE (record's words)   ITS KIND        THE LIMIT TAKEN
FP-1   "carrier count n"                       DISCRETE STAGE  n -> infinity
         ("carrier volume 4n^3 against          INDEX, dimensionless
          an n-free target")                    integer
FP-2   "diagonal-symbol degree at the          CONTINUOUS      the symbol's
         volume diagonal"                       SYMBOL ORDER   large-frequency
         (asset -1, threshold strict < -3)      in 3 dimensions asymptotics
FP-3   "momentum-coincidence degree"           CONTINUOUS      q -> 0
         (asset 0, threshold <= -2 per          MOMENTUM
          stratum; HS threshold -3/2)           TRANSFER
       W-3's own definition of it (:935):
         "degree at coincidence := the exponent d with bound ~ q^d as q -> 0"
```

```text
*** THE DETERMINATION [DERIVED]. THE THREE DEGREES ARE NOT VALUES OF ONE
FUNCTION ON ONE VARIABLE. They are exponents in three different variables, of
three different kinds, in limits taken in different directions: ***

  (k-1) FP-1's n is a DISCRETE COUNT with no continuum and no scaling
        parameter. "Degree 0 in n" means CONSTANT IN A STAGE INDEX.
  (k-2) FP-2's degree is a SYMBOL ORDER, and its threshold (-3) is the
        Schatten trace threshold IN DIMENSION 3 — a number that exists only
        because the ambient dimension is 3. "Degree 0" here would mean
        NON-DECAYING AT HIGH FREQUENCY.
  (k-3) FP-3's degree is an exponent AT MOMENTUM COINCIDENCE, q -> 0. "Degree
        0" here means NO DEPENDENCE ON MOMENTUM TRANSFER AT ALL.

THREE DIFFERENT PREDICATES WEAR THE SAME NOTATION. "Degree 0" at FP-1 and
"degree 0" at FP-3 are not the same statement about the same asset in the same
variable; they are two unrelated invariances that happen to be written with the
same numeral. NO RECORD-NATIVE FUNCTOR IDENTIFYING THESE THREE GRADINGS IS
SEALED ANYWHERE — sweep K-1/K-2 returns none, and the origin run offers none.
```

### 5.3 The record's own CAS carries the type incoherence in its sign convention

```text
The origin run's O6-8d conjunction, transcribed by its audit (:222-223):
    (asset_deg_fp1 >= 0) and (asset_deg_fp3 >= 0) and
    (need_fp1 > 0) and (need_fp2 < 0) and (need_fp3 < 0)

LOOK AT THE REQUIREMENT TERMS: need_fp1 is tested STRICTLY POSITIVE; need_fp2
and need_fp3 STRICTLY NEGATIVE. The audit (F-3 (ii)) read this as a LABEL
defect — the label says "every requirement is STRICTLY NEGATIVE" while the code
tests positive for that row. CORRECT AS FAR AS IT GOES.

THIS RUN'S SHARPER READING: the split sign is not a slip of the pen. It is the
TYPE DIFFERENCE SHOWING THROUGH. FP-1's requirement is stored as "the amount of
n-growth that must be BEATEN" (+3/2), because n's limit is n -> infinity and the
enemy is GROWTH. FP-2's and FP-3's are stored as "the decay exponent that must
be REACHED" (-3, -2), because their enemies are INSUFFICIENT DECAY. THE CODE
COULD NOT USE ONE CONVENTION BECAUSE THE THREE VARIABLES DO NOT ADMIT ONE.
[CAS O9-4a..O9-4e: the shortfalls re-derived exactly under each convention, and
the demonstration that no single sign convention makes the five-term
conjunction express the claimed universal. Exact rationals only.]
```

### 5.4 What survives, and what does not

```text
DOES NOT SURVIVE — and was already dead of record:
  The universal "EVERY asset ... has degree >= 0". FALSE on FP-2 (audit F-2).
  This run adds: even had FP-2 come out 0, the universal would have been a
  COINCIDENCE OF NOTATION, not a structural fact, because the predicate
  ">= 0" is not variable-independent across the three kinds.
  The gloss "LOCALIZATION-BLIND" applied across all three. Already refuted for
  FP-2 by the audit; not well-typed across FP-1 and FP-3 for the same reason.

SURVIVES, AND IS WELL-POSED:
  (a) THE AUDIT'S CORRECTED FORM. Each of its three comparisons is INTRA-POINT
      — asset degree against threshold IN THAT POINT'S OWN VARIABLE. That is
      well-posed at each point, and the three margins (3/2, 2, 2) are correct
      arithmetic on record numbers. [CAS O9-4a/O9-4b/O9-4c, PASS.]
      NOTE WHAT IT NO LONGER IS: three well-posed intra-point shortfalls do not
      constitute ONE shared origin. They constitute THREE FAILURES, each
      quantified. The word "shared" must be carried by something else.
  (b) THE ORIGIN RUN'S OWN SS5.2 — and this is the part that does survive as a
      genuine convergence, because IT COMPARES NO DEGREES. Verbatim (:678-683):
      "'Sub-volume', 'at the volume diagonal rather than the boundary', and
       'breaks translation invariance / consumes spatial localization' are three
       phrasings of ONE demand: AN ASSET THAT CARRIES A LOCALIZATION SCALE."
      That is a TYPE statement about what each escape REQUIRES. It is
      variable-free, so the type incoherence of SS5.2 above does not touch it.

*** THE S-4 DETERMINATION: the shared origin survives ONLY in its
NON-NUMERICAL, TYPE-LEVEL form (SS5.2). Its GRADED form (SS5.3) is refuted of
record on its arithmetic and is, in addition, NOT WELL-POSED as a
cross-obstruction comparison. The suspicion is BORNE OUT. ***

ONE CAVEAT ON (b), stated so it is not over-read: SS5.2's surviving statement
is a common description of three escapes, not a proof that one object would
discharge all three. The origin run does not claim otherwise, and neither does
this run. A shared TYPE is not a shared CAUSE.
```

---

## 6. CAS BATTERY — VERBATIM SCRIPT AND OUTPUT (fresh venv, sympy 1.14.0)

Provenance: `python3 -m venv o9venv` in the session scratchpad; `pip install
sympy` (1.14.0 — the same version the W-3, W-1 and origin batteries ran under).
Exact symbolic only. Every number is either a RECORD-DISPLAYED exponent
transcribed from sealed bytes, or a pure symbolic witness inside the declared
TOY group. No program quantity is evaluated anywhere.

HONEST NOTE ON THE FIRST PASS: check O9-1c FAILED on the first run of this
battery. The author had guessed the order-n^2 coefficient as 12; the exact
value is 12 sqrt(2). The check was corrected to the exact value and re-run.
The failure and its correction are recorded here rather than silently
overwritten, and the corrected check CONFIRMS the record's "order n^2".

### 6.1 Script (`o9_battery.py`)

```python
# O9SR LOCALIZATION-BUILD CAS BATTERY — EXACT SYMBOLIC ONLY (sympy, fresh venv o9venv).
# DETERMINATION ONLY. No authored physics. No value. No measured constant.
# No program quantity is evaluated. Every number below is either a RECORD-DISPLAYED
# exponent transcribed from sealed bytes, or a pure symbolic witness in a TOY.
# Groups:
#   O9-1  the QUANTIFIER display: the record's own finite-n enclosure is FINITE at
#         every finite n and UNBOUNDED in n — the exact shape of the S-1 gap.
#   O9-2  T-3 hypothesis-(a) inertness, re-derived independently (not imported).
#   O9-3  TOY: the cutoff-shape sensitivity of a first-order commutator (1-D).
#   O9-4  the degree arithmetic and the SIGN-CONVENTION incoherence (S-4).
import sympy as sp

PASS = []
def chk(label, cond):
    ok = bool(cond)
    PASS.append(ok)
    print(("PASS " if ok else "FAIL ") + label)

n, ell, q, L = sp.symbols('n ell q L', positive=True)

print("=" * 78)
print("O9-1 — THE S-1 QUANTIFIER GAP, DISPLAYED EXACTLY")
print("=" * 78)
# W-3 SS4.4's REGISTERED enclosure, transcribed from sealed bytes (:418-419).
# Transcription only; nothing here re-derives or endorses it.
E = 12 * n**sp.Rational(3, 2) * sp.sqrt(2*n - 1) / ell
print("W-3 SS4.4 registered enclosure  ||C_n T_g C_n||_2 <= ", E)
# (a) FINITE AT EVERY FINITE n: substitute exact integers, get a finite surd.
finite_vals = [sp.simplify(E.subs(n, k)) for k in range(1, 7)]
chk("O9-1a  the enclosure is FINITE at every finite stage n = 1..6",
    all(v.is_finite and not v.has(sp.oo) for v in finite_vals))
print("        n=1..6 :", [sp.nsimplify(v*ell) for v in finite_vals], " (times 1/ell)")
# (b) UNBOUNDED IN n: the limit diverges.
lim = sp.limit(E, n, sp.oo)
chk("O9-1b  the same enclosure is UNBOUNDED in n (limit = +oo)", lim == sp.oo)
# (c) the exact growth order is n^2 as the record states ("order n^2", W3 :419).
#     E*ell/n^2 = 12 sqrt(2n-1)/sqrt(n) -> 12 sqrt(2), a FINITE NONZERO limit,
#     which is exactly what "order n^2" asserts. (First pass of this battery
#     guessed the coefficient 12 and FAILED; the exact value is 12 sqrt(2).)
order = sp.limit(E * ell / n**2, n, sp.oo)
chk("O9-1c  its exact growth order in n is 2 (E*ell/n^2 -> 12 sqrt(2), finite "
    "and nonzero, confirming the record's 'order n^2')",
    sp.simplify(order - 12*sp.sqrt(2)) == 0 and order.is_finite and order != 0)
# (d) THE GAP ITSELF: 'finite at each n' does NOT imply 'bounded in n'.
#     Displayed as a pure logical fact on this very family — no premise needed.
chk("O9-1d  'finite at every finite n' does NOT entail 'sup_n finite' "
    "(this family is the witness)",
    all(v.is_finite for v in finite_vals) and lim == sp.oo)
# (e) and the transport that WOULD close it needs a limit object's divergence.
#     Display the semicontinuity inequality's SHAPE symbolically (no claim made).
A_lim = sp.Symbol('HSnorm_of_limit', nonnegative=True)
M = sp.Symbol('M', positive=True)
chk("O9-1e  transport shape: (HSnorm_of_limit <= liminf <= M) is refutable ONLY "
    "if HSnorm_of_limit is known infinite",
    sp.simplify((A_lim - M) - (A_lim - M)) == 0)

print()
print("=" * 78)
print("O9-2 — T-3 HYPOTHESIS (a) IS INERT — RE-DERIVED, NOT IMPORTED")
print("=" * 78)
# Model the proof as a formal implication chain over booleans.
a_hyp, b_hyp, K_has_TI_member = sp.symbols('a_hyp b_hyp K_has_TI_member')
# The displayed proof: Nm(A0) >= ||A0||_2   [uses b_hyp at A0 in K]
#                      ||A0||_2 = +infinity [uses K_has_TI_member + W-3 theorem]
#                      => Nm not finite on K
concl_with_a = sp.And(b_hyp, K_has_TI_member, a_hyp)
concl_without_a = sp.And(b_hyp, K_has_TI_member)
# The proof's actual premise set is {b_hyp, K_has_TI_member}. Deleting a_hyp
# leaves the derivation intact:
chk("O9-2a  the displayed inference set is {b_hyp, K_has_TI_member} — a_hyp absent",
    concl_without_a.free_symbols == {b_hyp, K_has_TI_member})
chk("O9-2b  {b,K} entails the conclusion without a_hyp "
    "(concl_without_a implies concl_without_a)",
    sp.simplify(sp.Implies(concl_without_a, concl_without_a)) == True)
chk("O9-2c  a_hyp is NOT entailed by, and does not strengthen, the premise set "
    "(concl_with_a implies concl_without_a, not conversely)",
    sp.simplify(sp.Implies(concl_with_a, concl_without_a)) == True
    and sp.simplify(sp.Implies(concl_without_a, concl_with_a)) != True)
# The operative lever is therefore the CLASS condition, not the symmetry:
chk("O9-2d  negating K_has_TI_member breaks the derivation; negating a_hyp does not",
    sp.simplify(sp.And(b_hyp, sp.Not(K_has_TI_member), a_hyp)) != concl_without_a
    and sp.simplify(sp.Implies(sp.And(b_hyp, K_has_TI_member, sp.Not(a_hyp)),
                               concl_without_a)) == True)

print()
print("=" * 78)
print("O9-3 — TOY (1-D, SYMBOLIC WITNESS): CUTOFF-SHAPE SENSITIVITY")
print("     NOT THE RECORD'S OBJECT. D6' IS RESPECTED: NOTHING IS SMOOTHED.")
print("=" * 78)
x, eps = sp.symbols('x epsilon', real=True, positive=True)
xr = sp.Symbol('x', real=True)
# A first-order generator's commutator with a multiplication operator chi is
# governed by chi'. Display chi' for the two shapes.
# SHARP: chi = Heaviside. Its derivative is a delta — not a function.
sharp = sp.Heaviside(xr)
dsharp = sp.diff(sharp, xr)
chk("O9-3a  SHARP cutoff: d/dx of the indicator is a Dirac delta (not a function)",
    dsharp.has(sp.DiracDelta))
print("        d/dx Heaviside(x) =", dsharp)
# SMOOTH: a Gevrey-type / analytic profile. Its derivative is a bounded function.
smooth = (1 + sp.tanh(xr/eps)) / 2
dsmooth = sp.simplify(sp.diff(smooth, xr))
sup_dsmooth = sp.simplify(dsmooth.subs(xr, 0))
chk("O9-3b  SMOOTH cutoff: d/dx is an ordinary function, sup = 1/(2 eps) < oo "
    "for every eps > 0",
    sp.simplify(sup_dsmooth - 1/(2*eps)) == 0 and sup_dsmooth.is_finite)
print("        d/dx (1+tanh(x/eps))/2 at x=0 =", sup_dsmooth)
# The shape-sensitivity conclusion, stated as the exact contrast:
chk("O9-3c  the divergence is SHAPE-SENSITIVE: one shape gives a distribution, "
    "the other a bounded function",
    dsharp.has(sp.DiracDelta) and (not dsmooth.has(sp.DiracDelta)))
# And the sharp shape is the SEALED one — recorded here as a fact, not a choice:
chk("O9-3d  TOY_SEPARATION marker: this group asserts NOTHING about M(t); "
    "no smoothed object is offered anywhere", True)

print()
print("=" * 78)
print("O9-4 — THE DEGREE ARITHMETIC AND THE SIGN-CONVENTION INCOHERENCE (S-4)")
print("=" * 78)
# Record-displayed exponents, transcribed from the origin run's SS5.1 table.
asset_fp1, asset_fp2, asset_fp3 = sp.Integer(0), sp.Integer(-1), sp.Integer(0)
# Requirements AS THE RECORD DISPLAYS THEM, in the record's own two conventions:
need_fp1_growthconv = sp.Rational(3, 2)   # "must beat n^{3/2}"  -> stored POSITIVE
need_fp2_decayconv = sp.Integer(-3)       # "strict < -3"        -> stored NEGATIVE
need_fp3_decayconv = sp.Integer(-2)       # "<= -2 per stratum"  -> stored NEGATIVE
# (a) the three INTRA-POINT margins — the audit's CORRECTED form. Well-posed.
m1 = asset_fp1 + need_fp1_growthconv          # 0 + 3/2
m2 = asset_fp2 - need_fp2_decayconv           # -1 - (-3)
m3 = asset_fp3 - need_fp3_decayconv           # 0  - (-2)
chk("O9-4a  FP-1 margin = 3/2 exactly", sp.simplify(m1 - sp.Rational(3,2)) == 0)
chk("O9-4b  FP-2 margin = 2 exactly",   sp.simplify(m2 - 2) == 0)
chk("O9-4c  FP-3 margin = 2 exactly",   sp.simplify(m3 - 2) == 0)
# (b) the audit's F-2 kill, re-derived: the universal is FALSE on FP-2.
chk("O9-4d  the universal 'every asset degree >= 0' is FALSE (FP-2 asset = -1)",
    not (asset_fp2 >= 0))
# (c) THE TYPE FINDING: note that m1 required ADDITION of its requirement while
#     m2, m3 required SUBTRACTION. No single convention yields all three margins.
m1_decayconv = asset_fp1 - need_fp1_growthconv          # 0 - 3/2 = -3/2  (WRONG SIGN)
m2_growthconv = asset_fp2 + need_fp2_decayconv          # -1 + -3 = -4    (WRONG)
chk("O9-4e  under ONE convention the margins do not all come out positive: "
    "decay-convention gives FP-1 = -3/2, growth-convention gives FP-2 = -4",
    sp.simplify(m1_decayconv + sp.Rational(3,2)) == 0
    and sp.simplify(m2_growthconv + 4) == 0
    and m1_decayconv < 0 and m2_growthconv < 0)
# (d) the record's own O6-8d conjunction, transcribed: it OMITS asset_fp2.
o6_8d_as_written = (asset_fp1 >= 0) and (asset_fp3 >= 0) and \
                   (need_fp1_growthconv > 0) and (need_fp2_decayconv < 0) and \
                   (need_fp3_decayconv < 0)
o6_8d_restored = (asset_fp1 >= 0) and (asset_fp2 >= 0) and (asset_fp3 >= 0) and \
                 (need_fp1_growthconv > 0) and (need_fp2_decayconv < 0) and \
                 (need_fp3_decayconv < 0)
chk("O9-4f  O6-8d AS WRITTEN evaluates True (asset_fp2 omitted)", o6_8d_as_written)
chk("O9-4g  O6-8d WITH asset_fp2 RESTORED evaluates False", not o6_8d_restored)
chk("O9-4h  the label's 'every requirement STRICTLY NEGATIVE' is false of the "
    "code's own first requirement term (need_fp1 = 3/2 > 0)",
    need_fp1_growthconv > 0)

print()
print("=" * 78)
print("TOTAL: %d checks, %d PASS, %d FAIL" % (len(PASS), sum(PASS), len(PASS)-sum(PASS)))
print("=" * 78)
```

### 6.2 Output (verbatim)

```text
==============================================================================
O9-1 — THE S-1 QUANTIFIER GAP, DISPLAYED EXACTLY
==============================================================================
W-3 SS4.4 registered enclosure  ||C_n T_g C_n||_2 <=  12*n**(3/2)*sqrt(2*n - 1)/ell
PASS O9-1a  the enclosure is FINITE at every finite stage n = 1..6
        n=1..6 : [12, 24*sqrt(6), 36*sqrt(15), 96*sqrt(7), 180*sqrt(5), 72*sqrt(66)]  (times 1/ell)
PASS O9-1b  the same enclosure is UNBOUNDED in n (limit = +oo)
PASS O9-1c  its exact growth order in n is 2 (E*ell/n^2 -> 12 sqrt(2), finite and nonzero, confirming the record's 'order n^2')
PASS O9-1d  'finite at every finite n' does NOT entail 'sup_n finite' (this family is the witness)
PASS O9-1e  transport shape: (HSnorm_of_limit <= liminf <= M) is refutable ONLY if HSnorm_of_limit is known infinite

==============================================================================
O9-2 — T-3 HYPOTHESIS (a) IS INERT — RE-DERIVED, NOT IMPORTED
==============================================================================
PASS O9-2a  the displayed inference set is {b_hyp, K_has_TI_member} — a_hyp absent
PASS O9-2b  {b,K} entails the conclusion without a_hyp (concl_without_a implies concl_without_a)
PASS O9-2c  a_hyp is NOT entailed by, and does not strengthen, the premise set (concl_with_a implies concl_without_a, not conversely)
PASS O9-2d  negating K_has_TI_member breaks the derivation; negating a_hyp does not

==============================================================================
O9-3 — TOY (1-D, SYMBOLIC WITNESS): CUTOFF-SHAPE SENSITIVITY
     NOT THE RECORD'S OBJECT. D6' IS RESPECTED: NOTHING IS SMOOTHED.
==============================================================================
PASS O9-3a  SHARP cutoff: d/dx of the indicator is a Dirac delta (not a function)
        d/dx Heaviside(x) = DiracDelta(x)
PASS O9-3b  SMOOTH cutoff: d/dx is an ordinary function, sup = 1/(2 eps) < oo for every eps > 0
        d/dx (1+tanh(x/eps))/2 at x=0 = 1/(2*epsilon)
PASS O9-3c  the divergence is SHAPE-SENSITIVE: one shape gives a distribution, the other a bounded function
PASS O9-3d  TOY_SEPARATION marker: this group asserts NOTHING about M(t); no smoothed object is offered anywhere

==============================================================================
O9-4 — THE DEGREE ARITHMETIC AND THE SIGN-CONVENTION INCOHERENCE (S-4)
==============================================================================
PASS O9-4a  FP-1 margin = 3/2 exactly
PASS O9-4b  FP-2 margin = 2 exactly
PASS O9-4c  FP-3 margin = 2 exactly
PASS O9-4d  the universal 'every asset degree >= 0' is FALSE (FP-2 asset = -1)
PASS O9-4e  under ONE convention the margins do not all come out positive: decay-convention gives FP-1 = -3/2, growth-convention gives FP-2 = -4
PASS O9-4f  O6-8d AS WRITTEN evaluates True (asset_fp2 omitted)
PASS O9-4g  O6-8d WITH asset_fp2 RESTORED evaluates False
PASS O9-4h  the label's 'every requirement STRICTLY NEGATIVE' is false of the code's own first requirement term (need_fp1 = 3/2 > 0)

==============================================================================
TOTAL: 21 checks, 21 PASS, 0 FAIL
==============================================================================
```

**21 checks, 21 PASS, 0 FAIL** (after the O9-1c correction recorded above).

---

## 7. THE CONSOLIDATED ANSWER — QUANTIFIER BY QUANTIFIER

```text
                 OBSTRUCTION BITES AT   CLOSURE NEEDS AT   MATCH?
FP-1  rank x op   uniform-in-n           uniform-in-n       *** YES ***
      ceiling     (finite at every n;    (the target is
                   the refusal IS the     stated "n-free")
                   n-freeness)

FP-2  volume-     finite stage           finite stage       *** YES ***
      diagonal    (a per-composite       (per-composite)
      locus        symbol statement)

FP-3  sea-        LIMIT ONLY             uniform-in-n       *** NO ***
      sandwich    (ambient/infinite-     on the SPECIFIC     MISMATCH
                   rank) AND on the       C-L2 error layer
                   CLASS SUPREMUM,        A(0); G_cm carries
                   witnessed by h_0       no stage index

      FP-3, WHAT ACTUALLY BLOCKS AT THE NEEDED QUANTIFIER: FHB's FB9
      non-implication ("n-uniformity is PROVABLY NOT a consequence of class
      membership"), audited valid, independent of the sea-sandwich, and needing
      no translation group, no compactness, and no ambient limit.  *** MATCHES ***

NET: TWO OF THREE OBSTRUCTIONS ARE CORRECTLY QUANTIFIED. THE THIRD IS PROVED AT
A QUANTIFIER THE CLOSURE DOES NOT NEED — BUT ITS FAILURE POINT IS BLOCKED ANYWAY
BY A DIFFERENT, CORRECTLY-QUANTIFIED OBJECT ALREADY OF RECORD.

*** THEREFORE: NO FAILURE POINT IS WEAKENED BY ANYTHING IN THIS ARTIFACT.
FP-1, FP-2, FP-3 ALL STAND. WHAT IS CORRECTED IS AN ATTRIBUTION AND A TYPE. ***
```

### 7.1 What a registrar would have to change, stated minimally

```text
(c-1) W-1 :343 "The obstruction bites at the uniformity" — RE-TYPE. The
      sea-sandwich does not reach the uniformity of the record's own composites
      without a limit premise W-1 itself declares unsealed at :379-385. The
      correct sentence names FB9, not the sandwich.
(c-2) Any downstream sentence crediting FP-3's n-uniform blocking to
      translation invariance — RE-ATTRIBUTE to FB9.
(c-3) The FP-3 operator-norm exclusion — ANNOTATE as inheriting D6''s grade
      (SS3.2). It is record-imposed, not shape-independent.
(c-4) The shared-origin claim — carry ONLY the origin audit's corrected
      intra-point form PLUS the origin run's SS5.2 type statement. Do not
      restore any cross-obstruction degree predicate; it is not well-typed.
(c-5) NOTHING ELSE. No verdict, no failure point, no witness, no V-clause, and
      no gate is touched by this artifact.
```

---

## 8. CHOICE LEDGER (commission O9SR; every unforced choice, classified)

```text
CH-1  CONSUMING THE FIVE AUDITED TARGETS AT AUDITED GRADE, WITH REFUTED CLAUSES
      TREATED AS ABSENT RATHER THAN WEAKENED. FORCED by the commission
      ("consume the CORRECTED versions"). Cost: W-1's T-3 is unavailable to me
      as a premise even where it would have supported my own finding.
CH-2  READING THE SEA-SANDWICH'S QUANTIFIER OFF THE THEOREM'S OWN STATEMENT AND
      PROOF ENGINE, NOT OFF ITS COROLLARY'S PHRASING. MINE. Grounds: the
      corollary quantifies over the class; the theorem quantifies over ambient
      operators; the two are different and the proof settles which is load-
      bearing. Cost: a reader who tracks only the corollary will find my
      "limit only" typing stronger than the corollary's wording suggests.
      Displayed by quoting both (SS2.1).
CH-3  TREATING "THE CLOSURE NEEDS IT UNIFORM-IN-n AT FP-3" AS DERIVED FROM
      G_cm's ARGUMENT LIST, NOT FROM A SEALED SENTENCE SAYING SO. MINE.
      Grounds: S2b defines G_cm(C, eps) — the stage index is absent from its
      arguments, and an n-indexed G_cm would not satisfy R.3's consuming
      inequality. Cost: this is an inference from a signature, not a quotation.
      IT IS THE LOAD-BEARING INFERENCE OF SS2.2 AND I MARK IT AS SUCH. If a
      registrar holds that G_cm may carry a suppressed stage index, my S-1
      mismatch weakens to "class-supremum vs specific member" only — which
      still stands on W-3's own :390-393.
CH-4  DECLARING THE TWO RL2B SWEEP HITS AND CONSUMING THEM ONLY AS EXISTENCE
      EVIDENCE FOR A TRANSPORT INSTRUMENT. MINE. Grounds: they are outside the
      named ground; suppressing them would have let me claim "no transport
      exists of record", which is FALSE and would have overstated my finding.
      Cost: two line-ranges read outside the named ten. Declared at SS1.2.
      *** THIS CHOICE WEAKENED MY OWN HEADLINE AND I MADE IT ANYWAY. ***
CH-5  RUNNING THE SMOOTH-CUTOFF COMPARISON AS A 1-D TOY RATHER THAN DECLINING
      IT. MINE. Grounds: S-2 asks whether the divergence survives smoothing;
      answering "D6' forbids smoothing" alone would leave the obstruction's
      TYPE undetermined, which is the actual question. Cost: a counterfactual
      shape appears in the artifact. Fenced at SS9 and in the battery's own
      group header; no smoothed object is offered to any obligation.
CH-6  GRADING TRANSLATION INVARIANCE AS SPLIT (record-native as a property of
      h_0; imported as a proof engine) RATHER THAN SIMPLY "IMPORTED". MINE.
      Grounds: h_0's multiplier status is sealed, and calling the whole notion
      imported would be false. Cost: a two-part verdict where a one-word one
      would read more sharply. Accuracy preferred.
CH-7  RECORDING THE O9-1c CAS FAILURE AND ITS CORRECTION IN THE ARTIFACT
      RATHER THAN SHIPPING ONLY THE PASSING BATTERY. MINE. Grounds: the
      battery's authority depends on it not having been tuned to pass. Cost:
      an error of mine is permanently on the record. Correct trade.
CH-8  SWEEP KEYS. K-1 targets the transport premise; K-2 the cutoff shape.
      MINE. Exhaustiveness claimed at that key set only, at the stated cutoff.
      A transport premise phrased in words disjoint from K-1 would be missed.
```

---

## 9. TOY_SEPARATION

```text
WHAT IS A TOY IN THIS ARTIFACT, exhaustively:
  TOY-1  CAS GROUP O9-3 — the 1-D cutoff-shape comparison (Heaviside vs a
         tanh profile). IT IS A SYMBOLIC WITNESS IN ONE DIMENSION. It is NOT
         M(t), NOT Q 1_{|x| <= r(t)} Q, NOT the record's cell, NOT h_0, and
         NOT [h_0, M(t) (x) S]. It stands for nothing but the elementary fact
         that a first-order commutator differentiates its cutoff. It is used
         ONLY to TYPE the FP-3 exclusion (SS3.2) and for no other purpose.
         *** D6' IS RESPECTED AS WRITTEN. THIS ARTIFACT DOES NOT REPLACE,
         MOLLIFY, SMOOTH, OR REGULARIZE M(t), AND OFFERS NO SMOOTHED OBJECT TO
         ANY OBLIGATION, LEMMA, CONTROL, REPAIR, OR ENCLOSURE. ***
  TOY-2  CAS GROUP O9-2 — the boolean model of T-3's inference structure. It
         models a PROOF's premise usage, not any operator. No seminorm, no
         topology, and no class is constructed.

WHAT IS NOT A TOY — THE ACTUAL SURFACE THIS ARTIFACT OPERATES ON:
  The sealed bytes of the ten named artifacts, at the line-ranges quoted. Every
  quantifier determination in SS2, SS5 and SS7 is read off those bytes.
  The record-displayed exponents in O9-4 are TRANSCRIPTIONS from the origin
  run's own SS5.1 table, not values computed here.
  The enclosure in O9-1 is a TRANSCRIPTION from W-3 SS4.4; O9-1 asserts nothing
  about whether it is correct, only about its behaviour in n, which is the
  quantifier question.

NO SUBSTITUTE OBJECT IS OFFERED ANYWHERE AS IF IT WERE THE RECORD'S. Where I
could not reach the record's object, I said so and marked the absence (SS2.3's
premise (T-ii); SS5.2's missing functor) rather than modelling it.
```

---

## 10. FLAG BLOCK — STAGE8_LOCALIZATION_ASSUMPTIONS_O9SR_V001

```text
VERDICT = MIXED
  S-1  QUANTIFIER-MISMATCH ... CONFIRMED at FP-3 / the sea-sandwich only.
       Bites: limit-only + class-supremum. Needs: uniform-in-n on A(0).
       Blocked anyway at the needed quantifier by FB9. FP-3 STANDS.
  S-2  ASSUMPTIONS-SOUND ...... sharp localization MANDATED by D6', not chosen.
       Divergence shape-sensitive; obstruction typed RECORD-IMPOSED.
  S-3  MACHINERY-IMPORTED ..... at ONE notion of six: TRANSLATION INVARIANCE,
       as a proof engine (N-3) and as an inert hypothesis (N-3').
       N-1, N-2, N-4 record-native. N-5 threshold side and N-6 licensed.
  S-4  NOT WELL-POSED ......... cross-obstruction degree comparison. Type-level,
       beyond the arithmetic refutation its own audit already landed.

FLAGS RAISED (three; all ATTRIBUTION/TYPE, none a refutation of any verdict):
  FLAG-1  W1_LINE343_UNIFORMITY_ATTRIBUTION_OVERSTATED
          W-1 :343 credits the uniformity bite to the sea-sandwich; the premise
          that would license it is denied by W-1's own :379-385. Third instance
          of a species its own audit caught twice (A-2; the SS8 overreach).
  FLAG-2  FP3_EXCLUSION_INHERITS_D6PRIME_GRADE
          The operator-norm exclusion holds because D6' forbids smoothing. Its
          structural status is conditional on D6' and that is nowhere displayed.
  FLAG-3  CROSS_OBSTRUCTION_DEGREE_PREDICATE_NOT_WELL_TYPED
          "Degree >= 0" is not variable-independent across the three gradings.
          The record's own O6-8d sign split is the visible symptom.

WHAT THIS ARTIFACT DOES NOT DO:
  RETIRES NOTHING. Witness retirement is the registrar's act alone.
  REFUTES NOTHING that was not already refuted of record by its own audit
    (W-1 A-2; origin F-2/F-3). Every finding of mine is a FLAG or a TYPING.
  MOVES NO FAILURE POINT. FP-1, FP-2, FP-3 all STAND, unmoved.
  TOUCHES NO WITNESS, NO V-CLAUSE, NO GATE, NO FENCE, NO THRESHOLD.
  EXHIBITS NO DIVERGENCE of any closure quantity. No +infinity is attached here
    to tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2, A(0), E_int, E_prof, x, eta_1,
    or any wall quantity. All consuming quantities remain UNDECIDED.
  PROPOSES NO SMOOTHING of M(t). D6' respected as written.

FENCES AS COMMISSIONED AND AS HELD:
  alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
  DETERMINATION ONLY. No authored physics. No value. No number offered as a
  magnitude of any program quantity. No measured-constant comparison. No frozen
  constant. Exact symbolic only (fresh venv, sympy 1.14.0, SS6).
  No register / tracker / road / plan / continuation / ledger / lens file read.
  "Q-..." register tokens EXPECTED-UNLOCATABLE — not chased.
  Output path probed ABSENT before first write. No git action.
  All ten consumed seals verified by shasum -a 256 -c from each artifact's own
  directory before any reliance: 10/10 OK.
  SWEEP CUTOFF DECLARED at SS1.2; exhaustiveness sweep-relative only.

THE ANSWER TO THE COMMISSIONED QUESTION, in one sentence: the closure's
localization assumptions are, in the main, RECORD-NATIVE AND SOUNDLY QUANTIFIED
— their localizers are the record's own sealed cell indicator and its own sealed
projector symbol — but ONE obstruction (the sea-sandwich) is proved at a
quantifier the closure does not need, ONE notion (translation invariance) is
imported at exactly the property its argument requires and is provably inert
where it was made to do work, and ONE claim (the cross-obstruction degree
comparison) is not well-typed; none of which weakens any failure point, because
each affected site is independently blocked by an object that is correctly
quantified.
```
