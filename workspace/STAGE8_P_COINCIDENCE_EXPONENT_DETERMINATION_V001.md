# Is p = 0 or p = 1 for the Sealed Subtracted Object? — Determination V001

Date: 2026-07-27

## Status — the answer

```text
**NEITHER. p IS UNDETERMINED BY SEALED TEXT FOR THE OBJECT.**

AND THE TENSION THE QUESTION POSED DOES NOT EXIST. It was a SYMBOL COLLISION, and the collision
is this lane's. p is defined on the UNSANDWICHED difference V(a)-V(0); the degree-0/degree--3
result is about the SANDWICHED object C(V(a)-V(0))C. Both artifacts write "X". They are
different objects, and no sealed step converts one grading into the other.

AND THE p = 0 BRANCH DOES NOT CLOSE THE PROGRAM EITHER: the counting DOES NOT EXCLUDE a
region-level route at p = 0. Undetermined, not dead.

TYPE: BOUNDED NEGATIVE EXISTENTIAL throughout. No p was derived. No region-level bound was
constructed. All five determinations and all four adversarial verifications returned
UNDETERMINED_BY_SEALED_TEXT; the verification standard was asymmetric AGAINST any definite p in
either direction.
PRODUCTION PROHIBITED. alpha_computed = false. proof_authorized = false.
```

## 1. WHAT p ACTUALLY IS — and why the tension dissolves

```text
p IS DEFINED IN EXACTLY TWO SEALED LOCI, both the same shape, and in both it is an exponent on
THE KERNEL OF THE UNSANDWICHED DIFFERENCE V(a) - V(0), measured in r = |x - y| as a
coincidence-jet order, entering as |X_diff(r)|^2 ~ r^{2p} inside the pair integral

      int_{|r| < 1} d^3 r  |C(r)|^2 |X_diff(r)|^2

WITH THE FACTOR |C(r)|^2 ~ r^{-6} CARRIED SEPARATELY. That separation is the whole point:
p indexes the MIDDLE factor only.

*** IT IS NOT AN EXPONENT ON THE SANDWICHED OBJECT'S KERNEL, AND NOT AN EXPONENT ON A NORM. ***
```

**So the two results the question set against each other are about different objects.**

```text
MY DERIVED IDENTITY  P alpha_x P = -n_hat_x P  supports: the SANDWICHED object C alpha_x C keeps
  degree 0 in momentum, hence a degree -3 kernel, hence (in the continuum, under the undischarged
  Galerkin hypothesis) is not Hilbert-Schmidt. THAT IS A STATEMENT ABOUT C(...)C.
p IS DEFINED ON  V(a) - V(0)  WITH THE C's FACTORED OUT.
CONVERTING THE FIRST INTO "p = 0" REQUIRES A GRADING STEP THE CORPUS NEVER PERFORMS. Performing
it would manufacture exactly the number the question asks for. NOT PERFORMED — here or by any
investigating agent; each recorded declining it.

THE COLLISION, EXHIBITED: the symbol "X" denotes V(a) - V(0) in the two artifacts that DEFINE p,
and denotes C(V(a) - V(0))C in the three that REASON ABOUT THE SANDWICHED OBJECT. One letter,
two objects, and the question inherited the ambiguity from this lane's own artifacts.
LOGGED AS INSTANCE 9 of the program's characteristic failure mode. It is mine: I posed the
tension in those terms.
```

**A second, independent typing fact points the same way.** S2 is explicit, verbatim:

```text
S2. G_hs(C, eps) := |C|_4^{-alpha} · || C(V(a) - V(0))C ||_2
      [SUBTRACTED HILBERT-SCHMIDT DENSITY. MUST be a TWO-TIME (cell-S-matrix)
       object; the equal-time version is FALSE by C6. ...]
THE SEALED OBJECT IS TWO-TIME. My degree computation was performed on the instantaneous
generator insertion. The spec EXPLICITLY EXCLUDES the equal-time version. So the computation is
not about the sealed object on this axis either.
```

## 2. "TIME INTEGRATION: AT MOST p = 1" IS A CEILING ON A METHOD — and weaker than that

```text
IT IS NOT EVEN ATTRIBUTED TO TIME INTEGRATION ALONE. The numeral is stated over a SET.
QUOTED, WARD:138-140: "the threshold is p > 3/2. The identity supplies 0. The five closed
mechanisms supply at most 1. THE SHORTFALL IS NOT CLOSED AND IS NOT NARROWED BY THIS ANSWER."
NO SEALED LINE STATES THE OBJECT'S COINCIDENCE EXPONENT IS 1.

AND THE LIVE "time-integration smoothing" STATEMENT IN THE CORPUS IS ON THE WRONG AXIS: its
target is "AMPLITUDE DECAY of at least R^-1 after the cell-time integrations" — LARGE SEPARATION,
not coincidence. Reading it as a coincidence-p result would be the separation-versus-coincidence
confusion this lane flagged yesterday against the shell-count logarithms.
```

## 3. p IS A SINGLE EXPONENT, NOT A SPECTRUM

```text
The corpus defines p ONCE, as the coincidence-vanishing order of the single kernel, and never
assigns it per term, per order in a, or per Dyson order. "p_effective" and "effective p" return
zero occurrences.
SO THE LEADING-VERSUS-SUBLEADING WORRY DOES NOT ARISE AS POSED. It is not that the leading term
carries 0 and subleading carry 1; it is that the corpus has one p and no value for it.
```

## 4. THE ANSWER TO THE UNCHECKED ITEM — region-level at p = 0

```text
*** THE COUNTING DOES NOT EXCLUDE A REGION-LEVEL ROUTE AT p = 0. THE ROUTE IS UNDETERMINED,
NOT DEAD. ***
The int r^-4 dr argument presupposes forming an ABSOLUTE NORM of the kernel. The corpus's own
region-level target — Gamma_K = -log|A_K|, and R_record,L = d^2/ds dt [Gamma_K/N_4(K)] — is built
from a LOG AND DERIVATIVES OF A NORMALIZED AMPLITUDE, a connected/cumulant object with
cancellations available. Whether that route is OBLIGED to pass through D5's absolute anchored sum
(sum |Phi_gamma(a)|, which does destroy cancellation) IS NOT SETTLED BY SEALED TEXT.
NOT SETTLED MEANS NOT EXCLUDED. No region-level bound was constructed here, as instructed; only
the exclusion question was answered, and the answer is that the counting does not deliver it.

CONSEQUENCE FOR THE FRAMING: the premise "p = 0 closes the program with a determinate negative"
DOES NOT HOLD ON SEALED TEXT. Even the unfavourable branch leaves a route undetermined.
```

## 5. NONE OF THE SEVEN MECHANISMS CLAIMS THE OBJECT'S p — the corpus says so itself

```text
Not one of the seven closures is a statement about the object. THREE SAY SO IN THEIR OWN FLAGS:
  COMM:159   p_known = false
  WARD:204   p_of_the_actual_object = UNKNOWN   (object-vs-bound discipline)
  C4:214     p_of_the_actual_object = UNKNOWN   (object-vs-bound, held to the end)
  DIAG:182   p_is_a_property_of_the_sealed_object = false
WARD:168-172 QUOTED: "it does NOT say p <= 3/2. It says NO IDENTIFIED MECHANISM SUPPLIES
p > 3/2 ... The difference kernel's actual p REMAINS UNKNOWN — object-vs-bound discipline: this
is a statement about the available mechanisms, NOT about the object."
THE TWO THAT CARRY NUMBERS CARRY DIFFERENT ONES: commutator algebra reaches p = 1; the Ward
identity supplies p = 0. The seven do not carry a single sealed number.
```

## 6. A DEFECT IN A COUNT THIS LANE HAS BEEN LEANING ON

```text
*** TWO OF THE "SEVEN CLOSED MECHANISMS" HAVE NO CLOSURE. ***
TIME INTEGRATION and Q SUPPORT STRUCTURE have NO primary source, NO derivation, NO witness code,
NO protected-status flag, and NO p value, in a bounded search over the patterns listed by the
investigating agent.
THEIR ONLY SUBSTANTIVE APPEARANCE IS AS OPEN CANDIDATES. QUOTED, COMM:123-133: "WHAT REMAINS AS
CANDIDATE MECHANISM, named and NOT claimed: (i) THE TIME INTEGRATION ... This is the candidate
this lane would attack next. (ii) THE RECORD PROJECTOR Q's STRUCTURE ... is unexamined. NEITHER
IS CLAIMED TO WORK." Flag COMM:156: candidate_mechanisms_named_not_claimed = time_integration;
Q_structure.
THEIR CLOSURE EXISTS ONLY IN ROLL-UP LIST SENTENCES, in three later artifacts.
TIMING, which is the ground of the finding: COMM (17:08) names them as the next things to
attack; WARD (17:32) lists them as closed; no artifact exists between them.
=> "SEVEN CLOSED MECHANISMS" IS A COUNT OF LIST ENTRIES, NOT A COUNT OF CLOSURES WITH SOURCES.
THE HONEST COUNT WITH PRIMARY SOURCES IS FIVE.

THIS IS RECORDED AGAINST THIS LANE'S OWN RECENT WORK. The seven-count is load-bearing in
STAGE8_IS_RL2B_FAILURE_FORCED_BY_F5_DETERMINATION_V001 §1(B) ("the seven closed mechanisms are
closed by algebra and physics; not one is F'-5") and is repeated in
STAGE8_ROUTE2_RATIFICATION_AND_FRAMING_CORRECTION_V001.
DOES THE F'-5 CONCLUSION SURVIVE? YES, AND UNCHANGED. The claim there was that none of the
closures is attributable to F'-5. Five sourced closures, none attributable to F'-5, plus two
entries that are not closures at all, still yields zero attributable to F'-5 — and two
non-closures cannot supply an attribution either. The NUMBER was wrong; the CONCLUSION is not
disturbed. Corrected by append, not by edit.

A SECOND, SMALLER DEFECT IN THE SAME FAMILY: C4:192-193 QUOTED, "which is why six mechanisms all
failed at p = 1". The sixth is the Ward identity, whose own primary source says it supplies 0
(WARD:137). The corpus's careful formulation is a ceiling over a set, not a shared value. The
roll-up compression is not supported by the record it summarizes.
```

## 7. INVENTION CHECK

```text
NO p WAS DERIVED. NO REGION-LEVEL BOUND WAS CONSTRUCTED. Every investigating agent received the
prohibition verbatim as its overriding rule; every one recorded the specific conversions it
declined, and the most consequential declination is the same one in each: converting "degree -3
kernel for the sandwiched X" into "p = 0", which requires a grading step the corpus never
performs.
VERIFICATION WAS ASYMMETRIC AGAINST DEFINITE ANSWERS IN BOTH DIRECTIONS — p = 1 preserves the
program and p = 0 closes it, so both were held to the airtight standard and only UNDETERMINED
was allowed to pass on good bounds alone. FOUR VERIFIERS, FOUR TIMES corrected_p_answer =
UNDETERMINED_BY_SEALED_TEXT. One determination was CONFIRMED and three WEAKENED; none was
refuted, and no verifier found imported physics.
```

## 8. WHAT THIS MEANS

```text
THE CRUX IS UNDETERMINED, NOT UNFAVOURABLE. That is the third of the three answers the principal
named as acceptable, and it is the one the sealed corpus supports.
NOTHING ON THE BOARD IS DECIDED AGAINST THE PROGRAM BY THIS RESULT. Per-cell majorization
remains dead at any p; region-level remains FEASIBLE-BUT-UNDETERMINED, including at p = 0.
THE WELL-POSED SUCCESSOR IS NOW NARROWER AND IS NOT THE ONE THE FRAMING ASSUMED:
  (1) does the connected region object (Gamma_K, R_record,L) have to pass through D5's ABSOLUTE
      anchored sum, or does a cancellation-preserving route exist? That, not p, is what decides
      region-level.
  (2) p for the two-time cell-S-matrix object remains a genuine open question, and the sealed
      definition places it on the UNSANDWICHED difference — which is a different calculation from
      the one this lane has been doing.
NEITHER IS BEGUN. Both are named.
```

## Protected status

```text
question = IS_p_0_OR_1_FOR_THE_SEALED_SUBTRACTED_OBJECT
answer = UNDETERMINED_BY_SEALED_TEXT
answer_type = BOUNDED_NEGATIVE_EXISTENTIAL
p_derived_here = NONE
region_level_bound_constructed = NONE
tension_between_the_two_results = DISSOLVED — it was a symbol collision on "X"
  p is defined on   V(a) - V(0)          (unsandwiched, |C|^2 ~ r^-6 carried separately)
  the degree result is about  C(V(a)-V(0))C   (sandwiched)
  conversion_step_exists_in_corpus = false
own_error_logged = INSTANCE_9 (this lane posed the tension in colliding symbols)
sealed_object_is_two_time_cell_S_matrix = true; equal-time version FALSE by C6
time_integration_p1 = CEILING_ON_A_METHOD, and stated over a SET of five, not that mechanism
p_is_a_spectrum = false   (single exponent; "p_effective" zero occurrences)
region_level_excluded_at_p0 = FALSE — the counting does not exclude it
premise_that_p0_closes_the_program = NOT SUPPORTED BY SEALED TEXT
mechanisms_claiming_the_object_s_p = 0 of 7   (three say UNKNOWN in their own flags)
seven_closed_mechanisms_with_primary_sources = 5
  time_integration_has_a_closure_artifact = false   (named as next-to-attack 24 min earlier)
  Q_support_structure_has_a_closure_artifact = false  (corpus word: "unexamined")
F5_determination_conclusion_disturbed = false   (zero attributable to F'-5 either way)
verifications = 4/4 returned UNDETERMINED; 1 CONFIRMED, 3 WEAKENED, 0 REFUTED
production_authorized = false
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
