# R3.4 Complete Causal Superconnection Parent Specification v001

Date: 2026-07-24

## Status

Forward-sealed construction specification. The complete-parent result has
not been executed.

This specification joins the primitive boundary superconnection, shared
charged source, compact causal incidence support, asymptotic state, generated
descendants, and outgoing record algebra in one parent. It supersedes no
prior result. Every earlier negative result remains active.

No alpha, measured coupling, particle mass, cosmological endpoint, or desired
spectral density may enter this construction.

## Pinned authorities

| File | Limited role | SHA-256 |
|---|---|---|
| `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md` | Adopted charged-record field content, no bare `F^2`, and one complete specification | `a27a2d571273494a0787e2283734ef1405d74dadfe16d64d3450bb4536e50732` |
| `PARENT_STATE_COVARIANCE_PRINCIPLE_V001.md` | Requires state, dynamics, root data, and outgoing limit from one parent | `532b0f0eac4ac749ba3e24954db356f7ca0f98c4f730030075c463efa3158efb` |
| `CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md` | Adopted one-use causal support and descendant accounting | `b0c636f3b2b00f0694ad001cb32a3a84c5d4fc09c25c57fd4fdb8885e8206b30` |
| `R3_3_GLOBAL_INTRINSIC_MEASURE_CLASSIFICATION_RESULT_V001.md` | Unique uniform intrinsic measure on flat primitive causal diamonds | `e4cfaef14309b3acf5674f8c8faee756f744fec4691d5e01d9de0fa422592be2` |
| `R3_3_GLOBAL_INTRINSIC_MEASURE_CLASSIFICATION_V001.seal.sha256` | Complete sealed global-measure bundle | `dcab767cf5d38548ae0fc4e30b3c674cc527082e398575907ce53efaae08e199` |
| `BID_GLOBAL_BOUNDARY_DESCENT_QUASI_FREE_COMPLETENESS_V001.md` | One shared source CAR carrier and operator-valued quasifree lift | `949181d78aca143ebef06eacc4ab1018d43cdf79962ee8c78c350f654a7555dd` |
| `BID_GRADED_BOUNDARY_SUPERCONNECTION_REPAIR_V001.md` | Typed record-odd superconnection and its square | `2215f79cbe69f1de1643427ad5d422b0c3064ff758327e43ff1629de8633f72d` |
| `BID_COMPLETE_ONE_NORMAL_ZERO_FORM_ENUMERATION_V001.md` | Complete one-normal zero-form inventory and branch boundary | `50b5a651df2aca90ee47c6f85b2a502461370a652706ebccad871f191565a2d9` |
| `BID_CHARGED_CELLULAR_CPT_INTERTWINER_DERIVATION_V001.md` | CPT-selected incidence quadrature `c_partial` | `0322763ac48a4428b432124a6947da81826a41f612efa6803ee9a87317929b98` |
| `BID_UNIQUE_CHARGED_CONTROLLED_COUPLING_DERIVATION_V001.md` | Unique primitive pure-charge control inside its declared branch | `b786db3adec8cc335967d49ec13b59923d67f424644f72c535b27b579dd1489f` |
| `BID_FIRST_OPENING_INTERVAL_DERIVATION_V001.md` | Dimensionless incidence interval and causal-diamond geometry | `7471988138233218430c6b6dd07b39f33508a75907557723654dbc712c0c4476` |
| `R3_4_LORENTZIAN_JOINT_ENDPOINT_COMPATIBILITY_RESULT_V001.md` | Exact endpoint is rest-normal only; no universal wavepacket transfer | `1868656d1881e67c8f6263062b27806f71bcc9de03d7eec0e612085fb47de0cf` |
| `R3_4_DRESSED_OUTGOING_RECORD_RANGE_RESULT_V001.md` | Correct source-dressed incoming representation of outgoing records | `3240d935ef558948e09dd6a70092a3f3592747b5e5c99ea559d64adc2a5c303e` |
| `R3_4_SHARED_SOURCE_OUTGOING_RANGE_ERRATUM_V001.md` | Supersedes the earlier bare-endomorphism label | `5f8e60629f2b0fc703b5b6de02ca2de96ffb9d5e8aa48f961a043c40f8c93f41` |
| `BID_SOURCE_PARENT_CLOSURE_GATE_V003.md` | Carries every unresolved source-parent obligation into this gate | `5c679e3741abe782688b8a75ffa1928c308775248e41af192d03976f01cb4dbf` |

## Declared branch

The construction is first attempted in the ordinary asymptotically
stationary `3+1` Lorentzian branch:

```text
globally hyperbolic spin spacetime;
one vectorlike charged Dirac source;
the adopted compact relative-phase U(1) connection;
finite causal diamonds for primitive record events;
and a source-free flat asymptotic region used only for the first complete
Moller construction.
```

Curved and nonstationary extensions remain later covariance tests. They may
not be counted as failures of the stated flat-asymptotic theorem, but the
result may not be promoted to universal curved-spacetime closure.

The source-free asymptotic choice fixes no electromagnetic coupling. The
connection has no independent bare parity-even curvature stiffness.

## Carrier and record incidence

On a Cauchy surface `Sigma`, let

```text
K_Sigma=L2(Sigma,S tensor L^q);
R(K)=tensor_(c in K) R_c;
R_c=span{|r_c>,|p_c>,|e_c>}.
```

There is one global source CAR algebra `CAR(K_Sigma)`. Record factors are
even and distinguishable; no source carrier is copied per record.

On each cell,

```text
Gamma_c=diag(+1,+1,-1);
b_c=
  [[0,0,-1],
   [0,0,+1],
   [-1,+1,0]];
c_c=i Gamma_c b_c.
```

The charged-control projector is the spectral projector of the disclosed
source charge, and `c_c` is extended by the identity on all other record
factors. No new cell coefficient is admitted.

## Intrinsic causal envelope

The flat causal diamond has tip-to-tip proper duration `T_R`. Its uniform
four-volume measure, now globally classified, has normalized time marginal

```text
w(s)=32 min(s,1-s)^3,  0<=s<=1;
integral_0^1 w(s) ds=1.
```

The dimensionless first-opening interval is

```text
tau_R=pi/sqrt(2).
```

For cell `c` with opening time `t_c`, define the fixed envelope

```text
v_c(t)
 =(tau_R/T_R) w((t-t_c)/T_R)
```

inside its causal interval and zero outside. The spatial support at each time
is the corresponding diamond section. A residual interior scalar profile,
post-evaluation pulse reshaping, or independently normalized tail is
forbidden.

The executable must verify that this envelope is the pushforward of the
uniform diamond measure and not a separately selected pulse.

## One complete first-order parent

Let `n` be the Cauchy normal and

```text
S_n=-i slash(n) gamma^5.
```

Let `h_0[g,a]` be the Dirac Hamiltonian obtained from the same spin-plus-U(1)
connection used in the covariant superconnection. On a finite causal complex,
the one-particle parent is

```text
h_K(t)
 =h_0[g,a]
  +sum_(c in K) v_c(t) M_c(t) tensor S_n tensor iota_c(c_c).
```

`M_c(t)` is multiplication by the spatial section of the causal cell in the
continuum construction. A finite incidence regulator may use the Galerkin
compression of this multiplication operator, but it may not replace it with
a target-selected projector.

The many-source parent is the operator-valued number-preserving quasifree
lift:

```text
H_K(t)=dGamma_R(h_K(t)).
```

The ready record state is part of the incoming boundary condition. The
source state is the stationary quasifree state of `h_0`, with any declared
finite-energy incoming excitation treated as boundary data rather than a
new coupling selector.

This is one local first-order parent. The earlier comoving transported
interaction is not used. No separately chosen post-write Hamiltonian is
attached.

## Generated descendants

The covariant first-order kernel is

```text
D_K=i gamma^mu nabla_mu+i gamma^5 C_K(x),
C_K(x)=sum_c v_c(x) M_c(x) iota_c(c_c).
```

Its square must be derived before any response calculation:

```text
D_K^2
 =(i gamma^mu nabla_mu)^2
  -C_K^2
  -gamma^mu gamma^5 nabla_mu C_K.
```

The first term contains the fixed spin-curvature and U(1)-curvature Clifford
contractions. The second contains:

```text
single-cell c_c^2 terms;
overlap terms for causally overlapping cells;
and no independent coefficient.
```

The third is the opening/closure-face derivative-support term. It may be
distributional for the sharp causal support and must be recovered as the
strong limit of smooth intrinsic approximants. It may not be dropped because
it complicates durability.

These terms are descendants of the one first-order operator. They are not
additional primitive action terms. The exact first-order propagator is the
authority for dynamics; squaring it may not create a second evolution law.

## Self-adjointness and evolution

For every finite `K`, the cell term is a bounded, compact-time-supported
self-adjoint perturbation of `h_0` on its fixed domain. The gate must derive:

```text
one common self-adjoint domain;
a unique unitary propagator U_K(t,s);
causal-factorization for spacelike-separated cells;
and strong convergence from smooth intrinsic envelopes to the sealed sharp
cell envelope.
```

If the sharp-support limit changes the evolution or leaves an arbitrary
boundary extension, the parent is blocked.

## In-state and Moller sector

Before the earliest causal cell and after the latest finite cell,

```text
h_K(t)=h_0.
```

Therefore the same parent must supply the algebraic wave operators

```text
Omega_- = lim_(t->-infinity) U_K(0,t) exp(-i h_0 t);
Omega_+ = lim_(t->+infinity) U_K(0,t) exp(-i h_0 t),
S_K=Omega_+^* Omega_-.
```

For compact time support these limits must reduce to finite propagator
expressions; no adiabatic switch or separately selected tail is allowed.

The asymptotic quasifree state is fixed by the positive/negative spectral
projectors of the same `h_0`. A finite-energy incoming source excitation may
vary as physical boundary data, but every durability theorem used for
promotion must hold for the complete declared class, not for one
outcome-selected profile.

## Outgoing records and durability

For a completed record factor `R_j`, every later primitive incidence acts on
the shared source and new record factors but as the identity on `R_j`.
After the closure face,

```text
[H_K(t),I_source tensor A_j]=0
```

for every bare observable `A_j` on that completed factor.

The gate must prove:

1. exact invariance of the completed record's reduced state under every later
   source/new-record unitary;
2. compatibility of the finite output states under
   `A -> A tensor I`;
3. existence of the quasi-local outgoing record state;
4. stabilization of the incoming pullback
   `S_K^*(I tensor A_j)S_K`;
5. that this pullback is generally a source-dressed monomorphism, not a bare
   record endomorphism;
6. recoverability by the already derived central record sequence; and
7. thresholded source-root nonreturn from the same asymptotic generator for
   every admitted `L2` finite-energy excitation.

Reduced-state persistence and root-return decay must remain distinct. Both
are required; neither may be substituted for the other.

## Asymptotic spectral obligation

In the first flat source-free branch,

```text
h_0(p)=alpha_D dot p
```

on the source carrier and acts trivially on completed record factors. The
gate must derive:

```text
purely absolutely-continuous source spectrum;
no source point spectrum or bound modes;
the spectral measure for every admitted L2 source excitation;
and the (T,delta) thresholded return theorem.
```

The record-label degeneracy is an intended invariant public sector, not a
source bound state. The result must report that distinction explicitly.

## Finite-to-continuum covariance

For a physical exhaustion `K_1 subset K_2 subset ...`, the gate must show:

```text
state restriction compatibility;
local propagator/derivation stabilization after a causal buffer;
existence of the quasi-local state and dressed outgoing algebra;
independence under reordering only spacelike-separated cells;
and regulator independence within the Lorentz-covariant continuum class.
```

The half-line, tree, and cubic-lattice models remain regulators or toy
reductions. They are not promoted merely because their decay exponents
agree.

## Mandatory negative controls

The construction must retain all of the following:

```text
static reuse of one completed incidence recurs;
generic finite-momentum packets do not have one exact endpoint time;
the old comoving transported rule is not derived;
the incoming pullback is generally source-dressed;
root-amplitude loss alone does not establish a record;
translation covariance alone does not select the intrinsic measure;
and adding an independent primitive F^2, Pauli, tail, damping, or record
detuning term violates the frozen parent.
```

## Predeclared verdicts

```text
if one parent supplies the stated propagator, descendants, in-state class,
Moller maps, compatible outgoing states, dressed algebra, source spectrum,
and thresholded durability:
  COMPLETE_CAUSAL_SOURCE_RECORD_PARENT_DERIVED_IN_FLAT_ASYMPTOTIC_BRANCH

if the finite parent is valid but one continuum/GNS/spectral obligation
remains:
  FINITE_CAUSAL_PARENT_DERIVED_CONTINUUM_COMPLETION_OPEN

if an envelope, root, tail, boundary extension, or descendant coefficient
must be selected independently:
  COMPLETE_CAUSAL_PARENT_BLOCKED_BY_SEPARATE_SELECTION

otherwise:
  COMPLETE_CAUSAL_PARENT_BLOCKED
```

## Fixed scope

Even the strongest verdict here does not derive the absolute SI value of
`T_R`, the full curved/nonstationary branch, the interacting massless-gauge
infraparticle spectrum, the generated Maxwell coefficient, a Thomson-limit
stiffness, or alpha.

```text
construction_uses_alpha = false
alpha_computed = false
coupling_evaluation_authorized = false
proof_authorized = false
```
