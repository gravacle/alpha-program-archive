# STAGE 8 / TASK 5 — J-II REALIZATION FAMILY: THE LAST CONSTRUCTION

Lane: **Codex Lane 3 (Sol, high effort)**  
Artifact: `STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md`  
Register preflight head: **Q-520**  
Mode: **symbolic only; construction/determination; no member binding**

## Lead determination

The three declaration chains consolidate to one exact specification, but the
sealed stock does **not** inhabit it.  This is a sealed-stock derivability
obstruction (`TYPE-U`), not a theorem that the abstract realization family is
empty and not a relational contradiction (`TYPE-R`).  The decisive missing
datum is an actual incidence/integration/evaluation rule

```text
physical local symbol  --->  finite cochain coefficients.             (L0)
```

DoR-019 gives a metric on the cochain carrier and hence identifies a cochain
with its dual **after** a cochain functional exists.  It does not construct a
functional on the symbol carrier.  The fixed-stage Hodge stock begins at
`C_N^k`; it cannot create the missing input.  The corrected finite declaration

```text
Loc_N^fin(K) := sigma_N^fin(K)                                  (L1)
```

is a finite shadow declaration and regression target.  It is not a physical
kernel-to-symbol construction or a symbol-to-cochain realization member.

The phrase “one construction both routes share” is therefore typed as follows.
Both A8 routes require the same addressed source-extension package through
`D_N^Loc`.  They then split:

```text
H route:
O_a^resp --Ker_(a,K)--> D_N^Loc
         --Loc_N^phys--> Sym_N^loc --iota_N^H--> C_N^k;

HOL route:
O_a^resp --Ker_(a,K)--> D_N^Loc --Xi_N--> Lambda_N^cyc.          (L2)
```

Thus `ExtSrc` and its address/transport structure are common; the physical
`Loc/iota^H` tail is H-specific, and the adopted A9 `Xi_N` tail is HOL-specific.
A9 licenses its interface but explicitly inhabits none of `ExtSrc`, `G4-D`,
`G5`, `FULL-G4`, or `G2-N`.

## 0. Preflight, authority ledger, and determination discipline

### 0.1 Access, register, correction, and no-clobber

The fresh-session STEP-0 access checks all passed before substantive work:

```text
cleanroom workspace = accessible
archive workspace   = accessible
supervision         = accessible
```

The questions-settled register was read at live head `Q-520`.  DoR-020-A9 was
read as adopted law.  The V003 correction was applied: the finite
`sigma^fin/Loc^fin` declaration is cited to the Maxwell-Hodge artifact sealed
at `f074ca24...`; the distinct `0bef9a00...` artifact supplies fixed-stage
Hodge operators and intertwiners, not that declaration.

Immediately before this write, neither the requested artifact nor its sidecar
existed in the cleanroom or archive workspace.

### 0.2 Verified load-bearing authorities

| Key | Authority | Verified SHA-256 | Use here |
|---|---|---|---|
| `FIN` | `STAGE8_TASK5_EQ6_MAXWELL_HODGE_PROJECTOR_CERT_LANE2_V001.md` | `f074ca24e8b96c576f5c64b856377f39ed8d4fc729c02cbf591326322558f816` | line-284 finite declaration `(L1)` |
| `HDG` | `STAGE8_TASK5_EQ6_PROJECTOR_CERT_V003_LANE1_V001.md` | `0bef9a003abb586666535689e98d7af29e7fc75b81e06fe439397ce2b8ef9100` | `d`, `delta`, `Delta`, `P_H`, and their proved rank-preserving intertwiners |
| `PJ2` | `STAGE8_TASK5_EQ6_PHYSICAL_J2_LANE2_V001.md` | `3fd4b924ec1a70f3e730bcf99f406d697c53937b26467d2ae6d5aa8c1bb6822c` | finite shadow/physical-map boundary |
| `J7` | `STAGE8_TASK5_EQ6_J7_FACE_AND_J2_DISPLAY_LANE3_V001.md` | `9c74ed3dbe748450e525102128b0367ce21c0e5029929eab33a111ca9802dc3c` | exact cochain-valued declaration and consumed `(F3)` premise |
| `NAT` | `STAGE8_TASK5_EQ6_LOC_NATURALITY_LANE3_V001.md` | `474bf721517f77b240e2a215325a86227d4e0fd4934e89ba910d955627b5ab60` | identity scope and missing nonidentity base equations |
| `LOC` | `STAGE8_TASK5_EQ6_THE_LOC_BUILD_LANE3_V001.md` | `b53d9e931efe1ebde333ef49fb4243e41917bb5d467f5a86e7052102ae5a0310` | complete candidate specification `(S1)`–`(S25)` |
| `GH` | `STAGE8_TASK5_GAMMA_H_ROUTE_LANE3_V001.md` | `f2317e41367dc906ffa23f6055f2ed96a0f59f74b4e412966809d292c23e5402` | assembly-first method and four stopped joints |
| `GHR` | `STAGE8_TASK5_GAMMA_H_REVIEW_DARIO_V001.md` | `61d41a3ed13039b8db1c149215763f97e0c4f2a5376ae717fd400f1962eae712` | three declarations; corrected J-I/J-II/J-IV gap statement |
| `SEAM1` | `STAGE8_TASK5_COMMON_SEAMS_LANE3_V001.md` | `5de94e16db09b982f85fdf117281af62df6ab0d05c9e7577ac258d47594b69b7` | addressed orientation orbit and remaining F3 bases |
| `SEAM2` | `STAGE8_TASK5_COMMON_SEAMS_LANE3_V002.md` | `2525096ba06973b70064d6b9f9578470e0afca9c48e2cca6cf0f5c1194d12c52` | repaired common-cell R9 quantifier |
| `KER` | `STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md` | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | actual Q-408 current/path-bilocal kernel |
| `SYM` | `STAGE8_TASK5_R4_LOCAL_SYMBOL_MAP_BUILD_LANE1_V001.md` | `bae34116c4d6792b5e39b913addeeff1650989660d89ba01bf5de62ec2d9aa50` | physical local-symbol contract and missing microlocal bridge |
| `ASM3` | `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md` | `a46beae26048a0d093de339363fb8597d362a0101d103cfc06305edabac53f4c` | confirmed finite assembly generators |
| `ASM4` | `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004.md` | `9bf34e27da9aca51966feb2b41f34b0060e2121bd85d783d35a81685bbb63514` | confirmed assembly repair |
| `MET` | `DOR_019_CARRIER_METRIC_AND_UNITS_RATIFICATION_2026-08-03.md` | `6ab72b0cb3a93e123eb1d3c5088fc83361d86c6dc739f0a886380dbd2d143f1f` | cochain metric and no implicit cross-sector conversion |
| `A1` | `DOR_020_A1_WHERE_CLAUSES_AMENDMENT_2026-08-04.md` | `c4dc5976d7e65fe8a6597870629e64cabe6a031b95de97f5572bb36379abb588` | actual path/current, bundle, support, and transport law |
| `A5` | `DOR_020_A5_CONTACT_LAPLACIAN_REDUCING_2026-08-04.md` | `5beccc617f4c5f5f76aba777c664f3dd4393f72257fc850950e2246109a7a424` | contact reducing law after cochain entry |
| `A6` | `DOR_020_A6_J2_SCOPED_PROJECTED_LAW_2026-08-05.md` | `202234ac77136592fdc24a96838909f1dad7e8f6a79fdab81342c5f12d6e82cd` | downward old-image scope; no upward new-cycle lift |
| `A7` | `DOR_020_A7_EC_BRANCH_CARRIED_2026-08-05.md` | `834e46029a292122c1e4c604af4f3e3249e6a9ee2638255464685623fba5eb8f` | both `E_C` branches carried; no selection |
| `A9` | `DOR_020_A9_XI_N_ADOPTED_2026-08-05.md` | `e0b661807f172337ad1cca1b89feec2b6472d531f6dbaae3bd3e39a953510c15` | adopted HOL interface; gates remain uninhabited |
| `CEN` | `STAGE8_TASK5_EQ6_DIAMOND_CENSUS_LANE3_V001.md` | `676863b5ac68108a87dc6b5cf070908bdb15ad45d44a0f80edd473fe2c6ee245` | three exhibited actual diamonds and asymmetric witness |
| `MAN` | `STAGE8_TASK5_EQ6_MANIFESTS_LANE3_V001.md` | `6cc774e2b81de0e597074b70af121575cd92f46ad21d17e959ef7a9adbd3958d` | exact unresolved manifest boundary |

### 0.3 Tags and fences

```text
[PROVABLE]      = consequence displayed from verified sealed stock.
[PART-PROVABLE] = consequence displayed only on the named subscope.
[YOURS]         = specification or typed alternative stated here; not law.

TYPE-U = required map/data not produced by sealed stock.
TYPE-R = incompatibility among already typed requirements.
```

The result below is `TYPE-U`.  No line consumes a desired reader value, the
false anchor, a seed outcome, an A7 branch choice, an R9 equality, or a HOL
return to construct the missing map.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
member_bound = false
fixed_point_executed = false
end_test_executed = false
numeric_evaluation = false
comparison_to_measured_constants = none
```

## 1. H1 — consolidated minimal specification

### 1.1 The three declarations, kept distinct

[PROVABLE] `FIN` declares

```text
Loc_N^fin(K) := sigma_N^fin(K),
m_T,N := P_H,N Loc_N^fin(Kernbar_N Q_N(L_T,N)).    (D-FIN)
```

It immediately consumes the first line in the second.  It does not display a
signature for `sigma_N^fin`, a physical action rule, or linearity.

[PROVABLE] `PJ2` records and internally labels the finite shadow

```text
Loc_N^shadow(K) := sigma_N^fin(K),                  (D-PJ2)
```

with Ward/restriction content, and then explicitly stops the physical map

```text
Ker_N^Q408  --->  Sym_N^loc                         (D-PJ2-stop)
```

as `NOT_BUILT / TYPE-U`.

[PROVABLE] `J7` gives the precise cochain-valued declaration

```text
Loc_N : Ker_N^Q408 -> C_N^k,
Loc_N(K) := Loc_N^fin(K) := sigma_N^fin(K),          (D-J7)
```

for composability.  It supplies no physical formula, contact descent, or
nonidentity base-square proof.  `NAT` confirms that the Stage-1 finite datum
does not independently supply those items.  These three declarations are
therefore specifications/shadows, not three constructions of a member.

### 1.2 Common prefix and the route split

[YOURS — consolidated type] The common source-extension package is

```text
ExtSrc_N :=
 (E_N,qtilde_N,Q_N^quot,O_prof,N,I_N^q,I_N^R5,
  O_R5,N^src,D_N^ker,D_N^Loc,
  iota_N^Q408,Sch_N,S_N,Kraw_N,Sbar_N,Kernbarhat_N,
  Res_(w,a)^resp,S_(w->N,K),Ker_(a,K),
  eta_f^K,tau_f^Loc,rho_a^Loc; source/response/transport laws).   (SPEC-1)
```

The seams and route tails are separate packages:

```text
Rlz_N^H :=
 (Sym_N^loc,Loc_N^phys,iotatilde_N^H,iota_N^H,Loc_N^C,
  j_f^Sym,j_f^C,rho_a^Sym,rho_a^C,S25Data_N,FullJ7_N;
  S21--S25 laws),                                          (SPEC-2)

Rlz_N^Hol :=
 (Lambda_N^cyc,Xi_N,S_f^Lambda;
  A9's adopted Xi/lattice clauses),                        (SPEC-3)

G4D_N :=(supp_D,N,q_D,N^addr,forg_N,Prim_N,phi_(N,S);
         A9 G4-D laws),

G5_N :=(r_N^D on each admitted sign-odd sector;
        A9 G5 reversal laws),

AddrOrb_N :=the whole addressed orientation orbit from SEAM1/SEAM2.
                                                               (SPEC-3a)
```

Here `S25Data_N` binds, rather than merely mentions,

```text
S25Data_N
 :=(sh,j^Shadow,sigma_field,j^Field,LW_2,
    R^D,R^Sym,U^D,U^Sym,z^D,z^Sym,
    Supp^D,Supp^Sym,Delta^*,Loc^batch),                 (SPEC-3b)
```

with the exact types and laws displayed in `(S25-types)`--`(S25g)`.
`FullJ7_N` is the H-only factor-through-`D_N^Loc` seam displayed after
`(SPEC-12c)`.  It is not part of common `ExtSrc_N`, so `Pkg_N^Hol` does not
consume it.

Each route has its own solution-family package over the same `ExtSrc` source:

```text
Pkg_N^H
 :=ExtSrc_N times_(source transport seam) Rlz_N^H
             times_(orientation covariance) AddrOrb_N,

Pkg_N^Hol
 :=ExtSrc_N times_(source transport seam) Rlz_N^Hol
             times G4D_N times G5_N
             times_(orientation covariance) AddrOrb_N,

JII_Rlz_N:=(family(Pkg_N^H),family(Pkg_N^Hol)).          (SPEC-4)
```

The A8 comparison is a separate partial pullback, formed only on one common
physical cell on which both route returns already exist:

```text
Cmp_N
 :=Pkg_N^H times_(same common physical cell) Pkg_N^Hol.  (SPEC-4b)
```

Neither route consumes the other route's tail: H does not consume `Xi_N`, and
HOL does not consume `Loc_N^phys/iota_N^H`.  `SPEC-4` retains every candidate
member and address representative; it chooses no route, A7 branch, primitive
orientation, or map.  The H/source fiber condition includes the transport seam

```text
etahat_f:=tau_f^Loc:D_N^Loc->D_M^Loc,
j_f^prof:=tau_f^prof:O_prof,N->O_prof,M,

eta_f^K:Ker_N^Q408->Ker_M^Q408,

D_M^old:=D_M^(Loc,old),
rho_a^D:=rho_a^Loc:D_M^old->D_N^Loc.                 (SPEC-4a)
```

Thus each route acts on the same typed source transports rather than on a
separately chosen isomorphic copy.  If `(SPEC-4a)` is absent, the alleged H
member is outside `Pkg_N^H`; if the A9 transport squares are absent, the
alleged HOL member is outside `Pkg_N^Hol`.  Absence of `Cmp_N` does not erase
either route family.

### 1.3 Exact H-tail domains, codomains, and action

[YOURS — minimal common H specification] Let

```text
Sym_N^loc
 := Sigma_N^(2),Ward / Ctc_N^sym,                        (SPEC-5)

sigma_2^op : Op_N^(loc,2) -> Sigma_N^(2),Ward,
sigma_2^ker: Sch_N(Op_N^(loc,2)) -> Sigma_N^(2),Ward,
sigma_2^ker(Sch_N(A)) := sigma_2^op(A).                 (SPEC-6)
```

The injectivity of `Sch_N` required in `(SPEC-12a)` makes the last definition
representation-independent.  Any weakened noninjective version must instead
prove `Sch_N(A)=Sch_N(B) => sigma_2^op(A)=sigma_2^op(B)`.

A realization member must provide continuous linear maps in named topologies

```text
Loc_N^phys : D_N^Loc -> Sym_N^loc,

iotatilde_N^H : Sigma_N^(2),Ward -> C_N^k,
iota_N^H      : Sym_N^loc -> C_N^k,

Loc_N^C := iota_N^H compose Loc_N^phys
         : D_N^Loc -> C_N^k.                            (SPEC-7)
```

The symbol-side action is required to be

```text
D_N^Loc subset Sch_N(Op_N^(loc,2)),
Loc_N^phys(K) := [sigma_2^ker(K)]_(Ctc_N^sym).          (SPEC-8)
```

The Hodge landing is required to descend:

```text
[E_C,N,Delta_N] = 0,
Ctc_N^sym subset ker(iotatilde_N^H),

iota_N^H([s]_(Ctc_N^sym)) := iotatilde_N^H(s),
iota_N^H([s]) = (1-E_C,N)iota_N^H([s]).                (SPEC-9)
```

The full commutator in `SPEC-9` is retained.  Neither
`P_H,N Loc_N^C(kappa_T,N) != 0` nor its negation is a membership clause.  The
previously struck range clause

```text
iota_N^H Loc_N^phys(kappa_T,N)
  in closure(ran(delta_N d_N))                          (STRUCK)
```

is not restored; no sealed Hodge-fiber law forces it.

### 1.4 Source extension and the `L_T` summand

The source must be formed through the current `SEAM1` corestriction/descent
package, not by juxtaposing two summand maps.  First keep the quotient and
physical image topologies distinct:

```text
E_N
 := R L_T,N direct-sum R Rhat_K,N
       direct-sum S_(1,sa)(K_N),

qtilde_N:E_N -> O_R5,N,
I_N:=im(qtilde_N),
Rel_N:=closure(ker(qtilde_N)),

O_prof,N:=E_N/Rel_N,
Q_N^quot:E_N->>O_prof,N,

I_N^q :=I_N with the qtilde_N quotient topology,
I_N^R5:=I_N with the O_R5,N subspace topology.          (SPEC-10)
```

The literal corestriction and underlying-set arrows have the displayed
direction

```text
Q_N^core:E_N->>I_N^q,
kappa_N:I_N^q->I_N^R5,
jmath_N:I_N^R5->O_R5,N,

Q_N^core(z)=qtilde_N(z),
kappa_N(x)=x,
jmath_N(x)=x.                                           (SPEC-10a)
```

The first-isomorphism arrow and the continuous physical path are explicitly
failure-capable:

```text
Rel_N=ker(qtilde_N)
 => beta_N:O_prof,N->I_N^q,
    beta_N(Q_N^quot z):=qtilde_N(z),
    Q_N^core=beta_N Q_N^quot;

kappa_N continuous and a topological embedding
 => jmath_N kappa_N beta_N Q_N^quot=qtilde_N.           (SPEC-10a')
```

The underlying-set identity does not itself prove the `kappa_N` topology
certificate.

At unaddressed scope only,

```text
O_w^resp:=O_R5,w,
iota_w^resp:O_R5,w->O_w^resp,
iota_w^resp(Y):=Y.                                     (SPEC-10b)
```

The physical source extension then has the exact type

```text
D_N^ker                                              Hausdorff TVS,
O_R5,N^src subset O_R5,N,
I_N^R5 subset O_R5,N^src,

S_N:O_R5,N^src->D_N^ker                             continuous linear,
Kraw_N:=S_N qtilde_N:E_N->D_N^ker,

Sbar_N:O_prof,N->D_N^ker,
Sbar_N(Q_N^quot z):=Kraw_N(z).                        (SPEC-11)
```

Continuity and Hausdorffness display the automatic descent:

```text
r in ker(qtilde_N)
 =>Kraw_N(r)=S_N(qtilde_N(r))=S_N(0)=0,

Rel_N=closure(ker(qtilde_N))
 subset ker(Kraw_N),

Q_N^quot z=Q_N^quot z'
 =>Kraw_N(z)=Kraw_N(z')
 =>Sbar_N(Q_N^quot z)=Sbar_N(Q_N^quot z').             (SPEC-11a)
```

The one source-agreement law that includes the `L_T` summand is

```text
S_N(qtilde_N(a,b,C))
 :=a Sch_N(M_N^loc)
   +iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)).         (SPEC-11b)

S_N(qtilde_N(1,0,0))
 =Sch_N(M_N^loc)=:kappa_T,N,

S_N(qtilde_N(0,b,C))
 =iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)).           (SPEC-11c)
```

Mixed relations in `O_R5,N` must receive one value under `(SPEC-11b)`; the
plus sign is not a direct-sum assertion.  The extension question is exactly
whether the restriction on `I_N^R5` has a continuous linear extension to
`O_R5,N^src`, without identifying `I_N^q` and `I_N^R5` topologically.

Define, only for a member satisfying those clauses,

```text
D_N^Loc:=im(Sbar_N)=im(Kraw_N) subset D_N^ker,

D_N^Loc carries the quotient topology from E_N/ker(Kraw_N),
D_N^Loc->D_N^ker is a topological embedding,

Krawhat_N:=Kraw_N,
Kernbarhat_N:=corestr_(D_N^Loc)(Sbar_N)
 :O_prof,N->D_N^Loc.                                  (SPEC-12)
```

The addressed factor is separate:

```text
iota_N^Q408:Ker_N^Q408->D_N^ker
 continuous and injective,

Sch_N:Op_N^(loc,2)->D_N^ker
 continuous and injective,

Res_(w,a)^resp:O_w^resp->O_a^resp,
Res_(w,a)^resp continuous, linear, and K-independent,

res_(w,a)^supp:Supp_w->Supp_a,
supp_a(Res_(w,a)^resp(Y))
 subset res_(w,a)^supp(supp_w(Y)),

S_(w->N,K):O_w^resp->D_N^ker,
S_(w->N,K) continuous and linear,

T_(w->N,K)^D:D_w^ker->D_N^ker,
T_(w->N,K)^D continuous and linear,

t_(w->N,K)^supp:Supp_w^ker->Supp_N^ker,

QuotResCert_(w,a):Res_(w,a)^resp is quotient onto its image,

Ker_(a,K)^im:im(Res_(w,a)^resp)->D_N^ker,
Ker_(a,K)^im continuous and linear,

Ker_(a,K)^ker:O_a^resp->D_N^ker,
Ker_(a,K)^ker continuous and linear in its response input,

Ker_(a,K)^im Res_(w,a)^resp=S_(w->N,K),
Ker_(a,K)^ker|_(im Res_(w,a)^resp)=Ker_(a,K)^im,
im(Ker_(a,K)^ker) subset D_N^Loc,

Ker_(a,K):=corestr_(D_N^Loc)(Ker_(a,K)^ker)
 :O_a^resp->D_N^Loc.                                  (SPEC-12a)
```

The response/source-extension coherence is displayed rather than hidden in
the word “package”:

```text
supp_N(T_(w->N,K)^D x)
 subset t_(w->N,K)^supp(supp_w(x)),

T_(N->P,K_N)^D T_(w->N,K_w)^D=T_(w->P,K_w)^D,
T_(w->w,K)^D=id_(D_w^ker),

t_(N->P,K_N)^supp t_(w->N,K_w)^supp
 =t_(w->P,K_w)^supp,
t_(w->w,K)^supp=id_(Supp_w^ker),

S_(w->N,K)|_(iota_w^resp(O_R5,w^src))
 =T_(w->N,K)^D S_w (iota_w^resp)^(-1),

ker(Res_(w,a)^resp) subset ker(S_(w->N,K)),
Ker_(a,K)^im Res_(w,a)^resp=S_(w->N,K).                (SPEC-12a')
```

For every admitted rank-preserving `f:N->M`, member data have the exact types

```text
tau_f^E:E_N->E_M,
tau_f^src:O_R5,N^src->O_R5,M^src,
tau_f^prof:O_prof,N->O_prof,M,
tau_f^D:D_N^ker->D_M^ker,
tau_f^Op:Op_N^(loc,2)->Op_M^(loc,2),
tau_f^supp:Supp_N^ker->Supp_M^ker,
tau_(f,w)^D:D_w^ker->D_(f.w)^ker,
tau_(f,w)^supp:Supp_w^ker->Supp_(f.w)^ker,
tau_f^resp:O_w^resp->O_(f.w)^resp,
tau_f^a:O_a^resp->O_(f.a)^resp,
tau_f^K:Kpar_N->Kpar_M,
tau_f^K(K)=:f.K.                                      (SPEC-12b0)
```

Their source, physical, response, and image squares are

```text
tau_f^src qtilde_N=qtilde_M tau_f^E,
tau_f^prof Q_N^quot=Q_M^quot tau_f^E,
tau_f^D S_N=S_M tau_f^src,
tau_f^D Kraw_N=Kraw_M tau_f^E,
tau_f^D Sbar_N=Sbar_M tau_f^prof,

tau_f^D iota_N^Q408=iota_M^Q408 eta_f^K,
tau_f^D Sch_N=Sch_M tau_f^Op,
tau_f^D(D_N^Loc) subset D_M^Loc,

tau_f^D S_(w->N,K)
 =S_(f.w->M,f.K) tau_f^resp,

tau_f^D T_(w->N,K)^D
 =T_(f.w->M,f.K)^D tau_(f,w)^D,

tau_f^supp t_(w->N,K)^supp
 =t_(f.w->M,f.K)^supp tau_(f,w)^supp,

tau_f^a Res_(w,a)^resp
 =Res_(f.w,f.a)^resp tau_f^resp,

tau_f^D Ker_(a,K)^ker
 =Ker_(f.a,f.K)^ker tau_f^a.                         (SPEC-12b1)
```

The image clause defines

```text
tau_f^Loc
 :=corestr_(D_M^Loc)(tau_f^D|_(D_N^Loc))
 :D_N^Loc->D_M^Loc,

tau_f^Loc Ker_(a,K)=Ker_(f.a,f.K) tau_f^a.             (SPEC-12b)
```

Identities, composition, and admitted common refinements are required at each
typed carrier:

```text
tau_id^X=id_X,
tau_(g compose f)^X=tau_g^X tau_f^X
 for X in {E,src,prof,D,Loc,Op,supp,resp,a,K},

eta_id^K=id_KerQ408,
eta_(g compose f)^K=eta_g^K eta_f^K,

tau_(id,w)^D=id_(D_w^ker),
tau_(g compose f,w)^D
 =tau_(g,f.w)^D tau_(f,w)^D,

tau_(id,w)^supp=id_(Supp_w^ker),
tau_(g compose f,w)^supp
 =tau_(g,f.w)^supp tau_(f,w)^supp,

g1 f1=g2 f2
 =>tau_g1^X tau_f1^X=tau_g2^X tau_f2^X
 for the same typed X,

g1 f1=g2 f2
 =>eta_g1^K eta_f1^K=eta_g2^K eta_f2^K,

g1 f1=g2 f2
 =>tau_(g1,f1.w)^D tau_(f1,w)^D
    =tau_(g2,f2.w)^D tau_(f2,w)^D,

   tau_(g1,f1.w)^supp tau_(f1,w)^supp
    =tau_(g2,f2.w)^supp tau_(f2,w)^supp.                (SPEC-12b2)
```

For a cycle-creating `h:N->M`, the old-image types are

```text
rho_h^src:O_R5,M^(src,old)->O_R5,N^src,
rho_h^D:D_M^(ker,old)->D_N^ker,
rho_h^a:O_(a_M^old)^resp->O_(a_N)^resp,
rho_h^K:Kpar_M^old->Kpar_N,

D_M^(Loc,old):=D_M^Loc intersection D_M^(ker,old),

rho_h^Loc
 :=corestr_(D_N^Loc)(rho_h^D|_(D_M^(Loc,old)))
 :D_M^(Loc,old)->D_N^Loc.                             (SPEC-12b3)
```

The downward source and addressed-kernel squares are

```text
S_M(O_R5,M^(src,old)) subset D_M^(ker,old),
rho_h^D S_M|_(O_R5,M^(src,old))=S_N rho_h^src,

Ker_(a_M^old,K_M)^ker(O_(a_M^old)^resp)
 subset D_M^(ker,old),

rho_h^D Ker_(a_M^old,K_M)^ker
 =Ker_(a_N,rho_h^K(K_M))^ker rho_h^a,

rho_h^D(D_M^(Loc,old)) subset D_N^Loc,

rho_h^Loc Ker_(a_M^old,K_M)
 =Ker_(a_N,rho_h^K(K_M)) rho_h^a.                     (SPEC-12b4)
```

For admitted identities, composable cycle-creating arrows
`N--h-->M--k-->P`, and equal pasted forward composites, the contravariant
old-image coherence is

```text
rho_id^Y=id_Y,
rho_(k compose h)^Y=rho_h^Y rho_k^Y,

k1 h1=k2 h2
 =>rho_h1^Y rho_k1^Y=rho_h2^Y rho_k2^Y

for Y in {src,D,a,K,Loc},
on the displayed common old-image domain.              (SPEC-12b5)
```

None is extended to the target-new-cycle complement.

The exact image obligations are

```text
kappa_T,N in D_N^Loc,
iota_N^Q408 Kernbar_N^cyc(O_N^cyc) subset D_N^Loc,

D_N^Loc
 =span{kappa_T,N}
  +iota_N^Q408 Kernbar_N^cyc(O_N^cyc),

D_N^Loc subset Sch_N(Op_N^(loc,2)).                    (SPEC-12c)
```

The represented-image inclusion is the narrower ExtSrc clause reached by the
existing declarations; no member is inferred.

The **H-only** full-J7 seam, outside common `ExtSrc_N`, is

```text
FullJ7_N
 :=(FullKerCert_N,iota_N^(Q408,Loc),
    Loc_N^(phys,Q408),Loc_N^(C,Q408)),

FullKerCert_N:
 iota_N^Q408(Ker_N^Q408) subset D_N^Loc,

iota_N^(Q408,Loc)
 :=corestr_(D_N^Loc)(iota_N^Q408)
 :Ker_N^Q408->D_N^Loc,

Loc_N^(phys,Q408)
 :=Loc_N^phys iota_N^(Q408,Loc)
 :Ker_N^Q408->Sym_N^loc,

Loc_N^(C,Q408)
 :=Loc_N^C iota_N^(Q408,Loc)
 :Ker_N^Q408->C_N^k.                                  (SPEC-12d)
```

`FullJ7_N` is required solely to recover J7's declared full domain.  It is a
failure-capable H-tail `TYPE-U` seam, not an inference that every Q-408 kernel
lies in the source-extension image.  Its final line is the physical map-level
object whose type matches J7; it is not the finite-shadow declaration.

`GHR` establishes that quotient descent through the R5 corestriction is not a
separate new obligation.  The still-missing half is the source-extension
member in the required direction, its response/address extension, image
membership, topology, support, and transport certificates.

### 1.5 Rank-preserving and cycle-creating laws actually consumed

For each **exhibited** rank-preserving arrow `f:N->M`, a member must supply

```text
Ker_N^rep:=Kernbar_N^cyc(O_N^cyc) subset Ker_N^Q408,

eta_f^K(Ker_N^rep) subset Ker_M^rep,

eta_f^(K,rep)
 :=eta_f^K|_(Ker_N^rep):Ker_N^rep->Ker_M^rep,

iota_N^rep
 :=iota_N^Q408|_(Ker_N^rep):Ker_N^rep->D_N^Loc.        (S22-rep)
```

The codomain in the last line is licensed by `(SPEC-12c)`.  The common ExtSrc
and H-tail laws on the represented scope are

```text
etahat_f:D_N^Loc -> D_M^Loc,
j_f^Sym:Sym_N^loc -> Sym_M^loc,                         (S22)

etahat_id=id_DLoc,
etahat_(g compose f)=etahat_g etahat_f,
j_id^Sym=id_Sym,
j_(g compose f)^Sym=j_g^Sym j_f^Sym,                   (S22a)

etahat_f iota_N^rep
 =iota_M^rep eta_f^(K,rep),

etahat_f Kernbarhat_N
 =Kernbarhat_M j_f^prof,                                (S23)

Loc_M^phys etahat_f
 =j_f^Sym Loc_N^phys,                                   (S24)

iota_M^H j_f^Sym
 =j_f^C iota_N^H.                                       (S25)
```

An H member carrying `FullJ7_N` and `FullJ7_M` must additionally prove the
full-domain square and its direct cochain consequence:

```text
etahat_f iota_N^(Q408,Loc)
 =iota_M^(Q408,Loc) eta_f^K,                           (S23-full-J7)

Loc_M^(C,Q408) eta_f^K
 =Loc_M^C iota_M^(Q408,Loc) eta_f^K
 =Loc_M^C etahat_f iota_N^(Q408,Loc)
 =j_f^C Loc_N^C iota_N^(Q408,Loc)
 =j_f^C Loc_N^(C,Q408).                                (S23-full-J7-C)
```

Only after `(S24)/(S25)` are instantiated may the `D_N^Loc` cochain square be written
as an equality chain:

```text
Loc_M^C etahat_f
 =iota_M^H Loc_M^phys etahat_f
 =iota_M^H j_f^Sym Loc_N^phys
 =j_f^C iota_N^H Loc_N^phys
 =j_f^C Loc_N^C.                                        (SPEC-13)
```

For a cycle-creating arrow `a:N->M`, only downward old-image equations are
allowed:

```text
D_M^old subset D_M^Loc,
Sym_M^old subset Sym_M^loc,
iota_M^H(Sym_M^old) subset C_M^(k,old),

rho_a^D:D_M^old->D_N^Loc,
rho_a^Sym:Sym_M^old->Sym_N^loc,
rho_a^C:C_M^(k,old)->C_N^k,                            (S25a)

rho_a^Sym Loc_M^phys|_(D_M^old)
 =Loc_N^phys rho_a^D,

rho_a^C iota_M^H|_(Sym_M^old)
 =iota_N^H rho_a^Sym.                                   (SPEC-14)
```

No upward map onto the target-new-cycle complement is introduced.

The support/reality/unit/zero-extension/batching and diamond clauses required
by the downstream family are displayed, with each named transport a separate
member datum:

```text
sh_N:Sym_N^loc->Shadow_N^fin,
j_f^Shadow:Shadow_N^fin->Shadow_M^fin,

sigma_field,N:Sym_N^loc->FieldSym_N,
j_f^Field:FieldSym_N->FieldSym_M,
LW_2,N:D_N^Loc->FieldSym_N,

R_N^D:D_N^Loc->D_Nbar^Loc,
R_N^Sym:Sym_N^loc->Sym_Nbar^loc,

U_f^D:D_N^Loc->D_M^Loc,
U_f^Sym:Sym_N^loc->Sym_M^loc,

z_f^D:D_N^Loc->D_M^Loc,
z_f^Sym:Sym_N^loc->Sym_M^loc,

Supp_N^D:D_N^Loc->P(SuppCarrier_N^D),
Supp_N^Sym:Sym_N^loc->P(SuppCarrier_N^Sym),
Delta_N^*:P(SuppCarrier_N^D)->P(SuppCarrier_N^Sym),

Loc_(N1,N2)^phys:
 D_N1^Loc times D_N2^Loc
 ->Sym_N1^loc times Sym_N2^loc.                         (S25-types)

sh_M j_f^Sym=j_f^Shadow sh_N,
sigma_field,M j_f^Sym=j_f^Field sigma_field,N,
LW_2,M etahat_f=j_f^Field LW_2,N,                     (S25c)

Loc_Nbar^phys R_N^D=R_N^Sym Loc_N^phys,
Loc_M^phys U_f^D=U_f^Sym Loc_N^phys,                  (S25d)

Loc_M^phys z_f^D=z_f^Sym Loc_N^phys,
Supp_N^Sym(Loc_N^phys K)
 subset Delta_N^*(Supp_N^D K),                         (S25e)

Loc_(N1,N2)^phys(K1,K2)
 =(Loc_N1^phys K1,Loc_N2^phys K2),                    (S25f)

g1 f1=g2 f2
 =>etahat_g1 etahat_f1=etahat_g2 etahat_f2,
   j_g1^Sym j_f1^Sym=j_g2^Sym j_f2^Sym.               (S25g)
```

`S25f` is an ordered product, not a scalar contraction.  The last implication
is required only on an actually admitted common-refinement diamond.

### 1.6 Address and support clauses, including the Q-520 micro-repair

The addressed orbit from `SEAM1/SEAM2` must be carried covariantly.  No address
is assigned `+`; if `a^-` is the mate of `a`, then the full orbit carries

```text
mu(a^-)=-mu(a).                                         (SPEC-15)
```

A9's `G4-D` source stratum is the separate, uninhabited package

```text
R_N^rec                                             record-support carrier,
supp_D,N:D_N^Loc->P(R_N^rec),
AddrOrb_N                                           primitive unoriented orbits,
Prim_N:P(R_N^rec)->P_fin(AddrOrb_N),
S_D,N^addr                                          fixed-point-free Z_2-set,
forg_N:S_D,N^addr->P(R_N^rec),
q_D,N^addr:D_N^Loc -> {0_D} disjoint-union S_D,N^addr,

q_D,N^addr^(-1)({0_D})={0},
supp_D,N(0)=emptyset,
supp_D,N^(-1)({emptyset})={0},

x!=0
 =>forg_N(q_D,N^addr(x))=supp_D,N(x),

forg_N(s^-)=forg_N(s),
q_D,N^addr(t x)=q_D,N^addr(x) for t>0,
supp_D,N(t x)=supp_D,N(x) for t>0,
(s^-)^-=s,
s^-!=s.                                               (SPEC-16)
```

For every realized nonzero support
`S in im(supp_D,N|_(D_N^Loc minus {0}))`, `G4-D` additionally requires

```text
Prim_N(S)!=emptyset,

phi_(N,S):forg_N^(-1)(S)
 isomorphic_to product_([a] in Prim_N(S)) [a],

phi_(N,S)(s^-)
 =(phi_(N,S)(s)_[a]^-)_[a in Prim_N(S)].             (SPEC-16a)
```

Q-520 found that reversal was undefined at the isolated basepoint.  The repair
is forced by zero preservation, not chosen.  On each future admitted G5
sign-odd sector, extend the candidate reversal to zero and require

```text
0_D^- := 0_D,

s^- != s for s in S_D,N^addr,

r_N^D(0)=0
 =>q_D,N^addr(r_N^D0)=0_D=0_D^-.                       (SPEC-17)
```

The nonzero G5 address/source equations remain

```text
q_D,N^addr(r_N^D x)=q_D,N^addr(x)^-,

r_N^D Ker_(a,K)
 =Ker_(a^-,K^-) r_(a,K)^O,

Xi_N(r_N^D x)=-Xi_N(x)                                (SPEC-17a)
```

on the specifically admitted sign-odd domain.  No global
`r_N^D:D_N^Loc->D_N^Loc` member is asserted before G5 is inhabited.

Thus the involution on the disjoint union is fixed-point-free away from the
basepoint.  `SPEC-17` repairs the type while inhabiting neither `G4-D` nor
`G5`.

### 1.7 Downstream-consumer walk

| Consumer | Exact content it uses from a member | What it does not get for free |
|---|---|---|
| `Gamma^H`, J-I | `ExtSrc`, `Ker_(a,K):O_a^resp->D_N^Loc` | corestriction alone does not inhabit source extension |
| `Gamma^H`, J-II | `Loc_N^C=iota_N^H Loc_N^phys` | finite `sigma_N^fin` does not build either physical arrow |
| F3 reciprocal base | actual `(S23)`, `(S24)`, `(S25)` on the reciprocal generator | `d/delta/P_H` naturality starts only after the cochain landing |
| F3 refinement base | the same three displays on the nontrivial `q=2` refinement | a generic declaration is not a base proof |
| S22--S25 | identity, composition, support, reality, common-refinement, and actual-arrow equations | package-wide naturality is not inferred from two arrows |
| A6 | rank-preserving square plus `(SPEC-14)` on the old image | no upward new-cycle lift |
| A9 ExtSrc | the common `D_N^Loc`, addressed `Ker`, and `tau_f^Loc/rho_a^Loc` | A9 adoption inhabits none of them |
| A9 `G4-D/G5` | actual support stratum and source reversal | the built orientation orbit is not a source-support map |
| A9 `FULL-G4/G2-N` | independent cross-carrier support and chart/normalization data | remains independent of H realization |
| A8 R9 | one actual common cell on which both returns are independently formed | R9 is a falsifier, not a constructor |

## 2. H2 — assembly-first piece ledger

Every piece is listed before any joint is tested.  `SEALED` means an actual
object/map is supplied by stock; `DECLARED` means a signature or equation is
written but no member is constructed; `REQUIRED` is part of the consolidated
family; `ADOPTED-UNINHABITED` is A9's status.

| # | Piece and exact type | Source | Status |
|---:|---|---|---|
| 1 | `E_N=R L_T,N direct-sum R Rhat_K,N direct-sum S_(1,sa)(K_N)` | `LOC/ASM` | `SEALED` carrier presentation |
| 2 | `Q_N^quot:E_N->>O_prof,N=E_N/Rel_N` | `SEAM1/PJ2/ASM` | `SEALED` quotient presentation |
| 3 | `O_N^cyc:=Q_N^quot({0} direct-sum R Rhat_K,N direct-sum S_(1,sa)(K_N))` | `LOC` | `SEALED` scoped subcarrier |
| 4 | `Kernbar_N^cyc:O_N^cyc->Ker_N^Q408` | `KER/PJ2` | `SEALED` on the cycle/profile branch |
| 5 | `qtilde_N:E_N->O_R5,N`; `O_w^resp:=O_R5,w`; `iota_w^resp:O_R5,w->O_w^resp` | `GHR/SEAM1` | `SEALED` raw realization and unaddressed relabelling |
| 6 | `D_N^ker`, one common topological kernel carrier | `LOC` | `REQUIRED` |
| 7 | `iota_N^Q408:Ker_N^Q408->D_N^ker` | `LOC` ExtSrc specification | common ambient injection `REQUIRED`; no full `D_N^Loc` image inferred |
| 8 | `M_N^loc=delta_(g,N)d in Op_N^(loc,2)` | `LOC/SYM` | operator declared; common-kernel realization open |
| 9 | `Sch_N:Op_N^(loc,2)->D_N^ker` | `LOC/SYM` | `REQUIRED` member/topology |
| 10 | `S_N:O_R5,N^src->D_N^ker`; `Kraw_N:=S_N qtilde_N:E_N->D_N^ker` | `SEAM1/LOC` | `REQUIRED` source extension |
| 11 | `kappa_T,N=S_N(qtilde_N(1,0,0))=Sch_N(M_N^loc)` | `SEAM1/LOC` | `REQUIRED`; not presently formed |
| 12 | `D_N^Loc:=im(Sbar_N)=im(Kraw_N)` with quotient topology and embedded inclusion | `SEAM1/LOC/A9` | `REQUIRED`; A9 consumes but does not inhabit |
| 13 | `Kernbarhat_N:=corestr_(D_N^Loc)(Sbar_N):O_prof,N->D_N^Loc` | `SEAM1/LOC` | `REQUIRED` |
| 14 | `Ker_(a,K):O_a^resp->D_N^Loc` | `GH/A9` | `REQUIRED` addressed source map |
| 15 | `Sigma_N^(2),Ward` | `SYM/LOC` | `DECLARED` symbol class |
| 16 | closed `Ctc_N^sym subset Sigma_N^(2),Ward` | `LOC` | `REQUIRED` ideal and closure proof |
| 17 | `Sym_N^loc:=Sigma_N^(2),Ward/Ctc_N^sym` | `LOC/PJ2` | `DECLARED` target |
| 18 | `sigma_2^op:Op_N^(loc,2)->Sigma_N^(2),Ward` | `SYM/LOC` | `REQUIRED` physical symbol member |
| 19 | `sigma_2^ker:Sch_N(Op_N^(loc,2))->Sigma_N^(2),Ward` | `LOC` | `REQUIRED` compatible kernel symbol |
| 20 | `Loc_N^phys:D_N^Loc->Sym_N^loc`; H-only conditional `Loc_N^(phys,Q408):=Loc_N^phys iota_N^(Q408,Loc):Ker_N^Q408->Sym_N^loc` | `PJ2/LOC/GH` plus `[YOURS] FullJ7` seam | `DECLARED/REQUIRED`; no member |
| 21 | `C_N^k`, `d_N`, `delta_N`, `Delta_N`, `P_H,N`; metric Riesz isomorphism `R_C,N:C_N^k->(C_N^k)^*` | `MET/HDG` | `SEALED` fixed-stage cochain/Hodge stock |
| 22 | `E_C,N:C_N^k->C_N^k`, `[E_C,N,Delta_N]=0` | A5/`LOC` | `SEALED` reducing law after cochain entry |
| 23 | `Phitilde_N:Sigma_N^(2),Ward->(C_N^k)^*`; `iotatilde_N^H:=R_C,N^(-1)Phitilde_N` | `[YOURS]` equivalent Riesz reparameterization using `MET` | conditional coordinate for the `LOC/GH`-required `iotatilde_N^H`; no member |
| 24 | `Phi_N:Sym_N^loc->(C_N^k)^*`; `iota_N^H:=R_C,N^(-1)Phi_N` | `[YOURS]` equivalent Riesz reparameterization using `MET` | conditional coordinate for the `LOC/GH`-required `iota_N^H`; no member |
| 25 | `Loc_N^C:=iota_N^H Loc_N^phys:D_N^Loc->C_N^k`; H-only conditional `Loc_N^(C,Q408):=Loc_N^C iota_N^(Q408,Loc):Ker_N^Q408->C_N^k` | `LOC/GH/J7` plus `[YOURS] FullJ7` seam | `REQUIRED`; unformed composite |
| 26 | `Loc_N^fin(K):=sigma_N^fin(K)` and `Loc_N^shadow(K):=sigma_N^fin(K)`; no independent `sigma_N^fin` domain/codomain signature | `FIN/PJ2/J7` | `DECLARED` finite shadow/regression expression; missing signature is part of the obstruction |
| 27 | `etahat_f:D_N^Loc->D_M^Loc` | `LOC/A9` | `REQUIRED`; no nonidentity member |
| 28 | `j_f^Sym:Sym_N^loc->Sym_M^loc` | `LOC` | `REQUIRED` |
| 29 | `j_f^C:C_N^k->C_M^k` | `HDG/J7` | cochain transport available on certified rank-preserving scope |
| 30 | `rho_a^D,rho_a^Sym,rho_a^C` on old images | `A6/LOC` | `REQUIRED` downward scope only |
| 31 | `supp_D,N:D_N^Loc->P(R_N^rec)`; `q_D,N^addr:D_N^Loc->{0_D} disjoint-union S_D,N^addr`; `forg_N,Prim_N,phi_(N,S)` as `(SPEC-16)/(SPEC-16a)` | A9/`SEAM2` | `ADOPTED-UNINHABITED` G4-D package; zero type repaired by `(SPEC-17)` |
| 32 | `r_N^D:D_N^(Loc,odd)->D_N^(Loc,odd)` on each admitted G5 sign-odd sector, with candidate zero extension | A9 | `ADOPTED-UNINHABITED`; no global reversal asserted |
| 33 | `R_or,[a]=(R x [a])/((q,a^-)~(-q,a))`; `Triv_[a]=Iso_R(R_or,[a],K_amb)`; `mu_T(a^-)=-mu_T(a)` | `SEAM1/SEAM2` | `SEALED` orbit family; no `T` or representative chosen |
| 34 | `Xi_N:D_N^Loc->Lambda_N^cyc` | A9 | `ADOPTED-UNINHABITED` set-map interface |
| 35 | `tau_f^Loc:D_N^Loc->D_M^Loc`; `rho_a^Loc:D_M^(Loc,old)->D_N^Loc`; `S_f^Lambda:Lambda_N^cyc->Lambda_M^cyc` | `SEAM1`/A9 | `ADOPTED-UNINHABITED` transport gates |
| 36 | H return `Gamma_(a,epsilon,K)^H:=Loc_N^C Ker_(a,K)` | `GH` | `REQUIRED`; unformed |
| 37 | `Theta_N^rel(x;A1,A0):=Hol_(A1)(Xi_Nx)Hol_(A0)(Xi_Nx)^(-1) in U(1)` with period class `[u_(Xi_Nx)(A1-A0)] in R/(2 pi Z)` | A9 | adopted type; gates and member uninhabited |
| 38 | representative-free return carrier `Kbar_[a]` and `R9-V002` | `SEAM2` | `SEALED` comparison type/falsifier; no common cell yet |
| 39 | actual reciprocal/refinement arrows; three executed diamond prototypes; generated `D_cert` closure | `CEN/MAN` | prototype geometry exhibited; closure-wide realization proof absent |
| 40 | reader/false-anchor/seed firewall | `LOC/PJ2/A9` | `SEALED` process restriction |

The high-level ledger has forty rows.  Because `ExtSrc` is itself the common
unbuilt construction, its row-level pieces are expanded here; none is hidden
inside the name `ExtSrc`:

| Subpiece | Exact type | Status |
|---|---|---|
| `X1` | `I_N:=im(qtilde_N)`; `Rel_N:=closure(ker(qtilde_N))` | `SEALED` definitions |
| `X2` | `I_N^q`: `I_N` with quotient topology; `I_N^R5`: `I_N` with inherited subspace topology | `SEALED` distinct carriers |
| `X3` | `Q_N^core:E_N->>I_N^q` | `SEALED` corestriction |
| `X4` | `beta_N:O_prof,N->I_N^q`, `Q_N^core=beta_N Q_N^quot` | `PART-PROVABLE` under stated kernel equality/topology |
| `X5` | `kappa_N:I_N^q->I_N^R5`, underlying-set identity | `REQUIRED` continuity/embedding certificate |
| `X6` | `jmath_N:I_N^R5->O_R5,N` | `SEALED` physical inclusion |
| `X7` | `jmath_N kappa_N beta_N Q_N^quot=qtilde_N` | `REQUIRED` as a continuous physical profile path |
| `X8` | `O_R5,N^src subset O_R5,N`, `I_N^R5 subset O_R5,N^src` | `REQUIRED` source scope |
| `X9` | `Sbar_N:O_prof,N->D_N^ker`, `Sbar_N(Q_N^quot z)=S_N(qtilde_N z)` | automatic after an `S_N` member |
| `X10` | `Res_(w,a)^resp:O_w^resp->O_a^resp`, continuous linear and `K`-independent | `REQUIRED` addressed restriction |
| `X11` | `res_(w,a)^supp:Supp_w->Supp_a` with support inclusion | `REQUIRED` |
| `X12` | `QuotResCert_(w,a)`: `Res_(w,a)^resp` quotient onto its image | `REQUIRED` |
| `X13` | `S_(w->N,K):O_w^resp->D_N^ker` | `REQUIRED` full response source map |
| `X14` | `T_(w->N,K)^D:D_w^ker->D_N^ker` | `REQUIRED` physical kernel transport |
| `X15` | `t_(w->N,K)^supp:Supp_w^ker->Supp_N^ker` | `REQUIRED` support transport |
| `X16` | `Ker_(a,K)^im:im(Res_(w,a)^resp)->D_N^ker` | `REQUIRED` quotient-factor map |
| `X17` | `Ker_(a,K)^ker:O_a^resp->D_N^ker` extending `Ker_(a,K)^im` | `REQUIRED` addressed full-domain extension |
| `X18` | `Ker_(a,K):=corestr_(D_N^Loc)(Ker_(a,K)^ker):O_a^resp->D_N^Loc` | `REQUIRED` image certificate |
| `X19` | `eta_f^K:Ker_N^Q408->Ker_M^Q408` with represented restriction `eta_f^(K,rep):Ker_N^rep->Ker_M^rep`; `tau_f^E,tau_f^src,tau_f^prof,tau_f^D,tau_f^Op,tau_f^supp,tau_(f,w)^D,tau_(f,w)^supp,tau_f^resp,tau_f^a,tau_f^K` with domains in `(SPEC-12b0)` | `REQUIRED` rank-preserving family; kernel transport is distinct from parameter transport `tau_f^K` |
| `X20` | `tau_f^Loc:D_N^Loc->D_M^Loc` and `tau_f^Loc Ker_(a,K)=Ker_(f.a,f.K)tau_f^a` | `REQUIRED` corestricted transport |
| `X21` | `rho_h^src:O_R5,M^(src,old)->O_R5,N^src`; `rho_h^D:D_M^(ker,old)->D_N^ker` | `REQUIRED` cycle-creating old-image descent |
| `X22` | `rho_h^a:O_(a_M^old)^resp->O_(a_N)^resp`; `rho_h^K:Kpar_M^old->Kpar_N` | `REQUIRED` address/parameter descent |
| `X23` | `rho_h^Loc:D_M^(Loc,old)->D_N^Loc` | `REQUIRED`; no new-cycle extension |
| `X24` | identity, composition, support, image, parameter, common-refinement, and old-image coherence squares for `X10`--`X23` | `REQUIRED`; equations are displayed in `(SPEC-12a')`--`(SPEC-12b5)` |

The H-tail provenance pieces used in `(S25c)`--`(S25g)` are also explicit:

| Subpiece | Exact type | Status |
|---|---|---|
| `H0` | `FullJ7_N=(FullKerCert_N,iota_N^(Q408,Loc),Loc_N^(phys,Q408),Loc_N^(C,Q408))` | `[YOURS — J7/LOC factor-through-DLoc seam]`; H-only `TYPE-U`, not consumed by HOL |
| `H1` | `sh_N:Sym_N^loc->Shadow_N^fin`; `j_f^Shadow:Shadow_N^fin->Shadow_M^fin` | `REQUIRED` shadow fidelity |
| `H2` | `sigma_field,N:Sym_N^loc->FieldSym_N`; `j_f^Field:FieldSym_N->FieldSym_M` | `REQUIRED` field-symbol fidelity |
| `H3` | `LW_2,N:D_N^Loc->FieldSym_N` | `REQUIRED` local Ward/source map |
| `H4` | `R_N^D:D_N^Loc->D_Nbar^Loc`; `R_N^Sym:Sym_N^loc->Sym_Nbar^loc` | `REQUIRED` reality/orientation transport |
| `H5` | `U_f^D:D_N^Loc->D_M^Loc`; `U_f^Sym:Sym_N^loc->Sym_M^loc` | `REQUIRED` unit transport |
| `H6` | `z_f^D:D_N^Loc->D_M^Loc`; `z_f^Sym:Sym_N^loc->Sym_M^loc` | `REQUIRED` zero extension |
| `H7` | `Supp_N^D:D_N^Loc->P(SuppCarrier_N^D)`; `Supp_N^Sym:Sym_N^loc->P(SuppCarrier_N^Sym)`; `Delta_N^*:P(SuppCarrier_N^D)->P(SuppCarrier_N^Sym)` | `REQUIRED` support control |
| `H8` | `Loc_(N1,N2)^phys:D_N1^Loc times D_N2^Loc->Sym_N1^loc times Sym_N2^loc` | `REQUIRED` ordered batching |
| `H9` | `j_f^prof:=tau_f^prof:O_prof,N->O_prof,M`; `etahat_f:=tau_f^Loc:D_N^Loc->D_M^Loc` | `REQUIRED` ExtSrc/H-tail consolidation seam |

No row silently promotes a declaration to a member.  Seven joint tests follow
in dependency order; a stop at one joint does not suppress the tests of the
others.

## 3. H2 — joint-by-joint inhabitance attempt

### 3.1 Joint A — source extension and addressed `Ker`

[YOURS — attempted map] The only source agreement respecting the existing
cycle branch and the `L_T` branch is `(SPEC-11b)`:

```text
S_N(qtilde_N(a,b,C))
 :=a Sch_N(M_N^loc)
  +iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)).     (JA-1)

Kraw_N:=S_N qtilde_N:E_N->D_N^ker.                 (JA-1a)
```

The quotient path and the unaddressed response relabelling are distinct:

```text
E_N --Q_N^quot--> O_prof,N
    --beta_N?--> I_N^q --kappa_N?--> I_N^R5
    --jmath_N--> O_R5,N,

O_w^resp:=O_R5,w,
iota_w^resp:O_R5,w->O_w^resp,

Res_(w,a)^resp:O_w^resp->O_a^resp.                (JA-2)
```

Question marks in `(JA-2)` are the exact equality/topology gates in
`(SPEC-10a')`; the addressed `Res` arrow is a separate future member.

But formation of `(JA-1)` requires all summands to land in one named
topological `D_N^ker`, then requires

```text
im(Kraw_N)=im(Sbar_N)=D_N^Loc,
Kernbarhat_N:O_prof,N->D_N^Loc,

S_(w->N,K):O_w^resp->D_N^ker,
Ker_(a,K)^im:im(Res_(w,a)^resp)->D_N^ker,
Ker_(a,K)^ker:O_a^resp->D_N^ker,
Ker_(a,K):O_a^resp -> D_N^Loc.                    (JA-3)
```

with the actual support/address laws.  No sealed member supplies the common
carrier injection, the `L_T` source extension, its image/topology theorem, or
the response restriction/factor/extension chain.

```text
JOINT_A = STOPPED
gap = SOURCE_EXTENSION_INHABITANCE
MACHINERY-APPEAL(JA) = one common Dker/S_N/Sch/Kraw image,
                       topology certificate, and addressed Res/Ker chain
```

This stop is narrower than the old J-I stop: quotient descent itself is not
counted twice.

### 3.2 Joint B — physical kernel eligibility for the symbol map

Assuming `(JA-1)` only for the purpose of an independent joint test, the
desired first physical step is

```text
K in D_N^Loc
 -> K in Sch_N(Op_N^(loc,2))
 -> sigma_2^ker(K) in Sigma_N^(2),Ward.            (JB-1)
```

The actual Q-408 stock instead supplies path-bilocal kernels supported on
unions of named paths.  A properly supported classical pseudodifferential
operator of order at most two requires the appropriate conormal/wavefront,
order, density, and principal-symbol structure; its kernel may also carry a
smooth off-diagonal part.  The path-bilocal description does not by itself
exclude an abstract realization, but it supplies none of those eligibility
certificates.  Ward homogeneity supplies no replacement.  No sealed
proper-support, wavefront/conormal, order/density, refinement, or realization
theorem proves

```text
D_N^Loc subset Sch_N(Op_N^(loc,2)).                (JB-2)
```

Therefore `sigma_2^ker(K)` is not formed on the common Q-408 image merely by
substitution into its declared signature.

```text
JOINT_B = STOPPED
gap = PHYSICAL_KERNEL_TO_LOCAL_OPERATOR_ELIGIBILITY
MACHINERY-APPEAL(JB) = actual wavefront/diagonal/refinement realization theorem
```

### 3.3 Joint C — symbol localization and contact quotient

Assuming the premise of `(JB-1)` only for this test, the candidate formula is

```text
Loc_N^phys(K)
 :=[sigma_2^ker(K)]_(Ctc_N^sym).                   (JC-1)
```

For `(JC-1)` to be a continuous linear map, stock must construct the closed
Ward class, the closed contact/boundary ideal, the quotient topology, and
compatibility of `sigma_2^ker` with that ideal.  The corpus declares those
objects as the physical target contract but supplies no member satisfying all
four obligations.  The finite `sigma_N^fin` is not a proof of `(JC-1)` because
its declared carrier and action are not the physical quotient.

```text
JOINT_C = STOPPED
gap = PHYSICAL_LOC_AND_CONTACT_QUOTIENT_MEMBER
MACHINERY-APPEAL(JC) = closed Ward/contact symbol calculus on the actual image
```

### 3.4 Joint HJ7 — H-only full Q-408 domain

This joint is not part of common `ExtSrc_N` and is not consumed by HOL.  The
H/J7 package requires

```text
FullKerCert_N:
 iota_N^Q408(Ker_N^Q408) subset D_N^Loc,

iota_N^(Q408,Loc):Ker_N^Q408->D_N^Loc,

Loc_N^(C,Q408)
 :=Loc_N^C iota_N^(Q408,Loc)
 :Ker_N^Q408->C_N^k,                                (JH-1)
```

and, on an actual rank-preserving arrow, `(S23-full-J7)`.  Stock reaches only
the represented candidate subscope

```text
iota_N^Q408 Kernbar_N^cyc(O_N^cyc) subset D_N^Loc;   (JH-2)
```

it supplies neither `FullKerCert_N` nor the full-domain base square.

```text
JOINT_HJ7 = STOPPED
gap = FULL_Q408_IMAGE_AND_FULL_DOMAIN_S23
MACHINERY-APPEAL(JH) = H-only FullJ7_N member
```

No conclusion that `FullJ7_N` is abstractly empty is drawn.

### 3.5 Joint D — symbol-to-cochain realization

DoR-019 permits the Riesz map on the already formed cochain carrier:

```text
R_C,N:C_N^k -> (C_N^k)^*.                         (JD-1)
```

The most economical attempted realization has the conditional form

```text
Phitilde_N:Sigma_N^(2),Ward -> (C_N^k)^*,
Ctc_N^sym subset ker(Phitilde_N),
im(Phitilde_N) subset R_C,N(ker(E_C,N)),

Phi_N:Sym_N^loc -> (C_N^k)^*,
Phi_N([s]_(Ctc_N^sym)):=Phitilde_N(s),

iotatilde_N^H:=R_C,N^(-1) compose Phitilde_N,
iota_N^H:=R_C,N^(-1) compose Phi_N,

E_C,N R_C,N^(-1) Phitilde_N=0,
E_C,N R_C,N^(-1) Phi_N=0.                          (JD-2)
```

To define `Phi_N`, an actual cell rule is needed.  For each oriented `k`-cell
`e`, let `delta_e in C_N^k` denote its basis cochain.  A candidate must supply
the sealed rule on the prequotient

```text
Phitilde_N(s)(delta_e)
 :=incidence/integration/evaluation of s on the named k-cell e,   (JD-3)

Phitilde_N(s)(sum_e a_e delta_e)
 :=sum_e a_e Phitilde_N(s)(delta_e).                    (JD-3a)
```

and must prove orientation covariance, presentation independence,
continuity, and `Ctc_N^sym subset ker(Phitilde_N)`; the latter two clauses
make `Phi_N` and `iota_N^H` presentation-independent quotient maps.
The confinement clause is also displayed on representatives:

```text
E_C,N iota_N^H([s])
 =E_C,N R_C,N^(-1) Phi_N([s])
 =E_C,N R_C,N^(-1) Phitilde_N(s)
 =0.                                                (JD-3c)
```

The combined `A1+KER` actual path/current correspondence has the incompatible
input/output shape

```text
exhibited path gamma
 -> current J_gamma and supp(J_gamma),

(J_gamma, already supplied connection/test 1-form A)
 -> <J_gamma,A>.                                         (JD-3b)
```

It does not take `[s] in Sym_N^loc`—an order-two Ward symbol class with a
cotangent variable and quantization data—to a functional on `C_N^k`.
Therefore A1 supplies physical geometry for testing `(JD-3)` but not the
cross-sector arrow itself.

No sealed authority supplies `(JD-3)`, proves independence from presentation,
or proves contact-ideal annihilation.  Metric positivity cannot manufacture
`Phitilde_N/Phi_N`; doing so would violate DoR-019's
no-implicit-cross-sector rule.
Likewise, `P_H,N:C_N^k->C_N^k` begins after `(JD-2)` and cannot serve as the
missing arrow.

Thus the required descent chain cannot be displayed beyond question marks:

```text
s in Sigma_N^(2),Ward
 -> [s] in Sym_N^loc
 -?-> iota_N^H([s]) in C_N^k,

s-s' in Ctc_N^sym
 -?-> iotatilde_N^H(s-s')=0.                     (JD-4)
```

```text
JOINT_D = STOPPED
gap = ACTUAL_SYMBOL_TO_COCHAIN_INCIDENCE_RULE
MACHINERY-APPEAL(JD) = Phitilde_N/Phi_N plus contact descent,
                       E_C confinement, and continuity
```

This is the load-bearing J-II obstruction.  It blocks sealed-stock
inhabitance even if Joints A--C were supplied externally.

### 3.6 Joint E — nonidentity transport and F3 bases

Assume only for this test that the ExtSrc transports, `Loc^phys`, and `iota^H`
have abstract members.
For the actual reciprocal rank-preserving generator `u:N_RL->M_RL`, the
two source-side `(S23)` bases are independently required:

```text
etahat_u iota_NRL^rep
 ?=iota_MRL^rep eta_u^(K,rep),

etahat_u Kernbarhat_NRL
 ?=Kernbarhat_MRL j_u^prof.                         (JE-0)
```

The H-tail display is

```text
Loc_MRL^C etahat_u
 =iota_MRL^H Loc_MRL^phys etahat_u
 ?=iota_MRL^H j_u^Sym Loc_NRL^phys
 ?=j_u^C iota_NRL^H Loc_NRL^phys
 =j_u^C Loc_NRL^C.                                 (JE-1)
```

The question marks in `(JE-0)` are the two `(S23)` equations; those in
`(JE-1)` are `(S24)` and `(S25)` on `u`.  For the actual nontrivial `q=2`
refinement `r_nm:N_n->N_m` (the Γ build also calls it `s_nm`), the two
source-side bases are

```text
etahat_(r_nm) iota_Nn^rep
 ?=iota_Nm^rep eta_(r_nm)^(K,rep),

etahat_(r_nm) Kernbarhat_Nn
 ?=Kernbarhat_Nm j_(r_nm)^prof,                    (JE-1a)
```

and the distinct H-tail display is

```text
Loc_Nm^C etahat_(r_nm)
 =iota_Nm^H Loc_Nm^phys etahat_(r_nm)
 ?=iota_Nm^H j_(r_nm)^Sym Loc_Nn^phys
 ?=j_(r_nm)^C iota_Nn^H Loc_Nn^phys
 =j_(r_nm)^C Loc_Nn^C.                             (JE-2)
```

The fixed-stage `d/delta/P_H` intertwiners fill none of these eight question
marks.  No sealed proof instantiates `(S23)/(S24)/(S25)` on either
nonidentity generator; hence the induction has no base beyond identities.

```text
JOINT_E = STOPPED
gap = S23_S24_S25_NONIDENTITY_BASES
MACHINERY-APPEAL(JE) = actual reciprocal and q=2 source/symbol/cochain transport proofs
```

### 3.7 Joint F — A9 support/address compatibility

The built seams provide the orbit law `(SPEC-15)`, but the A9 source gates
require actual maps on `D_N^Loc`:

```text
supp_D,N:D_N^Loc -> P(R_N^rec),
q_D,N^addr:D_N^Loc -> {0_D} disjoint-union S_D,N^addr,
r_N^D:D_N^(Loc,odd) -> D_N^(Loc,odd)               (JF-1)
```

where the last map is required only on each admitted G5 sign-odd sector,
with the forced zero extension in `(SPEC-17)`.  The full G4-D laws are
`(SPEC-16)/(SPEC-16a)`; the full G5 compatibility is `(SPEC-17a)`.

The repaired reversal square has the conditional form

```text
q_D,N^addr(r_N^D x)=q_D,N^addr(x)^-,

x=0:
q_D,N^addr(r_N^D0)=q_D,N^addr(0)=0_D=0_D^-.       (JF-2)
```

`JF-2` is now well typed, but no member of `(JF-1)` is produced.  `FULL-G4`
and `G2-N` remain separate even after `(JF-1)` is inhabited.

```text
JOINT_F = STOPPED
gap = G4_D_G5_SOURCE_MAPS
MACHINERY-APPEAL(JF) = actual support stratum and source reversal members
```

Joint F is an A9 package-gate stop.  It is not counted as an H-tail J-II
realization condition.

### 3.8 Finite-shadow regression: why it cannot bypass the joints

The finite declaration gives only

```text
Loc_N^fin(K)=sigma_N^fin(K).                       (JR-1)
```

Using `(JR-1)` as the desired H/J7 member requires the displayed
identifications

```text
domain(sigma_N^fin)=Ker_N^Q408,
codomain(sigma_N^fin)=C_N^k,

FullKerCert_N:
 iota_N^Q408(Ker_N^Q408) subset D_N^Loc,

sigma_N^fin(K)
 =Loc_N^(C,Q408)(K)
 =Loc_N^C(iota_N^(Q408,Loc)(K))
 =iota_N^H([sigma_2^ker(iota_N^(Q408,Loc)(K))]).    (JR-2)
```

plus linearity, contact descent, and all nonidentity `(S23)`--`(S25)` bases.
None is
sealed.  Its available pullback relation is finite/contravariant data, not an
actual covariant kernel-to-cochain realization.  Thus `(JR-1)` is retained as
a future regression target and not promoted to a constructor.

## 4. H4 — per-declaration obstruction display

| Declaring artifact | What is actually written | First missing formation step | Exact verdict |
|---|---|---|---|
| `FIN` (`f074ca24...`) | `Loc_N^fin(K):=sigma_N^fin(K)` and immediate use inside `P_H` | first, `Kernbar_N Q_N(L_T,N)` is unformed because sealed `Kernbar_N^cyc` excludes the `L_T` summand; second, no independently constructed `sigma_N^fin` signature/action/linearity places a repaired source image in `C_N^k` | declaration cannot inhabit `SPEC-1` or `SPEC-7` |
| `PJ2` (`3fd4b924...`) | `Loc_N^shadow:=sigma_N^fin` with finite Ward/restriction data | `Ker_N^Q408->Sym_N^loc` explicitly marked `NOT_BUILT / TYPE-U` | shadow stops before Joints B--D |
| `J7` (`9c74ed3d...`) | exact declared signature `Ker_N^Q408->C_N^k` | no `FullKerCert_N` placing the whole declared domain in `D_N^Loc`; no formula, contact descent, physical quotient action, or actual `(S23)/(S24)/(S25)` bases | signature makes composites parse but does not construct a full-domain member |

`NAT` is not a fourth construction.  It displays the missing reciprocal and
refinement equalities rather than proving them.  There is no sealed
contradiction among the required clauses, so this board proves only:

```text
no member is derivable from the cited sealed stock;                 (OB-1)

not proved:
family(Pkg_N^H)=emptyset;
family(Pkg_N^Hol)=emptyset.                                        (OB-2)
```

## 5. Mandatory SURFACE ANCHOR

### 5.1 Pass criterion

A declaration or commuting square in abstract carriers is not a surface
`PASS`.  A `PASS` below requires the displayed map or equality on the named
actual stage, cycle, arrow, or certified diamond.  Where an actual manifest is
absent, totality is not inferred.

### 5.2 Named actual objects and required displays

| Actual surface anchor | Minimum display required for `PASS` | Current result |
|---|---|---|
| Reciprocal stage `RL`; primitive `gamma_RL`, reverse `-gamma_RL`; `c_RL,-c_RL`; addresses `a_RL,a_RL^-` | form actual `kappa_T,RL`; display `Loc_RL^phys(kappa_T,RL)`; give the cell rule `(JD-3)`; display `Loc_RL^C(kappa_T,RL)`; carry both `E_C,RL c_RL=0` and `E_C,RL c_RL=c_RL` without selection | `STOPPED` at Joints A--D |
| Actual Q-408 reciprocal kernel | place that kernel in `D_RL^Loc`, prove `(JB-2)`, and display its physical image | `STOPPED`; finite shadow is regression only |
| J7 full `Ker_N^Q408` carrier | display `FullKerCert_N`, the corestriction `iota_N^(Q408,Loc)`, and the full `(S23)` square | `STOPPED`; only the represented subscope is presently typed inside the candidate chain |
| Reciprocal generator `u:N_RL->M_RL` | display both `(S23)` bases and then `(S24)/(S25)` in `(JE-0)/(JE-1)` | `STOPPED` at four question marks |
| Nontrivial `q=2` refinement `r_nm:N_n->N_m` | display both `(S23)` bases and then `(S24)/(S25)` in `(JE-1a)/(JE-2)` | `STOPPED` at four question marks |
| `D-sub` | stages `G_0^sub,G_s,G_t,G_st`; arrows `r_0s,r_0t,r_sst,r_tst` | geometry named; realization/naturality unproved |
| `D-disj` | stages `G_0^disj,G_U^disj,G_V^disj,G_UV^disj`; arrows `r_0U,r_0V,r_UV,r_VU`; downward old image only | geometry named; no upward new-cycle map admitted |
| `D-contact` | stages `G_0^ct,G_U^ct,G_V^ct,G_UV^ct`; arrows `r_0U^ct,r_0V^ct,r_UV^ct,r_VU^ct`; endpoint contact retained | geometry named; contact realization unproved |
| Generated `D_cert` closure | prove covariance-orbit, identity, transpose, certified whiskering, and finite-pasting induction from the three schema generators | no closure-wide realization induction exists |
| General `a_cc:N->M` | display `(SPEC-14)` on named old-image subspaces | `PARTIAL` type only; no universal member |
| Asymmetric clause-(vi) witness `a_xi:G->G_a` | test only the exhibited near leg | near leg retained; far `D_CX` leg unbuilt |
| Mixed `D_RC` and consumer-only arrows | exact new exhibits and realization maps | unbuilt; no total `I_F` claim |
| A9 addressed support | actual `supp_D,N`, `q_D,N^addr`, primitive orbit, and `r_N^D` on a named support | types stated; members absent |
| A8/R9 | one common physical cell on which both typed returns exist independently | no common cell is presently formed |

The certified family is the generated closure

```text
D_cert
 :=Cl_(covariance orbit, identity, transpose,
       whiskering by certified R_sub/C arrows,
       finite pasting)
     {D_RsubRsub,D_CC^disjoint,D_CC^contact}.       (SA-1)
```

`D-sub`, `D-disj`, and `D-contact` are the executed prototypes of the three
generating schemas, not the whole closure.  No realization proof performs the
closure induction in `(SA-1)`.  `MAN` also leaves the mixed, asymmetric
far-leg, and consumer-arrow manifests unresolved.  Therefore prototype checks
establish neither closure-wide naturality nor total `I_F` naturality.

### 5.3 Geometry versus organizing rails

**Physical geometry already present:** actual finite stages and incidences;
paths, currents, bundles, supports, cycles, and refinements; the Q-408
path-bilocal kernel; the actual local Maxwell operator; DoR-019's metric on
`C_N^k`; `d_N,delta_N,Delta_N,P_H,N,E_C,N`; primitive harmonic
representatives; orientation reversal; and the three exhibited diamonds.

**Organizing rails awaiting members:** `D_N^ker`, `D_N^Loc`, `Sym_N^loc`, the
contact quotient, `Sch_N`, `S_N/Kraw_N`, `Loc_N^phys`, `iotatilde_N^H`,
`iota_N^H`, their transports, and A9's source support maps.  More explicitly:

| New construct | Geometry it must organize | Present status |
|---|---|---|
| `ExtSrc_N` | actual `L_T` and Q-408 sources, response restrictions, supports, and surface-arrow transports | bookkeeping rail; no member |
| `Rlz_N^H` | physical symbol, actual cochain cells, contact/Hodge carrier | route rail; no `Phi_N/iota_N^H` member |
| `Rlz_N^Hol` | actual source support and loop-cycle/holonomy data | adopted A9 route rail; gates uninhabited |
| `JII_Rlz_N` | the paired H/HOL solution families over a common source type | bookkeeping rail; either route family may be empty |
| `Phi_N` in `(JD-2)` | oriented-cell evaluation `(JD-3)` | proposed rail until the actual cell rule exists |
| `0_D^-:=0_D` | zero-preserving source reversal at the basepoint | forced type-level rail condition; no surface `PASS` |

Orbit carriers organize covariance but do not select a representative.

The load-bearing geometry gap is `(JD-3)`: no actual incidence/integration
rule converts a physical symbol into cochain coefficients.  The metric and
finite shadow do not create that rule.

### 5.4 R9-lesson quantification check

The repaired falsifier is carried exactly as

```text
for every common physical cell c
on which both typed period-return constructions are formed,

Bbar^H(c)=Bbar^Hol(c).                              (R9-V002)
```

There is no independent quantifier over `Triv_[a]`, over a HOL scalarization
family, or over their Cartesian product; no representative is selected and no
output-dependent matching relation is added.  For representatives `T,T'` in
the same carried orbit, the covariance clause is

```text
B_(c,T')^H
 =g_(T',T)B_(c,T)^H
 =g_(T',T)B_(c,T)^Hol
 =B_(c,T')^Hol.                                    (R9-1)
```

Both sides move together, so `(R9-V002)` does not force a zero period.  In the
present obstructed state neither route period-return construction is formed,
so there is no `c` in the quantifier domain of `(R9-V002)`.  A8 is
`UNEXECUTABLE/PENDING`: it is neither passed, failed, nor vacuously true.
`A8_COMMON_CARRIER_DEFECT(c)` applies only when both upstream composites are
claimed formed on a particular `c` but at least one lacks its required return
carrier typing.  The realization family is not defined by forcing R9
agreement.

## 6. Mandatory dependency re-audit

Every argument depending on a clause stated here was re-walked after the
common-prefix/route-tail correction and the Q-520 basepoint repair.

| Root clause or datum | Re-audited dependents | Result |
|---|---|---|
| honest family and `(STRUCK)` | contact descent, seed subject, coefficient basis | struck range clause remains absent; neither seed outcome is membership |
| full `(SPEC-9)` / S21 | quotient descent, `E_C` confinement, both A7 branches | full commutator retained; A7 branch not selected |
| `(SPEC-10)`--`(SPEC-12)` | J-I, `kappa_T,N`, addressed `Ker`, A9 ExtSrc | all conditional after Joint A stop |
| `FullJ7_N` / `FullKerCert_N` | J7 full declared domain, full S23, finite-shadow regression `(JR-2)` | H-only Joint HJ7 `TYPE-U`; represented-subscope equations do not discharge it and HOL does not consume it |
| `(SPEC-3)/(SPEC-4)/(SPEC-4a)` | HOL formation, A9 gates, common-cell formation, shared source transport | route tails remain distinct; transport seam required and uninhabited |
| `(SPEC-5)`--`(SPEC-8)` | J-II, Ward/locality, finite-shadow regression | all conditional after Joints B/C; finite shadow not promoted |
| `(JD-2)/(JD-3)` | Hodge landing, period subject, contact annihilation | unformed after Joint D |
| S22/S23 | identity/composition and actual source transport | identity formalism survives; nonidentity members absent |
| S23/S24/S25 | F3, J-IV, common-refinement induction | no source/symbol/cochain base proof inferred from `d/delta/P_H` naturality |
| `(SPEC-14)` | A6 and cycle-creating diamonds | old-image/downward scope retained; no upward lift |
| support/reality/batching clauses | certified diamonds and consumer arrows | claims limited to the three executed prototypes; generated-closure claims remain conditional on the absent `(SA-1)` induction |
| `D_cert` / `(SA-1)` | generator displays, closure induction, full `I_F` boundary | three prototypes executed; closure induction absent; full manifest unresolved |
| `(SPEC-16)/(SPEC-17)` | A9 `G4-D/G5`, sign transport, fixed stratum | zero-point expression now typed; no gate inhabited |
| `(SPEC-15)` | orientation covariance, H scalar-return family, representative independence, R9 | full orbit retained; no scalar-return member or common cell formed |
| `FULL-G4` | hidden-counterterm test Z13 | remains independent and uninhabited |
| reader freedom | every construction and consequence | clean; false anchor never consumed |
| any joint stop | all downstream equations | downgraded to conditional/unformed, never inherited |
| common-prefix/route split `(L2)` | H/HOL comparison | no claim that HOL consumes `iota_N^H`; common debt is ExtSrc/address only |
| R9 pending/defect distinction | A8 execution and first common-cell falsifier | no quantifier-domain element now; defect label reserved for an alleged formed cell with bad return typing |

Three permanent regressions were rerun:

1. the struck range condition was not restored;
2. `(S24)/(S25)` were not inferred from Hodge intertwiners;
3. `sigma_N^fin` was not promoted from finite datum/shadow to physical
   realization.

The Q-520 micro-repair changes only the definition domain of the involution at
`0_D`.  Rechecking every dependent sign calculation shows:

```text
x=0      -> fixed basepoint 0_D^- = 0_D;
x!=0     -> q_D,N^addr(x) in S_D,N^addr and s^-!=s;
```

so the nonzero fixed-point-freeness and sign-collapse regression are
unchanged.  No new `Xi_N`, support map, or realization member follows.

## 7. H3/H4 — consequence board under the actual obstruction

### 7.1 Gamma and F3

```text
Gamma^H Joint I   = STOPPED (ExtSrc/source-extension member absent)
Gamma^H Joint II  = STOPPED (physical Loc and symbol-to-cochain member absent)
Gamma^H Joint III = addressed orientation orbit built,
                    but scalar return unformed because the upstream map stops
Gamma^H Joint IV  = STOPPED (S23/S24/S25 actual bases absent)
H-only FullJ7 seam = STOPPED (full Q-408 image and full-domain S23 absent)

F3 reciprocal base = UNPROVED
F3 q=2 base          = UNPROVED
F3 general induction = CANNOT START
```

No previously closed seam is revoked; it simply has no formed upstream input.

### 7.2 S22--S25, A6, and A9

```text
S22--S25 realization package = SPECIFIED, NOT INHABITED
S22--S25 actual reciprocal/refinement squares = UNFORMED

A6 rank-preserving/old-image law = retained on its adopted scope
A6 target-new-cycle lift          = absent by law

A9 interface = ADOPTED
A9 ExtSrc     = OPEN
A9 G4-D       = OPEN
A9 G5         = OPEN
A9 FULL-G4    = OPEN
A9 G2-N       = OPEN
```

The obstruction splits without double-counting:

```text
common source = Joint A and Joint E's represented S23 bases stopped;
H tail        = Joints B--D, Joint HJ7, and Joint E's S24/S25 bases stopped;
A9-only gates = Joint F open, plus FULL-G4 and G2-N open.
```

The Q-520 basepoint repair makes a future `G5` square well typed; it does not
inhabit it.

### 7.3 Seed, coefficient face, and identification falsifier

Because `Loc_N^C(kappa_T,N)` is not formed from sealed stock, the pairing

```text
<P_H,N Loc_N^C(kappa_T,N),c_N>_N                 (C-1)
```

is not presently a well-posed constructed subject.  Consequently no seed
nonvanishing, coefficient-face inhabitance, normalization, period value, or
reader reconstruction is obtained here.  No R9 common physical cell exists
until both route returns are independently formed.  R9 remains armed and
non-vacuous for the first such cell.

### 7.4 Guard/axiom carriage under obstruction

No adoption is performed here.  The exact carriage handed to a future guard
or axiom text is

```text
GUARD/AXIOM CARRIAGE =
  retain family(Pkg_N^H) and family(Pkg_N^Hol)
    as potentially empty typed families;
  retain A6 only on its projected old-image scope;
  retain A9 and each open gate without inhabitance;
  keep Gamma^H unformed;
  keep R9 PENDING until one common physical cell exists;
  add no route, branch, address, or member selection.              (CARRY-1)
```

### 7.5 Typed alternatives if the record chooses to extend stock

The following are analysis-only alternatives; none is selected or adopted.

| Alternative | New premise/member required | Immediate falsifier or void condition |
|---|---|---|
| microlocal/refinement realization | a theorem placing the actual Q-408 image in the properly-supported classical order-at-most-two domain with its conormal/wavefront, density, and symbol data, then constructing `(JD-3)` | one actual reciprocal/refinement kernel outside the claimed domain or failure of an S23/S24/S25 square |
| external-field localization family | an independently physical family `Loc^phys` with named topology, support, and contact quotient | reader dependence, hidden harmonic/holonomy counterterm, or failure on an actual support/reversal test |
| spectral/cochain construction | explicit `Phitilde_N/Phi_N` functionals and contact-annihilation proof on every actual stage | presentation dependence, contact ideal not in the kernel, or failure of actual-arrow transport |
| operator-space J2 retyping | move the reader contract to a genuinely sealed operator/symbol carrier | mismatch with A6/J7 codomains or inability to recover a lawful period carrier |
| retain A6 post-scope | leave full physical J2 and H route unformed while carrying A9 alternatives | no defect; explicit incompleteness is the price |

Each alternative must pass the Surface Anchor and R9 quantification checks; a
formal rail alone cannot count as an inhabitant.

## 8. H5 — battery

### 8.1 F_PLDEC and false-anchor regression

The only allowed dependency direction used in the construction attempt is

```text
actual paths/currents/bundle and support
 + DoR-019 metric
 + Q-408 kernels
 + actual local Maxwell operator
 + fixed-stage contact/Hodge data
 + actual incidence/refinement structure
 -> source realization
 -> physical symbol
 -> actual cochain realization.                  (BAT-1)
```

The following inputs were absent from every defining line:

```text
reader_f, chi, reader T, desired coefficient,
pi_Mx compose Loc compose Kernbar compose Q = 1,
S28 or its negation, Xi/HOL output, R9 equality,
threshold result, fixed point, end test, measured constants.       (BAT-2)
```

In particular, the false anchor is cited only as forbidden material.  The
failure to build `(JD-3)` was not patched by demanding its downstream answer.

### 8.2 Anti-tuning and no-selection ledger

| Attack | Check | Result |
|---|---|---|
| choose `E_C` branch to open a route | both A7 branches carried | clean |
| choose address receiving `+` | whole addressed orbit carried with `(SPEC-15)` | clean |
| choose a realization member | no member bound; family remains uninhabited from stock | clean |
| narrow the domain until `sigma_2^ker` applies | `(JB-2)` kept as a void-capable obligation | clean |
| replace J7's full Q-408 domain by `Ker_N^rep` | H-only `FullJ7_N/FullKerCert_N` remains a separate required clause; `Ker_N^rep` is used only for named surface-base tests | clean |
| use finite shadow as constructor | blocked by `(JR-2)` regression | clean |
| add hidden harmonic/holonomy counterterm | none; `FULL-G4` remains armed | clean |
| delete difficult arrow classes | mixed, asymmetric far-leg, and consumer debts retained | clean |
| create upward new-cycle lift | only `(SPEC-14)` downward old-image equations used | clean |
| force the two A8 routes to agree by definition | R9 retained solely as later falsifier | clean |
| tune topology, smoothing, boundary condition, frame, gauge, or basis | none selected | clean |
| infer nonvanishing from cycle presence | no seed outcome inferred | clean |
| use numeric or measured information | none used | clean |

### 8.3 Fence and machinery-appeal board

```text
MACHINERY-APPEAL =
  Joint A: common source-extension/response member;
  Joint HJ7: H-only FullJ7_N/full-Q408-domain member;
  Joint B: actual microlocal eligibility theorem;
  Joint C: closed Ward/contact quotient member;
  Joint D: symbol-to-cochain incidence functional plus contact confinement;
  Joint E: actual nonidentity S23/S24/S25 proofs;
  Joint F: separate A9 source support/reversal members.

FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

The fences did not prevent the negative stock determination.  Every open item
above is missing mathematical machinery or an uninhabited adopted gate.

### 8.4 Self verb audit

| Verb used | Display supporting it | Audit |
|---|---|---|
| `declares` for the three artifacts | `(D-FIN)`, `(D-PJ2)`, `(D-J7)` | clean; never upgraded to constructs |
| `consolidates/specifies` | `(SPEC-1)`--`(SPEC-17)` | clean; authored family, no inhabitance claim |
| `sealed` for fixed Hodge/geometry/orbit data | authority and piece ledgers | clean; scope stated |
| `stopped` for joint tests A--F plus HJ7 | each exact missing map/equality displayed | clean |
| `obstructed` | `(OB-1)` | clean; explicitly sealed-stock derivability only |
| `not proved empty` | `(OB-2)` | clean; no false no-go theorem |
| `adopted` for A9 | decision text plus open-gate board | clean; adoption not called inhabitance |
| `built` for addressed orientation orbit | `SEAM1/SEAM2`, retained as an orbit | clean; no representative selected |
| `unproved/unformed` downstream | consequence board traces each stopped input | clean |
| `repaired` at Q-520 | `(SPEC-17)` defines the formerly missing basepoint action | clean; no gate/member inferred |

No occurrence of “proves,” “constructs,” “inhabits,” or “closes” exceeds the
display immediately supporting it.

SPEC = CONSOLIDATED
REALIZATION = OBSTRUCTED (+sealed-stock TYPE-U: common-source Joint A source-extension/response member and Joint E represented-S23 bases; H-tail Joint B physical kernel eligibility, Joint C physical Loc/contact quotient, Joint HJ7 full-Q408-domain seam, Joint D actual symbol-to-cochain incidence/contact-confinement rule, and Joint E S24/S25 bases; separate A9 Joint F support/reversal gates remain open; per-declaration display in Section 4; abstract H/HOL family emptiness NOT proved)
CONSEQUENCES = stated
SURFACE_ANCHOR = present
VERB_AUDIT_SELF = CLEAN
