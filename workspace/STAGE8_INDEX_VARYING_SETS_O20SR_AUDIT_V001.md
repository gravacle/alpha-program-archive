# AUDIT — STAGE8 INDEX-VARYING ADMISSIBLE SETS (O20SR)

COMMISSION O20SR — INDEX-AUDIT — 2026-08-15
POSTURE: **DEFAULT-REFUTE.** Testimony carries zero weight. Every claim below was
re-derived at bytes from the sealed sources, never from the target's report of
them. Where the target and the bytes disagree, the bytes govern.

STATUS: **DETERMINATION ONLY.** Nothing is proposed, authored, adopted, or
reclassified here. No object is promoted. No class is changed.

FENCES CARRIED: `alpha_computed = false`; `proof_authorized = false`;
`kappa_record_computed = false`. No value, no number of physical import, no
measured-constant comparison. Index values, sweep hit counts, and vector-space
dimensions are displayed because they are the subject matter under audit.

---

## §0 — STEP 0: PROBE AND SEAL

```text
OUTPUT PATH PROBED ABSENT BEFORE ANY WRITE:
  STAGE8_INDEX_VARYING_SETS_O20SR_AUDIT_V001.md
  -> "No such file or directory".  ABSENT.  Proceeded.

TARGET, verified with shasum -a 256 -c EXECUTED FROM THE ARTIFACT'S OWN DIRECTORY
(/Users/bgm/MB Work/alpha-program-archive/workspace):
  STAGE8_INDEX_VARYING_SETS_O20SR_V001.md: OK
  c1b5f87a9b8b468f6f2c116d281167105975adc9300a72fb02fe728346de1bf3
  37791 bytes, 728 lines.
```

---

## §1 — SEALS I RE-VERIFIED (every consumed artifact, from its own directory)

```text
ALL FOUR of the target's consumed artifacts re-verified by me independently, with
shasum -a 256 -c run from the artifact's own directory, and the digests
RECOMPUTED and compared against the digests the target DISPLAYS in its §2:

K1  STAGE8_TYPING_RULE_CANDIDATE_V001.md        OK   618 lines / 40100 bytes
    74f9470189a72d0d214ce72153559e17f1dfb65abeb159afb30538f6774b954a
K2  STAGE8_ACT2_CARRIER_PACKAGE_S9AD_V001.md    OK   829 lines / 49196 bytes
    38239f93de8b3a720659beba62f52dc2899e507f0fca72b57d88053dd59a09d8
P1  STAGE8_GATE_SIGNATURE_O18SR_V001.md         OK   730 lines / 41514 bytes
    3bb7be90bdd47ade2b928e65bab29e0cd6b7951207136e2cbd85711bb5dd59b6
P2  STAGE8_GATE_SIGNATURE_O18SR_AUDIT_V001.md   OK   589 lines / 33208 bytes
    b3d73333ca4920e23dc50ff16a7bd56c016bf8b39afba3a3cc7aa8d85f84eb79

*** ALL FOUR DIGESTS MATCH THE TARGET'S §2 DISPLAY CHARACTER FOR CHARACTER, AND
ALL FOUR LINE/BYTE COUNTS MATCH. ***  The target's §2 is CONFIRMED at bytes.
K1's head status "PROPOSED_NOT_ADOPTED" is confirmed at K1 :1 / :4.
The target's "NEITHER K1 NOR K2 EXISTS IN ROOT (S)" is confirmed (§5 L4 below).
```

---

## §2 — HUNT (A): IS THE VARIATION DISPLAYED, OR ASSERTED AS DISPLAYED?

The kill I was sent to attempt: a family stated "at every index" whose content is
CONSTANT is a constant family, and calling it a variation is fatal. I demanded,
for each artifact, **two distinct index values with two distinct sets, both
quoted at bytes.** I re-quoted from the sources, never from the target.

### 2.1 K1 — the demand is MET at bytes

```text
SOURCE: STAGE8_TYPING_RULE_CANDIDATE_V001.md :152-161, re-read by me directly.
I confirm the target's §3.2 block is VERBATIM, character for character, and that
:152 is exactly "LEMMA 1 (exact)." and :161 exactly "(§8 N1)."

  INDEX VALUE 1   b_1(C_j) = 0
  SET, QUOTED     "if b_1(C_j) = 0 (tree) it is EMPTY — no nonzero conserved
                   cell-local write exists"
  INDEX VALUE 2   b_1(C_j) = 1
  SET, QUOTED     "If b_1(C_j) = 1 the admissible set is the circuit line
                   Q·gamma_j(C_j) \ {0}  (total-nonzero)"

*** THE TWO SETS ARE DISTINCT AND THE DISTINCTNESS NEEDS NO COMPUTATION: one is
EMPTY, the other is not. THIS IS NOT A CONSTANT FAMILY. ***
A third sector is written out at bytes: "if b_1(C_j) > 1 it is the
b_1(C_j)-dimensional cycle space minus zero".

THE TRAP WAS AVAILABLE HERE AND THE TARGET DID NOT FALL INTO IT.  The CITED span
D2 (:318-321, verbatim-confirmed by me) contains the exact trap phrasing —
"Lemmas 1-2 give its admissible set at every b_1".  Standing alone that is a
family asserted at every index.  The target REFUSED to rest on it (its C1),
went to the Lemmas, and quoted content that differs sector by sector.  Had it
rested on D2's "at every b_1" alone, the constant-family kill would have been
open.  It is not open.  D2 itself also writes differing content ("At b_1 > 1 ...
selects no class ... At b_1 = 0 the admissible set is empty"), so even the cited
span is not the trap — but it is the summary, and the target was right that the
sentence governs the summary.
```

### 2.2 K2 — the demand is MET at bytes, by membership flip

```text
SOURCE: STAGE8_ACT2_CARRIER_PACKAGE_S9AD_V001.md :181-190, re-read by me directly.

  INDEX VALUE I    "AT THE FULL REQUIRE SET (R-a..R-g, confirmed reading)"
  CLASS, QUOTED    "ADMISSIBLE = { the [N] K_dd variants } ∪ { A2, A2b, A1, W2,
                    A5, ... } — INFINITE, closed under conforming enlargement"
  INDEX VALUE II   "UNDER THE STRICT-HORN READING of R-g (unwritten, disclosed)"
  CLASS, QUOTED    "K_dd leaves the class; A5 is the minimal member; the class
                    remains infinite"

*** A NAMED MEMBER CHANGES MEMBERSHIP ACROSS TWO NAMED INDEX VALUES, VERBATIM.
K_dd ∈ CLASS(I); K_dd ∉ CLASS(II). THIS IS NOT A CONSTANT FAMILY. ***
This is the strongest available exhibition and the target identified it
correctly: not a cardinality change, not a dimension change — a named object
whose membership verdict is written out on both sides.

A third index value is written out at bytes: "ON AN ATTACH VOID (R-f/R-g
dissolve): the class WIDENS further — KLf-type complexes ... return".
```

### 2.3 (A) VERDICT

```text
*** (A) FINDS NOTHING. BOTH VARIATIONS ARE DISPLAYED, NOT ASSERTED-AS-DISPLAYED.
THE TARGET'S CENTRAL CLAIM SURVIVES DEFAULT-REFUTE AT BYTES. ***
```

---

## §3 — HUNT (B): IS EITHER INDEX MISTYPED? (condition vs stage)

The commission turns on this: an index over CONDITIONS is a classification
parameter; an index over STAGES is a production step. I re-derived the kind of
each index from the sources' own operative sentences.

### 3.1 K1's index — an invariant OF THE OBJECT TYPED. NOT a stage.

```text
AT BYTES, Lemma 1 fixes the kind itself: the set is "the cycle space
Z_1(C_j; Q) = ker(bdry)|_{C_j}, of dimension b_1(C_j) for connected C_j".
b_1(C_j) is therefore a DIMENSION OF A SPACE COMPUTED FROM C_j — a cardinality,
an invariant of the object being typed.  It is not supplied to the rule; it is
read off the object.

IS IT A STAGE?  *** NO — AND I TESTED THIS RATHER THAN ACCEPTING IT. ***
  - No successor operator appears at :152-161 or :318-321.
  - No construction step, no ordering language, no "then"/"next"/"proceeds to".
  - Lemma 2's three clauses are joined by "if ... ; if ... ; if ..." — a CASE
    SPLIT on a computed invariant, not a sequence.
  - D3 (:322-325) separates admission from the rule entirely ("which subcomplexes
    of K are admitted cells is an unsupplied, separate act"), so nothing in the
    rule advances an object from one sector to another.
The integers 0, 1, >1 are totally ordered AS INTEGERS.  That is an ordering on
the index, not a production order on the record.  The target says exactly this
and it is right at bytes.
```

### 3.2 K2's index — a parameter OF THE CONDITION. NOT a stage.

```text
AT BYTES, the three index values are conjunctions of named REQUIRES plus a
READING, and the sources' own words mark them as HYPOTHESES, not steps:
  - R-g at :141-144: "the stricter unwritten horn is a disclosed READING SUPPLY
    (f1881511 §2.4), NEVER RULED."  A never-ruled reading is an alternative
    hypothesis in force, not a stage the record passed through.
  - Value III is introduced counterfactually: "ON AN ATTACH VOID (R-f/R-g
    dissolve)" — a condition that would obtain, not a step that occurs.
  - The ordering that exists is by INCLUSION/STRENGTH of requires, and the class
    map runs the other way ("the class WIDENS" as requires dissolve).  An
    antitone map on a poset of hypotheses is a classification, not a pipeline.

*** I FOUND NO STAGE-READING ANYWHERE IN THE TARGET, AND NO PLACE WHERE A
CONDITION-INDEX IS DESCRIBED WITH STEP LANGUAGE, OR CONVERSELY. (B) FINDS
NOTHING. ***
```

### 3.3 The two indices are genuinely different objects — re-derived

```text
K1's index is computed FROM the object typed (dim of that object's cycle space).
K2's index is chosen AMONG premises under which typing happens (which requires,
which horn).  The yielded sets also have members of different type: K1's members
are 1-CHAINS inside one cell's cycle space (Lemma 1); K2's members are WHOLE
COMPLEXES (K_dd, A2, A5, W1, KLf-type).  CONFIRMED at bytes.  The target's §6
conclusion that a build treating them as one object is wrong at bytes is CORRECT.
```

---

## §4 — HUNT (C): IS INVARIANCE-STATABILITY CLAIMED OVER AN UNPOPULATED DOMAIN?

The demand: the domain must be SHOWN with more than one EXHIBITED member, not
merely asserted to exist. I checked each exhibited member against the source.

### 4.1 K2 — domain is POPULATED, with exhibited members. Claim survives.

```text
EXHIBITED INDEX VALUES — all three written out at :181-190, each on its own
headed line, each with class content. Three > one. POPULATED, not asserted.

EXHIBITED MEMBERS OF THE INTERSECTION, TRACED BY ME TO BYTES:
  A5 ∈ CLASS(I)     LITERAL.  :182 union list, and :174 "A5 ... are computed
                    rows".  Named on the page.
  A5 ∈ CLASS(II)    LITERAL.  :186 "A5 is the minimal member".
  A5 ∈ CLASS(III)   NOT LITERAL — an inference, and *** THE TARGET MARKS IT AS
                    ONE *** ("DERIVED-FROM-DISPLAYED, ONE STEP, MARKED AS SUCH").
                    The inference is sound: III's requires (R-a..R-e) are a
                    proper subset of I's (R-a..R-g), and the source's own word
                    for that direction is "WIDENS" (:187).  A member of the
                    narrower class is a member of the wider.

EXHIBITED NON-MEMBER:  W1, ":189-190 "W1 ([census]) stays OUT either way (its
  monogon face fails derivation-strength R-c)".  R-c survives all three index
  values, so the non-membership holds at all three.

*** (C) FINDS NOTHING AGAINST K2. THE DOMAIN IS EXHIBITED, NOT ASSERTED. ***
And the target does NOT overclaim: it states plainly that CLASS(II)'s full
membership is never displayed, so the intersection is BOUNDED and UNDETERMINED.
I verified that CLASS(II) is indeed never enumerated anywhere in K2 — :185-186
gives only the departure of K_dd and the minimality of A5.  The target's
restraint here is correct at bytes.
```

### 4.2 K1 — the target DECLINES statability-with-content. Correct at bytes.

```text
The target could have claimed an invariance here and did not.  I tested its
reason and it holds:
  - For a FIXED cell, b_1(C_j) is one determined integer, so the family over a
    fixed object is a SINGLETON — nothing to intersect.  Correct.
  - Moving the index requires moving the cell; by Lemma 1 the set lies in the
    cycle space of THAT cell, so the ambient moves with it.  Correct at :152-156.
  - Intersecting over the displayed range anyway is forced EMPTY by the b_1 = 0
    sector.  Correct — Lemma 2 gives EMPTY there, and any intersection with an
    empty member is empty.
*** THIS IS A REFUSAL, NOT A CLAIM. IT CANNOT BE THE (C) KILL. ***
```

### 4.3 One correction inside 4.1 — asymmetric marking of inferences

```text
CORRECTION C-1 (MINOR).  The source phrase at :189 is "stays OUT EITHER WAY" — a
TWO-way phrase.  The target extends W1's non-membership to THREE classes.  The
extension is SOUND and the target states its ground ("grounded on derivation-
strength R-c, which no displayed index value dissolves"), which I verified: R-c
lies in R-a..R-e and so survives value III.  But the target LABELS the parallel
A5 ∈ CLASS(III) step as DERIVED-FROM-DISPLAYED and does NOT so label this one.
The marking is asymmetric.  CORRECTION: the W1-across-three-values statement is
also derived-from-displayed, one step, and should carry the same mark.
EFFECT ON VERDICT: none.  Both inferences are sound at bytes.
```

---

## §5 — HUNT (D): THE SWEEP, RE-RUN UNCAPPED WITH MY OWN WIDER PATTERNS

### 5.1 MY SWEEP DECLARATION AND CUTOFF

```text
SWEEP CUTOFF: 2026-08-15, at the byte-states sealed and verified in §1.  Anything
              written after this artifact's seal is outside my sweep by
              construction.
ROOTS:        (P) /Users/bgm/MB Work/alpha-program-archive/workspace
              (S) /Users/bgm/Documents/New project/gravity_emergence_evidence_
                  program/alpha_fundamental_record_action_cleanroom_v003
DEPTH:        *** UNCAPPED (recursive, all depths), *.md, BOTH ROOTS. ***
              The target ran maxdepth 1.  Corpus size at my depth:
              (P) 1907 *.md files (1766 at depth 1); (S) 2308 (1522 at depth 1).
NEVER READ:   no register, tracker, road, plan, or continuation file was opened.
              Two hits are recorded below as NOT-OPENED-BY-RULE.
"Q-..." :     EXPECTED-UNLOCATABLE; not chased; carried as opaque tokens.

MY LEGS:
  A1  Re-execution of the BUILD's declared Sweep 4 string, verbatim, both roots,
      capped AND uncapped.
  A2  Re-execution of the AUDIT's AS2 pattern, verbatim, both roots, capped AND
      uncapped; plus AS1, and AS1|AS2.
  A3  MY OWN WIDER PATTERNS (W1-W8 below), case-insensitive, uncapped, both
      roots — designed to catch index-varying admissible sets phrased WITHOUT
      the tokens either prior pattern requires.
SCOPE LIMIT DISCLOSED: A3 is still pattern-bound.  I make no corpus-wide
  existence or absence claim beyond what these legs return.
```

### 5.2 A1 — THE BUILD'S OWN SWEEP 4 STRING. Target's count RECONCILES.

```text
STRING (verbatim from P1 :83-84): 'admissible set|set of admissible|admissible
  values|permitted values|set of permitted|admissible continuations|admissible next'

  (P) depth 1: 46 files    (P) UNCAPPED: 46 files
  (S) depth 1: 17 files    (S) UNCAPPED: 17 files
  K1: 7 matching lines.  K2: 0 matching lines.

*** THE TARGET DISPLAYS 45.  I GET 46.  THE DIFFERENCE IS THE TARGET ITSELF ***
(it contains 20 matching lines and did not exist when it swept).  46 - 1 = 45.
*** RECONCILED. THE TARGET'S L3 COUNT IS CORRECT. NO DEFECT. ***
Its finding — the build's literal Sweep 4 string returns K1 and does NOT return
K2, because K2 says "admissible CLASS" — is CONFIRMED at bytes.
The target displayed no (S) count for L3; for the record it is 17, and depth
makes no difference on either root.
```

### 5.3 A2 — THE AUDIT'S AS2. *** THE TARGET'S DISPLAYED HIT LIST IS WRONG. ***

```text
PATTERN, confirmed verbatim against P2 :44:
  'admissible (set|class|values)[^.]{0,60}(every|each|all|per |indexed)'

WHAT IT ACTUALLY RETURNS IN (P), uncapped (9 files, of which the target itself
is one and post-dates the build — so EIGHT pre-existing):
  STAGE8_7A_COMPLETENESS_PROOF_DARIO_V001.md
  STAGE8_ACT2_CARRIER_PACKAGE_S9AD_V001.md                    [K2 RETURNED]
  STAGE8_DISCHARGERS_VS_PARTITION_O11SR_AUDIT_V001.md
  STAGE8_GATE_SIGNATURE_O18SR_AUDIT_V001.md
  STAGE8_GATE_SIGNATURE_O18SR_V001.md
  STAGE8_R1_NAMING_CANDIDATE_V001.md
  STAGE8_TASK5_EQ6_WHERE_CLAUSES_FINAL_CHECK_LANE1_V001.md
  STAGE8_TYPING_RULE_CANDIDATE_V001.md                        [K1 RETURNED]
  (+ STAGE8_INDEX_VARYING_SETS_O20SR_V001.md — the target itself)
Depth makes no difference in (P).

*** THE TARGET DISPLAYS "11 FILES" AND NAMES THREE THAT THE PATTERN DOES NOT
RETURN AT ALL: ***
  STAGE8_7A_PUBLIC_LAYER_CROSSCHECK_CODEX2_V001.md            AS2 hits = 0
  STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md               AS2 hits = 0
    (only its AUDIT matches; the target lists BOTH)
  STAGE8_TASK4A_SOURCE_TO_OUTPUT_NORMALIZATION_N_UNIQUENESS_
    INSTALL_AND_FULL_DOOR_D_REEXECUTION_V001.md               AS2 hits = 0

I EXCLUDED THE OBVIOUS INNOCENT EXPLANATION: all three are SEALED and all three
`shasum -a 256 -c` *** OK *** against their own sidecars, with mtimes
2026-08-09, 2026-08-15 13:56 and 2026-08-02 — ALL BEFORE the target's seal
(15:57).  THE BYTES HAVE NOT CHANGED SINCE THE TARGET SWEPT.  The three files do
contain the string "admissible set/class/values" (1, 11 and 1 lines) but none
carries an index word within the pattern's 60-character no-period window.

I ALSO TESTED WHETHER A NEARBY PATTERN YIELDS THE TARGET'S LIST.  It does not:
  AS1 alone            -> 7 files
  AS1|AS2              -> 11 files  (count matches, LIST DOES NOT — it returns
                          STAGE8_AND_CLASS_CHECK_V001.md and
                          STAGE8_DESC_B03_DARIO_V002.md, which the target does
                          NOT list, and it does NOT return the three above)
  AS2 case-insensitive -> 10 files
  AS2 + "family"       -> 10 files
NO PATTERN I TRIED REPRODUCES THE TARGET'S ELEVEN NAMES.

IN ROOT (S): AS2 returns 2 files at depth 1 and 3 uncapped.
*** THE TARGET DISPLAYS 4. NOT REPRODUCIBLE AT EITHER DEPTH. ***
(AS1|AS2 in (S) returns 3, also not 4.)

A SECOND, SEPARATE DEFECT IN THE SAME LEG: P2 declares its sweeps at :42 as
"MY OWN SWEEPS, DECLARED (RECURSIVE, *.md, both roots)".  AS2 was RECURSIVE.
The target ran it at maxdepth 1 and calls that re-executing the pattern
"VERBATIM".  The depth cap is disclosed in the target's §1, but the DIVERGENCE
from the source sweep's own declared depth is not.  It matters in (S): recursion
adds one hit the cap suppresses.
```

### 5.4 WHAT SURVIVES §5.3 — the suppression finding itself

```text
*** THE CORE FINDING IS UNAFFECTED AND I CONFIRM IT INDEPENDENTLY. ***
  - "SWEEP 4" occurs EXACTLY ONCE in P1, at :83, the declaration.  I grepped the
    whole file.  NO count, NO list, NO member of its return appears anywhere.
    The target's §8.1 claim is CONFIRMED at bytes.
  - P1 :87-89 does narrate Sweep 3's result, exactly as the target says.
    CONFIRMED verbatim.
  - P1 :420-428 is VERBATIM as quoted, including "THE DOMAIN IS NOT MERELY
    UNPOPULATED — IT IS UNFORMED."  CONFIRMED character for character.
  - P2 :49-50 and :517-522 are VERBATIM as quoted.  CONFIRMED.
  - AS2 genuinely returns BOTH K1 and K2; both are sealed, both in the primary
    root, both carry no mark the build declared as excluding.  CONFIRMED.
THE CORRECTION IS TO THE DISPLAYED CENSUS OF THE RE-EXECUTION, NOT TO THE
CONCLUSION DRAWN FROM IT.
```

---

### 5.5 A3 — MY OWN WIDER PATTERNS. Is there a THIRD or FOURTH instance?

```text
RUN CASE-INSENSITIVE, UNCAPPED, BOTH ROOTS.  File counts (P / S):

W1  'admissible (set|class|family|region|space|collection|range)'      123 /  96
W2  '(set|class|family|collection) of (admissible|permitted|allowed|
     eligible|feasible)'                                                15 /  12
W3  'admissible[^.]{0,80}(at every|at each|for every|for each|indexed
     by|as a function of|varies|depends on)'                             6 /   1
W4  '(varies|varying|different|distinct)[^.]{0,50}(admissible|admitted|
     permitted|allowed) (set|class|family)'                              3 /   2
W5  'at (every|each) (b_1|index|stage|condition|require|value|level|
     node)'                                                             59 /  46
W6  '(admissible|permitted|allowed|eligible) (set|class|family)
     [^.]{0,60}(b_1|index|require|condition|stage)'                     13 /   5
W7  'index-(varying|indexed|dependent)|index varying'                    1 /   0
W8  '(admitted|admissible)[^.]{0,40}(per-cell|per cell|per-stage|
     per stage|per-condition|per condition)'                             9 /   4

W3 and W6 are the discriminating legs.  I examined their returns at bytes, by
scoped extraction of the matching lines only.
```

```text
*** RESULT: NO THIRD INDEX-VARYING ADMISSIBLE SET WAS FOUND IN EITHER ROOT. ***

Every non-K1/K2 return examined resolves to one of three kinds, none of which is
an admissible set varying across an index:

(i)  UNIVERSAL QUANTIFICATION OF A CONSTANT PROPERTY — not a varying set.
     STAGE8_AND_CLASS_INDEPENDENCE_V001.md :210, :766 ("at every point ... and
     every value of ..."); STAGE8_PIN_DETERMINATION_S9AD_V001.md :433 ("both
     admissible at every sealed byte").  Content is the SAME at every index
     value; these are the constant case.
(ii) A DECOMPOSITION OF ONE SET BY AXIS, not a family of sets.
     STAGE8_DESC_PHYSICALSIG_SELECTION_DARIO_V001.md :814-817 — "ADMISSIBLE SET
     = condition-sets compatible with one causal exclusion.  Structure:
     ALGEBRAIC nothing upstream bears. / CAUSAL one exclusion, at bytes. /
     ANALYTIC nothing upstream bears."  Three axes with differing entries, but
     the ADMISSIBLE SET IS ONE SET; the three lines say which upstream
     constraint bears on each axis, not what the admissible set is at each.
     NEAR-MISS, RECORDED, NOT COUNTED.
(iii) VOCABULARY-ONLY hits (a closed admissible class mentioned in passing; an
     "admissible-tuple"; a requirement not removed).  Not index-stated.

NOT OPENED, BY RULE (recorded so the gap is visible, not hidden):
  relay_inbox/RELAY_PASTE_870_GATED_DEMAND_RETYPING_CODEX2_V001.md  in (S) —
    a relay/commission paste, i.e. a plan-or-continuation instrument.  It is the
    ONE hit the target's depth cap suppressed in (S).  NOT OPENED.
  STAGE8_SLOT18_Q23_Q28_GOVERNING_REGISTRATION_V001.md in (P) and (S) —
    a registration file.  NOT OPENED.
  I therefore do not claim to have typed the contents of these two.
```

### 5.6 THE THIRD SITE THAT DOES EXIST — and it is a CONSTANT FAMILY

```text
My W6 leg returned a site the target never cites, and it is worth the record
precisely because it is the TRAP INSTANTIATED.

SOURCE, at its own bytes: STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md
:54-57 (re-read by me directly; quoted downstream at
STAGE8_FORCING_NOTION_O12SR_V001.md :116-119 and
STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md :81, both verbatim):

> "Forcing has ONE criterion (the admissible set is a point) and THREE shapes: a
>  single require with a unique root; EXACT JOINT DETERMINATION (n conditions, n
>  freedoms — the marker/C_R case, which the reviewer's 'two ways' silently folds
>  in); and overdetermined joint determination (surplus conditions)."

*** THIS IS AN ADMISSIBLE SET STATED ACROSS A THREE-VALUED INDEX WHOSE CONTENT
IS CONSTANT: THE SET IS A POINT AT ALL THREE SHAPES.  "ONE criterion ... and
THREE shapes."  IT IS A CONSTANT FAMILY, AND IT IS NOT A THIRD INSTANCE. ***
O12SR :127-129 types it the same way at bytes — "a taxonomy of the ADMISSIBLE
SET'S GEOMETRY (point vs. not; how many conditions produced the point)".

TWO CONSEQUENCES, BOTH FAVOURABLE TO THE TARGET AND NEITHER TESTIMONIAL:
  1. The index here is explicitly over CONDITIONS ("n conditions, n freedoms",
     "surplus conditions") — independent corroboration that the corpus's
     admissible-set indices are condition-indices, not stage-indices (§3).
  2. THE PRIOR BUILD ALREADY DISPLAYED THESE VERY LINES.  P1 :416-417 quotes
     "Forcing has ONE criterion (the admissible set is a point)" as evidence
     that no domain is stated.  So this site was NOT suppressed — it was seen,
     and correctly typed as domain-less.  The build's error was never about
     this site; it was about K1 and K2, which state domains and vary.
*** THE CONSTANT-FAMILY DISTINCTION THE TARGET DRAWS IS THEREFORE A REAL ONE
WITH A REAL NEGATIVE CONTROL IN THE CORPUS, NOT A RHETORICAL FLOURISH. ***
```

---

## §6 — HUNT (E): QUOTATION INTEGRITY AGAINST FULL SOURCE SPANS

The day's recurring defect is a hedge elided from inside a quoted sentence. I
re-read every cited span in full, past the target's closing quotation mark, and
compared. **Four defects found. None is a hedge-elision of the classic kind; one
is materially close and I state it as such.**

### 6.1 E-1 — *** THE MATERIAL ONE. *** §7.3 truncates mid-sentence, unmarked

```text
TARGET §7.3 quotes K2 :191-196 as:
  "every admitted CELL of K_dd — the trace cell K_square and the face closure —
   has b_1 = 1 ... A5's cells are likewise b_1 = 1 cell-wise"
and closes the quotation there.

THE SOURCE SENTENCE DOES NOT END THERE.  At :194-196 it continues, unbroken,
after a semicolon:
  "; its complex-level dim H = [dimension elided] reopens the class-selector
   question (N-2 at full strength), which on K_dd is reduced to the domain
   extension alone (dim H = [elided]: nothing to select)."

*** THE TARGET'S MID-SPAN ELLIPSIS IS MARKED; THIS TERMINAL TRUNCATION IS NOT.
THE QUOTE STOPS AT A SEMICOLON AND THE READER IS NOT TOLD. ***

WHY IT IS MATERIAL, AND NOT MERELY TIDY:  §7.3 is the paragraph that adjudicates
whether K2 supplies a BRIDGE to K1's index.  Its whole argument is that the
cell-wise fact "pins BOTH named members to the SAME b_1 value, so it supplies a
CONSTANT SECTION over K1's index, not a variation across it."  The elided
continuation is the sentence's own statement that THE TWO NAMED MEMBERS DIFFER
AT COMPLEX LEVEL, and that this difference "reopens the class-selector question"
— which is K1's N1 subject exactly (K1 :379-383, "NO CYCLE SELECTOR ... At
b_1(C_j) > 1 the rule ... freezes NO class selection").  The strongest material
against the target's own "constant section, no bridge" reading sits in the
second half of the sentence it cut.

*** BUT THE VERDICT SURVIVES, AND I SAY WHY RATHER THAN LETTING IT STAND ON
CHARITY. ***  K1's index is the first Betti number OF A CELL.  K2's differing
quantity is a COMPLEX-LEVEL dimension, not a cell b_1 — and the same sentence
says so, twice, in the words "complex-level" and "cell-wise", and K2 :192-193
adds that the extension needed "is for the COMPLEX-level read only, NOT for any
cell".  So no displayed correspondence carries a require-set value to a b_1
SECTOR, and §7.3's NOT-STATABLE verdict is CORRECT at bytes.  What is shared is
a QUESTION (class selection), not an index map.

CORRECTION REQUIRED: the quotation must run to the end of the sentence or carry
a terminal ellipsis, and the constant-section argument must be stated in the
form "constant over the CELL index, notwithstanding a displayed complex-level
difference" — which is what the bytes support and what the target asserts more
flatly than its own source allows.
```

### 6.2 E-2 — §4.1's span label is short by one line

```text
The target labels the K2 flag-block quotation ":769-773" and its IMPORT AUDIT
I2 repeats ":769-773".  The quoted text ends "...on an Attach void KLf-type
complexes return and W1 stays out ..." — and at bytes "on an Attach void KLf-type
complexes return and W1 stays out (derivation-" is line :774.
*** THE QUOTATION RUNS INTO :774. THE LABEL SAYS :769-773. ***
PROVENANCE, STATED FAIRLY: the target inherited this span label from P2 :519,
which cites K2 at ":769-773".  The error is carried, not invented.
EFFECT ON VERDICT: none.  The quoted words are verbatim.
```

### 6.3 E-3 — an elision marker that understates what it removed

```text
TARGET §4.2:  "KLf-type complexes ([census elided]) return"
SOURCE :188:  "KLf-type complexes (3-circuit trace + plaquette, [census]) return"
The parenthesis contained a SHAPE DESCRIPTION as well as a census.  The marker
says "census elided"; a non-numeric structural descriptor went with it.
The same target line elides W1's parenthesis, which IS purely a census — so the
marker is accurate there and inaccurate here.
EFFECT ON VERDICT: none — the descriptor is not load-bearing for membership.
CORRECTION: the marker should read "[shape + census elided]" at the KLf-type
occurrence.
```

### 6.4 E-4 — §3.4's K-1 quotation truncated without a terminal ellipsis

```text
TARGET §3.4:  "the admissible set is Q·gamma_j — the entered read theta_j =
  <A, gamma_j> is EXACTLY a cell-local write under R-CLW"
SOURCE :165-167 continues, same sentence: ", with the entered normalization the
  coefficient [elided] (S3)."
Unmarked terminal truncation again.  The removed clause is a normalization
specification, NOT a hedge, and removing it weakens nothing the target claims.
CORRECTION: terminal ellipsis.  EFFECT ON VERDICT: none.
```

### 6.5 WHAT I CHECKED AND FOUND CLEAN

```text
VERBATIM, CHARACTER FOR CHARACTER, AGAINST FULL SOURCE SPANS:
  K1 :152-161 (Lemmas 1-2)          CLEAN — and the span boundaries are exact.
  K1 :318-321 (D2)                  CLEAN.
  K1 :379-383 (N1)                  CLEAN; "may not freeze" is genuinely in the
                                    source, inside K1's own verbatim carry of
                                    the entered bar.  Target uppercases it for
                                    emphasis — case change, not content change,
                                    and I note it only for completeness.
  K2 :181-190 (§3.3 THE DISPLAY)    CLEAN apart from E-3.
  K2 :163-169 (§3.2, "B4 is load-bearing only for uniqueness")  CLEAN.
  K2 :746-762 (TOY_SEPARATION)      CLEAN; the target's §11 characterisation
                                    ("CANDIDATE stratum", "NONE_SEALED ...
                                    SURVIVES ... untouched") is verbatim-backed.
  P1 :80-85 (sweep declaration)     CLEAN.
  P1 :420-428 (the absence claim)   CLEAN.
  P2 :44 (AS2 pattern)              CLEAN — the pattern is quoted exactly.
  P2 :49-50, :517-522               CLEAN.
NO HEDGE WAS FOUND ELIDED FROM INSIDE ANY QUOTED SENTENCE.  The E-1 truncation
removes a CONTRARY-LEANING CLAUSE rather than a hedge, which is the same family
of defect and is treated above as the material finding.
```

---

## §7 — ONE OVERREACH, NOT A QUOTATION DEFECT

```text
CORRECTION C-2.  TARGET §7.2, on K1's missing common ambient, says of F3:
  "*** THE ARTIFACT NAMES THIS ABSENCE ITSELF. ***  F3 (:350-355) records ...
   That is PRECISELY the transport an intersection across cells would need, and
   it is marked UNSUPPLIED at bytes."

AT BYTES, F3 :350-355 names "the sealed-absent DIAMOND-TO-COMPLEX transport ...
(the Ref_a-indexed current-density receiver + the exhaustion<->refinement index
bridge, the two UNSUPPLIED slots of record ...)".

*** THAT IS A DIAMOND-TO-COMPLEX TRANSPORT. THE INTERSECTION §7.2 DISCUSSES
WOULD NEED A CELL-TO-CELL IDENTIFICATION OF Z_1(C_j; Q) ACROSS DISTINCT CELLS.
NO SEALED TEXT DISPLAYED TO ME IDENTIFIES THESE TWO OBJECTS. "PRECISELY" IS NOT
EARNED. ***  The most that is earned: F3 marks an index bridge UNSUPPLIED of
record, which is ADJACENT to, not identical with, the identification §7.2 needs.

EFFECT ON VERDICT: *** NONE. ***  §7.2's conclusion rests on a structural
argument that needs no F3 at all, and that argument is sound at bytes:
  for a FIXED cell, b_1(C_j) is one determined integer  ->  the family over a
  fixed object is a SINGLETON  ->  moving the index requires moving the cell
  ->  by Lemma 1 the set lies in THAT cell's cycle space  ->  the ambient moves.
The F3 citation is corroboration the target did not need and overstated.
CORRECTION: "precisely" -> "adjacent to; K1 marks an index bridge unsupplied of
record, which is the nearest displayed thing and is not the same object."
```

## §8 — HUNT (F) AND (G): ACTS, LENS TOKENS, NUMBERS

```text
(F) PROPOSAL / ADOPTION / AUTHORED INVARIANCE / RECLASSIFICATION
    *** NONE FOUND. ***  I scanned the whole target for act verbs.  Every
    occurrence of "reclassif-" is a NEGATION (:5, :622, :688).  There is no
    "I propose", "I adopt", "I author", "hereby", "is adopted", "now classified".
    §7 of the target repeatedly declines: "I do not build such an invariance",
    "Stating it would be a further act, and I do not take it", "I STATE NO
    INVARIANCE, BUILD NOTHING, AND PROPOSE NOTHING."
    THE SHARPEST TEST — the target had a live opportunity to author an
    invariance at its §7.1, having established all five pieces present, and it
    stopped at STATABLE and expressly refused to state it, disclosing instead
    that CLASS(II)'s membership is undisplayed so the intersection is bounded
    and uncomputed.  *** THAT IS THE CORRECT STOP AND IT TOOK IT. ***
    NO STRATUM CROSSED: K1 is carried as PROPOSED_NOT_ADOPTED (verified at its
    head), K2's objects as CANDIDATE with NONE_SEALED intact (verified at
    K2 :749-752).  Displaying that an artifact states a varying admissible set
    promotes nothing, and the target says so.

(G) LENS TOKENS      *** NONE. ***  No "lens", "lensing", "lensed" anywhere in
    the target.
(G) NUMBERS          *** NO NUMBER OF PHYSICAL IMPORT. ***  I enumerated every
    integer in the target.  They are exhaustively: index values (0, 1); section
    and line references; sweep hit counts; digest/`shasum -a 256` labels; line
    and byte counts; and the two hit counts quoted from P1's own sweep
    declaration.  NO census leaked — I searched specifically for the elided
    (F,E,V) triples, the variant count, and the extra-realization count, and for
    "dim H".  NONE APPEARS.  No constant, no measured value, no comparison.
    THE ELISION DISCIPLINE THE TARGET DECLARED WAS ACTUALLY EXECUTED.
```

---

## §9 — INSTRUMENT: I RE-DERIVED THE TARGET'S §9 INDEPENDENTLY

```text
I did not accept the target's §9 table.  I wrote my own boundary map from
scratch and re-ran it in MY OWN FRESH VENV (sympy 1.14.0), exact rationals, no
floats, no physical quantity.  Objects: a path on three vertices (tree); a
4-cycle; a theta graph (two vertices, three parallel edges).

MY RESULT, INDEPENDENTLY OBTAINED:
  tree    rank(d_1)=2   b_1 = 0   dim ker = 0    ->  A = EMPTY
  4-cycle rank(d_1)=3   b_1 = 1   dim ker = 1    ->  A = punctured LINE
  theta   rank(d_1)=1   b_1 = 2   dim ker = 2    ->  A = punctured PLANE
  in-script assertion b_1 = E - rank(d_1) == dim ker(d_1) held on all three.
  EMPTY vs NONEMPTY: True.  dim 1 vs dim 2: True.  three distinct dims: True.
  d_1(c*gamma) == 0 identically for symbolic c: True.
  d_1(a*k1 + b*k2) == 0 identically for symbolic a, b: True.
  rank([k1|k2]) == 2, so the b_1 > 1 sector is NOT a line: True.
  EXIT 0.

*** EVERY LINE OF THE TARGET'S §9 REPRODUCES EXACTLY, INCLUDING ALL THREE RANKS
AND ALL SEVEN BOOLEAN ASSERTIONS. §9 IS CONFIRMED. ***
The target's own statement of what this does NOT establish is also correct: it
checks arithmetic K1 already states and adds no content to the record.

MY TOY_SEPARATION: my three graphs are INSTRUMENT OBJECTS ONLY.  They are not
carriers, not candidates, not admitted cells, not physical.  None is proposed
for admission, none is identified with K_square or any sealed object, and no
property of any of them transfers to the record.
```

---

## §10 — CHOICE LEDGER

```text
A1  RAN MY SWEEPS UNCAPPED (recursive) WHERE THE TARGET RAN maxdepth 1.  BASIS:
    the commission ordered an uncapped re-run, and P2's own AS2 was declared
    recursive.  CONSEQUENCE: (P) is unchanged at any depth; (S) gains exactly
    one hit, which is a relay paste I did not open by rule.  COST IF WRONG: the
    (S) count would be the capped 2, still not the target's displayed 4.
A2  BUILT MY OWN W1-W8 PATTERNS RATHER THAN REUSING THE BUILD'S OR THE AUDIT'S.
    BASIS: (D) demanded wider patterns, and both prior patterns require tokens a
    differently-phrased instance would lack.  COST IF WRONG: my legs remain
    pattern-bound and I claim only what they returned (N5 below).
A3  TREATED "shasum OK + mtime before the target's seal" AS SUFFICIENT TO RULE
    OUT POST-SWEEP DRIFT for the three phantom AS2 files.  BASIS: a sealed file
    that still verifies has not changed.  COST IF WRONG: none available — a
    seal mismatch would have shown, and none did.
A4  DID NOT OPEN THE RELAY PASTE OR THE REGISTRATION FILE returned by my legs.
    BASIS: the never-read rule.  DISCLOSED CONSEQUENCE: I do not claim to have
    typed their contents, and I record them rather than silently dropping them.
A5  MARKED E-1 AS THE MATERIAL QUOTATION FINDING BUT DID NOT LET IT CARRY THE
    VERDICT.  BASIS: I re-read the elided continuation and it distinguishes
    "complex-level" from "cell-wise" in its own words, which preserves the
    target's conclusion.  COST IF WRONG: if the complex-level dimension were a
    cell b_1, §7.3 would flip to STATABLE and the verdict on §7 alone would
    become REFUTED.  It is not; K2 :192-193 says so explicitly.
A6  ELIDED ALL CENSUSES, VARIANT COUNTS AND COMPLEX-LEVEL DIMENSIONS FROM MY OWN
    QUOTATIONS, while DISPLAYING index values (b_1 = 0, 1, >1) and sweep hit
    counts.  BASIS: my fence bars numbers; index values are the commission's
    subject and cannot be withheld without withholding the determination itself,
    and hit counts are the evidence for §5's correction.
A7  DID NOT OPEN ANY REGISTER, TRACKER, ROAD, PLAN OR CONTINUATION FILE, and did
    not resolve any "Q-..." token, including those appearing inside consumed
    text (P1 :76-78 carries a list of them; I passed them through opaque).
A8  NO GIT.  No repository operation of any kind was performed or inspected.
```

## §11 — TOY_SEPARATION

```text
THE THREE CHECK GRAPHS IN MY §9 (path, 4-cycle, theta) ARE INSTRUMENT OBJECTS
AND NOTHING ELSE.  They are not carriers, not candidates, not admitted cells,
not physical.  I constructed them to re-derive an arithmetic identity K1 already
states.  None is proposed for admission, none is compared to or identified with
any sealed object — in particular the 4-cycle is a SHAPE and is NOT K_square —
and no property of any of them transfers to the record.

EVERYTHING ELSE IN THIS AUDIT IS QUOTATION, RE-EXECUTION, OR TYPING OF THOSE.
The strata of every consumed artifact are carried unchanged: K1 remains
PROPOSED_NOT_ADOPTED at its own head; K2's objects remain at the CANDIDATE
stratum with NONE_SEALED at the physical write-carrier stratum untouched; P1 and
P2 are consumed only for §5.  Auditing a determination promotes nothing.  No
stratum was crossed by this audit, and the corrections I state are corrections
to a DISPLAY, not changes to any classification.
```

## §12 — IMPORT AUDIT

```text
IMPORTED INTO THIS AUDIT — the complete list:
  J1  The target, whole file, 728 lines — read at bytes.
  J2  K1 spans :1-20, :145-175, :312-325, :346-358, :375-388 — read at bytes.
  J3  K2 spans :138-200, :744-782 — read at bytes.
  J4  P1 spans :76-95, :416-432, plus a whole-file grep for "SWEEP 4".
  J5  P2 spans :40-52, :225-262, :514-524.
  J6  STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md :50-62;
      STAGE8_FORCING_NOTION_O12SR_V001.md :108-140;
      STAGE8_DESC_PHYSICALSIG_SELECTION_DARIO_V001.md :800-840 — scoped reads
      for §5.5/§5.6 only.  Matched-line-only extractions from four further files
      named in §5.5.
  J7  sympy 1.14.0, my own fresh venv, for §9's exact linear algebra ONLY.
  J8  find/grep/shasum over both roots, as declared in §5.1.

NOT IMPORTED — asserted:
  N1  NO TESTIMONY.  No claim of the target, P1 or P2 was accepted as evidence.
      Every finding above was re-derived from source bytes or re-executed.
  N2  NO PHYSICS PREMISE.  No quantity computed, compared, or carried.
  N3  NO NUMBER OF PHYSICAL IMPORT.  No constant, measured or derived, appears.
  N4  NO SEALED TEXT AMENDED OR RECLASSIFIED.  Corrections are stated ABOUT the
      target's display; nothing is rewritten and no seal is disturbed.
  N5  NO CORPUS-WIDE EXISTENCE OR ABSENCE CLAIM.  My legs are pattern-bound and
      two returned files were not opened by rule.
  N6  NO INVARIANCE BUILT, STATED, OR PROPOSED.  I audit statability only.
  N7  NO POSITION TAKEN on whether K1's rule or K2's admission should be entered.
```

---

## §13 — FLAG BLOCK

```text
STEP_0 = output path probed ABSENT before any write; target seal *** OK *** by
  shasum -a 256 -c from the artifact's own directory.
ALL_CONSUMED_SEALS_REVERIFIED = true.  K1, K2, P1, P2 — all four re-checked by
  me from each artifact's OWN directory, all *** OK ***, and all four digests
  and line/byte counts MATCH the target's §2 display exactly.
A_VARIATION_ASSERTED_AS_DISPLAYED = *** NOT FOUND — THE KILL DOES NOT LAND. ***
  K1: two index values, two sets, both quoted at bytes, distinct without
  computation (b_1 = 0 EMPTY vs b_1 = 1 the punctured circuit line).
  K2: two index values, two classes, a NAMED member flipping membership
  verbatim (K_dd ∈ I; "K_dd leaves the class" at II).  NEITHER IS CONSTANT.
  The trap phrasing DOES exist at K1's CITED span D2 ("at every b_1"), and the
  target declined to rest on it and went to the Lemmas.  Correct move.
B_INDEX_MISTYPED = *** NOT FOUND. ***  K1's index is an invariant OF THE OBJECT
  TYPED (a cardinality, dim of that cell's cycle space); K2's index is a
  parameter OF THE CONDITION (require set + reading of R-g).  NEITHER IS A
  STAGE — no successor, no construction step, no ordering language at bytes; K2's
  alternatives are marked "never ruled" and counterfactual in the source itself.
  The two indices are distinct objects and yield members of different type
  (1-chains vs whole complexes).  CONFIRMED.
C_UNPOPULATED_DOMAIN = *** NOT FOUND. ***  K2's domain is EXHIBITED with three
  index values written out on their own lines, with A5 traced as a member at two
  values literally and at the third by a marked one-step inference, and W1 as a
  named non-member.  More than one exhibited member.  The target does NOT
  overclaim: it stops at STATABLE and declares the intersection undetermined
  because CLASS(II) is never enumerated — which I verified is true of K2.
D_THIRD_OR_FOURTH_INSTANCE = *** NONE FOUND, UNCAPPED, BOTH ROOTS, MY OWN
  PATTERNS (W1-W8). ***  Non-K1/K2 returns resolve to universal quantification
  of constant properties, one decomposition-by-axis near-miss
  (DESC_PHYSICALSIG :814-817, recorded), or vocabulary-only hits.  A THIRD SITE
  stating an admissible set across a three-valued index DOES exist — the
  forcing-boundary adjudication's "ONE criterion (the admissible set is a point)
  and THREE shapes" — and it is a *** CONSTANT FAMILY ***, the negative control.
  P1 itself already displayed those lines (:416-417), so it was never suppressed.
E_QUOTATION_INTEGRITY = *** FOUR DEFECTS, ONE MATERIAL. ***
  E-1 MATERIAL: §7.3 truncates K2 :191-196 mid-sentence at a semicolon, unmarked,
      dropping the clause that says the two named members DIFFER at complex level
      and that this "reopens the class-selector question".  The paragraph's own
      thesis is that the fact is a CONSTANT SECTION.  Verdict survives — the
      elided clause says "complex-level" where K1's index is a CELL b_1, and
      K2 :192-193 states the extension is "for the COMPLEX-level read only, NOT
      for any cell" — but the argument as written is under-supported.
  E-2 §4.1/I2 label the flag-block quote :769-773; the text runs into :774.
      Inherited from P2 :519.
  E-3 §4.2's "[census elided]" also removed a non-numeric shape descriptor.
  E-4 §3.4's K-1 quote truncated without a terminal ellipsis.
  NO HEDGE FOUND ELIDED FROM INSIDE ANY QUOTED SENTENCE.
F_ACTS = *** NONE. ***  No proposal, adoption, authored invariance, or
  reclassification.  Every "reclassif-" occurrence is a negation.  The target had
  a live opportunity to author an invariance at its §7.1 and expressly refused.
G_LENS_TOKENS = *** NONE. ***  G_NUMBERS = *** NO NUMBER OF PHYSICAL IMPORT ***;
  no census leaked; the declared elision discipline was actually executed.
SWEEP_CORRECTION_MAJOR = *** THE TARGET'S §8.4 L4 CENSUS IS NOT REPRODUCIBLE. ***
  It displays "11 FILES" in (P) and names three that the declared AS2 pattern
  returns ZERO times at unchanged, seal-verified bytes
  (7A_PUBLIC_LAYER_CROSSCHECK_CODEX2; DISCHARGERS_VS_PARTITION_O11SR_V001, whose
  AUDIT alone matches; TASK4A_SOURCE_TO_OUTPUT_NORMALIZATION...).  The true
  return is EIGHT pre-existing files (nine including the target itself).  In (S)
  it displays 4; AS2 returns 2 capped, 3 uncapped.  No pattern variant I tried
  (AS1, AS1|AS2, case-insensitive, +family) reproduces the eleven names.
  SECOND DEFECT IN THE SAME LEG: P2 :42 declares AS2 RECURSIVE; the target ran it
  at maxdepth 1 and calls that verbatim re-execution.
SWEEP_L3_RECONCILED = the target's "45" is CORRECT.  I get 46 because the target
  itself now matches its own sweep string.  46 - 1 = 45.  NO DEFECT.
SUPPRESSION_FINDING_ITSELF = *** CONFIRMED INDEPENDENTLY. ***  "SWEEP 4" occurs
  exactly ONCE in P1 (:83, the declaration) — no count, no list, no member
  anywhere.  P1 :420-428 ("THE DOMAIN IS NOT MERELY UNPOPULATED — IT IS
  UNFORMED") is verbatim.  AS2 genuinely returns BOTH K1 and K2, both sealed,
  primary-root, unexcluded.  The correction is to the CENSUS OF THE
  RE-EXECUTION, not to the conclusion drawn from it.
OVERREACH = C-2, §7.2's "PRECISELY the transport an intersection across cells
  would need".  F3 names a DIAMOND-TO-COMPLEX transport; the intersection needs
  a CELL-TO-CELL identification of Z_1(C_j;Q).  Adjacent, not identical.
  §7.2's conclusion survives on its own structural argument, which needs no F3.
INSTRUMENT = *** §9 REPRODUCED EXACTLY IN MY OWN FRESH VENV *** (sympy 1.14.0,
  exact rationals, exit 0): all three ranks, all three Betti numbers, all seven
  boolean assertions.  CONSISTENCY = ALL-MATCH.
FENCES = alpha_computed false; proof_authorized false; kappa_record_computed
  false; no value, no number of physical import, no measured-constant comparison.
ACTS_TAKEN = NONE beyond determination.  Nothing proposed, authored, adopted,
  reclassified, or promoted.  No git.
SWEEP_CUTOFF = 2026-08-15, at the byte-states verified in §1.  Legs are
  pattern-bound and disclosed; two returned files not opened by rule; no
  corpus-wide existence or absence claim is made.
```

---

## §14 — VERDICT

```text
PER DELIVERABLE OF THE TARGET:

  §2  SEALS VERIFIED                        *** CONFIRMED ***
  §3  DISPLAY 1 (K1)                        CONFIRMED-WITH-CORRECTIONS  (E-4)
  §4  DISPLAY 2 (K2)                        CONFIRMED-WITH-CORRECTIONS  (E-2,
                                              E-3, C-1)
  §5  TYPE THE SIGNATURE                    *** CONFIRMED ***
  §6  WHAT IS THE INDEX (the kinds)         *** CONFIRMED ***
  §7  THE INVARIANCE QUESTION               CONFIRMED-WITH-CORRECTIONS  (E-1
                                              material; C-2 overreach)
  §8  THE SUPPRESSION, OF RECORD            CONFIRMED-WITH-CORRECTIONS
                                              (conclusion CONFIRMED; the
                                               displayed L4 census is WRONG)
  §9  INSTRUMENT                            *** CONFIRMED *** (reproduced)
  §10-13 LEDGER / TOY / IMPORT / FLAGS      CONFIRMED-WITH-CORRECTIONS (the
                                              FLAG BLOCK repeats the wrong 11;
                                              IMPORT AUDIT repeats :769-773)
  §14 VERDICT                               *** CONFIRMED ***

*** OVERALL: CONFIRMED-WITH-CORRECTIONS. ***

I WAS SENT TO KILL THIS AND IT DOES NOT DIE.  The four hunts aimed at the
substance — variation asserted as displayed, index mistyped, invariance claimed
over an unpopulated domain, a third instance suppressed — ALL RETURN NOTHING.
Both artifacts do state admissible sets that genuinely vary across an index; the
variation is DISPLAYED at bytes in both, by quoted content that differs at
quoted index values; the two indices are of different kinds and NEITHER is a
stage; and K2's invariance domain is populated with exhibited members while K1's
is correctly refused.  My uncapped sweep with my own patterns adds no third
variation, and the one further index-stated site it does find is a CONSTANT
family — which makes the target's constant-family distinction a real one with a
negative control rather than a rhetorical hedge.

THE CORRECTIONS ARE TO DISPLAY, AND ONE OF THEM IS SERIOUS.  The target's §8.4
re-execution census names three files its own declared pattern does not return
and inflates both root counts; a determination whose whole force is "re-executed
at bytes" must have a reproducible census, and this one does not.  That defect
is contained: it touches the SIZE of the re-execution's return, never its
DECISIVE members, and K1 and K2 are genuinely returned.  The E-1 truncation is
the quotation defect that matters, because the clause it cut leans against the
paragraph that cut it; the verdict survives, but on a ground the target did not
state and should have.

*** THE PRIOR BUILD'S ABSENCE CLAIM REMAINS FALSE AT BYTES, AND THE TARGET'S
CENTRAL DETERMINATION STANDS. ***

I STATE NO INVARIANCE, BUILD NOTHING, PROPOSE NOTHING, AND RECLASSIFY NOTHING.
AUDIT ENDS.
```

--- END OF ARTIFACT ---
