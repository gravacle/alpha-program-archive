# STAGE 8 — THE DECIDING DISPLAY: THE EXTENT OF ker(qtilde_N) AT THE ANCHOR STAGES — V001

## BLIND BUILDER — CODENAME IDX-BUILD — [SEALED]

Date: 2026-08-15 (session UTC). Commission: the extent of the relation
ideal ker(qtilde_N) at the anchor stages — the ONE object round 3
(`824eb72f…` + audit `a7255f38…`, consumed at audited grade) proved
decides B-1 essentially outright: at the displayed census, under the
fixed pins, at dim M_N >= 2 with TOP-1/TOP-2, B-1 INHABITED <=> NO
MIXING RELATION at the anchor stages; any mixing relation at a
dim M_N >= 2 stage kills BOTH families (NG-3' under forced PIN-1; the
zero-value case NG-3 caveat-free). The B-3 displays (`98ee31ba…` +
audit `68202c7b…`) showed the displayed relations are exactly two
classes, both pure-cycle; the question here is the ideal's FULL extent.

Fences held throughout: `alpha_computed = false` ; `proof_authorized =
false` ; `kappa_record_computed = false`. DISPLAY/DETERMINATION ONLY —
exact integer/symbolic work on sealed carriers; no member bound, no
candidate authored, no adoption, no route/branch/address/representative
selected, no score/gate moved. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened (one
disclosure at CL-8); "Q-…" tokens EXPECTED-UNLOCATABLE (cited only as
carried inside sealed artifacts). No git; no existing file edited;
output + sidecar probed ABSENT at session start and re-probed ABSENT at
2026-08-15T05:32:22Z immediately before this write; ONE output file
plus its seal sidecar. The battery ran in a FRESH venv (sympy 1.14.0;
15/15 checks PASS; exact integers/rationals/symbols; no float anywhere;
no value frozen).

**SWEEP CUTOFF: 2026-08-15T05:30Z** (SW-1..SW-6 below; anything sealed
after this cutoff is outside this artifact). Exhaustiveness is
sweep-displayed only; nothing absolute is claimed.

---

## 0. VERDICT

```text
VERDICT = NOT-CLOSABLE-TODAY — but the presentation is now closed up to
ONE BIT, and that bit has a sharper name than B-3(ii) gave it.

What THIS artifact closes from sealed structure (new, exact, §4):

  X-1  THE PURE-CYCLE SLICE IS CLOSED EXACTLY. At every displayed
       carried stage (any displayed rank), the a=0 slice of
       ker(qtilde_N) is EXACTLY the one-dimensional (R4) line
       span{(0, 1, -R_K,N)} — not "at least" it. Forced by the
       [PROVABLE] formation of Kernbar_N^cyc on O_N^cyc (LOC (S2a))
       + (B2-7) faithfulness: well-definedness of the sealed scoped
       map bounds the slice ABOVE by the faithfulness kernel, and
       (R4) meets the bound below. Corollaries: j_R4,N is injective
       on the placed cycle span; Rhat_K,N != 0.
  X-2  THE FULL EXTENT MENU AT EACH ANCHOR STAGE HAS EXACTLY TWO
       ENTRIES (exact linear algebra, dim E_N = 3, battery B1-B6):
         E-1 (no mixing): ker = span{(0,1,-R_K)}, dim 1
             — PURE-CYCLE-COMPLETE;
         E-2 (mixing):    ker = span{(0,1,-R_K), (-a_K,1,0)}, dim 2
             — ONE mixing line, coefficient a_K != 0 undisplayed.
       dim ker(qtilde_N) is in {1,2} — no third extent exists.
  X-3  ZERO-CYCLE-SIDE MIXING IS EXCLUDED OF RECORD (given the sealed
       typing L_T,N != 0, CL-3): any mixing relation's cycle-side
       value equals Kernbar(Q(0,b/a,C/a)) = Kern((b R_K + C)/a) != 0,
       because b R_K + C = 0 would force a L_T,N = 0. NG-3's
       antecedent is EMPTY at every stage; the ENTIRE live kill
       surface of the mixing bit is NG-3' (the dim M_N >= 2 cell).
  X-4  THE FOUR ANCHOR BITS COLLAPSE. Via the sealed isometry S_nm
       (LOC-NAT (Q13)) + the R5 covariance cube (LM1-12/13:
       alpha_U(Rel_G) = Rel_G', preserving the a-coordinate), the
       N_n and N_m bits are ONE bit of record. The N_RL/M_RL pair
       collapses the same way IF u's carrier map is an admitted
       isometry (u is typed only "rank-preserving" at fdf20bd4
       l.1258 — the isometry byte is not displayed; conditional).
       The deciding display is AT MOST TWO independent bits, and
       exactly one per isometry-linked class.

What remains open, exactly (§5): whether Rhat_K,N lies in R L_T,N
inside O_R5,N — and this is a question the sealed record already OWNS
under another name: R_K_EQUALS_L_T = NO_VERDICT / TYPE-U (two sealed
flag lines, 70dde1c0 §8 and 041498bb §3.4), with both directions
explicitly unproved ("does not prove: R_K=L_T; R_K lies wholly in the
nonlocal/higher-derivative complement", 041498bb at bytes) and every
closure shape (S1-5) unproved of record. SW-1..SW-6 re-confirm: no
sealed byte fixes the bit. The structural reason it is open: O_R5,N is
a PRIOR completed physical carrier — K/K Schur forms after RetExtract
(W1-4/W1-5: Schur_m = H_KK - H_KC Inv_CC H_CK) — so the C-sector is
consumed INSIDE the Schur complement and no block/direct-sum
separation survives into O_R5,N; (LM1-10) states the quotient "does
not assume they are independent" in so many words.

MISSING CLOSURE OBJECT (named exactly): one sealed certificate, at ONE
anchor stage per isometry class, deciding Rhat_K,N in R L_T,N — i.e.
EITHER (a) an independence certificate (any (S1-5) shape: a certified
decomposition Rhat_K = a_K L_T + R_perp with R_perp != 0, or a
covariant functional pair separating L_T,N from Rhat_K,N), which
yields E-1 = PURE-CYCLE-COMPLETE and, per round 3's biconditional, the
fiber INHABITED (modulo TOP-1/TOP-2 and the dim M_N cell); OR (b) the
exhibited proportionality Rhat_K,N = a_K L_T,N with its exact nonzero
a_K, which IS the mixing relation (-a_K, 1, 0) with cycle-side value
-(1/a_K) Kern(R_K,N) != 0 exactly — by X-3 necessarily NONZERO-valued,
so the kill it arms is NG-3' on the dim M_N >= 2 cell ONLY (at
undisplayed dim M_N the kill does not close; round 3 §5.2).

SK-2 = NOT FIRED ; ACT-3 = NOT FORCED ; D-1 MENU = UNCHANGED.
PIN-2-H: every display below is IDENTICAL under R-A and R-B (no step
touches (JD-3)'s words).
```

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), THIS SESSION, BEFORE RELIANCE

```text
COMMISSION STOCK (read in full)
824eb72f  STAGE8_EXTSRC_ROUND3_S9AD_V001.md              MATCH
a7255f38  STAGE8_EXTSRC_ROUND3_S9AD_AUDIT_V001.md        MATCH (hash verified;
          consumed through the pair's displays)
98ee31ba  STAGE8_B3_DISPLAYS_S9AD_V001.md                MATCH (read in full)
68202c7b  STAGE8_B3_DISPLAYS_S9AD_AUDIT_V001.md          MATCH (hash verified)
CHASED CARRIERS (each verified before its bytes were used)
d662889b  STAGE8_TASK5_EXTSRC_BUILD_LANE3_V002.md        MATCH ((S1)/(S2)/(Q5)/
          (R1)-(R10)/§4.4/(T1)-(T8) at bytes)
b53d9e93  STAGE8_TASK5_EQ6_THE_LOC_BUILD_LANE3_V001.md   MATCH ((S1)-(S8) at
          bytes: (S2a) [PROVABLE] formation; (S8) plus-not-direct; §2.4 route
          bar on assuming R_K=L_T)
bae34116  STAGE8_TASK5_R4_LOCAL_SYMBOL_MAP_BUILD_LANE1_V001.md  MATCH
          ((LM1-6)-(LM1-13), (LM2-2)-(LM2-11) at bytes)
70dde1c0  STAGE8_TASK4B_LOCAL_MAXWELL_SYMBOL_AND_PLOC_RK_LANE2_V001.md  MATCH
          (§2 types; (S1-1)-(S1-5); R_K_EQUALS_L_T flag at bytes)
041498bb  STAGE8_TASK4B_CONSUMPTION_VERIFICATION_AND_PUSHFORWARD_SWEEP_
          LANE1_V001.md                                  MATCH (§3.4-3.5 both-
          directions-unproved display at bytes)
be570c18  STAGE8_TASK4B_STATIONARY_RESPONSE_COMPUTATION_LANE1_V001.md  MATCH
          (§1.2-1.3 blocks/Schur/Response at bytes)
6b7f8f98  STAGE8_TASK4B_RETEXTRACT_KERNEL_CERTIFICATE_LANE1_V001.md    MATCH
          (RetExtract typing and K/K Schur-form domain at bytes)
fdf20bd4  STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md     MATCH (l.1250-1290:
          u typing, (JE-0)/(JE-1a))
5de94e16  STAGE8_TASK5_COMMON_SEAMS_LANE3_V001.md        MATCH (hash verified;
          O_R5,N-topology content consumed via round 3's displays)
abf6d366  square V004 / DoR-017 object                   MATCH at path
          (STAGE8_TASK4A_ACTION_COMPARISON_SQUARE_ADOPTION_PROPOSAL_LANE2_
          V004.md; content consumed via its consumers bae34116/be570c18 at
          audited grade)
MISMATCHES = 0. LOC-NAT (474bf721), PJ2 (3fd4b924), KER (ccb94dfa) clause
bytes consumed at audited grade exactly as quoted inside the commission
stock. The held construction 506bfe2a untouched, unopened, unpresupposed.
```

## 2. SWEEPS RUN (cutoff 2026-08-15T05:30Z; roots = workspace, supervision, cleanroom_output, relay_inbox, relay_outbox; fenced filename classes excluded)

```text
SW-1  "IDEAL_EXTENT" -> zero hits. No competing artifact; the output path
      is first-use (probed ABSENT twice).
SW-2  new-file sweep since round 3's 05:00Z cutoff -> zero new sealed .md
      in any root. Round 3's SW-2 result stands: no mixing-relation
      display has landed anywhere.
SW-3  "R_K_EQUALS_L_T|Rhat_K=a_K L_T|R_K=L_T|proportional to L_T" ->
      exactly three sites (70dde1c0, 041498bb, b53d9e93). The first two
      display NO_VERDICT / TYPE-U with both directions unproved; the
      third BARS assuming R_K=L_T as a route ("not admissible
      alternatives") — a fence on assuming, not a fact either way.
SW-4  "ker(qtilde_N) =|Rel_N = span|basis of ker|generates ker" -> no
      extent-fixing line anywhere (one RA27 hit is a different object:
      a d_0^T cochain basis — disposed).
SW-5  "transverse Maxwell kernel" -> three sites; definitional bytes are
      70dde1c0 §2 ("uniquely normalized ... in O_R5,G") and the BIND
      row ("COMPLETED; it spans the local subspace"); no byte places
      Rhat_K inside or outside span{L_T}.
SW-6  Schur/RetExtract home -> be570c18/6b7f8f98: O_R5's response home
      is the K/K Schur-form class post-extraction; the C-sector enters
      only through Inv_CC inside Schur_m. No sealed block separation
      survives into O_R5,N (this is WHY 70dde1c0 l.13-14 can say no
      ratified relation "places R_K in its complement").
DISCLOSURE: sweep listings displayed fenced-class BASENAMES; one
register-class LINE surfaced in an early listing through a
case-sensitive filter defect (CL-8); its content was not used.
```

---

## 3. THE DEFINING CARRIERS AT BYTES (task 1)

### 3.1 What generates ker(qtilde_N) of record

```text
E_N   := R L_T,N (+) R Rhat_K,N (+) S_(1,sa)(K_N)      ((S1)/(S2) d662889b;
         a formal direct sum — dim E_N = 2 + r(r+1)/2 EXACT, real bytes)
qtilde_N(a,b,C) := a L_T,N + j_R4,N(b R_K,N + C) in O_R5,N   ((Q5); (LM1-9))
Rel_N := closure(ker(qtilde_N)); O_prof,N := E_N/Rel_N       ((LM1-10);
         closure = ker at the finite census, round 3 u2 discharge)
DISPLAYED GENERATORS (B-3 (ii), complete sweep): exactly two classes,
  (R4)  r_x = (0, -t, t R_K,n),  qtilde_n(r_x) = 0   [pure-cycle]
  (R6)-(R7) its q=2 transport image (0, -t, t R_K,m)  [pure-cycle]
— one line per stage, linked by the sealed transport. NOTHING ELSE is
displayed; no sealed law proves (R4) generates every kernel relation
((R8)/§4.4 d662889b at bytes).
```

### 3.2 What the quotient map's sealed definition fixes about its kernel

Three sealed bytes type the kernel as a FACT of a prior carrier, not a
stipulated ideal:

```text
(LM1-10) "Rel_G retains every actual relation between L_T, Rhat_K, and
   the trace-class directions. In particular, the construction does not
   assume Rhat_K = L_T, does not assume they are independent."
(S8) LOC: "The plus sign in (S8) is not declared a direct sum. Mixed
   relations may identify the two displayed pieces."
O_R5,G := "the completed physical retarded action-kernel class"
   (70dde1c0 §2) — a named prior physical carrier: K/K Schur forms
   after RetExtract (W1-4/W1-5, be570c18; 6b7f8f98 "S := an admitted
   K/K Schur form K->K^*").
```

So the presentation is NOT free-modulo-displayed-relations: computing
ker(qtilde_N) requires facts about O_R5,N. The sealed record supplies
exactly one such fact strong enough to close a slice — §4.1.

### 3.3 The anchor stages per round 3's own display

```text
The four anchor stages: N_RL, M_RL (reciprocal pair, generator u),
N_n, N_m (q=2 Ref_path pair, generator r_nm = s_nm) — all cycle rank 1
(B-3 census; round 3 §4 intro). At rank 1 (real bytes; identical under
the complex reading at this rank): dim S_(1,sa)(K_N) = 1, R_K,N spans
it, dim E_N = 3 with coordinates (a, b, c) wrt {L_T-slot, Rhat_K-slot,
P_e-slot}, and qtilde_N(a,b,c) = a L_T,N + (b+c) Rhat_K,N.
```

---

## 4. THE EXTENT, EXACTLY (task 2 — exact linear algebra, battery 15/15 PASS)

### 4.1 The pure-cycle slice is closed [PROVABLE from sealed structure]

```text
LOC (S2a) at bytes: "[PROVABLE] The sealed Q-408 formula constructs a
reader-free path-bilocal kernel on (S2). ... Write that exact scoped
map as Kernbar_N^cyc : O_N^cyc -> Ker_N^Q408."  O_N^cyc := Q_N(E_N^cyc).

The map is FORMED on the quotient of record. Well-definedness forces
   Rel_N ∩ E_N^cyc  subset  ker(Kern ∘ proj) = {(0,b,C): b R_K + C = 0}
                                             = span{(0, 1, -R_K,N)}
(the equality by (B2-7) FAITHFULNESS: Kern(H) = 0 iff H = 0), and (R4)
displays the reverse containment. THEREFORE, at every displayed carried
stage and any displayed rank:
   ker(qtilde_N)|_(a=0) = span{(0, 1, -R_K,N)}   — dim 1 EXACT
(battery B5/B5': the constraint C = -b R_K is 1-dimensional at rank 2
as well, choice-invariantly). COROLLARIES: j_R4,N injective on the
placed cycle span; Rhat_K,N != 0 (else (0,1,0) would be a second
pure-cycle relation, collapsing the slice bound).
CROSS-CHECK: this reproduces B-3 §3.2's audited exact display
dim Kernbar_N^cyc(O_N^cyc) = dim S_(1,sa)(K_N): the quotient loses
exactly one cycle dimension — the (R4) line — and no other.
```

### 4.2 The kernel at an anchor stage — the complete two-entry menu

```text
dim E_N = 3;  im(qtilde_N) = span{L_T,N, Rhat_K,N}, both nonzero
(L_T,N by CL-3; Rhat_K,N by §4.1)  =>  dim im in {1,2}, dim ker in {1,2}.

E-1  L_T,N, Rhat_K,N independent in O_R5,N (NO MIXING):
       ker(qtilde_N) = span{(0, 1, -R_K,N)}          dim 1
     — the ideal IS the displayed generators' span: PURE-CYCLE-COMPLETE.
E-2  Rhat_K,N = a_K L_T,N for an (undisplayed) a_K != 0 (MIXING):
       ker(qtilde_N) = span{(0, 1, -R_K,N), (-a_K, 1, 0)}   dim 2
     — exactly ONE new line; its cycle-side value, exact:
       Kernbar(Q(0, 1/(-a_K), 0)) = -(1/a_K) Kern(R_K,N)  != 0
       by (B2-7) faithfulness. (Battery B1-B3, B6.)
NO THIRD EXTENT EXISTS. In particular the ideal can exceed the
displayed span ONLY by a mixing line — every pure-cycle possibility is
already closed by §4.1.
```

### 4.3 Zero-value mixing is excluded of record

```text
For ANY mixing relation (a,b,C), a != 0, at any displayed carried stage:
   cycle-side value = Kern((b R_K + C)/a);
   value = 0  <=(B2-7)=>  b R_K + C = 0  =>  a L_T,N = 0  =>  L_T,N = 0,
against the sealed typing L_T,N != 0 (CL-3). (Battery B4.)
=> NG-3's antecedent (mixing with ZERO cycle-side value) is EMPTY of
   record. K-b as stated by B-3 can never fire; the mixing kill surface
   is EXACTLY NG-3' — nonzero value, live only on the dim M_N >= 2
   cell. Round 3's CL-5 ("L_T,N = 0 would BE a zero-value mixing
   relation") is the degenerate case of this display, now closed
   against the typing rather than premised.
```

### 4.4 The four anchor bits collapse to at most two

```text
(LM1-12)/(LM1-13) at bytes: for an admitted isometry U : K_G -> K_G',
alpha_U(a,b,C) := (a, b, U C U^*) satisfies Q_G' alpha_U = Alpha_U Q_G
(the ratified R5 covariance cube, abf6d366 by reference), hence
alpha_U(Rel_G) = Rel_G' — an isomorphism of ideals PRESERVING the
a-coordinate (battery B8): mixing at G <=> mixing at G'.
  N_n / N_m:  S_nm is a SEALED rank-preserving bijective isometry
      (LOC-NAT (Q13), consumed at audited grade) => ONE bit of record.
  N_RL / M_RL: u is typed "the actual reciprocal rank-preserving
      generator" (fdf20bd4 l.1258); NO sealed byte at that line types
      its carrier map an admitted isometry => the collapse is
      CONDITIONAL on that one typing byte; otherwise two bits.
=> The deciding display is one bit per isometry class: at most TWO
   independent bits, likely ONE PAIR EACH; each bit is the SAME
   question (§5).
Diamond slots (rank 2, non-anchor, displayed for completeness): the
a=0 slice is still exactly the 1-dim (R4) line (§4.1); the mixing
question widens to L_T,N in j_R4,N(S_(1,sa)) (3-dim span, real) — a
weaker condition than anchor-stage proportionality; outside this
commission's anchor scope.
```

---

## 5. THE ONE OPEN BIT, AND WHY IT IS NOT CLOSABLE TODAY (task 3)

```text
THE BIT: Rhat_K,N in R L_T,N inside O_R5,N?  (= E-2 vs E-1.)

THE SEALED RECORD ALREADY OWNS THIS QUESTION UNDER ANOTHER NAME:
  70dde1c0 §8:  R_K_EQUALS_L_T = NO_VERDICT / TYPE-U
  041498bb §3.4: R_K_EQUALS_L_T = NO_VERDICT / TYPE-U ; §3.5 "It does
    not prove: ... R_K=L_T; R_K lies wholly in the nonlocal/higher-
    derivative complement."  — BOTH directions open at bytes.
  70dde1c0 (S1-5): the three shapes any closure must take —
    Rhat_K = a_K L_T + R_perp with p_loc(R_perp) = 0; or
    Rhat_K in ker(sigma_loc); or
    O_R5/span{L_T} has zero admitted covariant dual —
    "No authority proves any line of (S1-5)."
  b53d9e93 §2.4: assuming R_K=L_T is BARRED as a route ("not
    admissible alternatives") — a fence on assuming, not a verdict.
WHY NO SEALED STRUCTURE CLOSES IT: (i) O_R5,N is a prior completed
carrier, not a presented object (§3.2); (ii) the only candidate
separation — the C_prop/K_cycle block split — does NOT survive
RetExtract: Response_m = RetExtract_m(H_KK - H_KC Inv_CC H_CK), so
L_T,N's home and j_R4,N's image share the one post-Schur K/K class;
(iii) the reader fiber (LM2-3/LM2-4) is DOWNSTREAM of Rel_G — readers
are constrained BY the ideal and cannot fix it; (iv) the local/nonlocal
separation sigma_loc that would decide (S1-5) line 2 is itself typed a
future construction (70dde1c0 §3.1 locality row). SW-1..SW-6 concur.

VERDICT PER THE COMMISSION'S MENU: NOT-CLOSABLE-TODAY.
MISSING CLOSURE OBJECT, exactly: one sealed determination of
R_K_EQUALS_L_T at one anchor stage per isometry class — equivalently
one certificate having an (S1-5) shape (=> E-1, PURE-CYCLE-COMPLETE;
round 3's biconditional then gives B-1 INHABITED modulo TOP-1/TOP-2
and the dim M_N cell), or the exhibited proportionality with its exact
a_K != 0 (=> E-2, the mixing relation (-a_K,1,0) displayed at §4.2
with nonzero cycle-side value -(1/a_K) Kern(R_K,N); kills BOTH
families per NG-3' on the dim M_N >= 2 cell only).
WHAT THIS ARTIFACT CHANGED ABOUT THE GAP: it is ONE bit (not a basis
hunt), it is at most TWO stage-instances (not four), its zero-value
branch is retired (X-3), and it is IDENTIFIED with a question the
corpus has already carried, flagged, and fenced for one task cycle —
so the closure work has a named home, not a blank page.
```

---

## 6. CONSISTENCY

Nothing here binds a member, adopts anything, authors physics, selects
a route/branch/address/representative, or touches the held
construction. The two extents E-1/E-2 are DISPLAYS of the only lawful
possibilities, not candidates; a_K is never valued; no verdict of any
consumed artifact is moved. B-1 remains UNDECIDABLE-STILL of record;
this artifact narrows only the shape of what would decide it. SK-2
does not fire; ACT-3 is not forced; the D-1 menu is unchanged. R9 and
A8 remain UNEXECUTABLE/PENDING. No score, flag, criterion, or gate
moves.

## 7. CHOICE LEDGER (terminal; per the guard)

```text
CL-1  CORPUS BOUNDARY: roots = workspace, supervision, cleanroom_output,
      relay_inbox, relay_outbox minus fenced filename classes;
      a32_holdout not content-swept (holdout discipline). Sweeps
      SW-1..SW-6, cutoff 2026-08-15T05:30Z. A wider sweep changes this
      determination only by exhibiting a sealed R_K_EQUALS_L_T
      determination, an (S1-5) certificate, an exhibited mixing
      relation, or an isometry typing for u. PREMISE(named).
CL-2  AUDITED-GRADE CONSUMPTION: the round-3 pair and B-3 pair consumed
      as commissioned; LOC-NAT/PJ2/KER clause bytes consumed as quoted
      inside them; square V004 (abf6d366) consumed via its verified
      consumers; SEAM1 consumed via round 3's displays. All chased
      carriers seal-verified at path before reliance (§1).
      PREMISE(named).
CL-3  L_T,N != 0 consumed at SEALED-TYPING grade: "the uniquely
      normalized transverse Maxwell kernel in O_R5,G" (70dde1c0 §2),
      the ratified lineage clause p_loc,G[L_T,G] = 1 ((S1-1), from
      13d227ce by reference), and the BIND row "COMPLETED ... it spans
      the local subspace" — the same grade at which OB-2 CL-5 consumed
      M_N^loc != 0. Round 3's CL-5 subsumed this typing into the
      no-mixing bit; here it discharges ONLY the zero-value branch
      (X-3), never the bit itself. PREMISE(named).
CL-4  ANCHOR-STAGE READING: the four stages and their rank-1 typing
      read from round 3 §4 and the B-3 census at audited grade. At
      rank 1 the display is IDENTICAL under the real and complex
      readings (dim S_(1,sa) = dim Herm = 1); no field pin adopted.
      PREMISE(named).
CL-5  COVARIANCE COLLAPSE: (LM1-12)/(LM1-13) applied to S_nm as a
      sealed bijective rank-preserving isometry (LOC-NAT (Q13) at
      audited grade); the u-pair collapse stated CONDITIONALLY on an
      undisplayed isometry typing byte and never asserted.
      PREMISE(named).
CL-6  THE (S2a) READING: "[PROVABLE] ... exact scoped map" (b53d9e93
      l.136-144) read as the formation of Kernbar_N^cyc on the
      quotient O_N^cyc of record — the reading B-3 §3.1/§3.2 already
      consumed at audited grade (its exact dimension display
      presupposes it); §4.1's closure adds no other premise.
      PREMISE(named).
CL-7  EXTERNAL-MATH IMPORTS: finite-dimensional linear algebra only
      (rank-nullity on dim <= 5 carriers) — exact, no numeric content,
      verified in the battery. No topology, no wavefront, no
      Hahn-Banach import is used anywhere in this artifact.
      PREMISE(named).
CL-8  DISCLOSURE: one register-class LINE (a Q-364 headline) surfaced
      in an early SW-3-family grep listing because the fence filter
      was case-sensitive; the defect was caught at once, the filter
      re-run case-insensitively, and the line's content is used
      NOWHERE (the Q-364 object — square V004 adjudication lineage —
      was reached only through its sealed workspace consumers).
      Fenced-class BASENAMES also appeared in listings; no fenced
      file's content was opened, read, or used. PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The battery's model targets (a 2-dim and a 1-dim abstract codomain,
  the identity and one generic SPD 2x2 R_K, one exchange matrix) are
  verification instruments ONLY: they check that the displayed kernels
  are forced by the stated hypotheses (independence vs proportionality,
  faithfulness, the direct-sum coordinates), with a_K and t symbolic
  and never evaluated. No toy value, stage, address, dimension, or
  number enters E-1, E-2, any corollary, or the verdict. The two
  extents are conditional displays over the sealed carriers, not
  constructions; neither is promoted past the existence of the one
  undisplayed bit.
```

## FLAG BLOCK

```text
IDEAL_EXTENT = NOT-CLOSABLE-TODAY ; the presentation is closed up to
  ONE BIT per isometry class of anchor stages
PURE_CYCLE_SLICE = CLOSED EXACTLY (X-1): the a=0 slice of ker(qtilde_N)
  = the (R4) line span{(0,1,-R_K,N)}, dim 1, at every displayed carried
  stage, any displayed rank; j_R4,N injective on the placed cycle span;
  Rhat_K,N != 0
EXTENT_MENU = exactly TWO (X-2): E-1 no-mixing ker dim 1 =
  PURE-CYCLE-COMPLETE ; E-2 mixing ker dim 2 = the one extra line
  (-a_K,1,0), cycle-side value -(1/a_K) Kern(R_K,N) != 0 exact; no
  third extent exists; dim ker in {1,2} forced
NG3_ANTECEDENT = EMPTY of record (X-3): zero-cycle-side mixing is
  impossible given the sealed typing L_T,N != 0; B-3's K-b cannot fire
  as stated; the live mixing kill is EXACTLY NG-3' (dim M_N >= 2 cell)
ANCHOR_BITS = collapse (X-4): N_n/N_m ONE bit of record (sealed S_nm
  isometry + R5 covariance cube); N_RL/M_RL collapse conditional on
  u's undisplayed isometry typing; deciding display <= 2 bits
THE_BIT = Rhat_K,N in R L_T,N inside O_R5,N — IDENTIFIED with the
  sealed question R_K_EQUALS_L_T = NO_VERDICT / TYPE-U (70dde1c0,
  041498bb); both directions unproved at bytes; (S1-5) shapes all
  unproved; assuming it is BARRED as a route (b53d9e93)
MISSING_CLOSURE_OBJECT = one sealed R_K_EQUALS_L_T determination at one
  anchor stage per isometry class: an (S1-5)-shaped independence
  certificate => E-1 => B-1 INHABITED per round 3 (modulo TOP-1/TOP-2,
  dim M_N) ; or the exhibited a_K != 0 => E-2 => NG-3' kill on the
  dim M_N >= 2 cell only
WHY_OPEN = O_R5,N is a prior completed carrier (K/K Schur forms post-
  RetExtract; the C-sector consumed inside Inv_CC — no block
  separation survives); readers are downstream of Rel_N; sigma_loc
  unbuilt; (LM1-10) declines independence in so many words
PIN2H_HANDLING = every display identical under R-A and R-B
SK2 = NOT FIRED ; ACT3 = NOT FORCED ; D1_MENU = UNCHANGED
SEALS = commission 4/4 + chased 9/9 MATCH ; MISMATCHES = 0
SWEEPS = SW-1..SW-6 ; CUTOFF 2026-08-15T05:30Z ; exhaustiveness
  sweep-displayed only ; no competing artifact
BATTERY = fresh venv, sympy 1.14.0, 15/15 PASS, exact symbolic only,
  no float, no value frozen ; NUMERIC_EVALUATION = exact
  integers/rationals/symbols only ; MEASURED_CONSTANT_COMPARISON = none
DETERMINATION_ONLY = held (no member bound, no candidate authored, no
  adoption, no selection, no common cell, no reader, no fixed point,
  no end test) ; MEMBER_BOUND = false ; CHAIN_INVOKED = false
HELD_CONSTRUCTION = untouched, unopened, unpresupposed (506bfe2a by bar)
R9 = UNEXECUTABLE/PENDING ; A8 = UNEXECUTABLE/PENDING
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = the deciding display round 3 asked for is not a basis hunt: the
  ideal's every pure-cycle possibility is closed at sealed bytes, the
  menu of extents has exactly two entries, the zero-value kill branch
  is retired, and the whole of B-1's decider is ONE already-named
  sealed question — R_K_EQUALS_L_T — instanced at most twice across
  the four anchor stages. Nothing moved; the gates are where the
  principal left them.
```
