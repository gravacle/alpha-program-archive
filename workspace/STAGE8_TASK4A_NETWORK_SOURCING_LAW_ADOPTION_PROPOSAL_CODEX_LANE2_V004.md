# Stage 8 Task 4a Network-Sourcing Law Adoption Proposal — Codex Lane 2 V004

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-016/017 RESERVED)**

Date: 2026-08-03  
Task: PASTE 424 / Task 4a / single-law ratification candidate  
Lane: CODEX LANE 2  
Register head: Q-341  
Custody: builder consolidating its reviewed V003; Lane 1 final review required  

```text
NETWORK_SOURCING_LAW_V004 = PROPOSED_NOT_ADOPTED
LIVE_LAW = BRANCH_B_DOUBLED_CTP_ENDPOINT_ACCESS
BRANCH_B_CARRIED_FROM_V003 = UNCHANGED_IN_SUBSTANCE

BRANCH_A = KILLED_BY_DOR015_FAMILY_NATURALITY
BRANCH_A_LIVE_LAW_CONTENT_IN_V004 = none
BRANCH_A_FIXED_FILTRATION_LEMMA = BANKED_ONLY

NETWORK_SOURCING_LAW_V004_SINGLE_LAW_CANDIDATE = true [PROPOSAL STATUS]
READY_FOR_FINAL_REVIEW = yes [PROCESS STATUS; NOT SELF-CERTIFICATION]
REVIEW_CUSTODY = LANE_1

AUTONOMOUS_READY_NETWORK_NO_IGNITION = NO_VERDICT / TYPE-U
ONE_TIER_DELAY = AUTHORED_NOT_UNIQUELY_FORCED

TYPE_P_CLAIMS_IN_THIS_PROPOSAL = 0
RANK_PAIR_SELECTED = false | TYPE-S | scope: every displayed formula
RANK_RATIO_EVALUATED = false | TYPE-S | scope: every displayed formula
ORIENTATION_MEMBER_SELECTED = false | TYPE-S | scope: covariant family only
ENDPOINT_FRAME_SELECTED = false | TYPE-S | scope: full endpoint torsor retained
FILTRATION_MEMBER_SELECTED = false | TYPE-S | scope: killed Branch A and live Branch B
CONTRACTION_SELECTED = false | TYPE-S | scope: only the ratified finite trace is consumed

alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
```

---

## 0. Lead determination

V004 is a single-law ratification candidate. Its live mathematical content
is exactly the Branch-B doubled-CTP endpoint-access law that survived every
item of the firing-3 Lane-1 review. It exports the doubled pair, forms the
relative CTP endomorphism, consumes the exact finite DoR-009 doubled trace,
and builds an ordered pair of per-system projector towers. It does not form a
network amplitude product and does not select a frame, orientation, rank,
filtration, or missing contraction.

Branch A is not carried as a proposed law. Q-341's firing-3 review killed it
because its physical nonzero sector depends on the unratified grouping of
edges into filtration stages. V004 records only the killed disposition, the
fixed-filtration all-prefix theorem as a banked lemma, the two W3 refutations,
and the exact revival condition. None of those statements supplies a live
Branch-A emission map.

```text
V003_PACKAGE_DISPOSITION = KILLED_BECAUSE_IT_CONTAINED_KILLED_BRANCH_A
V004_CONSOLIDATION_MOVE = REMOVE_BRANCH_A_FROM_LIVE_PROPOSAL
BRANCH_B_MATHEMATICS_CHANGED = false [TEXTUAL CARRY-FORWARD AUDIT]
```

---

## 1. Preflight and custody

### 1.1 Preflight

```text
REGISTER_HEAD_REQUIRED = Q-341
REGISTER_HEAD_OBSERVED = Q-341

FIRING_3_RE_REVIEW =
  STAGE8_TASK4A_NETWORK_SOURCING_LAW_V003_RE_REVIEW_LANE1_V001.md
EXPECTED_SHA256 =
  daec1110cefa6ec4328594c59a030a3447807c84acba33a0ed93f12d85643b2a
VERIFIED_SHA256 =
  daec1110cefa6ec4328594c59a030a3447807c84acba33a0ed93f12d85643b2a
RE_REVIEW_SIDECAR_CHECK = PASS

V003_SHA256 =
  51724fae30f6985d4dbf0b4df72a47d0c4ede8dfecdabfd8266d9a60e18296ec

PREFLIGHT = PASS
```

The firing-3 review seal was verified before the review was read. The review
was then read in full. Its controlling verdict is:

```text
BRANCH_A = KILLED | items: T2, T3, T6, T8-A
BRANCH_B = SURVIVES
OVERALL_V003 = KILLED | package contained a killed branch
```

### 1.2 Frozen authorities

| Authority | Standing consumed here |
|---|---|
| `LOCKED_PROCESS.md` | custody, act-based fences, typing, seal/mirror/stop |
| DoR-009 | C1-C8, `E_post`, exact finite doubled ready-record trace |
| DoR-014 plus amendments / germ V007 | source ports, normalization, symbolic `p` discipline |
| DoR-015 / FIELD_SIGNATURE_PHYS V005 | open-path access typing, scalar cycle typing, realization-family discipline, W3 precision |
| Q-335 through Q-341 | receiver seam, corrections, repairs, firing-3 disposition |
| V003 firing-3 review | Branch-B pass, Branch-A kill, W3 correction, six doors |

### 1.3 Custody

This artifact is a proposal and does not self-certify. It performs no
register, governing-plan, tracker, git, commit, or push action. It does not
enter, list, or search the fenced holdout.

---

## 2. Branch-B carry-forward and delta table

The complete reviewed Branch-B Section 6 from V003 is incorporated below
verbatim. Historical references to “V003” inside that block identify the
reviewed source text and are adopted into V004 without semantic alteration.
The live V004 law is that incorporated block subject to V004's consolidated
status, doors, and custody statements.

| V003 Branch-B line class | V004 change | Effect |
|---|---|---|
| Section heading and subsections 6.1-6.6 | none | exact textual carry-forward |
| `(NS3-B1)` through `(NS3-B11)` | none | exact mathematical carry-forward |
| covariance and reality laws | none | exact mathematical carry-forward |
| `Z_N^CTP` finite trace | none | exact mathematical carry-forward |
| `F_(i,t)^B`, `A_(i,k)^B`, ordered network tower | none | exact mathematical carry-forward |
| prohibition on network amplitude product | none | exact standing carry-forward |
| historical token `V003` inside the incorporated block | none | source-version label retained intentionally |

```text
BRANCH_B_CHANGED_LINES_IN_INCORPORATED_BLOCK = 0
BRANCH_B_FORMULA_DELTA = empty
BRANCH_B_SEMANTIC_DELTA = empty
```

---

## 3. Branch A — killed disposition only

**NOT LIVE LAW CONTENT — REVIEWED DISPOSITION AND BANKED LEMMA**

### 3.1 DoR-015 family-naturality kill

Branch A's projective family is a valid construction after a filtration is
fixed, but DoR-015 keeps the complete realization family alive and requires
every downstream consumer either to be natural over that full family or to
carry an independently ratified member-selection rule. Branch A has neither.

The coarse-versus-edge-by-edge counterexample is decisive. Introduce a
reciprocal pair as one stage increment. The increment cycle kernel contains
the nonzero scalar source `s(1,1)`. Introduce the same final pair as two
singleton increments. Each singleton has zero cycle kernel, so all-prefix
conservation forces both coefficients to zero. Thus the source-space
dimensions differ between live realization members; there is no natural
isomorphism carrying the claimed nonzero physical sector across the family.

The one-edge-prefix counterexample is the smallest instance of the same
failure. A displayed reciprocal-loop source has a first singleton prefix
`(s)`, but on a connected one-edge tree

```text
ker(B_1^T)={0}.
```

Hence the prefix is conserved only for `s=0`. Nonemptiness of the reciprocal
loop depends on choosing the coarse member, and no such member is ratified.

```text
BRANCH_A_FAMILY_NATURAL_PHYSICAL_LAW = KILLED_BY_DOR015
BRANCH_A_NONZERO_SECTOR_FAMILY_NATURAL = false | TYPE-R
BRANCH_A_MEMBER_RULE_RATIFIED = false | TYPE-S
BRANCH_A_LIVE_PROPOSAL_CONTENT = none
```

### 3.2 Banked lemma — exact scope

For one fixed declared filtration

```text
C_base -> C_1 -> ... -> C_m,
```

the all-prefix theorem remains correct. A coefficient history belongs to the
projectively conserved family exactly when its base coefficient lies in the
base cycle kernel and each later increment coefficient lies in that
increment's cycle kernel. Equivalently,

```text
q in Hist_A^proj
  iff q_base in ker(B_base^T)
      and r_j in ker(C_j^T) for every fixed increment j.   (BANK-A1)
```

This proves restriction closure and the fixed-filtration reciprocal-loop
algebra fiberwise. It does not prove independence of filtration, provide a
physical family-natural law, or authorize any member selection.

```text
BRANCH_A_FIXED_FILTRATION_ALL_PREFIX_THEOREM = BANKED_LEMMA
BANKED_LEMMA_SCOPE = ONE_INDEPENDENTLY_FIXED_FILTRATION_ONLY
BANKED_LEMMA_IS_LIVE_NETWORK_SOURCING_LAW = false | TYPE-R
```

### 3.3 Revival condition — verbatim

> a ratified filtration/member rule PLUS a family-naturality theorem

Both conjuncts are required. Neither exists in the current ratified stack.

---

## 4. W3 record corrected

For `L<=N<=M`, with isometric inclusions and their adjoint restrictions,

```text
i_NM:C_N->C_M,
rho_MN:=i_NM^*,
rho_MN compose i_NM=Id_(C_N),
rho_ML = rho_NL compose rho_MN.                     (W3-V004-1)
```

The last display corrects V003's ill-typed `NS3-A14`. It does not revive
Branch A. The two standing W3 refutations remain:

1. **Strong history square.** On the S8-A triple
   `q_M=epsilon(1,0,-1)`, raw history restriction gives
   `epsilon(1,0)`, while the W3 adjoint gives
   `(epsilon/2)(1,1)`. Therefore W3 source restriction does not equal raw
   history restriction.
2. **Global C5 character square.** On the dangling-edge closure example,
   the larger coefficient `epsilon(0,0,1,1)` is conserved. Raw restriction
   is `epsilon(0,0,1)` with generically nontrivial C5 character, while W3
   maps the new reciprocal cycle to zero in the old cycle line, whose
   character is one. The agreement on the balanced S8-A substage was only a
   special case.

```text
W3_FUNCTORIAL_ADJOINT_IDENTITY = CORRECTED_AS_W3_V004_1
W3_STRONG_HISTORY_SOURCE_SQUARE = REFUTED | TYPE-R
W3_GLOBAL_C5_CHARACTER_SQUARE = REFUTED | TYPE-R
W3_COMPATIBLE_HISTORY_RESTRICTION = NOT_BUILT / TYPE-U
```

The open W3 door would require a history-level lift of the adjoint source
restriction that commutes with endpoint covariance, reality, exact C5
character preservation, no-post-output supplementation, and the complete
realization family.

---

## 5. Consolidated doors ledger

| Door | Current standing | Exact would-build |
|---|---|---|
| Branch-B transport to scalar source | `NOT_BUILT / TYPE-U` | a family-natural endpoint-transport-to-V007 scalar map with quotient, C5, reality, and restriction certificates |
| Branch-B network `R` | `NOT_BUILT / TYPE-U` | a ratified network bilocal receiver/emitter map with domain, covariance, symmetry, and finite-trace certificates |
| Port/access to write history | `NOT_BUILT / TYPE-U` | an update `(received access, current history)->next compact write history` respecting `E_post`, identity, restriction, and no supplementation |
| Joint two-system state/effect contraction | `NOT_BUILT / TYPE-U` | a joint state, effect, and interaction contraction whose restrictions reproduce both single-record traces |
| Completed physical/continuum contraction | `NOT_BUILT / TYPE-U` | a completed-domain state/effect contraction with topology, contour, boundary, tail, and finite-square certificates |
| W3-compatible history restriction | `NOT_BUILT / TYPE-U` | the history-level W3 lift specified in Section 4 |

```text
DOOR_B_SCALAR_SOURCE = NOT_OPENED / TYPE-U
DOOR_B_NETWORK_R = NOT_OPENED / TYPE-U
DOOR_PORT_TO_HISTORY_UPDATE = NOT_OPENED / TYPE-U
DOOR_JOINT_TWO_SYSTEM_CONTRACTION = NOT_OPENED / TYPE-U
DOOR_COMPLETED_PHYSICAL_CONTRACTION = NOT_OPENED / TYPE-U
DOOR_W3_COMPATIBLE_HISTORY_RESTRICTION = NOT_OPENED / TYPE-U
```

None of these doors is silently discharged by the exact finite DoR-009
single-record trace.

---

## 6. Branch B — doubled CTP endpoint access

**PROPOSED_NOT_ADOPTED — THREE LEVELS KEPT DISTINCT**

### 6.1 Level I: raw doubled endpoint-covariant access

For each oriented open path/cell and CTP branch `sigma in {+,-}`, retain the
endpoint fibers and export

```text
T_sigma[h]:E_(sigma,s)->E_(sigma,t),

d_acc^B[h]:=(T_+[h],T_-[h]).                        (NS3-B1)
```

Under the endpoint torsor family,

```text
T_sigma[g_sigma.h]
  =g_(sigma,t) T_sigma[h] g_(sigma,s)^(-1).         (NS3-B2)
```

No endpoint frame is selected. The pair, not either component alone, is the
emitted object. Under CTP reality, branch exchange and adjoint send the pair
to the corresponding reversed pair; no scalar reality claim is made before
Level II.

### 6.2 Level II: relative CTP endomorphism

For the common physical endpoint identification used by the ratified doubled
trace, define

```text
R_CTP[h]:=T_-[h]^dagger T_+[h]
  in End(E_s).                                      (NS3-B3)
```

Under a common endpoint change on both CTP branches,

```text
T_+ -> g_t T_+ g_s^(-1),
T_- -> g_t T_- g_s^(-1),

T_-^dagger -> g_s T_-^dagger g_t^(-1),

R_CTP ->g_s T_-^dagger g_t^(-1)g_tT_+g_s^(-1)
       =g_s R_CTP g_s^(-1).                         (NS3-B4)
```

The target-frame factors cancel exactly. `R_CTP` is an endomorphism at the
source endpoint and transforms by conjugation. For U(1) its character is
invariant, but V003 retains the conjugation law rather than using abelianity
to erase the carrier.

Independent endpoint changes on `+` and `-` do not cancel; they encode a
relative source transformation and are not part of the common physical gauge
quotient. This is covariance, not an invariance overclaim.

CTP branch exchange gives

```text
R_CTP[Theta h]=R_CTP[h]^dagger,                     (NS3-B5)
```

with the faithful-character law supplying complex conjugation.

### 6.3 Level III: exact finite C5 trace, and the contraction sweep

At a finite receiver stage, the ratified ready-record trace exists. For each
cell `j`, let `z_(+,j)` and `z_(-,j)` be the faithful characters of the two
raw transports. Then

```text
Z_N^CTP[h]
  :=product_(j=1)^N chi_n(R_(CTP,j)[h])
   =product_(j=1)^N conjugate(z_(-,j)^n)z_(+,j)^n. (NS3-B6)
```

This is exactly the DoR-009 finite doubled trace. Since characters are
conjugation-invariant, `(NS3-B6)` is family-wide over the endpoint torsor;
no frame is selected.

The three levels are:

| Level | Object | Standing |
|---|---|---|
| I | `(T_+,T_-)`, two raw endpoint-covariant morphisms | proposed Branch-B emission |
| II | `R_CTP=T_-^dagger T_+`, relative endpoint endomorphism | derived within proposal from Level I |
| III-a | single-receiver finite ready-record trace `Z_N^CTP` | ratified finite C5 consumer, used proposal-conditionally |
| III-b | joint two-system scalar state/effect contraction | not found, `TYPE-U` |
| III-c | completed physical/continuum contraction | not found, `TYPE-U` |

The bounded sweep is unchanged from the re-review: DoR-009 supplies III-a
inside one finite receiver, but Q-335 and the five-door audit find no joint
network contraction and no completed physical contraction. V003 does not
identify III-a with either missing object.

```text
FINITE_SINGLE_RECORD_C5_CONTRACTION = EXISTS |
  PASS_WITHIN_PROPOSAL
JOINT_TWO_SYSTEM_STATE_EFFECT_CONTRACTION = NOT_BUILT / TYPE-U
COMPLETED_PHYSICAL_STATE_EFFECT_CONTRACTION = NOT_BUILT / TYPE-U
```

### 6.4 Tower, only where Level III licenses it

Let `Z_(i,t)^CTP` be the already traced scalar `(NS3-B6)` for receiver `i`
at tier `t`. The exact charged-projector multiplication gives

```text
F_(i,t)^B=P_(0,i)+Z_(i,t)^CTP P_(ch,i),

product_(t=1)^k F_(i,t)^B
  =P_(0,i)+[product_(t=1)^k Z_(i,t)^CTP]P_(ch,i),

A_(i,k)^B
  =(1-p_i)+p_i product_(t=1)^k Z_(i,t)^CTP.        (NS3-B7)
```

This is not V002's bare product of raw endpoint coordinates. Every factor is
the frame-independent result of the exact doubled C5 trace. The complete
network object is only the ordered pair

```text
Tower_AB,k^B:=((F_(1,k)^B,A_(1,k)^B),
               (F_(2,k)^B,A_(2,k)^B)).             (NS3-B8)
```

V003 does not multiply the two amplitudes. Such a product would consume the
missing joint state/effect contraction.

For a repeated doubled history with `Z_(i,t)^CTP=Z_i^CTP`,

```text
A_(i,k)^B=(1-p_i)+p_i(Z_i^CTP)^k.                  (NS3-B9)
```

All `p_i` remain symbolic rank-ratio forms; no pair or value is selected.

### 6.5 S8-B permanent pair regression

Fix `T_+` and take `T_-!=T_-'`. The raw exports differ:

```text
(T_+,T_-) != (T_+,T_-').                            (NS3-B10)
```

Since the transports are unitary on the compact U(1) fibers,

```text
T_-^dagger T_+ != (T_-')^dagger T_+                (NS3-B11)
```

whenever the backward transports differ. Their faithful characters therefore
change generically, and the exact C5 trace detects the change outside its
ordinary character kernel.

```text
S8B_FIXED_FORWARD_VARY_BACKWARD_CHANGES_EXPORT = PASS_WITHIN_PROPOSAL
S8B_RELATIVE_CTP_OBJECT_PRESERVED = PASS_WITHIN_PROPOSAL
S8B_SINGLE_MORPHISM_COLLAPSE_REPEATED = false | TYPE-S |
  scope: every Branch-B signature
```

### 6.6 Branch-B costs preserved

There is still no global map from raw endpoint transport to V007's scalar
`E_J`, and C5 still contains no network `R` receiver. The repaired finite CTP
trace is an access-side consumer, not a construction of either missing port.

```text
GLOBAL_BRANCH_B_SCALAR_dJ = NOT_BUILT / TYPE-U
BRANCH_B_NETWORK_R_RECEIVER = NOT_BUILT / TYPE-U
LOCAL_C5_J_CHART_CHECK = PASS_WITHIN_PROPOSAL
```

---

## 7. Standing preserved

### 7.1 No-ignition — verbatim standing

```text
READY_IDENTITY_FIRST_EMISSION = ZERO_OR_IDENTITY |
  PASS_WITHIN_PROPOSAL

ALL_ZERO_EXTERNALLY_STIPULATED_HISTORY_SEQUENCE = IDENTITY_TOWER |
  CONDITIONAL

AUTONOMOUS_READY_NETWORK_NO_IGNITION = NO_VERDICT / TYPE-U |
  missing: the port-to-write-history update and a zero-fixed-point certificate
```

For the live Branch-B law, identity history gives `(T_+,T_-)=(I,I)`,
`R_CTP=I`, and `Z_N^CTP=1` at the first delivery. No ratified recurrence maps
received access back into the next compact write history, so no autonomous
all-tier inference follows.

### 7.2 Delay and selections

No-post-output supplementation requires a positive delivery delay. It does
not force the smallest positive delay.

```text
POSITIVE_DELAY_REQUIRED = true | inherited constraint
EXACT_ONE_TIER_DELAY_DERIVED = false | TYPE-R |
  counterfamily: every fixed positive integer delay obeys the timing constraints
ONE_TIER_DELAY = AUTHORED_NOT_UNIQUELY_FORCED

P_SELECTED = false | TYPE-S | scope: p remains symbolic
RANK_OR_RATIO_SELECTED = false | TYPE-S | scope: V004
ORIENTATION_OR_FRAME_SELECTED = false | TYPE-S | scope: V004
FILTRATION_SELECTED = false | TYPE-S | scope: V004
MISSING_CONTRACTION_SELECTED = false | TYPE-S | scope: V004
```

---

## 8. Full V004 regression suite

**PROPOSED_NOT_ADOPTED — NO SELF-CERTIFICATION**

### 8.1 R7 one-edge regression

On a one-edge connected tree, the scalar cycle source space is zero. The live
law emits no scalar. It may export a nonidentity doubled endpoint pair on the
access side, which is exactly DoR-015's open-path type.

```text
R7_ONE_EDGE_SCALAR_EMISSION = ZERO | PASS_WITHIN_PROPOSAL
R7_ONE_EDGE_DOUBLED_ACCESS = ALLOWED_COVARIANT_PAIR | PASS_WITHIN_PROPOSAL
R7_ALLOW_TO_REQUIRE_SMUGGLING = false | TYPE-S
```

### 8.2 S8-A triple regression

The triple `q_M=(1,0,-1)` belongs only to the killed Branch-A comparison. It
does not enter the live law, whose input is the doubled transport pair. Its
permanent disposition is:

```text
S8A_TRIPLE_LANDS_IN = KILLED_BRANCH_A_ARCHIVE
S8A_TRIPLE_CONSUMED_BY_LIVE_V004 = false | TYPE-R
S8A_RAW_RESTRICTION = (1,0)
S8A_W3_IMAGE = (1/2,1/2)
S8A_STRONG_SQUARE = REFUTED | TYPE-R
```

### 8.3 S8-B fixed-forward/vary-backward regression

Fix `T_+` and vary `T_-`. The Level-I pair changes, and the Level-II relative
endomorphism changes generically:

```text
(T_+,T_-) != (T_+,T_-'),
T_-^dagger T_+ != (T_-')^dagger T_+.
```

The export therefore cannot collapse to a forward-only morphism.

```text
S8B_PAIR_TEST = PASS_WITHIN_PROPOSAL
```

### 8.4 T8-B identity-zero-extension attack

Append an identity cell with `z_+=z_-=1`. Its relative character is one, so

```text
Z_(N+1)^CTP=Z_N^CTP,
F_(N+1)^B=F_N^B,
A_(N+1)^B=A_N^B.                                   (REG-B1)
```

No source coordinate, frame, or contraction is added. The attack passes.

### 8.5 T8-B reality attack

Branch exchange and the ratified involution give

```text
R_CTP[Theta h]=R_CTP[h]^dagger,
Z_N^CTP[Theta h]=conjugate(Z_N^CTP[h]),
F_N^B[Theta h]=(F_N^B[h])^dagger,
A_N^B[Theta h]=conjugate(A_N^B[h]),                 (REG-B2)
```

with symbolic real `p`. The law is covariant; no frame-invariance upgrade is
made.

### 8.6 Six V001 hostile attacks, where applicable

| Attack | V004 result |
|---|---|
| Additive/replacement offset | **PASS_WITHIN_PROPOSAL:** identity history exports `(I,I)`; no fixed nonidentity offset is admitted |
| State-valued freedom | **PASS_WITHIN_PROPOSAL:** no network `R`, state, density, or joint effect is introduced |
| Declared-data disguise | **PASS_WITHIN_PROPOSAL:** both transports are outputs of the actual doubled history, not external probes relabeled as emission |
| Circularity/target awareness | **PASS_WITHIN_PROPOSAL:** doubled typing and the exact DoR-009 trace fix the construction before any tower output; Branch A is removed by DoR-015, not by an outcome |
| Hidden normalization | **PASS_WITHIN_PROPOSAL:** faithful character and projector weights are inherited from DoR-009; no new scalar-emitter normalization exists |
| History-update attack | **FINDS TYPE-U:** the live export does not generate the next write history; the door remains explicit |

### 8.7 Fresh consolidated-law attack — removal-induced scalarization

**Attack.** Once Branch A is removed, reinterpret `R_CTP` or `Z_N^CTP` as the
missing scalar source so that V004 appears to contain a conventional `d_J`.

**Result.** Killed by type. `R_CTP` is a relative endpoint endomorphism and
`Z_N^CTP` is the output of the already-ratified finite single-record
state/effect trace. Neither is a family-natural map from open-path transport
to V007's scalar physical source dual. Such a reinterpretation would silently
open the first door and repeat R7's allow-to-require error.

```text
REMOVAL_INDUCED_BRANCH_B_SCALARIZATION = REFUTED | TYPE-R
LIVE_LAW_REMAINS_OPEN_PATH_ACCESS = true | PASS_WITHIN_PROPOSAL
```

### 8.8 Fresh consolidated-law attack — accidental network product

**Attack.** Treat the ordered pair of per-system amplitudes as a joint scalar
by multiplying its entries.

**Result.** Refused. No ratified joint state/effect contraction licenses that
product. The ordered pair remains the terminal network object.

```text
NETWORK_AMPLITUDE_PRODUCT_CONSTRUCTED = false | TYPE-U |
  missing: joint two-system state/effect contraction
```

---

## 9. Operation and restriction ledger

| Operation | Domain | Image | Kernel | Restriction/covariance | Tail/class | Standing |
|---|---|---|---|---|---|---|
| doubled raw export | actual doubled histories | `(T_+,T_-)` | common identity history at identity output | branchwise endpoint covariance | finite endpoint torsor | `PASS_WITHIN_PROPOSAL` |
| relative product | doubled pair | `End(E_s)` | pairs with equal relative endomorphism | common gauge acts by conjugation | finite bounded | `PASS_WITHIN_PROPOSAL` |
| finite C5 trace | relative cell endomorphisms | `Z_N^CTP` | faithful-character kernel | conjugation invariant; identity extension exact | finite scalar | existing ratified consumer used proposal-conditionally |
| projector multiplication | traced per-tier scalars | per-system `(F,A)` | ordinary scalar cancellations | exact prefix/identity extension | finite only | `PASS_WITHIN_PROPOSAL` |
| network assembly | two per-system towers | ordered pair | none added | node exchange permutes entries | finite ordered product object, not scalar product | `PASS_WITHIN_PROPOSAL` |
| scalar-source conversion | no map | none | unbuilt | no commuting square | new source class | `TYPE-U` |
| network `R` conversion | no map | none | unbuilt | no commuting square | new bilocal class | `TYPE-U` |
| history update | no map | none | unbuilt | no recurrence square | new dynamics | `TYPE-U` |
| joint contraction | no map | none | unbuilt | no restriction square | new network state/effect | `TYPE-U` |
| completed contraction | no map | none | unbuilt | no physical square | continuum/tail class | `TYPE-U` |

No continuum completion, response, root, `K_*`, or alpha operation is
introduced.

---

## 10. Single-law comparison board

| Question | Live V004 law — Branch B | Killed Branch A record |
|---|---|---|
| Carrier | doubled endpoint-covariant access pair | scalar cycle coefficients on a fixed filtration |
| Required structure | both CTP branches and common endpoint covariance | a ratified filtration/member rule plus family naturality |
| What it permits | nonzero open-path access transport | only a banked fiberwise cycle lemma |
| What it forbids | forward-only collapse, scalar-source relabeling, bare endpoint product, joint amplitude product | any live scalar sourcing claim in V004 |
| Family standing | survives complete realization-family review | killed: nonzero source-space dimension varies by member |
| Exact finite consumer | DoR-009 doubled ready-record trace | none in the live proposal |
| Tower | per-system charged-projector tower, network output ordered only | fixed-filtration tower algebra banked, not operative |
| No-ignition | `NO_VERDICT / TYPE-U` | `NO_VERDICT / TYPE-U` in the historical branch |
| Delay | positive required; one-tier choice authored, not forced | same historical timing standing |
| Main `TYPE-U` residue | six doors in Section 5 | revival requires both stated conjuncts |

This board records the kill; it does not recommend between live alternatives.
There is one live law.

---

## 11. Final board

```text
BRANCH_B_DOUBLED_EXPORT = CARRIED_UNCHANGED | PASS_WITHIN_PROPOSAL
BRANCH_B_RELATIVE_CTP_ENDOMORPHISM = CARRIED_UNCHANGED | PASS_WITHIN_PROPOSAL
BRANCH_B_FINITE_DOR009_TRACE = CARRIED_UNCHANGED | PASS_WITHIN_PROPOSAL
BRANCH_B_PROJECTOR_TOWER = CARRIED_UNCHANGED | PASS_WITHIN_PROPOSAL
BRANCH_B_NETWORK_OBJECT = ORDERED_PAIR_ONLY | PASS_WITHIN_PROPOSAL
BRANCH_B_NETWORK_AMPLITUDE_PRODUCT = NOT_CONSTRUCTED / TYPE-U

BRANCH_A = KILLED_BY_DOR015_FAMILY_NATURALITY
BRANCH_A_FIXED_FILTRATION_THEOREM = BANKED_LEMMA_ONLY
BRANCH_A_REVIVAL_CONDITION =
  a ratified filtration/member rule PLUS a family-naturality theorem

W3_ADJOINT_FUNCTORIALITY = rho_ML = rho_NL compose rho_MN
W3_STRONG_HISTORY_SQUARE = REFUTED | TYPE-R
W3_GLOBAL_C5_CHARACTER_SQUARE = REFUTED | TYPE-R
W3_COMPATIBLE_HISTORY_RESTRICTION = NOT_BUILT / TYPE-U

AUTONOMOUS_READY_NETWORK_NO_IGNITION = NO_VERDICT / TYPE-U
ONE_TIER_DELAY = AUTHORED_NOT_UNIQUELY_FORCED

BRANCH_SELECTED_FOR_OUTCOME = false | TYPE-S
RANK_RATIO_OR_P_EVALUATED = false | TYPE-S
FRAME_OR_ORIENTATION_SELECTED = false | TYPE-S
FILTRATION_MEMBER_SELECTED = false | TYPE-S
MISSING_CONTRACTION_SELECTED = false | TYPE-S

NETWORK_SOURCING_LAW_V004_SINGLE_LAW_CANDIDATE = true
READY_FOR_FINAL_REVIEW = yes
V004_SELF_CERTIFIED = false [PROCESS STATUS; Q54_EXEMPT]
REVIEW_CUSTODY = LANE_1

REGISTER_HEAD_AT_BUILD = Q-341
FENCE_BLOCKED_STRUCTURAL_RESULT = false | TYPE-S |
  scope: every requested structural result was permitted

alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
```

## 12. Custody and stop

Codex Lane 2 seals this lane-tagged V004 artifact, verifies its sidecar,
mirrors both files byte-identically to
`/Users/bgm/MB Work/alpha-program-archive/workspace/`, reports both hashes,
and stops. It does not edit the register, governing plan, tracker, or git
state and performs no commit or push action.
