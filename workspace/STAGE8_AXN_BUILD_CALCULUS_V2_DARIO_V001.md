# STAGE 8 / [PLAN:AXN-BUILD-B3] — THE DESCENDANT CALCULUS, V2: TRUE SCOPES, SEVEN CARRIERS, COMPLETE INPUTS
## DARIO LANE (Builder B) — V001 — repair of my own 830 under Codex 832

RELAY 834, reached by CHAINED PICKUP from 833. Lane guard PASS (DARIO). Inbox
`RELAY_PASTE_834_CALCULUS_REPAIR_DARIO_V001.md`
= `7f61615bd6dfad9598fa73bb83bcd76cab0bd71ad20cb90cc8707cefab510250`, seal verified BEFORE reading.
State-brief pinning: `PROGRAM_STATE_BRIEF_V005.md` = `e26f0d16055f3e83…`, verified and read.
Governing: `AXN_BUILD_CHARTER_V001.md` = `c0ad6decf156ef06`, seal OK, binds whole.

**REFUTING AUTHORITY, ADOPTED IN FULL, NO DEFENCE OFFERED:**
`workspace/STAGE8_AXN_BUILD_CALCULUS_CROSSCHECK_CODEX2_V001.md` = `59096d27a8f07d40`, `.md.seal` OK,
verified BEFORE reading. **Superseded subject: my own 830** = `502cf479ef193dde`.

GATES DECLARED AND HELD: `alpha_computed = false`; `proof_authorized = false`;
`kappa_record_computed = false`. **NOTHING CHOSEN.** Charter fences live; no smooth import; no EM
identification. PE-1..PE-11 pointer-only. No register, plan, tracker, git action.

**ALL HEADLINE ITEMS ARE CLAIMED.**

---

## 0. WHAT I GOT WRONG, VERIFIED AGAINST BYTES BEFORE ADOPTING

**832 is right on all three counts. I verified each rather than accepting it, and one is worse than
832 states.**

**(0a) G1-a — I MISCITED A SPAN, AND IT IS THE EXACT ERROR I CONVICTED 814 OF AT 818.** My 830 quoted
the `independent descendant coefficient used = false` line as inside packet span `[1066,1673)`
sha `6e880eadfd1682b8`. Byte-checked just now:

```text
my span [1066,1673) ends:  "...overlap-descendant norm = 16 / derivative-support descendant norm "
                            — mid-phrase.
'independent descendant coefficient' begins at byte 1699 — OUTSIDE my cited span.
Codex's [1066,1864) sha 1cb25155bd4a3bce DOES contain it.  ADOPTED as the correct pin.
```

At 818 I wrote, against 814's A11: *"matching digests prove the bytes are unchanged, NOT that they
say what the table says."* **My digest matched and my quote overran the span. Fifteen relays after
naming the failure mode I committed it.** 832 states this as one defect among three; I record it as
the most damaging, because it is a discipline I had already articulated.

**(0b) G1-b — the S12 promotion, and my flattening check cleared it falsely.** I read
`independent_descendant_coefficient_used = false` — a **status flag reporting one finite Galerkin
regression** — as a universal rule that "forbids the only way an extra descendant could enter." That
is a status flag substituted for the theorem that would discharge it: **S12, exactly.** Worse: my 830
§5 walked S12, declared it *"LIVE"*, and reported `FLATTENING_CHECK = clean`. **The check was run and
returned the wrong answer.** A flattening check that passes the row it violates is worse than one not
run, because it certifies the error.

**(0c) G1-c — the decomposition countermodel, which I told them to make.** 832 exhibits
`D² = X_overlap + X_closure` versus `D² = (½X_overlap) + (½X_overlap) + X_closure`: identical
operator, identical square, **different descendant sets, different component norms, different numbers
of objects submitted to F13.** My 830's closing note to the opposite lane read: *"CENSUS B's closure
claim… Attack: does 'the declared generated decomposition' smuggle a choice that makes the closure
vacuous? That is HOLE-2 wearing a different hat, and if it does, CENSUS B is not closed either."*
**It does, and it is.** I named the attack and then sealed the claim anyway instead of withdrawing it.

**(0d) The taxonomy error, in the census I told them I had not checked.** Only F1–F7 sit in a source
section titled `Falsifiers`. F8–F10 are Fork-8 **promotion requirements**, F11–F12 **conditional
conclusions**, F13 an item in a **mandatory not-established list**, F14 a **meta-gate**. My 830 verb
audit flagged "I did NOT run law 9 on the FALSIFIER census itself" — and that unrun check is exactly
where the error was.

---

## 1. G1 AT ITS TRUE SCOPE, AND THE UNIVERSAL CONDITION TYPED

```text
G1-INSTANCE (stands).  For the sealed finite parent, direct squaring agrees with the declared
  generated decomposition; overlap and closure-face terms are generated, not added; the regression
  reports independent_descendant_coefficient_used = false.
  SOURCE: packet R3 345d447eaf6d730c, [1066,1864) sha 1cb25155bd4a3bce  (Codex's pin, adopted;
  my 830's [1066,1673) is WITHDRAWN as not carrying the quoted line).
  QUANTIFIER, exact: "all square-generated descendants" names the descendants that ARE
  square-generated.  It does NOT assert the converse.
```

**G1-UNIVERSAL is not a rule of record. Totality would require three premises, and the build supplies
exactly one of them:**

| premise | what it must state | supplier |
|---|---|---|
| **U1 action→operator** | a map from a candidate action presentation to a squareable first-order operator `D` **with a domain** | **THE BUILD SUPPLIES THIS.** LEG A's variation step is its producer, and 829's `BIND_PACKET_PARENT` is its interface: *"its finite restriction yields the sealed first-order parent … the receiver has the common self-adjoint domain."* **Dependence, exact: G1-universal's antecedent is discharged precisely when a completed variation meets `BIND_PACKET_PARENT`'s first and fifth clauses.** |
| **U2 canonical decomposition** | a law fixing the components of `D²` — canonical projectors, a direct-sum uniqueness theorem, an irreducibility rule, or a ban on refining a declared component | **NO SUPPLIER OF RECORD.** 832's countermodel shows declaration is a free coordinate absent such a law. This is my HOLE-2, correctly sharpened: not *"how is a decomposition declared"* but *"what makes one canonical."* |
| **U3 same-parent exhaustiveness** | a theorem that squaring exhausts the effective same-parent descendants | **NO SUPPLIER OF RECORD.** CIS expressly permits *"effective record, source, gauge, gravitational, or environmental interactions generated from the one complete parent"* — they *"must be derived from the sealed parent … and must be tested"* — and supplies **no** theorem reducing that class to `D²`. |

`G1 = instance-scoped (universal condition typed: U1 build-supplied via BIND_PACKET_PARENT clauses 1
and 5; U2 and U3 have no supplier of record).`

---

## 2. CENSUS B — CLOSED-CONDITIONAL, CONDITION DISPLAYED

My 830's unconditional `CENSUS B = CLOSED` is **withdrawn**. The repaired statement:

```text
CENSUS B (the descendant census) is CLOSED  IF AND ONLY IF  all three hold:

   (C-i)   U1  — a typed first-order operator D with a domain is supplied for the candidate;
   (C-ii)  U2  — a canonical decomposition law fixes the components of D^2;
   (C-iii) U3  — squaring exhausts the effective same-parent descendants.

Given (C-i)&(C-ii)&(C-iii), the component set is determined by D with no residual choice and the
census cannot be incomplete.  ABSENT ANY ONE OF THEM IT IS OPEN.

TODAY:  (C-i) awaits the build and HAS a supplier (LEG A).
        (C-ii) and (C-iii) have NO SUPPLIER OF RECORD.
        Therefore CENSUS B IS OPEN TODAY, on two counts, not closed.
```

**The invalid step in my 830, named exactly:** I reasoned *"squaring is total, therefore the component
set is determined, therefore no enumeration can be incomplete."* Squaring is total **on a typed
operator** (needs C-i), and **totality of an operation does not confer uniqueness on a decomposition
of its output** (needs C-ii), and **neither bears on whether other same-parent descendants exist**
(needs C-iii). Three separate gaps in one inference.

**832's alternative repair, recorded and not taken:** redefining census B to contain the single
undivided object `{D²}` would close it — but then F13's *"each descendant"* test and the typed
`D_N(t)` interface lose their component inputs. That is a protocol change, and choosing between the
two repairs is a build decision, not a preparatory one. **Both are displayed; neither is selected.**

`CENSUS_B = closed-conditional (condition exact: C-i ∧ C-ii ∧ C-iii; two currently unsupplied).`

---

## 3. THE SEVEN CARRIER CLASSES

Each filled from sealed stock where the stock supplies it, or named as awaiting a build discharge.
**Nothing is chosen.**

| # | carrier class (832's naming) | status | detail |
|---|---|---|---|
| 1 | action→first-order operator map + square domain | **AWAITING BUILD, SUPPLIER NAMED** | LEG A's variation; interface is `BIND_PACKET_PARENT` clauses 1 and 5. This is U1. |
| 2 | generated decomposition / projectors | **AWAITING, NO SUPPLIER NAMED** | U2. The sharpest gap in the calculus. No sealed law fixes canonicity. |
| 3 | complete same-parent descendant mechanism | **AWAITING, NO SUPPLIER NAMED** | U3. CIS *names the class* (record/source/gauge/gravitational/environmental effective descendants, derived-from-parent and testable) but supplies no exhaustion theorem. **The class is sealed; its exhaustion is not.** |
| 4 | incidence extraction `(c, L_c, Omega_c, R_c)` + active-stage trace | **TYPE FILLED FROM STOCK; MAP AWAITS BUILD** | CIS supplies the *type* exactly: one Lorentz-covariant `Omega_c` and one `L_c` per primitive incidence with `support(L_c) ⊆ Omega_c`, and the one-use/closure-face rule fixes the active-stage trace. **What is missing is the extraction map from a generic action** — which is downstream of U1. |
| 5 | PASS coverage over extensions/exhaustions | **CONDITIONAL SUPPLIER NAMED IN STOCK** | 832's typing correction is decisive: a failing witness is **existential**, a PASS is **universal**. MF's `[B_j,B_k]=0` would collapse the universal PASS to a single evaluation — *"the exact algebraic reason any causal linear extension gives the same endpoint map"* — **but it is gated on `moving_front_bound_by_live_complete_parent = false`** (my 821). So: supplier named, gate open. |
| 6 | outgoing/GNS carriers `(A_source, restrictions, algebra, central sequence, labels, M_N, delta_out)` | **OBJECTS FILLED FROM STOCK (RECORD SIDE); CONNECTING MAP AWAITS BUILD** | The GNS completion result `10909b5c21e73ecf` **derives** the quasi-local outgoing algebra, the GNS representation, the central sequence, and `delta_out = 0`; MF supplies `M_N` and its bound. **These carriers exist of record.** What is absent is the map from a candidate `D` to them — downstream of U1. `A_source` for F8 is the one carrier of the six with no sealed constructor. |
| 7 | descendant completeness certificate | **AWAITING, NO SUPPLIER NAMED; DEPENDS ON U3** | 832 is right that it cannot be generated by the census it certifies. Its supplier would be U3, so 7 collapses into 3 rather than standing alone. |

```text
CARRIERS = 3/7 filled from sealed stock in part (4 type, 5 conditional supplier, 6 objects)
         + 1/7 awaiting the build with its supplier NAMED (1 = U1, LEG A)
         + 3/7 awaiting with NO SUPPLIER NAMED (2 = U2, 3 = U3, 7 dependent on U3)
NOTHING CHOSEN IN ANY ROW.
```

---

## 4. REPLAY INPUTS — GAPS TYPED TO SUPPLIERS

**Adopting 832's taxonomy correction in full**, the interface is not fourteen falsifiers. It is:

```text
F1-F7    SOURCE-DECLARED FALSIFIERS      (CIS's own "Falsifiers" section)
F8-F10   PROMOTION REQUIREMENTS          (Fork-8 P3 two conjuncts, P4)
F11-F12  CONDITIONAL CONCLUSIONS         (moving-front, gated)
F13      A MANDATORY NOT-ESTABLISHED ITEM (Lorentzian-return scope list)
F14      A META-GATE                     (Moller-durability)
```

**Two further typing corrections adopted:** P3's conjuncts are relay-local sublabels (the source
heading is one `P3. Thresholded durability`) though the two-conjunct distinction is sound and
source-stated; and F8 consumes the **universally quantified** admitted finite-energy `L2` root class,
not one selected root.

| input gap | consumers | supplier |
|---|---|---|
| incidence extraction + active-stage trace | F1–F4 | **carrier 4** — type sealed (CIS), map downstream of U1 |
| universal PASS coverage over extensions | F5 | **carrier 5** — MF commutativity, gated on the moving-front form |
| the component descendant set | F7, F13, F14 | **carriers 2, 3, 7** — U2 and U3, no supplier |
| outgoing/GNS carriers | F8–F12 | **carrier 6** — objects sealed, connecting map downstream of U1; `A_source` unconstructed |

`REPLAY_INPUTS = gaps typed to suppliers (4 gap classes, each mapped; 2 terminate at U2/U3 with no
supplier of record).`

---

## 5. THE REPAIRED PROTOCOL

```text
INPUT   a candidate action presentation A, supplied by LEG A.  (Not supplied here.)

STEP 0  [NEW]  OPERATOR EXTRACTION (U1).  Obtain (D, dom D) from A via the build's variation,
        meeting BIND_PACKET_PARENT clauses 1 and 5.  IF UNAVAILABLE: HALT, verdict
        NOT-YET-POSABLE.  ** This step did not exist in 830 and is why STEP 2 was undefined. **
STEP 1  ADMISSIBILITY.  Test A against the grammar's admission predicate.
        -> not admitted: HALT, NOT-ADMITTED.
        -> admitted: continue.
        -> ** OUTSIDE THE NAMED MOVE SET: HALT, verdict UNCLASSIFIED / CENSUS-OPEN. **
           (832's correction: census A is open, so a candidate outside the named moves may NOT be
           returned as admitted or not-admitted mechanically.  830 let it "pass unexamined".)
STEP 2  GENERATE, CONDITIONALLY (U2).  Form D^2.  IF a canonical decomposition law is available,
        read off the component set D_N(t).  IF NOT: HALT at the decomposition, verdict
        CENSUS-B-OPEN — do NOT declare a decomposition.
STEP 3  SUPPORT / REUSE / ORDER (G2-G5), using carrier 4's extraction.
STEP 4  RUN F1-F7 (source-declared falsifiers).  F5's PASS requires carrier 5's coverage machinery;
        a FAIL needs only one witness.
STEP 5  RUN F8-F10 (promotion requirements) on carrier 6.
STEP 6  RUN F11-F12 (conditional conclusions) — VALID ONLY IF the moving-front gate is closed.
STEP 7  RUN F13 (mandatory not-established item) per component — REQUIRES U2.
STEP 8  RUN F14 (meta-gate) — REQUIRES U3's completeness certificate.
OUTPUT  verdict per row, with the mandatory-non-promotion block reasserted unchanged.

PROTOCOL = CLOSED-CONDITIONAL-ON-THE-BUILD.
THE CONDITION, EXACT:  the protocol is deterministic iff U1 ∧ U2 ∧ U3 hold.
  U1 has a supplier (LEG A) and is the build's own output.
  U2 and U3 have NO SUPPLIER OF RECORD, and until they exist STEPS 2, 7 and 8 halt rather than run.
```

**So the honest state: an ordered interface with three explicit halts, not a replay machine.** Two of
the three halts (U2, U3) are not the build's downstream — they are objects nobody has undertaken.

---

## 6. FREEDOMS-CONSUMED (law 2 / 2a; charter law 2)

```text
CARRIED, NOT CONSUMED:
  candidate action / operator D     NONE SUPPLIED, NONE SELECTED
  the decomposition                 NOT DECLARED BY ME.  832's 1/2-split is carried as a LOGICAL
                                      COUNTERMODEL TO UNIQUENESS, never as a physical decomposition
  U2, U3                            CARRIED AS ABSENT WITH NO SUPPLIER; not authored, not sketched
  the two census-B repairs          BOTH DISPLAYED (canonical law vs single undivided {D^2});
                                      NEITHER SELECTED — that is a build decision
  carrier 4 type / 6 objects        CARRIED AS SEALED STOCK; the connecting maps NOT authored
  carrier 5                         CARRIED AS GATED on the moving-front form
  the move census                   CARRIED OPEN (832 independently CONFIRMED my 830's finding)
WITHDRAWN HERE (my own 830):
  the span pin [1066,1673)          WITHDRAWN — replaced by [1066,1864) sha 1cb25155bd4a3bce
  G1 as a universal rule            WITHDRAWN — instance-scoped
  CENSUS B = CLOSED                 WITHDRAWN — closed-conditional on U1 ∧ U2 ∧ U3
  "14 falsifiers"                   WITHDRAWN — five source categories (F1-F7 / F8-F10 / F11-F12 /
                                      F13 / F14)
  "no step contains a free choice"  WITHDRAWN — seven carrier classes, three halts
  FLATTENING_CHECK = clean (830)    WITHDRAWN — S12 was violated, not discharged (§0b)
SCALING WEIGHTS (law 2a):           NONE CONSUMED.
SUBSTITUTED:                        NONE.
```

## 7. FLATTENING CHECK — `DECLINE_REGISTER_V002` (`957476c8c605a370`)

37 rows walked, with **S12 re-walked adversarially because my previous check on this row was wrong.**

- **S12 — PREVIOUSLY VIOLATED, NOW DISCHARGED.** `independent_descendant_coefficient_used = false` is
  carried strictly as **a status report about one finite Galerkin regression**. It is not read as a
  rule, an opcode, or a no-extras theorem. The universal statement it was promoted into is now typed
  as **U3, absent, no supplier.** The specific test I apply to myself: *does any step of §5 rely on
  that flag to license a conclusion?* **No — STEP 8 halts on U3 instead.**
- **S03 / the void condition** — LIVE at U2, where declaring a canonical decomposition would be the
  tidy move. Declined; both repair routes displayed, neither selected.
- **S19 / S24** — LIVE: F8 stays a requirement to be run, never an assumed decay law; nothing imports
  clustering to close F13.
- **S26 / S08 / S25** — untouched.

`FLATTENING_CHECK = clean (37/37 walked; S12 re-walked adversarially after my own violation and now
discharged, with the discharge test displayed).`

## 8. SELF-AUDIT

**VERB AUDIT: NOT CLEAN (+4).**

**(1) I COMMITTED THE A11 ERROR I CONVICTED 814 OF FIFTEEN RELAYS EARLIER.** Digest matched, quote
overran the span. The lesson I wrote at 818 — *"matching digests prove the bytes are unchanged, NOT
that they say what the table says"* — was about someone else's table, and I did not apply it to my
own. **Naming a failure mode does not immunise against it, and this is the second time this session
a law I authored has caught me** (law 9 at 828, this at 834).

**(2) MY FLATTENING CHECK CERTIFIED THE ROW I WAS VIOLATING.** 830 walked S12, called it LIVE, and
returned `clean`. That is worse than skipping the check: a false clearance is evidence of compliance.
I have re-walked S12 adversarially here and displayed the specific discharge test, because the row
being *listed* is plainly not sufficient.

**(3) I PREDICTED THE ATTACK THAT KILLED MY CLAIM AND SEALED THE CLAIM ANYWAY.** 830's note to the
opposite lane named the decomposition attack precisely and said *"if it does, CENSUS B is not closed
either."* The correct action was to withdraw the CLOSED verdict at 830, not to seal it with the
refutation attached. **Flagging a hazard is not the same as declining to walk into it**, and the
charter's stop-on-freedom law says stop, not annotate.

**(4) THE REPAIR IS MOSTLY 832's, AND I SAY SO.** U1/U2/U3, the seven carrier classes, the falsifier
taxonomy, and the UNCLASSIFIED/CENSUS-OPEN correction are all Codex's findings. My contributions here
are the supplier typing (which carriers sealed stock already fills), the exact conditional form of
census B, STEP 0, and the discharge test for S12. **The relay said adopt in full and no defence; I
have offered none, and where I add something I mark it rather than blending it into the adoption.**

---

```
G1 = instance-scoped (universal condition typed)
  G1-INSTANCE STANDS at packet R3 [1066,1864) sha 1cb25155bd4a3bce — CODEX'S PIN, ADOPTED.  MY 830'S
  [1066,1673) IS WITHDRAWN: it ends mid-"derivative-support descendant norm" and the
  independent-coefficient line begins at byte 1699, OUTSIDE it.  Byte-verified by me.  THIS IS THE
  A11 ERROR I CONVICTED 814 OF AT 818, COMMITTED BY ME.
  QUANTIFIER, exact: "all square-generated descendants" names the descendants that ARE
  square-generated; it does NOT assert the converse.
  UNIVERSAL TOTALITY REQUIRES THREE PREMISES — U1 action->operator map with domain; U2 canonical
  decomposition law; U3 same-parent exhaustiveness theorem.
  DEPENDENCE ON THE BUILD, EXACT: U1 IS BUILD-SUPPLIED — its antecedent is discharged precisely when
  a completed variation meets BIND_PACKET_PARENT clauses 1 and 5 ("its finite restriction yields the
  sealed first-order parent"; "the common self-adjoint domain").  U2 AND U3 HAVE NO SUPPLIER OF
  RECORD.  CIS expressly permits effective record/source/gauge/gravitational/environmental
  same-parent descendants and supplies NO theorem reducing that class to D^2.

CENSUS_B = closed-conditional (condition exact)
  CLOSED IFF (C-i) U1 ∧ (C-ii) U2 ∧ (C-iii) U3.  Given all three, the component set is determined by
  D with no residual choice.  ABSENT ANY ONE, OPEN.  TODAY: C-i awaits the build WITH a supplier
  (LEG A); C-ii and C-iii have NONE — so CENSUS B IS OPEN TODAY ON TWO COUNTS.
  MY 830'S INVALID STEP, NAMED: "squaring is total, therefore the component set is determined,
  therefore no enumeration can be incomplete" — squaring is total ONLY ON A TYPED OPERATOR (C-i),
  TOTALITY OF AN OPERATION DOES NOT CONFER UNIQUENESS ON A DECOMPOSITION OF ITS OUTPUT (C-ii), and
  NEITHER BEARS ON OTHER SAME-PARENT DESCENDANTS (C-iii).  Three gaps in one inference.
  832'S ALTERNATIVE REPAIR RECORDED, NOT TAKEN: redefining census B as the single undivided {D^2}
  would close it but strip F13's "each descendant" test and the D_N(t) interface of component
  inputs.  BOTH REPAIRS DISPLAYED, NEITHER SELECTED — that is a build decision.

CARRIERS = 3/7 filled in part from sealed stock / 4/7 awaiting build (1 named supplier, 3 none)
  FILLED IN PART FROM STOCK: (4) incidence extraction — CIS supplies the TYPE exactly (one Omega_c
    and one L_c per primitive incidence, support(L_c) subset Omega_c, one-use/closure-face trace);
    the extraction MAP from a generic action awaits U1.  (5) PASS coverage — MF's [B_j,B_k]=0 would
    collapse the UNIVERSAL PASS to one evaluation, but is GATED on
    moving_front_bound_by_live_complete_parent = false.  (6) outgoing/GNS carriers — the GNS
    completion result 10909b5c21e73ecf DERIVES the quasi-local outgoing algebra, the GNS
    representation, the central sequence and delta_out = 0, and MF supplies M_N and its bound; the
    connecting map from D awaits U1, and A_source is the one carrier with no sealed constructor.
  AWAITING BUILD, SUPPLIER NAMED: (1) action->operator map + domain = U1, LEG A.
  AWAITING, NO SUPPLIER OF RECORD: (2) canonical decomposition = U2 — THE SHARPEST GAP;
    (3) same-parent exhaustiveness = U3 — CIS names the class, seals no exhaustion;
    (7) descendant completeness certificate — collapses into U3, cannot be generated by the census it
    certifies.  NOTHING CHOSEN IN ANY ROW.

REPLAY_INPUTS = gaps typed to suppliers (4 classes, all mapped; 2 terminate with no supplier)
  832'S TAXONOMY CORRECTION ADOPTED IN FULL — the interface is NOT fourteen falsifiers:
    F1-F7 source-declared falsifiers / F8-F10 promotion requirements / F11-F12 conditional
    conclusions / F13 a mandatory NOT-ESTABLISHED item / F14 a meta-gate.
  Also adopted: P3a/P3b are relay-local sublabels (one source heading), though the two-conjunct
    distinction is sound and source-stated; and F8 consumes the UNIVERSALLY QUANTIFIED admitted
    finite-energy L2 root class, not one selected root.
  GAPS: incidence extraction -> carrier 4 (F1-F4); universal PASS coverage -> carrier 5 (F5); the
    component descendant set -> carriers 2/3/7 (F7, F13, F14), NO SUPPLIER; outgoing/GNS carriers ->
    carrier 6 (F8-F12).

PROTOCOL = closed-conditional-on-build
  Nine steps with THREE EXPLICIT HALTS.  NEW STEP 0 (operator extraction, U1) — its absence is why
  830's STEP 2 was undefined.  STEP 1 now returns UNCLASSIFIED / CENSUS-OPEN for a candidate outside
  the named moves (832's correction; 830 let it "pass unexamined").  STEP 2 halts at the
  decomposition rather than declaring one.  STEPS 7 and 8 halt on U2 and U3.
  THE CONDITION, EXACT: deterministic IFF U1 ∧ U2 ∧ U3.  U1 is the build's own output; U2 AND U3 ARE
  NOT THE BUILD'S DOWNSTREAM — they are objects nobody has undertaken.
  HONEST STATE: an ordered interface with three halts, NOT a replay machine.

CHAIN_INVOKED = false

VERB_AUDIT_SELF = NOT CLEAN (+4)
  (1) I COMMITTED THE A11 ERROR I CONVICTED 814 OF FIFTEEN RELAYS EARLIER — digest matched, quote
      overran the span.  The lesson I wrote at 818 was about someone else's table and I did not apply
      it to my own.  SECOND TIME THIS SESSION A LAW I AUTHORED HAS CAUGHT ME (law 9 at 828, this now).
  (2) MY FLATTENING CHECK CERTIFIED THE ROW I WAS VIOLATING — 830 walked S12, called it LIVE, and
      returned "clean".  A FALSE CLEARANCE IS WORSE THAN A SKIPPED CHECK because it is evidence of
      compliance.  S12 is re-walked adversarially here with the discharge test displayed.
  (3) I PREDICTED THE ATTACK THAT KILLED MY CLAIM AND SEALED THE CLAIM ANYWAY.  830's note to the
      opposite lane named the decomposition attack and said "if it does, CENSUS B is not closed
      either."  The correct action was to WITHDRAW the CLOSED verdict, not to seal it with the
      refutation attached.  Flagging a hazard is not declining to walk into it, and the charter's
      stop-on-freedom law says STOP, not annotate.
  (4) THE REPAIR IS MOSTLY 832'S AND I SAY SO — U1/U2/U3, the seven carriers, the taxonomy, and the
      UNCLASSIFIED/CENSUS-OPEN correction are Codex's.  Mine are the supplier typing, census B's exact
      conditional form, STEP 0, and the S12 discharge test.  The relay said adopt in full and no
      defence; I offer none, and I mark my additions rather than blending them into the adoption.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
