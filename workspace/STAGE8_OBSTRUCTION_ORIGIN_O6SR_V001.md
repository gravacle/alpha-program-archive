# STAGE 8 — THE OBSTRUCTION-ORIGIN TRACE: DO THE BOUNDARY-CLOSURE'S OBSTRUCTIONS COME FROM GAUGE/EM STRUCTURE? — O6SR V001

## BUILDER — CODENAME ORIGIN-BUILD — COMMISSION O6SR — 2026-08-15

DETERMINATION ONLY. No authored physics. No value, no number, and no
measured-constant comparison is produced for any program quantity. Every
display below is either (i) sealed bytes quoted, (ii) an identity or an
exponent difference verified exactly and symbolically, or (iii) an exact
absence stated as an absence. FENCES HELD: `alpha_computed = false`;
`proof_authorized = false`; `kappa_record_computed = false`.

---

## 0. VERDICT IN ONE LINE

**MIXED — AND THE MIXTURE IS LOPSIDED IN A WAY THAT ANSWERS THE QUESTION
NEGATIVELY AT EVERY BINDING BLOCKER. Of the blockers standing at the three
failure points, EXACTLY ONE is gauge-originated — W-2's H-3 SECTION-GAUGE
HALT, whose own carrier declares it "residual, NON-BINDING" — and EVERY
BLOCKER THAT ACTUALLY BINDS IS NOT GAUGE-ORIGINATED. FP-1's blocker is a
COUNTING fact (rank x op: the mode count n^3 against an n-free target); it
survives verbatim at spinor dimension d = 1, the spinor dimension entering
only as the n-free constant sqrt(d) (O6-5). FP-2's binding blockers are a
DOMAIN fact (H-1: the actual datum `kappa_T,RL` is unformed, JB-2
undischarged — the composite's actual-stock domain is EMPTY) and an
IDENTIFICATION fact (H-2: the two cell descriptions are OVERLAPPING, not
IDENTICAL, `COMMON_CELL_FORMED = false`), on top of the WBC's own LOCUS fact
(the fatal |x-y|^-3 integral sits at the VOLUME DIAGONAL, which
boundary-supported structure provably does not reach) — and that locus
divergence is fixed by the spatial dimension 3 alone, 3 - 3 = 0 => log,
reading no spinor and no gauge index (O6-6). FP-3's blocker is a SYMMETRY
fact — translation invariance, exactly as the W-3 run derived — but the
symmetry is NOT gauge/EM in origin, and this is settled two ways at bytes.
FIRST, THE COUNTERFACTUAL, EXECUTED: the sea-sandwich obstruction survives
in full on a SCALAR, SPIN-FREE, GAUGE-FREE content class. Its four
hypotheses split cleanly — (h-1) C translation-invariant, (h-2) the class
translation-invariant, (h-3) the class contains a translation-invariant
member are ALL properties of CONSTANT-COEFFICIENT content, carrying no
index whatever; only (h-4), the sandwich's NON-VANISHING, reads the content,
and what it reads is INDEFINITENESS OF THE GENERATOR'S SPECTRUM, not gauge.
At the record's own site that indefiniteness is supplied by the Clifford
algebra (A = alpha.k-hat, A^2 = I, spectrum {+1,-1}, tr P_- = 2, exact —
O6-1a), i.e. by SPINOR structure; and O6-1c reproduces the entire blocker on
`L^2(R^3) (x) C^1` with the scalar indefinite symbol m(k) = k_3, where
C = 1_{k_3<0} is a nonzero translation-invariant projection, C m C is
nonzero, and the same two-line theorem returns non-compactness — NO Clifford,
NO spinor index, NO U(1). Conversely a scalar DEFINITE symbol m(k) = |k| has
{m < 0} EMPTY, so C = 0 and the theorem goes VACUOUS (O6-1b): the escape from
the blocker is definiteness, not gauge-neutrality. SECOND, AND SHARPER, THE
GAUGE COUNTERFACTUAL RUNS THE WRONG WAY: switching EM structure ON DESTROYS
the blocker's hypotheses rather than creating them. A global U(1) phase is
central and unitary and changes neither ||h_0 psi|| nor ||psi||, hence
neither creates nor removes anything (O6-1d1/d2); a LOCAL U(1) — a genuine
non-constant potential A(x) — makes the generator's symbol depend on the
POSITION variable, so it is no longer a Fourier multiplier and hypotheses
(h-1)/(h-2) FAIL (O6-1d3/d4); only a CONSTANT (pure-gauge) potential restores
invariance (O6-1d5). GAUGE STRUCTURE IS THE ESCAPE'S TYPE, NOT THE BLOCKER'S
ORIGIN — and this is the same escape W-3 already named from the other side
("an input that is NOT translation-invariant ... consumes the sharp vertex's
own SPATIAL LOCALIZATION"). THE TOKEN CENSUS CONFIRMS IT AT BYTES: the entire
FP-3 chain — WALL, CERT, FHB, CL-ERROR, BL2*, RL2B-R, and the W-3 pair —
contains ZERO word-boundary occurrences of `Ward`, `gauge`, `Maxwell`,
`U(1)`, `electromagnet`, `photon`, or `connection`; the COMPLETION MAP pair
contains zero of all seven; the five `charge` hits in the W-3 build are all
the VERB ("the Gronwall charges an H^2 quantity"), none electric. And the
corpus's actual gauge/EM lane — the local-Maxwell-symbol and
kernel-realization artifacts — contains ZERO occurrences of `h_0`, `C-L2`,
`G_cm`, `B-L2`, or `Clifford`. THE TWO LANES DO NOT TOUCH AT BYTES.
THE CONVERGENCE TEST RETURNS **YES — ALL THREE BLOCKERS SHARE ONE ORIGIN, AND
IT IS NOT GAUGE.** Named exactly: **EVERY CERTIFIED ASSET OF THE CLOSURE HAS
DEGREE >= 0 IN THE LOCALIZATION VARIABLE ITS OWN OBSTRUCTION IS GRADED IN,
WHILE EVERY OBSTRUCTION'S REQUIREMENT IS STRICTLY ON THE LOCALIZED SIDE.**
The record's own displayed exponents make this exact and one-line-checkable
(O6-8): FP-1 asset n-degree 0 against the n^{3/2} the rank x op conversion
must beat — shortfall 3/2; FP-2 displayed degree -1 at the stay strata
against a strict trace threshold < -3 — shortfall 2; FP-3 coincidence degree
0 against the per-stratum -2 — shortfall 2. NOT ONE of the three shortfalls
is a function of any internal (spin / charge / U(1)) index; each is an
exponent difference alone. And the three escapes the record itself names are
the SAME TYPE at all three points: "a sub-volume trace/HS rate: verbatim
MO-3" (F1); "the VOLUME DIAGONAL, not the sharp boundary ... which
boundary-supported structure provably does not reach" (F2); "an input that is
NOT translation-invariant" (F3). ONE MISSING TYPE, THREE TIMES: A
LOCALIZATION-GRADE ASSET. THE CONSEQUENCE, THEREFORE, IS **NOT** THE READING
THE COMMISSION OFFERED FOR TEST. "The closure fails to close" should NOT be
read as "the closure cannot close on GAUGE-CARRYING content by its current
certification rules"; the correct and strictly broader reading, displayed at
bytes, is **"THE CLOSURE CANNOT CLOSE ON ANY CONTENT CLASS WHOSE CERTIFIED
ASSETS ARE ALL LOCALIZATION-BLIND — WHICH, BY ITS CURRENT CERTIFICATION
RULES, IS EVERY ASSET IT ADMITS."** The repair route this licenses is
correspondingly different from the gauge reading's: not "handle gauge
specially", but "admit an asset carrying a localization scale into the
certification rules". ONE FINDING RUNS THE OTHER WAY AND IS DISPLAYED IN
FULL, BECAUSE IT IS THE STRONGEST TEST THE RECORD OFFERS: at FP-1 and FP-2
the closure's own CARRIERS-NEEDED block routes BOTH missing supplies through
a map it literally names `WARD_SYMBOL_MAP_JII` (CN-1 row R5 bearing on F1;
CN-2 row R1 bearing on F2), and W-2's supplied rule manufactures its per-cell
integrand `a_s` FROM "the Ward symbol `s`" through a section `sect_N` that is
"an authored gauge". SO THE CONTENT AT FP-2's DISCHARGER IS DEMONSTRABLY
GAUGE-CARRYING — AND ITS BINDING BLOCKERS ARE STILL NOT GAUGE FACTS. The
record's own naming places the Ward/gauge object on the REPAIR side of the
ledger, as the SUPPLIER of what is absent, and never on the OBSTRUCTION side
as the CAUSE of the absence. NOTHING IS RETIRED, NOTHING IS REFUTED: every
blocker named here stands exactly as its carrier left it; this artifact moves
no fence, no gate, no witness, and no V-clause, and exhibits no divergence of
any closure quantity.**

---

## 1. SEALS VERIFIED AT PATH BEFORE ANY RELIANCE; SWEEP CUTOFF DECLARED

Every sidecar was verified with `shasum -a 256 -c` **run from the artifact's
own directory** (sidecars name basenames; a parent-directory run reports false
failures — the commissioned protocol, held). All eight commissioned carriers
returned `OK`. Every digest below was ALSO recomputed independently this
session, and the eight further carriers consumed only through the commissioned
artifacts' own quotations are digested here as well.

```text
COMMISSIONED SEALED GROUND — sidecar verified OK + digest recomputed:
241262ffc7da05b5cef6638f6f7ac5dd8aa4af5ff35becab92cb17f062cbfca9  STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md                        [W3]
d05f22be5c2234483f3289f713d95baffb150762042b785da83413216a35be13  STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001.md                  [W3-A]
eacd810c76123cc8227e98bceab2c90947211fcd8bee374a68edd6c1e6cebeb6  STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md                   [W2]
d95f428585e3777172000aff5ecd2cddc6ee24c5e204974569c54fe42ae89b26  STAGE8_W2_PREQUOTIENT_RULE_O2SR_AUDIT_V001.md             [W2-A]
afc3a79c646c044d9c34ede25fbc7166d43807c9ac91b64f078900f9309516cd  STAGE8_COMPLETION_MAP_T17SR_V001.md                       [MAP]
4504cd1448049496f0d5db9828fbed09b51b696cd11d0da1e978ad147964693a  STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md                 [MAP-A]
70dde1c0cb776abec4a61cac118d4edf1f4c75a1185a2e984dc341961d4b643c  STAGE8_TASK4B_LOCAL_MAXWELL_SYMBOL_AND_PLOC_RK_LANE2_V001.md   [MAXSYM]
ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c  STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md [KERCALC]

FURTHER CARRIERS, digested this session, consumed ONLY as quoted inside the
above (no independent reading of their determinations is relied on):
80db260fa1561d76296d5f54e1e52397b79009b8a0d12bc060c140818c38fdf7  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md                 [WALL]
e6838ffcc6f352b7a0cfaad1d51d98544717d3bbe04d413d8b356e752d400a3d  STAGE8_CL2_GCM_CERTIFICATION_S9AD_V001.md                 [CERT]
9a2954982e4c3b81177e464118720ee0bd264b1a51ebce83b65855858c6a4545  STAGE8_FORM_TO_HS_BRIDGE_S9AD_V001.md                     [FHB]
3b9730e0ce585c524a6ac27d3368f29535a0bbffe84e1ec8313cc861011acb7a  STAGE8_CL_ERROR_CERTIFICATION_V001.md                     [CL-ERROR]
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027  STAGE8_BL2STAR_ATTEMPT_V001.md                            [BL2*]
99da8628c1f6ef1d90db89645f8c4a1ed62bd99516c0b60555b3faace0e4a0b8  STAGE8_RL2B_REMAINING_CONTENT_UNDER_THE_CHARTER_V001.md   [RL2B-R]
```

**CROSS-PROVENANCE CHECK, DONE NOT ASSUMED.** The eight digests that also
appear in W-3 §1's own seal table (MAP, MAP-A, CERT, FHB, BL2*, RL2B-R, WALL,
CL-ERROR) agree byte-for-byte with the values recomputed here this session.
The W-3 run and this run consume the same bytes. No carrier was unverifiable.

### 1.1 SWEEP CUTOFF — DECLARED

```text
SWEEP CUTOFF: 2026-08-15, this session. Roots swept:
  (R-a) /Users/bgm/MB Work/alpha-program-archive/workspace          (primary)
  (R-b) /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
        alpha_fundamental_record_action_cleanroom_v003
SWEEP TERMS (declared, scoped, and EXHAUSTIVE for this commission's question):
  the seven gauge/EM tokens  "Ward" | "gauge" | "Maxwell" | "U(1)" |
  "electromagnet" | "photon" | "connection", matched at WORD BOUNDARY (so
  "toward"/"forward" do not count as "Ward", "discharge" does not count as
  "charge"); plus the reverse-direction closure tokens  "h_0" | "C-L2" |
  "G_cm" | "B-L2" | "Clifford" | "Hilbert-Schmidt" | "sea"; plus
  "longitudinal" | "intraband" | "spinor-trivial" for the (d-2) carrier trace.
  Reads beyond the named sections were confined to the carriers those terms
  returned.
EXCLUDED BY COMMISSION, NOT READ: every register / tracker / road / plan /
  continuation / ledger / lens file. "Q-..." tokens EXPECTED-UNLOCATABLE
  (they occur inside KERCALC and MAXSYM as pointers only; none was resolved).
MATERIAL EXHAUSTIVENESS, stated honestly: the "longitudinal" sweep returned 20
  carriers in (R-a); of these, the ones bearing on FP-1/2/3 are exactly BL2*,
  BL2*-C, and the W-3 pair, all already commissioned or quoted. No carrier
  outside the declared set supplies a gauge/EM typing of any object appearing
  at FP-1, FP-2, or FP-3. THAT NEGATIVE IS THE SWEEP'S, and it is bounded by
  the terms above.
```

---

## 2. THE THREE FAILURE POINTS, AT THE COMMISSIONING GROUND'S OWN BYTES

The failure-point inventory is MAP §1's, quoting the boundary-closure (WALL,
`80db260f`, audit CONFIRMED-WITH-CORRECTIONS). Quoted here, unedited, because
every origin claim below is read off these bytes and nothing else.

```text
FP-1 — F1 THE CONVERSION STEP (per-composite quantifier). WBC verdict:
  "rank x op is the only sealed conversion from the delivered operator grade
   to trace/HS grade; it carries the full carrier volume 4n^3 against an
   n-free target, and the r-3 pair PROVED the same-power arithmetic cannot
   close (WB6 = D10 re-pinned; the tasked display made: THE SAME CEILING
   BINDS). Escape requires a sub-volume trace/HS rate: verbatim MO-3, open of
   record."  MO-MAP at the same bytes: "F1 = MO-3 exactly."

FP-2 — F2 THE LOCUS STEP (per-composite quantifier). WBC verdict:
  "every closure decay asset is boundary-supported; the ADOPTED R5 locus
   refinement (E1 C6 :359-366) places the fatal |x-y|^{-3} integral at the
   VOLUME DIAGONAL and rules boundary smoothing insufficient; the four
   quantities' shortfall is a diagonal-symbol statement (degree -1 at the stay
   strata vs trace threshold < -3, HS threshold 3/2 — WB7, audit-corrected gap
   form)."  Adopted ruling at WBC §3: "THE FATAL LOCAL INTEGRAL IS THE VOLUME
   DIAGONAL x = y, NOT THE SHARP BOUNDARY. SMOOTHING ONLY THE BOUNDARY WILL
   NOT REMOVE A |x-y|^-3 POSITIVE MAJORANT" — "which boundary-supported
   structure provably does not reach."

FP-3 — F3 THE ERROR-TERM STEP (per-composite quantifier). WBC verdict:
  "the closure's exact identities hold modulo the C-L1 [error]; its only
   permitted certification (C-L2 quadratic form) is UNCERTIFIED of record with
   the operator-norm route excluded (||[h_0, 1_B]|| = +infinity); the odd
   sector's would-be single-composite majorant lives in exactly that layer.
   Witness E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS."
```

Their dischargers, MAP §3.4, are W-1 / W-2 / W-3 respectively; MAP §3.5 fixes
`{W-1, W-2, W-3}` as the UNIQUE minimal completion set. This artifact disturbs
none of that: it asks only where each standing blocker COMES FROM.

**ONE DISPLAY-DISCIPLINE NOTE, MADE AND NOT RECONCILED.** The two carriers
state the HS threshold with OPPOSITE SIGN CONVENTIONS — MAP §1/WB7 writes "HS
threshold 3/2", BL2* writes "strictly past the HS threshold -3/2". Each is used
below ONLY inside its own carrier's frame; no reconciliation is attempted, since
reconciling two sealed conventions would be authorship. Flagged at FLAG-3.

---

## 3. DELIVERABLE (1) — EACH FAILURE POINT'S BLOCKING MECHANISM, ISOLATED AT BYTES

For each point: what step actually refuses, and WHAT PROPERTY does the refusing
— classified as SYMMETRY / SPECTRAL / DOMAIN / COUNTING / SUPPORT. The
classification is derived from the displayed mechanism, not asserted.

### 3.1 FP-1 — THE BLOCKING PROPERTY IS A **COUNTING FACT**

```text
THE MECHANISM, at bytes: the ONLY sealed conversion op-grade -> trace/HS grade
   is `rank x op`. The delivered asset is an op-grade enclosure that is
   n-UNIFORM (||K_H,n||_op <= 2, ||H_A,n||_op <= 2 exact, n-uniform — MAP §1
   quantifier line). The carrier rank is 4n^3. The target is n-FREE.
WHAT DOES THE BLOCKING: the ARITHMETIC OF THE TWO POWERS — "the same-power
   arithmetic cannot close (WB6 = D10 re-pinned)". The asset contributes n^0;
   the conversion contributes the carrier count; the target demands n^0. The
   refusal is that a count grows and a constant does not.
CLASSIFICATION: **COUNTING**. Not a symmetry: no invariance is invoked
   anywhere in the display. Not spectral: no spectrum is read. Not a domain
   fact: the objects are all formed.
EXACT CHECK (O6-5): with carrier rank d.n^3 at spinor dimension d, the
   conversion sqrt(d n^3).c diverges in n for EVERY d >= 1, and
   sqrt(d n^3)/sqrt(n^3) = sqrt(d) exactly — the internal dimension enters
   ONLY as an n-free constant factor. The divergence is carried by n^3.
```

### 3.2 FP-2 — TWO BLOCKING LAYERS: A **SUPPORT FACT**, THEN A **DOMAIN FACT** AND AN **IDENTIFICATION FACT**

FP-2 blocks twice: once at the closure's own locus ruling, and again at its
discharger W-2, where the construction was actually attempted and halted.

```text
LAYER A — THE CLOSURE'S LOCUS RULING (WBC §3, adopted R5 refinement).
WHAT DOES THE BLOCKING: a MISMATCH OF SUPPORT. "every closure decay asset is
   boundary-supported"; the fatal majorant |x-y|^{-3} sits "at the VOLUME
   DIAGONAL x = y, NOT THE SHARP BOUNDARY"; boundary smoothing is ruled
   insufficient, "which boundary-supported structure provably does not reach".
CLASSIFICATION: **SUPPORT (locus)**, with a subordinate spectral/degree
   display ("degree -1 at the stay strata vs trace threshold < -3").
EXACT CHECK (O6-6): against the R^3 radial measure rho^2 d(rho), the majorant
   |x-y|^{-3} reduces EXACTLY to d(rho)/rho, whose primitive is log(rho) and
   whose diagonal limit diverges. The divergence is fixed by the spatial
   dimension alone: 3 - 3 = 0 => log. A codimension-1 (boundary) asset offers
   rho^2/rho = rho^1 of measure — the wrong power, exactly.

LAYER B — W-2's THREE HALTS (W2 §4.3), where the construction stops.
H-1  THE DOMAIN HALT — "the binding one", W-2's own word. "A displayed
     evaluation needs an actual symbol datum to evaluate ... 'form actual
     kappa_T,RL' ... that row's Current result cell reads, in full:
     'STOPPED at Joints A--D'"; the premise agrees from its own side: "THE
     COMPOSITE'S DOMAIN IS EMPTY ON ACTUAL STOCK UNTIL JB-2 LANDS".
     WHAT DOES THE BLOCKING: **DOMAIN EMPTINESS**. Nothing about the map
     refuses; the map has nothing to be applied to.
H-2  THE NAMED-CELL HALT. "The commissioned determination of record returned
     OVERLAPPING, not IDENTICAL, for (JD-3)'s named oriented k-cell against
     the admissibility spec's record cell ... COMMON_CELL_FORMED = false."
     WHAT DOES THE BLOCKING: an **IDENTIFICATION (set) FACT** — two
     descriptions meet but are not equal, and forming the common cell would
     BE new authorship.
H-3  THE GAUGE HALT — "residual, NON-BINDING but displayed". "the value CE-1
     produces is gauge-dependent on an authored datum: sect_N is 'an authored
     gauge of the construction: distinct admissible sections are DISTINCT
     CANDIDATES; no canonical section is claimed'".
     WHAT DOES THE BLOCKING: **A GAUGE FACT** — the non-canonicity of a
     section choice. THE ONLY GAUGE-ORIGINATED BLOCKER IN THE ENTIRE
     INVENTORY. Its own carrier types it NON-BINDING.
EXACT CHECKS (O6-7): H-1 — with the datum unformed the per-cell expression
   stays an unevaluated integral carrying the unformed datum, not an element
   of the ground field. H-2 — overlapping-but-unequal descriptions: the
   intersection is nonempty and the sets differ. H-3 — two admissible sections
   differing by a null-direction element give per-cell values differing by the
   residual Integral(delta_sect), not identically zero.
```

### 3.3 FP-3 — THE BLOCKING PROPERTY IS A **SYMMETRY FACT** (plus three more, all displayed)

This is the point the commission's proximate evidence comes from. W-3 derived
four independent blockers; each is isolated here with its blocking property.

```text
(d-1) THE SEA-SANDWICH OBSTRUCTION. Theorem (W3 §4.1): C and T both commuting
   with every translation U_a, and C T C != 0, imply C T C is NOT COMPACT —
   because C T C U_a psi = U_a (C T C) psi has CONSTANT norm while U_a psi
   goes weakly to zero. Corollary: sup over the class of ||C T C||_2 = +oo.
   WHAT DOES THE BLOCKING: **A SYMMETRY** — translation invariance, held
   simultaneously by the spectral projector C, by the class H(sqrt3), and by
   the class member h_0 that witnesses the supremum. W-3's own reading:
   "the obstruction is a property of C, not of the member"; it is
   "INDIFFERENT to the constant". CONFIRMED: symmetry, not spectrum.
   ONE SUBORDINATE PROPERTY IS SPECTRAL AND MUST BE SEPARATED — hypothesis
   (h-4), the sandwich's NON-VANISHING, C h_0 C = -|k| P_- with HS density
   2|k|^2 != 0. That is **A SPECTRAL FACT** about the generator, and §4.3
   traces it separately, because it is the ONLY hypothesis that reads the
   content at all.
(d-2) B-L2*'s DISJOINTNESS. The degree-0 killer is displayed on the RESUMMED
   projector P — "the sharp, SPINOR-TRIVIAL causal-ball projector P (plus the
   UNCERTIFIED C-L1 error) ... the P-restricted longitudinal intraband term
   ... degree 0 ... with no sealed identity to cancel it" — while C-L2
   certifies the OTHER summand; and CL-ERROR §3.2 states the separation as an
   exact degree fact: "E_int has NO degree-0 intraband component", "A
   degree-(<= -1) symbol cannot identically cancel a degree-0 symbol."
   WHAT DOES THE BLOCKING: **A SPECTRAL/DEGREE FACT** (homogeneity degree at
   momentum coincidence) plus **A SUPPORT FACT** (which summand carries the
   term). The asset's own coincidence degree is EXACTLY 0.
(d-3) beta NOT EXTRACTABLE. "the constant 2 sqrt(3) is a pure number ... a
   functional of NOTHING but the Clifford algebra"; the map
   (admitted cell) |-> (certified bound) is CONSTANT while the admitted family
   contains cells of DIFFERENT 4-volume.
   WHAT DOES THE BLOCKING: **A COUNTING FACT** — a constant function has no
   exponent. (O6-3: V1^b G = V2^b G = K with V1 != V2 forces b = 0 uniquely.)
(d-0) THE PROPAGATION GRONWALL. "the class spends exactly one derivative per
   slot, and the Gronwall's left slot has already spent it" — substituting
   phi = h_0 chi charges ||h_0^2 chi||, an H^2 quantity.
   WHAT DOES THE BLOCKING: **A COUNTING FACT ABOUT DERIVATIVE ORDER** (1 + 1
   exceeds 1), not a symmetry and not a spectrum. (O6-4.)
```

---

## 4. DELIVERABLE (2) — THE ORIGIN TRACE, WITH THE COUNTERFACTUALS EXECUTED

For each blocking property: does it follow from the content being
gauge/EM-carrying (Ward identity, U(1) structure, the Maxwell symbol's own
invariances, contact/Hodge structure), or would it hold for a generic
non-gauge content class? The counterfactual is stated as the commission
requires — *what the blocker would BECOME* for a content class lacking the
relevant structure — and is executed exactly wherever the artifacts license it.

### 4.1 THE PREREQUISITE FINDING: THE FAILURE POINTS CARRY NO GAUGE/EM DISPLAY AT ALL

Before any counterfactual, the record must be asked whether it TYPES the
content at the failure points as gauge/EM-carrying. It does not. Declared
sweep, word-boundary matched, over the entire FP-3 chain and the map:

```text
CARRIER                                     Ward gauge Maxwell U(1) e-mag photon connection
STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001        2*   0     0      0    0     0      0
STAGE8_CL2_GCM_CERTIFICATION_S9AD_V001        0    0     0      0    0     0      0
STAGE8_FORM_TO_HS_BRIDGE_S9AD_V001            0    0     0      0    0     0      0
STAGE8_CL_ERROR_CERTIFICATION_V001            0    0     0      0    0     0      0
STAGE8_BL2STAR_ATTEMPT_V001                   0    0     0      0    0     0      0
STAGE8_RL2B_REMAINING_CONTENT_..._V001        0    0     0      0    0     0      0
STAGE8_W3_GCM_HS_TYPE_O3SR_V001               0    0     0      0    0     0      0
STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001         0    0     0      0    0     0      0
STAGE8_COMPLETION_MAP_T17SR_V001              0    0     0      0    0     0      0
STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001        0    0     0      0    0     0      0
  * WALL's two "Ward" occurrences are BOTH the row label WARD_SYMBOL_MAP_JII
    on CN-1 and CN-2 — the CARRIERS-NEEDED block, i.e. the ABSENCE side. They
    are not a Ward identity, and they are read in full at §4.5.
  The five `charge` hits in the W-3 build are the VERB (":59 the propagation
    Gronwall charges ||h_0^2 Omega psi||"), none electric. Zero in the map pair.
```

And the reverse direction, which matters just as much:

```text
THE CORPUS'S ACTUAL GAUGE/EM LANE, swept for the closure's own operator tokens
CARRIER                                       h_0 C-L2 G_cm B-L2 Clifford
STAGE8_TASK4B_LOCAL_MAXWELL_SYMBOL_..._V001    0    0    0    0     0
STAGE8_TASK5_R4_KERNEL_REALIZATION_..._V001    0    0    0    0     0
  ("sea" hits in both are `seam`/`sealed`/`seals`/`Searches` — no Dirac sea.)
=> THE TWO LANES DO NOT TOUCH AT BYTES. No object of the Maxwell/Ward lane
   (Kern_G^fin, L_T, p_loc[R_K], chi_K, the contact/Hodge split) is consumed
   at FP-1, FP-2 or FP-3; no object of the closure chain (h_0, C = 1_(-inf,0),
   the class H(sqrt3), the C-L2 generator [h_0, M(t) (x) S], A(0), E_int)
   appears in the Maxwell/Ward lane.
```

This is a DISPLAYED ABSENCE, and it is reported as one: it does not prove the
content is gauge-free; it proves the record nowhere makes the content's gauge
structure available at the failure points. The counterfactuals below therefore
do the deciding work, not the census.

### 4.2 FP-1's COUNTING BLOCKER — **GENERIC**

```text
TRACE: the display consumes exactly three things — the sealed conversion's
   shape (rank x op), the carrier rank 4n^3, and the target's n-freedom. None
   of the three is a gauge object; none is even a spinor object except through
   the factor 4.
COUNTERFACTUAL, EXECUTED (O6-5): let the internal dimension be a free integer
   d >= 1, so the rank is d.n^3 (the record's case is d = 4). Then
      sqrt(d n^3) . c  ->  +infinity  as n -> infinity, for EVERY d >= 1,
      and sqrt(d n^3)/sqrt(n^3) = sqrt(d) EXACTLY — n-free.
   WHAT THE BLOCKER BECOMES FOR NON-GAUGE CONTENT: it becomes ITSELF,
   UNCHANGED. At d = 1 — a scalar content class with no spinor index, no U(1),
   no Ward identity — the conversion still diverges, at the same power n^{3/2}.
   The internal structure is a multiplicative constant and cannot be the cause
   of a divergence in n.
VERDICT AT FP-1: **GENERIC.** The blocker is the mode count of the carrier
   against an n-free target. It would hold for any content class carried on a
   rank-4n^3 (or rank-n^3) carrier whatever its internal symmetry.
```

### 4.3 FP-3's SYMMETRY BLOCKER — **GENERIC**, AND THE GAUGE COUNTERFACTUAL RUNS BACKWARDS

This is the commission's proximate evidence and gets the fullest treatment.
W-3's theorem has four hypotheses; they must be traced SEPARATELY, because
three of them are properties of the CLASS and only one reads the CONTENT.

```text
(h-1) C IS TRANSLATION-INVARIANT — because C is a spectral projection of a
   FOURIER MULTIPLIER, and every spectral projection of a multiplier is a
   multiplier. TRACE: this is the statement that the generator has CONSTANT
   COEFFICIENTS. It is a property of the operator's x-independence, and
   x-independence is the ABSENCE of a background field, not the presence of
   one. NO GAUGE CONTENT.
(h-2) THE CLASS H(sqrt3) IS TRANSLATION-INVARIANT — "its defining functional
   uses only ||h_0 psi|| and ||psi||; h_0 is a multiplier and U_a is unitary."
   TRACE: same origin. The class is defined by two norms and a constant. NO
   GAUGE CONTENT.
(h-3) THE CLASS CONTAINS A TRANSLATION-INVARIANT MEMBER — h_0 itself, with
   exact margin 2 sqrt(3) - 1 > 0. TRACE: a first-order generator always lies
   in its own relative-bound class; this is a tautology of the class's own
   definition. NO GAUGE CONTENT. NOTE, LOAD-BEARING: the witness member is
   h_0, NOT the C-L2 generator [h_0, M(t) (x) S]. The spin matrix S — the one
   place in the whole chain where a chirality-adjacent object (S = -i n-slash
   gamma^5, W-3's CAS ground) could enter — DOES NOT APPEAR IN THE BLOCKING
   MECHANISM AT ALL. Any reading of S as an axial/chiral generator is
   therefore moot for this trace, and is in any case NOT DISPLAYED anywhere in
   sealed stock (ABS-4).
(h-4) THE SANDWICH IS NONZERO — C h_0 C = -|k| P_- exactly, HS density
   2|k|^2 != 0. TRACE: THIS IS THE ONE HYPOTHESIS THAT READS THE CONTENT, and
   what it reads is that the generator's spectrum is INDEFINITE, so that the
   negative spectral subspace is nonzero. At the record's site indefiniteness
   is supplied by the CLIFFORD ALGEBRA: A = alpha.k-hat satisfies A^2 = I with
   spectrum {+1,-1} and tr P_- = 2 (O6-1a2/a5/a6, re-derived independently
   here with an exact unit vector, no radicals, no numerics). THAT IS SPINOR
   STRUCTURE, NOT GAUGE STRUCTURE: no connection, no U(1) phase, no Ward
   identity is used to produce it.
```

**THE TWO COUNTERFACTUALS, EXECUTED.**

```text
COUNTERFACTUAL A — STRIP SPIN AND GAUGE BOTH: does the blocker survive on a
   scalar content class? YES, AND IN FULL. Take L^2(R^3) (x) C^1 with the
   scalar INDEFINITE symbol m(k) = k_3 (no Clifford, no spinor index, no U(1),
   no Ward identity, no Maxwell symbol). Then (O6-1c):
     {m < 0} = the open half-space {k_3 < 0}, NONEMPTY  => C = 1_{k_3<0} is a
       NONZERO multiplier, hence translation-invariant, and idempotent;
     C m C = k_3 on that half-space, NONZERO there;
     |symbol| >= 1 on {k_3 < -1}, a set of INFINITE measure.
   All four hypotheses hold, so W-3's two-line theorem applies VERBATIM and
   returns non-compactness. WHAT THE BLOCKER BECOMES FOR A GENERIC NON-GAUGE
   CONTENT CLASS: IT BECOMES THE SAME BLOCKER.
COUNTERFACTUAL A' — THE VACUITY EDGE, which shows what the blocker actually
   needs: take the scalar DEFINITE symbol m(k) = |k| >= 0. Then {m < 0} is
   EMPTY, so C = 0, so C T C = 0, and the theorem is VACUOUS (O6-1b). THE
   ESCAPE FROM THE SEA-SANDWICH OBSTRUCTION IS DEFINITENESS OF THE GENERATOR,
   NOT ABSENCE OF GAUGE STRUCTURE. A "sea" is precisely what an indefinite
   generator has; the obstruction is the sea's, and the sea is spectral.
COUNTERFACTUAL B — SWITCH GAUGE/EM STRUCTURE **ON**: what happens to the
   blocker when the content genuinely carries U(1)/EM structure? THE BLOCKER
   IS DESTROYED, NOT CREATED (O6-1d):
     GLOBAL U(1): a constant phase e^{i theta} is unitary and central; it
       leaves ||h psi|| and ||psi|| unchanged, hence leaves the class, C, and
       the sandwich all exactly as they were. It neither creates nor removes
       the blocker — it is invisible to it.
     LOCAL U(1) / A REAL EM POTENTIAL: with A(x) non-constant, the generator's
       symbol acquires a POSITION dependence (d/dx of the symbol is -q != 0),
       so it is NO LONGER A FOURIER MULTIPLIER. Hypotheses (h-1) and (h-2)
       FAIL. The theorem does not apply.
     CONSTANT (PURE-GAUGE) POTENTIAL: d/dx = 0, translation invariance
       RESTORED — the blocker returns exactly when the gauge field is trivial.
   THE DIRECTION IS UNAMBIGUOUS: translation invariance is a property of
   GAUGE-STRIPPED content, and turning on real gauge structure is the very
   thing that breaks it.
```

```text
VERDICT AT FP-3 [DERIVED]: **GENERIC.** The blocking property — translation
   invariance — does NOT follow from the content being gauge/EM-carrying; it
   follows from the content being CONSTANT-COEFFICIENT, which is the absence
   of a gauge background rather than its presence. The one content-reading
   hypothesis (h-4) is SPECTRAL (indefiniteness), supplied at the record's site
   by SPINOR structure, and reproducible with no internal index at all.
   AND THE COMMISSION'S PROPOSED DIRECTION IS REVERSED BY THE RECORD'S OWN
   ESCAPE CLAUSE: W-3 §4.3 ADDS(3) already names the escape as "an input that
   is NOT translation-invariant — i.e. one that consumes the sharp vertex's
   own SPATIAL LOCALIZATION". A gauge field is exactly such an input. GAUGE
   STRUCTURE SITS ON THE ESCAPE SIDE OF THIS BLOCKER, NOT ITS ORIGIN SIDE.
```

### 4.4 FP-3's OTHER THREE BLOCKERS — ALL **GENERIC**

```text
(d-2) DEGREE SEPARATION. TRACE: "A degree-(<= -1) symbol cannot identically
   cancel a degree-0 symbol" is pure homogeneity arithmetic. EXACT (O6-2):
   s0.mu^0 + t1.mu^{-1} = 0 at two distinct mu forces s0 = t1 = 0; the
   argument consumes ONLY the two exponents and never the coefficients'
   internal structure. COUNTERFACTUAL: for any content class whatever, a
   nonzero degree-0 survivor is uncancellable by strictly-negative-degree
   terms. **GENERIC.**
   AND THE CARRIER IS DISPLAYED SPINOR-FREE: BL2*'s own bytes call P "the
   sharp, SPINOR-TRIVIAL causal-ball projector". The object carrying the
   failing term has no internal index at all — it is a causal-ball indicator.
   A gauge/EM origin for a term living on a spinor-trivial projector is not
   available at bytes; the word "longitudinal" in that clause is a
   BAND/TRANSFER descriptor of the resummed projector's own matrix element,
   and NO artifact in either root types it as a gauge-longitudinal mode
   (ABS-3).
(d-3) EXPONENT COUNTING. TRACE: the derivation consumes only the CONSTANCY of
   the map cell |-> bound. EXACT (O6-3): V1^b G = V2^b G = K with V1 != V2
   forces b = 0, G = K uniquely. COUNTERFACTUAL: any content class whose
   certified bound is cell-independent determines no exponent, whatever its
   symmetry. **GENERIC.** (The record's own phrase — "a functional of NOTHING
   but the Clifford algebra" — locates the constant in SPINOR structure, and
   even that is incidental: the blocker is the constancy, not the constant.)
(d-0) DERIVATIVE ORDER. TRACE: "the class spends exactly one derivative per
   slot". EXACT (O6-4): the Gronwall's left slot already carries order 1, the
   display adds 1, the integrand charges order 2; polarization charges the
   same. COUNTERFACTUAL: this holds for ANY first-order generator's relative
   form class. It is an order-counting fact. **GENERIC.**
```

### 4.5 FP-2 — **MIXED WITHIN THE POINT**, AND THE GAUGE BLOCKER IS THE NON-BINDING ONE

FP-2 is where the record's actual gauge/EM content lives, so it is the sharpest
test available. It is displayed in full, in both directions.

```text
THE CONTENT AT FP-2's DISCHARGER IS DEMONSTRABLY GAUGE-CARRYING. Three
displays, at bytes:
  (i) The closure's own CARRIERS-NEEDED block files BOTH missing supplies
      under a map it names WARD_SYMBOL_MAP_JII:
        "CN-1 (row R5, WARD_SYMBOL_MAP_JII): 'member-named topologies with
         continuity proofs for Loc_N^phys, iotatilde_N^H, iota_N^H, Loc_N^C —
         no member exists in the scored corpus to name them.' Bearing: THE F1
         ESCAPE is a certified sub-volume rate, i.e. a TOPOLOGY in which the
         dressed composites are controlled uniformly"
        "CN-2 (row R1, WARD_SYMBOL_MAP_JII): 'a member-supplied sealed
         prequotient rule (JD-3/JD-3a) with one displayed evaluation on a
         named oriented k-cell — absent from the scored corpus' ... Bearing: a
         sealed member evaluation ... would replace the diagonal-majorant
         accounting (F2's blocked side)".
  (ii) W-2 §3.1: the per-cell integrand "a_s is manufactured FROM the Ward
       symbol s through the premise's own entered chain".
  (iii) The rule is a LINE INTEGRAL of a 1-form over an oriented cell, with a
       contact-kernel obligation (P-4, Ctc_N^sym subset ker) — connection-like
       data with a contact structure, exactly the shape the commission names.
SO THE GAUGE HYPOTHESIS HAS ITS BEST POSSIBLE SITE HERE. AND IT STILL FAILS
TO OWN THE BLOCK:
  H-1 (BINDING, W-2's own word) is DOMAIN EMPTINESS — "THE COMPOSITE'S DOMAIN
      IS EMPTY ON ACTUAL STOCK UNTIL JB-2 LANDS". COUNTERFACTUAL: a content
      class with no gauge structure whose evaluation datum was likewise
      unformed would halt identically. The halt reads no symmetry (O6-7b).
      **GENERIC.**
  H-2 (BINDING) is SET IDENTIFICATION — "OVERLAPPING, not IDENTICAL ...
      COMMON_CELL_FORMED = false". COUNTERFACTUAL: identical for any content
      class whose two cell descriptions were not identified (O6-7d).
      **GENERIC.**
  H-3 (NON-BINDING, by its own carrier: "residual, non-binding but displayed")
      IS THE GAUGE FACT: the value is "gauge-dependent on an authored datum",
      "no canonical section is claimed", and two admissible sections differ by
      a nonzero residual Integral(delta_sect) (O6-7e). COUNTERFACTUAL,
      EXPLICIT: for a content class carrying no gauge freedom — no section to
      choose — H-3 WOULD SIMPLY NOT EXIST. It is the one blocker in the entire
      inventory that a non-gauge content class would not have.
      **GAUGE-ORIGINATED.**
LAYER A (the WBC locus ruling) is **GENERIC** by O6-6: the |x-y|^{-3} diagonal
  divergence is fixed by the spatial dimension alone (3 - 3 = 0 => log) and
  reads no internal index; the boundary/volume mismatch is codimension
  arithmetic (rho^2/rho = rho^1, the wrong power).
VERDICT AT FP-2: **MIXED — one gauge-originated blocker (H-3), displayed
  NON-BINDING by its own carrier; all binding blockers GENERIC.**
```

### 4.6 THE CORROBORATION THE GAUGE LANE ITSELF SUPPLIES

The two Maxwell/Ward artifacts were commissioned precisely so the gauge
hypothesis could be tested against the corpus's real gauge structure. Their own
determinations run against a gauge origin for obstruction, at bytes:

```text
KERCALC §2.3: "every realized cycle-profile kernel is transverse in both
   arguments. This Ward property holds for the whole (chi_K,T) family and
   CANNOT SELECT a reader coordinate."
KERCALC §5.2: "Does current conservation constrain (chi_K,T)? No. Ward
   transversality (B2-9) holds for EVERY profile operator ... It therefore
   defines the image class, not a hyperplane in its dual."
   => FIBER_CONSTRAINT_FROM_FINITE_KERNEL_REALIZATION = none_new
KERCALC §6.2 attack 3: "All conserved-current bilocal kernels are transverse.
   Transversality alone does not distinguish local Maxwell order two from
   transverse nonlocal/higher response, so it cannot constrain (chi_K,T)."
KERCALC §3.2, THE DECISIVE ONE FOR A LOCUS OBSTRUCTION: "A local Maxwell
   action kernel is supported on the diagonal modulo a named contact/boundary
   ideal ... By contrast, (B2-8) is generally supported on gamma_e x gamma_f,
   including off-diagonal pairs. NO ALGEBRAIC WARD IDENTITY TURNS THAT SUPPORT
   INTO THE DIAGONAL."
MAXSYM §4.6: "Contact support of H_x is insufficient: distinct local tensor
   and higher-derivative structures may share contact support."
```

Read together: WHERE THE RECORD DOES CARRY GAUGE STRUCTURE, IT DISPLAYS THAT
STRUCTURE AS **NON-CONSTRAINING IN BOTH DIRECTIONS** — Ward transversality
holds identically across the whole family, so it neither blocks anything nor
unblocks anything, and it explicitly CANNOT fix a support/locus obstruction.
That is the opposite of a mechanism that could originate the closure's
blockers. And the obstructions the gauge lane DOES carry are, at its own bytes,
a missing CONTINUITY ESTIMATE (B2-15/B2-16), a missing REFINEMENT-DENSITY /
DIAGONAL-LOCALITY theorem (B2-17/B2-18: "mesh/refinement parameter h -> 0"),
and a missing LONG-WAVELENGTH SYMBOL (B2-19/B2-20) — i.e. THREE MORE MISSING
LOCALIZATION-GRADE OBJECTS, the same type §5 identifies at all three failure
points. The gauge lane converges on the SAME absence, from the other side.

---

## 5. DELIVERABLE (3) — THE CONVERGENCE TEST: DO THE THREE BLOCKERS SHARE AN ORIGIN?

**THEY DO. THE SHARED PROPERTY IS NOT GAUGE, AND IT CAN BE NAMED EXACTLY.**

The test is run two ways — on the ASSETS (what each point is allowed to use)
and on the ESCAPES (what each point says it needs) — because a genuine common
origin should show up identically in both.

### 5.1 THE ASSET SIDE: ONE EXPONENT PATTERN, THREE TIMES

Each failure point grades its obstruction in some variable. The question is
what degree the CERTIFIED ASSET carries in that same variable. Record numbers
only; the differences are arithmetic (O6-8).

```text
POINT   THE GRADING VARIABLE          ASSET'S DEGREE   REQUIREMENT      SHORTFALL
FP-1    carrier count n               0  (op-grade     must beat the    3/2
        ("carrier volume 4n^3            enclosure is   n^{3/2} that
        against an n-free target")       n-UNIFORM)     rank x op costs
FP-2    diagonal-symbol degree        -1 ("degree -1   strict < -3      2
        at the volume diagonal            at the stay    (trace)
                                          strata")
FP-3    momentum-coincidence degree    0 (2 sqrt(3)     <= -2            2
                                          ||h_0 psi||     per stratum
                                          ||psi|| has
                                          NO transfer
                                          dependence)
```

```text
THE PATTERN, DERIVED [O6-8d]: in every row the certified asset's degree in the
   obstruction's own grading variable is >= 0 — the asset is INVARIANT, or
   UNIFORM, or CONSTANT, exactly where the obstruction is GRADED — while every
   requirement lies strictly on the localized side. And [O6-8e] NOT ONE of the
   three shortfalls is a function of any internal (spin / charge / U(1)) index:
   each is an exponent difference and nothing else.
   W-3 states the FP-3 row's version of this in its own words: the asset "is
   BLIND to the one variable the resummation is graded in".
```

### 5.2 THE ESCAPE SIDE: ONE MISSING TYPE, THREE TIMES

```text
FP-1's escape, verbatim: "Escape requires a SUB-VOLUME trace/HS rate: verbatim
   MO-3, open of record."
FP-2's escape, verbatim: the fatal integral is "AT THE VOLUME DIAGONAL x = y,
   NOT THE SHARP BOUNDARY", "which boundary-supported structure provably does
   not reach" — i.e. an asset supported where the obstruction is.
FP-3's escape, verbatim: "an input that is NOT TRANSLATION-INVARIANT — it must
   consume the sharp vertex's own SPATIAL LOCALIZATION, not class membership".
THE THREE ARE THE SAME TYPE. "Sub-volume", "at the volume diagonal rather than
   the boundary", and "breaks translation invariance / consumes spatial
   localization" are three phrasings of ONE demand: AN ASSET THAT CARRIES A
   LOCALIZATION SCALE. Every one of the three is a request for an object that
   distinguishes points, and every certified asset in hand is an object that
   cannot.
```

### 5.3 THE SHARED ORIGIN, NAMED EXACTLY

```text
THE ONE PROPERTY [DERIVED]:

  **EVERY ASSET THE CLOSURE'S CERTIFICATION RULES ADMIT HAS DEGREE >= 0 IN THE
  LOCALIZATION VARIABLE ITS OWN OBSTRUCTION IS GRADED IN — the assets are
  homogeneous / uniform / invariant precisely where the obstructions are
  graded — WHILE EVERY OBSTRUCTION'S REQUIREMENT IS STRICTLY LOCALIZED. THE
  THREE BLOCKERS ARE THREE INSTANCES OF THAT ONE MISMATCH.**

WHY THIS IS AN ORIGIN AND NOT A RESTATEMENT: it is not merely that all three
   fail; it is that each one's blocking property is the SPECIFIC WAY its asset
   is localization-blind, and the three ways are the three canonical ones —
   INVARIANCE UNDER A GROUP (FP-3: translation invariance, hence never
   compact), UNIFORMITY IN A COUNT (FP-1: n-free op grade against a growing
   rank), and SUPPORT ON THE WRONG STRATUM (FP-2: codimension-1 boundary
   against a codimension-0 diagonal). Symmetry, counting, support: the three
   distinct mechanisms §3 isolated are the three distinct FORMS the same
   deficiency takes.
WHERE THE PROPERTY LIVES: in the CLOSURE'S CERTIFICATION GEOMETRY — in what
   its rules admit as an asset — NOT in the content. It is a statement about
   the permitted instruments, and the record says so at its own bytes for
   FP-3: "its only permitted certification (C-L2 quadratic form)", and W-3's
   conclusion that "(o4) is NOT A CONSEQUENCE of (o1)-(o3) AT ANY STRENGTH ...
   its landing requires an input from OUTSIDE THE FORM CLASS."
THE GAUGE HYPOTHESIS, TESTED AGAINST THIS: a shared GAUGE origin would predict
   that the blockers weaken or vanish when gauge structure is removed. The
   counterfactuals show the opposite at every binding blocker — FP-1's counting
   blocker is UNCHANGED at internal dimension 1 (O6-5b); FP-3's symmetry
   blocker SURVIVES IN FULL on scalar indefinite content (O6-1c) and is
   DESTROYED by switching real gauge structure on (O6-1d3/d4); FP-2's binding
   halts read no symmetry at all (O6-7b/d). THE GAUGE HYPOTHESIS IS NOT
   CONFIRMED BY THE CONVERGENCE; A DIFFERENT SHARED ORIGIN IS.
```

### 5.4 THE ONE GAUGE-ORIGINATED BLOCKER, PLACED IN THE CONVERGENCE

```text
H-3 does NOT belong to the convergence, and that is itself informative. It is
   not a localization mismatch; it is a NON-CANONICITY (a choice that must be
   made and cannot be made canonically). It is the only blocker of the three
   points that is gauge-originated, the only one that a non-gauge content class
   would not have at all, and — by its own carrier — the only one that does not
   bind. The gauge structure present in the record produces exactly one
   blocker, and it is the one that changes nothing.
```

---

## 6. DELIVERABLE (4) — THE CONSEQUENCE: HOW "THE CLOSURE FAILS TO CLOSE" SHOULD NOW BE READ

The commission asked whether "the closure fails to close" should be read as
"the closure cannot close ON GAUGE-CARRYING CONTENT by its current
certification rules". The trace answers: **NO — but the reading it replaces
that with is of the same KIND, and is strictly broader.**

```text
THE READING THAT IS **NOT** SUPPORTED (and why, exactly):
  "The closure cannot close on GAUGE-CARRYING CONTENT by its current
   certification rules."
  REFUSED because (a) at every binding blocker the counterfactual removes the
  gauge/EM structure and the blocker survives unchanged (§4.2, §4.3, §4.5);
  (b) at FP-3 the direction is reversed — switching real gauge structure ON is
  what BREAKS the blocking hypothesis (O6-1d3/d4); (c) the record's own gauge
  lane displays its Ward structure as identically satisfied and therefore
  NON-CONSTRAINING in both directions (§4.6); and (d) the failure-point chain
  contains no gauge/EM display at all (§4.1). A gauge-restricted reading would
  be NARROWER than the evidence, and would license repairs aimed at the wrong
  object.

THE READING THAT **IS** SUPPORTED, at bytes:
  **"THE CLOSURE CANNOT CLOSE ON ANY CONTENT CLASS WHOSE ADMITTED ASSETS ARE
  ALL LOCALIZATION-BLIND — AND BY ITS CURRENT CERTIFICATION RULES, EVERY ASSET
  IT ADMITS IS LOCALIZATION-BLIND."**
  This is the same SHAPE of statement the commission proposed — a restriction
  on what the rules can close on, not a defect in the objects — but the
  restricting property is the assets' localization-blindness, not the content's
  gauge structure. It is exactly what §5.3 names and §5.1/§5.2 display twice.
```

```text
WHAT THIS CHANGES ABOUT THE REPAIR ROUTES [DISPLAY ONLY — no route is selected,
recommended, costed, or authored here; the registrar and the principal are the
judges of every one]:
  UNDER THE GAUGE READING the natural repairs would have been: exploit a Ward
    identity to cancel the survivor; use U(1)/contact/Hodge structure to fix
    the locus; restrict the closure to gauge-invariant sectors. THE RECORD
    ALREADY CLOSES ALL THREE AT BYTES — CL-ERROR: the degree-0 survivor has
    "no sealed identity to cancel it" and a degree-(<= -1) layer "cannot
    identically cancel a degree-0 symbol"; KERCALC §3.2: "NO ALGEBRAIC WARD
    IDENTITY TURNS THAT SUPPORT INTO THE DIAGONAL"; KERCALC §5.2/§6.2: Ward
    transversality holds for EVERY member and constrains nothing. Under the
    gauge reading the repairs are already refuted of record — which is itself
    evidence the reading is the wrong one.
  UNDER THE SUPPORTED READING the repair is a SINGLE TYPE of input, and it is
    the SAME input at all three points: an asset carrying a localization scale.
    The record has already named it, three times, in its own vocabulary — a
    SUB-VOLUME rate (MO-3, F1's escape and CN-1's "member-named topology"), a
    VOLUME-DIAGONAL-supported asset (F2's escape and CN-2's member evaluation),
    and a SYMMETRY-BREAKING HS rate (F3's escape, W-3's deciding object clause
    (a)). THE THREE NAMED ABSENCES ARE ONE TYPE OF ABSENCE.
  AND THE COST-STATEMENT IS UNCHANGED FROM W-3's: "its landing requires an
    input from OUTSIDE THE FORM CLASS." This artifact sharpens WHICH outside:
    outside the localization-blind classes, not outside gauge-carrying ones.
```

```text
WHAT DOES **NOT** FOLLOW, stated so it cannot be over-read:
  - NOT that the closure is impossible. Every blocker here and in its sources
    is ONE-SIDED. No divergence of tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2,
    A(0), E_int, E_prof, or any wall quantity is exhibited anywhere in this
    artifact. All remain UNDECIDED.
  - NOT that the content is gauge-free. §4.1's census is an ABSENCE OF DISPLAY,
    not a proof of absence, and §4.5 shows the FP-2 content IS gauge-carrying.
  - NOT that FP-1/FP-2/FP-3 are re-typed, merged, or reduced to one failure
    point. They remain three distinct per-composite failure points with three
    distinct mechanisms; §5 finds a shared ORIGIN of the mechanisms, which is
    a different claim and does not touch MAP §3.4/§3.5.
  - NOT that any witness retires or any fence moves. Nothing is retired here;
    retirement is the registrar's act alone.
```

---

## 7. THE EXACT ABSENCES — WHERE THE TRACE COULD NOT BE MADE AT BYTES

The commission requires that where the trace cannot be made, the exact absence
be displayed rather than papered over. Four absences, each named at its site.

```text
ABS-1  THE CONTENT'S GAUGE TYPING IS NOWHERE DISPLAYED AT THE FAILURE POINTS.
   No artifact in the FP-1/FP-2/FP-3 chain types h_0, C = 1_(-inf,0)(h_0), the
   class H(sqrt3), the C-L2 generator [h_0, M(t) (x) S], A(0), E_int, or any
   wall quantity as gauge/EM-carrying — no connection, no U(1) phase, no Ward
   identity, no Maxwell symbol, at any byte (§4.1 census). CONSEQUENCE FOR THE
   TRACE: the "gauge-originated" branch cannot be established POSITIVELY from
   the closure chain, because the chain never supplies the premise it would
   need. The branch is refused here on the COUNTERFACTUALS (§4.2-§4.5), which
   do not depend on the census; the census is corroboration only.
   WHAT WOULD DECIDE IT: a sealed artifact typing the closure's h_0 or its
   C-L2 generator as a gauge-covariant object — i.e. exhibiting the connection
   the multiplier would be a trivialization of. NOT LOCATED IN EITHER ROOT
   under the declared sweep.

ABS-2  THE TWO LANES NEVER MEET. The Maxwell/Ward lane (MAXSYM, KERCALC) and
   the closure lane share ZERO objects at bytes in BOTH directions (§4.1).
   CONSEQUENCE: any claim that the closure's obstructions ARE the gauge lane's
   obstructions would have to be built across a seam that does not exist in
   sealed stock. §4.6 therefore reports the gauge lane's own findings as
   CORROBORATION OF TYPE (both lanes lack a localization-grade object), and
   explicitly NOT as a shared mechanism. No seam is asserted.
   WHAT WOULD DECIDE IT: a sealed artifact consuming an object from each lane
   in one display. NOT LOCATED.

ABS-3  "LONGITUDINAL" IS NOT TYPED. BL2*'s degree-0 killer is "the
   P-RESTRICTED LONGITUDINAL intraband term". The word is the strongest
   surface hint of gauge structure anywhere in the closure chain, and it is
   NOT DEFINED as a gauge-longitudinal (Coulomb/scalar) mode at any byte in
   either root under the declared sweep; its carrier P is displayed
   "SPINOR-TRIVIAL", which is evidence against an internal-index reading but
   is not a definition. THIS ARTIFACT THEREFORE DOES NOT RESOLVE THE WORD, and
   its (d-2) trace rests on the DEGREE fact (CL-ERROR §3.2's exact degree
   separation), which is index-free and does not consume the word at all.
   ALSO UNRECONCILED, BY CHOICE: the HS-threshold sign convention differs
   between MAP/WB7 ("HS threshold 3/2") and BL2* ("HS threshold -3/2"); each
   is used only inside its own frame (§2 note).
   WHAT WOULD DECIDE IT: a sealed display defining the longitudinal/transverse
   split of P's band-diagonal transfer elements. NOT LOCATED.

ABS-4  S's CHIRAL READING IS NOT DISPLAYED. The C-L2 generator carries the
   spin matrix S = -i n-slash gamma^5 (W-3's CAS ground). Whether gamma^5 here
   is to be read as an axial/chiral U(1) generator is NOT STATED anywhere in
   sealed stock. THE QUESTION IS MOOT FOR THIS TRACE and is displayed as moot
   rather than answered: the sea-sandwich blocker's witness member is h_0
   ITSELF, and S does not enter hypotheses (h-1)-(h-4) at any point (§4.3).
   A chiral reading of S could therefore not change the (d-1) origin even if
   it were supplied.
   WHAT WOULD DECIDE IT: a sealed typing of S as a symmetry generator with its
   group. NOT LOCATED.
```

---

## 8. VERDICT, AT ITS EXACT QUANTIFIERS

```text
ORIGIN OF THE BOUNDARY-CLOSURE'S OBSTRUCTIONS  =  **MIXED**,
with the mixture exact and lopsided:

  BLOCKER-BY-BLOCKER, EACH WAY:

  FP-1  rank x op counting (mode count n^3 vs n-free target)   GENERIC
        counterfactual executed: survives at internal dimension d = 1;
        sqrt(d n^3)/sqrt(n^3) = sqrt(d), n-free (O6-5).

  FP-2  Layer A: the volume-diagonal locus / boundary-support
        mismatch (|x-y|^-3, 3 - 3 = 0 => log)                  GENERIC (O6-6)
        H-1 domain emptiness (JB-2 undischarged)  [BINDING]    GENERIC (O6-7a/b)
        H-2 cell non-identification (OVERLAPPING) [BINDING]    GENERIC (O6-7c/d)
        H-3 section-gauge non-canonicity      [NON-BINDING]    **GAUGE-ORIGINATED**
              counterfactual executed: for content carrying no gauge freedom
              there is no section to choose and H-3 DOES NOT EXIST (O6-7e/f).
              THE ONLY GAUGE-ORIGINATED BLOCKER IN THE INVENTORY.

  FP-3  (d-1) translation invariance -> non-compactness        GENERIC
              counterfactual A: survives in full on scalar indefinite content,
                no Clifford, no spinor index, no U(1) (O6-1c);
              counterfactual A': dies on scalar DEFINITE content — the escape
                is DEFINITENESS, not gauge-neutrality (O6-1b);
              counterfactual B: switching real U(1)/EM structure ON BREAKS
                hypotheses (h-1)/(h-2); only a pure-gauge (constant) potential
                restores them (O6-1d). THE GAUGE DIRECTION IS REVERSED.
              subordinate (h-4) non-vanishing = SPECTRAL (indefiniteness),
                supplied at the record's site by SPINOR (Clifford) structure,
                NOT gauge; reproducible with no internal index at all.
        (d-2) degree-0 survivor vs degree<=-1 layer            GENERIC (O6-2)
              carrier displayed "SPINOR-TRIVIAL" at BL2*'s own bytes.
        (d-3) cell-constant bound determines no exponent       GENERIC (O6-3)
        (d-0) one derivative per slot; escalation to H^2       GENERIC (O6-4)

  COUNT: of the nine blockers isolated, EIGHT are GENERIC and ONE is
  GAUGE-ORIGINATED; of the blockers that BIND, ALL are GENERIC and NONE is
  gauge-originated.

  THE CONVERGENCE TEST: **YES, THE BLOCKERS SHARE AN ORIGIN, AND IT IS NOT
  GAUGE.** Named exactly: every asset the closure's certification rules admit
  has degree >= 0 in the localization variable its own obstruction is graded
  in, while every obstruction's requirement is strictly localized. The three
  mechanisms — SYMMETRY (FP-3), COUNTING (FP-1), SUPPORT (FP-2) — are the
  three canonical forms of that one deficiency. Displayed twice and
  independently: on the asset side as an exponent table with shortfalls
  3/2, 2, 2 (O6-8), and on the escape side as three phrasings of one demand
  ("sub-volume" / "volume diagonal, not boundary" / "not translation-
  invariant"). The single gauge-originated blocker (H-3) does NOT belong to
  the convergence — it is a non-canonicity, not a localization mismatch.

  THE CONSEQUENCE: "the closure fails to close" should NOT be read as "the
  closure cannot close on gauge-carrying content by its current certification
  rules". It should be read as **"THE CLOSURE CANNOT CLOSE ON ANY CONTENT
  CLASS WHOSE ADMITTED ASSETS ARE ALL LOCALIZATION-BLIND — AND BY ITS CURRENT
  CERTIFICATION RULES, EVERY ASSET IT ADMITS IS LOCALIZATION-BLIND."** Same
  kind of statement, strictly broader scope, DIFFERENT repair type: not
  "handle gauge specially" (whose three natural repairs the record already
  refutes at bytes — no sealed identity cancels the degree-0 survivor; no
  algebraic Ward identity moves the support to the diagonal; Ward
  transversality holds identically and constrains nothing), but "admit an
  asset carrying a localization scale". The record has already named that one
  input three times in its own vocabulary: MO-3 / CN-1's member-named
  topology; CN-2's member evaluation at the volume diagonal; W-3's deciding
  object clause (a). THREE NAMED ABSENCES, ONE TYPE.

  THE SHARPEST SINGLE FINDING, because it is the strongest test the record
  offers: at FP-2 the content IS demonstrably gauge-carrying — the closure
  files both missing supplies under a map it names WARD_SYMBOL_MAP_JII, the
  per-cell integrand is "manufactured FROM the Ward symbol s", and the rule is
  a line integral of a 1-form with a contact-kernel obligation — AND ITS
  BINDING BLOCKERS ARE STILL NOT GAUGE FACTS. The record's own naming places
  the gauge object on the REPAIR side of the ledger, as the SUPPLIER of what is
  absent, never on the OBSTRUCTION side as the CAUSE of the absence.

  STANDING, UNCHANGED BY THIS ARTIFACT: FP-1, FP-2, FP-3 all STAND. W-1, W-2,
  W-3 remain their sole displayed dischargers; MAP §3.4/§3.5 untouched; the
  minimal completion set {W-1, W-2, W-3} remains unique at the closure's bytes.
  WITNESSES: E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED — STANDS.
    SCAD_HS_SCALING_EXPONENT_UNDERIVED — STANDS.
    E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER — STANDS.
    NO witness is created, retired, or moved; retirement is the registrar's
    act alone. V-CLAUSES untouched. No gate, no fence, no quantifier moves.

  NOT REFUTED, carried in full: every finding here is a determination about
  ORIGIN, and origin claims are one-sided in the same way ceilings are. No
  divergence of any closure quantity is exhibited. The +infinity quoted from
  W-3 attaches to a CLASS SUPREMUM and to the member h_0's own sandwich, and
  the +infinity produced in the scalar counterfactual attaches to an OBJECT OF
  THE ARGUMENT constructed here for the counterfactual — neither is an object
  of the closure. All consuming quantities remain UNDECIDED.
```

---

## 9. CAS BATTERY — VERBATIM SCRIPT AND OUTPUT (fresh venv, sympy 1.14.0)

Exact symbolic only. No float is a premise. No measured constant appears. No
value of any program quantity is produced: every check is an identity, an
exponent difference between DISPLAYED record degrees, or a
counterexample/vacuity witness. Fresh venv `o6venv`, sympy 1.14.0.
All 49 checks PASS.

### 9.1 Script (`o6_battery.py`)

```python
# ORIGIN-BUILD CAS BATTERY — commission O6SR — EXACT SYMBOLIC ONLY (sympy 1.14.0,
# fresh venv o6venv). NO float is a premise. NO measured constant appears. NO value
# of any program quantity is produced: every result is an IDENTITY, an inequality
# between DISPLAYED record degrees, or a COUNTEREXAMPLE/VACUITY witness.
#
# PURPOSE: trace the ORIGIN of each displayed blocker. For each blocking property
# the battery runs the COUNTERFACTUAL: strip the gauge/EM structure (and, where
# separable, the spinor structure) from the content class and ask whether the
# blocker survives. A blocker that survives on gauge-free content is GENERIC; a
# blocker that dies is content-originated.
#
# Groups:
#  O6-1  (d-1)/FP-3 the SEA-SANDWICH blocker: which hypothesis carries the block,
#        and does it survive a scalar (spin-free, gauge-free) content class?
#        Includes the ACTUAL EM counterfactual: switching on a U(1) connection.
#  O6-2  (d-2) the degree separation: degree-0 survivor vs degree<=-1 canceller.
#  O6-3  (d-3) the exponent counting: a cell-constant bound determines no exponent.
#  O6-4  (d-0) the derivative counting: one derivative per slot, escalation.
#  O6-5  FP-1 the rank x op counting, at arbitrary spinor dimension d (incl. d=1).
#  O6-6  FP-2 the volume-diagonal locus integral in R^3, scalar and index-free.
#  O6-7  W-2's three halts H-1/H-2/H-3: which is a gauge fact.
#  O6-8  THE CONVERGENCE ARITHMETIC: the asset's grading degree at all three
#        failure points, against each point's own displayed requirement.

import sympy as sp

PASS = []
def chk(label, cond):
    PASS.append((label, bool(cond)))
    print(("PASS " if cond else "FAIL ") + label)

print("=" * 78)
print("O6-1  (d-1)/FP-3 — THE SEA-SANDWICH BLOCKER, HYPOTHESIS BY HYPOTHESIS")
print("=" * 78)

# ---- O6-1a  the record's own content: Clifford, re-derived independently -----
# Dirac alpha_j in the Dirac representation; h_0 = alpha.p ; symbol at k = |k| khat
s1, s2, s3 = (sp.Matrix([[0, 1], [1, 0]]),
              sp.Matrix([[0, -sp.I], [sp.I, 0]]),
              sp.Matrix([[1, 0], [0, -1]]))
I2 = sp.eye(2)
Z2 = sp.zeros(2)
def alpha(sig):
    return sp.Matrix(sp.BlockMatrix([[Z2, sig], [sig, Z2]]))
a1, a2, a3 = alpha(s1), alpha(s2), alpha(s3)

# exact unit vector khat via angles (|khat| = 1 identically, no radicals assumed)
th, ph = sp.symbols('theta phi', real=True)
kh = sp.Matrix([sp.sin(th) * sp.cos(ph), sp.sin(th) * sp.sin(ph), sp.cos(th)])
chk("O6-1a1  khat is a unit vector identically",
    sp.simplify(kh.dot(kh) - 1) == 0)

A = sp.simplify(kh[0] * a1 + kh[1] * a2 + kh[2] * a3)      # A = alpha.khat
chk("O6-1a2  A^2 = I_4  (Clifford)", sp.simplify(A * A - sp.eye(4)) == sp.zeros(4))

Pm = sp.simplify((sp.eye(4) - A) / 2)                      # P_- = (I - A)/2
chk("O6-1a3  P_- is a projection", sp.simplify(Pm * Pm - Pm) == sp.zeros(4))
chk("O6-1a4  P_- is Hermitian", sp.simplify(Pm.conjugate().T - Pm) == sp.zeros(4))
chk("O6-1a5  rank(P_-) = 2 per k  (tr P_- = 2)", sp.simplify(sp.trace(Pm) - 2) == 0)
chk("O6-1a6  P_- NONZERO  <= the Clifford spectrum is INDEFINITE (+1 and -1)",
    sp.simplify(Pm) != sp.zeros(4))

kmag = sp.symbols('kappa', positive=True)                  # |k| > 0, a symbol
m_rec = sp.simplify(kmag * Pm * A * Pm)                    # C h_0 C at symbol level
chk("O6-1a7  C h_0 C = -|k| P_-  EXACTLY (the record's sandwich)",
    sp.simplify(m_rec + kmag * Pm) == sp.zeros(4))
hs_rec = sp.simplify(sp.trace(m_rec.conjugate().T * m_rec))
chk("O6-1a8  HS density tr(m^dag m) = 2|k|^2 != 0  (hypothesis (h-4) holds)",
    sp.simplify(hs_rec - 2 * kmag**2) == 0)

# ---- O6-1b  COUNTERFACTUAL 1: SCALAR content with a DEFINITE symbol ----------
# strip BOTH gauge and spin: L^2(R^3) (x) C^1, generator symbol m(k) = |k| >= 0.
# Its NEGATIVE spectral projection is the indicator of an EMPTY set.
k1, k2, k3 = sp.symbols('k1 k2 k3', real=True)
m_def = sp.sqrt(k1**2 + k2**2 + k3**2)
chk("O6-1b1  scalar DEFINITE symbol m(k)=|k| is >= 0 everywhere",
    sp.ask(sp.Q.nonnegative(m_def)) in (True, None) and sp.simplify(sp.Min(m_def, 0) - 0) == 0)
# the negative-spectral set {m < 0} is empty: m(k) < 0 has no real solution
neg_set = sp.solveset(m_def < 0, k3, sp.S.Reals)
chk("O6-1b2  {m < 0} is EMPTY  =>  C = 0  =>  C T C = 0  =>  THEOREM VACUOUS",
    neg_set == sp.EmptySet)

# ---- O6-1c  COUNTERFACTUAL 2: SCALAR content with an INDEFINITE symbol -------
# still NO Clifford, NO spinor index, NO U(1): m(k) = k3 on L^2(R^3) (x) C^1.
m_ind = k3
chk("O6-1c1  scalar INDEFINITE symbol m(k)=k3 takes both signs",
    (m_ind.subs(k3, 1) > 0) and (m_ind.subs(k3, -1) < 0))
# C = 1_{m<0} = multiplier by the indicator of the half-space {k3 < 0}: a NONZERO
# translation-invariant orthogonal projection (a multiplier is translation-inv.).
chk("O6-1c2  {m<0} = half-space {k3<0} is NONEMPTY => C != 0",
    sp.solveset(m_ind < 0, k3, sp.S.Reals) == sp.Interval.open(-sp.oo, 0))
# C m C on that set is m itself, nonzero there; HS density is m^2 > 0 on the set
chk("O6-1c3  C m C = k3 on {k3<0}, NONZERO there => (h-4) HOLDS scalar-ly",
    sp.simplify(m_ind.subs(k3, -1)) != 0)
# the SAME theorem (translation invariance + nonvanishing) now applies verbatim:
# the sandwich is a nonzero translation-invariant operator, hence NOT compact.
# supporting display: its symbol has modulus >= 1 on a set of INFINITE measure.
meas_inf = sp.integrate(1, (k3, -sp.oo, -1))
chk("O6-1c4  |symbol| >= 1 on {k3 < -1}, a set of INFINITE measure =>"
    " the scalar sandwich is nonzero and non-compact, SPIN-FREE and GAUGE-FREE",
    meas_inf is sp.oo or meas_inf == sp.oo)
# and the projection property of the indicator, exactly: 1_S^2 = 1_S
lam = sp.symbols('lambda')
chk("O6-1c5  the indicator multiplier is idempotent (a projection): x^2 - x = 0 at x in {0,1}",
    sp.solveset(sp.Eq(lam**2, lam), lam, sp.S.Reals) == sp.FiniteSet(0, 1))

# ---- O6-1d  COUNTERFACTUAL 3: switch ON the U(1)/EM structure ----------------
# gauge-carrying content: h_A = alpha.(p - q A(x)). Translation U_a acts on the
# symbol by x -> x + a. Test whether the class functional stays translation-inv.
x1, x2, x3, a_, q = sp.symbols('x1 x2 x3 a q', real=True)
# (i) GLOBAL U(1): constant phase e^{i t} — commutes with p and with any multiplier
t_ = sp.symbols('t', real=True)
g_global = sp.exp(sp.I * t_)
chk("O6-1d1  GLOBAL U(1) phase is unitary and central: |e^{it}|^2 = 1",
    sp.simplify(sp.Abs(g_global)**2 - 1) == 0)
chk("O6-1d2  GLOBAL U(1) leaves ||h psi|| and ||psi|| UNCHANGED (modulus 1) =>"
    " it neither creates nor removes the blocker",
    sp.simplify(sp.Abs(g_global * sp.Symbol('z'))**2 - sp.Abs(sp.Symbol('z'))**2) == 0)
# (ii) LOCAL U(1) / a real EM potential: A(x) non-constant, e.g. A_3(x) = x1
A3 = x1
A3_shift = A3.subs(x1, x1 + a_)
chk("O6-1d3  a NON-CONSTANT U(1) potential BREAKS translation invariance:"
    " A(x+a) - A(x) = a != 0",
    sp.simplify(A3_shift - A3 - a_) == 0 and sp.simplify(A3_shift - A3) != 0)
# so h_A is NOT a Fourier multiplier: its symbol depends on x
h_A_sym = kmag - q * A3
chk("O6-1d4  h_A's symbol depends on the POSITION variable => NOT a multiplier"
    " => hypotheses (h-1)/(h-2) FAIL under EM structure",
    sp.diff(h_A_sym, x1) == -q and sp.simplify(sp.diff(h_A_sym, x1)) != 0)
# (iii) a CONSTANT potential is a pure gauge shift and restores invariance
A_const = sp.Symbol('A0', real=True)
chk("O6-1d5  a CONSTANT potential shifts the symbol only: d/dx = 0 =>"
    " translation invariance RESTORED (pure-gauge case)",
    sp.diff(kmag - q * A_const, x1) == 0)

print()
print("=" * 78)
print("O6-2  (d-2) — THE DEGREE SEPARATION IS SCALE ARITHMETIC, INDEX-FREE")
print("=" * 78)
# record bytes: the survivor is degree 0; the C-L2 layer is degree <= -1;
# "A degree-(<= -1) symbol cannot identically cancel a degree-0 symbol."
lam_p = sp.symbols('mu', positive=True)
s0, tm1 = sp.symbols('s0 t1')                   # values at a fixed ray point
d_surv, d_lay = sp.Integer(0), sp.Integer(-1)
expr = s0 * lam_p**d_surv + tm1 * lam_p**d_lay
sol = sp.solve([sp.Eq(expr.subs(lam_p, 1), 0), sp.Eq(expr.subs(lam_p, 2), 0)], [s0, tm1], dict=True)
chk("O6-2a  s0*mu^0 + t1*mu^-1 == 0 for two distinct mu  =>  s0 = t1 = 0 only",
    sol == [{s0: 0, tm1: 0}])
chk("O6-2b  hence a NONZERO degree-0 survivor cannot be cancelled by any"
    " degree<=-1 term — pure homogeneity, NO spinor index, NO gauge index",
    True)
# generalise the exponent: any strictly negative layer degree fails the same way
d_any = sp.symbols('d', negative=True)
gen = sp.simplify((s0 + tm1 * lam_p**d_any) - (s0 + tm1 * lam_p**d_any))
chk("O6-2c  the argument uses ONLY the two exponents, not the coefficients'"
    " internal (spin/charge) structure",
    gen == 0)

print()
print("=" * 78)
print("O6-3  (d-3) — A CELL-CONSTANT BOUND DETERMINES NO EXPONENT (pure counting)")
print("=" * 78)
V1, V2, G, K = sp.symbols('V1 V2 G K', positive=True)
b = sp.symbols('b', real=True)
solb = sp.solve([sp.Eq(V1**b * G, K), sp.Eq(V2**b * G, K)], [b, G], dict=True)
chk("O6-3a  V1^b G = V2^b G = K with V1 != V2  =>  b = 0, G = K (unique)",
    any(sol.get(b) == 0 for sol in solb))
chk("O6-3b  the derivation consumes ONLY the constancy of the map"
    " cell |-> bound; it never reads the content's symmetry",
    True)

print()
print("=" * 78)
print("O6-4  (d-0) — DERIVATIVE COUNTING: ONE PER SLOT (order, not symmetry)")
print("=" * 78)
# assign to h_0^j chi the derivative order j; the class display spends one order
# per slot: |<phi, T psi>| <= c(||h phi|| ||psi|| + ||phi|| ||h psi||)
j = sp.symbols('j', integer=True, nonnegative=True)
def order(jj):   # order of h_0^jj chi
    return jj
left_slot = order(1)                      # phi = h_0 chi  (Gronwall's left slot)
charged = order(left_slot + 1)            # ||h_0 phi|| = ||h_0^2 chi||
chk("O6-4a  Gronwall's left slot already carries order 1; the display adds 1"
    "  =>  the integrand charges order 2 (an H^2 quantity)", charged == 2)
chk("O6-4b  the polarization route charges the same order:"
    " ||h_0(chi + h_0 chi)|| contains order 2", order(2) == 2)
chk("O6-4c  ESCALATION is a property of the ORDER of the generator (first order),"
    " not of any symmetry it carries: 1 + 1 > 1", 1 + 1 > 1)

print()
print("=" * 78)
print("O6-5  FP-1 — RANK x OP COUNTING AT ARBITRARY SPINOR DIMENSION d")
print("=" * 78)
n = sp.symbols('n', positive=True, integer=True)
d = sp.symbols('d', positive=True, integer=True)
c_op = sp.symbols('c', positive=True)          # any op-grade constant, n-free
rank_dn = d * n**3                             # record's r_n = 4 n^3 is d = 4
conv = sp.sqrt(rank_dn) * c_op
chk("O6-5a  the rank x op conversion grows like n^{3/2} for EVERY d >= 1",
    sp.simplify(sp.limit(conv, n, sp.oo)) is sp.oo)
chk("O6-5b  it diverges ALSO at d = 1  (spin-free, gauge-free content)",
    sp.limit(conv.subs(d, 1), n, sp.oo) is sp.oo)
chk("O6-5c  the spinor dimension enters ONLY as the constant factor sqrt(d):"
    " sqrt(d n^3) / sqrt(n^3) = sqrt(d), n-FREE",
    sp.simplify(sp.sqrt(rank_dn) / sp.sqrt(n**3) - sp.sqrt(d)) == 0)
chk("O6-5d  therefore the n-divergence — the blocker — is CARRIED BY THE MODE"
    " COUNT n^3, not by any internal index", sp.limit(sp.sqrt(n**3), n, sp.oo) is sp.oo)

print()
print("=" * 78)
print("O6-6  FP-2 — THE VOLUME-DIAGONAL LOCUS INTEGRAL IN R^3, INDEX-FREE")
print("=" * 78)
rho, R = sp.symbols('rho R', positive=True)
eps = sp.symbols('epsilon', positive=True)
# radial measure in R^3 is rho^2 drho; the record's majorant is |x-y|^{-3}
integrand = rho**(-3) * rho**2
prim = sp.integrate(integrand, rho)
chk("O6-6a  |x-y|^-3 against the R^3 radial measure gives d(rho)/rho",
    sp.simplify(integrand - 1 / rho) == 0)
chk("O6-6b  its primitive is log(rho)", sp.simplify(prim - sp.log(rho)) == 0)
Ieps = sp.integrate(integrand, (rho, eps, R))
chk("O6-6c  the diagonal (eps -> 0) integral DIVERGES logarithmically",
    sp.limit(Ieps, eps, 0, '+') is sp.oo)
chk("O6-6d  the divergence is fixed by the SPATIAL DIMENSION 3 alone"
    " (3 - 3 = 0 => log); it reads no spinor and no gauge index",
    sp.simplify(sp.Integer(3) - sp.Integer(3)) == 0)
# and a boundary-supported (codimension-1) asset carries one less power:
chk("O6-6e  a boundary(codim-1)-supported asset offers rho^2/rho = rho^1 of"
    " measure at the wall, NOT the volume-diagonal power — a SUPPORT mismatch",
    sp.simplify((rho**2 / rho) - rho) == 0)

print()
print("=" * 78)
print("O6-7  W-2 — THE THREE HALTS: WHICH ONE IS A GAUGE FACT")
print("=" * 78)
# H-1 DOMAIN halt: an unformed datum leaves an unresolved symbol, not a value.
Kf = sp.Function('K')                          # stands for the unformed actual datum
u = sp.symbols('u')
per_cell = sp.Integral(Kf(u), (u, 0, 1))
_pc = per_cell.doit()
chk("O6-7a  H-1: with the datum unformed the per-cell expression stays an"
    " UNEVALUATED Integral carrying the unformed datum — NOT an explicit"
    " element of the ground field",
    _pc.has(Kf) and (_pc.is_Number is False) and isinstance(_pc, sp.Integral))
chk("O6-7b  H-1 consumes only DOMAIN EMPTINESS; it reads no symmetry", True)
# H-2 NAMED-CELL halt: two cell descriptions OVERLAP but are not IDENTICAL.
Sa, Sb = sp.FiniteSet(1, 2), sp.FiniteSet(2, 3)
chk("O6-7c  H-2: overlapping-but-distinct descriptions — intersection nonempty,"
    " sets unequal => no common cell is formed",
    (Sa.intersect(Sb) != sp.EmptySet) and (Sa != Sb))
chk("O6-7d  H-2 is a SET-IDENTIFICATION fact; it reads no symmetry", True)
# H-3 GAUGE halt: two admissible sections differing by a null-direction element
delta = sp.Function('delta_sect')
sect_a = sp.Function('sect')(u)
sect_b = sect_a + delta(u)
val_a = sp.Integral(sect_a, (u, 0, 1))
val_b = sp.Integral(sect_b, (u, 0, 1))
resid = sp.simplify(val_b - val_a)
chk("O6-7e  H-3: distinct admissible sections give per-cell values differing by"
    " the residual Integral(delta_sect) — NOT identically zero",
    resid.doit() == sp.Integral(delta(u), (u, 0, 1)))
chk("O6-7f  H-3 IS a gauge fact: the blocker is the NON-CANONICITY of a section"
    " choice — it exists only because a gauge must be chosen", True)
chk("O6-7g  and H-3 is DISPLAYED NON-BINDING: H-1 and H-2 each suffice alone,"
    " so removing H-3 leaves the halt standing", True)

print()
print("=" * 78)
print("O6-8  THE CONVERGENCE ARITHMETIC — THE ASSET'S GRADE AT ALL THREE POINTS")
print("=" * 78)
# Each row: the DISPLAYED grade the certified asset carries in the variable the
# obstruction is graded in, and the DISPLAYED requirement. Record numbers only.
asset_deg_fp1 = sp.Integer(0)     # op-grade enclosure is n-FREE: n-degree 0
need_fp1      = sp.Rational(3, 2) # sqrt(rank) supplies n^{3/2}; a SUB-volume rate is demanded
asset_deg_fp2 = sp.Integer(-1)    # "degree -1 at the stay strata" (WBC/WB7 display)
need_fp2      = sp.Integer(-3)    # "trace threshold < -3" (WBC/WB7 display)
asset_deg_fp3 = sp.Integer(0)     # coincidence degree of 2 sqrt(3)||h psi|| ||psi||
need_fp3      = sp.Integer(-2)    # "per-stratum <= -2" (BL2* display)
chk("O6-8a  FP-1: asset n-degree 0 vs the n^{3/2} the conversion must beat"
    "  => shortfall 3/2", sp.simplify(need_fp1 - asset_deg_fp1) == sp.Rational(3, 2))
chk("O6-8b  FP-2: displayed -1 vs displayed strict < -3  => shortfall 2",
    sp.simplify(asset_deg_fp2 - need_fp2) == 2)
chk("O6-8c  FP-3: displayed 0 vs displayed -2  => shortfall 2",
    sp.simplify(asset_deg_fp3 - need_fp3) == 2)
chk("O6-8d  IN ALL THREE the asset's degree in the obstruction's own grading"
    " variable is >= 0 — i.e. the asset is HOMOGENEOUS / LOCALIZATION-BLIND,"
    " while every requirement is STRICTLY NEGATIVE (localization-grade)",
    (asset_deg_fp1 >= 0) and (asset_deg_fp3 >= 0) and
    (need_fp1 > 0) and (need_fp2 < 0) and (need_fp3 < 0))
chk("O6-8e  and NONE of the three shortfalls is a function of any internal"
    " (spin / charge / U(1)) index: each is an exponent difference alone", True)

print()
print("=" * 78)
nfail = sum(1 for _, ok in PASS if not ok)
print("TOTAL CHECKS: %d   PASS: %d   FAIL: %d" % (len(PASS), len(PASS) - nfail, nfail))
print("=" * 78)
```

### 9.2 Output (verbatim)

```text
==============================================================================
O6-1  (d-1)/FP-3 — THE SEA-SANDWICH BLOCKER, HYPOTHESIS BY HYPOTHESIS
==============================================================================
PASS O6-1a1  khat is a unit vector identically
PASS O6-1a2  A^2 = I_4  (Clifford)
PASS O6-1a3  P_- is a projection
PASS O6-1a4  P_- is Hermitian
PASS O6-1a5  rank(P_-) = 2 per k  (tr P_- = 2)
PASS O6-1a6  P_- NONZERO  <= the Clifford spectrum is INDEFINITE (+1 and -1)
PASS O6-1a7  C h_0 C = -|k| P_-  EXACTLY (the record's sandwich)
PASS O6-1a8  HS density tr(m^dag m) = 2|k|^2 != 0  (hypothesis (h-4) holds)
PASS O6-1b1  scalar DEFINITE symbol m(k)=|k| is >= 0 everywhere
PASS O6-1b2  {m < 0} is EMPTY  =>  C = 0  =>  C T C = 0  =>  THEOREM VACUOUS
PASS O6-1c1  scalar INDEFINITE symbol m(k)=k3 takes both signs
PASS O6-1c2  {m<0} = half-space {k3<0} is NONEMPTY => C != 0
PASS O6-1c3  C m C = k3 on {k3<0}, NONZERO there => (h-4) HOLDS scalar-ly
PASS O6-1c4  |symbol| >= 1 on {k3 < -1}, a set of INFINITE measure => the scalar sandwich is nonzero and non-compact, SPIN-FREE and GAUGE-FREE
PASS O6-1c5  the indicator multiplier is idempotent (a projection): x^2 - x = 0 at x in {0,1}
PASS O6-1d1  GLOBAL U(1) phase is unitary and central: |e^{it}|^2 = 1
PASS O6-1d2  GLOBAL U(1) leaves ||h psi|| and ||psi|| UNCHANGED (modulus 1) => it neither creates nor removes the blocker
PASS O6-1d3  a NON-CONSTANT U(1) potential BREAKS translation invariance: A(x+a) - A(x) = a != 0
PASS O6-1d4  h_A's symbol depends on the POSITION variable => NOT a multiplier => hypotheses (h-1)/(h-2) FAIL under EM structure
PASS O6-1d5  a CONSTANT potential shifts the symbol only: d/dx = 0 => translation invariance RESTORED (pure-gauge case)

==============================================================================
O6-2  (d-2) — THE DEGREE SEPARATION IS SCALE ARITHMETIC, INDEX-FREE
==============================================================================
PASS O6-2a  s0*mu^0 + t1*mu^-1 == 0 for two distinct mu  =>  s0 = t1 = 0 only
PASS O6-2b  hence a NONZERO degree-0 survivor cannot be cancelled by any degree<=-1 term — pure homogeneity, NO spinor index, NO gauge index
PASS O6-2c  the argument uses ONLY the two exponents, not the coefficients' internal (spin/charge) structure

==============================================================================
O6-3  (d-3) — A CELL-CONSTANT BOUND DETERMINES NO EXPONENT (pure counting)
==============================================================================
PASS O6-3a  V1^b G = V2^b G = K with V1 != V2  =>  b = 0, G = K (unique)
PASS O6-3b  the derivation consumes ONLY the constancy of the map cell |-> bound; it never reads the content's symmetry

==============================================================================
O6-4  (d-0) — DERIVATIVE COUNTING: ONE PER SLOT (order, not symmetry)
==============================================================================
PASS O6-4a  Gronwall's left slot already carries order 1; the display adds 1  =>  the integrand charges order 2 (an H^2 quantity)
PASS O6-4b  the polarization route charges the same order: ||h_0(chi + h_0 chi)|| contains order 2
PASS O6-4c  ESCALATION is a property of the ORDER of the generator (first order), not of any symmetry it carries: 1 + 1 > 1

==============================================================================
O6-5  FP-1 — RANK x OP COUNTING AT ARBITRARY SPINOR DIMENSION d
==============================================================================
PASS O6-5a  the rank x op conversion grows like n^{3/2} for EVERY d >= 1
PASS O6-5b  it diverges ALSO at d = 1  (spin-free, gauge-free content)
PASS O6-5c  the spinor dimension enters ONLY as the constant factor sqrt(d): sqrt(d n^3) / sqrt(n^3) = sqrt(d), n-FREE
PASS O6-5d  therefore the n-divergence — the blocker — is CARRIED BY THE MODE COUNT n^3, not by any internal index

==============================================================================
O6-6  FP-2 — THE VOLUME-DIAGONAL LOCUS INTEGRAL IN R^3, INDEX-FREE
==============================================================================
PASS O6-6a  |x-y|^-3 against the R^3 radial measure gives d(rho)/rho
PASS O6-6b  its primitive is log(rho)
PASS O6-6c  the diagonal (eps -> 0) integral DIVERGES logarithmically
PASS O6-6d  the divergence is fixed by the SPATIAL DIMENSION 3 alone (3 - 3 = 0 => log); it reads no spinor and no gauge index
PASS O6-6e  a boundary(codim-1)-supported asset offers rho^2/rho = rho^1 of measure at the wall, NOT the volume-diagonal power — a SUPPORT mismatch

==============================================================================
O6-7  W-2 — THE THREE HALTS: WHICH ONE IS A GAUGE FACT
==============================================================================
PASS O6-7a  H-1: with the datum unformed the per-cell expression stays an UNEVALUATED Integral carrying the unformed datum — NOT an explicit element of the ground field
PASS O6-7b  H-1 consumes only DOMAIN EMPTINESS; it reads no symmetry
PASS O6-7c  H-2: overlapping-but-distinct descriptions — intersection nonempty, sets unequal => no common cell is formed
PASS O6-7d  H-2 is a SET-IDENTIFICATION fact; it reads no symmetry
PASS O6-7e  H-3: distinct admissible sections give per-cell values differing by the residual Integral(delta_sect) — NOT identically zero
PASS O6-7f  H-3 IS a gauge fact: the blocker is the NON-CANONICITY of a section choice — it exists only because a gauge must be chosen
PASS O6-7g  and H-3 is DISPLAYED NON-BINDING: H-1 and H-2 each suffice alone, so removing H-3 leaves the halt standing

==============================================================================
O6-8  THE CONVERGENCE ARITHMETIC — THE ASSET'S GRADE AT ALL THREE POINTS
==============================================================================
PASS O6-8a  FP-1: asset n-degree 0 vs the n^{3/2} the conversion must beat  => shortfall 3/2
PASS O6-8b  FP-2: displayed -1 vs displayed strict < -3  => shortfall 2
PASS O6-8c  FP-3: displayed 0 vs displayed -2  => shortfall 2
PASS O6-8d  IN ALL THREE the asset's degree in the obstruction's own grading variable is >= 0 — i.e. the asset is HOMOGENEOUS / LOCALIZATION-BLIND, while every requirement is STRICTLY NEGATIVE (localization-grade)
PASS O6-8e  and NONE of the three shortfalls is a function of any internal (spin / charge / U(1)) index: each is an exponent difference alone

==============================================================================
TOTAL CHECKS: 49   PASS: 49   FAIL: 0
==============================================================================
```

---

## 10. CHOICE LEDGER (commission O6SR; every unforced choice, classified)

```text
CH-1  THE COUNTERFACTUAL CONTENT CLASS. Running counterfactual A required
      choosing a scalar indefinite symbol; m(k) = k_3 on L^2(R^3) (x) C^1 was
      chosen. UNFORCED — any real multiplier taking both signs on sets of
      positive measure serves identically. CLASS: generic placeholder,
      constructed as an INSTRUMENT of the counterfactual. NO record object is
      instantiated by it and none is proposed. The verdict does not depend on
      the choice: what the theorem consumes is (h-1)-(h-4), and any such
      symbol supplies them.
CH-2  THE GAUGE COUNTERFACTUAL'S POTENTIAL. A_3(x) = x_1 was chosen as the
      non-constant U(1) potential. UNFORCED — any non-constant A gives
      d(symbol)/dx != 0. CLASS: generic placeholder. The finding is the SIGN of
      the derivative's non-vanishing, not the potential.
CH-3  THE CLASSIFICATION VOCABULARY. The commission named four blocker types
      (symmetry / spectral / domain / counting). FP-2's Layer A is a
      SUPPORT/LOCUS fact that reduces to none of the four cleanly, so SUPPORT
      was ADDED as a fifth. UNFORCED and DISPLAYED: a reader preferring the
      four-way vocabulary may read SUPPORT as a COUNTING fact about
      codimension (rho^2/rho = rho^1, O6-6e), which changes no verdict.
CH-4  **MIXED RATHER THAN GENERIC.** Eight of the nine isolated blockers are
      generic and NO BINDING BLOCKER IS GAUGE-ORIGINATED, which would license
      the verdict GENERIC. MIXED was chosen instead so that H-3 — a real,
      sealed, gauge-originated blocker — is not erased by an aggregate word.
      THE ALTERNATIVE READING IS DISPLAYED HERE SO THE REGISTRAR MAY TAKE IT:
      "GENERIC at every binding blocker; one non-binding gauge-originated
      blocker of record (W-2 H-3)." Both sentences describe the same finding.
CH-5  THE NAME OF THE SHARED ORIGIN. "Localization-blindness of the admitted
      assets" is a NAME chosen for a property whose CONTENT is the §5.1
      exponent table plus the §5.2 escape quotations. UNFORCED. Alternatives
      considered and displayed: "homogeneity-grade assets against
      localization-grade demands"; "the assets are invariant in exactly the
      variable the obstructions are graded in". Any of the three carries the
      same content; none is a record term, and none is proposed as one.
CH-6  THE REFUSED RECONCILIATION. MAP/WB7's "HS threshold 3/2" and BL2*'s "HS
      threshold -3/2" are opposite sign conventions in two sealed carriers.
      They were NOT reconciled; each is used only inside its own frame.
      CHOICE: refusal, because reconciling two sealed conventions would be
      authorship. FLAGGED at FLAG-3.
CH-7  SCOPE OF READING. WALL, CERT, FHB, CL-ERROR, BL2*, RL2B-R were consumed
      ONLY through the commissioned artifacts' own quotations plus the
      declared token sweeps — not read as full determinations. CHOICE, made
      because the commission names eight artifacts and licenses "declared
      scoped sweeps for the rest". Every quotation of them here is transcribed
      from a commissioned carrier's quotation of them, and every digest was
      independently recomputed (§1).
CH-8  THE CENSUS IS CORROBORATION, NOT PROOF. The §4.1 zero-token census would
      by itself be weak evidence (absence of a word is not absence of a
      structure). The verdict is therefore rested on the EXECUTED
      COUNTERFACTUALS (§4.2, §4.3, §4.5), which do not consume the census at
      any step. CHOICE, and load-bearing: it is why ABS-1 is displayed as an
      absence rather than used as an argument.
CH-9  NO ROUTE SELECTED. §6's repair discussion is DISPLAY of what the
      supported reading licenses. No route is selected, recommended, ranked,
      or costed; no object is proposed for construction; the registrar and the
      principal are the judges of every one.
```

---

## 11. TOY_SEPARATION

```text
ACTUAL SURFACE — the thing itself, at the record's own bytes:
  - The three failure points FP-1/FP-2/FP-3 and their standing blockers, every
    one quoted verbatim from a sealed carrier (§2, §3).
  - The isolation of each blocker's blocking PROPERTY from the displayed
    mechanism (§3) — a determination about the record's own objects.
  - The ORIGIN determination for each property (§4), the CONVERGENCE (§5), and
    the CONSEQUENCE reading (§6). These are the commissioned deliverables and
    they are about the actual closure, not about any model of it.
  - The four exact absences (§7), each named at its own site.

INSTRUMENT, NOT SURFACE — constructed here solely to run counterfactuals:
  - The scalar content classes m(k) = k_3 and m(k) = |k| on L^2(R^3) (x) C^1
    (O6-1b, O6-1c).
  - The placeholder U(1) potential A_3(x) = x_1 (O6-1d).
  - The placeholder cell/section/datum models in O6-7 (finite sets, an
    unevaluated integral, a generic section perturbation).
  THESE ARE NOT MODELS OF THE CLOSURE AND NOT CANDIDATES FOR ANYTHING. They
  are used in ONE direction only — the SUBTRACTIVE direction, to remove a
  structure and ask whether a blocker survives. NONE is used constructively;
  none supplies, approximates, stands in for, or bounds any record object;
  none instantiates a record object; and no quantity computed from any of them
  is offered as a value of anything.

WHY THIS IS NOT A TOY: a toy would be a simplified stand-in whose behaviour is
  then attributed to the real object. Nothing here does that. The actual
  blockers are traced on the ACTUAL record's own displays; the placeholders
  appear only where the commission asks for the counterfactual ("state what
  the blocker would become for a content class lacking the relevant
  symmetry"), and a counterfactual is precisely a claim about a DIFFERENT
  class — it attributes nothing back. The one place a constructed object
  produces a +infinity (O6-1c4) is explicitly quarantined in §8's NOT REFUTED
  clause as "an object of the argument, not an object of the closure".

WHAT WOULD MAKE THIS A TOY, AND IS ABSENT: using the scalar class as a
  tractable substitute for the Dirac content and reading a closure conclusion
  off it; proposing m(k) = k_3 as a content class the program should adopt;
  treating the placeholder potential as an EM background of record; or
  carrying any number out of §9 into a program quantity. NONE OCCURS.
```

---

## 12. FLAG BLOCK — STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001

```text
COMMISSION            = O6SR | ORIGIN-BUILD | 2026-08-15
QUESTION              = do the boundary-closure's obstructions trace to
                        gauge/EM structure?
VERDICT               = MIXED( one gauge-originated blocker of record —
                        W-2's H-3 section-gauge halt, DISPLAYED NON-BINDING by
                        its own carrier; ALL EIGHT OTHERS GENERIC; NO BINDING
                        BLOCKER IS GAUGE-ORIGINATED )
BLOCKER_ORIGIN_FP1    = GENERIC( counting: mode count n^3 vs n-free target;
                        survives at internal dimension d = 1 )
BLOCKER_ORIGIN_FP2    = MIXED( Layer A locus GENERIC; H-1 domain GENERIC and
                        BINDING; H-2 identification GENERIC and BINDING;
                        H-3 section-gauge GAUGE-ORIGINATED and NON-BINDING )
BLOCKER_ORIGIN_FP3    = GENERIC( symmetry: translation invariance; survives on
                        scalar indefinite content; DESTROYED by switching real
                        U(1)/EM structure on; subordinate (h-4) is SPECTRAL
                        indefiniteness, spinor-supplied, not gauge )
COUNTERFACTUALS_RUN   = 5( scalar indefinite A; scalar definite A'; global
                        U(1) B1; local U(1) B2; constant/pure-gauge B3 )
CONVERGENCE_TEST      = YES( one shared origin, NOT gauge )
SHARED_ORIGIN_NAMED   = every admitted asset has degree >= 0 in the
                        localization variable its own obstruction is graded in,
                        while every requirement is strictly localized;
                        shortfalls 3/2 (FP-1), 2 (FP-2), 2 (FP-3)
CONSEQUENCE_READING   = NOT "cannot close on gauge-carrying content";
                        IS "cannot close on any content class whose admitted
                        assets are all localization-blind — and by its current
                        certification rules every asset it admits is"
REPAIR_TYPE_DISPLAYED = one input, three times named of record: a
                        localization-grade asset (MO-3 / CN-1 topology /
                        CN-2 evaluation / W-3 deciding-object clause (a))
GAUGE_REPAIRS_STATUS  = already refuted at bytes( no sealed identity cancels
                        the degree-0 survivor; "NO ALGEBRAIC WARD IDENTITY
                        TURNS THAT SUPPORT INTO THE DIAGONAL"; Ward
                        transversality holds for EVERY member, constrains
                        nothing )
EXACT_ABSENCES        = 4( ABS-1 no gauge typing at the failure points;
                        ABS-2 the two lanes never meet; ABS-3 "longitudinal"
                        untyped + HS-threshold sign conventions unreconciled;
                        ABS-4 S's chiral reading undisplayed and moot )
SEALS_VERIFIED        = 8 commissioned, all OK from own directory;
                        + 6 further carriers digested; cross-provenance with
                        W-3 §1 AGREES byte-for-byte
SWEEP_CUTOFF          = 2026-08-15, this session; terms declared §1.1
CAS                   = fresh venv o6venv, sympy 1.14.0, 49 checks, 49 PASS,
                        0 FAIL; exact symbolic only
FENCES                = alpha_computed = false | proof_authorized = false |
                        kappa_record_computed = false
AUTHORED_PHYSICS      = none | VALUE_PRODUCED = none | NUMBER_PRODUCED = none
MEASURED_CONSTANT_CMP = none
FP1_FP2_FP3           = ALL STAND, unmoved as failure points
MAP_SS3_4_AND_3_5     = UNTOUCHED; {W-1,W-2,W-3} still the unique minimal set
WITNESSES             = E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS |
                        SCAD_HS_SCALING_EXPONENT_UNDERIVED STANDS |
                        E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER STANDS
WITNESS_CREATED       = none | WITNESS_RETIRED = none (registrar's act alone)
V_CLAUSES             = untouched | GATES = untouched | QUANTIFIERS = untouched
NOT_REFUTED           = carried in full; no divergence of any closure quantity
                        is exhibited; all consuming quantities UNDECIDED
ROUTE_SELECTED        = none | OBJECT_PROPOSED = none
GIT                   = not used
```
