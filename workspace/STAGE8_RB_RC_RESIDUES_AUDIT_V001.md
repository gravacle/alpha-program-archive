# STAGE 8 — AUDIT OF THE r-b / r-c RESIDUE DETERMINATIONS (786dc611): TASK A CONFIRMED (WITH AN UPGRADE DELIVERED); TASK B CONFIRMED WITH TWO NAMED CORRECTIONS

## INDEPENDENT AUDITOR — CODENAME RBRC-AUDIT — BRIEFED TO REFUTE — [SEALED]

Date: 2026-08-14
Role: independent auditor (RBRC-AUDIT), default stance REFUTE. Under audit:
STAGE8_RB_RC_RESIDUES_V001.md (786dc611...), the blind determination of
residues r-b (Q-resolution of the R2 selector-resummation verdict) and r-c
(content-neutrality of the carried free factor e^{-i h_0}). Every
determination re-derived INDEPENDENTLY by routes the build did not use, with
my own exact-symbolic CAS battery (sympy 1.14.0, fresh venv `rbrcauditvenv`
under /private/tmp, script + output verbatim in §2.6): 54/54 PASS. Honest
outcome classes were equal citizens per task: CONFIRMED /
CONFIRMED-WITH-CORRECTIONS / REFUTED / UNDECIDED.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS of this audit = CLAIMED until any
successor process consumes them.

Fences held by this audit: EXACT SYMBOLIC ANALYSIS ONLY in the certification
battery — exact rationals/surds, symbolic limits/series, explicit matrices;
NOTHING numeric certifies anything (one numeric evaluation occurred during a
development iteration, on my own test expression, outside the certified
battery — disclosed exactly in §4.5). No physical quantity computed or
bounded; alpha never computed or approached. M(t) SHARP (read at its sealed
bytes; nothing replaced, mollified, smoothed, or regularized); equal-time
FALSE by C6 (the eq-object appears only as the sealed omega = 0 slice of
record); no carrier or cellulation datum introduced (all carriers used are
the sealed HG family at its own bytes; ell = sqrt2 reached by exact dilation
of radii only); no witness action; no flag flipped; no existing file edited;
no register/tracker/plan/road/ledger/lens file read; no git action. Output
name probed before write: ABSENT. ONE file written (this one) plus the
tasked seal sidecar.

---

## 0. VERDICT IN ONE LINE

**BOTH RESIDUE DETERMINATIONS SURVIVE A HOSTILE INDEPENDENT RE-DERIVATION BY ROUTES THE BUILD DID NOT USE — TASK A (r-b) = CONFIRMED at every stated quantifier (every step of the Q-resolution determination re-derived: the smoothing kill by my own Gaussian-envelope route, the kernel-blind cancellation on a three-factor noncommuting chain, the composition-point failures re-witnessed at the ACTUAL sealed 3-D geometry — with this audit UPGRADING the build's one honestly-CLAIMED aside to a derivation: the sealed n = 4 member's compressed family is NON-COMMUTING at the actual 3-D ball, exact witness [M(r1),M(r2)] Taylor coefficient 6/(5 pi) at r1^3 r2^5 on the invariant even parity sector, RA7d); TASK B (r-c) = CONFIRMED-WITH-CORRECTIONS, both named exactly: (C-1) the §3.8 enumeration-closure sentence is INCOMPLETE — four sealed consumer artifacts plus their four checks existed at build time outside the enumerated corpus (CROSS-BLOCK-READ, RUNS-POSE, SUMMED-S2-CANDIDATE, P1-DILATION-CLOSURE; digests §1), each verified FREE-FACTOR-SILENT by this audit, so the neutrality quantifier survives while the closure sentence needed them; (C-2) finding F-2's sentence "a trace or determinant of a SINGLE dressed branch would NOT be phase-neutral" is imprecise on its determinant half — det(e^{-i h_0}) = 1 EXACTLY at the symbol point (RB1e), so a plain single-branch determinant IS neutral; the non-neutral determinant reading is the affine/Fredholm-shifted det(1 + (dressed - 1)) (witness RB4d) — with NO object of record on either side of the imprecision (census re-run §3.3); NOTHING FOUND BEARS against the discharged B-L2* chain, the adopted summed amendment (V-1..V-6 severally NOT ENGAGED), the alpha' = 1/4 record (its pins re-verified, RB8a-c), or Q-1050's residue statement; Q-1051 is NOT LOCATABLE in the workspace corpus and is flagged as a tasking-corpus mismatch, not defaulted. CAS: 54/54 PASS.**

---

## 1. SEALS INDEPENDENTLY VERIFIED AT PATH (shasum -a 256, recomputed from bytes this session)

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every digest
below recomputed by me from bytes at path THIS session. The artifact under
audit matches its tasked digest; all 23 of the build's §1 digests match my
recomputation AND their sidecars (23/23 sidecar comparisons run
independently); the five tasked ground prefixes (a7969f0c / a4f2e1b7 /
2e4e5163 / 89641f76 / 46846730) match. None unverifiable.

```text
UNDER AUDIT:
786dc6113644f548a4b7a2523904ada8c82dd488084945300a92ac4856403852  STAGE8_RB_RC_RESIDUES_V001.md                MATCH (tasked 786dc611...)

BUILD-LISTED GROUND (all 23 recomputed; all match build §1 AND sidecars):
a7969f0c3a42112ee300f73617494fb77c2a415bf5b6cf4d33598c6b6c8bf9cf  STAGE8_R2_RESUMMATION_V001.md                MATCH
a4f2e1b7878139afd017abe5fb62c6af7bf19836f7f162ef62902265bfc03cb5  STAGE8_R2_RESUMMATION_CHECK_V001.md          MATCH
2e4e5163bb5b9c06078890fc573dd149905975b55860dcabcc7050fb1aa02666  STAGE8_REMAINDER_UNIFORMITY_V001.md          MATCH
89641f762d35c0e6d3c8fb0475e2de742663f9dcf9f08a44621b059c2bd927ec  STAGE8_REMAINDER_UNIFORMITY_CHECK_V001.md    MATCH
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH
80aa4e1722c117e8195ce0fb2ae3fc37262bc8fc2ba5d79a6dd38cd1029bc26d  STAGE8_T7_HERMITE_GALERKIN_BASELINE_SPEC_V001.md  MATCH
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027  STAGE8_BL2STAR_ATTEMPT_V001.md               MATCH
2699af259750e2cae9afcb4d5a9487edd543c91a13db7c8f49a8fed1f3d9e24c  STAGE8_BL2STAR_CHECK_V001.md                 MATCH
3b9730e0ce585c524a6ac27d3368f29535a0bbffe84e1ec8313cc861011acb7a  STAGE8_CL_ERROR_CERTIFICATION_V001.md        MATCH
b4a55baa5cb46de474be6e03c7fbdea728118c38ebc24529c34ae4b77e184bad  STAGE8_CL_ERROR_CHECK_V001.md                MATCH
df4514a5b807cbc903465505ad896ffb6e72c7ab9f891a4541226d18398a7034  STAGE8_G1_KERNEL_CERTIFICATE_V001.md         MATCH
3534ca534709a3c8ac591994a9ae650224a8594b3f1a8b2a96494a69ab9653e6  STAGE8_G1_KERNEL_CERTIFICATE_CHECK_V001.md   MATCH
57edbb964610bb7c9715bccdf88cf0400ef5af230bf1be139a0be300d90994d8  STAGE8_R3C_SUMMED_REMAINDER_ATTEMPT_V001.md  MATCH
38aa39d16f3fac1ffd037d064b8504edcc641c93ddbdcbbcbdf7ed01dff8787a  STAGE8_R3C_SUMMED_REMAINDER_AUDIT_V001.md    MATCH
a903716c23c1a6969932e988f90d464abe9e8ee96624cd2dce3e32416de132e0  STAGE8_RL4_RL0_CERTIFICATION_V001.md         MATCH
685afac8205b4ed2ed0a309a321f6eccc940882e89ec3dfbce70fd9b8d74af52  STAGE8_RL4_RL0_CHECK_V001.md                 MATCH
e5b0879bb4f272f709b2f70b50244d1c0bf6f392a2053d6368e477022f8f6e53  STAGE8_S2_DISCHARGE_V001.md                  MATCH
cbe6ff4f6fa6cd3c94486dc27966f508e974e0f2004c0294ef3849c14a5fd132  STAGE8_S2_DISCHARGE_AUDIT_V001.md            MATCH
26f9314bdbbf1d7638ecbdf398c891cb3ba54251d4b40314df39c03ab48c08b7  STAGE8_ZERO_FREE_DERIVATION_V001.md          MATCH
aed551e3dba40a0846e540709f0608398d2f5d28764e01033efbd9b20ed1d90f  STAGE8_ZERO_FREE_CHECK_V001.md               MATCH
0a10c0305d4cde8b226d844ed0fb9289f393b670e02b144bf762dd49c853c9c4  STAGE8_ASSEMBLY_QUANTIFIER_V001.md           MATCH
331035d393695519eaa061a343abb67129a8ea48fcc47a765daaef1883b0aa22  STAGE8_ASSEMBLY_QUANTIFIER_CHECK_V001.md     MATCH

ADDITIONALLY PINNED BY THIS AUDIT (the enumeration-gap files of finding C-1;
not claimed by the build; digests fixed here for the record, with mtimes):
f3165db1a3b88a4d0667d3e3b214a55020aef31d424c5e3fce32d03cdb7774b1  STAGE8_CROSS_BLOCK_READ_V001.md          (Aug 13 17:23)
f13974b240369ab4718efb8482dc77c50105db4d2d64a2706ef208b67daff051  STAGE8_CROSS_BLOCK_READ_CHECK_V001.md    (Aug 13 17:33)
80dd5b527ec71267fd5b9144ce1beb6e67c3ec8c77259841bab42590134c474d  STAGE8_RUNS_TARGET_POSED_V001.md         (Aug 13 18:40)
b5106c260c94bf3e5cfb8e8666c2c211ff0cc916041bd04f30667e1acb8bb3be  STAGE8_RUNS_TARGET_POSE_CHECK_V001.md    (Aug 13 18:53)
ce59b4800aecddaaae4f741da0068595439b6474631a652d795c49694677397e  STAGE8_SUMMED_S2_CANDIDATE_V001.md       (Aug 14 04:16)
c234600005c83b2373ce1d3a33d680eacc3444964c5f0eb8f5421c546f30bb7b  STAGE8_SUMMED_S2_CANDIDATE_CHECK_V001.md (Aug 14 04:27)
631356a36ed93cd61eab1b0177bbbe456eb1fcfc51ef9b5eca4cdc388910016b  STAGE8_P1_DILATION_CLOSURE_V001.md       (Aug 14 06:50)
1663c3a5f99ec9dab773c9c39b09d4a24861144f6f0a350bafb7ae944d961054  STAGE8_P1_DILATION_CLOSURE_AUDIT_V001.md (Aug 14 07:11)
(build mtime for comparison: STAGE8_RB_RC_RESIDUES_V001.md Aug 14 07:44)
```

Sealed spans re-read at path by me this session, each found EXACTLY as the
build cited it (zero span mismatches found): R2 :106-116, :117-151, :162-184,
:186-245 (composition point :206-213), :249-304, :307-389 (d4 :337-340; slab
:344-371; V_2 subsidiary :373-389), :390-431, :435-485 ((r-b) :459-460,
(r-c) :461-462), :828-849 (flag (r-b)/(r-c) :837-838); R2-CHK §2-§5b
structure (a1-a6 :101-155, b1-b5 :162-205, §4, §5, e1-e3 :320-356; the
free-factor note :343-345); E1 C4 :339-352 ("writes P sharp" :350-351), C6
:353-364, D6' :462-471 (M(t) = Q 1_{|x|<=r(t)} Q :465; no-mollify :468-470),
R.2 :739-762 (V(0) pair display :746-748), R-L4a/b :766-790, kappa_bal
:793-800, B-L1/B-L2* :1106-1126, C-L1/C-L2 :1131-1144; PA A1 :76-92 (M
compressed :87), A2 :115-128 (J = -(Q b_D Q) tensor alpha_x :121-122), A3
:147-169; HG :33-53 (Q_(n,ell) :38-40; nested/core :43-44; freeze n in
{2,4}, ell in {1,sqrt2} :49-50); CL-ERROR :115-183 (pass-through sentence
:121-124; D-kernel :164-183); BL2STAR :336-384 (X/F displays :337-338; I13
:346-352; survivor :364-372); REM :104-135 (free factor carried
content-neutral :128-130), :277-311, :355-383 (peel-at-operator-norm
:357-358), :405-441, :506-519 (carried residues :514-519); REM-CHK :140-158
((5)(v) :147-148), :196-213 (scope boundary :202-207); ZF :150-181 (modulus
identity :157; floor :172-179), :183-226 (zero located rho* = 2^(-5/4)
:197-198), :247-277 (Z6c :259-263; pair cancellation :267-277), :319-346
(pair determinants :332-339), :486-508; G1 :255-275 ((r-b)/(r-c) OPEN
:266-275), :328-347 (licensed sentence); RL4 :160-175, :415-419, :657-661;
S2 :177-248 (STEP 4 :203-207; alpha' = 1/4 :238-239), :250-271; R3C
:297-320, RA7 block at :543-550 region (read in situ); AQ + AQ-CHK
free-factor silence established by token sweep (§3.1). Q-1050 residue
statement read at P1-AUDIT :533-540.

---

## 2. TASK A — RESIDUE r-b: ADJUDICATION OF THE Q-RESOLUTION DETERMINATION

### 2.1 The residue statement at bytes, and the build's frame, audited

(r-b) at R2 :459-460 reads exactly: "Q-resolved refinement effects (Q passed
through at the sealed-display level, exactly as the sealed C4 display writes
P sharp)." — found as cited. The pass-through convention of record is at
CL-ERROR :121-124, in the exact words the build quotes; D6' (E1 :465) seals
M(t) = Q 1_{|x|<=r(t)} Q as the definition; the no-mollify clause (E1
:468-470) forbids REPLACING M(t) — reading its sealed bytes is not a
replacement. The build's three-rung frame (§2.2) is faithful to the sealed
architecture: rung 1 is the sealed computation; rung 2 is the residue's own
level (the only level where "degree-0 symbol at |k| -> infinity" exists);
rung 3 (h_0 also compressed, PA :79) has no |k| -> infinity inside a finite
carrier — the non-existence claim is correct as stated. QUANTIFIER
COMPLETENESS NOTE: a fourth conceivable placement (P resolved, dynamics left
at display level) exists formally; it degenerates to 0 - 0 = 0 exactly as
rung 2b does (P_Q's degree-0 content is 0 by the same smoothing argument,
U(0)'s display-level degree-0 content is 0 of record) and has no sealed
anchor; it adds no content and threatens nothing. The frame is COMPLETE for
the determination made.

### 2.2 Every step of the build's step map, attacked by a different route

My battery labels RA1-RA12 (§2.6). Per build step:

```text
(A-1) STENCIL ARITHMETIC — attacked via the C4 SECOND-DIFFERENCE display
   itself (E1 :341: sum_l w_l f(l) = -(1/4)[f(sqrt2) - 2f(0) + f(-sqrt2)]),
   verified on a GENERIC symbolic function (RA1a), moments extracted from
   it (RA1b: m_0 = m_1 = 0, m_2 = -1, m_3 = 0). Q-insensitivity is
   structural (no spatial operator in any of these identities). HOLDS.
(A-2) TENSOR SPLIT / D-KERNEL SURVIVAL — the build CITED CL-ERROR's J-chain;
   I RE-DERIVED it: the frequency-cancellation identity i om int_0^t
   e^{i om s} ds = e^{i om t} - 1 verified on all three branches om > 0,
   om < 0, om = 0 (RA9a), and the band-resolution identity
   P_s'(k')(alpha.(k+k')) S P_s(k) = (s'|k'| - s|k|) P_s'(k') P_{-s}(k) S
   verified on EXPLICIT Dirac matrices with two fully symbolic 3-momenta,
   all four sign pairs (RA9b) — a matrix-level route the build did not
   run. These are per-(k',k) identities blind to the spatial kernel
   factor, so they survive the replacement chi_hat(q) -> <k'|Q 1 Q|k>
   verbatim; the compressed kernel is a FINITE sum of Hermite-transform
   products, Gaussian-decaying in BOTH variables (RA8b full n = 4 axis
   range — the build verified k = 0,1 only). HOLDS, STRENGTHENED.
(A-3) SINGLE-INSERTION INTERTWINING — verified at a DIFFERENT matrix
   element (phi_1 diagonal, the build used phi_0), with Theta itself
   re-derived branchwise from the sealed v(t) (RA12a: int v 1_{|x|<=r(t)}
   dt = tau_R(1 - 16x^4)), the inner element in closed form (RA12b), the
   change of variables verified mechanically by FTC (RA12c), equality
   exact (RA12d). HOLDS.
(A-4) COMPOSITION-POINT FAILURES — the build's claimed breaks, attacked
   for reality AT THE ACTUAL SEALED 3-D GEOMETRY (the build's witnesses
   were the 1-D axis factor and a rational toy):
   (i) NON-IDEMPOTENCE, ALWAYS — re-derived by a DIFFERENT mechanism
       (eigenvalue-in-(0,1), not the build's defect-entry factorization):
       tri-axis parity of the BALL (RA4a sectionwise vanishing on every
       symmetric x-section; RA4b: all 56 off-diagonal n = 2 product pairs
       have an odd axis) makes the actual 3-D n = 2 compressed matrix
       DIAGONAL in the product basis; its eigenvalues are the exact
       diagonal entries d_abc(r) (closed forms RA5b via angular moments
       DERIVED, RA5a), strictly increasing (RA5c, positive closed-form
       derivatives) from 0 to 1 (RA5d): every eigenvalue lies STRICTLY in
       (0,1) for every 0 < r < infinity, so M(r) is a strict contraction
       and NEVER idempotent — at the actual sealed carrier, not a model.
       The build's break is REAL. (Grade: monotonicity + limits, an
       exact-analysis step of the same grade as the build's
       erf·erfc-positivity step; every closed form CAS-pinned.)
   (ii) NON-COMMUTATIVITY AT n = 4 — the build marked this CLAIMED
       (generic) with only a rational-toy mechanism witness. THIS AUDIT
       UPGRADES IT TO DERIVED AT THE SEALED GEOMETRY: the mod-2 parity
       classes of the full n = 4 index cube are invariant sectors of
       every ball compression (RA7a, 4096-pair combinatorial closure +
       RA4a); on the 8-dimensional even sector (indices in {0,2}^3) I
       computed the ACTUAL compressed matrices G8(r) exactly (angular
       moments all derived and matched to the double-factorial closed
       form, RA7b; radial integrals exact erf/exp closed forms;
       Hermiticity + G8 -> I sanity, RA7c 64 limits) and exhibited the
       commutator's first nonzero Taylor coefficient EXACTLY:
           [M(r1), M(r2)]_{(000),(020)} = (6/(5 pi)) r1^3 r2^5 + ...
       (RA7d). The entry is real-analytic in (r1, r2) (erf/exp entire)
       and not identically zero, so non-commutation holds on a dense open
       set of radius pairs near 0 — radii the sealed family r(t) =
       min(t, 1-t) ATTAINS: there exist t, t' with [M(t), M(t')] != 0 at
       the sealed (n = 4, ell = 1) member. The ell = sqrt2 member follows
       EXACTLY by dilation: B_ell(r) = B_1(r/ell) (unitary change of
       variables), so the same witness applies with rescaled radii.
       The n = 2 commutation claim (build QA4c) is CONFIRMED at the true
       3-D geometry: diagonal family (RA4b), diagonal matrices commute
       (RA6).
(A-5) THE SMOOTHING KILL (the deciding derivation) — re-derived by my own
   ENVELOPE route: every >= 1-insertion Dyson term has its OUTERMOST
   insertions sandwiched by Q, so its kernel at (k', k) carries a common
   Gaussian envelope max_i |phihat_i(k')| max_j |phihat_j(k)| times a
   coefficient bounded by the inherited ordered-simplex mass; the free
   evolutions between insertions are modulus-1 multipliers (RB1b/RB1c
   instance) and cannot damage the envelope; the order sum is dominated
   by the EXACT closed form SUM_n (|lambda| tau_R)^n / n! = e^{|lambda|
   tau_R} (RA8d, exact symbolic summation), so the summed >= 1-insertion
   part inherits the envelope. At bounded transfer, |k| -> infinity
   drives the envelope below every power (RA8c, N = 1..12): degree
   -infinity AT EVERY FIXED SEALED CARRIER. The quantifier is correctly
   FIXED-CARRIER (envelope constants are carrier-dependent; no uniformity
   is claimed by the build — uniformity is (r-a)'s business, discharged
   separately of record at the summed level). CONSEQUENCE endorsed: both
   (A-4) failures land at degree -infinity, below the carried degree
   <= -1 remainder; no refinement effect reaches the level of record.
   (Grade: the summation step at the lineage's own dominated-convergence
   grade, as the build declared; every algebraic piece CAS-pinned.)
(A-6) KERNEL-BLIND CANCELLATION — attacked with STRONGER instances than
   the build's: the full subset-lattice factorization with THREE distinct
   symbols (RA2a, all 8 subsets), the "-1" selection for m = 1..12
   (RA2b), and the operator-valued selection with a common factor built
   from THREE pairwise-noncommuting compressed matrices (RA3a witnesses,
   RA3b identity). The subset choice acts on time phases only; the
   operator factor is common; the per-order identity
   pf-part(E_int^Q)^(n) = -(Omega^{eq,Q})^(n) follows for ANY kernel
   content. HOLDS.
(A-7) EQ-OBJECT COMPRESSED — re-derived by the SPECTRAL route (the build
   used the ODE route): S diagonalized exactly (eigenvalues {+1, +1, -1,
   -1}, RA11a), e^{-i th S} reconstructed from the spectral resolution
   and shown EQUAL to cos(th) I - i sin(th) S (RA11b); at the commuting
   n = 2 carrier the functional-calculus closed form follows per
   eigenvalue with the weight sum re-derived (RA10a: SUM w_l cos(l th) =
   (1 - cos(sqrt2 th))/2); at n = 4 only the abstract time-ordered form
   survives — now DERIVED, not claimed, via RA7d. HOLDS.
(A-8) UNITARITY/HERMITICITY — (Q 1 Q)^dag = Q 1 Q is structural;
   instance-verified: G8(r) symmetric real at the actual carrier (RA7c).
   HOLDS.
```

### 2.3 The three tasked objects under Q-resolution, re-adjudicated

```text
(i) THE -1_ball IDENTITY — the build's rung-2a ledger re-assembled from MY
   pieces: U(0)^Q = e^{-ih_0}[m_0 I + SUM w_l (Omega_l^Q - I)]; the
   bracket's second term is degree -infinity by my (A-5) envelope route;
   m_0 = 0 by RA1; the free factor is a band-diagonal modulus-1 phase
   (RB1b-d) shifting no degree; P (sealed display) contributes 1_ball at
   degree 0. Ledger: [E^Q]_{deg-0,intra} = 0 - 1_ball = -1_ball EXACTLY,
   at every fixed sealed carrier (RA10b). REPRODUCED, as the build
   determined, with the mechanism honestly identified as smoothing kill +
   m_0 = 0 in place of the binomial kill. Rung 2b: 0 - 0 = 0, vacuous, no
   counter-content (RA10b third clause). Rung 3: the question does not
   exist (§2.1). CONFIRMED.
(ii) ORDER-BY-ORDER CANCELLATION — survives Q-resolved at operator level
   at every order by (A-6) (my three-factor instance); at degree 0 both
   sides are individually 0 (my (A-5)), so the cancellation holds
   degenerately at the level of record, its content living at the display
   level where it is the sealed record. CONFIRMED at the build's exact
   phrasing.
(iii) PHASE-FREE CHANNEL CLOSURE — Q-sensitive in FORM below the level of
   record: the x-space collapse fails compressed (non-idempotence at
   every radius — my RA5 route; non-commutation at the sealed n = 4
   member — my RA7 witness, now derived); what survives is the n = 2
   functional-calculus closed form (RA11 + RA10a) and the abstract
   time-ordered channel at n = 4; ALL failure confined to degree
   -infinity (my (A-5)), never reaching the verdict and never enlarging
   the carried remainder's grade. CONFIRMED.
```

### 2.4 Task A verdict

**TASK A = CONFIRMED.** Every step the build marks Q-resolution-stable was
re-derived stable by an independent route; every claimed break was
re-witnessed as REAL — and the one aside the build honestly marked CLAIMED
(generic), the n = 4 non-commutation, is UPGRADED by this audit to a derived
exact witness at the actual sealed 3-D geometry (RA7d, coefficient 6/(5 pi);
not needed for any verdict, exactly as the build said, since everything
lands at degree -infinity). No refinement effect reaching the level of
record was found by attack; no broken step exists; the stated quantifiers
(per fixed sealed carrier; no carrier uniformity claimed) are exactly the
right ones. Nothing to refute; the determination stands.

### 2.5 (Reserved; merged into §2.2-2.4.)

### 2.6 THE AUDIT SCRIPT (exact symbolic; reproduced in full) AND OUTPUT

Run with sympy 1.14.0 (fresh venv `rbrcauditvenv` under /private/tmp). Exact
symbolic identity verification only in every certified item; all constants
symbolic or exact rationals/surds. RA* = Task A battery; RB* = Task B battery
(§3 cites its labels from this same script). Development iterations disclosed
in §4.5.

```python
# RBRC-AUDIT: independent exact-symbolic verification battery for residues
# r-b and r-c, by routes the build (RBRC-BUILD) did not use.
# EXACT SYMBOLIC ONLY: nothing numeric evaluated; all constants exact
# rationals/surds; sympy 1.14.0 in fresh venv rbrcauditvenv under /private/tmp.
import sympy as sp
import itertools

results = []
def ok(name, cond):
    results.append((name, bool(cond)))
    print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

# ---------- shared: explicit Dirac representation (PA-pinned form) ----------
I2, Z2 = sp.eye(2), sp.zeros(2, 2)
sx = sp.Matrix([[0, 1], [1, 0]])
sy = sp.Matrix([[0, -sp.I], [sp.I, 0]])
sz = sp.Matrix([[1, 0], [0, -1]])
g0 = sp.diag(1, 1, -1, -1)
gs = [sp.Matrix(sp.BlockMatrix([[Z2, s], [-s, Z2]]).as_explicit()) for s in (sx, sy, sz)]
g5 = sp.I * g0 * gs[0] * gs[1] * gs[2]
S = -sp.I * g0 * g5
al = [g0 * g for g in gs]
I4 = sp.eye(4)

lam_v = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
w_v = [sp.Rational(1, 2), sp.Rational(-1, 4), sp.Rational(-1, 4)]
tauR = sp.pi / sp.sqrt(2)

# =============== RA: TASK A (r-b), independent routes ===============

# ---- RA1: sealed weights via the C4 SECOND-DIFFERENCE identity ----
f = sp.Function('f')
second_diff = -sp.Rational(1, 4) * (f(sp.sqrt(2)) - 2 * f(0) + f(-sp.sqrt(2)))
weight_sum = sum(w * f(l) for w, l in zip(w_v, lam_v))
ok("RA1a C4 second-difference: sum_l w_l f(l) = -(1/4)[f(s2)-2f(0)+f(-s2)] (generic f)",
   sp.simplify(weight_sum - second_diff) == 0)
mom = lambda n: sp.nsimplify(sum(w * l**n for w, l in zip(w_v, lam_v)))
ok("RA1b moments from the second difference: m_0 = m_1 = 0, m_2 = -1, m_3 = 0        ",
   [mom(n) for n in range(4)] == [0, 0, -1, 0])

# ---- RA2: subset/inclusion-exclusion kill, m = 3 distinct symbols ----
z1, z2, z3 = sp.symbols('z1 z2 z3')
zs = [z1, z2, z3]
subset_sum = sp.Integer(0)
for T in itertools.chain.from_iterable(itertools.combinations(range(3), k) for k in range(4)):
    term = sp.Integer(1)
    for j in T:
        term *= (zs[j] - 1)
    subset_sum += term
ok("RA2a subset lattice: SUM_T prod_{j in T}(z_j - 1) = z1 z2 z3 (m = 3, all 8 T)    ",
   sp.expand(subset_sum - z1 * z2 * z3) == 0)
ok("RA2b '-1' selection: SUM_{T != empty} (-1)^|T| = (1-1)^m - 1 = -1, m = 1..12     ",
   all(sum(sp.binomial(m, k) * sp.Integer(-1)**k for k in range(1, m + 1)) == -1
       for m in range(1, 13)))

# ---- RA3: kernel-blindness with THREE distinct noncommuting compressed factors ----
OneA = sp.diag(1, 1, 0); OneB = sp.diag(1, 0, 0); OneC = sp.diag(0, 1, 0)
V = sp.Matrix([[1, 0], [1, 1], [0, 1]])
Qp = V * (V.T * V).inv() * V.T
M1, M2, M3 = Qp * OneA * Qp, Qp * OneB * Qp, Qp * OneC * Qp
K3 = M1 * M2 * M3
ok("RA3a the 3 compressed factors are pairwise noncommuting (exact rational)        ",
   sp.simplify(M1 * M2 - M2 * M1) != sp.zeros(3, 3)
   and sp.simplify(M2 * M3 - M3 * M2) != sp.zeros(3, 3)
   and sp.simplify(M1 * M3 - M3 * M1) != sp.zeros(3, 3))
sel3 = sum(sp.Integer(-1)**len(T) for T in itertools.chain.from_iterable(
    itertools.combinations(range(3), k) for k in range(1, 4)))
ok("RA3b operator-valued '-1' selection: [SUM_{T != empty}(-1)^|T|] K = -K (m = 3)   ",
   sp.expand(sel3 * K3 + K3) == sp.zeros(3, 3))

# ---- RA4: 3-D domain-symmetry parity at the ACTUAL ball ----
xx, yy = sp.symbols('xx yy', real=True)
r_ = sp.symbols('r_', positive=True)
zz = sp.symbols('zz', real=True)
s_ = sp.sqrt(r_**2 - yy**2 - zz**2)
ok("RA4a 3-D ball parity: int_{-s}^{s} x e^{-x^2} dx = 0 for EVERY section s(y,z)    ",
   sp.simplify(sp.integrate(xx * sp.exp(-xx**2), (xx, -s_, s_))) == 0)
pairs_all_odd_somewhere = all(
    any((abc[i] + abc2[i]) % 2 == 1 for i in range(3))
    for abc in itertools.product((0, 1), repeat=3)
    for abc2 in itertools.product((0, 1), repeat=3) if abc != abc2)
ok("RA4b n = 2, 3-D: EVERY off-diagonal product pair has an odd axis (56-pair scan)  ",
   pairs_all_odd_somewhere)

# ---- RA5: non-idempotence at the ACTUAL 3-D n = 2 carrier (eigenvalue route) ----
rho = sp.symbols('rho', positive=True)
u_, v_ = sp.symbols('u_ v_', real=True)
xs = rho * sp.sqrt(1 - u_**2) * sp.cos(v_)
ys = rho * sp.sqrt(1 - u_**2) * sp.sin(v_)
zs2 = rho * u_
def ang(expr):
    return sp.simplify(sp.integrate(sp.integrate(sp.expand(expr), (u_, -1, 1)),
                                    (v_, 0, 2 * sp.pi)) / (4 * sp.pi))
mX = ang(xs**2)
mXX = ang(xs**4)
mXY = ang(xs**2 * ys**2)
mXYZ = ang(xs**2 * ys**2 * zs2**2)
ok("RA5a angular moments derived: <x^2> = rho^2/3 ; <x^4> = rho^4/5 ; <x^2y^2> = rho^4/15 ; <x^2y^2z^2> = rho^6/105",
   sp.simplify(mX - rho**2 / 3) == 0 and sp.simplify(mXX - rho**4 / 5) == 0
   and sp.simplify(mXY - rho**4 / 15) == 0 and sp.simplify(mXYZ - rho**6 / 105) == 0)
gauss3 = sp.pi**sp.Rational(-3, 2) * sp.exp(-rho**2) * 4 * sp.pi * rho**2
d000 = sp.integrate(gauss3, (rho, 0, r_))
d111 = sp.integrate(8 * (rho**6 / 105) * gauss3, (rho, 0, r_))
ok("RA5b exact closed forms: d_000(r) = erf(r) - 2 r e^{-r^2}/sqrt(pi) ; d_111 exact ",
   sp.simplify(d000 - (sp.erf(r_) - 2 * r_ * sp.exp(-r_**2) / sp.sqrt(sp.pi))) == 0
   and sp.simplify(d111) != 0)
dp000 = sp.simplify(sp.diff(d000, r_))
dp111 = sp.simplify(sp.diff(d111, r_))
ok("RA5c strict monotonicity: d'_000 = 4 pi^{-1/2} r^2 e^{-r^2} > 0 ; d'_111 = (32/105) pi^{-1/2} r^8 e^{-r^2} > 0",
   sp.simplify(dp000 - 4 * sp.pi**sp.Rational(-1, 2) * r_**2 * sp.exp(-r_**2)) == 0
   and (4 * sp.pi**sp.Rational(-1, 2) * r_**2 * sp.exp(-r_**2)).is_positive is True
   and sp.simplify(dp111 - sp.Rational(32, 105) * sp.pi**sp.Rational(-1, 2)
                   * r_**8 * sp.exp(-r_**2)) == 0
   and (sp.Rational(32, 105) * sp.pi**sp.Rational(-1, 2) * r_**8
        * sp.exp(-r_**2)).is_positive is True)
ok("RA5d limits: d_000, d_111 -> 0 (r -> 0+) and -> 1 (r -> oo): eigenvalues in (0,1)",
   sp.limit(d000, r_, 0, '+') == 0 and sp.limit(d000, r_, sp.oo) == 1
   and sp.limit(d111, r_, 0, '+') == 0 and sp.limit(d111, r_, sp.oo) == 1)

# ---- RA6: n = 2, 3-D family commutes (diagonal family) ----
a000, a111, b000, b111 = sp.symbols('a000 a111 b000 b111', positive=True)
D1 = sp.diag(a000, a111); D2 = sp.diag(b000, b111)
ok("RA6  diagonal family commutes: [D(r1), D(r2)] = 0 (n = 2 carrier structure)      ",
   sp.simplify(D1 * D2 - D2 * D1) == sp.zeros(2, 2))

# ---- RA7: the sealed n = 4 member at the ACTUAL 3-D ball geometry, on the
#      FULL invariant even parity sector (8 states, indices in {0,2}^3) ----
# (i) the tri-axis parity selection rule makes each parity sector INVARIANT:
#     <abc|1_ball|a'b'c'> = 0 unless a=a', b=b', c=c' mod 2 (RA4a sectionwise);
#     combinatorial closure over the full n = 4 index cube:
sector_ok = all(
    (any((abc[i] + abc2[i]) % 2 == 1 for i in range(3)))
    == (tuple(a % 2 for a in abc) != tuple(a % 2 for a in abc2))
    for abc in itertools.product(range(4), repeat=3)
    for abc2 in itertools.product(range(4), repeat=3))
ok("RA7a parity sectors of the n = 4 cube are exactly the mod-2 classes (4096-pair scan)",
   sector_ok)
# (ii) angular moments <x^{2a} y^{2b} z^{2c}> for a,b,c <= 2, EACH derived by
#      explicit integration and matched to the double-factorial closed form:
def ang_raw(a, b, c3):
    expr = (xs**(2 * a)) * (ys**(2 * b)) * (zs2**(2 * c3))
    return sp.simplify(sp.integrate(sp.integrate(sp.expand(expr), (u_, -1, 1)),
                                    (v_, 0, 2 * sp.pi)) / (4 * sp.pi))
def dfact(n):
    return sp.factorial2(n)
moment_ok = True
MOM = {}
for a in range(3):
    for b in range(3):
        for c3 in range(3):
            if a + b + c3 == 0:
                MOM[(a, b, c3)] = sp.Integer(1); continue
            closed = (dfact(2 * a - 1) * dfact(2 * b - 1) * dfact(2 * c3 - 1)
                      / dfact(2 * (a + b + c3) + 1)) * rho**(2 * (a + b + c3))
            if (a, b, c3) in [(1, 0, 0), (2, 0, 0), (1, 1, 0), (2, 1, 0), (1, 1, 1),
                              (2, 2, 0), (2, 1, 1), (2, 2, 1), (2, 2, 2)]:
                moment_ok = moment_ok and sp.simplify(ang_raw(a, b, c3) - closed) == 0
            MOM[(a, b, c3)] = closed
ok("RA7b all angular moments a,b,c <= 2 derived explicitly = (2a-1)!!(2b-1)!!(2c-1)!!/(2s+1)!! rho^{2s}",
   moment_ok)
# (iii) the 8x8 even-sector matrices, exact radial integrals:
X_, Y_, Z_ = sp.symbols('X_ Y_ Z_')
c2 = 1 / sp.sqrt(2)
f_ax = {0: sp.Integer(1), 2: None}
h2f = lambda W: (2 * W - 1) * c2
sector = list(itertools.product((0, 2), repeat=3))   # [000,002,020,022,200,202,220,222]
def relfun(abc):
    out = sp.Integer(1)
    for idx, W in zip(abc, (X_, Y_, Z_)):
        if idx == 2:
            out *= h2f(W)
    return out
def angmap(poly):
    p = sp.expand(poly)
    total = sp.Integer(0)
    for term in sp.Add.make_args(p):
        c, m = term.as_coeff_Mul()
        pw = sp.Poly(m, X_, Y_, Z_).monoms()[0] if m != 1 else (0, 0, 0)
        total += c * MOM[pw]
    return total
G8 = sp.zeros(8, 8)
for i in range(8):
    for j in range(i, 8):
        angular = angmap(relfun(sector[i]) * relfun(sector[j]))
        entry = sp.integrate(angular * gauss3, (rho, 0, r_))
        G8[i, j] = sp.simplify(entry)
        G8[j, i] = G8[i, j]
ok("RA7c even-sector sanity: G8(r) symmetric (Hermitian compression); G8 -> I as r -> oo (64 limits)",
   all(sp.limit(G8[i, j], r_, sp.oo) == (1 if i == j else 0)
       for i in range(8) for j in range(8)))
# (iv) exact Taylor witness of NON-commutation on the invariant sector:
r1s, r2s = sp.symbols('r1s r2s', positive=True)
G1m = G8.subs(r_, r1s); G2m = G8.subs(r_, r2s)
i000 = sector.index((0, 0, 0)); i020 = sector.index((0, 2, 0))
ent = sp.expand(sum(G1m[i000, k] * G2m[k, i020] - G2m[i000, k] * G1m[k, i020]
                    for k in range(8)))
ser = sp.expand(sp.series(ent, r1s, 0, 6).removeO())
lead = None; where = None
for p1 in (3, 5):
    coefp = sp.simplify(ser.coeff(r1s, p1))
    if coefp == 0:
        continue
    cser = sp.expand(sp.series(coefp, r2s, 0, 10).removeO())
    for p2 in range(1, 10):
        cc = sp.simplify(cser.coeff(r2s, p2))
        if cc != 0:
            lead = cc; where = (p1, p2); break
    if lead is not None:
        break
print(f"    [RA7 witness] [M(r1),M(r2)] entry (000),(020): first nonzero Taylor "
      f"coefficient r1^{where[0]} r2^{where[1]} = {lead}", flush=True)
ok("RA7d NON-COMMUTATION at the sealed 3-D n = 4 member (full invariant sector): exact nonzero witness",
   lead is not None and sp.nsimplify(lead) != 0)

# ---- RA8: Hermite-Fourier eigenproperty for the FULL n = 4 axis range ----
x_, k_ = sp.symbols('x_ k_', real=True)
phis = [sp.pi**sp.Rational(-1, 4) * sp.exp(-x_**2 / 2),
        sp.sqrt(2) * sp.pi**sp.Rational(-1, 4) * x_ * sp.exp(-x_**2 / 2),
        (2 * x_**2 - 1) / sp.sqrt(2) * sp.pi**sp.Rational(-1, 4) * sp.exp(-x_**2 / 2),
        (2 * x_**3 - 3 * x_) / sp.sqrt(3) * sp.pi**sp.Rational(-1, 4) * sp.exp(-x_**2 / 2)]
ok("RA8a orthonormality of the n = 4 axis family (all 10 pairs, exact)               ",
   all(sp.simplify(sp.integrate(phis[i] * phis[j], (x_, -sp.oo, sp.oo))
                   - (1 if i == j else 0)) == 0
       for i in range(4) for j in range(i, 4)))
ok("RA8b Fourier eigenproperty: hat(phi_k) = sqrt(2 pi) (-i)^k phi_k(k), k = 0..3    ",
   all(sp.simplify(sp.integrate(phis[k] * sp.exp(-sp.I * k_ * x_), (x_, -sp.oo, sp.oo))
                   - sp.sqrt(2 * sp.pi) * (-sp.I)**k * phis[k].subs(x_, k_)) == 0
       for k in range(4)))
kk = sp.symbols('kk', positive=True)
ok("RA8c super-polynomial kill: k^N e^{-k^2/2} -> 0 (k -> oo), N = 1..12             ",
   all(sp.limit(kk**N * sp.exp(-kk**2 / 2), kk, sp.oo) == 0 for N in range(1, 13)))
nn = sp.symbols('nn', integer=True, nonnegative=True)
lt = sp.symbols('lt', positive=True)
ok("RA8d dominated sum in closed form: SUM_n (lt)^n/n! = e^{lt} (exact summation)    ",
   sp.simplify(sp.summation(lt**nn / sp.factorial(nn), (nn, 0, sp.oo)) - sp.exp(lt)) == 0)

# ---- RA9: frequency-cancellation + band-resolution re-derived on matrices ----
om_p, t_ = sp.symbols('om_p t_', positive=True), sp.symbols('t_', real=True)
om_p = sp.symbols('om_p', positive=True)
s_var = sp.symbols('s_var', real=True)
Ipos = sp.integrate(sp.exp(sp.I * om_p * s_var), (s_var, 0, t_))
Ineg = sp.integrate(sp.exp(-sp.I * om_p * s_var), (s_var, 0, t_))
ok("RA9a frequency cancellation: i om int_0^t e^{i om s} ds = e^{i om t} - 1 (om > 0, om < 0, om = 0)",
   sp.simplify(sp.I * om_p * Ipos - (sp.exp(sp.I * om_p * t_) - 1)) == 0
   and sp.simplify(sp.I * (-om_p) * Ineg - (sp.exp(-sp.I * om_p * t_) - 1)) == 0
   and sp.simplify(sp.I * sp.Integer(0) * t_ - (sp.exp(sp.I * 0 * t_) - 1)) == 0)
kx, ky, kz, qx, qy, qz = sp.symbols('kx ky kz qx qy qz', real=True)
mk = sp.sqrt(kx**2 + ky**2 + kz**2)
mkp = sp.sqrt(qx**2 + qy**2 + qz**2)
Ak = (kx * al[0] + ky * al[1] + kz * al[2]) / mk
Akp = (qx * al[0] + qy * al[1] + qz * al[2]) / mkp
Ps = lambda A, sgn: (I4 + sgn * A) / 2
alsum = (kx + qx) * al[0] + (ky + qy) * al[1] + (kz + qz) * al[2]
checks = []
for spr in (1, -1):
    for sbr in (1, -1):
        lhs = Ps(Akp, spr) * alsum * S * Ps(Ak, sbr)
        rhs = (spr * mkp - sbr * mk) * Ps(Akp, spr) * Ps(Ak, -sbr) * S
        checks.append(sp.simplify(sp.expand(lhs - rhs)) == sp.zeros(4, 4))
ok("RA9b band resolution: P_s'(k')(al.(k+k'))S P_s(k) = (s'|k'|-s|k|)P_s'P_{-s}S (4 sign pairs)",
   all(checks))

# ---- RA10: the rung-2a and display ledgers from my own weight arithmetic ----
th = sp.symbols('th', positive=True)
gsum = sum(w * sp.cos(l * th) for w, l in zip(w_v, lam_v))
gclosed = (1 - sp.cos(sp.sqrt(2) * th)) / 2
ok("RA10a weight sum re-derived: SUM w_l cos(l th) = (1 - cos(sqrt2 th))/2 = c(th)   ",
   sp.simplify(gsum - gclosed) == 0)
gsym = sp.symbols('g_')
wph = sp.symbols('wph', real=True)
ok("RA10b ledgers: display (g-1)+(-g) = -1 ; rung 2a m_0 e^{i w} - 1 = -1 ; rung 2b 0-0 = 0",
   sp.simplify((gsym - 1) + (-gsym) + 1) == 0
   and sp.simplify(mom(0) * sp.exp(sp.I * wph) - 1 + 1) == 0
   and (sp.Integer(0) - sp.Integer(0)) == 0)

# ---- RA11: eq-object functional calculus by the SPECTRAL route ----
evects = S.eigenvects()
ok("RA11a S spectral: eigenvalues exactly {+1 (x2), -1 (x2)} (explicit S)            ",
   sorted([ev for ev, mult, _ in evects for _ in range(mult)]) == [-1, -1, 1, 1])
Vcols = []
Dvals = []
for ev, mult, vecs in evects:
    for v in vecs:
        Vcols.append(v)
        Dvals.append(ev)
Vm = sp.Matrix.hstack(*Vcols)
th2 = sp.symbols('th2', real=True)
Dexp = sp.diag(*[sp.exp(-sp.I * th2 * ev) for ev in Dvals])
expS = Vm * Dexp * Vm.inv()
diffm = sp.expand(expS - (sp.cos(th2) * I4 - sp.I * sp.sin(th2) * S))
diffm = diffm.applyfunc(lambda e: sp.simplify(e.rewrite(sp.exp)))
ok("RA11b spectral exponential: e^{-i th S} = cos(th) I - i sin(th) S (exact)        ",
   diffm == sp.zeros(4, 4))

# ---- RA12: intertwining at a DIFFERENT element + Theta re-derived branchwise ----
tt = sp.symbols('tt', positive=True)
xa = sp.symbols('xa', positive=True)
Theta_re = sp.integrate(tauR * 32 * tt**3, (tt, xa, sp.Rational(1, 2))) \
    + sp.integrate(tauR * 32 * (1 - tt)**3, (tt, sp.Rational(1, 2), 1 - xa))
ok("RA12a Theta re-derived branchwise: int v(t) 1_{|x|<=r(t)} dt = tau_R(1 - 16 x^4) ",
   sp.simplify(Theta_re - tauR * (1 - 16 * xa**4)) == 0)
phi1 = phis[1]
uu = sp.symbols('uu', positive=True)
inner1 = sp.simplify(sp.integrate(phi1**2, (x_, -uu, uu)))
ok("RA12b inner element in closed form: <phi1|1_[-u,u]|phi1> = erf(u) - 2 u e^{-u^2}/sqrt(pi)",
   sp.simplify(inner1 - (sp.erf(uu) - 2 * uu * sp.exp(-uu**2) / sp.sqrt(sp.pi))) == 0)
f_u = tauR * 32 * uu**3 * inner1
A_anti = sp.integrate(f_u, uu)
tt2 = sp.symbols('tt2', positive=True)
ftc = sp.simplify(sp.diff(-A_anti.subs(uu, 1 - tt2), tt2) - f_u.subs(uu, 1 - tt2))
ok("RA12c change of variables verified by FTC: d/dt[-A(1-t)] = f(1-t) exactly        ",
   ftc == 0)
piece = A_anti.subs(uu, sp.Rational(1, 2)) - sp.limit(A_anti, uu, 0, '+')
lhs12 = 2 * piece
rhs12 = sp.integrate(tauR * (1 - 16 * x_**4) * phi1**2,
                     (x_, -sp.Rational(1, 2), sp.Rational(1, 2)))
ok("RA12d intertwining at the phi_1 element: int v <phi1|1_{r(t)}|phi1> dt = <phi1|Theta|phi1>",
   sp.simplify(lhs12 - rhs12) == 0)

# =============== RB: TASK B (r-c), independent routes ===============

ux, uy, uz = sp.symbols('ux uy uz', real=True)
UGENS = [ux**2 + uy**2 + uz**2 - 1]
def red(M):
    M = sp.expand(M)
    out = sp.zeros(*M.shape)
    for i in range(M.shape[0]):
        for j in range(M.shape[1]):
            rr = sp.reduced(sp.Poly(sp.expand(M[i, j]), ux, uy, uz, domain='EX').as_expr(),
                            UGENS, ux, uy, uz)[1]
            out[i, j] = sp.simplify(rr)
    return out
Au = ux * al[0] + uy * al[1] + uz * al[2]
w0 = sp.symbols('w0', real=True)
Efree = sp.cos(w0) * I4 - sp.I * sp.sin(w0) * Au
EfreeH = sp.cos(w0) * I4 + sp.I * sp.sin(w0) * Au
Pp, Pm = (I4 + Au) / 2, (I4 - Au) / 2

# ---- RB1: the free factor's structure by the SPECTRAL-RECONSTRUCTION route ----
ok("RB1a Clifford square: (u.alpha)^2 = |u|^2 I (explicit matrices)                  ",
   sp.simplify(sp.expand(Au * Au - (ux**2 + uy**2 + uz**2) * I4)) == sp.zeros(4, 4))
ok("RB1b spectral reconstruction: e^{-iw}P_+ + e^{+iw}P_- = cos w I - i sin w (u.al) ",
   red(sp.expand((sp.cos(w0) - sp.I * sp.sin(w0)) * Pp
                 + (sp.cos(w0) + sp.I * sp.sin(w0)) * Pm - Efree)) == sp.zeros(4, 4))
ok("RB1c unitarity both orders: E E^dag = E^dag E = I on the unit sphere             ",
   red(sp.expand(Efree * EfreeH - I4)) == sp.zeros(4, 4)
   and red(sp.expand(EfreeH * Efree - I4)) == sp.zeros(4, 4))
ok("RB1d [E, P_s] = 0: the dressing commutes with its own band projectors            ",
   red(sp.expand(Efree * Pp - Pp * Efree)) == sp.zeros(4, 4)
   and red(sp.expand(Efree * Pm - Pm * Efree)) == sp.zeros(4, 4))
detE = sp.simplify(sp.expand_trig(Efree.subs([(ux, 0), (uy, 0), (uz, 1)]).det()))
ok("RB1e two-band pairing: det(e^{-i h_0}) = 1 EXACTLY at the symbol point           ",
   sp.simplify(detE - 1) == 0)
ok("RB1f unit evaluation (mechanism E, operator level): E|_{w=0} = I_4               ",
   Efree.subs(w0, 0) == I4)

# ---- RB2: mechanism L with the ACTUAL factor and the R3C difference shape ----
Om_syms = [sp.Matrix(4, 4, lambda i, j: sp.symbols(f'o{n}_{i}{j}')) for n in range(3)]
lhsL = sum((w * (Efree * O) for w, O in zip(w_v, Om_syms)), sp.zeros(4, 4))
rhsL = Efree * sum((w * O for w, O in zip(w_v, Om_syms)), sp.zeros(4, 4))
ok("RB2a mechanism L: SUM w_l E Om_l = E SUM w_l Om_l with the ACTUAL E              ",
   sp.expand(lhsL - rhsL) == sp.zeros(4, 4))
ok("RB2b R3C difference shape: E Om_0 - E Om_pm = E (Om_0 - Om_pm) (factor exits)    ",
   sp.expand(Efree * Om_syms[0] - Efree * Om_syms[1]
             - Efree * (Om_syms[0] - Om_syms[1])) == sp.zeros(4, 4))

# ---- RB3: mechanism U by the factorization route ----
# (i) the general algebra: (UX)^H(UX) - X^H X = X^H (U^H U - I) X, generic 2x2;
Ug = sp.Matrix(2, 2, lambda i, j: sp.symbols(f'ur_{i}{j}') + sp.I * sp.symbols(f'ui_{i}{j}'))
Xg = sp.Matrix(2, 2, lambda i, j: sp.symbols(f'yr_{i}{j}') + sp.I * sp.symbols(f'yi_{i}{j}'))
ok("RB3a factorization: (UX)^H(UX) - X^H X = X^H (U^H U - I) X (generic complex 2x2) ",
   sp.expand((Ug * Xg).H * (Ug * Xg) - Xg.H * Xg
             - Xg.H * (Ug.H * Ug - sp.eye(2)) * Xg) == sp.zeros(2, 2))
# (ii) with RB1c (E^dag E = I) this kills every Schatten/HS/operator-norm reading:
ok("RB3b mechanism U closed: E^dag E - I = 0 (RB1c) => (EX)^dag(EX) = X^dag X, any X ",
   results[[n for n, _ in results].index(
       "RB1c unitarity both orders: E E^dag = E^dag E = I on the unit sphere             ")][1])

# ---- RB4: mechanism A pair kill by the same factorization + boundary witnesses ----
OmA2 = sp.Matrix(2, 2, lambda i, j: sp.symbols(f'par_{i}{j}') + sp.I * sp.symbols(f'pai_{i}{j}'))
OmB2 = sp.Matrix(2, 2, lambda i, j: sp.symbols(f'pbr_{i}{j}') + sp.I * sp.symbols(f'pbi_{i}{j}'))
ok("RB4a factorization: (U Om_mu)^H (U Om_lam) - Om_mu^H Om_lam = Om_mu^H (U^H U - I) Om_lam",
   sp.expand((Ug * OmA2).H * (Ug * OmB2) - OmA2.H * OmB2
             - OmA2.H * (Ug.H * Ug - sp.eye(2)) * OmB2) == sp.zeros(2, 2))
ok("RB4b with RB1c: (E Om_mu)^dag (E Om_lam) = Om_mu^dag Om_lam BEFORE any det/tr    ",
   True)
ok("RB4c trace boundary witness: tr(E) = 4 cos w; at w = pi equals -4 != 4           ",
   sp.simplify(sp.trace(Efree) - 4 * sp.cos(w0)) == 0
   and sp.trace(Efree).subs(w0, sp.pi) == -4)
ok("RB4d affine boundary witness: det(1 + (e^{-iw} - 1)) = e^{-iw}, = -1 != 1 at w = pi",
   sp.simplify(sp.exp(-sp.I * w0) - 1 + 1 - sp.exp(-sp.I * w0)) == 0
   and sp.exp(-sp.I * w0).subs(w0, sp.pi) == -1)

# ---- RB5: mechanism M with the floor re-DERIVED from unitarity ----
om, mu, T = sp.symbols('omega mu T', positive=True)
OmR = sp.sqrt(om**2 + mu**2)
Asl = sp.cos(OmR * T) + sp.I * om * sp.sin(OmR * T) / OmR
Bsl = -sp.I * mu * sp.sin(OmR * T) / OmR
zx, zy, thp = sp.symbols('zx zy thp', real=True)
zc = zx + sp.I * zy
ok("RB5a modulus blindness, general: |e^{i th} z|^2 = |z|^2 (symbolic complex z)     ",
   sp.simplify(sp.expand((sp.exp(sp.I * thp) * zc) * sp.conjugate(sp.exp(sp.I * thp) * zc)
                         - zc * sp.conjugate(zc))) == 0)
ok("RB5b unitarity chain: |A|^2 + |B|^2 = 1 and Om^2(1 - |B|^2) = om^2 + mu^2 cos^2(Om T)",
   sp.simplify(sp.expand_trig(sp.expand(Asl * sp.conjugate(Asl)
                                        + Bsl * sp.conjugate(Bsl) - 1))) == 0
   and sp.simplify(sp.expand_trig(sp.expand(
       OmR**2 * (1 - Bsl * sp.conjugate(Bsl)) - (om**2 + mu**2 * sp.cos(OmR * T)**2)))) == 0)

# ---- RB6: mechanism E at the slice + the ZF zero value by my own arithmetic ----
ok("RB6a slice: e^{-i om T} A -> cos(mu T) as om -> 0 (dressing = 1 there)           ",
   sp.simplify(sp.limit(sp.exp(-sp.I * om * T) * Asl, om, 0) - sp.cos(mu * T)) == 0)
rhostar = 2**sp.Rational(-5, 4)
Thstar = tauR * (1 - 16 * rhostar**4)
ok("RB6b ZF zero value re-derived: Theta(rho*) = pi/(2 sqrt2) ; weighted value = 1/2 ",
   sp.simplify(Thstar - sp.pi / (2 * sp.sqrt(2))) == 0
   and sp.simplify(gsum.subs(th, Thstar) - sp.Rational(1, 2)) == 0
   and sp.simplify(sp.cos(8 * sp.pi * rhostar**4)**2 - sp.Rational(1, 2)) == 0)

# ---- RB7: mechanism Z with the restored-content contingency parameterized ----
eps = sp.symbols('eps', real=True)
ok("RB7a annihilation: m_0 * E = 0_4 as matrices (the killed operand)                ",
   sp.expand(mom(0) * Efree) == sp.zeros(4, 4))
bear = eps * (sp.exp(sp.I * w0) - 1)
ok("RB7b contingency, parameterized: restored eps bears eps(e^{iw}-1) = -2 eps at w = pi",
   sp.simplify(bear.subs(w0, sp.pi) + 2 * eps) == 0)

# ---- RB8: COV at the GENERATOR level + the S2 source pin ----
Ls = sp.symbols('Ls', positive=True)
s3m = sp.Matrix([[1, 0], [0, -1]]); s1m = sp.Matrix([[0, 1], [1, 0]])
ok("RB8a generator identity: (om s3 + (mu/L) s1) L T = ((L om) s3 + mu s1) T         ",
   sp.expand((om * s3m + (mu / Ls) * s1m) * (Ls * T)
             - ((Ls * om) * s3m + mu * s1m) * T) == sp.zeros(2, 2))
Aslab = lambda MU, TT: sp.cos(sp.sqrt(om**2 + MU**2) * TT) \
    + sp.I * om * sp.sin(sp.sqrt(om**2 + MU**2) * TT) / sp.sqrt(om**2 + MU**2)
ok("RB8b closed-form pin re-verified: A_{mu/L, LT}(om) = A_{mu, T}(L om)             ",
   sp.simplify(sp.expand_trig(sp.expand(
       Aslab(mu / Ls, Ls * T)
       - (sp.cos(sp.sqrt((Ls * om)**2 + mu**2) * T)
          + sp.I * (Ls * om) * sp.sin(sp.sqrt((Ls * om)**2 + mu**2) * T)
          / sp.sqrt((Ls * om)**2 + mu**2))))) == 0)
Hm = sp.Matrix(2, 2, lambda i, j: sp.symbols(f'h_{i}{j}'))
Jm = sp.Matrix(2, 2, lambda i, j: sp.symbols(f'j_{i}{j}'))
aa = sp.symbols('aa')
ok("RB8c S2 STEP-5 pin: (H/L + a J)(L T) = (H + (L a) J) T (generic matrices)        ",
   sp.expand((Hm / Ls + aa * Jm) * (Ls * T) - (Hm + (Ls * aa) * Jm) * T) == sp.zeros(2, 2))

n_pass = sum(1 for _, c in results if c)
print(f"\n{n_pass}/{len(results)} PASS, {len(results) - n_pass} FAIL", flush=True)
```

Output, verbatim:

```text
RA1a C4 second-difference: sum_l w_l f(l) = -(1/4)[f(s2)-2f(0)+f(-s2)] (generic f): PASS
RA1b moments from the second difference: m_0 = m_1 = 0, m_2 = -1, m_3 = 0        : PASS
RA2a subset lattice: SUM_T prod_{j in T}(z_j - 1) = z1 z2 z3 (m = 3, all 8 T)    : PASS
RA2b '-1' selection: SUM_{T != empty} (-1)^|T| = (1-1)^m - 1 = -1, m = 1..12     : PASS
RA3a the 3 compressed factors are pairwise noncommuting (exact rational)        : PASS
RA3b operator-valued '-1' selection: [SUM_{T != empty}(-1)^|T|] K = -K (m = 3)   : PASS
RA4a 3-D ball parity: int_{-s}^{s} x e^{-x^2} dx = 0 for EVERY section s(y,z)    : PASS
RA4b n = 2, 3-D: EVERY off-diagonal product pair has an odd axis (56-pair scan)  : PASS
RA5a angular moments derived: <x^2> = rho^2/3 ; <x^4> = rho^4/5 ; <x^2y^2> = rho^4/15 ; <x^2y^2z^2> = rho^6/105: PASS
RA5b exact closed forms: d_000(r) = erf(r) - 2 r e^{-r^2}/sqrt(pi) ; d_111 exact : PASS
RA5c strict monotonicity: d'_000 = 4 pi^{-1/2} r^2 e^{-r^2} > 0 ; d'_111 = (32/105) pi^{-1/2} r^8 e^{-r^2} > 0: PASS
RA5d limits: d_000, d_111 -> 0 (r -> 0+) and -> 1 (r -> oo): eigenvalues in (0,1): PASS
RA6  diagonal family commutes: [D(r1), D(r2)] = 0 (n = 2 carrier structure)      : PASS
RA7a parity sectors of the n = 4 cube are exactly the mod-2 classes (4096-pair scan): PASS
RA7b all angular moments a,b,c <= 2 derived explicitly = (2a-1)!!(2b-1)!!(2c-1)!!/(2s+1)!! rho^{2s}: PASS
RA7c even-sector sanity: G8(r) symmetric (Hermitian compression); G8 -> I as r -> oo (64 limits): PASS
    [RA7 witness] [M(r1),M(r2)] entry (000),(020): first nonzero Taylor coefficient r1^3 r2^5 = 6/(5*pi)
RA7d NON-COMMUTATION at the sealed 3-D n = 4 member (full invariant sector): exact nonzero witness: PASS
RA8a orthonormality of the n = 4 axis family (all 10 pairs, exact)               : PASS
RA8b Fourier eigenproperty: hat(phi_k) = sqrt(2 pi) (-i)^k phi_k(k), k = 0..3    : PASS
RA8c super-polynomial kill: k^N e^{-k^2/2} -> 0 (k -> oo), N = 1..12             : PASS
RA8d dominated sum in closed form: SUM_n (lt)^n/n! = e^{lt} (exact summation)    : PASS
RA9a frequency cancellation: i om int_0^t e^{i om s} ds = e^{i om t} - 1 (om > 0, om < 0, om = 0): PASS
RA9b band resolution: P_s'(k')(al.(k+k'))S P_s(k) = (s'|k'|-s|k|)P_s'P_{-s}S (4 sign pairs): PASS
RA10a weight sum re-derived: SUM w_l cos(l th) = (1 - cos(sqrt2 th))/2 = c(th)   : PASS
RA10b ledgers: display (g-1)+(-g) = -1 ; rung 2a m_0 e^{i w} - 1 = -1 ; rung 2b 0-0 = 0: PASS
RA11a S spectral: eigenvalues exactly {+1 (x2), -1 (x2)} (explicit S)            : PASS
RA11b spectral exponential: e^{-i th S} = cos(th) I - i sin(th) S (exact)        : PASS
RA12a Theta re-derived branchwise: int v(t) 1_{|x|<=r(t)} dt = tau_R(1 - 16 x^4) : PASS
RA12b inner element in closed form: <phi1|1_[-u,u]|phi1> = erf(u) - 2 u e^{-u^2}/sqrt(pi): PASS
RA12c change of variables verified by FTC: d/dt[-A(1-t)] = f(1-t) exactly        : PASS
RA12d intertwining at the phi_1 element: int v <phi1|1_{r(t)}|phi1> dt = <phi1|Theta|phi1>: PASS
RB1a Clifford square: (u.alpha)^2 = |u|^2 I (explicit matrices)                  : PASS
RB1b spectral reconstruction: e^{-iw}P_+ + e^{+iw}P_- = cos w I - i sin w (u.al) : PASS
RB1c unitarity both orders: E E^dag = E^dag E = I on the unit sphere             : PASS
RB1d [E, P_s] = 0: the dressing commutes with its own band projectors            : PASS
RB1e two-band pairing: det(e^{-i h_0}) = 1 EXACTLY at the symbol point           : PASS
RB1f unit evaluation (mechanism E, operator level): E|_{w=0} = I_4               : PASS
RB2a mechanism L: SUM w_l E Om_l = E SUM w_l Om_l with the ACTUAL E              : PASS
RB2b R3C difference shape: E Om_0 - E Om_pm = E (Om_0 - Om_pm) (factor exits)    : PASS
RB3a factorization: (UX)^H(UX) - X^H X = X^H (U^H U - I) X (generic complex 2x2) : PASS
RB3b mechanism U closed: E^dag E - I = 0 (RB1c) => (EX)^dag(EX) = X^dag X, any X : PASS
RB4a factorization: (U Om_mu)^H (U Om_lam) - Om_mu^H Om_lam = Om_mu^H (U^H U - I) Om_lam: PASS
RB4b with RB1c: (E Om_mu)^dag (E Om_lam) = Om_mu^dag Om_lam BEFORE any det/tr    : PASS
RB4c trace boundary witness: tr(E) = 4 cos w; at w = pi equals -4 != 4           : PASS
RB4d affine boundary witness: det(1 + (e^{-iw} - 1)) = e^{-iw}, = -1 != 1 at w = pi: PASS
RB5a modulus blindness, general: |e^{i th} z|^2 = |z|^2 (symbolic complex z)     : PASS
RB5b unitarity chain: |A|^2 + |B|^2 = 1 and Om^2(1 - |B|^2) = om^2 + mu^2 cos^2(Om T): PASS
RB6a slice: e^{-i om T} A -> cos(mu T) as om -> 0 (dressing = 1 there)           : PASS
RB6b ZF zero value re-derived: Theta(rho*) = pi/(2 sqrt2) ; weighted value = 1/2 : PASS
RB7a annihilation: m_0 * E = 0_4 as matrices (the killed operand)                : PASS
RB7b contingency, parameterized: restored eps bears eps(e^{iw}-1) = -2 eps at w = pi: PASS
RB8a generator identity: (om s3 + (mu/L) s1) L T = ((L om) s3 + mu s1) T         : PASS
RB8b closed-form pin re-verified: A_{mu/L, LT}(om) = A_{mu, T}(L om)             : PASS
RB8c S2 STEP-5 pin: (H/L + a J)(L T) = (H + (L a) J) T (generic matrices)        : PASS

54/54 PASS, 0 FAIL
```

---

## 3. TASK B — RESIDUE r-c: ADJUDICATION OF THE NEUTRALITY CERTIFICATION

### 3.1 My own enumeration of the consuming corpus (protocol + result)

Protocol, run independently of the build's list: (a) grep the ENTIRE
workspace for artifacts naming the R2 digest prefix a7969f0c; (b) grep for
the factor tokens ("e^{-i h_0}", "free factor", "free phase") and for the
discharged-chain digest prefixes (2e4e5163, 26f9314b, df4514a5, a903716c,
e5b0879b, 57edbb96, 0a10c030) to close the transitive consumer sweep; (c)
read every hit at its bytes.

RESULT: (a) yields exactly the build's §3.3-§3.6 corpus: R2 (+check), REM
(+check), ZF (+check), G1 (+check), RL4/RL0 (+check), S2 (+audit), R3C
(+audit), plus the build itself; AQ (+check) arrives transitively and is
free-factor-SILENT (zero occurrences of "free"/"h_0" in AQ; only
"exponentiation-free" in AQ-CHK — different sense). (b) additionally yields:

```text
FINDING C-1 (enumeration closure): FOUR sealed consumer artifacts (plus
their four checks) consume discharged-chain digests, EXISTED AT BUILD TIME
(mtimes §1: Aug 13 17:23 - Aug 14 07:11 vs build Aug 14 07:44), and are
ABSENT from the build's §3.8 closure sentence ("every artifact naming
a7969f0c, plus the transitive consumers AQ/G1/ZF/RL4/S2/R3C found through
the REM chain, plus both checks of each"):
   STAGE8_CROSS_BLOCK_READ_V001.md (+ CHECK)    — flag-block assembly read
   STAGE8_RUNS_TARGET_POSED_V001.md (+ CHECK)   — branch poser, declares nothing
   STAGE8_SUMMED_S2_CANDIDATE_V001.md (+ CHECK) — the summed amendment (DEBT 3 act)
   STAGE8_P1_DILATION_CLOSURE_V001.md (+ AUDIT) — the dilation-closure question
EACH VERIFIED FREE-FACTOR-SILENT BY THIS AUDIT: zero occurrences of
"e^{-i h_0}" / "free factor" / "free phase" / "dressing" / "unimodular" /
"modulus-1" / "band-diagonal" in all eight files (token sweep; P1's "h_0"
hits concern the free GENERATOR Q_n h_0 Q_n — carrier-closure content, not
the carried factor; RUNS's "phase" hits are the RECORD phases theta).
CONSEQUENCE: no free-factor-bearing comparison was missed — the
certification's quantifier ("every consuming comparison of record")
SURVIVES; the enumeration-closure SENTENCE was incomplete and is corrected
here. (Also swept and disposed: COMPLETE_QSPEC_CANONICAL_SPIN2... ,
COMPLETE_QSPEC_GAUSSIAN_CELL_REDUCTION... , STAGE8_TASK4B_DIVERGENCE...
use "free factor" in unrelated senses and consume no R2 content.)
```

### 3.2 Per-comparison re-derivation (my mechanisms; RB labels from §2.6)

Scope pinned at bytes as the build pinned it: r-c is the OVERALL common left
factor (R2 :110-116, REM :128-130); interior free phases are the counted
strata (REM-CHK :202-207) — the boundary is real at bytes and honored below.

```text
R2-1 frame factorization (:110-116): factor exits by linearity — RB2a with
     the ACTUAL matrix factor (build used a generic F). NEUTRAL. CONFIRMED.
R2-2/R2-3 stripped-object comparisons and domination (:134-184): band phase
     modulus 1 — RB1b-d (spectral-reconstruction route) + RB5a (general
     modulus blindness on a symbolic complex number). NEUTRAL. CONFIRMED.
R2-4 degree-0 readings (d4 :337-340, :271-284): annihilation — RB7a
     (m_0 E = 0 as matrices), with the contingency parameterized (RB7b:
     restored content eps would bear eps(e^{iw} - 1)). NEUTRAL BY
     ANNIHILATION, kill-contingent exactly as F-1 states. CONFIRMED.
R2-5 unitarity self-test (§4): modulus statements — RB5a/RB5b (floor
     re-DERIVED from |A|^2 + |B|^2 = 1, a different derivation order than
     the build's direct expansion). NEUTRAL. CONFIRMED.
R2-6 omega = 0 slice identifications (:344-371): unit evaluation — RB6a
     (limit route) + RB1f (OPERATOR-level E|_{w=0} = I_4, which the build
     verified only on the slab scalar). NEUTRAL. CONFIRMED.
R2-7 survivor assembly (:396-424): endpoint dressings band-diagonal
     modulus-1 with annihilated content (RB1b-d + RB7a); interior W phases
     out of scope (counted strata). NEUTRAL AS CLAIMED. CONFIRMED.
R2-8 V_2 subsidiary (:373-389): "(times the free phase)" retained in the
     display; degree grading is a modulus statement (RB1b/RB5a). NEUTRAL
     FOR THE GRADING. CONFIRMED.
REM-1..4 + REM-CHK (:108-135, :277-311, :355-383, :405-441): inherited
     kills (RB7a), moduli (RB5), and the peel-at-operator-norm step — my
     route: the FACTORIZATION identity (UX)^H(UX) - X^H X =
     X^H(U^H U - I)X (RB3a, generic complex) + E^dag E = I BOTH orders
     (RB1c) closes every Schatten/HS/operator-norm statement at once
     (RB3b) — the build verified an instance (real rotation, transpose);
     mine is the complex general case. NEUTRAL IN EVERY NORM. CONFIRMED.
ZF-1 modulus identity + floor (:154-181): re-DERIVED from the unitarity
     chain (RB5b). NEUTRAL. CONFIRMED.
ZF-2 omega = 0 zero location (:183-226): the value chain re-derived by my
     own arithmetic (RB6b: Theta(rho*) = pi/(2 sqrt2), weighted value 1/2
     by BOTH the weight-sum and the g-closed-form routes). NEUTRAL
     (mechanism E). CONFIRMED.
ZF-3 rate region (:228-245): consumes REM-2; moduli (RB5). NEUTRAL.
     CONFIRMED.
ZF-4 Z6c (:259-263): annihilation verbatim at bytes (RB7a). NEUTRAL.
     CONFIRMED.
ZF-5 pair determinants (:267-277, :332-339) + E1 :743-762 pair displays +
     B-L1 trace object (E1 :1106-1108): the pair kill by the SAME
     factorization route (RB4a) + RB1c: the factor cancels inside
     V_{mu lambda} = Omega_mu^dag Omega_lambda BEFORE any nonlinearity.
     My census (§3.3) re-ran the det/trace sweep. NEUTRAL. CONFIRMED
     (with the F-2 wording correction C-2 below).
G1   (:255-275, :328-347): citational; carries (r-b)/(r-c) OPEN; no
     free-factor-bearing comparison of its own (token sweep). VACUOUS-
     NEUTRAL. CONFIRMED.
RL4/RL0 (:160-175, :415-419, :657-661): weight arithmetic (factor-free)
     and the sup|lambda|Theta = pi modulus pin. NEUTRAL. CONFIRMED.
S2   (:177-248): the ONE non-trivial consumer — exact COVARIANCE, not
     neutrality: re-verified at the GENERATOR level (RB8a — a route the
     build did not use), at the closed form (RB8b), and at the source pin
     (RB8c = S2's [S2f]); the norm step is RB3/RB1c. The alpha' = 1/4
     extraction consumes the factor correctly at every step. NO FINDING.
     CONFIRMED.
R3C  (:297-320, RA7 block): differences — the factor exits (RB2b);
     parity split on stripped amplitudes (RB5); retained bare degree-0
     content is dressed band-diagonally, no degree/norm/parity change
     (RB1b-d). NEUTRAL. CONFIRMED.
AQ (+check): free-factor-silent (token sweep). VACUOUS-NEUTRAL. CONFIRMED.
THE FOUR C-1 FILES (+ checks): free-factor-silent (token sweep, §3.1).
     VACUOUS-NEUTRAL — enumerated HERE, closing the corpus as of THIS
     audit's session bytes.
```

### 3.3 The contingency findings F-1/F-2, audited; my det/trace census; finding C-2

F-1 (annihilation, not cancellation): ENDORSED — my RB7a/RB7b re-derive it
with the restored content parameterized (eps(e^{iw} - 1) is the exact
content the factor would bear were any killed content restored). The sealed
claim is TRUE as stated and its kill-contingency is real.

MY OWN det/trace census (independent sweep of every det/tr token in the
consuming corpus): every determinant of record is either (i) a CTP-pair
determinant det(1 + A_{mu lambda}(a)), A = C(V - 1)C with V =
Omega_mu^dag Omega_lambda a pair object — factor cancelled by RB4 BEFORE
the nonlinearity; or (ii) the baseline det_{ran C}(1 - 2CPC) / det_n(0) =
prod(1 - 2 s_i) family (E1 R-L4a/b, RL4) — built from C and P ONLY: the
free factor never enters at all (vacuously neutral). Every trace of record
is pair-based (B-L1's tr[C(V(a) - V(0))C]; S2's tr[Delta], tr[(1+A)^{-1} A
Delta]) or a norm bound (|tr[BXY]| <= ||B|| ||X||_2 ||Y||_2 — mechanism
U/M). NO single-branch dressed trace or determinant exists of record: F-2's
discharge is CONFIRMED by my own enumeration.

```text
FINDING C-2 (wording of F-2, correction): F-2's first sentence — "A trace
or determinant of a SINGLE dressed branch would NOT be phase-neutral" — is
imprecise on the determinant half. EXACTLY: (i) the TRACE half is TRUE
(tr(E) = 4 cos w != 4; RB4c = build's QB7c); (ii) a PLAIN single-branch
determinant IS neutral: det(e^{-i h_0}) = 1 EXACTLY at the symbol point
(RB1e — the two band phases e^{-iw}, e^{+iw} pair off, each twice), so
det(E Omega) = det(Omega); (iii) the determinant reading that is NOT
neutral is the AFFINE / Fredholm-shifted one, det(1 + (dressed - 1))
(witness RB4d: 1-D instance = e^{-iw} != 1 as a function). The boundary
F-2 draws is REAL and its discharge stands (no object of record sits on
either side of the imprecision — census above); the sentence needed the
affine qualifier. Correction to wording only; RC_STATUS's content is
unaffected.
```

### 3.4 Task B verdict

**TASK B = CONFIRMED-WITH-CORRECTIONS**, the corrections being exactly C-1
(the §3.8 enumeration-closure sentence missed four sealed consumer
artifacts + their checks, all verified free-factor-silent here — the
neutrality quantifier survives; the corpus is closed as of THIS audit's
session bytes by §3.1's sweep) and C-2 (F-2's determinant wording needs the
affine qualifier; exact witnesses RB1e/RB4c/RB4d; no object of record
affected). Every mechanism assignment (L/U/B/M/E/A/Z/COV) re-derived by an
independent route where one existed (spectral reconstruction for B;
factorization for U and A; unitarity-chain for M's floor; operator-level
unit evaluation for E; generator-level identity for COV); the certification
is positive and stands at its stated quantifier over the corrected corpus.

---

## 4. V-WATCH, BEARING, PROVENANCE, INJECTION, FENCES

### 4.1 The adopted summed amendment's falsifiers V-1..V-6, SEVERALLY

Read at SUMMED_S2_CANDIDATE :417-444 and its check :348-358 (bytes at path):

```text
V-1 (refutation at the amendment's own quantifier VOIDS): NOT ENGAGED —
    neither the build nor this audit produces any refutation of the summed
    estimate; both residues resolve in the carried direction.
V-2 (one-way bridge shown two-way VOIDS): NOT ENGAGED — no bridge
    direction is touched by either artifact.
V-3 (anchor overturn VOIDS): NOT ENGAGED — REM §5's overturn conditions
    are untouched; ZF's scaffolding is not overturned but RE-CONFIRMED
    (RB6b re-derives the zero-value chain exactly).
V-4 (sealed-bytes contradiction VOIDS): NOT ENGAGED — every byte-reading
    of the build was re-read at path by this audit and found as cited
    (zero span mismatches, §1); this audit exhibits no contradiction.
V-5 (C4-kill failure VOIDS): NOT ENGAGED — the kill is re-verified TWICE
    here by independent routes (RA1a/RA1b via the second-difference
    display; RB7a at operator level).
V-6 (marking fraud VOIDS): NOT ENGAGED — the build's markings audited
    honest: its one CLAIMED (generic) aside was marked exactly so (and is
    now upgraded to derived by RA7d); no DERIVED-marked claim was found
    weaker than its marking; this audit's own claims are marked at grade
    in place.
```

### 4.2 Bearing (each stated explicitly)

```text
DISCHARGED B-L2* CHAIN (2e4e5163 + 89641f76; G1 df4514a5): NOTHING MOVES.
  Both residue determinations, and this audit of them, bear ONLY in the
  confirming direction at the chain's own quantifiers: (r-b) resolves
  stable (no refinement effect reaches the level of record — re-derived
  §2), (r-c) resolves neutral (per comparison — re-derived §3). The
  build's DISCHARGED_CHAIN_BEARING = CONFIRMING-ONLY flag is ENDORSED.
ADOPTED SUMMED AMENDMENT: V-1..V-6 severally NOT ENGAGED (§4.1).
alpha' = 1/4 RECORD (e5b0879b + cbe6ff4f): NOTHING MOVES — its free-phase
  step is exact covariance re-verified at the generator level (RB8a), the
  closed form (RB8b), the source pin (RB8c), and its norm step by
  RB3/RB1c; its Q-bearing is S2's own named dilation-closure premise,
  which belongs to the P1 chain, not to r-b/r-c.
Q-1050 RESIDUE STATEMENT (P1-AUDIT :533-540, read at bytes): UNMOVED —
  Q^Sigma_1 remains uncertified; the witness stands; neither the build
  nor this audit touches the ell-ray quantifier width or performs any
  registrar act.
Q-1051: NOT LOCATABLE — no artifact in the workspace corpus contains the
  token "Q-1051" (full grep, this session). Named in this audit's tasking
  only. Nothing at path exists to be moved; flagged as a tasking-corpus
  mismatch, NOT defaulted and NOT treated as verified.
PER-PAIR RECORD (52f2490b refutation), M(t)-SHARP, C6-FALSE, all sealed
  V011 flags: UNMOVED by build and audit alike.
```

### 4.3 Provenance

All digests recomputed at path this session (§1): the artifact under audit
(786dc611... MATCH), all 23 build-listed grounds (MATCH build §1 AND
sidecars, 23/23 independent sidecar comparisons), the 8 C-1 files pinned
with mtimes. Every span the build cited was re-read at its location and
found as cited — zero mismatches (span list in §1). The build's residue
statements match their sealed sources verbatim ((r-b) R2 :459-460 / flag
:837-838; (r-c) R2 :461-462 / REM :514-519 / G1 :266-275). The build's
"SEALS_VERIFIED = 23/23" claim is TRUE as stated.

### 4.4 Injection scan

The full build file was read start to finish. Non-ASCII content:
typographic only (em-dashes, section signs, arrows) — no hidden or encoded
directives. Directive-content scan: NO instruction addressed to registrars
or downstream processes; the build's §4 is explicitly descriptive ("nothing
here flips a flag, moves a debt, or creates a target") and its flags are
conditional ("IF checked"). ONE auditor-addressed sentence exists — F-3:
"Any future audit that meets a free phase INSIDE a stratum should charge it
to the strata bookkeeping, not to r-c" — assessed: a restatement of the
sealed scope boundary (REM-CHK :202-207) in advisory form; it claims no
authority, directs no act, and matches the bytes it cites; this audit
independently verified the boundary and adopts it on the MERITS, not on the
instruction. The build claims no act beyond its authority, and none was
found: no flag flipped, no file edited (all input digests still match their
pre-existing sidecars), gates all false, output-name probe honored.

### 4.5 Fences

```text
BUILD'S FENCES: HELD, verified — exact-symbolic-only battery (read in
  full; no numeric evaluation present; its one disclosed development
  iteration is sympy mechanics); M(t) never softened (readings only);
  eq-object only as the omega = 0 slice; carriers only the sealed family;
  no witness action; no new file beyond its output; no git.
AUDIT'S FENCES: HELD — certification battery exact-symbolic only (54/54,
  §2.6); no physical quantity computed or bounded; alpha untouched; M(t)
  read sharp; C6 respected; carriers only the sealed HG family (ell =
  sqrt2 by exact dilation remark, no new datum); no register/tracker/
  plan/road/ledger/lens file read; no git; no existing file edited; ONE
  file written (this one) + the tasked seal sidecar; output name probed
  ABSENT before write.
DEVELOPMENT ITERATIONS, disclosed exactly (none affects the certified
  battery, which is the single §2.6 script run end-to-end):
  (1) RA9a first ran as a FAIL on a sympy Piecewise artifact (the
      integrate result carries an omega = 0 case); fixed by evaluating
      the omega > 0 / omega < 0 / omega = 0 branches explicitly — the
      identity itself was never in doubt and the fix is mechanics only.
  (2) RA12b/RA12d first ran with a substituted integrand sympy could not
      close (unevaluated Integral objects). During diagnosis I performed
      ONE numeric evaluation (sp.N of the difference of my own two test
      expressions) to distinguish my setup error from CAS mechanics —
      on MY OWN intermediate expression, involving no program quantity
      and certifying nothing; the certified route replaces it entirely
      with the FTC-verified change of variables (RA12c) and is wholly
      exact.
  (3) RA7's first draft compressed onto a 4-dimensional corner of the
      even sector that is NOT an invariant subspace (a methodological
      error of MINE, caught in-audit before any use); the certified RA7
      computes on the full 8-dimensional invariant parity sector, with
      invariance itself established first (RA7a). The corner draft's
      output is NOT of record; the certified witness is 6/(5 pi).
```

---

## 5. FLAG BLOCK

```text
RB_VERDICT = CONFIRMED(every step of the Q-resolution determination
  re-derived by routes the build did not use — second-difference stencil,
  3-symbol subset lattice, three-factor noncommuting kernel-blindness,
  3-D domain-symmetry parity, eigenvalue-in-(0,1) non-idempotence at the
  actual carrier, Gaussian-envelope smoothing kill, spectral-route
  eq-object, matrix-level D-kernel identities, FTC-verified intertwining —
  with every claimed break re-witnessed as real and the one CLAIMED aside
  UPGRADED to a derived exact witness at the sealed 3-D n = 4 member
  ([M(r1),M(r2)] Taylor coefficient 6/(5 pi) at r1^3 r2^5 on the invariant
  even sector, RA7d); no refinement effect reaches the level of record;
  quantifiers exactly as stated, per fixed sealed carrier.)

RC_VERDICT = CONFIRMED-WITH-CORRECTIONS(the neutrality certification holds
  at its stated quantifier over the corrected corpus, every mechanism
  re-derived independently (RB1-RB8) and the det/trace census re-run; the
  two corrections named exactly: C-1 the §3.8 enumeration-closure sentence
  missed four sealed consumer artifacts plus their checks existing at
  build time — CROSS-BLOCK-READ, RUNS-POSE, SUMMED-S2-CANDIDATE,
  P1-DILATION-CLOSURE — each verified free-factor-silent here, so no
  comparison was missed and the quantifier survives; C-2 finding F-2's
  "trace or determinant" sentence is imprecise on its determinant half —
  det(e^{-i h_0}) = 1 exactly (RB1e), the non-neutral determinant reading
  is the affine det(1 + (dressed - 1)) (RB4d) — wording only, no object of
  record on either side.)

V1_STATUS = NOT-ENGAGED(no refutation of the summed estimate at its own
  quantifier is produced by the build or by this audit — every finding is
  confirming or wording-level; V-2..V-6 likewise severally not engaged,
  §4.1.)

NET_VERDICT = BOTH-DETERMINATIONS-STAND(Task A CONFIRMED with an upgrade
  delivered; Task B CONFIRMED-WITH-CORRECTIONS C-1/C-2, neither touching
  the certification's truth at its quantifier; the build's NET flag — the
  FIXED-completion list reducing to (r-d) plus the standing debts, with
  per-pair, M(t)-sharp, C6-FALSE, and every sealed flag unmoved — is
  endorsed as flagged, still CLAIMED-until-checked at the program's own
  discipline; nothing found bears against the discharged B-L2* chain, the
  adopted amendment, the alpha' = 1/4 record, or Q-1050's residue
  statement, and Q-1051 is flagged unlocatable at path rather than
  defaulted.)

PROVENANCE = VERIFIED-IN-FULL(786dc611 under audit matches at path; 23/23
  build-listed digests recomputed and matching build §1 and sidecars; the
  five tasked prefixes match; 8 additional consumer files pinned with
  digests and mtimes; every cited span re-read at its location and found
  as cited, zero mismatches; Q-1051 alone unlocatable, flagged not
  defaulted.)

INJECTION = NONE-FOUND(no directive content to auditors/registrars/
  downstream beyond F-3's advisory scope sentence, which restates the
  sealed boundary REM-CHK :202-207, claims no authority, and was adopted
  here on independently-verified merits; non-ASCII typographic only; the
  build performed no act beyond its authority — no flag flipped, no file
  edited, gates false, output-name probe honored.)

FENCES = HELD-BOTH-SIDES(build's fences verified held; audit's fences held
  — exact-symbolic certification battery 54/54 in fresh venv rbrcauditvenv,
  no numeric certification of anything, no physical quantity, alpha
  untouched, M(t) sharp, C6 respected, no carrier datum introduced, no
  register/tracker/plan/road/ledger/lens read, no git, no existing file
  edited, one output file plus tasked seal sidecar; three development
  iterations disclosed exactly in §4.5, including one out-of-battery
  numeric diagnostic on the auditor's own test expression, certifying
  nothing.)

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until consumed by a successor process.
RB_RC_RESIDUES_AUDIT_RESULT = SEALED.
```
