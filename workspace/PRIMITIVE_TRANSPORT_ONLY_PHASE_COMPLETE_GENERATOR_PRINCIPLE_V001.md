# Primitive Transport-Only Phase-Complete Generator Principle v001

## Logical status

This is a new Level-1 Gravacle principle, adopted before any finite response
or coupling is evaluated. It is not claimed as a theorem of the older
recoverability results.

It operationalizes the prior physical statement that the charged boundary
handle is compact action-phase transport. It contains no measured alpha,
endpoint radius, fitted coefficient, or historical alpha formula.

## Primitive-versus-composite rule

At the primitive pre-record level, the compact charged handle enters the
microscopic generator only through unit-character parallel transport.
Curvature is the failure of composed transports to close; it is not an
independent primitive vertex.

For the frozen vectorlike source branch, the charged dependence is therefore

```text
D_A psi
  = gamma^mu (nabla_mu + i A_mu) psi,

m_bare = 0,
q_primitive = 1.
```

For a primitive record comparator transported on a path `x(tau)`,

```text
D_tau z
  = [d/dtau + i Q dot(x)^mu A_mu] z,

Q = |1><1|.
```

The complete microscopic source-plus-record generator must be a unitary,
phase-complete dilation. Open-system channels and durable record instruments
are obtained only after tracing or conditioning downstream degrees.

## Excluded primitive mutations

The following are not independent terms in the primitive generator:

```text
c[A] I                       gauge-history-dependent scalar phases;
c_F integral F^2             a bare Maxwell action;
ell_* bar(psi) sigma.F psi   a primitive Pauli insertion;
higher-covariant-derivative charged vertices;
independent Lindblad/dephasing rates;
an independently normalized record-curvature term.
```

Such structures may arise as calculated composite terms of the exact closed
transport dynamics. They may not be inserted as microscopic inputs or chosen
after inspecting a response.

Gauge-independent constant phases remain physically irrelevant.

## Why this is a substantive principle

Gauge and Lorentz covariance alone do not imply this rule. The rule chooses a
physical hierarchy:

```text
primitive charged access = transport;
closed-loop curvature and kinetic response = composite output.
```

It therefore removes the explicit countermodels in
`COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md` only inside this
adopted branch. Calling the choice "minimal" would not have been enough; the
transport-only content is the declared microscopic physics.

## Independent consequences

Before radiative or record-composite corrections, the source has:

```text
minimal unit charge;
no intrinsic Pauli term;
no intrinsic electric dipole term;
the tree-level Dirac magnetic structure associated with minimal coupling.
```

These are consequences of the adopted generator class and may serve as
non-alpha checks. They are not evidence that its absolute induced response is
already correct.

## Remaining obligations

The principle fixes the allowed charged-vertex class, but it does not yet
derive:

1. the complete finite causal record cell and its stitching rule;
2. the source-record interaction that generates a public mass;
3. the state, CTP contour, boundary/edge data, and measure;
4. the exact finite electric and magnetic response;
5. the physical connection as a public dynamical field;
6. the Lorentzian retarded continuation and Thomson matching; or
7. a pristine unused quantitative prediction.

No response may be evaluated until those missing parts of `Q_spec` are
sealed.

## Status

```text
transport_only_phase_complete_principle_adopted = true
principle_derived_from_older_record_theorems = false
primitive_F_dependent_identity_phase_allowed = false
primitive_Pauli_vertex_allowed = false
primitive_dissipative_rate_allowed = false
minimal_unit_character_vertex_fixed = true
complete_finite_Q_spec_derived = false
finite_response_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
