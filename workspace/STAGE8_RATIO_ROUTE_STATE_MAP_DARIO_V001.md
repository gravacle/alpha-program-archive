# STAGE 8 / [PLAN:F1-ADVANCE] — THE RATIO-ROUTE STATE MAP: FIVE STRATA, ZERO EDGES, AND A MISSING POWER THAT WAS NEVER LOST

Lane: DARIO (Builder B, independent verifier). Relay 782. **MAP ONLY — no estimate attempted.**
State brief pinned by the relay: `PROGRAM_STATE_BRIEF_V003.md` = `63549f639b751247…`, digest
**matches the relay's stated pin**, seal OK.
All headline statements enter as **CLAIMED** (alignment law 3).

## Lead determination — CLAIMED

**Three of the relay's own premises do not survive contact with the record, and the fourth
resolves in a direction that reframes the whole question.**

```text
1. "FOUR STRATA"        The record REFUSES four and re-enumerates as FIVE, the same night,
                        adversarially confirmed and SEALED.  The only sealed enumeration in
                        the glob is titled "One Route Class, Five Strata."
2. "TEN CROSSINGS"      The COUNT is displayed in exactly two files.  NEITHER ENUMERATES THEM.
                        The enumeration is ABSENT from the lawful glob.
3. "R-L2b ... its known  Under S13 (verified byte-exact) the sealed relation is ONE-WAY:
   gap: arm 2 short      "arm 2 may consume R-L2b, never the reverse."  Arm 2's gap is the
   one power"            CONSUMER's gap.  Attributing it to R-L2b is the declined identification.
4. "the ratio route      SEALED Q-34, a PRINCIPAL ACT: "ALPHA'S STIFFNESS IS K_*", and the
   ... to a lawful       CURRENT plan states "The R-L2b/linked-cluster campaign is OFF alpha's
   kappa_record"         value path.  The kappa-only chain is short but is NOT a shortcut."
```

**The missing power is the sharpest finding, and it is not what "short one power" sounds like.**
The power is **not lost in a computation**. The integration by parts that would deliver it **has
never been performed anywhere in the corpus**, its integrand is never written, and the object it
would act on — *"the smooth cell profiles"* — **has no sealed referent at all** (two occurrences,
no definition, three candidate objects, all disqualified). The record does not have a bound that
falls one power short; it has **an unwritten computation over an undefined object**.

**Classification: `UNDECIDABLE_FROM_STOCK`.** The object kind the record itself names is **a
profile / transversality / projection-tail LEMMA with three uncertified components** — explicitly
**not** a sharper bound and **not** a different decomposition. All three named alternative
suppliers are dead of record.

**And four custody findings the map surfaced, reported because nobody else is positioned to see
them.** One is a genuine seal failure; one supersedes a check in my own last artifact.

---

## 0. Preflight, searched space, and custody

### 0.1 Output name
Probed recursively before any write: artifact and sidecar **ABSENT**.

### 0.2 SEARCHED SPACE — state the glob

```text
GLOB:  ./workspace/**/*.{md,json}  +  ./supervision/**/*.{md,json}
FILES SEARCHED: 3,003
EXCLUDED BY LAW: evaluator_build_A/ and checks/ (Builder-A code, off limits) — excluded from
   every result below.  memory-bank NEVER searched.  ~/.codex untouched.
DISCLOSURE: one grep enumerated a PATH under evaluator_build_A/inputs/evidence/.  A filename
   was listed; NO file under that tree was opened.  The tree is excluded from all findings.
SUB-CORPUS: files bearing `R-L2b` or `A-L0` = 121, of which 86 carry a verifying sidecar.
   Seal grade is reported per item below; UNSEALED sources are marked and never upgraded.
CONTROLS (case-sensitive, all non-zero): R-L2b 111 files, A-L0 47, A32 169,
   cluster.summab 7, "ratio route" 23, S13 25.
```

### 0.3 Four custody findings

**(i) A SEAL GENUINELY FAILS.**

```text
supervision/EXECUTION_TRACKER.md
   sidecar says : f42addcc9303d1f4785f4adef7412143321d7ab24d0628fb9b2dfae8ea403fae
   file is      : ad0740f9ac9880d9b531fae408e727530831a2d7d0b234703f171ca98ea2c2f8
   shasum -c run FROM THE FILE'S OWN DIRECTORY (the 742 lesson) : FAILED
   mtimes identical (Aug 9 08:52:28) => the tracker was written AFTER its own seal was computed.
```
Consistent with a live registrar file, but **a sidecar that does not verify is a custody break
whatever the cause.** I do not touch trackers; reported only.

**(ii) MY OWN 781 FLATTENING CHECK IS AGAINST A SUPERSEDED REGISTER.**
`DECLINE_REGISTER_V002.md` (`957476c8c605a370…`, sealed OK, 15,523 B) states: *"V002 supersedes
V001 … FLATTENING CHECK (LOCKED_PROCESS law 1) runs against THIS table"* — S01–S37 from sweep 779.
At my 781 preflight, `find` returned **V001 only**, so the check was correct when run. It is not
correct now. **781's `FLATTENING_CHECK = clean` must be re-run against V002 by whoever registers
it.** (My `+1` decline is S01 there, sourced to the same 768 span I verified.)

**(iii) LIVE-FILE RACES DURING A SINGLE RELAY.** `PROGRAM_STATE_BRIEF` moved **V003 → V005** and
`THE_PLAN` moved **V001 → V003** while this relay ran; V005's own pointer names plan **V002**,
already stale. I complied with the relay's explicit pin (V003, digest matched) and **read V005 and
plan V003 as well**, disclosing both rather than silently substituting either. **Alignment law 4's
"current version" is not well-defined under a live registrar** — a relay's pin and the current
brief can differ by two versions before the lane finishes.

**(iv) A STRATUM'S BUILT ARTIFACT LIVES OUTSIDE THE ARCHIVE.** Stratum S1/S2's product is pinned
by path+hash into a parent tree (`…/gravity_emergence_evidence_program/reports/`), is **ABSENT
from the lawful glob**, and the record itself states its hash *"is pinned NOWHERE"* with *"No
`.seal.sha256` sidecars … in the parent tree for this family."* The most-executed stratum is the
least custodied.

### 0.4 Gates and law compliance

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
NO ESTIMATE ATTEMPTED (map only).  No member binding; no fixed-point execution; no end test;
no numeric evaluation of physical quantities; no comparison to measured constants.
PE-1..PE-6: recorded, NOT consulted, NOT opened.
Law 5(a) register grep run before task work (QUESTIONS_SETTLED_REGISTER_V001, 1,391,345 B).
No register, plan, tracker, git action.
```

---

## 1. AS1 — THE STRATA: **FIVE**, NOT FOUR

### 1.1 Why the count is five

| | |
|---|---|
| origin of "four" | `supervision/CONTINUATION_STATE.md` `[95981,96176)` — a **2026-07-28 NIGHT** session header, SEALED-OK. **CLAIMED**, a log header, not a register row. |
| the only four-enumeration | `supervision/BOHM_CTP_ABSOLUTE_RESPONSE_ROUTE_SWEEP_2026-07-28.md` `[20970,21816)` — **NO SIDECAR**. And its own next section files "connect the four strata formally" as a **TO-DO**, not a structure. |
| the upgrade | `CONTINUATION_STATE.md` `[98300,99229)` — **2026-07-28 LATE NIGHT**, ~26 lines later: *"FIVE strata now, not four"*. **CLAIMED then adversarially CONFIRMED** (12 agents, "every decisive claim adversarially verified, none refuted"). |
| the re-audit | `BOHM_REPLAN_AUDIT_RESULTS_2026-07-29.md`: *"The only later refinement is four strata → FIVE … an addition, not a negation."* |
| **the sealed enumeration** | **`workspace/STAGE8_GRAVITY_EVIDENCE_ADMISSION_AND_FIVE_STRATA_BINDING_V001.md`** = `bad9795072739e35…`, **SEALED-OK** — the ONLY sealed artifact in the glob that enumerates them. Title: **"One Route Class, Five Strata."** |

**The binding is a bookkeeping act, not a result.** Its own scope block displays
`five_strata_bound_as_one_route_class = true`, `routes_merged = false`, `routes_ranked = false`,
`x_computed = false`, `rho_computed = false`, `T_R_computed = false`, `alpha_computed = false`,
`proof_authorized = false`, and the clause *"They are not merged, ranked, or promoted."*

### 1.2 The five, located

| # | stratum | built where | seal | claim vs proved | interface to next (quoted requirement) |
|---|---|---|---|---|---|
| **S1/S2** | BR induced coefficient functions ("forced ratios executed" + "one-operator-both-couplings, depth open") | product in the **parent tree**, `f7caa5a2…` | **binding SEALED-OK; product ABSENT from glob, hash pinned NOWHERE, no sidecars** | CLAIMS ratios `K_H=(3/2)K_Q`, `K_QH=K_Q` EXECUTED; **PROVED at producer only** (trace-level assertion `abs(h2_trace − 8.0) < 1e-13`). Ledger types it **CLOSED_BUT_INSUFFICIENT** | *"evaluate the full compact proper-time action and public superdeterminant to select or reject a stable `x` and geometric saddle before computing alpha"* — with the no-go: *"do not patch or choose `x`; derive and vary the complete compact nonlocal coupled action on a physically provenanced carrier"* |
| **S3** | Coupled record-bundle modulus gate ("sealed open ratio gate, saddle open") | `workspace/COUPLED_RECORD_BUNDLE_MODULUS_GATE_V002.md` `52664ddbd02e04fa…` | SEALED-OK | **CLAIMED as a CONDITIONAL TREE RELATION ONLY** (`alpha_tree = 4 ell_P²/R²`). Own status block: `coupled_bundle_computes_alpha = false`, `radion_stabilization_derived = false`, `charge_fiber_radius_derived = false` | *"computes alpha only after a target-independent derivation supplies: 1. the physical free `U(1)_rel` lift and its total-space metric; 2. a unique action class excluding independent connection and localized `F²` terms; 3. a parameter-free saddle selecting `rho = R_*/ell_P`, including breathing and squashing"* |
| **S4** | BID minimal cell + Hamilton–Jacobi scale bridge ("lone `T_R/t_P`, √2 blocked") | `workspace/BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md` | **NO SIDECAR** on the gate; sealed only by digest pin inside the binding | **CLAIMED CONDITIONAL, never PROVED**: `absolute_record_interval_derived = false`, with a **√2 blocker** | √2 is **derivation-only and chained behind O1** (the universal additive exclusion) |
| **S5** | Joint-handle character Hessian — **the fifth, absent from every four-list** | `primitive_joint_handle_character_hessian_v001` (pinned in the binding) | binding SEALED-OK | **CLAIMED CONDITIONAL** — ledger row 498: one character action, no independent coefficient per block, fixed-handle 3/16 / Schur 1/8. *"in no prior supervision note"* | not stated |
| *(orphan)* | coupled-gravity gate | recorded in the binding `[4486,5667)` | SEALED-OK | **NOT ADOPTED** — *"recorded as a route-class member, not adopted as a route"*; the map calls it *"dropped thread, not refuted"* | n/a |

**Claim-status law applied, and the answer is uniform: NO stratum carries a PROVED status
anywhere in the glob.** The strongest any of the five holds is `CLOSED_BUT_INSUFFICIENT`.

### 1.3 A same-name trap, disclosed

`supervision/BOHM_RATIO_ROUTE_ADJUDICATION_RESULTS_2026-07-28.md` carries **a different
four-way stratification** — a *ratio-precedent* stratification (where in the corpus a ratio was
ever made), not these route strata. **Same word, different object.** Merging them is the
name-match trap; they are kept apart here.

---

## 2. AS2 — THE DISCONNECTIONS

### 2.1 The record's own answer to "what connects them": **nothing**

`supervision/RATIO_ROUTE_CROSS_CITATION_MAP_2026-07-28.md` (**NO SIDECAR**) is the deliverable
that was to connect them; its finding, adversarially CONFIRMED, is that the strata have **zero
cross-citations**. The sealed binding adds a **NON-COLLAPSE LAW** `[5668,5994)` — three scale
obligations that forbid shortcutting the interfaces — corroborated by an independently sealed
lane result on modulus radius vs record cell.

### 2.2 Classification (776-style). Tag: **[YOURS]** — the requirements are quoted; the typing is mine.

| boundary | the missing object, as the record states it | class | basis |
|---|---|---|---|
| **S1/S2 → S3** | a **parameter-free selection or rejection of the depth `x = r²/k_R²`**, via "the complete compact nonlocal coupled action on a physically provenanced carrier" | **UNDECIDABLE_FROM_STOCK** | the action is not in stock; the depth no-go **forbids** choosing `x`, so no free-multiple sketch is lawful either |
| **S3 → S4** | the three named suppliers: free `U(1)_rel` lift + total-space metric; a unique action class; a **parameter-free saddle selecting `rho`** incl. breathing and squashing | **UNDECIDABLE_FROM_STOCK** | none of the three is in stock; the gate's own flags are all `false` |
| **S4 → (scale)** | resolution of the **√2 ambiguity**, chained behind O1 | **FREE_MULTIPLE** | an *ambiguity* of record with the resolution chained behind an undischarged obligation — two readings, neither forced |
| **S5 → (all)** | any cross-citation at all; S5 postdates every four-list and no artifact relates it to S1–S4 | **UNDECIDABLE_FROM_STOCK** | zero cross-citations; S5 is *"in no prior supervision note"* |
| *(orphan)* | adoption as a route | **FREE_MULTIPLE** | expressly *"not adopted as a route"*; adoption is a principal act |

Cross-referenced to `DECLINE_REGISTER_V002`: **S04** (Δτ / `T_R` identification refused, bounded
negative — the ratio-route scale leg), **S29** (`C_R=1` selects only after *"an unbuilt
source-record-gravity Hamilton–Jacobi bridge"*, energy fork unresolved), **S33/S34** (the
four-dimensional coefficient and absolute scale, each conditional on a quoted unmet guard). Span
digests for S04/S16/S30 were independently recomputed and **match the register**.

```text
DISCONNECTS = 5 classified   (FORCED_UNIQUE: 0 / FREE_MULTIPLE: 2 / UNDECIDABLE_FROM_STOCK: 3)
```

**Zero FORCED.** Not one stratum boundary is closed by pinning constraints already in stock.

---

## 3. AS3 — R-L2b ITSELF

### 3.1 The estimate, as the record has it

Chartered in `workspace/STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md` (SEALED-OK) `[52138,52719)`
as the **R-L2b SCALING-EXPONENT DERIVATION**, typed as an **OBLIGATION** with a named failure
witness `SCAD_HS_SCALING_EXPONENT_UNDERIVED`. The spec is explicit that the exponent is not
granted: *"THE EXPONENT alpha IS NOT ASSERTED"*, *"Until R-L2b closes, alpha is a symbol, not
1/2."*

### 3.2 The four obligations — cited individually

All four in `workspace/STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md`, SEALED-OK,
under the header **"ONE CAMPAIGN, FOUR DISCHARGES"** `[4670,4979)`:

```text
O1  [4137,4192)  R-L2b itself                                   OPEN (later REFUTED AS WRITTEN)
O2  [4192,4298)  T11's response half                            OPEN — measure DERIVED, naturality
                                                                NOT proved (cellulation_independence
                                                                _proved = false)
O3  [4298,4414)  the D3 refinement-natural weight, RESPONSE side RESPONSE OPEN; the WEIGHT side is
                                                                DERIVED/CLOSED unconditionally on
                                                                full D3 (P-S2 "HIT")
O4  [4414,4670)  CONNECTED EXTENSIVITY, with its exact           DERIVED REDUCTION adopted; the
                 reduction to R-L2b's exponent                   extensivity question itself
                                                                REQUIRED BUT NOT DERIVED, AND NOT
                                                                CONTRADICTED
```

**Discrepancies displayed, three:**
1. **R-L2b is TWO exponents, not one.** The campaign sweep records `alpha` **and** `beta` as
   *"part of R-L2b's deliverable, routinely omitted when the campaign is summarised."* So "one
   estimate discharging four obligations" is itself carrying two deliverables.
2. **A different four-set can be mistaken for these four** — the transport charter's O-D1/O-D2/O-D3
   (accepted, open) plus O-D4 (flagged, **not** accepted). Distinct object; kept apart.
3. **The supervision restatement of the four ("THE FOUR CONSUMERS") is UNSEALED**
   (`BOHM_SWEEP_2026-07-27_rl2b_campaign.md`, no sidecar). The sealed carrier is the campaign
   opening, not the sweep.

### 3.3 The ten crossings — **the enumeration is ABSENT**

The count appears in exactly **two** files — `supervision/SESSION_RECORD_2026-07-27_route_survey.md`
`[5060,5291)` and `[9991,10078)` (**NO SIDECAR**), and `CONTINUATION_STATE.md` `[59773,60128)`
(SEALED-OK) — and **neither lists them.** Probes run: `ten crossings|10 crossings|not one
bridge|one bridge` → 2 hits, both unrelated; `crossings` → 5 hits, none an enumeration.

**And the ten is not a construction — it is a REFUTATION.** It records the defeat of a reviewer's
*"one finite-to-physical bridge"* hypothesis, on type signatures: the route needs **ten crossings,
not one bridge**. The number is a measure of distance, not of progress.

### 3.4 Route standing — four steps, and they do not point the same way

```text
1. R-L2b's OWN COMMUTATOR ROUTE — REFUTED BY ITS OWN LANE on two independent grounds
   (STAGE8_RL2B_COMMUTATOR_ROUTE_REFUTED_AND_TARGET_SHARPENED_V001.md, SEALED-OK;
    status: R_L2b_closed = false / alpha_derived = false / p_known = false)
2. R-L2b's UNIFORMITY — REFUTED (Einstein lane, blind inputs, SEALED-OK). "A REFUTATION, ESTABLISHED."
3. DISPOSITION OF RECORD — the principal rules R-L2b SUPERSEDED / RE-POSED, not solved and not
   merely refuted (FOURTH_HORN_PRINCIPAL_DECISION_2026-07-29, SEALED-OK).  Ground disclosed as
   the principal's, NOT a sealed physical principle.  THE SAME RULING states any replacement
   MUST STILL REACH THE FOUR CONSUMERS — the four survive the supersession, undischarged.
4. CONTINUATION_STATE's blunt line: R-L2b is REFUTED AS WRITTEN, typed TYPE-R.
```

**Route (b) — a different object, and it is LIVE.** Route (b) is one of three principal routes to
RA27-2 and *is* the R-L2b sliver exponent. Two decisions of record dated 2026-08-08 —
`DECISION_RESTRICT_AND_SAY_SO` and `DECISION_FLAT_CHAMBER_ROUTE` — both leave route (b) **live and
unconsumed**, the latter *"regardless."* **R-L2b the campaign is refuted-as-written; route (b) the
option is live.** Conflating them would be an error in either direction.

### 3.5 THE MISSING POWER — located, and it is not a shortfall in a bound

**The phrase.** `supervision/C4_DISPOSITION_BRIEF_2026-07-28.md` `[5130,5574)` — **UNSEALED**, and
self-typed *"STATUS: decision document. Nothing here is ruled."* Its sealed restatement is
`C4_REFUTED_CLAUSE_STRIKE_PRINCIPAL_DECISION_2026-07-29.md` `[5574,5969)` (**SEALED-OK**): *"short
exactly one power of decay"*, under a heading that reads **"A LEAD, DELIBERATELY NOT RULED ON."**

**The failing term.** `STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md` (SEALED-OK) §O.A0.2
`[35505,36573)` — the arm-2 target and its **two** one-power-short failure modes — with the shell
sum written out at `[36573,37641)`, labelled by the corpus itself *"EXACT ARITHMETIC OF THE
TARGET"*, i.e. **arithmetic conditional on an assumed exponent, not a derived result.**

**Where the power was supposed to come from.**
`STAGE8_T7_ARM2_SHARPENING_AND_PROJECTION_TAIL_BINDING_V001.md` (SEALED-OK) `[1135,1624)`: the
`delta'` **integrated by parts onto the smooth cell profiles** — carried only as a **CONTINUUM
BOUND-CLASS, explicitly NOT an object theorem.**

**THE POINT OF LOSS, and it is the finding.** `STAGE8_IBP_SCALAR_WEIGHT_DETERMINATION_V001.md`
(SEALED-OK) `[112,974)` and `[1625,2467)`:

```text
IBP_performed_anywhere_in_corpus = FALSE      the integration by parts has NEVER been performed
                                              and its integrand is NEVER WRITTEN
smooth_cell_profiles_referent  = ABSENT       2 occurrences, no definition, three candidate
                                              objects, ALL DISQUALIFIED
```

Corroborated by `STAGE8_BRIDGE_DECAY_DETERMINATION_V001.md` (SEALED-OK) `[4982,5697)`:
**the missing power "is not lost in a computation, it is never supplied; the convergent sum
presupposes it."** Type: `BOUND_CLASS_NOT_OBJECT_THEOREM`, *"Neither >= R^-1 nor a number less
than it."*

**What kind of object would supply it — the record answers, and it is not a bound.**
`ARM2_SHARPENING…` `[1628,2160)` names a **LEMMA with three uncertified components**:

```text
U1  time-profile norms                                    uncertified
U2  lightcone transversality over full D3                 uncertified
U3  projection tail                                       uncertified
```
`BRIDGE_DECAY_DETERMINATION` `[2171,2486)` states the class in one line: a **profile /
transversality / projection-tail lemma**. **Not a sharper bound. Not a different decomposition.**
And unblocking U1 requires *a sealed spec act fixing seven inputs* — *"NOT ATTEMPTED. NOT SCOPED.
NOT DESIGNED."*

**All three alternative suppliers are dead of record:**
```text
HS volume scaling supplies the R^-1     DEAD (closed door, recorded)
time integration                        DOES_NOT_REACH — "cell_scale_role_already_spent = true"
Q-support structure                     DEGENERATE, no regime — "IT IS U3 WEARING A DIFFERENT NAME"
graveyard: the IBP route                DEAD AT THE OBJECT GATE (with an explicit revival trigger)
```

**Classification: `UNDECIDABLE_FROM_STOCK`.** Not FREE_MULTIPLE — there are not two inequivalent
sketches; there is **no object for the lemma to act on.** The crux input is ABSENT, so the
question cannot be posed from stock, let alone answered. **The named sealed answer that is
missing: a definition of "the smooth cell profiles."**

### 3.6 The S13 caution, discharged — and the direction matters

Verified **byte-exact by me**, file digest and span digest both reproducing the register's values
(`eed30aec…`; `[19029,19502)`; span `f18ee4f9…`):

> *"Arm 2 was NOT identified with R-L2b in either direction; the corpus's one-way coupling
> (**arm 2 may consume R-L2b, never the reverse**) is preserved."*

The origin D-1 determination adds *"Neither gates the other"*, and the S13 source artifact states
independently: ***"R2 DOES NOT REACH R-L2b. NOT PARTIALLY, NOT WEAKLY, NOT VIA ARM 2."***

```text
CONSEQUENCE, mapped as sealed and not beyond it:
   R-L2b is the SUPPLIER; arm 2 is the CONSUMER.  The missing power sits in arm 2.
   Therefore CLOSING R-L2b DOES NOT CLOSE THE MISSING POWER — that inference is exactly the
   identification S13 declines.  The relay's own CAUTION is well placed, and its framing
   ("R-L2b ... its known gap: the cluster-summability arm") is the phrasing the caution warns
   against.  I map the relation, not the framing.
```

---

## 4. AS4 — A32

### 4.1 What it is, what ratification required, where it stands

```text
DEFINING ROW   BID_CONSOLIDATED_HOSTILE_AUDIT_MATRIX_V005.md [13418,13997)  — status PENDING
                                                                              (NO SIDECAR)
FULL SPEC      V011 "External holdout requirement" [64592,67660); A32_FAIL clause [66760,67660)
               *** CUSTODY: the workspace working copy of V011 is a DIFFERENT BYTE-VERSION from
               *** the sealed mirror aa7c6d49…, and the A32 line numbers differ between them.
               *** Spans are NOT interchangeable.  The sealed mirror is used here.
ACT 1          A32_FREEZE_V002_RATIFIED_2026-07-28  SEALED-OK — the holdout freeze, four per-item
               rulings; expressly NOT a ladder-grade flag flip and NOT attainment (five flags
               remain false)
ACT 2          DECISION_A32_INSTRUMENT_RATIFIED_2026-08-06  SEALED-OK — "RATIFIED AS BOUND";
               "NOTHING IS ATTAINED BY THIS RATIFICATION"; three gates explicitly false
COMPLETION     the M5a-V002 ratified predicate, ~23 conjuncts.  CURRENT partition (SEALED-OK,
PREDICATE      STAGE8_TASK6_M5A_STATUS_LANE3_V001): 7 TRUE / 13 FALSE-SUPPLIED-BY / 3 FALSE-LONG-POLE
ROOT BLOCK     passed_A01_A29_and_A35 = FALSE of record => SPEC-SEAL false AT THE GRAPH ROOT,
               propagating to everything above it
GATES          A34 makes "passed structure-sensitive A32" a LITERAL dependency of proof_authorized
ENLARGEMENT    A32 as ratified requires a SECOND, NON-ALPHA prediction at D>=5 — it enlarges the
               definition of done
PERMANENT      Q-25: process independence and independent attestation were NEVER established —
QUALIFICATION  "IN-TENSION — PERMANENT CARRIAGE"; travels with every A32 headline
```

### 4.2 Does AS1–AS3 change A32's scope? **No — and the relay's premise is unsupported.**

```text
"A32-gated" as a literal phrase: ABSENT from the glob.
ANY record tying A32's SCOPE to the strata: ABSENT.  Probes: "four strata|four-strata|4 strata"
   -> 3 hits, all read; (grep strata) INTERSECT (grep A32) -> 8 files, all read at their A32 spans.
   NONE states that A32's scope, obligations, or ratification is defined over the strata.
CONNECTING THE STRATA IS A SEPARATE PLAN ROW, NOT AN A32 OBLIGATION (plan V006 lists them apart).
The only A32/R-L2b tie in any A32-defining artifact is a FILING-PATTERN precedent — how to record
   a principal ruling — with NO substantive dependency.
```

**A tension of record, displayed not resolved:** the plan's F1 is an unstarted Phase-F task, while
`STAGE8_LANE_STATUS.md` row R-22 records the A32 holdout freeze as already *"RATIFIED AND WRITTEN
INTO V011"* with an explicit scope limitation, and a 2026-08-09 lane audit scores **F1 = DONE /
SCAFFOLD**. Three different statuses for one row. Under the claim-status law the lane audit enters
as **CLAIMED**, not PROVED.

```text
A32 = scope stated: an external-holdout instrument, twice ratified and attaining nothing;
   completion predicate 7/23 TRUE with 3 long poles and a FALSE graph root; gates
   proof_authorized via A34; NOT scoped over the strata (ABSENT); unchanged by AS1-AS3.
```

---

## 5. AS5 — THE VERDICT LINE FOR THE PRINCIPAL

### 5.1 The reframing, first, because it governs everything below

```text
SEALED Q-34 (PRINCIPAL ACT, 2026-07-30):
   "RULING: K_*, THE ONSET ROOT.  SLOT 16 IS RE-POSED TO CARRY K_* -> kappa_Thomson."
   "kappa_record is an INTENSIVE HESSIAN, a density on the completed chain requiring the
    volume-density limit and Theorem 3's five hypotheses.  K_* is an ONSET ROOT on ONE cell,
    alpha_micro = 1/(4 pi K_*)."
   "the two Hessians are generically DIFFERENT physical quantities."
   Bounded negative of record: 20 files carry K_*, 263 carry kappa_record, 7 carry both and all
   seven were created 2026-07-30.  No pre-existing artifact relates them.

THE CURRENT PLAN (V003, SEALED-OK, supersedes the V001 this chain was framed against):
   "KEY TYPING (sealed Q-34): kappa_record is the record-side intensive Hessian; ALPHA'S
    STIFFNESS IS K_*.  THE R-L2b/LINKED-CLUSTER CAMPAIGN IS OFF ALPHA'S VALUE PATH.
    The kappa-only chain is short but is NOT a shortcut to alpha."
```

**So AS5's question — "what stands between the ratio route and a lawful `kappa_record`" — is posed
across a link the record has ruled absent.** The ratio-route / R-L2b campaign is **off alpha's
value path**; `kappa_record` is reached instead by the forced stitching spine
(`measure → [3 FREE B1 fields] → M04 gluing obstruction → [chain/density inputs] → M11
kappa_record`). I answer the question as asked **and** display the typing, because answering it
without the typing would imply a connection the principal has ruled out.

### 5.2 `PATH_REMAINDER` — enumerated, nothing elided (14 items)

Assume, as AS5 directs, that the missing power closes and the strata connect as classified. **What
still stands:**

```text
 1  Q-34 TYPING.  Alpha's stiffness is K_*, not kappa_record; the R-L2b campaign is OFF alpha's
    value path.  Closing the ratio route does not, of record, deliver alpha's stiffness.
 2  THE FOUR CONSUMERS survive R-L2b's supersession and are UNDISCHARGED (the ruling says any
    replacement must still reach them).  O2 naturality, O3 response side, O4 extensivity.
 3  R-L2b IS TWO EXPONENTS (alpha and beta); the beta deliverable is routinely omitted.
 4  THE TEN CROSSINGS ARE NEVER ENUMERATED.  The count is a refutation of "one bridge", so the
    work between the strata is unquantified as well as unbuilt.
 5  THE STRATA ARE UNCONNECTED — zero cross-citations; 0 of 5 boundaries FORCED; the NON-COLLAPSE
    LAW forbids shortcutting them.
 6  S1/S2's PRODUCT IS OUTSIDE THE ARCHIVE, unsealed and hash-pinned nowhere.
 7  A32: 3 FALSE-LONG-POLE conjuncts, and passed_A01_A29_and_A35 FALSE at the graph root.
 8  SPEC-SEAL = FALSE (Task-6 certification of record).
 9  THE COMPARISON BAN'S RELEASE CONDITION IS ABSENT — never authored.  Four files mention it;
    EVERY ONE ASSERTS ITS NONEXISTENCE.  Plan F2 standing debt.
10  kappa_record = kappa_Thomson IS NOT ASSUMED — V011 requires it DERIVED or the route fails;
    the assumption is on V011's own sealed FAILURE list.
11  A28 FIREWALL: BLOCKED on the absent downstream Q-spec.
12  C7 THOMSON CHAIN: S03 is DECLARED EXPLICIT INCOMPLETENESS; S04-S08 inherit its object-side
    block.  (S03 at least HAS a written release condition — the comparison ban does not.)
13  THE FIVE PHYSICS ROWS: all BLOCKED.  Phase C prerequisite unmet — the common cell is not
    formed, so the frozen recognition prereg (9f0d12b4) cannot run.
14  THE FORCED SPINE'S THREE B1 FIELDS ARE ALL FREE: connection (32-dim non-gauge free set, no
    sealed closure), support (needs a unique complex; none fixed), current-density (unsupplied).
    Gate-5's own battery adds six named PENDING items and BID_core_result_sealed = false.
```

**Stated plainly for the principal:** the missing power is the *smallest* of these. It is one
undefined object inside one arm of one obligation of a campaign the current plan places **off
alpha's value path**. Items 9, 10, 13 and 14 are on the path and are not estimates at all — they
are absent objects and unwritten authorizations.

---

## 6. FREEDOMS CONSUMED, FLATTENING CHECK, AUDITS

### 6.1 `FREEDOMS_CONSUMED` (law 2 — an empty block is itself a claim)

| free datum touched | tag |
|---|---|
| the depth `x = r²/k_R²` | **CARRIED-AS-PARAMETER** — quoted as unselected; the no-go against choosing it is quoted |
| the modulus `rho = R_*/ell_P` | **CARRIED-AS-PARAMETER** — the saddle is quoted as required, never supplied |
| the √2 branch (S4) | **CARRIED-AS-PARAMETER** — displayed as an ambiguity chained behind O1 |
| the R-L2b exponents `alpha`, `beta` | **CARRIED-AS-PARAMETER** — "alpha is a symbol, not 1/2" quoted; no value used |
| "the smooth cell profiles" | **CARRIED-AS-ABSENT** — reported ABSENT; no referent supplied or guessed |
| `K_*` vs `kappa_record` | **CONDITIONED-ON** the sealed Q-34 typing, quoted verbatim; not merged |

**SUBSTITUTED: none.** No estimate attempted; no bound computed; no object authored.

### 6.2 `FLATTENING_CHECK` — run against `DECLINE_REGISTER_V002` (S01–S37), the authoritative table

```text
S13  Arm 2 / R-L2b identification    CLEAN — the one-way relation is mapped as sealed and the
                                     identification is expressly NOT made in either direction;
                                     §3.6 states the non-entailment explicitly.
S04  Delta tau / T_R identification  CLEAN — cited as a decline, not used.
S16  Hessian-first supersession      CLEAN — not chartered here.
S29  C_R = 1                         CLEAN — quoted with its unbuilt-bridge guard.
S30  Hessian-first "IF R-L2b FAILS"  CLEAN — quoted as a conditional grant; no failure declared,
                                     no fallback invoked.
S33/S34 coefficient / absolute scale CLEAN — quoted with their guards.
S23/S24/S25/S26 BARRED-ABSOLUTE      CLEAN — none approached.
S36/S37 RULED-ABSENT                 CLEAN — cited as absences, not filled.
S01-S03, S05-S12, S14-S15, S17-S22, S27-S28, S31-S32, S35   not touched by this map.
FLATTENING_CHECK = clean (37 rows walked; 11 live to this map, each discharged above).
```

### 6.3 Jurisdiction

**On reporting that three of the relay's premises fail.** *What was the rule written to protect?*
AS1 says explicitly *"If the record enumerates fewer or more than four, SAY SO."* The relay
anticipated this. *Does the outcome space distinguish a real correction from lane preference?*
Yes — §1.1 shows the origin of "four", its supersession 26 lines later, the adversarial
confirmation, and the sealed enumeration, in that order. *Would the evidence look different if
"four" were right?* Yes: there would be no sealed five-strata binding and no `primitive_joint_
handle_character_hessian_v001` row.

**On the VOID CONDITION.** Nothing chosen from a desired outcome. The classification in §2.2 is
tagged YOURS with the quoted requirement beside it; where the record is silent I wrote
UNDECIDABLE rather than inventing a forcing.

**On builder independence.** No `evaluator_build_A/` or `checks/` file was opened by me or by any
commissioned hunter; one grep enumerated a path under that tree and it is disclosed in §0.2.
`~/.codex` untouched; `memory-bank` never searched.

**On RULING-LAST (law 5).** Nothing here routes as a ruling. Where a principal act would be
needed, it is listed as a remainder item, not requested.

### 6.4 Self verb audit — **NOT CLEAN: five disclosures**

1. **I loaded the brief the relay pinned (V003) and it went stale twice during the relay** (V005
   now current; the plan moved V001→V003). I read the current ones too and disclosed both rather
   than silently substituting. **Alignment law 4 is not well-defined under a live registrar** and
   I report that as a process finding, not a task defect.
2. **My own 781 flattening check is now against a superseded register** (V001 → V002). It was
   correct when run — V002 did not exist at my 781 preflight — and it is not correct now. Flagged
   for re-running by whoever registers 781. *This is the CORRECTION-PROPAGATION failure mode
   again, in the opposite direction: last relay a correction failed to propagate forward; this
   relay the ground moved under a completed check.*
3. **A seal genuinely fails** (`EXECUTION_TRACKER.md`) and I confirmed it in isolation, from the
   file's own directory, because at 742 I generated six false mismatches doing this wrong.
4. **The classification in §2.2 is mine, not the record's.** The record supplies quoted interface
   requirements; the FORCED/FREE/UNDECIDABLE typing is a lane judgment and is tagged YOURS. A
   different lane could type the S4 √2 boundary as UNDECIDABLE rather than FREE_MULTIPLE.
5. **I answered AS5 as asked and also displayed the typing that undercuts its premise.** Answering
   "what stands between the ratio route and kappa_record" without stating Q-34 would have implied
   a connection the principal ruled absent; stating only the typing would have refused the task.
   I did both, in that order, and flag that the ordering is a judgment.

*Instrument note:* the six-hunter fan-out plus a completeness critic located the material; **every
decisive item in this artifact — the S13 span and its digests, the five-strata binding, the
EXECUTION_TRACKER mismatch, the register supersession, the Q-34 ruling, the plan V003 typing — I
re-opened and verified myself.** The critic's most useful output was naming what no hunt had read.

---

```text
STRATA = 5 located, NOT 4 (the relay's premise corrected, as AS1 directs).
   S1/S2 BR induced coefficient functions (forced ratios executed; depth x open) — product ABSENT
      from the glob, in a parent tree, hash "pinned NOWHERE", no sidecars;
   S3 coupled record-bundle modulus gate (alpha_tree conditional; saddle open) — SEALED-OK;
   S4 BID minimal cell + Hamilton-Jacobi scale bridge (sqrt(2) blocked, chained behind O1) — gate
      carries NO SIDECAR, sealed only by digest pin;
   S5 joint-handle character Hessian — the FIFTH, absent from every four-list;
   plus one ORPHAN node recorded as a route-class member and expressly NOT ADOPTED.
   Sole sealed enumeration: STAGE8_GRAVITY_EVIDENCE_ADMISSION_AND_FIVE_STRATA_BINDING_V001.md
   (bad9795072739e35…), titled "One Route Class, Five Strata", whose own scope block reads
   routes_merged = false / routes_ranked = false / alpha_computed = false.  CLAIM-STATUS LAW
   APPLIED: NO stratum carries PROVED anywhere in the glob; the strongest is CLOSED_BUT_INSUFFICIENT.
   Same-name trap disclosed: BOHM_RATIO_ROUTE_ADJUDICATION's four-way "strata" is a ratio-PRECEDENT
   stratification, a different object.
DISCONNECTS = 5 classified (FORCED_UNIQUE: 0 / FREE_MULTIPLE: 2 / UNDECIDABLE_FROM_STOCK: 3).
   ZERO boundaries are closed by constraints already in stock.  The record's own cross-citation
   map finds ZERO cross-citations between strata, and the sealed binding carries a NON-COLLAPSE
   LAW (three scale obligations) forbidding shortcuts.  Cross-referenced to DECLINE_REGISTER_V002
   rows S04, S29, S30, S33, S34 (span digests recomputed and matching).
MISSING_POWER = located, and it is not a shortfall in a bound.
   SPANS: phrase at C4_DISPOSITION_BRIEF [5130,5574) (UNSEALED, "nothing here is ruled"); sealed
   restatement "short exactly one power of decay" at C4_REFUTED_CLAUSE_STRIKE_PRINCIPAL_DECISION
   [5574,5969) under the heading "A LEAD, DELIBERATELY NOT RULED ON"; failing term at
   T7_E1_SUCCESSOR_SPEC_V002 SS-O.A0.2 [35505,36573) with its shell arithmetic [36573,37641)
   labelled "EXACT ARITHMETIC OF THE TARGET" (conditional on an ASSUMED exponent); point of loss
   at IBP_SCALAR_WEIGHT_DETERMINATION [112,974) and [1625,2467).
   THE FINDING: IBP_performed_anywhere_in_corpus = FALSE — the integration by parts that would
   deliver the power has NEVER been performed and its integrand is NEVER WRITTEN; and
   smooth_cell_profiles_referent = ABSENT (2 occurrences, no definition, three candidates all
   disqualified).  BRIDGE_DECAY_DETERMINATION: the power "is not lost in a computation, it is
   never supplied; the convergent sum presupposes it" (BOUND_CLASS_NOT_OBJECT_THEOREM).
   OBJECT KIND (the record's own): a PROFILE / TRANSVERSALITY / PROJECTION-TAIL LEMMA with three
   uncertified components U1 time-profile norms, U2 lightcone transversality over full D3,
   U3 projection tail — explicitly NOT a sharper bound and NOT a different decomposition;
   unblocking U1 needs a sealed spec act fixing seven inputs, "NOT ATTEMPTED. NOT SCOPED. NOT
   DESIGNED."  All three alternative suppliers DEAD: HS volume scaling DEAD; time integration
   DOES_NOT_REACH; Q-support DEGENERATE ("U3 wearing a different name").
   CLASSIFICATION = UNDECIDABLE_FROM_STOCK.  Not FREE_MULTIPLE: there are not two inequivalent
   sketches, there is NO OBJECT for the lemma to act on.  The missing sealed answer, named: a
   definition of "the smooth cell profiles".
   S13 DISCHARGED, VERIFIED BYTE-EXACT BY ME (file eed30aec…, span [19029,19502), span sha
   f18ee4f9…): "arm 2 may consume R-L2b, never the reverse."  R-L2b SUPPLIES; arm 2 CONSUMES;
   the missing power is the CONSUMER's.  CLOSING R-L2b DOES NOT CLOSE IT — that entailment is
   the identification S13 declines, and it is the relay's own framing.
OBLIGATIONS = 4 cited individually (EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001, SEALED-OK,
   "ONE CAMPAIGN, FOUR DISCHARGES"): O1 R-L2b itself [4137,4192); O2 T11's response half
   [4192,4298) (measure DERIVED, naturality NOT proved); O3 the D3 refinement-natural weight's
   RESPONSE side [4298,4414) (weight side DERIVED/CLOSED on full D3); O4 connected extensivity
   with its exact reduction to R-L2b's exponent [4414,4670) (reduction DERIVED; extensivity
   REQUIRED BUT NOT DERIVED AND NOT CONTRADICTED).
   THREE DISCREPANCIES DISPLAYED: (i) R-L2b is TWO exponents, alpha AND beta, the second
   "routinely omitted when the campaign is summarised"; (ii) a DIFFERENT four-set (transport
   charter O-D1/O-D2/O-D3 accepted + O-D4 flagged-not-accepted) is confusable with these four;
   (iii) the supervision restatement "THE FOUR CONSUMERS" is UNSEALED — the sealed carrier is the
   campaign opening, not the sweep.
   TEN CROSSINGS = ENUMERATION ABSENT.  The count appears in exactly two files and NEITHER lists
   them; and the ten is itself a REFUTATION of a reviewer's "one finite-to-physical bridge"
   hypothesis on type signatures — a measure of distance, not of progress.
   ROUTE STANDING: R-L2b's commutator route REFUTED BY ITS OWN LANE (two independent grounds);
   its uniformity REFUTED (blind inputs); the principal ruled it SUPERSEDED / RE-POSED with the
   ground disclosed as the principal's and NOT a sealed physical principle; CONTINUATION_STATE
   types it REFUTED AS WRITTEN / TYPE-R.  THE FOUR CONSUMERS SURVIVE THE SUPERSESSION.
   Route (b) — a DIFFERENT object, the R-L2b sliver exponent as one of three routes to RA27-2 —
   is LIVE and unconsumed per two decisions of record dated 2026-08-08.
A32 = scope stated.  An external-holdout instrument; TWICE ratified (holdout freeze 2026-07-28;
   instrument "RATIFIED AS BOUND" 2026-08-06) and BOTH acts expressly attain nothing ("NOTHING IS
   ATTAINED BY THIS RATIFICATION").  Completion predicate M5a-V002: 23 conjuncts, 7 TRUE / 13
   FALSE-SUPPLIED-BY / 3 FALSE-LONG-POLE, with passed_A01_A29_and_A35 FALSE at the graph root so
   SPEC-SEAL is false at the root.  Gates proof_authorized via A34.  Enlarges the definition of
   done (a SECOND, NON-ALPHA prediction at D>=5).  Q-25 permanent carriage: process independence
   and independent attestation NEVER established.
   SCOPE UNCHANGED BY AS1-AS3, and the relay's premise is unsupported: the literal phrase
   "A32-gated" is ABSENT from the glob, and NO record ties A32's scope to the strata (probes
   stated; 8 intersecting files read at their A32 spans).  Connecting the strata is a SEPARATE
   plan row.  Tension displayed, unresolved: F1 is an unstarted Phase-F task, R-22 records the
   freeze as already written into V011, and a lane audit scores F1 = DONE / SCAFFOLD — three
   statuses for one row, the last entering as CLAIMED.
   CUSTODY: the workspace working copy of V011 is a DIFFERENT byte-version from the sealed mirror
   and the A32 line numbers differ; spans are NOT interchangeable.  The sealed mirror was used.
PATH_REMAINDER = enumerated (14 items), and it is governed by a typing the relay's question does
   not carry.  SEALED Q-34 (PRINCIPAL ACT): "ALPHA'S STIFFNESS IS K_*"; kappa_record is the
   record-side INTENSIVE Hessian; "the two Hessians are generically DIFFERENT physical
   quantities".  THE CURRENT PLAN V003: "The R-L2b/linked-cluster campaign is OFF alpha's value
   path.  The kappa-only chain is short but is NOT a shortcut to alpha."  kappa_record is reached
   by the forced stitching spine (measure -> [3 FREE B1 fields] -> M04 gluing obstruction ->
   [chain/density inputs] -> M11), NOT by the ratio route.
   THE 14: (1) the Q-34 typing itself; (2) the four consumers, undischarged, surviving
   supersession; (3) R-L2b's second exponent beta; (4) the ten crossings never enumerated;
   (5) five strata unconnected, 0/5 boundaries forced, non-collapse law forbidding shortcuts;
   (6) S1/S2's product outside the archive, unsealed, pinned nowhere; (7) A32's 3 long poles and
   FALSE graph root; (8) SPEC-SEAL = false; (9) THE COMPARISON BAN'S RELEASE CONDITION ABSENT —
   never authored, four files mention it and EVERY ONE ASSERTS ITS NONEXISTENCE; (10)
   kappa_record = kappa_Thomson NOT ASSUMED, must be DERIVED or the route fails (on V011's own
   sealed FAILURE list); (11) A28 firewall BLOCKED on the absent Q-spec; (12) C7 chain — S03
   declared EXPLICIT INCOMPLETENESS, S04-S08 inheriting its block; (13) five physics rows BLOCKED
   and the common cell not formed, so the frozen prereg 9f0d12b4 cannot run; (14) the forced
   spine's three B1 fields ALL FREE (connection 32-dim, support needs an unfixed unique complex,
   current-density unsupplied), plus Gate-5's six PENDING items and BID_core_result_sealed=false.
   FOR THE PRINCIPAL, PLAINLY: the missing power is the SMALLEST of these — one undefined object
   inside one arm of one obligation of a campaign the current plan places OFF alpha's value path.
   Items 9, 10, 13 and 14 are ON the path and are not estimates at all: they are absent objects
   and unwritten authorizations.
FREEDOMS_CONSUMED = depth x CARRIED-AS-PARAMETER ; modulus rho CARRIED-AS-PARAMETER ; the sqrt(2)
   branch CARRIED-AS-PARAMETER ; the R-L2b exponents alpha and beta CARRIED-AS-PARAMETER ("alpha
   is a symbol, not 1/2") ; "the smooth cell profiles" CARRIED-AS-ABSENT (reported absent, never
   supplied or guessed) ; K_* vs kappa_record CONDITIONED-ON the sealed Q-34 typing, quoted, not
   merged.  SUBSTITUTED: NONE.  No estimate attempted, no bound computed, no object authored.
FLATTENING_CHECK = clean, RUN AGAINST DECLINE_REGISTER_V002 (S01-S37, the authoritative table),
   NOT the superseded V001.  37 rows walked; 11 live to this map (S04, S13, S16, S23-S26, S29,
   S30, S33, S34, S36, S37), each discharged in section 6.2.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+5): (1) the brief the relay pinned went stale TWICE during the relay
   (V003 -> V005; plan V001 -> V003, and V005's own pointer to plan V002 is already stale) — I
   read the current ones and disclosed rather than silently substituting, and I report that
   alignment law 4's "current version" is not well-defined under a live registrar; (2) MY OWN 781
   FLATTENING CHECK IS NOW AGAINST A SUPERSEDED REGISTER — correct when run (V002 did not exist at
   my 781 preflight), not correct now, and flagged for re-running; (3) a seal GENUINELY FAILS,
   supervision/EXECUTION_TRACKER.md (sidecar f42addcc… vs file ad0740f9…), confirmed in isolation
   from the file's own directory because at 742 I generated six false mismatches doing this wrong;
   (4) the FORCED/FREE/UNDECIDABLE typing in section 2.2 is MINE, not the record's — the record
   supplies the quoted requirements only, and another lane could type the S4 sqrt(2) boundary
   differently; (5) I answered AS5 as asked AND displayed the Q-34 typing that undercuts its
   premise, in that order, and flag the ordering as a judgment.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
