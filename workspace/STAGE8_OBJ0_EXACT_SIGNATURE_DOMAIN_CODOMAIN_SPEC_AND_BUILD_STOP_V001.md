# Stage 8 `Obj_0` Exact Signature, Domain, Codomain: Specification and Build Stop v001

Lane: CODEX 2  
Date: 2026-07-31  
Register head at seal issue: Q-98 (Q-95 target; Q-97/Q-98 consumed)  
Road justification: ADVANCES STEP 1  
Artifact kind: Q-52 REQUIREMENT-SCHEMA SPECIFICATION / Q-92(c,e,f) BUILD STOP

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 0. F-GK3 premises declared before any proposed formalism

### 0.1 Sealed inputs used

```text
F0. The physical class and P0-P7 roles are exactly the class definition and
    rows at STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md:34-39,229-263.

F1. Q-43 preserves antecedent source/record sectors and formation order at
    STAGE8_GAMMA_K_SOURCE_RECORD_COMPOSITION_TYPING_TESTS_V001.md:167-175,
    316-327.

F2. Q-94 requires actual/canonical structure for every P-row at
    QUESTIONS_SETTLED_REGISTER_V001.md:3847-3864.

F3. Q-69 separates flags from discharge objects; Q-95's graph excludes flags
    at STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md:892-910.

F4. Q-92 permits specification when construction prerequisites do not exist at
    QUESTIONS_SETTLED_REGISTER_V001.md:3777-3792.

F5. P0 buildability remains NO_VERDICT under Q-95 at the register :3883-3920.

F6. Q-97 rules that the primitive prefreeze is too thin and records
    P0_primitive_inventory_sufficient_to_build_Obj_0_kind=false | TYPE-C at
    QUESTIONS_SETTLED_REGISTER_V001.md:3970-3990.

F7. Q-98 amends Q-92 to require a dedicated verdict owner for every test and an
    adversarial countermodel attempted and failed at
    QUESTIONS_SETTLED_REGISTER_V001.md:3994-4025.
```

### 0.2 Declared specification premises, not derived physics

```text
D0. Dependent/refinement records are used as one formal implementation of this
    requirement schema. They carry no physical existence claim, and no claim is
    made that this representation is mathematically unique.

D1. A successful Obj_0 instance has one immutable object identity, one immutable
    construction-record identity, and one immutable emitted graph identity;
    every successful port result and origin trace is indexed by all three.

D2. Construction and port-reference contexts are split. I_prim is the only
    primitive context visible to ConstructObj0Instance_0. S_sector is a frozen
    context of independently derived sector references visible only to port
    execution and never reclassified as primitive. Every I_i is a strict frozen
    subcontext of I_prim; every S_i is a strict frozen subcontext of S_sector;
    prior c_j values occur only as explicit port arguments. No context contains
    a P-row flag, target, validation fixture, or independently supplied output
    of that same port.

D3. Cross-row dependencies proposed below are typing dependencies of descendant
    port executions, not inputs to constructing Obj_0.

D4. Origin traces for P2/P3 cover the jointly constructed embedding/coherence
    maps, not temporal creation of antecedent source/record carriers.

D5. P1 may use an equivalent-domain branch, and P6 may use an alternative to
    S_CTP, only after each applicable exact equivalence predicate and witness
    type is frozen. No equivalence is presumed here.

D6. The independent validation package cannot be read by the signature freezer,
    the Obj_0 constructor, or any port generator.

D7. Every static and instance test has one dedicated verdict-owner object, and
    no construction may be reported derived until a hostile countermodel for
    that exact test has been submitted and rejected by the test/owner pair.
```

Every D-row is a failure-capable design premise. None is reported derived.

## Lead determination

The previous `Sig_0` does **not** enforce `COMMON_ORIGIN`.  It contains only
object/type metadata; the seven attempt ports and the origin certificate are
sibling members of the outer presentation.  This is an executed
specification-level refutation of that signature schema, not a refutation of
joint P0 and not an impossibility theorem.

One proposed replacement requirement schema is named here:

```text
CERTIFIED_PROVENANCE_INDEXED_DEPENDENT_PRODUCER_SIGNATURE
```

Definition: a proof-carrying, many-sorted signature schema whose successful
instance contains one `Obj_0`, seven actual/canonical structure-producing
interfaces indexed by that same object, one replayable construction record,
one explicit direction-bearing dependency graph, per-port origin-trace
generators, and an independently checkable aggregate-certificate generator.

This is a declared **formal requirement schema**, not a new physical ontology
and not yet a Q-80 class ruling.  A flat record plus a separately validated
relational invariant might express the same obligation; no test here excludes
that alternative.  The terms *dependent type*, *refinement*, and
*proof-carrying interface* are imported mathematical/software vocabulary. They
are one precise way to make co-reference and failure conditions explicit. They
do not supply a physical premise, a constructor, or an existence theorem.

The exact **physical** category, internal domain, internal codomain, P1 domain
equivalence (if that branch is used), and P6 action/evolution equivalence remain
unbuilt. Therefore this artifact
specifies a conditional requirement schema carrying explicit exactness
obligations; it does not specify or construct the exact physical `Sig_0`, and it
does not construct `Obj_0` under Q-92.

```text
prior_flat_Sig_0_forces_common_origin = false | TYPE-R |
  test: FLAT-SIGNATURE-INDEPENDENT-ORIGIN-REASSEMBLY; the declared Sig_0 fields
  do not depend on, contain, or validate the seven port witnesses or aggregate
  certificate

conditional_signature_requirement_schema_specified = true
CERTIFIED_PROVENANCE_INDEXED_DEPENDENT_PRODUCER_SIGNATURE_derived = false | TYPE-U |
  would-build: freeze the exact physical Obj_0 category/signature, exact port
  schemas, origin-trace semantics, construction rule, and independent acceptance
  oracle, then construct an inhabitant and pass Section 11

Obj_0_exact_signature_specified = false | TYPE-U |
  would-build: select every PhysicalSig_0 field, bind every free primitive
  ground type and derived-sector reference type, freeze the exact
  port/cross-row/graph and origin-predicate schemas including applicable P1/P6
  equivalence, and pass every row of STATIC_TEST_SET_0

Obj_0_exact_signature_freezable_now = false | TYPE-C |
  constraint: Q-92(c,e,f), signature-freeze prerequisites and owner/countermodel
  releases do not yet exist |
  release: exact physical category/domain/codomain ground, sufficient frozen
  primitive bindings, frozen derived-sector reference type bindings, exact
  port/cross-row/graph schemas, origin semantics, and the branch-conditioned
  P1/P6 equivalence predicates exist; dedicated static verdict slots adjudicate
  every row of STATIC_TEST_SET_0 and every named hostile static countermodel
  fails to break the candidate

one_physical_Obj_0_inhabits_all_seven_ports = NO_VERDICT |
  deciding evidence: one constructed instance passing all seven executed port
  and origin tests, or a joint incompatibility theorem/countermodel
no_signature_can_supply_all_seven_ports = NO_VERDICT |
  deciding evidence: a theorem/countermodel covering every admissible exact
  signature, not the present failure to freeze one
Q80_new_formal_class_required = NO_VERDICT |
  deciding evidence: a failure-capable comparison showing every flat
  presentation with independently validated relational invariants fails the
  common-origin obligation
```

No port-specific impossibility is established. All seven sealed output-field
requirements are transcribed into proposed dependent type schemas below;
static well-typing and simultaneous physical inhabitation remain undecided.

## 1. Authority, road position, and fences

### 1.1 Register head and construction rule

At the beginning of this work the last substantive question in
`/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md`
was Q-95 at `:3883-3920`.  Its ruling says verbatim:

> `RULING: NO_VERDICT. NEITHER A QUALIFYING CONSTRUCTOR NOR AN IMPOSSIBILITY THEOREM EXISTS IN THE BOUNDED NAMED AND TOKEN-INDEPENDENT SIGNATURE CENSUSES.`

The same ruling types P0 at `:3896-3898`:

> `P0 IS TYPED: a joint source-record-field provenance/dynamics object that makes P1-P7's actual structures constructible.`

It names the exact signature and primitive inventory among the missing inputs
at `:3900-3908`:

> `THE SIX MISSING CONSTRUCTION INPUTS, NAMED: the exact signature/domain/codomain; a frozen primitive inventory; a target-independent construction rule; a certificate-generation rule; port and graph schemas; and a separately isolated validation package`

Q-92 at the register `:3777-3792` permits construction only when:

> `premises are declared, its tests are wired, its prerequisites EXIST, and it names the step it completes.`

and commands:

> `If (c) fails, specify and name what is missing`

During hostile review the register advanced. Q-97 at `:3970-3990` ruled:

> `THE INVENTORY IS TOO THIN. A PREFREEZE AUDIT WAS PRODUCED, NOT A FROZEN INVENTORY.`

and records:

> `P0_primitive_inventory_sufficient_to_build_Obj_0_kind = false | TYPE-C`

Q-98 at `:3994-4025` then amended Q-92 with:

> `(e) a DEDICATED VERDICT OWNER exists for each test`

and:

> `(f) an ADVERSARIAL COUNTERMODEL was attempted and failed`

Those later rulings control this final pass. They add release conditions; they
do not supply the exact signature or an `Obj_0` inhabitant.

This artifact takes that specification branch.  The road justification is
`ADVANCES STEP 1`: Q-93 says at `:3840-3843`:

> `EVERYTHING NOW FUNNELS THROUGH STEP 1.`

Relay 191 assigns this item the process status `ADVANCES STEP 1`; this artifact
claims no `STEP_1_COMPLETION` verdict.

### 1.2 Fences honored

```text
a32_holdout/custodian_private/ = NOT OPENED, NOT LISTED, NOT SEARCHED
primitive inventory contents = NOT ENUMERATED; Q-97 register disposition only
S1/S2/S4 vacuity defects = NOT READ OR ADJUDICATED; Einstein item excluded
Obj_0 = NOT IDENTIFIED WITH B0, U_BR, D_BR, A BID GENERATOR, OR A FLAG
git commands = NOT RUN under Q-91
baseline = NOT TOUCHED
deploy_status.sh = NOT RUN
gate = NOT RUN
```

No alpha, `kappa_record`, `kappa_Thomson`, coupling, scale, root, eigenvalue,
beta function, `E_R`, `T_R`, `k_R`, absolute interval, or measured-constant
comparison was computed or evaluated.

## 2. Bounded scope and word-boundaried correspondence check

### 2.1 Roots inspected

```text
R1 = /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
     alpha_fundamental_record_action_cleanroom_v003
R2 = /Users/bgm/Documents/New project/gravity_emergence_evidence_program
R3 = /Users/bgm/MB Work/alpha_supervision
R4 = /Users/bgm/MB Work/alpha-program-archive/workspace
```

R4 mirrors were used only to check correspondence and mirror presence, never as
independent evidence.

### 2.2 Exact controlling packet inspected

```text
R1/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md
R1/STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md
R1/STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md
R1/STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md
R1/STAGE8_GAMMA_K_ROOT_PRODUCER_ATTEMPT_F_RP1_V001.md
R1/STAGE8_GAMMA_K_QUASIFREE_ROOT_ROUTE_ATTEMPT_V001.md
R1/STAGE8_GAMMA_K_SOURCE_RECORD_COMPOSITION_TYPING_TESTS_V001.md
R1/STAGE8_GAMMA_K_COMPLETED_ALGEBRA_EXTENSION_ATTEMPT_V001.md
R1/STAGE8_P1_P7_CONSUMER_STRUCTURE_REQUIREMENT_AUDIT_V001.md
R1/STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md
R2/primitive_complete_boundary_transition_functional_principle_v002.md
R2/primitive_record_cell_selection_principle_v004.md
R3/QUESTIONS_SETTLED_REGISTER_V001.md
R4 mirrors of the R1 artifacts where present
```

### 2.3 Explicit exclusions

```text
a32_holdout/custodian_private/
.git/ and generated/binary material
relay 192 and the concurrent primitive-inventory result
S1/S2/S4 vacuity work
the current relay request as evidence
the current artifact
mirrors as independent evidence
surface resemblance, role names, flags, and downstream objects as identities
```

### 2.4 Queries and file lists

Exact-token searches used word boundaries for:

```text
Obj_0 | Sig_0 | MINIMAL_JOINT_P0_PRESENTATION
P0 | P1 | P2 | P3 | P4 | P5 | P6 | P7
object type | arity | domain | codomain | representation | map | flow |
functional | generator | provenance | origin | certificate | descent
S_CTP | U_BR | equivalent | equivalence
P0 = B0 | P0 is B0 | P0 = U_BR | P0 = D_BR | P0 = B_chi |
P0 is a flag, and reverse-direction identity forms
```

The exact `Obj_0|Sig_0|MINIMAL_JOINT_P0_PRESENTATION` search returned in R1:

```text
STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md
```

R3 returned the Q-95 register entry and relay snapshots.  R4 returned the mirror
of the same Q-95 artifact.  R2 returned no top-level qualifying source.  Relay
snapshots and mirrors did not qualify as independent constructions.

The broader semantic query returned the class, Q-90, and Q-95 artifacts in R1,
plus the register and mirrors.  The only pre-relay-191 mathematical `Sig_0` is
the Q-95 flat metadata tuple analyzed in Section 5.

```text
Obj_0_exact_mathematical_category_domain_codomain_found = false | TYPE-S |
  roots: R1-R4 and exact controlling packet above |
  excl: Section 2.3 |
  fences: semantic words operator/dynamics do not select a mathematical
  category; descendant P-row outputs and analogues are not Obj_0; mirrors are
  not independent evidence |
  query: word-boundaried Obj_0/P0 intersected with category, object type, arity,
  domain, codomain, representation, map, flow, functional, generator |
  raw_candidate_file_list:
    R1/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md
    R1/STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md
    R1/STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md
    R1/STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md
    R1/STAGE8_GAMMA_K_ROOT_PRODUCER_ATTEMPT_F_RP1_V001.md
    R1/STAGE8_P1_P7_CONSUMER_STRUCTURE_REQUIREMENT_AUDIT_V001.md
    R3/QUESTIONS_SETTLED_REGISTER_V001.md |
  qualifying_exact_physical_signature_file_list: EMPTY
```

This is bounded absence, not impossibility.

### 2.5 Non-identity check

The broader co-occurrence query returned the class phrase `B0-like` and prior
negative guards; the stricter direct-identity query returned no qualifying
identity. Q-90 already says at
`STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md:873-890`:

> `The class says B0-like, not P0 = B0.`

and reports a qualifying identity list of `EMPTY`.  Q-95 similarly requires a
future direction-bearing correspondence rather than identity at
`STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md:921-927`:

> `It may consume package-B0 information only after an independently derived, direction-bearing B0 -> P0 correspondence exists; the two objects are never identified by resemblance.`

```text
P0_identified_with_package_B0_found = false | TYPE-S |
  roots: exact controlling packet in Section 2.2 |
  excl: B0-like resemblance and prior negative sentences |
  fences: Q-69 exact object identity |
  query: direct same-line identity forms in both directions |
  broader_cooccurrence_file_list:
    R1/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md
    R1/STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md
    R1/STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md |
  qualifying_identity_file_list: EMPTY

P0_identified_with_U_BR_found = false | TYPE-S |
  roots: exact controlling packet in Section 2.2 plus
  R2/primitive_complete_boundary_transition_functional_principle_v002.md |
  excl: P6/evolution uses of U_BR, the functional's separate input role,
  analogies, and negative guards |
  fences: exact two-way identity sentence only |
  query: word-boundaried P0 within a direct identity form of U_BR, and reverse |
  raw_direct_identity_file_list: EMPTY |
  qualifying_identity_file_list: EMPTY

P0_identified_with_D_BR_found = false | TYPE-S |
  roots: exact controlling packet in Section 2.2 plus
  R1/BID_GRADED_BOUNDARY_SUPERCONNECTION_REPAIR_V001.md |
  excl: the historical D_BR signature at that source :102-118, downstream
  operator mentions, the concurrent primitive inventory, and analogies |
  fences: exact two-way identity sentence only; no concurrent theorem read |
  query: word-boundaried P0 within a direct identity form of D_BR, and reverse |
  raw_direct_identity_file_list: EMPTY |
  qualifying_identity_file_list: EMPTY

P0_identified_with_B_chi_or_BID_generator_found = false | TYPE-S |
  roots: exact controlling packet in Section 2.2 plus
  R1/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V001.md |
  excl: the historical B_chi candidate at that source :76-99, the uncompleted
  source-record-environment flag at :230-249, and analogies |
  fences: exact two-way identity sentence only |
  query: word-boundaried P0 within a direct identity form of B_chi or BID
  generator, and reverse |
  raw_direct_identity_file_list: EMPTY |
  qualifying_identity_file_list: EMPTY

P0_identified_with_a_discharge_flag = false | TYPE-R |
  test: Q69-FLAG/OBJECT-ROLE-SEPARATION; Q-95 defines Obj_0 and flags as
  distinct allowed/forbidden graph-node kinds at the Q-95 artifact :892-910
```

No alias is imported into the signature below.

## 3. What sealed text fixes about `Obj_0`

### 3.1 Physical class and semantic role

The class definition at
`STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md:34-39` is:

> `a completed source-record-field CTP algebraic carrier together with faithful source and record embeddings, a physical field/CTP quotient package, a positive normalized pre-state, admitted record effects and domains, source-record-field dynamics, and a raw contour-correlator output interface, all derived from one microscopic source-record-field operator/dynamics.`

P0 itself is only stated at `:229-233`:

> `P0. One microscopic source-record-field operator/dynamics B0-like source.`

Q-95 refines the semantic type at the register `:3896-3898`:

> `a joint source-record-field provenance/dynamics object that makes P1-P7's actual structures constructible.`

Therefore the sealed semantic type is:

```text
Obj_0_semantic_type = JOINT_SOURCE_RECORD_FIELD_PROVENANCE_DYNAMICS_ORIGIN
```

It is not a source that temporally creates the antecedent carrier.  Q-43's
executed typing result says at
`STAGE8_GAMMA_K_SOURCE_RECORD_COMPOSITION_TYPING_TESTS_V001.md:167-175`:

> `record degrees exist as labeled record factors with ready states before a particular write`

and:

> `a public record is formed only after the source-controlled/write-and-persist structure succeeds.`

At `:316-327` it further says:

> `They are not copies of the source algebra and are not generated solely by source algebra automorphisms.`

Thus an origin witness for P2 or P3 traces the embedding/coherence map into the
joint package; it must not assert temporal creation of the already derived
source or record sector.

### 3.2 What sealed text does not fix

The Q-95 artifact states at
`STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md:995-997`:

> `P0_exact_mathematical_signature_derived = false | TYPE-U | would-build: select and justify the actual object type, arity, domain, codomain, parameter inventory, and representation data from sealed upstream physics`

The closest analogous B0 audit warns at
`STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md:938-969` that
the text does not choose a representation and that a candidate could be a flow,
family, relation, or algebraic generator rather than one Hilbert-valued arrow.
That warning is not a P0/B0 identity and transfers no B0 type.  It shows why a
linear-operator choice for P0 would be an extra premise.

The strongest failed producer attempt explains the missing ground at
`STAGE8_GAMMA_K_ROOT_PRODUCER_ATTEMPT_F_RP1_V001.md:116-126`:

> `the declarations still lack definite constructive content`

and at `:130-160`:

> `These lines state the type of the desired carrier and contractions. They do not give the states, inner product, completed domain, or quotient map.`

Consequently an object in an exact physical category cannot be selected from
sealed text in this turn.

```text
Obj_0_is_a_linear_operator = NO_VERDICT |
  deciding evidence: selected exact PhysicalSig_0
Obj_0_is_a_functional = NO_VERDICT |
  deciding evidence: selected exact PhysicalSig_0
Obj_0_is_a_flow_or_evolution = NO_VERDICT |
  deciding evidence: selected exact PhysicalSig_0
Obj_0_is_an_algebraic_generator = NO_VERDICT |
  deciding evidence: selected exact PhysicalSig_0
```

These are `NO_VERDICT`, not negative claims: the category selection is missing.

## 4. Q-94 structure boundary

Q-94 rules at the register `:3847-3864`:

> `STRUCTURE. ALL SEVEN. ZERO PURE-EXISTENCE ROWS.`

and:

> `EACH LINK ... CONSUMES ACTUAL OR CANONICAL STRUCTURES, NOT BARE EXISTENCE.`

The consumer audit's row verdicts at
`STAGE8_P1_P7_CONSUMER_STRUCTURE_REQUIREMENT_AUDIT_V001.md:309-330` identify the
actual maps, domains, state, effects, quotient, measure, dynamics, and raw
interface each row needs.  A Boolean `exists(P_i)`, a role name, or an opaque
certificate cannot satisfy a port.

```text
bare_Obj_0_existence_supplies_P1_through_P7 = false | TYPE-R |
  test: Q94-CONSUMER-STRUCTURE-COVERAGE
flat_metadata_tuple_supplies_P1_through_P7 = false | TYPE-R |
  test: Q94-ACCESSOR-SURFACE; the tuple exposes no constructor or canonical
  accessor for any required P-row structure
```

## 5. Refutation of the inherited flat `Sig_0`

The Q-95 presentation lists at
`STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md:849-864`:

> `Obj_0, Sig_0, Core_0, ConstructionRecord_0, PortDependencyGraph_0, AttemptPort_1, ... AttemptPort_7, CommonOriginCertificate_0`

as sibling fields.  It then defines `Sig_0` separately at `:866-874` as:

> `object_type, arity, domain, codomain, parameter_inventory, representation_data_if_required`

The certificate is only assembled later at `:913-918`:

> `CommonOriginCertificate_0 := aggregate_by_reference(P0_origin_provenance_witness_1,...,P0_origin_provenance_witness_7,PortDependencyGraph_0)`

An adversarial package can therefore keep the same flat metadata while replacing
one sibling port output with a same-typed externally sourced constant.  Nothing
inside the flat `Sig_0` rejects that substitution.  This is an executed
field-dependency counterexample to *signature enforcement*.  It does not say the
outer presentation's intended tests can never detect it.

```text
relay189_flat_Sig_0_forces_CommonOriginCertificate_0 = false | TYPE-R |
  test: DECLARED-FIELD-DEPENDENCY-CHECK; certificate and per-port witnesses are
  not fields, dependent codomains, or invariants of Sig_0

relay189_flat_Sig_0_rejects_same_typed_independent_origin_substitution = false | TYPE-R |
  test: EXTERNAL-CONSTANT-SUBSTITUTION; substitute one same-typed sibling port
  while holding the metadata tuple fixed; the tuple's predicates do not change
```

Q-90 had already isolated the same residue at
`STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md:490-509`:

> `No explicit provenance-certificate output or descent map for P1, P4, or P7 is listed in P0-P7 or T1-T7.`

and left sufficiency at `NO_VERDICT`.  The present field-dependency test narrows
that residue: the **flat signature** is insufficient.  Physical common origin
remains open.

## 6. Formal schema proposal and Q-80 status

The proposed schema has two levels:

1. one exact physical signature for `Obj_0`; and
2. port-operation, trace-generation, graph, coherence, and certificate schemas
   parameterized by an eventual `Obj_0` instance and its construction record.

The inherited flat **metadata tuple** fails Section 5's substitution test. That
does not prove that every flat record plus an independently validated relational
invariant must fail. Therefore Q-80 does not yet authorize a required new class:

```text
Q80_new_formal_class_required = NO_VERDICT |
  deciding evidence: a comparison test showing every flat presentation with
  independently validated relational invariants fails common-origin enforcement

CERTIFIED_PROVENANCE_INDEXED_DEPENDENT_PRODUCER_SIGNATURE =
  DECLARED_FORMAL_REQUIREMENT_SCHEMA, NOT A Q80 CLASS RULING
```

The Q-95 artifact says at `:798-809` that
`MINIMAL_JOINT_P0_PRESENTATION` is not a new physical class. This artifact does
not supersede that determination.

A dependent sum is written `Sigma(x:X).Y(x)`: an inhabitant contains one `x`
and one value whose type explicitly refers to that same `x`. A refinement
attaches a testable predicate or witness to a value. These imported devices are
used here as one formalization of mandatory co-reference, not as the only
possible formalization.

They can force **carriage** of a witness. They cannot make an uninterpreted or
self-asserted witness true. Truth needs fixed origin semantics, a replayable
construction trace, hostile different-origin controls, and an independent
acceptance oracle.

```text
dependent_schema_forces_origin_witness_field_presence = true [SCHEMA FACT]
dependent_schema_alone_proves_physical_origin_truth = false | TYPE-R |
  test: FORGED-WITNESS-CONTROL; an uninterpreted witness term can occupy a field
  unless independent semantics and validation reject it
physical_common_origin_of_P1_through_P7 = NO_VERDICT |
  deciding evidence: an independently validated executed bundle from one
  constructed Obj_0, or a failure-capable origin countermodel
```

## 7. Premise pointer

All sealed inputs and declared design premises were stated before the first
proposed formalism in Section 0. No additional premise is introduced here.

## 8. Proposed conditional signature requirement schema

### 8.1 Separate the physical signature from the presentation signature

The unresolved physical record is:

```text
PhysicalSig_0 :=
  (
    PhysicalCategory_0,
    PhysicalObjectType_0,
    PhysicalArityAndOperations_0,
    PhysicalOperationDomains_0,
    PhysicalOperationCodomains_0,
    PhysicalParameterRecord_0,
    PhysicalSupportAndCommonCore_0,
    PhysicalRepresentationData_0,
    PhysicalAlgebraicCausalAnalyticConditions_0
  )
```

No entry is assigned a physical value here.  In particular, this schema does
not use a union such as `operator OR functional OR flow OR generator`; such a
union would postpone rather than specify the exact type.

```text
PhysicalSig_0_selected = false | TYPE-U | would-build:
  derive and freeze one justified physical category and every field above from
  a sufficient upstream primitive inventory without descendant data
```

Given a future fixed `PhysicalSig_0`, let:

```text
ObjType_0[I_prim] :=
  objects satisfying PhysicalSig_0 over frozen primitive context I_prim
```

Then `Obj_0` is typed as:

```text
Obj_0 : ObjType_0[I_prim]
```

An object of an arbitrary physical category need not itself have one function
domain and codomain. Its internal operation domains/codomains are exactly the
fields of `PhysicalSig_0`; those fields remain TYPE-U. The construction and
port-arrow **requirements** below state the fields that must be frozen; they do
not become exact arrows until `ExactSig_0` exists.

### 8.2 Signature-freeze domain and codomain

The exact-signature freeze is distinct from constructing an inhabitant:

```text
SignatureFreezeInputs_0 :=
  (
    PhysicalSig_0,
    FrozenPrimitiveBindingSchema_0,
    FrozenDerivedSectorReferenceTypeSchema_0,
    FrozenPortContractSchemas_1_to_7,
    FrozenCrossRowCoherenceSchema_0,
    FrozenPortDependencyGraphSchema_0,
    FrozenOriginPredicateAndTraceSchema_0,
    FrozenCertificateSchema_0,
    DomainEquivalence_1_if_used,
    EvolutionEquivalence_6
  )

FreezeExactSig_0 :
  SignatureFreezeInputs_0
    -> Result(ExactSig_0, SignatureFreezeFailure_0)
```

`ExactSig_0` is the frozen two-level type/interface record. It contains no
physical `Obj_0` inhabitant and no executed `c1...c7` output. Its static
exactness and closure tests are separate from later construction tests.

### 8.3 Later `Obj_0` instance and port-execution objects

Q-97 at the register `:3976-3979` makes a type distinction that the first draft
of this section did not implement:

> `THE SOURCE GNS, THE RECORD DIRECT LIMIT, AND THE BASE TENSOR JOIN ARE REAL BUT DERIVED/SECTORAL — NOT PRIMITIVE INPUTS.`

Accordingly there are two contexts with disjoint roles:

```text
AdmissiblePrimitiveContext_0[ExactSig_0] :=
  I_prim satisfying PRIMITIVE_INTERFACE_REQUIREMENT in Section 10

FrozenDerivedSectorReferenceContext_0[ExactSig_0] :=
  S_sector satisfying DERIVED_SECTOR_REFERENCE_REQUIREMENT in Section 10
```

Only `I_prim` is visible to the `Obj_0` constructor.  The actual
`S_sector` values are not constructor inputs:

```text
Obj0PortInterfaceInstance_0[I_prim] :=
  Sigma(o : ObjType_0[I_prim]).
  Sigma(r : ConstructionRecord_0(o,I_prim)).
  Sigma(g : PortDependencyGraph_0(o,r)).
  (
    AttemptPort_1[o,r,g] : ExactPortContract_1,
    AttemptPort_2[o,r,g] : ExactPortContract_2,
    AttemptPort_3[o,r,g] : ExactPortContract_3,
    AttemptPort_4[o,r,g] : ExactPortContract_4,
    AttemptPort_5[o,r,g] : ExactPortContract_5,
    AttemptPort_6[o,r,g] : ExactPortContract_6,
    AttemptPort_7[o,r,g] : ExactPortContract_7,
    CrossRowCoherenceConstructor_0(o,r,g),
    CommonOriginCertificateAssembler_0(o,r,g)
  )

ConstructObj0Instance_0 :
  (
    ExactSig_0,
    I_prim : AdmissiblePrimitiveContext_0[ExactSig_0],
    TargetIndependentObj0ConstructionRule_0,
    OriginTraceGenerationRule_0
  )
    -> Result(Obj0PortInterfaceInstance_0[I_prim],
              Obj0ConstructionFailure_0)
```

Only later port execution can read the independently derived reference
context.  Primitive and sector-reference subcontexts are separately bound, and
prior `c_j` values are explicit port arguments rather than members of either
context:

```text
I_i : AllowedPrimitivePortContext_i(I_prim,r,g)
p_i : FrozenPrimitiveSubcontextWitness_i(I_prim,I_i,r,g)

S_i : AllowedDerivedSectorReferenceContext_i(S_sector)
d_i : FrozenDerivedSectorSubcontextWitness_i(S_sector,S_i)
```

The exact port contract must freeze whether `S_i` is empty and, if nonempty,
which already-derived reference sorts it may expose.  It cannot add a sector
reference after seeing a candidate.  No free `I_1...I_7` or `S_1...S_7` is
admitted. A successful port result has the form:

```text
WitnessedResult_i(o,r,g,I_i,p_i,S_i,d_i) :=
  Result(
    Sigma(c_i : P_i_Structure(...)).
      OriginTrace_i(o,r,g,I_i,p_i,S_i,d_i,c_i),
    PortFailure_i
  )
```

An origin trace certifies the jointly constructed port result. It does not
claim that `Obj_0` constructed a referenced antecedent sector object.  The
certificate assembler is typed by the actual record, graph, contexts, results,
and traces rather than by sibling labels:

```text
CommonOriginCertificateAssembler_0(o,r,g) :
  (I_1,p_1,S_1,d_1,c1,t1,...,I_7,p_7,S_7,d_7,c7,t7,
   q : CrossRowCoherence_0(c1,...,c7)) ->
  CommonOriginCertificate_0(
    o,r,g,I_1,p_1,S_1,d_1,c1,t1,...,
    I_7,p_7,S_7,d_7,c7,t7,q)
```

Actual P-row candidate structures exist only in the downstream execution
object.  Its dependent order puts P5 before completed P2 because P2's sealed
restricted-state clause consumes `rho_pre`:

```text
ExecutedPortBundle_0(
  o,r,g,I_prim,S_sector;
  I_1,p_1,S_1,d_1,...,I_7,p_7,S_7,d_7) :=
  Sigma(c1 : P1Structure(...)).
  Sigma(t1 : OriginTrace_1(o,r,g,I_1,p_1,S_1,d_1,c1)).
  Sigma(c3 : P3Structure(...)).
  Sigma(t3 : OriginTrace_3(o,r,g,I_3,p_3,S_3,d_3,c3)).
  Sigma(c4 : P4Structure(...)).
  Sigma(t4 : OriginTrace_4(o,r,g,I_4,p_4,S_4,d_4,c4)).
  Sigma(c5 : P5Structure(...)).
  Sigma(t5 : OriginTrace_5(o,r,g,I_5,p_5,S_5,d_5,c5)).
  Sigma(c2 : P2Structure(...,c5.rho_pre,...)).
  Sigma(t2 : OriginTrace_2(o,r,g,I_2,p_2,S_2,d_2,c2)).
  Sigma(c6 : P6Structure(...)).
  Sigma(t6 : OriginTrace_6(o,r,g,I_6,p_6,S_6,d_6,c6)).
  Sigma(c7 : P7Structure(...)).
  Sigma(t7 : OriginTrace_7(o,r,g,I_7,p_7,S_7,d_7,c7)).
  Sigma(q : CrossRowCoherence_0(c1,...,c7)).
  CommonOriginCertificate_0(
    o,r,g,I_1,p_1,S_1,d_1,c1,t1,...,
    I_7,p_7,S_7,d_7,c7,t7,q)

ExecutePorts_0 :
  (
    Obj0PortInterfaceInstance_0[I_prim],
    S_sector : FrozenDerivedSectorReferenceContext_0[ExactSig_0],
    FrozenPrimitivePortSubcontextsAndWitnesses_1_to_7,
    FrozenDerivedSectorSubcontextsAndWitnesses_1_to_7
  )
    -> Result(
         ExecutedPortBundle_0(
           o,r,g,I_prim,S_sector;
           I_1,p_1,S_1,d_1,...,I_7,p_7,S_7,d_7),
         PortExecutionFailure_0)

ValidateObj0Instance_0 :
  (Obj0PortInterfaceInstance_0[I_prim], ExecutedPortBundle_0,
   IsolatedValidationPackage_0)
    -> WIRED_INSTANCE_PORT_AND_ORIGIN_VERDICTS
```

`Result(success,failure)` is imported error-explicit notation. It does not make
a constructor or successful port exist. The nonempty-fixture tests in Section
11 reject an instance whose ports always return failure.

The validation package is unreadable by `FreezeExactSig_0`,
`ConstructObj0Instance_0`, and the port operations. For nonvacuity testing, the
validator may invoke a frozen port operation on a typed input drawn from its
isolated fixture package; the operation receives that domain value, never the
fixture label, expected answer, owner state, or verdict channel.

### 8.4 This is not an Obj_0/descendant cycle

The Q-95 artifact distinguishes construction outputs and P-row descendants at
`:716-725` and says:

> `Treating any row in this table as a preconstruction input would make the graph circular.`

Here `FreezeExactSig_0` constructs only a type/interface record.
`ConstructObj0Instance_0` constructs `Obj_0`, its record, graph, and port
operations. P1-P7 candidate structures occur only in `ExecutedPortBundle_0`
after those operations run. No P-row flag or candidate is an input to the
construction of `Obj_0` itself.

```text
proposed_schema_identifies_Obj_0_with_any_P_row = false | TYPE-R |
  test: INTERFACE/EXECUTED-OUTPUT-ROLE-SEPARATION; Obj_0 is an instance
  projection, while c1...c7 occur only in the later execution result
proposed_schema_consumes_a_P_row_flag = false | TYPE-R |
  test: DOMAIN-FIELD-INSPECTION; flags occur in neither construction nor port
  input domains
proposed_schema_allows_free_external_I_i_or_S_i = false | TYPE-R |
  test: PORT-SUBCONTEXT-BINDING; every I_i and S_i carries its separate frozen
  subcontext witness to I_prim or S_sector, and prior c_j values are explicit
  arguments rather than context members
```

## 9. Seven port contracts

The sealed rows are verbatim at
`STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md:235-262`.  The consumer
audit confirms the corresponding structure requirements at
`STAGE8_P1_P7_CONSUMER_STRUCTURE_REQUIREMENT_AUDIT_V001.md:311-330`.

### 9.1 Port 1 — completed carrier and domain

Sealed P1 says at the class spec `:235-236`:

> `A completed source-record-field carrier/algebra A_SRF_CTP with a common dense domain or equivalent domain object.`

The same six-file exactness packet used for P6 contains three repetitions of
`equivalent domain object` and no definition of its equivalence predicate:

```text
P1_domain_equivalence_relation_found = false | TYPE-S |
  roots:
    R1/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md
    R1/STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md
    R1/STAGE8_P1_P7_CONSUMER_STRUCTURE_REQUIREMENT_AUDIT_V001.md
    R1/STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md
    R1/STAGE8_CTP_PHYS_INPUT_PACKAGE_TRIAGE_AND_LOAD_BEARING_FUNCTIONAL_SPEC_V001.md
    R1/STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md |
  excl: repetitions of the P1 phrase and unrelated equivalence relations |
  fences: exact common-domain/equivalent-domain predicate for P1 |
  query: word-boundaried equivalent domain, domain equivalence, and local
  equivalence/domain forms |
  raw_hit_file_list:
    R1/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md
    R1/STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md
    R1/STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md |
  qualifying_definition_file_list: EMPTY
```

Proposed port signature:

```text
DomainKind_1 := COMMON_DENSE | PROVED_EQUIVALENT

AttemptPort_1(
  o,r,g,I_1,p_1,S_1,d_1;
  DomainKind_1,DomainEquivalence_1_if_used) ->
  Result(
    Sigma(c1 : CarrierPackage_1).
      OriginTrace_1(o,r,g,I_1,p_1,S_1,d_1,c1),
    PortFailure_1
  )

CarrierPackage_1 :=
  (A_SRF_CTP, exact_common_domain_object, domain_kind : DomainKind_1,
   if domain_kind=PROVED_EQUIVALENT then DomainEquivalence_1_witness,
   canonical_carrier_domain_accessors)
```

The port must supply the actual/canonical package, not an existence bit.  The
word `equivalent` in P1 does not authorize an arbitrary domain. The
`PROVED_EQUIVALENT` branch cannot be frozen until its exact relation and witness
type exist; this artifact does not choose a branch.

### 9.2 Port 2 — source embedding and restriction coherence

Sealed P2 at `:238-240` says:

> `A faithful source embedding i_src: A_src -> A_SRF_CTP, where A_src is the Q-41 source CAR algebra, and the restricted state reproduces the Q-41 source quasifree covariance/GNS data.`

The first draft placed P2 before P5 and deferred the required state-restriction
witness. That did not supply the sealed P2 structure. The completed P2 contract
must execute after P5 and consume `c5.rho_pre`; this is a specification repair,
not a physical derivation:

```text
AttemptPort_2(o,r,g,I_2,p_2,S_2,d_2,c1,c5) ->
  Result(
    Sigma(c2 : SourceEmbeddingPackage_2(o,c1,c5,S_2)).
      OriginTrace_2(o,r,g,I_2,p_2,S_2,d_2,c2),
    PortFailure_2
  )

SourceEmbeddingPackage_2(o,c1,c5,S_2) :=
  (
    i_src : A_src[S_2] -> c1.A_SRF_CTP,
    faithfulness_witness,
    CAR_preservation_witness,
    source_state_restriction_accessor(c5.rho_pre,i_src),
    Q41_covariance_GNS_reference_accessor(S_2),
    Q41_restricted_state_covariance_GNS_coherence_witness(
      c5.rho_pre,i_src,S_2)
  )
```

`S_2` supplies only the already-derived Q-41 reference endpoint and its frozen
provenance; it is not relabelled as a primitive and is not an `Obj_0`
construction input. The origin trace covers the jointly constructed embedding,
restriction accessor, and coherence witness. It does not claim `Obj_0` creates
the antecedent Q-41 source algebra or GNS data.

### 9.3 Port 3 — record embedding and formation-compatible coherence

Sealed P3 at `:242-245` says:

> `A completed record embedding compatible with the Q-42 direct-limit record algebra and the Q-43 stable dressed outgoing-record monomorphism.`

Proposed port signature:

```text
AttemptPort_3(o,r,g,I_3,p_3,S_3,d_3,c1) ->
  Result(
    Sigma(c3 : RecordEmbeddingPackage_3(o,c1,S_3)).
      OriginTrace_3(o,r,g,I_3,p_3,S_3,d_3,c3),
    PortFailure_3
  )

RecordEmbeddingPackage_3(o,c1,S_3) :=
  (
    i_rec : RecordSector[S_3] -> c1.A_SRF_CTP,
    Q42_direct_limit_reference_accessor(S_3),
    Q43_dressed_monomorphism_reference_accessor(S_3),
    base_tensor_join_reference_accessor(S_3),
    Q42_direct_limit_compatibility_witness(i_rec,S_3),
    Q43_dressed_monomorphism_compatibility_witness(i_rec,S_3),
    base_tensor_carrier_compatibility_witness(i_rec,S_3)
  )
```

The reference endpoints in `S_3` are independently derived/sectoral under
Q-97. The origin trace is on the jointly constructed embedding and coherence
maps, not on the temporal creation of the antecedent record carrier or on the
derivation of the referenced direct limit/base join. This is required by
Q-43's quoted formation order in Section 3.1.

### 9.4 Port 4 — physical field/CTP package

Sealed P4 at `:247-249` says:

> `branch embeddings, compound index ordering, branch metric, CTP reality/Hermiticity involution, physical source maps, gauge-fixed physical quotient, and invariant contour/spacetime measure.`

Proposed port signature:

```text
AttemptPort_4(o,r,g,I_4,p_4,S_4,d_4,c1) ->
  Result(
    Sigma(c4 : PhysicalCTPPackage_4(o,c1)).
      OriginTrace_4(o,r,g,I_4,p_4,S_4,d_4,c4),
    PortFailure_4
  )

PhysicalCTPPackage_4 :=
  (branch_embeddings, compound_index_order, branch_metric,
   reality_Hermiticity_involution, physical_source_maps,
   gauge_fixed_physical_quotient, invariant_contour_spacetime_measure)
```

No downstream retarded Hessian, response scalar, or value is added to this
codomain.

### 9.5 Port 5 — state, effects, domains, and origin

Sealed P5 at `:251-253` says:

> `A positive normalized rho_pre on the completed object and admitted record effects E_r, with domains, all supplied by the same microscopic source as P0.`

The Q-77-unadjudicated root text, read here only as noncontrolling
correspondence evidence at
`../primitive_complete_boundary_transition_functional_principle_v002.md:106-113`
says:

> `U_BR, rho_pre, every admitted record effect, and their domains follow from one complete microscopic operator`

This read-only quote licenses no schema field beyond sealed P5 and is not used
as construction ground.

The Q-95 artifact's prior Port 5 contract at `:1040-1067` correctly separates
frozen outcome labels/admissibility criteria from effect operators and domains,
which must be outputs.

Proposed port signature:

```text
FamilyKind_5 := EXHAUSTIVE | NONEXHAUSTIVE

family_kind_5 := I_5.family_kind_5 : FamilyKind_5
  frozen before AttemptPort_5 executes

AttemptPort_5(
  o,r,g,I_5,p_5,S_5,d_5,c1,c3,c4) ->
  Result(
    Sigma(c5 : StateEffectPackage_5(
      o,c1,c3,c4;family_kind_5)).
      OriginTrace_5(o,r,g,I_5,p_5,S_5,d_5,c5),
    PortFailure_5
  )

I_5 may contain only frozen outcome labels and target-independent effect
admissibility criteria, the pre-execution `family_kind_5`, and—when the kind is
`NONEXHAUSTIVE`—its exact target-independent nonexhaustive criterion. It may
never contain effect operators or effect domains.

StateEffectPackage_5(o,c1,c3,c4;family_kind_5) :=
  (rho_pre, admitted_E_r_family, state_domain, effect_domains,
   operator_domains, positivity_normalization_domain_contract,
   completeness_witness :
     if family_kind_5=EXHAUSTIVE then ExactCompletenessWitness
     else ExactFrozenNonexhaustiveCriterionWitness(I_5))
```

The returned package cannot select `NONEXHAUSTIVE` after inspecting its effect
family. Any mismatch between the frozen kind/criterion and the output fails
`SIG-T4.5` and `SIG-T11` under their dedicated owners.

The actual physical origin of this package remains `NO_VERDICT`; the signature
requires a trace but cannot validate it without Section 11's oracle.

### 9.6 Port 6 — dynamics and normalization; exactness stop

Sealed P6 at the class spec `:255-257` says:

> `Source-record-field dynamics: S_CTP, or an equivalent evolution such as U_BR[A,g], acting on the common domain and supplying the normalized inclusive CTP amplitude at zero sources.`

A word-boundaried inspection of the exact six-file P6 packet found repetitions
of `equivalent evolution` but no definition of the equivalence relation:

```text
P6_action_evolution_equivalence_relation_found = false | TYPE-S |
  roots:
    R1/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md
    R1/STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md
    R1/STAGE8_P1_P7_CONSUMER_STRUCTURE_REQUIREMENT_AUDIT_V001.md
    R1/STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md
    R1/STAGE8_CTP_PHYS_INPUT_PACKAGE_TRIAGE_AND_LOAD_BEARING_FUNCTIONAL_SPEC_V001.md
    R1/STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md |
  excl: repetitions of the phrase equivalent evolution, unrelated equivalence
  relations, and tests that presuppose rather than define it |
  fences: exact S_CTP/U_BR action-evolution equivalence predicate |
  query: word-boundaried S_CTP or U_BR in the same local context as equivalent,
  equivalence, or correspondence |
  raw_hit_file_list:
    R1/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md
    R1/STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md
    R1/STAGE8_P1_P7_CONSUMER_STRUCTURE_REQUIREMENT_AUDIT_V001.md
    R1/STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md |
  qualifying_definition_file_list: EMPTY
```

Therefore the conditional Port 6 requirement carries an unresolved frozen
parameter that must be discharged before exact freezing:

```text
EvolutionEquivalence_6 := EXACT RELATION TO BE DERIVED AND FROZEN

AttemptPort_6(
  o,r,g,I_6,p_6,S_6,d_6,c1,c4,c5;
  EvolutionEquivalence_6) ->
  Result(
    Sigma(c6 : DynamicsPackage_6(o,c1,c4,c5;EvolutionEquivalence_6)).
      OriginTrace_6(o,r,g,I_6,p_6,S_6,d_6,c6),
    PortFailure_6
  )

DynamicsPackage_6 :=
  (chosen_dynamics_form, action_on_common_domain,
   normalized_zero_source_inclusive_amplitude_interface,
   S_CTP_identity_or_proved_Equivalence_6_witness)
```

This is a schema, not an exact physical port, until
`EvolutionEquivalence_6` exists.  The signature is not widened to accept an
undefined `or equivalent` branch, and this artifact does not choose between
forms.

### 9.7 Port 7 — contacts, sources, and raw correlators

Sealed P7 at the class spec `:259-262` says:

> `Contact/source rules and raw contour-correlator output interface sufficient for later response-extraction tests to ask their own questions.`

and expressly says the producer does not compute the physical kernel, scalar,
or value.  The downstream raw-map domain at
`STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md:641-670` requires
the actual raw `G`, compound indices, branch metric, measure, involution,
physical quotient, inverse/contact/boundary domains, and source normalization.

Proposed port signature:

```text
AttemptPort_7(o,r,g,I_7,p_7,S_7,d_7,c1,c4,c5,c6) ->
  Result(
    Sigma(c7 : RawInterfacePackage_7(o,c1,c4,c5,c6)).
      OriginTrace_7(o,r,g,I_7,p_7,S_7,d_7,c7),
    PortFailure_7
  )

RawInterfacePackage_7 :=
  (contact_rules, source_rules, raw_contour_correlator_evaluator,
   raw_correlator_domain_contact_metadata,
   source_convention_and_normalization_accessors)
```

The codomain excludes the raw-to-retarded-Hessian map and every later response
object.

### 9.8 Cross-row coherence and dependency graph

The bundle, rather than any widened individual port, carries:

```text
CrossRowCoherence_0 :=
  (
    c2.Q41_restricted_state_covariance_GNS_coherence_witness_matches_c5,
    P2_source_embedding_agrees_with_P4_physical_source_maps(c2,c4),
    P3_embedding_commutes_with_Q42_Q43_reference_maps,
    P4_P5_P6_P7_domains_reference_the_same_P1_domain,
    P6_zero_source_amplitude_uses_P5_state(c5,c6),
    P4_P6_P7_source_and_CTP_conventions_agree,
    P7_raw_evaluator_uses_the_P5_state_and_P6_dynamics,
    no_post_output_external_supplement
  )
```

Proposed direction-bearing production/consumption order:

```text
(Obj_0,r,g,I_1,p_1,S_1,d_1) -> AttemptPort_1 -> (c1,t1)
(Obj_0,r,g,I_3,p_3,S_3,d_3,c1) -> AttemptPort_3 -> (c3,t3)
(Obj_0,r,g,I_4,p_4,S_4,d_4,c1) -> AttemptPort_4 -> (c4,t4)
(Obj_0,r,g,I_5,p_5,S_5,d_5,c1,c3,c4)
  -> AttemptPort_5 -> (c5,t5)
(Obj_0,r,g,I_2,p_2,S_2,d_2,c1,c5) -> AttemptPort_2 -> (c2,t2)
(Obj_0,r,g,I_6,p_6,S_6,d_6,c1,c4,c5) -> AttemptPort_6 -> (c6,t6)
(Obj_0,r,g,I_7,p_7,S_7,d_7,c1,c4,c5,c6) -> AttemptPort_7 -> (c7,t7)

(c2,c5,S_2) -> completed P2 restricted-state/Q41 coherence check
(c2,c4) -> P2/P4 source-map coherence
(c3,S_3) -> P3/Q42/Q43/base-tensor compatibility coherence
(c1,c4,c5,c6,c7) -> common-domain/source/normalization coherence

(Obj_0,r,g,I_i,p_i,S_i,d_i,c_i,t_i for every i,CrossRowCoherence_0)
  -> CommonOriginCertificateAssembler_0
  -> CommonOriginCertificate_0
```

This order is a declared testable schema, not a derived physical dependency
theorem.  It keeps all descendant outputs out of the construction domain of
`Obj_0`. The edge kinds above must be present in any frozen graph; graph
completeness remains TYPE-U and no claim is made that this prose list has
derived every physical edge.

```text
FrozenPortDependencyGraph_0_derived = false | TYPE-U | would-build:
  freeze the exact graph from the selected physical signature and primitive
  inventory, then pass completeness, orientation, and acyclicity tests
```

### 9.9 Port supply disposition

```text
all_seven_sealed_codomain_requirements_enumerated = true [PROCESS FACT]
all_seven_port_contracts_schematically_well_typed = NO_VERDICT |
  deciding evidence: a frozen PhysicalSig_0, separately bound I_i and S_i
  subcontexts, exact P1/P6 equivalence when used, frozen dependency graph, and
  successful static type/closure test
all_seven_ports_jointly_physically_inhabitable = NO_VERDICT |
  deciding evidence: one Obj_0 instance whose seven ports return successful
  structures on independent nonempty fixtures, or a joint incompatibility proof

port_specific_signature_impossibility_theorem_found = false | TYPE-S |
  roots:
    R1/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md
    R1/STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md
    R1/STAGE8_P1_P7_CONSUMER_STRUCTURE_REQUIREMENT_AUDIT_V001.md
    R1/STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md |
  excl: TYPE-U construction stops, role statements, structure requirements, and
  statements that expressly disclaim future impossibility |
  fences: a theorem/countermodel that every fixed signature fails a named port |
  query: word-boundaried P1-P7/port in local context with impossible,
  impossibility, no-go, unbuildable, or cannot supply |
  raw_hit_file_list:
    R1/STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md |
  qualifying_impossibility_file_list: EMPTY
```

## 10. What the signature requires of the primitive inventory

This section states a checkable interface requirement.  It does **not**
enumerate the separately owned inventory or prejudge its result.

```text
PRIMITIVE_INTERFACE_REQUIREMENT(I_prim,PhysicalSig_0) :=
  frozen_before_any_target_or_candidate_output(I_prim)
  AND authority_path_and_content_hash_recorded_for_every_entry(I_prim)
  AND exact_cover_of_every_free_primitive_ground_sort_operation_parameter_domain(
        I_prim, PhysicalSig_0, AttemptPort_1_to_7)
  AND unique_unambiguous_type_and_domain_assignment(I_prim)
  AND closure_of_every_declared_primitive_constructor_composition(I_prim)
  AND upstream_only_acyclic_dependency(
        I_prim -> Obj_0 -> port_interfaces -> P_candidates)
  AND Obj_0_construction_record_terminates_only_in_entries_of(I_prim)
  AND no_independently_derived_sector_reference_is_reclassified_as_primitive(
        I_prim)
  AND no_P1_to_P7_output_or_discharge_flag_is_an_input(I_prim)
  AND no_downstream_response_target_or_validation_fixture_is_an_input(I_prim)
  AND no_unproved_B0_U_BR_D_BR_BID_identity_is_used(I_prim)
  AND no_optional_catch_all_or_post_output_supplement_is_permitted(I_prim)

DERIVED_SECTOR_REFERENCE_REQUIREMENT(S_sector,ExactSig_0) :=
  frozen_before_port_execution_and_candidate_output(S_sector)
  AND authority_path_content_hash_and_derivation_status_recorded_for_every_entry(
        S_sector)
  AND exact_cover_only_of_reference_sorts_named_by_frozen_port_contracts(
        S_sector, ExactSig_0)
  AND every_entry_is_an_independently_derived_sector_reference_not_a_primitive(
        S_sector)
  AND no_entry_is_visible_to_ConstructObj0Instance_0(S_sector)
  AND no_entry_is_a_P1_to_P7_candidate_or_discharge_flag(S_sector)
  AND no_entry_is_a_target_or_validation_fixture(S_sector)
  AND every_S_i_has_a_frozen_exact_subcontext_witness(S_sector,S_i)
  AND no_post_output_reference_addition_or_alias_substitution(S_sector)
```

In prose, the primitive inventory must provide an explicit frozen authoritative
binding for every free **primitive** ground type, operation, parameter,
support/domain object, and compatibility datum called by the selected physical
signature and seven port schemas. Multiplicity or alternatives may occur only
when the selected exact signature fixes their type and admissible multiplicity;
no binding may remain unresolved or target-selected. The bindings must suffice
to type and replay every primitive-side construction.

Q-97's split at the register `:3976-3979` is binding: derived sector objects are
real, but they are not primitive inputs. They therefore enter only through the
separate frozen `S_sector` reference context at port execution. P2/P3 traces
terminate on the jointly produced embedding/coherence maps and list the
external reference endpoints; they do not reassign the endpoints' provenance
to `Obj_0`. Neither context may hide prior `c_j` outputs; those are explicit
dependent port arguments.

`I_prim` must not contain P1-P7 outputs as independent primitives. For P5,
frozen labels, a target-independent admissibility criterion, the pre-execution
`family_kind_5`, and any exact frozen nonexhaustive criterion may be ground
inputs, but effect operators and domains remain descended outputs, as the Q-95
artifact says at `:1040-1067`.

```text
current_prefreeze_inventory_satisfies_PRIMITIVE_INTERFACE_REQUIREMENT = false | TYPE-C |
  constraint: Q-97, the available object is a too-thin prefreeze audit rather
  than a frozen inventory sufficient for Obj_0-kind construction |
  release: a frozen inventory with explicit bindings that passes the exact
  free-symbol/domain/provenance coverage audit against one selected
  PhysicalSig_0 and frozen seven-port schema

current_derived_sector_reference_context_frozen_for_ExactSig_0 = false | TYPE-C |
  constraint: no selected ExactSig_0 or frozen port-specific sector-reference
  type/subcontext schema exists; Q-97 establishes real sector objects but does
  not package them as this execution context |
  release: an independently sealed S_sector with exact authority, type,
  derivation-status, immutability, and per-port subcontext witnesses passes the
  reference-closure and primitive/sector non-reclassification tests

complete_corpus_wide_primitive_inventory_exhaustiveness = NO_VERDICT |
  deciding evidence: the exhaustive corpus-wide inventory determination left
  open by Q-97 at the register :3981-3986
```

Even a complete inventory does not alone settle the physical signature. The
category choice, applicable P1/P6 equivalence relations, origin semantics, and port/cross-row/
graph schemas remain signature-freeze prerequisites. The target-independent
constructor, trace-generation rule, and isolated validator are distinct later
instance prerequisites. Q-95 names these debt kinds at the register
`:3905-3908`; Section 12 keeps the two gates separate.

## 11. Failure-capable tests and exact verdict wiring

No test in this section is executed on a physical candidate.  Each test is
wired only to the property it tests.  A missing fixture returns TYPE-C or
`NO_VERDICT`; it does not pass.

| Test | Verdict tested | Failure condition | Routing guard |
|---|---|---|---|
| `SIG-T1-PHYSICAL-EXACTNESS` | `OBJ0_EXACT_PHYSICAL_SIGNATURE` | Any category, object type, operation, arity, domain, codomain, parameter, support, representation, or claimed analytic condition remains a disjunction, placeholder, or post-output choice. | Only exact-signature adequacy; never P0 impossibility. |
| `SIG-T2-PRIMITIVE-COVERAGE` | `SIGNATURE_PRIMITIVE_INTERFACE_COMPLETE` | A free primitive ground item lacks an explicit frozen authoritative binding with exact type/domain/multiplicity; a derived sector reference is reclassified as primitive; or a binding is unresolved, target-selected, fixture-supplied, or descendant-supplied. | Only primitive-interface and primitive/sector separation sufficiency. |
| `SIG-T3-DOMAIN-CLOSURE` | `SIGNATURE_DOMAIN_CODOMAIN_CONFORMANCE` | Any constructor/accessor is undefined on its declared domain, leaves its codomain, changes domain after output, reads `S_sector` during Obj_0 construction, or needs repair. | Only domain/codomain and context-channel conformance. |
| `SIG-T4.i-PORT-STRUCTURE` | `PORT_i_SIGNATURE_COVERAGE`, separately for i=1...7 | A port omits a sealed field/Q-94-called accessor, returns only existence/Boolean/placeholder, includes a forbidden downstream output, mandates an extra field without sealed-row/consumer authority, leaves P2's state restriction outside `c2`, or lets P5 choose `family_kind_5` after output. | Only that port's schema/output structure; never `P_i_derived`. |
| `SIG-T5-CROSS-ROW-COHERENCE` | `PORT_BUNDLE_CROSS_ROW_COHERENCE` | State restriction, record diagram, common domains, CTP/source conventions, or raw-evaluator inputs disagree. | Only bundle coherence. |
| `SIG-T6-ORIGIN-EXTERNAL-CONSTANT` | `SIGNATURE_COMMON_ORIGIN_ENFORCED` | Replacing any port result with a same-typed independently originated constant remains accepted while `Obj_0` and frozen inputs are fixed. | Only signature-level origin enforcement. |
| `SIG-T7-ORIGIN-HIDDEN-INPUT` | `SIGNATURE_ORIGIN_INPUT_CLOSURE` | Fixed `Obj_0`, `I_prim`, `S_sector`, explicit prior `c_j`, and frozen subcontexts permit an unlisted external change to alter a port result or witness. | Only hidden-input and context-channel exclusion. |
| `SIG-T8-CERTIFICATE-SOUNDNESS` | `COMMON_ORIGIN_CERTIFICATE_VALID` | Independent replay accepts a deleted, relabelled, dummy, self-asserted, mismatched-root, descendant-rooted, flag-rooted, or cyclic witness. | Only certificate validity. |
| `SIG-T8b-RECORD-GRAPH-SUBSTITUTION` | `SIGNATURE_RECORD_GRAPH_COREFERENCE` | Replacing `r` or `g` by a same-typed different record/graph leaves a port trace or aggregate certificate accepted. | Only record/graph co-reference. |
| `SIG-T9-DEPENDENCY-ORIENTATION` | `SIGNATURE_Q69_NONCIRCULARITY` | A P-row output/flag, target, fixture, `S_sector`, or unproved analogue identity is upstream of `Obj_0`, or an emitted graph contains/omits a direction-bearing edge so as to create/hide a cycle. | Only dependency orientation/completeness. |
| `SIG-T10a-P1-DOMAIN-EXACTNESS` | `P1_DOMAIN_FORM_EXACTNESS` | An equivalent-domain branch is admitted without a frozen equivalence predicate and witness. | Only P1 exactness. |
| `SIG-T10b-P6-ALTERNATIVE-EXACTNESS` | `P6_EVOLUTION_FORM_EXACTNESS` | A non-`S_CTP` dynamics form is admitted without a frozen equivalence predicate and witness. | Only P6 exactness. |
| `SIG-T11-SIGNATURE-FREEZE` | `SIGNATURE_TARGET_INDEPENDENCE` | The physical signature, port schema, origin relation, graph, port subcontext, or P5 `family_kind_5`/criterion changes after candidate/target output is seen. | Only target independence. |
| `SIG-T12.i-PORT-NONVACUITY` | `PORT_i_ATTEMPT_INTERFACE_NONVACUOUS`, separately for i=1...7 | On an independently frozen nonempty admissible fixture, the port always fails or does not return every exact structural field and trace. | Only that port's interface/nonvacuity; never `P_i_derived` or P0 impossibility. |
| `SIG-T13-ROAD-COVERAGE` | `STEP_1_COMPLETION` | The signature alone is reported as making the response chain or `C_record(K)` evaluable. | Only a claimed road-step-completion verdict; the relay-assigned process status remains `ADVANCES STEP 1`. |

### 11.1 Q-98 dedicated verdict-owner schema

Each row has a distinct owner object; `T4.i` and `T12.i` have seven distinct
owners rather than one aggregate owner:

| Test | Dedicated verdict-owner object required |
|---|---|
| `SIG-T1` | `OWNER_SIG_EXACTNESS_0` |
| `SIG-T2` | `OWNER_PRIMITIVE_COVERAGE_0` |
| `SIG-T3` | `OWNER_DOMAIN_CLOSURE_0` |
| `SIG-T4.i` | `OWNER_PORT_SCHEMA_i`, separately for each i |
| `SIG-T5` | `OWNER_CROSS_ROW_COHERENCE_0` |
| `SIG-T6` | `OWNER_ORIGIN_EXTERNAL_CONSTANT_0` |
| `SIG-T7` | `OWNER_ORIGIN_INPUT_CLOSURE_0` |
| `SIG-T8` | `OWNER_CERTIFICATE_SOUNDNESS_0` |
| `SIG-T8b` | `OWNER_RECORD_GRAPH_COREFERENCE_0` |
| `SIG-T9` | `OWNER_DEPENDENCY_ORIENTATION_0` |
| `SIG-T10a` | `OWNER_P1_DOMAIN_EXACTNESS_0` |
| `SIG-T10b` | `OWNER_P6_EXACTNESS_0` |
| `SIG-T11` | `OWNER_SIGNATURE_FREEZE_0` |
| `SIG-T12.i` | `OWNER_PORT_NONVACUITY_i`, separately for each i |
| `SIG-T13` | `OWNER_ROAD_COVERAGE_0` |

An owner is not the test, candidate, verdict flag, constructor, or certificate.
It is the isolated validation object authorized to receive only that test's
evidence and set only that test's named verdict.

```text
DedicatedVerdictOwnerRegistry_0_specified = true
DedicatedVerdictOwnerRegistry_0_derived = false | TYPE-U | would-build:
  instantiate every owner above in the isolated validation package, prove
  single-verdict write authority and separation from constructors/candidates,
  and execute owner-routing tests
```

### 11.2 Exhaustive static/instance phase assignment

Q-98 does not permit an undefined word such as *applicable* to hide a test.
The following partition is exhaustive over every test row above, with each
indexed family expanded separately for `i=1...7`:

```text
STATIC_ONLY_TEST_SET_0 :=
  {SIG-T1, SIG-T10a, SIG-T10b}

BOTH_PHASE_TEST_SET_0 :=
  {SIG-T2, SIG-T3, SIG-T4.i for every i,
   SIG-T5, SIG-T6, SIG-T7, SIG-T8, SIG-T8b,
   SIG-T9, SIG-T11, SIG-T13}

INSTANCE_ONLY_TEST_SET_0 :=
  {SIG-T12.i for every i}

STATIC_TEST_SET_0 := STATIC_ONLY_TEST_SET_0 union BOTH_PHASE_TEST_SET_0
INSTANCE_TEST_SET_0 := INSTANCE_ONLY_TEST_SET_0 union BOTH_PHASE_TEST_SET_0
```

For a BOTH-phase row the dedicated owner has distinct static-schema and
instance-evidence verdict slots; neither verdict substitutes for the other.
`SIG-T2` statically checks the primitive/reference binding schema and later
checks actual `I_prim`/`S_sector` bindings. `SIG-T3`, `SIG-T4.i`, and `SIG-T9`
likewise check both the frozen schema and the instantiated operations/graph.
At the static phase `SIG-T5` checks that the cross-row schema contains every
nonvacuous required relation; `SIG-T6`/`SIG-T7` attack the origin schema by
independent-constant and hidden-input substitutions; `SIG-T8` attacks the
certificate type/acceptance rule with dummy and name-only witnesses; and
`SIG-T8b` substitutes same-typed record/graph indices. Their separate instance
verdict slots repeat the corresponding attacks against actual `c_i`, traces,
and certificates. `SIG-T11` attacks both a post-target schema mutation and a
post-output subcontext or P5-family-kind mutation. `SIG-T13` rejects a Step-1
completion overclaim at either phase. Only `SIG-T12.i`, which requires an
inhabited nonempty port fixture, is instance-only.

```text
every_signature_test_has_a_phase_assignment = true [PROCESS FACT]
static_instance_phase_sets_overlap_only_on_declared_BOTH_rows = true [PROCESS FACT]
```

### 11.3 Q-98 adversarial-countermodel roster

For Q-98(f), *countermodel failed* means the hostile candidate was actually
submitted and the named test/owner pair rejected it. Merely writing the failure
condition is insufficient.

| Test | Mandatory hostile countermodel |
|---|---|
| `SIG-T1` | unresolved category/disjunctive physical signature presented as exact |
| `SIG-T2` | missing, target-selected, multiplicity-ambiguous, or descendant primitive binding; separately, a Q-97 derived sector reference relabelled as primitive |
| `SIG-T3` | operation undefined on one declared domain point or escaping codomain; separately, `S_sector` made visible to the Obj_0 constructor |
| `SIG-T4.i` | one sealed field omitted; separately, one unsealed mandatory field injected; for `i=2`, state/GNS restriction deferred outside `c2`; for `i=5`, `NONEXHAUSTIVE` selected only after the effect family is seen |
| `SIG-T5` | mismatched P2/P5 state, P2/P4 source maps, P1 domains, or P5/P6 normalization |
| `SIG-T6` | one port output replaced by a same-typed independently originated constant |
| `SIG-T7` | one `I_i`, `p_i`, `S_i`, or `d_i` replaced by an unlisted/forged external subcontext/witness, or a prior `c_j` hidden in either context |
| `SIG-T8` | name-only, dummy, self-asserted, mismatched-root, deleted trace, or trace falsely claiming an antecedent sector reference originated in `Obj_0` |
| `SIG-T8b` | same-typed different construction record or graph substituted |
| `SIG-T9` | descendant/flag/`S_sector` upstream-of-Obj_0 edge, hidden consumption edge, or directed cycle |
| `SIG-T10a` | equivalent-domain branch carrying an undefined/name-only equivalence witness |
| `SIG-T10b` | non-`S_CTP` branch carrying an undefined/name-only equivalence witness |
| `SIG-T11` | signature, graph, subcontext, or frozen P5 family-kind/criterion mutated after target/candidate output is observed |
| `SIG-T12.i` | always-failure port on an independently frozen nonempty admissible fixture |
| `SIG-T13` | class designator or signature name substituted for completed Step 1 output |

The Section 5 external-constant attack was executed against the **old flat
metadata `Sig_0` and succeeded in breaking it**. That refutes the old schema; it
does not count as a failed countermodel against the proposed future
`ExactSig_0`. No proposed physical signature or instance exists on which the
full roster can run.

```text
Q98_static_countermodel_suite_attempted_against_ExactSig_0 = false | TYPE-C |
  constraint: ExactSig_0 and its instantiated static owners do not exist |
  release: every hostile model for every row of STATIC_TEST_SET_0 is submitted
  and fails to break acceptance under its dedicated static verdict slot

Q98_instance_countermodel_suite_attempted_against_Obj_0 = false | TYPE-C |
  constraint: no Obj_0 interface instance, executed bundle, instantiated
  instance owners, or hostile fixtures exist |
  release: every hostile model for every row of INSTANCE_TEST_SET_0 is submitted
  and fails to break acceptance under its dedicated instance verdict slot

old_flat_Sig_0_external_constant_countermodel_failed_to_break_candidate = false | TYPE-R |
  test: Section 5; the attack succeeded in breaking the old flat signature
```

The old flat signature already fails `SIG-T6` at the specification level, as
Section 5 records.  That result is not routed into physical P0 buildability.

```text
relay_191_static_signature_tests_executed = false | TYPE-C |
  constraint: the exact signature-freeze inputs in Section 12.1 do not exist |
  release: they exist and every row of STATIC_TEST_SET_0 executes under its
  instantiated dedicated static verdict slot, with its hostile countermodel
  submitted

relay_191_instance_port_origin_tests_executed = false | TYPE-C |
  constraint: no ExactSig_0, Obj_0 instance, executed port bundle, or isolated
  hostile/nonempty fixture package exists |
  release: all exist and every row of INSTANCE_TEST_SET_0 executes under its
  instantiated dedicated instance verdict slot, with its hostile countermodel
  submitted

relay_191_static_signature_test_passes = NO_VERDICT |
  deciding evidence: execution of every row in STATIC_TEST_SET_0
relay_191_instance_port_origin_test_passes = NO_VERDICT |
  deciding evidence: execution of every row in INSTANCE_TEST_SET_0

road_step_status = ADVANCES_STEP_1_ONLY [PROCESS FACT; RELAY-191 AUTHORITY]
STEP_1_COMPLETION_verdict_claimed = NO_VERDICT |
  deciding evidence: future execution of SIG-T13 under its dedicated static
  and instance verdict slots, or a later principal road-status ruling

ADVANCES_STEP_1_satisfies_literal_Q92_d = NO_VERDICT |
  deciding authority: a principal/task-specific ruling on whether an exact
  signature subconstruction counts as the road step named by Q-92(d)
```

## 12. Q-92 disposition

There are two different Q-92 gates and they are not merged.

### 12.1 Exact-signature freeze gate

The signature itself needs:

1. one selected exact physical category and internal operation signature;
2. exact frozen primitive-binding and derived-sector-reference **type** schemas
   for every free ground type/domain used by that signature, without
   enumerating the separately owned primitive inventory here;
3. the P1 domain equivalence predicate if that branch is admitted, and the P6
   action/evolution equivalence predicate if that branch is admitted; and
4. frozen exact port, cross-row, direction-bearing graph, origin-predicate,
   trace-type, and certificate schemas.

Q-97 decides the present status of row 2:

```text
signature_ground_binding_inventory_sufficient_now = false | TYPE-C |
  constraint: Q-97 too-thin prefreeze audit |
  release: a frozen inventory satisfying Section 10 exists
```

Under Q-98, freezing may be reported derived only after every row of
`STATIC_TEST_SET_0` has its dedicated static verdict slot and its adversarial
countermodel is submitted and rejected.

It does **not** need an `Obj_0` inhabitant, the P0 construction rule, generated
traces, or the hostile instance-validation package merely to become a frozen
type/interface object. Those are later-instance requirements.

```text
FreezeExactSig_0_attempted = false | TYPE-C |
  constraint: Q-92(c), the four signature-freeze prerequisite rows above do not
  yet exist as exact frozen objects; Q-98(e)-(f) owner/countermodel release also
  is not instantiated |
  release: all four exist and every row of STATIC_TEST_SET_0 runs under its
  dedicated static verdict slot and rejects its hostile countermodel

conditional_signature_requirement_schema_specified = true

ExactSig_0_derived = false | TYPE-U | would-build:
  freeze all four signature prerequisites, execute FreezeExactSig_0, and pass
  every row of STATIC_TEST_SET_0 under dedicated static verdict slots after
  every hostile static countermodel fails to break the candidate, without
  constructing Obj_0
```

### 12.2 Later Obj_0-instance gate

After `ExactSig_0` exists, constructing an instance additionally needs:

1. an actual sufficient frozen primitive inventory satisfying Section 10;
2. an actual frozen `S_sector` satisfying the separate derived-sector-reference
   requirement in Section 10, visible only at port execution;
3. a target-independent `Obj_0` construction rule;
4. an origin-trace generation rule and emitted direction-bearing instance graph;
5. an isolated certificate/graph/port validation package;
6. independent nonempty fixtures for every core and port test;
7. an instantiated dedicated verdict-owner registry; and
8. an adversarial countermodel suite submitted to and rejected by every row of
   `INSTANCE_TEST_SET_0`.

The Q-95 artifact names these construction/validation debts at `:992-1016`.
Q-92(c,e,f) therefore separately blocks the instance.

```text
ConstructObj0Instance_0_attempted = false | TYPE-C |
  constraint: Q-92(c), ExactSig_0 and the eight later-instance prerequisite rows
  do not exist |
  release: ExactSig_0 plus all eight rows exist, every row of
  INSTANCE_TEST_SET_0 has its dedicated instance verdict slot, and every named
  hostile countermodel is rejected

Obj_0_constructed = false | TYPE-U | would-build:
  execute ConstructObj0Instance_0, run every core/port/origin/nonvacuity test,
  and validate the executed port bundle
```

No `derived = true` physical claim is made.

## 13. Five requested answers

### Answer 1 — What is `Obj_0` as a typed object?

**Read from sealed text:** `Obj_0` is a joint source-record-field
provenance/dynamics origin that makes P1-P7's structures constructible, not a
source that creates the antecedent carrier.  Evidence is the class definition
and P0 row at `STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md:34-39,229-233`,
Q-95 at the register `:3896-3908`, and Q-43 at
`STAGE8_GAMMA_K_SOURCE_RECORD_COMPOSITION_TYPING_TESTS_V001.md:167-175,316-327`.

**Not fixed by sealed text:** a physical category, internal object type, arity,
domain, codomain, support/core, representation, and analytic conditions.  The
Q-95 artifact expressly leaves these TYPE-U at `:995-997`.

**Declared here under F-GK3:** the conditional formal setting is the
`CERTIFIED_PROVENANCE_INDEXED_DEPENDENT_PRODUCER_SIGNATURE` of Sections 6-8.
For any future fixed `PhysicalSig_0`, `Obj_0 : ObjType_0[I_prim]`.
`FreezeExactSig_0` has domain `SignatureFreezeInputs_0` and codomain
`Result(ExactSig_0,SignatureFreezeFailure_0)`. The distinct later instance arrow
`ConstructObj0Instance_0` has the domain and codomain in Section 8.3. This is a
conditional requirement schema carrying exactness obligations, not a selected
physical operator signature.

### Answer 2 — What must the signature supply to all seven ports?

It must supply actual/canonical constructors or accessors for: P1 completed
carrier/domain; P2 source embedding and restriction coherence; P3 record
embedding and Q-42/Q-43 coherence; P4 exact physical CTP tuple; P5 state,
effects, domains, property contract, and origin trace; P6 an exact dynamics
form, common-domain action, normalization interface, and any required proven
equivalence; and P7 contacts, source rules, raw correlator evaluator, and
metadata. Sections 9.1-9.7 give the conditional codomain requirements and cite every
sealed row.

No named port is proved impossible. P1's equivalent-domain alternative and
P6's alternative-evolution branch prevent a presently exact *physical*
signature unless their applicable predicates are derived and frozen. One joint
physical inhabitant remains `NO_VERDICT`.

### Answer 3 — Does the signature force common origin?

The inherited flat metadata `Sig_0` merely permits it and fails the
independent-origin substitution test. A future exact inhabitant of the proposed
schema would force the **presence and same-`Obj_0`/record/graph indexing** of
seven traces and an aggregate certificate. The present schema is not derived,
and even an inhabited schema cannot by itself prove physical truth. Truth
remains `NO_VERDICT` until fixed origin
semantics and an independent validator reject forged, mismatched, hidden-input,
same-type/different-origin, record-substitution, and graph-substitution controls.

### Answer 4 — Construct under Q-92 if prerequisites exist

They do not. Q-92(c,e,f) separately blocks the exact-signature freeze and the
later `Obj_0` instance. Sections 8-10 specify the conditional requirement
schema; Section 12 separates the four signature prerequisites from the eight
additional instance prerequisites, including the separate `S_sector`,
dedicated owners, and failed hostile
countermodels. No physical test ran and no physical `derived = true` is
reported. The work carries Relay 191's process status `ADVANCES STEP 1` by
making the first missing Q-95 input checkable; no `STEP_1_COMPLETION` verdict is
claimed.

### Answer 5 — What does this require of the primitives?

Without enumerating them, the primitive inventory must be frozen and
target-independent;
carry authority, content identity, exact types/domains, and provenance; cover
every free ground sort/operation/parameter/domain of the selected physical
signature and ports; be closed under all declared compositions; remain
upstream-only and acyclic; and contain no P1-P7 output/flag, downstream target,
validation fixture, catch-all supplement, derived-sector reference, or unproved
analogue identity. Independently derived sector references must instead occupy
the separately frozen, immutable `S_sector` execution context and may never be
made inputs to `ConstructObj0Instance_0`. Section 10 gives both exact interface
predicates. Q-97 now makes the present primitive prefreeze status
`false | TYPE-C`: it is too thin and is not a frozen inventory sufficient for
`Obj_0`-kind construction. Corpus-wide primitive-inventory exhaustiveness,
future primitive coverage, and the exact frozen `S_sector` package remain open
pending their deciding audits.

## 14. Consolidated Q-54 status ledger

```text
prior_flat_Sig_0_forces_common_origin = false | TYPE-R |
  test: FLAT-SIGNATURE-INDEPENDENT-ORIGIN-REASSEMBLY

flat_metadata_tuple_supplies_P1_through_P7 = false | TYPE-R |
  test: Q94-ACCESSOR-SURFACE

Obj_0_exact_mathematical_category_domain_codomain_found = false | TYPE-S |
  scope/query/file lists: Section 2.4

P0_identified_with_package_B0_found = false | TYPE-S |
  scope/query/file lists: Section 2.5

P0_identified_with_U_BR_found = false | TYPE-S |
  scope/query/file lists: Section 2.5
P0_identified_with_D_BR_found = false | TYPE-S |
  scope/query/file lists: Section 2.5
P0_identified_with_B_chi_or_BID_generator_found = false | TYPE-S |
  scope/query/file lists: Section 2.5

P0_identified_with_a_discharge_flag = false | TYPE-R |
  test: Q69-FLAG/OBJECT-ROLE-SEPARATION

P6_action_evolution_equivalence_relation_found = false | TYPE-S |
  scope/query/file lists: Section 9.6

P1_domain_equivalence_relation_found = false | TYPE-S |
  scope/query/file lists: Section 9.1

port_specific_signature_impossibility_theorem_found = false | TYPE-S |
  scope/query/file lists: Section 9.9

Obj_0_is_a_linear_operator = NO_VERDICT |
  deciding evidence: selected exact PhysicalSig_0
Obj_0_is_a_functional = NO_VERDICT |
  deciding evidence: selected exact PhysicalSig_0
Obj_0_is_a_flow_or_evolution = NO_VERDICT |
  deciding evidence: selected exact PhysicalSig_0
Obj_0_is_an_algebraic_generator = NO_VERDICT |
  deciding evidence: selected exact PhysicalSig_0

one_physical_Obj_0_inhabits_all_seven_ports = NO_VERDICT |
  deciding evidence: Section 9.9
no_signature_can_supply_all_seven_ports = NO_VERDICT |
  deciding evidence: Section 9.9
physical_common_origin_of_P1_through_P7 = NO_VERDICT |
  deciding evidence: Section 6 and SIG-T6 through SIG-T8b
current_prefreeze_inventory_satisfies_PRIMITIVE_INTERFACE_REQUIREMENT = false | TYPE-C |
  constraint/release: Section 10, Q-97
future_complete_inventory_satisfies_selected_ExactSig_0 = NO_VERDICT |
  deciding evidence: a future frozen inventory and exact coverage audit
complete_corpus_wide_primitive_inventory_exhaustiveness = NO_VERDICT |
  deciding evidence: Q-97 leaves that determination open
Q80_new_formal_class_required = NO_VERDICT |
  deciding evidence: Section 6 flat-record-plus-invariant comparison

PhysicalSig_0_selected = false | TYPE-U | would-build:
  derive and freeze one exact physical category/signature from sufficient
  upstream primitives

FrozenPortDependencyGraph_0_derived = false | TYPE-U | would-build:
  freeze and test the exact graph from the selected physical signature

conditional_signature_requirement_schema_specified = true
Obj_0_exact_signature_specified = false | TYPE-U |
  would-build: freeze every Section 12.1 field and pass static exactness/closure
FreezeExactSig_0_attempted = false | TYPE-C |
  constraint/release: Section 12.1
ExactSig_0_derived = false | TYPE-U |
  would-build: FreezeExactSig_0, dedicated static owners, every row of
  STATIC_TEST_SET_0, and every hostile static countermodel rejected

Obj_0_constructed = false | TYPE-U |
  would-build: Section 12.2 instance construction and successful
  core/port/origin/nonvacuity tests under dedicated owners with every hostile
  instance countermodel rejected

DedicatedVerdictOwnerRegistry_0_derived = false | TYPE-U |
  would-build: Section 11.1
Q98_static_countermodel_suite_attempted_against_ExactSig_0 = false | TYPE-C |
  constraint/release: Section 11.3
Q98_instance_countermodel_suite_attempted_against_Obj_0 = false | TYPE-C |
  constraint/release: Section 11.3

relay_191_static_signature_tests_executed = false | TYPE-C |
  constraint/release: Section 11
relay_191_instance_port_origin_tests_executed = false | TYPE-C |
  constraint/release: Section 11

relay_191_static_signature_test_passes = NO_VERDICT |
  deciding evidence: Section 11 static test execution
relay_191_instance_port_origin_test_passes = NO_VERDICT |
  deciding evidence: Section 11 instance test execution

road_step_status = ADVANCES_STEP_1_ONLY [PROCESS FACT; RELAY-191 AUTHORITY]
STEP_1_COMPLETION_verdict_claimed = NO_VERDICT |
  deciding evidence: SIG-T13 execution or later principal road-status ruling
```

## 15. Pre-seal hostile-review record

The first hostile-review round against the pre-repair draft identified five
schema defects: primitive subcontexts contained prior outputs; Q-97's
primitive/derived-sector split was declared but not wired; the test phases were
not exhaustive; P2 omitted its P5-dependent restricted-state witness; and P5
could select `NONEXHAUSTIVE` after seeing its output. All five were preserved as
review findings and corrected before the next candidate.

The second round against candidate
`b164c95462f243ee325b505ba134f6afbfee01dd84372252f0b90e60e031080a`
identified two further defects: an unexecuted `SIG-T13` was credited as a
TYPE-R result, and the common-origin/coherence/certificate tests `SIG-T5`
through `SIG-T8b` were incorrectly instance-only. The present artifact carries
the resulting process-status/`NO_VERDICT` repair and places those tests in both
phases.

The final substantive candidate
`02fb986b0ca4e300130b094aaf4c2f8261f90005f24c85db1cca6a3315053099`
received three independent read-only `SEAL-READY` reviews: exact-signature and
Q-97/Q-98 correspondence; seven-port/origin/graph typing; and Q-92/primitive/
typed-negative accounting. The reviewers made no edits.

Q-97 and Q-98 landed while the draft was open. The artifact was held, both
later rulings were consumed from the register at `:3970-4025`, and the review
was restarted against the revised head rather than sealing against Q-95.

These are artifact-review process facts. They are not executions of the future
physical/static/instance tests in Section 11 and discharge none of their Q-98
owner or countermodel obligations.

```text
preseal_hostile_review_status = SEAL_READY [PROCESS FACT]
physical_test_status_from_hostile_review = NONE [PROCESS FACT]
```

## 16. Terminal declarations

```text
artifact_sealable = true
mirror_required_under_Q91 = true
git_commands_run = false | TYPE-C | constraint: Q-91
gate_run = false | TYPE-C | constraint: Q-91
deploy_status_run = false | TYPE-C | constraint: Q-91
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
