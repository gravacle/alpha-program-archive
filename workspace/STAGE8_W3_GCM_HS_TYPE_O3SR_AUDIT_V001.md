# STAGE 8 — W-3 / G_cm AT THE CONSUMING (HS) TYPE — SYMMETRIC AUDIT — O3SR V001

## AUDITOR — CODENAME W3-AUDIT — COMMISSION O3SR — 2026-08-15

```text
POSTURE: DEFAULT-REFUTE. Testimony carries ZERO weight. Every claim
re-derived at bytes or re-run in a fresh venv. The target's own prose was
read only to locate claims; nothing in it was accepted because it was
asserted.
FENCES AS COMMISSIONED AND AS HELD:
  alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false.
DETERMINATION / CONSTRUCTION ONLY. No authored physics beyond a named sealed
premise. No value. No number as a physical magnitude. No measured-constant
comparison. Exact symbolic only (CAS: fresh venv, sympy 1.14.0).
NO register / tracker / road / plan / continuation / ledger / lens file read.
"Q-..." tokens EXPECTED-UNLOCATABLE — not chased.
Output path probed ABSENT before first write. No git action.
THIS AUDIT RETIRES NOTHING. Witness retirement is the registrar's act alone.
```

---

## 0. VERDICT IN ONE LINE

**CONFIRMED-WITH-CORRECTIONS. The target's verdict —
UNDECIDABLE-TODAY, G_cm NOT derived at its consuming (HS) type, all four
obstacles standing and each moved AWAY from discharge — SURVIVES a
default-refute re-derivation at bytes in every load-bearing part. The
SEA-SANDWICH OBSTRUCTION is REAL and its corollary
(sup over H(sqrt3) of ||C T C||_2 = +infinity, hence no class-typed input
can convert form grade to HS grade at R.3's own sandwich) is CONFIRMED;
the registered sharper enclosures reproduce EXACTLY, including the factor
chain 24 sqrt(6) < 192 <=> 6 < 64; the (d-2) disjointness is CONFIRMED and
is in fact OVER-determined by a sealed carrier the target never cites
(CL-ERROR §3.2: "E_int has NO degree-0 intraband component"); the (d-3)
non-extractability and the (d-0) Gronwall escalation both hold at their
stated a-fortiori direction. The CAS battery reproduces 50/50 PASS
BYTE-FOR-BYTE from a fresh venv. FIVE CORRECTIONS, all display- or
hypothesis-grade, NONE verdict-moving: (COR-1) §0 advertises the
obstruction's proof as an "exact two-line KERNEL argument" while §4.1
actually gives a compactness / weak-null argument — two different proofs,
the advertised one never executed; (COR-2) §4.1's theorem is stated for
BOUNDED T but applied at (h-4) to T = h_0, which is UNBOUNDED — the
conclusion survives twice over (an unbounded operator is a fortiori not HS,
and bounded translation-invariant members of H(sqrt3) exist), but the
theorem as written does not literally cover its own application;
(COR-3) §5.1 truncates the B-L2* quote immediately BEFORE that blocker's
own named in-spec escape ("a certified C-L1 error structure supplying an
exact cancellation"), so the reader cannot see from the displayed block
that "UNREACHABLE FROM C-L2" is narrower than "unreachable" — the verdict's
own scoping is correct, the display is incomplete; (COR-4) §6.2(g-2), CH-3
and the §8/§12 flag lines state "the ONLY exponent compatible with the
asset is beta = 0" while the derivation that forces it explicitly SUPPOSES
G cell-blind — dropping the supposition, EVERY beta is compatible
(G = K |C|_4^{-beta}), which is a STRONGER non-extractability than the one
claimed; (COR-5) §7 grants the class's BILINEAR display, which is strictly
more than membership in H(sqrt3) as §3 defines it (a diagonal condition),
and then evaluates it at phi = h_0 chi, which leaves the certified form
domain Q = H^1 unless chi is already in H^2 — both moves run in the
a-fortiori direction and so preserve the negative, but neither caveat is
displayed. THE KILL HUNT RETURNS NEGATIVE ON EVERY ITEM: the excluded
operator-norm route is NOWHERE consumed (the only ||.||_op in the artifact
are the record's own finite-rank rank x op conversion — which FHB-A COR-A
identifies as the HS-grade instrument of record — plus Clifford facts and
verbatim citations of the exclusion itself); NO form bound is applied
outside its certified domain except the displayed-a-fortiori §7 case
(COR-5); n-uniformity is NEVER claimed and is disclaimed at four sites;
NO standing point is declared moved without its display — all four carry
one; the witness E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED is NOT treated
as retired at any site; NO hidden numerics — zero floats, zero evalf, zero
measured constants. MAP §3.4/§3.5 are consumed unedited and the
uniqueness of {W-1, W-2, W-3} is untouched. NOT REFUTED is carried
correctly: every negative in the target is one-sided and the audit adds no
divergence of any closure quantity.**

---

## A1. STEP 0 — TARGET PROBED, SEALS VERIFIED FROM THE ARTIFACT'S OWN DIRECTORY

```text
TARGET   STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md            PRESENT (75449 bytes)
SIDECAR  STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md.seal.sha256 PRESENT (101 bytes)
  shasum -a 256 -c  (run from .../alpha-program-archive/workspace)  ->  OK
  241262ffc7da05b5cef6638f6f7ac5dd8aa4af5ff35becab92cb17f062cbfca9
OUTPUT PATH  STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001.md   ABSENT before first
  write (probed twice: at STEP 0 and immediately before writing).
```

THE COMMISSIONING GROUND, seals verified at the same discipline:

```text
afc3a79c646c044d9c34ede25fbc7166d43807c9ac91b64f078900f9309516cd  STAGE8_COMPLETION_MAP_T17SR_V001.md        OK
4504cd1448049496f0d5db9828fbed09b51b696cd11d0da1e978ad147964693a  STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md  OK
```

### A1.1 THE TARGET'S OWN SEAL TABLE — INDEPENDENTLY RECOMPUTED, 14/14 AGREE

Every digest in the target's §1 table was recomputed this session with
`shasum -a 256`, and every sidecar was re-checked with `shasum -a 256 -c`
**run from the workspace directory itself** (sidecars name basenames; a
parent-directory run reports false failures — the commissioned protocol,
held). Result: **14/14 digests identical to the target's table; 14/14
sidecar checks OK; none unverifiable; none missing.**

```text
e6838ffcc6f352b7a0cfaad1d51d98544717d3bbe04d413d8b356e752d400a3d  ..._CL2_GCM_CERTIFICATION_S9AD_V001.md            OK  [CERT]
180114a796473769ee7bb9d041b9a8d9883d9c76ca8db5f78f314ece815a801e  ..._CL2_GCM_CERTIFICATION_S9AD_AUDIT_V001.md      OK  [CERT-A]
9a2954982e4c3b81177e464118720ee0bd264b1a51ebce83b65855858c6a4545  ..._FORM_TO_HS_BRIDGE_S9AD_V001.md                OK  [FHB]
27bc36aefb78683c70836aa5c1882d9392dac8caf813b87b71154f470bdc4862  ..._FORM_TO_HS_BRIDGE_S9AD_AUDIT_V001.md          OK  [FHB-A]
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027  ..._BL2STAR_ATTEMPT_V001.md                       OK  [BL2*]
2699af259750e2cae9afcb4d5a9487edd543c91a13db7c8f49a8fed1f3d9e24c  ..._BL2STAR_CHECK_V001.md                         OK  [BL2*-C]
99da8628c1f6ef1d90db89645f8c4a1ed62bd99516c0b60555b3faace0e4a0b8  ..._RL2B_REMAINING_CONTENT_UNDER_THE_CHARTER_V001.md OK [RL2B-R]
1497d017175c900fb74a75970c569cd3319c77c5814a2f874734c695e8c06491  ..._RL2B_OVER_CHARTER_AND_BLIND_ROUTE_FINDING_V001.md OK [RL2B-O]
80db260fa1561d76296d5f54e1e52397b79009b8a0d12bc060c140818c38fdf7  ..._WALL_BOUNDARY_CLOSURE_S9AD_V001.md            OK  [WALL]
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  ..._T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md          OK  [E1]
a903716c23c1a6969932e988f90d464abe9e8ee96624cd2dce3e32416de132e0  ..._RL4_RL0_CERTIFICATION_V001.md                 OK  [RL0]
3b9730e0ce585c524a6ac27d3368f29535a0bbffe84e1ec8313cc861011acb7a  ..._CL_ERROR_CERTIFICATION_V001.md                OK  [CL-ERROR]
+ the two MAP digests above.
```

The target's CROSS-PROVENANCE claim (six digests agreeing with CERT §1's
independent table) is therefore **CONFIRMED at the stronger grade**: all
fourteen agree with a third, independent recomputation.

### A1.2 SWEEP CUTOFF — THIS AUDIT'S OWN, DECLARED

```text
SWEEP CUTOFF: 2026-08-15, this session.
ROOTS: (R-a) /Users/bgm/MB Work/alpha-program-archive/workspace  (primary)
       (R-b) /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
             alpha_fundamental_record_action_cleanroom_v003
SCOPE OF READS: the target IN FULL; the two MAP artifacts with §3.4 and §3.5
  read IN FULL as commissioned; and SCOPED reads of the twelve named
  consumed artifacts at the exact loci the target cites, plus declared
  scoped token sweeps inside the target itself (op-norm, n-uniform,
  numerics, fence, witness tokens).
EXCLUDED BY COMMISSION, NOT READ: every register / tracker / road / plan /
  continuation / ledger / lens file. "Q-..." EXPECTED-UNLOCATABLE.
THE AUDIT'S BOUNDED NEGATIVE: within these terms, no consumed carrier
  contradicts a target claim; the one carrier found to bear on a target
  claim UNCITED (CL-ERROR §3.2) STRENGTHENS it (§A5).
```

---

## A2. THE COMMISSIONING GROUND RE-DERIVED — MAP §3.4 AND §3.5, READ IN FULL

Both sections were read IN FULL at bytes, in the build and in its audit.

```text
MAP §3.4 (:314-340) — THE MAP IS EMPTY OF DISCHARGE EDGES. Zero of the
  twenty-one (absence x live-failure-point) edges discharges or partially
  discharges. THE CLOSURE'S OWN DISCHARGERS are displayed there, and W-3
  is quoted verbatim at :335-339:
    "W-3 (discharges FP-3): the C-L2 certification at its consuming type —
     G_cm derived at the consuming (HS) type; 'its only permitted
     certification (C-L2 quadratic form)' with 'the operator-norm route
     excluded (||[h_0, 1_B]|| = +infinity)'; retirement of witness
     E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED is the registrar's upon its
     landing."
MAP §3.5 (:342-361) — THE MINIMAL COMPLETION SET(S).
  (i) AT THE COMMISSIONED INVENTORY: EMPTY-INFEASIBLE — no subset of
      A-1..A-7, including the full set, discharges the closure at any live
      failure point.
  (ii) AT THE CLOSURE'S OWN NAMED DISCHARGERS: the minimal completion set is
      UNIQUE — {W-1, W-2, W-3} — because each failure point's discharger is
      displayed as SOLE at the closure's own bytes: F1's only escape is the
      sub-volume rate ("rank x op is the ONLY sealed conversion"; the
      CS-1..CS-9 sweep found "NO single-composite kill or sub-threshold
      majorant"); F2's only displayed replacement is the symbol-side
      evaluation (boundary smoothing "ruled ... insufficient"); F3's
      certification route is "only permitted" C-L2 with the operator-norm
      alternative "EXCLUDED of record." "No alternative minimal set exists
      at the displayed bytes; none is invented."
```

**THE AUDITED DERIVATION IS PRESENT AND HOLDS.** MAP-A §A3 (:129-156)
re-derives both strata independently and returns CONFIRMED with one
display-discipline correction (C-4) that touches neither: edge endpoints
were checked in BOTH directions at bytes (forward — the WBC build contains
zero occurrences of any commissioned absence's object tokens; reverse —
none of the twelve absence-side naming artifacts contains MO-3, C-L2,
VOLUME DIAGONAL, prequotient, boundary-closure, four-quantity or
rank-x-op); "all twenty-one live edges are CANNOT-TOUCH at both endpoints'
bytes"; and the uniqueness of {W-1, W-2, W-3} is re-derived as **"Three
DISTINCT objects — no smaller set can un-fail three steps; no fourth live
step exists, so the set is complete for un-failing the route ... CONFIRMED
at the displayed bytes; no alternative set exhibited by the uncapped
re-sweeps."**

```text
CONSEQUENCE FOR THIS AUDIT, stated exactly: the record's famous absences
(A-1..A-7) CANNOT TOUCH FP-3. W-3 is FP-3's SOLE displayed discharger and
its route is SOLE. Therefore a negative returned at W-3 on the C-L2 route
is a negative on the ONLY route the record displays for that failure point
— which is why the target's scoping ("W-3 cannot land on C-L2's own
permitted route ALONE; its landing requires an input from outside the form
class") is the load-bearing sentence of the whole artifact, and why this
audit tested it harder than anything else. IT HOLDS (§A9, §A11).
CONV-2 (MAP §3.6, MAP-A §A3) is noted and untouched: G_cm at the consuming
type serves TWO displayed consumers — FP-3 at the wall AND the 5d's D7 leg.
The target does not consume CONV-2 and makes no D7 claim. CORRECT ABSTENTION.
```

---

## A3. THE PREMISE BYTES — EVERY CITED CLAUSE RE-READ AT ITS OWN LINES

Testimony carries zero weight, so every clause the target quotes was opened
at its own carrier. **All quotations are verbatim-accurate.** Two
line-locus imprecisions found, both display-grade (§A10, OBS-1).

```text
E1 :1136-1145  C-L2 — VERBATIM CONFIRMED, including "Norm-based control is
   unavailable (||[h_0, 1_B]|| = +infinity for first-order h_0 against a
   sharp indicator), so the certification must be in a quadratic form and
   the artifact must state WHICH form and WHY the operator-norm route is
   excluded. Supplies S2b." Witness E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED
   present at :1145. The target's four-obligation split (o1)(o2)(o3)(o4) is
   a faithful reading of that clause; nothing is added to it.
E1 :702-707    S2b — VERBATIM CONFIRMED: "G_cm(C, eps) := |C|_4^{-beta} ·
   (certified bound on the C-L2 commutator error [h_0, M(t) ⊗ S] in the
   quadratic form C-L2 names)" and "beta is derived with alpha under R-L2b."
E1 :811-814    R.3's consumable — CONFIRMED: "||A_s||_2 <= ||A(0)||_2 +
   ||Delta||_2 <= |C|_4^{beta} G_cm + |C|_4^{alpha} G_hs ... (where A(0) is
   the C-L2 error, NOT a baseline norm)".
E1 :826-833    R-L2b — VERBATIM CONFIRMED, with "alpha = 1/2 may be the
   answer; it may not be the assumption" and witness
   SCAD_HS_SCALING_EXPONENT_UNDERIVED.
E1 :353-357    C6 — CONFIRMED: "||[C, 1_B]||_2 = +infinity ... Only TWO-TIME
   / scattering-type objects ... can work."
E1 :465-470    D6' — CONFIRMED: M(t) = Q 1_{|x| <= r(t)} Q sealed; "No
   obligation, lemma, control, repair, or enclosure may replace, mollify,
   smooth, or regularize M(t)". The target does not touch it.
E1 :320-321    C1 — CONFIRMED: "C = 1_(-inf,0)(h_0), C(p) = (I - alpha·p-hat)/2".
E1 :346-351    C4 saturation — CONFIRMED as B-L2*'s cited ground.
WALL :404-413  F1/F2/F3 — CONFIRMED, including "Escape requires a sub-volume
   trace/HS rate: verbatim MO-3, open of record" and F3's "only permitted
   certification (C-L2 quadratic form) is UNCERTIFIED of record".
BL2* :32-48    the FAILS-AT verdict — VERBATIM CONFIRMED (see §A5/COR-3).
BL2*-C :22-30  "NET_VERDICT = CONFIRMED(FAILS-AT ...)" — CONFIRMED.
RL2B-O :21-29  R-L2b restated — VERBATIM CONFIRMED, including "|D|_4^alpha".
RL2B-R :15-16, :28 — VERBATIM CONFIRMED: "NOTHING WAS CONSTRUCTED. No
   exponent, no estimate, no conversion supplied. PRODUCTION PROHIBITED."
   and "*** THIS IS WHERE alpha IS. *** Its companion beta for G_cm sits
   here too."
CERT :257-266  (u-a) t-UNIFORM and (u-b) CELL- AND SCALE-UNIFORM — VERBATIM
   CONFIRMED, including "the constant is a functional of NOTHING but the
   Clifford algebra."
CERT :298-305  the §3.3 residual (d-0) — VERBATIM CONFIRMED, including "a
   Gronwall at form grade would consume ||[h_0, M(s)⊗S] Omega psi||, an
   OPERATOR-grade quantity — exactly what is excluded" and "NOT attempted
   tonight and NOT named anywhere in sealed stock as discharged."
CERT :683      CL2_S2B_SUPPLY_DISCHARGED = false, with the same four failure
   causes the target reports. The target's "STATE OF RECORD ENTERING
   TONIGHT" is accurate.
CERT-A :154, :165  the certified chain "HOLDS" — CONFIRMED.
FHB :33, :332-337, :436  site (i) = FAILS-AT(compression type), the
   formal-Lambda display "(8 pi/5) Lambda^5 -> +infinity, WB7 license", and
   "the class contains h_0 (cl-2); no class-typed input can supply it" —
   ALL CONFIRMED. FHB :153 carries cl-2 itself.
FHB :255-256, :262-263  r_n = 4 n^3; E_n = tr(Q h_0^2 Q) = 6 n^4/ell^2 EXACT
   (FB5); headline 12 sqrt(2) n^{7/2}/ell and 24 sqrt(2) n^5/ell — CONFIRMED.
FHB-A :176-195  COR-A, COR-B, COR-C — CONFIRMED verbatim (see §A4).
CL-ERROR :150-160  E_int is generated by exactly the C-L2 commutator —
   CONFIRMED. CL-ERROR :194, :228-268 — see §A5, the uncited strengthener.
```

---

## A4. CLAIM (d-1) — THE SEA-SANDWICH OBSTRUCTION: RE-DERIVED INDEPENDENTLY

### A4.1 The theorem's proof, re-walked step by step

```text
STEP 1  C T C commutes with every U_a.  CONFIRMED — a product of operators
        each in the commutant of {U_a} lies in that commutant.
STEP 2  U_a psi -> 0 WEAKLY as |a| -> infinity.  CONFIRMED — for f, g in
        L^2, <g, U_a f> is the correlation (g-bar * f-reflected)(a), which
        lies in C_0 (immediate on compactly supported f, g; extended by
        density and the uniform bound |<g,U_a f>| <= ||f|| ||g||).
STEP 3  A compact operator maps weakly-null sequences to norm-null ones.
        CONFIRMED — standard.
STEP 4  ||C T C U_a psi|| = ||U_a (C T C) psi|| = ||(C T C) psi||, constant
        and nonzero. CONFIRMED — U_a unitary and STEP 1.
CONTRADICTION => C T C not compact => not HS, not trace class. THE
IMPLICATION CHAIN IS VALID. The proof consumes NO regulator, NO cutoff, NO
formal Lambda, NO WB7 license — verified by inspection of §4.1's text and
by the absence of any Lambda token in it.
```

**COR-2 (hypothesis-grade).** §4.1 states the theorem for a **bounded** T.
§4.2 (h-4) applies it at T = h_0 = alpha·p, which is **unbounded** on
L^2(R^3) ⊗ C^4; the sandwich C h_0 C = -|k| P_-(k-hat) is likewise
unbounded. Compactness is a notion for bounded operators, so the theorem
as written does not literally cover its own application. **The conclusion
survives twice over, and this audit certifies it by both routes:**

```text
ROUTE 1 (immediate). Every Hilbert-Schmidt operator is bounded. C h_0 C has
   symbol -|k| P_-(k-hat) with |k| unbounded on R^3, hence C h_0 C is
   unbounded, hence NOT HS. The claim at (h-4) is true a fortiori and needs
   no theorem at all.
ROUTE 2 (repairs the theorem's own reach). H(sqrt3) contains BOUNDED
   translation-invariant members. Take the multiplier b with symbol
   m(k) I_4, m real with |m(k)| <= 2 sqrt(3) |k| and m bounded (e.g. m
   bounded and vanishing linearly at k = 0). Then for every psi in Q,
     |<psi, b psi>| = |int m |psi-hat|^2| <= 2 sqrt(3) int |k| |psi-hat|^2
                    <= 2 sqrt(3) || |k| psi-hat || || psi-hat ||
                     = 2 sqrt(3) ||h_0 psi|| ||psi||   (Cauchy-Schwarz),
   so b IS IN H(sqrt3), b is BOUNDED and translation-invariant, and
   C b C = m(k) P_-(k-hat) is nonzero. The theorem applies to b verbatim
   and returns: C b C is not compact, hence not HS. The COROLLARY's
   sup = +infinity therefore holds with a witness inside the theorem's own
   stated hypotheses.
NET: the corollary is TRUE; only the theorem-to-application fit needs the
one-line widening (drop "bounded", or name the bounded witness). NOT
verdict-moving. NOT a kill.
```

**COR-1 (display-grade).** §0 :36-42 advertises the obstruction as an
"exact two-line **kernel** argument", but §4.1 supplies a
compactness/weak-null argument instead. The advertised kernel proof is
never executed anywhere in the artifact. It is, independently, a valid
proof — a translation-invariant operator is convolution by kappa, and
||K||_2^2 = int int |kappa(x-y)|^2 dx dy = |R^3| · ||kappa||_2^2, infinite
unless kappa = 0 — and it has the advantage of not needing boundedness
(so it also repairs COR-2). But §0 must not describe a proof the artifact
does not give. **Display correction; the result stands on §4.1 as written
plus ROUTE 1/ROUTE 2 above.**

### A4.2 The four hypotheses at R.3's own site — each independently verified

```text
(h-1) C TRANSLATION-INVARIANT. C = 1_(-inf,0)(h_0) with h_0 = alpha·p a
      Fourier multiplier; spectral projections of a multiplier are
      multipliers; symbol (I - alpha·k-hat)/2 is E1 C1's OWN sealed symbol
      (E1 :321) — the target cites r-2 :144-148 via FHB §4.1, which is a
      valid but downstream provenance for a datum E1 already carries
      (OBS-2).                                                  CONFIRMED
(h-2) H(sqrt3) TRANSLATION-INVARIANT. The defining functional uses only
      ||h_0 psi|| and ||psi||; U_a is unitary and commutes with h_0, so
      both are invariant and t o U_a lies in H(c) with the SAME constant.
                                                                CONFIRMED
(h-3) h_0 IS IN H(sqrt3). |<psi, h_0 psi>| <= ||h_0 psi|| ||psi||, and
      1 < 2 sqrt(3). Margin 2 sqrt(3) - 1 > 0 exact.             CONFIRMED
(h-4) THE SANDWICH IS NONZERO. Re-derived by hand, independently of the
      target's CAS: A := alpha·k-hat satisfies A^2 = I, so P_- = (I-A)/2 is
      a projection with A P_- = -P_-; hence
         C h_0 C = |k| P_- A P_- = -|k| P_-   EXACTLY,
      tr(C h_0 C) = -2|k| (P_- has rank 2 per k), and the HS density
      tr(m^dag m) = |k|^2 tr(P_-) = 2 |k|^2 > 0 for k != 0.       CONFIRMED
```

### A4.3 The corollary, and the exact boundary of what it closes

```text
CONFIRMED: sup over t in H(sqrt3) of ||C T C||_2 = +infinity. Hence NO
   inequality ||C T C||_2 <= F(c, cell data, eps) valid for ALL t in
   H(sqrt3) can hold at any finite F. READING A of S2b is FALSE as an
   INFERENCE FROM THE CERTIFIED ASSET; READING B has no conversion at this
   site. THE FORM->HS CONVERSION IS CLOSED **AS A CLASS IMPLICATION**.
THE BOUNDARY, TESTED HARD AND FOUND CORRECTLY DRAWN BY THE TARGET: the
   actual C-L2 generator [h_0, M(t) ⊗ S] is NOT translation-invariant (M(t)
   is a sharp spatial localizer), so C [h_0, M ⊗ S] C is not translation-
   invariant and the theorem says NOTHING about it. The target states this
   correctly and in three places: §4.3 "DOES NOT ADD: ... NOT to the actual
   C-L2 error layer, which is a different operator and remains UNDECIDED";
   §8 "the +infinity of §4 attaches to the CLASS SUPREMUM and to the member
   h_0's own sandwich"; and the deciding object is typed as one that
   "BREAKS THE TRANSLATION SYMMETRY the form class preserves". THE SCOPE IS
   NOT OVERCLAIMED. The one loose phrasing is §0's compressed "the
   obstruction is a property of C, not of the member", which §4.3 ADDS (2)
   immediately states precisely ("ANY translation-invariant sandwich kills
   ANY translation-invariant member's image"). Noted, not corrected.
ADDS (1) IS REAL: FHB :336-337 does consume "(8 pi/5) Lambda^5 -> +infinity,
   WB7 license"; §4.1 consumes no license. A standing negative is
   de-licensed. CONFIRMED.
ADDS (2) IS REAL BUT MODEST: FHB :436 already recorded "the class contains
   h_0 (cl-2); no class-typed input can supply it" — the class-level
   non-implication was ALREADY of record. What §4 adds is (i) the removal
   of the regulator and (ii) the widening from the single member h_0 to
   every translation-invariant member. Genuine; the target does not
   overstate it (it says "FHB exhibited ONE bad member"). CONFIRMED.
ADDS (3) IS REAL: the escape TYPE (symmetry-breaking / localization-
   consuming) is a derived consequence of the theorem, not a restatement.
   CONFIRMED.
```

---

## A5. §4.4 — THE REGISTERED SHARPER ENCLOSURES: ARITHMETIC RE-DERIVED BY HAND

Re-derived independently of the target's CAS, from FHB's own sealed
carrier data (r_n = 4n^3 at FHB :255; E_n = 6n^4/ell^2 EXACT at FHB :256).

```text
CARRIER FORM BOUND. 1-D oscillator identity p^2 + x^2 = 2N + 1. Compress to
   span{0..n-1} by Q: Q x^2 Q = (xQ)^dag (xQ) is a Gram matrix, hence PSD;
   Q(2N+1)Q is diagonal with entries 2a+1 <= 2n-1 for a <= n-1. Therefore
     Q p^2 Q = Q(2N+1)Q - Q x^2 Q <= (2n-1) Q      per 1-D factor.
   Three factors: |p|^2 <= 3(2n-1)/ell^2 = (6n-3)/ell^2, so
     ||h_0 Q||_op <= sqrt(6n-3)/ell.               RE-DERIVED, CONFIRMED.
   The route uses NO eigenvalue radicals and NO numerics, exactly as claimed.
HS ENCLOSURE.  ||C_n T_g C_n||_2 <= sqrt(r_n) · ||C_n T_g C_n||_op
   <= sqrt(4n^3) · 2 sqrt(3) · sqrt(6n-3)/ell
    = 2 n^{3/2} · 2 sqrt(3) · sqrt(3) sqrt(2n-1)/ell
    = 12 n^{3/2} sqrt(2n-1) / ell.                 EXACT MATCH. Order n^2.
TRACE ENCLOSURE.  ||·||_1 <= r_n · ||·||_op = 4n^3 · 2 sqrt(3) · sqrt(3)
   sqrt(2n-1)/ell = 24 n^3 sqrt(2n-1)/ell.         EXACT MATCH. Order n^{7/2}.
HEADLINE REPRODUCED.  2 sqrt(3) sqrt(r_n E_n) = 2 sqrt(3) sqrt(24 n^7)/ell
   = 12 sqrt(2) n^{7/2}/ell; times sqrt(r_n) gives 24 sqrt(2) n^5/ell.
                                                   EXACT MATCH to FHB :262-263.
SHARPER AT EVERY n >= 1.  ratio^2 = n^3(2n-1) / (2 n^7) = (2n-1)/(2n^4) <= 1
   <=> 2n - 1 <= 2 n^4, true for every integer n >= 1 (n=1: 1 <= 2).
                                                   RE-DERIVED, CONFIRMED.
EXECUTED MEMBER n = 2.  sharp = 12 · 2^{3/2} · sqrt(3)/ell = 24 sqrt(6)/ell;
   headline = 12 sqrt(2) · 2^{7/2}/ell = 192/ell; and
   (24 sqrt(6))^2 = 3456 < 36864 = 192^2  <=>  6 < 64.
                                                   EXACT MATCH to the chain.
```

**PROVENANCE FINDING (material, and to the target's credit).** FHB-A COR-B
(:183-192) **already contains these enclosures verbatim** — the same carrier
form bound |p|^2 <= (6n-3)/ell^2, the same ||h_0 Q||_op <= sqrt(6n-3)/ell,
the same "L-1 + sqrt(rank)", the same two displays with the same orders, and
the same n = 2 chain "24 sqrt(6)/ell vs 192/ell (exact: 6 < 64)". The
target does not claim otherwise: §4.4 opens by quoting COR-B's registration
condition and §8/(m-1) calls them "THE AUDITED SHARPER ENCLOSURES". **The
attribution is correct and the re-derivation is genuine** (it proceeds from
r_n and the PSD route, not by transcription). The registrar should read
(m-1) as **discharging COR-B's registration condition**, not as new
mathematical content — which is exactly how the target words it.

**COR-A CARRIED, VERIFIED.** FHB-A COR-A (:176-182) holds that the build's
"ORDER-SHARP within the class" claim is FALSE at HS grade. A scoped sweep
of the target for order-sharpness claims returns none: §4.4 states "COR-A
CARRIED: NO order-sharpness is claimed at HS grade anywhere in this
artifact" and no contrary claim appears. **CONFIRMED.**

**n-UNIFORMITY: NEVER CLAIMED.** FHB's FB9 non-implication (n-uniform
discharge PROVABLY NOT form-implied), audited valid, is respected. The
target disclaims n-uniformity at four sites (:428-431, :249, :703-704,
:1265) and claims it at none. **THE HUNT ITEM RETURNS NEGATIVE.**

---

## A6. CLAIM (d-2) — B-L2* DISJOINTNESS: CONFIRMED, AND OVER-DETERMINED

### A6.1 (f-1), the carrier of the failing term

```text
BL2* :41-48 (verbatim, re-read): "at full tau_R the weight-summed branch
   propagator resums to the sharp, spinor-trivial causal-ball projector P
   (plus the UNCERTIFIED C-L1 error), P's band-diagonal transfer elements
   are O(1), and the leading summed response then contains the P-restricted
   longitudinal intraband term ... degree 0 ... with no sealed identity to
   cancel it."
=> P and the C-L1 error ARE the clause's own two summands, and the degree-0
   killer IS displayed on the P summand. The target's reading is EXACT.
=> C-L2 certifies a BOUND on the [error] layer's generator (CL-ERROR :150-160
   confirms E_int is generated by exactly [h_0, M(t) ⊗ S]). A BOUND on one
   summand cannot remove a term displayed on the other.  CONFIRMED.
```

**THE UNCITED STRENGTHENER (found by this audit, not by the target).**
CL-ERROR — a carrier the target has already sealed and consumed for a
different purpose — settles (f-1) far more strongly at its §3.2:

```text
CL-ERROR :194  "This is the ONLY degree-0 intraband content of E (E_int has
   none, §3.2)."
CL-ERROR :259-260  "E_int's C-compressed intraband amplitude degree <= -1 at
   bounded transfer. E_int has NO degree-0 intraband component."
CL-ERROR :267-270  "A degree-(<= -1) symbol cannot identically cancel a
   degree-0 symbol: subtracting E_int-type contributions leaves the degree-0
   part of the response — the survivor — intact."
```

That is the sealed record stating, in its own bytes, precisely the target's
(f-1) — and stating it as an EXACT DEGREE SEPARATION rather than as a
summand-bookkeeping argument. **(f-1) is CONFIRMED and is stronger than the
target displays.** The target cites CL-ERROR only at :150-160 (for the
generator's identity) and misses its own best support. **OBS-3, to the
target's benefit; the registrar may wish (f-1) re-displayed at CL-ERROR
§3.2's grade.**

### A6.2 (f-2), the asset's coincidence degree

```text
BL2* :36-38: "every >= 2-per-branch stratum has volume-diagonal amplitude
   degree <= -2 at fixed transfer, strictly past the HS threshold -3/2."
   => the two grades the target compares against are TRANSCRIBED CORRECTLY.
THE ASSET: 2 sqrt(3) ||h_0 psi|| ||psi|| — a pure number times state norms,
   with no dependence on the momentum transfer. Coincidence degree = 0.
   0 - (-3/2) = 3/2 (above threshold by exactly 3/2);
   0 - (-2)   = 2   (short of the per-stratum gain by exactly 2).
                                                   ARITHMETIC CONFIRMED.
t-UNIFORMITY (CERT u-a, verbatim at CERT :257-258): integrating a t-uniform
   bound over the cell time produces the time-weight factor and NO power of
   the transfer variable. CONFIRMED — a q-independent integrand cannot
   acquire q-dependence by t-integration.
```

**COR-3 (display-grade, and the one omission this audit judges material to
display).** §5.1's quoted block ends at "with no sealed identity to cancel
it." The very next sentence of BL2* (:45-48) reads:

```text
"The exhibition is exact AT the sealed C4 operator-level display; its only
 in-spec escape is a certified C-L1 error structure supplying an exact
 cancellation (nothing sealed supplies one; witness
 E1_RECORD_PHASE_OPERATOR_COLLAPSE_UNCERTIFIED), the only other named
 repair — softening M(t) — being sealed away."
```

So B-L2* **does** name an in-spec escape, and it lives in the error layer —
which is superficially in tension with a section arguing that the error
layer cannot reach the obstruction. **The target's verdict is nevertheless
correct as scoped, and this audit certifies the distinction:** the named
escape is a **C-L1 error STRUCTURE supplying an exact CANCELLATION**, under
C-L1's own witness (E1_RECORD_PHASE_OPERATOR_COLLAPSE_UNCERTIFIED). C-L2
supplies a **BOUND**, not a cancellation structure, and carries a different
witness. A bound can never furnish an exact cancellation. Hence "B-L2* is
UNREACHABLE **FROM C-L2**" survives intact — but the reader of §5.1 alone
cannot see that the qualifier "FROM C-L2" is doing real work. **The quote
should be extended to carry BL2*'s escape clause. Display-grade; the
verdict does not move.**

```text
VERDICT AT (d-2): CONFIRMED. B-L2* STANDS and is DISJOINT FROM THE C-L2
   ASSET'S REACH. The equal-time shortcut is cited only as an exclusion and
   is NOT resurrected — verified by scoped sweep: every occurrence of C6 in
   the target is an exclusion citation. NOT REFUTED carried correctly:
   nothing here shows any two-time object diverges.
```

---

## A7. CLAIM (d-3) — beta NOT EXTRACTABLE: CONFIRMED IN SUBSTANCE, CORRECTED IN FORM

```text
(g-1) THE ASSET MEETS TWO OF R-L2b's THREE DEMANDS. CERT u-b is verbatim:
      the constant 2 sqrt(3) is "a functional of NOTHING but the Clifford
      algebra", the estimate "holds for the indicator of ANY measurable
      region — every admitted cell of the D3 refinement, every radius, no
      cellulation datum consumed". That IS carrier-blindness plus
      cell-uniformity. The third demand — the EXPONENT — is untouched by
      such an object.                                          CONFIRMED.
(g-2) THE TWO-CELL SOLVE. Under the SUPPOSITION that G is cell-blind,
      V1^beta G = K = V2^beta G with V1 != V2 forces (V1/V2)^beta = 1, hence
      beta = 0 and G = K.                        ALGEBRA CONFIRMED (X1c).
(g-3) THE REFUSAL. beta is reserved to R-L2b, whose carrier says
      "PRODUCTION PROHIBITED ... No exponent" (RL2B-R :15-16, verbatim
      confirmed). Displaying the forced reading and declining to adopt it is
      the correct move under the commission's fences, and
      SCAD_HS_SCALING_EXPONENT_UNDERIVED is left standing.       CONFIRMED.
```

**COR-4 (correction-grade; the target's conclusion is UNDERSTATED, not
overstated).** Three sites — §6.2(g-2) :546, CH-3 :1114, and the flag block
:1253 — state that **"the ONLY exponent compatible with the asset is
beta = 0"**, dropping the supposition ("with G cell-blind") that the
derivation two lines earlier explicitly makes. Dropped, the statement is
false, and its correct replacement is stronger. Verified in a fresh venv
(this audit's own battery, X1a-X1d):

```text
X1a  S2b's OWN definition is G_cm := |C|_4^{-beta} · (certified bound).
     Then |C|_4^{beta} · G_cm = K identically, for EVERY beta. The product
     R.3 consumes is beta-FREE. No exponent is selected at all.
X1b  the two-cell system is satisfied IDENTICALLY for every beta once G is
     allowed the shape G = K |C|_4^{-beta}.
X1c  beta = 0 emerges ONLY under the extra supposition that G is cell-blind
     (sympy solve returns exactly {G: K, beta: 0} in that case).
X1d  what R-L2b actually demands is UNIFORMITY over the refinement
     quantifier, which is a ONE-SIDED constraint, not an equation:
     beta = +1/2 makes K V^{-beta} diverge as V -> 0; beta = -1/2 leaves it
     finite. Uniformity alone therefore bounds beta on one side and
     determines nothing.
CORRECTED STATEMENT (stronger, and the one the registrar should carry):
   THE CERTIFIED ASSET DETERMINES NO EXPONENT WHATSOEVER. It is not that
   beta must be 0; it is that every beta is compatible with a cell-constant
   bound, so the asset lies in the kernel of any map that could determine an
   exponent. The target's own VERDICT AT (d-3) (:558-563) already says
   exactly this ("the asset's cell-dependence is identically trivial, so it
   lies in the KERNEL of any map that could determine an exponent") — the
   three "ONLY exponent" sentences are inconsistent with the target's own
   verdict line and should be brought into agreement with it.
NET: (d-3) STANDS as NOT EXTRACTABLE. The correction removes a
   value-adjacent sentence ("the only compatible exponent is beta = 0") that
   the artifact then has to refuse; on the corrected reading there is
   nothing to refuse, which is cleaner under the fences. NOT verdict-moving.
```

---

## A8. CLAIM (d-0) — THE GRONWALL ESCALATION: CONFIRMED, WITH TWO CAVEATS OWED

### A8.1 The differential step, re-walked

```text
chi := Omega(s) psi, d/ds Omega = -i G(s) Omega, G(s) Hermitian.
d/ds ||h_0 chi||^2 = 2 Re <h_0 chi, h_0(-iG) chi>
                   = 2 Re <h_0 chi, -i(G h_0 + [h_0,G]) chi>.
The G h_0 piece: 2 Re(-i <h_0 chi, G h_0 chi>) = 2 Im <h_0 chi, G h_0 chi>,
and <h_0 chi, G h_0 chi> is REAL because G is Hermitian, so the piece
VANISHES.                                                    CONFIRMED.
=> d/ds ||h_0 chi||^2 = 2 Re <h_0 chi, -i [h_0,G] chi>.       CONFIRMED.
BILINEAR DISPLAY at phi = h_0 chi, psi = chi:
  sqrt(3)(||h_0 phi|| ||psi|| + ||phi|| ||h_0 psi||)
      = sqrt(3)(||h_0^2 chi|| ||chi|| + ||h_0 chi||^2).
The H^2 quantity ||h_0^2 chi|| appears with coefficient exactly
sqrt(3)||chi||; it does not cancel and is not dominated by
(||h_0 chi||, ||chi||). THE DIFFERENTIAL INEQUALITY DOES NOT CLOSE ON H^1.
                                                              CONFIRMED.
POLARIZATION: the quadratic display at chi + h_0 chi requires
||h_0(chi + h_0 chi)||, which again contains ||h_0^2 chi||.   CONFIRMED.
CONSISTENCY OF THE BILINEAR CONSTANT: setting phi = psi returns
2 sqrt(3) ||h_0 psi|| ||psi||, the class's own diagonal bound. The
bilinear display is the right normalization.                  CONFIRMED.
```

**COR-5 (two caveats owed, both running in the a-fortiori direction).**

```text
(5a) THE INSTRUMENT GRANTED EXCEEDS CLASS MEMBERSHIP. §3 defines H(c) by a
     DIAGONAL condition, |t[psi,psi]| <= 2c ||h_0 psi|| ||psi||. §7 then
     applies the class's BILINEAR display at an OFF-DIAGONAL pair. A
     diagonal bound does not yield that bilinear display with the same
     constant; polarization of a sesquilinear form costs a factor. §7's
     CH-4 grants class membership a fortiori but does not say it is also
     granting the bilinear display. IT MUST SAY SO. Direction: the granted
     instrument is STRONGER than the certified one, and the finding is that
     even it fails — so the negative is preserved and, if anything,
     reinforced.
(5b) THE EVALUATION LEAVES THE CERTIFIED FORM DOMAIN. The class lives on
     Q = D(h_0) = H^1(R^3) ⊗ C^4. The substitution phi := h_0 chi puts phi
     in Q only when chi lies in H^2. So the display's applicability and the
     finiteness of the majorant it produces coincide exactly: the step is
     licensed precisely when the H^2 control it concludes is needed is
     already in hand. This is the SAME fact the section reports, arrived at
     from the domain side — and it means the Gronwall fails to close for
     TWO reasons rather than one. Direction: preserved. It should
     nevertheless be displayed, since "a form bound applied outside its
     certified domain" is exactly the failure mode an audit must surface,
     and here it is BENIGN only because the conclusion is negative.
NET: (d-0) STANDS as NOT CLOSEABLE on the permitted instrument. CONFIRMED.
```

### A8.2 (d-0) is genuinely distinct from CERT's own statement of it

```text
CERT §3.3 (:298-305) names the residual and gives ONE reason it blocks: "a
Gronwall at form grade would consume ||[h_0, M(s)⊗S] Omega psi||, an
OPERATOR-grade quantity — exactly what is excluded."
§7 GRANTS THAT AWAY (CH-4) — it supposes the commutator form IS in the
class, which is precisely the hypothesis that would remove CERT's stated
obstruction — and then finds a SECOND, independent obstruction: the class
spends one derivative per slot and the Gronwall's left slot has already
spent it. THE FINDING IS NEW RELATIVE TO CERT, and correctly labelled as
"the CERT's own fourth" obstacle rather than as one of the commissioned
three. CH-8's decision to give it its own label is sound: without it,
§4.4's per-(n,cell) enclosures could be misread as reaching A(0).
                                                              CONFIRMED.
```

---

## A9. THE CAS BATTERY — RE-RUN IN A FRESH VENV, REPRODUCED BYTE-FOR-BYTE

```text
PROCEDURE (no testimony consumed): the script was EXTRACTED FROM THE SEALED
ARTIFACT BYTES (lines 733-997 of the target, 265 lines,
sha256 dd74290cf61c8bbe66f934cc3e9548df467ea97c28688d2450451d433c73ba05),
written to this session's scratchpad, and executed in a FRESH venv
(python3 -m venv; pip install sympy) reporting sympy 1.14.0 — the version
the target declares.
RESULT: exit 0; 50 PASS lines; 0 FAIL lines; "W3-BUILD BATTERY: 50/50 PASS";
        "FAILURES: NONE".
DIFF AGAINST THE ARTIFACT'S RECORDED §9.2 OUTPUT BLOCK (89 lines each):
        IDENTICAL — byte-for-byte, zero differing lines.
=> THE RECORDED VERBATIM OUTPUT IS AUTHENTIC. The target's claim "fresh
   venv, sympy 1.14.0, 50/50 PASS" is CONFIRMED at execution, not accepted
   at testimony.
```

### A9.1 The battery's substantive content, checked check-by-check

```text
GENUINELY COMPUTATIONAL AND LOAD-BEARING (verified independently by hand in
§A4-§A8, and re-verified in a second representation in §A9.2):
  W1a-W1h  Clifford relations, S^2 = I at the timelike axis, the spacelike
           alternative squaring to -I, h_0 symbol squaring to |k|^2 I_4.
  W2a-W2h  P_- projection/Hermitian/trace-2; C h_0 C = -|k| P_- EXACTLY;
           HS density 2|k|^2; multiplier symbols fixed under translation
           conjugation.
  W4b1-W4b3, W4b-W4k  the compressed oscillator identity, the Gram-PSD
           argument, the diagonal bound, both sharper enclosures, the
           headline reproduction, sharpness at every n, and the exact n = 2
           chain.  ALL RE-DERIVED BY HAND IN §A5 AND MATCHING.
  W6a, W6c the two-cell solve and the cell-independence of the constant.
  W7a, W7b, W7d  the H^2 term's appearance, its exact coefficient
           sqrt(3)||chi||, and its survival under polarization.
DECLARATIVE RATHER THAN COMPUTATIONAL — five of the fifty PASS lines assert
nothing that could have failed (OBS-4, methodology, not a defect of result):
  W3c   chk(..., True, ...)          — a structural statement, hardcoded.
  W4a   simplify((2N+1)-(2N+1)) == 0 — X - X = 0; the real check is W4b1.
  W4n   chk(..., True, ...)          — a carriage declaration (COR-A).
  W6b   chk(..., True, ...)          — a refusal declaration (CH-3).
  W5b   deg_asset == 0 where deg_asset = 0 two lines above — 0 == 0.
  Each is HONESTLY ANNOTATED in the script's own note field ("structural, no
  numeric content"; "identity carried; matrix check below"), so nothing is
  disguised. But a reader counting "50/50 PASS" should know that five are
  declarations. NONE of the five carries a load-bearing derivation: every
  substantive claim is either computed elsewhere in the battery or
  re-derived by hand in this audit.
TRANSCRIPTION-DEPENDENT (verified against the source carrier by this audit):
  W5c, W5d import the grades 0, -2, -3/2. The -2 and -3/2 are transcribed
  from BL2* :36-38 and CHECKED THERE BY THIS AUDIT (§A6.2). The arithmetic
  they then perform is trivial; the content is the transcription, and the
  transcription is correct.
```

### A9.2 THE AUDIT'S OWN INDEPENDENT BATTERY (14/14 PASS, fresh venv)

Written by this audit, not derived from the target's script:

```text
X1a-X1d  COR-4: the exponent is undetermined once cell-blindness is dropped
         (§A7). 4/4 PASS.
X2a-X2e  COR-2 ROUTE 2: a BOUNDED translation-invariant member of H(sqrt3)
         exists — symbol m(k) = k/(1+k), bounded with limit 1, vanishing
         linearly at k = 0, dominated by 2 sqrt(3)|k| since
         2 sqrt(3) k - k/(1+k) = k(2 sqrt(3)(1+k) - 1)/(1+k) > 0 — so the
         sea-sandwich theorem has a witness INSIDE its own stated
         hypotheses, and the corollary sup = +infinity is established
         without relying on an unbounded T. 5/5 PASS.
X3a-X3e  REPRESENTATION-INDEPENDENCE: the whole sandwich algebra re-derived
         in a SECOND representation (chiral/Weyl, alpha_j = diag(sigma_j,
         -sigma_j)) — Clifford relations, A^2 = I, P_- projection/Hermitian/
         trace 2, C h_0 C = -|k| P_- EXACTLY, HS density 2|k|^2. The
         target's TOY_SEPARATION claim that its Dirac representation is "an
         exhibit" and the identities are representation-independent is
         therefore VERIFIED, not accepted. 5/5 PASS.
TOTAL: W3-AUDIT EXTRA 14/14 PASS, FAILURES: NONE. Exact symbolic; no float
is a premise; no measured constant appears.
```

---

## A10. THE KILL HUNT — EVERY COMMISSIONED ITEM, RUN TO GROUND

```text
(K-1) THE OPERATOR-NORM ROUTE CONSUMED ANYWHERE  ->  NEGATIVE. NOT A KILL.
      A scoped sweep of the target for _op / "operator-norm" / "operator
      norm" returns twelve hits. Every one classified:
        :161, :171, :178, :188, :267, :275  — VERBATIM CITATIONS of the
          exclusion itself (quoting FP-3, W-3, MAP §3.5, E1 C-L2, and the
          artifact's own statement that it uses "nothing operator-normed").
        :594  — a statement that the operator-norm route IS excluded, given
          as the reason for using the bilinear display.
        :414, :672, :890, :1276  — ||h_0 Q||_op on the FINITE-RANK sealed
          Hermite compression, feeding the rank x op conversion.
        :782, :868, :1011  — the Clifford fact ||alpha_j S||_op = 1 and a
          sympy variable name (N_op) in the ladder construction.
      THE ADJUDICATION, at the record's own bytes: the route E1 excludes is
      the OPERATOR-NORM CERTIFICATION OF THE C-L2 COMMUTATOR against a sharp
      indicator, excluded because ||[h_0, 1_B]|| = +infinity (E1 :1138-1141).
      What §4.4 uses is the rank x (compressed op) conversion on a
      finite-rank carrier — which is (i) FINITE, (ii) the record's OWN sealed
      conversion (WALL F1: "rank x op is the ONLY sealed conversion"), (iii)
      the instrument FHB-A COR-A identifies as the one operating at HS grade
      ("at HS grade the witness saturates rank x (compressed op)"), and
      (iv) the exact instrument FHB-A COR-B itself used to derive the very
      enclosures being registered. NOWHERE does the target norm the
      continuum commutator, the sea sandwich, or any un-compressed C-L2
      object. THE EXCLUDED ROUTE IS NOT CONSUMED.
(K-2) A FORM BOUND APPLIED OUTSIDE ITS CERTIFIED DOMAIN  ->  ONE INSTANCE,
      DISPLAYED-A-FORTIORI, NOT A KILL (COR-5b). §7 evaluates the class
      display at phi = h_0 chi, which lies in Q = H^1 only for chi in H^2.
      The conclusion drawn is NEGATIVE, so the domain restriction can only
      strengthen it. Every other application is in domain: §4 applies the
      class to h_0 on D(h_0); §4.4 applies it to Hermite-carrier
      compressions, whose range is Schwartz and hence in H^1.
(K-3) n-UNIFORMITY CLAIMED WHERE THE RECORD SAYS "PROVABLY NOT FORM-IMPLIED"
      ->  NEGATIVE. Never claimed; disclaimed at :249, :428-431, :703-704,
      :1265. FHB's FB9 conclusion is consumed at audited grade and its
      witness is explicitly not re-run or extended.
(K-4) A STANDING BLOCKED POINT DECLARED MOVED WITHOUT ITS DISPLAY
      ->  NEGATIVE. Each of (d-1), (d-2), (d-3) — and the target's own added
      (d-0) — is declared moved and each carries a display: §4 (theorem +
      four verified hypotheses + corollary), §5 ((f-1) summand attribution
      at BL2*'s bytes + (f-2) degree arithmetic), §6 ((g-1) uniformity
      audit + (g-2) two-cell solve), §7 (differential step + bilinear
      substitution + polarization). ALL FOUR ARE DISPLAYED. Moreover none is
      declared DISCHARGED: every one is declared STANDING and moved AWAY
      from discharge, which is the direction that costs the program nothing
      it had.
(K-5) THE WITNESS TREATED AS RETIRED  ->  NEGATIVE. The target states
      "THIS ARTIFACT RETIRES NOTHING" at :21, ":82-83", ":690-691" and
      ":1292-1293", and names retirement as the registrar's act alone at
      each. E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED — STANDS.
      SCAD_HS_SCALING_EXPONENT_UNDERIVED — untouched, STANDS.
      E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER — untouched, STANDS.
      NO witness created. CORRECT ON ALL COUNTS.
(K-6) HIDDEN NUMERICS  ->  NEGATIVE. Scoped sweep for .evalf / N( / float( /
      multi-digit decimals / exponent notation / "approx" returns ZERO hits
      across all 1329 lines. Every constant in the artifact is an exact
      integer, rational, or surd (2 sqrt(3), sqrt(6n-3), 12, 24, 192,
      sqrt(6), -3/2, -2, 6 < 64). The only integers presented with
      magnitudes are PURE-MATHEMATICAL inequality constants, none offered as
      a physical quantity. Confirmed independently by the byte-identical
      re-run: no numeric evaluation occurs anywhere in the battery.
(K-7) FENCE-SCAN  ->  CLEAN. alpha_computed = false, proof_authorized =
      false, kappa_record_computed = false declared at :7 and re-declared at
      :1298-1299. No value, no number as magnitude, no measured-constant
      comparison, no frozen constant, no "137"/fine-structure/CODATA/
      measured/observed token anywhere except in the fence declarations
      themselves. Sharp M(t) unmodified (D6' :465-470 respected — no
      mollify/smooth/regularize token applied to M). Equal-time HS route not
      resurrected (every C6 occurrence is an exclusion citation). No
      baseline normed (no G_bl reappearance — E1 :715-718's
      SCAD_BASELINE_NORMED trigger is not tripped). No cellulation datum
      consumed. No register/tracker/road/plan/continuation file read.
(K-8) THE MAP EDITED  ->  NEGATIVE. §8 :684-687 and §12 :1287-1289 state
      that MAP §3.4/§3.5 stand unedited, that W-3 remains FP-3's sole
      displayed discharger, and that {W-1, W-2, W-3} remains the unique
      minimal completion set. What the target adds is a COST statement about
      W-3's landing, explicitly handed to the registrar. This audit
      re-derived MAP §3.4/§3.5 independently (§A2) and finds them untouched
      and still correct. CH-13's classification [FORCED BY SCOPE] is right.
```

---

## A11. THE LOAD-BEARING SENTENCE, TESTED HARDEST

The whole artifact turns on one claim, at §8 :681-687 and §12 :1285-1289:

```text
"C-L2's obligations (o1), (o2), (o3) are CERTIFIED of record. Obligation
 (o4) — 'Supplies S2b' — is now shown to be NOT A CONSEQUENCE of (o1)-(o3)
 AT ANY STRENGTH. Therefore W-3 CANNOT LAND ON C-L2's OWN PERMITTED ROUTE
 ALONE: its landing requires an input from outside the form class."
```

**VERDICT: CONFIRMED.** The derivation of that sentence is exactly the
conjunction §4 + §5 + §6 + §7, and each conjunct was re-derived above:

```text
(o1)(o2)(o3) certified — CERT §5 and CERT-A, both re-read; CERT's own
   status line CL2_S2B_SUPPLY_DISCHARGED = false (:683) names the same
   four failure causes. Not the target's claim; the record's.
(o4) not implied, at the CONVERSION step — §4's corollary: no finite F
   bounds ||C T C||_2 over the class, so no class-typed input converts form
   grade to HS grade at R.3's own sandwich. The quantifier is exactly right:
   the negative is over the CLASS, and the actual kernel is left UNDECIDED.
(o4) not implied, at the RESUMMATION step — §5: the failing degree-0 term
   is carried by P, and CL-ERROR §3.2 independently certifies that the
   C-L2 layer has NO degree-0 intraband component and cannot cancel one.
(o4) not implied, at the EXPONENT — §6 as corrected (§A7): the asset is
   cell-constant and determines no exponent at all.
(o4) not implied, at the GENERATOR->LAYER step — §7: the class is not
   closed under its own propagation Gronwall.
FOUR INDEPENDENT STEPS, EACH BLOCKED. "AT ANY STRENGTH" is justified for
(d-1) (constant-indifference is proved: the obstruction is insensitive to
the class constant) and for (d-3) (cell-constancy is a property of the
asset, not of its sharpness). For (d-2) it is justified in the scoped sense
"no sharpening of the quadratic-form certification" — which is what the
target actually writes at :490-495. For (d-0) it is justified relative to
the permitted instrument, which is what :620-621 writes. THE PHRASE IS NOT
OVERREACHED at any of the four.
```

**AND THE COST-STATEMENT IS CORRECTLY FENCED.** Saying "W-3's landing
requires an input from outside the form class" is a statement about what
W-3 costs, not a statement that W-3 is impossible, not a re-typing of FP-3,
and not an amendment to the map. MAP §3.5's uniqueness argument turns on
F3's certification route being SOLE, and that soleness is unaffected by a
finding that the sole route needs an external input. **The map survives the
target verbatim, as the target says it does.**

---

## A12. CORRECTIONS (five; all display- or hypothesis-grade; none verdict-moving)

```text
COR-1  §0 :36-42 advertises the sea-sandwich proof as an "exact two-line
       KERNEL argument"; §4.1 supplies a compactness/weak-null argument.
       The advertised proof is never executed. FIX: either re-word §0 to
       name the compactness argument, or add the kernel proof (||K||_2^2 =
       |R^3| ||kappa||_2^2) — which would also discharge COR-2.
COR-2  §4.1's theorem hypothesizes T BOUNDED; §4.2 (h-4) applies it at
       T = h_0, which is unbounded. The corollary is TRUE — by immediate
       unboundedness, and independently via the bounded translation-
       invariant member exhibited in this audit's X2 (symbol k/(1+k)) — but
       the theorem as stated does not cover its own application. FIX: name
       the bounded witness, or drop "bounded" and argue via the kernel.
COR-3  §5.1 truncates the B-L2* quote immediately before that blocker's own
       named in-spec escape ("a certified C-L1 error structure supplying an
       exact cancellation ... witness
       E1_RECORD_PHASE_OPERATOR_COLLAPSE_UNCERTIFIED"). The verdict
       "UNREACHABLE FROM C-L2" survives — a C-L2 BOUND cannot furnish a
       C-L1 CANCELLATION STRUCTURE, and the two carry different witnesses —
       but the displayed block hides that the qualifier is load-bearing.
       FIX: extend the quote and add the one-line distinction.
COR-4  §6.2(g-2) :546, CH-3 :1114, and flag :1253 state "the ONLY exponent
       compatible with the asset is beta = 0" while dropping the "G
       cell-blind" supposition the derivation makes. Without it EVERY beta
       is compatible (X1a/X1b), which is a STRONGER non-extractability and
       agrees with the target's own verdict line :558-563. FIX: restore the
       supposition, or replace the three sentences with the stronger
       statement — which also removes a value-adjacent sentence the artifact
       then has to refuse.
COR-5  §7 (a) grants the class's BILINEAR display, strictly more than
       membership in the DIAGONAL class §3 defines, without saying so in
       CH-4; and (b) evaluates it at phi = h_0 chi, outside the certified
       form domain Q = H^1 unless chi is in H^2. Both run a fortiori and
       preserve the negative; (b) in fact supplies a SECOND reason the
       Gronwall does not close. FIX: display both caveats in CH-4 and §7.1.
```

## A13. OBSERVATIONS (no correction required)

```text
OBS-1  Two line-locus imprecisions, both harmless. (i) §3 :290-292 cites
       "E1 :811-814" for the sandwich A(0) = C (error layer) C; :812 does
       say "A(0) is the C-L2 error", but the SANDWICH form is written at
       E1 :659-661 (A(a) := C(V(a)-1)C) with C fixed at E1 :320 and the
       same-phase reduction at E1 :755-756. The claim is TRUE of record;
       the pointer is partial. (ii) §5.1 cites "BL2* :30-45"; the block
       actually runs :32-48. Neither affects a result.
OBS-2  §4.2 (h-1) sources the sea symbol (I - alpha·k-hat)/2 to "r-2
       :144-148, via FHB §4.1" when E1 C1 :321 — already sealed and
       consumed in the same artifact — carries it directly. Downstream but
       valid provenance.
OBS-3  §5's (f-1) has a stronger sealed basis than it uses. CL-ERROR §3.2
       (:194, :259-260, :267-270) states outright that "E_int has NO
       degree-0 intraband component" and that "A degree-(<= -1) symbol
       cannot identically cancel a degree-0 symbol". The target cites
       CL-ERROR only for the generator's identity. Recommend re-displaying
       (f-1) at CL-ERROR §3.2's grade — it converts a summand-bookkeeping
       argument into an exact degree separation already of record.
OBS-4  Five of the battery's fifty PASS lines are declarations rather than
       computations (W3c, W4a, W4n, W6b, W5b — see §A9.1). All are honestly
       annotated in the script and none carries a load-bearing derivation.
       A registrar reading "50/50" should read it as "45 computed + 5
       declared".
OBS-5  (m-1) is a REGISTRATION of FHB-A COR-B's own displays, independently
       re-derived, not new mathematics. The target says so; this audit
       records it explicitly so the registrar does not double-count it as
       program movement.
OBS-6  §0's compressed phrase "the obstruction is a property of C, not of
       the member" is looser than §4.3 ADDS (2)'s precise "ANY translation-
       invariant sandwich kills ANY translation-invariant member's image".
       The precise form is the one to carry.
OBS-7  The target's verdict-slot choice (CH-1: UNDECIDABLE-TODAY rather than
       PARTIAL) is defensible and, in this audit's judgement, correct: no
       consuming quantity moved, and the only positive content (m-1) is a
       carry-forward. The target explicitly hands the re-slotting to the
       registrar, which is the right disposition.
```

---

## A14. THE TARGET'S CHOICE LEDGER, AUDITED CHOICE BY CHOICE

```text
CH-1  verdict slot UNDECIDABLE-TODAY over PARTIAL.        SOUND (OBS-7).
CH-2  compactness proof form over re-citing the formal-Lambda display.
      SOUND — it removes the WB7 license from a standing negative, which is
      real gain (FHB :336-337 confirmed as the licensed display). The two
      standard facts invoked (HS => compact; U_a -> 0 weakly) are genuinely
      standard and were re-walked in §A4.1. See COR-1/COR-2 on the write-up.
CH-3  beta = 0 displayed but refused.                     SOUND IN POSTURE,
      but the premise sentence needs COR-4. Refusing to write a reserved
      symbol's value is exactly right under the fences.
CH-4  §7 grants a favourable assumption to defeat it.     SOUND — the
      direction is correct and no positive claim rests on it. Incomplete as
      written: it must also declare the bilinear display grant (COR-5a).
CH-5  FHB consumed at AUDITED grade, not headline grade.  SOUND and
      important — COR-A, COR-B, COR-C are all applied, and the build's
      headline enclosures appear only as the objects superseded. Verified.
CH-6  the sharper enclosures registered here.             SOUND — COR-B's
      condition ("if the enclosures are consumed") is genuinely met, and
      the re-derivation is genuine rather than transcribed (§A5). OBS-5.
CH-7  both readings of S2b attempted.                     SOUND — declining
      to pick one made the negative broader, not cheaper. Both do fail for
      the same reason (§4.2's corollary), as claimed.
CH-8  the CERT residual labelled (d-0) and confronted.    SOUND — and this
      audit judges it the target's best judgement call: without (d-0), §4.4
      would read as reaching A(0), which it does not (§A8.2).
CH-9  F1 / r-3 / MO-3 cited, not re-walked.               CORRECTLY CLASSED
      [FORCED BY SCOPE]. The commission is W-3, not F1.
CH-10 sweep terms and cutoff declared.                    SOUND — the
      negative is stated as bounded by its terms, which is the honest form.
      This audit's own sweep (§A1.2) is declared on the same discipline.
CH-11 "Q-..." not chased.                                 FORCED. Correct.
CH-12 no H^2 estimate / localization input / dressing lemma authored.
      CORRECTLY CLASSED [FORCED BY FENCE] — each is exactly the missing
      object, and authoring any would be the authored-physics fence. The
      target does not author any of them; verified by reading §7 and §8's
      deciding-object block, which name them as MEMBER-SUPPLIED.
CH-13 the map is not edited.                              VERIFIED (§A10 K-8).
NO UNDECLARED UNFORCED CHOICE FOUND. This audit searched for one and found
none: the four verdict-bearing sections each declare their instrument, and
the two places where an undeclared choice WAS made (the bilinear-display
grant, and the cell-blindness supposition) are surfaced here as COR-5a and
COR-4 rather than hidden.
```

## A15. THIS AUDIT'S CHOICE LEDGER (commission O3SR; every unforced choice)

```text
AC-1  VERDICT SLOT: CONFIRMED-WITH-CORRECTIONS rather than CONFIRMED.
                                                      [UNFORCED — DECLARED]
      CONFIRMED was available: none of the five corrections moves a verdict,
      and three are pure display. It was declined because COR-2 (a theorem
      applied outside its stated hypotheses) and COR-4 (a stated uniqueness
      that does not follow from the displayed derivation) are the kind of
      defect a DEFAULT-REFUTE audit exists to name, even when the result
      survives. REFUTED was never in reach: every load-bearing step
      re-derived.
AC-2  I RE-DERIVED RATHER THAN RE-READ.                        [UNFORCED]
      §A4-§A8 re-walk the mathematics by hand before consulting the
      target's CAS; §A5's enclosures were re-derived from FHB's sealed
      r_n/E_n rather than from the target's script; the battery was
      extracted from sealed bytes and re-run rather than read.
AC-3  I WROTE AN INDEPENDENT BATTERY (X1-X3).                  [UNFORCED]
      Re-running the target's script only tests reproducibility. X1 tests
      whether the beta claim survives dropping its supposition; X2 tests
      whether the theorem has an in-hypothesis witness; X3 tests
      representation-independence. Each targets a claim the target's own
      battery could not falsify.
AC-4  I CONSULTED CL-ERROR §3.2 THOUGH THE TARGET DID NOT.     [UNFORCED]
      CL-ERROR is a sealed artifact already in the target's consumed set,
      so opening it at a second locus is a scoped read within the declared
      sweep. It STRENGTHENED the target (OBS-3); had it contradicted it,
      that would have been a kill. Hunting where the builder did not look
      is the audit's job.
AC-5  I DID NOT AUTHOR THE MISSING OBJECTS.        [FORCED BY FENCE]
      The kernel proof of COR-1, the bounded witness of COR-2, and the
      corrected beta statement of COR-4 are all DETERMINATIONS about
      existing displays — no new estimate, no exponent, no rate, no H^2
      lemma, no localization input is supplied here. The deciding object
      remains member-supplied and absent.
AC-6  I DID NOT RE-SLOT THE TARGET'S VERDICT.      [FORCED BY SCOPE]
      UNDECIDABLE-TODAY is the builder's slot and the registrar's to
      change; this audit confirms it is defensible and stops there.
AC-7  SWEEP TERMS AND CUTOFF as declared at §A1.2.  [UNFORCED — DECLARED]
```

## A16. TOY_SEPARATION (this audit)

```text
ACTUAL SURFACE — this audit reasoned about the sealed objects directly:
  * The sealed digests themselves: 14 recomputed, 14 sidecars re-checked
    from the artifact's own directory. Not testimony — bytes.
  * C = 1_(-inf,0)(h_0) with E1 C1's own symbol; h_0 = alpha·p; the class
    H(sqrt3) with its actual constant and its actual domain D(h_0) = H^1;
    the sealed Hermite carrier with r_n = 4n^3 and E_n = 6n^4/ell^2; the
    clause bytes of C-L2, S2b, R.3, R-L2b, C6, D6', C1, C4, F1/F2/F3, B-L2*.
  * The two MAP sections read IN FULL, and their audit's re-derivation.
  * The target's CAS SCRIPT EXTRACTED FROM SEALED BYTES and executed.
FINITE EXHIBITS, NEVER GROUND:
  * X3's chiral representation is an EXHIBIT establishing that the Dirac-rep
    identities are representation-independent consequences of the Clifford
    relations. The ground is the Clifford algebra, not either matrix set.
  * X2's symbol k/(1+k) is an EXHIBIT witnessing non-emptiness of {bounded
    TI members of H(sqrt3)}. No property of the actual C-L2 kernel is
    inferred from it; it repairs a theorem's hypothesis fit, nothing more.
  * The target's W4b1-W4b3 finite range n = 2..6 is corroboration; the
    ground is the general PSD/Gram argument, which this audit re-derived in
    general form at §A5 without reference to any finite n.
SYMBOLIC BOOKKEEPING, NOT PHYSICAL CONTENT:
  * X1's V, K, beta, G are placeholders in a determination about what a
    cell-CONSTANT bound can and cannot select. No cell is evaluated, no
    4-volume is valued, no exponent is produced or adopted.
  * The degrees 0, -2, -3/2 are READ OFF B-L2*'s own graded bookkeeping and
    checked against its bytes. No amplitude is computed and no grading is
    invented.
NO TOY IS PROMOTED. Nothing in this audit converts an exhibit into a claim
about any closure quantity. Every verdict above is either (i) an exact
statement about sealed objects, (ii) a reproduction check on sealed bytes,
or (iii) an explicitly one-sided statement about an INSTRUMENT.
```

---

## A17. VERDICT PER CLAIM

```text
CLAIM                                                          VERDICT
--------------------------------------------------------------------------
C1  Seals: 14 consumed artifacts verified from the artifact's
    own directory; 6 cross-checked against CERT §1              CONFIRMED
    (this audit recomputed all 14 independently: 14/14 agree)   (STRENGTHENED)
C2  Sweep cutoff declared; bridge located by sweep and
    identified at bytes                                         CONFIRMED
C3  The clause bytes (C-L2, S2b, R.3, R-L2b, C6, D6', C1, C4,
    F1/F2/F3, B-L2*, CERT u-a/u-b/§3.3, FHB, FHB-A) quoted
    verbatim-accurately                                         CONFIRMED
    (two line-locus imprecisions, OBS-1)                        (w/ OBS)
C4  (d-1) THE SEA-SANDWICH OBSTRUCTION: theorem valid           CONFIRMED
C5  (d-1) applied at R.3's site: four hypotheses verified       CONFIRMED
    (theorem stated for bounded T, applied at unbounded h_0)    w/ COR-2
C6  (d-1) COROLLARY sup over H(sqrt3) of ||C T C||_2 = +inf;
    no finite F; READING A false as an inference; READING B
    has no conversion at this site                              CONFIRMED
C7  (d-1) ADDS (1) license removed / (2) member -> sandwich /
    (3) escape type derived as symmetry-breaking                CONFIRMED
C8  (d-1) DOES-NOT-ADD scoping: the +infinity attaches to the
    class supremum and to h_0's sandwich, NOT to the actual
    C-L2 error layer, which remains UNDECIDED                   CONFIRMED
C9  §4.4 carrier form bound |p|^2 <= (6n-3)/ell^2 by exact
    PSD/Gram route, no radicals, no numerics                    CONFIRMED
C10 §4.4 enclosures 12 n^{3/2} sqrt(2n-1)/ell (order n^2) and
    24 n^3 sqrt(2n-1)/ell (order n^{7/2}); supersede the FHB
    headline at every n >= 1; n = 2 chain 24 sqrt(6) < 192
    <=> 6 < 64                                                  CONFIRMED
    (= FHB-A COR-B's own displays, correctly attributed)        (OBS-5)
C11 COR-A carried: no HS-grade order-sharpness claimed          CONFIRMED
C12 §4.4 not n-uniform, not G_cm; FB9 respected                 CONFIRMED
C13 (d-2)(f-1) the degree-0 term is carried by P, not by the
    [error] layer C-L2 certifies                                CONFIRMED
    (CL-ERROR §3.2 certifies it more strongly — OBS-3;
     BL2*'s own escape clause omitted from the quote)           w/ COR-3
C14 (d-2)(f-2) the asset's coincidence degree is exactly 0;
    above the -3/2 HS threshold by 3/2; short of the -2
    per-stratum gain by 2; t-uniformity creates no q-power      CONFIRMED
C15 (d-2) B-L2* STANDS and is UNREACHABLE FROM C-L2             CONFIRMED
C16 (d-3)(g-1) the asset meets carrier-blindness and
    cell-uniformity but cannot carry an exponent                CONFIRMED
C17 (d-3)(g-2) "the ONLY exponent compatible is beta = 0"       CORRECTED
    -> the asset determines NO exponent whatsoever; every beta
       is compatible once the unstated cell-blindness
       supposition is dropped (stronger, and agreeing with the
       target's own verdict line)                              (COR-4)
C18 (d-3)(g-3) the reading refused, symbol left to R-L2b        CONFIRMED
C19 (d-0) the Gronwall charges ||h_0^2 chi|| with coefficient
    exactly sqrt(3)||chi||; does not close on H^1; polarization
    does not evade it; the class is not closed under its own
    propagation Gronwall                                        CONFIRMED
    (bilinear grant and out-of-domain evaluation undisplayed)   w/ COR-5
C20 (d-0) is distinct from CERT §3.3's own stated obstruction   CONFIRMED
C21 CAS: fresh venv, sympy 1.14.0, 50/50 PASS, verbatim output  CONFIRMED
    (reproduced BYTE-FOR-BYTE from sealed script bytes)         (EXECUTED)
C22 Fences held; no value; no measured constant; M(t) unsoftened;
    C6 not resurrected; no baseline normed                      CONFIRMED
C23 Witnesses stand; nothing retired; no witness created        CONFIRMED
C24 MAP §3.4/§3.5 unedited; W-3 sole discharger; {W-1,W-2,W-3}
    unique minimal completion set untouched                     CONFIRMED
C25 THE LOAD-BEARING SENTENCE: (o4) is NOT A CONSEQUENCE of
    (o1)-(o3) at any strength; W-3 cannot land on C-L2's own
    permitted route alone                                       CONFIRMED
C26 NOT REFUTED carried in full: every negative one-sided; no
    divergence exhibited for any closure quantity               CONFIRMED
C27 §0 describes the proof as a "kernel argument"               CORRECTED
    -> §4.1 gives a compactness argument; advertised proof
       never executed                                          (COR-1)
--------------------------------------------------------------------------
OVERALL                                          CONFIRMED-WITH-CORRECTIONS
```

---

## A18. FLAG BLOCK — STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001

```text
COMMISSION      : O3SR — symmetric audit of W-3 / G_cm at the consuming
                  (HS) type. DEFAULT-REFUTE; testimony zero weight.
AUDITOR         : W3-AUDIT, 2026-08-15.
TARGET          : STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md
                  sha256 241262ffc7da05b5cef6638f6f7ac5dd8aa4af5ff35becab92cb17f062cbfca9
                  seal verified OK from the artifact's own directory.

OVERALL VERDICT : CONFIRMED-WITH-CORRECTIONS.
  The target's UNDECIDABLE-TODAY verdict, its four standing obstacles, its
  scoping, its fences and its refusal to retire anything all SURVIVE a
  default-refute re-derivation at bytes. 27 claims adjudicated: 25
  CONFIRMED (3 of them carrying a correction), 2 CORRECTED. NONE REFUTED.

CORRECTIONS (5; none verdict-moving):
  COR-1  §0 advertises a "kernel argument"; §4.1 gives a compactness
         argument. The advertised proof is never executed. DISPLAY.
  COR-2  §4.1's theorem hypothesizes T BOUNDED; (h-4) applies it at the
         UNBOUNDED h_0. Conclusion survives twice over (unboundedness =>
         not HS; and a bounded TI member of H(sqrt3) exists — this audit
         exhibits symbol k/(1+k), X2). HYPOTHESIS FIT.
  COR-3  §5.1 truncates the B-L2* quote before that blocker's own named
         in-spec escape (a certified C-L1 error STRUCTURE supplying an
         exact CANCELLATION, under C-L1's own witness). "UNREACHABLE FROM
         C-L2" survives — a bound is not a cancellation structure — but the
         qualifier's load-bearing role is hidden. DISPLAY / COMPLETENESS.
  COR-4  "the ONLY exponent compatible with the asset is beta = 0" (three
         sites) drops the derivation's own cell-blindness supposition.
         Corrected and STRENGTHENED: the asset determines NO exponent
         whatsoever. Agrees with the target's own verdict line. SUBSTANCE,
         in the target's favour.
  COR-5  §7 grants the BILINEAR display (more than the diagonal class
         membership §3 defines) and evaluates it at phi = h_0 chi, outside
         Q = H^1 unless chi is in H^2. Both a fortiori; (b) supplies a
         second reason the Gronwall fails. CAVEATS OWED.

OBSERVATIONS: 7 (OBS-1..OBS-7), including OBS-3 — CL-ERROR §3.2 ("E_int
  has NO degree-0 intraband component"; "a degree-(<= -1) symbol cannot
  identically cancel a degree-0 symbol") certifies the target's (f-1) more
  strongly than the target's own display, and is uncited there.

KILL HUNT: NEGATIVE ON EVERY COMMISSIONED ITEM.
  operator-norm route consumed .................... NO (12 hits classified;
    all are exclusion citations, Clifford facts, or the record's OWN
    finite-rank rank x op conversion — the instrument FHB-A COR-A names as
    operating at HS grade and COR-B itself used).
  form bound outside its certified domain ......... ONE, displayed-a-fortiori
    (COR-5b); every other application in domain.
  n-uniformity claimed ............................ NO (disclaimed 4x).
  standing point moved without its display ........ NO (all four displayed;
    none declared discharged).
  witness treated as retired ...................... NO (retirement named as
    the registrar's act alone at four sites).
  hidden numerics ................................. NO (zero floats, zero
    evalf, zero measured constants across 1329 lines).

CAS: target battery EXTRACTED FROM SEALED BYTES (script sha256
  dd74290cf61c8bbe66f934cc3e9548df467ea97c28688d2450451d433c73ba05) and
  re-run in a FRESH venv, sympy 1.14.0 -> 50/50 PASS, output IDENTICAL
  byte-for-byte to the artifact's recorded §9.2 block (89/89 lines).
  This audit's OWN independent battery: 14/14 PASS (X1 beta
  undetermination; X2 bounded in-hypothesis witness; X3 the sandwich
  algebra re-derived in a second, chiral representation).

FENCES HELD (this audit): alpha_computed = false ; proof_authorized = false ;
  kappa_record_computed = false. No value, no number as magnitude, no
  measured-constant comparison, no frozen constant. DETERMINATION /
  CONSTRUCTION ONLY — the kernel proof, the bounded witness and the
  corrected beta statement are determinations about existing displays, not
  new estimates, exponents, rates, or lemmas. Sharp M(t) untouched. C6 not
  resurrected. No baseline normed. No register/tracker/road/plan/
  continuation/ledger/lens file read. "Q-..." EXPECTED-UNLOCATABLE, not
  chased. No git action. Output path probed ABSENT before first write.

MAP: STAGE8_COMPLETION_MAP_T17SR_V001 §3.4 and §3.5 READ IN FULL and
  independently re-derived (§A2). The unique minimal completion set
  {W-1, W-2, W-3} STANDS; W-3 remains FP-3's sole displayed discharger;
  the commissioned absences A-1..A-7 still cannot touch FP-3. THE TARGET
  DOES NOT EDIT THE MAP AND THIS AUDIT DOES NOT EITHER.

FP-3 : STANDS.
WITNESSES: E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED — STANDS.
  SCAD_HS_SCALING_EXPONENT_UNDERIVED — STANDS.
  E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER — STANDS.
  THIS AUDIT RETIRES NOTHING AND CREATES NOTHING. Retirement is the
  registrar's act alone.

NOT REFUTED: every negative in the target AND in this audit is ONE-SIDED.
  No divergence is exhibited for tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2,
  A(0), E_int, E_prof, x, eta_1, or any wall quantity; all remain
  UNDECIDED. The +infinity of §4 attaches to a CLASS SUPREMUM and to
  translation-invariant members' sandwiches — objects of the argument, not
  objects of the closure.

NO STRONGER READING: nothing in this audit bounds, values, or decides any
  closure quantity. Its content is adjudication of a sealed artifact.

SWEEP CUTOFF: 2026-08-15, this session; roots and terms declared at §A1.2.
```

## A19. CAS — THIS AUDIT'S OWN BATTERY: SCRIPT AND VERBATIM OUTPUT

Provenance: `python3 -m venv w3auditvenv` in this session's scratchpad;
`pip install sympy` (reports 1.14.0); script run once, output captured
verbatim and appended here by file-append. Exact symbolic only.

### A19.1 Script (`w3_audit_extra.py`)

```python
# W3-AUDIT INDEPENDENT CHECKS — exact symbolic only (sympy, fresh venv).
# X1  COR-4: without the cell-blindness supposition, EVERY beta is compatible.
# X2  COR-2 ROUTE 2: a BOUNDED translation-invariant member of H(sqrt3) exists.
# X3  the sandwich algebra re-derived in a SECOND (Weyl/chiral) representation.
from sympy import *
P=[]
def chk(t,c,n=""):
    P.append((t,bool(c))); print(("PASS " if c else "FAIL ")+t+("  "+n if n else ""))

print("="*74); print("X1 — COR-4: beta is UNDETERMINED once cell-blindness is dropped"); print("="*74)
b,V,K = symbols('beta V K', positive=True), symbols('V', positive=True), symbols('K', positive=True)
bta = symbols('beta', real=True); V = symbols('V', positive=True); K = symbols('K', positive=True)
G_of_V = K*V**(-bta)                      # S2b's own shape: G_cm = |C|_4^{-beta} * (bound)
chk("X1a  S2b's own definition makes |C|_4^beta * G_cm = K for EVERY beta",
    simplify(V**bta * G_of_V - K)==0, "=> the product is beta-free: no exponent is selected")
V1,V2 = symbols('V1 V2', positive=True)
chk("X1b  the two-cell system is satisfied identically for EVERY beta",
    simplify(V1**bta*(K*V1**(-bta)) - K)==0 and simplify(V2**bta*(K*V2**(-bta)) - K)==0)
G = symbols('G', positive=True)
sol = solve([Eq(V1**bta*G,K), Eq(V2**bta*G,K)],[bta,G],dict=True)
print("        with G FORCED cell-blind ->", sol)
chk("X1c  beta = 0 emerges ONLY under the extra supposition 'G cell-blind'",
    any(s.get(bta)==0 for s in sol))
chk("X1d  uniformity under refinement (V->0) alone forces only beta <= 0, not beta = 0",
    (limit(K*V**(-Rational(1,2)),V,0,'+')==oo) and (limit(K*V**(Rational(1,2)),V,0,'+')==0),
    "beta=+1/2 diverges; beta=-1/2 stays finite => a one-sided constraint, not an equation")

print(); print("="*74); print("X2 — COR-2 ROUTE 2: a BOUNDED TI member of H(sqrt3)"); print("="*74)
k = symbols('k', positive=True)
m = k/(1+k)                                # bounded, real, vanishes linearly at k=0
chk("X2a  m is bounded on (0,oo): limit = 1", limit(m,k,oo)==1)
chk("X2b  m vanishes linearly at k=0: m/k -> 1", limit(m/k,k,0,'+')==1)
chk("X2c  |m(k)| <= 2 sqrt(3) |k| for all k > 0 (so Cauchy-Schwarz gives membership)",
    simplify(2*sqrt(3)*k - m).is_positive is not False and
    simplify((2*sqrt(3)*k - m)*(1+k)) == simplify(2*sqrt(3)*k*(1+k) - k),
    "2sqrt3 k - k/(1+k) = k(2sqrt3(1+k)-1)/(1+k) > 0 since 2sqrt3 > 1")
chk("X2d  its sandwich m(k) P_-(khat) is NONZERO (tr = 2 m(k) != 0)", simplify(2*m)!=0)
chk("X2e  => theorem applies with BOUNDED T; corollary sup = +oo has an in-hypothesis witness",
    True, "structural: b bounded + TI + in H(sqrt3) + C b C != 0")

print(); print("="*74); print("X3 — THE SANDWICH IN A SECOND REPRESENTATION (Weyl/chiral)"); print("="*74)
I2=eye(2); Z2=zeros(2,2)
s1=Matrix([[0,1],[1,0]]); s2=Matrix([[0,-I],[I,0]]); s3=Matrix([[1,0],[0,-1]]); sg=[s1,s2,s3]
alW=[Matrix(BlockMatrix([[ s,Z2],[Z2,-s]])) for s in sg]      # chiral alpha_j = diag(sigma, -sigma)
I4=eye(4)
chk("X3a  chiral alphas obey {alpha_i,alpha_j} = 2 delta_ij I4",
    all(simplify(alW[i]*alW[j]+alW[j]*alW[i]-2*(1 if i==j else 0)*I4)==zeros(4,4)
        for i in range(3) for j in range(3)))
th,ph=symbols('theta phi', real=True)
kh=[sin(th)*cos(ph), sin(th)*sin(ph), cos(th)]
A=sum([kh[j]*alW[j] for j in range(3)], zeros(4,4))
chk("X3b  A^2 = I4 in the chiral rep", simplify(A*A-I4)==zeros(4,4))
Pm=(I4-A)/2
chk("X3c  P_- projection, Hermitian, trace 2 (rep-independent)",
    simplify(Pm*Pm-Pm)==zeros(4,4) and simplify(Pm-Pm.H)==zeros(4,4) and simplify(trace(Pm)-2)==0)
km=symbols('kmag', positive=True)
ms=simplify(Pm*(km*A)*Pm)
chk("X3d  C h_0 C = -|k| P_-  EXACTLY in the chiral rep too", simplify(ms+km*Pm)==zeros(4,4))
chk("X3e  HS density tr(m^dag m) = 2|k|^2 in the chiral rep too",
    simplify(trace(ms.H*ms)-2*km**2)==0)

print(); print("="*74)
print("W3-AUDIT EXTRA: %d/%d PASS" % (sum(1 for _,x in P if x), len(P)))
print("FAILURES:", [t for t,x in P if not x] or "NONE")
print("="*74)
```

### A19.2 Output (verbatim)

```text
==========================================================================
X1 — COR-4: beta is UNDETERMINED once cell-blindness is dropped
==========================================================================
PASS X1a  S2b's own definition makes |C|_4^beta * G_cm = K for EVERY beta  => the product is beta-free: no exponent is selected
PASS X1b  the two-cell system is satisfied identically for EVERY beta
        with G FORCED cell-blind -> [{G: K, beta: 0}]
PASS X1c  beta = 0 emerges ONLY under the extra supposition 'G cell-blind'
PASS X1d  uniformity under refinement (V->0) alone forces only beta <= 0, not beta = 0  beta=+1/2 diverges; beta=-1/2 stays finite => a one-sided constraint, not an equation

==========================================================================
X2 — COR-2 ROUTE 2: a BOUNDED TI member of H(sqrt3)
==========================================================================
PASS X2a  m is bounded on (0,oo): limit = 1
PASS X2b  m vanishes linearly at k=0: m/k -> 1
PASS X2c  |m(k)| <= 2 sqrt(3) |k| for all k > 0 (so Cauchy-Schwarz gives membership)  2sqrt3 k - k/(1+k) = k(2sqrt3(1+k)-1)/(1+k) > 0 since 2sqrt3 > 1
PASS X2d  its sandwich m(k) P_-(khat) is NONZERO (tr = 2 m(k) != 0)
PASS X2e  => theorem applies with BOUNDED T; corollary sup = +oo has an in-hypothesis witness  structural: b bounded + TI + in H(sqrt3) + C b C != 0

==========================================================================
X3 — THE SANDWICH IN A SECOND REPRESENTATION (Weyl/chiral)
==========================================================================
PASS X3a  chiral alphas obey {alpha_i,alpha_j} = 2 delta_ij I4
PASS X3b  A^2 = I4 in the chiral rep
PASS X3c  P_- projection, Hermitian, trace 2 (rep-independent)
PASS X3d  C h_0 C = -|k| P_-  EXACTLY in the chiral rep too
PASS X3e  HS density tr(m^dag m) = 2|k|^2 in the chiral rep too

==========================================================================
W3-AUDIT EXTRA: 14/14 PASS
FAILURES: NONE
==========================================================================
```

### A19.3 Reproduction of the TARGET's battery (fresh venv, sealed script bytes)

```text
script extracted from target lines 733-997 (265 lines)
script sha256 = dd74290cf61c8bbe66f934cc3e9548df467ea97c28688d2450451d433c73ba05
venv: python3 -m venv w3auditvenv ; sympy 1.14.0
run  : exit 0 ; 50 PASS lines ; 0 FAIL lines
tail : W3-BUILD BATTERY: 50/50 PASS  /  FAILURES: NONE
diff against the artifact's recorded §9.2 block (89 lines vs 89 lines):
  IDENTICAL — byte-for-byte, zero differing lines.
```

---

*END — STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001*
