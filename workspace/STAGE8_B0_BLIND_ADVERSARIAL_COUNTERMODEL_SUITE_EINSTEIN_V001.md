# STAGE8_B0_BLIND_ADVERSARIAL_COUNTERMODEL_SUITE_EINSTEIN_V001

LANE: EINSTEIN
RELAY: 203 ("BUILD THE COUNTERMODEL THAT WOULD KILL ANY `B0` CONSTRUCTION")
DATE: 2026-07-31
REGISTER HEAD AT ISSUE: Q-109.
ROAD JUSTIFICATION (Q-83): `ADVANCES STEP 1` — by supplying the Q-92(f) adversarial countermodel
that any `B0` construction must survive, and by finding that one countermodel cannot be
discriminated at all.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.
NOTHING WAS COMPUTED. **`B0` WAS NOT CONSTRUCTED.** Every object below is a description of a
*wrong* object, built to be checked against sealed constraints — never a candidate `B0`.
No flag moves; no debt is discharged; `COMPLETE_MICROSCOPIC_BOUNDARY_OPERATOR_derived = false |
TYPE-U` and `B0_constructible_now = NO_VERDICT` are undisturbed.

*** BLIND-ATTACK FENCE, HONOURED AND ATTESTED. The two route artifacts —
`STAGE8_B0_CODOMAIN_ROUTE_UNDERDETERMINATION_TEST_V001.md` (Codex 1) and
`STAGE8_IPRIM_ROUTE_TO_B0_STOPPING_POINT_AUDIT_V001.md` (Codex 2) — WERE NEVER OPENED, by this
lane or by any agent under it, and were excluded by name from every recursive search. Their
filenames were visible in a directory listing and were not used as evidence. One agent
self-reported a possible exposure to a register summary and failed to file the disclosure; that
lapse is recorded in §7 and the affected sub-claim is downgraded accordingly. ***

METHOD: five attack tasks (modes / build / codomain / discriminators / indiscriminable), each
adversarially verified under a **symmetric-framing duty** — verifiers were required to report a
working corpus defence as enthusiastically as a hole, and to hunt the clause that would kill each
surviving countermodel. Run wf_5a8e307b-9ab, ten agents. Statuses: modes WEAKENED, build WEAKENED,
codomain WEAKENED, discriminators WEAKENED, indiscriminable **CONFIRMED**. *** TWO VERIFIER
CORRECTIONS **ENLARGED** THE HOLE RATHER THAN SHRINKING IT; both are folded, and I re-verified both
at source myself. ***

FILE: "the spec" = `STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md` (1600 lines,
archive workspace). "the triage" = `STAGE8_CTP_PHYS_INPUT_PACKAGE_TRIAGE_AND_LOAD_BEARING_
FUNCTIONAL_SPEC_V001.md`.

---

## 0. LEAD

*** A WRONG OBJECT EXISTS THAT NO TEST THE CORPUS CAN RUN — AND NO TEST IN THE SEALED BATTERY EVEN
AFTER EVERY NAMED BLOCKER LANDS — CAN DISTINGUISH FROM `B0`. `B0` IS NOT MERELY UNBUILT. IT IS
**UNPINNED**. ***

**THE COUNTERMODEL — CM-3, THE OPAQUE-CARRIER CONSTANT-DESCENT ROOT.** Take an atom `o` with a
declared signature, and define its four descent maps to be **total constant maps** onto the
(eventually) independently derived `C0*`, `U1*`, `U2*`, `U3*`. Nothing is an input to `o`;
everything descends from `o`; every descent output equals its reference exactly. Walked against all
nine sealed failure-condition blocks, **not one fires** — and the reason compresses to a single
sentence:

> *** NO SEALED TEST REQUIRES THAT VARYING `Obj_B0` CHANGES A DESCENDANT. T6B CATCHES **EXTRA**
> DEPENDENCE AND IS BLIND TO **ZERO** DEPENDENCE. ***

I verified T6B's failure condition myself: *"an undeclared physical input changes a descendant"*
(spec `:1368-1369`). Under a constant descent map **no** input changes any descendant, so the
condition cannot be instantiated. The leg has no dual anywhere in the corpus.

**WHY THIS IS NOT A COMPLAINT BUT A TEST.** Five independent kill attempts were run against CM-3
and all five failed, each on the sealed wording: T1's "independent oracle" adjudicates
*conformance to the candidate's own signature*, not correctness (`:966-969`); T2's failure word is
*"undeclared"*, and the descent maps are given interface data, not external supplements
(`:1188-1190`, `:1202-1203`); T6B fails on the quantifier above; `DESCEND_B0`'s provenance language
is a **would-build, not a test** (`:1033-1036`, TYPE-U — *a defence that cannot run is not a
defence*); and the corpus's one genuinely adequate apparatus, `VALIDATED_COREFERENCE_INVARIANT_0`
(K0-K6), is `Obj_0`-quantified and **doubly unavailable** to `B0`.

**THE MISSING DISCRIMINATOR, NAMED:** a **descent non-degeneracy (faithfulness) requirement on
`DESCEND_B0`** — that distinct `B0` candidates induce distinct descendants. *** IT HAS NO SEALED
COUNTERPART ANYWHERE IN THE THREE ROOTS AND IT IS ON NO BLOCKER LIST. IT IS NEW. *** Until it
exists, **no `B0` construction should be accepted**, because passing the battery is compatible with
the candidate doing no work at all.

**AND THE HOLE IS LARGER THAN MY OWN ATTACK FIRST REPORTED.** A verifier corrected me: the sealed
kill I had credited against the containing-tuple countermodel is scoped to **one named object**
whose *quoted signature* is inspectable — `package_B0_is_FULL_SOURCE_RECORD_FIELD_CTP_EXTENSION =
false | TYPE-R` (spec `:661-663`), because *"its quoted signature already includes the quotient,
measure, state, effects, and response interface"* (triage `:362-365`). **A novel tuple candidate
has no quoted signature to inspect, so the row does not reach it** — and transporting the verdict
anyway would be H3, the very defect this program keeps hitting. I verified both lines myself. The
tuple countermodel survives too.

---

## 1. ANSWER 1 — THE FAILURE MODES, AND THE CLASSIFICATION THAT IS THE SPINE

**THE CORPUS'S OWN SEVEN GENERA, TRANSPOSED TO THE ROOT.** H1 a certificate accepted by type name;
H2 a vacuous pass on an empty class; H3 a PASS transported across an unproved identity; H4 a test
wired to the wrong verdict; H5 a check that cannot return evidence against itself; H6 a definite
description that fails to denote; H7 independent-origin reassembly.

*** FOUR OF THE SEVEN ARE GENUINELY DEFENDED, AND I COULD NOT BREAK THEM. REPORTED FIRST, BECAUSE
SYMMETRY IS BINDING AND A WORKING DEFENCE IS A REAL RESULT: ***

- **H4/H5 (cannot_fail) is defeated BY NAME and in advance** — the spec excludes a direct
  existence test as `EXCLUDED_AS_CANNOT_FAIL`, and every test carries one named property verdict
  with explicit non-target wiring (*"a U2 positivity failure or U3 quotient failure does not report
  into T1"*).
- **H3 is structurally impossible in Section 6's per-leg routing** — each leg's failure routes
  only to its own verdict.
- **H7 is defeated at the descent interface**, in exactly Q-99's demanded co-reference form.
- **H1's flag/object costume is refuted seven times**, by signature rather than by token — and
  B-P6 states it as a premise.

**THE CLASSIFICATION OF THE BATTERY — THE SPINE OF THE RELAY.** Of the nine separately routed
failure-condition blocks:

| kind | tests | can it discriminate today? |
|---|---|---|
| **SELF-REFERENTIAL** — checks the candidate against data the candidate itself declares | T1 (checks `Obj_B0` against *its own* `Sig_B0`) | no — passes by self-consistency |
| **BY-CONSTRUCTION** — an object built to satisfy it passes necessarily | T2, T6B | no |
| **DISCRIMINATING** — compares against something independent | T3 (4 legs), T4, T5A, T5B, T6A, T6C | **references absent → `NO_VERDICT`** |

*** EXACTLY ONE LEG (T6A) HAS AN INDEPENDENT REFERENCE THAT EXISTS TODAY, AND ITS OWN WIRING ROW
FORBIDS IT FROM ATTRIBUTING FAILURE TO `B0`. ZERO LEGS CAN TODAY RETURN TYPE-R AGAINST A `B0`
CANDIDATE. *** All nine executed-statuses are `false | TYPE-U`, and every one of the nine
`no_verdict_route` conditions is satisfied right now.

**THE POSITIONAL THEOREM BEHIND ALL OF IT.** B-P4 gives `B0` no inputs — so no test can compare it
against anything *upstream*, because there is nothing upstream. Every reference must be (i) `B0`'s
own descendants, (ii) the candidate's own declarations, or (iii) an externally sealed non-`B0`
object. Class (iii) is the only structurally clean one; it has exactly two members (T5B's
generator, T6A's item-1 signature); the generator is unbuilt and **T5B is expressly wired away from
`B0`** — *"That source does not identify the generator with B0"*. **The battery's only genuinely
physical failure test is the one that is not about `B0` — and that is correct wiring, not an
error.**

**THREE B0-POSITION-SPECIFIC MODES H1-H7 DO NOT COVER:** (1) the **unchecked provenance field** —
`Prov_B0` occurs *once* in 1600 lines (`:951`), in no applicability line, no procedure, no failure
condition, no wiring row, and `Sig_B0 = (Type, Arity, Dom, Cod, Params)` (`:958`) **excludes it**
while T1 checks only *"every field of `Sig_B0`"* (`:1154`); (2) **no inhabitation gate anywhere** —
word-boundaried search of the whole spec returns **zero** hits for nonempty / non-empty / inhabited
/ nonvacuous / vacuous / at-least-one; (3) **the Q-100 inversion arriving as a test reference** —
Q-100 bars descendants as *constructor* inputs, and **nothing bars them as the standard `B0` is
measured against.**

---

## 2. ANSWER 2 — THE COUNTERMODELS, EXECUTED AGAINST THE SEALED CONSTRAINT SET

Each was walked against B-P0…B-P6, the 6.1 wiring rule, and all nine failure-condition blocks.

**DEAD — CORPUS DEFENCES THAT WORK, REPORTED AS PLAINLY AS THE HOLES:**

- **CM-A the codomain-closure impostor (named form)** and **CM-E the descendant-relabelling root**
  — both die to **B-P2**'s *"target-independent construction rule"* and the witness would-build's
  bar on constructing `B0` *"without using C0/U1/U2/U3, item 1, a B0 discharge flag, or any
  downstream response output"*, with B-P1 freezing every datum *"before any descendant output is
  inspected"*. **B-P2 is the best-drafted defence in the packet.**
- **CM-C the name-labelled witness (named form)** — dies to the same clause.
- **CM-2′ the unnamed relabelled descendant** — dies to T3/T4 **the moment** independently derived
  `C0`/`U1`/`U2`/`U3` or `U_BR`/`rho_pre`/effects references exist. *** THAT IS A GENUINE FUTURE WIN
  AND I RECORD IT AS ONE: once the named blockers land, the battery buys real discrimination
  against relabelling. ***

**SURVIVING:**

- **CM-3 OPAQUE-CARRIER CONSTANT-DESCENT ROOT** — §0. Survives today **and after** every named
  blocker lands.
- **CM-4 DESCENT-EQUIVALENT TWIN** — two candidates whose four descent maps agree are separated by
  no leg; the battery's resolving power is exactly descent-equivalence, and no leg compares
  `Sig_B0` to an independent correctness standard.
- **CM-5 UNVALIDATED-PROVENANCE CANDIDATE** — H1 verbatim, at the field the whole of Q-109/Q-100
  makes `B0`'s *defining* property.
- **CM-1 CONTAINING-TUPLE** — survives after correction (§0); the sealed kill is scoped to a named
  object with an inspectable quoted signature.
- **CM-B EMPTY-DOMAIN CANDIDATE** — H2 transposed intact: every T1 failure leg is existentially
  quantified over admitted inputs, and there are none. **The corpus proves it knows this defence —
  the P0 ladder requires an independent nonempty fixture for every construction test — but that
  clause is P0-quantified and, since `P0 = B0` is *not* established, it does not govern `B0` and I
  do not transport it.**
- **CM-4′ VERIFIER-SIDE INVERSION** — a constructor satisfies T4's applicability by supplying the
  "independently specified" references **from its own candidate's descent**, then runs T4 against
  its own output. Every anti-inversion clause in the corpus is worded about *construction*.

**Q-92 SELF-AUDIT OF THIS SUITE.** (a) premises declared at the outset — yes, §1; (b)
failure-capable tests wired — each countermodel is wired to the specific sealed leg it defeats;
(c) **prerequisites EXIST** — the constraint set is sealed text, and every walk was executed
against it; (d) road step — STEP 1; (e) verdict owners named in §4; (f) **adversarial countermodel
attempted against my own suite and it FAILED** — five kill attempts, all recorded with their
failure reasons. *** THE SUITE IS THEREFORE CONSTRUCTED, NOT SPECIFIED, AND ITS TESTS WERE EXECUTED
AGAINST EXISTING SEALED TEXT. It constructs no `B0` and sets no `B0` flag. ***

---

## 3. ANSWER 3 — THE CODOMAIN HAZARD: **INSUFFICIENT. AN IMPOSTOR EXISTS.**

`codomain_constraint_route_determines_B0 = false | TYPE-R`, executed.

**THE ARGUMENT, FROM THE CORPUS'S OWN SENTENCE.** Section 3 states what sealed text gives `B0`:

> *"The primary source therefore supplies a single-origin role and mandatory outputs.
> **It does not supply an internal operator signature.**"* (spec `:795-796`)

```text
sealed_B0_semantic_role = COMPLETE_MICROSCOPIC_SOURCE_RECORD_FIELD_BOUNDARY_OPERATOR_OR_DYNAMICS
sealed_B0_mathematical_signature_status = NOT_FOUND_IN_BOUNDED_SCOPE [TYPE-S]
```

So the sealed characterization is **a role-phrase plus a set of mandatory outputs** — and the
outputs *are* the codomain. The role-phrase is real content to a human reader but **no sealed test
operationalizes it**: nothing defines what makes an operator "microscopic" or
"source-record-field" beyond the fact that these outputs descend from it. **I searched for a
surplus-content clause — some property `B0` must have that its consumers do not impose — and found
none.**

**THE LOGICAL FORM, CHECKED ON ITS OWN TERMS.** A *universal property* is positional and **does**
determine its object — but only via the universal quantifier plus **uniqueness of the mediating
morphism**. The sealed text gives the bare existential instead: *there exist* maps with
`C0 = d_C0(B0)`, `Ui = d_Ui(B0,C0)`. There is no initiality, terminality, freeness, minimality, or
generation clause anywhere in the spec. *** AND SINCE A CONSTANT MAP FROM ANY NONEMPTY OBJECT TO
ANY FIXED TARGET ALWAYS EXISTS, THE POSITIONAL CONDITION IS SATISFIED BY EVERY WELL-TYPED OBJECT.
THE CHARACTERIZATION IS NOT MERELY COARSE — ON THE DESCENT AXIS IT IS VACUOUS. ***

**THE WARNING, WRITTEN TO BE ACTIONABLE WITHOUT READING ANYONE'S ARTIFACT.** A codomain-route
construction must demonstrate, **over and above codomain satisfaction**: (1) **descent
non-degeneracy** — that varying the candidate varies a descendant; (2) **validated provenance** —
that `Prov_B0` is checked, not merely present; (3) **target-independence of the construction rule**
in a form some test can *fail*. Without all three, an object satisfying every codomain constraint
is indistinguishable from CM-3, which does no work at all.

---

## 4. ANSWER 4 — THE DISCRIMINATORS

| # | countermodel | discriminating test | verdict owner | inputs exist today? |
|---|---|---|---|---|
| MD-1 | CM-5, CM-4′ | construction-replay / dependency-orientation / target-independence, `B0`-quantified (shape sealed for P0) | `B0_CONSTRUCTION_PROVENANCE_VALIDITY` — **must be created** | no |
| MD-2 | CM-1, CM-5 | a `B0` common-origin certificate **with a validity test** — the K0-K6 shape, whose K2 (*"t_i is not a label, name, or self-assertion"*) and K6 (*"A certificate name or generator name is not itself a verdict"*) are exactly the missing clauses | `B0_COMMON_ORIGIN_CERTIFICATE_VALIDITY` — **must be created** | no |
| **MD-3** | **CM-3, CM-4** | **descent non-degeneracy / faithfulness on `DESCEND_B0`** | `B0_DESCENT_NON_DEGENERACY` — **must be created** | **no — and NO SEALED COUNTERPART EXISTS ANYWHERE** |
| MD-4 | CM-B | an inhabitation gate on the admitted-input class | `B0_ADMITTED_DOMAIN_INHABITED` — must be created | shape exists (P0 ladder), but is P0-quantified |

**THE WIRING-RULE CONSEQUENCE, STATED SYMMETRICALLY AND NOT OVERSTATED.** Section 6.1 says a
missing reference returns `NO_VERDICT` and *"it is not evidence against B0"*. *** THAT RULE IS
INDIVIDUALLY CORRECT AND I ENDORSE IT — converting an absent oracle into a negative would be the
fabricated-negative defect this program already guards against. *** The collective consequence is
nonetheless a fact about the present state: with all nine `no_verdict_route` conditions satisfied,
**the battery cannot today return anything but PASS or `NO_VERDICT`**. The legs that still *run*
(T1, and T2/T6B's supplement legs) are precisely the self-referential and by-construction ones.
**A wrong `B0` built today faces no test that can fail it.** That is a property of the current
state, not a defect in the rule.

**RANKING (the actionable output).** Buildable today: **none** — every discriminator needs either
a reference object or a new verdict owner. Buildable after one named object: MD-1 and MD-2 (both
need only wiring plus the witness). Blocked behind the whole foundation: MD-4. **And MD-3 is
blocked behind nothing — it is simply unwritten.** *** MD-3 IS THE CHEAPEST AND MOST URGENT ITEM
THIS RELAY PRODUCES: IT IS A WIRING OBLIGATION ON AN EXISTING NAMED INTERFACE, IT IS ON NO BLOCKER
LIST, AND WITHOUT IT THE ROOT OF THE ENTIRE DAG IS UNPINNED. ***

---

## 5. ANSWER 5 — THE ROUTE DISCRIMINATOR

*** THE ATTACK KILLS ONE ROUTE AND NOT THE OTHER — BUT ONE HALF IS WEAKER THAN IT FIRST APPEARED
AND I REPORT IT AT ITS TRUE STRENGTH. ***

**IT DOES NOT KILL `I_prim → B0`.** An object built from a frozen primitive inventory by a
target-independent rule carries provenance content — inventory, rule, replay record — **that is not
a function of the descendants**. *** THAT IS THE ONLY PLACE IN THE ENTIRE SEALED STRUCTURE WHERE
NON-POSITIONAL CONTENT CAN ENTER `B0`. *** My attack is about the poverty of the descent
characterization, and provenance is not part of it. **The `I_prim` route is the only route capable
in principle of producing a `B0` that a discriminator could confirm.**

**IT KILLS `codomain → B0` — UNDER ONE OF TWO READINGS THE CORPUS NEVER FIXES.** On reading (i),
taking the descendant **objects** as construction inputs, the route dies on **sealed premises**
(B-P1, B-P2, the witness would-build). On reading (ii), taking the descendants' **specifications**
as constraints on a search whose inputs are elsewhere, it dies only to my structural argument and
to **no sealed clause** — the sealed prohibitions name *objects* and *outputs*, never
*specifications*. *** THE UNFIXED READING IS ITSELF THE FINDING: IT IS THE EXACT SEAM THROUGH WHICH
A TARGET-DEPENDENT CONSTRUCTION CAN BE PRESENTED AS TARGET-INDEPENDENT, WITH NO TEST ABLE TO TELL.
*** Accordingly `codomain_route_killed_by_this_attack = NO_VERDICT` on reading (ii), and the
headline claim is not asserted as a verdict.

**THREE COUNTERWEIGHTS AGAINST MY OWN CONCLUSION.** (CW-1) The asymmetry is about **where a
discriminator could live**, not about one existing: today `Prov_B0` is validated by nothing, so an
`I_prim`-built object's provenance is as unchecked as a codomain-built one's. Surviving my attack
is **not** being verified. (CW-2) The `I_prim` route is separately blocked by other sealed text —
`B0_exact_named_construction_prerequisite_found = false | TYPE-S` (the corpus names no upstream
node into `B0` to build from). **"Not killed by my attack" is not "open."** (CW-3) the unfixed
reading above.

---

## 6. TYPED NEGATIVES (Q-54)

- **N1 [TYPE-R, executed]** `B0_verifiable_today = false`. Test: all nine `no_verdict_route`
  conditions satisfied at their cited lines; all nine executed-statuses `false | TYPE-U`; zero legs
  can return TYPE-R against a candidate.
- **N2 [TYPE-R, executed]** `sealed_battery_discriminates_CM3 = false`. Nine-leg walk; five
  independent kill attempts, all failed with reasons recorded (§0).
- **N3 [TYPE-R, executed]** `codomain_constraint_route_determines_B0 = false` (§3).
- **N4 [TYPE-R, executed]** `T6B_detects_zero_dependence = false`. Its failure condition is *"an
  undeclared physical input changes a descendant"* (`:1368-1369`) — verified by me at source.
- **N5 [TYPE-S]** `descent_non_degeneracy_requirement_found = false`. Roots: all three; exclusions:
  the two fenced route artifacts, `a32_holdout/custodian_private/` (never entered), `.proof_deps`;
  query: word-boundaried faithful, non-degenerate, degeneracy, injective, distinguish,
  discriminate, separate, reflects, unique/uniqueness over the spec and corpus-wide. Qualifying
  list: **EMPTY**. **On no blocker list — NEW.**
- **N6 [TYPE-S]** `inhabitation_gate_in_B0_battery_found = false`. Zero hits for nonempty /
  non-empty / inhabited / nonvacuous / vacuous / at-least-one in the whole 1600-line spec.
- **N7 [TYPE-S]** `Prov_B0_read_by_any_test = false` — one occurrence in 1600 lines (`:951`), in no
  applicability line, procedure, failure condition, or wiring row; excluded from `Sig_B0` (`:958`).
- **N8 [TYPE-R, corrected]** `containing_tuple_countermodel_excluded_by_sealed_text = false` for a
  **novel** candidate — the sealed row targets one *named* object with an inspectable quoted
  signature (§0). Retyped from my draft's EXECUTED-kill to a surviving countermodel.
- **N9 [TYPE-S, corrected]** `P0_identified_with_package_B0 = false | **TYPE-S**` — bounded
  absence, **not** TYPE-R refutation. A draft inflated the typing; the downstream conclusion (P0's
  tests cannot be borrowed for `B0`) survives, since absence of an identity certificate blocks
  transport just as well.
- **N10 [TYPE-C, carried]** `ValidatedFlatCoReferenceSchema_0_physical_derived = false | TYPE-C`
  and `flat_record_plus_validated_relational_invariant_common_origin = NO_VERDICT` — the K0-K6
  apparatus is the right shape and is itself unavailable, on top of being `Obj_0`-quantified.
- **N11 [NO_VERDICT]** `codomain_route_killed_by_this_attack` under reading (ii) — the corpus never
  fixes the inputs-versus-specifications reading (§5).
- **N12 [TYPE-R, executed]** `a_new_Q80_class_was_required = false`. Every finding routed into
  existing bins. **This lane has struck four manufactured classes; I add no fifth.**

---

## 7. CORRECTIONS FOLDED, UNDER THIS LANE'S NAME

1. **My attack under-reported its own hole, twice.** The containing-tuple countermodel is *not*
   dead (N8), and the descent characterization is not merely coarse but **vacuous on that axis**
   (§3). Both corrections came from verifiers and both **enlarge** the finding; I re-verified both
   at source before folding.
2. **A typing inflation corrected:** `P0 = B0` is TYPE-S, not TYPE-R (N9) — the exact Q-52/Q-54
   error this program polices, committed by my own attack.
3. **One overstated genus label:** "CM-5 survives; H1 verbatim" is prospective, not present —
   nothing currently *reads* `Prov_B0`, so it is H1-shaped rather than H1 occurring.
4. **A citation that does not denote** was struck (a claimed "F6"/Q-97 reference in a 373-line file
   containing none of those tokens); the surviving half of that counterweight stands on its own
   sealed flag.
5. **Process lapse recorded against this lane:** one agent asserted a self-reported fence exposure
   to a register summary *and did not file the disclosure*, and another referenced sections absent
   from its own return. The affected sub-claim (§5's codomain half) is downgraded to `NO_VERDICT`
   partly on that ground. **Neither lapse involved opening a fenced file**; independent audit of
   every citation found no content attributable to either route artifact.
6. **A second unfixed reading found and recorded:** T1's *"independent signature/domain/codomain
   oracle"* is nowhere defined extensionally — the same genus as CW-3. It does not rescue T1,
   because `:966-969` governs.

---

## 8. DISCIPLINE

- **Q-52:** nothing discharged; nothing declared derived; `B0` not constructed.
- **Q-54:** every negative typed with executed test, scope, or missing-information statement.
- **Q-69:** flags kept distinct from discharge objects throughout; the `Prov_B0` finding is
  precisely that a *field* is being trusted where a *validated object* is owed.
- **Q-80:** no new class constituted (N12); imports named — universal properties / mediating-morphism
  uniqueness, free and formal objects, constant maps, model-theoretic indiscernibility, and the
  relay's own H1-H7 genus vocabulary.
- **Q-83:** ADVANCES STEP 1 by supplying the Q-92(f) countermodel suite and naming MD-3.
- **Q-91:** no `git` of any kind; baseline untouched; `deploy_status.sh` not run; no publication
  authorization requested. Artifact and verified sidecar in the archive workspace; stop.
- **Q-92:** the suite is constructed with executed tests (§2); it constructs no `B0`.
- **SYMMETRIC FRAMING, AUDITED IN BOTH DIRECTIONS.** Four working corpus defences are reported
  first and in full (§1), and a genuine future win is recorded (§2). A verifier caught me applying a
  **lower** bar to the corpus-defends answer than to the corpus-holds answer — accepting two
  countermodel deaths on single clauses while walking survivors against all nine legs — and that
  asymmetry is corrected in N8. **The attack was not softened because constructions are running in
  parallel; and where my own argument was weaker than it looked, it is reported at its true
  strength (§5, N11).**

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
