## PASTE 539 — CODEX LANE 2 (HIGH EFFORT) — [TASK 5 / EQ6] STAGE 2 OPENS: THE PHYSICAL J2 EQUALITY

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-462.
- The target: the physical reader equality `reader = pi_Mx o Loc o Kernbar o Q` as a FINITE theorem on the actual surface — FC10's physical J2 face. Your stage-1 V002 (`e5381e66…`) supplies the shared tuple; J2 is independent of its three faces and must not presuppose their confirmation (the review runs in parallel).
- Authorities (hash-verify each): the p-verdict reduction record (p_loc[R_K] = chi_K + the local Maxwell symbol; the explicit reader family p_(chi,T)[H_x] = f(||x||^2) chi_K + 2 f_1(||x||^2) <x,Tx>), DoR-020-A1 (the U(1)-bundle field members and path/current correspondence), the F_PLDEC kill of record (a pi_Mx DEFINED FROM the desired reader is circular and dead — pi_Mx must be constructed from the Maxwell/Hodge side independently, then the equality PROVEN).
- Register-sweep first: grep the questions-settled register for prior reader/pi_Mx/Loc/Kernbar constructions — build nothing that exists; cite what does.
- Custody: builder builds; J2 returns to Lane 1. The circularity trap is the named killer here.

TASK:
Z1. **Construct each factor independently:** `Q` (the actual finite current/kernel map), `Kernbar` (its kernel closure on the finite scope), `Loc` (localization to the record surface), `pi_Mx` (the Maxwell projection from the LOCAL SYMBOL side — from the sealed Ward/contact split and Hodge–Maxwell finite data, with NO reference to the reader it must later match). Each factor premise-marked to its sealed licensor.
Z2. **Prove the equality** on the actual finite scope: `reader = pi_Mx o Loc o Kernbar o Q` — on the nose, per arrow, against the explicit reader family. If it holds only on a subscope, state the exact boundary and what obstructs beyond it.
Z3. **State J7 precisely:** the comparison face J2 induces on the shared tuple — its exact equation and what stage 2 must still verify (do not build J7 here; type it).
Z4. **Honesty clause:** if the equality FAILS on the actual stock — the reader is NOT the Maxwell composite — report the mismatch exactly with its witness. A genuine failure here is first-order physics (it would say the reader carries non-Maxwell content) and outranks a completed build.
Z5. **Battery:** the F_PLDEC circularity regression (show pi_Mx's construction nowhere references the reader); anti-tuning ledger; the nine geometric regressions where they apply.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_PHYSICAL_J2_LANE2_V001.md`
with final lines: `J2_EQUALITY = PROVEN / SUBSCOPE (+boundary) / FAILED (+witness)` and `PI_MX_INDEPENDENT = yes/no`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
