PASTE 368 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE RAW-G LIFT — SOURCE LAYER TO PHYSICAL FIELD LAYER, THE ONE CONSTRUCTION THEME LEFT

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-284)

```text
DOES THE OBJECT EXIST?   NO -- and it is now THE stop: Q-284's chain halts exactly at the
                         source-to-physical lift of raw G, and Q-281's background residual
                         is the SAME theme (STAT_BG_LIFT_FIBER: source-field intertwiners,
                         connected subtraction, stationary 2PI map). One lift, two
                         consumers. Build it as far as ratified material carries.
IS THE VERSION CURRENT?  YES through Q-284. Marks: TYPE-P | premises: DoR-008/009/013/014.
                         Rank + W-convention + family discipline bind. THE ACCOUNTING
                         DISCIPLINE BINDS: every lift operation gets its row (kernel /
                         image / sector transfers / restriction square / Tail_R action) --
                         extend Q-284's table, do not fork it.
ARE ITS INPUTS PRESENT?  THE LIFT'S RAW MATERIAL: the exact source derivative tensor and
                         connected shadow (Q-284), the seed (Q-281: D_J W(0), D_R W(0)
                         with p_A = r_ch/(r_0+r_ch)), the germ's source-field ports (J
                         couples to WHAT field object -- quote the sealed port text),
                         P2 V002, P4, the law-side subpackage, T_cyl (Q-258 -- the derived
                         kinematic continuum, so far UNUSED by the P5 chain: check whether
                         the lift's field side lives THERE).
```

## 1. THE TASK — BUILD THE LIFT'S EXECUTABLE PART, TYPE THE REST

```text
1  DEFINE the lift's contract precisely: raw G is a FIELD-side bilocal; the built tensor
   is SOURCE-side. The lift = the source-field intertwiner pair carrying source
   derivatives to field correlators (the LSZ-shaped step of this program). From the
   sealed port definitions: what field object does each source couple to, and is the
   coupling's intertwiner DETERMINED by ratified data (the germ's ports + the law's
   write structure) or does it carry freedom (type it)?
2  THE T_cyl CANDIDATE: the program owns ONE derived continuum object (T_cyl, Q-258,
   with retraction naturality and separation). TEST whether the field side of the lift
   can live on T_cyl: source content lifts along the colimit structure; the intertwiner
   becomes the universal map. If it fits, the lift is DERIVED on the kinematic
   continuum; if T_cyl is too small (which consumers need more?), say exactly what.
3  CONSTRUCT the executable part: the lifted G on whatever domain (1)-(2) support --
   bounded/cylinder level, with its sector structure and p-content per block (restriction
   to Q-279 verified). ACCOUNT every operation.
4  THE CONNECTED SUBTRACTION and (if reachable) the physical inverse on the lifted
   domain: construct or type. Then state what the BACKGROUND lift (Q-281's fiber) still
   needs beyond what this lift built -- the two share parts; name the shared and the
   distinct.
5  KILL-PASSES: no field structure imported from (M,g) or convention (the field side
   must come from ratified objects: T_cyl, the GNS spaces, the germ's ports); every
   limit names its topology; accounting complete; no rank values.
DELIVERABLE: the lift's executable part built + accounted, the intertwiner's
determined/free status, the T_cyl verdict, the residue list updated (what remains of
Q-284's six objects after this build). Typed throughout.
```

## 2. LEADS

```text
IF THE LIFT LIVES ON T_cyl -- *** LEAD WITH IT: the program's one derived continuum
object turns out to be the field layer the transport needed, and the lift is DERIVED,
not authored. State how far the chain then runs. ***
IF THE INTERTWINER CARRIES FREEDOM -- lead with it typed; that freedom goes to the
principal before any consumer depends on it.
IF THE FIELD SIDE NEEDS MORE THAN T_cyl -- lead with the exact deficit; that is the
geometric residue (O-D3 territory) arriving on the critical path, and the program must
know it moved.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
