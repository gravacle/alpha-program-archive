## PASTE 657 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / BUILD] BIND THE EVIDENCE ROOT; THE LEDGER STAYS SENTINEL

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. Builder A's CODE remains off limits; its sealed INPUTS are shared run inputs and lawful to read.)

CONTEXT (Q-597): run 006 reached `VERIFIER_INPUT_ROOT`. The parent expects a concrete `evidence_root_sha256` pre-launch — the machine's own adjudication of your §3.2 question: the evidence manifest is an INPUT, computable pre-run; only the LEDGER digest is a producer output. Your proposed pinned/run split corrects from 3/2 to 4/1.

TASK:
N1. **Bind `evidence_root_sha256` in your manifest instance by INDEPENDENT computation** from the sealed evidence manifest (`evaluator_build_A/inputs/structural_evidence_manifest.json` in the archive mirror — a shared sealed run input), using the spec's own content-root definition. Display your computation. You may NOT source the value from the parent's error output — if your independently computed value then matches the parent's expectation at the next run, that agreement is the point; if it differs, that disagreement is a finding.
N2. `ledger_sha256` stays the self-detecting sentinel with `require_roots_bound()` guarding it — confirm the guard's behavior is unchanged.
N3. Update the instance + sidecar (canonical, tight — the Q-594 canon); disclose the delta.
N4. **The addendum V002 proposal** (proposal only, one section in the artifact): §3.2 split as pinned_roots(4) = spec, snapshot, gate, evidence_root / run_roots(1) = ledger, with binding semantics for each class.

OUTPUT: updated instance + sidecar + one sealed artifact
`STAGE8_TASK6_EVIDENCE_ROOT_BINDING_DARIO_V001.md`
with final lines: `EVIDENCE_ROOT = independently computed (+value, +method)`, `LEDGER = sentinel + guard unchanged`, `INSTANCE = canonical, sidecar-pinned`, `ADDENDUM_V002 = proposed 4/1`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the artifact name exists, STOP. Seal, report hashes, STOP (the registrar mirrors and re-invokes). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
