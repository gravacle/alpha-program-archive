## PASTE 625 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 6 / BLOCKED-REPAIR] THE TWO FINITE-WORK CERTIFICATES: RA25-2 CENSUS + RA27-1 HODGE SQUARE

(Same Lane 3 session rules.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-560 (your scoping inventory, registered).
- THE TARGETS: the two items your inventory (`02bbe362…`) typed FINITE WORK — bounded exact derivations, no physical premise, no selector. Everything else in RA25/RA27 stays untouched.
- **LINEAGE DISCIPLINE:** these certificates are HELD OUT of the audit subject. Sealing them does NOT increment LINEAGE_LP and triggers no thirty-row rerun; they become lineage members only by a later explicit batched act. State this in the artifact.
- Custody: you derive; Dario reviews. Tag PROVABLE / PART-PROVABLE / YOURS.

TASK:
F1. **RA27-1 — the local Hodge certificate.** From the frozen data (`eta = diag(+1,-1,-1,-1)`, `epsilon_0123 = +1`, `(*F)_(mu nu) = (1/2) epsilon_(mu nu rho sigma) F^(rho sigma)`, ordered basis `(01,02,03,23,31,12)`): generate every entry of `J_star` in the frozen ordered basis (display the 6x6 matrix with index bookkeeping shown, signs derived not asserted), multiply it by itself exactly, and certify `J_star^2 = -I_6`. Scope: the local six-dimensional reconstructed tensor space ONLY — state explicitly that this does not extend to the full `F_phys` carrier and does not touch C27.3-C27.5.
F2. **RA25-2 — the invariant-competitor census.** On the frozen finite subject: derive and type the induced translation action `T_mu^(1)` on `C_1(K_L;L)` from the frozen vertex shifts and the odd periodic structure (display the derivation, including any sign/transport subtleties at the periodic seam for odd L); form `T_tilde_mu = T_mu^(0) (+) T_mu^(1)`; compute the joint fixed space `Fix_L` inside `H_BID(K_L)` exactly; projectivize the WHOLE fixed space including mixed vertex-edge rays; give a finite generating description uniform in all admitted odd L; classify every component by degree and root/address incidence; close with a completeness certificate (why no invariant ray is outside the display). **NO admissibility or exclusion claims** — which rays are lawful preparations depends on the missing C25.1 grammar and is out of scope; say so.
F3. **Consequence board:** exactly which repair-map fields these two certificates fill (RA27-1 closes C27.2; RA25-2 supplies the C25.2 census display), which A25/A27 conjuncts remain untouched, and the statement that neither row moves on the thirty-row board until a batched lineage increment and full rerun.
F4. **Battery:** F_PLDEC; anti-tuning (no ray favored, no coefficient consulted); surface anchor; M-2; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_RA25_2_RA27_1_FINITE_CERTS_LANE3_V001.md`
with final lines: `J_STAR_CERT = J_star^2 = -I_6 displayed / FAILED (+defect)`, `CENSUS = complete (+component count and classification) / INCOMPLETE (+missing)`, `ADMISSIBILITY_CLAIMS = none`, `LINEAGE_TOUCHED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity (the exact symbolic matrix/kernel algebra above is structural, not a physical-quantity evaluation); no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
