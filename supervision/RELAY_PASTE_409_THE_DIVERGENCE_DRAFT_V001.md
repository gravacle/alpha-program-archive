PASTE 409 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THE DIVERGENCE DRAFT — THE TRANSVERSE CYCLE ACTION DETERMINED BY THE ACCUMULATED RECORD-DEPTH DIVERGENCE STRUCTURE (THE PRINCIPAL'S THREAD; DoR-016 RESERVED)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-326 + C41)

```text
DOES THE OBJECT EXIST?   COMMISSIONED BY RULING (C41): the determining principle for the
                         transverse cycle action -- THE PRINCIPAL'S STANDING THREAD: the
                         log divergence IS the coupling; the accumulated record-depth
                         divergence structure DETERMINES the non-perturbative content.
                         The corpus's divergence/depth material has never been brought
                         to this question.
IS THE VERSION CURRENT?  YES through Q-326 + C41. THE TEST TARGETS ARE EXACT: Q-408's
                         dependence forms (Delta M = D_C D_K phi; delta G_K =
                         -H_CC^{-1} D_C(delta phi)) -- whatever the draft determines
                         must feed THESE, and its consequences are computable through
                         them. The kill lineage binds (countermodels, no target-tuning,
                         no selection, the door flags, C40's void clause).
ARE ITS INPUTS PRESENT?  THE DIVERGENCE MATERIAL, sealed across the corpus: the log
                         divergence results (the divergence-is-the-coupling lineage --
                         locate every sealed artifact), the record-depth/accumulation
                         structure (the sequential N-growth, T_cyl's colimit as depth's
                         completion), the dephasing accumulation (Q_N growth in the
                         sealed weights), the singlet result (gravity/gauge ratio
                         depth-DEPENDENT -- the old theorem that pointed here), the
                         flat-family characterization (what the determined action must
                         BE a member of, or provably replace).
```

## 1. THE DRAFT — THE PRINCIPLE MADE EXECUTABLE, THEN GATED

```text
1  STATE the principle precisely: the transverse action's content is the ACCUMULATED
   DIVERGENCE of record depth along cycle content -- construct the candidate:
   phi_div := the depth-accumulation functional the sealed divergence structure
   defines on the cycle carrier (extract the sealed log-divergence's exact form; its
   accumulation along the sequential tower; its T_cyl completion). The construction
   must CITE sealed divergence artifacts at every step -- the principle is the
   principal's, but the CONTENT must come from the corpus's own divergence data.
2  CHECK THE FORM: is phi_div (a) all-orders-flat on the active section (a member of
   the admitted family -- consistency); (b) nonzero off-section (the non-perturbative
   content real); (c) reality-covariant, quotient-compatible, rank-symbolic? If the
   sealed divergence structure forces a UNIQUE phi_div (up to the rank pair), the
   determining principle is a DERIVATION and the fifth object joins the law as found
   physics. If a family survives, its parameters named.
3  COMPUTE THE CONSEQUENCES through Q-408's exact forms: Delta M and delta G_K with
   phi = phi_div -- the stationary response package assembles with DETERMINED content;
   then Pi_R,ind; then TRACE p. *** THE VERDICT, if the chain closes: with the
   divergence-determined action, does p appear in the physical response? AND the
   second question the thread was always about: WHERE DOES THE COUPLING'S VALUE SIT
   in phi_div's structure -- symbolic only, no evaluation. ***
4  KILL-TESTS: the countermodel lineage against phi_div's construction (is the
   determination generative or a relabeled choice?); no target-tuning (phi_div's form
   from divergence data ONLY -- never from its p or alpha consequence); the Q-313/C40
   disciplines; restriction to Q-243/Q-279 EXACT.
5  DELIVERABLE: the principle stated, phi_div constructed with sealed citations, the
   uniqueness-or-family determination, the consequences through the exact forms, and
   -- if the chain closes -- THE VERDICT with premises. Nothing TYPE-P beyond premise-
   marked; DoR-016 reserved; cross-review follows.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
