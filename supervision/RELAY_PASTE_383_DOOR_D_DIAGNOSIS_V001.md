PASTE 383 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] DOOR-D DIAGNOSIS — DEFECTIVE ADOPTION OR MIS-SPECIFIED CHECK? (DoR-015 FROZEN; NO DOWNSTREAM USE)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-300)

```text
DOES THE OBJECT EXIST?   THE FIRED FALSIFIER does (Q-300, artifact bffd7ef2...): on c_sq =
                         (1,-1,1,-1), D_J A-bar = 0 while G_R != 0 -- the same-correlator
                         identity D_J A-bar = (i/hbar) G fails on the ratified carrier.
                         DoR-015-dependent work is FROZEN. THIS RELAY USES NOTHING
                         DOWNSTREAM OF DoR-015 -- it examines the failure itself.
IS THE VERSION CURRENT?  YES through Q-300.
ARE ITS INPUTS PRESENT?  The Door-D execution artifact, V005, the finite references
                         (Q-243/Q-279 -- still valid, exactly reproduced), the Keldysh
                         structure (Q-243), the cycle-current apparatus (Q-298/Q-299).
```

## 1. THE DIAGNOSIS — TWO HYPOTHESES, DECIDED AT THE FINITE LEVEL

```text
H-A (DEFECTIVE ADOPTION): the ratified field representation / bilocal class genuinely
    produce an inadmissible physical G -- the identity SHOULD hold on cycles and the
    adopted structures break it. TEST: state the identity's PROVENANCE -- which sealed
    text mandates D_J A-bar = (i/hbar) G, on WHAT carrier, for WHAT source class? Then
    check at the FINITE level (no DoR-015 content needed): does the sealed finite
    structure itself satisfy the identity ON CYCLES -- compute D_J and G on c_sq from
    Q-243/Q-279's exact objects directly. If the FINITE sealed objects also give
    D_J = 0, G != 0 on the cycle, the identity NEVER held on cycles and H-A is refuted.
H-B (MIS-SPECIFIED CHECK): the Keldysh typing says the one-point/coherent sector is
    DIFFERENCE-typed and cancels on closed cycles (a loop encloses no net endpoint
    charge -- the coherent p-term is endpoint-typed, Q-249's covector finding), while
    same-cell noise is diagonal-typed and survives. If so, D_J A-bar = (i/hbar) G is an
    OPEN-PATH/SOURCE-LEVEL identity, and demanding it of the cycle quotient was a
    category error in the CHECK -- the correct cycle-level admissibility identity is a
    DIFFERENT equation. TEST: derive from the sealed Keldysh structure what the
    identity's cycle-restriction actually IS (both sides restricted honestly), and
    verify the executed objects satisfy THAT.
2  THE VERDICT: H-A (the adoption is defective -- name the structure and the repair
   shape) / H-B (the check was mis-specified -- state the CORRECT cycle-level
   admissibility condition, verify the execution passes it, and state exactly what
   Door D should have demanded) / BOTH-PARTIAL (split it).
3  THE ENDPOINT-COVARIANT CROSS-CHECK: Q-299 established open-path content lives as
   endpoint-covariant transport. The identity involves D_J (source/open-path-typed).
   Determine whether the identity holds in the ENDPOINT-COVARIANT sector (where it
   plausibly belongs) on the ratified carrier -- if it holds there and fails only as a
   scalar-cycle demand, H-B is confirmed with the repair being Door D's restatement.
4  KILL-PASSES: no DoR-015-downstream construction; no repair of either the adoption or
   the check within this relay (diagnosis only -- disposition is the principal's); the
   finite computations from sealed objects only.
DELIVERABLE: the H-A/H-B verdict with proofs, the identity's correct carrier and
provenance, and the disposition options stated neutrally for the principal.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
