PASTE 357 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] BUILD P3 — COMPLETE THE U3 PACKAGE: MEASURE, CONTOUR, BOUNDARY, DOMAINS, ON THE RATIFIED GERM AND P2 (CONSTRUCTION)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-273)

```text
DOES THE OBJECT EXIST?   HALF: Q-239 built U3's exact finite skeleton (Q_N = U(1)^N quotient,
                         F_N/A_N descent, branch grammar, finite boundary/glue, bounded
                         domains) and left FIVE TYPE-U fields: (1) descended contour/
                         spacetime measure, (2) interacting contour/i-epsilon prescription,
                         (3) complete CTP boundary/edge data, (4) unbounded endpoint
                         domains, (5) common-origin provenance. FIELD (5) IS NOW DISCHARGED
                         BY DoR-013/014 -- say so with citation. Fields (1)-(4) are P3's
                         remaining build, NOW ON REAL FOOTING: the ratified germ supplies
                         the source structure, P2 the topology/calculus.
IS THE VERSION CURRENT?  YES through Q-273. Marks: TYPE-P | premises: DoR-008/009/013/014.
                         Rank discipline binds. THE OLD KILL BINDS TOO: Haar on Q_N is
                         REFUTED as the descended measure (Q-239) -- the measure must
                         DESCEND from the ratified germ's structure, not be imported.
ARE ITS INPUTS PRESENT?  YES: the finite skeleton (b0118d89...), the germ (d4cdbb66...),
                         P2 (1339e3ce...), the ratified contour declarations in the DoR-008
                         adoption texts.
```

## 1. THE TASK — THE FOUR REMAINING FIELDS, DESCENDED NOT IMPORTED

```text
1  FIELD (1), THE MEASURE: construct the descended measure on the physical source/history
   space FROM the germ's finite operational certificates and P2's trace-class structure
   (the natural candidate: the germ's own finite-stage weights, extended by the dense-core
   density -- P2's contractive restrictions give the projective system; prove consistency
   and uniqueness or type the freedom). The Haar kill stands: cite it, avoid it.
2  FIELD (2), THE CONTOUR: the interacting contour/i-epsilon prescription on the ratified
   CTP doubling -- constructed from the sealed contour declarations + P2's topology
   (in WHICH topology do the i-epsilon limits exist? Q-252's three-zeros discipline binds:
   difference sources, probe sources, connection history kept distinct).
3  FIELD (3), BOUNDARY/EDGE DATA: complete the finite boundary/glue package to the
   physical level (the sequential N<=M glue is exact -- what does its P2-limit carry?).
4  FIELD (4), DOMAINS: the unbounded endpoint operator domains, via P2's calculus
   (the finite bounded domains are certified; the unbounded closures in topology_src --
   prove essential self-adjointness where it holds, type where it fails).
5  FIELD (5): state DISCHARGED with the DoR-013/014 citation -- the common-origin
   provenance now EXISTS by ratification.
6  CERTIFY: every field against DoR-008 (finite restriction reproduction); U3_008_INSTANCE
   status restated -- does the INSTANCE now exist (the Q-239 flag flips) or what exactly
   remains; consumer-readiness for P5 (the correlator chain needs measure + contour +
   domains together).
7  KILL-PASSES: no import (measure, contour, or domain assumption from convention); every
   limit names its topology; no rank selection; the moving-tail check at each field.
DELIVERABLE: P3 as a certified construction -- the four fields with proofs, field (5)'s
discharge citation, the U3 instance verdict, consumer-readiness statement. TYPE-P
positives expected.
```

## 2. LEADS

```text
IF U3's INSTANCE COMPLETES -- *** LEAD WITH IT: the package whose absence defined the
program's continuum boundary since Q-239 would EXIST. State what P5 can consume the same
day. ***
IF A FIELD RESISTS -- lead with which and the exact missing datum; typed.
IF THE MEASURE IS NON-UNIQUE -- lead with the freedom typed and its downstream consequence
(what varies with it), BEFORE any consumer depends on it.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
