PASTE 370 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] ASSEMBLE THE CORRECTED ACCOUNTING PACKAGE — Q-282 TAXONOMY + THE SCHUR AND CLASS-FORMATION ACCOUNTS

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-286)

```text
DOES THE OBJECT EXIST?   HALF: you (Q-286) independently recomputed every entry of the P5
                         chain and found the ONLY defect to be the accounting TAXONOMY --
                         Q-284 accounted by operation; Q-282 requires accounts BY FIELD
                         (measure, contour, boundary/contact, domain closure), plus a
                         STATIONARY SCHUR account, plus (Q-285) the CLASS-FORMATION
                         image/topology account. You specified the required shape --
                         now BUILD it over the chain you verified.
IS THE VERSION CURRENT?  YES through Q-286.
ARE ITS INPUTS PRESENT?  YES: your verification (260fb9fc...), the chain (2fc227d0...),
                         Q-282's definition, Q-285's creator characterization.
```

## 1. THE TASK — THE LEDGER, ON THE RIGHT AXIS

```text
1  FOR EACH of the four FIELD accounts (measure, contour, boundary/contact, domain
   closure): record what the built chain DOES and DOES NOT yet do that touches the
   field's territory -- kernel, image, sector transfers, restriction square, Tail_R
   action -- and the interface premise the unbuilt part enters under. An account with
   "the built chain performs NO operation of this field's kind" is a complete and
   valuable row: it certifies where the doors are NOT yet open.
2  THE STATIONARY SCHUR ACCOUNT: the Schur/stationary reduction structure the response
   inversion will use -- what the built chain contains of it, what the lift must add,
   its p and Tail_R exposure.
3  THE CLASS-FORMATION ACCOUNT (Q-285's requirement, THE DOOR'S OWN LEDGER): for the
   class-formation steps the physical chain will need, the image/topology account --
   which topologies are licensed by built interfaces, where weak-* completion COULD be
   invoked, and the explicit flag structure a future lift artifact must fill so that
   invoking the creator is IMPOSSIBLE to do silently.
4  CROSS-INDEX: map Q-284's four operation-accounts INTO the field taxonomy (nothing
   verified is discarded; it is re-filed), and verify completeness against Q-282's
   definition clause by clause.
5  KILL-PASSES: no new mathematics (this is the LEDGER of verified content -- any new
   claim needs its own proof or gets typed); no silent narrowing of Q-282's definition.
DELIVERABLE: the corrected accounting package V002 -- six accounts (four fields + Schur +
class-formation), cross-indexed, with the door-flag structure for future lift artifacts.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
