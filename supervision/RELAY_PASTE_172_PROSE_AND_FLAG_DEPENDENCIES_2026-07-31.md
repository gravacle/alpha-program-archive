PASTE 172 — CODEX 1 — ITEM: EXTRACT THE DEPENDENCIES THE GRAPH CANNOT SEE

2026-07-31. Snapshot; later rulings override. **REGISTER HEAD AT ISSUE: Q-73.**

```text
CODEX 1 (you) = prose-stated and flag-stated dependencies      [relay 172]
CODEX 2       = specify B0, the package's load-bearing stop    [relay 171]
EINSTEIN      = axioms-or-debts boundary classification        [relay 169]
```

**OFF LIMITS TO YOU:** `CTP_PHYS_INPUT_PACKAGE` internals and B0 (Codex 2); the axiom/debt classification
of boundary leaves (Einstein). **You may record a dependency that touches those objects — you may not
classify or specify them.**

Fences: never touch `a32_holdout/custodian_private/`. **Do not compute** alpha, kappa_record,
kappa_Thomson, a coupling, a radius, a scale, a root, an eigenvalue, a beta function, `E_R`, `T_R`, `k_R`,
or any absolute interval. **No comparison to any measured constant.** **Q-54:** every negative typed;
**`NO_VERDICT` is legal.**

---

### WHY: THE GRAPH IS INCOMPLETE IN A WAY THAT ALREADY PRODUCED A WRONG SCHEDULING CLAIM

Q-64's census built the dependency graph from **`would-build` fields only**. Einstein typed the result
precisely and warned that zero shared nodes proved **nobody had STATED a shared prerequisite, not that the
physics was independent.** *** THE WARNING WAS CORRECT AND THE REVIEWER IGNORED IT. ***

**Q-73 found the counterexample.** `STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md:64-67` says the closure
residual carries the `A_4` logarithm, **"so `k_R` — the floor — appears in the equation that fixes
`K_*`"** — and a status flag names the missing connector: **`T_R_and_k_R_have_derived_bridge = false`.**

*** SO A REAL DEPENDENCY LINKS THE TWO "DISJOINT" CRITICAL PATHS, IS STATED TWICE IN SEALED TEXT, AND THE
CENSUS COULD SEE NEITHER STATEMENT. On that basis the reviewer told the principal the two paths could be
worked in parallel. That was wrong. ***

### TWO LANES ARE WRITING WHILE YOU RUN — SWEEP THEIR OUTPUT TOO

*** DO NOT REPORT STALENESS AND STOP. *** Einstein (relay 169) and Codex 2 (relay 171) will land artifacts
during your run. **Extract flag- and prose-stated edges from them as well**, and say which artifacts you
covered. **You may read their outputs for EDGES ONLY — you may not classify boundary leaves (Einstein's
item) or specify B0 (Codex 2's).**

**If a ruling lands that changes the extraction RULES rather than the corpus, report it and continue.**

### THE TASK

1. **EXTRACT FLAG-STATED DEPENDENCIES.** Sweep for status flags that assert a missing or existing relation
   between two named objects — the `T_R_and_k_R_have_derived_bridge` shape. Patterns to cover, **and add
   any you find**: `*_have_derived_bridge`, `*_bridge_derived`, `*_relation_derived`, `*_map_derived`,
   `*_identified`, `*_equals_*_derived`, `*_consistent_with_*`. **Each names an edge — present if `true`,
   MISSING-AND-REQUIRED if `false`.**

2. **EXTRACT PROSE-STATED DEPENDENCIES.** Sentences asserting that one object enters, fixes, appears in,
   or is required by another. **"appears in the equation that fixes"** is the exemplar. Others to cover:
   "enters the", "is required by", "cannot be written without", "is built from", "carries the", "feeds
   into", "is an input to", "is fixed by". *** REPORT THE SENTENCE VERBATIM WITH file:line. A paraphrase
   is not evidence. ***

3. *** DO ANY NEW EDGES CONNECT THE THREE CHAINS? *** The **Hamilton-Jacobi scale bridge** (six Q-59
   objects), the **response-extraction layer** (package → map → `Gamma_K`), and the **`T_R`→`k_R`
   bridge**. **Q-64 reported the first two as sharing zero nodes. With prose and flag edges included, is
   that still true?** **Answer from the extracted edges, not from Q-73's single example.**

4. *** RE-RUN CYCLE DETECTION ON THE ENLARGED GRAPH. *** Test edges held out **by kind** — a test gated on
   an unbuilt object is the Q-52 problem, not a construction cycle, and conflating them would be a false
   alarm on the most consequential question in the census. **IF A CYCLE APPEARS, STOP AND LEAD WITH IT.**

5. **HOW MANY EDGES WAS THE GRAPH MISSING, AND WHAT FRACTION?** Q-64 recorded 122 stated edges over 172
   nodes. **Give the new count and say plainly whether 122 was a serious undercount.** **If it was, every
   parallelism claim in the register is suspect and should be listed.**

### CONSTRAINTS

- **Do not infer an edge.** *** ONLY EXTRACT WHAT SEALED TEXT LITERALLY STATES. *** Q-64's discipline —
  exact identifier, verifier-noted correspondence, or nothing — holds here. **A guessed edge is worse than
  a missing one: it can manufacture a cycle.**
- **Q-69 standing rule: NEVER IDENTIFY A FLAG WITH THE OBJECT THAT DISCHARGES IT.** Two conditional cycles
  found overnight both fire on exactly that operation, and this task is full of flags naming objects.
- **Word-boundaried matching only.** The substring failure mode has produced false positives in the
  corpus, in an audit script, in the audit of the audit, **and in the register itself** — the reviewer
  matched `constant_root_uniqueness` to the threshold root yesterday and was wrong.
- **State your scope**: roots, exclusions, queries. **An extraction with unstated scope is a void TYPE-S.**

---

REPORT BACK, with **CODEX 1** on your first line: hashes, exact committed paths, gate verdict, the five
answers, **every negative typed.** *** IF THE ENLARGED GRAPH HAS A CYCLE, LEAD WITH IT. OTHERWISE LEAD WITH
THE EDGE COUNT AND WHETHER THE THREE CHAINS STILL SEPARATE. *** The program has been scheduling work on the
assumption that they do.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
