PASTE 397 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE TRANSPORT INFRASTRUCTURE — THE COMMON COMPLETED DOMAIN AND THE PHYSICAL SQUARES (BUILD i OF THE FINAL FOUR)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-314)

```text
DOES THE OBJECT EXIST?   NO (synonym count zero -- genuinely unbuilt): the COMMON COMPLETED
                         DOMAIN and the PHYSICAL RESTRICTION/TAIL SQUARES -- the transport-
                         infrastructure pair, SEPARABLE from the stationary pair (Q-314's
                         decomposition) and buildable now.
IS THE VERSION CURRENT?  YES through Q-314. P7's proved certificate (Q-257: 6/6 + the
                         class-transport lemma) is NOT these squares -- but it is the
                         nearest proved machinery: consume its retractions/naturality
                         where they apply, cite the distinction where they do not.
ARE ITS INPUTS PRESENT?  YES: the ratified domains (Q-278 bounded + P2 closures + the
                         signature's structures), the completed kernel (Q-309), the
                         two-sector layout, Map 1 (Q-313), the finite references, the
                         Q-288 ledger's domain-closure and restriction-square accounts.
```

## 1. THE TASK

```text
1  THE COMMON COMPLETED DOMAIN: the invariant domain on which the full physical structure
   (both sectors, Map 1's image, the response operators) acts -- from the ratified
   closures; essential self-adjointness where the structure provides it, typed where
   not; invariance under the quotient, restriction, and reality (as covariance) PROVEN.
2  THE PHYSICAL SQUARES: the commuting squares tying the physical structure to the
   finite restrictions (Q-243/Q-279 as the shadows) and the Tail_R accounting through
   physical operations -- constructed on the domain from (1); P7's proofs consumed
   where they genuinely apply (cite), the physically-new parts built and certified.
3  CONSUMER STATEMENT: what the background realization (build ii, other lane) and the
   stationary blocks (build iii) will consume from this pair -- the interface, stated.
4  KILL-PASSES: six-account rows; door flags; every limit names its topology; no
   selection; no evaluation; restriction executed.
DELIVERABLE: the domain + squares with certificates, and the build-ii/iii interface.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
