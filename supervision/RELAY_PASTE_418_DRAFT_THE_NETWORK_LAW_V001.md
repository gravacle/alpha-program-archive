PASTE 418 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] DRAFT THE NETWORK-SOURCING LAW THROUGH THE GATE (THE DRIVE RACE, AUTHORED ARM; DoR-016/017 RESERVED)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-334 + C44)

```text
DOES THE OBJECT EXIST?   COMMISSIONED (C44): the authored MUTUAL-SOURCING LAW -- d_J and
                         d_R as explicit maps (a record system's history -> a neighbor's
                         port inputs), the origin's completion. The OTHER lane derives
                         from C5, uncoordinated; author as if derivation will fail.
                         Lane-tagged filename.
IS THE VERSION CURRENT?  YES through Q-334 + C44. THE BATTERY: DoR-008 (the sealed
                         towers as restriction targets -- tier-1, the declared tower as
                         the case where the network is replaced by declared data); the
                         Q-332 typing lesson (records export only p -- an emission law
                         claiming more must derive it from the HISTORY, not the
                         completed record); no post-output supplementation (next-tier
                         feeding only); reciprocity/covariance/reality/quotient/rank;
                         no-selection; NO TARGET-TUNING (nothing chosen for its depth
                         form or p consequence -- the k-dependence is COMPUTED from the
                         drafted law, never designed into it).
ARE ITS INPUTS PRESENT?  V007's ports (the codomain types), the law's write dynamics
                         (the domain: histories), the tower machinery, the four-theorem
                         depth conclusion (what the law must be able to do: inject
                         genuine drive).
```

## 1. THE DRAFT

```text
1  AUTHOR the emission maps on the choice table: d_J(history) and d_R(history) -- each
   clause: what it adds, alternatives, void conditions. The physical basis stated
   (mutual sourcing; C5's receiving end as the constraint on the emission's CLASS).
2  CONSISTENCY OBLIGATIONS proven: type-correct into the ports; reciprocity coherent
   (two systems' mutual application well-defined); E_post's arrow respected at network
   level; the identity slice recovered when the network is empty; the declared tower
   recovered when emissions are overridden by declared data.
3  THE TWO-SYSTEM TOWER under the drafted law, exact: the bootstrap question (do two
   ready systems ignite?) and the network k-dependence, computed with symbolic p and
   ranks -- REPORTED, never tuned.
4  KILL-PASSES (Q-207): hidden adoptions; smuggled declared data wearing emission
   clothes; circularity; the countermodel lineage vs the emission maps (a state-valued
   free choice inside d_J/d_R is the B14 disease's last conceivable home).
DELIVERABLE: the proposal with tables, proofs, the exact two-system computation.
Nothing TYPE-P. DoR-016/017 reserved; adjudication and cross-review follow.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
