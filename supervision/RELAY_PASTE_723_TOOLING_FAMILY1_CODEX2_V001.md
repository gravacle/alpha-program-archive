## PASTE 723 — CODEX 2 LANE (5.6 SOL EXTRA HIGH) — [7A / STEP 11] TOOLING FAMILY 1: THE CARRIER COMPILER FOR THE CONTRACT-READY SUBSET

PICKUP-ACK + LANE GUARD in force (CODEX 2).
(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. ALL standing laws apply. FIRST-TIME-RIGHT rules 1 and 3 govern this build especially: pins generated never transcribed; no unexecuted path ships.)

CONTEXT (Q-628): four boxes are sealed and contract-ready (BX01, BX03, BX09, BX13 in delta b52e66b7…), directly unblocking 19 C elements. The thirteen corpus-instance boxes await the principal's selection-authority ruling — OUT OF SCOPE here.
TASK:
HH1. BUILD generator family 1 — the carrier compiler — scoped to exactly the C elements the four sealed boxes unblock: deterministic compilation from sealed source bytes (content-addressed inputs only) into schema-conforming carrier components; fail-closed on any input whose digest, span, or schema conformance fails; nothing hand-written that the tool can generate.
HH2. RUN IT on the 19 directly unblocked elements. Each produced component content-addressed, schema-validated against its sealed box, and bound into its row's carrier/envelope with the honest partial/stateable status recomputed. Admission stays barred (Step-11 subgate); state it per output.
HH3. Self-check the TOOL (negative controls: a perturbed input digest, a nonconforming instance, a truncated span — all must refuse) and the OUTPUTS (displayed census: N produced / M still missing per row with owners).
OUTPUT: the tool + produced components + one sealed report `STAGE8_7A_TOOLING_FAMILY1_CODEX2_V001.md`
Final lines: `TOOL = built, negative controls displayed`, `PRODUCED = N components / 19 targets (shortfalls named)`, `ROWS_ADVANCED = list (new partial/stateable statuses)`, `ADMISSION = barred, stated`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, write 723_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
