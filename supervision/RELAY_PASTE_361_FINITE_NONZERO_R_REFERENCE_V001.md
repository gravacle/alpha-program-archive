PASTE 361 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] BUILD THE FINITE NONZERO-R REFERENCE — THE EXACT PROBE-SOURCE COMPUTATION P5 WILL BE CHECKED AGAINST

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-277)

```text
DOES THE OBJECT EXIST?   NO -- flagged TYPE-U at Q-274 ("the nonzero-R independent finite
                         reference"). Every exact finite computation so far (Q-243's chain,
                         P4's log germ) runs at R = 0 or in the A-expansion around zero
                         history. THE TRANSPORT NEEDS A NONZERO-PROBE REFERENCE: when P5
                         builds the physical response, the DoR-008 discipline demands a
                         sealed finite result to check its restriction against -- WITH THE
                         PROBE ON. This relay builds that reference, exact at finite N.
IS THE VERSION CURRENT?  YES through Q-277 (P2 stands as V002, 40b2af34...). Marks:
                         TYPE-P | premises: DoR-008/009/013/014. Rank discipline binds
                         (symbolic p_A; no rank values). W-CONVENTION BINDS (Q-274/Q-276):
                         Gamma_N = -Log Z_N; the response-facing object is W_N = -i hbar
                         Log Z_N; D^2 Log Z|_0 = -p(1-p) ell_delta (x) ell_delta.
ARE ITS INPUTS PRESENT?  YES, all sealed/ratified: F_N = P_0 + Z_N P_ch, A_N^(p), the
                         ratified law's certificates (C1: A = 0 exact reduction -- which
                         this relay finally probes AWAY from), the germ's source structure
                         (J and R ports), the finite Keldysh rotation (Q-243).
```

## 1. THE TASK — EXACT FINITE COMPUTATION, PROBES ON

```text
1  DEFINE the finite generating object with BOTH source families switched on: Z_N[J, R]
   per the germ's declared J/R ports on the sequential finite stages (quote the port
   definitions). State exactly which objects J and R couple to in the ratified finite
   dynamics.
2  COMPUTE, exact at finite N, symbolic p: (a) Z_N[J,R] in closed form where the pure-
   phase structure permits; (b) the first derivatives in J and in R at the reference
   point; (c) the MIXED second derivatives -- in particular the retarded-candidate mixed
   block (delta-source against c-source WITH R nonzero), via the Q-243 Keldysh rotation;
   (d) the R-dependence of the previously-zero mixed retarded block: DOES IT REMAIN ZERO
   AT NONZERO R, or does a nonzero retarded response appear at finite N with the probe
   on? EITHER ANSWER IS THE REFERENCE -- this is the computation the entire transport
   exists to extend, done exactly where it CAN be done exactly.
3  TRACE p symbolically through every result (the p_ch/p_A thread): state which blocks
   carry p, p(1-p), or are p-free WITH THE PROBE ON. The deciding question's finite
   shadow: if the nonzero-R retarded block is p-free at every finite N, cancellation
   gains its strongest finite evidence; if p enters with R, THE FORM is the finding.
4  CERTIFY: equal-history collapse, zero-extension (N <= M) with probes on, gauge
   covariance on the Q-239 quotient, restriction to the R = 0 results (must reproduce
   Q-243 exactly -- the internal falsifier check).
5  KILL-PASSES: no continuum objects (this is FINITE work; P3/P5 are elsewhere); the
   W-convention carried exactly; no rank values; no evaluation of any coupling.
DELIVERABLE: the sealed finite nonzero-R reference -- closed forms, the derivative table
with p-dependence per block, certificates. This artifact becomes the restriction target
P5's response chain must reproduce; write it to be consumed as such.
```

## 2. LEADS

```text
IF THE MIXED RETARDED BLOCK STAYS ZERO AND p-FREE WITH PROBES ON -- *** LEAD WITH IT:
the finite theory would be saying the retarded response is a PURE CONTINUUM EFFECT and
p-free at every finite stage -- the sharpest finite evidence for cancellation available. ***
IF A NONZERO FINITE RETARDED RESPONSE APPEARS -- lead with its exact form and p-content;
that is the object the transport carries to the continuum.
IF Z_N[J,R] HAS NO CLOSED FORM -- the derivative table at the reference point is still
the deliverable; say what resists closure.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
