PASTE 134

Written 2026-07-30, for CODEX LANE 2. **A PASTE IS A SNAPSHOT.** Any ruling later than this overrides it
without notice. Lane 1 is authoring the `Gamma_K` construction spec under paste 133 — do not touch
`Gamma_K`, joint-selector, O-SC1, measure or namespace artifacts.

Your paste-132 return is verified: both hashes match (`3a1a9d98…`, `880575cb…`), you committed exactly
four paths and nothing of another lane's, and `custodian_private/` appears in zero tracked paths.
**Three things you did are worth naming.** You led with the controlling defect rather than the inventory.
You stopped on the numeric-looking suffix, reproduced nothing, and recorded it as a masking-assurance
warning — that is the correct response and it is the second independent reason masking got ruled on today.
And your careful phrasing on custodian independence — that the evidence does not establish it rather than
that it was violated — is exactly the right register for that finding.

Three items. Item 1 registers four rulings and unblocks the deploy gate, which is RED on your account
only in the sense that a principal ruling is uncited. **Items 2 and 3 are the slot-18 work that is NOT
blocked on the principal.**

Fences unchanged: read-only outside your own writes; compute no alpha, kappa, coupling, radius, scale,
root or eigenvalue; **no comparison to any measured constant**; file:line + verbatim quote for every claim;
bounded negatives state roots and exclusions and report FILE LISTS; report refutations, never repair them.
`alpha_computed = false; proof_authorized = false` on everything you write. **NEVER touch
`a32_holdout/custodian_private/`.**

---

### ITEM 1 — REGISTER Q-23, Q-24, Q-25. THE DEPLOY GATE IS RED UNTIL THE PRINCIPAL RULING IS CITED.

**Q-23 — "unused" has no sealed definition.** Your finding, verified. Register that
`STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md:119-139` requires "one **unused** structure-sensitive
prediction"; that `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:2080-2089` defines structure-sensitive
operationally but not "unused"; and the consequence that matters beyond slot 18 — **slot 18 is the
mechanism that would EARN the induced-only axiom whose status alpha's conditionality equals (Q-18/Q-22),
so an earning mechanism with an undefined admissibility condition cannot be relied on to earn anything.**
Defining it is a PRINCIPAL ACT, queued, and a lane may not define its own admissibility predicate.

**Q-24 — the masking findings.** Both deviations from rule 9 (`V011:2008-2011`), and the fence-relevant
clarification that `D` compares theory predictions, with measurement entering only through
uncertainty/covariance — so computing `D` does not breach the no-comparison fences.

**Q-25 — THE DISPOSITION, PRINCIPAL ACT.** Authority:
`MB Work/alpha_supervision/A32_MASKING_DISPOSITION_PRINCIPAL_DECISION_2026-07-30.md`.

- **CUSTODIAN: the arrangement stands, limitation recorded PERMANENTLY.** Commitments are not withdrawn;
  the holdout is not re-collected. The concealment is cryptographically sound — `SHA256(salt || payload)`,
  256-bit salts, fail-closed on short entropy reads — but **process independence was never established and
  no independent attestation exists.** *** That discount must travel with every A32 headline and is
  inherited by FINAL-CLAIM. Register it as a standing qualification, not a footnote. ***
- **COVARIANCE: CLOSED BY EVIDENCE, VACUOUS BY CONSTRUCTION.** Reviewer check on the collector source
  only, no candidate content read: `covar|corr_matrix|correlation` returns **ZERO occurrences** in
  `collector_v001.py`, and the parser's fields at `:231-258` are **name | value | uncertainty | units**.
  The source is a fixed-column table with no covariance to commit. The payload omits covariance because
  the universe has none — the commitment binds everything the source supplies. **No re-commitment.**
- **FORWARD CONDITION, BINDING:** this collector **cannot commit a multi-component observable** — no
  covariance field, no code path for one. If the eligible universe is ever extended to vector or
  multi-component candidates, the collector must be revised and those candidates re-committed **before any
  prediction exists.** Live rather than hypothetical, because the universe is not sealed
  (`provenance/bid_stage_subjects_v011.json:22-24`).

### ITEM 2 — PREREGISTER THE COMPARATOR. THIS IS THE PRIORITY AND THE REASON IS TIMING.

You found that "same-alpha comparator" names a CLASS — "published QED/SM expression, identical alpha
input, frozen formula-specific order, and no candidate-specific fitting" — with **no concrete publication,
expression, order, or comparator payload anywhere in the bounded governing-source inspection.**

*** THE WORD IN THE SEALED CONDITION IS "PREREGISTERED": "BID and at least one PREREGISTERED comparator
calibrated to the same alpha must make distinct predictions" (`V011:2088-2089`). A comparator chosen after
predictions exist is not preregistered, and `D` consumes its mean. So the only honest time to fix the
comparator is BEFORE any prediction exists — which is now, and will not be true later. ***

DELIVERABLE: a preregistration spec for the comparator, not a computed comparison. For each candidate
observable family you can reach:
- the specific published QED/SM expression, cited to publication and equation;
- **the frozen formula-specific order** — stated explicitly, because "no candidate-specific fitting"
  means the order cannot be chosen per candidate later;
- the alpha input convention, so "identical alpha input" is checkable;
- what `sigma_comp` is and where it comes from;
- unit conventions, so `D`'s "after unit conversion" step is defined in advance.

**DO NOT** evaluate any comparator expression, compute any `mu_comp`, `sigma`, or `D`, or compare anything
to a measured value. This is a spec that freezes choices; it computes nothing.

**IF A FAMILY HAS NO PUBLISHED SAME-ALPHA COMPARATOR, SAY SO PER FAMILY.** A family with no comparator
cannot yield a `D` and is therefore ineligible regardless of how well the theory predicts it. That is a
finding, and it may shrink the eligible set materially — which the program needs to know now, since "an
empty eligible set fails closed."

### ITEM 3 — DERIVATION REQUIREMENTS PER FAMILY, FROM YOUR OWN LIST

You identified six typed non-alpha output families at
`STAGE8_SLOT18_BOUNDED_SCOPE_INVENTORY_V001.md:623` — finite holonomy response,
temporal-plaquette/connected-kernel susceptibility, flux/retarded response, causal-record durability,
charged spectrum/threshold response, magnetic form-factor channels.

For each, state precisely what would have to be DERIVED for the theory to make a structure-sensitive
statement, and **whether that derivation is blocked behind `Gamma_K`** — which is now the sole
construction target (Q-21) and the root of the dependency graph. **A family reachable WITHOUT `Gamma_K`
is worth more than one behind it**, because it could produce a prediction while lane 1 is still building.
Rank them on that basis and say which, if any, is reachable now.

**DO NOT** derive anything, and do not assert admissibility — "unused" is undefined until the principal
rules, so nothing can be declared eligible yet.

---

REPORT BACK: hashes, exact committed paths, `corpus_check` gate verdict, deploy state, and for item 2 the
per-family comparator availability. **If item 2 finds that most families have no published same-alpha
comparator, lead with that** — it would mean the eligible set is small or empty for reasons independent of
the theory, and "an empty eligible set fails closed."

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
