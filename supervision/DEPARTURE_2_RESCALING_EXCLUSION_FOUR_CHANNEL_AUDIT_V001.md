# DEPARTURE 2 — THE RESCALING EXCLUSION: FOUR-CHANNEL AUDIT, V001

LIVE WORKING DOCUMENT. Opened 2026-07-30 on the principal's instruction to pursue this. It is a
TRACKER, not a plan: each channel below gets updated in place as evidence arrives, and the version
number increments. Do not write a second plan document — update this one.

WHY THIS IS THE LEVER. Of the four departures this program made from standard field theory, three
(`K_bare = 0`; the record surface prior to the connection; records as fundamental boundary degrees)
are SCALE-COVARIANT and each carries an independent "does not select" ruling in its own lineage.
**Departure 2 is different in kind. It is not a statement about structure — it is a DEMAND THAT THE
SCALING FAMILY BE DEFEATED.** It is the only thing this program ever wrote that aims at the
obstruction that accounts for the largest single class of its blocked routes.

## 1. THE TEXT, VERBATIM, VERIFIED AT SOURCE 2026-07-30

`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_record_cell_selection_principle_v001.md:95-109`

```
## Rescaling exclusion

The selector closes the old normalization gap only if no target-independent
one-parameter family

    A_* -> lambda A_*,
    K_R -> K_R / lambda^2

or corresponding clock, source, measure, or cell deformation preserves every
upstream record principle while changing `K_R,*`. Compact unit winding blocks
a bare rescaling of `A_*`, but it does not by itself fix the cell, duration,
matter action, or field-energy integral. Those must be fixed by the complete
selector.
```

READ IT AS WHAT IT IS: a NECESSARY CONDITION on any selector, stated as a demand for
overdetermination against a one-parameter group. That is exactly the shape the corpus now requires
of a cross-sector candidate (`STAGE8_CROSS_SECTOR_METRIC_RULE_ADMISSIBILITY_SPEC_V001.md` §3, and
its falsifier F1 `beta_family_survives`). **The requirement was written ten days before the spec
that re-derives it.**

## 2. THE TIMELINE, AND THE CORRECTION IT FORCES TO OUR OWN ERRATUM

Measured by mtime 2026-07-30, `primitive_record_cell_selection_principle_v00*.md`, with a count of
the `^## Rescaling exclusion` heading in each:

```
2026-07-20 15:10   v001   heading present   <- written
2026-07-20 15:13   v002   heading ABSENT    <- deleted THREE MINUTES LATER
2026-07-22 22:13   v003   heading absent
2026-07-22 22:58   v004   heading absent
```

**CORRECTION OF RECORD, TWO PARTS.** `ERRATUM` / `RECOVERY_STIFFNESS_AND_ALLOW_REQUIRE_2026-07-29.md`
and `STAGE8_RESCALING_EXCLUSION_RECOVERY_ERRATUM_V001.md` attribute the deletion to "the v003 edit."
**THAT IS WRONG. The heading's last appearance is v001; it is already absent at v002, three minutes
later.** And the recovery erratum checked v001 and v003 only — **v004 WAS NEVER CHECKED** and is also
missing it. Both corrections are owed in the governing chain.

**AND THE FACT THAT MATTERS MOST.** `BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md` is dated
**2026-07-24 06:29 — four days AFTER the exclusion was written and deleted.** So the work that
plausibly closes two of the four channels below **could not have known the requirement existed.**
That is not a coincidence to note; it is the reason this audit is worth running at all.

No git history exists to recover from: the parent tree is on branch `main` with **zero commits**.
The v001 file on disk is the only carrier. An unversioned tree is how a necessary condition
disappears in three minutes with no supersession note.

## 3. WHAT IS ALREADY CLOSED

**COMPACT UNIT WINDING BLOCKS THE BARE RESCALING `A_* -> lambda A_*`.** The exclusion says so on its
face, and the integer character lattice is stated across the corpus
(`FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:40-41`,
`PRIMITIVE_RELATIVE_PHASE_CONNECTION_V001.md:113`,
`ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V001.md:32`,
`COUPLED_GRAVITY_CHARGED_FIXED_POINT_SELECTOR_GATE_V001.md:70`).

So the residual freedom is NOT the naive field rescaling. It lives in AT MOST FOUR NAMED CHANNELS,
and the exclusion enumerates them. **That is the whole value of the recovered section: it converts
"a scaling symmetry nobody can break" into a finite, named checklist.**

## 4. THE FOUR CHANNELS

For each: does a target-independent one-parameter deformation of THIS object preserve every upstream
record principle while changing the stiffness? If yes, the channel is OPEN and the selector fails.

### CHANNEL 1 — THE CELL
STATUS: **PLAUSIBLY CLOSED, CONDITIONALLY. NOT YET AUDITED AGAINST THIS REQUIREMENT.**
Candidate closer: `BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md` (2026-07-24), which fixes the
cell via four inputs, the fourth being the marginal closure rule `C_R = 1` (CLEANROOM compactness
ratio) — "This fourth input is the selector. Without it, public recoverability gives a half-line of
allowed durations and no absolute record scale."
CARRIED CONDITIONS THAT MUST BE STATED IN ANY CLAIM OF CLOSURE:
- `C_R = 1` is an **ADOPTED Level-1 rule**, not a projected curvature condition; the threshold is
  passed into the producer as a function argument, with `physical_premise_proved_by_script = false`.
- The file's own flags read `absolute_record_interval_derived_in_declared_branch = false` and
  `primitive_record_radius_derived_in_declared_branch = false`; only
  `conditional_absolute_record_interval_formula_derived = true` is affirmative.
- `BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:184-185`: "the causal-cell formulas are exact
  conditional algebra, **not an absolute scale derivation**."
WHAT WOULD CLOSE IT PROPERLY: show that no admissible one-parameter cell deformation preserves the
upstream principles while moving the stiffness — i.e. that `C_R = 1` is forced rather than adopted,
OR that the adoption is itself the second requirement and say so in those words.

### CHANNEL 2 — THE DURATION
STATUS: **PLAUSIBLY CLOSED, CONDITIONALLY, AND WITH A KNOWN RESIDUAL AMBIGUITY.**
Candidate closer: the same derivation's record interval.
CARRIED CONDITION: a `sqrt(2)` ambiguity between two standard quasi-local energies
(`BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:89-104`), which that gate says cannot be
resolved "by Hamilton-Jacobi language alone." **A residual factor is not a one-parameter family, so
this channel may be closed even while the ambiguity stands — but the ambiguity must be disclosed
wherever the closure is claimed.**
WHAT WOULD CLOSE IT PROPERLY: the energy-convention selection, plus the same no-surviving-family
argument as channel 1.

### CHANNEL 3 — THE MATTER ACTION
STATUS: **OPEN. NO CANDIDATE CLOSER IDENTIFIED.**
This is the first of the two channels where the audit is expected to bite. Relevant known blocks:
`COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md:66-76` — "This is not the ordinary finite
`c_R F^2` counterterm objection. It survives even after an independent bare Maxwell term is
forbidden: distinct parameter-free charged-record dynamics induce different finite responses." Also
`STAGE8_ACTION_FORM_UNDERDETERMINATION_INVENTORY_V001.md` and the corrected action-completion count
(at least 4, not 2 — `S_rot` and `S_BF` exist).
WHAT WOULD CLOSE IT: a microscopic-generator uniqueness theorem. This is the obligation four
independent lines of work converged on and it is the program's single most-cited missing object.

### CHANNEL 4 — THE FIELD-ENERGY INTEGRAL
STATUS: **OPEN. NO CANDIDATE CLOSER IDENTIFIED. LEAST EXAMINED OF THE FOUR.**
This is the channel with the least prior work and it is where the reviewer expects the surviving
freedom to live, precisely because nobody has looked. It is also the channel that would carry a
charge self-energy, if a charge self-energy is admissible here at all — which is itself unsettled.
WHAT WOULD CLOSE IT: unknown. Determining what the corpus even says about it is step one.

## 5. WHAT THE AUDIT MUST NOT CONCLUDE

Three guards, each earned by a documented error of the last two days.

1. **A shift is not a dilation.** `c_R` (slot 9) is an ADDITIVE offset, not a multiplicative one, and
   `partial c_R / partial x = 0`. **NOTHING IN THIS AUDIT REACHES SLOT 9.** Closing all four channels
   would defeat the scaling family and leave the additive freedom untouched. Do not let a
   four-channel success be reported as closing slot 9.
2. **`beta`, `c_R` and `C_R` are three different objects** — a fiber-radius metric datum, an additive
   `F^2` coefficient, and a compactness ratio. Pairing any two of them is the over-generalization
   that has already been made once and struck.
3. **Scale-breaking conditions exist in this corpus and they failed differently** — at least eight,
   which failed by MONOTONICITY (no interior stationary point) or by producing a definite wrong
   number. So "break the symmetry" is necessary and nowhere near sufficient: a channel closure must
   also survive the monotonicity failure mode.

## 6. THE HONEST STATUS OF THE WHOLE DEPARTURE

`STAGE8_RESCALING_EXCLUSION_RECOVERY_ERRATUM_V001.md:76-83`:
`rescaling_exclusion_recovered_to_working_set = true`,
`rescaling_exclusion_discharged = false`,
`v001_rescaling_exclusion_dropped_silently_relative_to_working_set = true`.

And by its own text the exclusion is a NECESSARY condition on a selector, not a selector. Discharging
it does not produce a number. `ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V001.md:45-54` types the
related threshold "RETAINED AS A SUPPORT/CELL CONSTRAINT; RETIRED AS A STANDALONE `K_R` SELECTOR."

**SO THE CLAIM THIS AUDIT CAN SUPPORT, AT MOST: the scaling family is defeated in N of 4 channels,
and the residual freedom is exactly the union of the open ones.** That is a real result and it is
not alpha.

## 7. CHANNEL LEDGER — UPDATE THIS TABLE, DO NOT REWRITE THIS FILE

| Channel | Status | Candidate closer | Blocking condition | Updated |
|---|---|---|---|---|
| 1 cell | PLAUSIBLY CLOSED (conditional) | BID minimal cell, 2026-07-24 | `C_R = 1` adopted, not forced | 2026-07-30 |
| 2 duration | PLAUSIBLY CLOSED (conditional) | same | `sqrt(2)` energy convention | 2026-07-30 |
| 3 matter action | OPEN | none | microscopic-generator uniqueness | 2026-07-30 |
| 4 field-energy integral | OPEN, LEAST EXAMINED | none | unknown — scope it first | 2026-07-30 |

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
