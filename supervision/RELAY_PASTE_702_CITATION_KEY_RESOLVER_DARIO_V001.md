## PASTE 702 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / TRANCHE] V011 ENVELOPE + THE RESOLVER KEYED BY CITATION — LAST STEP BEFORE THE CONFIRMATION RUN

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. THE FIRST-TIME-RIGHT RULES apply. A's V011 is mirrored: spec V011 = d48e0fa7…, report 82873bd5…, inventory d1cd7e70…. Registrar pre-verified: seals; parent.py/producer.py byte-unchanged against the pre-relay mirror; fence clean.)

CONTEXT (Q-615 ruling now spec text): condition 2 resolves a ground atom ONLY through its sealed source-and-span citation — R9 keys its P0-verified evidence table by (source_sha256, span) and member_key binds to exactly the row whose citation the sealed descriptor carries. Your P1 refusal is what this amendment answers.

TASK (bounded):
P1. ENVELOPE-CHECK V011: one schema-only statement and mechanical carriage; 66/66 rows byte-identical; the mapping closed (no filename, no digest-self-reference, no producer mapping); confirm A's byte-unchanged claim from bytes. Anything more is a finding.
P2. KEY THE RESOLVER by the citation per V011; the condition-2 refusal path becomes the resolution path on real inputs — show r_ground resolve end-to-end on the true member (and the perturbation control still flip it). Re-pin; regenerate sidecar and instance TOGETHER from final bytes (your own 700 lesson).
P3. Dry-run every touched path; PIN CLOSURE grep value AND name (superseded root ddc09a3d… iff bytes change, V010 31ccee9c… live references).
OUTPUT: updated package + one sealed artifact `STAGE8_TASK6_CITATION_KEY_RESOLVER_DARIO_V001.md`
with final lines: `ENVELOPE = one statement only / (+findings)`, `ROWS_CHANGED_CONFIRMED = 0`, `RESOLVER_KEYED = citation; r_ground resolves on real inputs (controls hold)`, `VERIFIER_ROOT = <hex> (changed/unchanged)`, `PIN_CLOSURE = N hits, all resolved`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors and invokes RUN 032 — the confirmation run). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
