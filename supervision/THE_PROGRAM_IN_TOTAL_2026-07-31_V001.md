THE PROGRAM IN TOTAL — 2026-07-31

Complete statement of where the alpha derivation stands. Supersedes no ruling; assembles them.
Register head Q-84. alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## 1. THE TARGET AND WHAT WOULD FORCE IT

```text
alpha_micro = 1/(4 pi K_*)          K_* = the root of C_record(K) = 0
```

*** WHAT FORCES A NUMBER IS THE THRESHOLD — A REQUIRE WHOSE EXISTENCE AND UNIQUENESS ARE BOTH DERIVED
(Q-65, Q-71). NOT OVERDETERMINATION. *** The old principle "only overdetermination forces a number" fused
two different things: **forcing is a fact about the admissible set; anti-fitting is a fact about how we know
the point was not chosen.** Overdetermination is not required, is not pursued, and was struck from S9-A
(Q-76).

**And the gate for the threshold already exists** — `STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md` §5.3, written
a day before the grading that justifies it. It passes only on **one simple positive root, no inequivalent
admitted positive root, no continuous modulus**, with the root **solved once and independently reproduced**,
and it fails if **uniqueness is obtained by narrowing the admitted family after seeing the root.**

**What "fitting" means here, exactly (Q-83 discussion).** Under an allow-shaped structure the theory's
output is a **set**, not a number; selecting a member needs an input the theory did not supply, and the only
source is the measurement. *** SO FITTING IS NOT PRODUCING A WRONG VALUE. IT IS REPORTING A SET AS THOUGH IT
WERE A VALUE. The failure is representational, and you cannot fit accidentally if you report honestly. ***
There is a second level: choosing the **premises** so the forced value lands where you want. That is what
target-blindness and §5.3's narrowing clause defend against.

---

## 2. THE ROAD — THREE STEPS, NO FOURTH (Q-83)

```text
STEP 1   C_record(K) EVALUABLE    the loop-closing map / response-extraction layer
STEP 2   THE SCALE FIXED          C_R = 1 selects, via the Hamilton-Jacobi bridge;
                                  k_R sits INSIDE step 1's equation (Q-73)
STEP 3   RUN SECTION 5.3          the uniqueness gate
```

**No relay is issued unless it names which step it advances, or names the specific blockage it clears.**

---

## 3. THE ONE OBJECT THAT CARRIES THE RISK

*** `COMMON_ORIGIN_CTP_PRODUCER_ALGEBRA` (Q-81). *** A new class — **not** a tensor product, crossed
product, direct limit or C*-extension. `pure_algebraic_extension_class_sufficient_for_rank1_object = false |
TYPE-R`, an executed refutation. **For two days the program asked "which extension is it?" The question was
malformed.**

**Its seven components, as one package:** P0 one microscopic source-record-field operator and dynamics
(B0-like); P1 the completed carrier with a common dense domain; P2 a faithful source embedding reproducing
the Q-41 quasifree/GNS data; P3 a record embedding compatible with the Q-42 direct limit and Q-43
monomorphism; P4 the CTP apparatus — branch embeddings, index ordering, branch metric, reality involution,
gauge-fixed physical quotient, invariant measure; **P5 a positive normalized `rho_pre` and admitted record
effects, SUPPLIED BY THE SAME MICROSCOPIC SOURCE AS P0**; P6 the dynamics `S_CTP` / `U_BR[A,g]` on the common
domain; P7 contact rules and the raw correlator output interface.

*** THE CONTENT IS THE WORD "COMMON." Most pieces exist in some form. The requirement is that they all
descend from ONE operator. That is why it is not an extension — you are not gluing algebras, you are
demanding they be aspects of a single thing. ***

**Why that is physics and not bookkeeping:** alpha is meant to be an **induced** response. If the record's
state, effects and dynamics come from separate postulates, the response is partly postulated and "induced"
stops meaning anything. **Common origin is what makes `K_bare = 0` more than a slogan.**

**The hardness sits in P5 and P6.** Requiring the algebra to exist is assembly; requiring `rho_pre` and the
effects to be **supplied by the same source as the dynamics** is a derivation demand nobody has met. **What
exists today is a signature. P0 is unbuilt.**

---

## 4. WHAT IS WORK, WHAT IS HARD, AND WHAT MAY BE MALFORMED

**JUST WORK — method known, effort large.** The `CTP_PHYS_INPUT_PACKAGE` is **0 complete / 9 partial / 0
wholly absent** — nothing missing outright. The bridge ladder `S1 -> {S2,S4} -> S3 -> S5 -> S6` has S1, S2,
S4 specified, S3 with a named route, `S3 -> S5` a sealed inference. **36 of 64 debts are routed**, ~13 of
those naming specs that already exist. **The debt is mostly queue, not wish.**

**GENUINELY HARD — no known method.** The **two wish-grade proofs**: the grading-forcing derivation and the
crossed-product exclusion. *** BOTH ROOT IN THE PRODUCER ALGEBRA (Q-78). *** `tau = 1`'s discharge object —
now named as one **complete public `D_BR`/`L_BR` spectral theorem** (Q-84), where a day ago it had no route
at all. The **~28 unexamined `PATHLESS-CLAIMED`** obligations, four of them `ROUTE-FORECLOSED` — naming was
tried and died.

*** POSSIBLY MALFORMED — AND THIS IS THE SHARPEST OPEN CONCERN. §5.3 DEMANDS AN EXHAUSTIVE AUDIT OVER THE
ADMITTED FAMILY. CODEX 1 CHARACTERIZED WHAT THE THEORY ADMITS AS A `MIXED_TYPE_ALLOWANCE_ENVELOPE`
CONTAINING AN **OPEN ACTION-FORM CLASS**. YOU CANNOT EXHAUSTIVELY AUDIT AN OPEN CLASS. *** Two caveats,
both untested: the §5.3 admitted family may not be the same object as the allowance envelope, and "open"
may mean *not yet enumerated* rather than *not enumerable*. **If both collapse the wrong way, step 3 is
unreachable as written and steps 1 and 2 build toward a gate that cannot be passed.** **NOT ESTABLISHED.
NOT SCHEDULED. NAMED.**

---

## 5. THE CONDITIONALITY — WHAT A COMPUTED ALPHA WOULD BE WORTH TODAY

**A conditional derivation, singly-attested (Q-65).** Exact modulo named adopted premises, each
independently testable — **one grade below a pair, not disqualified.**

*** IT WOULD REST ON EIGHT ADOPTIONS THE CORPUS ITSELF RECORDS AS OWED DERIVATIONS (Q-74). The axiom floor
is real, small and clean — 18 axioms, few, motivated, target-blind, several falsifier-armed, NONE carrying
absolute scale. BUT EVERY ADOPTION THE VALUE PATH TOUCHES IS DEBT-IN-AXIOM-CLOTHING, 8 OF 8. THE CLOTHING
DOES NOT SCATTER; IT SITS EXACTLY WHERE ALPHA WOULD COME FROM. ***

**The tell:** the one scale-carrying leaf is `C_R = 1`, and its own release condition reads *"derives,
rather than assumes, the marginal closure condition."* **The corpus wrote down that a derivation was owed
and adopted it anyway.**

**And the disclosure that must travel with any number (Q-76):** the threshold route **cannot self-check one
thing.** §5.3's "solved once and independently reproduced" catches computational and implementation error.
**It cannot catch the condition itself being physically wrong, because there is nothing to disagree with
it.** `S9A_RESIDUAL_ERROR_RISK_CLOSED = false | TYPE-C`.

---

## 6. THE COUNTS

```text
0 of 18   Q_spec slots derived            unmoved
0 of 11   Section-1 pieces                unmoved
18        clean scale-free axioms         the floor, and it is defensible
8         debt-in-axiom-clothing          100% of the value path's adoptions
~64       debts                           exactly ONE is a structural axiom
~32       PATHLESS-CLAIMED                obligations with no route; 4 ROUTE-FORECLOSED
172+      named unbuilt objects           >=163 edges, acyclic, depth >=5
          the graph's entire boundary is unbuilt; no chain reaches derived ground
```

*** THESE COUNTS MEASURE DERIVED SLOTS AND WORK STATE. THEY ARE NOT A PROGRESS METRIC. A day can move
everything that matters and leave the first two untouched. ***

---

## 7. THE METHOD FINDINGS THAT OUTLIVE ANY RESULT

- **Permissions never force a number** — but a **unique require** does, and so does exact joint
  determination. Three forcing shapes, not one (Q-65).
- **Exhaustion over an adopted premise stack forces nothing — it restates the stack** (Q-56).
- **Only TYPE-R negatives are physical content.** TYPE-U unbuilt, TYPE-S scope-empty, TYPE-C
  constraint-blocked are work-state (Q-54).
- **A well-founded theory bottoms out in axioms, not derived results** (Q-70).
- **Pathlessness is diagnostic** — a node with no discharge route is terminal, and terminal is what an axiom
  looks like (Q-82).
- **Never identify a flag with the object that discharges it** — both conditional cycles fired on exactly
  that operation (Q-69).
- **When something fits no class, name a new one.** *** FIVE IMPORTED FRAMINGS HAVE FAILED IN THREE DAYS,
  FOUR OF THEM THE REVIEWER'S: only-overdetermination-forces, no-chain-reaches-derived-ground,
  two-disjoint-critical-paths, which-extension-is-it, and the tau-pin operator identification. EVERY FIX WAS
  A NEW CATEGORY, NEVER A BETTER CLASSIFICATION *** (Q-80).
- **Read the artifact, not the report.** Lane summaries lead with the verdict asked for; incidental findings
  land in the flag block and never reach the summary.
- **Substring and name matching have produced false positives in the corpus, in an audit script, in the
  audit of the audit, and in this register.** Word-boundaried matching only.

---

## 8. WHAT IS RUNNING, AND THE ONE UNASKED QUESTION

**Einstein — is `C_record(K)` a self-consistency residual?** If yes, step 1's blocker narrows from "build
the producer algebra" to "close one loop," and OBS-08 becomes live: **a fixed point looks exactly like a
cycle to a graph walker.**

**Codex 2 — the S3 join**, where the `sqrt(2)` Misner-Sharp / Brown-York fork could close by **derivation
rather than choice.** *** THE FORK MUST BE DERIVED, NEVER CHOSEN: a pick makes it a second scale-bearing
adoption and moves the program toward a fit. ***

*** THE UNASKED QUESTION: DO INSTANCES 3, 4, 7 AND 8 BLOCK A STEP, OR ONLY DEGRADE THE CONDITIONALITY? Q-81
found they advance only at interface level. If they do not block, they are DISCLOSURE work — writable at the
end — and the road is roughly half as long as the register implies. ***

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
