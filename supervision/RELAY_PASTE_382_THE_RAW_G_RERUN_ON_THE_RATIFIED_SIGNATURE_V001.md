PASTE 382 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE RAW-G RERUN ON THE RATIFIED SIGNATURE — DOOR D's NAMED CHECK, EXECUTED (DoR-015 IN FORCE)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-299 + DoR-015/C35)

```text
DOES THE OBJECT EXIST?   THE CHECK does, named since Q-287 and posed in every proposal
                         version: the bounded-extension test for the raw-G bilocal, which
                         FAILED on unweighted T_cyl (unit-modulus no-go, Q-290) and is now
                         re-run ON THE RATIFIED SIGNATURE (DoR-015: the six amplitude-side
                         structures exist as adopted premises -- the weight structure the
                         no-go said was missing is now IN FORCE).
IS THE VERSION CURRENT?  YES: DoR-015 ratified (C35). Marks: TYPE-P | premises: DoR-008/
                         009/013/014/015. ALL door flags binding, F included. The W3
                         precision binds: restrictions are ADJOINTS of isometric
                         inclusions. Rank + no-selection discipline binds.
ARE ITS INPUTS PRESENT?  YES, for the first time: the ratified signature (V005), X_phys,
                         the cycle currents, P2 V002, P4, the seed, the verified stack.
```

## 1. THE TASK — EXECUTE THE CHECK THAT EVERYTHING WAITED ON

```text
1  CONSTRUCT the signature-weighted bilocal map on the ratified field layer: the source
   derivative tensor (Q-284, verified) carried through the ratified field representation
   and bilocal class, on X_phys, with the cycle-current structure as the response family.
2  *** EXECUTE THE BOUNDED-EXTENSION TEST: does raw G now extend boundedly from the
   trace-class source domain into the ratified field layer? This is Door D's physical
   image check -- the adoption's own named falsifier arm. PASS: the lift EXISTS and the
   chain runs. FAIL: DoR-015's Door D fires and the adoption is in question -- REPORT A
   FAIL AS LOUDLY AS A PASS. ***
3  IF THE LIFT EXISTS: run the chain as far as it now goes -- lifted G with sector
   structure and p-content per block; the connected subtraction; what the physical
   inverse and retarded extraction (the ratified Schur account) now support. RESTRICTION
   CHECK at every stage: Q-243/Q-279 reproduced EXACTLY (the DoR-008 falsifier's bite).
4  THE ACCOUNTING: every operation gets its six-account row (the Q-288 taxonomy + F);
   the Tail_R column explicit; any completion flagged.
5  THE p-TRACE: p-content per block through the lifted chain, symbolic; where the
   retarded image stands after the lift (the verdict shadow updated).
6  KILL-PASSES: no selection (torsor/intertwiner/anchor/rank); no unflagged completion;
   no naive truncation (W3); no evaluation.
DELIVERABLE: the Door-D execution verdict (EXTENDS / FAILS, with proofs), the lifted
chain as far as it runs, accounting complete, the p-trace, and the updated
remaining-objects list toward the transport verdict.
```

## 2. LEADS

```text
IF THE LIFT EXISTS AND THE RETARDED IMAGE STAYS p-CLEAN -- *** LEAD WITH BOTH: Door D
closes, the field layer is CONFIRMED IN EXECUTION, and the verdict chain's remaining
distance shrinks to the background fiber + consumption signature. ***
IF THE LIFT FAILS -- *** LEAD WITH THE FAILURE: Door D fires on the ratified adoption;
the principal must see it first. ***
IF p ENTERS THE RETARDED IMAGE THROUGH THE LIFTED STRUCTURE -- lead with the exact
entry: that would be survival's mechanism, on the record at last.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
