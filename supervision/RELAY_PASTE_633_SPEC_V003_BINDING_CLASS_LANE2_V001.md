## PASTE 633 — CODEX LANE 2 (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / SUBGATE] EVALUATOR SPEC V003: THE BINDING-CLASS REPAIR

(Same Lane 2 session rules: write and seal in the CLEANROOM only; the registrar mirrors. Cite sealed packet copies, never the unsealed root.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-568 (your scoping V003, registered).
- Base: cleanroom `STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE3_V002.md`, SHA-256 `25e89ed24933d66fcb6991b83bb971775be834070f8570a8b89cd0248f22edb7`. Verify before reading.
- The conviction of record (Q-567): `STAGE8_TASK6_SPEC_V002_RERENDER_CHECK_DARIO_V001.md`, SHA-256 `da6575b0f366ec5af6aef008dd48c21ce028290fd7019a55b615afbbab67d772`. Verify, read, implement; do not relitigate. Also consult the census source `BID_FULL_STACK_REVIEW_LEDGER_V003.md` (`c09f2c24…`) for every blocker text you touch.
- THE PRINCIPLE (Q-567): V002's sweep validated VOCABULARY (terms map to opcodes) but not BINDING (the opcode actually receives the object the blocker demands). V003 repairs the CLASS.

TASK:
B1. **Kill the escape hatch (SP1-07).** Remove the `{DERIVED_IN_SCOPE, CARRIED_OUT_OF_SCOPE}` coverage manifest and its identifiers (`M_required_scope`, `M_coverage`, `in_scope`) — the sealed blocker grants no carve-out. Re-render on the SP2-05 pattern: completeness bound to a proof index via `ENUM(grammar,cert)` + `COMPARE` against the enumerated IDs + `KERNEL` replay for every derivation demanded, so a PASS with zero derived IDs is structurally impossible.
B2. **Fix SP2-07's confirmed weakened-direction drift** (quote the blocker at its span; restore the exact demand). **Close SP2-06's gap** (name the reality-factor carrier as a content-addressed input consumed by its SPECTRAL/DOMAIN procedure).
B3. **The class sweep, binding edition — all 66 rows.** For every criterion: every reference to a proof, premise, derivation, certificate, or "displayed/target-independent/certified/exhaustive" property must BIND — the proof a pinned input replayed through `KERNEL.success`; completeness through `ENUM(grammar,cert)`; comparisons through `COMPARE` on enumerated objects; adjectives without a receiving opcode deleted or rendered. Repair the two convicted rows first (`C-B-V010-06` — the criterion must DERIVE, not merely BE, target-independent, per its own requirement line; `C-B-V009-10`), then explicitly adjudicate the three warned rows (`MR-06`, `SP1-04`, `V010-10`), then sweep the remainder. Report per-row: BOUND / RE-RENDERED (+what was unbound). The verifier's expected-census section and any counts updated to match.
B4. **Protected sections untouched:** the runtime pin (§9.1), the authority firewall, the aggregate rule (§9.5) — their pinned block hashes must verify unchanged.
B5. **Carriage (complete finite delta):** every hunk in the delta table with its authorizing item (B1/B2/B3), everything else verbatim, verified by re-diff, summary displayed. Battery: F_PLDEC; anti-tuning; M-2 (four modes); self verb audit.

OUTPUT: one artifact, lane-tagged filename, written and sealed IN THE CLEANROOM
`STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE2_V003.md`
with final lines: `ESCAPE_HATCH = removed (+SP1-07 rebound)`, `SP2_07 = restored`, `SP2_06 = carrier_named`, `BINDING_SWEEP = 66/66 (+BOUND count / RE-RENDERED count with row list)`, `PROTECTED_SECTIONS = unchanged (hashes verify)`, `CARRIAGE = complete_finite_delta (+hunk count)`, `AUTHORIZATION = not_claimed`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the cleanroom or archive workspace, STOP and report — do not overwrite. Seal in the cleanroom, report both hashes, STOP (the registrar mirrors). No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
