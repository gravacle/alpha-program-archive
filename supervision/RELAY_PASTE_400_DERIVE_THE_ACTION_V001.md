PASTE 400 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] DERIVE THE LEGENDRE PAIRING AND ACTION MAP FROM WHAT EXISTS (THE ACTION RACE, DERIVATION ARM)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-317 + C39)

```text
DOES THE OBJECT EXIST?   NO: the COMPLETED_PHYSICAL_LEGENDRE_PAIRING_AND_ACTION_MAP --
                         the ONE remaining arrow (Q-317). THIS ARM DERIVES; the other
                         lane authors, uncoordinated.
IS THE VERSION CURRENT?  YES through Q-317 + C39.
ARE ITS INPUTS PRESENT?  THE CONSTRAINING STOCK IS ENORMOUS AND ALL PROVEN: REC_GEOM_DIAG
                         _399 (the one-object geometry); the adopted six (DoR-015 --
                         including the bilocal class and source rigging, which a pairing
                         PAIRS); the proven normalizations that a Legendre structure must
                         reproduce (the W-convention, N = i·Id, the Q-276 sign, the
                         sealed identity coefficient); the seed's exact first derivatives;
                         G^007's closed form; P2's calculus (Frechet, the duality P2
                         ALREADY carries: l1+trace-class has an explicit dual); the
                         reality law; the finite references. The measure/contour/boundary
                         content is the expected authored residue -- FIND OUT how much.
```

## 1. THE TASK — DERIVE EVERY COMPONENT THE STOCK FORCES, NAME THE REST

```text
1  THE PAIRING: the physical (Abar, G) tangent/duality structure and scalar pairing --
   attempt the derivation from P2's EXISTING duality (the trace-class/operator pairing
   is canonical), the adopted bilocal class, and the sealed identity's own bilinear
   structure (the same-correlator identity IS a pairing statement -- extract the pairing
   it presupposes; the identity is sealed, so the pairing it uses may be FORCED).
2  THE LEGENDRE MAP: sources <-> fields duality -- the germ's J/R ports against the
   signature's field representation; the Legendre structure that makes the PROVEN
   relations (D_J W = (i/hbar) G . seed forms . G^007) into its OWN first-order
   conditions. The candidate theorem: the Legendre map is DETERMINED by requiring the
   proven derivative relations to BE its stationarity/duality relations -- uniqueness
   up to the battery, or the exact freedom named.
3  THE ACTION: Theta_phys/Gamma_2PI as the Legendre transform of the built W-structure
   over the derived pairing -- constructed as far as forced; every place the MEASURE,
   CONTOUR, or BOUNDARY data genuinely enters gets NAMED as residue (the authored arm's
   exact target list -- the race's value is this list's precision).
4  IF THE DERIVED PART SUFFICES FOR THE LOCUS: run steps 3-4 of the Q-316 program
   (locus existence/naturality; uniformity) on the derived action; then the stationary
   blocks; then THE VERDICT with the full chain. If residue blocks the locus, state
   exactly which residue item and why.
5  KILL-PASSES: derivations cite proven structure only; no convention import; no
   selection; symbolic ranks; six-account rows; door flags; no evaluation; DO NOT
   COORDINATE with the authoring arm.
DELIVERABLE: the derived components with proofs, the named authored residue (exact), and
-- if the locus runs -- THE p-VERDICT. The race adjudicates on your residue list.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
