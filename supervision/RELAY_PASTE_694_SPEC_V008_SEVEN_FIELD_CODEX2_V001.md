## PASTE 694 — CODEX 2 LANE = BUILDER A (5.6 SOL EXTRA HIGH) — [TASK 6 / TRANCHE] SPEC V008 + SEVEN-FIELD INVOCATION + PARENT ARGV: ONE WHOLE DELTA

(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. THE FIRST-TIME-RIGHT RULES apply: pins generated never transcribed — one pin manifest, full closure in this delta, grep value AND name; interfaces are schemas never prose; no unexecuted path ships; state changes ship whole.)

CONTEXT (Q-611, registrar-verified against sealed V007 text): run 030's REPLAY_DISAGREE is adjudicated. Neither builder erred on the graph — B's independent recomputation of r_dag agrees with your PASS atom (11 nodes, root SPEC-SEAL, sink FINAL-CLAIM-SEAL, five negative controls). Two defects remain, and this relay ships both fixes whole:

TASK:
H1. AUTHOR SPEC V008 (finite delta from V007 d38d3171…; the 66 descriptor rows UNCHANGED). Exactly three statements, closing the P0 replay gap B named without guessing:
  (a) R9 COMPUTES P0 ITSELF from supplied inputs. A producer-emitted P0 result object is a fault, never an input (BR-1: a producer-declared object may not carry a criterion's direction).
  (b) R9's launch contract gains `--subject-manifest <path> --subject-manifest-sha256 <hex>` and `--evidence-manifest <path> --evidence-manifest-sha256 <hex>` on the existing `--ledger`/`--ledger-sha256` pattern. State the new CLOSED argv inventory and the verifier-manifest instance's new closed `argv` and `input_roots` field inventories AS SCHEMAS (exact keys), never prose. §2.1's six conjuncts are then all replayable by R9; no exemption.
  (c) A named refusal value for "precondition not replayable" — a fail-closed refusal DISTINCT from criterion-FAIL, recorded with the missing carrier named. A verdict of FAIL that was never evaluated is not a verdict.
H2. PRODUCER: emit the full SEVEN-field invocation carrier V007 §9.4 already requires — `{opcode, result_name, args, instance_id, source_sha256, span:[start,end], span_sha256}` — where `span_sha256` is the digest of exactly the raw span bytes and the three linkage fields agree with the `instance_id` packing. (B's contract now enforces seven and will refuse four; both builders were below the sealed text here — this is conformance to V007, not a change.)
H3. PARENT: validate the extended verifier-manifest schema per H1(b) and pass the new argv. Your package re-pins to V008 — full pin closure in this same delta (grep the V007 value AND name across the package; every hit updated or justified in the artifact).
H4. Dry-run: producer emission shape displayed for C-B-V009-06 (seven fields, real values); parent manifest-validation path executed against a syntactically valid V008-shaped instance you construct as a TEST FIXTURE clearly marked non-authoritative (B authors the real instance next relay). No chain invocation.

OUTPUT: `STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE2_V008.md` + updated package + one sealed artifact
`STAGE8_TASK6_V008_SEVEN_FIELD_CODEX2_V001.md`
with final lines: `SPEC_DELTA = three statements only / (+items)`, `ROWS_CHANGED = 0`, `SEVEN_FIELD_EMITTED = displayed`, `PIN_CLOSURE = grep value+name, N hits, all resolved`, `DRY_RUN = executed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, STOP (the registrar mirrors; Builder B integrates next — run 031 only after that). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
