PASTE 338 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] DRAFT SOURCE_GERM_PHYS THROUGH THE GATE (RATIFICATION RESERVED: DoR-013)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-254)

```text
DOES THE OBJECT EXIST?   COMMISSIONED BY RULING (plan C30): a DRAFT -- not an adoption -- of
                         SOURCE_GERM_PHYS, the common-origin pointwise Z_inc[J,R]: the ONE
                         authored gate of the 8-package minimal deciding subset (Q-254).
                         The D6/law pattern, second use: authored through the gate, the
                         principal ratifies or rejects (DoR-013 reserved).
IS THE VERSION CURRENT?  YES through Q-254. The consolidated spec artifact (241bf43f...)
                         defines the germ's REQUIRED FIELDS (P1, incl. topology_src,
                         Diff_src) and its consumers.
ARE ITS INPUTS PRESENT?  The ratified finite data (the law, F_N, A_N^(p), the Q-239
                         skeleton), the Q-254 spec, and the CONSTRAINT BATTERY (below).
```

## 1. THE TASK — AUTHOR THE GERM AGAINST THE BATTERY, THEN TRY TO KILL IT

```text
1  DRAFT SOURCE_GERM_PHYS complete per the Q-254 P1 field list: the pointwise common-origin
   functional Z_inc[J,R] with its source topology, source calculus/differentiation, common/
   difference source family, and its restriction behavior to every finite stage. EVERY field
   explicit; nothing "to be specified"; a germ with a hole is not a draft.
2  THE CONSTRAINT BATTERY IS BINDING -- certify the draft against EVERY item, one by one:
   (a) DoR-008 falsifier: the germ's restriction to every finite stage reproduces the sealed
       finite results -- INCLUDING Q-243's zero, p_ch-free finite retarded block;
   (b) Q-245: no naive continuous extension exists -- the germ must DECLARE its extension
       mechanism and topology explicitly, not inherit one by continuity;
   (c) Q-247: state IN WHICH CLASS the germ's Z_inc and its derivatives live (norm/module vs
       bidual) -- this typing decides the tail exposure downstream and MUST be explicit;
   (d) Q-251/Q-253: the germ determines how H_R[G] and ultimately p_loc's argument are fed --
       state the consumption it induces for each of the six outputs, B_ind FIRST;
   (e) Q-252: the germ must either determine the background pair (Abar_*, G_*) or declare it
       a separate input -- no silent fixing;
   (f) *** FINITE-AUTHORITY (the principal's basis, battery item): if the germ gives ANY
       alpha-facing output a tail-visible consumption, it imports non-finite physics into
       alpha -- the gate KILLS such a draft. ***
3  THE COMMON-ORIGIN HALF: state exactly what "common origin" the germ supplies -- and TEST
   THE IDENTIFICATION against Q-242's Gen_Omega spec (generative preparation/carrier-descent
   producing rho_S, ready ray, and law from shared primitive data): does THIS germ realize
   that role, partially, or not at all? PROVE the answer; the P5 witness rides on it. An
   honest "the germ is common-origin for SOURCES only, not for state/law" is a complete
   answer.
4  THE p_ch TRACE, structural only: with the germ in place (as premise), state what the
   8-package subset's dependency chain yields for p_ch -- which packages become buildable,
   and WHERE the cancellation/survival verdict would then be computed. DO NOT COMPUTE IT;
   ratification gates everything downstream.
5  KILL-PASSES ON YOUR OWN DRAFT (Q-207 gate): hidden adoptions inside definitions; smuggled
   measure/contour (the germ may DECLARE them as authored fields but every one must be on
   the choice table with alternatives and void-conditions); any field whose only support is
   "standard practice"; any answer-defined structure (a field chosen BECAUSE it makes p_ch
   cancel -- or survive -- is target-tuning and dies). REPORT what the hunt found; repair by
   removal where possible.
6  DELIVERABLE: the draft as a PROPOSAL with a choice table (each authored field: what it
   adds, alternatives considered, what would void it), every battery certificate PASS/FAIL/
   CONDITIONAL, the Gen_Omega identification verdict, and the p_ch dependency statement.
   Nothing TYPE-P. DoR-013 is reserved.
```

## 2. LEADS

```text
IF THE DRAFT SURVIVES THE FULL BATTERY -- lead with the choice table and what one
ratification unlocks (packages, in order). If the Gen_Omega identification HOLDS, say so in
the same breath -- one ratification, two witnesses.
IF THE FINITE-AUTHORITY ITEM FORCES THE GERM'S TYPING -- *** LEAD WITH THAT: if every
battery-surviving germ is NECESSARILY tail-annihilating, then p_ch's tail route is closed BY
THE BATTERY ITSELF, before ratification -- the strongest available sentence. ***
IF NO GERM SURVIVES THE BATTERY -- lead with the killing constraint pair: two battery items
no germ can satisfy simultaneously would be a finding about the program itself, and the
principal must see it exactly.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
