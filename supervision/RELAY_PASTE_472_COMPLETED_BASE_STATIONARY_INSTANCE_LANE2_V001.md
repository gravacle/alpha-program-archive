## PASTE 472 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4b] THE INSTANCE ROUTE: THE EXECUTABLE COMPLETED BASE STATIONARY FAMILY THROUGH THE GATE

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-392. The (P3-8) decision needs either this instance or the kernel certificate (471, running in parallel in Codex 1, blind to you).
- Authorities (verify hashes): the confirmed computation `27790d53…` and its confirmation `7aa42db3…` (what the instance must feed); metric V005 `2a379098…` (the ratified completed carrier — the R5 identification and completion convention are IN FORCE); square V004 `abf6d366…` (Γ_base's ratified structure, the stationary equation); DoR-009 (the base law); Q-243/279/309 (the finite zeros ANY instance must reproduce on restriction — the DoR-008 falsifier is binding and failure-capable).
- Custody: through the full gate — derivation first (does the ratified completed carrier + base law FORCE the completed stationary family on the minimal stages?), authored residue disclosed if any; PROPOSED_NOT_ADOPTED; no response-facing justification anywhere (the G2 standard); p and ν symbolic.

TASK:
M1. **Derivation check:** on the ratified completed carrier, is the completed base stationary family on the MINIMAL STAGES (the reciprocal loop; S8-A) forced by the ratified base law and the completion convention? Work it — the completion may make the minimal-stage stationary problem exactly solvable.
M2. **Construct the executable instance** (derived where forced, authored-through-the-gate where not): the completed base stationary point y*(p) and the base blocks A, B, C, D with their explicit p-dependence on the minimal stages; every certificate (covariance, reality, batching, restriction — the finite zeros reproduced EXACTLY on restriction).
M3. **Evaluate (P3-8) on the instance:** compute RetExtract[∂_p Schur_base] on the minimal stages symbolically. Report the result exactly — zero, nonzero with the form, or conditional with the exact condition. THE REPORT IS NOT THE VERDICT; it registers only after the parallel route and cross-verification.
M4. **Falsifiers:** DoR-008 restriction to the finite shadows; all standing regressions; the anti-tuning ledger (order of construction — the instance must not be shaped by a desired (P3-8) outcome; the derivation-first discipline is the defense).

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK4B_COMPLETED_BASE_STATIONARY_INSTANCE_LANE2_V001.md`
with a lead-result block (`INSTANCE = DERIVED / DERIVED_WITH_AUTHORED_RESIDUE (+items) / BLOCKED (+gap)`; `P38_ON_INSTANCE = ZERO / NONZERO (+form) / CONDITIONAL (+condition)`), the register-sweep list, and the M1–M4 chain. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No numeric evaluation; no registered verdict; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
