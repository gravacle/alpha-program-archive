PASTE 133

Written 2026-07-30, for CODEX LANE 1. **A PASTE IS A SNAPSHOT.** Any ruling later than this overrides it
without notice. Lane 2 is running slot 18 under paste 132 — do not touch slot-18 or A32 artifacts.

Your paste-131 return is verified: hash `e8d87f06…` matches, R-52 appended, `MISSING SPECIFICATION` is
accepted, and F-GK1 does not fire. **Two things you did are worth naming.** You declined to commit the ten
supervision files that were not yours and said so explicitly — that is exactly the protocol working, and it
is the first time in this program that a lane has left another lane's staged work alone. And your
path-list detector found **18 sites**, which is 18 more than I expected; about eleven are in real reviewer
artifacts from 07-27 to 07-29. I am re-running those myself.

**ONE ITEM. Author the construction spec. Do not construct.**

Fences unchanged: read-only outside your own writes; compute no alpha, kappa, coupling, radius, scale,
root or eigenvalue; no comparison to any measured constant; file:line + verbatim quote for every claim;
bounded negatives state roots and exclusions and report FILE LISTS; report refutations, never repair them.
`alpha_computed = false; proof_authorized = false` on everything you write. Never touch
`a32_holdout/custodian_private/`.

---

### NEW SINCE PASTE 131 — READ THIS BEFORE WRITING THE SPEC. IT CHANGES WHAT THE SPEC MUST DECLARE.

**Q-22 settled F-FL1** (`MB Work/alpha_supervision/RESULT_FFL1_SMALL_S_END_2026-07-30.md`). Your own
F-FL1 note reached the same place independently — no artifact fixes the small-`s` end — and the ranked
landscape says why:

`alpha_spectral_ncg_absolute_stiffness_research_v001.md:349-366` carries a seven-row PASS/BLOCK table.
**RANK 1** — "Fully finite **total** record-cell triple plus exact normalized determinant/CTP trace" —
would fix the small-`s` end outright: "Finite matrix determinant gives an absolute cell Hessian once
`D[A]`, state, measure, and unit character are fixed." Its blocking fact: "Current carrier is only
internally finite; **spacetime spectral support**, cell density, and CTP construction are missing."

**RANK 6 IS THE CURRENT CARRIER**, and it is the row your construction lands in: "Exact continuum
determinant of the current single Dirac carrier ... `A_4` logarithm and finite local `F^2` mutation
require subtraction/matching" — verdict **PASS-RUNNING / BLOCK-ABSOLUTE**.

*** THE CONSEQUENCE THE SPEC MUST CARRY: `Gamma_K` SUPPLIES THE CTP CONSTRUCTION AND THE MEASURE. IT DOES
NOT SUPPLY SPACETIME FINITENESS. SO A COMPLETED CONSTRUCTION WITH A UNIQUE SIMPLE POSITIVE `C_record` ROOT
DETERMINES `K_*` CONDITIONALLY ON THE INDUCED-ONLY AXIOM, NOT ABSOLUTELY. *** The closure residual is
built from an action carrying the `A_4` log, so `k_R` — the proper-time floor — appears in the equation
that fixes `K_*`.

This does not fire F-GK1 and does not weaken the charter. **It is what F-GK3 exists to force into the
open**, and declaring it at the outset is the difference between a conditional result and a result that
later turns out to have been conditional. Do not discover this at the end.

Also register-worthy and already in Q-22: the cancellation route is dead structurally — the `F^2`
divergence is LOGARITHMIC, a log cannot be cancelled by mode counting, and a vanishing `A_4` `F^2` part
would be superconvergence, contradicting `alpha_step5_zero_bare_compositeness_boundary_v002.md:53`,
`lim d K_1D/d ln Q = -1/(6 pi^2)`. **And the reframing: the proper-time floor is the price of an
incomplete carrier** — the induced-only axiom stands in for a finiteness property the carrier lacks, so
the floor and the stitching gap are ONE problem, and a derived stitching/continuum rule would move the
construction from rank 6 to rank 1.

### THE TASK — `STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001`

**A SPEC, SEALED BEFORE ANY EXECUTION.** Covering your own assembly steps 1-4 only. Steps 5-6 (the
mutation/uniqueness gates, then solving once for the root) are specified as ACCEPTANCE CRITERIA in this
document and executed later, in that order, never before.

Why a spec and not the build: the program's discipline is sealed spec, then hostile review, then
execution, with predictions frozen before computation. And preregistration v002's own evaluation order
says "Run mutation and uniqueness gates before solving for K." A construction that runs ahead of its own
acceptance criteria cannot be checked, and this object is too central to produce unfalsifiably.

**SECTION 0 — DECLARED CONDITIONS, STATED FIRST.** Not a caveat at the end.
- The induced-only axiom (`K_bare = 0` with the proper-time floor at `k_R`) is a CONDITION of this
  construction, not an output of it. Cite `alpha_induced_only_boundary_action_principle_v001.md:9-19`.
- On the current carrier this construction is rank 6: `K_*` will be determinate and CONDITIONAL, not
  absolute.
- Every premise beyond the current stack that the construction turns out to need is declared HERE or the
  result is void (F-GK3).
- State where a derived stitching/continuum rule would enter if one is ever produced, so the spec does
  not need rebuilding to consume it.

**SECTION 1 — `Gamma_K` AND ITS MEASURE.** What must be derived: the complete normalized
source-record-gravity CTP functional; the measure and operator domains; the gauge-fixed physical
quotient; the contour measure. For each: what it is, what it is derived FROM at file:line, and what
check would show it wrong. Name every piece you do NOT have.

**SECTION 2 — THE STATIONARY CELL `X_K`.** Including the Lorentz-invariant proper interval **derived
rather than inserted** — no record clock put in by hand. This is where the `sqrt(2)` energy question
lives, and you should note it: neither Misner-Sharp nor Brown-York is automatically the Hamiltonian
conjugate to the tip-to-tip proper interval, because the diamond is a CTP history-difference support and
not a material timelike boundary (`BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md`, and its ratio
`E_BY/E_MS = 2/[1+sqrt(1-C)]` is exactly 2 at the adopted `C = 1` and 1 at `C = 0`). **The required
conjugate energy does not exist yet. Specify it as a target, do not pick one of the two.**

**SECTION 3 — THE BR CLOSURE OPERATOR AND ITS SPECTRUM ON `X_K`.**

**SECTION 4 — THE SCALAR RESIDUAL `C_record(K)`.** Derived from the on-shell problem. **It may not be
defined to vanish at a desired value** — that is failure rule 2 of the selector principle and it is the
single most likely way this construction goes wrong.

**SECTION 5 — ACCEPTANCE CRITERIA, FROZEN NOW.** The five-channel mutation audit (geometry, clock,
measure, regulator, action-partition) and the uniqueness gate, with pass/fail conditions and tolerances
written before any number exists. **F-GK4: the audit does not count if it passes only on a pinned
cellulation or pinned skeleton** — that is quantifier pinning and F-2 forbids it under any outcome.
Then, and only then, one root solve with independent reproduction (F-GK5: solving early voids the result
whatever its value).

**SECTION 6 — FALSIFIERS.** F-GK1..F-GK5 restated as this spec's own, plus any the construction adds.

**PROHIBITED IN THIS TURN:** executing any step; solving for `K_*`; evaluating any response; running the
mutation audit; reusing `L_open` in any renamed form; selecting between `E_MS` and `E_BY`.

### TWO SMALL THINGS

**NAMESPACE.** `Gamma_K` collides — coupling-indexed CTP functional here, BID cell-set amplitude
(`Gamma_K(A) = -log|A_K(A)|`, 12 files) there. Your v004 draft has it. **State the sense explicitly at
first use in the spec**, or disambiguate the symbol; a spec whose central object shares a name with a
different live object is a trap for the next lane.

**ROOTS.** Your target list spelled the parent tree two ways — `Documents/New project/...` and
`Documents/Documents - Brian's MacBook Pro/New project/...`. Same inode, same 3,291 `.md` files, same 34
modified since 07-29: **one tree, two paths.** No divergence. But any search stating both roots
double-counts every file, which is the file-count erratum's family. State roots canonically.

---

REPORT BACK: hashes, the exact committed paths, `corpus_check` gate verdict, deploy state, and the list of
pieces Section 1 could not supply. **If while specifying you find that a step cannot be specified without
an undeclared premise, lead with that** — under F-GK3 it is more valuable than a complete-looking spec.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
