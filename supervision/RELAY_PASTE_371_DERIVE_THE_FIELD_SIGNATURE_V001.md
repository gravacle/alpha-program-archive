PASTE 371 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] DERIVE THE FIELD SIGNATURE FROM THE RECORD SURFACE — THE BETA-GAP ATTACK WITH TONIGHT'S MACHINERY (THE RACE, DERIVATION ARM)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-288)

```text
DOES THE OBJECT EXIST?   THE TARGET is exact (Q-287): the physical field/spacetime SIGNATURE
                         raw G requires -- what T_cyl provably lacks. THE GAP is the
                         program's oldest: the internal/external conversion of a record
                         cell (the beta gap), never attacked with what now exists.
IS THE VERSION CURRENT?  YES through Q-288. *** WHAT IS NEW SINCE THE GAP WAS LAST HIT:
                         (1) the RATIFIED ORIGIN'S SECOND PRIMITIVE -- the rooted
                         SIGNED-INCIDENCE realization generating the ready ray and LOCAL
                         WRITE STRUCTURE (DoR-013; it survived the selection-trap attack,
                         Q-260 A1) -- ratified geometric structure at the origin;
                         (2) T_cyl with retraction naturality (Q-258); (3) the derived
                         discrete record-side connection (the old Gate-4 result);
                         (4) the separation machinery and P2's topology; (5) the law's
                         E_post orientation. ***
ARE ITS INPUTS PRESENT?  The above, all ratified/sealed. The four failed smooth-bridge
                         attempts and their walls (the 270-era record) BIND as known
                         graves -- this is NOT the patch/cover/functor direction.
```

## 1. THE TASK — SIGNATURE FROM WRITE STRUCTURE, NOT SMOOTHNESS FROM PATCHES

```text
1  STATE what "field/spacetime signature" minimally means for raw G's contract (from the
   Q-287 artifact and the sealed response texts): what structure must the field layer
   carry that T_cyl lacks -- a causal/temporal split? a locality/support structure? a
   conjugation/reality structure? ENUMERATE the signature's required components; the
   derivation targets THE COMPONENTS, not the word "spacetime."
2  THE DERIVATION CANDIDATE, component by component, from the ratified write structure:
   the origin's signed-incidence realization generates LOCAL WRITE STRUCTURE -- rooted,
   signed, incident. Signs give orientation (with E_post: the time-arrow); incidence
   gives locality/adjacency; roots give basepoints; the law's sequential composition
   gives causal order; T_cyl gives the completion these live on. ATTEMPT: construct the
   signature components AS INVARIANTS of the ratified write structure on T_cyl -- the
   internal/external conversion becoming "the external structure IS the write
   structure's public shadow" (the program's own thesis, finally testable).
3  TEST against raw G's contract: with the derived components, does the bilocal map gain
   its bounded extension (the Q-287 refutation was for UNWEIGHTED T_cyl -- does the
   signature-weighted version exist)? Run the exact test that failed and report the
   difference.
4  THE HONEST STOPS: any component NOT derivable from ratified write structure gets
   named and typed -- that residue is exactly what the authoring arm (running in the
   other lane; DO NOT COORDINATE) must supply, and the race's value is the precision
   of your residue list.
5  KILL-PASSES: no (M,g) import; no smoothness demanded anywhere (signature, not
   manifold); every limit names its topology; the Q-288 door flags FILLED for every
   class-formation step you perform; no rank values.
DELIVERABLE: the derived signature components with proofs, the raw-G extension re-test,
the residue list typed. This is the PROVABLE arm of the race -- what it derives, no
authoring ever needs to assume.
```

## 2. LEADS

```text
IF THE SIGNATURE DERIVES ENTIRE -- *** LEAD WITH IT: the beta gap CLOSES BY DERIVATION,
the field layer is the write structure's shadow, and the lift can run. The program's
oldest wall would fall to its own thesis. ***
IF COMPONENTS SPLIT -- lead with the residue list: derived components + the exact
authored remainder is the race's decision-grade output.
IF THE WRITE STRUCTURE IS TOO POOR -- lead with which component fails and why; the
authored arm then carries everything, knowingly.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
