## PASTE 726 — CODEX 2 LANE (5.6 SOL EXTRA HIGH) — [7A / STEP 11] THE FIVE INSTANCES, AUTHORED FROM SEALED PROSE

PICKUP-ACK + LANE GUARD in force (CODEX 2).
(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. ALL standing laws apply.)

CONTEXT (Q-631): the compiler is proven and starved. The five contract-ready targets (rows C-B-V008-05, V008-10, V009-08, V010-11 + the fifth in your target manifest 477d0389…) need SEALED SCHEMA-INSTANCES — the actual content, extracted from sealed prose sources into the BX01/BX03/BX09/BX13 forms.
TASK:
KK1. For each of the five targets: author the instance FROM SEALED SOURCES ONLY — every element of the instance grounded (source + span, block-covered, object-names probed); an element the sources underdetermine is NAMED and the instance delivered PARTIAL; no element manufactured; requirement prose never serialized as its own witness.
KK2. RUN YOUR COMPILER on each authored instance (this is what it was built for): validation, binding into the row's carrier/envelope, statuses recomputed honestly. Admission stays barred (Step-11 subgate); state it.
KK3. Census: per row — components produced / still missing with owners.
OUTPUT: instances + compiler outputs + one sealed report `STAGE8_7A_INSTANCE_BATCH1_CODEX2_V001.md`
Final lines: `INSTANCES = 5 authored (complete/partial per target, gaps named)`, `COMPILED = N bound (compiler output digests)`, `ROWS_ADVANCED = list (new statuses)`, `ADMISSION = barred, stated`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, write 726_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
