## PASTE 536 — CODEX LANE 2 (HIGH EFFORT) — [TASK 5 / EQ6] ASSEMBLY STAGE 1 V002: THE THREE PHYSICAL FACES, NOT THEIR SHADOWS

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-459.
- Your stage-1 build (`e21cee30…`) was killed on all six review items. Read the review of record first:
  `alpha-program-archive/workspace/STAGE8_TASK5_EQ6_ASSEMBLY_STAGE1_REVIEW_LANE1_V001.md`
  SHA-256 `049f6386835adcf5089a74d49add67c76973c5bb89fe3571c1e5fbceb8b0f5df`. Verify before reading — especially V1 (compression V1-5 vs forward V1-4), V6 (the incompatible-section witness), and the corrected remainder.
- What SURVIVES of your build (do not rebuild): scalar/Hessian pullback composition; the Q-243/Q-279 shadows; the ordinary action/Hessian and Ward clash rows; the compressions as lawful weaker objects on their proved scopes.
- Custody: builder repairs; V002 returns to Lane 1. The failure mode to avoid is NAMED: a rails-level look-alike claimed as a physical face. Every closure claim must state which governing equation it satisfies ON THE NOSE.

TASK — build the three missing physical faces:
W1. **The action-family compatibility certificate.** One family section on ALL arrows with proven naturality — not stagewise fibers with maps between them. The certificate must EXCLUDE the V6-1 incompatible-section witness by proof (show the construction cannot admit it), not by scope restriction. If no such certificate is provable from the sealed stock, report the exact obstruction — which arrow, which equation — and STOP on W1.
W2. **The forward Eta naturality square.** J12's forward response equality on every I_F arrow — the forward leg (V1-4), not its adjoint compression (V1-5). State precisely the relation between the forward square and the lawful compression (when does the compression determine the forward leg, and why that condition holds or fails here).
W3. **The one exact package square.** With W1+W2: the package-wide natural transformation `Bot(f)`/`rho^pkg` covering every sealed consumer through the one functor — J15's single exact square. Equality on the nose; spot-state three instances explicitly.
W4. **Honesty clause:** a genuine obstruction on any of W1–W3 outranks a completed build — report it exactly. An obstruction here would mean the finite package itself resists joint assembly, which is first-order physics for the program.
W5. **Battery:** the V6 attack rerun as a permanent regression; the nine geometric regressions; anti-tuning ledger; delta vs V001 (what is kept, what is replaced, what is new).

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V002.md`
with final lines:
`ACTION_FAMILY_CERT = BUILT / BLOCKED (+obstruction)`, `FORWARD_ETA_SQUARE = BUILT / BLOCKED (+obstruction)`, `PACKAGE_SQUARE = BUILT / BLOCKED (+obstruction)`, `CLASH_FOUND = none / witness`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
