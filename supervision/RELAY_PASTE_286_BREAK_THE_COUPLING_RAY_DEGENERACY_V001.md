PASTE 286 — CODEX LANE 2 (HIGH EFFORT) — [TASK 3d] RUN THE FORCING PROTOCOL ON THE COUPLING-RAY DEGENERACY

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-202)

```text
DOES THE OBJECT EXIST?   YES, AND IT IS THE RARE FULLY-INSTANTIATED CASE: the 16-dim odd/odd span
                         {X_S,Y_S}(x){X_R,Y_R}(x){I,X,Y,Z}_E, grading Z_w = w_S Z_S + w_R Z_R +
                         w_E Z_E, and the sealed census 22/8/12/4/3
                         (FULL_CARRIER_DILATION_SELECTOR_OBSTRUCTION_GATE_V001.md).
IS THE VERSION CURRENT?  YES; and the adversarial recomputation (Q-183, 8bc5f105...) is BINDING.
ARE ITS INPUTS PRESENT?  *** YES -- THE FAMILY IS INSTANTIATED: 8 nullity-2 grading laws forming
                         4 distinct kernel planes. Q-200's no-schemas rule is SATISFIED HERE,
                         which almost nowhere else on the board is true. ***
```

## 1. THE OBJECT AND THE STANDING RESULTS

```text
FAMILY      8 primitive triples of nullity 2 -> *** 4 DISTINCT KERNEL PLANES *** (verified, Q-183)
STANDING    seam 10: "STEP 5 FAILS, IT DOES NOT MERELY GO UNRUN" -- the degeneracy is REAL
BINDING     Q-183 refuted the reviewer's narrowing: the unsigned-sum selector was CIRCULAR -- "it
            selects the announced channel IN ITS PREMISE." *** ALL FOUR PLANES ALREADY SATISFY
            THEIR OWN SIGNED CONSERVATION EQUATION -- a selector must cut BETWEEN them, not
            restate what each satisfies. ***
ALSO        every nullity-2 kernel uses only X_E/Y_E -- the environment factor is OFF-DIAGONAL in
            all survivors (verified). Stated nowhere in the corpus; may be a handle.
```

## 2. THE TASK — THE PROTOCOL, WITH THE FAMILY ALREADY FROZEN

```text
STEP 1  DONE BY THE CORPUS: the family is the 8 laws / 4 planes. FROZEN. Do not re-derive.
STEP 2  DECLARE THE EQUIVALENCE. Q-183 preserved BOTH counts (8 laws, 4 planes) because no sealed
        equivalence identifies same-plane laws. *** IS THERE A SEALED PHYSICAL EQUIVALENCE (frame
        relabeling, convention change) THAT DOES? Quote it or type TYPE-U. THIS ALONE DECIDES
        WHETHER THE TARGET IS 8 OR 4. ***
STEP 3  ATTACH FAILURE-CAPABLE CONSTRAINTS FROM SEALED PHYSICS -- candidates to test: the compact
        vector-U(1) conservation law (the CORPUS'S, not an import) . CPT/reality . positivity of
        the closure sector . the witness-flip behavior the gate itself records . Lorentz/sector
        typing of S vs R vs E. *** FOR EACH: NAME WHICH PLANE(S) IT KILLS. A CONSTRAINT SATISFIED
        BY ALL FOUR SELECTS NOTHING (that was the Q-183 failure -- do not repeat it). ***
STEP 4  COVERAGE: the enumeration |w_i| <= 2 is the census's own bound. *** IS THAT BOUND DERIVED
        OR CONVENTIONAL? If conventional, say what a larger bound admits at nullity 2. ***
STEP 5  COUNT THE SURVIVORS. 1 -> the coupling ray is FORCED. 2-4 -> the residual, with what
        distinguishes it. 0 -> the constraints overcut; report which pair conflicts.
```

## 3. LEADS

```text
IF ONE PLANE SURVIVES -- *** LEAD WITH THE SELECTOR AND ITS SEALED PROVENANCE. Seam 10 is the
coupling ray itself; forcing it is the largest single step toward Task 4 on the board. THEN ATTACK
IT: is the selector derived, or selection wearing a forcing costume? ***
IF THE EQUIVALENCE IN STEP 2 CUTS 8 -> 4 BUT NOTHING CUTS FURTHER -- lead with that; "forced up to
a four-fold residual with no sealed cutter" names the next physical input exactly.
IF THE |w| <= 2 BOUND IS CONVENTIONAL AND NULLITY-2 IS UNBOUNDED -- *** LEAD WITH THAT ABOVE
EVERYTHING; the family was never closed and the census must be re-run before any selection. ***
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
