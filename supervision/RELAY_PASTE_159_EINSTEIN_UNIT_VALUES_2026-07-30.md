PASTE 159 — EINSTEIN — ITEM: ARE THE PROGRAM'S UNIT-VALUE ADOPTIONS INDEPENDENT?

2026-07-30. Snapshot; later rulings override. **REGISTER HEAD AT ISSUE: Q-55.** If a ruling lands mid-run
that bears on your item, **report it rather than finishing against a stale set.**

```text
EINSTEIN  (you) = independence of C_R = 1, tau = 1, tau_R, T_R, tau_orth    [paste 159]
CODEX 1         = S8 write-tail join — RETURNED (Q-55)
CODEX 2         = raw-correlator-to-retarded-Hessian map                    [paste 158]
```

**OFF LIMITS TO YOU:** the response-extraction layer, the correlator-to-Hessian map, the scalarization
bridge, `Gamma_K` (Codex 2 holds these). You may **cite** Q-55's S8 result; do not reopen S8 itself.

Fences: never touch `a32_holdout/custodian_private/`. Do not compute alpha, kappa_record, kappa_Thomson, a
coupling, a radius, a scale, a root, an eigenvalue, or a beta function.

---

### THE RULES NOW IN FORCE

**Q-52:** no constraint may prevent a test. **You may specify a missing object in order to test it** —
declared at the outset, `derived = false`, never reported as derived. **Q-54:** every negative carries a
type — **TYPE-R** refuted (the only type that is physical content), **TYPE-U** unbuilt, **TYPE-S**
scope-empty (must carry roots, exclusions, fences, query), **TYPE-C** constraint-blocked. **`NO_VERDICT` is
a legal answer** when a failure condition rests on untested, inapplicable or unbuilt premises. **Reporting
"not found" requires stating the search.**

---

### WHY THIS ITEM, NOW

Three separate findings today each landed on an **unselected unit value**, and nobody has asked whether
they are the same choice appearing repeatedly.

- **`C_R = 1`** — Q-45: *the only absolute-scale selector in the program*, UNTESTED, no frozen falsifier.
  Lane 2 later showed a falsifier is **designable** but gated on an object Q-51 says cannot be started.
- **`tau = 1`, the proper-time floor** — Q-49: a unit-value adoption with **a complete positive-`tau`
  competitor family open**, and V011 explicitly denying that the unit interval follows.
- **`tau_R = pi/sqrt(2)`, with `A_c = tau_R`** — Q-55, one hour ago: what closes the S8 exchange
  magnitude, **fixed by ER-A as disclosed branch data, not by a derived law.** ER-A is itself UNTESTED.

*** IF THESE ARE ONE CHOICE, THE PROGRAM HAS ONE UNSELECTED SCALE. IF THEY ARE FOUR, IT HAS FOUR. NOBODY
KNOWS WHICH, AND EVERY FUTURE NUMBER IS CONDITIONAL ON THE ANSWER. ***

### THE TASK

1. **INVENTORY THE UNIT-LIKE ADOPTIONS.** At file:line, from sealed text: `C_R = 1`, `tau = 1`, `tau_R`,
   `T_R`, `tau_orth`, and any others the search turns up. **State what each one IS** — a duration, a ratio,
   a normalization, a boundary condition — because they are being discussed as though interchangeable and
   **they may not even be the same kind of quantity.**

2. *** ARE THEY INDEPENDENT? *** For each pair: does fixing one fix the other? Is one definitionally the
   other in different units? **Type it; do not name-match — "tau" appearing in four names is not
   evidence.** The standing conditionality `tau_orth = T_R` is a live example of exactly this and should be
   checked rather than assumed.

3. **HOW MANY FREE SCALE CHOICES DOES THE PROGRAM ACTUALLY HAVE?** Give a number and defend it. **If the
   honest answer is that it cannot be determined from sealed text, say so with the search stated** — that
   is TYPE-S and it is a real finding.

4. **DOES ANY OF THEM CARRY ABSOLUTE SCALE BESIDES `C_R = 1`?** Q-45 called `C_R = 1` *the only* absolute
   scale selector. **Test that claim.** If `tau = 1` or `tau_R` also carries absolute scale, Q-45's
   characterization is wrong and the conditionality is worse than recorded. **If `C_R = 1` really is the
   only one, say what makes the others relative.**

### CONSTRAINTS

- **Do not evaluate `tau_R`, compare it to anything measured, or draw any inference about alpha from
  `pi/sqrt(2)`.** It is a cell-time integral. **Nothing about the fine-structure constant follows from a
  `pi` appearing in a write chain, and treating it as a clue is exactly the fitting risk this program is
  most exposed to.**
- **Report refutations; never repair them.** If Q-45's "only absolute-scale selector" claim is wrong, that
  is my error to record, not yours to soften.
- **F-GK3:** declare any premise beyond the current stack at the outset.

---

REPORT BACK, with **EINSTEIN** on your first line: hashes, exact committed paths, gate verdict, the four
answers, **and every negative typed.** **If the program has more than one unselected absolute scale, LEAD
WITH THAT** — it would change what a computed alpha could honestly claim, and it is currently recorded as
one.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
