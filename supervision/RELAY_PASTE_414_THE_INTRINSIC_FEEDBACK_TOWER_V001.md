PASTE 414 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE INTRINSIC FEEDBACK TOWER — THE CHANNEL DERIVED FROM ACCESS, THE TOWER FED BY ITS OWN RECORDS (THE FINITE DEPTH PROGRAM, COMPUTATION TWO)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-331)

```text
DOES THE OBJECT EXIST?   THE CHANNEL: NO -- the corpus lacks the record-output -> source
                         map (Q-330's named gap). THE DERIVATION CANDIDATE (Q-331): a
                         record's exposed content IS its required charge/flux access
                         (the ratified law's own access certificate); the germ's source
                         ports define what a source consumes; the channel = access
                         composed with ports -- expose then feed. Potentially DERIVED
                         from ratified structure, no authorship.
IS THE VERSION CURRENT?  YES through Q-331 (the declared-probe tower is VERIFIED sealed
                         structure -- your own build, now cross-confirmed; consume it).
ARE ITS INPUTS PRESENT?  YES: the ratified law's charge/flux access certificate (READ
                         its exact content -- what a completed record EXPOSES, as maps),
                         germ V007's source ports (J and R -- their exact consumption
                         types), the verified tower (034a7aab + a5f10f92), the
                         no-post-output-supplementation discipline (the channel must be
                         structured as EACH NEW TIER'S INPUT -- the tower's lawful
                         pattern -- never as retroactive supplementation).
```

## 1. THE TASK — DERIVE THE CHANNEL, COMPUTE THE INTRINSIC TOWER

```text
1  DERIVE the channel: the composition record-access -> source-ports, type-checked
   exactly (does what the access certificate exposes MATCH what the ports consume?
   If the types match, the channel is DERIVED; if a conversion is needed, name it and
   its provenance -- a typed gap, not a silent fix). Certify: no post-output
   supplementation (the record feeds the NEXT tier only); reality/quotient/rank
   discipline.
2  COMPUTE THE INTRINSIC TOWER, exact: tiers fed by their own records' exposed access
   content -- NO declared probes anywhere. Closed forms for the tier-k state, weights,
   amplitude, and the accumulated exponent, as functions of (k, p) ALONE (intrinsic =
   source-free = the k-dependence is the record dynamics' own).
3  *** THE INTRINSIC DEPTH QUESTION: does the intrinsic tower's accumulated quantity
   GROW with k -- and with WHAT FORM? Linear? LOGARITHMIC (the principal's thread's
   prediction)? Saturating? The FORM, exact, is the deliverable -- and if logarithmic,
   the log-divergence-is-the-coupling thread lands as a sealed finite theorem. ***
4  FALSIFIER DISCIPLINE: tier-1 = the sealed one-cell structure; the declared-probe
   tower recovered as the special case where the channel is overridden by declared
   sources; identity behavior; restriction structure.
5  KILL-PASSES: no authored conversion (typed if needed); no probe smuggled as
   "access"; the k-dependence COMPUTED never chosen; no continuum objects; no bridge
   claims; no evaluation; door flags.
DELIVERABLE: the channel (derived or typed-gap), the intrinsic tower in closed form,
THE INTRINSIC k-DEPENDENCE -- the datum the coupling has been waiting for -- or the
exact proof that intrinsic feedback also fails to deepen.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
