PASTE 306 — CODEX LANE 2 (HIGH EFFORT) — [TASK 2d] RUN THE FORCING PROTOCOL AT THE STATE-CLASS STRATUM

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-223)

```text
DOES THE OBJECT EXIST?   The STRATUM does. At the functional level the constraints only ALLOW
                         (continuum, Q-219). The class level is UNTESTED -- and the program's
                         pattern is exact: laws allowed/planes required (Q-204/205). The
                         principal ruled: check whether the class is REQUIRED before any
                         physics is authored.
IS THE VERSION CURRENT?  YES -- Q-223's corrections bind (quasifree = response cumulants
                         DETERMINED-BY the two-point covariance, not vanishing; the source
                         branch does NOT govern the joint state).
ARE ITS INPUTS PRESENT?  The ratified carrier, the sealed constraints, the class taxonomy to be
                         DECLARED in step 1.
```

## 1. THE PROTOCOL, AT THE CLASS STRATUM

```text
STEP 1  DECLARE THE FAMILY OF JOINT STATE CLASSES on the ratified carrier, by STRUCTURAL CLOSURE
        PROPERTY, before any outcome: at minimum -- multiplicative/product . quasifree-type
        (Wick-determined by a two-point covariance) . finitely-correlated . unrestricted.
        *** THE TAXONOMY MUST BE EXHAUSTIVE UNDER ITS DECLARED CLOSURE PROPERTIES OR COVERAGE
        FAILS (the Q-200 lesson). If exhaustiveness cannot be proved, say so at step 4. ***
STEP 2  EQUIVALENCE: classes equal under the sealed conventions are one.
STEP 3  FAILURE-CAPABLE CONSTRAINTS, each with its sealed provenance and a named victim:
        (i)   PARENT-STATE COVARIANCE -- the common-origin requirement (sealed);
        (ii)  STATIONARITY on the declared contour;
        (iii) U1 reality/branch compatibility;
        (iv)  THE ONE-CELL FINITE AUTHORITY -- the restriction must reproduce the sealed
              one-cell structure;
        (v)   *** RECORD EXISTENCE: a record IS a source-record correlation -- the sealed
              REQUIRED side of the origin table (charge/flux access; thresholded nonreturn;
              completed-record persistence) demands NONVANISHING source-record connected
              correlations. DERIVE THIS CONSTRAINT'S EXACT SEALED FORM BEFORE USING IT; if the
              corpus does not support it as stated, USE ONLY WHAT IT SUPPORTS. Its named victim
              if it holds: THE MULTIPLICATIVE CLASS -- killed by record existence, NOT by any
              statement about the coupling. ***
        *** THE TARGET-AWARENESS FENCE IS ABSOLUTE HERE: "the response must be nonzero because
        alpha is nonzero" IS FORBIDDEN AS A CONSTRAINT. Record existence is lawful; the
        measured world is not. ***
STEP 4  COVERAGE, honestly. STEP 5: SURVIVORS, counted.
```

## 2. LEADS

```text
IF ONE CLASS SURVIVES -- *** LEAD WITH THE REQUIRE. The ask becomes ONE TWO-POINT COVARIANCE on
the carrier; D6 dissolves without authorship; and the state port's route is: derive the
covariance, place the state. THE BEST AVAILABLE OUTCOME. ***
IF A SIZED FAMILY SURVIVES -- lead with the list; the principal authors from it, honestly.
IF COVERAGE FAILS -- lead with why; the taxonomy itself is then the missing object.
IF RECORD-EXISTENCE CANNOT BE DERIVED AS A CONSTRAINT -- *** SAY SO PLAINLY; do not use it
anyway. The protocol's integrity outranks the wished-for kill. ***
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
