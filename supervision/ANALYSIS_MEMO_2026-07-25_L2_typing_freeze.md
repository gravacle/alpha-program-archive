# Analysis Memo — L2 Typing Freeze (completed vs exhaustive Hessian)
Claude lane · 2026-07-25 (night) · fresh-context typing-analysis lane return,
recorded verbatim. Verdict: freeze on the COMPLETED chain (option a) — the
sealed record adjudicated it three times (V011 semantics; battery T7(i)/NC5/
T15; the amplitude-selection derivation); A4(3)-as-relocation violates A4's
own no-weakening clause; the two Hessians are physically different (the
exhaustive one is dominated by unrecorded sectors at small completed
probability q). Cost of (a): one append-only A4(3) erratum (Brian sign-off),
one conditioned-covariance normal-form lemma (cheap: sealed O5 Q_comp tangent
already exists), one typing note on the periodic zero-free lineage. Zero
battery changes; zero v004 critical-path collision.

All reads complete. Here is the analysis memo.

---

# ANALYSIS MEMO — L2 Typing Freeze: Which Object Carries the Intensive Hessian Sourcing kappa_record

Fresh-context typing-analysis lane, 2026-07-25, per DISPOSITION_2026-07-25_gamma_killtest_beta.md item (5.4). Read-only; no measured constants consulted or used. Root for all cited artifacts: `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/` (route memos at `/Users/bgm/MB Work/alpha_supervision/`).

**VERDICT UP FRONT: Freeze on the COMPLETED chain (option a). The two objects are physically different Hessians on the actual parent — but the sealed record has already adjudicated which one kappa_record means, three separate times, so the freeze is a typing clarification, not an open physics choice. A4(3) read as an exhaustive relocation contradicts A4's own no-weakening clause and cannot be the operative reading. One append-only erratum to the amendment is required; the completed-chain re-derivation of H_K = g_{D,K} is cheap because the operator-level Duhamel tangent is already sealed for the completed-sector kernel (O5, Q = Q_comp). The item is construction-lane-sealable with a single Brian sign-off on the erratum (because it annotates a sealed artifact), not a Brian-level physics fork.**

---

## 1. The exact textual evidence

**The battery authority builds Gamma on the completed amplitude.** STAGE8_GATE5_KAPPA_RECORD_THEOREM_BATTERY_SPEC_AUTHORITY_FABLE_V002.md, T7:

> "(i) derive the primitive completed-record amplitude Z_h(A) = a_h(A)/a_h(0) per BID_PUBLIC_RECORD_TRANSITION_AMPLITUDE_DERIVATION_V001.md (pinned) — the log defining Γ is ill-defined without it; (ii) prove a volume-uniform zero-free neighborhood for **that amplitude** (V011 1232–1233; A35) — failure is BLOCKED; … (iv) verify the Duhamel covariance computed from G_L reproduces the intensive Hessian (V011 1299–1300). The intensive limit of R_record,L is the candidate kappa_record source."

Its negative control NC5 makes the typing load-bearing: "reproduce that the root survival amplitude at τ_R is exactly zero (log-singular) and REJECT it as a response object (A24/A35); the completed-record amplitude Z_h of T7(i) is the derived replacement." The pinned V011 semantics (BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md 1217–1300) are univocal: "The root survival amplitude is not the response amplitude… The primitive completed-record amplitude is instead derived…"; "The remaining connected theory must prove a volume-uniform zero-free neighborhood for **the derived amplitude**"; `Gamma = −log|A|` with A the completed amplitude (1257); `R_record,L` is the intensive Hessian of `Gamma_K/N_4(K)` and "The Duhamel covariance computed from G_L must reproduce this Hessian" (1292–1300).

**The amendment relocates the same obligations onto the exhaustive closure.** STAGE8_T7_OPERATOR_VALUED_PRIMITIVE_RESPONSE_ARCHITECTURE_AMENDMENT_V001.md, A2: `Z_K[A_+,A_-] = omega_source(R_all,K) = omega_in(W_K[A_-]^dagger W_K[A_+])`, with "the completed-sector operator is not substituted for the exhaustive operator." A4(3):

> "the zero-free neighborhood, logarithm branch, connected linked-cluster density, and intensive Hessian are obligations on `Z_K`, not on a nonexistent primitive scalar;"

**The conflict, precisely.** T7(ii)'s "that amplitude" is Z_h (completed); A4(3)'s `Z_K` is by A2's definition the exhaustive all-outcome closure. Both sentences claim the same four obligations for different objects, and the substitution fences run in both directions: A2 (above); STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_SPEC_V001.md O6 ("The all-outcome sum may not be substituted for the Route-1 completed component"); STAGE8_ROUTE2_CAR_STATE_BRIDGE_SCOPE_CORRECTION_V001.md, which types the expressions `omega(dGamma(R_all^(1))); omega(Gamma(R_all^(1))); Tr(C R_all^(1))` as "different constructions [that] may not be selected or normalized after comparison" and itself keeps the two objects apart ("it computes a completed-record amplitude rather than the exhaustive relative-history state functional"). So neither object may be quietly swapped for the other — yet the two authorities point the same obligations at different objects. The gamma memo (ROUTE_MEMO_2026-07-25_gamma_record_level_insensitivity.md, 1.1 and 3(4)) flagged exactly this as L2.

**Where they reconcile.** A4's own closing sentences are the key: "This amendment changes the **type location** of scalar closure. It does not weaken any numerical, analytic, continuum, durability, or locality gate." A4(3)'s contrast is "on `Z_K`, **not on a nonexistent primitive scalar**" — its target is the scalarization no-go (the completed Kraus member is non-scalar; no primitive scalar exists before state evaluation), not the completed-vs-exhaustive sector choice. Read as "analytic obligations attach only to state-evaluated scalars," A4(3) is consistent with the battery: the completed chain also becomes a legitimate state-evaluated scalar (the Phase-A spec STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md A4, sealed after the amendment, constructs exactly that: `K_pointer(a) = sum_lambda w_lambda Gamma(u_lambda(a))` alongside `R_all`). Read instead as a sector relocation, A4(3) contradicts A4's own no-weakening clause — see Section 2 — and contradicts the battery it lists as its first frozen authority. Under the program's reading rules, the self-consistent reading governs.

## 2. The physics

**Completed object** (T7(i)/V011 lineage; Phase-A realization `Z_ER(a) = Σ_λ w_λ ω(Γ(u_λ(a)))`, pointer weights `w_λ = <pointer|P_λ|ready>` with m0 = Σw = 0): a ready-to-pointer transition amplitude — the response of the completed public record to a connection tangent. Gamma = −log of it is a **record-conditioned susceptibility**. Its raw modulus is small (witnessed 5.9e-4…6.8e-2 per the gamma memo's reading of the sealed V002-gate context); its baseline anchor Z_h(0) = 1 is a derived normalization (BID_PUBLIC_RECORD_TRANSITION_AMPLITUDE_DERIVATION_V001.md: `a_h(0) = 1`, "not introduced to avoid the zero of the rejected survival amplitude"). Its Hessian is a conditioned (weak-value-type) covariance: positivity is contingent, not structural.

**Exhaustive object** (`Z_K = ω_in(W_-^† W_+)`, COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_RESULT_V001.md): the all-outcome relative-history functional — a decoherence-functional/Bures object measuring how distinguishable two connection histories are given the entire source-record state, recorded or not. `Z_K[A,A] = 1` exactly.

**Degeneracy question — answered: the exhaustive Hessian is NOT degenerate.** The diagonal is a flat direction by unitarity (log ≡ 0 there), so the same-branch diagonal second derivative vanishes identically; all second-order content sits in the mixed (+/−) branch derivative — exactly the Duhamel memo's pinned normal form ("Z_K[A,A] = 1 anchors the log branch; all second-order content is in the mixed branch derivative"). The mixed Hessian is the positive-semidefinite Duhamel covariance `g_D = <η|η> − |<ψ|η>|²`, with sealed finite witnesses: the one-cell `[−log|Z(δ,0)|]''_(δ=0) = 1/4` (CTP result) and the three-way H_CTP/g_FS/g_Duhamel agreement at ~0.0648 (COMPLETE_QSPEC_FINITE_HOLONOMY_RESPONSE_RESULT_V001.md) — note that agreement is entirely **internal to the exhaustive chain** ("no record outcome postselected"); it is not a completed-exhaustive bridge.

**Small-modulus question — answered: yes, the asymmetry is real and load-bearing.** The exhaustive object's zero-free obligation is a perturbation-from-unity statement (|Z| = 1 on the diagonal, contractive off it) — which is why COMPLETE_QSPEC_PERIODIC_UNIFORM_ZERO_FREE_THEOREM_SPEC/RESULT_V001 could prove it via `|Z_N(z) − 1| < 1`. The completed object has no unitality anchor; its witnessed small modulus means T7(ii) is a genuinely hard obligation, and nothing sealed prevents |Z_comp| → 0 along the exhaustion (gamma memo 3(c); the V002 division-safety fence is its finite shadow). **This is why A4(3)-as-relocation would violate A4's no-weakening clause: pointing T7(ii) at Z_K swaps the hard completed-chain zero-free gate (whose failure is BLOCKED) for the easy exhaustive one.** Caution flag: the periodic zero-free RESULT's prose says "every finite completed-record amplitude is nonzero," while its executor (`scripts/derive_complete_qspec_periodic_uniform_zero_free_v001.py`) builds the all-outcome Kraus composite against the disclosed density with no pointer conditioning — the L2 ambiguity has already leaked into sealed prose. That result discharges nothing for battery T7(ii) — its own status flags agree (`all_stage8_regulators_zero_free_proved = false`, and the battery-level flag stays false).

**Do the two Hessians physically agree? Generically no.** Exact operator identity (O3 completeness): `R_all = R_comp + R_rest`; after state evaluation `Z_all = z_c + z_r`. Log-Hessians do not add across a sum: the exhaustive Hessian mixes the completed and non-completed sector responses with weights set by the baseline completed probability q = z_c(0,0). On the actual parent q is tiny (the small witnessed modulus), so the exhaustive Hessian is numerically the response of the **unrecorded** sectors — a different physical quantity from the record-conditioned Hessian. The gamma memo's toy shows both carry O(1) envelope-shape structure, in different functional forms. The sealed Route-1 comparator, where BOTH components were computed (STAGE8_ROUTE1_SPECIAL_CASE_CONSISTENCY_BINDING_V001.md: completed-component error 4.45e-16, exhaustive-kernel error 3.34e-16), is the degenerate q = 1 endpoint: there `Z_comp(δ,0) = A_+(0)^*A_+(δ)` with A_-(0) = 0, so `Z_all(δ,0) = Z_comp(δ,0)` identically and both Hessians equal 1/4. The sealed record thus establishes the additive relation and demonstrates coincidence only in the degenerate case — no sealed identity equates the Hessians in general, and the structure says they differ on the actual parent.

**Which object does the sealed intent pick? Completed — three times over.** (1) V011 1217–1233 already faced precisely this shape of choice — overlap-type object vs record-conditioned object — and sealed the record-conditioned one ("The root survival amplitude is not the response amplitude"), with the selection principle "the final support is fixed by the completed public alternative" (BID_PUBLIC_RECORD_TRANSITION_AMPLITUDE_DERIVATION_V001.md). The exhaustive split-history functional is an overlap-type (generalized survival) object; reading A4(3) as exhaustive would silently reverse that sealed adjudication at K-level. (2) The battery makes the rejection a mandatory negative control (NC5) and names its one output "the extensive dimensionless primitive **record** susceptibility." (3) The program thesis — records are the physics — means kappa_record is the stiffness of what gets recorded; the exhaustive Hessian counts Fisher information carried by sectors that never become records.

## 3. Options with consequences

**(a) Freeze on completed (battery as written).**
- No battery change. One **append-only erratum** to the amendment restating A4(3) as the type-location claim: analytic obligations attach to state-evaluated scalars; for the kappa_record chain that scalar is the state-evaluated completed-sector object (Phase-A A4's `K_pointer` lineage), with Z_K retained as the exhaustive companion. Re-labeling, not re-derivation, of the amendment.
- The Duhamel memo's piece-1 identity must be **re-derived on the completed chain** — and this is cheap: O5 of STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_SPEC_V001.md already derives and seals the operator Duhamel tangent "for `Q` equal to either `I_R` or `Q_comp`" (flag `finite_primitive_operator_Duhamel_tangent_derived = true`). What is genuinely new: one finite-dimensional normal-form lemma replacing the variance `g_D = <η|η> − |<ψ|η>|²` by the conditioned covariance (ratio form with the pointer-projected state and tangent), plus the named baseline hypothesis Z_comp(0) ≠ 0 — which is just T7(ii) where it always lived. C1/C2 and the Route-A Vitali–Cauchy interchange schema are object-agnostic and carry over verbatim. The T9/FS check stays as written (V011 1300–1302 anchors FS on "each normalized local output ray" — the completed output ray).
- Honest cost: T7(ii) zero-free stays the hard obligation on the hard object. The sealed periodic zero-free theorem does not discharge it (it never claimed to). Option (a) adds no new obligation; it refuses an illusory discharge.

**(b) Freeze on exhaustive (A4(3) literal).**
- Requires an append-only battery amendment re-pointing T7(i)/(ii)/Γ and T9, i.e., battery v003 plus hostile re-review — colliding with the in-flight v004 critical path (disposition item 5). Worse: V011's kappa_record semantics (1235–1315) are pinned by hash in the battery and V011 is immutable; the battery cannot amend its own authority. (b) is not a re-labeling — it is a semantics change against the sealed principle text, and it weakens T7(ii) (see Section 2), which A4 itself forbids. It would also strand the executed ER-fork lanes and the beta kill-test (all built on the completed/pointer proxy chain) and undercut NC5. New derivation needed: none for H = g (the memo's piece 1 stands as typed) — but everything else re-anchors.

**(c) Freeze BOTH with a derived relation.**
- The only sealed/derivable cross-object identity is the additive decomposition `R_all = R_comp + R_rest` (O3, exact; state-evaluated: `Z_all = z_c + z_r`) plus the resulting Hessian-mixing formula weighted by the baseline completed probability q. This relation does **not** equate the two Hessians — it quantifies exactly how they differ (they differ by unrecorded-sector response terms, not by a boundary/normalization term). So (c) cannot substitute for the choice; it collapses back into (a) vs (b) for the actual output. It IS worth sealing as a lemma **under** the (a) freeze: it is the substitution-fence-compatible way to state both objects' roles and makes the fence mechanically auditable.

## 4. Recommendation and dependency map

**Freeze option (a): the intensive Hessian sourcing kappa_record lives on the completed chain — T7(i)'s Z_h/Gamma lineage, state-evaluated per Phase-A — with Z_K retained as the exhaustive companion object (Route-1 falsifier, gamma-memo control 2's sensitivity control, and the COMPLETE_QSPEC continuum-provenance program, all of which stay untouched).**

- **Is it typing or Brian-level?** The disposition's own test: the two objects give physically different kappa_record values (they do, generically — Section 2), *but* the sealed intent is not ambiguous: V011's semantics, the battery's T7(i)/NC5/T15, and the amplitude-selection derivation all pick completed, and the amendment's literal alternative reading self-contradicts (no-weakening clause). No physics is being chosen now; it was chosen when V011 sealed. Therefore: **construction-lane-sealable typing clarification**, with one Brian sign-off required only because the erratum annotates a sealed, forward-sealed amendment. If Brian instead wishes to *re-open* the V011 semantics — making kappa_record the all-outcome Fisher stiffness — that is an architecture decision of the same class as the refused (5.1), and nothing in the current record supports it.
- **Fastest unblock of T7(iii)/(iv):** (a) requires zero battery changes and zero collision with the v004 critical path. Concretely: (1) seal the L2 freeze note + A4(3) erratum (one small artifact); (2) the Duhamel derivation spec (already waiting on the majorant memo per its own disposition) is authored with the completed-chain conditioned-covariance normal form, consuming the sealed O5 Q_comp tangent, C1/C2 as named lemmas, and the interchange schema unchanged; (3) T7(iii) cluster work and L1 (disposition 5.3) proceed on the completed chain — L1's per-cell-to-K question is unaffected by the L2 choice; (4) add a one-line typing note to the periodic zero-free result's lineage recording that its object is the all-outcome closure and its "completed-record amplitude" phrasing is descriptive, so the T15-style grep discipline never mistakes it for a T7(ii) discharge.
- **No sealed obligation weakens.** Every exhaustive-chain result (CTP closure, finite-holonomy three-way agreement, periodic zero-free/linked-density) stands exactly as sealed — each already sets the battery-level flags false in its own status block. The substitution fences remain active in both directions; the (c)-lemma makes them auditable.
- **What must not happen:** the exhaustive chain's easy zero-free neighborhood must never be cited against T7(ii); the completed chain's Hessian must never be checked against the exhaustive three-way agreement as if it were the same number; and the Duhamel spec must not inherit the memo's piece-1 wording verbatim, since that wording is anchored on unitality the completed object does not have.

This contradiction does not survive into T7(iii)/(iv) under option (a) at the cost of one erratum, one normal-form lemma, and one typing note.
