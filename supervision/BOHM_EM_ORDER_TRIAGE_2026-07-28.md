# THE BACK HALF: WHICH OF THE NINE EM-ORDER STEPS IS STARTABLE (Bohm, 2026-07-28)

Nine steps triaged, each then attacked in BOTH directions — once to show the object IS defined
somewhere the triage missed, once to break a claimed seal. Every verdict survived.

---

# THE BACK HALF: WHICH OF THE NINE EM-ORDER STEPS IS STARTABLE

Corpus: `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003`. Read-only throughout. No value of alpha, kappa_record, kappa_Thomson, or any function of them was computed. The three senses of "alpha" (Schatten-2 exponent, Dirac matrices, fine-structure constant) were kept distinct.

---

## 1. THE TALLY

| Verdict | Count | Steps |
|---|---|---|
| **MISSING_SPEC** | **9** | RD-26, RD-27, RD-28, RD-29, RD-30, RD-31, RD-32, RD-33, RD-34 |
| HARD_MATH | 0 | — |
| MALFORMED | 0 | — (RD-32 ruled on in §4) |
| PARTIAL | 0 | — |

**Nine of nine.** Not one of the nine EM-order steps is lane work today. Every one of them is blocked on a specification act that only you can perform. The back half of the program is not blocked on mathematics.

Each step was triaged and then attacked in both directions — once to show the object *is* defined somewhere the triage did not look, once to break a claimed seal. **Every verdict survived.** The strongest attack, on RD-29, produced a real correction (a sealed regulator class the triage missed) and the verdict still stood, because the correction relocated the gap rather than closing it.

Two things worth having up front.

**The prior was too optimistic in the right direction.** You expected two of nine to be missing specs. It is nine.

**But five of the nine specification acts are smaller than a first pass suggests.** In RD-26, RD-28, RD-29, RD-31 and RD-32 the adversarial check found sealed material the triage had missed, and in every case it *narrowed* what you must supply. A sealed seven-condition regulator class already exists (RD-29). A sealed Lorentz-covariance axiom already exists (RD-31). The primitive response closure is already selected and proved unique (RD-28). The pole-branch carrier is already named in a sealed current-authority postulate (RD-32). The descendant kernel forms are already enumerated (RD-34). You are mostly routing and completing, not inventing.

One governance finding, because it will bite whoever drafts next: **a sealed claim is currently being overridden by an unsealed correction.** `STAGE8_T7_PARENT_STATE_REGULATOR_RESTRICTION_RESULT_V001.md` is sealed and carries verdict `PARENT_STATE_REGULATOR_RESTRICTION_DERIVED`; `STAGE8_T7_CONTINUUM_GALERKIN_PROVENANCE_CORRECTION_V001.md:13-14` withdraws that label ("Its stronger label, `PARENT_STATE_REGULATOR_RESTRICTION_DERIVED`, is withdrawn.") and carries no seal in any of the three forms — yet a script pins it by sha256 and treats it as authority in the machine adjudication. Reading seals alone gets this backwards.

---

## 2. THE MISSING SPECIFICATIONS — WHAT YOU MUST SUPPLY

### RD-26 — Dynamical U(1) action

**Missing object:** a weight on connection field histories. The corpus names it exactly once, and negatively: `PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:26-27` — "The primitive record result fixes the unit winding and action-character normalization. It does not determine the field-history weight."

**Correction adopted from the check.** The triage's blocking item — "nothing in the corpus decides whether `a` is promoted to a fluctuating field" — is **false**. A sealed Level-1 postulate rules the direction. `PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md:67-69` (verified verbatim by me):

```
primitive charged access = transport;
closed-loop curvature and kinetic response = composite output.
```

and at `:49-50` it excludes as primitive mutations both "`c_F integral F^2`  a bare Maxwell action" and "`ell_* bar(psi) sigma.F psi`  a primitive Pauli insertion." That second exclusion also discharges the parent-action fork inside the adopted branch (`:72-75`), so the triage's fifth demand comes off your list.

**What a principal act must fix, in order:**

1. **Whether the external connection `A` is ever promoted to an integration variable.** The postulate defers exactly this at `:101`, remaining obligation 5: "the physical connection as a public dynamical field;". In every sealed executed artifact `A` is external, never integrated. If the answer is *no*, RD-26 has no independent content — it collapses into RD-29 — and freeze steps 1 and 2 must be rewritten, because both presuppose an integration variable the adopted branch does not have.
2. **If promoted: the measure on connection histories.** It does not exist. Four zero-hit searches ("integrate over the connection", "sum over connection", "measure on connection", "gauge-field measure") plus zero for Haar, Villain, Wilson action, lattice gauge, BRST, integration variable.
3. **The action/measure split convention.** `ONE_CELL_FIDELITY_ACTION_SELECTOR_GATE_V001.md:66-72` (verified): under `dmu' = J dmu, S' = S + hbar log(J)` the full Euclidean weight is unchanged — "A one-cell action cannot be declared absolutely normalized in isolation from the measure." So the deliverable is typed as a *weight*, not an action, unless you fix a reference measure that pins `J`.

**Size:** item 1 is one sentence with a large blast radius. Items 2-3 are real construction, contingent on item 1.

---

### RD-27 — Gauge fixing, ghosts, gauge edge modes

**Missing object:** all three clauses. There is no gauge group and no gauge orbit named anywhere in the corpus (both terms: 0 hits). Ghosts are conditional on an unstated condition — `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:1598`: "the gauge, constraint, **ghost if required**, and public edge sectors;". The requiring condition is stated nowhere.

**What must be fixed:**

1. **The prerequisite is RD-26's, and the check sharpened it.** The ruling is not merely "is `a` promoted" but: *does the induced classical route at `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V009.md:1206-1215` constitute the RD-26 action?* That passage does construct an EM Lagrangian — `L_EM,cell = D dot E - H_EM,cell = (kappa_cell/2)(|E|^2-|B|^2)` — via a declared canonical symplectic/Legendre map. It is gated by audit rows A25/A27/A28, all `PENDING` (`BID_CONSOLIDATED_HOSTILE_AUDIT_MATRIX_V005.md:75,77,78`), and BID V009:1322-1323 disclaims a determinant-derived coefficient. **If you rule that the induced route IS the action, RD-27 as written may be malformed rather than unspecified** — an induced response of a background probe has no orbits to slice.
2. **The gauge group and its action.** What group, acting on what — vertices of the cellular complex, a continuum region, the CAR source carrier?
3. **Whether a ghost sector is required at all, and under what condition.** For an abelian group on a finite cellular carrier the Faddeev-Popov determinant is field-independent and ghosts decouple; in the continuum limit under the compact unit-character normalization it may not. This decides whether clause 2 of the step exists.
4. **The TYPE of a "gauge edge mode."** This is the sharpest item in the step. `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:72` already asserts "Durable records are generated by physical boundary degrees of freedom." **Are gauge edge modes and durable-record boundary degrees of freedom the same objects?** That identification is nowhere made and nowhere refused — "boundary degrees of freedom" occurs at exactly three non-duplicate lines corpus-wide, all assertions, none a construction. In a boundary-incidence theory this is structural, not detail, and your own plan flags it: `STAGE8_MASTER_PLAN_FINDINGS_V001.md:74-76` — "Edge modes in particular are not a detail in a cellular/boundary-incidence setting — they are where boundary degrees of freedom live, and this program is a boundary-incidence theory."

**Size:** item 4 is answerable today from already-sealed material (the cellular complex `K_L`, its closure faces, and the boundary record carrier all exist independently of the gauge field's dynamical status). Items 2-3 wait on RD-26's ruling.

---

### RD-28 — Normalized interacting CTP amplitude

**What is already defined, so you do not re-spend on it:** "normalized" is settled — five sealed spellings, all meaning the `a=0` baseline (`STAGE8_X_TO_KAPPA_RECORD_EXTRACTION_DETERMINATION_V001.md:70-73`; *note the triage cited :48-51, which is wrong — adopt the check's line numbers*). The downstream consumer is fully specified: `Gamma_Q[A]=-i Log(Z_Q[A]/Z_Q[0])` and the transverse quadratic form (V011:1613-1636, verified). The sealed abstract predecessor is typed: `Z_K[A_+,A_-] = omega_in(W_K[A_-]^dagger W_K[A_+])`.

**Missing object:** the word **"interacting."** Two mutually exclusive readings are each supported by sealed text and neither is adjudicated:
- **Reading A** — `A` stays external; "interacting" means the full source dynamics beyond the free quasifree contour. Object is `Z[A_+,A_-]`; steps 1-2 supply only a gauge quotient.
- **Reading B** — `A` becomes dynamical and is integrated. Object is `Z[J_+,J_-]`; steps 1-2 supply the action and measure that get integrated. Supported by `STAGE7_QSPEC_REVIEW_CANDIDATE_V002.md:199-200`: "A normalized interacting CTP amplitude **and its** gauge/ghost/edge functional measure remain open."

These differ in the object's **arity** and in whether a measure is part of the object at all.

**Correction adopted from the check — one demand comes off, one goes on.**

*Off:* do **not** ask for a response-closure selection at the primitive layer. It is done. `STAGE8_T7_RESPONSE_CLOSURE_SELECTION_DERIVATION_RESULT_V001.md:43-57` (verified) proves uniqueness — "Because `End_C(L_r)=span_C{I}`, every complex-linear functional has `C(z I)=c z`", unit normalization gives `c=1`, "is the unique normalized complex-linear primitive closure" — with `response_closure_selection_derived = true` at `:70`.

*On (new, unaddressed anywhere):* the closure the corpus selected is the **complex-linear** one, and the form it rejected as curvature-0 is the "inclusive unitary sandwich" (`:36`) — but the sealed CTP object **is** a sandwich, `omega_in(W_K[A_-]^dagger W_K[A_+])`, with `Z_K[A,A]=1` on the diagonal. **Whether the selected primitive closure is even compatible with a CTP carrier is now an open question the corpus does not address.**

**Also must be fixed:** the carrier (`Q_n K_Sigma`, `K_Sigma`, or new); the admissible continuum history class; and the **arity reconciliation** between the two-history producer and the one-history consumer `Z_Q[A]/Z_Q[0]`. That reduction is not free — it lands directly on `kappa_Q(q^2)`.

**Name-collision warning.** `STAGE8_T7_CONNECTED_ANALYTIC_CLOSURE_SCOPE_ERRATUM_V001.md:33-38` defines a **one-history** object `Z_K(A)=<p_K|exp(-i tau_R B_K(A))|r_K>/<p_K|exp(-i tau_R B_K(0))|r_K>` wearing the same symbol `Z_K` as the sealed two-history CTP closure. Different arity, different layer, different status.

---

### RD-29 — Parent-derived regulator and finite renormalization

Treated in full at **§6**. In brief: the regulator's *type* is supplied by a sealed class the triage missed; the *finite renormalization condition* is stated nowhere in the corpus, in any form, and is the load-bearing half.

---

### RD-30 — Induced-polarization transversality and photon-mass exclusion

**Missing object:** an unrestricted polarization tensor. `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:1621-1628` (verified) writes the response with the transverse projector **already factored out**:

```
Gamma_Q^(2)[A] = (1/2) integral d^4q/(2 pi)^4
   A_mu(-q) (q^2 eta^(mu nu)-q^mu q^nu) kappa_Q(q^2) A_nu(q).
```

Transversality is an **ansatz on the definition**, not a property of a defined object. The hash-pinned parent authority does the same thing — "On the transverse public connection sector, the quadratic expansion is" — with `microscopic_transverse_Hessian_computed=false`.

**Correction adopted from the check.** The triage's "NO unrestricted polarization tensor is written anywhere" is too strong. `COMPLETE_QSPEC_TEMPORAL_PLAQUETTE_RESPONSE_DIAGNOSTIC_SPEC_V001.md:109-110, 135-142` (verified) defines `H_v(N,a) = [-log|Z_v(+a)|-log|Z_v(-a)|]/a^2` and the response matrix `M_ii = H_(f_i)`, `M_ij = [H_(f_i+f_j)-H_(f_i)-H_(f_j)]/2` — an unrestricted second variation of the effective action in a connection amplitude, no projector imposed, sealed. But it carries **no (mu,nu) indices and no q**, so `q_mu Pi^(mu nu) = 0` is still not a typed proposition anywhere.

**What must be fixed:**

1. **Define `Pi^(mu nu)(q)` with no projector factored out**, say the second variation of `Gamma_Q` at `A=0`, with its type, variables and domain. Only then does V011's form become a *theorem* rather than a *definition*.
2. **Rule where it lives** — a cell-level Ward identity on the cellular complex (then define the cell-level current and what `q_mu Pi^(mu nu)=0` means with no momentum space), or only after a continuum limit, or both with a compatibility lemma. This fork is now sharper than the triage stated: the corpus has a working sealed recipe for a cell-level unrestricted Hessian, so you are ruling on whether to **extend an existing construction** to the (mu,nu) and magnetic sectors, not to invent one. The magnetic half is named and unbuilt (`COMPLETE_QSPEC_TEMPORAL_PLAQUETTE_RESPONSE_RESULT_V001.md:147-149`).
3. **State photon-mass exclusion as a proposition.** Pick one: no pole at `q^2=0` in `kappa_Q`; no `m^2 eta^(mu nu)` surviving regulator removal; or no mass gap in the interacting gauge sector. These are inequivalent and the corpus supplies none of them — only the four-word phrase, ten times, always inside a list. Worse, masslessness currently enters as an **assumed frame** in an unsealed file: `BID_FREE_QUASIFREE_CTP_PROPAGATOR_DERIVATION_V001.md:103` — "In an interacting massless gauge theory the charged support may be an infraparticle threshold rather than an isolated pole."
4. **Rule what RD-30 is for.** Two independently sealed files say a discharged transversality delivers the *form* and nothing about the *coefficient*: `ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V003.md:23-27` ("Gauge invariance, Lorentz covariance, compactness, charge quantization, Ward identities, anomalies, and the charge-character lattice do not forbid the finite second term") and `COMPLETE_QSPEC_ABSOLUTE_SCALE_AND_CONTINUUM_PREREQUISITE_AUDIT_V001.md:113-116` ("Gauge/Ward identities enforce transversality but do not make its coefficient vanish"). The `c_R` mutation `K -> K + c_R` is *itself transverse*, so RD-30 cannot exclude it. **Any plan that hopes RD-30 will constrain the coefficient is contradicted by two sealed files.**

---

### RD-31 — Lorentz- and packing-independent renormalized response

**Missing object:** the renormalized response is never typed. Not as `Pi^(mu nu)(k)`, not as a Hessian, not as an intensive quadratic coefficient, not as a scalar. `grep -rni "renormalized response"` returns five hits, all restatements of the step title.

**Correction adopted from the check.** The triage said the Lorentz half "is a term with no referent anywhere." **False.** `R3_3_GLOBAL_INTRINSIC_MEASURE_CLASSIFICATION_SPEC_V001.md:62-78`, axiom A3, is a sealed typed Lorentz-invariance criterion with the group named and the equation written: "For every proper-orthochronous Poincare transformation `g`, `mu_(gD)(gA) = mu_D(A)`. No preferred origin, inertial frame, timelike covector, or external density profile is supplied." It is already upstream of this step in the dependency chain. **The two halves are symmetric:** each has a sealed template attached to a *different* object (packing: `C_ref` at V011:1397-1412 plus the T13 protocol; Lorentz: A3), and neither is attached to *this* step's object.

**What must be fixed:**

1. **Name the object** and state its type and arity.
2. **Define packing-independence for it** — over what family, in what norm, uniformly in what. The template is V011:1407-1409: "The response map must commute with pullback to a common refinement, and the intensive quadratic coefficient must be invariant under each elementary refinement up to a boundary term whose ratio to four-volume tends to zero."
3. **Route the Lorentz half** — rule whether A3's covariance condition is what step 6 discharges, and on which object. This is a routing decision, not an invention.
4. **Resolve or route around C_ref vs D3.** `STAGE8_T7_CREF_VS_D3_FACTUAL_DETERMINATION_V001.md:85-88`: "D3's CLASS IS STRICTLY LARGER THAN C_ref's... generically produces SLIVERS, which are not shape-regular... NO SEALED ARTIFACT ANYWHERE RECONCILES THE DIFFERENCE," with `D3_object_spec_status = HELD_PENDING_PRINCIPAL`. **A ruling taken for R-L2b's benefit silently decides half of RD-31.**
5. **New, from the check, and it is the hardest item.** `ONE_CELL_FIDELITY_ACTION_SELECTOR_GATE_V001.md:81-95` (verified) contains the **only typed quadratic continuum response in the corpus**:

   ```
   M^(mu nu rho sigma) = integral dmu rho sum_p m_p B_p^(mu nu) B_p^(rho sigma).
   ```

   followed by: "Cell scale cancels from the leading four-dimensional `F^2` term for a self-similar ensemble, but **shape, filling, incidence, multiplicity, and orientation normalization do not.**" Step 6 demands packing-*independence*; the one response tensor the corpus has actually typed says five kinds of packing data survive. Nothing reconciles this. It also introduces a **fifth packing vocabulary** in mutual non-correspondence with the other four (the fence's five counting variables; the F'-5 erratum's index-vs-geometric-datum split at `STAGE8_F5_PROVENANCE_ERRATUM_AND_FORCING_GROUND_CORRECTION_V001.md:42`; V011's shape-regularity; and this). **You must specify on each axis separately — combinatorial label, counting datum, geometric datum — and you may not borrow parental authority for the geometric axis, because per the erratum there is none.**

**Note also:** the step's title is the only authored sentence naming it. The two ledger copies are machine-generated from one Python string literal (`scripts/build_stage6_parent_action_ledger_v002.py:221`).

---

### RD-32 — Interacting charged pole or infraparticle threshold

**Correction adopted from the check.** The triage's "NO CARRIER OBJECT IS NAMED" is **false**. `PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md:121-122` (verified) names it with a type: "This is a free mass-shell statement. **The physical interacting pole and residue still require the complete 1PI two-point function.**" That file is sealed and is current authority. `SOURCE_RECORD_MASS_TRANSFER_GATE_V001.md:32-33` names it again, and at `:87` (verified) supplies a **partial pole-branch predicate**: reopen condition 4, "yields an isolated positive pole with the correct residue."

**Also correct the triage's seal finding:** `LOCKED_ALPHA_PLAN_DEPENDENCY_REPAIR_V001.md` *is* sealed, by manifest membership at `review_packets/STAGE7_QSPEC_CANDIDATE_V001/STAGE7_PACKET_MANIFEST_V001.sha256:28` — the same 113-line manifest whose line 3 the triage used to certify a different file. So the placement conflict below is **between two sealed artifacts**, not between a sealed one and a draft.

**What is still missing:**

1. **Promote the named carrier to a gauge-invariant object.** The 1PI two-point function of `psi` is gauge-*variant*; pole-vs-infraparticle is a gauge-invariant-charged-sector question. `BID_LORENTZIAN_SOURCE_SCHUR_POLE_DERIVATION_V001.md:151` lists "the gauge-invariant Gauss-law/edge dressing of a charged source" as not established. Zero corpus hits for Dollard, Kulish, Bloch-Nordsieck, coherent dressing, Wilson-line-dressed field, superselection sector — **the freeze's clause "the matching rule must be reformulated" has no candidate reformulation anywhere in the corpus.**
2. **Define the infraparticle branch as a predicate on a spectral measure.** Zero content. The pole branch has a partial predicate; this one has none.
3. **State the decision procedure and its null outcome** — what computation returns which branch, and what counts as BLOCKED rather than either branch.
4. **Adjudicate where the step sits.** Three incompatible placements: item 7 of the sealed EM order; an open Q_spec slot (i.e. *inside* the specification); and item 8 of the sealed dependency repair, *after* the Q_spec freeze, because `LOCKED_ALPHA_PLAN_DEPENDENCY_REPAIR_V001.md:9-12` finds the naive order **cyclic**.

---

### RD-33 — Thomson matching: kappa_record → kappa_Thomson

**What is defined:** the target only. `kappa_Thomson=lim_(q^2->0) kappa_Q(q^2)` (V011:1635, verified). Do not let this stand in for the step.

**Missing object:** the matching *procedure*. `STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V001.md:97` (verified) — "| Thomson matching rule | Open |". Its canonical flag `Thomson_matching_map_derived = false` appears in a sealed result file and its producing script. Every corpus statement about it is an achievement clause, a prohibition, or an undefined pass token ("threshold matching", V011:1631, defined nowhere).

**What must be fixed:**

1. **The rule in the pole branch** — what quantity is matched to what, at what kinematic point, under what normalization condition. `PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:119` says "Only then may the response be run and matched to the Thomson limit," and that file carries a running `K_R(mu)`, but **`K_R(mu)` and `kappa_Q(q^2)` never co-occur in any file.** Either matching is "evaluate `kappa_Q` at `q^2=0` and stop" — in which case step 8 is not a matching and collapses into step 6 — or there is a scheme with a matching scale, and it is unwritten.
2. **The rule in the infraparticle branch.** `EM_DEPENDENCY_ORDER_FREEZE_V001.md:28-30` (verified) pre-registers that it does not exist.
3. **What role `kappa_record` plays, if any.** V011:1644-1651 (verified) says it "may constrain or appear inside this construction" and that the equality "is not assumed. It must follow from the complete amplitude or fail." Those two sentences do not determine whether `kappa_record` is an input, an independent prediction to compare against, or irrelevant. **The road-map name silently commits to "input"; the sealed text commits to none of the three.**
4. **Define the pass token "threshold matching."**
5. **Resolve the conflict between two sealed orders.** The repair requires matching data frozen *inside* Q_spec before the threshold is derived (`:32-33`); the freeze reserves the right to reformulate the rule *after* seeing the threshold (`:29-30`). Jointly satisfiable only if Q_spec freezes prescriptions for **both** branches in advance. If that is not said, the reformulation clause is a target-selected normalization channel of exactly the kind the guardrails at freeze:36-43 forbid.
6. **New, from the check, and it is bigger than the step.** "**The matching scale**" is load-bearing in the program's *terminal anti-self-deception test* — `ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V001.md:19-22`: "The construction has not computed alpha if an arbitrary finite `delta S = -(hbar c/4) integral sqrt(-g) F_mu_nu F^mu_nu d^4x` remains admissible **at the matching scale**... This is the terminal anti-self-deception test." The phrase occurs at four corpus lines and **is defined at none of them.** Until the matching spec exists, the test that certifies the whole program against a target-selected counterterm is quantified over an undefined scale. **This missing specification does not merely block step 8; it blocks your own falsifier.**

---

### RD-34 — Causal Incidence Support descendant test

**This one is different: the object is named, typed, and form-constrained — and set to zero.** `R3_4_CAUSAL_SHARED_SOURCE_MOLLER_DURABILITY_SPEC_V001.md:51,63` puts `D_N(t)` in the Hamiltonian and says "`D_N(t)` denotes effective descendants generated by the complete parent," then `:64-65` zeroes it: "The primitive-sector calculation sets `D_N=0`; the complete physical claim must remain false until all generated descendants are derived and tested." `D_N` occurs at exactly four word-bounded lines in the corpus; three are these, one is an unrelated name collision.

**Correction adopted from the check:** the object is form-constrained, not a bare placeholder. `BID_GLOBAL_BOUNDARY_DESCENT_QUASI_FREE_COMPLETENESS_V001.md:16-20` enumerates the admissible shapes — "No independent primitive higher-CAR, record-only, contact, or overlap kernel is allowed. Terms of those forms may arise only as effective descendants after shared gauge, gravitational, record, or environmental fields are eliminated from the one complete action." **So the act is: supply the generating map and coefficient rule for four already-named kernel forms** — not invent the object.

**What must be fixed:**

1. **The generating rule.** At what order, in what expansion parameter, with what closure, with what stopping rule. Zero hits for Schrieffer-Wolff, adiabatic elimination, influence-functional elimination of a gauge field, order-by-order, expansion parameter — the corpus has open-system machinery but none of it eliminates a gauge, gravitational or environmental field, and none is connected to `D_N`.
2. **The elimination list, and which members are in scope.** Two of the five named sectors are not constructible today: "full gravity action and gravitational quantum measure" is an open Q_spec slot, and "environmental" has no Hilbert space, coupling, or tracing map anywhere. You must either commission them or rule that RD-34 is dischargeable on the gauge sector alone with the other arms explicitly deferred — **and the deferral must be recorded**, because `CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md:88` forbids the alternative: "no effective descendant may be omitted because it spoils durability."
3. **The pass criterion.** "Destroys the claimed public outgoing sector" is not one. The target is the **dressed** monomorphism, whose range is explicitly not contained in the bare record-only algebra — so a naive "`[D_N, Z_j] = 0` on the record factor" test is the *wrong* test and would pass a descendant that rewrites the dressed image.
4. **Which durability mechanism `D_N` may use.** `BID_FINITE_RECORD_DURABILITY_NO_GO_V001.md:59-70` (verified) lists four inequivalent mechanisms — including "an infinite causal/environmental limit with asymptotic outgoing sectors," which is this step's own subject matter — and rules: "**These mechanisms are physically inequivalent and may not be selected after a response is evaluated.**" See §5; this collides with the step's frozen position.

**Trap to flag:** `STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V001.md:78` (verified) reads "| Generated descendants | Derived with no independent coefficient |". That row is about **square-generated** descendants of the first-order superconnection kernel — a different sense of the word, already inside the propagator by construction. It reads as a discharge of RD-34 and is not one.

---

## 3. THE GENUINE MATHEMATICS — WHAT A LANE COULD START TODAY

**This section is empty of the nine steps. Zero of nine has a proposition a lane could start today.** In every case the object the proposition would be about lacks a type, a domain, a measure, or a predicate.

What follows are **adjacent** well-posed propositions the corpus itself names. They are prerequisites of the nine steps, not the steps.

1. **The three continuum prerequisites.** `COMPLETE_QSPEC_ABSOLUTE_SCALE_AND_CONTINUUM_PREREQUISITE_AUDIT_V001.md:124-131`, items 1-3: a common-polarization implementability theorem through the sharp-cell limit (a Shale-Stinespring question); one common implementer phase or determinant-line trivialization for the signed histories; the actual UV divergence and its completed-history combination. These consume only the already-sealed parent and history weights. **Item 4 of that same list is RD-29 and must not be pulled in with them.**

2. **Construct a genuine Galerkin family.** `STAGE8_T7_CONTINUUM_GALERKIN_PROVENANCE_CORRECTION_V001.md:43-51` (verified) states exactly what a member must satisfy: finite rank and nested; `Q_n -> I` strongly; `Q_n h_0 Q_n -> h_0` on a common core; `Q_n M_c(t) Q_n -> M_c(t)` strongly; finite propagators converging strongly, uniformly on compact times — with `:53` "Exact commutation with `h_0` is neither required nor possible." The sealed class currently has **no member**: `:24-28` proves the specified family "does not exist for the free massless Dirac multiplier," because a nonzero finite-dimensional reducing subspace would contain an `L2` eigenvector of `h_0`, whose spectrum is purely continuous. That is an ordinary functional-analysis construction with a stated target.

3. **The temporal locality gate.** `COMPLETE_QSPEC_TEMPORAL_PLAQUETTE_RESPONSE_RESULT_V001.md:136-145` (verified) names it: derive the connected translation-invariant many-cell limit and test whether the response kernel becomes local at long wavelength, the normalized low-frequency diagonals converge, cross-frequency terms vanish, and the intensive result is independent of cellulation and packing. Adjacent to RD-30/RD-31; identical to neither.

4. **Parent-action uniqueness outside the adopted branch.** `COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md:81-88` pre-registers three sufficient closure shapes, with `:90` "Merely declaring one option 'minimal' is not a derivation." Note this is now **partly discharged inside** the transport-only branch, so the residue is: prove it outside that branch, or accept the branch as an adoption.

---

## 4. MALFORMED — RULING

### RD-32 is NOT MALFORMED. The objection fails on four independent sealed grounds.

The candidate objection was: every Thomson carrier presupposes a massive pole, while the sealed source is massless by construction.

**The quote is verbatim true and the artifact is sealed.** `PRIMITIVE_VECTORLIKE_CHARGED_SOURCE_BRANCH_V003.md:74` (verified): `primitive_bare_mass = 0`.

**The inference fails:**

1. **That is a *bare* mass, and the corpus derives a nonzero shell from record incidence.** `BID_LORENTZIAN_SOURCE_SCHUR_POLE_DERIVATION_V001.md:88-96` (verified): `p^2=m_*^2, m_*=sqrt(2) mu`, and "Using only the independently derived record interval `tau_R=pi/sqrt(2)`, `m_*=pi/T_R`." Zero bare mass and a massive shell are consistent here; the mass is **generated**. A second independent generation route exists at `PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md:115-118`.
2. **The sealed gate row says so in terms.** `BID_SOURCE_PARENT_CLOSURE_GATE_V003.md:34`: "the exact edge compression has a proper-Lorentz timelike shell `p^2=2 mu^2`, **no edge massless pole**, and strictly positive free residue."
3. **The corpus pre-registers the generation as an obligation, not an exclusion.** `record_generated_mass_derived = false` (`V003:87`, verified); "A public mass and threshold must be generated by the later record-closure solution" (`V002:70`); and the *failure* rule is "the Thomson limit is asserted **without** a derived massive charged sector" (`ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V003.md:126`). A massive sector is something the corpus demands be derived, not something it forbids.
4. **"Every Thomson carrier presupposes a massive pole" is not this corpus's Thomson definition.** Its definition is a zero-momentum limit of the transverse vacuum-polarization coefficient — `kappa_Thomson=lim_(q^2->0) kappa_Q(q^2)` (V011:1635, verified) — not a cross-section off a massive carrier.

**Further:** the freeze already fenced the presupposition — `:28-30`, "Thomson matching may not presuppose a sharp massive charged pole." That is evidence *against* malformed and *for* missing spec, because the freeze creates an obligation (reformulate) which it never discharges.

**The physics behind the worry survives and is real,** and it belongs to the missing predicates in §2: whether the `q^2 -> 0` limit exists, is finite, and is path-independent when the charged sector has no mass gap is exactly what the fork decides. That is unaskable until the carrier and the branch predicates exist.

### One contingent malformedness you should know about.

**RD-26 and RD-27 are not malformed today, but their well-posedness hangs on a single ruling.** If you rule that the connection is not promoted — or that the induced classical `L_EM,cell` route *is* the RD-26 action — then RD-26 has no independent content and RD-27's "gauge fixing" has no orbits to slice. Under that ruling the nine-step order becomes eight or seven and the freeze must be amended. Nothing else in the corpus is at risk of malformedness.

---

## 5. WHAT CAN BE MOVED EARLIER

**The single most useful finding in this sweep is a distinction the freeze does not draw: EXECUTION order versus SPECIFICATION order.**

### Execution: the order is genuine, throughout. Nothing can be executed out of order.

Every one of the nine consumes its predecessor's object, and in most cases there is no partial object to work with:

- **RD-27 ← RD-26.** You cannot gauge-fix an action that does not exist. With `K_bare = 0` there is not even a bare kinetic term to define a Gaussian measure over, so "quotient by the gauge group" and "Faddeev-Popov determinant" have no arguments.
- **RD-28 ← RD-26+27.** `STAGE7_QSPEC_REVIEW_CANDIDATE_V002.md:199-200` binds the amplitude and the gauge/ghost/edge measure in one sentence.
- **RD-29 ← RD-28.** It regulates RD-28's object.
- **RD-30 ← RD-29.** Transversality is precisely what a gauge-non-invariant regulator destroys — which is why "explicit transversality" is a **step-4** guardrail (freeze:40), not a step-5 one. The freeze itself treats the two as inseparable.
- **RD-31 ← RD-29/30.** No response to test independence of.
- **RD-32 ← the interaction.** You cannot ask "pole or threshold" of a theory whose interaction is unwritten.
- **RD-33 ← RD-32 and `kappa_Q`.** `STAGE8_NINE_ITEM_PLAN_REVIEW_AND_EXTRACTION_ERRATUM_V001.md:127`: step 7 "DETERMINES WHETHER STEP 8 IS WELL-POSED AT ALL."
- **RD-34 ← RD-26/27** only, per the freeze's own prose (below).

### Specification: almost entirely convention. **All nine specification acts are available today.**

Available with **no upstream EM machinery whatsoever**:

| Step | Available now |
|---|---|
| RD-26 | The promotion ruling (item 1). Decides RD-27's well-posedness too. |
| RD-27 | The edge-mode TYPE question (item 4) — the sealed complex `K_L`, its closure faces, and the boundary record carrier all exist independently of the gauge field's dynamical status. |
| RD-28 | Reading A vs Reading B; the carrier; the arity reconciliation. Definitions over a class of theories. |
| RD-29 | The Branch A / Branch B fork; the finite renormalization condition. |
| RD-30 | Define unrestricted `Pi`; rule cell vs continuum; state the photon-mass proposition. |
| RD-31 | All five items, including the C_ref/D3 resolution and the `M^(mu nu rho sigma)` tension. |
| RD-32 | Promote the named carrier; define both branch predicates; state the decision procedure. |
| RD-33 | The matching rule for **both** branches; define "the matching scale". |
| RD-34 | The elimination-list scope ruling and the durability-mechanism ruling. Pure scope, zero EM machinery. |

### Four orderings in the freeze that do not hold up, stated as findings.

1. **RD-31's specification is a prerequisite for RD-29's execution.** Step 4 cannot know whether a candidate regulator or finite counterterm is admissible until the test step 6 will apply is written down. Specifying RD-31 after RD-29 means step 4 executes against an unstated acceptance criterion — the "no target-selected finite counterterm" hazard the guardrails exist to prevent (freeze:36-43).

2. **RD-34's ninth position collides with a sealed no-go.** `BID_FINITE_RECORD_DURABILITY_NO_GO_V001.md:69` (verified): the four durability mechanisms "may not be selected after a response is evaluated." Ninth is after Thomson matching, i.e. after the response is evaluated. And the freeze contradicts itself: the list at `:22-23` says ninth, while the prose at `:45-47` (verified) states a much weaker condition — "The outgoing Causal Incidence Support test occurs **after the dynamical gauge sector is constructed** and before durability is promoted to the interacting theory," which is after RD-26/RD-27 only, and says nothing about steps 3-8.

3. **RD-33 sits under two conflicting sealed orders** — the freeze's reformulate-after-threshold clause versus the dependency repair's freeze-matching-data-inside-Q_spec. Both are sealed.

4. **The freeze contains no Q_spec node, yet every authority discussing RD-29 defers to one.** "may appear here only when derived by `Q_spec`" (V011:1653-1654); "the prescription ultimately fixed by `Q_spec`" (FBRAP V002:122-123). The true predecessor of step 4 is an object the frozen order does not contain — **and this is the same defect class the corpus already caught and repaired once**, at `LOCKED_ALPHA_PLAN_DEPENDENCY_REPAIR_V001.md:7-33`, a repair the EM freeze did not carry forward.

### And one scheduling fact that stands on its own.

**None of the nine consumes `kappa_record`.** V011:1644-1651 explicitly refuses the identification. The entire back half is independent of the Stage-8 bottleneck — it does not wait on R-L2b in the sliver direction. It waits on you. The only qualification is RD-33 item 3: until you rule what role `kappa_record` plays, the *road-map name* of that step asserts a dependency the sealed text does not.

---

## 6. THE ONE THAT MATTERS MOST — RD-29

**Why it is load-bearing.** `EM_DEPENDENCY_ORDER_FREEZE_V001.md:32-34` (verified): "Because zero bare `F^2` is adopted, the functional-regulator and finite renormalization step is where response normalization can originate." And the premise it rests on is an *adoption*: `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:130` — "This is an adopted compositeness condition, not a result of compactness, projective geometry, or gauge covariance." The adoption is also *insufficient on its own*: `PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:98-100` — "until the physical measure and finite extension are uniquely derived, the zero-bare declaration alone does not determine the finite induced coefficient." **The freeze correctly identifies where normalization can originate and does not supply what makes it originate there.**

**Status, in full.**

*Named five ways:* the frozen order (`:17`); two open Q_spec slots, machine-mirrored as `"functional_regulator_and_finite_renormalization": "OPEN"`; a required content of Q_spec (V011:1600-1601); an **unfilled argument slot of a sealed equation** — `K_bare[Q_spec, regulator, causal-cell scale] = 0` (FBRAP V002:127); and the renormalization condition itself (`PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:80-83`).

*One half now has a type — adopt this correction.* The triage claimed the regulator's type is never stated. It is. `STAGE8_T7_COMPLETED_CONTINUUM_RESPONSE_PROVENANCE_SPEC_V001.md:77-93` (verified verbatim) is headed "## Admissible Galerkin class" and gives seven typed conditions: nested orthogonal projections `Q_n -> I` strongly; preservation of charge, record grading, spinor typing; convergence on a common core for `h_0`; strong and interaction-norm-uniform approximation of every compact causal-cell multiplication operator; the same intrinsic causal envelope with no fitted pulse profile; **"introduce no zero-mode filling, thermal parameter, finite counterterm, or coupling normalization"**; and at least two cofinal realizations not related by relabeling one finite matrix — with `:92-93` "The proof may use an arbitrary member of this class only after showing that the final object is independent of that member." **The step-4 guardrail is already encoded as condition 6.** That file carries its own seal.

*But the verdict survives, three ways, each sufficient:*

- **Scope.** The class regulates the fermionic carrier for the **record** amplitude `Z_K(A)=lim_(n->infinity) a_K,n(A)/a_K,n(0)` (`:71`, verified), not `Z_Q[A]/Z_Q[0]` and `Gamma_Q^(2)`. Nothing states the transfer; asserting it is a principal act.
- **No member exists.** `STAGE8_T7_CONTINUUM_GALERKIN_PROVENANCE_CORRECTION_V001.md:24-28` (verified) proves the sealed family cannot exist for the free massless Dirac multiplier, with `:35-36` `genuine_finite_rank_continuum_restriction_constructed = false` / `parent_state_regulator_restriction_derived = false`.
- **The other half is unstated everywhere.** "Renormalization condition" occurs at **three** lines corpus-wide: one saying it "ha[s] not been derived"; one listing it as a closure obligation; and one **prohibition** — `STAGE8_T7_COMPLETED_CONTINUUM_RESPONSE_PROVENANCE_SPEC_V001.md:129-130`, "The adopted absence of an independent primitive `F^2` coefficient may not silently be used as a finite renormalization condition." **The one candidate condition is explicitly ruled out and nothing has been put in its place.** Widened searches (Epstein-Glaser, BPHZ, Hadamard, point-splitting, Wetterich/FRG, Wilsonian, regularization scheme, covariant regulator) return zero adopted scheme.

*Inputs: 2 of 12 sealed* — corrected down from the triage's 3, because input #3's verdict is withdrawn (see §1). "Boundary reduction," one of the seven items the uniqueness gate says must be derived, occurs **once** in the entire corpus: in the gate line itself.

*The sharpest thing the corpus knows about this step is a derived negative.* `COMPLETE_QSPEC_ABSOLUTE_SCALE_AND_CONTINUUM_PREREQUISITE_AUDIT_V001.md:83-117` computes the sealed three-history weight functional exactly (`sum_lambda w_lambda exp(-i lambda x) = sin^2(x/sqrt(2))`, `M_0=M_1=0`, `M_2=-1`) and concludes that the completed-history combination cancels constant, linear and odd single-cell insertions but **does not cancel the quadratic sector** — "Gauge/Ward identities enforce transversality but do not make its coefficient vanish. The sealed history weights therefore do not, by themselves, provide a canonical finite continuum electromagnetic normalization." The record structure was the natural candidate for making the `F^2` coefficient canonical, and it provably is not.

**What it would take.**

1. **Rule the fork.** `COMPLETE_QSPEC_ABSOLUTE_SCALE_AND_CONTINUUM_PREREQUISITE_AUDIT_V001.md:131-132` states it in one line and never resolves it: "a unique finite `F^2` normalization, **or** a finite microscopic completion that leaves no such subtraction freedom." These are two different programs and only one contains a functional regulator at all.
   - *Branch A* (continuum + regulator + subtraction) is what the step's name presupposes. Its ordinary realization is already graded `BLOCK_ABSOLUTE` — `ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V003.md:40-45`: "The exact determinant has the standard local `F^2` divergence and requires a subtraction condition. Cutoff and zeta spectral actions retain an action normalization, cutoff profile, matching condition, or equivalent finite prescription." Adopting Branch A requires stating what "parent-derived" supplies that defeats a grading the corpus reached without it.
   - *Branch B* (finite cell, no regulator) is named "the only surviving strict route" (`:90-108`), with the evaluation protocol already frozen. In a genuinely finite cell there is no UV divergence, hence **no functional regulator and no finite renormalization** — step 4 collapses to "derive Q_cell", a different obligation with a different owner, blocked on `complete_Q_cell_derived = false`.
2. **If Branch A: name the object regulated.** `Gamma_Q`? the connection-sector functional determinant? the CTP generating functional? Never stated.
3. **If Branch A: transfer or restate the Admissible Galerkin class** from `Z_K(A)` to the connection sector, and supply a **constructed member** — the existence proof the correction says is missing, using the corrected conditions at `STAGE8_T7_CONTINUUM_GALERKIN_PROVENANCE_CORRECTION_V001.md:43-51`.
4. **State the finite renormalization condition as an equation:** a target-independent statement of the form "`kappa_Q` evaluated at [stated, derived kinematic point] equals [stated, derived quantity]." It must be **frozen before step 4 executes**, or the "no target-selected finite counterterm" guardrail is unenforceable — any condition stated after seeing a response value is disqualified by construction.
5. **Reconcile with RD-31**, whose acceptance criteria do not exist.

**And RD-30 cannot rescue it.** Two independently sealed files establish that transversality delivers form and never coefficient, and that the `c_R` mutation `K -> K + c_R` is itself transverse. The entire normalization burden stays here alone, exactly as the freeze says.

---

## 7. ESTABLISHED / BOUNDED NEGATIVE / UNRESOLVED

### ESTABLISHED (sealed; the ~25 load-bearing lines below were re-verified against the files by me)

- The nine-step order, its guardrails, and its gate state — all nine `false` (`EM_DEPENDENCY_ORDER_FREEZE_V001.md:11-61`). `em_order_items_closed = 0`.
- `kappa_Thomson`'s definition and its pass conditions (V011:1613-1636).
- The transport-only hierarchy: transport is primitive, curvature and kinetic response are composite output; bare Maxwell and primitive Pauli insertions excluded (`PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md:49-50, 67-69`) — sealed, current authority.
- The seven-condition Admissible Galerkin class (`STAGE8_T7_COMPLETED_CONTINUUM_RESPONSE_PROVENANCE_SPEC_V001.md:77-93`).
- The unique normalized complex-linear primitive closure (`STAGE8_T7_RESPONSE_CLOSURE_SELECTION_DERIVATION_RESULT_V001.md:43-57, :70`).
- The abstract CTP scalar closure `Z_K[A_+,A_-]`, sealed and hash-verified.
- The free edge shell `m_*=sqrt(2) mu = pi/T_R`; no edge massless pole; strictly positive free residue.
- The dressed outgoing-record monomorphism and primitive Moller durability, with independent verifier.
- Axiom A3, proper-orthochronous Poincare covariance of the intrinsic cell measure.
- The typed continuum quadratic response tensor `M^(mu nu rho sigma)` — **with its negative attached**: cell scale cancels, shape/filling/incidence/multiplicity/orientation normalization do not.
- Executed and independently confirmed: finite holonomy response present; temporal plaquette response present **and** `FINITE_TEMPORAL_RESPONSE_NONLOCAL_OR_INHOMOGENEOUS`.

**Established negatives** (these are results, not gaps): two response-inequivalent parent actions outside the adopted branch; the sealed history weights do not cancel the quadratic `F^2` sector; gauge invariance / Lorentz covariance / compactness / charge quantization / Ward identities / anomalies / the charge-character lattice **do not forbid** the finite second term; the sealed Galerkin family does not exist for the free massless Dirac multiplier; the four durability mechanisms are inequivalent and may not be selected post hoc; the local Maxwell form is unsupported for the current two-cell parent; the ordinary continuum-determinant route is `BLOCK_ABSOLUTE`.

### BOUNDED NEGATIVE (searched and not found — bounded by the searches named)

- **No file in the corpus is named for any of the nine steps.** Full recursive filename scan over 3,909 files for `u1|gauge|ghost|maxwell|photon` → **0**. `find -iname "*packing*"` → 0. `find -iname "*descendant*"` → 0. `find -iname "*cisp*"` → 0. `find -iname "*thomson*" -o -iname "*match*"` → 0. The only `EM_`-prefixed file is the freeze itself.
- **Zero corpus hits**, each searched literally: gauge group; gauge orbit; BRST; Noether; ghost number; Berezin; anticommuting; gauge-fixing term; `R_xi`; Gribov; Kallen/Kalen/Kaellen; Lehmann; Bloch-Nordsieck; Dollard; Kulish; Buchholz; Frohlich; Morchio; Strocchi; LSZ; asymptotic completeness; regularization scheme / regularisation; gauge-invariant regulator; covariant regulator; Epstein-Glaser; BPHZ; Wetterich; Polchinski; functional renormalization group; integrate over the connection; sum over connection; measure on connection; gauge-field measure; Haar; Villain; Wilson action; lattice gauge; induced action; integration variable; dynamical connection; frame-independent; boost-independent; boost invarian; Proca; Stueckelberg; gauge boson mass; `H_eff`/`V_eff`/`H_desc`.
- No status flag in the descendant family is true anywhere in the corpus.
- **Caveat on reproducibility:** all counts exclude the vendored `.proof_deps/sympy` tree (~3,000 files). Counts published without that exclusion will not reproduce.

### UNRESOLVED — what this sweep could not determine

1. Whether freeze steps 1 and 2 survive at all under the transport-only branch. Contingent on a ruling you have not made.
2. Whether the induced classical `L_EM,cell` route (BID V009:1206-1215) is or is not the RD-26 action. Its audit rows are `PENDING`.
3. **New and unaddressed anywhere:** whether the selected complex-linear primitive closure is compatible with a CTP carrier, given that the rejected zero-curvature form is the inclusive sandwich and the sealed CTP object *is* a sandwich.
4. Whether the Admissible Galerkin class transfers from `Z_K(A)` to `Z_Q[A]`. Not stated anywhere.
5. C_ref vs D3 — `HELD_PENDING_PRINCIPAL`.
6. Where RD-32 sits: three incompatible placements, two of them sealed.
7. Whether RD-34's ninth position is compatible with the durability no-go. The freeze's list and its own prose disagree.
8. Whether "the matching scale," quantified in the program's terminal anti-self-deception test, has any referent. Today it does not.
9. Which of `M^(mu nu rho sigma)`'s five surviving packing dependencies RD-31's fence is meant to forbid. Four vocabularies, no correspondence, and no parental authority on the geometric axis.
10. I recomputed or read-verified roughly twenty load-bearing lines and adopted the remainder on the adversarial check's attestation. Seals were tested in all three attachment forms where claimed; I did not independently re-hash every artifact cited. The ten `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE` versions were covered by keyword, not read exhaustively.