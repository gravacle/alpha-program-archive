# THE RELEASE CONDITION (F2) — DRAFT V001 — for the principal's review

**Purpose.** The sealed, PRE-registered statement of exactly what must be true before each fence flips — `kappa_record_computed` → `alpha_computed` → the comparison (end test) — each with its evidence, **authored before any number is computed**, so the conditions cannot be retrofitted to the answer. This is the program's anti-fitting gate. Nothing here computes anything; it is the license, authored blind. Drafted 2026-08-13 at the |n|-forced gate (Q-1011). alpha_computed=false, proof_authorized=false, kappa_record_computed=false at authoring.

---

## GATE 1 — conditions before `kappa_record_computed` may flip to true
**G1.1 — THE WINDING-FORCING IS FULLY CLOSED (the Q-1011 residuals resolved).** Not "adjudicated" but *closed*:
  (a) q_N's completeness is **fully assembled** — U3 upgraded from PARTIAL to complete of record (the orbit invariant genuinely complete, not premise-level partial);
  (b) the U(1)_rel determinacy (response-null quotient = the diagonal only; the kernel physical) is **cross-confirmed** by an independent lane beyond the registrar's adjudication;
  (c) the Q-1011 verdict carries no un-discharged TYPE-R/TYPE-U dependency into the computand.
**G1.2 — THE COMPUTAND IS DEFINED BY CITATION (F0), not assumed.** The sealed chain that defines κ_record / K_* is **pinned by citation** to sealed text — "compute κ_record" has no meaning until this chain exists of record.
**G1.3 — RECORD-NATIVE AND BLIND.** The computation consumes only the record's own derived content — **no imported scale, no ℓ_P, no KK/metric, no faithfulness-as-premise** (faithfulness enters only as the *derived* conclusion of Q-1011) — and references **no measured value** (no 137, no CODATA). The lens is not an input.
**G1.4 — RATIONALITY CHECKED FIRST (structure before value).** Whether K_* is rational by construction is decided from its **defining form**, fence-safe, BEFORE any value is computed (the Q-34 / exact-rational discipline).

## GATE 2 — conditions before `alpha_computed` may flip to true
**G2.1** — κ_record / K_* computed under Gate 1, blind, sealed, opposite-lane checked.
**G2.2** — the assembly formula is the **frozen convention** `α = 1/(4π K_*)` (or the frozen winding form), fixed of record BEFORE the number — no post-hoc formula selection.
**G2.3** — α is assembled from **only** the derived K_* and the pure geometric 4π; nothing measured, nothing fitted, enters the assembly.

## GATE 3 — conditions before the comparison (end test) may flip
**G3.1** — α computed under Gates 1–2, blind, sealed.
**G3.2** — the end-test comparison spec is **frozen before** the comparison is looked at (the preregistered spec, one shot).
**G3.3** — the comparison is run **once**; whatever it says is the result, recorded plainly, no re-run, no adjustment.

---

## FALSIFIERS (armed at authoring — any tripping BLOCKS the corresponding flip)
- G1.1: q_N proves incompletable, or an independent lane finds the response-null quotient removes more than the diagonal (kernel not physical) → winding-forcing REOPENS, no κ_record.
- G1.2: no sealed chain defines K_* → "compute" is undefined, no κ_record.
- G1.3/G2.3: any imported/measured value in the chain → fitting; the run is void.
- G1.4: K_* proves non-rational-by-construction in a way that reintroduces a free scale → reopen.
- G3: any re-run or spec change after seeing the comparison → the result is void.

## ORDER OF RELEASE (each flip is the principal's, in order, with evidence)
`author F2 (this)` → close G1 → **principal flips `kappa_record_computed`** → compute K_* → close G2 → **principal flips `alpha_computed`** → assemble α → close G3 → **principal flips the comparison fence** → end test, once.

**Status: DRAFT for review.** The load-bearing clause is **G1.1** — we do not compute anything until the winding-forcing is *closed*, not merely adjudicated. The gate to κ_record is the closure of Q-1011's residuals.

---

**UPDATE 2026-08-13 — G1.1 EXECUTED, FALSIFIER FIRED (Q-1012).** The two G1.1 residuals were run by independent, cross-lineage, fenced lanes: OPUS-URELCHECK (254f691d) and QN-COMPLETE-BUILD (e0800892). Result: the U(1)_rel *carrier* determinacy and Reading-A q_N completeness CONFIRM, but the load-bearing "ker(χ_n) is physical structure acting nontrivially on q_N, distinguishing |n|" is REFUTED on bedrock — a homomorphism kills its own kernel, so ker(χ_n) acts *trivially* on q_N for |n|>1; "kernel acts nontrivially on q_N" is faithfulness *restated*, not derived. The G1.1 falsifier ("an independent lane finds the response-null quotient removes/nullifies the kernel → kernel not physical → winding-forcing REOPENS, no κ_record") is TRIPPED. **Gate 1 is NOT passable.** κ_record stays fenced. The gate worked exactly as designed: it caught a merely-adjudicated verdict before any computation. The revised state and its evidence are booked at **Q-1012**; the founding question is UNDETERMINED, localized to whether the boundary-closure forces faithfulness (the require-side).
