## PASTE 682 — CODEX LANE 2 = BUILDER A (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / TRANCHE] THE V009-06 ENVELOPE + SPEC V006'S CORPUS RULE

(Same Lane 2 session rules: cleanroom writes; the registrar mirrors. C77 governs; the Q-604 guard: one lawful row is one row.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-607.
- The relocation of record: `STAGE8_TASK6_GROUNDING_RELOCATION_DARIO_V001.md` = `69334875b94679c16da9b8d6153242241ca3c202f0facc6130596b9807189e6f` — its V009-06 grounding is the ONLY authorized envelope source: `stage_dependencies` at the provenance preregistration file, bytes [18898,19830), populating BOTH DAG arguments; the second encoding and the status field expressly barred; the precedence decision is the authority.
TASK:
C1. **The envelope:** the evidence payload for `C-B-V009-06` built from EXACTLY the relocated bytes (content-addressed copy + the canonical DAG-argument serialization the descriptor's opcode consumes, each step shown); the evidence manifest row -> available=true with grounding citations; declared_root recomputed (old->new).
C2. **Spec V006 — the corpus-definition rule, minimal:** one new law in §2's language — every `M2(q,S)` invocation's `S` MUST name a SEALED corpus definition (a content-addressed member list of record); an M2 row without one is SPEC-INCOMPLETE and cannot be populated (its evidence stays honestly absent). A registry of the currently affected rows (the four from Q-606 + any others your sweep finds). NO row criteria rewritten — the rule and the registry only. Carriage per current law (complete finite delta; conventions declared).
C3. Static self-check; PIN CHECK; the does-not-do statement (one envelope does not close anything; the board and seals untouched).

OUTPUT: envelope + updated manifest + spec V006 + one sealed artifact
`STAGE8_TASK6_V009_06_ENVELOPE_SPEC_V006_LANE2_V001.md`
with final lines: `ENVELOPE = built from relocated bytes (+serialization display)`, `DECLARED_ROOT = old->new`, `SPEC_V006 = corpus rule installed (+affected-row registry, N rows)`, `CARRIAGE = complete_finite_delta`, `SELF_CHECK = passed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal in the cleanroom, report hashes, STOP (the registrar mirrors; Dario checks; then run 023). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
