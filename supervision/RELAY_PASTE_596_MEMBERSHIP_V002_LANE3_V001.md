## PASTE 596 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5 / STEP 6] MEMBERSHIP V002: THE HONEST FINAL FORM

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-526. THE FALLBACK IS INVOKED (read the sealed addendum in `DECISION_SEQUENCED_PROGRAM_2026-08-06.md`): CARRY-1 is the live path; this relay produces the membership theorem in its HONEST FINAL FORM.
- The V001 membership (`0d00c9cf…`) and its kill review (`ba9430c1…`'s predecessor `9caef0f7…` — the four Dario kills D1/D2/D3/D6) are the base. The repairs and the current state of every input are sealed across the arc: the native consumer registry + manifests (`6cc774e2…`), the FC6 scope-qualified row (Q-491), the A6 scoped J2 with the displayed reconstruction and its premise state (identity-scope naturality; the Loc-naturality subscope `474bf721…`), the J7 carried contract, the confirmed faces/functor/bottom/diamonds/C_ret chain, A7/A8/A9 as law, CARRY-1 (`fdf20bd4…` §7.4), the ExtSrc TYPE-U of record (`d662889b…`).
- MANDATED: surface anchor; dependency re-audit; the R9 quantification check; the verb discipline (every row's verb matched to what its source carries — the D6 lesson).
- Custody: you build; Dario re-reviews. Tag PROVABLE / PART-PROVABLE / YOURS / TYPE-U per row.

TASK — produce MEMBERSHIP V002:
M1. **The four D-repairs executed:** (D1) clause (vi) discharged by the native registry + the B2.6 rerun against the completed manifest rows; (D2) FC6 as the scope-qualified row the guard's own wording licenses + J2's rows at their TRUE verbs (full-displayed on A_RP⁺ identity-scope; premise-gated beyond; the A6 scoped square on A_CC); (D3) J7 as the carried corrected contract (the coefficient face on its formable support, the completion theorem conditional exactly as proven); (D6) every final-board verb re-derived from its source.
M2. **The CARRY-1 conditional structure typed row by row:** the FC10/FC11 rows carrying their exact conditions; both route families as potentially-empty typed families; A9 licensed-not-inhabited; the seed end-test-structural; the ExtSrc TYPE-U named where it gates; NOTHING asserted beyond its sealed strength.
M3. **The theorem in final form:** FiniteCoherent_020(F_actual) with its conditional rows — the exact statement of what IS proven jointly on the one tuple and what is carried; what an adopted A2 then discharges (Step 1 in the conditional form) and what it cannot.
M4. **Battery:** the three permanent counterexamples; the circularity falsifier; F_PLDEC; anti-tuning with the cross-sector-unit row; surface anchor; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_MEMBERSHIP_THEOREM_LANE3_V002.md`
with final lines: `D_REPAIRS = 4/4 EXECUTED`, `MEMBERSHIP = PROVEN_IN_CONDITIONAL_FORM (+the carried-row count)`, `STEP1_DISCHARGE = conditional_form_stated`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
