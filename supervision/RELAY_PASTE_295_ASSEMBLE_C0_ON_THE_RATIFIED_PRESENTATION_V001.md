PASTE 295 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2b] ASSEMBLE `C0` AND `d_C0` ON THE RATIFIED PRESENTATION

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-212)

```text
DOES THE OBJECT EXIST?   *** DoR 008 IS RATIFIED. C0_prop IS AVAILABLE FOR USE. *** The V002
                         presentation (76916244...) survived two adversarial passes.
IS THE VERSION CURRENT?  YES.
ARE ITS INPUTS PRESENT?  Q-201's frozen source-record tuple + the ratified seven-field presentation.
```

## 1. THE BUILD — NOW LEGAL, AND MARKED

**Assemble, as instances with certificates, every claim marked `TYPE-P | premises: DoR-008`:**

```text
1  the joint carrier: the even spatial join of the field module to Q-201's tuple, EXECUTED
2  the joint algebra with its generators/relations on that carrier
3  the joint representation (the Hilbert C*-module structure, scalarization NOT taken)
4  the common dense domain, verified invariant for every unbounded generator
5  the forward/backward CTP embeddings and the bounded finite-support source maps, EXECUTED
6  *** d_C0: the descent map from the adopted presentation as B0-role realization -> C0, with ONE
   FROZEN CONSTRUCTION TRACE and the provenance scope stated. What d_C0's common-origin provenance
   still LACKS is reported, not papered (DoR 008 does not discharge it). ***
```

## 2. THE FALSIFIER CHECK — RUN ITS FIRST INSTANCE NOW

*** DoR 008's STANDING FALSIFIER: the completion must reproduce every sealed finite result on
restriction. RUN THE FIRST TWO CHECKS AS PART OF THIS BUILD: restrict the assembled joint structure
to (a) a single record cell and (b) the K_square complex — and verify the restriction reproduces
the sealed one-cell structure and is CONSISTENT with the sealed composition-loop spectrum's carrier.
A RESTRICTION FAILURE VOIDS THE ADOPTION — report it immediately and stop. ***

## 3. LEADS

```text
IF C0 + d_C0 ASSEMBLE AND THE RESTRICTIONS PASS -- *** LEAD WITH IT: THE PACKAGE'S FIRST COMPONENT
EXISTS. *** State the residual gap list for d_C0's provenance, typed.
IF AN ASSEMBLY STEP FAILS -- lead with which field of the ratified presentation is insufficient and
why; that is a defect in the adoption the kill-passes missed, and the principal must hear it.
IF A RESTRICTION FAILS -- *** LEAD WITH IT ABOVE EVERYTHING. THE FALSIFIER FIRED. ***
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
