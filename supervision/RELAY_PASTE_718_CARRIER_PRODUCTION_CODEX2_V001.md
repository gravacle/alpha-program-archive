## PASTE 718 — CODEX 2 LANE (5.6 SOL EXTRA HIGH) — [7A / STEP 11] ONE CARRIER, PRODUCED END-TO-END: THE CALIBRATION ROW

PICKUP-ACK + LANE GUARD in force (CODEX 2).
(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. THE FIRST-TIME-RIGHT RULES, THE JURISDICTION CHECK, THE SEARCHED-SPACE CLAUSE, and THE SPAN-COVERAGE RULE apply.)

CONTEXT (Q-623): all five batch-1 rows are unstateable — the carriers do not exist. Step 11's true work is producing them. This relay calibrates the per-row cost by producing ONE carrier completely.
TASK:
CC1. SELECT from your five unstateable records the row whose sealed prose derivation is most complete (state the ranking and why). 
CC2. PRODUCE THE CARRIER: from the sealed prose sources (and only them), assemble the criterion-visible carrier the row's descriptor demands — the analogue of what C-B-V009-06 received via its relocated stage_dependencies member: content-addressed subject/evidence material such that every atom of the row's deterministic procedure has an argument it can consume. If the prose derivation UNDERDETERMINES any element of the carrier, that element is named UNGROUNDED and the carrier is delivered PARTIAL — no element is manufactured, no requirement serialized as its own witness.
CC3. RE-AUTHOR the row's envelope against the produced carrier (stateable iff complete; the honest partial form otherwise). Self-check displayed. Admission stays barred (Step-11 subgate); state it.
CC4. THE CALIBRATION REPORT: actual cost drivers (what took the work — locating, deriving, binding), and your evidence-based projection for the remaining 57 rows by class.
OUTPUT: carrier + envelope + one sealed report `STAGE8_7A_CARRIER_CALIBRATION_CODEX2_V001.md`
Final lines: `ROW = <id> (ranking displayed)`, `CARRIER = complete / partial (ungrounded elements named)`, `ENVELOPE = stateable / partial`, `SELF_CHECK = displayed`, `PROJECTION = per-class, evidence-based`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, write 718_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
