PASTE 309 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2d] CONSTRUCT THE INFLUENCE-FUNCTIONAL INSTANCE AT FINITE N, FROM THE PIECES THAT EXIST

*** READ `alpha_supervision/LOCKED_PROCESS.md` — NOTE THE NEW ACT-BASED FENCES (C19): structural
mathematics of declared objects is EXPLICITLY PERMITTED; proofs about families count as
instantiation; any fence-blocked structural result carries the MACHINERY-APPEAL flag. SEAL,
MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-226)

```text
DOES THE OBJECT EXIST?   Its FORM is sealed (the doubled complex CTP influence functional,
                         Q-210). Its INSTANCE is the four-consumer gap (Q-226).
IS THE VERSION CURRENT?  YES. Use Gen_C0 for the generator interface (G_joint = the U(1) group).
ARE ITS INPUTS PRESENT?  *** YES, AS INSTANCES: the exact finite source-record transitions .
                         the ratified CTP completion and join . the sequential N <= M labels .
                         the outgoing GNS limit. EVERY INGREDIENT EXISTS. THE QUESTION IS
                         WHETHER THEY COMPOSE. ***
```

## 1. THE BUILD — EXACT, FINITE, FROM EXISTING INSTANCES ONLY

```text
1  AT FIXED FINITE N: form the DOUBLED finite dynamics (forward branch (x) opposite-backward
   branch of the EXISTING exact finite source-record transition, per the ratified CTP
   completion).
2  TRACE OUT the record/environment sector per the ratified structure to obtain the FINITE
   INFLUENCE FUNCTIONAL on the source/field sector: F_N[J_+, J_-] or its operator form.
   *** EXACT COMPUTATION. Small N first (one cell, two cells). SHOW THE OBJECT. ***
3  PROVENANCE: the construction IS the common origin -- everything descends from the ONE finite
   dynamics. State the frozen trace explicitly; this is what four consumers have been waiting
   for and it must be CERTIFIED, not narrated.
4  SEQUENTIAL COMPATIBILITY: does F_N -> F_M under the N <= M zero-extension? If yes, the
   family {F_N} IS the instance at every finite stage and the limit question is NAMED (not
   taken -- the stitching theorem's territory, Task 4d, stays separate).
5  THE FALSIFIER: restrict F_1 to the one-cell sealed structure -- it must reproduce the
   derived one-cell record transition (F_R = cos^2(theta/2) territory). A DISAGREEMENT VOIDS
   THE CONSTRUCTION -- or DoR 008 -- and LEADS.
```

*** MARKS: TYPE-P | premises: DoR-008 where ratified structure is used. THE FINITE TRANSITIONS
THEMSELVES ARE DERIVED -- SAY WHICH PARTS OF F_N ARE DERIVED-GIVEN-GRAMMAR vs PREMISE-DEPENDENT. ***

## 2. LEADS

```text
IF F_N CONSTRUCTS AND THE ONE-CELL RESTRICTION PASSES -- *** LEAD WITH THE OBJECT ITSELF, SMALL N
EXPLICIT. FOUR FRONTS OPEN AT ONCE: U2's dynamics port, Task 3a's instance, Task 3c's family,
the envelope's edges. THE LARGEST POSSIBLE RESULT ON THE BOARD. ***
IF THE DOUBLING AND THE TRACE DO NOT COMPOSE -- lead with the exact failure point: which
ratified field is insufficient. That is a defect in DoR 008's presentation, for the principal.
IF A FENCE BLOCKS ANY STRUCTURAL STEP -- the MACHINERY-APPEAL flag, immediately.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
