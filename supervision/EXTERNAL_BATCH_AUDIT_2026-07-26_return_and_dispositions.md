# External Batch Audit (v005+L2+majorant P1+Duhamel) + Disarm Authorization — Return and Dispositions
Received 2026-07-26 via Brian's relay. 23 raised, 7 confirmed, 16 refuted.

CLEARED under direct attack (recorded per the reviewer's instruction): the
L2 typing freeze; majorant Phase-1; the Duhamel spec + interchange schema;
the F1 pair-evaluated reading pin (legitimate — both readings witnessed
pre-pin); F2's victory-class ordering block; B1 piece-reconstruction and
FIX 2 stand; both v004 BLOCKINGs fixed for v005 routes. "The physics
artifacts are in better shape than the pipeline."

CONFIRMED findings and dispositions (all executed this date, anchored):
1. DISARM AUTHORIZED by Brian and EXECUTED: 11 superseded entry points
   (launch v002-v004, run_pipeline v001-v004, compare v001-v004) moved to
   scripts/superseded_quarantine/ as git-tracked renames, bytes preserved;
   sealed quarantine manifest dc41d278; SEVEN fail-closed invocation
   attempts recorded (incl. the feared v003 keystroke and quarantine-path
   invocations) — zero artifacts. Urgency confirmed: v003's receipt write
   preceded its checks, so mere invocation poisoned.
2. BLOCKING (launcher v005 docstring asserting an absent mechanism —
   FIFTH repetition of the defect class): disposition per the reviewer —
   the authorized disarm makes the claim TRUE in the same change; RULE 4
   sealed into the standing discipline (docstring claims are checkable
   claims; every mechanism claim names the test that drives it).
3. BLOCKING (comparison pre-flight covers 1 of 4 provenance conditions):
   controller v006 repair binding sealed (hoist all four, mechanically
   enumerated from the comparator's requires; M-a/M-b lane-precondition
   hoists; M-c startability-test teeth restored); v006 authoring lane
   running under rules 1-4.
4. MAJOR (gamma blind commitment unsealed in /tmp): rescued IMMEDIATELY —
   sealed as T07_gamma_refutation_blind_commit_v001.json (fde6e29e) and
   anchored (ad73f20) with an honest relocation record: original mtime
   2026-07-25 21:26 recorded; the reviewer's before-the-re-run ordering
   was NOT met (re-run had launched) — recorded as a deviation — but the
   commitment sealed strictly before ANY primary output exists (absence
   verified at seal). Reviewer position honored of record: gamma is NOT
   established as refuted-by-computation until the reviewer accepts the
   chain; the route memo's structural argument carries the refutation
   meanwhile. STANDING RULE adopted: a commitment in a scratchpad is not
   a commitment — commitments seal in the workspace at commitment time.

Production remains prohibited (both authorizations absent); nothing in the
relay authorizes it. Report-back for re-audit will be sealed artifacts.
