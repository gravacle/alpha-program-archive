## PASTE 606 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / Q-540] THE IDENTIFICATION DETERMINATION: IS LP-JII THE BETA GAP?

(Same Dario session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-540 (read its entry — the hypothesis this relay determines).
- THE TWO OBJECTS: (A) LP-JII — the J-II/ExtSrc TYPE-U crossing: the LOCAL-SHADOW → physical alpha-adjacent-carrier realization (its chain: `fdf20bd4…` J-II, `ba9430c1…`, the status column `bcb8cced…` row 4, P4's "exact domain bridge" per `d13bcf1f…`); (B) THE BETA GAP — the internal/external metric conversion of a record cell, the geometric route's single registered blocker of long standing (register-sweep for its sealed statements: beta gap / metric conversion / internal-external under their era's names; the corpus-audit cautions apply).
- Custody: a determination against sealed text. Tag PROVABLE / PART-PROVABLE / YOURS. Register-sweep FIRST for both objects' full sealed histories.

TASK:
J1. **Both objects stated exactly from their sealed sources** — (A)'s demand as the realization/bridge; (B)'s demand as the conversion — domains, codomains, what each consumer actually needs, quoted.
J2. **THE DETERMINATION:** exactly one of — IDENTICAL (one object of record under two names: the displayed type-level identification, with every consumer of each shown to consume the same missing map); OVERLAPPING (the shared core displayed + each side's residue exactly); DISTINCT (the type-level separation displayed — what (A) needs that (B) does not and vice versa). No preference; the sealed text decides.
J3. **The consequence under each outcome:** for the Task 6 comparator eligibility, for the geometric route, and for Task 7's charter (if IDENTICAL: the correspondence boundary files as one named object with its full double history — state what Task 7 inherits).
J4. **One fresh attack + surface anchor + verb audit.**
Final lines: `IDENTIFICATION = IDENTICAL / OVERLAPPING (+core and residues) / DISTINCT`, `CONSEQUENCES = stated`, `VERB_AUDIT_SELF = CLEAN / (+items)`.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_JII_BETA_IDENTIFICATION_DARIO_V001.md`
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
