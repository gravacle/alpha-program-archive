PASTE 310 — CODEX LANE 2 (HIGH EFFORT) — [TASK 2d] THE SOURCE-HISTORY MAP FROM COVARIANT INCIDENCE — DERIVED OR CHOSEN?

*** READ `alpha_supervision/LOCKED_PROCESS.md` (act-based fences, C19). SEAL, MIRROR, REPORT,
STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-227)

```text
DOES THE OBJECT EXIST?   The TARGET (U_N[J,A,g]) is lack 1 of Q-227. The CANDIDATE CONSTRUCTION
                         uses only sealed instances: Gate 4's unit-weight COVARIANT incidence
                         (incidence WITH the connection) + the exact finite transitions of Q-227.
IS THE VERSION CURRENT?  YES. I_N = delta and Phi_N = charge dephasing are the sealed baseline.
ARE ITS INPUTS PRESENT?  YES, as instances.
```

## 1. THE QUESTION BEFORE THE BUILD — STANDING

*** IS DECORATING THE FINITE TRANSITION WITH THE RECORD-SIDE U(1) HOLONOMY THE **SEALED CONTENT
OF COVARIANCE**, OR A CHOICE? Read Gate 4 at source: does "covariant incidence" REQUIRE that
transport along an edge carries the connection's phase at unit weight -- i.e., is minimal
coupling DERIVED here? ADJUDICATE FIRST. If it is a choice, name the alternatives and STOP
before building on one. ***

## 2. THE BUILD (only if standing = derived, or explicitly marked TYPE-P on the ratified data)

```text
1  DECORATE: U_N[a] = the exact finite transition with each incidence step carrying the U(1)
   holonomy of the background a along it, at unit weight. EXACT, small N first.
2  RETRACE: F_N[a_+, a_-] = <R_N| U_N[a_-]^dagger U_N[a_+] |R_N> -- the SAME trace Q-227
   already executed, now with source dependence. SHOW IT at N = 1, 2 as an explicit function
   of the holonomy differences.
3  CHECKS THAT RIDE ALONG:
   (a) a_+ = a_- must return I_N = identity (Q-227's sealed baseline);
   (b) gauge covariance: a common gauge transformation of a_+/a_- must act as the sealed
       conventions dictate;
   (c) the dephasing structure Phi_N must persist at zero holonomy difference.
4  WHAT F_N[a_+, a_-] IS: the record sector's INFLUENCE ON THE FIELD -- the very object whose
   second variation is the induced response. *** STOP AT THE OBJECT. NO SECOND VARIATION, NO
   KERNEL EXTRACTION, NO B_ind -- those live downstream with their own authorizations. ***
```

## 3. LEADS

```text
IF MINIMAL COUPLING IS DERIVED AND F_N CONSTRUCTS -- *** LEAD WITH THE EXPLICIT N = 1, 2
FUNCTIONALS. The four-consumer object exists at every finite stage, from sealed content. ***
IF THE DECORATION IS A CHOICE -- lead with the alternatives, sized; the principal rules through
the gate.
IF THE CHECKS FAIL -- lead with which; (a) failing impeaches the construction, (b) failing
impeaches the conventions, (c) failing impeaches Q-227's baseline. Name the impeached object.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
