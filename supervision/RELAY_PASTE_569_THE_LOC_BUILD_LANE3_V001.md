## PASTE 569 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5 / EQ6] THE LOC BUILD: THE PHYSICAL LOCALIZATION MAP, THE LAST OBJECT

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-495.
- THREE artifacts converge on this object — read their Loc-relevant sections first: your two-lemmas artifact (`48616c23…` — (M5)'s undefined subject term; the Kernbar scoping), the seed adjudication (`STAGE8_TASK5_EQ6_SEED_ADJUDICATION_DARIO_V001.md`, `e287b057…` — the false-anchor warning, the F²/holonomy headwind, the foreclosure of authoring), your Loc-naturality artifact (`474bf721…` — the identity-scope proof, the (L3)/(L8) missing base cases, the linearity debt).
- Authorities: the projector cert (`0bef9a00…` — the FINITE Loc = sigma^fin and the T-typing), the physical-J2 factor stock (`3fd4b924…` — what Z1 built and where it stopped), DoR-020-A1 (the path/current correspondence and bundle members — the most plausible derivation resource: it ties currents to paths on the surface), DoR-019 (the metric), A5/A6.
- Custody: you build; Dario reviews. Tag PROVABLE / PART-PROVABLE / YOURS. The display standard applies throughout. THE FALSE ANCHOR IS NAMED: the equation-of-record pi_Mx∘Loc∘Kernbar∘Q = 1 must not be consumed as a theorem anywhere in this build.

TASK — with the determination discipline embedded:
B1. **The definition question first:** what must Loc BE — the sealed map-level localization from the kernel/profile side onto the Maxwell/contact carrier — its exact domain (including how the L_T summand enters, resolving the Kernbar scoping split), codomain, and required properties (linearity; the naturality base cases (L3)/(L8); reader-freedom). State the complete specification BEFORE attempting construction.
B2. **The determination:** is a map satisfying B1 DERIVABLE from sealed stock? The A1 path/current correspondence is the prime candidate resource — currents are tied to paths on the actual surface; localization of a kernel image onto the carrier plausibly factors through it. Derive it displayed if so. If the stock underdetermines Loc: the FULL-GATE candidate (the exact freedom, genuine alternatives, minimality, falsifiable void conditions — noting Dario's filing-level signal: any candidate must carry a genuine falsifier class). If obstructed: the proof.
B3. **If built: the immediate consequences, each displayed:** (i) the (L3)/(L8) base cases — does naturality now extend beyond identities?; (ii) (M5)'s subject term — DEFINED; state the seed condition as a well-posed displayed pairing (do NOT evaluate it — it is the end-test condition of record); (iii) J2's rank-preserving premise board; (iv) the coefficient face's inhabitance basis. If any consequence fails, report exactly.
B4. **Battery:** F_PLDEC (the build must be reader-free — the false anchor's shadow is the named hazard); the F²/holonomy headwind confronted honestly (does the built Loc give the L_T image any route to the harmonic line, and if it structurally cannot, SAY SO — that is physics, not failure); anti-tuning; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_THE_LOC_BUILD_LANE3_V001.md`
with final lines: `LOC_SPEC = stated`, `LOC = DERIVED / AUTHORABLE_CANDIDATE (+gate) / OBSTRUCTED (+proof)`, `SEED_CONDITION = WELL_POSED / STILL_UNDEFINED`, `NATURALITY_BASE_CASES = EXTENDED / IDENTITY_ONLY`, `HOLONOMY_ROUTE = exists / structurally_absent`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
