PASTE 398 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THE BACKGROUND REALIZATION — CONSUME THE ADOPTED FIELD AT LAST (BUILD ii OF THE FINAL FOUR)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-314)

```text
DOES THE OBJECT EXIST?   NO (synonym count zero): the PHYSICAL BACKGROUND REALIZATION
                         (Abar_*, G_*) -- required by active v004 for B_ind and on-shell
                         Delta_Phi (Q-314's citation). Its inputs have three proven
                         pieces that are NOT it but feed it: DoR-015's ADOPTED external-
                         background field (never yet consumed), Q-281's bounded seed with
                         the forced state, and Q-252's critical-set theorem (pullback
                         stationary set = critical set of Theta, TYPE-P).
IS THE VERSION CURRENT?  YES through Q-314. NOTE THE SCOPE WON BY THE AUDIT: p_loc is
                         BACKGROUND-AGNOSTIC and kappa_Thomson has its own A=0 reference
                         -- the background serves Pi_R,ind's stationary evaluation ONLY.
                         Build exactly that much; scope creep is the kill.
ARE ITS INPUTS PRESENT?  YES, all ratified/proven: the adopted field (V005's F-structure),
                         the seed (D_J W(0), D_R W(0) with p_A = r_ch/(r_0+r_ch)
                         symbolic), the critical-set theorem, the lift fiber (state-
                         freedom-free), Q-314's requirement citation (what the background
                         must DO, from v004's own words).
```

## 1. THE TASK

```text
1  READ v004's requirement precisely: what the stationary evaluation of Pi_R,ind
   consumes from (Abar_*, G_*) -- the contract, quoted.
2  CONSTRUCT: the background as the completed-level critical structure of Theta (the
   Q-252 theorem is the characterization), realized IN the adopted external-background
   field (consume it -- cite the DoR-015 clauses), seeded by Q-281's forced-state seed,
   lifted along the state-freedom-free fiber. The p-content: symbolic forced form ONLY.
3  CERTIFY: consistency with Q-252's determinations (the three zeros distinct; the
   partly-fixed surface); restriction behavior (what the background looks like at
   finite stages -- Q-252 says NO finite stationary point, so the background must have
   NO finite stationary shadow: prove its restriction is the honest non-stationary
   finite structure, or type the tension); uniqueness ON the adopted field or the
   family typed (no selection).
4  CONSUMER STATEMENT: what the stationary 2PI/Schur blocks (build iii) consume.
5  KILL-PASSES: consume the adopted field (a second authored background is the kill);
   symbolic ranks only; six-account rows; door flags; no evaluation.
DELIVERABLE: (Abar_*, G_*) realized with certificates, or the exact typed obstruction.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
