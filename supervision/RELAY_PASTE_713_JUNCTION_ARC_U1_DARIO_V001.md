## PASTE 713 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / STEP 6 / JUNCTION ARC] THE OPENING MOVE: THE U1 SHARED-CORE OBLIGATION

LANE GUARD (in force): before executing, verify this header names YOUR lane (DARIO); if it names another lane, write a misdelivery report to the outbox and STOP.

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. THE FIRST-TIME-RIGHT RULES and THE JURISDICTION CHECK apply. TASK 6 IS CLOSED of record; 7A IS OPEN. This relay runs UNDER THE FROZEN PREREGISTRATION 9f0d12b4… — its criteria may not be altered here; any needed change is a surfaced revision of record.)

CONTEXT (handoff package a0a7e1d1…, §X5): the junction arc's first work item is U1 — the corpus types the Ward-symbol map (J-II) and the length normalization as OVERLAPPING, states "a spacetime length normalization is not progress on (JD-3)" (8e31df94…[12199,12257)), and NOWHERE states the compatibility condition the two objects must jointly discharge. Until a sealed statement exists, "the maps land JOINTLY geometric" has no test. This is physics, not paperwork.

TASK — derive the SHARED-CORE OBLIGATION:
Y1. From the sealed corpus, derive what the two objects' overlap IS: which obligation of the JD-3 cell rule (its four obligations) and which clause of the admissibility spec bind the SAME record-cell structure, and state the joint-discharge condition as a typed statement over that shared structure. Ground every step (source + span); the jurisdiction check applies to every rule you invoke; UNGROUNDED steps are named, never bridged.
Y2. Type the result per the preregistration: if the derivation lands, it is the missing U1 carrier — state it in the form the frozen prereg's U1 row says is missing (a sealed statement of the shared-core obligation). If the corpus UNDERDETERMINES the condition, that is a lawful result: state exactly what is missing, in the NOT-RECOGNIZABLE-AS-STATED form, and what smallest sealed addition would determine it.
Y3. Any likeness to a familiar classical structure encountered on the way goes to the CORRESPONDENCE LEDGER (zero verdict weight) — log it and move on. No junction map is EVALUATED; no numeric is produced; this is the compatibility statement's derivation only.
Tag every step PROVABLE / PART-PROVABLE / YOURS.
OUTPUT: one sealed artifact `STAGE8_7A_JUNCTION_U1_SHARED_CORE_DARIO_V001.md`
Final lines: `U1 = derived (statement displayed) / underdetermined (missing carrier named)`, `GROUNDED_STEPS = N / (+ungrounded named)`, `LEDGER_ENTRIES = k`, `PREREG_UNTOUCHED = true`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 713_DONE.md to the outbox, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
