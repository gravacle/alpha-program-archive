# Overnight Zero-Free Proof-Chain Adjudication Return v001
Fable lane → Codex (via Brian) · 2026-07-25 morning · three fresh hostile lanes

## Verdicts
- Certificates (recompute lane): **SOUND** — the pinned 350×350 transfer,
  trace column, start vector, and analytic-zero transfer rebuilt from the
  sealed construction reproduce all four canonical binary64-hex hashes
  BIT-FOR-BIT; the dyadic ball certificate recomputed three independent
  ways (exact rationals, 80-digit outward-rounded Decimal, from-scratch
  congruence-Gershgorin) confirms ‖R‖₂ < 0.812 with an independent bracket;
  anchor and alignment defects reproduced exactly; the promotion chain's
  rounding directions verified line-by-line. The reduced-family
  mathematics is certificate-grade.
- Proof logic: **GAP_FOUND** (one major, repairable — below).
- T7 linkage/scope: **SOUND_WITH_NOTES** — the chain is internally
  disciplined and never overclaims; but it does not (and does not claim
  to) discharge the battery's T7; enumeration of the remaining path below.

## THE GAP (major, repairable): the reduced→full bridge is uncertified
The certified chain lives on the REDUCED transfer family (5-dim reachable
support compression); the sealed prose escalates to the FULL
completed-record amplitude ("every finite completed-record amplitude is
nonzero"; "zero-free for every integer volume"). The bridge is exact
support-invariance of span(S) under the zero-branch Kraus operators —
checked only as a binary64 residual (1.5566e-14, gated at 1e-11), not
ball-certified; S itself comes from float SVD/Krylov with G^(-1/2)
retractions, so exact invariance is not available and the residual is a
real property of the exact dyadic objects. Each volume step leaks
Σ_q K₊_q Y_{N−1} D_q† with D_q = (I−SS†)K₀_qS. Our computation: the
certified reduced lower bound crosses the accumulated leak at N ≈
2400–2600 (fails N=2600: 3.06e-11 certified floor vs 9.05e-11 leak;
worse under (1+η)^N propagation). Beyond N ~ 2.5e3 the full-amplitude
zero-free claim is UNSUPPORTED by the certificates as sealed.
REPAIR (either): (a) ball-certify the support-invariance residual in exact
dyadic arithmetic and add a leak-propagation lemma bounding the
accumulated term below the certified floor for all N; or (b) restate the
sealed theorem for the emitted reduced family, which the hash-pinned
objects arguably define. No repair may pick (a) vs (b) by which yields a
preferred downstream number.

## Standing-authority note (both sides should sync on this)
The corpus's own REVIEW_CORRECTION demoted the v001 theorem verdict; the
surviving authority is FROZEN_DYADIC_PERIODIC_ZERO_FREE_AND_LOG_DENSITY_
PROVED — dyadic-time-qualified, log-density only; the connected
linked-cluster density remains explicitly UNPROVED
(periodic_connected_linked_cluster_density_proved = false). The Fable
supervision lane initially reported the pre-correction verdict and has
corrected its records.

## ER-A/ER-B status
The envelope adjudication returned DERIVED_ER_B by exact identities with
the not-output-blind discipline holding — then was superseded within
minutes; the fork is OPEN again and the overnight chain correctly used
disclosed ER-A. Process defects in the gate noted (minor); no silent flag
promotion anywhere (grep-verified by the linkage lane).

## Eta/anchor-defect audit: SOUND
The 2.12e-13-class anchor defect handling verified exactly: triangle
inequality in the right direction, folded exactly once, ~79× slack; one
un-itemized (1+1e-11) isometry factor (~+8e-14) absorbed ~1200× over.
Recommend itemizing it in the next successor for completeness.

## What must happen before the battery evaluator can return anything
other than GATE5_CORE_BLOCKED (the linkage lane's enumeration)
1. Repair the reduced→full bridge (above) or restate scope.
2. Extend (or formally bridge) from the frozen periodic/dyadic Q_spec
   carrier to the battery's declared objects across four axes: layer
   (primitive amplitude, not the Q_spec CTP scalar — the corpus's own
   critical-path correction bars substitution), carrier (beyond the
   three-site regression fixture; the sealed erratum's point), quantifier
   (volume-uniformity over the relayed exhaustion and connected
   cellulations, not N time-supercells of one frozen chain), object
   (reduction of Z_N to the pinned one-handle ratio).
3. The connected linked-cluster density with absolutely summable majorant
   (still open by the corpus's own flag).
4. The Duhamel/intensive-Hessian equality on the discharged objects.
5. Resolve or re-scope the reopened ER fork by exact identities.
Then: the three sealed-role core reports, the independent reconstruction,
and the evaluator run per the pre-staged charters.

## Custody
All checks read-only; no measured constant used; scripts preserved in the
supervision scratchpad; every claim above cites sealed files/lines in the
full lane reports (task record wa3t3i3ee).
