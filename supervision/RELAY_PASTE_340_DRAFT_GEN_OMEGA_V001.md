PASTE 340 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] DRAFT Gen_Omega — THE GENERATIVE ORIGIN RULE, THROUGH THE FULL GATE (DoR-013 RESERVED)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-256)

```text
DOES THE OBJECT EXIST?   COMMISSIONED BY RULING (plan C31): a DRAFT of Gen_Omega -- the
                         generative preparation/carrier-descent rule producing, FROM SHARED
                         PRIMITIVE DATA: (1) the complete source density rho_S, (2) the
                         ready ray, (3) the transition law. The Q-242 spec is the contract;
                         the Q-256 germ V001 (112a6658...) is the consumer awaiting it.
IS THE VERSION CURRENT?  YES through Q-256. BINDING PRIOR RESULTS: Q-240 (at zero history
                         the law EXCHANGES ready and pointer rays -- the law CANNOT fix its
                         own ready state; Gen_Omega must supply the selection from primitive
                         data, NOT from the law); Q-242 (bundling is relabeling; the bar is
                         GENERATIVE: outputs OBTAINED by the rule, not projected); Q-194
                         (role standard: NO unique microscopic origin required).
ARE ITS INPUTS PRESENT?  The ratified finite structures (the law DoR-009 with E_post, F_N,
                         A_N^(p), the Q-239 skeleton), the Q-254 battery, the germ V001
                         consumer interface.
```

## 1. THE TASK — AUTHOR THE ORIGIN, THEN SUBJECT IT TO EVERYTHING

```text
1  DECLARE the primitive data FIRST, before any output: the shared stratum Omega_prim from
   which all three outputs generate. State its fields bare, each on the choice table (what
   it adds, alternatives, void-conditions). THE PRIMITIVES MUST BE FEWER/WEAKER THAN THE
   OUTPUTS -- a primitive stratum that secretly contains rho_S, the ready ray, or the law
   as a field is bundling with extra steps and dies at the countermodel.
2  DEFINE Gen_Omega as EXECUTABLE MAPS: d_state: Omega_prim -> rho_S (complete, all source
   density blocks), d_ready: Omega_prim -> the ready ray, d_law: Omega_prim -> the
   transition law. *** d_law MUST OUTPUT THE RATIFIED LAW (DoR-009, E_post orientation) --
   a rule generating a DIFFERENT law contradicts the ratified record and dies. Deriving
   the ratified law from something smaller is the prize; restate its eight certificates
   as CONSEQUENCES where the generation proves them. ***
3  *** THE MANDATORY KILL-TEST -- THE Q-242 COUNTERMODEL: two distinct admissible source
   states with identical law and carrier defeated every bundled candidate. RUN IT: does
   Gen_Omega DISTINGUISH them -- i.e., does the rule GENERATE exactly one of the two (or
   generate the distinguishing datum)? A rule both states satisfy is bundling in
   disguise and V001 of Gen_Omega dies the same death as its predecessors. ***
4  p_ch, WITH THE TARGET-TUNING FENCE AT MAXIMUM: state honestly whether Gen_Omega
   (a) DETERMINES p_ch from primitive data (exhibit the determination -- structural form
   only, NO numeric evaluation), (b) PARAMETERIZES it (p_ch is a primitive field -- then
   say so plainly: the origin rule inherits the program's one free number), or
   (c) leaves it undefined (a hole -- the draft is incomplete). *** ANY structure chosen
   BECAUSE of its p_ch consequence is target-tuning and dies. The verdict (a)/(b) is
   REPORTED, never SELECTED. ***
5  CERTIFY against the FULL Q-254 battery (all rows, PASS/FAIL/CONDITIONAL, cited) PLUS
   the Q-242 certificates: common-input, covariance, domain, target-independence,
   no-post-output-supplementation. The finite-authority item binds: primitives invisible
   to every finite restriction are non-finite physics at the origin -- flag any.
6  KILL-PASSES ON YOUR OWN DRAFT (Q-207): hidden adoptions; smuggled measure/contour/
   topology; answer-defined membership; "standard practice" fields; and the B14 test
   re-run against YOURSELF: is anything obtained by projection rather than generation?
7  DELIVERABLE: the draft as a PROPOSAL -- primitives table, the three maps, certificate
   table, countermodel verdict, p_ch verdict (a)/(b)/(c), battery table. Nothing TYPE-P.
   DoR-013 is reserved for the principal.
```

## 2. LEADS

```text
IF THE DRAFT SURVIVES INCLUDING THE COUNTERMODEL -- lead with the primitives table and the
p_ch verdict: what the origin is made of, and whether the program's last number is derived
or primitive. THAT SENTENCE IS THE HEADLINE EITHER WAY.
IF THE COUNTERMODEL KILLS EVERY CANDIDATE RULE -- *** LEAD WITH THE OBSTRUCTION: if no
generative rule can distinguish the two states from ANY primitive stratum weaker than the
outputs, that is a NO-GO THEOREM about the program's origin physics and outranks a draft. ***
IF d_law CANNOT REPRODUCE THE RATIFIED LAW from any candidate primitives -- lead with the
exact certificate of the law that resists generation; that names what the law contains
beyond every candidate origin.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
