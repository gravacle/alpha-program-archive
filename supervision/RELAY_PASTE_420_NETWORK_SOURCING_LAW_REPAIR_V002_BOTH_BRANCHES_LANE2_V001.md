## PASTE 420 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] REPAIR V002: THE NETWORK-SOURCING LAW, BOTH DoR-015-FORCED BRANCHES

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-337.
- Your V001 proposal was KILLED by cross-review (OVERALL = KILLED by R7; independently R3, R8). Read the review before writing anything:
  `alpha-program-archive/workspace/STAGE8_TASK4A_NETWORK_SOURCING_LAW_CROSS_REVIEW_LANE1_V001.md`
  SHA-256 `e937869879b4613175c814c6b5a02eabc54607c1f9751a4e758d475ced6de21b`. Verify before reading.
- Your V001 (`87f69626…`) survives on R1/R2/R4/R5/R6: the d_J seam, the exact exponents, the scope boundary, reciprocity, and the tower echo all PASS. The kill is the port-typing of the EMISSION and the TYPE-R promotion of no-ignition. Repair, do not rebuild from scratch.
- Frozen authorities: DoR-015 / FIELD_SIGNATURE_PHYS V005 (scalar physical source dual = ker(B_Nᵀ); open-path content = endpoint-covariant transport; on connected trees ker(B_Nᵀ) = {0}), DoR-014 + Amendments (germ V007), DoR-009 (C1–C8, E_post), Q-335/336/337 (Q-336 carries a correction of record: autonomous no-ignition = NO_VERDICT/TYPE-U).
- Custody: you are the builder repairing your own build (the Door-D pattern). The repair goes back to Lane 1 for re-review; do not self-certify.

TASK — write V002 carrying BOTH branches the typing forces. Derive each as far as DoR-015 pushes it; SELECT NEITHER (choosing a branch because of its outcome is target tuning).

BRANCH A — cycle-constrained scalar emission.
A1. Impose the missing hypothesis explicitly: emitted q ∈ ker(B_Nᵀ). State the projected emission map (project the V001 emitter onto ker(B_Nᵀ), or derive whatever DoR-015 forces instead of projection — justify which).
A2. Derive the tree consequence as a theorem: on any connected tree stage the scalar emission is EXACTLY ZERO (V005 kernel result). State it plainly: two systems joined by open paths alone exchange no scalar drive.
A3. Compute the minimal driving stage: the smallest two-system network whose ker(B_Nᵀ) ≠ 0 (parallel edges / the smallest cycle through both systems — derive it, don't assert it). Recompute the two-system tower NS-17–NS-26 on that stage with the constrained emitter: what are x, Ξ, A_k now? Exact, symbolic p, no rank selected.
A4. The R7 one-edge counterexample becomes a permanent regression test: V002 must emit zero scalar on the one-edge stage. Include the check.

BRANCH B — endpoint-covariant emission.
B1. Rebuild the emission as an endpoint-covariant open-path construction, consumed on the access side (DoR-015: open paths = charge/flux access), with NO claim of scalar-source typing anywhere.
B2. Type the receiving end honestly: which ratified receiver consumes endpoint-covariant data? Check C5's chart typing (the R1-passed seam) — does the d_J seam survive when the object is typed as transport rather than scalar source? If no ratified receiver exists for it, say so: that is a door, not a defect to paper over.
B3. Recompute what survives of the tower on this typing, and state exactly which V001 equations change meaning.

MANDATORY RETYPES (both branches):
M1. No-ignition: NS-27 is the base case only. State the corrected standing verbatim: the identity tower is CONDITIONAL on an externally stipulated all-zero history sequence; AUTONOMOUS_READY_NETWORK_NO_IGNITION = NO_VERDICT / TYPE-U (missing: the port-to-write-history update and a zero-fixed-point certificate). No TYPE-R promotion.
M2. Flag the one-tier delay as AUTHORED, NOT UNIQUELY FORCED (the R5 residue).
M3. Everything PROPOSED_NOT_ADOPTED / PASS_WITHIN_PROPOSAL; p symbolic; no rank selected, no ratio evaluated.
M4. Refresh the self-kill battery: rerun your six attacks against BOTH branches plus the R7 attack; add at least one fresh attack per branch.
M5. Close with a comparison table: Branch A vs Branch B — what each requires, what each forbids, what each leaves TYPE-U — WITHOUT recommending one.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK4A_NETWORK_SOURCING_LAW_ADOPTION_PROPOSAL_CODEX_LANE2_V002.md`
Seal (.seal.sha256 sidecar), mirror both files byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, then STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No evaluation of alpha, K_*, or roots; no comparison to measured constants. Structural mathematics of declared objects is permitted; if a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
