PASTE 329 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] PROVE OR REFUTE THE SEPARATION THEOREM — DOES ANY COMPLETED CONTENT HIDE FROM EVERY FINITE RESTRICTION? (DoR-011)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-245)

```text
DOES THE OBJECT EXIST?   THE STATEMENT does (Q-245): SEPARATION -- the intersection over all
                         finite stages of ker(rho_H,N) on the ratified completion is {0}.
                         If TRUE: no completed content is invisible to every finite
                         restriction, so the falsifier's constraints SEE EVERYTHING and
                         re-entry channel (a) -- the TAIL -- CLOSES.
                         If FALSE: the nontrivial intersection IS the tail space, exactly
                         the room p_ch has to re-enter. Either answer is decisive.
IS THE VERSION CURRENT?  YES: Q-245 (falsifier forces every finite restriction p_ch-free;
                         naive continuous C*-extension REFUTED by a moving-tail sequence --
                         WARNING: that same moving-tail mechanism is the natural candidate
                         for a separation counterexample; test it FIRST).
ARE ITS INPUTS PRESENT?  The ratified completion (DoR-008: C* field algebra, CTP tensor
                         completion, Hilbert C*-module representation, sequential labels with
                         disclosed zero-extension) and the finite restriction maps rho_H,N
                         as instantiated in the Q-238 PathCert / Q-245 cylindrical arrow.
```

## 1. THE TASK — A THEOREM, WITH THE COUNTEREXAMPLE TRIED FIRST

```text
1  STATE the theorem exactly on the ratified objects: define rho_H,N (quote the ratified
   source), the space it acts on, and what ker means there (C*-module: inner-product-zero
   vs representation-kernel -- DISTINGUISH; if the two give different theorems, prove or
   refute BOTH and say which one the falsifier's pinning argument actually needs).
2  *** TRY TO REFUTE FIRST: run the Q-245 moving-tail sequence against separation. Does the
   moving tail (or any weak-*/norm limit of it) produce a NONZERO element killed by every
   rho_H,N? If yes -- SEPARATION FAILS; characterize the tail space COMPLETELY (generators,
   closure, and whether the p_ch-carrying one-point coherent block has nonzero component
   in it). ***
3  IF NO COUNTEREXAMPLE SURVIVES: prove separation from the ratified structure (candidate
   route: cylindrical density Q-245 established -- Q_fin dense in the compact spectrum --
   plus faithfulness of the ratified representation; certify each step and NAME any premise
   beyond ratified data).
4  TYPE the result: a proof is TYPE-P | DoR-008 (+ any named premise); a counterexample is
   TYPE-R against pinning WITH the tail space as its content; neither is NO_VERDICT with
   the exact missing lemma.
5  KILL-PASSES: do not weaken the theorem to a dense subspace where the full statement is
   what pinning needs; do not quietly change topology mid-proof (the Q-245 refutation shows
   topology choice DECIDES existence questions here -- every limit must name its topology).
6  FENCES (DoR-011): construction/proof only. No evaluation, no roots, no numbers.
```

## 2. LEADS

```text
IF SEPARATION HOLDS -- *** LEAD WITH IT: channel (a) CLOSES; combined with Q-243/Q-245 the
completed retarded block is pinned p_ch-free up to the BACKGROUND channel (b) alone. The
deciding question becomes one object: the stationary background. ***
IF SEPARATION FAILS -- *** LEAD WITH THE TAIL SPACE and whether p_ch's coherent block lives
in it. "The tail is exactly <T> and p_ch's component in it is <zero/nonzero>" is the
sharpest available sentence. ***
IF IT SPLITS BY KERNEL NOTION -- lead with the split and which notion pinning needs.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
