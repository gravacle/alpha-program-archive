## PASTE 645 — CODEX LANE 2 (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / SUBGATE] CLOSING CHECK: THE ASSEMBLY V005 — THE DECOMPOSITION AND THE SCOPE

(Same Lane 2 session rules: write and seal in the CLEANROOM only; the registrar mirrors. All process law applies, incl. C-V5a.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-582 (live appends recording 646's outputs are verify-and-proceed).
- Under review: `STAGE8_TASK6_LP_QSPEC_ASSEMBLY_DARIO_V005.md` = `76589e94bb4af318880c61c3d677dc2518add8480100a7afaf675e4dd3a394a8`, vs base V004 `5e46e8f4…` (both in cleanroom and archive workspace). Verify before reading.
- Your conviction (Q-577) and the C-V5a amendment are the standard.
- Its claims (Q-581): seven hunks decomposed to disjoint spans with exact sums; H20 proven NEVER shared (six true shared hunks); the phantom-newline off-by-one owned and corrected (759-26+244=977); the new certificate C-V5a-compliant by construction (20 hunks, one row each); operative content byte-identical to V004 (no hunk in lines 277-598).

TASK — bounded closing check:
U1. **The decomposition:** each of the seven span tables re-verified against the actual V003->V004 diff (spans disjoint; sums exact; rows correctly attributed); the H20 finding re-verified at the bytes (both inserted lines genuinely the renumber).
U2. **The counts:** the 759/977 arithmetic and the stated counting method re-run; the 977-49+214=1142 reconciliation for V004->V005 re-run under declared conventions.
U3. **The scope:** re-diff V004->V005 yourself; zero operative-region hunks; the 18/18 TYPE-U cells and FREEZE block byte-identical.
U4. **Verdict** under the VERDICT-LINE SCOPE RULE; if clean, say STANDS plainly.

OUTPUT: one artifact, lane-tagged filename, written and sealed IN THE CLEANROOM
`STAGE8_TASK6_ASSEMBLY_V005_CHECK_LANE2_V001.md`
with final lines: `DECOMPOSITION = CONFIRMED (+H20 verdict)`, `COUNTS = CONFIRMED / (+items)`, `SCOPE = CONFIRMED untouched`, `ASSEMBLY_V005 = STANDS / NEEDS_V006 (+reason)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in either location, STOP and report — do not overwrite. Seal in the cleanroom, report both hashes, STOP (the registrar mirrors). No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
