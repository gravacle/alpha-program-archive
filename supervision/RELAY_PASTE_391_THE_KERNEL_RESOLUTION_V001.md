PASTE 391 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE KERNEL RESOLUTION — HOW THE DYNAMICS ACTS WHERE THE RESPONSE LIVES (THE VERDICT'S TRUE GATE)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-308)

```text
DOES THE OBJECT EXIST?   NO -- and it is now THE gate (Q-308): the physical cycle current
                         u_sq lies in ker L, so the response's own sector is invisible to
                         G^007's rank-one structure. The named package: the kernel
                         resolution (the dynamics ON ker L), the stationary 2PI blocks,
                         the completed domains, the physical background realization.
IS THE VERSION CURRENT?  YES through Q-308. Marks: TYPE-P | premises: the full ratified
                         stack. The refusal discipline binds: nothing that deletes
                         record-visible content; the [q(1-q)]^-1 quotient reciprocal is
                         DIAGNOSTIC ONLY.
ARE ITS INPUTS PRESENT?  YES: G^007 and its exact kernel structure, the cycle-current
                         family (V005: u_c, c in ker(B^T)), the ratified law's dynamics
                         (F_N pure phase -- the dynamics DOES act on cycle content: Z_N
                         IS a cycle object), the stationary-Schur ledger account, the
                         signature's structures, the finite references.
```

## 1. THE TASK — RESOLVE THE KERNEL, THEN THE VERDICT STANDS OR WAITS

```text
1  THE KERNEL RESOLUTION: characterize the dynamics' action ON ker L -- the sector where
   the physical currents live. THE RATIFIED DYNAMICS IS NOT SILENT THERE: Z_N is a cycle
   phase (the influence functional's product IS cycle content), and the dephasing weight
   1 - p + p e^{-Q/2} (Q-279, probes on) is the law's own cycle-sector response. Build
   the kernel-sector response structure from the RATIFIED finite dynamics lifted through
   the (now-existing) chain: what does the second variation of the law's own cycle
   functional look like at the physical level? THIS -- not G^007's rank-one part -- is
   the candidate physical RetHess on the response sector.
2  THE STATIONARY 2PI BLOCKS: assemble the two-sector structure (ker L and its
   complement) as the Schur ledger prescribes: the complement carries G^007's q(1-q)
   weight; the kernel carries the cycle-sector response from (1); the 2PI mixing blocks
   between them COMPUTED, not assumed zero (a nonzero mixing is exactly how p could
   enter the kernel sector -- the verdict rides on this block; compute it exactly at
   the finite level first, where everything is exact, then lift).
3  DOMAINS AND BACKGROUND: the completed domains for the two-sector structure; the
   background realization at the level this needs (the seed + the anchored state's
   forced form; Q-281's fiber machinery).
4  *** THE VERDICT, IF THE STRUCTURE CLOSES: with the two-sector RetHess assembled --
   kernel sector (the physical response), complement (the q(1-q) noise), and the mixing
   computed -- TRACE p SYMBOLICALLY. THE QUESTION: does p appear in the KERNEL-SECTOR
   response (the physical coupling's home)? The finite evidence says the retarded
   mixing vanished at every stage (Q-243/Q-279) -- if the physical mixing block
   inherits that zero, p stays confined to the noise sector and CANCELLATION IS THE
   VERDICT with the mechanism exact: the response lives where the noise weight has no
   support. If the mixing is nonzero, p's entry is exactly that block -- state its
   form. ***
5  RESTRICTION at every stage (Q-243/Q-279 exact); six-account rows; door flags; no
   deletion of record-visible content; no selection; no evaluation.
DELIVERABLE: the kernel resolution + 2PI blocks + domains + background at the level
built, and THE p-VERDICT with its mechanism if the structure closes -- or the exact
remaining object if it does not.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
