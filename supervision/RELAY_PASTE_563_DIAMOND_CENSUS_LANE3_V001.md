## PASTE 563 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5 / EQ6] THE DIAMOND CENSUS: WHERE DOES THE ACTUAL TOWER DEMAND THE COEFFICIENT FACE?

(Same Lane 3 session rules as before; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-489.
- Your own artifact: `alpha-program-archive/workspace/STAGE8_TASK5_EQ6_J7_FACE_AND_J2_DISPLAY_LANE3_V001.md`, SHA-256 `9c74ed3dbe748450e525102128b0367ce21c0e5029929eab33a111ca9802dc3c` — the face is BUILT on A_RP⁺ and OBSTRUCTED(O1,O2) beyond it. This relay decides whether the obstruction MATTERS on the actual tower.
- Authorities: the membership build's I_F enumeration (`0d00c9cf…` M1 — noting its D1 defect: the consumer arrows of guard clause (vi) must be INCLUDED in your census, since membership V002 will add them), the diamonds artifact (`5539b53d…` — the actual common-refinement diamonds), the guard (`a681c784…` clause (vi)), the assembly chain (`a46beae2…`, `9bf34e27…`).
- SYMBOLIC ONLY: classify m_T at each stage as ZERO / NONZERO / UNDETERMINED by structural argument (harmonic sector present or absent — cycle content), never by numeric evaluation. No values, no magnitudes.

TASK:
N1. **Enumerate the census domain:** every common-refinement diamond of the actual I_F — INCLUDING the consumer arrows clause (vi) requires (enumerate them explicitly; this simultaneously discharges Dario's D1 for the coming membership V002) — each diamond's four stages and four arrows listed with their classes (A_RP⁺ / A_RP with an m_T = 0 end / A_CC old-image / new-cycle-only positions).
N2. **Classify every position:** for each stage in each diamond — m_T ZERO or NONZERO by cycle-content argument (acyclic ⇒ no harmonic sector ⇒ ZERO; cycle-carrying ⇒ argue presence structurally); for each face position — is the coefficient face DEMANDED there by the completion theorem's contract, and if demanded, is it FORMABLE (within the built A_RP⁺ scope) or UNFORMABLE (O1/O2 territory)?
N3. **The census verdict:** exactly one of — (a) VACUOUS OBSTRUCTION: no actual diamond demands the face where it cannot exist (state the proof: every demanded position lands in A_RP⁺); (b) DEMANDED-UNFORMABLE positions exist: list each exactly with which obstruction (O1 or O2) bites and what the diamond needs there.
N4. **Honesty and battery:** no diamond skipped (state the count and the completeness argument against the assembly chain's own diamond list); anti-tuning ledger; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_DIAMOND_CENSUS_LANE3_V001.md`
with final lines: `DIAMOND_COUNT = n (+consumer arrows enumerated)`, `CENSUS = VACUOUS_OBSTRUCTION / DEMANDED_UNFORMABLE (+positions)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
