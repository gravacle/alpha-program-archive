PASTE 341 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4d] THE STITCHING THEOREM — STATE IT EXACTLY, PROVE THE GERM-INDEPENDENT HALF (THE P7 PATTERN)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-257)

```text
DOES THE OBJECT EXIST?   THE OBLIGATION does (plan Task 4d, created at C11 with DoR-007:
                         the smooth fork is resolved by DERIVING THE LIMIT, never by
                         choosing). The THEOREM does not: no derived stitching/continuum
                         rule exists anywhere in the corpus, and its absence caps every
                         finite-cell route in advance. Task 4d is the discrete-to-continuum
                         equivalence theorem.
IS THE VERSION CURRENT?  YES through Q-257. NEW MACHINERY SINCE 4d WAS WRITTEN: the ratified
                         completion (DoR-008), cylindrical density (Q-245), the retraction
                         system and separation theorems (Q-247), modulo-tail determinacy
                         (Q-250), the P7 certificate with the class-transport lemma (Q-257).
                         4d was authored BEFORE all of this existed -- the theorem may be
                         far more provable now than when it was queued.
ARE ITS INPUTS PRESENT?  The sealed smooth-fork texts (DoR-007 basis), the plan's 4d entry,
                         the derived discrete connection (the Q-270-era results: record-side
                         connection DERIVED, discrete Gate-4 scope), and the ratified
                         completion machinery above.
```

## 1. THE TASK — STATE, PARTITION, PROVE (THE P7 PATTERN EXACTLY)

```text
1  STATE the stitching theorem precisely: WHAT discrete structure (the derived record-side
   connection and its finite-cell data), WHAT continuum object it must equal/converge to,
   IN WHICH topology (Q-245/Q-247 discipline: every limit names its topology), and WHAT
   "equivalence" means claim by claim. Source the statement from the plan's 4d entry and
   DoR-007's own words -- quote both. If the sealed texts underdetermine the statement,
   enumerate the candidate statements and carry each (family-then-freeze).
2  PARTITION every claim: PROVABLE-NOW (from the ratified completion machinery -- expected
   material: sequential N <= M zero-extension gives the directed system; cylindrical
   density gives the embedding; retraction naturality gives coherence of the finite
   stages) vs INTERFACE-CONDITIONAL (needs a named germ/P2-P6 field -- take as premise,
   the P7 move) vs BLOCKED (needs an object that does not exist even as interface --
   name it; O-D3 territory).
3  PROVE the provable-now half with certificates. The expected shape: the DISCRETE side
   assembles into a directed limit INSIDE the ratified classes (where separation holds);
   what "the continuum" then IS, is the limit object ITSELF -- derived, not imported.
   *** IF THE LIMIT OBJECT EXISTS IN THE RATIFIED CLASSES, THE SMOOTH FORK'S "DERIVE THE
   LIMIT" RULING IS PARTIALLY DISCHARGED BY CONSTRUCTION -- say exactly which part. ***
4  THE FOUR FAILED SMOOTH-BRIDGE ATTEMPTS ARE NOT RE-RUN: this is the LIMIT direction
   (discrete -> continuum by derivation), not the patch/cover/functor direction that
   failed four times. If the limit direction ALSO hits one of those four walls, name
   which wall and why the new machinery does not pass it -- that is a finding.
5  KILL-PASSES: no smooth domain supplied from the adopted (M,g) (its price stays
   unpaid); no continuum rule imported from convention; no topology switch; the moving-
   tail hazard (Q-245) checked at every limit step -- a limit that only exists in the
   bidual is NOT a derived continuum, it is the tail wearing a hat.
6  FENCES (DoR-011 discipline): construction/proof only. No evaluation, no roots, no
   numbers, no comparison to measured constants.
```

## 2. LEADS

```text
IF THE DIRECTED LIMIT EXISTS IN THE RATIFIED CLASSES -- *** LEAD WITH IT: the program
would have its FIRST derived continuum object, the stitching cap lifts, and DoR-007's
fork discharges by construction. State exactly what remains conditional. ***
IF THE LIMIT NEEDS ONE INTERFACE FIELD -- lead with the field; 4d then discharges on the
same germ ratification as everything else, and say so.
IF THE LIMIT ONLY EXISTS IN THE BIDUAL -- *** LEAD WITH THAT: the continuum would be
tail-typed, colliding with finite authority at the geometry level -- the principal must
see that sentence exactly. ***
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
