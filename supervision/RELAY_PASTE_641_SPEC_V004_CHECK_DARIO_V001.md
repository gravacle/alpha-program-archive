## PASTE 641 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / SUBGATE] BOUNDED CHECK: THE SPEC V004 — BR CONFORMANCE AND THE CLOSED LIST

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. All process law applies.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-576 (the spec V004, registered).
- Under review: archive `STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE2_V004.md` = `2c767bfc953c7efeeaf4a33542974b10e0a674a161a5f1a651f3486ac36fad8b`, vs base V003 `f910815c…`. Verify both before reading.
- Your own closed list and law texts: `STAGE8_TASK6_SPEC_V003_CLASS_CHECK_CORRECTION_DARIO_V001.md` = `1f588534…`.
- Its claims (Q-576): BR-1/BR-2 installed as law with named conformance tests; both sweeps zero failures with transcripts; closed list 11/11 per your repair column; 58 descriptors untouched; protected hashes unchanged; carriage 26/350/86.

TASK — bounded, this is a LIST check, not an open sweep:
D1. **The law installation:** BR-1/BR-2 texts faithful to your statements (no weakening — especially the "carries a criterion's direction" scope and the "not a truth value" rule); the conformance tests genuinely mechanical (a reviewer can run them from the spec alone); RUN BOTH YOURSELF over the final file and report your own transcripts against the claimed zero failures.
D2. **The closed list 11/11:** each repair against your own per-item repair and verification columns — especially V009-04 (the directional predicate restored; `r_distinct_x`/`r_one_x` genuinely spec-forced, no producer object carrying direction) and MR-07 (the SYMBOLIC paired). Any repair that deviates from your column is a finding even if plausible.
D3. **The 58 carried descriptors:** confirm by diff scope that no row outside the closed list changed (any out-of-list change = finding, per the relay that built it).
D4. **Protected sections + carriage:** pinned hashes recomputed; your own re-diff under the declared conventions against 26/350/86.
D5. **Battery + verdict** under the VERDICT-LINE SCOPE RULE: state your coverage exactly.

OUTPUT: one artifact, lane-tagged filename, written and sealed IN THE ARCHIVE WORKSPACE
`STAGE8_TASK6_SPEC_V004_CHECK_DARIO_V001.md`
with final lines: `BR_LAWS = FAITHFUL / (+items)`, `BR_SWEEPS_RERUN = 0/0 confirmed / (+failures)`, `CLOSED_LIST = 11/11 CONFIRMED / (+items)`, `OUT_OF_LIST_CHANGES = none / (list)`, `PROTECTED+CARRIAGE = CONFIRMED / (+items)`, `SPEC_V004 = STANDS / NEEDS_V005 (+reason)`, `BUILD_READY = yes/no (spec readiness only)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists, STOP and report — do not overwrite. Seal in the archive workspace, report both hashes, STOP (the registrar mirrors). No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
