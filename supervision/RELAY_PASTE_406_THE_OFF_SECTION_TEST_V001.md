PASTE 406 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE OFF-SECTION TEST — THE FLAT-GERM FAMILY AGAINST THE SEALED FINITE DATA WHERE IT IS VISIBLE (C40's COMPANION)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-323 + C40)

```text
DOES THE OBJECT EXIST?   THE TEST does, created by Q-323's refutation: flat transverse
                         germs are FINITE-VISIBLE away from the active section (the
                         reviewer's invisibility claim was TYPE-R). C40 commissions this
                         test as the amendment's companion: the cycle sector's physics
                         TESTED, not assumed. The Q-52 spec is now complement-scoped
                         WITH the standing void clause -- respect it (nothing here may
                         invert on a cycle direction).
IS THE VERSION CURRENT?  YES through Q-323 + C40.
ARE ITS INPUTS PRESENT?  YES: V003's explicit flat-germ construction (ef6f151d... -- the
                         family and WHERE it is finite-visible off-section), the sealed
                         finite closed forms (Q-243/Q-279: the dephasing weight, kappa_eta,
                         the probe tables -- exact at every point, not just jets at the
                         reference), the off-section restriction machinery (Q-315's
                         squares; P2's dense core covers off-section configurations).
```

## 1. THE TEST — SEALED DATA VS THE FAMILY, WHERE THE FAMILY SHOWS

```text
1  MAP THE VISIBILITY: from V003's construction, characterize exactly WHERE off the
   active section the flat family produces nonzero finite shadows, and WHICH sealed
   finite quantities live there (the closed forms are global in their arguments -- the
   dephasing weight at arbitrary Q, kappa_eta at arbitrary eta, the probe tables at
   arbitrary sources; extract the off-section values EXACTLY).
2  RUN THE COMPARISON: does the sealed off-section finite data ADMIT a nonzero flat
   germ? Three outcomes, each decisive:
   KILLED -- the sealed values contradict every nonzero member: the cycle action is
   ZERO by the falsifier, the topological reading is DERIVED, and the last authored
   field collapses to a theorem;
   FIXED -- the sealed values select a unique member (or a family parameterized only
   by the rank pair): the non-perturbative reading is DERIVED with its content named
   -- the principal's divergence thread lands as sealed fact;
   BOUNDED/OPEN -- the sealed data constrains but does not determine: state the exact
   surviving space and what data WOULD close it.
3  THE RELEVANCE COROLLARY: for whichever outcome, state what the alpha-facing verdict
   chain consumes from the cycle sector -- with the complement-scoped spec, does ANY
   consumer read the surviving content? (The Q-251 factorizations, pointed at this
   one sector.)
4  KILL-PASSES: the comparison EXECUTED on exact closed forms (no jets-only shortcut --
   that mistake died at Q-321); no inversion on cycle directions (the void clause);
   no selection; no evaluation of couplings; door flags.
DELIVERABLE: the visibility map, the comparison verdict (KILLED / FIXED / BOUNDED),
the relevance corollary, and -- if KILLED or FIXED -- the last authored field's status
rewritten by theorem.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
