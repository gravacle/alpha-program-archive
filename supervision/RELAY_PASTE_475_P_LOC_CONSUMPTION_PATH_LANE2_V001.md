## PASTE 475 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4b→5] THE p_loc CONSUMPTION PATH: THE REGISTERED QUESTION'S OWN ROUTE

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-396. The state: the completed operator-response representative is PHYSICAL INPUT (NOT_INVARIANT, witness pair — `f0f8b09b…`); the finite limit is canonical, zero, p-free (`337769f4…`); the registered deciding question K = B_ind(K) = p_loc[Π_R,ind[G_K]] consumes through p_loc, whose consumption path is TYPE-U (Q-395).
- Authorities (verify hashes): the seam audit `337769f4…` (the R5/B_ind/ΔΦ value path it mapped), the witness artifact `f0f8b09b…`, the confirmed computation `27790d53…`, square V004 `abf6d366…`, metric V005 `2a379098…`, and the ratified corpus definitions of p_loc, Π_R,ind, and B_ind (sweep the register and the sealed corpus for their defining artifacts — cite exactly; the core equation is the program's oldest object).
- Custody: derivation. THE LAST FORK: either outcome is the p-verdict's route. p, ν symbolic; no representative selected; no numeric evaluation.

TASK:
Q1. **The consumption inventory.** From the ratified definitions: what does p_loc[Π_R,ind[G_K]] actually consume? Type the chain G_K → Π_R,ind → p_loc exactly: which object of the response architecture is G_K (finite? completed? which sector?), what Π_R,ind projects/induces, what p_loc localizes — every step cited to its defining artifact.
Q2. **THE FORK, decided by derivation if the definitions decide it:** (a) the chain factors through the FINITE/invariant content (the canonical limit; the sealed finite towers) ⇒ the registered question discharges at the finite level — state exactly what the finite theorems then give for the fixed point's p-dependence; (b) the chain consumes the COMPLETED representative ⇒ the completion fiber is inside the registered question — type the fiber and what a ratification would involve; (c) the definitions underdetermine the consumption ⇒ state the exact gap as the next object (do not force).
Q3. **The identification theorem's role.** Whichever way Q2 falls: does the missing finite↔completed identification theorem (Q-395) change the answer, and is it derivable on the consumption path's actual carrier?
Q4. **Falsifiers:** every claim's finite check run; the minimal stages exercised; all standing regressions.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK4B_P_LOC_CONSUMPTION_PATH_LANE2_V001.md`
with a lead-result block (`CONSUMPTION = FINITE_FACTORING / COMPLETED_CONSUMING / UNDERDETERMINED (+gap)`; and if (a): the registered question's finite discharge stated exactly), the register-sweep list, and the Q1–Q4 chain. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No numeric evaluation; no registered verdict; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
