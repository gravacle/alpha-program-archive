# Supervision review — 4-lane hostile panel on the Complete Causal Superconnection Parent spec
Date: 2026-07-24 (evening) · Reviewer: Fable panel (analytic / principles / fidelity / anti-numerology, fresh contexts, max effort)

## Headline

All four lanes: **CHALLENGE** — and all four *independently* predicted
`FINITE_CAUSAL_PARENT_DERIVED_CONTINUUM_COMPLETION_OPEN` as the honest
outcome, which is precisely the verdict Codex's own self-review downgraded
to before sealing. The panel therefore **ratifies the sealed verdict** while
finding real spec-level gaps the self-review did not name. None of the
findings invalidates a sealed numerical claim; they are obligations for a
V002 spec erratum and preconditions for Stage 7/Stage 10.

The panel also independently *explains* the continuum failure from first
principles — see Finding A. What Codex found empirically, the panel derives
structurally. The two lanes agree without communication; that is the
strongest possible confirmation that the downgrade was not excess caution
but mathematical necessity.

## Findings ranked by convergence and consequence

### A. (analytic, major) The continuum gap IS Shale–Stinespring
S_n anticommutes with every α_j, hence with h_0 — so the cell term is
PAIR-CREATING relative to the very P_± splitting that defines the asymptotic
quasifree state. The intermediate-time Hilbert–Schmidt integral diverges
linearly in 3d (classic external-field Dirac phenomenon): the interpolating
dynamics is NOT unitarily implementable in the ω_0-GNS representation. Only
the completed S-matrix survives implementability — and only because the C²
diamond envelope buys (E_p+E_q)⁻⁴ decay. Consequences: (i) this is the
structural reason the source-inclusive limit failed and the record-GNS gate
was the right (indeed only) continuum route; (ii) the parent result's
intermediate-time statements must be tagged algebraic-level, not
vector-level; (iii) a V002 should add the implementability obligation
explicitly. Note the pleasing detail: the DERIVED envelope's smoothness is
exactly what saves asymptotic implementability — the measure theorem is
load-bearing for the analysis.

### B. (3 lanes, major; convergent) The admitted incoming-excitation class is unpinned
"Every durability theorem must hold for the complete declared class" — but
the class is never defined. Three independent failure modes flagged:
post-hoc narrowing (vacuates the theorems), quantifier-order retrofit on the
(T,δ) statement (the uniform reading is provably FALSE via time-translated
conspiracies ψ = e^{+ih_0T'}·root — so the per-state asymptotic reading must
be sealed as THE reading), and a separate-selection risk if exclusions are
added after outcomes are known. **Required: seal the class definition and
quantifier order before any gate consumes the (T,δ) theorem.**

### C. (2 lanes, major/blocking) The envelope realization is genuinely ambiguous
Two pinned-consistent readings of "the uniform diamond measure as
interaction": (i) time-marginal as envelope (v_c = τ_R·w — what this spec
and the durability gate used) vs (ii) uniform spacetime interaction density
at constant rate (what SP08 and the endpoint-compatibility derivations
effectively used). These give DIFFERENT parents, and the difference is
load-bearing: profile-independence was proven only for the isolated write;
with the always-on tail the transport gate proved profile DOES matter. The
spec's self-check ("verify the envelope is the pushforward") cannot
discriminate — as literally worded it is ill-posed (v_c is τ_R × the
pushforward density, not the pushforward). **Required: a derivation
selecting (i) or (ii) from the classified measure + a corrected two-clause
check (shape and amplitude), or an honest new-premise declaration.** This is
the panel's only finding rated blocking by a lane.

### D. (2 lanes, major) The kernel's Lorentzian i-factor is unpinned provenance
The covariant kernel iγ⁵C_K imports a reality factor from the Schur-pole
derivation, which is NOT in the pin table; it flips the sign of the C²
descendant relative to the pinned repair file's square, and the pinned CPT
authority selects the un-rotated quadrature. **Required: pin the derivation
or add a CPT obligation on the assembled parent.**

### E. (analytic, major) Solution concept: mild propagator only
Sharp characteristic-function support makes V(t) norm-discontinuous and
H¹-non-invariant: no strongly-differentiable propagator exists; the unique
object is the mild/Duhamel propagator, and durability commutators must be
phrased in integrated Heisenberg form. The closure-face term exists only as
a quadratic form (trace theorem) — "strong limit" is the wrong topology; the
right pair is strong propagator limit + form limit for the square. The
feared arbitrary-boundary-extension risk is actually LOW at first order
(bounded perturbation) — the verdict tree guards a phantom while the real
tags go unnamed.

### F. (2 lanes, major) Verdict-boundary porosity + unoperationalized carry
Verdict 2 ("continuum completion open") can absorb flat-branch durability
FAILURES that should hit BLOCKED; the SP08/09/14 closure-gate carry is
claimed in the pin table but no verdict condition references it; the spec
lists 7 negative controls where the closure gate mandates 13.

### G. (anti-numerology, major; forward-looking) Cell placement/packing is the surviving alpha-adjacent dial
The admissible cell-complex class (how diamonds pack/tile) is underived and
absent from the fixed-scope fence. Downstream, packing density multiplies
response — this is the one dial that could still be turned toward a target
at Stage 10. **Required: either derive the packing class or add it to the
fence with a Stage-10 prohibition.** Also fence: physical source mass and
pole residue (two alpha-adjacent precursors currently unfenced).

### H. (principles, major) PSC's exhaustion-inequivalence falsifier has no test
"Different admissible exhaustions give inequivalent locally normal limits"
would falsify PSC — no obligation exercises it. Cheap to add: two exhaustion
orders, compare limits.

## What is already mooted by later sealed artifacts
The GNS-completion gate's coherent-net design (no fixed-space operator
limit) is exactly the correct response to Finding A — sealed before the
panel returned. Findings B, C, D, E, F, G, H remain live for V002/Stage 7.

## Clean bills
All lanes: 15/15 pinned hashes byte-verified; c_c, τ_R, w, dressed-
monomorphism carry, no-bare-F² all verified by direct computation; status
flags correctly false; NO measured-constant leak route found by any lane;
the ordinary-local-sum sidestep of the underived transport rule ruled
legitimate and predeclared (principles lane, explicitly).

## Recommended relay to Codex (via Brian)
Draft a `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_ERRATUM_V001` (or
V002 spec) that: (1) adds the Shale–Stinespring implementability obligation
and algebraic-vs-representation tags on obligations 1–5; (2) seals the
admitted-excitation class and the per-state (T,δ) quantifier order; (3)
resolves the envelope realization (marginal-envelope vs uniform-density) by
derivation from the classified measure, with the corrected two-clause
pushforward check; (4) pins the Schur-pole i-factor derivation or adds a
CPT obligation; (5) declares the mild-propagator solution concept and
form-limit topology; (6) tightens verdict boundaries and operationalizes
the SP08/09/14 carry + full 13 negative controls; (7) adds the
exhaustion-inequivalence test; (8) adds cell-packing, source mass, and pole
residue to the fixed-scope fence with Stage-10 prohibitions. None of this
reopens sealed numerics; all of it must precede Stage-7 SPEC-SEAL.
