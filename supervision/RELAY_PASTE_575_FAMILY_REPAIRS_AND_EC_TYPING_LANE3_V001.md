## PASTE 575 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5 / EQ6] FAMILY V003 DISPLAY REPAIRS + TYPE E_C, THE UNTYPED DECIDING OBJECT

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-499.
- Your family V002 (`c4826773…`) was reviewed DEFECTIVE(+3) with the verdicts intact. Read the review first: `alpha-program-archive/workspace/STAGE8_TASK5_EQ6_LOC_FAMILY_REVIEW_DARIO_V001.md`, SHA-256 `9c232579d2953f3810ecd0b725555076be8820a99c3008e7f06ac06540f189ea` — especially K1 (the two display defects) and K2/K6 (the E_C second route: restoring [E_C,Δ]=0 derives m_T ∈ im(P_H) ∩ ker(E_C), so threading requires E_C,RL·c_RL = 0; your §2.6 separation model set E_C,M = 0 by fiat and cannot test it; your own line 862 wrote the intersection without carrying it to the blocking quantity).
- THE RECORD-FIRST RULE APPLIES (LOCKED_PROCESS): E_C,RL·c_RL is a value-like quantity — open with the determination question, not a proof demand.
- Custody: builder repairs; Dario reviews. Tag PROVABLE / PART-PROVABLE / YOURS.

TASK:
Z1. **Family V003, display repairs:** the §1.2 rendering of (S21) restored in full (the [E_C,Δ]=0 conjunct back; the [PROVABLE] tag corrected to the true scope); the (C1b) ledger carrying the void-12 deletion at its point of use; the §2.6 enumeration extended to include the E_C route as a second vanishing mechanism; nothing else changed.
Z2. **TYPE E_C,RL·c_RL — determination first:** does the sealed stock decide whether the contact projector annihilates the loop cycle? Walk: the A4 pushout structure (what E_C IS on the actual carrier), A5's reducing law (E_C commutes with Δ — what does that force on the harmonic line?), the sealed contact/Ward data at the reciprocal stage. Outcomes: DERIVED (either value — display it; note E_C·c = 0 makes threading depend only on the pairing again, E_C·c ≠ 0 makes the E_C route live), AUTHORABLE (gate candidate), or END_TEST (typed per the K5 criterion: is it produced or consumed by the loop?). NOTE: A5 gives [E_C, Δ] = 0, hence [E_C, P_H] = 0 by functional calculus — E_C restricted to the harmonic line is a projector ON that line; at the reciprocal stage the harmonic line is spanned by c_RL — so E_C·c_RL ∈ {0, c_RL} (the restriction is 0 or identity on a one-dimensional space): DETERMINE WHICH, from whether c_RL's class lies in the contact subspace — a structural fact of the sealed contact split. This may be fully derivable; display the computation either way.
Z3. **The consequence:** the threading question's deciding-data board updated (E_C branch resolved or typed; the pairing branch as it stands); what this hands 574's convergence adjudication.
Z4. **Battery:** F_PLDEC; anti-tuning; self verb audit (the review's standard: verbs matched to displays).

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_LOC_FAMILY_V003_AND_EC_LANE3_V001.md`
with final lines: `DISPLAY_REPAIRS = DONE`, `E_C_ON_LOOP = DERIVED_ZERO / DERIVED_IDENTITY / AUTHORABLE / END_TEST (+basis)`, `THREADING_BOARD = updated`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
