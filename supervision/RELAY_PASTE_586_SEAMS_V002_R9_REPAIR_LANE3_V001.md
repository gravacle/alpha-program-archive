## PASTE 586 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5] SEAMS V002: THE (R9) REPAIR AND THE ε=0 CARRIAGE TYPING

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-512.
- Your seams build (`5de94e16…`) was reviewed: S1 SOUND (re-derived), but (R9) DEGENERATE as rendered. Read the review first: `alpha-program-archive/workspace/STAGE8_TASK5_SEAMS_REVIEW_DARIO_V001.md` (hash in its sealed sidecar; verify) — especially the (R9) two-member computation and "the build's own third option."
- Custody: builder repairs its own rendering; Dario reviews. Bounded delta.

TASK:
T1. **The (R9) repair:** rewrite the identification falsifier in the non-degenerate form — the comparison per common member / covariant over the orbit (the third option your own build stated), NOT universally over Triv_[a]; display the two-member computation against the repaired form (it must no longer force p = 0); verify A8's text is honored (the falsifier compares the two routes' returns, quantifying over nothing A8 does not name).
T2. **The ε=0 carriage typing:** q_T is family-dependent — type A7's carriage condition on the ε=0 branch (well-defined via the orbit-covariant class, or the exact obstruction stated); the ε=1 branch's well-definedness displayed for contrast.
T3. **Bounded delta board; battery; self verb audit.**
Final lines: `R9 = REPAIRED (+the non-degenerate form)`, `EPSILON0_CARRIAGE = TYPED / OBSTRUCTED (+statement)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_COMMON_SEAMS_LANE3_V002.md`
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
