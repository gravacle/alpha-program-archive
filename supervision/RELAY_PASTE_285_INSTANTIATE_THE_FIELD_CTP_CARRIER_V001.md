PASTE 285 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2b] INSTANTIATE THE FIELD/CTP CARRIER AND JOIN IT

*** READ `alpha_supervision/LOCKED_PROCESS.md`. THE FOUR STEPS THERE ARE REVIEWER STEPS — SEAL,
MIRROR, REPORT, STOP. TYPING PER DoR 006 (TYPE-P available). ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-202)

```text
DOES THE OBJECT EXIST?   As a SCHEMA only: v004's formal H_CTP,phys, J, R. Q-201 built everything
                         else: source CAR/GNS, finite source-record algebra, outgoing-record
                         algebra/GNS -- ALL INSTANTIATED, frozen as an exact tuple.
IS THE VERSION CURRENT?  YES -- Q-201 (f64d12de...) is the newest word and this relay continues it.
ARE ITS INPUTS PRESENT?  The finite half yes; the field half is THE BUILD.
```

## 1. WHY THIS IS THE ONE WALL

**Three consecutive returns stopped at the same missing object class:**

```text
Q-200  P5 exclusion: census rows are SCHEMAS, no instantiated packages
Q-201  C0: the FIELD/CTP sector has no instantiated carrier
Q-202  multiplier c: the physical family needs the COMPLETE CTP AMPLITUDE -- uninstantiated
```

*** ONE INSTANTIATION JOB UNBLOCKS THREE STOPPED FRONTS. Q-201's own would-build list is the spec: ***

```text
1  instantiate the FIELD/CTP CARRIER and JOIN it to the existing frozen tuple
2  field/CTP GENERATORS AND RELATIONS, certified
3  ONE JOINT REPRESENTATION extending the source and record sectoral ones
4  ONE INVARIANT COMMON DENSE DOMAIN for every unbounded physical operator
5  EXPLICIT FORWARD/BACKWARD CTP EMBEDDINGS into the joint carrier, with source maps
-> then d_C0, with the frozen trace and provenance witness
```

## 2. THE TASK

**2.1 BUILD ITEMS 1–5 IN ORDER, AS INSTANCES.** *** THE Q-200 RULE IS BINDING: INSTANTIATED OBJECTS,
NOT SCHEMAS. An item you cannot instantiate is reported TYPE-U with its exact missing input — DO NOT
PRESENT A SCHEMA AS AN INSTANCE. ***

**2.2 START FROM WHAT EXISTS.** Q-201 froze the exact Gate/source/record tuple. *** DO NOT REBUILD
IT. JOIN TO IT. *** The two closed identity traps stay closed: Gate 3's vertex-chain C_0 is NOT
package C0; charge-conjugate doubling is NOT CTP branch doubling.

**2.3 THE FIELD CARRIER'S PROVENANCE MATTERS AS MUCH AS ITS EXISTENCE.** For each instantiated item,
type its standing: derived-from-record-structure / adopted / imported. *** A FIELD CARRIER ADOPTED
FROM ORDINARY QFT IS AN ADOPTION AND MUST SAY SO — the re-import entrances are live, and DoR 007
rules out supplying continuum structure from (M,g). THE CARRIER HERE IS THE ALGEBRAIC/CTP ONE; it
does NOT require base-smoothness (Q-199: continuum obligations bite later, at Task 4d). ***

**2.4 EXCLUSIONS HOLD.** No state, dynamics, quotient, measure, effects, contacts, Ward data,
inverse results, or U1 conventions inside C0. No post-output supplementation.

## 3. LEADS

```text
IF ALL FIVE INSTANTIATE -- *** LEAD WITH C0 + d_C0 COMPLETE. The package's first component would
exist and THREE stopped fronts (P5 packages, the multiplier family, C0) reopen. Type every item's
standing. ***
IF SOME INSTANTIATE -- lead with the split: which are instances, which stay TYPE-U, and the exact
missing input of each.
IF THE FIELD CARRIER CANNOT BE INSTANTIATED WITHOUT AN ADOPTION -- *** LEAD WITH THE ADOPTION AND
ITS PRICE, PRECISELY. That is a principal's decision and it must arrive as one. ***
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
