PASTE 337 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] p_loc ON THE TAIL — DOES "LOCAL" MEAN FINITE-SHADOWED? THE ONE-CERTIFICATE DERIVATION (DoR-011)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-253)

```text
DOES THE OBJECT EXIST?   THE CERTIFICATE is named, unbuilt (Q-253): p_loc|Tail_ind = 0.
                         At fixed background and fixed p_loc, B_ind is tail-blind IFF this
                         holds. Alpha's entire tail exposure is this ONE functional on this
                         ONE subspace.
IS THE VERSION CURRENT?  YES: Q-247 (Tail = M_cyl(Y)^perp = z_tail B**, killed by every
                         finite restriction; separation holds on norm/module/left-multiplier
                         classes), Q-251 (p_loc's consumption unspecified IN GENERAL),
                         Q-253 (the reduction; B_ind = p_loc[Pi_R,ind[G_K]], completed-layer).
ARE ITS INPUTS PRESENT?  The sealed definitions of p_loc across the lineage (ALL of them --
                         the corpus-audit traps bind; p_loc may be defined more than once;
                         report every definition and version differences, repair nothing).
```

## 1. THE TASK — READ WHAT "LOCAL" IS SEALED TO MEAN, THEN PROVE OR NAME

```text
1  COLLECT every sealed definition/characterization of p_loc: its domain, codomain, and HOW
   it consumes its argument. Quote paths and lines. The question is whether the sealed text
   types p_loc's consumption as any of:
   (a) FINITE-SHADOWED: p_loc[X] depends on X only through finite-region / finite-stage /
       restriction data (evaluations, quasi-local integrals, limits of finite-region
       functionals in a NAMED topology);
   (b) NORM-CLASS: p_loc is a norm-continuous (or module-adjointable) functional on a class
       where Q-247 separation holds;
   (c) WEAK-*/BIDUAL-CAPABLE: p_loc pairs with its argument in a way that CAN see bidual
       content;
   (d) UNSPECIFIED even for this one object.
2  *** IF (a) OR (b): PROVE THE CERTIFICATE. p_loc|Tail_ind = 0 follows because Tail_ind is
   killed by every finite restriction (a) or by separation on the class (b). EXHIBIT the
   factorization/continuity step from the sealed text -- the proof must cite the defining
   words, not the name "local". State the conclusion at strength: AT FIXED BACKGROUND AND
   FIXED p_loc, THE FIXED POINT IS TAIL-BLIND; the tail route to p_ch-in-alpha is CLOSED
   (background channel Q-252 remains separate and open -- restate that boundary). ***
3  IF (c): exhibit the pairing term -- that is the tail's live route into alpha, exactly.
4  IF (d): the certificate is UNBUILDABLE FROM SEALED TEXT -- state the two candidate
   authored versions (p_loc typed finite-shadowed vs bidual-capable), what each implies for
   p_ch-in-alpha, and note the FINITE-AUTHORITY battery item: an authored p_loc that sees
   the tail imports non-finite physics into alpha's defining map. The typing then goes to
   the principal with both readings on the table.
5  KILL-PASSES: the name "local" proves NOTHING (substring is not typing); no topology
   switch mid-proof; if different lineage definitions type p_loc differently, THE
   DIFFERENCE IS THE FINDING -- report which definition is CURRENT under v004 and whether
   the older typings were superseded or silently dropped.
6  FENCES (DoR-011): reading and proof only. No evaluation, no roots, no numbers, no
   comparison to measured constants.
```

## 2. LEADS

```text
IF THE CERTIFICATE PROVES -- *** LEAD WITH IT AT FULL STRENGTH: the tail channel into alpha
CLOSES BY THEOREM. The deciding question then stands on the background channel alone. ***
IF p_loc CAN SEE THE TAIL -- *** LEAD WITH THE PAIRING TERM: alpha's defining map has a
named non-finite input, and the program's finite-authority basis is in tension with its own
sealed definition. Say it plainly. ***
IF UNSPECIFIED -- lead with the two authored candidates and the battery constraint; that is
the ruling package for the principal.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
