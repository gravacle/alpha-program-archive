PASTE 130

Written 2026-07-30, after paste 129. **A PASTE IS A SNAPSHOT, NOT A STANDING INSTRUCTION.** Any ruling
dated later than this paste overrides it without further notice. Paste 129 was accurate at 03:07 and
stale by 05:56 — five register rows landed after you had already written your artifacts. Nothing you
did was wrong; the chain moved. Your six paste-129 hashes were verified and all six match.

Ten items. Items 1-3 register reviewer results. **Items 4-7 are FOUR PRINCIPAL RULINGS and one of them
carries an unruled consequence you must flag rather than resolve.** Item 8 is a namespace collision.
Item 9 is a standing rule. **Item 10 is the constructive work and it is the first executable step on
the critical path in days.**

Fences unchanged: read-only outside your own writes; compute no alpha, kappa, coupling, radius, scale,
root or eigenvalue; no comparison to any measured constant; file:line + verbatim quote for every claim;
bounded negatives state roots and exclusions; report refutations, never repair them.
`alpha_computed = false; proof_authorized = false` on everything you write. Never touch
`a32_holdout/custodian_private/`.

Collisions: CLEANROOM `C_R` compactness ratio vs PARENT `C_R(x)` induced Einstein coefficient. `K_H`
Maxwell stiffness vs Hermitised Dirac operator. **AND A NEW ONE — item 8, `Gamma_K`, which sits directly
on item 10's object.**

---

### ITEM 1 — REGISTER THE SLOT CLASSIFICATION (Q-13). It re-prioritises the whole slot list.

Source: `MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md`, row Q-13.

Test: does discharging the slot introduce a NEW DIMENSIONFUL, INDEPENDENTLY FIXED quantity (could
select), or only an invariance / limit / exhaustion / matching / protocol requirement (cannot)?

- FOUR can break a scale: **1, 2, 6, 13 (pole branch only)**.
- TWO act on other freedoms: **9** (additive shift, translation-breaking not scale-breaking) and **14**
  (dimensionless ratios, attacking the already scale-invariant depth `x`).
- **TWELVE CANNOT FIX A NORMALIZATION UNDER ANY CIRCUMSTANCES: 3, 4, 5, 7, 8, 10, 11, 12, 15, 16, 17,
  18.** Slot 8 is actively anti-selective — it DEMANDS invariance under cell count, packing density,
  valence and refinement rate. Slot 16 transports a scale from slot 13 and cannot originate one.
  **Discharging all twelve perfectly changes nothing about the number.**

Also register: THREE independent freedoms, not one obstruction — **F-scale** (multiplicative, 21 ledger
rows), **F-shift** (additive `c_R`/`K0`, 10 rows,
`alpha_strict_route_decision_ledger_v001.csv:429`), **F-ratio** (the depth `x`, already
scale-invariant). And that at least EIGHT imposed conditions DID break the scaling and failed by
MONOTONICITY with no interior root (ledger:130, 138, 142, 156, 164, 263) — breaking the symmetry is
necessary and nowhere near sufficient.

Reopen condition verbatim: reopens if challenged at a specific slot with the obligation text quoted, or
if a thirteenth slot is shown to carry a dimensionful independently fixed quantity.

### ITEM 2 — REGISTER Q-14, AND CORRECT YOUR OWN RESCALING ERRATUM

Source: rows Q-14/Q-15; working in
`MB Work/alpha_supervision/DEPARTURE_2_RESCALING_EXCLUSION_FOUR_CHANNEL_AUDIT_V002.md`.

**THE CORRECTION YOU OWE, in `STAGE8_RESCALING_EXCLUSION_RECOVERY_ERRATUM_V001.md`, append-only:**

1. The deletion is at **v002**, not "the v003 edit." Measured mtimes, all 2026-07-20: v001 15:10
   (heading present), v002 15:13 (heading ABSENT), v003 22:13, v004 22:58.
2. **v004 was never checked** and is also missing the heading.
3. **THE SUPERSESSION NOTE EXISTS.** `primitive_record_cell_selection_principle_v002.md:3-16`, headed
   "## Correction to v001": the division rule `K_R = 4(Phi_* - Gamma_rest,*)/I_F,*` "is valid only if
   the selected field configuration and the action partition are already independent of `K_R`. A coupled
   matter-electromagnetic-gravitational saddle need not have that property." Plus "No numerical cell or
   alpha value was evaluated between the two versions." So
   `v001_rescaling_exclusion_dropped_silently_relative_to_working_set = true` must be restated as a
   CITATION-GRAPH loss, not an unrecorded deletion.

Then register the rest of Q-14: the four channels were the four INGREDIENTS of that superseded division
rule; the live list is FIVE — **geometry, clock, measure, regulator, action-partition**
(`v002:131-133` failure rule 3, a BLOCKING rule, plus preregistration v002 `required_construction`);
"field-energy integral" is NOT a live channel, since `I_F` survives only in the envelope identity at
`v002:111-120`, which `:121-123` disposes of — "This identity checks the implementation. **It does not
select `K_*`**."

Sequencing fact: `results/primitive_record_cell_joint_selector_readiness_v001.json` lists five failed
authorization checks, the fifth being `joint_operator_passes_mutation_audit` — a check ON the operator.
No `C_record(K)` exists, so no `K_*`, so a channel-by-channel audit has nothing to move.

### ITEM 3 — REGISTER Q-15: DO NOT CONFLATE THE REFUTED ARM WITH THE UNBUILT ONE

Q-11 refuted the first-opening principle **as a fixer of `beta`** — `beta` does not appear in `L_open`,
so every member of `R = beta c Delta tau` satisfies the condition trivially. **That argument does not
touch `K_*`**, which is what `L_open` was built to select. And
`STAGE8_FIRST_OPENING_ROOT_PRINCIPLE_RECOVERY_INDEX_V001.md:70-79` retires the route by
NON-COMPUTABILITY, not refutation, while stating "Whether a first-opening root can be formed from one
microscopic CTP functional is open."

Register: **beta arm REFUTED; `K_*` arm UNBUILT.** Prohibition unchanged — "the retired `L_open`
subtraction may not be reused by renaming its terms."

### ITEM 4 — PRINCIPAL RULING Q-16: SLOT 12 RE-POSED AS SCHEME-COVARIANCE

Authority: `MB Work/alpha_supervision/SLOT12_SCHEME_COVARIANCE_PRINCIPAL_DECISION_2026-07-30.md`
sha256 `028e355afc5f5d078ee39beb18de01269c57e09b85ba39a1721ebbf8e56ad451`.

The contradiction: `EM_DEPENDENCY_ORDER_FREEZE_V001.md:32-34` makes slot 6 the only named ORIGIN of
response normalization; slot 12 demanded regulator independence with no hidden finite selector.
Discharging 12 as written closed 6, and nothing had recorded it.

RULING, verbatim: *Physical outputs must be independent of the regulator FAMILY. Exactly ONE finite
renormalization condition is permitted, and it is admissible only if it is DERIVED from the parent
rather than chosen. A condition forced by the theory is not a hidden finite selector; a condition
selected by its outcome is, and remains forbidden.*

Grounds: with `K_bare = 0` there is no counterterm to absorb anything, so the induced coefficient IS
what the regulated calculation produces — total regulator-independence would demand the coupling not
depend on what generates it. Scheme-independence never meant condition-freeness. And slot 12's absolute
form is a machinery-shaped requirement standing in front of a physical mechanism, which the principal's
binding rule of 2026-07-29 forbids.

Creates **O-SC1** (derive the condition from the parent, target-blind, sealed before any response is
evaluated) and **O-SC2** (covariance across a regulator FAMILY whose membership is defined in advance).
Falsifiers F-SC1 (condition chosen -> slot 6's originating role LAPSES), F-SC2 (outputs vary across the
family -> the re-posing FAILS; do not narrow the family after the fact), F-SC3 (condition fixes a
PRODUCT not a magnitude -> delivers nothing).

SCOPE: discharges no slot; adopts no regulator, scheme or subtraction point; reaches NOT slot 9;
APPLIED NOT AMENDED on the R-14/R-29 precedent.

### ITEM 5 — PRINCIPAL RULING Q-17: SLOT 6 RELEASED TO THE LANE, O-SC1 FIRST

Authority: `MB Work/alpha_supervision/SLOT6_RELEASE_PRINCIPAL_DECISION_2026-07-30.md`.

The guard is lifted because F-SC1 now carries its protective function: a lane that ADOPTS the condition
gets a recorded LAPSE, not a number. Order is fixed and is the substance:
`O-SC1 -> O-SC2 -> response evaluation`, never any other order. Evaluate first and condition after and
the condition was selected by its outcome — the move that produced ledger row 516's
`K = 3/16 + E1(1)/(12 pi^2)` and got it rejected at row 517.

Falsifier F-S6: skipping O-SC1, deferring it past a response evaluation, or discharging it by adoption
voids the release, and the guard returns by default rather than by a further decision.

### ITEM 6 — REGISTER RESULT Q-18: THE FLOOR BOUNDARY VALUE IS THE INDUCED-ONLY AXIOM

Source: `MB Work/alpha_supervision/RESULT_FLOOR_BOUNDARY_VALUE_SETTLED_2026-07-30.md`
sha256 `5c3ee7355bb5c744192cbefd046ffc85b53e64b0b41e859bca6c101f55b46554`. Executed at the principal's
instruction to settle a standing dispute.

THE DISPUTE: was `Gamma_BR,k_R = 0` a derived spectral fact (operator -> floor -> boundary value) or a
chosen subtraction ("the floor IS the subtraction")?

**THE DECIDING ARGUMENT, WHICH IS NEW: the capacity condition and the proper-time floor act on OPPOSITE
ENDS of the same integral.** `lambda_0(D_BR^2) = k_R^2` is a bottom-of-spectrum statement, and the
bottom of the spectrum governs LARGE `s` in `STr' exp(-s L)` — it gives infrared decay
`exp(-s lambda_0)`. The floor `s >= 1/k_R^2` cuts SMALL `s`, where the `F^2` logarithm lives.
`exp(-s L)` is well defined and nonvanishing for every `s > 0` regardless of where the spectrum starts,
so **no value of `lambda_0` can empty the small-`s` range.** OPERATOR -> FLOOR is a TYPE MISMATCH, not
an unfinished proof.

**CONSEQUENCE FOR YOUR OWN CHAIN:** `STAGE8_OPERATOR_FLOOR_BOUNDARY_CHAIN_CANDIDATE_DERIVATION_V001.md`
names its obstructions at step 1 (complete normalized `L_BR`) and step 2 (lowest eigenvalue for the
complete public operator). **Closing BOTH would still not yield the floor.** Your own flags were already
correct — `proper_time_floor_status = ADOPTED_BY_INDUCED_ONLY_FUNCTIONAL`,
`gamma_at_floor_zero = DERIVED_GIVEN_FLOOR`. Register that the chain's binding defect is at step 4 and
that no repair of steps 1-3 reaches it. **Your step-2 GAP is not the binding one for this purpose.**

BOUNDED NEGATIVE (roots parent tree + supervision; types md/json; exclusions node_modules, `external/`,
custodian_private): lower-endpoint/lower-limit files **12**; `lambda_0` files **16**; **BOTH ZERO.** The
two objects have never appeared in the same file in either direction, and no artifact anywhere
distinguishes the small-`s` from the large-`s` end of this integral.

THE AXIOM SAYS IT ITSELF — `alpha_induced_only_boundary_action_principle_v001.md:16-19`: "The lower
proper-time boundary is the first durable record scale; `Gamma_BR,k_R=0` **states** that no separate
public stiffness is installed before the record branch opens."

Register also: **alpha's conditionality equals the induced-only axiom's status, exactly and no more**;
and that the attack's escape clause ("unless the postulate is independently earned by predicting
something else first") **is slot 18** — so slot 18 is load-bearing for alpha's EPISTEMIC STATUS while
remaining irrelevant to alpha's VALUE. Frozen repair F-FL1: exhibit a condition fixing the SMALL-`s`
end. A bottom-of-spectrum condition provably cannot; a domain / measure / admissible-mode-content
statement might.

### ITEM 7 — PRINCIPAL RULING Q-19: SLOT 9 RETIRED AS POSED. **AND IT CARRIES AN UNRULED CONSEQUENCE.**

Authority: `MB Work/alpha_supervision/SLOT9_RETIRED_AS_POSED_PRINCIPAL_DECISION_2026-07-30.md`.

Slot 9 is not deleted; the count stays eighteen; its content is re-posed as:
- **S9-A (DETERMINATION).** Determine the TOTAL physical stiffness by a DERIVED and OVERDETERMINED
  condition. The induced/counterterm split is bookkeeping and need not be separated;
  `alpha_micro = 1/(4 pi K_*)` consumes the total.
- **S9-B (THE EXIT QUESTION).** State which exit obtains: E1, E2, E3, E4, a NAMED FIFTH, or NONE.
  "NONE" is permitted and informative.

Grounds: the attack's own recommendation, on the ground that "the question has no solution inside the
class it is asked in" — the condition class is closed under `K -> K + c`; plus Q-18 removing the last
apparent fifth exit; plus the asymmetry that **"excluded" and "determined" are different demands and
only the second is what alpha needs.** Precedent for re-posing a sealed obligation: R-31.

Five guards: no ADOPTED determining condition; no selection by outcome; **"moot" is NOT discharge of the
epistemic obligation**; no claim that a scale-breaker now reaches the additive freedom
(`d c_R/d x = 0` stands); S9-B may not be answered with silence.

*** THE CONSEQUENCE — FLAG IT, DO NOT RESOLVE IT. *** R-30's frozen falsifier **F2 fires if slot 9 is
"underivable on this route except by adopted postulate."** Q-18 established exactly that. **On its face
F2 HAS FIRED and the primary-route declaration lapses by its own terms.** Two readings: (a) F2 fires and
the BR/EM-GR route loses declared primacy without being refuted; (b) F2 is explicitly RESTATED against
S9-A, in which case it has not fired because S9-A has not been attempted. Reading (b) is defensible only
if made explicitly and recorded. **A declared label may not survive on a technicality.** Record the
question in the lane status as awaiting the principal; take neither reading.

### ITEM 8 — `Gamma_K` COLLISION -> NAMESPACE DRAFT v004

- `primitive_record_cell_selection_principle_v002.md` — the complete **microscopic CTP functional indexed
  by the COUPLING `K`**.
- `BID_MONOIDAL_EXTENSIVITY_DERIVATION_V001.md:34` — a **BID amplitude functional indexed by a CELL
  SET**: `Gamma_K(A) = -log|A_K(A)|`; `:51` `Gamma_K(A) = sum_i Gamma_(K_i)(A_i)`. 12 cleanroom files.

Third demonstrated collision after `C_R` and `K_H`, and it sits on item 10's object.

### ITEM 9 — STANDING RULE: REGISTER THE ROW, NOT THE ARTIFACT

Registration order is not authority order. **Authority follows WRITE TIME.** Before registering any
supervision result, check its write time against the timing table in
`MB Work/alpha_supervision/EXECUTION_TRACKER.md` ("TIMING AND REVERSION GUARD"). If an artifact predates
a register row touching its subject, register the ROW. Append-only means a later row supersedes an
earlier one; it does NOT mean the most recently registered text wins.

Two of the six named reversion hazards are yours: item 2's erratum correction and item 8's namespace
draft.

### ITEM 10 — THE CONSTRUCTIVE WORK: O-SC1. START HERE.

**Slot 6 is released and O-SC1 is the active construction item — the first executable step on the
F-scale band since the band was identified.**

DELIVERABLE: a target-blind derivation of the finite renormalization condition from the parent —
traceable to parent structure, stated and SEALED BEFORE any response is evaluated. Not a scheme choice,
not a subtraction point picked for convenience, not a condition reverse-engineered from a response.

STEP ZERO, bounded: **inventory what the corpus actually has for slot 6.** State roots and exclusions.
Slot 6 has never been scoped. The precedent is worth knowing — one bounded scoping session on Departure 2
found its four-channel framing was built on a formula superseded three minutes after it was written, and
four obligations in two days turned out to be missing SPECIFICATIONS rather than hard proofs.

WORTH TESTING EARLY, because Q-18 hands you a well-posed target: **F-FL1 — is there a condition that
fixes the SMALL-`s` end of the proper-time integral?** A bottom-of-spectrum condition provably cannot. A
statement about the operator DOMAIN, the MEASURE, or the ADMISSIBLE MODE CONTENT might, and that class is
essentially unexplored. If such a condition exists and is derived, it is simultaneously a candidate for
O-SC1's single permitted condition and a candidate FIFTH EXIT for S9-B. **Those two would then be the
same object, which is the overdetermination shape the program is looking for.** Do not assume it; test it.

THREE OUTCOMES, ALL ACCEPTABLE AND ALL INFORMATIVE: **DERIVED** (seal it; O-SC2 follows);
**UNDERIVABLE, REPORTED** (F-SC1 fires, slot 6's originating role lapses honestly, and the F-scale band
narrows to slot 1 and `Gamma_K` + `C_record(K)` — report it, do not repair it); **BLOCKED BY ORDERING**
(slot 6 is position 4 of 9 in the frozen EM order and slots 3, 4, 5 are open — say so, with the missing
input named at file:line).

FORBIDDEN FOURTH OUTCOME: condition adopted, response evaluated. That voids the release under F-S6.

DO NOT WORK: the four-channel mutation audit (no object until `C_record(K)` exists); `L_open` in any
renamed form; anything touching slot 9's old form — a shift is not a dilation.

---

REPORT BACK: hashes for every artifact written, the corpus_check gate verdict, deploy state, and for item
10 the outcome type (DERIVED / UNDERIVABLE / BLOCKED-BY-ORDERING) with file:line evidence. The two RED
`authority_currency` findings — principal rulings uncited by the governing chain — should clear once
items 4, 5 and 7 land.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
