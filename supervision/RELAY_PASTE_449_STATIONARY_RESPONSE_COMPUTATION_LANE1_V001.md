## PASTE 449 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4b] THE STATIONARY RESPONSE COMPUTATION ON THE RATIFIED SQUARE

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-367. **DoR-017 IS IN FORCE:** the action-comparison square closed by the N member (certified covariant divergence-generated; ν SYMBOLIC), under the standing falsifier — member-sensitivity tagging on EVERY result, void-on-downstream-failure. Decision file: `alpha-program-archive/supervision/DOR_017_ACTION_COMPARISON_SQUARE_N_MEMBER_RATIFICATION_2026-08-03.md`.
- Load-bearing authorities (verify hashes before reading):
  Square V004 (the ratified candidate): `STAGE8_TASK4A_ACTION_COMPARISON_SQUARE_ADOPTION_PROPOSAL_LANE2_V004.md`, `abf6d366a5a7e375b9b53df75402f35d37f7c6a4b1bb0b10a44309ad3b0e1912`.
  The derive standard: `a9b733c7…`. Descent V003: `a03e8363…`. DoR-016/V004: `69f4d93b…`. DoR-015/V005: `7ecf04e9…`. The draft: `ed49624b…`.
- The deciding question this computation serves: does the physical stationary response depend on p (and on the integer pair beyond p)? The finite side is already proven one-sided (every finite retarded/kernel block exactly zero and p-free — Q-243/Q-279/Q-309). DO NOT anticipate the verdict; compute.
- Register sweep before computing; list entries checked. Custody: you compute; Lane 2 cross-verifies.

TASK — compute the stationary response on the ratified square:

W1. **Assemble the stationary system.** The R5 package with the N member installed: the stationary 2PI blocks on the completed domain, the complement inverse, the Schur reduction, the retarded extraction — each step tagged MEMBER-SENSITIVE or MEMBER-INDEPENDENT, with the member's certificates (covariance, reality, batching, restriction) verified at each stage (any failure = report VOID-CANDIDATE and STOP that branch; do not patch).
W2. **Compute the physical stationary response exactly.** Symbolic p_i = r_ch,i/(r_0,i + r_ch,i), symbolic ν, no rank/ratio/orientation/frame/member selected. Work on the minimal nontrivial stages first (the reciprocal loop; the S8-A stage), then state what the general finite stage gives.
W3. **The p-dependence structure — REPORT, do not verdict.** For each computed block: state exactly where p appears, where ν appears, and where neither does. Tag which statements are member-sensitive. If any block's p-dependence is exact and stage-independent, state it as a finding; the VERDICT itself is not this relay's to declare (it needs the cross-verification and the registrar).
W4. **Falsifiers.** Every finite check the computation implies, run: the DoR-008 restrictions, the finite-shadow reproduction (Q-243/Q-279 tables), reality, batching, the permanent regressions at the response level.
W5. **Honest boundaries.** Whatever the completed domain cannot yet support, state as TYPE-U with the exact missing object — no forcing.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK4B_STATIONARY_RESPONSE_COMPUTATION_LANE1_V001.md`
with a lead-result block (the computed response structure, the p/ν appearance map, the tag ledger, VOID-CANDIDATE flags if any), the register-sweep list, and the W1–W5 chain. Seal (.seal.sha256 sidecar), mirror both files byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, then STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No evaluation of alpha, K_*, or roots; no numeric evaluation of any response value; no comparison to measured constants. Structural/symbolic mathematics of declared objects is permitted; if a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
