PASTE 358 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] CROSS-VERIFY P2 — THE FOUNDATION EVERY PACKAGE NOW SITS ON

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-274)

```text
DOES THE OBJECT EXIST?   YES: P2 (1339e3ce..., built by the OTHER lane, Q-273): the
                         discrete-character x (l1 + l1 + trace-class) topology, Frechet
                         calculus, dense finite core, contractive restrictions,
                         differentiation-commutes-with-restriction, Tail_src = {0}.
                         P4 (YOUR build) and P3 (in flight) both STAND ON IT. The
                         construction chain has run without cross-verification; P2 is
                         the foundation -- verify it now, before P5 compounds on top.
IS THE VERSION CURRENT?  YES through Q-274. Note P4's load-bearing sign finding: small
                         errors at this level propagate; that is WHY this relay exists.
ARE ITS INPUTS PRESENT?  P2's artifact, the germ, the Q-245/Q-247 discipline.
```

## 1. THE CHECKS — INDEPENDENT PROOFS, NOT PROOFREADING

```text
V1 THE TOPOLOGY: is the product topology well-defined on the germ's actual export domain;
   is the trace-class component the right dual pairing for the germ's operator-valued
   sources; does the discrete-character factor interact correctly with the sequential
   zero-extension?
V2 DENSE CORE: verify finite sources are dense with YOUR OWN approximation argument;
   verify contractivity of restrictions (exhibit the contraction constant or its bound).
V3 DIFFERENTIATION-COMMUTES-WITH-RESTRICTION: the property the transport runs on --
   re-prove it independently, including at second/mixed order (where Q-243 lives).
V4 THE TAIL EXCLUSION: re-run the moving-tail sequence against THIS topology yourself;
   verify no limit exists (and that no OTHER tail-like sequence -- weak-* or otherwise --
   sneaks a limit into topology_src; the claim is Tail_src = {0}, not merely "the known
   sequence fails").
V5 CONSISTENCY WITH P4: the anchored Log_0 neighborhood |Z-1| < 1/2 -- open in P2's
   topology? The zero hyperplanes closed? The derivative tower's topology claims
   consistent between the two artifacts (including the Gamma_N = -Log Z_N sign)?
V6 RANK DISCIPLINE + no-import audit: no hidden rank selection, measure, or contour
   anywhere in P2's construction.
VERDICT: CONFIRMED / DEFECT (exact location and consequence -- which downstream results
inherit it) / CONFIRMED-WITH-NOTES.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
