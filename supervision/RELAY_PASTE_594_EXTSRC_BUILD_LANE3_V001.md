## PASTE 594 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5 / STEP 4] THE EXTSRC MEMBER — ROUND 1 OF THE TIMEBOX

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-523. THE SEQUENCED PROGRAM is ruled (read `alpha-program-archive/supervision/DECISION_SEQUENCED_PROGRAM_2026-08-06.md`): ExtSrc first, timeboxed; explicit incompleteness the standing fallback — an honest obstruction display is a fully successful round.
- The target: JOINT A's source-extension/response member — the common bottleneck of both Γ routes. The specification: your own J-II artifact's H1 consolidated spec (`fdf20bd4…`) restricted to the Joint-A/ExtSrc fields, as corrected by the review (`ba9430c1…` — read its K1/K2: the near-miss A_G evaluation map and WHY it dies on domain; the sector-not-scope finding).
- THE STANDING OBLIGATION: if any candidate member crosses sectors, its cross-sector unit MUST be declared per DoR-019's NO_IMPLICIT_CROSS_SECTOR_UNIT, and the anti-tuning ledger carries the cross-sector-unit row.
- MANDATED SECTIONS: SURFACE ANCHOR; dependency re-audit; the R9 quantification check.
- Custody: you build; Dario reviews. Tag PROVABLE / PART-PROVABLE / YOURS.

TASK:
A1. **The Joint-A specification isolated:** exactly what the ExtSrc member must be — domain, codomain, the extension and response properties each consumer uses (walk: Γ^H Joint I, the HOL route's actual consumption per (L2), S22/S22a) — the minimal common core, displayed; note where the A_G near-miss's domain would need to sit and why it cannot.
A2. **The inhabitance attempt, assembly-first:** pieces cited with types; the construction attempted; every equality displayed or the exact stop named. The A_G map may be USED as a component if a lawful domain bridge exists — attempt that bridge explicitly as its own joint (it is the nearest sealed object; the review died it on domain, not on content).
A3. **If inhabited:** the consequence board (which joints unblock per route; the timebox status). **If stopped:** the exact gap per attempt, and the round-2 plan (what a second round would try that this round could not).
A4. **Battery:** F_PLDEC; anti-tuning WITH the cross-sector-unit row; the surface anchor; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EXTSRC_BUILD_LANE3_V001.md`
with final lines: `SPEC = ISOLATED`, `EXTSRC = INHABITED / STOPPED (+gaps, +round-2 plan)`, `AG_BRIDGE = FORMED / STOPPED (+gap)`, `CROSS_SECTOR_UNIT = declared / not_applicable`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
