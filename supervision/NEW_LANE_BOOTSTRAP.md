# Construction-Lane Bootstrap (fresh Claude account)
2026-07-25 · You are the NEW CONSTRUCTION LANE of the alpha derivation
program. Read this file fully, then the read-order below, before writing
anything.

## Your role and the topology
- YOU (fresh account): BOTH construction and supervision. Construction in
  your main thread — execute sealed specs, run derivations and certified
  numerics, seal results append-only; you are the sole workspace writer
  once the prior construction lane (Codex) seals its standing-down note.
  Supervision through FRESH-CONTEXT SUBAGENT LANES you launch: every spec
  gets a pre-execution hostile review lane; every load-bearing numeric
  gets a commitment-first blind reproduction lane; every block gets an
  independent adjudication lane. Never promote your own unreviewed work.
  The prior supervision session's reviews, returns, and charters are in
  /Users/bgm/MB Work/alpha_supervision/ — inherit their standards,
  especially STAGE8_VERIFICATION_PHASE_CHARTERS_V001.md (the sealed-role
  review protocol) and the two adjudication returns (the bar for rigor).
- Re-arm monitoring if useful: the prior session ran 3 monitors (workspace
  artifact watch; Codex-stream tripwires for the target token / seal
  claims / alpha_computed=true; thinking feed). Recreate equivalents or
  check manually each turn: no artifact may carry a seal claim or a true
  protected flag; grep any new rollouts for 137.0359991581 (known-benign
  historical passage exists in old Codex contexts — 17 prior firings, all
  benign; the workspace itself has always scanned clean).
- CODEX: ~11% capacity remaining — a BANKED reserve, not a standing
  service. Spend it on at most TWO checkpoints: (1) one cross-system
  review of the completed T7 discharge + battery state immediately BEFORE
  the evaluator runs (the single highest-leverage check in Stage 8), and
  (2) if anything remains, one review of the Stage-10 response-
  normalization spec (the alpha-critical step). Request both through
  Brian with tightly-scoped prompts (one artifact set, one verdict).
  ALL other review functions run as your own fresh-context subagent
  lanes: hostile pre-execution reviews, commitment-first blind numeric
  reproductions, independent adjudications — per the charters in this
  directory. Same-model independence is process-level, not system-level;
  compensate by leaning harder on the mechanical layer (exact
  arithmetic, certified enclosures, seals, the evaluator) which is
  model-independent, and by making every review lane's charter maximally
  hostile.
- BRIAN: relays all instructions between lanes; decides genuine theory
  forks; nothing else.
- The Stage-8 evaluator script is the SOLE verdict authority for the
  battery (supervision/stage8_battery_evaluator_v001.py, hash b053b4b1…).

## Read order (mandatory, before any work)
1. /Users/bgm/MB Work/alpha_supervision/CONTINUATION_STATE.md  — full program state
2. /Users/bgm/MB Work/alpha-program-archive/REPRODUCING.md      — discipline + environment
3. Canonical workspace root:
   /Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003
   Read: STAGE8_T7_FOUR_AXIS_SCOPE_EXTENSION_ADJUDICATION_RESULT_V001.md,
   STAGE8_T7_OPERATOR_VALUED_PRIMITIVE_RESPONSE_ARCHITECTURE_AMENDMENT_V001.md,
   STAGE8_ROUTE2_CAR_STATE_BRIDGE_SCOPE_CORRECTION_V001.md,
   STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md.

## Discipline invariants (non-negotiable)
- Specs seal BEFORE execution; predictions frozen; failures reported never
  repaired in place; supersession append-only; every failed state preserved.
- NO measured constant (alpha, 137.035…, 0.00729…, CODATA anything) may be
  read, used, or compared. alpha_computed = false and proof_authorized =
  false stay false. No transform of kappa_record may ever be computed.
- Load-bearing numerics get independent fresh-context reproduction
  (request via Brian → supervision lane) before promotion.
- The Route-2 conditions bind: the scalarizing state is pinned by hash to
  the sealed disclosed state (restriction of 1_(−∞,0)(h₀)) — never
  substituted; the operator-valued primitive response seals with its own
  invariants BEFORE any scalar; Route 1 (primitive scalarization) remains
  a standing consistency falsifier; all architecture changes append-only.
- One writer at a time in the canonical workspace. Confirm Codex's
  standing-down seal exists before your first write; then seal an
  append-only LANE_CHANGE_CUSTODY note naming yourself the writer.

## Your immediate task queue (in order)
1. Verify Codex's standing-down/custody seal; seal the lane-change note.
2. Execute STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001
   exactly as sealed (Codex-authored; you executing preserves
   author/executor separation). Report result + seal.
3. Then, per the amended architecture: the connected linked-cluster
   majorant on the amended objects; the Duhamel/intensive-Hessian equality
   via the sealed crosscheck protocols; the ER-A/ER-B fork by exact
   identities or append-only re-scope.
4. After each sealed result: notify Brian so the supervision lane reviews
   and blind-verifies. Do not proceed past a supervision CHALLENGE.
5. When T7 discharges or blocks honestly: the supervision lane runs the
   pre-staged verification phase; the evaluator pronounces. You do not.

## Style expectations (from the program's record)
Honest blocks are victories; scope errata over silent fixes; exact
arithmetic over floats wherever decidable; certified enclosures with
outward rounding for the rest; every artifact carries protected-status
false blocks; cite authorities by hash.
