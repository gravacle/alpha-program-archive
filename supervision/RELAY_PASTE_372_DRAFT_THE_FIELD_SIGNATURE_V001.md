PASTE 372 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] DRAFT FIELD_SIGNATURE_PHYS THROUGH THE GATE (THE RACE, AUTHORED ARM; DoR-015 RESERVED)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-288)

```text
DOES THE OBJECT EXIST?   COMMISSIONED BY RULING (plan C34): a DRAFT of FIELD_SIGNATURE_PHYS
                         -- the physical field/spacetime signature raw G requires (Q-287:
                         what T_cyl lacks). The gate's third use; the OTHER lane runs the
                         derivation arm -- DO NOT COORDINATE; you author as if derivation
                         will fail, and only the gate decides.
IS THE VERSION CURRENT?  YES through Q-288. THE BATTERY: DoR-008's falsifier; the Q-288
                         ledger's MANDATORY DOOR FLAGS (every class-formation step
                         declares its topology; weak-* invocation cannot be silent --
                         Q-285); finite authority; no target-tuning (nothing chosen for
                         its p_ch or alpha consequence); rank discipline; consistency
                         with the ratified stack (DoR-009 E_post orientation, DoR-013's
                         incidence primitive -- the signature must CONSUME ratified
                         structure, never duplicate or contradict it).
ARE ITS INPUTS PRESENT?  The Q-287 artifact (the exact deficit), the raw-G contract, the
                         Q-288 ledger, the consumers (the lift, the background fiber, the
                         four analytic fields).
```

## 1. THE TASK — THE PROPOSAL, GATED AS HARD AS THE ORIGIN WAS

```text
1  DECLARE the signature's components as authored fields, each on the choice table:
   what it adds beyond ratified data, alternatives, void-conditions. The component list
   comes from raw G's contract (causal/temporal split, locality/support, conjugation/
   reality, whatever the contract genuinely demands -- derive the LIST from the sealed
   texts even though the CONTENT is authored).
2  CONSISTENCY OBLIGATIONS, proven not asserted: E_post orientation respected; the
   incidence primitive consumed (the signature's locality must RESTRICT to the ratified
   write adjacency -- a signature contradicting the origin's own geometry dies); T_cyl
   embedded (the kinematic completion must sit inside the field layer canonically);
   DoR-008 restriction reproduction per component.
3  THE DOOR FLAGS, filled: every completion/class-formation the signature performs
   declares its topology; any weak-* step is FLAGGED LOUDLY with its tail-creation
   status per Q-285. An unflagged completion is a defect.
4  THE CONSUMERS' TEST: with the drafted signature, state (do not execute) how the
   raw-G lift's failed bounded-extension test would be re-run, and which of the four
   analytic fields gain their missing structure.
5  KILL-PASSES ON YOUR OWN DRAFT (Q-207): hidden adoptions; smuggled (M,g) or smooth
   structure; answer-defined components; anything chosen FOR its consequence; the
   B14-style circularity check on every consistency obligation.
DELIVERABLE: the proposal with choice tables, consistency proofs, filled door flags,
consumer statements. Nothing TYPE-P. DoR-015 reserved; the race's adjudication and
cross-review follow.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
