PASTE 333 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] DERIVE THE RESPONSE CLASS — DOES THE FALSIFIER'S OWN DISCIPLINE FORCE RetHess_phys INTO THE SEPARABLE CLASS? (DoR-011)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-249)

```text
DOES THE OBJECT EXIST?   THE QUESTION does (Q-249): the physical response class is UNTYPED by
                         sealed text (symmetric TYPE-S both directions). Before any typing
                         RULING, the provable-first rule demands the DERIVATION attempt:
                         does ratified structure FORCE the class?
IS THE VERSION CURRENT?  YES: Q-247 (separation holds on norm C*/module/left-multiplier,
                         fails on bidual), Q-249 (finite s_J/s_R ARE norm/module-class;
                         w_N is a covector, no direct tail occupation; Z_inc/G/H_R/Thomson
                         untyped).
ARE ITS INPUTS PRESENT?  The candidate forcing data: DoR-008's falsifier (every physical
                         object must reproduce every sealed finite result on restriction),
                         the ratified module representation, Q-245's cylindrical density,
                         Q-247's retractions, and the norm-class finite sources.
```

## 1. THE TASK — A FORCING THEOREM, ATTEMPTED HONESTLY BOTH WAYS

```text
1  STATE the candidate theorem: any physical response object that (a) is built from the
   norm-class finite sources s_J/s_R by the operations the sealed chain actually specifies
   (differentiation in the declared topology, restriction, composition -- LIST them from the
   sealed chain), and (b) satisfies DoR-008's restriction discipline at every finite stage,
   NECESSARILY lies in a class where Q-247's separation applies.
2  ATTEMPT THE PROOF: candidate route -- the finite-core: every DoR-008-compliant object is
   determined by its finite restrictions (separation on the ratified classes, Q-247) PROVIDED
   it lies in a class where separation holds; the question is whether (a)'s operations can
   EXIT that class. Check each listed operation: does it preserve the norm/module class?
   The known danger is the LIMIT operation (Q-245: the naive continuous extension FAILS; the
   moving tail lives in the bidual). If the sealed chain requires a limit, determine in WHICH
   topology the sealed text takes it -- that single word may decide the theorem.
3  ATTEMPT THE REFUTATION with equal force: construct, if possible, a DoR-008-compliant
   response object OUTSIDE the separable class -- the bidual tail z_tail B** is the material
   (Q-247); check whether adding a tail element to a compliant object PRESERVES compliance
   (the tail is killed by every finite restriction, so restriction-compliance is AUTOMATIC --
   if the sealed chain's operations tolerate the tail, the class is NOT forced and the
   freedom is EXACTLY a tail summand).
4  *** THE EXPECTED SHAPE, stated so it cannot be fudged: if (3) succeeds, the theorem
   becomes "RetHess_phys is forced UP TO a tail summand invisible to every finite
   restriction" -- and then THE DECIDING QUESTION IS WHETHER ANY SEALED PHYSICAL OUTPUT
   (Delta_Phi, kappa_Thomson's defining ratio, the response quotients) CAN SEE a tail
   summand at all: if every sealed output factors through finite restrictions or
   norm-continuous functionals, the tail is PHYSICALLY INERT and p_ch's tail route is dead
   even without typing RetHess_phys. CHECK THIS LAST STEP -- it is the sharpest available
   and it is output-side, not object-side. ***
5  TYPE the result; kill-passes: no topology switch mid-proof (Q-247 discipline), no
   "physical objects are naturally continuous" arguments, no treating tail-inertness as
   proved if any sealed output's factorization is unestablished -- list per output.
6  FENCES (DoR-011): proof/construction only; no evaluation, no roots, no numbers.
```

## 2. LEADS

```text
IF THE CLASS IS FORCED -- *** LEAD WITH THE PROOF STEP THAT DOES IT. Tail channel CLOSES for
the physical response; with 332's background verdict, state what (if anything) is left of
p_ch survival. ***
IF FORCED-UP-TO-TAIL AND THE TAIL IS OUTPUT-INERT -- *** LEAD WITH THE INERTNESS: same
conclusion by the output-side route, and name which sealed outputs carried the proof. ***
IF THE TAIL SURVIVES INTO A SEALED OUTPUT -- *** LEAD WITH THE OUTPUT: that is p_ch's live
route, named exactly. ***
IF THE PROOF NEEDS ONE MISSING LEMMA -- name it; that lemma becomes the program's sharpest
outstanding object.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
