## PASTE 503 — CODEX LANE 2 (HIGH EFFORT) — [TASK 5 / EQ6] EXHIBIT THE ACTUAL Q-408 PRIMITIVE GENERATOR: THE FIRST NEW-PHYSICS ROOT

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-424. The frontier map (`b8294e91…`) types B_Q408_REFINEMENT as GENUINELY NEW PHYSICS with the exhibit route: one TARGET-BLIND actual-Q408 primitive generator per orbit (coframe/density/connection/curvature data on Ref_path; path/test-form/current/coframe/density generators on new edges), then free-category composition and common refinement proven. This runs in parallel with 502 (Codex 1's C1 build), blind to it.
- Authorities: the frontier map row 1A; the hunt's Ref_path subpackages `452f1bb8…`; package V005 `09883a0d…`; DoR-016/V004 `69f4d93b…` (the rank-one profiles the generators must serve); DoR-020's decision file.
- Custody: this is member DATA for an adopted clause — an exhibit through the gate (choice table for every authored datum; carrier-structural justifications only; the five regressions bind absolutely — no abstract stand-ins, no circularity). Cross-check follows. All symbolic; target-blind.

TASK:
T1. **Per orbit: exhibit the primitive generator** — the actual coframe/density/connection/curvature data on Ref_path and the actual path/test-form/current/coframe/density generators on new edges, each a concrete declared object with its certificates (covariance, reality, units, restriction).
T2. **Prove free-category composition:** the exhibited generators compose freely (the category axioms verified on the exhibits).
T3. **Prove common refinement:** the exhibits behave under common refinement per the adopted clauses (the refinement diamonds tested).
T4. Falsifiers; the anti-tuning ledger (each exhibit's construction order documented); honest stopping point if any orbit resists (exact).

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V001.md`
with a lead-result block (`GENERATORS = EXHIBITED (per-orbit ledger) / STOPPED_AT (+exact)`; `COMPOSITION = PROVEN/…`; `REFINEMENT = PROVEN/…`), the register-sweep list, and the T1–T4 chain. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
