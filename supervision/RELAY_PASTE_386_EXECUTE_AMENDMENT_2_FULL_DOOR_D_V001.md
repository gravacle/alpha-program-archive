PASTE 386 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] EXECUTE DoR-014 AMENDMENT 2 — THE EVEN PAIRING b := hbar L(x)L, THEN THE FULL FOUR-SUBTEST DOOR D

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-303 + DoR-014 AMENDMENT 2 / C37)

```text
DOES THE OBJECT EXIST?   THE RULING does: the corrected derived pairing b := hbar L(x)L
                         (Theta-EVEN, source slot), the identity's i supplied by the
                         W-convention (W = -i hbar Log Z). Amendment 1's form is
                         superseded; the DERIVATION requirement stands.
IS THE VERSION CURRENT?  YES. The parity table binds (Q-303): exponent Theta-even; W/
                         Hessian Theta-odd. Marks: TYPE-P | premises: DoR-008/009/013/
                         014-as-amended(2)/015. Freeze stands until the FULL Door D
                         passes.
ARE ITS INPUTS PRESENT?  YES: germ V004 + the V005 attempt (b3b0a7d8... -- reuse its
                         verified identity machinery, replace the normalization), the
                         determination (323852e8... -- the parity proofs), the Door-D
                         test suite (bffd7ef2... + 365db191...).
```

## 1. THE TASK

```text
1  EXECUTE the corrected derivation: b := hbar L(x)L in the source slot. Re-certify the
   tensor precertificate for the EVEN form: well-defined, quotient-compatible,
   restriction-natural, adjoint-compatible (W3), and THETA-EVEN PROVEN (the new clause).
2  VERIFY the identity's bookkeeping: D_J A-bar = (i/hbar) G with the i entering through
   W's own definition -- write the chain out once explicitly so no i is double-counted
   or dropped (the two prior firings were both bookkeeping-depth defects; make this
   impossible to repeat).
3  ISSUE germ V006 = V004 with the corrected derived pairing (minimal diff; list changed
   clauses). The 14-row battery re-run on the rows the pairing touches; inheritance
   cited on the rest.
4  *** THE FULL DOOR D, ALL FOUR SUBTESTS: (i) bounded extension; (ii) same-correlator
   identity on cycles; (iii) on endpoint-covariant open paths; (iv) U1-reality under the
   ratified involution with the sealed law Q_(-n)(Theta R) = conj(Q_n(R)). CLEAN PASS:
   the freeze lifts. ANY FAIL: the principal hears it first. ***
5  ON PASS: run the lifted chain (the 382 continuation): lifted G, sector structure,
   p-content per block, connected subtraction, six-account rows, the p-trace, the
   updated remaining-objects list toward the transport verdict.
6  KILL-PASSES: no new choices; minimal diff; no selection; no unflagged completion;
   Q-243/Q-279 restriction executed at every stage; the parity of EVERY object stated
   where it is used.
DELIVERABLE: the even pairing + precertificate, germ V006, the four-subtest Door D
verdict, and on PASS the lifted chain with accounting and p-trace.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
