PASTE 326 — CODEX LANE 2 (HIGH EFFORT) — [TASK 2f] FIX X_K INDEPENDENTLY AND EXECUTE THE K_SQUARE FALSIFIER ARM

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-242)

```text
DOES THE OBJECT EXIST?   THE REQUIREMENT does (Q-241): the falsifier's K_square arm executes
                         only with an independently fixed finite representation, a source
                         incidence operator X_K, and an intertwiner with T_K X_K = B_K T_K.
                         Q-241 PROVED the verdicts are choice-invariant across all 1,088
                         filtrations -- the ONLY missing piece is X_K, fixed WITHOUT pulling
                         B_K backward (that tautology is already refused, TYPE-U).
IS THE VERSION CURRENT?  YES: Q-241 (choice-invariance; C0_SOURCE_INCIDENCE_OPERATOR_FOUND =
                         false | TYPE-S at the sealed-search level), Q-242 (bundling refuted
                         for P5 -- not this question, but the same anti-relabeling standard
                         BINDS here).
ARE ITS INPUTS PRESENT?  The sealed K_square incidence data (edges, vertices, holonomies,
                         charpoly z^8-8z^6+20z^4-16z^2+4, Tr B^2 = 16, Tr B^4 = 48,
                         R_square = 3/16) and DoR-008's ratified source maps / branch
                         embeddings on the sequential side.
```

## 1. THE TASK — CONSTRUCT X_K FROM THE INCIDENCE SIDE ALONE, THEN TEST

```text
1  CONSTRUCT the candidate X_K from the sealed K_square incidence data ITSELF: the square's
   own edge/vertex incidence structure and the sealed source conventions -- with NO reference
   to B_K's matrix elements and NO reference to T_K. Declare the candidate FAMILY first
   (forcing protocol Q-176): what incidence-side data determines a source operator, and is
   the determination unique? If a finite family survives, enumerate it -- a SMALL family with
   an invariance argument is legal (Q-241 precedent: verdicts may be choice-invariant).
2  CONSTRUCT the intertwiner T_K on its own terms (the sealed comparison signature), again
   without tuning it to make the test pass. FREEZE both before testing.
3  *** EXECUTE: does T_K X_K = B_K T_K hold? THIS IS THE FIRST NONTAUTOLOGICAL CONTACT
   BETWEEN THE RATIFIED COMPLETION AND THE SEALED K_SQUARE RESULTS. ***
4  IF (3) HOLDS: run the DoR-008 restriction test on the strongest sealed result
   (the charpoly). PASS confirms the arm; *** FAIL VOIDS DoR-008 AND EVERYTHING TYPE-P ON
   IT -- REPORT A FAIL AS LOUDLY AS A PASS. NO SOFTENING. ***
5  IF NO X_K IS CONSTRUCTIBLE from incidence-side sealed data: TYPE-U with the exact
   would-build; if the candidate family is empty BY PROOF, that is TYPE-R against the arm's
   executability and must say what that means for the falsifier (TYPE-C persists, arm
   unexecutable -- state whether DoR-008 can EVER be audited on this arm).
6  ANTI-RELABELING BINDS (Q-242 standard): an X_K DEFINED so that the intertwining relation
   holds is the tautology in new clothes. The construction must cite only incidence-side
   sealed text at every step.
```

## 2. LEADS

```text
IF THE ARM EXECUTES AND PASSES -- *** LEAD WITH IT: the falsifier gains its second arm and
DoR-008's authority extends to incidence-class results. Task 2f CLOSES. ***
IF THE ARM EXECUTES AND FAILS -- *** LEAD WITH THE VOID. That is the single most consequential
sentence available to this program and it must be the first line. ***
IF X_K IS UNDERDETERMINED -- lead with the family and whether the TEST's verdict is invariant
across it (the Q-241 move); a family-invariant PASS is a PASS.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
