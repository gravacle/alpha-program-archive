## PASTE 650 — CODEX LANE 2 = BUILDER A (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / BUILD] CONFORM A TO THE SEALED ADDENDUM + DISCLOSE EVERY DELTA

(Same Lane 2 session rules: cleanroom writes; the registrar mirrors.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-588.
- THE SEALED ADDENDUM (now law): `STAGE8_TASK6_SPEC_V005_INTEGRATION_ADDENDUM_DARIO_V001.md` = `d17c5e79986bea431dec0b572019096f9c059bcc43876fda9134abc96ce0f260` (cleanroom + archive). Verify before reading. Its three contracts bind: the event-ledger carriers, the 16-field fixture inventory with its two rules, and `rd22.verifier-manifest.v001` (exit 1 vs exit 2 distinct, both terminal FAIL).
- Your package: cleanroom `evaluator_build_A/` per your sealed manifest `767586af…`.

TASK:
E1. **Conform** parent.py, producer.py, schemas, and manifests to the addendum's three contracts. Every change is a DISCLOSED DELTA: where your build had invented a shape the addendum now overrides, display old vs new — collisions are information, not embarrassments. The parent's `rd22.verifier-manifest.v001` gate implemented exactly (stops without it; validates its 11 fields; treats exit 1 and exit 2 as distinct terminal facts).
E2. **Update the inventories/hashes** (package_inventory, normal/optimized manifests) and re-run the static self-check.
E3. **The conformance artifact** (sealed): the delta table (file, old shape, new shape, addendum clause), the updated file inventory with hashes, the self-check transcript, PIN CHECK, VERDICT-LINE SCOPE.

OUTPUT: updated code under `evaluator_build_A/` + one sealed artifact
`STAGE8_TASK6_BUILD_A_CONFORMANCE_LANE2_V001.md`
with final lines: `CONFORMED = 3/3 contracts`, `DELTAS = N disclosed (+table)`, `SELF_CHECK = passed (+transcript)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal in the cleanroom, report hashes, STOP (the registrar mirrors). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
