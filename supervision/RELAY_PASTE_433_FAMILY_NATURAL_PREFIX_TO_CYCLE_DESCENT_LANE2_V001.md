## PASTE 433 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE FAMILY-NATURAL PREFIX-TO-CYCLE DESCENT: CONSTRUCT THE OPEN ESCAPE

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-350. This runs IN PARALLEL with relay 432 (Lane 1's bounded j_NM^Q repair of the extension artifact); cite that repair as PENDING where the rank-0/1 square is consumed, and list every such consumption point.
- Foundation (all confirmed by cross-review; verify hashes before reading):
  The sufficiency theorem (yours): `STAGE8_TASK4A_TRACE_KERNEL_SUFFICIENCY_ARM_LANE2_V001.md`, `d9a507fc…` — CONFIRMED Q-349: the prefix family = the cellwise data (triangular isomorphism), availability ratified.
  Your extension cross-review: `STAGE8_TASK4A_MAP1_EXTENSION_CROSS_REVIEW_LANE2_V001.md`, `4bcc286e…` — your own §7.2 S8-A algebra (the obstruction does not persist at family level) is the SEED of this build.
  Q-313 build `ea8f4e9a…`; DoR-015/V005 `7ecf04e9…`; DoR-016/V004 `69f4d93b…`.
- Standing lesson: register sweep before construction; list the entries checked.
- Custody: you build; joint review with Lane 1's repair follows.

TASK — construct the single remaining unbuilt object of the bridge's information layer: the family-natural descent from the prefix-trace family to the physical cycle quotient.

G1. **State the object.** Domain: the complete every-prefix trace family on the no-selection realization family (equivalently, by Q-348, the cellwise relative characters). Codomain: the record-visible physical cycle quotient (V005's conserved cycle lattice, all ranks). Required: family-naturality (no realization/edge/orientation/filtration/cycle-basis member selected), restriction-naturality across stages, reality/batching/zero-extension covariance, agreement with Q-313's Map 1 where cycle rank ≤ 1.
G2. **Construct it.** Develop your §7.2 algebra into the general map: at a rank-r stage, the prefix family supplies enough characters to hit every conserved cycle direction — build the assignment explicitly, prove it well-defined on the quotient, and prove each certificate. Where the automorphism argument bit the terminal scalar (Y3), show explicitly how the family evades it (the edge exchange must act covariantly on your coordinates, not trivially).
G3. **The kernel disclosure.** State exactly what the descent forgets (the lawful kernel) and prove no record-visible cycle is deleted at ANY rank — the certificate the terminal scalar provably failed.
G4. **Boundary honesty.** If full family-naturality fails at some rank or realization class, prove the exact boundary and the residual family — a proven boundary is a full success; do not force totality.
G5. **Regressions:** one-edge (descent lands zero in the cycle quotient while access content survives upstream); the S8-A stage (all three cycle directions hit, c₃ NOT deleted — the direct refutation of the terminal obstruction); reality; batching; identity zero-extension.
G6. **No selections; p symbolic; everything typed honestly** (TYPE-P where proven; TYPE-U where a door is touched — the action/2PI square is NOT this commission and stays TYPE-U).

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK4A_FAMILY_NATURAL_PREFIX_TO_CYCLE_DESCENT_LANE2_V001.md`
with a lead-result block: CONSTRUCTED / CONSTRUCTED-WITH-BOUNDARY / NO-CONSTRUCTION (+ proof), the register-sweep list, and the G1–G6 chain. Seal (.seal.sha256 sidecar), mirror both files byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, then STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No evaluation of alpha, K_*, or roots; no comparison to measured constants. Structural mathematics of declared objects is permitted; if a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
