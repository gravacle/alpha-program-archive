PASTE 324 — CODEX LANE 2 (HIGH EFFORT) — [TASK 2d] ATTEMPT THE MINIMAL OMEGA REALIZATION — THE P5 COMMON-ORIGIN ROLE

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-240)

```text
DOES THE OBJECT EXIST?   THE SPEC does (Q-240's would-build): one origin Omega with executable
                         d_law, d_state, and a ready-carrier bridge, plus common-input /
                         covariance / domain / target-independence / no-supplementation
                         certificates. Q-240 also proved the BAR IS A ROLE (Q-194 standard):
                         MINIMAL_WOULD_BUILD_IS_FULL_ORIGIN_DETERMINATION = false | TYPE-R.
                         No unique microscopic origin is required.
IS THE VERSION CURRENT?  YES. Q-239 (finite skeleton, Q_N quotient), Q-240 (no certified
                         origin; law-characterizes-ready REFUTED -- do not retry it).
ARE ITS INPUTS PRESENT?  THE STARTING MATERIAL IS SEALED AND TYPE-P:
                         RATIFIED_LAW_AND_READY_STATE_HAVE_ONE_DECLARED_INPUT_TUPLE = true.
                         The same finite data already feeds both. The question is whether that
                         tuple, promoted to a declared origin, MEETS THE CERTIFICATES.
```

## 1. THE TASK — BUILD, THEN TRY TO KILL

```text
1  DECLARE the candidate: Omega := the shared executable finite input tuple (the one Q-240
   certified TYPE-P), FROZEN, with its two evaluation maps promoted to descent maps:
   d_law(Omega) = the ratified transition law, d_state(Omega) = rho_ready,N, and the
   ready-carrier bridge as the tuple's own carrier assignment.
2  CERTIFY or refute each required certificate, one by one, with proofs at finite N:
   common-input . covariance (gauge + parent-state, on the Q-239 quotient where applicable) .
   domain . target-independence . NO POST-OUTPUT SUPPLEMENTATION (the frozen-trace test:
   nothing enters after the outputs are read).
3  *** THE KILL-PASS, MANDATORY: IS THIS A REALIZATION OR A RELABELING? State the exact
   criterion separating "the pair (law, rho_ready) renamed Omega" from a genuine common
   origin, USING ONLY SEALED P5 TEXT -- what does P5's own wording demand beyond a common
   input tuple? If P5 demands a DERIVATIONAL root (each output OBTAINED from Omega by the
   maps, not merely BUNDLED), test whether the promoted evaluation maps meet that wording.
   AN HONEST "RELABELING -- FAILS P5's OWN TEXT" IS A COMPLETE ANSWER. ***
4  IF THE TRIVIAL CANDIDATE FAILS: name the SMALLEST enrichment that would pass (what must
   Omega contain beyond the tuple?), typed TYPE-U. Do not build speculative physics; name it.
```

## 2. LEADS

```text
IF THE CERTIFICATES PASS AND THE KILL-PASS SURVIVES -- *** LEAD WITH IT: the P5 witness
completes at the finite level under the role standard, and the two-origins gap CLOSES. Then
attack it hardest: which certificate is weakest? ***
IF RELABELING -- lead with P5's own quoted wording and the exact clause the tuple fails;
that converts a vague unease into a named textual bar.
IF ONE CERTIFICATE FAILS -- name it; one failed certificate is the next work item, not a
program defect.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
