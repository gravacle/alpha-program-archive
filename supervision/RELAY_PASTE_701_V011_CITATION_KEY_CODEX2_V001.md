## PASTE 701 — CODEX 2 LANE = BUILDER A (5.6 SOL EXTRA HIGH) — [TASK 6 / TRANCHE] V011: CONDITION 2'S MAPPING — ONE STATEMENT

(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. THE FIRST-TIME-RIGHT RULES apply. B's resolver package is mirrored: new root ddc09a3d… over 14 members — membership is instance data; B report 08a6bb7b….)

CONTEXT (Q-615): B's envelope confirms V010 whole and the resolver is built with all controls biting. ONE gap remains, found by B and RULED by the registrar as provable by exclusion: V010's condition 2 says `member_key` "resolves exactly one row in R9's own P0-verified evidence table" but states no mapping — and both reachable mappings are barred by V010's own text (payload filename = producer choice, condition 5; constant's own digest = COMPARE(X,X), barred by the row). The ruling: the resolution mapping IS the sealed SOURCE-AND-SPAN CITATION the descriptor already carries (source_sha256 + half-open byte span — the linkage instance_id packs). It is the only principal-ruled, producer-independent, non-degenerate candidate.

TASK (bounded):
O1. AUTHOR SPEC V011: finite delta from V010 (31ccee9c…), 66 rows unchanged, ONE statement: condition 2's member_key resolves through the sealed descriptor's source-and-span citation — R9 keys its P0-verified evidence table by (source_sha256, span) and member_key binds to exactly the row whose citation the sealed descriptor row carries for that atom; no filename, no digest-self-reference, no producer mapping. State it as a closed schema amendment to rd22.r9-ground-atom.v001 (or its successor const), not prose.
O2. Confirm from bytes that parent/producer remain byte-unchanged (expected); any needed change is a displayed finding. Re-pin V010→V011; full pin closure grep value AND name; regenerate inventory; dry-run touched paths.
OUTPUT: `STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE2_V011.md` + one sealed artifact `STAGE8_TASK6_V011_CITATION_KEY_CODEX2_V001.md`
with final lines: `SPEC_DELTA = one statement only / (+items)`, `ROWS_CHANGED = 0`, `CODE_CHANGED = none / (displayed)`, `PIN_CLOSURE = N hits, all resolved`, `DRY_RUN = executed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, STOP (the registrar mirrors; B confirms and keys the resolver; RUN 032 is the confirmation run). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
