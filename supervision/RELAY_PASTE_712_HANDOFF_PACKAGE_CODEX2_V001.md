## PASTE 712 — CODEX 2 LANE (5.6 SOL EXTRA HIGH) — [TASK 6 / CLOSE] THE 7A HANDOFF PACKAGE — ASSEMBLED

LANE GUARD (in force from this relay): before executing any queued relay, verify its header names YOUR lane (CODEX 2); if it names another lane, write a misdelivery report to the outbox and STOP.

(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. THE FIRST-TIME-RIGHT RULES and THE JURISDICTION CHECK apply.)

CONTEXT: Task 6 closes on this package. It is a COMPILATION with citations — no new claims, no re-derivations. Everything it points to is already sealed.
TASK — assemble `STAGE8_7A_HANDOFF_PACKAGE_V001.md`, one document, schema'd sections:
X1. THE FIVE PHYSICS ROWS: A23, A24, A25, A27, A28 — each with its exact remaining conjuncts from the lineage-V2 board (fe4c083d…), the owning road step (10, 10, 9, 8, 10), and the named V2 blocker text carried verbatim.
X2. THE FORMALIZATION REMAINDER: the 58 envelopes by Q-595 classification (SI/EC/KP/MX, effort S/M/L, the six NM! rows called out as the physics gaps in envelope form), plus the four deferred contract items (CV2-03/04/05a/05b) as Step-11 ENTRY CONDITIONS carried from c0af7131….
X3. THE GOVERNING LAW: the frozen recognition preregistration 9f0d12b4… (pointer + its 15 criteria/2 ungrounded/kill-set summary), the F1 split decision e76746ae…, LP-JII, the S03 release condition (Q-557), the jurisdiction check, and the divergence-typing requirement.
X4. THE STANDING RISK LEDGER: the 34 standing falsifiers and 12 open observations from 8a48ad04…, each mapped to the 7A step that can move it; OBS-22's pre-registered statement displayed verbatim as the junction arc's opening test.
X5. THE JUNCTION ARC ENTRY STATE: what step 6 needs on day one — the JD-3 cell-rule obligations, the U1 joint-landing gap as the likely first work item, and the two evaluator entry conditions that gate any 7A machine execution.
Every pointer carries name + SHA-256 + span where applicable; PIN CHECK all of them; no unsealed source cited.
OUTPUT: the package + seal. Final lines: `SECTIONS = 5`, `POINTERS = N, all pinned`, `NEW_CLAIMS = 0`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 712_DONE.md to the outbox, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
