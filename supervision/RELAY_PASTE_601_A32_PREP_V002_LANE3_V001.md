## PASTE 601 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 6 / STEP 1] A32 PREP V002: THE SIXTEENTH SOURCE AND THE RULED PARENTS

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-534.
- Your prep (`0f5976a2…`) was reviewed with one decisive find: read the review first — `alpha-program-archive/workspace/STAGE8_TASK6_A32_PREP_REVIEW_DARIO_V001.md`, SHA-256 `d13bcf1fdadc5f70fd6e2d1a854a8d5b103c5756197f8fb039bad3ce08a126fa` — especially the missed-source adjudication and the (M5a) weakening-by-inheritance analysis.
- THE SIXTEENTH SOURCE: `PREREGISTRATION_ENCODING_PRECEDENCE_PRINCIPAL_DECISION_2026-07-29.md`, SHA-256 `70c4080e…` (complete the hash from its sidecar and verify) — the principal act ruling the machine encoding stage_dependencies AUTHORITATIVE and demoting the V011 prose DAG. Read the authoritative encoding ITSELF (the 11-node graph), not the ruling's summary.
- THE LIVING-FILE PIN RULE now applies (LOCKED_PROCESS): cite the questions register BY ENTRY, never by whole-file hash — repair your source-10 row accordingly.
- Custody: builder repairs; a bounded Dario confirm follows; the principal ratifies after. Bounded delta.

TASK — produce prep V002:
C1. **Census 16:** the sixteenth source consolidated with its obligations extracted from the AUTHORITATIVE ENCODING directly; the ladder rebuilt where it stood on the demoted V011 prose DAG; any obligation whose content changes under the machine encoding re-derived and re-mapped.
C2. **(M5a) with the ruled parents:** the subgate's conjuncts renamed and re-ordered per the seal graph — HOLDOUT-UNIVERSE-SEAL ← [SPEC-SEAL]; PREDICTION-MAP-SEAL ← [HOLDOUT-UNIVERSE-SEAL, QSPEC-SPEC-SEAL] — the sealing SEQUENCE binding ("this act cannot loosen any gate" quoted in place); the evaluator-certified conjunct list complete with no inherited weakening.
C3. **The instrument revised:** the heterogeneous tension typing INSIDE the operative text — M9's permanence by Q-25 traveling with every headline; P4 named as the J-II type-crossing (the same object of record, Q-521/Q-534) so the ratifying reader sees it; the living-register citation repaired to entries; the four required items retained; attainment-by-preparation still refused by enumeration.
C4. **Battery:** F_PLDEC; anti-tuning with the cross-sector-unit row; surface anchor; dependency re-audit; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_A32_PREP_LANE3_V002.md`
with final lines: `CENSUS = 16`, `M5A = RULED_PARENTS_NAMED (+sequence bound)`, `INSTRUMENT = REVISED`, `REGISTER_CITATIONS = by_entry`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
