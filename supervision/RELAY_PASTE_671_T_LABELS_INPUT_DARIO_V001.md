## PASTE 671 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / BUILD] T0-T3 IN THE INPUT CONTEXT; T4 IS TERMINAL-ONLY

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors.)

CONTEXT (Q-601, registrar adjudication): Codex 2's audit found its own parent fabricating T4 = T3 in the verifier-input ledger — a post-verifier snapshot attested before the verifier runs. Ruling from the program's absent-vs-fabricated principle: the verifier-input trust record carries T0-T3 EXACTLY; T4 exists only in the terminal ledger as parent testimony; fabrication barred. A's parent is being fixed in parallel (672).

TASK: your `revalidate_trust_snapshots` (and any other consumer) accepts EXACTLY T0-T3 in the verifier-input context — T4's ABSENCE is the lawful state there, its PRESENCE a fault ("a fabricated snapshot must never validate"); keep the T0-T4 form only for any terminal-ledger validation mode you expose. Dry-run demonstration; disclosed delta; canonical instance untouched unless a field list changes (then sidecar too); PIN CHECK.

OUTPUT: updated verifier + one sealed artifact
`STAGE8_TASK6_T_LABELS_INPUT_DARIO_V001.md`
with final lines: `INPUT_CONTEXT = T0-T3 exact (T4 presence = fault)`, `TERMINAL_MODE = T0-T4 (if exposed)`, `SELF_CHECK = passed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors and re-invokes). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
