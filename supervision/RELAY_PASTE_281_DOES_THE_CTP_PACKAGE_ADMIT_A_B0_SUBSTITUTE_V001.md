PASTE 281 — CODEX LANE 1 (HIGH EFFORT) — DOES `CTP_PHYS_INPUT_PACKAGE` ADMIT A **B0 SUBSTITUTE**?

*** READ `alpha_supervision/LOCKED_PROCESS.md`. **THE FOUR STEPS THERE ARE REVIEWER STEPS — YOU SEAL,
MIRROR, REPORT AND STOP.** ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT

```text
DOES THE OBJECT EXIST?   YES -- CTP_PHYS_INPUT_PACKAGE is named and specified across several sealed
                         artifacts, including a 1600-line load-bearing stop spec.
IS THE VERSION CURRENT?  CHECK IT. Three symbol collisions have been found at the target today.
ARE ITS INPUTS PRESENT?  *** NO -- ONE IS B0, AND THAT IS THE QUESTION. ***
```

## 1. WHY THIS IS NOW THE SHARPEST QUESTION ON THE BOARD

**A reviewer trace ran the dependency chain to its root today:**

```text
alpha = 1/(4 pi K_*)  <-  K = B_ind(K)  <-  absolute_B_ind  <-  complete induced CTP operator
   <-  raw-correlator-to-retarded-Hessian map (SEAM 9, whose ALGEBRA IS ALREADY FIXED)
      <-  *** CTP_PHYS_INPUT_PACKAGE ***  <-  P1-P8 and T1-T6  <-  ...
```

**And the package's own spec stops dead:**

> `B0 = COMPLETE_MICROSCOPIC_BOUNDARY_OPERATOR`
> `COMPLETE_MICROSCOPIC_BOUNDARY_OPERATOR_derived = false | TYPE-U`
> *** "Specification stops here. This artifact does not specify `B0`'s internal construction." ***

*** `B0` IS THE OBJECT **SIX INDEPENDENT ROUTES FAILED TO DETERMINE** (Q-106, Q-110, Q-111, Q-112,
Q-113, Q-114). IF THE PACKAGE NEEDS `B0` ITSELF, THE PROGRAM IS BLOCKED AT ITS HARDEST OBJECT. ***

## 2. BUT THE PROGRAM HAS ALREADY ESTABLISHED, THREE TIMES, THAT `B0` ADMITS SUBSTITUTES

```text
Q-151  the tower needs a RESPONSE-COMPLETE FINITE PRESENTATION -- weaker than full origin. TYPE-R x2
Q-156  sector-reach admits *** "B0 provenance OR THE COMPLETE B0-REPLACEMENT PROVENANCE" ***
Q-158  P5 needs a DESCENT PRESENTATION, not a unique microscopic origin. Distinct sources may
       satisfy it if they descend to ISOMORPHIC CERTIFIED PACKAGES.
```

> ***THE QUESTION: **DOES `CTP_PHYS_INPUT_PACKAGE` REQUIRE `B0` ITSELF, OR WOULD A B0-REPLACEMENT
> PRESENTATION SATISFY IT?***

## 3. THE TASK

**3.1 READ WHAT THE PACKAGE ACTUALLY CONSUMES `B0` **FOR**.** *** NOT THAT IT NAMES `B0` — **WHAT
FUNCTION `B0` PERFORMS IN THE SPEC.** The stop spec says `C0`/`U1-U3` "must be derived from" it. **NAME
THOSE FOUR OBJECTS AND SAY WHAT EACH NEEDS.** ***

**3.2 COULD `C0`/`U1-U3` BE DERIVED FROM A DESCENT PRESENTATION INSTEAD?** *** Q-158's standard is
explicit: distinct sources satisfy the requirement if they descend to **isomorphic certified
packages**. **APPLY THAT STANDARD HERE AND SAY WHETHER IT REACHES.** ***

**3.3 CHECK THE FOUR PACKAGE REQUIREMENTS AGAINST A SUBSTITUTE.** The package must supply one named
gauge-fixed physical space with: (a) `G` a bilocal correlator with an invertible physical inverse;
(b) `Log_0` and contour measure defined; (c) branch metric, reality involution, compound-index/DeWitt
conventions; (d) contacts, boundary data, Ward-compatible endpoint domains. *** WHICH OF (a)-(d)
GENUINELY NEEDS A **MICROSCOPIC ORIGIN**, AND WHICH NEEDS ONLY A **CERTIFIED PRESENTATION**? THAT SPLIT
IS THE CORE OF THIS RELAY. ***

**3.4 THE `B0` SYMBOL COLLISION — DO NOT LET IT CONTAMINATE THE ANSWER.** *** THE PACKAGE'S `B0` IS AN
OPERATOR/DYNAMICS ROLE. `STAGE8_T7_COMPLETED_CONDITIONED_DUHAMEL_IDENTITY_AND_INTERCHANGE_SCHEMA_SPEC_V001.md:188-204`
DEFINES A DIFFERENT `B0(K, rho): q_K(0) = z_c,K(0,0) != 0`, AND SAYS **"the anchor is a hypothesis,
never a structural identity."** **SAY WHICH `B0` EACH REQUIREMENT NEEDS.** ***

**3.5 VERDICT, TYPED.** `B0` REQUIRED · SUBSTITUTE SUFFICES · SPLIT (name which parts) · NO_VERDICT.

## 4. LEADS

```text
IF A SUBSTITUTE SUFFICES -- *** LEAD WITH IT. It would open the front-line target for the first time
and route around the object six attempts could not reach. THEN ATTACK IT: say exactly what the
substitute must certify, and whether ANY existing artifact supplies it. ***
IF B0 ITSELF IS REQUIRED -- *** LEAD WITH THAT AND SAY WHY THE THREE SUBSTITUTE FINDINGS DO NOT
REACH. A hard blocker honestly named beats a soft one hopefully worded. ***
IF IT SPLITS -- *** LEAD WITH THE SPLIT. "(a) and (b) need only a presentation; (c) and (d) need the
origin" would tell the program exactly what to build and what to route around. ***
```

*** DO NOT MANUFACTURE A SUBSTITUTE. Q-156's clause is "**the COMPLETE B0-replacement provenance**" —
**COMPLETE** IS LOAD-BEARING. A partial presentation that merely avoids naming `B0` is the same
blockage with better wording, and the concreteness clause already survived one weakening: *"'Some
source exists' or 'all fields appear in one file' is insufficient."* ***

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
