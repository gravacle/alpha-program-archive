PASTE 170 — CODEX 1 — ITEM: THE 13 JSON AUTHORITY LEDGERS NOBODY HAS EVER SWEPT

2026-07-31. Snapshot; later rulings override. **REGISTER HEAD AT ISSUE: Q-70.**

```text
CODEX 1 (you) = the 13 unswept JSON authority ledgers          [relay 170]
CODEX 2       = specify B0, the package's load-bearing stop    [relay 171]
EINSTEIN      = axioms-or-debts boundary classification        [relay 169]
```

**OFF LIMITS TO YOU:** `CTP_PHYS_INPUT_PACKAGE` internals and B0 (Codex 2); the boundary
axiom/debt classification (Einstein). **Commit normally — the overnight seal-only protocol is lifted.**

Fences: never touch `a32_holdout/custodian_private/`. **Do not compute** alpha, kappa_record,
kappa_Thomson, a coupling, a radius, a scale, a root, an eigenvalue, a beta function, `E_R`, `T_R`, or any
absolute interval. **No comparison to any measured constant.** **Q-54:** every negative typed;
**`NO_VERDICT` is legal.**

---

### A HOLE NOTHING HAS EVER LOOKED INTO

Einstein's canonical-registry pass (Q-66) escalated one scope hole explicitly:

```text
THE 13 JSON AUTHORITY LEDGERS HAVE NEVER BEEN SWEPT BY ANYTHING
```

*** EVERY AUDIT THIS PROGRAM HAS RUN IS MARKDOWN-SHAPED. *** The dependency census walked `.md`. The
`untyped_negative` gate check scans `.md`. The registry sweep, the premise classification, the boundary
walk — all `.md`. **13 JSON ledgers carry authority and have been invisible to all of it.**

**Whatever is in them is currently invisible to every finding in the register**, including the 172-node
graph, the acyclicity verdict, the 17-of-26 premise count, and the 3309 untyped-negative baseline.

### THE TASK

1. **FIND AND LIST THEM.** All 13, at exact paths, with what each claims authority over. **If there are
   more or fewer than 13, say so — Einstein's count was incidental to another task and has not been
   verified.**

2. **WHAT DO THEY ASSERT?** For each: flags, statuses, verdicts, provenance. *** ANY FLAG ASSERTING
   `derived`, `proved`, `excluded`, `selected`, `PASS`, or `authorized` IS THE POINT OF THIS SWEEP. ***

3. *** DOES ANY OF THEM CONTRADICT A SEALED MARKDOWN RULING? *** **This is the highest-value question.**
   A JSON ledger asserting `derived = true` where the register records TYPE-U, or a PASS where the corpus
   records NO_VERDICT, **would mean a finding has been sitting unread while the program reported the
   opposite.** **Report every contradiction; do not adjudicate which side wins — that is a principal act.**

4. **DOES ANY OF THEM ADD NODES OR EDGES TO THE DEPENDENCY GRAPH?** Q-64 built the graph from `.md`
   `would-build` fields only. **If a ledger names a construction dependency, the graph is incomplete.**
   *** IF ANY LEDGER EDGE WOULD CREATE A CYCLE, STOP AND LEAD WITH IT. *** Test edges held out by kind —
   a test gated on an unbuilt object is the Q-52 problem, not a construction cycle.

5. **DOES ANY LEDGER LEAF REACH `derived = true` GROUND?** Q-70 reframed this: the question is not whether
   a floor of derived objects exists but **whether the boundary is axioms or debts.** **Do not classify
   them — Einstein holds that — but report any ledger leaf that is plainly one or the other.**

### CONSTRAINTS

- **Word-boundaried matching only.** The substring failure mode has produced false positives in the
  corpus, in an audit script, and in the audit of the audit.
- **Do not merge anything, and do not identify a flag with the object that discharges it** (Q-69 standing
  rule — two conditional cycles found overnight both fire on exactly that operation).
- **Report refutations; never repair them.** **If a ledger contradicts a register ruling, the register may
  be wrong. Say so.**
- **State your scope**: roots, exclusions, queries. **A sweep with unstated scope is a void TYPE-S.**

---

REPORT BACK, with **CODEX 1** on your first line: hashes, exact committed paths, gate verdict, the five
answers, **every negative typed.** *** IF ANY LEDGER CONTRADICTS A SEALED RULING, LEAD WITH IT. *** The
program has been auditing one file format and calling the result corpus-wide.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
