## PASTE 680 — CODEX LANE 2 = BUILDER A (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / S TRANCHE] BATCH 1: THE TEN CHEAPEST ENVELOPES

(Same Lane 2 session rules: cleanroom writes; the registrar mirrors. The C76 ruling governs; the Q-604 guard stands: partial-row success is never evaluator closure.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-604.
- THE MAP (governs membership and grounding): `STAGE8_TASK6_ENVELOPE_FORMALIZATION_MAP_LANE2_V001.md` = `e85a6113e5b45624d19f987ae2603f63ac418df10f33669cc6a44742e5918ed5` — its §4.2 ten-cheapest ranking is this batch: V009-06, V010-12, V008-10, V010-14, V010-11, V009-08, V009-01, V009-03, V008-05, SP2-02.
- THE LAW OF ENVELOPES: an envelope is a MACHINE SERIALIZATION of structure ALREADY DISPLAYED in sealed prose, grounded by the map's pinned spans — carrier tables, DAGs, typed graphs, exact identities — in the canonical forms the descriptors' opcodes consume. The map's own custody warning binds: "a delivery relay must first locate an actual sealed derivation…; it may not serialize the requirement sentence as its own proof." Every envelope cites its grounding spans; any span that proves insufficient on close reading makes that envelope ABSENT_OF_RECORD-still, reported not papered.
TASK:
A1. For each of the ten: build the evidence payload files (canonical JSON per the descriptor's input contract) under `evaluator_build_A/inputs/evidence/`, update the evidence manifest rows (available=true, payload digests, grounding citations), and show per-envelope: the sealed spans consumed, the serialization decisions, and the opcode-consumability check.
A2. Static self-check incl. evidence-manifest validation and declared_root recomputation; disclosed delta; PIN CHECK.
A3. State plainly what this batch does NOT do: no row passes until the chain runs; ten envelopes do not close the evaluator; the board and seals are untouched.

OUTPUT: envelopes + updated manifest + one sealed artifact
`STAGE8_TASK6_S_TRANCHE_BATCH1_LANE2_V001.md`
with final lines: `ENVELOPES = N/10 built (+per-envelope grounding)`, `STILL_ABSENT = (any, with reasons)`, `DECLARED_ROOT = old->new`, `SELF_CHECK = passed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal in the cleanroom, report hashes, STOP (the registrar mirrors; the chain runs after Dario's envelope review). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
