PASTE 381 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THIRD-PASS ADVERSARIAL REVIEW OF FIELD_SIGNATURE_PHYS V005 — THE CYCLE CURRENTS UNDER FIRE, THEN PACKAGE DoR-015 OR KILL

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-298)

```text
DOES THE OBJECT EXIST?   YES: V005 (7ecf04e9...): X_phys = framed/(Gate-4 rephasing +
                         path-invisible); both kill-tangents in the kernel; the sealed
                         composition-loop witness survives; physical currents = the CYCLE
                         family u_c, c in ker(B_N^T), dPhi_c = u_c; no augmentation.
                         YOU killed V003 and V004 -- this is the shape your two kills
                         forced. Kill it too if you can.
IS THE VERSION CURRENT?  YES through Q-298.
ARE ITS INPUTS PRESENT?  V005, both kill determinations, the V003 quotient apparatus,
                         the Q-288 ledger + Door F.
```

## 1. THE ATTACKS — THE CYCLE FAMILY IS THE TARGET

```text
W1 SEPARATION BY CYCLES: do the cycle currents SEPARATE X_phys? The danger is now
   UNDER-separation: connections differing by something every CYCLE misses but some
   physical structure sees (open-path content that is NOT gauge -- e.g., content between
   distinct roots/boundary components if the complex has them). Construct the test on
   the admitted family: is ker(all u_c) exactly the quotient kernel? If open-path
   physical content EXISTS (boundary-to-boundary transport that no rephasing kills),
   the cycle family under-separates and V005 has cut too deep -- THE EXACT DUAL of the
   V003/V004 errors. This single check decides.
W2 THE KERNEL EXACTNESS: re-verify both kill-tangents in the kernel AND the surviving
   witness's survival, independently.
W3 dPhi_c = u_c re-proven; basis-freeness (ker(B_N^T) genuinely basis-free? N-dependence
   coherent under zero-extension?).
W4 CONSUMERS: every consumer re-checked on cycle-invariant content -- ESPECIALLY the
   raw-G contract and B_ind's chain (does anything downstream genuinely need OPEN-path
   content? If yes, W1's answer matters doubly).
W5 FULL REGRESSION: seams, doors (F re-verified), six-count, no-selection, E_post/
   incidence/T_cyl, DoR-008 restriction, no forbidden completions, no augmentation
   reintroduced anywhere.
W6 IF ALL PASS -- PACKAGE DoR-015 in one block: adopts (the six authored structures;
   X_phys as THE physical carrier with the double quotient as definition; the cycle
   currents as THE physical response family; the torsor/intertwiner families under
   no-selection; the derived components as premises), opens (the raw-G rerun, the lift,
   the background fiber, the consumption signature -- the verdict chain's execution),
   falsifiers (DoR-008; all door flags incl. F; no-selection; Door D's physical image),
   and the FINAL PROVENANCE LINE for OBS-17 (derived vs authored, the counts).
VERDICT: RATIFICATION-READY (with the package) / REPAIR-THEN-READY / DEAD.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
