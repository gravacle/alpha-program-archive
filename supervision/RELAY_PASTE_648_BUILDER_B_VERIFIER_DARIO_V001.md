## PASTE 648 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / BUILD] THE INDEPENDENT VERIFIER, PER SPEC V005

(Same Dario session rules. ARCHIVE-SIDE ONLY: write code under a new archive-workspace subdirectory `evaluator_build_B/`; the registrar mirrors. You are BUILDER B of the RD-22 custody graph.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-585 (the RD-22 authorization of record: `DECISION_RD22_BUILD_AUTHORIZED_2026-08-07.md` in archive supervision, SHA-256 `ff84c4a8ba5c7f8eabfbcc587475d3a5050c21d758a2788c5b9e28b7ee022340`; read it — it binds scope, pin, and custody).
- THE GOVERNING SPEC: archive `STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE2_V005.md` = `f8d1a7dc02798229f0ea22b0e855d1d09bb4a5b7eea9069c419357a56b6a067b`. Verify before reading. Its R9 section defines your duties.
- **INDEPENDENCE IS THE LAW OF THIS BUILD:** you may consume ONLY the sealed spec, the sealed schemas, immutable inputs, and output contracts. You may NOT read, import, or consult Builder A's code, its expected-verdict generator, its comparison function, or any mutable receipt — Builder A's `evaluator_build_A/` directory is OFF LIMITS even if visible. If you find yourself wanting a detail that only A's code could supply, that detail is a SPEC GAP to report, not a peek to take.
- THE RUNTIME PIN (authorized): snapshot `50a6fc14…` + gate `2ad7f72a…` + trust root extracted at R0 and displayed.

TASK:
C1. **The verifier package** under archive `evaluator_build_B/`, per the spec's R9: independently obtain and rehash every input and exact compiled source buffer; strict-parse all manifests, outputs, and receipts; check the exact 63+3 census and the 56/10 class partition; replay each PASS predicate from evidence bytes; replay every fixture's expected result; reclassify runtime/module/native/open/process/network/mutation events; revalidate before/after trust records; compare normal and optimized semantic outputs; check the authorization hash and gate discipline; check all authority-firewall fields false unless separately authorized. Canonical-JSON verdict output per the schema.
C2. **Content addressing throughout;** no load-bearing `assert`; explicit fail-closed branches; no path trust.
C3. **The build manifest artifact** (markdown, sealed in the archive workspace): complete file inventory with per-file SHA-256; entry points and the invocation contract for Custodian C; what is implemented vs deferred (listed, not silent); your self-check transcript (syntax/schema validation only — the chain is invoked by Custodian C, never by you).
C4. Battery: F_PLDEC; anti-tuning; PIN CHECK; VERDICT-LINE SCOPE RULE.

OUTPUT: code under archive `evaluator_build_B/` + one sealed artifact
`STAGE8_TASK6_EVALUATOR_BUILD_B_MANIFEST_DARIO_V001.md`
with final lines: `PACKAGE = complete / (+deferred list)`, `FILES = N (+inventory hashed)`, `INDEPENDENCE = sealed inputs only (attested)`, `SELF_CHECK = syntax/schema only (+transcript)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output artifact name already exists, STOP and report — do not overwrite. Seal in the archive workspace, report hashes, STOP (the registrar mirrors and invokes). No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
