PASTE 137

2026-07-30, CODEX LANE 2. Snapshot; later rulings override. Lane 1 is checking a modular-theory candidate
for the conjugate energy under paste 136 — do not touch `Gamma_K`, joint-selector, measure or namespace
artifacts.

Your paste-134 work is verified and committed. **Your comparator preregistration is the most consequential
artifact produced today**, for a reason you stated but did not draw out: `formula_available_families = 1`.
Five of six families have no same-alpha comparator at all, and the survivor is the electron zero-momentum
magnetic anomaly.

Two principal rulings follow from it, plus a finding that is yours by rights.

Fences unchanged. `alpha_computed = false; proof_authorized = false`. **NEVER touch
`a32_holdout/custodian_private/`.**

---

### ITEM 1 — THE FINDING YOUR ARTIFACT IMPLIES

`FINDING_D_TEST_DEGENERATE_ON_PRECISION_OBSERVABLES_2026-07-30.md`:

*** ON THE ELECTRON MAGNETIC ANOMALY, `D >= 5` IS PASSED BY A REFUTED THEORY AND FAILED BY A CORRECT
ONE. *** QED and experiment agree to roughly ten figures, so `sigma_comp` and `sigma_meas` in `D`'s
denominator are minute. Any BID prediction differing by more than a whisker gives `D` far above 5 — so the
threshold that was meant to mean "a substantial, hard-to-fake difference" here means "differs at all." And
any difference that large is already excluded by measurement, so **passing would be evidence BID is
wrong.**

The escape via a large `sigma_BID` cuts the other way: a prediction too uncertain to conflict with QED is
too uncertain to make the claim FINAL-CLAIM requires.

Register this as a finding on the candidate SPACE, not on A32's machinery, which is sound.

### ITEM 2 — REGISTER PRINCIPAL RULING Q-27: "unused" IS IDENTIFIED

Authority: `MB Work/alpha_supervision/SLOT18_ADMISSIBILITY_PRINCIPAL_DECISION_2026-07-30.md`.

**`unused` IS the sealed clause at `V011:2080-2089`** — "be absent as an outcome or target from the
complete historical lineage" — **with "complete historical lineage" operationalized by the scope V011 rule
11 already defines** for the contamination audit: "search all accessible repository history, attachments,
logs, prompts, tickets, browser-history exports."

**Register it as an IDENTIFICATION, not a definition.** Both halves were sealed text all along; what was
missing was the statement that they are the same condition. No new predicate is adopted.

Frozen: **F-U1** — if the rule-11 scope proves unbounded or unsearchable in practice, the identification
FAILS and `unused` returns to undefined; report that rather than substituting a narrower scope. **F-U2** —
the stronger reading (never *consumed by the derivation*, whether or not it appears in the record) was
considered and NOT adopted; a candidate passing the lineage test that is later shown to have shaped the
construction is not protected, and must be escalated.

### ITEM 3 — REGISTER PRINCIPAL RULING Q-28: A THIRD ADMISSIBILITY CLAUSE

> An observable is admissible only if **the comparator's own predictive uncertainty is not so far below any
> plausible BID deviation that a `D >= 5` difference would be automatically in conflict with established
> measurement.**

**FENCE POSTURE, register it explicitly: this is a condition on comparator PRECISION, never on a measured
central value.** `D` already admits `sigma_meas` into its denominator, so admitting uncertainties into an
admissibility test is the posture the protocol already takes. No measured central value enters and no
comparison to one is performed.

**APPLIED AT THE ELIGIBILITY STAGE, before the beacon selects** — not at collection, so the universe needs
no re-collection; and not after selection, which would be selection-by-outcome.

Frozen: **F-C3** — if clause 3 cannot be applied without consulting a measured central value, it breaches
the comparison fence and must be WITHDRAWN. **Report the breach; do not soften the clause to fit.**

### ITEM 4 — APPLY CLAUSE 3 TO THE SIX FAMILIES AND REPORT WHAT SURVIVES

**I EXPECT ZERO. SAY SO IF THAT IS WHAT YOU FIND.**

Family 6 holds the only comparator payload and is the clearest failure of clause 3. The other five have no
comparator, so clause 3 cannot even be evaluated for them — record that as `NOT EVALUABLE` rather than as
a pass or a fail.

**"An empty eligible set fails closed" is an honest outcome and not a defect.** A terminal claim resting
on an observable where the test cannot discriminate is worth less than no claim. **DO NOT relax clause 3
to keep a family alive**, and do not treat a `NOT EVALUABLE` as a provisional pass.

**THEN, AND THIS IS THE CONSTRUCTIVE HALF:** state what an observable satisfying **all three** clauses
would have to look like — structure-sensitive, absent from the rule-11 lineage, and carrying a comparator
whose precision leaves room for a meaningful difference. Roughly: not yet precisely measured, or measured
in a regime the comparator does not cover, or carrying comparator uncertainty large enough that a real
difference is informative rather than fatal. **Do the six families suggest where such an observable would
live, even if none of them is one?** That is the question worth your next turn.

### ITEM 5 — THE BRIDGE. THIS IS WHERE SLOT 18 IS ACTUALLY BLOCKED, AND IT IS NOT WHERE ANYONE THOUGHT.

Your dependency map establishes something the charter did not know: **no family has a direct edge to
Q-21.** Slot 18 is NOT waiting on `Gamma_K`. Good — the two workstreams really are parallel.

But it also shows that **three families already have quantitative theory-side output** — finite
global-holonomy response, temporal-plaquette / connected-kernel diagnostics, and causal-record
durability — and that each is missing the same four-part bridge: **the physical observable, the A32
candidate, the comparator, and the sealed uncertainty.**

*** THAT IS AN INTERFACE PROBLEM, NOT A CONSTRUCTION PROBLEM. The theory already produces numbers.
Nobody has said what dimensionful physical quantity they are numbers OF. *** And it explains the
comparator famine from the other side: you cannot select a same-alpha comparator until you know which
physical quantity you are predicting. **The comparator gap and the observable-mapping gap are one gap.**

FOR ONE FAMILY OF YOUR CHOOSING — I would take finite global-holonomy response, but say why if you pick
another — state precisely:
1. **What dimensionful physical quantity would the existing diagnostic correspond to?** Not a
   prediction — the identification of the observable, with its units.
2. **Is it a scalar?** The Q-25 forward condition means the present collector accepts scalar candidates
   only; a vector or multi-component observable requires collector revision and re-commitment BEFORE any
   prediction exists. If the natural observable is multi-component, that is a finding and it changes what
   must happen first.
3. **What exactly is missing** between the existing diagnostic and that observable — a normalization, a
   unit map, a kinematic domain, a continuum limit? Name it at file:line.
4. **Would that observable plausibly satisfy all three admissibility clauses?** Structure-sensitive,
   absent from the rule-11 lineage, and carrying a comparator loose enough for a meaningful difference.

**DO NOT** derive the observable's value, or assert admissibility. This is the identification of a target,
in the same register as lane 1's spec work.

**DO NOT** derive any prediction, evaluate any expression, compute any `mu`, `sigma` or `D`, unmask
anything, or enter the custodian directory.

---

REPORT BACK: hashes, exact committed paths, gate verdict, the per-family clause-3 verdict plus the
characterization (item 4), and the bridge identification (item 5). **If the answer is zero admissible families, lead with that** — it is the most
important thing the program could learn about its terminal claim today, and far better learned now than
after a prediction is frozen.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
