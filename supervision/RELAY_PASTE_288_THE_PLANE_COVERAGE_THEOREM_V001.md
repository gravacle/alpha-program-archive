PASTE 288 — CODEX LANE 2 (HIGH EFFORT) — [TASK 3d] PROVE OR REFUTE THE PLANE-COVERAGE THEOREM

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-204)

```text
DOES THE OBJECT EXIST?   YES: the 16-dim odd/odd span, gradings Z_w, and the Q-204 finding.
IS THE VERSION CURRENT?  YES -- this continues relay 286's own coverage failure.
ARE ITS INPUTS PRESENT?  YES -- fully instantiated finite structures. No schemas anywhere.
```

## 1. THE CLAIM TO PROVE OR KILL

**Q-204 (verified): the law family is INFINITE — `w(n) = (n+1,n,1)` all have nullity 2 — but every
computed member lands on the SAME kernel plane as `(2,1,1)`.** The reviewer's conjecture, which this
relay must settle:

```text
*** PLANE-COVERAGE THEOREM (conjectured): EVERY nullity-2 grading law's kernel is one of the FOUR
sign-pair planes. Equivalently: the map (law -> kernel plane) has image of size at most 4 on the
nullity-2 stratum. ***

Suggested mechanism to prove or break: on the verified X_E/Y_E support, the commutator acts
diagonally in the ladder basis with weight w . s per sign vector s in {+1,-1}^3; nullity 2 means
EXACTLY ONE +/- sign pair is annihilated (w . s = 0); four pairs exist, so at most four planes.
*** THE GAP IN THAT SKETCH: the reviewer's ladder rule FAILED on 15 of 49 triples OFF this support
(Q-180 correction history). PROVE the support restriction first or the theorem inherits the hole. ***
```

*** THE PRIOR IS AGAINST THE REVIEWER: his scorecard on exactly this kind of claim is 6 refuted of
10. HUNT THE COUNTEREXAMPLE: a nullity-2 law whose kernel is OFF the four planes. Search
structurally, not by enumeration -- enumeration is what Q-204 just killed. ***

## 2. IF THE THEOREM HOLDS — THE CONSEQUENCE, STATED IN ADVANCE

```text
1  the selection family becomes the FOUR PLANES: finite, instantiated, Q-200-COMPLIANT
2  the infinite law-multiplicity is DESCRIPTIVE REDUNDANCY -- and that IS the protocol's step-2
   equivalence, DERIVED rather than adopted. Coverage is REPAIRED; selection becomes legal.
3  Task 3d resumes at step 3 with a lawful family of four.
*** DO NOT PROCEED TO STEP 3 IN THIS RELAY. THE THEOREM IS THE DELIVERABLE. ***
```

## 3. LEADS

```text
IF PROVED -- lead with the theorem and its exact hypotheses (especially the support lemma). Type
each: derived / adopted. Then STOP.
IF REFUTED -- *** LEAD WITH THE COUNTEREXAMPLE LAW AND ITS PLANE. Seam 10 would need a new physical
input and the program must know that before spending anything more here. ***
IF THE SUPPORT LEMMA CANNOT BE CLOSED -- lead with exactly what fails off the X_E/Y_E support; the
theorem's scope shrinks to the provable stratum and says so.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
