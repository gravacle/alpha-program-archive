## PASTE 730 — CODEX 2 LANE (5.6 SOL EXTRA HIGH) — [7A / STEP 11] THE PROGRAM-FUTURE RECLASSIFICATION SWEEP

PICKUP-ACK + LANE GUARD in force (CODEX 2).
(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. ALL standing laws apply.)

CONTEXT (Q-634): your 728 established a new carrier class — PROGRAM-FUTURE: elements whose realizations ARE the road's own future milestones (the V008-10 stage seals). The planning map (Q-626) must absorb it.
TASK:
OO1. Sweep all 57 rows' missing elements (your split artifact + the batch records): reclassify each D/U/C element that is actually PROGRAM-FUTURE (realized only by later road steps — name WHICH step realizes it). Evidence per reclassification: the status/ceiling text recording the realization as pending (source + span, block-covered).
OO2. THE CORRECTED PLANNING BANDS: restate the authoring bands (Q-625/Q-626) with program-future elements REMOVED from the pre-alpha workload and listed as auto-completing at their owning steps. State what remains truly pre-alpha in step 11.
OO3. Flag any row whose CRITERION could never pass before its program-future elements exist — those rows' expected pre-alpha status is honest INPUT_INTEGRITY failure, and lineage V3's expectations must say so (finding, not fix).
OUTPUT: one sealed artifact `STAGE8_7A_PROGRAM_FUTURE_SWEEP_CODEX2_V001.md`
Final lines: `RECLASSIFIED = N elements -> PROGRAM-FUTURE (owning steps named)`, `PRE_ALPHA_REMAINDER = corrected bands`, `V3_EXPECTATION_FLAGS = rows listed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, write 730_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
