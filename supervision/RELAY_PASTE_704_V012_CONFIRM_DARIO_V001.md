## PASTE 704 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / TRANCHE] V012 CONFIRMATION — THE LAST GATE BEFORE RUN 032

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. THE FIRST-TIME-RIGHT RULES apply. A's V012 is mirrored: spec 382052c4…, report 135424d1…, inventory fbcd75f2…. Registrar pre-verified from bytes: ONLY descriptor_sha256/descriptor_bytes_sha256 and the `inputs` linkage field changed on the V009-06 row — every criterion-bearing field byte-identical; payload census 21 with the source 13cf1e17… supplied; parent/producer byte-unchanged.)

CONTEXT (Q-616 cure shipped): the sealed row now carries its own grounding citation, and the source is a supplied payload. Your resolver was already proven on exactly this shape in 702.

TASK (bounded):
R1. CONFIRM THE AMENDMENT INDEPENDENTLY: recompute the old->new row delta from bytes (your own derivation, not the registrar's or A's display); the anti-tuning question is yours to answer of record — is this linkage completion ONLY, with the criterion, procedure, predicate, and partition untouched? Any drift is a finding.
R2. RESOLVE ON REAL INPUTS: with the amended row and supplied source, r_ground must now resolve through the citation key — show it end-to-end (rehash from the verified source at the sealed span; both perturbation controls still flip; producer_carrier false). Update your contract only where V012 requires (the new row hash pin); re-pin sidecar+instance together from final bytes.
R3. Dry-run touched paths; PIN CLOSURE grep value AND name (old row hash b63a1335…, V011 d48e0fa7… live references, superseded roots).
OUTPUT: updated package + one sealed artifact `STAGE8_TASK6_V012_CONFIRM_DARIO_V001.md`
with final lines: `AMENDMENT = linkage only, independently recomputed / (+findings)`, `R_GROUND = resolves on real inputs (controls hold)`, `VERIFIER_ROOT = <hex> (changed/unchanged)`, `PIN_CLOSURE = N hits, all resolved`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors and invokes RUN 032 — the confirmation run). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
