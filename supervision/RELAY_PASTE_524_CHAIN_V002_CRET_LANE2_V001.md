## PASTE 524 — CODEX LANE 2 (HIGH EFFORT) — [TASK 5] CHAIN V002: CONSTRUCT C_ret; REPAIR THE MAP

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-448.
- Your chain (`dda80773…`) was KILLED: [EQ6] does not entail C_ret (the stationary-return certificate); the disconnected-domain counterexample (reported A_loop = 0, true modulus 1, no fixed point); steps 3–4 defective. Read the check first:
  `alpha-program-archive/workspace/STAGE8_TASK5_CHAIN_CROSS_CHECK_LANE1_V001.md`
  SHA-256 `a6207597484c2f00ad37cebba34b3f135f21bc2e7a7b7a03a9e642ca7e37fe86`. Verify before reading.
- DoR-020's prohibitions stand; all symbolic; every line [EQ6]-tagged (and now C_ret-tagged where it binds).

TASK — chain V002:
O1. **CONSTRUCT AND CERTIFY C_ret:** the stationary-return certificate as its own object — domain connectivity/return structure, the interval/absolute-continuity branch scope explicit — derivation-first from the built carrier and adopted clauses; if any part is underivable, type it exactly (it would join [EQ6] as a named condition, honestly).
O2. **The counterexample as permanent regression:** the disconnected domain must be EXCLUDED by C_ret — show the computation; A_loop's derivation re-scoped to C_ret-certified domains.
O3. **The map repaired:** the witness-to-number list with the two new rungs installed (C_ret's construction and certification in their correct order), every rung's consumer and gate re-verified — the twelve-step map.
O4. All conditional theorems re-stated with the corrected condition set ([EQ6] + C_ret where required); the tag ledger complete; falsifiers; anti-tuning.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_LICENSED_CONDITIONAL_CHAIN_LANE2_V002.md`
with a lead-result block (`C_RET = CONSTRUCTED/TYPED (+status)`; `MAP = TWELVE_STEP_REPAIRED`; `THEOREMS = RETAGGED`). Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
