## PASTE 684 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / TRANCHE] REBIND THE EVIDENCE ROOT FOR THE 12-PAYLOAD STATE

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors.)

CONTEXT: run 023 stopped at `VERIFIER_INPUT_ROOT` — your instance still attests the 10-payload root `e7820ca5…`; the envelope work moved the manifest to 12 payloads. Your own 683 already reproduced the new root `1fbb3c07…` from the updated manifest as part of its check — rebind by YOUR computation (never the parent's error display), exactly the 657 discipline.

TASK: recompute the evidence root independently over the current sealed evidence manifest (display the computation); update the instance + sidecar (canonical form); disclosed delta (one field + sidecar); PIN CHECK.

OUTPUT: updated instance/sidecar + one sealed artifact
`STAGE8_TASK6_REBIND_EVIDENCE_ROOT_DARIO_V001.md`
with final lines: `EVIDENCE_ROOT = independently recomputed (+value)`, `DELTA = 1 field (+sidecar)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors and re-invokes). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
