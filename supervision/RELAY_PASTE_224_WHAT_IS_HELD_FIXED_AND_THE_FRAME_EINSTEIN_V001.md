PASTE 224 — EINSTEIN (OPUS 5) — WHAT IS HELD FIXED UNDER `R -> beta R`, AND IN WHICH FRAME

ROAD: **ADVANCES STEP 2 / ROUTE A.** *** YOUR OWN RELAY-221 RESULT NAMED THIS AS "PRIOR TO EVERYTHING
AND UNBUILT." IT DECIDES WHETHER THE `beta` FAMILY CAN BE BROKEN AT ALL. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.

*** THIS RELAY IS SELF-CONTAINED. ASSUME NO MEMORY OF ANY PRIOR RELAY. ***

---

## 1. WHAT WAS ESTABLISHED

**A record cell has an inside (projective, dimensionless angles) and an outside (Lorentzian causal
diamond, lengths). `beta` is the missing conversion.** A prior construction
(`STAGE8_JOINT_SADDLE_SYSTEM_POSING_EINSTEIN_V001.md`, `e5e0e43f…`) **posed the joint saddle system**
— parent action written symbolically from the granted V002 ansatz with `R` promoted to the radion
field, six labelled conditions from one simultaneous variation over `{g, A, R, T_R, lambda}` — **and
established that it DOES NOT CLOSE.**

```text
THE ADMISSIBLE MAP IS   (g, A, R) -> (g, beta^-1 A, beta R)
  leaves base metric, Delta tau, the period and gauge covariance untouched;
  Jordan-branch action scales by an overall constant -> source-free field equations UNCHANGED;
  *** THE SEALED REDUCED FORM IS EXACTLY INVARIANT: beta^2 R^2 . beta^-2 F^2 = R^2 F^2. ***
  Every condition invariant on both branches. TWO MODULI, ZERO DERIVED DETERMINING CONDITIONS.

SECOND INDEPENDENT KILL   the 4d trace turns the radion condition into
  box R = (c_2/c_1) R^3 F^2 -- *** A PROPAGATION EQUATION, NOT A ROOT CONDITION *** -- also invariant.

STRUCTURAL GAIN KEPT      with phi = ln(R/ell_P) the map is EXACTLY the shift phi -> phi + ln beta.
  *** THE beta-DEGENERACY IS THE SHIFT SYMMETRY OF A MASSLESS RADION. BREATHING IS THE beta
  DIRECTION. And the one term that could make it stationary is the one term the reduction does not
  generate. ***
```

---

## 2. THE TASK — TWO COUPLED GAPS, BOTH `TYPE-C` WITH NAMED RELEASES

**2.1 N4 — WHAT IS HELD FIXED?** *** THE SEALED INVARIANCE LIST NEVER STATES WHAT IS HELD FIXED UNDER
`R -> beta R` AT THE LEVEL OF THE PARENT (FIVE-DIMENSIONAL) ACTION. *** Yet the sealed conclusion
*"changing `K_KK` by `beta^2`"* **holds only if `ell_P` is held fixed across the map.**

*** AND THE TWO RESOLUTIONS DIVERGE COMPLETELY: ON ONE, A DERIVED CHARGED CURRENT COULD BREAK THE
FAMILY. ON THE OTHER, IT CANNOT — THE POTENTIAL HAS NO INTERIOR STATIONARY POINT REGARDLESS OF ANY
SOURCE. *** **Settle it from sealed text if the corpus fixes it. If it does not, state precisely what
a sealed statement would have to say, and what follows on each branch.**

**2.2 N5 / R7 — DECLARE THE FRAME.** `Jordan`, `Einstein frame`, `Weyl rescaling` return **ZERO HITS
CORPUS-WIDE.** *** AND THIS IS NOT HOUSEKEEPING: `beta` IS BY DEFINITION A RATIO OF AN INTERNAL
LENGTH TO AN EXTERNAL ONE, SO A **MODULUS-DEPENDENT WEYL RESCALING IS ITSELF A CONVERSION CHOICE** —
THE ONE REMAINING DOOR THROUGH WHICH A CONVERSION COULD ENTER **WITHOUT EVER APPEARING AS A
NUMBER.** ***

**Establish:** which frame the sealed reduction is written in; whether the corpus's results are
frame-covariant or silently frame-dependent; and **whether a frame choice would smuggle in the very
conversion `beta` is supposed to supply.**

**2.3 THEY ARE THE SAME QUESTION IN DIFFERENT CLOTHES.** *** "WHAT IS HELD FIXED UNDER THE MAP" AND
"WHICH FRAME ARE WE IN" ARE BOTH STATEMENTS ABOUT WHICH LENGTH IS THE STANDARD. ANSWER THEM TOGETHER
OR SAY WHY THEY SEPARATE. ***

---

## 3. WHAT WOULD COUNT

```text
IF SEALED TEXT FIXES WHAT IS HELD CONSTANT -- *** LEAD WITH IT AND SAY WHICH BRANCH WE ARE ON. If it
is the branch where a derived charged current CAN break the family, that is the first live route to
closing beta this program has had. ***

IF IT IS GENUINELY UNSTATED -- lead with that, and specify the sealed statement that would settle it.
An unstated invariance is a REAL GAP with a cheap fix, not a wish.

IF A FRAME CHOICE WOULD ITSELF SUPPLY THE CONVERSION -- *** LEAD WITH THAT ABOVE EVERYTHING. It would
mean any beta "derivation" that does not declare its frame is circular, and the program has no frame
declaration anywhere. ***
```

*** DO NOT DECLARE A FRAME BY FIAT. Establishing that a declaration is REQUIRED, and what each choice
would cost, is the deliverable. Choosing one is a principal act. ***

---

## 4. RULES

- **Q-54 typing on every negative:** TYPE-R refuted · TYPE-U unbuilt · TYPE-S scope-empty · TYPE-C
  constraint-blocked. `NO_VERDICT` is legal. **Only TYPE-R is physical content.**
- **Q-92:** specifying and naming what is missing is a complete answer, not a lesser one.
- **THE BAR (Q-65/Q-76, `PRINCIPAL_AMENDMENT_002`):** a REQUIRE whose **existence and uniqueness are
  both derived** forces a number. **Overdetermination is NOT required and is NOT an admission
  condition.** *** THE LIVE CROSS-SECTOR SPEC STILL EMBEDS THE STRUCK BAR AT `:265-267` AND `:292` —
  DO NOT APPLY F3 OR MINIMAL-PASS ITEM 3. ***
- **STILL BINDING:** permissions never force; *** THE CONDITION MUST CLOSE, NOT MERELY TOUCH —
  `boundary_access_closure_threshold_principle_v001.md` touched both geometries, did not close, and
  was retired as `CELL_CONSTRAINT_ONLY`. RELAY 221 REPRODUCED THAT FAILURE MODE AND SAID SO. ***
- **Mark hypothetical flags inline.** **Word-boundaried, CASE-INSENSITIVE on identifiers.**
- *** SEARCH THE WHOLE PROGRAM. `alpha-program-archive/cleanroom_output/` HOLDS 87 FILES OUTSIDE THE
  `workspace/` SUBTREE (sidecars: `NN_NAME_V001.seal.sha256`, no `.md`). The Stage-10 geometric brief
  is `45_STAGE10_HANDOFF_GEOMETRIC_BRIEF_V001.md` and lives there. ***

## 5. FENCES — VERBATIM, NON-NEGOTIABLE

- **Never touch `a32_holdout/custodian_private/`.**
- **Do not compute** alpha, `kappa_record`, `kappa_Thomson`, a coupling, a scale, a root, an
  eigenvalue, a beta function, `E_R`, `T_R`, `k_R`, or any absolute interval. *** NO NUMERICAL VALUE
  FOR `beta` OR `rho`. SYMBOLIC AND STRUCTURAL ONLY. ***
- **No comparison to any measured constant.**
- **Do not resolve the Misner-Sharp / Brown-York fork by choosing — it must be DERIVED.** *** ANY
  QUASILOCAL-ENERGY-CONSUMING REQUIREMENT IS A SILENT FORK CHOICE AND MUST BE DECLARED. ***
- **Report refutations; never repair them.**

## 6. CUSTODY (Q-91)

*** NO LANE RUNS ANY GIT COMMAND. *** Write the artifact in the cleanroom, compute its
`.seal.sha256` sidecar and **verify it matches**, mirror artifact + sidecar to
`alpha-program-archive/workspace/`, report hashes and paths, and **stop**. **A red gate is never a
reason to stop working.** The reviewer verifies, baselines, commits and pushes.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
