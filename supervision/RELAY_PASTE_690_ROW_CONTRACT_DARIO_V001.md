## PASTE 690 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / TRANCHE] RECEIVE THE FIELD YOU SPECIFIED

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors.)

CONTEXT: run 027 — `checks[0]: field inventory mismatch (undeclared=['invocation'])`. At 686 you wrote out the byte-span-linkage/invocation field Builder A must emit; at 687 A emitted it; your `CHECK_ROW_FIELDS` exact inventory still says 14. Extend your own contract to receive your own specification.

TASK: extend the row contract to include `invocation` (typed per your 686 write-out — validate its shape, don't merely admit it; the replay may now consume it for the byte-span linkage you noted as undelivered); dry-run on a synthetic row both directions (valid accepted, malformed refused); disclosed delta; PIN CHECK.

OUTPUT: updated verifier + one sealed artifact
`STAGE8_TASK6_ROW_CONTRACT_DARIO_V001.md`
with final lines: `ROW_CONTRACT = 15 fields (invocation typed + validated)`, `SPAN_LINKAGE = now consumed / (+status)`, `BOTH_DIRECTIONS = demonstrated`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors and re-invokes). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
