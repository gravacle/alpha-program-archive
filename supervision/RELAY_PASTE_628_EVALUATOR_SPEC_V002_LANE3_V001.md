## PASTE 628 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 6 / SUBGATE] EVALUATOR SPEC V002: THE FOUR RE-RENDERS + THE OPCODE-REDUCIBILITY SELF-AUDIT

(Same Lane 3 session rules.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-563 (your finite certificates, registered).
- Base artifact (yours): `alpha-program-archive/workspace/STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE3_V001.md`, SHA-256 `eb2073ebb4f23cbc0c0bfa20a36c482e24c59dc6b6e1ccbcd1ef1bd1150d0ecb`. Verify before reading.
- The reviews of record: Dario's partial (`a1f5b4c3…`, Q-559: census + firewall CONFIRMED) and completion (`0f57b662…`, Q-561: map/runner/pin CONFIRMED with FOUR named defects). Verify before reading. Their verdicts stand; V002 implements, does not relitigate.
- The four defective rows (Q-561): `C-B-V011-SP1-07`, `-SP2-03`, `-SP2-05`, `-SP2-06` — each (a) tests its blocker's negative half and weakens the positive "must be derived" half, and (b) carries criterion terms no opcode decides ("certified", "derived", "proven", "silently set", "all structural laws hold" — the last defined nowhere).
- CITATION RULE (Q-562's custody catch): cite SEALED PACKET copies and their line numbers, never the unsealed root's numbering.
- CARRIAGE DISCIPLINE (C-V5): V002 = V001 + a NAMED FINITE DIFF; delta table with authority per row; everything untabled carries verbatim; verify by re-diff and display the summary.

TASK:
G1. **The four re-renders.** For each defective row: quote its blocker's positive half at the sealed byte span; restore it as executable content using the spec's own instruments (`KERNEL` proof-replay for "derived/proven"; `ENUM(grammar,cert)` for completeness; `SCHEMA`/`TYPE` for typed presence; content-addressed certificate inputs for "certified"); replace every non-opcode term with its opcode-reducible form; state the new exact Boolean PASS criterion. The SP2-05 case must restore "full source-record orientation covariance" as a tested predicate (it survives in your census gloss — bring the executable row up to the gloss). Class assignments stay as reviewed.
G2. **The opcode-reducibility self-audit of ALL 66 rows.** Mechanical sweep: for every row, every term of the PASS criterion either names an opcode, a schema field, a content-addressed input, or a defined term — or the row is listed as defective and re-rendered in this same artifact. Report the sweep as a 66-row table (row, verdict CLEAN / RE-RENDERED, and for re-rendered rows the defect class). The four known rows must appear as RE-RENDERED; any newly found defective row is a finding, not an embarrassment — Q-561's sampling covered 23 of 66 and expressly did not establish the pattern's absence in the other 43.
G3. **Consistency propagation:** counts and class partitions restated if any row's inputs changed; the verifier's expected-census section updated to match; the runtime-pin section untouched (confirmed of record); the aggregate rule untouched.
G4. **Battery:** F_PLDEC; anti-tuning; surface anchor; M-2 with all four false-negative modes; self verb audit; carriage verification displayed.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE3_V002.md`
with final lines: `RE_RENDERS = 4 + N newly found (+row list)`, `SELF_AUDIT = 66/66 swept (+CLEAN count)`, `CRITERIA = all opcode-reducible`, `CARRIAGE = verified_by_rediff (+delta row count)`, `AUTHORIZATION = not_claimed`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
