## PASTE 564 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5 / EQ6] THE MANIFEST BUILD: THE DIAMOND FAMILY AND THE CONSUMER ARROWS, EXPLICIT

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-490.
- Your census (`676863b5…`) recorded a MACHINERY-APPEAL: no diamond-family manifest and no clause-(vi) consumer-arrow manifest exist anywhere in the corpus. This relay BUILDS both — the one object three consumers need: the census completion, membership V002's clause (vi), and the coming J7 adjudication's scope table.
- Authorities: the guard (`a681c784…` — clause (vi)'s exact wording and its B2.6 asymmetric-primitive attack), your census (the three prototypes, the asymmetric witness, the schematic class), the assembly chain (`a46beae2…`, `9bf34e27…` — the actual generators of I_F), the membership build (`0d00c9cf…` M1 — the four arrow classes), the sealed finite-consumer registry (the DoR-008/Gate-1..4/Q-243/Q-279/Q-309 shadows — every sealed consumer named in the bottom construction).
- SYMBOLIC ONLY; no numeric evaluation. Honesty: if the family is infinite or not finitely presentable, say so exactly and give the finite generating description instead — do not truncate silently.

TASK:
P1. **The consumer-arrow manifest:** for every sealed package consumer (walk the sealed registry — name each), the finite-bottom arrows it needs, displayed as explicit arrows of I_F with their classes (A_RP⁺ / A_RP-with-m_T=0-end / A_CC-old-image / new-cycle) — clause (vi) discharged by exhibition; rerun the guard's B2.6 asymmetric-primitive attack against the completed manifest.
P2. **The diamond-family manifest:** the complete common-refinement diamond family of I_F over the P1-completed arrow set — as a finite list if finite, or a finite generating description (orbit representatives under the admitted symmetries) if not; each diamond's four stages classified (m_T zero/nonzero by cycle content; position classes); state which diamonds the three census prototypes represent and what is genuinely new.
P3. **The census completion:** rerun the census verdict over the full manifest — the exact set of DEMANDED-UNFORMABLE positions (by O1 / by O2), and the exact support (the sub-family where the coefficient face IS formable) — this is the scope table the J7 amendment will quantify over.
P4. **Battery:** completeness argument (why nothing is missing — tie each manifest row to its generating sealed source); anti-tuning; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_MANIFESTS_LANE3_V001.md`
with final lines: `CONSUMER_MANIFEST = COMPLETE (+count)`, `DIAMOND_MANIFEST = COMPLETE_FINITE (+count) / GENERATED (+description)`, `UNFORMABLE_SET = exact`, `FORMABLE_SUPPORT = exact`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
