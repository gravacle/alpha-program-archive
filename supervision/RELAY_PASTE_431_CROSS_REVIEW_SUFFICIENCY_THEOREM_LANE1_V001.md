## PASTE 431 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] CROSS-REVIEW: THE PREFIX-TRACE SUFFICIENCY THEOREM (Q-348)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-348.
- Artifact under review (Lane 2's build): `alpha-program-archive/workspace/STAGE8_TASK4A_TRACE_KERNEL_SUFFICIENCY_ARM_LANE2_V001.md`
  SHA-256 `d9a507fc8b5645981ed1519a04e180620ee7c22f65d5c9425437a701185f9001`. Verify before reading.
- Authorities: DoR-016/V004 `69f4d93b…`, DoR-009 (the finite doubled trace and faithful characters), your own extension arm `04002fb4…` (Q-347), the Door-7 cross-review `d738661e…` (the X7 seed attack).
- Custody: adversarial cross-review of the parallel arm. The comparison-square commission will be built ON this theorem — attack it.

TASK — PASS / KILL per item:

W1. **The isomorphism.** Recompute the triangular structure: (R₁,…,R_N) → (Z₁,…,Z_N), r_m = Z_{m−1}⁻¹Z_m; verify the trivial kernel by the induction; verify the faithful-character premise (n ∈ {±1}) is genuinely ratified and load-bearing (what breaks if the character is unfaithful?).
W2. **The terminal-kernel claim.** Verify K_term ≅ U(1)^{N−1} and the N=2 counterexample (uw, vw⁻¹); confirm this correctly absorbs the X7 attack (valid against terminal, not against the family).
W3. **The domain honesty.** The theorem lives on the RELATIVE R_CTP level. Verify the raw doubled-pair fiber statement (what the relative data forgets about (T₊, T₋)) is correctly typed NO_VERDICT/TYPE-U, and that no claim silently extends to untraced operator content.
W4. **The prefix-family availability.** The theorem consumes every-prefix traces. Verify DoR-016 actually LICENSES access to all prefix traces (the per-system tower is built from per-tier factors — is the per-tier factor a ratified observable, or only the accumulated product?). If only the product is ratified, the reconstruction needs an unratified input — that would be a KILL or a new door.
W5. **The seam with the extension theorem.** Combine with Q-347 explicitly: state exactly what a prefix-family-consuming descent has available at a rank-≥2 stage, and whether the automorphism obstruction re-binds at family level (the σ-exchange fixes EVERY prefix trace or not — compute it). This is the load-bearing item for the next commission.
W6. **Regression + selection scan.** Reality, batching, zero-extension at family level; p symbolic; no selections.
W7. **One fresh attack** of your own on the sufficiency claim.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK4A_SUFFICIENCY_THEOREM_CROSS_REVIEW_LANE1_V001.md`
with the verdict table W1–W7, computations, and final lines:
`SUFFICIENCY_THEOREM = CONFIRMED / KILLED (+item)` and `PREFIX_FAMILY_AT_RANK2 = CARRIES_CYCLE_CONTENT / STILL_OBSTRUCTED (+proof)`.
Seal (.seal.sha256 sidecar), mirror both files byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, then STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No evaluation of alpha, K_*, or roots; no comparison to measured constants. Structural mathematics of declared objects is permitted; if a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
