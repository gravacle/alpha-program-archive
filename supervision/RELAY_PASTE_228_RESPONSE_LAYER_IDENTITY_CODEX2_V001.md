PASTE 228 — CODEX LANE 2 — ARE THE TWO RESPONSE LAYERS THE SAME OBJECT?

ROAD: **COULD UNBLOCK STEP 1 AND STEP 2 WITH ONE BUILD.** *** BOTH ROUTES TO ALPHA NOW TERMINATE IN
A "RESPONSE LAYER." IF THEY ARE THE SAME OBJECT, IT IS THE HIGHEST-VALUE SINGLE BUILD IN THE PROGRAM.
IF THEY ARE NOT, WE MUST KNOW BEFORE ANYONE CONFLATES THEM. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.

*** THIS RELAY IS SELF-CONTAINED. ASSUME NO MEMORY OF ANY PRIOR RELAY. ***

---

## 1. THE COINCIDENCE THAT PROMPTS THIS

```text
alpha = 1/(4 pi K_*)        K_* = the root of C_record(K) = 0

ROUTE B / STEP 1.3   blocked on "the complete BR/CTP fluctuation-response operator" -- the
                     response-extraction layer: the exact induced kernel, the raw-correlator-to-
                     retarded-Hessian map, and the covariant local projector.

ROUTE A / SCALE      the E_ref fork is the remaining discrete scale-side blocker:
                     *** E_ref in { E_* = m_* , mu = m_*/sqrt(2) } ***, consumed by
                     rho = (3/16) g_N^2 / (m_* E_ref).
                     Its would-build (STAGE8_EREF_FORK_DERIVATION_SCOPE_AUDIT_V001.md, 7340488a...):
                     *** "Stage-10 PHYSICAL RESPONSE/MATCHING with pre-frozen predictions and
                     Ward/causal tests." ***
```

*** TWO INDEPENDENT BLOCKERS, ARRIVED AT BY DIFFERENT ROUTES ON DIFFERENT DAYS, BOTH NAMING A
RESPONSE LAYER. THAT MAY BE ONE OBJECT OR TWO. NOBODY HAS CHECKED. ***

---

## 2. THE TASK

**2.1 CHARACTERIZE EACH, SEPARATELY AND FROM SEALED TEXT.** Do not compare yet. For each: **what it
consumes, what it produces, its domain and codomain, and what it is a response OF, to WHAT.** Paths
and lines.

**2.2 THEN COMPARE, FEATURE BY FEATURE.** Same carrier? Same source? Same probe? Same output type? Is
one a restriction, projection, or completion of the other? *** REPORT THE COMPARISON AS A TABLE OF
FEATURES, NOT AS A VERDICT SENTENCE. ***

**2.3 RULE, AND STATE WHICH:**

```text
SAME OBJECT        a sealed identity, or a derivation that they coincide. *** QUOTE IT. ***
ONE CONTAINS THE   one is a component/restriction of the other -- say which way, and what the
OTHER              remainder is.
DIFFERENT OBJECTS  they share a WORD and not a referent. *** THIS IS A REAL AND USEFUL RESULT --
                   it prevents a conflation that would otherwise look like progress. ***
NO_VERDICT         the corpus does not settle it. Name what would.
```

*** THIS IS THE ERROR THIS PROGRAM COMMITS MOST OFTEN. Six cross-construction identifications were
refuted on 2026-07-29; six `B0` routes closed for want of a bridge; a sealed `TYPE-R` was transported
beyond the candidate it named and had to be scoped back. **A SHARED NOUN IS NOT AN IDENTITY. THE
DEFAULT ANSWER IS "DIFFERENT UNTIL SHOWN OTHERWISE."** ***

**2.4 IF THEY ARE THE SAME OR NESTED, STATE WHAT BUILDING IT WOULD REQUIRE** — the union of both
would-builds, deduplicated, with each item typed sealed / adopted / absent.

---

## 3. SYMMETRIC LEADS

```text
IF THEY ARE THE SAME OBJECT (or one contains the other) -- *** LEAD WITH IT. One build would unblock
the scale side of Route A and step 1 of Route B simultaneously. Nothing else on the board has that
leverage. ***

IF THEY ARE DIFFERENT -- *** LEAD WITH THAT, EQUALLY PLAINLY. It is a real result: it stops a
conflation that would have looked like progress and would have had two lanes building one object for
two purposes it cannot serve. ***
```

*** DO NOT STRAIN FOR IDENTITY. A forced identification here would be worse than a clean negative,
because everything downstream would inherit it. ***

---

## 4. RULES

- **Q-54 typing on every negative:** TYPE-R refuted · TYPE-U unbuilt · TYPE-S scope-empty · TYPE-C
  constraint-blocked. `NO_VERDICT` is legal. **Only TYPE-R is physical content.**
- **Q-92:** specifying and naming what is missing is a complete answer, not a lesser one.
- **Q-69:** never identify a flag with the object that discharges it.
- *** NO UNPROVED-IDENTITY TRANSPORT — THIS RELAY IS ENTIRELY ABOUT THAT RULE. A seal reaches what it
  names and no further. ***
- **Word-boundaried, CASE-INSENSITIVE on identifiers. Mark hypothetical flags inline.**
- *** SEARCH THE WHOLE PROGRAM. `alpha-program-archive/cleanroom_output/` HOLDS 87 FILES **OUTSIDE**
  THE `workspace/` SUBTREE — including the Stage-10 brief
  `45_STAGE10_HANDOFF_GEOMETRIC_BRIEF_V001.md` AND `21_DIMENSIONLESS_RATIO_RESULT_V001.md`, BOTH
  DIRECTLY RELEVANT HERE. Sidecars: `NN_NAME_V001.seal.sha256`, no `.md` before `.seal`. ***

## 5. FENCES — VERBATIM, NON-NEGOTIABLE

- **Never touch `a32_holdout/custodian_private/`.**
- **Do not compute** alpha, `kappa_record`, `kappa_Thomson`, a coupling, a scale, a root, an
  eigenvalue, a beta function, `E_R`, `T_R`, `k_R`, or any absolute interval. *** THIS RELAY COMPARES
  TWO OBJECTS' SPECIFICATIONS. IT DOES NOT BUILD OR EVALUATE EITHER. ***
- **No comparison to any measured constant.**
- **Do not resolve the Misner-Sharp / Brown-York fork by choosing — it must be DERIVED.** *** `E_ref`
  IS THAT FORK. DO NOT PICK A SIDE WHILE CHARACTERIZING WHAT WOULD DECIDE IT. ***
- **Report refutations; never repair them.**

## 6. CUSTODY (Q-91)

*** NO LANE RUNS ANY GIT COMMAND. *** Write the artifact in the cleanroom, compute its
`.seal.sha256` sidecar and **verify it matches**, mirror artifact + sidecar to
`alpha-program-archive/workspace/`, report hashes and paths, and **stop**. **A red gate is never a
reason to stop working.** The reviewer verifies, baselines, commits and pushes.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
