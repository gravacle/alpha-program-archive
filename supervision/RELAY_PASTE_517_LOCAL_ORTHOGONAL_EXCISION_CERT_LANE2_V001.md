## PASTE 517 — CODEX LANE 2 (HIGH EFFORT) — [TASK 5 / EQ6] BUILD THE LOCAL_ORTHOGONAL_EXCISION_CERT; RERUN THE CYCLE-CREATING DIAMONDS

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-442. The joint admission is a GAP with the route named: your build (`dec99497…`) plus the check (`6cd40961…` — read its direct-sum compatibility model and the excision-cert typing; the contact diamond's corrected requirement set: OLD_FID + exclusive-region RNL, not only LR).
- The nine regressions, the adopted clauses, and the standing rule govern. All symbolic; target-blind.
- Custody: builder; hostile check follows.

TASK:
H1. **Build the LOCAL_ORTHOGONAL_EXCISION_CERT** per the check's typing — the certificate that lets a cycle-creating admission excise locally and orthogonally (the direct-sum model's mechanism made general): definition, certificates (covariance, metric fidelity, no-leakage compatibility, the bundle discipline), and the proof it composes with the diamonds.
H2. **Correct the contact diamond:** the requirement set fixed to OLD_FID + exclusive-region RNL; the diamond re-executed.
H3. **Rerun both cycle-creating diamonds** with the excision cert in hand — the joint tests confronted; PASS or the exact remaining stop.
H4. **The cascade + [EQ6]:** with the results, the all-rank statement, the fiber ledger, the joint distance.
H5. Falsifiers; anti-tuning; honest stopping points.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_LOCAL_ORTHOGONAL_EXCISION_CERT_LANE2_V001.md`
with a lead-result block (`EXCISION_CERT = BUILT/STOPPED_AT`; `DIAMONDS = <each>`; `ALL_RANK = <state>`; `JOINT_EQ6 = <state>`), the register-sweep list, and the H1–H5 chain. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
