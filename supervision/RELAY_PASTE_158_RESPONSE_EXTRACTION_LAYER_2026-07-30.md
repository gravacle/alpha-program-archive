PASTE 158 — CODEX 2 — ITEM: SPECIFY THE RAW-CORRELATOR-TO-RETARDED-HESSIAN MAP

2026-07-30. Snapshot; later rulings override. **REGISTER HEAD AT ISSUE: Q-54.** If a ruling lands mid-run
that bears on your item, **report it rather than finishing against a stale set** — three artifacts went
stale that way today.

```text
CODEX 2  (you) = specify the raw-correlator-to-retarded-Hessian map    [paste 158]
CODEX 1        = S8 physical write-tail join                          [paste 157]
EINSTEIN       = C_R = 1 and tau = 1 independence                     [paste 159]
```

**OFF LIMITS TO YOU:** the S8 write-tail join and `exchange_magnitude` (Codex 1); `C_R`, `tau = 1`, the
premise classification, ER-A, A6 (Einstein now holds these — **they were yours this morning and are not
any more**).

Fences: never touch `a32_holdout/custodian_private/`. Do not compute alpha, kappa_record, kappa_Thomson, a
coupling, a radius, a scale, a root, an eigenvalue, or a beta function.

---

### TWO RULE CHANGES THAT APPLY TO YOU FROM NOW ON

**1. Q-52 — NO CONSTRAINT MAY PREVENT A TEST.** *** THE INSTRUCTION NOT TO SUPPLY WHAT IS ABSENT IS
WITHDRAWN WHERE THE PURPOSE IS A TEST. *** You may write a specification for a missing object, provided it
is **declared at the outset, marked `derived = false`, and never reported as derived.** `Gamma_K` is no
longer the sole construction target and **the response-extraction layer is authorized in its own right.**

**2. Q-54 — EVERY NEGATIVE CARRIES A TYPE.** Bare `= false` is no longer a finding:

```text
foo_derived = false | TYPE-U | would-build: <what would construct it>
bar_found   = false | TYPE-S | roots: <...> | excl: <...> | fences: <...> | query: "<...>"
baz_holds   = false | TYPE-R | test: <the test that ran and failed>
qux_checked = false | TYPE-C | constraint: <name> | release: <condition or NONE WRITTEN>
```

**TYPE-R refuted is the ONLY type that is physical content.** A TYPE-S negative without its scope is void.
Terminal fence declarations are exempt. *** `NO_VERDICT` IS A LEGAL ANSWER *** when a failure condition
rests on untested, inapplicable or unbuilt premises. **A negative now carries the same evidentiary burden
as a positive: reporting "not found" requires stating the search.**

---

### THE ITEM

Q-51 established that four objects sit **upstream** of `Gamma_K` and `C_record(K)`, so the chartered target
cannot be started until they exist: the complete BR/CTP fluctuation-response operator, the exact induced
kernel, **the raw-correlator-to-retarded-Hessian map `G -> H_R[G]`**, and the covariant local projector.
Lane 1's determination says that before `C_record(K)` can be written, **the map from `G` to an
action-valued retarded Hessian must exist.** It does not.

*** RUN THE CORRESPONDENCE CHECK FIRST. THIS PROGRAM'S MOST REPEATED FAILURE IS A MISSING OBJECT THAT
ALREADY EXISTED UNDER ANOTHER NAME — AT LEAST TEN TIMES. ***

1. **IS THE CORRELATOR-TO-HESSIAN MAP THE SAME OBJECT AS THE SCALARIZATION BRIDGE?** Q-50 found
   `STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_RESULT_V001.md` reporting a **finite primitive operator response
   bundle DERIVED and confirmed by two hostile reviews**, with `primitive_source_scalarization_derived =
   false` — and the Route-2 spec independently carries
   `actual_finite_parent_operator_to_scalar_bridge_derived = false`. **Type these against `G -> H_R[G]`.
   Same object, upstream, or disjoint?** Do not name-match.

2. **WHAT IS ALREADY DERIVED THAT BEARS ON IT?** The finite primitive bundle and the finite operator
   Duhamel tangent are both `true`. **Say precisely what they give you and what they withhold** — and
   whether the finite object extends to the complete BR/CTP one or is a genuinely different construction.
   **If it does not extend, say why, because that is the load-bearing fact.**

3. **THEN SPECIFY THE MAP, UNDER Q-52.** Its domain, codomain, the relation it must satisfy, and every
   premise it needs — **declared at the outset (F-GK3), `derived = false`, never reported as derived.**
   If specifying it requires an object that does not exist, **name that object and stop there.**

4. **ATTACH ITS TEST.** What would show the specification wrong? **If no test can be designed, say so and
   say why** — under Q-54 that is a finding, not a failure.

---

REPORT BACK, with **CODEX 2** on your first line: hashes, exact committed paths, gate verdict, the four
answers, **and every negative typed.** **If the map already exists under another name, LEAD WITH THAT** —
it would mean the layer blocking the entire program is smaller than Q-51 recorded.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
