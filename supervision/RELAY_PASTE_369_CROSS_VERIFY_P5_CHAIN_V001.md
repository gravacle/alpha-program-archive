PASTE 369 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] CROSS-VERIFY THE P5 MAXIMAL CHAIN AND ITS ACCOUNTING TABLE — NOW THE LOAD-BEARING LAYER

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-285)

```text
DOES THE OBJECT EXIST?   YES: the P5 maximal chain + four-arrow accounting table
                         (2fc227d0..., built by the OTHER lane, Q-284), claiming: retarded
                         image p_ch-clean on everything built; full Q-279 restriction
                         pattern reproduced; stop exactly at the raw-G lift.
IS THE VERSION CURRENT?  YES through Q-285 -- WHICH RAISES THE STAKES: the weak-* creator
                         result makes the accounting table's Tail_R column and every
                         topology claim in the chain THE load-bearing layer. The P2
                         verification (Q-276) caught a sign at exactly this kind of depth.
ARE ITS INPUTS PRESENT?  The chain artifact, P2 V002, P4, the seed (Q-281), the references
                         (Q-243, Q-279), Q-285's creator characterization.
```

## 1. THE CHECKS — INDEPENDENT RECOMPUTATION, THE ACCOUNTING HARDEST

```text
X1 THE DERIVATIVE TENSOR: recompute the exact source derivative tensor and connected
   shadow independently (the W-convention and the Q-276 sign carried correctly?).
X2 THE RESTRICTION PATTERN: re-verify the FULL Q-279 reproduction with your own
   restriction computations -- the p-free retarded block AND the exact noise forms
   (kappa_eta, the dephasing weight).
X3 THE ACCOUNTING TABLE, row by row: every operation's kernel/image/sector-transfer/
   restriction-square/Tail_R entries re-derived. THE CRITICAL COLUMN AFTER Q-285: the
   Tail_R action -- confirm NO built operation is (or hides) a weak-* bidual completion;
   every limit's named topology checked against what the operation ACTUALLY does.
X4 THE p-CLEANLINESS CLAIM: "the retarded image is p_ch-clean on everything built" --
   attack it: construct any path through the built chain by which a p-carrying block
   could reach the retarded image (sector-transfer compositions included). Exhibit or
   confirm impossible on the built domain.
X5 THE STOP: verify the chain genuinely stops at the raw-G lift (no step quietly assumed
   lift structure), and the six-object residue list is complete against Q-280/Q-281/
   Q-283's own lists.
VERDICT: CONFIRMED / DEFECT (location + inheritance) / CONFIRMED-WITH-NOTES.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
