PASTE 149 — LANE 2

2026-07-30, CODEX LANE 2. Snapshot; later rulings override. Lane 1 is attempting the `Gamma_K` root by the GNS/covariance route under paste 148 — do not touch `Gamma_K`, joint-selector, CAR/GNS, covariance, measure or namespace artifacts.

Your slot-16 audit is verified (`df0d1c74…`) and registered as Q-39. **Refuting my "threshold-conditioned means onset-shaped" hypothesis was the valuable part**, and so was recording that the closest statement on the density question is a supervision artifact written the same day — a negative resting on absence rather than argument, said plainly.

**ONE ITEM: classify the standing premise stack against the rule established this morning. Mechanical, and it produces the honest accounting of what any future number would be conditional on.**

Fences unchanged. `alpha_computed = false; proof_authorized = false`. **NEVER touch `a32_holdout/custodian_private/`.**

---

### THE RULE, FROM Q-37

*** EVERY ADOPTION CARRIES ITS TEST, OR IS MARKED UNTESTED. ***

It was established when the principal corrected the framing of the root-premise decision: **one adopts a hypothesis in order to test it — the adoption is what makes the thing falsifiable.** A premise never stated is a premise never refutable.

**AND THE RULE IMMEDIATELY EARNED ITSELF.** The root adoption was made at 11:20 with a test frozen in advance, and **F-RP1 fired at 11:47** — the adoption died in one lane-turn instead of after eleven pieces (Q-38). That is what a tested adoption looks like.

**THE CONTRAST IS A6.** The axiom map found the minimality selector **adopted nine separate times in nine separate files**, never counted, never tested — "no inventory in the corpus counts it once, so its weight is invisible at every point of use." The later split correction reduced it to about five and a half genuine sites. **Same word, "adopted", doing two opposite jobs.**

**THE CORPUS HAS NO WAY TO TELL THEM APART.** `K_bare = 0` adopted, `C_R = 1` adopted, mutation exclusion adopted, ER-A disclosed — all one category, with no distinction between premises exposed to refutation and premises nobody will ever check.

### THE TASK — CLASSIFY EVERY STANDING PREMISE

For each, at file:line, with roots and exclusions stated and FILE LISTS reported:

**(a) the premise, quoted;** **(b) its status flag as sealed;** **(c) TESTED or UNTESTED** — and if tested, **by what**, named specifically: a frozen falsifier, an executed countermodel, a gate it must pass, a holdout. **A test that cannot fail is UNTESTED — say so.** **(d) if untested, what test WOULD expose it**, or "none identified."

**START WITH THESE, THEN SWEEP FOR OTHERS:**

1. **`C_R = 1`.** Newly urgent: Q-07 established it is **the corpus's only absolute-scale selector** — "This fourth input is the selector. Without it, public recoverability gives a half-line of allowed durations and no absolute record scale" — and it is passed into the producer as a function argument, `scale_ratios(action_marker, causal_radius_fraction, compactness_threshold)`, with `"physical_premise_proved_by_script": false`. **In content it is a marginal self-gravitation condition.** Is it tested by anything at all?
2. **`K_bare = 0`, the induced-only axiom.** Q-18 established that **alpha's conditionality equals this axiom's status, exactly**, and Q-22 that it stands in for a finiteness property the carrier lacks. Its earning clause is slot 18, which Q-23/Q-35 show is unreachable. **Is there any test short of slot 18?**
3. **Mutation exclusion / transport-only.** Recorded as a postulate in three independent places, with `excluded_by_theorem` and `theorem_excluded` true NOWHERE in the corpus.
4. **ER-A.** `ER_B_excluded = false` is its sole occurrence corpus-wide; `ER_fork_closed = false` at all five. Tracked in six sealed places with a carry-clause that has no consumer.
5. **The A6 sites**, at the corrected count of about five and a half, per the split correction — the constraint is not the selector, and only the selector sites belong here.

### WHAT I EXPECT, AND SAY SO IF I AM WRONG

**I expect nearly all of them to come back UNTESTED**, and I expect `C_R = 1` to be the most consequential of those, since it is the only absolute-scale selector and it is adopted rather than derived. **If any premise turns out to carry a real frozen test, that is the more interesting finding** and I want it named.

**DO NOT** rule on any premise, retire one, propose adopting or withdrawing anything, or compute any value. **This is a classification, not an adjudication.** A premise being untested is not an argument against it — it is a fact about its epistemic status, and the whole point is to make that fact visible where the corpus currently hides it in one undifferentiated category.

---

REPORT BACK: hashes, exact committed paths, gate verdict, and the classification table. **If the count of UNTESTED premises on the value path is large, lead with the count** — that number is the honest measure of what a computed alpha would be conditional on, and nobody has ever stated it.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
