PASTE 396 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THE STATIONARY PACKAGE — THE PROGRAM'S LAST UNBUILT PHYSICS, BUILT FROM ITS PROVEN BRIDGEHEADS

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-313)

```text
DOES THE OBJECT EXIST?   NO -- and it is the LAST one (Q-313): the stationary 2PI blocks,
                         the common completed domain, the physical background realization,
                         and the restriction/Tail squares. Everything else in the
                         deciding question's chain is BUILT and VERIFIED.
IS THE VERSION CURRENT?  YES through Q-313. THE STRUCTURAL KEY (use it, do not fight it):
                         Q-252 PROVED no finite stationary point exists for p in (0,1) --
                         the stationary structure is intrinsically COMPLETED-LEVEL. Its
                         bridgeheads are all TYPE-P: the pullback-stationary-set =
                         critical-set-of-Theta theorem (Q-252); the bounded seed with the
                         forced state p_A = r_ch/(r_0+r_ch) (Q-281); the state-freedom-
                         free lift fiber; the ratified signature's six structures (the
                         external background IS one of them -- DoR-015's adopted field,
                         never yet consumed for exactly this); T_cyl; the completed
                         kernel; Map 1 (Q-313).
ARE ITS INPUTS PRESENT?  YES -- LISTED ABOVE, ALL SEALED/RATIFIED/PROVEN. Notably: the
                         signature's EXTERNAL BACKGROUND and EXTERNAL REALIZATION fields
                         were ADOPTED for this moment -- the background realization
                         should CONSUME the adopted field, not be built from nothing.
```

## 1. THE TASK — THE FOUR OBJECTS, IN DEPENDENCY ORDER

```text
1  THE PHYSICAL BACKGROUND REALIZATION: instantiate (Abar_*, G_*) by consuming DoR-015's
   adopted external background/realization fields, seeded by Q-281's bounded seed (the
   forced state transported through the germ), lifted along the state-freedom-free fiber.
   The Q-252 critical-set theorem is the characterization: the background is the
   completed-level critical structure of Theta -- construct it THERE, where it exists.
2  THE COMMON COMPLETED DOMAIN: the invariant domain for the stationary structure on the
   completed spaces (the kernel + complement two-sector layout), from the ratified
   domains + P2 closures; essential self-adjointness where the structure gives it.
3  THE STATIONARY 2PI BLOCKS: the second-variation blocks AT the realized background --
   kernel sector, complement sector, and the mixing -- via Map 1 and the source-level
   descent (which PRESERVES the lifted zeros; the question is what the STATIONARY
   evaluation adds). Compute the blocks exactly where exact, premise-level where not.
4  THE RESTRICTION/TAIL SQUARES: the commuting squares tying the stationary structure to
   the finite restrictions (Q-243/Q-279 must be the shadows) and the Tail_R accounting
   through the stationary evaluation.
5  *** THEN THE VERDICT, LAWFULLY: physical_cycle_sector_RetHess exists on the built
   package -- TRACE p. The complete premise chain. The sentence. ***
6  KILL-PASSES: consume the ADOPTED background field (do not author a second one; do not
   bypass it); the forced state's symbolic form only (no rank values); every limit names
   its topology; six-account rows; door flags; restriction executed; no selection; no
   evaluation.
DELIVERABLE: the four objects with certificates, and THE p-VERDICT with its complete
premise chain -- or the exact typed obstruction, which would now be the program's ONLY
remaining sentence.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
