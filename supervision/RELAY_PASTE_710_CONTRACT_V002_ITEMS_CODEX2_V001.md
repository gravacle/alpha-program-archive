## PASTE 710 — CODEX 2 LANE (5.6 SOL EXTRA HIGH) — [TASK 6 / CLOSE] THE CONTRACT V002 DEFERRED ITEMS — DISPOSITIONED

### STANDING QUEUE RULE (installs now, applies from the NEXT relay on)
From the next relay onward, the principal will send you a BARE NUMBER N instead of pasting the relay text. On receiving a number: read the single file matching `<cleanroom>/relay_inbox/RELAY_PASTE_N_*.md` (cleanroom = alpha_fundamental_record_action_cleanroom_v003), verify its `.seal.sha256` sidecar, execute it under ALL standing session rules exactly as if pasted, seal your outputs as usual, ADDITIONALLY write your full completion report to `<cleanroom>/relay_outbox/N_DONE.md`, then STOP. If the inbox file is missing or its seal fails, report that and STOP. For THIS relay (710), also write `710_DONE.md` to that outbox on completion.

(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. THE FIRST-TIME-RIGHT RULES and THE JURISDICTION CHECK apply.)

CONTEXT: Task 6 close requires disposition of the integration-contract V002 deferred items. Several may already be discharged by later work (the verifier_root_members schema field, the citation-key law, the seven-field carrier); dispositions must CITE the discharging bytes, not assert them.
TASK: for each deferred contract V002 item — (1) pin-manifest/membership-in-instance, (2) the name-binding scheme, (3) positional fixture identity, (4) replay-ledger coverage, (5) the deferred schema amendments (unrequired_args reporting; coverage-direction reporting) — rule it RESOLVED-BY (cite the sealed artifact + span/field that discharged it), IMPLEMENT-NOW (only if the fix is bounded and ships whole in this relay, dry-run), or DEFER-TO-7A (name the owning road step and why deferral is lawful). No item dropped; no partial implementations.
OUTPUT: one sealed artifact `STAGE8_TASK6_CONTRACT_V002_DISPOSITIONS_CODEX2_V001.md`
Final lines: `ITEMS = 5+ (R resolved-by / I implemented / D deferred, each cited)`, `CODE_CHANGED = none / (displayed, dry-run)`, `PIN_CLOSURE = N hits, all resolved`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 710_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
