## PASTE 635 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / SUBGATE] BOUNDED CLASS-CHECK: THE SPEC V003 BINDING REPAIR

(Same Dario session rules. ARCHIVE-SIDE ONLY: read from and write+seal in `/Users/bgm/MB Work/alpha-program-archive/workspace/`; do not touch the Documents tree; the registrar carries the cleanroom mirror.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-571 (your assembly V003, registered).
- Under review: archive `STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE2_V003.md`, SHA-256 `f910815c84fd29d3dc6ca55f43ebe5d4c7015f457ddcde7bd7a69afdf81658dc`, vs base V002 `25e89ed2…` (also in the archive workspace). Verify both before reading.
- Its claims (Q-569): escape hatch removed (scope identifiers zero hits); SP2-07 restored; SP2-06 carrier named; BINDING_SWEEP 66/66 = 38 BOUND / 28 RE-RENDERED; protected sections unchanged (pinned hashes); carriage 21 hunks complete.
- Your standard (Q-567): binding, not vocabulary — does each opcode actually RECEIVE the object the blocker demands? The PRE-SEAL PIN CHECK is now process law; hold this artifact to it.
- Custody: Codex 2 repaired per your findings; you verify. Bounded — not a fresh full-spec review.

TASK — bounded, verdict per item:
C1. **The three named repairs:** SP1-07 (the escape hatch genuinely dead — no residual scope semantics under any spelling, M-2 four modes; completeness bound so zero-derived-IDs cannot pass); SP2-07 (the weakened-direction drift genuinely reversed at the blocker's span); SP2-06 (the reality-factor carrier a real content-addressed input consumed by the procedure).
C2. **The 28 re-renders:** each against its blocker at the sealed span — positive half restored as executable binding (KERNEL with the proof a pinned input / ENUM with certificate / COMPARE on enumerated objects); PASS criterion exact and Boolean; class unchanged. Any re-render that introduces a new degree of freedom (the SP1-07 lesson) is a finding.
C3. **The 38 BOUND rows:** spot-check at least 12 across all six families — is BOUND earned under the binding standard, or does the Q-567 class persist anywhere?
C4. **Protected sections + carriage:** the pinned block hashes verify; your own re-diff against the declared 21 hunks; uncovered hunks classified.
C5. **Battery + verdict:** F_PLDEC; M-2 (four modes); self verb audit.

OUTPUT: one artifact, lane-tagged filename, written and sealed IN THE ARCHIVE WORKSPACE
`STAGE8_TASK6_SPEC_V003_CLASS_CHECK_DARIO_V001.md`
with final lines: `NAMED_REPAIRS = 3/3 CONFIRMED / (+items)`, `RE_RENDERS = 28/28 CONFIRMED / (+items)`, `BOUND_SAMPLE = CLEAN / (+rows)`, `PROTECTED+CARRIAGE = CONFIRMED / (+items)`, `SPEC_V003 = STANDS / NEEDS_V004 (+reason)`, `BUILD_READY = yes/no (spec readiness only)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists, STOP and report — do not overwrite. Seal in the archive workspace, report both hashes, STOP (the registrar mirrors to the cleanroom). No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
