# External Re-Audit — v004 (Fable reviewer lane, relayed by Brian): NO-GO
Received at the start of Brian's ~7-hour autonomous window. 16 raised, 11
confirmed after adversarial refutation, 5 refuted. Read-only terms.

## Root cause (adopted as sealed standing discipline)
Four repetitions of one defect class in one day (GPG post-consumption
check; v003 manifest binding; launcher-vs-comparator v004; now
launcher-vs-CONTROLLER v004): preconditions checked after the
irreversible step, or rules enforced in docstrings while tests drove
stubs. The controller suite reported 13/13 while the production pipeline
was completely unstartable (run_controller() drives a fixture STUB
launcher). MECHANICAL RULE adopted and sealed as
STAGE8_REAL_COMPONENT_PRECONDITION_DISCIPLINE_V001: real-component
end-to-end tests for any allowlist/manifest/executor-row/launch-
precondition change; every precondition verified before the first
irreversible action, enumerated and ordering-proven; superseded chains
made unlaunchable mechanically.

## BLOCKING
B1: launcher v004 has NO row for controller v004 (the supplement
repointed the comparator and two test files and missed the controller);
controller v004 requires the launcher's attestation marker; every v004
lane is unlaunchable (fail-closed deadlock, zero artifacts).
B2: the known-defective v003 chain (controller+comparator v003 rows
retained in launcher v004, outside manifest v003) remains ARMED on the
SAME canonical paths — one stale invocation permanently poisons the
canonical comparison path with a BLOCKED artifact at chmod 444. Must be
made unlaunchable mechanically.

## MAJOR/minor
M1: comparison-lane pre-flight omits the bundle-stamp member of the
bridge condition (inter-invocation regeneration hazard). M2: frozen
lanes'/comparator's authority/spec digests verified only AFTER canonical
path commitment — hash them in pre-flight. M3: the v004 verification
returns exist in no sealed artifact (recurrence of v003-R1 one cycle
after being flagged) — seal them. m4: comparator v004 header docstring
misstates manifest rows + omits its launcher repoint. m5: supplement
says three rows, diff adds four — record the fourth append-only. m6:
both disclosed sign-off items ACCEPTED on merits by the reviewer;
enumerations must be corrected to match what was done.

## REFUTED (no action)
Bridge "purely structural" concern; push --dry-run concern; unsealed-
receipt-on-write-failure; two duplicate launcher findings. The bridge
mechanism is SOUND; FIX 2 is REAL; B1(piece authenticity) stays CLOSED;
frozen tolerances untouched.

## Autonomous queue (D0-D6) — dispositions
D0: repair once (this record + the discipline artifact + v005 cycle),
then stop touching the pipeline. D1: L2 typing freeze — ALREADY SEALED
pre-window (STAGE8_T7_L2_INTENSIVE_HESSIAN_TYPING_FREEZE_V001, completed
chain; A4(3) erratum pending Brian sign-off). D2: gamma kill-test —
spec was hostile-reviewed (NOT_READY), repair amendment sealed,
primary + blind execution lanes IN FLIGHT at window start. D3: parity
lemmas — Part A of the running gate, seal on return (with the
involution correction already in the amendment). D4: beta artifact —
after the kill-test verdict (per the sealed disposition's contingency).
D5: L1 assessment — to be sealed this window. D6: T7(iii) majorant spec
+ T7(iv) Duhamel spec on the completed chain per the L2 freeze — the
window's main physics work.
