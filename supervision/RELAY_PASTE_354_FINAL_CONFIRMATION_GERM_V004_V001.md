PASTE 354 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] FINAL CONFIRMATION OF GERM V004 — VERIFY THE GAUGE THEOREM, PACKAGE DoR-014

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-271)

```text
DOES THE OBJECT EXIST?   YES: germ V004 (d4cdbb66..., the other lane). Claims: the same-rank
                         gauge theorem PROVED (trace-preserving block-unitary equivalence;
                         p_A, Z_inc, derivatives invariant; residual freedom = ordered
                         integer rank data); all four Q-270 repairs complete; 11/3/0.
IS THE VERSION CURRENT?  YES through Q-271. YOU ordered the four repairs (Q-270) -- verify
                         they are executed as ordered.
ARE ITS INPUTS PRESENT?  V004, your Q-270 determination, the V001-V003 lineage.
```

## 1. THE CHECKS

```text
E1 THE GAUGE THEOREM, verified with your own construction: does the block-unitary quotient
   really identify ALL same-rank presentations, and are p_A, Z_inc AND every derivative
   the germ exports genuinely invariant? Probe the edges: degenerate ranks, the trace
   normalization, and whether "ordered" rank data is the right quotient label (could a
   permutation of equal ranks be a residual discrete gauge? -- if so, the freedom is
   MULTISETS of ranks, not ordered tuples; a labeling precision, but DoR-014 should adopt
   the right object).
E2 THE FOUR REPAIRS: executed as ordered, no scope creep, no new claims smuggled in.
E3 FULL REGRESSION: battery spot-checks, attack lineage, anchor non-selection, B14 intact,
   the p_ch entry path unchanged.
E4 PACKAGE DoR-014: if all checks pass, state in one block exactly what ratification
   adopts (the germ with A0 as its one DISCRETE parameter, pinning NO_VERDICT, the
   K_square arm as named future detector, the neutrality and DoR-008 falsifiers binding)
   and what it OPENS (P1, the P2/P4 analytic interfaces, and the construction chain).
VERDICT: RATIFICATION-READY / REPAIR-THEN-READY / DEAD.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
