PASTE 411 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THE REDUNDANCY DRAFT — THE COLLECTIVE BRANCH-REDUNDANCY FUNCTIONAL AS THE DEPTH OBJECT (C42; DoR-016 RESERVED)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-328 + C42)

```text
DOES THE OBJECT EXIST?   COMMISSIONED (C42): the depth object as the COLLECTIVE REDUNDANCY
                         of the branch state -- how deeply the cell ensemble has
                         committed to the shared charge branch. The Q-328 kill PROVED
                         accumulation is collective (the mixture's exact defect
                         p(1-p)(1-u1)(1-u2) IS cross-cell correlation through the one
                         branch -- record emergence in formula); the draft builds the
                         functional that MEASURES it.
IS THE VERSION CURRENT?  YES through Q-328 + C42. THE KILL'S CONSTRAINTS ARE BATTERY
                         ROWS: no per-cell additivity anywhere; the global mixture
                         structure respected as such; the THREE Q_N objects kept
                         distinct (carrier / probe functional / V007's version --
                         cite each); nothing assumed that the bridge package requires
                         built (Q-409's 8 items are the CONTRACT: each either built
                         here or typed as the draft's own residue).
ARE ITS INPUTS PRESENT?  THE COLLECTIVE STATE IS SEALED: the branch mixture (the
                         weights' closed forms, c7624d88), the forced state p_A =
                         r_ch/(r_0+r_ch) (symbolic), the finite ensembles (PathCert,
                         the sequential tower), T_cyl. THE SECOND TEST SURFACE IS
                         SEALED: the accumulated-record GEOMETRY ESTIMATOR artifact
                         (STAGE8_G_PRIMITIVE_OR_EMERGENT_ADJUDICATION_V001.md --
                         read its estimator's actual functional form; the candidate
                         must RELATE to it or the difference typed). l_P's declared
                         status (D2 evidence: a physical input) binds.
```

## 1. THE DRAFT — MEASURE THE COMMITMENT, THEN BRIDGE, THEN TEST TWICE

```text
1  DEFINE the candidate: R_N := the collective redundancy of the branch state at stage
   N -- the distinguishability of the two charge sectors GIVEN the cell ensemble
   (construct from the sealed mixture: the natural candidates are the sector-
   distinguishability functionals of the sealed weights -- extract, do not invent;
   every ingredient cited to closed forms). Establish its exact properties: behavior
   under zero-extension (identity cells add NO distinguishability -- verify from the
   sealed forms, this is the collective analogue of the dead cocycle's K1 and it must
   PASS where the cocycle failed); monotonicity in genuine cells; the p-dependence
   (symbolic; manifest); reality behavior; quotient descent.
2  THE BRIDGE ATTEMPT, honestly scoped: from R_N toward a scalar functional on K_cycle
   -- Q-409's 8 items as the checklist, each BUILT or TYPED. The kill's lesson binds:
   no additive shortcut; whatever reaches the cycle carrier must go through the
   COLLECTIVE state's structure (candidate: the redundancy's response to cycle-sector
   probes -- the sealed probe tables give R_N WITH probes on; the cycle content's
   effect on distinguishability is computable from Q-279's forms).
3  THE DOUBLE TEST: (a) against the coupling's requirements -- the Q-408 exact forms
   with whatever the bridge yields; (b) against the SEALED GEOMETRY ESTIMATOR -- does
   R_N relate to the estimator's functional (the classical-intersection anchor)? State
   the relation or its absence exactly; a proven relation would be the program's first
   contact between the coupling chain and gravity's classical formulas.
4  KILL-PASSES: the Q-328 constraints; no target-tuning; no selection; symbolic ranks;
   restriction executed; door flags; no evaluation. DELIVERABLE: the functional with
   sealed provenance, its exact properties, the 8-item bridge accounting, the double
   test, and the residue typed. Nothing TYPE-P; DoR-016 reserved; verification follows.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
