PASTE 355 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] BUILD P2 — THE PHYSICAL SOURCE TOPOLOGY AND DIFFERENTIAL CALCULUS, ON THE RATIFIED GERM (CONSTRUCTION)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-272)

```text
DOES THE OBJECT EXIST?   SPECIFIED as P2 in the Q-254 consolidated spec (241bf43f..., the
                         P2 section at :206-228): the physical source topology and
                         differential calculus -- topology_src and Diff_src, the fields
                         the germ's interface declares. NOW BUILDABLE: DoR-014 supplies
                         the ratified germ whose P2/P4 analytic interfaces are OPEN.
IS THE VERSION CURRENT?  YES through Q-272 + DoR-014 (C33). Marks: TYPE-P | premises:
                         DoR-008, DoR-009, DoR-013, DoR-014. RANK DISCIPLINE BINDS: any
                         step needing a SPECIFIC (r_0, r_ch) value voids the step -- carry
                         rank-sensitive content symbolically or family-wide.
ARE ITS INPUTS PRESENT?  YES: the ratified germ (d4cdbb66...), its declared topology_src /
                         Diff_src interface fields, the Q-245 topology discipline (every
                         limit names its topology; the moving-tail hazard), Q-247's classes.
```

## 1. THE TASK — CONSTRUCTION, THE FIRST OF THE CHAIN

```text
1  READ P2's contract from the Q-254 spec and the germ's interface: what topology_src and
   Diff_src must PROVIDE to their consumers (P3's contour/measure work, P4's
   PhysicalLogGerm, P5's derivatives). Quote the consumer signatures.
2  CONSTRUCT topology_src: the topology on the physical source family the germ exports --
   from the ratified data (the germ's finite operational certificates, the sequential
   zero-extension, the cylindrical density). EVERY choice on the choice table with its
   void-condition; where the ratified data DETERMINES the topology, prove determination;
   where freedom remains, name it typed (the Q-249 lesson: an untyped link is where p_ch
   could hide -- type everything).
3  CONSTRUCT Diff_src: the differential calculus (which derivatives exist, in which
   topology, with which chain rules) sufficient for the exported Frechet derivatives the
   germ already carries and for P5's retarded-Hessian extraction downstream. The Q-243
   Keldysh structure must be REACHABLE: verify the finite mixed-derivative computations
   embed as instances.
4  CERTIFY against the battery items that bite here: no naive continuous extension
   (Q-245 -- the topology must handle the moving tail EXPLICITLY: state which class the
   completed sources live in and what the tail's status is IN THIS TOPOLOGY); separation
   compatibility (Q-247); finite restriction reproduction (DoR-008).
5  KILL-PASSES: no rank-value selection; no smuggled measure (P3's job, not P2's); no
   topology chosen to make a downstream verdict come out -- the Q-252 lesson (topology
   choice DECIDES existence questions) cuts both ways.
DELIVERABLE: P2 as a certified construction -- topology_src + Diff_src with proofs,
consumer-readiness statement for P3/P4/P5, typed residual freedoms. This is CONSTRUCTION
on ratified premises: TYPE-P positives are expected and correct here.
```

## 2. LEADS

```text
IF P2 BUILDS DETERMINED -- lead with what the ratified data forced and what P3/P4 can
start consuming immediately.
IF A GENUINE TOPOLOGY FREEDOM REMAINS -- lead with it typed: the choice goes on the
record BEFORE consumers depend on it, with its downstream consequences stated.
IF THE TAIL IS NOT HANDLEABLE IN ANY CANDIDATE TOPOLOGY -- lead with that; it would be
the first sign the deciding subset has a structural problem.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
