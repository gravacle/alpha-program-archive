PASTE 373 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] FIELD_SIGNATURE_PHYS V002 — THE ADJUDICATED MERGE: AUTHORED V001 REDUCED BY THE DERIVED COMPONENTS (DoR-015 RESERVED)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-290)

```text
DOES THE OBJECT EXIST?   BOTH ARMS DO: the authored V001 (d2d88dc5..., nine fields, doors
                         clean) and YOUR derivation (65e4dd6a...: incidence, orientation,
                         locality, CTP-reality DERIVE premise-conditionally; the ten-item
                         residue does not). THE MERGE does not exist. Q-290's adjudication:
                         V002 = V001 REDUCED by everything you derived, repair-by-removal.
IS THE VERSION CURRENT?  YES through Q-290. Marks: derived components enter TYPE-P |
                         premises: DoR-008/009/013/014 (+ their own stated premises);
                         authored fields stay PROPOSED. Door flags mandatory throughout.
ARE ITS INPUTS PRESENT?  YES: both arm artifacts, the Q-288 ledger, the raw-G contract.
```

## 1. THE TASK — SUBTRACT WHAT DERIVES, PROVE THE SEAMS

```text
1  FOR EACH of V001's nine fields: mark DERIVED (your four families cover it -- cite the
   derivation's proof and REMOVE the authored version), PARTIALLY DERIVED (split the
   field: the derived sub-part cited, the authored remainder restated minimally), or
   AUTHORED (one of the ten residue items -- retained with its choice table).
2  PROVE THE SEAMS: where a derived component meets an authored field (e.g., derived
   locality meeting the authored causal support; derived CTP-reality meeting the authored
   scalar CTP representation), prove compatibility explicitly -- a seam mismatch is the
   V002-killer the review will hunt first.
3  RE-RUN the class-formation door audit on the MERGED object (the derivation performed
   its own class formations -- their flags must merge into the ledger without conflict).
4  THE CONSUMERS RESTATED on V002: the raw-G bounded-extension rerun POSED with the
   merged signature (still not executed -- execution follows ratification); the four
   analytic fields' gains; the background-interface statement.
5  KILL-PASSES: nothing authored that the derivation proved (the removal must be
   complete); nothing derived weakened in restatement (compare texts); the ten-item
   residue count reconciled against V001's nine fields (explain the mapping -- if the
   counts disagree, the disagreement is a finding, not a rounding).
DELIVERABLE: FIELD_SIGNATURE_PHYS V002 -- the merged proposal with per-field provenance
(DERIVED with proof / AUTHORED with choice table), seam proofs, merged door flags,
consumer statements. Nothing authored marked TYPE-P. DoR-015 reserved; the cross-lane
adversarial review follows.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
