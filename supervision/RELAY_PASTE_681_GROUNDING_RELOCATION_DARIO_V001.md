## PASTE 681 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / TRANCHE] THE GROUNDING RELOCATION

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-605.
- Batch 1's refusal of record: `STAGE8_TASK6_S_TRANCHE_BATCH1_LANE2_V001.md` = `5bea1f59…` — its per-envelope missing-input lists are your work orders. The map (`e85a6113…`) supplies the original pins.
- Known relocation leads of record (verify, do not assume): the seal-stage DAG lives literally in the AUTHORITATIVE A32 machine graph (the stage_dependencies you parsed and verified at Q-546/Q-551) — candidate grounding for V009-06/V010-12/V008-10; V009-01's carriers are V011:128-192 IN FULL; V008-05's plaquette word expansion is displayed in V011's own equations; V009-03's global/local objects live in V011's filtration sections.

TASK — for each of the ten:
B1. Locate the actual COMPLETE sealed display that determines every opcode input Batch 1 found missing (M-2, all four modes, whole-corpus scope incl. the packet and the sealed supervision decisions); output the corrected grounding: {source path, sha256, exact spans, which opcode argument each span populates}. The bar is Batch 1's own: the spans must DETERMINE the arguments — no edge, carrier, corpus, or outcome chosen.
B2. Any envelope with NO complete display anywhere: classify TRUE-ABSENT with the bounded search displayed — it leaves the S tranche honestly (and joins the 7A remainder).
B3. For the M2-opcode rows (V008-10, V010-14, V009-08, V009-01): the demanded source CORPORA must themselves be sealed named sets — propose the lawful corpus definition per row (e.g., the packet manifest's member list) rather than a hand-picked list; flag any row where no sealed corpus definition exists.
B4. Battery: PIN CHECK; VERDICT-LINE SCOPE; self verb audit.

OUTPUT: one sealed artifact IN THE ARCHIVE WORKSPACE
`STAGE8_TASK6_GROUNDING_RELOCATION_DARIO_V001.md`
with final lines: `RELOCATED = N/10 (+per-envelope span sets)`, `TRUE_ABSENT = N (+list)`, `CORPORA = defined/flagged per M2 row`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors; Builder A rebuilds against the relocated grounding). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
