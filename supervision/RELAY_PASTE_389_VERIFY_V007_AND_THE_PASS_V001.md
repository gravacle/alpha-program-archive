PASTE 389 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] CROSS-VERIFY THE N-UNIQUENESS PROOF, GERM V007, AND THE FULL DOOR-D PASS

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-306)

```text
DOES THE OBJECT EXIST?   YES: the uniqueness proof + V007 install + four-subtest PASS
                         (00414b0d..., bd33e54c..., the other lane). THE FREEZE LIFTED ON
                         THIS RESULT -- everything downstream now compounds on it. Your
                         Q-305 audit certified the test; verify the PASS with the same
                         independence.
IS THE VERSION CURRENT?  YES through Q-306.
ARE ITS INPUTS PRESENT?  Both artifacts, your own audit (0fcf42e8...), the parity
                         determination (323852e8...), the sealed laws.
```

## 1. THE CHECKS

```text
V1 THE UNIQUENESS PROOF: re-derive it -- Condition 6 fixing the coefficient, U1 fixing
   the anti-intertwining law, the cycle-kernel non-freedom, and the SCOPE honesty (the
   unused bilocal complement's NO_VERDICT -- verify nothing installed there and nothing
   downstream consumes it).
V2 G^007 = -hbar^2 q(1-q) L(x)L: recompute from V007's definitions; verify the identity
   D_J A-bar = (i/hbar) G^007 EXACTLY, and the i-ownership table (identity's i sealed,
   W's i in W's chain, N's i from the theorem -- each billed once).
V3 THE FOUR SUBTESTS: re-run each independently (your Q-305 transport diagram is the
   licensed route). Any FAIL revives the freeze and leads your report.
V4 V007 MINIMAL DIFF + battery inheritance: verify.
V5 THE q(1-q) FORM: verify the physical G's noise weight restricts EXACTLY to Q-279's
   finite forms (kappa_eta at eta=0 consistency, the dephasing weights) -- the DoR-008
   falsifier's bite on the new object.
V6 THE HONEST STOP: confirm the chain stops at the physical inverse/Schur + completed-
   domain package and that the retarded shadow stays zero and p-free through everything
   newly installed.
VERDICT: CONFIRMED (the freeze-lift stands) / DEFECT (location + whether the freeze
re-imposes) / CONFIRMED-WITH-NOTES.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
