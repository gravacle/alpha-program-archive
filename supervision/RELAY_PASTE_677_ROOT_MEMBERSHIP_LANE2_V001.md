## PASTE 677 — CODEX LANE 2 = BUILDER A (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / BUILD] THE ROOT MEMBER SET: TWELVE

(Same Lane 2 session rules: cleanroom writes; the registrar mirrors.)

CONTEXT: run 020 — `VERIFIER_ROOT_DIGEST` mismatch, registrar-reproduced exactly: B's declared `dba5377d…` = the 12-member set (ten `verifier/*.py` + `run_verifier.py` + `contracts/verifier_verdict.schema.json`, per B's sealed 676 artifact); your computed `e198ef9d…` = 11 members, missing the schema YOUR OWN 675 fix now validates against. Your membership list is a transcription of B's root definition, and it went stale one relay after being written.

TASK:
Y1. Update your root computation to the 12-member set per the sealed 676 artifact (`de913976…` — a sealed source you may read; B's code remains off limits).
Y2. RECORD, do not settle: the structural note that root MEMBERSHIP is transcribed on your side and will drift again on any future membership change — the durable fix is the instance carrying the member list explicitly (a contract V002 item for the closing docs, alongside B's recorded name-binding observation from 667).
Y3. Static self-check; disclosed delta; PIN CHECK.

OUTPUT: updated parent + one sealed artifact
`STAGE8_TASK6_ROOT_MEMBERSHIP_LANE2_V001.md`
with final lines: `MEMBERS = 12 (per the sealed 676 list)`, `NOTE = membership-in-instance recorded for contract V002`, `SELF_CHECK = passed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal in the cleanroom, report hashes, STOP (the registrar mirrors and re-invokes). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
