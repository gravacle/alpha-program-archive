PASTE 304 — CODEX LANE 2 (HIGH EFFORT) — [TASK 2d] RUN THE RESPONSE-STATE FACTORIZATION TEST

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-221)

```text
DOES THE OBJECT EXIST?   The TEST is specified by Q-221's audit: freeze one complete non-state
                         CTP package; place >= 2 concrete admissible states; compare the FULL
                         induced operators BEFORE projection.
IS THE VERSION CURRENT?  YES.
ARE ITS INPUTS PRESENT?  The carrier/grammar exist (C0_008, U1_008, the U2 skeleton). The two
                         concrete states exist: *** Q-211's two distinguished scalarization
                         candidates -- name and use exactly those. *** The dynamics slot may be
                         a DECLARED TEST FIXTURE (the K_L precedent): finite, explicit,
                         target-independent, marked FIXTURE_NOT_PHYSICAL.
```

## 1. THE TEST — AND ITS ASYMMETRY, WHICH IS THE WHOLE DISCIPLINE

```text
1  FREEZE the fixture package: one finite, fully explicit non-state CTP package on the ratified
   carrier -- dynamics fixture declared, boundary data declared, everything else from the built
   components. FROZEN BEFORE ANY COMPARISON OUTPUT IS SEEN.
2  PLACE both Q-211 candidate states on it. Two full induced operators, BEFORE projection.
3  COMPARE, structurally: equal as operators / equal up to the sealed conventions / DIFFERENT.
   *** COMPARE OPERATORS, NOT NUMBERS -- no spectra, no values, nothing evaluated. ***

THE ASYMMETRY (pre-registered at Q-221):
  DIFFERENT  -> *** DECISIVE: the chain CAN consume the state's identity. OBS-14 DIES. The
                new-physics ask is REAL and goes to the principal, named. ***
  EQUAL      -> SUPPORTING ONLY: invariance on a fixture does not prove invariance on the
                physical package. Report it as exactly that -- the reviewer's scorecard on
                hopeful upgrades is 7 refuted of 11.
```

## 2. LEADS

```text
IF DIFFERENT -- lead with WHERE they differ: which operator component varies with the state.
That component IS the new-physics requirement, localized.
IF EQUAL -- lead with WHY, structurally: what in the fixture's composition makes the state drop
out before projection. If the reason is FIXTURE-SPECIFIC, say so; if it looks structural, state
the candidate theorem WITHOUT claiming it.
IF THE FIXTURE CANNOT BE FROZEN honestly -- lead with what is missing; do not bend the fixture
to make the test runnable.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
