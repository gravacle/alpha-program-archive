PASTE 366 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] BUILD P5 WITH THE DEPENDENCE-ACCOUNTING PACKAGE — THE TRANSPORT CHAIN, MAXIMAL ON BUILT MATERIAL

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-282)

```text
DOES THE OBJECT EXIST?   THE MATERIAL does, at last: P2 V002 (topology/calculus), P4 (the
                         anchored W = -i hbar Log Z), the law-side subpackage (Q-278), the
                         seed with explicit p_A = r_ch/(r_0+r_ch) (Q-281), the finite
                         restriction targets (Q-243 at R=0, Q-279 with probes). P5 -- raw
                         correlator -> inverse Hessian -> retarded Hessian -> induced
                         response -- has never been attempted ON this material. And Q-282
                         defines its mandatory companion: THE FOUR-ARROW DEPENDENCE-
                         ACCOUNTING PACKAGE.
IS THE VERSION CURRENT?  YES through Q-282. Marks: TYPE-P | premises: DoR-008/009/013/014.
                         Rank discipline binds (symbolic (r_0, r_ch)). W-convention binds.
ARE ITS INPUTS PRESENT?  The four missing fields enter ONLY as named interface premises
                         where an operation genuinely needs one -- WITH ITS KERNEL
                         ACCOUNTED (the Q-282 corrected object): per operation, record
                         kernel, image, sector transfers, the restriction square, and the
                         Tail_R action. An unaccounted operation is a defect.
```

## 1. THE TASK — THE CHAIN, AS FAR AS IT BUILDS, EVERY ARROW ACCOUNTED

```text
1  ARROW ONE -- THE RAW BILOCAL: construct G from P4's W second derivatives on the built
   domain (P2's calculus; the anchored branch; the seed's first-order data). State its
   sector structure (which blocks exist: retarded-candidate, noise, J_delta/R) and its
   EXACT p-content per block (must match Q-279 on restriction -- verify).
2  ARROW TWO -- THE INVERSE HESSIAN: where inversion is needed, state ON WHICH BLOCKS it
   is defined on built material (bounded/cylinder level), where it needs a missing field
   (WHICH: measure for the integration? domains for the unbounded closure?), and account
   the operation's kernel honestly.
3  ARROW THREE -- THE RETARDED EXTRACTION: the Keldysh rotation at the physical level
   (Q-243's structure through P4's convention). THE CENTRAL ACCOUNTING ROW: what is the
   retarded operation's kernel, and does any p-carrying block from arrow one TRANSFER
   into the retarded image? On the finite shadow the answer is proven NO (Q-279); state
   what the completed operation adds or cannot add, per the accounting.
4  ARROW FOUR -- THE INDUCED RESPONSE Pi_R,ind: assemble what B_ind's argument
   (Q-253: p_loc[Pi_R,ind[G_K]]) needs; state exactly how far it now EXISTS (which parts
   bounded-built, which interface-conditional) and its p-content as built.
5  THE ACCOUNTING PACKAGE, assembled: the four arrows' table -- kernel / image / sector
   transfers / restriction square / Tail_R action -- THIS TABLE IS THE DELIVERABLE AS
   MUCH AS THE CHAIN. Q-282's conditional finite-shadow theorem is the standing check:
   verify the built chain reproduces Q-279's ENTIRE pattern on restriction.
6  THE VERDICT SHADOW: with the chain as built, state the CURRENT p_ch verdict status:
   which arrows are p-clean by construction, where p could still enter the retarded
   image (only through unaccounted interface premises? through Tail_R? through the lift
   fiber?), and what SINGLE list of remaining objects separates this artifact from the
   executable verdict. BE EXACT -- this list is what the program runs on next.
7  KILL-PASSES: no missing-field VALUE used; no positivity forced; no rank values; the
   accounting complete (an operation without its row is a defect); restriction checks
   executed not asserted.
DELIVERABLE: the P5 chain (maximal on built material) + the complete accounting table +
the verdict-status statement with its exact remaining-objects list. Typed throughout.
```

## 2. LEADS

```text
IF THE RETARDED IMAGE IS p-CLEAN ON EVERYTHING BUILT WITH ALL TRANSFERS ACCOUNTED --
*** LEAD WITH IT AND THE REMAINING-OBJECTS LIST: the distance to the verdict, in named
objects, is the sentence the program has been building toward all night. ***
IF A SECTOR TRANSFER CARRIES p INTO THE RETARDED IMAGE -- lead with the transfer: that
is survival's mechanism, named, and the verdict question becomes whether an admissible
realization must or must not perform that transfer.
IF THE CHAIN STOPS EARLY -- lead with the arrow and its missing field; the accounting
of what stopped it is still the deliverable.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
