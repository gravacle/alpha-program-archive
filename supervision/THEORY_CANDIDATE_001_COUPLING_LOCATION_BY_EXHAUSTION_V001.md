THEORY CANDIDATE 001 — THE COUPLING'S LOCATION IS FORCED BY EXHAUSTION

STATUS: CANDIDATE. Not a result, not a derivation, not a producer.
DATE: 2026-07-30. Reviewer lane (supervision).
alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
theory_status = CANDIDATE_WITH_PREREGISTERED_PREDICTION
derived = false; adopted = false; test_attached = true (Q-37 rule)

---

## 0. WHY THIS IS BEING WRITTEN NOW

Four rulings landed today, each answering a different question, each returning a
NEGATIVE. Separately they read as four places the program failed to find what it
was looking for. Together they have a shape, and the shape is a claim.

**It is written BEFORE lane 1 reports under paste 153**, whose answer bears
directly on section 4. Writing it after would make the prediction unfalsifiable
by construction. The commitment is the point.

---

## 1. THE CLAIM

*** IN THE INDUCED-ONLY BRANCH, THERE IS NO PLACE LEFT TO PUT A FREE COUPLING
PARAMETER. ***

The source-to-durable-record chain has exactly four points where a strength
could have been inserted. Four independent rulings, none made for this purpose,
have each closed one. If the closures hold, then alpha — if this program
produces it at all — cannot be a strength assigned anywhere in the chain. It
must be **the impedance of the induced response, evaluated at the durability
threshold.**

Not a parameter that is set. A quantity that is left.

---

## 2. THE FOUR CLOSURES

**(1) NOT IN THE CARRIER.** Q-43: the source-record composition is a graded
tensor product, and source and record observables COMMUTE. A coupling is a
failure of independence; commuting algebras have none. Nothing lives here.

**(2) NOT IN THE COMPOSITION.** Q-43: the record sector enters as a
MONOMORPHISM, not a quotient and not a crossed product. A crossed product would
have encoded the strength of the source's action on the record inside the action
itself. The corpus does not specify one and does not force one. There is no
normalization to carry.

**(3) NOT IN THE WRITE.** Q-44, and this is the sharpest of the four. The finite
reversible write is `B_ch = P_ch tensor B_Q`, and `lambda != 1` **fails the
retraction axiom**. The write is not merely specified without a strength — it is
*forbidden* one. This is stronger than a missing parameter: a missing parameter
invites supply, a forbidden one closes the site.

**(4) NOT BARE.** `K_bare = 0`, the induced-only axiom, whose content is the
proper-time floor (Q-18). There is no pre-existing stiffness to normalize
against. Whatever stiffness exists is generated.

**THE FOUR WERE NOT COORDINATED.** (1) and (2) came from typing an algebra; (3)
from an attempt to KILL an observation, which succeeded; (4) is an inherited
axiom older than this thread. That they close a complete set of insertion points
is the observation. It is not evidence that they are right, but it is why the
conjunction is worth stating.

---

## 3. WHAT THIS DOES NOT CLAIM

*** IT DOES NOT DETERMINE A VALUE, AND MUST NOT BE READ AS DOING SO. ***

Closing insertion points says WHERE alpha lives. It says nothing about what it
equals. The program's own standard is unchanged: a number is forced only by
**overdetermination** — two independently-motivated conditions landing on one
value — and permissions never force a number no matter how many compose.

Specifically it does NOT claim:
- that `K_*` exists, is unique, or is computable;
- that the running fixes the value (running fixes a SLOPE; `dK/d ln Q = -1/(6 pi^2)` is a beta function);
- that any of the eighteen `Q_spec` slots is thereby derived — the count is still ZERO;
- that the seventeen untested premises (Q-45) are affected. They are not. `C_R = 1` in particular is untouched by this and remains the only absolute-scale selector, still without a falsifier.

**This theory is compatible with the program producing no number at all.**

---

## 4. THE PREREGISTERED PREDICTION — AND I EXPECT IT TO FAIL

If the coupling is the response impedance, and the response is a loop over the
charged continuum (OBS-06), then the question is whether **durability** runs
through that same continuum. Non-return — the durability condition — holds
because a state disperses into a continuum rather than recurring. If that is the
CHARGED continuum, durability and the coupling share one cause.

*** PREDICTION AS POSED: non-return's derivation consumes charged-sector content. ***

*** MY ACTUAL EXPECTATION: IT DOES NOT, AND THIS PREDICTION FAILS. ***

Riemann-Lebesgue is a theorem about measures. Absolute continuity and absence of
point spectrum are generic spectral hypotheses; they hold for any operator with
those properties and refer to nothing charged. **The honest prior is that
non-return is generic.** It is recorded here in advance so that the outcome
cannot be reinterpreted after the fact.

**IF GENERIC (expected):** section 2 stands, section 4 fails. The theory reduces
to "the coupling is the response impedance," which is real but weaker — it
locates alpha without connecting it to durability. OBS-06 closes as suggestive.

**IF CHARGED (not expected):** the durability threshold and the response value
are traceable to one structure. That is the overdetermination SHAPE, though not
yet overdetermination — one would still need the two conditions to force a
number rather than merely share an origin.

---

## 4A. TEST RETURNED — PREDICTION FAILED, AS PREREGISTERED

**Lane 1, 2026-07-30, `STAGE8_GAMMA_K_NONRETURN_CHARGED_SPECTRUM_OBS06_TEST_V001.md` (`2f99c574…`),
archive `d98ff6d`, gate GREEN.**

Non-return is derived **generically**: coarea + `L1` spectral density + Riemann-Lebesgue on the stated
source-record Hamiltonian's non-flat absolutely continuous spectral bands. **The proof consumes no
charged-sector content** — no species, no charge assignments, no thresholds, no particle masses, no
induced-loop carrier inventory. **This is the outcome section 4 recorded as expected.** OBS-06 is
SUGGESTIVE, not structural. Durability and the induced response do not share a sealed cause.

**Sections 1-2 STAND. Section 4 FAILED. The theory reduces as stated: alpha is located as the response
impedance, and is NOT thereby connected to durability.**

*** AND THE TEST RETURNED SOMETHING THE PREDICTION DID NOT ANTICIPATE. *** The induced-response side is
not merely unproven-identical to the non-return spectrum — **it is a different object that DOES NOT YET
EXIST.** The response route "is still missing the operator and kernel" (`:226`); what is required is "a
future complete BR/CTP fluctuation/response operator and exact induced kernel" plus a covariant local
projector (`:272`, `:304`).

**SO THIS THEORY'S SURVIVING HALF NAMES AN UNBUILT OBJECT.** Section 1 says alpha must be the impedance of
the induced response; the induced response operator is not constructed. That converts the claim from a
statement about where alpha lives into **a construction target** — and one that must be checked against
the standing `Gamma_K` charter, since `K` is typed as a local surrogate for induced connection response
(Q-44) and the two may be the same object under different names. **That check is not done here.**

---

## 5. HOW THIS THEORY DIES

- **Any of the four closures is wrong.** Most exposed: (2). "Not specified and
  not forced" is weaker than the other three — it is an absence of
  specification, and absences get filled. If sealed text is later found to
  specify a crossed product, closure (2) fails and a normalization site reopens.
- **A fifth insertion point exists** that the four do not cover. The chain was
  enumerated by the reviewer, not derived. **This is the most likely failure
  mode and no artifact certifies the enumeration complete.**
- **The retraction axiom is not binding** on the write in the relevant branch,
  reopening (3).
- **The induced-only branch is abandoned**, which voids (4) and the whole claim.

---

## 6. STANDING

This is a CANDIDATE in the sense established by the observations register: an
observation whose significance is UNCLAIMED. It is not adopted, not a premise,
not a licence, and confers no authority on any construction. Under Q-37 it
carries its test — section 4 — and is marked untested until that test returns.

It changes no flag. `0 of 11` and `0 of 18` are unmoved.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
