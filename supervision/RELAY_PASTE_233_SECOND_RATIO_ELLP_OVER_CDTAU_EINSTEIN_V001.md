PASTE 233 — EINSTEIN (OPUS 5) — THE THIRD EQUATION: WHAT DERIVES `ell_P/(c Delta tau)`?

ROAD: **ADVANCES STEP 2.** *** THIS IS THE MISSING RELATION IN THE ONLY FORK-FREE CONDITION THE
PROGRAM HAS. IF IT EXISTS, THE CONDITION CLOSES **WITHOUT EVER TOUCHING THE `sqrt(2)` FORK.** ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.

*** THIS RELAY IS SELF-CONTAINED. ASSUME NO MEMORY OF ANY PRIOR RELAY. ***

---

## 1. WHERE THIS COMES FROM

A record cell has an **inside** (projective, dimensionless angles) and an **outside** (Lorentzian causal
diamond, lengths). A prior determination
(`STAGE8_INSIDE_OUTSIDE_CONSISTENCY_CONDITION_EINSTEIN_V001.md`, `15793db5…`) produced the program's
**only fork-free consistency relation**:

```text
K_* = K_KK   ->   *** rho^2 = 16 pi K_*     [ THE rho-FORM: FORK-FREE ] ***
                  rho = R_*/ell_P
```

*** THE `rho`-FORM IS FORK-FREE; THE `beta`-FORM IS THE ONE THAT IMPORTS THE MISNER-SHARP /
BROWN-YORK `sqrt(2)`. WORK IN `rho`. ***

**It does not close: three unknowns against two equations.** And the missing third relation is named
precisely: *** A SECOND DIMENSIONLESS RATIO, `ell_P/(c Delta tau)`, THAT NOTHING IN THE CORPUS DERIVES
(`:43`, `:209`). ***

**Both quantities now exist.** `ell_P` was undefined in every root until today; it is now
`ell_P^2 := kappa_5^2/(2 pi R_0)` on the parent action `S_5 = (1/2 kappa_5^2) integral sqrt(-G) R_5`
(`STAGE8_PARENT_KK_ACTION_ELL_P_DEFINITION_AND_REDUCTION_V001.md`, `01c4c0db…`). **`c Delta tau` is the
record interval — a proper interval in the base block of the same five-metric.**

---

## 2. THE TASK

**2.1 WHAT COULD DERIVE `ell_P/(c Delta tau)`?** Go through what the corpus has and, for each, say
whether it bears — **quoting paths and lines, and typing each**:

```text
the record interval's own construction -- what fixes c Delta tau, and relative to what
the durability / thresholded-durability condition (cycle 7 forces it for many-cell work)
the marginal closure condition C_R = 1  -- *** the ONLY absolute-scale selector the program has,
  and it CANNOT SELECT ALONE. Establish whether it bears on THIS ratio specifically. ***
the spectral floor: N_BR(k_R) = 1, and k_R -- *** NOTE: k_R SITS UNBRIDGED INSIDE THE EQUATION
  FIXING K_*, so a k_R route may be circular. CHECK FOR CIRCULARITY EXPLICITLY. ***
the A_4 logarithm -- "a logarithm needs a reference scale", so a scale-side quantity already sits
  inside the threshold route. WHAT reference scale does it need, and is it this ratio?
tau_R = pi/sqrt(2) and the proper-time floor s = 1/k_R^2
anything in the parent action that sets a length independent of R_0
```

**2.2 IF SOMETHING BEARS, WRITE THE RELATION SYMBOLICALLY** and state what it consumes. **If it
consumes a quasilocal energy, DECLARE IT — that is a silent Misner-Sharp / Brown-York fork choice.**

**2.3 CHECK THE PARAMETER COUNT.** With the candidate relation added: **three unknowns, three
equations?** *** RUN THE COUNT EXPLICITLY. A relation that adds an unknown along with an equation has
bought nothing. ***

**2.4 IF NOTHING BEARS, SAY SO AND TYPE IT.** Q-92: naming what is missing is a complete answer.

---

## 3. THE OBSTRUCTIONS THIS MUST GET PAST — ALL THREE, OR SAY WHICH SURVIVE

*** THE PRIOR DETERMINATION LISTED THREE INDEPENDENTLY SUFFICIENT OBSTRUCTIONS TO CLOSURE. A THIRD
EQUATION REMOVES ONLY THE PARAMETER-COUNT ONE. STATE EXPLICITLY WHAT HAPPENS TO THE OTHER TWO: ***

```text
1  THE HELD-FIXED BRANCH IS UNDETERMINED, so the outside's weight is branch-indexed:
   beta^2 if ell_P is held fixed, *** beta^3 if kappa_5 is held fixed ***, and the corpus types
   that choice TERMINAL. Nothing forces either branch.
   *** DOES YOUR CANDIDATE RELATION DEPEND ON THE BRANCH? IF SO, IT INHERITS AN UNDERIVED CHOICE
   AND MUST SAY SO. IF IT IS BRANCH-FREE, THAT IS A MAJOR RESULT -- LEAD WITH IT. ***
2  k_R sits UNBRIDGED inside the equation fixing K_*.
3  BOTH MATCHING MAPS ARE UNBUILT.
```

**And one thing that is settled and must not be re-litigated:** *** THE CONDITION MAY NOT BE WRITTEN AS
`alpha_threshold = alpha_geometric`. *** V011 states that the equality of `alpha_micro` and `alpha(0)`
*"is not assumed. It must follow from the complete amplitude or fail,"* with falsifiers firing on that
equality being assumed. **An alpha-equality is an adoption of the kappa identification wearing a
condition's clothing.** **Work from `rho^2 = 16 pi K_*`, not from an alpha equality.**

---

## 4. SYMMETRIC LEADS

```text
IF A BRANCH-FREE RELATION EXISTS -- *** LEAD WITH IT, THEN ATTACK IT YOURSELF. It would close the
parameter count on the only fork-free condition the program has, without touching the sqrt(2). ***

IF A RELATION EXISTS BUT IS BRANCH-DEPENDENT -- lead with that and say which branch, so the program
knows what it would be inheriting.

IF NOTHING DERIVES IT -- *** LEAD WITH THAT PLAINLY. Then rho^2 = 16 pi K_* is a true relation the
program cannot use, and that is worth knowing before more is built on it. ***
```

*** DO NOT MANUFACTURE THE RELATION. The prior lane convicted its own condition on parameter count and
reported it; do the same. A relation that closes the count by importing an undeclared scale would be
worse than none. ***

---

## 5. RULES

- **Q-54 typing on every negative:** TYPE-R refuted · TYPE-U unbuilt · TYPE-S scope-empty · TYPE-C
  constraint-blocked. `NO_VERDICT` is legal. **Only TYPE-R is physical content.**
- **Q-92:** specifying and naming what is missing is a complete answer, not a lesser one.
- **THE BAR (Q-65/Q-76, `PRINCIPAL_AMENDMENT_002`):** a REQUIRE whose **existence and uniqueness are
  both derived**. **Overdetermination is NOT required.** *** THE LIVE CROSS-SECTOR SPEC STILL EMBEDS
  THE STRUCK BAR AT `:265-267` AND `:292` — DO NOT APPLY F3 OR MINIMAL-PASS ITEM 3. ***
- **Permissions never force.** *** THE CONDITION MUST CLOSE, NOT MERELY TOUCH —
  `boundary_access_closure_threshold_principle_v001.md` was retired as `CELL_CONSTRAINT_ONLY` for
  exactly that, and two candidates have since reproduced the failure. ***
- **Q-69:** never identify a flag with the object that discharges it.
- **No unproved-identity transport.** *** AND NOTE: "the same-object ground was NOT EXTENDED TO THE
  FIBER." Do not assume the two routes describe one object at route level — that inference is
  UNDEFEATED BUT UNAFFIRMED. ***
- **Word-boundaried, CASE-INSENSITIVE. Mark hypothetical flags inline. Check scope before asserting a
  negative.** *** `alpha-program-archive/cleanroom_output/` holds 87 files OUTSIDE `workspace/`;
  sidecars are `NN_NAME_V001.seal.sha256`, no `.md` before `.seal`. ***

## 6. FENCES — VERBATIM, NON-NEGOTIABLE

- **Never touch `a32_holdout/custodian_private/`.**
- **Do not compute** alpha, `kappa_record`, `kappa_Thomson`, a coupling, a scale, a root, an
  eigenvalue, a beta function, `E_R`, `T_R`, `k_R`, or any absolute interval. *** WRITE THE RELATION;
  DO NOT SOLVE IT. NO NUMERICAL VALUE FOR `rho`, `beta`, `ell_P`, `kappa_5` OR THE RATIO. ***
- **No comparison to any measured constant.**
- **Do not resolve the Misner-Sharp / Brown-York fork by choosing — it must be DERIVED.**
- **Report refutations; never repair them.**

## 7. CUSTODY (Q-91)

*** NO LANE RUNS ANY GIT COMMAND. *** Write the artifact in the cleanroom, compute its
`.seal.sha256` sidecar and **verify it matches**, mirror artifact + sidecar to
`alpha-program-archive/workspace/`, report hashes and paths, and **stop**. **A red gate is never a
reason to stop working.** The reviewer verifies, baselines, commits and pushes.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
