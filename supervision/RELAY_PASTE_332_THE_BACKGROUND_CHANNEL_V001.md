PASTE 332 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE BACKGROUND CHANNEL — IS THE EVALUATION POINT p_ch-DEPENDENT, AND IS IT FORCED? (DoR-011)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-248)

```text
DOES THE OBJECT EXIST?   THE QUESTION does (Q-245 channel (b)): p_ch can re-enter the complete
                         retarded response through a p_ch-DEPENDENT STATIONARY/ON-SHELL
                         BACKGROUND. Q-243 proved the retarded BLOCK at a = 0 is p_ch-free;
                         the open question is the EVALUATION POINT itself.
IS THE VERSION CURRENT?  YES: Q-243 (dGamma|0 = -i p_ch -- the one-point block is EXACTLY the
                         gradient), Q-245 (channels), Q-247 (tail closed on ratified classes),
                         Q-248 (2f capped; not this question).
ARE ITS INPUTS PRESENT?  YES, EXACT AND FINITE: A_N^(p) = (1-p) + p Z_N with Z_N the pure
                         phase product; Gamma_N = log A_N; the ratified law's certificates,
                         INCLUDING C1: the A = 0 EXACT OPERATOR REDUCTION.
```

## 1. THE TASK — COMPUTE THE STATIONARY CONDITION EXACTLY, THEN ASK WHO FIXES THE POINT

```text
1  COMPUTE, exact at finite N: the stationarity condition dGamma_N/da = 0 on the ratified
   domain. Since dGamma|0 = -i p_ch != 0 for p_ch in (0,1), a = 0 is NOT stationary. Derive
   the stationary set a*(p_ch) EXPLICITLY (symbolic p, structure only) and certify its
   p_ch-dependence or independence. Handle the pure-phase structure honestly: if
   stationarity fails to exist on the real domain (|A_N| considerations), SAY SO -- an empty
   stationary set is a finding, not a failure.
2  *** THE FORCING QUESTION, WHICH IS THE POINT: WHAT DOES SEALED/RATIFIED TEXT SAY THE
   RESPONSE IS EVALUATED AT? Candidates to check deliberately: (a) the law's C1 certificate
   -- the A = 0 EXACT operator reduction: does its sealed wording FORCE evaluation at a = 0
   (in which case Q-243's p_ch-free block IS the evaluated block and channel (b) CLOSES)?
   (b) any sealed retarded/Thomson response text specifying its expansion point; (c) any
   stationary-phase or on-shell prescription in the sealed CTP texts. QUOTE EACH. ***
3  THE THREE OUTCOMES, decide which the record supports:
   (i) EVALUATION AT a = 0 IS FORCED -- channel (b) closes; combined with Q-247's tail
       closure on ratified classes, state exactly what remains of the deciding question;
   (ii) EVALUATION AT a*(p_ch) IS FORCED and a* depends on p_ch -- channel (b) is REAL;
       exhibit the exact functional form by which p_ch enters the evaluated Hessian;
   (iii) THE EVALUATION POINT IS UNTYPED BY SEALED TEXT -- name the freedom; that is a
       ruling for the principal, not a choice for a lane.
4  KILL-PASSES: no importing a stationarity prescription from convention (stationary phase
   is STANDARD physics but standard is not sealed -- the corpus-audit trap binds); no
   conflating the record-write stationarity with response-evaluation stationarity if the
   sealed texts distinguish them; every quoted forcing text checked at source.
5  FENCES (DoR-011): construction and symbolic trace only. NO evaluation of any root or
   number, no comparison to measured constants.
```

## 2. LEADS

```text
IF (i) -- *** LEAD WITH THE QUOTE THAT FORCES a = 0. Channel (b) closing would leave the
deciding question standing on the response-class typing alone (relay 331's terrain) -- say
so explicitly. ***
IF (ii) -- *** LEAD WITH THE FORM a*(p_ch) AND THE EVALUATED HESSIAN'S p_ch STRUCTURE. That
is p_ch SURVIVING, and the program must hear it plainly. ***
IF (iii) -- lead with the untyped freedom and the exact texts that fail to close it.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
