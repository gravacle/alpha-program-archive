PASTE 375 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] FIELD_SIGNATURE_PHYS V003 — THE THREE REPAIRS: ROOTED FRAMES, THE VISIBILITY QUOTIENT, THE RESTORED INTERTWINER (DoR-015 RESERVED)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-292)

```text
DOES THE OBJECT EXIST?   V002 (deaa86ee..., YOUR merge) is REPAIR-THEN-READY (Q-292, review
                         d11c0ffd...): S3 and S5 refuted as written; R6's endpoint
                         intertwiner dropped. The repair candidates are NAMED in Q-292 --
                         both from the program's own structure, not new authorship.
IS THE VERSION CURRENT?  YES through Q-292.
ARE ITS INPUTS PRESENT?  V002, the review, the derivation artifact (the ROOTED incidence
                         with its proofs), OBS-16 (the visibility-quotient principle),
                         the Q-288 ledger.
```

## 1. THE THREE REPAIRS

```text
R-A (S3): ENDPOINT FRAMES FROM THE DERIVED ROOTS. The origin's rooted signed-incidence
   realization is DERIVED premise-conditional structure (Q-290) -- roots are basepoints.
   Construct the endpoint-frame assignment from the rooted structure and PROVE it renders
   open-path transport scalar-valued on the admitted A1 family (or prove which sub-family
   it covers -- if the roots frame only realized-path endpoints, state the exact scope
   and whether C_Emb/Door A need more). NO global trivialization ASSUMED -- if the rooted
   frames yield one, DERIVE it; if they yield a groupoid-valued structure instead, take
   THAT seriously as the honest object and state what consumers need of it.
R-B (S5): THE VISIBILITY QUOTIENT. Connections differing outside every realized path are
   record-invisible. Per the program's finite-authority basis and OBS-16 (allow-side
   freedom is gauge): DEFINE the physical connection carrier as the PATH-VISIBLE QUOTIENT
   and prove A4/A5's coordinates SEPARATE the quotient (they should by construction --
   prove it, don't assume it). State what this changes for consumers: downstream objects
   may only consume quotient-invariant content -- check each of the six authored
   structures for quotient-compatibility. If any consumer NEEDS sub-quotient information,
   that need is a finding (name it) -- it would say a consumer wants record-invisible
   physics, the exact thing the program forbids.
R-C: RESTORE R6's physical endpoint intertwiner to the reconciliation with its provenance
   (authored or derivable from the rooted frames -- CHECK: endpoint frames and endpoint
   intertwiners are adjacent structure; R-A may supply R-C).
2  RE-RUN: the seven seams on V003; the door audit; the choice tables updated (anything
   R-A/R-B DERIVED comes OFF the authored list -- the provenance line moves again and
   OBS-17's record must show it).
3  KILL-PASSES: the quotient must not delete record-VISIBLE content (only path-invisible
   freedom); the rooted frames must cite the derivation's proofs, not re-author them;
   no forbidden completions; door flags filled.
DELIVERABLE: V003 with the three repairs, updated provenance line (authored count stated),
seam and door re-runs. DoR-015 reserved; final review follows.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
