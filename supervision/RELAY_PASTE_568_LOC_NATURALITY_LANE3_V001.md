## PASTE 568 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5 / EQ6] THE LOC-NATURALITY LEMMA, STANDALONE

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-493.
- This is L2 of your two-lemmas relay, correctly left unrun by the stop rule; it is INDEPENDENT of the seed question and proceeds on its own. Your audited artifact: `alpha-program-archive/workspace/STAGE8_TASK5_EQ6_TWO_LEMMAS_LANE3_V001.md`, SHA-256 `48616c239ccdd777d1ac7cf5a049f324b98ca6abdba84966c5dc98151c461de4`.
- The target premise (named by the cross-family review, `f76139e5…` F1(b)): `Loc_M ∘ eta_f = j_f^C ∘ Loc_N` on rank-preserving arrows, and its (F1) companion — the step carrying the J2 chain from the Q-408 kernel side onto the Hodge carrier. The projector cert (`0bef9a00…`) proves d/delta naturality only; Loc is a DIFFERENT map — its naturality must be proven from Loc's own sealed construction (the finite symbol/localization factor).
- Custody: builder builds; Dario reviews. Tag PROVABLE / PART-PROVABLE / YOURS. The display standard applies: every intermediate equality written out.

TASK:
X1. **Prove or refute** `Loc_M ∘ eta_f = j_f^C ∘ Loc_N` on rank-preserving arrows from the sealed definitions of Loc, eta_f, and j_f^C — displayed on the reciprocal-loop generator and one nontrivial refinement arrow, then the general statement with displayed induction. If it holds only on a sub-scope: the exact boundary. If refuted: the witness.
X2. **The (F1) companion** likewise.
X3. **The consequence:** with X1–X2, the J2 display's premise status — is the rank-preserving J2 chain now premise-complete (per the cross-family standard), or what exactly remains?
X4. **Battery:** F_PLDEC (Loc's construction reader-free — cite); anti-tuning; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_LOC_NATURALITY_LANE3_V001.md`
with final lines: `LOC_NATURALITY = PROVEN / SUBSCOPE (+boundary) / REFUTED (+witness)`, `F1_COMPANION = PROVEN / SUBSCOPE / REFUTED`, `J2_RP_PREMISES = COMPLETE / (+remaining)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
