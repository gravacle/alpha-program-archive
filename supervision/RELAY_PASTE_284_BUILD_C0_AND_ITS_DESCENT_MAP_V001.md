PASTE 284 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2b] BUILD `C0` AND ITS DESCENT MAP `d_C0`

*** READ `alpha_supervision/LOCKED_PROCESS.md`. THE FOUR STEPS THERE ARE REVIEWER STEPS — YOU SEAL,
MIRROR, REPORT AND STOP. TYPING PER DoR 006: TYPE-R refuted . TYPE-U unbuilt . TYPE-S scope-empty .
TYPE-C constraint-blocked checks only . TYPE-P premise-conditional . NO_VERDICT legal. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-200)

```text
DOES THE OBJECT EXIST?   C0 is SPECIFIED, not built: "narrow carrier/algebra extension"
                         (STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md:653-656,
                         836) and "C0 EXCLUDES state, dynamics, quotient, measure, effects,
                         contacts, Ward identities, and inverse results" (:765-767).
IS THE VERSION CURRENT?  YES -- the substitute adjudication (3cd4b080, Q-194) is the newest word.
ARE ITS INPUTS PRESENT?  PARTLY -- that is the build.
```

## 1. WHAT THIS IS

**The first component of the front-line package.** Q-194 ruled a `B0` substitute admissible: `C0`
needs *"a certified carrier/algebra/representation/domain/source-map presentation and a provenance
witness"* — NOT a microscopic ontology — with

```text
d_C0 : B0_candidate -> C0
```

**Build order is settled:** DoR 005 — the package assembles first, P7 solves on it after. **The build
is acyclic (Kahn + Tarjan).**

## 2. THE TASK

**2.1 ENUMERATE C0's REQUIRED COMPONENTS FROM SEALED TEXT.** Carrier . algebra . representation .
common domain . embeddings . source maps. Paths and lines.

**2.2 FOR EACH COMPONENT: WHICH DERIVED OBJECT SUPPLIES IT?** *** THE CANDIDATE SUPPLY IS THE DERIVED
RECORD STRUCTURE — Gates 1–4's incidence carrier and algebra, the U(1) comparison group, the record-cell
structure, v004's declared source-record-field Hilbert frame. **MATCH COMPONENT TO SUPPLIER, OR TYPE
THE GAP.** ***

**2.3 CONSTRUCT THE PRESENTATION AND `d_C0`.** One frozen construction trace. A provenance witness.
The certificate. *** THE Q-200 LESSON IS BINDING: **INSTANTIATED OBJECTS, NOT SCHEMAS.** The P5
exclusion attempt died because the census held open schemas with no generator — a C0 presented as a
schema is the same failure. ***

**2.4 CHECK THE EXCLUSION LIST.** `:765-767` — if your C0 smuggles state, dynamics, quotient,
measure, effects, contacts, Ward identities, or inverse results, IT IS NOT C0. Say what was kept out.

**2.5 NO POST-OUTPUT SUPPLEMENTATION.** If a required datum is missing at freeze time, the build
REPORTS it; it does not patch after.

## 3. LEADS

```text
IF C0 ASSEMBLES FROM DERIVED OBJECTS -- *** LEAD WITH THE PRESENTATION. The package's first
component would exist, from record structure, with provenance. THEN ATTACK IT: type every
component's standing sealed / derived / adopted / imported. ***
IF A COMPONENT NEEDS AN ADOPTION -- name the adoption and its price. Do not pay it silently.
IF THE CARRIER IS AMBIGUOUS -- *** four symbol-identity defects were found at this target in one
day. If two objects answer to a component's name, REPORT THE COLLISION; do not pick. ***
IF IT CANNOT BE BUILT -- TYPE-U with the exact missing input. Complete under Q-92.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
