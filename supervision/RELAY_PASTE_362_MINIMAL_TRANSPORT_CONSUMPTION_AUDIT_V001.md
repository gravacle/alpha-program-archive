PASTE 362 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE MINIMAL-CONSUMPTION AUDIT — DOES THE p_ch VERDICT ACTUALLY NEED THE FOUR MISSING FIELDS? (THE Q-270 MOVE)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-278)

```text
DOES THE OBJECT EXIST?   THE QUESTION does. Q-278: the law-side SUBPACKAGE built (cylinder
                         amplitudes, orientation, glue, bounded domains, common origin);
                         the four residues (measure with marginal consistency, interacting
                         i-epsilon, joint boundary package, unbounded generators) need
                         structure NEITHER kinematics nor the law contains. BEFORE any
                         authoring: does the p_ch cancellation/survival verdict CONSUME
                         them? The precedent is relay 270 -- the dependency audit that
                         converted four failed builds into a moot road. Run it here.
IS THE VERSION CURRENT?  YES through Q-278. THE BUILT MATERIAL: P2 V002, P4 (anchored log
                         germ, W-convention), the law-side subpackage, the finite Keldysh
                         chain (Q-243), the p_ch entry map (d_state -> p_A -> Z_inc ->
                         derivatives, Q-267), P7's conditional theorems (Q-257), the
                         source-level tail exclusion (Q-273).
ARE ITS INPUTS PRESENT?  YES -- this is a dependency audit over existing artifacts.
```

## 1. THE AUDIT — STEP BY STEP THROUGH THE VERDICT'S OWN CHAIN

```text
1  WRITE OUT the p_ch verdict's minimal computation as a chain of named steps, from the
   built material to the verdict statement: (i) the physical W (P4's -i hbar Log Z on the
   anchored branch); (ii) its second/mixed derivatives (P2 V002 calculus); (iii) the
   retarded extraction (Q-243's rotation, now with P4's convention); (iv) the evaluation
   point / background question (Q-252's finding: no finite stationary point; C1 not an
   evaluation rule); (v) the response quotient Delta_Phi's consumption (Q-251: unspecified
   -- but WHAT SPECIFICALLY does the p_ch TRACE need from it: the full functional, or only
   its p-dependence structure?); (vi) the verdict: p present or absent in the result.
2  FOR EACH STEP: which of the four missing fields does it CONSUME, on the built
   subpackage? Verdicts per step: RUNS-ON-BUILT (exhibit it) / NEEDS FIELD <k> ESSENTIALLY
   (name the exact use -- an integration? an i-epsilon limit? an unbounded generator?) /
   NEEDS FIELD <k> ONLY AS DOMAIN FORMALITY (the step runs on the dense core / bounded
   part -- prove the reduction).
3  *** THE SHARPEST SUB-QUESTION: the p_ch trace is a STRUCTURAL statement (does the
   symbol p appear?), not an evaluation. Symbolic p-tracking may commute with limits the
   program cannot yet TAKE: if every step's p-dependence is determined on the dense core
   (where everything is built and exact), and the four fields only govern the LIMITS'
   EXISTENCE -- then the VERDICT is decidable on the core even though the physical
   response's VALUE is not. PROVE OR REFUTE THIS REDUCTION. It is the difference between
   "alpha's input count is decidable now" and "blocked on the continuum-dynamics layer." ***
4  IF THE REDUCTION HOLDS: state exactly what remains to EXECUTE the verdict on the core
   (which computations, all finite/bounded -- the 361 reference among them?) and what the
   four fields then gate (the VALUE chain: K_*, kappa, alpha -- Task 5/6 territory).
5  KILL-PASSES: no weakening a step to make it run-on-built (each reduction proven); no
   conflating verdict-decidability with value-computability -- keep them apart EXPLICITLY;
   the Q-270 discipline (check what each step's own machinery uses, not what it mentions).
DELIVERABLE: the step table with per-step verdicts, the reduction theorem proved or
refuted, and the resulting split: VERDICT-CHAIN (what it needs, when it can run) vs
VALUE-CHAIN (what the four fields gate). Typed throughout.
```

## 2. LEADS

```text
IF THE REDUCTION HOLDS -- *** LEAD WITH IT: THE DECIDING QUESTION IS DECIDABLE ON THE
BUILT CORE. State the exact remaining executable steps. The four fields move to the
value chain and authoring them stops being urgent. ***
IF A STEP NEEDS A FIELD ESSENTIALLY -- lead with the step and the exact use; that field
becomes the program's next commissioning target with its purpose nailed down.
IF THE EVALUATION-POINT QUESTION (iv) BLOCKS EVERYTHING -- say so; Q-252's untyped
evaluation surface would then be the true residue, not the four fields.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
