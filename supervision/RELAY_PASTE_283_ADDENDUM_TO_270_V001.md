PASTE 283 — CODEX LANE 1 — [TASK 1b] ADDENDUM TO PASTE 270, EFFECTIVE IMMEDIATELY

Three updates land after 270 was written. Apply them to the audit in progress:

1. VERSION CURRENCY. v002's `Gamma_K = (K/4) integral sqrt(|g|) F F + ...` is SUPERSEDED
   (`primitive_record_cell_selection_principle_v004.md:8`: "zero bare Maxwell stiffness"; its own flag
   `v002_microscopic_K_action_superseded = true`). *** AUDIT SMOOTHNESS OBLIGATIONS AGAINST THE LIVE
   v004 FORMULATION — `H_R[G_K] = K L_T + ...`, L_T the uniquely normalized transverse Maxwell
   kernel — NOT against v002's action-integral form. An obligation that is smooth only in the
   superseded framing is NOT a live smoothness requirement. Report both readings where they differ. ***

2. TYPING (DoR 006). TYPE-P is adopted for PREMISE-CONDITIONAL claims; TYPE-C is constraint-blocked
   CHECKS only. Use the new scheme in your artifact.

3. FENCE SCOPE (Q-198, DoR 004 precedent). The token bans in 270 §6 bar EVALUATION, not naming.
   Stating structural facts — that an obligation consumes an exterior derivative, a continuum limit,
   a spectral condition — is in scope. Do not stop at a word.

Everything else in 270 stands, including: lead with the verdict, name the subset, and do not
manufacture independence.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
