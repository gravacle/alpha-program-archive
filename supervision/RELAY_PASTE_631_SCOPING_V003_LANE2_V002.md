## PASTE 631 V002 — CODEX LANE 2 (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / BLOCKED-REPAIR] SCOPING V003: THE TWO-CLAUSE FIX + QUOTATION REPAIRS

(V002 SUPERSEDES V001. Your Step 0 stop was CORRECT on both counts — the fixes are on our side: (1) the V011 you hashed at `20a3a17d…` is the UNSEALED ROOT copy in the cleanroom root; the sealed packet member this relay means lives at the explicit path given below and hashes to `aa7c6d49…` — the registrar has re-verified this; (2) the archive workspace is not writable from your session, so this version removes the mirror step from your duties — write and seal in the CLEANROOM ONLY; the registrar mirrors and verifies. Re-run Step 0 with these corrections and proceed.)

### BOOTSTRAP (fresh session — read first)

You are Codex Lane 2 of the Gravacle alpha-derivation program: a build/correction lane. You derive and correct; you adopt nothing, register nothing, and never verify your own work (Dario reviews). Your final artifact is sealed, mirrored, reported, and then you STOP — no register, plan, tracker, git, commit, or push action. Tag every claim [PROVABLE] / [PART-PROVABLE] / [YOURS]. Over-claiming is worse than an honest stop.

Working locations (absolute paths):
- Cleanroom (write your artifact here): `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/`
- Archive workspace (mirror byte-identically here): `/Users/bgm/MB Work/alpha-program-archive/workspace/`
- Questions-settled register (read-only, cite by entry): `/Users/bgm/MB Work/alpha-program-archive/supervision/QUESTIONS_SETTLED_REGISTER_V001.md`

Sealing protocol (adjusted for this session's permissions): after writing the artifact IN THE CLEANROOM, create `<name>.md.seal.sha256` there containing `<sha256>  <name>.md` (two spaces). Do NOT attempt the archive mirror — your session cannot write there; the registrar mirrors and verifies. Report both hashes and STOP.

Search protocol M-2 (three guards + four known false-negative modes): fixed strings; whitespace normalization; scope-and-synonym reading in context; guard against regex-metacharacter, line-wrap, self-reference, and hyphenation/underscore variance. A zero hit is a bounded no-member finding, never impossibility. Cite SEALED PACKET copies and their line numbers, never the unsealed root's numbering.

STEP 0 — access checks (report any failure and STOP):
1. List the cleanroom (must be readable/writable) and the archive workspace (readable is sufficient — do not require write).
2. Read the register's head entry; confirm the head is Q-566 (live-append tolerance: a head advanced only by entries recording this relay's own inputs is verify-and-proceed).
3. Verify by shasum before reading:
   - Cleanroom `STAGE8_TASK6_A25_A27_SCOPING_LANE3_V002.md` = `02bbe362622bf560d83f222812d78726a0e650da8d9ba7fae6f1b65bd4f66094` (the base — a CONFIRMED inventory with three quotation defects)
   - `STAGE8_TASK6_A25_A27_SCOPING_REVIEW_DARIO_V001.md` = `bed94669d592c95805d3e6854f0c8dc6ee6032c043233337662e8dfc6f8510c9` (the review of record, Q-565 — its verdicts stand; implement, do not relitigate)
   - THE SEALED PACKET V011, full path `<cleanroom>/review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` = `aa7c6d4904706276514728819df20f48e8fdca0ff83f97ad5f1724c5f81f108a` (the sealed source for all three repairs; the root-level copy `20a3a17d…` is UNSEALED — never consume it)
4. Confirm the output filename below does not exist in either location; STOP if it does.

### TASK — three bounded repairs, nothing else

The inventory's judgments are CONFIRMED of record (Q-565): ten grades, both maps, the circularity argument. Your job is quotation fidelity only.

Q1. **§2.6.3 — the fix that matters.** V011:1397-1412 (sealed packet numbering) states TWO separate clauses: (i) the response must commute with pullback to a common refinement — EXACT commutation; (ii) the intensive quadratic coefficient must be invariant under each elementary refinement UP TO a boundary term whose ratio to four-volume tends to zero. V002's §2.6.3 merged them into commutation-modulo-a-boundary/contact-ideal — the weakened direction. Repair: quote both clauses verbatim at the sealed pin; re-render the would-have-to-be specification so the naturality square demands EXACT commutation in the named topology/quotient and the boundary tolerance attaches ONLY to the coefficient-invariance certificate (C27.5), never to the commutation square (C27.4). Propagate: check RA27-4/RA27-5 in the repair map for consistency with the two-clause form and adjust their wording if needed (a strengthened RA27-4 demand is the expected outcome).
Q2. **§1.4.1** — either restore the verbatim V011:1155-1160 text including the omitted custody sentence "Gate A24 must test this domain restriction", or reformat the block as an explicit paraphrase with the custody sentence quoted separately. State which you chose.
Q3. **§2.2's C_ref block** — restore a verbatim rendering (no mid-sentence start, no inlined-list-as-prose, no altered noun) or mark it as paraphrase; state which.

CARRIAGE (C-V5): V003 = V002 + a COMPLETE named finite diff — every hunk in the delta table with its authorizing item (Q1/Q2/Q3), everything else verbatim, verified by re-diff with the summary displayed. A carriage certificate that under-covers the diff is a false certificate (Q-566 just booked one; do not book another).

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_A25_A27_SCOPING_LANE2_V003.md`
with final lines: `Q1 = two_clauses_restored (+RA27-4/5 propagation)`, `Q2 = verbatim_restored / marked_paraphrase`, `Q3 = verbatim_restored / marked_paraphrase`, `CARRIAGE = complete_finite_delta (+hunk count, all accounted)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
Seal in the cleanroom, report hashes, STOP (the registrar mirrors).

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
