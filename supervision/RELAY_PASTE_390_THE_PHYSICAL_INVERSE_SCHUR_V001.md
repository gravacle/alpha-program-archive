PASTE 390 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE PHYSICAL INVERSE / SCHUR AND THE COMPLETED-DOMAIN PACKAGE — THE LAST CONSTRUCTION BEFORE THE VERDICT

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-306)

```text
DOES THE OBJECT EXIST?   NO -- and it is the chain's LAST stop (Q-306): the physical
                         inverse/Schur reduction and the completed-domain package, after
                         which the physical RetHess exists and ITS p-VERDICT COMPUTES.
                         Everything upstream now exists: G^007 = -hbar^2 q(1-q) L(x)L
                         (bounded, connected-subtracted), the ratified field layer, the
                         verified stack, the six-account ledger.
IS THE VERSION CURRENT?  YES through Q-306 (the freeze lifted; 389's cross-verification
                         is in flight in the other lane -- your build must cite Q-306's
                         artifacts, and any 389 defect will be reconciled by the
                         reviewer). Marks: TYPE-P | premises: DoR-008/009/013/
                         014-as-amended(2 + N)/015.
ARE ITS INPUTS PRESENT?  YES: G^007 with its closed form, the stationary-Schur account
                         (Q-288's ledger row), the seed (Q-281), P2 V002's calculus, the
                         Q-247 domain machinery, PathCert's GNS structure, the sealed
                         Keldysh rotation, the finite references (Q-243/Q-279).
```

## 1. THE TASK — INVERT, REDUCE, EXTRACT, VERDICT

```text
1  THE PHYSICAL INVERSE: on the lifted domain, construct the inverse structure the
   response needs -- G^007's block structure inverted where the response consumes it
   (state exactly which blocks the retarded extraction requires inverted; the closed
   form G^007 = -hbar^2 q(1-q) L(x)L is RANK-STRUCTURED -- handle its kernel/rank
   honestly: a rank-one-type tensor has no full inverse; the SCHUR reduction is the
   correct object, not a naive inverse. Build the stationary-Schur reduction per the
   ledger's account).
2  THE COMPLETED-DOMAIN PACKAGE: the unbounded closures the extraction needs, on the
   ratified layer (the Q-278 subpackage's bounded domains + the signature's structures;
   essential self-adjointness where the pure-phase + ratified structure gives it; typed
   where not).
3  *** THE PHYSICAL RETARDED HESSIAN: assemble RetHess_phys from the inverted/reduced
   structure via the sealed Keldysh extraction (P4's convention; the audited transport
   diagram is the licensed route). THEN THE VERDICT: TRACE p (the q(1-q) thread and any
   other entry) THROUGH THE ASSEMBLED RetHess_phys SYMBOLICALLY. THE QUESTION OF RECORD:
   DOES p APPEAR IN THE PHYSICAL RETARDED RESPONSE, OR DOES IT CANCEL? This is the
   deciding question's computation -- the one the program has built toward all season.
   State the verdict with its complete derivation chain and every premise cited. ***
4  RESTRICTION: the assembled RetHess_phys must restrict to the finite references
   EXACTLY (Q-243's zero p-free block; Q-279's probes-on pattern) -- the DoR-008 bite,
   executed not asserted.
5  ACCOUNTING: six-account rows for every new operation (inverse/Schur and domain
   closures have the largest kernels in the program -- their rows are the load-bearing
   ones); Tail_R status through the new operations; door flags filled.
6  KILL-PASSES: no naive inversion of rank-deficient structure; no unflagged completion;
   no selection; no evaluation (the verdict is SYMBOLIC: present/absent/form -- never a
   number); every limit names its topology.
DELIVERABLE: the inverse/Schur structure, the completed-domain package, RetHess_phys,
THE p-VERDICT with its derivation, restriction certificates, accounting. If the chain
stops short of the verdict, the exact missing object -- but the expectation after Q-306
is that it RUNS.
```

## 2. LEADS

```text
IF p CANCELS IN RetHess_phys -- *** LEAD WITH THE MECHANISM AND THE SENTENCE: alpha needs
no input beyond structure already ratified; the program's free parameter list is EMPTY at
the response level. ***
IF p SURVIVES -- *** LEAD WITH THE FORM: p enters the physical response as <the exact
expression>; combined with the forced p = r_ch/(r_0+r_ch), alpha's remaining input is the
integer pair -- state it exactly. ***
IF THE CHAIN STOPS -- the exact object, typed; nothing else.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
