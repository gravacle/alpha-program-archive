PASTE 161 — CODEX 1 — ITEM: THE SOLE ABSOLUTE-SCALE SELECTOR HAS NOT SELECTED. WHY?

2026-07-30. Snapshot; later rulings override. **REGISTER HEAD AT ISSUE: Q-58.** If a ruling lands mid-run
that bears on your item, **report it rather than finishing against a stale set.**

```text
CODEX 1 (you) = why C_R = 1 has not selected, and what would make it   [relay 161]
CODEX 2       = the CTP_PHYS_INPUT_PACKAGE                             [relay 162]
EINSTEIN      = what shape of argument could force a number            [relay 163]
```

**OFF LIMITS TO YOU:** the correlator-to-Hessian map, `CTP_PHYS_INPUT_PACKAGE`, the response-extraction
layer, `Gamma_K` (Codex 2); the overdetermination-shape question (Einstein). **You may read and cite
Einstein's unit-value audit — you must, it is the source of this item.**

Fences: never touch `a32_holdout/custodian_private/`. **Do not compute** alpha, kappa_record,
kappa_Thomson, a coupling, a radius, a scale, a root, an eigenvalue, or a beta function. **Do not evaluate
`E_R`, `T_R`, or any absolute interval.** *** AND MAKE NO COMPARISON TO ANY MEASURED CONSTANT. ***

**Q-52:** you may specify a missing object in order to test it — declared, `derived = false`, never
reported as derived. **Q-54:** every negative typed **TYPE-R / TYPE-U / TYPE-S / TYPE-C**; TYPE-R is the
only type that is physical content; a TYPE-S negative without its scope is void; **`NO_VERDICT` is a legal
answer** when prerequisites are unbuilt or the failure condition does not apply.

---

### WHY THIS IS NOW THE SHARPEST ITEM ON THE VALUE PATH

Einstein established (Q-58) that the program has **exactly one unselected absolute scale**, `C_R = 1`, in
every branch. The record layer alone has **no** absolute scale — the sealed orbit `T_R -> lambda T_R`,
`H_R -> H_R/lambda` is broken **only** by `chi_* = C_R = 1`. `s = pi` and `xi = 1/2` are orbit-invariant.
**`E_R` has no independent fixing, and the thing that fixes it IS `C_R = 1`.**

*** AND THEN THE CAVEAT: THE SOLE SELECTOR HAS NOT YET SELECTED. ***

```text
absolute_record_interval_derived_in_declared_branch = false
```

**with a disclosed `sqrt(2)` energy-identification ambiguity at marginality.**

**So the program's entire absolute scale hangs on one condition, that condition is adopted rather than
derived, AND IT HAS NOT PRODUCED A SCALE.** Nobody has asked what is stopping it.

### THE TASK

1. **WHAT EXACTLY DOES `C_R = 1` ASSERT, AND WHAT WOULD "SELECTING" MEAN?** At file:line. `C_R = 1` is
   marginal public closure — a **marginal self-gravitation condition**. **State what quantity it would fix
   if it selected, and in what units.** Distinguish clearly: does it fail to select because it is
   underdetermined, because an input is missing, or because a step is unbuilt?

2. *** WHAT IS THE `sqrt(2)` ENERGY-IDENTIFICATION AMBIGUITY? *** At file:line, from sealed text. **This is
   the crux.** What two identifications are available, what distinguishes them, and **is the ambiguity a
   genuine physical fork or a convention that a sealed rule already settles?** Type the answer.

3. **WHAT IS MISSING BETWEEN `C_R = 1` AND `absolute_record_interval_derived = true`?** Name the objects,
   at file:line, with `would-build` fields. **If the gap is the same response-extraction layer Codex 2 is
   working, say so — do not assume it, and do not open that layer yourself.**

4. **IS `C_R = 1` EVEN CAPABLE OF SELECTING ALONE?** Einstein showed it is the only orbit-breaking
   condition. **But being the only candidate is not the same as being sufficient.** Does it fix a scale by
   itself, or does it fix one only in combination with something else — and if so, what? **If it cannot
   select alone, that is the most important thing you could report**, because the program's one absolute
   scale would then rest on an unnamed partner.

### CONSTRAINTS

- **Do not resolve the `sqrt(2)` ambiguity by choosing.** Report both branches and what would settle it.
  **Choosing would be exactly the fitting failure this program is most exposed to.**
- **Do not derive the interval.** This is a diagnosis of what blocks it, not an attempt to unblock it.
- **Report refutations; never repair them.** If Einstein's Q-58 finding is wrong, say so.
- **F-GK3:** declare any premise beyond the current stack at the outset.

---

REPORT BACK, with **CODEX 1** on your first line: hashes, exact committed paths, gate verdict, the four
answers, **and every negative typed.** **If `C_R = 1` cannot select alone, LEAD WITH THAT.** Q-58 concluded
the program could honestly claim a conditional derivation because there is exactly one unselected absolute
scale — **that conclusion assumes the one selector can actually do the selecting.**

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
