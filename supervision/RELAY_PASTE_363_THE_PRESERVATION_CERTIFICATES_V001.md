PASTE 363 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE FOUR DEPENDENCE-PRESERVATION CERTIFICATES, AT FAMILY LEVEL (Q-280's ITEM (a))

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-280)

```text
DOES THE OBJECT EXIST?   THE SHAPE does (Q-280): for each of the four missing P3 fields
                         (measure with marginal consistency, interacting i-epsilon, joint
                         boundary package, unbounded generators), a FAMILY-LEVEL certificate
                         that ANY admissible realization NEITHER INJECTS NOR DELETES
                         p-dependence. The verdict needs the certificates, not the fields.
IS THE VERSION CURRENT?  YES through Q-280. THE STRUCTURAL FACTS THAT MAKE THIS PROVABLE:
                         p enters everything through ONE channel (d_state -> p_A -> Z_inc,
                         Q-267); every finite p-form is EXACT (Q-243, Q-279: retarded
                         blocks p-free, noise blocks carry kappa_eta and the dephasing
                         weight in closed form); Tail_src = {0} (Q-273); the admissibility
                         constraints on realizations (the battery) NEVER MENTION p.
ARE ITS INPUTS PRESENT?  YES: the built subpackage (Q-278), P2 V002, P4, the references,
                         the battery definitions.
```

## 1. THE TASK — FOUR CERTIFICATES, ONE PROOF PATTERN

```text
1  DEFINE "admissible realization" per field from the standing constraints (the battery +
   the subpackage interfaces + DoR-008 restriction discipline). VERIFY the definition
   never references p (quote the defining clauses -- this is the certificates' engine).
2  PROVE, per field, the PRESERVATION CERTIFICATE: for any admissible realization F of
   field k, and any germ output X whose p-dependence is the mapped channel's (Q-267 form),
   the p-dependence of F's action on X is EXACTLY the p-dependence X carried -- no
   injection (F's operations are p-blind by (1)), no deletion (F does not quotient or
   annihilate the p-carrying blocks -- the noise sector and the dephasing weights must
   SURVIVE F; check against Q-279's exact forms). State each certificate as a theorem
   with its proof; where a field's realizations COULD delete (e.g., a measure that
   integrates out the p-carrying blocks entirely), determine whether admissibility
   FORBIDS that (restriction reproduction should: the finite restrictions carry the
   p-forms, and DoR-008 demands their reproduction -- make that argument exact).
3  THE RESIDUAL-RISK ROW, honestly: the certificates cover field OPERATIONS. They do NOT
   cover (b) the background map or (c) the output consumption -- restate the boundary
   (Q-280's remaining items) so no one reads the certificates as more than they are.
4  KILL-PASSES: no certificate weakened to "preserves on the dense core" if the claim
   needs the full realization (say which is proved); no silent use of any unbuilt field's
   VALUE; the tail-at-response-level (Tail_R) treated as the open object it is -- state
   per certificate whether Tail_R content could evade it (the Q-250 modulo-tail structure:
   a realization's action on the tail summand is exactly where "injection" could hide;
   handle it explicitly via Q-247's classes or type the gap).
DELIVERABLE: four certificates (proved / proved-with-stated-scope / refuted-with-the-
evading-construction), the admissibility-never-mentions-p verification, the boundary
restatement. Typed throughout.
```

## 2. THE LEADS

```text
IF ALL FOUR PROVE -- *** LEAD WITH IT: item (a) of the verdict chain is DONE and the
deciding question stands on the background map and the consumption signature alone. ***
IF A FIELD'S CERTIFICATE FAILS -- lead with the evading construction: where p could be
injected or deleted is exactly what a future ratification must forbid; name the clause.
IF Tail_R EVADES A CERTIFICATE -- lead with that: the response-level tail would be the
last hiding place, stated precisely.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
