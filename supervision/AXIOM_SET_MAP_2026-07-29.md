# THE AXIOM SET MAP (2026-07-29)

Executed by Opus, chartered by the reviewer lane at the principal's direction. THE FIRST TIME
THIS PROGRAM'S AXIOM SET HAS BEEN TRACED TO ITS TERMINUSES AND COUNTED.

THE HEADLINE, AND IT IS NOT ONE OF THE TWO OUTCOMES THE CHARTER ANTICIPATED:

  TWELVE terminal axioms; NINE substantively contested. The corpus's own machine-readable
  self-count is SIX and omits five of the twelve entirely.

  ONE axiom — A6, the allow/require boundary / first-admissible / minimality / saturation
  selector — IS APPLIED NINE SEPARATE TIMES IN NINE SEPARATE FILES AND CARRIES EVERY
  DIMENSIONLESS NUMBER IN THE CONSTRUCTION. No inventory in the corpus counts it once, so its
  total weight is invisible at every point of use.

  THE VERDICT IS A THIRD THING: "The terminal set is not small, is not independently motivated,
  and does not contain the measured coupling. It contains a NUMBER-MANUFACTURING DEVICE that
  will produce a determinate coupling from any carrier you hand it. It produced one, and that
  one was refuted."

  THE DECISIVE EVIDENCE, ALREADY IN THE LEDGER: rows 516/517. This axiom stack WAS carried to a
  stiffness once. It produced a determinate number. Row 517 records the outcome as REJECTED,
  with the observed value about 57.59 times the prediction and NO RESIDUAL REPAIR AUTHORIZED.
  And the floor convention (tau = 1) appears LITERALLY in that expression as the argument of the
  coefficient — the axiom visible inside the answer.

  WHAT IT MEANS: this is evidence AGAINST the axioms secretly encoding the measured value, and
  evidence FOR A6 being an open-ended licence that will manufacture some number from anything.
  Nine instances exist; nothing in the corpus bounds the tenth.

I have what I need. Two of my four parallel searches were still outstanding when I finished; nothing below depends on them — I covered their assignments directly.

---

## Scope and exclusions

Searched: (A) the cleanroom `alpha_fundamental_record_action_cleanroom_v003` (913 files), (B) the parent tree `gravity_emergence_evidence_program` top level plus `reports/`, `scripts/`, `results/`, `provenance/`, `tests/`, (C) `/Users/bgm/MB Work/alpha_supervision`, (D) `_external_handoffs`. Excluded throughout: `.python_deps*`, `.python_runtime_deps`, `.uv-cache`, `.julia-depot`, `.codex_deps`, `.proof_deps`, `.mpl-cache`, `.cache`, `cachedir`, `.pycache*`, `extracted/`, `node_modules`, `third_party/`, `external/`, `sources/`, and the dated review-packet/zip mirrors. `/Users/bgm/Documents/New project` used throughout as the single canonical path. I computed nothing and compared nothing to a measured constant; the one comparison quoted in Part 4 is a verbatim pre-existing ledger row, not mine. The namespace register was read first and its `C_R` rows are honored: `C_R` = cleanroom compactness ratio, `C_R(x)` = parent induced Einstein coefficient, kept apart below.

---

# PART 5 — WHAT IS ALREADY WRITTEN

Seven prior inventories exist. One of them declares itself the authority. It is wrong about its own completeness, and a different one — outside the cleanroom — is the best piece of work in the set.

**The self-declared authority.** `alpha_fundamental_record_action_cleanroom_v003/STAGE8_T7_BETA_ER_A_DISCLOSED_PREMISE_ADOPTION_V001.md:52` heads a section "`## CUMULATIVE CONDITIONALITY REGISTER (the one place; keep it here)`" and at :73-75 states: *"Any artifact adding a disclosed premise MUST add it to this register in the same change. The register, not the individual artifacts, is the authority for 'what the result is conditional on'."* Its content is thirteen items (:57-70): `C1. ER-A envelope realization`, `C2. complex-vs-real selection`, then an "Inherited historical Level-1 stack" of *"imported winding k = +1; MPCP three-axis / d = 4; onset rule; transport-only; QFC; DCC; vectorlike inventory (undeclared minimality); K_bare = 0; fidelity action k = 1; PSC and CISP (sealed principles); tau_orth = T_R (cycle-6 conditionality)."* **Its content governs and mine is an extension.** What it misses is in Part 1; the short version is that it omits the SU(5)/chiral-16 carrier entirely, omits every one of the nine scale-and-number-fixing saturation adoptions except the two it names in passing, and carries one item (`fidelity action k = 1`) that a sibling artifact had already retired.

It also has a defect worth recording: four of its thirteen entries have no defining artifact in the cleanroom. `QFC`, `DCC`, and `MPCP` appear **only** in this register and in `CONTINUATION_STATE.md` under the whole of roots A and B (bounded grep, `\bQFC\b`/`\bDCC\b`/`\bMPCP\b`, all `.md`/`.json`/`.py`/`.csv`, exclusions as above). Their expansions exist only in roots B and D: `DCC` = "Durable-Record Closure Criterion" (`_external_handoffs/fable_alpha_cleanroom/OUTPUT/11_DURABILITY_CLOSURE_PRINCIPLE_V001.md:1`), `QFC` = "Global Boundary Descent and Quasi-Free Completeness" (`.../OUTPUT/14_DCC_QFC_ADJUDICATION_PROTOCOL_V001.md:15`), `MPCP` = Minimal Public Carrier Principle (`gravity_emergence_evidence_program/minimal_public_carrier_principle_v001.md`). `complex-vs-real selection` has **no defining artifact anywhere in roots A–D** — it is named in five cleanroom files and nowhere stated. An authoritative register with four unresolvable entries is not usable as an axiom list.

**The corpus's own machine-readable count is six.** `STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.json:13-20`:

```
  "adopted_microscopic_principles": {
    "boundary_superconnection_single_operator_completeness": "ADOPTED_BRANCH_PREMISE",
    "causal_incidence_support": "ADOPTED_LEVEL_1",
    "fundamental_boundary_record_action": "ADOPTED_LEVEL_1",
    "global_boundary_descent_quasifree_completeness": "ADOPTED_BRANCH_PREMISE",
    "parent_state_covariance": "ADOPTED_LEVEL_1",
    "zero_independent_bare_F2_compositeness": "ADOPTED_COMPOSITENESS_CONDITION"
  },
```

matching the prose "`## Adopted premise classes`" at `STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md:29-37`. Six. That is the number the program would give if asked. It omits transport-only, the whole minimality apparatus, and the entire internal-carrier assignment.

**Three further cleanroom inventories, all partial and mutually inconsistent.** `LEVEL1_MICROSCOPIC_ACTION_PREMISE_LEDGER_V001.json` lists three premises at `"status": "ADOPTED_LEVEL_1"`. `CURRENT_AUTHORITY_LEDGER_V010.json` `current_level_1_postulates` lists four (adding transport-only and the paired-return identification); V011–V013 add two more under `new_level_1_postulates` (`relative_record_onset_saturation`, `zero_flux_no_charged_write`). `STAGE1_PREMISE_DISPOSITION_V001.md:112-118` classifies four items and reaches the opposite conclusion to the conditionality register on one of them: *"unit fidelity multiplicity: RETIRED from the physical alpha path"* with `information_multiplicity_enters_Q_spec = false` (:93-94) — yet `fidelity action k = 1` still stands in the register as a live inherited premise. Three, four, six, six, thirteen. No two agree.

**The best prior work is in root D and is uncited by the cleanroom.** `_external_handoffs/fable_alpha_cleanroom/OUTPUT/01_PREMISE_LEDGER.md` is a full independent classification of the eight `AUDIT_ONLY_ADOPTED_INPUTS` files under a declared "least-favorable rule", with totals at :21-22: *"4 ADOPTED_PRINCIPLE, 3 DERIVED (conditional; all arithmetic independently re-verified), 1 DISCLOSED_PHYSICAL_INPUT."* It then does what none of the cleanroom inventories do — names four hidden premises (:24-52), including (a) *"**Undeclared minimality selector (alpha-critical, strongest finding).** File 7 fixes `primitive_fundamental_vectorlike_pairs = 1` and `primitive_charge_magnitude = 1` via 'The smallest vectorlike completion' (line 55), but minimality is nowhere adopted as a principle in the package... Pair count and charge multiply the induced response directly"*; (b) unit winding *"presented as derived"*, least-favorable status *"adopted/imported — load-bearing for the holonomy normalization on which alpha directly depends"*; (c) unit information multiplicity *"k=1 ... is fixed solely by the adopted weight postulate and scales any induced stiffness"*; (d) premise-set non-closure. Its exit assessment (:70-74): *"The Stage-1 exit criterion — 'one explicit ledger with no hidden premise and no target-based selector' — is **NOT met**."* A companion inventory at `.../OUTPUT/45_STAGE10_HANDOFF_GEOMETRIC_BRIEF_V001.md:27-32` gives "**The conditionality stack** (travels with every number)" in seven items. The existence of the eight-file directory literally named `AUDIT_ONLY_ADOPTED_INPUTS` is itself a pre-existing declared axiom set, and note the discrepancy: that directory and the cleanroom's Level-1 ledger do not contain the same files.

**In supervision**, `BOHM_SWEEP_2026-07-27_derived_adopted_open.md` is a derived/adopted/open sweep, and `BOHM_SLOT9_THEORY_ATTACK_2026-07-29.md:125` and `:166` already state the parameter-count diagnostic this task asks for. So Part 4 below is a re-derivation and an extension, not a discovery, and I mark it as such.

---

# PART 1 — THE ADOPTION INVENTORY

The relocation chain does terminate. It terminates in an unbounded-but-enumerable set of adoptions of which one is applied nine times. Below, each adoption with its own status flag.

**1. Record fundamentality and one complete parent.** `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:71-72` *"Durable records are generated by physical boundary degrees of freedom"*; :84-91 *"Gravity, the adopted charged comparison field, charged sources, and record closure belong to one complete quantum specification `Q_spec[g, a, psi, R]`"*. Its own logical status, :23-26: *"This is a target-value-free, target-aware, forward-sealed Level-1 Gravacle principle. It is an adopted microscopic theory premise, not a consequence of the sealed pre-alpha results."* Same class: `PARENT_STATE_COVARIANCE_PRINCIPLE_V001.md:7` *"This is an adopted Level-1 Gravacle principle"*, flag `parent_state_covariance_principle_adopted = true`; `CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md:7-8` *"This is an adopted Level-1 Gravacle microscopic-action principle"*, flag `causal_incidence_support_principle_adopted_Level_1 = true`.

**2. Adopted charged field content.** `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:47-52`: *"The ordinary charged-record branch contains a smooth principal `U(1)_rel` bundle and an auxiliary compact connection `a` on that bundle. This localization and connection are adopted Level-1 field content."* Then :62-68: *"The primitive unit character fixes the normalization of holonomy... No additional charge-normalization factor may be inserted later."* Flags: `smooth_principal_relative_U1_bundle_adopted = true`, `auxiliary_compact_connection_adopted = true`, `physical_public_EM_connection_derived = false`. V002 exists *because* V001 misreported this as derived (:5-17). Corroborated at `results/level1_record_action_stack_v002.json:17`: `"connection_status": "ADOPTED_LEVEL_1_NOT_DERIVED"`.

**3. Pre-record link independence (P1, P2).** `alpha_prerecord_independent_comparison_measure_principle_v001.md:10-13`:

```
BEGIN_FROZEN_PRERECORD_PREMISES
P1=each one-link marginal is normalized and invariant under independent left and right local-frame action
P2=distinct elementary comparison links are statistically independent before a closed durable record relates them
END_FROZEN_PRERECORD_PREMISES
```

Status, :15-19 and :28-30: *"`P2` is a foundational branch axiom, not a consequence of merely failing to observe a record... This is a first-principles branch axiom, frozen before any Step-5 numerical alpha evaluation."*

**4. K_bare = 0, the compositeness condition, as a standalone adoption.** `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:122-132`: *"At the microscopic scale and prescription ultimately fixed by `Q_spec`, the auxiliary connection has no independent bare parity-even curvature stiffness: `K_bare[Q_spec, regulator, causal-cell scale] = 0`. This is an adopted compositeness condition, not a result of compactness, projective geometry, or gauge covariance."* Flag `K_bare_zero_adopted_as_compositeness_condition = true`. Its parent-tree extension is stronger and covers **both** sectors: `alpha_induced_only_boundary_action_principle_v001.md:3-8` *"## Adopted principle — Before durable public record formation there is no independent bare metric or gauge stiffness."* Producer-verified: `scripts/audit_alpha_br_induced_only_boundary_provenance_v001.py:366` hardcodes `"zero_stiffness_boundary_condition": "ADOPTED_NOT_DERIVED"`.

**5. Generator-class restriction. Two independent artifacts, same content, both self-typed as postulate.** `PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md:5-7`: *"This is a new Level-1 Gravacle principle, adopted before any finite response or coupling is evaluated. It is not claimed as a theorem of the older recoverability results."* Excluded list at :47-54 (`c_F integral F^2` a bare Maxwell action; a primitive Pauli insertion; higher-covariant-derivative charged vertices; independent Lindblad rates; an independently normalized record-curvature term). Its own concession, :64-65: *"Gauge and Lorentz covariance alone do not imply this rule."* Flags: `transport_only_phase_complete_principle_adopted = true`, `principle_derived_from_older_record_theorems = false`. Independently, `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:2257-2261`:

```
single_operator_completeness_postulate_declared = true
independent_primitive_F2_axiomatically_disallowed = true
independent_primitive_Pauli_axiomatically_disallowed = true
primitive_F2_theorem_excluded = false
primitive_Pauli_theorem_excluded = false
```

and at :1086-1087 the same file's own guard: *"independent `F_2` coefficients are excluded by the BID postulate, not proved absent by restating that postulate."*

**6. THE ALLOW/REQUIRE BOUNDARY — one axiom, nine applications. This is the item every prior inventory understates.** The general form is stated once, at `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V001.md:52-58`: *"The allow/require boundary condition is strengthened as a new microscopic postulate: when a primitive durable record forms, the boundary law selects one physical Lorentz-covariant interval, support region, and closure configuration over which its action is accumulated."* Every concrete number in the construction is an instance of it. In each case the corpus derives an **inequality or a family** and then adopts the **equality or the least member**:

- *d = 4.* `minimal_public_carrier_principle_v001.md:36-45`: derives `d - 1 >= 3, d >= 4`, then *"The Minimal Public Carrier Principle selects the lower bound: `d_primitive = 4`."* Heading is `## Adopted principle`.
- *The absolute record cell scale.* `BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md` input 3 derives `C_R = 2 G E_R / (c^4 R_R) <= 1`; input 4 (:46-57) is *"**Marginal public-closure rule (adopted Level-1 Gravacle rule):** The allow/require boundary is the first physically admissible public record cell... `C_R = 1`."* Then, decisively, :59-61: *"This fourth input is the selector. Without it, public recoverability gives a half-line of allowed durations and no absolute record scale."* Its own deformation-control block (:112-127) exposes the three normalizations as `s = pi, xi = 1/2, chi_* = 1` and states *"Changing the action marker, the causal support, or the marginal selector changes the absolute scale."* Flags: `marginal_public_closure_rule_adopted_Level_1 = true`, `strict_untrapped_inequality_alone_selects_unique_scale = false`, `absolute_record_interval_derived_in_declared_branch = false`.
- *The orthogonalization budget.* `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:99-108`: bound `>= pi hbar/2` derived, then *"The allow/require boundary is adopted to select first admissible record onset... `J_FS,rel = pi hbar/2`. The lower bound is derived from the imported theorem. **Saturation is adopted, not derived.**"* Flags `relative_onset_saturation_derived = false`, `relative_onset_saturation_adopted_Level_1 = true`.
- *The first-record spectral scale.* `alpha_first_durable_record_capacity_principle_v001.md:5-22`, heading `## Adopted Rule`: `N_BR(k) = 0` for `k < k_R`, `N_BR(k_R) = 1`. No status flag block at all. Its own §Selection Requirement (:46-50): *"If a continuous family of radii, spin sectors, bundle classes, or finite depths satisfies the same capacity condition, the numerical alpha calculation remains blocked."*
- *The proper-time floor.* `primitive_boundary_native_record_CAR_functional_principle_v001.md:32-41`: *"This minimal matching branch adopts the convention that the first admitted boundary interval begins at `tau=1`. A different positive floor multiplier remains mathematically possible. The choice `tau=1` is a frozen target-independent branch convention, not a theorem forced by the existence of one dimensionful scale."* The same choice, stated as a prohibition rather than a derivation, at `alpha_boundary_spectral_semigroup_principle_v001.md:38-40`: *"An additional multiplier of `C2_parent,hat` would be a second physical scale and is forbidden unless a new boundary principle derives it."*
- *Balanced calibration.* `PRIMITIVE_BINARY_CLOSURE_GENERATOR_GATE_V002.md:50-57`: *"an exact autocorrelation zero exists only for the separately chosen balanced calibration `p=1/2`"*, flag `balanced_calibration_derived = false`.
- *One vectorlike pair, unit charge.* `PRIMITIVE_VECTORLIKE_CHARGED_SOURCE_BRANCH_V003.md:54` *"The smallest vectorlike completion then obeys..."*, frozen inventory at :66-75, with :77 conceding *"Additional vectorlike pairs are consistent and are not proved impossible."* Independently flagged as the strongest hidden premise by `OUTPUT/01_PREMISE_LEDGER.md:26-33`.
- *Spin 1/2.* `primitive_durable_source_orientation_principle_v001.md:12-14`: the `2 pi` sign *"does not by itself distinguish spin `1/2` from higher half-integer irreducible representations. **This principle additionally adopts primitive minimality.**"*
- *The source-mass depth ratio.* `primitive_boundary_native_record_CAR_functional_principle_v001.md:54-55`: *"For the reviewed one unit-charge Dirac field with `m_D/k_R=1`..."*

**7. The parent internal carrier.** `alpha_presplit_parent_connection_principle_v001.md:10-12`: *"This principle is adopted as a Gravacle theory axiom. It is not inferred from the measured fine-structure constant, gauge-coupling unification data, the source-free endpoint, or a residual against any of them."* Content: `K_parent = C^5`, `G_parent = SU(5)`, and the derived-from-it relations `alpha_EM = (3/8) alpha_5`, `sin^2(theta_W) = 3/8` (:75-84). The matter carrier is separately adopted: `alpha_boundary_spectral_semigroup_principle_v001.md:9-13` (`## Adopted rule`) *"the source carrier is its chiral half `E_parent=Lambda^even(C5)=16`"*; `reports/alpha_br_determinant_line_anomaly_v001.md:5` *"The explicit carrier is the adopted chiral `16 = Lambda^even(C^5)`"*. This is what the producer actually uses: `reports/alpha_br_common_induced_coefficients_v001.md:5-9` *"The coefficients are generated from the exact `Lambda^even(C5)` chiral-16 inventory"*, with grouping `C2=0:dim=1`, `C2=2.4:dim=5`, `C2=3.6:dim=10` and `I_n(c)=integral_1^infinity tau^(-n) exp(-c tau) d tau`.

**7a. The su(5)-invariance premise the Schur ratio protection needs is *not* an adoption — it is an unmet antecedent.** `STAGE8_SCHUR_RATIO_PROTECTION_SCOPE_ATTACK_V001.md:129-137`: *"IF every admissible additive deformation and every response-changing mutation that reaches the Cartan Hessian is constrained to a full su(5)-invariant symmetric 2-tensor... THEN the Cartan ratios are protected up to one common scalar. The corpus does not establish that antecedent."* Flags: `full_su5_invariance_of_all_admissible_deformations_established = false`, `verdict = CONDITIONAL_ON_FULL_SU5_INVARIANT_DEFORMATION_CLASS_AND_TRANSPORT`.

**8. Sector independence and multiplicative composition.** `primitive_boundary_native_record_CAR_functional_principle_v001.md:20-22`: *"This principle **declares** these to be independent integration variables. Their logarithms therefore add inside this branch; the independence is not derived from the older closure action."* Cognates: `global_boundary_descent_quasifree_completeness: ADOPTED_BRANCH_PREMISE` (STAGE6 JSON:17); `BID_CONSOLIDATED_HOSTILE_AUDIT_MATRIX_V005.md:74` A24 *"The record theory must derive or explicitly adopt a strong symmetric-monoidal functor into `(Hilb,tensor)` before tensor composition is used"* (status `PENDING`, adopt arm taken); `BID_SOURCE_PARENT_SUBORDINATE_OUTPUT_CONTRACT_V001.json:393` `"degree_one_primitive_filter": "ADOPTED_NOT_DERIVED"`.

**9. ER-A envelope realization.** `STAGE8_T7_BETA_ER_A_DISCLOSED_PREMISE_ADOPTION_V001.md:19-27`: *"ER-A is carried as a DISCLOSED PREMISE: the intrinsic-action envelope realization used throughout is ER-A (write envelope tau_R w(t) M(t), w(t) = 32 r(t)^3), NOT derived and NOT selected — assumed, and disclosed as assumed. ER-B (tau_R rho M(t), rho = 24/pi) remains UNEXCLUDED but BATTERY-UNDEFINED."* Load-bearing by its own admission (:46-49): *"An alternative admissible envelope (ER-B) exists, is not excluded, and would in general give a different value: the record is provably sensitive to the envelope's cumulative profile."* Flags `envelope_realization_derived = false`, `ER_B_excluded = false`.

**10. The disclosed ordinary branch.** `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:99-101`: *"The ordinary public branch is `3+1` dimensional, Lorentzian, diffeomorphism-covariant, CPT-compatible, and local at the primitive causal cell before coarse graining. These are disclosed inputs, not outputs."* `STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.json:201`: `"record_axioms": "DISCLOSED_KINEMATIC_INPUT"`.

**11. Complex-vs-real selection.** `STAGE8_T7_BETA_ER_A_DISCLOSED_PREMISE_ADOPTION_V001.md:58-59`: *"C2. complex-vs-real selection (standing; assumed, not derived — the program's long-recorded recurring gap)."* Bounded negative: no artifact stating this premise exists anywhere in roots A–D (greps `complex-vs-real`, `complex vs real`, `complex versus real`, all suffixes, exclusions as above; five hits, all of them the register or files pointing back at the register).

**12. The C-L3 nonzero typing.** `STAGE8_T7_CL3_NONZERO_TYPING_PRINCIPAL_DECISION_V001.md:102` *"A2 THE NONZERO-AS-FAILURE TYPING IS ADOPTED / SPEC-TYPED, *** NOT DERIVED. ***"*; retyped by principal act with `CL3_typing_prior_status = ADOPTED_NOT_DERIVED`, `CL3_ruling_made_blind = true`, `both_readings_predict_same_form = true`, `no_value_could_discriminate = true`.

**One item the prior sweeps get wrong and I correct.** `kappa_record = kappa_Thomson` is *not* an adoption. `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:1644-1650` says the equality *"is not assumed. It must follow from the complete amplitude or fail"*, and :2174 lists *"the equality kappa_record=kappa_Thomson is assumed rather than derived"* as a **failure condition**. `STAGE8_FOUR_CLEANUP_WRITES_DISPOSITION_V001.md:106` records the current status as `NOT_ASSUMED (machine-enforced) AND NOT_DERIVED (open)`, superseding an earlier `ASSUMED_NOT_DERIVED` flag. Any lane that assumes it fires a declared failure condition; it is a live defect, not an axiom.

---

# PART 2 — DERIVABILITY, ONE LEVEL AT A TIME

**K_bare = 0 has two lineages with two different producer-verified statuses.** Lineage 1 terminates immediately: `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:130` says outright it is *"an adopted compositeness condition, not a result of compactness, projective geometry, or gauge covariance"* — **ADOPTED WITH NO REASON GIVEN**, and the producer flag reads `ADOPTED_NOT_DERIVED`. Lineage 2 is a genuine one-step derivation: `alpha_prerecord_independent_comparison_measure_principle_v001.md:36-53` derives `beta_0 = 0` from P1+P2 (*"The conclusion follows from pre-record link independence, not from gauge symmetry alone; gauge symmetry by itself permits the term"*), and the corresponding producer flag is different — `scripts/audit_alpha_br_full124_structure_payload_provenance_v001.py:1105-1106`, `"induced_only_zero_stiffness_boundary_condition": "DERIVED_CONDITIONAL_ON_FROZEN_PRERECORD_P1_P2_AXIOMS_FINITE_LINK_CONTINUUM_OPEN"`. Terminus of lineage 2 is **P2**, adopted **with a stated reason and a stated falsifier** (:71-77) and with an explicit refusal to overclaim (:79-80: *"It is not a theorem that all gauge theories must have zero bare stiffness"*). Lineage 2 does not yet reach the coupling: the corpus's own note is that P1+P2 *"conditionally remove an independent bare plaquette stiffness, but the induced effective Maxwell stiffness remains unidentified."* Net: K_bare = 0 collapses into P2 **if and only if** the finite-link continuum limit closes; today it is a separate terminus.

**Transport-only is ADOPTED WITH A STATED REASON.** `PRIMITIVE_TRANSPORT_ONLY_...V001.md:62-75`: *"Gauge and Lorentz covariance alone do not imply this rule. The rule chooses a physical hierarchy: primitive charged access = transport; closed-loop curvature and kinetic response = composite output... Calling the choice 'minimal' would not have been enough; the transport-only content is the declared microscopic physics."* Chain terminates there: `principle_derived_from_older_record_theorems = false`. Its consumer relationship to the underdetermination gate is exactly as this task's brief states — `COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md:95-98` records `current_premises_admit_two_response_inequivalent_parent_actions = true` and `finite_c_F2_deformation_excluded = false`, and transport-only removes those countermodels *"only inside this adopted branch"* (:72-75). Verified at producer: `scripts/audit_current_authority_v010.py:63-64` hardcodes `"finite_c_F2_deformation_excluded_as_universal_theorem": False` and `"finite_c_F2_deformation_excluded_inside_adopted_primitive_branch": True` as literals in an `EXPECTED_EXECUTION_STATE` dict against which the ledger JSON is compared — so the load-bearing flag is an assertion the audit cannot fail on, exactly as the trap warning says. The specific flags named in this task's brief (`exhibited_parameter_free_parent_action_completions = 2`, `larger_equivalent_mutation_class_bounded = false`) are **not corpus flags** — bounded grep over roots A–D finds them only at `CONTINUATION_STATE.md:2616-2618`; they are the reviewer's own summary of the corpus, correctly derived from it but not carried by it.

**SU(5) is ADOPTED WITH NO REASON GIVEN, and its terminus is out of tree.** `alpha_presplit_parent_connection_principle_v001.md:10` states the adoption flatly. The provenance is stated in the Lie-closure report and quoted at `STAGE8_SCHUR_RATIO_PROTECTION_SCOPE_ATTACK_V001.md:65-72`: *"This is an alpha-blind algebraic result conditional on a structurally target-aware C5, 3+2, and off-block-mixing setup. It does not derive that a physical pre-split off-block mixer exists, nor independently remove the extra central U(1). **The pre-alpha ledger explicitly called the parent an ansatz, and the later alpha work adopted it as an axiom. Therefore the SU(5) parent is not yet independently provenanced.**"* The chain ends at a pre-alpha ledger row typed as an ansatz — i.e. outside the alpha corpus entirely. The chiral-16 carrier hangs off the same adoption and is separately typed *"the adopted chiral 16"*.

**The nine minimality instances each terminate in themselves, one level down, at the same general postulate.** Every one of them is one step from a derived inequality/family to an adopted equality/least-member, and each states its own reason in the same words: *first admissible*, *least positive*, *smallest*, *minimality*, *saturation*, *marginal*. Two of them name the general rule explicitly (`BID_MINIMAL_PUBLIC_CAUSAL_CELL:51`, `BOUNDARY_RECORD_ONSET_SATURATION_V003:99`), and the general rule is stated as a postulate at `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V001.md:52`. **So the chain is: nine numbers ← nine local saturation adoptions ← one allow/require-boundary postulate ← nothing.** The corpus's own verb calibration makes the classification unavoidable: `STAGE8_T7_Q2_TRIGGER_AND_CLASSIFICATION_STANDARD_V001.md:64` — *"DERIVED means the principles REQUIRE it; anything they merely ALLOW is a premise, however well-motivated."* An inequality allows; it does not require.

**ER-A: ADOPTED WITH A STATED REASON that is explicitly a non-reason** — the reason given is that the kill-test *"blocked on CONTROL DESIGN, not on physics"* (:12-13), i.e. the discharge is procedural, not evidential.

**PSC and CISP: ADOPTED WITH NO REASON GIVEN** beyond framing; both self-flag `historically_target_blind = false`.

**Complex-vs-real: cannot be assessed** — the premise has no statement anywhere. This is a defect, not a finding about the premise.

---

# PART 3 — THE TERMINAL SET

Deduplicated, the terminuses are **twelve**:

| # | Terminal axiom | Status in its own text | Reason given? |
|---|---|---|---|
| A1 | Record fundamentality / one complete parent (FBRAP core + PSC + CISP + DCC) | ADOPTED_LEVEL_1 ×3 | No |
| A2 | Adopted charged field content: principal `U(1)_rel` bundle + compact connection + holonomy normalization | `..._adopted = true`, `physical_public_EM_connection_derived = false` | No |
| A3 | Pre-record link independence (P1, P2) | "a foundational branch axiom" | **Yes, with falsifier** |
| A4 | Zero bare stiffness as a standalone condition (both sectors in the parent tree) | "an adopted compositeness condition, not a result"; producer: `ADOPTED_NOT_DERIVED` | No |
| A5 | Generator-class restriction (transport-only ≡ single-operator completeness) | `..._adopted = true`; `primitive_F2_theorem_excluded = false` | **Yes** |
| A6 | **The allow/require boundary: first-admissible / minimality / saturation selector — 9 applications** | "adopted, not derived"; "the selector"; "a convention, not a theorem" | Restates itself |
| A7 | `K_parent = C^5` / `SU(5)` / chiral-16 `Λ^even(C^5)` | "adopted as a Gravacle theory axiom"; upstream: `ANSATZ` | No |
| A8 | Sector independence / multiplicative composition (declared independent variables; QFC; A24 adopt-arm) | "**declares**... the independence is not derived"; `ADOPTED_NOT_DERIVED` | No |
| A9 | ER-A envelope realization | "NOT derived and NOT selected — assumed" | Procedural only |
| A10 | Disclosed ordinary branch (3+1, Lorentzian, CPT, spin-statistics, Dirac/CAR, locality) | "disclosed inputs, not outputs" | N/A — conceded |
| A11 | Complex-vs-real selection | "assumed, not derived" | **No statement exists** |
| A12 | C-L3 nonzero typing | `ADOPTED_NOT_DERIVED`, retyped blind | **Yes** |

**Twelve.** Of these: A10 is a branch disclosure any reader would grant; A11 cannot be stated; A12 is a typing convention its own flags show carries no numerical content (`no_value_could_discriminate = true`). That leaves **nine substantive contested adoptions**. Three of the nine (A3, A5, A12) are adopted with an actual stated reason. **Four carry numbers: A6, A7, A9, and A2 (partly).** A6 alone carries all nine dimensionless numbers.

Compare: the corpus's own machine-readable count is six, and it omits A6, A7, A9, A11, A12 in their entirety.

---

# PART 4 — THE DIAGNOSTIC

**Does a terminal axiom carry the answer? Yes — A6 does, precisely and by design.** Every dimensionless number the construction contains is manufactured by the same device, and the corpus says so in its own words at the two places where the device does the most work. `BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:59-61`: *"This fourth input is **the selector**. Without it, public recoverability gives a half-line of allowed durations and no absolute record scale."* And `primitive_boundary_native_record_CAR_functional_principle_v001.md:39-41`: *"A different positive floor multiplier remains mathematically possible. The choice `tau=1` is a frozen target-independent branch convention, **not a theorem** forced by the existence of one dimensionful scale."*

The parameter accounting reproduces cleanly and I confirm it independently. With induced-only adopted (A4) the induced coefficients are functions of two unknowns — `reports/alpha_br_common_induced_coefficients_v001.md:9-22` shows `C_V(x)`, `C_R(x)`, `K_Q(x)` all built from `I_n(c) = integral_1^infinity tau^(-n) exp(-c tau) d tau` with `x = r^2/k_R^2`. Two: `x` and `k_R`. The floor's lower limit of exactly `1` is A6 instance (v); `k_R` is fixed by A6 instance (iv) (`N_BR(k_R) = 1`); the multiplicities `(1, 5, 10)` and the Casimir offsets are A7; the sectors then supply more coefficients than two. So the postulates supply exactly the missing constraints. The reviewer's judgement at `BOHM_SLOT9_THEORY_ATTACK_2026-07-29.md:125` is correct and I do not soften it: *"The induced-only postulate is doing precisely as much work as the number of coefficients the program wants to explain, no more and no less. It is not a convenience. It is the load-bearing input."* And at :166: *"Predicting a dimensionless number from a postulate that contains exactly one free dimensionless number per predicted coefficient is a reparametrization, not a prediction."*

**But the naive form of verdict (b) is false, and the fact that refutes it is already in the corpus.** The one time this axiom stack was carried all the way to a stiffness, it produced a determinate number, and that number was wrong. `alpha_strict_route_decision_ledger_v001.csv` row 516 records the branch as `CONDITIONAL` with the note *"the new target-independent branch explicitly adopts independent bosonic record and Grassmann source variables a unit dimensionless proper-time floor zero separate finite F2 matching and external A at first matching; hostile review verifies K=3/16+E1(1)/(12pi^2) ... while requiring all four choices remain labeled adopted rather than forced"* — four adoptions, named as such, assembling into one number. Row 517 then records the outcome: `primitive_boundary_native_alpha_postfreeze_comparison_v001, REJECTED, standalone_K_native_equals_K_Thomson_identification_fails_CODATA_holdout`, *"the observed value about 57.59 times the prediction and decisively rejects the standalone physical Thomson identification... no residual repair is authorized."* (Quoted verbatim from the pre-existing ledger; I performed no comparison.) The `E1(1)` in that expression is the floor convention showing up literally as the argument of the coefficient — the axiom in the answer, in plain sight — and the answer was off by a factor of order fifty.

So the honest verdict is neither (a) nor (b) as posed. It is a third thing, and it is worse than (a) and more interesting than (b):

**The terminal set is not small, is not independently motivated, and does not contain the measured coupling. It contains a number-manufacturing device that will produce a determinate coupling from any carrier you hand it. It produced one, and that one was refuted.**

The three parts of that, each load-bearing:

*Not small.* Twelve terminuses, nine contested, against a corpus self-count of six. The device A6 is adopted nine separate times in nine separate files, so its total weight is invisible at every point of use. No inventory in the corpus counts it once.

*Not independently motivated.* Six of the nine contested adoptions give no reason at all. A7 — which supplies the multiplicities and the relative gauge normalizations `alpha_EM = (3/8) alpha_5`, `sin^2(theta_W) = 3/8` — terminates in a pre-alpha row typed `ANSATZ`, and adopts as an axiom a group and a representation assignment that are the known content of a known theory. Every principle file in the stack self-flags `historically_target_blind = false`. The one axiom that *is* independently motivated with a stated falsifier is A3, and its continuum limit is open.

*Does not currently contain the answer.* No artifact in roots A–D carries `alpha_computed = true` (the only three hits are a negative-authorization list at `primitive_same_cell_discrete_parent_action_gate_v001.md:161`, a statement that no artifact carries it at `LANE_CHANGE_CUSTODY_CLAUDE_CONSTRUCTION_V001.md:52`, and a JSON-parsing adversarial test). `alpha_computed = false` appears 205 times in the cleanroom's `.md`/`.json` alone. `STAGE8_QSPEC_SLOT_STATUS_MAP_V001.md` totals: `slots_total = 18`, `closed_slots = 0`. There is no coupling result to reparametrize.

**The forward risk, stated plainly, because it is the actual decision.** The danger is not that the axioms secretly encode the measured value — the refutation at row 517 is direct evidence against that. The danger is that A6 is an open-ended licence. Each time the construction needs a number, a new inequality is derived and its equality case is adopted in a new file with a new name, and the register that is supposed to total them has four unresolvable entries and misses the whole class. Nine instances exist; nothing in the corpus bounds the tenth. The corpus's own guards against this are real and well-drafted — the no-retuning rules, the forward seals, `V011:2136` firing on *"an F^2 or Pauli term is declared theorem-excluded only because completeness postulated it away"*, the slot-9 bar requiring exclusion *"by theorem rather than postulate relabeling"* — but they are prose. The mechanism that is supposed to enforce them certifies by hardcoded literal: I verified at `scripts/audit_current_authority_v010.py:50-80` that the load-bearing exclusion flag is a Python literal in the expected-state dict.

**Two consequences that should govern how the program is described and worked.** First, A6 should be named once, as a single axiom, with its nine applications enumerated under it — because the moment it is counted once, the claim "given N axioms the coupling is determined" becomes visibly the claim "given a rule that picks the smallest admissible value of every free quantity, the coupling is determined", and that is not a result, it is the statement of the method. Second, the corpus already knows which of its axioms is the good one. A3 is the only terminus in the set that is a substantive physical proposal with a falsifier and an explicit refusal to overclaim. The program's strongest available position is the one A3 and A5 point at — a conditional structural result about what pre-record link independence and a transport-only generator class do and do not imply — and not a coupling. `A6` is what stands between those two honest axioms and a number, and it is the weakest thing in the stack.