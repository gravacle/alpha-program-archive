PASTE 311 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2d] RUN THE FORCING PROTOCOL ON THE FINITE COUPLING FAMILY

*** READ `alpha_supervision/LOCKED_PROCESS.md` (act-based fences, C19). SEAL, MIRROR, REPORT,
STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-228)

```text
DOES THE OBJECT EXIST?   AS A FINITE INSTANTIABLE FAMILY: at most 4 attachment placements
                         (incidence-line . source generator . record-write operator .
                         history-independent baseline) x characters n = +/-1 (faithfulness
                         already cut the integer family, Q-228). EVERY CELL CAN BE WRITTEN
                         DOWN EXACTLY AT N = 1, 2.
IS THE VERSION CURRENT?  YES. Q-227's baselines bind: I_N = delta at a_+ = a_-; Phi_N = charge
                         dephasing; sequential compatibility.
ARE ITS INPUTS PRESENT?  YES -- the exact finite transitions, the ratified structure, the
                         derived U(1).
```

## 1. THE PROTOCOL — ON THE PROGRAM'S FIRST FINITE COUPLING FAMILY

```text
STEP 1  INSTANTIATE ALL CELLS: for each placement x character, write U_N^cell[a] explicitly at
        N = 1 (and 2 where the placements differ only there). A placement that CANNOT be
        instantiated exactly is reported TYPE-U, not padded. Check placement-degeneracy: cells
        giving IDENTICAL U_N at all N collapse (that is derivation, not choice).
STEP 2  EQUIVALENCE: sealed conventions only. *** THE n = +/-1 QUESTION LIVES HERE: does a
        sealed CPT/reality/charge-conjugation convention identify the two characters' PHYSICAL
        maps? Read the conventions at source (U1_008's reality involution included). DO NOT
        ASSERT IT. ***
STEP 3  FAILURE-CAPABLE CONSTRAINTS, each with named victims:
        (i)   BASELINE: a_+ = a_- must return I_N = delta (Q-227, sealed);
        (ii)  GAUGE COVARIANCE under the sealed conventions;
        (iii) DEPHASING PERSISTENCE: Phi_N's charge-sector structure at zero holonomy
              difference;
        (iv)  the untraced ONE-CELL restriction (the finite authority);
        (v)   SEQUENTIAL N <= M compatibility under zero-extension;
        (vi)  charge/flux ACCESS: the REQUIRED side of the origin table -- a placement under
              which the background CANNOT access the charge sector fails record physics.
STEP 4  COVERAGE: the four placements came from 310's audit -- state whether they exhaust the
        attachment sites of the finite dynamics' explicit form, or whether a fifth is possible.
STEP 5  SURVIVORS. 1 -> *** THE SOURCE-HISTORY MAP IS FORCED; BUILD F_N[a_+, a_-] AT N = 1, 2
        IMMEDIATELY, WITH THE Q-227 CHECKS. THE FOUR-CONSUMER OBJECT ARRIVES. *** 2-8 -> the
        sized list for the principal, with what distinguishes the cells. 0 -> name the
        conflicting pair.
```

## 2. LEADS

```text
IF ONE CELL SURVIVES AND F_N CONSTRUCTS -- *** LEAD WITH THE EXPLICIT FUNCTIONALS. Then attack:
which steps are derived, which TYPE-P. ***
IF n = +/-1 IS THE ONLY RESIDUE -- lead with exactly what physical statement distinguishes them
(that is charge-conjugation's role in record formation, and the principal should see it plainly).
IF A FIFTH PLACEMENT EXISTS -- lead with it; coverage honesty first.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
