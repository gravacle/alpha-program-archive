## PASTE 502 — CODEX LANE 1 (HIGH EFFORT) — [TASK 5 / EQ6] BUILD B_C1_COMPLETION: THE ROUTE'S KEYSTONE

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-424. DoR-020 in force conditionally on [EQ6]; the frontier map (`b8294e91…`) types B_C1_COMPLETION as CONSTRUCTIBLE WITH ROUTE and the keystone (Faithfulness and C3 both build on its carrier). This runs in parallel with 503 (Codex 2's 1A exhibit), blind to it.
- Authorities (verify hashes): the frontier map `b8294e91…` (row 2's route — follow it exactly), the hunt's subpackages `452f1bb8…` (the finite/orbitwise bounds are your starting material), package V005 `09883a0d…`, metric V005 `2a379098…`, DoR-020's decision file.
- Custody: construction under the adopted theory; the regressions bind; condition-tag [EQ6] on everything. All symbolic; target-blind.

TASK — the route, executed:
S1. From the actual P4 core: build its local seminorm orbit.
S2. Prove the bounded-geometry estimates (cross-orbit uniform constants; attained bounds; the separating physical topology; W5).
S3. Construct and verify the covariant Hilbertization — the completed C1 carrier, with covariance, reality, unit-seam, restriction, and the five regressions.
S4. State exactly what the built carrier unblocks: the ker(Kernbar) computation (Faithfulness's route) and the closed-d domain (C3's route) — typed handoffs, not started here.
S5. Falsifiers run; anti-tuning ledger; honest stopping point if the route obstructs (exact, per the standing standard).

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_C1_COMPLETION_BUILD_LANE1_V001.md`
with a lead-result block (`C1 = BUILT / STOPPED_AT (+exact)`; the handoff types), the register-sweep list, and the S1–S5 chain. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
