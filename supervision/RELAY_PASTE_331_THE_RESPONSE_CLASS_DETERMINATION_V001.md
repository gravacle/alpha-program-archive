PASTE 331 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] WHICH CLASS DOES THE PHYSICAL RESPONSE LIVE IN? THE SEALED-SIGNATURE DETERMINATION (DoR-011)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-247)

```text
DOES THE OBJECT EXIST?   THE QUESTION does (Q-247): separation is PROVED on every ratified
                         class (norm C*, standard module, left-multiplier) and FAILS exactly
                         on the bidual (Tail_B** = z_tail B**, p_ch-capable). WHICH CLASS the
                         physical response construction requires is now the tail half of the
                         deciding question.
IS THE VERSION CURRENT?  YES: Q-243 (finite retarded block p_ch-free), Q-245 (re-entry only
                         via tail or background), Q-247 (the split). NOTE Q-247's honesty
                         flag: the physical rho_H,N are NOT instantiated -- this relay READS
                         SIGNATURES; it does not pretend to build the response.
ARE ITS INPUTS PRESENT?  The sealed corpus texts defining the response chain: Delta_Phi[K;X_K]
                         and its consumed response operator, the retarded Hessian H_R[G], the
                         Thomson response, Z_inc[J,R], the source families s_J/s_R, and the
                         ratified DoR-008 classes (C* algebra, Hilbert C*-module,
                         left-multiplier representation).
```

## 1. THE TASK — A TYPING AUDIT OF THE SEALED RESPONSE CHAIN, NOT A BUILD

```text
1  ENUMERATE every sealed statement that types or constrains the objects the response chain
   consumes, from Delta_Phi backward: Delta_Phi[K;X_K] <- response operator <- H_R[G] <-
   raw bilocal G <- Z_inc[J,R] <- sources. Quote paths and lines. For EACH arrow, determine
   what the sealed text REQUIRES of the object's class: bounded/adjointable? norm limits or
   weak-* limits? states/functionals (which would live in the DUAL -- say what that implies
   for bidual content)? derivatives in which topology?
2  *** THE VERDICT SOUGHT, per object: NORM/MODULE-CLASS REQUIRED (separation applies -- the
   tail CANNOT carry p_ch into it) vs BIDUAL CONTENT REQUIRED (the tail CAN) vs UNTYPED BY
   SEALED TEXT (name the freedom -- and note an untyped object is where a CHOICE would enter,
   which the program must then make explicitly, not silently). ***
3  THE KNOWN PRESSURE POINTS, check deliberately: (a) retarded/advanced boundary values and
   i-epsilon limits often live in weak topologies -- does the sealed contour text force
   weak-* content or only norm-continuous families? (b) the stationary/on-shell background
   (channel (b)): does any sealed text type the background's class, and does its
   p_ch-dependence question reduce to the same audit? (c) Q-243's coherent one-point block:
   which class does the sealed source-family text put IT in -- is it even ELIGIBLE to feed
   the bidual tail?
4  DELIVERABLE: a typing table -- object, sealed citation, class verdict, consequence for
   p_ch re-entry. Type every gap honestly; NO_VERDICT is legal per row.
5  KILL-PASSES: do not infer class from mathematical convenience or convention -- ONLY from
   sealed text (the corpus-audit trap memory binds: substring presence is not typing); do
   not treat "the response is physical so it must be normal/continuous" as an argument.
6  FENCES (DoR-011): reading and typing only. No evaluation, no roots, no numbers, no
   comparison to measured constants.
```

## 2. LEADS

```text
IF EVERY CONSUMED OBJECT IS NORM/MODULE-CLASS -- *** LEAD WITH IT: the tail channel CLOSES
outright (Q-247's proof applies); p_ch survival then rides ONLY on the background channel,
and say what its audit found under 3(b). ***
IF ANY OBJECT REQUIRES BIDUAL CONTENT -- *** LEAD WITH THE OBJECT AND ITS CITATION: that is
the exact door p_ch walks through, and the program must decide whether the requirement is
ratified data or an unforced convention. ***
IF THE CHAIN IS UNTYPED AT A LINK -- lead with the link: an explicit typing decision falls to
the principal, and the table is what he rules on.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
