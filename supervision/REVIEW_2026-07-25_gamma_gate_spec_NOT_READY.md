# Hostile Pre-Execution Review — Gamma Gate Spec V001: NOT_READY
Claude lane · 2026-07-25 (late night) · fresh-context lane return; operative
summary (full return in session record). All four blocking findings accepted;
repair amendment sealed same night (STAGE8_T7_GAMMA_GATE_SPEC_REPAIR_
AMENDMENT_V001).

BLOCKING (all repaired append-only):
1. Lemma 2's displayed identities sign-wrong as literals ((gamma^1)^2 = -I;
   T' not an involution) — conclusion correct in conjugation form; repaired
   by redefining T' = P_x (x) (i gamma^1) (genuine Hermitian involution,
   identical action). Lemma 1 exact as sealed.
2. Lambda-odd control tuple mis-ordered vs the sealed (-sqrt2, 0, +sqrt2)
   ordering (inherited verbatim from the memo); would have violated its own
   1e-14 exact-zero bar at the scale of Z itself. Repaired: explicit
   lambda-indexed table w~(lambda) = lambda/4.
3. THE MEMO'S TOY kappa_B = 1.802 IS WRONG (formula valid only at real
   e^{-i theta}; every memo check sat at theta = pi). True value 0.534,
   verified two independent ways by the reviewer. THE MEMO'S CONCLUSION
   SURVIVES: envelope difference persists (0.707 vs 0.534). Superseded of
   record in the amendment; corrected general-phase assembly pinned.
4. B1 underdetermined (fixture state/functional, record multiplicity/mask,
   fixture P_x all unpinned; the fixture h_0 has a 4-dim kernel needing a
   convention; the naive site reflection fixes the WRONG mask). Repaired:
   full B1 definition cluster frozen (single-record adaptation; mask
   diag(1,1,0); ring reflection j -> 1-j; sea state with kernel excluded
   deciding, mixed-analogue emission non-deciding).

CONDITIONS (repaired): zero-denominator fence; THE RE/IM CORNER — kappa
difference is -Re[Delta_Xi] exactly, so the REFUTED arm now fires only on
certified Re != 0, and the corner Re = 0 with Im != 0 (insensitivity HOLDS
while Xi differs) escalates to Brian as GATE_BLOCKED_PENDING_PRINCIPAL
(the disposition's own "nonzero => dead" wording carried the same gap —
flagged to the principal); B2 expected value pinned (identically zero;
deviation = apparatus fault); missing hashes pinned (envelope comparison
spec has NO adjacent seal on disk — its hash 4059eff5… is now its
authority of record for this gate); broken-P_x absolute floors; result
artifact named; certified-rational-enclosure admissibility clarified.

DECIDABILITY (adopted into the amendment): v_B branch closed-form
(time-independent generator; Lindemann-Weierstrass-decidable); v_A branch
certifies NONZERO via certified partial sums (tail pi^(N+1)/(N+1)!);
exact-zero certifiable only by closed form; undecidable cases route to
GATE_BLOCKED. The structural asymmetry (SURVIVES needs more machinery than
REFUTED) is honest and recorded.

LEMMA_CHECK: every (anti)commutation independently rederived and confirmed
({gamma^5, S} = 0 despite S containing gamma^5 — the anticommutator
vanishes though the commutator does not); both pinned Hermite-carrier
states verified invariant under both symmetries; n=2 carrier has no zero
modes (h_0^2 = (3/2 ell^2) I). The reduction chain (m0 = m1 = 0, m2 = -1,
Z = -(1/2)[D_sqrt2 - D_0]) reverified exactly.

Calibration note: third construction-lane sealed-text defect caught by
hostile review today (theta_kappa arithmetic; R2 fabrication-economics
inherited claim; now the memo-inherited toy formula + tuple ordering).
The layered-review topology continues to catch everything pre-execution;
the pre-seal double-computation rule is extended to inherited numbers,
not just newly frozen ones.
