## PASTE 525 — CODEX LANE 1 (HIGH EFFORT) — [TASK 5 / EQ6] THE SCOPED-WITNESS COMPLETION ON I_flip

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-449.
- Your flip section stands CONFIRMED (your build `a78c2450…`; the check `0e61e6eb…` — read its N3: the scoped witness's exact needs). The needs list is the contract: (i) the scoped SIX-GENERATOR J1–J15 equalizer on I_flip; (ii) DoR-020 scope-admissibility; (iii) the full finite-bottom certificate. C_ret is separate (being built in parallel as 524, blind to you).
- The nine regressions, the adopted clauses (DoR-020 + A1), the standing rule. All symbolic; target-blind.

TASK:
P1. **The scoped six-generator equalizer:** extend the confirmed flip inhabitant to the full six-generator J1–J15 equalizer ON the I_flip scope — each generator's certificate discharged on the scope; the joint conditions verified (the equalizer regression binds: separate ≠ joint).
P2. **Scope-admissibility:** prove I_flip a lawful scope per DoR-020 (covariant, no-selection, DoR-008-compatible).
P3. **The finite-bottom certificate:** constructed and proven on the scope.
P4. On success: the scoped-witness package assembled for certification (what the certification review must verify, listed); on resistance: the exact stop, typed.
P5. Falsifiers; anti-tuning; the regressions run.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_SCOPED_EQUALIZER_COMPLETION_LANE1_V001.md`
with a lead-result block (`SCOPED_EQUALIZER = INHABITED/STOPPED_AT`; `ADMISSIBILITY = PROVEN/…`; `FINITE_BOTTOM = PROVEN/…`; `WITNESS_PACKAGE = ASSEMBLED/…`). Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
