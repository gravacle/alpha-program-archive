# Hostile Pre-Execution Review — ER Insensitivity Spec V002: READY_WITH_CONDITIONS
Claude lane · 2026-07-25 (night) · fresh-context lane return; full text in session record.

All three V001 BLOCKING repairs verified FIXED against the sealed sources
(not the spec's own assertions): raw-amplitude convention + fence deletion
(stencil normalization-invariance independently verified); threshold
refoundation (floor prefactor 2/49 rederived exactly; witnessed floors
confirmed: pure max 6.36, mixed max 75.33); D1 restated at a=0 (verified in
the sealed executor's source — no connection operator exists; the lambda=0
shortcut is exactly exp(-i h0 T) and is exact only at a=0). All four
assembly pins verified against Phase-A line 375 and the sealed quadratures.
Richardson ratios 3.96/3.99 confirm second order. Prediction hygiene clean.
V001 + seal untouched.

FOUR CONDITIONS (all pre-execution, append-only, narrowing-only — sealed in
the workspace as the V002 EXECUTION NARROWING BINDING):
- C1: RESOLVABLE requires certified LB > floor(ER-A,state) + floor(ER-B,state)
  (sum, not max — coordinated two-branch tolerance ambiguity; exposure
  windows pure (6.36, 6.96], mixed (75.33, 81.90] would otherwise be
  mislabeled RESOLVABLE).
- C2: e(node) gains the independent lane's own error addend
  |Z_192 - Z_96|_independent / 3, outward; log-enclosure working precision
  pinned.
- C3: the result artifact must carry the RESOLVABLE_BY_BATTERY scope note:
  "a fortiori resolvable at battery-grade precision on this same finite
  proxy and carrier; NOT a demonstration that kappa_record differs; NOT an
  output of the sealed ER-A-premise battery" (the sealed battery pins ER-A
  and never computes ER-B; kappa_proxy-to-kappa_record is underived; the
  intensive limit could contract a fixed-regulator difference).
- C4: corrected self-check digit 0.60148 recorded in the execution record.

TRANSFER_CLAIM_CHECK adopted in full: what RESOLVABLE does and does not
establish is now pinned in the binding's scope note. The NOT_RESOLVED arm
(the spec's own P3 prediction) was found honestly minimal as drafted.

Minor findings: display slip 0.6016 -> 0.60148 (non-binding); stencil-sign
prose note (|Delta| unaffected); D4 log-precision freedom is
soundness-safe (any loosening biases toward NOT_RESOLVED) but now pinned
via C2; D1 row scope harmless either reading.
