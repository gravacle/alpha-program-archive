PASTE 214 — CODEX LANE 2 — IS THE OPEN ACTION-FORM CLASS INSIDE §5.3's ADMITTED FAMILY?

ROAD: **UNBLOCKS STEP 3.** *** DOES NOT WAIT ON `B0`. *** This is the single conditional left
standing from the census audit, and it sizes everything downstream of it.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.

*** THIS RELAY IS SELF-CONTAINED. ASSUME NO MEMORY OF ANY PRIOR RELAY. ***

---

## 1. WHERE THIS STANDS

`alpha = 1/(4 pi K_*)`, `K_*` the root of `C_record(K) = 0`. **§5.3 of
`STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md` is the uniqueness gate** that would establish a computed
value was **forced rather than fitted**. It requires **no inequivalent admitted positive root** and
**forbids narrowing the admitted family after the root is seen** (`:581`).

**A prior audit — `STAGE8_SECTION53_ADMITTED_FAMILY_CENSUS_REACHABILITY_AUDIT_V001.md`, `4411b91a…` —
established:**

```text
OPEN_ACTION_FORM_not_enumerable    = false | TYPE-S     "open" means NOT YET ENUMERATED
Section_5_3_reachable_in_principle = true  | TYPE-C     condition: a frozen pre-root census
Section_5_3_executable_now         = false | TYPE-U
```

**and that §5.3's admitted family and the `MIXED_TYPE_ALLOWANCE_ENVELOPE` are OVERLAPPING BUT NOT
IDENTICAL** — §5.3 quantifies over a **gate-specific admitted family of response-changing microscopic
constructions**, while the envelope is a typed set-valued object with scalar, discrete,
operator/action-form, algebraic, admissibility and categorical axes
(`STAGE8_ALLOWANCE_ENVELOPE_WITH_CLOTHING_WITHDRAWN_V001.md:55-75`).

*** IT LEFT EXACTLY ONE CONDITIONAL STANDING: "THE OPEN ACTION-FORM CLASS IS RELEVANT ONLY IF ITS
MEMBERS LIE IN THAT GATE'S ADMITTED FAMILY; THE ENVELOPE NAME ALONE DOES NOT PROVE THAT INCLUSION."
***

---

## 2. THE TASK

**2.1 ARE THE OPEN ACTION-FORM CLASS'S MEMBERS ADMITTED MUTATIONS IN §5.3's SENSE?**

Determine, from sealed text: what makes something a member of §5.3's admitted family — the **exact
admission criterion**, quoted — and whether the action-form class's members satisfy it. **All of them,
some of them, or none.** *** A SPLIT ANSWER IS A REAL ANSWER AND MAY BE THE TRUE ONE. Report which
members fall which side and on what criterion. ***

**2.2 IF SOME OR ALL ARE ADMITTED — HOW BIG IS THE CENSUS?** Not the census itself. **Its size and
shape:** how many members, along which axes, and which of the eight ledger fields
(canonical identity · domain/codomain · action form and branch choices · source/record embeddings ·
provenance and adoption time · target-awareness declaration · mutation relation · membership-predicate
coverage proof) are already recoverable from sealed text versus wholly missing.

**2.3 IF NONE ARE ADMITTED** — say so plainly and state what the §5.3 family then consists of instead.
*** THAT WOULD SHRINK THE STEP-3 OBLIGATION SUBSTANTIALLY AND IS THE CHEAPEST GOOD OUTCOME AVAILABLE.
***

---

## 3. WHY IT IS URGENT, WHICH IS NOT OBVIOUS

The census audit's §2.3 says: *** "FREEZE IT **BEFORE ANY ROOT IS SEEN**." ***

**The census cannot be built after the number exists without destroying the very property §5.3
checks.** *** SO THIS IS NOT END-OF-ROAD WORK. THE PROGRAM HAS BEEN TREATING §5.3 AS THE LAST STEP; IT
IS NOT. Sizing the census now is what makes it possible to freeze it in time. ***

---

## 4. SYMMETRIC LEADS

```text
IF NONE OR FEW ARE ADMITTED -- LEAD WITH IT. Step 3's obligation shrinks, and this program has very
few clean positives. *** DO NOT UNDERSTATE A GOOD RESULT. ***

IF ALL ARE ADMITTED AND THE CENSUS IS LARGE -- LEAD WITH THAT, with the size estimate, so it can be
scheduled before a root exists rather than after.
```

**Do not frame the work to find a defect.** The last two structural questions both returned positives.

---

## 5. RULES

- **Q-54 typing on every negative:** TYPE-R refuted · TYPE-U unbuilt · TYPE-S scope-empty · TYPE-C
  constraint-blocked. `NO_VERDICT` is legal. **Only TYPE-R is physical content.**
- **Q-92:** specifying and naming what is missing is a complete answer, not a lesser one.
- **Q-69:** never identify a flag with the object that discharges it.
- **No unproved-identity transport.** *** A NAME IN COMMON IS NOT AN INCLUSION — that is precisely
  what the census audit refused to assume, and it is the crux of this relay. ***
- **Word-boundaried matching, and CASE-INSENSITIVE on identifiers.** A capitalized flag was invisible
  to a reviewer scan this session; do not repeat it.
- **Report search scope**: roots, exclusions, and the queries actually run.
- **No repair.** If one is obvious, name it in one line at the end and stop.

## 6. FENCES — VERBATIM, NON-NEGOTIABLE

- **Never touch `a32_holdout/custodian_private/`.**
- **Do not compute** alpha, `kappa_record`, `kappa_Thomson`, a coupling, a scale, a root, an
  eigenvalue, a beta function, `E_R`, `T_R`, `k_R`, or any absolute interval.
- **No comparison to any measured constant.**
- **Do not resolve the Misner-Sharp / Brown-York fork by choosing — it must be DERIVED.**
- **Report refutations; never repair them.**

## 7. CUSTODY (Q-91)

*** NO LANE RUNS ANY GIT COMMAND. *** Write the artifact in the cleanroom, compute its
`.seal.sha256` sidecar and **verify it matches**, mirror artifact + sidecar to
`alpha-program-archive/workspace/`, report hashes and paths, and **stop**. You may run
`corpus_check.py --report` on your own artifact; `--gate` is not your concern and **a red gate is
never a reason to stop working.** The reviewer verifies, baselines, commits and pushes.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
