## PASTE 485 — CODEX LANE 1 (HIGH EFFORT) — [TASK 5 / R4] BUILD THE COVARIANT COMPLETED LOCAL-SYMBOL MAP

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-406. The residue ledger is of record (`c35ef846…`); the lawful order starts at R4: the covariant completed local-symbol map, including the carrier-to-field seam — the object that makes the reading-rule family R2 physically enumerable and computes p_loc on the rank-one profiles.
- Authorities (verify hashes): the ledger `c35ef846…` (its R4 row and the LM-1/LM-2 route — follow it exactly), the Maxwell-symbol block result `70dde1c0…` (what blocked and why), metric V005 `2a379098…`, square V004 `abf6d366…`, DoR-016/V004 `69f4d93b…` (the rank-one profiles), Q-398/400/406.
- Custody: construction, derivation-first; authored steps at the choice table with carrier-structural justifications only; cross-review follows. p, ν symbolic; no values.

TASK:
A1. **LM-1:** the completed local-symbol topology — the carrier-to-field seam constructed (the completed carrier's local structure mapped to field data), with covariance, reality, the R4-only unit seam, and restriction certificates proven.
A2. **LM-2:** the spectral/long-wavelength symbol of the rank-one profiles; the Ward/contact split; the kernel certificates — per the ledger's route.
A3. **The deliverable:** p_loc computed on the rank-one profiles as far as the built map determines it; the reading-rule family R2 physically enumerated (the normalized covariant real unit-correct restriction/batching-compatible readers, as a typed family — no member selected); χ_K = p_loc[R̂_K] expressed on the built family (its fiber coordinates explicit).
A4. **Falsifiers:** finite restrictions; the standing regressions; the anti-tuning ledger; every certificate failure-capable.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_R4_LOCAL_SYMBOL_MAP_BUILD_LANE1_V001.md`
with a lead-result block (`LM1 = BUILT/BLOCKED`; `LM2 = BUILT/BLOCKED`; `R2_FAMILY = ENUMERATED (+dimension/coordinates) / BLOCKED`; `CHI_K = <expression on the family>`), the register-sweep list, and the A1–A4 chain. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No numeric evaluation; no registered verdict; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
