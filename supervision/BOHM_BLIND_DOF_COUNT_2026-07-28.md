# BLIND DEGREES-OF-FREEDOM COUNT — kappa_record (Bohm cross-check, 2026-07-28)

Independent of Codex's count (paste 100). Three counters from different starting points — from the
fences, from the battery output chain, from the parameter side — each deduplicated by an agent whose
job was to merge duplicates and find omissions, then reconciled.

---

# RECONCILED MEASUREMENT — CONSTRAINTS vs FREE PARAMETERS ON kappa_record

**Corpus:** `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003` (read-only; no writes made). No value of kappa_record, kappa_Thomson, alpha, or any function of them — including 1/(4·pi·kappa) — was computed or evaluated. I encountered the definitional relations `K_R(mu) e_HL(mu)^2 = 1` and `alpha(mu) = 1/[4 pi K_R(mu)]` at `PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:55-56` and did not evaluate them. The three senses of "alpha" (Schatten-2 exponent, Dirac matrices, fine-structure constant) are kept apart throughout; the corpus in fact carries a **fourth** "p"-collision (the balanced-calibration `p=1/2`) and a **fourth** "alpha" (the `alpha = 0` sliver pathology), both noted where they arise.

**Verification performed for this reconciliation** (not merely merging the three reports): I read the disputed primary text directly at `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:755-806, 1294-1316, 2060-2110`; `STAGE8_P_COINCIDENCE_EXPONENT_DETERMINATION_V001.md:190-219`; `STAGE8_RL2B_COMMUTATOR_ROUTE_REFUTED_AND_TARGET_SHARPENED_V001.md:105-163`; `STAGE8_DISCRIMINATOR_CLAIM_WITHDRAWAL_V001.md:50-90`; `ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V003.md` (all 142 lines); `PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md` (all 138 lines); `COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md:50-101`; `ONE_CELL_FIDELITY_ACTION_SELECTOR_GATE_V001.md:100-140`; `COMPLETE_QSPEC_ABSOLUTE_SCALE_AND_CONTINUUM_PREREQUISITE_AUDIT_V001.md:160-190`; `STAGE8_T7_CONNECTED_PRIMITIVE_RESPONSE_GATE_V001.md:20-60`; `STAGE8_RL2B_OVER_CHARTER_AND_BLIND_ROUTE_FINDING_V001.md:118-185`; `STAGE8_KAPPA_RULE_ADOPTION_STAGE12_ERRATUM_AND_D1_D2_D3_RETURNS_V001.md:215-240`; `STAGE8_PRINCIPAL_RULINGS_APPLIED_EQ1_OPTION3_AND_O4_BLOCKED_V001.md:80-95`. Plus seal/reference checks and two corpus-wide greps named in §7.

---

## 1. THE CONSTRAINT LIST

Organised by the **type of object constrained**, because that is what the arithmetic turns on. Duplicates removed with the ruling stated. `[D]` = discharged/proved. `[O]` = open obligation (a demand, not a supply). `[X]` = never executed.

### 1A — Constraints whose subject is the SCALAR kappa_record (3)

| # | Constraint | file:line | Status |
|---|---|---|---|
| **S1** | `kappa_record,E = kappa_record,B = kappa_record >= 0` — an equality **and** a sign, both emitted by the one predeclared Hodge test `[M_record, J_star]=0` | `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:1572-1579` | DERIVED `[X]` (no T14 report) |
| **S2** | The enclosure must **exclude zero** | `STAGE8_KAPPA_RULE_ADOPTION_STAGE12_ERRATUM_AND_D1_D2_D3_RETURNS_V001.md:225`; `STAGE8_GATE5_KAPPA_RECORD_THEOREM_BATTERY_SPEC_V001.md:400`; `STAGE8_MASTER_PLAN_AMENDMENT_001.md:40` | ADOPTED, binding (`kappa_rule_adopted = true`, :232) |
| **S3** | kappa_record is an **extensive dimensionless** record susceptibility — hence invariant along the sealed isotropic scale orbit | `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:2102`; `STAGE8_GATE5_..._SPEC_V001.md:502`; mechanism at `STAGE8_RL2B_OVER_CHARTER_AND_BLIND_ROUTE_FINDING_V001.md:162-166` | DERIVED `[D]` |

**RULING (against `from-fences` raw, which counted S1's sign as a separate constraint K5 from the Hodge test K4):** the `>= 0` predicate is the *output* of the Hodge test, not an independent axiom. V011:1574 reads "Only the predeclared exact test … **may yield** `kappa_record,E=kappa_record,B=kappa_record>=0`". One constraint, two outputs. Counting them twice inflates the value-side by one — and the value-side is exactly where inflation is most misleading.

**RULING (against `from-fences` raw's "there are exactly TWO"):** there are three, and `from-parameters`/`from-battery` are right that S3 was used (to delete absolute `T_R` from the parameter side) by an agent that never counted it. But note the **scope limit** the reviewers missed in both directions: the *same* artifact that supplies S3's mechanism records at `:145-147` that "the recorded alpha = 0 pathology arises under ISOTROPIC dilation, inside it". S3 fixes the **value** along the isotropic orbit; it does **not** clear the isotropic direction for the bound machinery. State S3 narrowly or it will be over-claimed downstream.

**NOT COUNTED — reporting discipline, not physics:** exact-rational enclosure endpoints, width `< 10^-8` with monotone tail decrease (`STAGE8_GATE5_..._SPEC_V001.md:173`; schema at `:425`). This constrains the **report format**. See §7 — it does *not* say kappa_record is rational.

### 1B — Constraints on the TENSOR M_record (6)

| # | Constraint | file:line | Status |
|---|---|---|---|
| **T1** | Exact rank 20 with a one-dimensional nullspace spanned by the parity-odd `F∧F` | `V011:1487` | DERIVED `[X]` |
| **T2** | Canonical lift = unique representative with `<M_L,T_top>_F = 0` | `V011:1509` | DERIVED `[X]` |
| **T3** | Reduction to `H_trial` with **vanishing cross and anisotropy blocks** (does not equate the two coefficients) | `V011:1565-1568` | DERIVED `[X]` |
| **T4** | Hodge commutator `[M_record, J_star] = 0`, `J*^2 = -I_6` — the sole route to a scalar | `V011:1572` | DERIVED `[X]` |
| **T5** | `M_record = lim_(odd L→∞) M_L`, invariant under deleting any one verification row **and** under the sealed refinement class | `V011:1531` | DERIVED `[X]` |
| **T6** | Gate-3 identity Hilbert forms `M_0 = M_1 = M_2 = I`, one unitary equivalence class | `V011:1686` | **ADOPTED-conditional** — `Elementary_Record_Hilbertization_Hypothesis_derived_from_deeper_principle = false` (`BID_ELEMENTARY_RECORD_HILBERT_FUNCTOR_CLASSIFICATION_V001.md:118`); `Hilbert_functor_gate_passed = false` (V011:2220) |

### 1C — Constraints on FUNCTIONALS in the response chain (14)

| # | Constraint | file:line | Status |
|---|---|---|---|
| **F1** | Intensive Hessian definition `R_record,L = d²/ds dt [Gamma_K/N_4(K)]` after the linked-cluster limit | `V011:1295-1299` | DERIVED `[O]` |
| **F2** | Duhamel covariance from `G_L` must **reproduce** that Hessian — genuine second construction | `V011:1299` | DERIVED, **NOT_EXECUTABLE** (`stage8_execution/t_reports/T07.json`) |
| **F3** | Fubini-Study pullback: `-log\|A\| = ½ ds²_FS + O(A³)`, explicitly a check and **not** a selector | `V011:1300`; `battery:258` | DERIVED `[X]` |
| **F4** | Amplitude normalization `Z_h(A)=a_h(A)/a_h(0)`, `Z_h(0)=1` | `V011:1225` | DERIVED `[D]` (T07(i) PASS) |
| **F5** | Volume-uniform zero-free neighbourhood for the amplitude *(absorbs H-Z0)* | `V011:1232` | DERIVED, **BLOCKED** (T07.json; flag :2184 false) |
| **F6** | Exact disjoint monoidality `Gamma_disjoint = Σ_c Gamma_c`, no inserted extensive factor *(absorbs the V010 zero-stiffness termination)* | `V011:1246, 1257`; `BID_MONOIDAL_EXTENSIVITY_DERIVATION_V001.md:123, 126-127` | DERIVED `[D]` — **proved, and exactly additive** |
| **F7** | Connected linked-cluster density exists, boundary corrections subextensive | `V011:1268-1271` | DERIVED, **BLOCKED** (:2185 false) |
| **F8** | Uniform full-neighbourhood locality: `‖Rem(p)‖ ≤ C\|p\|^(2+δ)` for all directions | `V011:1537` | DERIVED, **BLOCKED** (:2193 false) |
| **F9** | Unique horizontal minimum-norm flux lift `Q_flux` | `V011:1341` | DERIVED `[D]` (T10.json, `horizontal_zero_flux_addition_dimension = 0`) |
| **F10** | Tetrad face metric is a **pullback**, not an adjustable weight (A27) | `V011:1391` | DERIVED `[O]` |
| **F11** | Positive face response must pass the **source-free Maxwell energy test** | `V011:2065`; flag `source_free_Maxwell_energy_response_proved = false` at `:2215` | DERIVED, **open** |
| **F12** | Root-generated preparation domain; every translation-invariant competitor excluded or reported surviving | `battery:218` | DERIVED `[X]` |
| **F13** | Transport weight `Σ_i \|D_i\|_4 = \|C\|_4` **exactly** | `STAGE8_TRANSPORT_FUNCTOR_CHARTER_V001.md:50` | ADOPTED; weight side closed unconditionally, **response side open** |
| **F14** | Base-point curvature generated from the sealed `F_1` operator, machine-checkable to the Gate-4 core | `battery:197` | DERIVED, **routes BLOCKED as over-claimed** (see ruling R-b) |

**Plus one EQUALITY over-constraint on the finite-L design:** `y_c = y_s` exactly or by preregistered enclosure (`V011:1440`) — DERIVED `[X]`.

### 1D — INDEPENDENCE conditions (4)

| # | Constraint | file:line | Status |
|---|---|---|---|
| **I1** | Cellulation independence / refinement naturality *(absorbs the packing fence and NC2)* | `V011:1407`; fence at `battery:153` | DERIVED, `cellulation_independence_proved = false` (:2189) |
| **I2** | Physical quotient contains exactly one positive ray before normalization, exactly one point after | `V011:804-806` | DERIVED, `differential_uniqueness_gate_passed = false` (:2222) |
| **I3** | F'-5 parametric-dependence fence: every constant is a functional of `(‖b_D‖, tau_R, sea-kernel decay data, \|w_lambda\|)` only *(absorbs D5''s argument-list clause)* | `STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md:1671` | **ADOPTED**, inherited not originated (`STAGE8_IS_RL2B_FAILURE_FORCED_BY_F5_DETERMINATION_V001.md:29-33`) |
| **I4** | Anti-tuning: no response complex, background, root embedding, generator, record interval or preparation domain chosen after a response is seen | `V011:2043` | ADOPTED — **and see §7: `stage8_execution/commitments/` and `reveals/` are EMPTY** |

### 1E — Frozen scalars and structures that are **not** kappa_record (9)

`tau_R = pi/sqrt(2)` (`V011:1205`, DERIVED but **conditional on Gate 4**, `BID_FIRST_OPENING_INTERVAL_DERIVATION_V001.md:97`, and a *consistency check not a selector* per battery T4) · one-record normalization `= 2` (`V011:755`) · `K_Pauli,kin = -1/2` (`battery:208`, `[D]`) · augmentation-ideal orders, **lower bounds only** (`V011:1074`, with `V011:1080-1082` refusing exact-order claims from ideal membership) · completed-history cancellation order 2, `M_0=M_1=0, M_2=-1` (`COMPLETE_QSPEC_..._AUDIT_V001.md:179`) · uniform intrinsic flat-cell measure `mu_D(A)=Vol_4(A)/Vol_4(D)` (`R3_3_GLOBAL_INTRINSIC_MEASURE_CLASSIFICATION_RESULT_V001.md:15`, `[D]`) · U(1) unit-character holonomy + action-character normalization (`PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:26`, `[D]`) · `kappa_bal = 1` (`STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md:794-800`, `[D]`) · frozen truncation schedule odd `L ∈ {3..21}` (`battery:165`).

### RULINGS ON THE ONE-OR-TWO DISPUTES (constraint side)

**R-a. Packing fence = cellulation independence. MERGED.** All three agents converged; the battery says it itself ("T7/T13's cellulation-independent limit IS the required packing-independence demonstration", `:155-156`). One constraint.

**R-b. T01/T02/T11 are not in good standing.** `from-battery`'s deduper caught what `from-fences` missed: all three `.json` reports carry `pass=true` yet the principal ruled "T01, T02 and T11 all route BLOCKED, not CONDITIONAL" (`STAGE8_PRINCIPAL_RULINGS_APPLIED_EQ1_OPTION3_AND_O4_BLOCKED_V001.md:87-89`, verified verbatim). This is precisely the status-flag trap the brief named. F14 and the order claims are demands, not supplies.

**R-c. `C3 + C4` (action multiplier / unit-character holonomy) → ONE. MERGED**, per `from-parameters`' deduper — the uniqueness gate states both in one sentence at `:26`. But **overruled on what it fixes**: it fixes the *unit winding and action-character normalization* and the same line says it "does not determine the field-history weight." It is **not** a fixing of the multiplier `c` in `Gamma_c = -c log|A|`. See R-f.

**R-d. `C7 + C8` (disjoint monoidality / V010 zero-stiffness termination) → ONE. MERGED.** Decisive: both of C8's contents are status flags *inside* C7's artifact (`BID_MONOIDAL_EXTENSIVITY_DERIVATION_V001.md:126-127`), and that artifact opens by declaring it "replaces the terminated V010 normalized direct-sum response."

**R-e. `C15` (transport-charter eliminations) is DELETED from the constraint side.** `from-parameters` cited `:190` for a constraint and `:193` for a parameter — adjacent lines of one fenced block. Counting one finding on both sides is the named error. It is F4/P5's cost structure. Counted once, on the parameter side.

**R-f. `K30`/F'-4 "no measured constants anywhere" is CONDUCT, not a constraint.** It forbids a *method* of resolving the remaining freedoms; it removes no degree of freedom. Struck, per `from-fences`' deduper. Likewise struck as conduct: T0 lineage, T15 output discipline, the entire E1-successor NC1–NC11 block, battery NC3/NC5, the Lorentzian/unitary execution fence, F'-11/12/13/14/16, and AMENDMENT_002's evaluator-successor precondition. **The two NC namespaces (battery NC1–NC5 vs successor NC1–NC11) are DISJOINT sets** — merging them would have invented eleven constraints; all three agents correctly kept them apart.

**R-g. The Cumulative Conditionality Register is a REGISTER, not a constraint.** `from-fences` raw listed it as constraint #33 alongside two of its own members. Retyped as an inventory; its members are counted individually where load-bearing and **not** added to it.

**R-h. `K_bare = 0` — one premise, three labels. MERGED and typed ADOPTED.** `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:167`; battery premise ledger `:452`; `V011:2161`. `primitive_F2_theorem_excluded = false` (V011:2163).

**R-i. `N_4(K)` — a live definitional fork, not a settled constraint.** `from-parameters`' deduper is right and this is a real defect nobody else caught: V011:1271 normalises by `number_of_4_cells(K)`, D-3 defines `N_4(K) := |X(K)|_4`, the cellulation **4-volume** (`STAGE8_T7_D6_SPECS_REPAIR_AMENDMENT_V001.md:84`), and the reconciliation is explicitly only *unit-cell-skeleton* equivalence. Off the skeleton — i.e. in the anisotropic sliver direction where the whole open problem lives — the two differ by the mean cell 4-volume, which is the packing-dependent quantity I1 forbids. Counted once as a constraint (F1's denominator) **and once as an open ruling** (P9), because the ruling is a distinct act.

---

## 2. THE FREE-PARAMETER LIST

### 2A — OBJECT-SIDE: upstream of kappa_record having any value (13)

| # | Free object | file:line | Type |
|---|---|---|---|
| **P1** | **The mandatory hostile counterfamily**: `D_x,e z = i_t(√(2-x) U_e z) - i_s(√x z), 0 < x < 2`, plus arbitrary positive-definite `M_0`/`M_1`, the `epsilon_(c,d)` family for all nonzero `(c,d)`, independent edge and handle magnitudes, and unremoved phases. **"`D_x` satisfies the displayed norm for every `x`."** | `V011:771-787` (verified verbatim) | CONTINUOUS 1-param branch + discrete/phase family, **upstream of M_record** |
| **P2** | `B_lambda = \|10><01\| + \|01><10\| + lambda\|11><11\|` — target-free family, same vacuum and one-record restriction, two-record matrix element `0 → 1` | `STAGE8_T7_CONNECTED_PRIMITIVE_RESPONSE_GATE_V001.md:31, 45-49` | SCALAR inside a functional family |
| **P3** | The **write-envelope profile** ER-A vs ER-B — "would in general give a different value: the record is provably sensitive to the envelope's cumulative profile" | `STAGE8_T7_BETA_ER_A_DISCLOSED_PREMISE_ADOPTION_V001.md:19, 46`; fork executed and unresolved: `STAGE8_T7_ER_FORK_KAPPA_INSENSITIVITY_RESULT_V001.md:8` | FUNCTIONAL (profile), **ADOPTED not free** |
| **P4** | **`omega_in` = the operator-to-scalar bridge** (merged) | `STAGE8_X_TO_KAPPA_RECORD_EXTRACTION_DETERMINATION_V001.md:153, 171, 180, 272` | FUNCTIONAL (a state) / MISSING MAP |
| **P5** | The **transport rule (Q2)** — five pins collapse to one decision with ≥4 incompatible answers; absorbs C15's costs and the role-3 separation pin | `STAGE8_TRANSPORT_CHARTER_OPTIONS_WITH_COSTS_V001.md:188-195`; `STAGE8_TRANSPORT_FUNCTOR_CHARTER_V001.md:183` | DISCRETE |
| **P6** | Source-independence unit: **atom or diamond** (O-D4) — "THIS LANE DOES NOT CHOOSE" | `STAGE8_TRANSPORT_FUNCTOR_CHARTER_V001.md:142` | BINARY |
| **P7** | **Carrier typing / F'-5 trilemma** — "No horn delivers R-L2b as written" | `STAGE8_RL2B_CONVERGENCE_HYPOTHESIS_RESOLUTION_AND_F5_TRILEMMA_V001.md:176`; `STAGE8_LANE_STATUS.md:52` | TYPING (discrete) |
| **P8** | `C_ref` vs `D3` — which admissible cellulation class governs | `STAGE8_LANE_STATUS.md:78` | QUANTIFIER RANGE |
| **P9** | The `N_4` fork (see R-i) | `V011:1271` vs `STAGE8_T7_D6_SPECS_REPAIR_AMENDMENT_V001.md:84` | DEFINITIONAL |
| **P10** | **Complex-vs-real selection** — "the program's long-recorded recurring gap" | `STAGE8_T7_BETA_ER_A_DISCLOSED_PREMISE_ADOPTION_V001.md:58` | BINARY, ADOPTED |
| **P11** | **The R-L2b estimate** — the Schatten-2 exponent `alpha`, with `beta` and the coincidence exponent `p` inside it. Absorbs T11's response half, D3's response side, connected extensivity, and R-L0 inside the shape-regular class | `STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md:696-700`; merge authority `STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md:86` | SCALAR exponent, proof-side |
| **P12** | **H-B**, the parent baseline hypothesis — *not* H-B0 | `STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md:1030`, with `:787-789` forbidding baseline bookkeeping as its discharge | SCALAR, proof-side |
| **P13** | `rho_res` / `rho_bar` (H-R) — may never default to 1 or any value | `STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md:725` | SCALAR, proof-side |

Plus three smaller open items I count but do not weight: `X_*(eps)` / R-L0 over the **unrestricted** D3 class (`spec:867` — kept separate from P11 only because R-L0 is strictly broader outside the shape-regular class); `Q6_UNIFORM_CELL_ADJACENCY_DEGREE_UNDERIVED`; and the region-level route question — "does the connected region object have to pass through D5's ABSOLUTE anchored sum … That, **not p**, is what decides region-level" (`STAGE8_P_COINCIDENCE_EXPONENT_DETERMINATION_V001.md:182`).

**Adopted-premise mass, not counted as free parameters but in force:** the eleven-item inherited Level-1 stack (`STAGE8_T7_BETA_ER_A_DISCLOSED_PREMISE_ADOPTION_V001.md:62-67`, under a mandatory add-rule at `:73-75` naming the register as *the* authority); the battery's own partly-overlapping premise ledger (`battery:447-460`); four adopted scope/branch restrictions (`battery:146`); the three premises under C1's "derived" tau_R (adopted saturation, separately chosen balanced calibration `p=1/2`, adopted `tau_orth = T_R`); and two narrow premises the corpus found in itself (`STAGE8_DISCRIMINATOR_CLAIM_WITHDRAWAL_V001.md:79`).

### 2B — BRIDGE / Q_spec-SIDE: type-disjoint from the kappa_record ledger (9)

`c_R`/`K_R` the finite F² deformation (§4) · the complete parent action, `S_0` vs `S_1` Pauli fork and equivalent mutations (`COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md:58, 95`) · the finite-part prescription: regulator, coincident-point extension, renormalization condition (`PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:80-83`) · the **physical field-history measure** and normalized partition functional (`FINITE_RECORD_CELL_FLUX_RESPONSE_PROTOCOL_V001.md:21`) · boundary reduction / boundary data / edge modes (`:20`) · species, masses, doublers, zero-mode prescriptions (`SOURCE_RECORD_CLOSURE_ACTION_UNDERDETERMINATION_GATE_V001.md:63`) · absolute record scale `T_R` (`BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md:27`) · the grading weight triple (`FULL_CARRIER_DILATION_SELECTOR_OBSTRUCTION_GATE_V001.md:85`) · **the identification `kappa_record = kappa_Thomson`** (`V011:2077`, listed as a *failure condition*).

### RULINGS ON FIXED-vs-FREE DISPUTES

**R-1. `c`, the action multiplier — THREE-WAY DISPUTE. RULED: neither free nor fixed. It is a DEFERRED DETERMINATION and belongs on neither side of the primitive ledger.**

- `from-parameters` counted it as a **constraint** (c = 1). `from-fences` narrowed it to "a seal-status gap." `from-battery` confirmed it **FREE** on executed evidence (`T08.json`: `selected_multiplier = null`).
- I verified the decisive facts myself. `BID_PRIMITIVE_ACTION_MULTIPLIER_DERIVATION_V001.md` has **no `.seal.sha256`** (`ls` over the corpus root: the `.md` exists, no seal file), and `grep -rl "BID_PRIMITIVE_ACTION_MULTIPLIER"` over the whole tree returns **exactly one file: its own audit script**. It is an unsealed, unmanifested, uncited orphan. It cannot be counted as sealed supply.
- **But** `from-battery`'s "FREE" is also wrong, because V011 itself carries the rule: *"The complete charged amplitude later fixes `c=1` by `Gamma_Q[A]=-i Log(Z_Q[A]/Z_Q[0])` … **Until `Z_Q` is constructed from complete `Q_spec`, the conversion from `kappa_record` to a physical action coefficient remains unauthorized.**"* (`V011:1304-1315`, verified verbatim.)
- **The distinction that matters, and it is the single most useful ruling in this report:** a parameter for which *a rule exists and its input is missing* is categorically different from one for which *no rule exists anywhere*. `c` is the first. `c_R` is the second. Conflating them makes the shortfall look uniform when it is not.

**R-2. `p` vs the Schatten-2 exponent `alpha` — RULED: ONE unknown plus ONE named proof obligation. Not two parameters.**

- `from-fences` and `from-battery` merged them; `from-parameters` split them, citing `conversion_step_exists_in_corpus = false`.
- I read both sources. `from-parameters` **over-read its own flag**: that line sits inside a block headed `tension_between_the_two_results = DISSOLVED — it was a symbol collision on "X"` (`STAGE8_P_COINCIDENCE_EXPONENT_DETERMINATION_V001.md:199-202`). It dissolves a claimed *tension* between a sandwiched and an unsandwiched object; it is not a statement that no p→alpha route exists.
- The route **does** exist in sealed text: *"p > 3/2 → C X C is Hilbert-Schmidt; combined with the aggregation arithmetic, alpha = 1 and D5's action-density form holds, flat in aspect ratio"* (`STAGE8_RL2B_COMMUTATOR_ROUTE_REFUTED_AND_TARGET_SHARPENED_V001.md:116-118`, verified).
- And the corpus itself types the seam: **`p -> alpha    disclosed at POWER-COUNTING grade, never upgraded    => GAP`** (`STAGE8_DISCRIMINATOR_CLAIM_WITHDRAWAL_V001.md:65`, verified).
- **Ruling:** a gap in a *proof step* is an obligation, not a degree of freedom that can take multiple values and move a number. Count **one** parameter (P11) and **one** named obligation (the power-counting seam). Counting two parameters double-counts one hole; counting one and ignoring the seam hides work.

**R-3. `omega_in` and the missing X→kappa_record bridge — MERGED.** `from-battery`'s deduper is right; the corpus states the identification outright: *"THIS IS WHAT omega_in IS. Link 1 needs a state to turn K_pointer into a scalar, and the canonical selection of that state is exactly what the no-go blocks in primitive Stage 8"* (`STAGE8_X_TO_KAPPA_RECORD_EXTRACTION_DETERMINATION_V001.md:180`). `from-parameters` conceded the same. One hole, two ends.

**R-4. `H-B` vs `H-B0` — SPLIT, against `from-fences` raw.** R-L5 lists them separately (`spec:1030`). H-B0 (`kappa_bal`) is genuinely retired by the exact weight arithmetic at `spec:794-800`; the register row is stale. **H-B is not**, and R-L4 at `:787-789` explicitly forbids treating the baseline bookkeeping as its discharge. `from-fences` raw deleted a live unknown — the mirror error.

**R-5. Absolute `T_R` — NOT a free parameter of kappa_record.** All three converged; the ground is S3 plus the exact scale orbit `T_R → λT_R, H_R → H_R/λ` (`BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md`). It is a gauge orbit for a dimensionless quantity. It remains a live **Part-C blocker on the bridge**.

**R-6. `c_R` is DISTINCT from `K_bare = 0` and DISTINCT from parent-action non-uniqueness. THREE items.** Sealed text is explicit: *"This is **not** the ordinary finite-`c_R F^2` counterterm objection. It survives even after an independent bare Maxwell term is forbidden: distinct parameter-free charged-record dynamics induce different finite responses."* (`COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md:73-75`, verified.) `from-fences` had `c_R` only as a downstream note and never separated the parent-action fork at all — that is the largest single omission across the three counts.

**R-7. `c_R` vs the finite-part prescription — RULED DISTINCT (against the natural merge).** They look like one freedom, and `PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:99-100` ties them ("until the physical measure and finite extension are uniquely derived, the zero-bare declaration alone does not determine the finite induced coefficient"). But `ONE_CELL_FIDELITY_ACTION_SELECTOR_GATE_V001.md:115-116` is decisive the other way: *"Nothing in the one-cell formula excludes an independent finite `c F^2` deformation **after regularization and coarse graining**."* It survives the prescription. Two items.

**R-8. `D_x` — the omission that breaks the strongest positive claim any agent made.** `from-fences` raw's headline was "21 unknowns, 21 determining conditions … THE SHAPE OF THE RESPONSE IS FORCED, NOT CHOSEN." That arithmetic silently presumes a unique primitive operator. V011:771-787 seals a **mandatory** hostile family whose continuous branch satisfies the one-record normalization for every `x ∈ (0,2)`, and every gate that would dispose of it reads false: `differential_uniqueness_gate_passed = false` (:2222), `equal_endpoint_ratio_gate_passed = false` (:2157), `equal_endpoint_ratio_inserted_into_hostile_family = false` (:2156), `Hilbert_functor_gate_passed = false` (:2220). `from-fences`' own Layer-3 principle — *you cannot overdetermine an object you have not shown to exist* — applies to its own Layer 1.

---

## 3. THE ARITHMETIC — BY TYPE

**The count does not close to one number. That is the correct answer, and the reason is structural, not bookkeeping: the constraints and the unknowns are of different types on every axis that matters, and the axis that decides alpha has zero constraints of any type.**

### Axis 1 — the SCALAR kappa_record (its magnitude)

| | Count |
|---|---|
| Constraints bearing on the value | **3** (S1 sign, S2 nonzero, S3 dimensionless) |
| Of those, fixing a **magnitude** | **0** |
| Free objects that move the magnitude | ≥ 3: P3 (envelope profile, sensitivity **proved**), P2 (`B_lambda`), P1 (`D_x` cluster) |

**Joint content of all three constraints: `kappa_record > 0` and dimensionless. Nothing more.** Two inequalities and a dimension cannot fix a positive real. **Shortfall on the magnitude axis: at least 3 — and the types do not match**, because P3 is a *functional* (a write-rate profile `w(t)`), and no number of scalar inequalities reduces a functional. The direction is forced; the magnitude is untouched.

### Axis 2 — the TENSOR M_record (its shape)

| | Count |
|---|---|
| Free symmetric components | 21 |
| Determining conditions | 21 (T1 supplies 20, T2 supplies the 21st) |
| Independent over-constraints | **5** (T3, T4, T5, `y_c = y_s`, F11) |

**Nominal surplus: +5.** This is the one axis where the p > 3/2 pattern reappears — a requirement and an independent supply meeting, with falsifiers left over. **But the surplus is conditional and the condition is not met:** the whole computation presupposes a unique primitive operator, and P1 is a live continuous family inside it with four disposing gates reading false. **Corrected verdict: the shape is forced *given* the operator; the operator is not yet forced.** Six of the seven tensor conditions (T1–T5, and T14's steps) are additionally `[X]` — never executed; no T05, T09, T12, T13, T14 or T15 report exists in `stage8_execution/t_reports/` (directory listing verified).

### Axis 3 — FUNCTIONALS (the response chain)

| | Count |
|---|---|
| Constraints | 14 + 1 equality |
| **Discharged** | **3** — F4 (T07(i) PASS), F6 (proved, exactly additive), F9 (T10, `horizontal_zero_flux_addition_dimension = 0`) |
| BLOCKED / not-executable | F2, F5, F7, F8, F11, F14 |
| Free functionals, primitive side | 2: P4 (`omega_in`/bridge), P3 (envelope) |

**Shortfall: ≥2, and again the types are wrong** — a free *state* on the source algebra is not cancelled by an existence theorem for a limit. **And the deeper point stands unchallenged by any of the three counts: you cannot overdetermine an object you have not shown to exist.** F5 and F7 are the existence proofs for the object kappa_record *is*, and both are BLOCKED. **One genuine deflation is owed to the program here:** four of the open obligations on this axis are **one estimate**, by the corpus's own ruling — *"FOUR OBLIGATIONS ARE ONE ESTIMATE … ONE CAMPAIGN, FOUR DISCHARGES"* (`STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md:86`). The functional axis is less fragmented than a naive read suggests. And F6's exact additivity means the density exists on any *fixed* cellulation with no limit and no cluster expansion (`STAGE8_RL2B_OVER_CHARTER_AND_BLIND_ROUTE_FINDING_V001.md:122-127`) — the open part is the **connected cross-cell corrections only**.

### Axis 4 — INDEPENDENCE conditions

4 constraints (I1–I4); **all four undischarged**; and I4's teeth are unverified because `stage8_execution/commitments/` and `stage8_execution/reveals/` are **empty directories** — the predeclared commitments were never made. Free objects of matching type: P7, P8, P9, P5, P6. **Shortfall: 5 discrete rulings against 0 discharged independence results.**

### Axis 5 — the BRIDGE (kappa_record → kappa_Thomson → alpha)

| | Count |
|---|---|
| Constraints of matching type | **0** |
| Free objects | **9** (§2B) |
| Assumed equalities standing in for derivations | **1** (`V011:2077`) |

**This is the axis that decides alpha, and it has no constraints on it at all.** Not one entry in §1 touches `c_R`, the parent action, the field-history measure, or the identification. This is not a small shortfall. It is an empty column.

### PLAIN STATEMENT

**Constraints ≥ unknowns is FALSE, and it fails differently on each of five axes.** The tensor axis has a surplus that is conditional on an unmet uniqueness gate. The magnitude axis has three constraints and none of them fixes a magnitude. The functional axis cannot be counted because the object's existence is unproved. The bridge axis has zero constraints against nine unknowns. **There is no single integer, and forcing one would conceal that the best-determined axis (the tensor) and the axis that decides alpha (the bridge) are not connected to each other by anything except an assumption the governing principle lists as a failure condition.**

**The honest ceiling on the constraint column:** of ~35 constraints after deduplication, **approximately 8 are actually discharged** — F4, F6, F9, the intrinsic cell measure, `kappa_bal = 1`, `K_Pauli,kin = -1/2`, the U(1) unit-character normalization, and the composition loop (T00/T03/T04/T06/T08/T10/T16 are the reports the corpus's own audit calls "SOUND AS WRITTEN"). **Everything else in §1 is a demand the program has placed on itself, and a demand is not a supply.** That distinction — which `from-parameters` applied correctly to `p > 3/2` and then failed to apply to the packing fence, and which `from-fences` applied nowhere — is the largest single correction in this reconciliation.

---

## 4. IS c_R REACHABLE BY OVERDETERMINATION?

**Answer: NO today — as a bounded negative with the boundary stated exactly. But the corpus already contains the overdetermination test in three places, it is not a new theorem, and `c_R` is NOT an independent obstruction: it is the shadow of two holes already on the ledger.** Both halves of that are load-bearing; I state them separately.

### 4A — What jointly narrows `c_R` today (the complete list)

`c_R` is defined by `S_M[A] = (ħK/4)∫F∧*F`, `ΔS_c[A] = (ħc_R/4)∫F∧*F`, `K → K + c_R` (`ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V003.md:17-20`). Everything the sealed corpus jointly imposes on it:

1. **The mutation class is ONE-DIMENSIONAL.** `c_R` multiplies the parity-even `F∧*F` invariant only. This is a real narrowing — the deformation is a single real number, not a function. (`Ledger:14-21`.)
2. **The resulting stiffness must remain positive.** *"The ordinary positive branch additionally requires the resulting stiffness to remain positive"* (`PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:94-95`), with the family scoped throughout as *"the positive-`K_R` family"* (`:66`). **This confines `c_R` to a half-line: `c_R > −K_induced`.**

**That is the entire joint narrowing. A one-dimensional class confined to a half-line.**

### 4B — The bounded negative, stated at its strongest

The corpus's non-selection statement is **already joint and already class-level** — it is not "no single principle excludes it":

> *"For every ordinary local `3+1` compact-`U(1)` continuum construction reviewed, the parity-even action admits [`K → K + c_R`]. Gauge invariance, Lorentz covariance, compactness, charge quantization, Ward identities, anomalies, and the charge-character lattice do not forbid the finite second term. They can fix charge ratios, topological data, beta-function slopes, or relative kinetic coefficients, **but not the absolute Thomson stiffness**."* — `ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V003.md:14-27`

That is **seven principles applied in conjunction to an entire construction class, returning non-exclusion.** This is the strongest form a negative can take, and it means the conjunction has already been tried. Four further joint attempts are recorded as missing, each with its own citation:

- **`K_bare = 0` does not reach it** — `COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md:73-75`.
- **The U(1) unit-character normalization does not reach it** — *"It does not determine the field-history weight"* (`PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:26-27`); *"It does not fix the absolute cost of curvature. That missing normalization is the quantity needed before alpha can be computed"* (`:104-106`).
- **The completed-history cancellation reaches order 2 and stops exactly one structural order short**: `continuum_completed_history_cancellation_order = 2`, `continuum_quadratic_F2_sector_cancelled = false` (`COMPLETE_QSPEC_..._AUDIT_V001.md:179-180`). A real cancellation mechanism exists and demonstrably misses the F² sector.
- **The one-cell fidelity route does not reach it** — *"Nothing in the one-cell formula excludes an independent finite `c F^2` deformation after regularization and coarse graining"* (`ONE_CELL_FIDELITY_ACTION_SELECTOR_GATE_V001.md:115-116`).

Six independently audited routes, all blocked: `BLOCK_ABSOLUTE` ×2, `BLOCK_CURRENT_SPECIFICATION` ×2, `BLOCK_CURRENT_BRANCH` ×1, `CONDITIONAL_PARENT_ROUTE_NOT_DERIVED` ×1 (`Ledger:37-88`). `finite_c_F2_deformation_excluded = false` in 11–13 sealed artifacts.

**Bounded negative, narrowly: over the searches named in §7, no set of sealed constraints narrows `c_R` beyond a positive half-line.**

### 4C — The finding that changes the shape of the problem

**The corpus already specifies the exclusion mechanism, and it is overdetermination, not a new theorem.** Three independent sealed statements say the same thing:

- **Failure rule:** the route fails if *"a finite `c_R F^2` term can be added **without violating the complete microscopic specification**"* — `ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V003.md:121-122`.
- **Closure condition, item 4:** *"**forbid every competing finite local response compatible with that same complete specification**"* — `PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:115-116`.
- **Failure condition:** *"an independent finite `F^2` counterterm survives regulator removal"* — `V011:2076`.

None of these asks for a no-go theorem about `c_R`. All three ask for the **specification to be tight enough that the mutation contradicts something already fixed**. That is precisely the p > 3/2 pattern: a requirement, an independent supply, and the two meeting.

**And the type structure the corpus itself assigns tells you where the supply would come from.** `c_R` is a deformation of the **public/Thomson** action. `kappa_record` is a Hessian on the **primitive record** side, and V011 lists equating the two directly as a *failure* (`V011:2068-2069`) precisely because they are distinct objects. An added `c_R` therefore moves `kappa_Thomson` and leaves `kappa_record` untouched. **It follows from the corpus's own typing — I am not supplying a premise, only reading it — that if (i) `kappa_record` is computed and (ii) `kappa_record = kappa_Thomson` is *derived* rather than assumed, then with `K_bare = 0` already adopted, `c_R` is fixed by subtraction with no further principle.** Two independent determinations of one coefficient, forced to agree.

**Therefore: `c_R` is not a separate wall.** It is downstream of exactly the two holes that already dominate every other axis of this measurement — **the value (P4/P11: no map, no estimate) and the bridge (`V011:2077`: assumed, not derived)**. That should *reduce* the principal's estimate of how many independent problems remain, and it should reprice work on the bridge: closing `kappa_record = kappa_Thomson` does not merely license a conversion, it retires the mutation class.

**Two caveats stated so this does not read stronger than its evidence.** (i) The argument requires the identification to be of **values at a stated scale**, not of roles; a weaker identification ("kappa_Thomson is whatever the record gives, up to the finite-part prescription") leaves `c_R` alive inside the prescription — and the finite-part prescription is itself an uncounted freedom (§2B, ruling R-7). (ii) `ONE_CELL_FIDELITY_ACTION_SELECTOR_GATE_V001.md:115-116` establishes that `c_R` survives regularization *and coarse graining*, so finiteness of the cell alone does not kill it; the killing has to come from the agreement, not from the finiteness.

---

## 5. WHAT WOULD CLOSE THE SHORTFALL

Per remaining free parameter: **[IMPLIED]** = plausibly already forced by a sealed constraint, needing only execution or a proof; **[PREMISE]** = a genuinely new premise or principal act is required. I supply none.

**Plausibly already implied — the work is to execute or prove, not to decide:**

- **P1 (`D_x` and the hostile family)** — [IMPLIED]. I2 (`V011:804-806`) is exactly the constraint written to kill it, and the closed-boundary/naturality conditions are already preregistered (`V011:785-787`). This is a gate that has not been run, not a decision that has not been made. **It is also the highest-leverage unrun gate in the corpus**, because the tensor axis's +5 surplus is conditional on it and on nothing else.
- **P11 (R-L2b)** — [IMPLIED-if-true]. The threshold `p > 3/2` is derived; the target is sharpened to a single question (`COMMUTATOR:113-118`); two candidate mechanisms are named and unattacked (`:124-132`). But the corpus's own count is `mechanisms_claiming_the_object_s_p = 0 of 7`. It may be true and unprovable by present means; nothing suggests a premise is *missing*.
- **P9 (`N_4` fork)** and **P8 (`C_ref` vs `D3`)** — [IMPLIED]. Both are reconciliations between two sealed authorities about one object. A ruling, not a premise.
- **P12/P13 (H-B, `rho_res`)**, **`X_*(eps)`** — [IMPLIED]. Bound constants inside one obligation; they are finiteness hypotheses, not value axes.
- **`c` (the action multiplier)** — [IMPLIED, and already ruled]. The rule exists (`V011:1304-1315`); its input `Z_Q` does not. Nothing else is needed. Secondarily, sealing and citing `BID_PRIMITIVE_ACTION_MULTIPLIER_DERIVATION_V001.md` would close it at the primitive gate — that is a **custody act**, not a premise.
- **`c_R`** — [IMPLIED, conditionally] — see §4C. Implied by the conjunction of a computed value and a derived bridge; not implied by anything currently in force.

**Genuinely required — a new premise or a principal act:**

- **P3 (the envelope profile ER-A/ER-B)** — [PREMISE]. The fork was executed and returned `ER_FORK_NOT_RESOLVED_AT_FINITE_LANE_PRECISION`; `envelope_realization_derived = false`, `ER_A_selected = false`, `ER_B_selected = false`. Sensitivity is **proved**, not feared. This is the strongest surviving object-side determinant of the magnitude, and nothing in the corpus selects it.
- **P4 (`omega_in` / the bridge)** — [PREMISE]. A sealed no-go: no normalized tracial state exists on the infinite source algebra, and smaller covariance groups leave *multiple* functionals (`STAGE8_X_TO_KAPPA_RECORD_EXTRACTION_DETERMINATION_V001.md:171`). A no-go is not a gap to be filled by effort.
- **P5 (transport rule), P6 (atom-or-diamond), P7 (carrier typing)** — [PREMISE / principal act]. Each is explicitly reserved: *"CHOOSE WHICH SEALED REQUIREMENT TO RENEGOTIATE, OR ACCEPT THREE NEW OBLIGATIONS INSTEAD… that is why it is the principal's"*; *"THIS LANE DOES NOT CHOOSE"*; *"No horn delivers R-L2b as written."*
- **P2 (`B_lambda`)** — [PREMISE]. Global boundary descent narrowed it and its own status *"leaves connected preparation, time-dependent continuum ordering, and the thermodynamic domain open."*
- **P10 (complex-vs-real)** — [PREMISE]. Named by the corpus as its own long-recorded recurring gap.
- **The bridge column (§2B) minus `c_R`** — [PREMISE], every entry. The parent action requires a derived minimality theorem or a derived total-space geometric action (`COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md:83-88`); the field-history measure is **not** closed by the derived intrinsic cell measure (different measures — this is the most natural way to fake closure here and all three deduping agents correctly refused it).

**Count: ~7 [IMPLIED], ~10 [PREMISE], with the [PREMISE] mass concentrated on the two axes that decide the magnitude and the bridge.**

---

## 6. CONFIDENCE, AND THE DOMINANT ERROR RISK

**How much rests on judgement about independence:** substantial, but unevenly, and I can separate it.

- **Eight merges rest on explicit corpus statements, not my judgement**: the packing fence into cellulation independence (`battery:155-156`); four obligations into one estimate (`EXTENSIVITY_VERDICT:86`); five pins into one decision (`TRANSPORT_CHARTER_OPTIONS:12, 86`); `omega_in` into the bridge (`X_TO_KAPPA:180`); `tau_R`'s three sites; `K_bare=0`'s three labels; C7/C8 (flags inside the same artifact); C3/C4 (one sentence). Confidence here: **high**.
- **Four rest on my judgement**: the p/alpha ruling (R-2), `c_R` vs the finite-part prescription (R-7), treating the `D_x` family as one cluster because one gate disposes of all members or none, and the inherited premise stack as one entry. Each is flagged and each is reversible; reversing all four moves the parameter count by +3/−1.
- **Verbatim accuracy: high.** I re-read the primary text behind every disputed claim and found no fabricated quote in any of the six reports. Two *descriptions* overreached their quotes (`from-fences`' "exactly TWO" value-side constraints; `from-parameters`' gloss of `N_4`), and both are corrected above.

**THE DOMINANT ERROR RISK IS ON THE CONSTRAINT SIDE, AND IT IS NOT DUPLICATION — IT IS STATUS INFLATION.** The brief warned about counting one constraint twice. The bigger hazard here, which all three raw counts committed and only one deduper systematically caught, is **counting a demand as a supply**. Most of §1 consists of obligations the program has placed on itself and has not discharged: `volume_uniform_zero_free_neighborhood_proved = false`, `connected_linked_cluster_density_proved = false`, `cellulation_independence_proved = false`, `uniform_locality_test_passed = false`, `unique_flux_lift_proved = false` (contradicted by T10's pass — a further status conflict), `differential_uniqueness_gate_passed = false`, `Hilbert_functor_gate_passed = false`, `source_free_Maxwell_energy_response_proved = false`. **If you re-run the arithmetic keeping only discharged constraints, the constraint column collapses from ~35 to ~8, and every axis except the frozen benchmarks goes negative.** That, not any merge dispute, is where this measurement is most likely to be wrong in the program's favour.

**Second risk: the registers are not a census.** `STAGE8_REGISTER_COMPLETENESS_AUDIT_GAP_REPORT_V001.md:18-31, 297` reports 18 missing ruled decisions as a "LOWER BOUND, NOT A CENSUS", two stale rows, three false-present defects, and a sweep that died mid-stream. This was borne out by direct measurement in this exercise: one deduping agent found eleven further adopted premises by reading four lines past a register another agent had already opened. **The residual risk is asymmetric and unfavourable** — an unindexed decision is more likely to have *opened* a parameter than closed one, since most missing items are principal acts and open-side rows.

**Third: I did not verify seals cryptographically.** I checked file presence, `.seal.sha256` presence, and reference graphs (which settled the `c` dispute decisively). I recomputed no hash and checked no manifest membership.

---

## 7. ESTABLISHED / BOUNDED NEGATIVE / UNRESOLVED

### ESTABLISHED

1. **kappa_record is not currently a defined quantity.** Its existence prerequisites are BLOCKED (`T07.json`: T7(ii) BLOCKED, T7(iii)-connected BLOCKED, T7(iv) NOT_EXECUTABLE) and no map from the constructed operator to any scalar exists (`BRIDGE_ABSENT`, `X_TO_KAPPA:153`; `kappa_record_computed = false` across ~40 status blocks). It is therefore **strictly neither over- nor underdetermined**; the measurement is of the ledger, not of a number.
2. **Exactly three sealed facts bear on the scalar's value, and none fixes a magnitude**: `> 0` (S1+S2) and dimensionless (S3).
3. **The tensor axis is the only axis with a surplus (+5), and that surplus is conditional on an unrun uniqueness gate** (P1, `V011:771-787`; four disposing gates false).
4. **`c` is a deferred determination with a stated rule** (`V011:1304-1315`); **`c_R` has no rule anywhere** (§4B). The two are categorically different and were conflated by two of the three counts.
5. **`c_R` is not an independent obstruction** — it is downstream of the value and the bridge (§4C).
6. **Six of seventeen battery obligations were never executed** (no T05, T09, T12, T13, T14, T15 reports — directory listing verified), and **three that report `pass=true` route BLOCKED as over-claimed** (T01, T02, T11; `PRINCIPAL_RULINGS:87-89`).
7. **`stage8_execution/commitments/` and `stage8_execution/reveals/` are empty** — the anti-tuning protocol's predeclared commitments were never made.
8. **The p > 3/2 precedent does not read as the brief states it.** The threshold is genuinely forced by cell-counting; **the supply side is absent** — `p_known = false` (`COMMUTATOR:159`), `answer = UNDETERMINED_BY_SEALED_TEXT`, `mechanisms_claiming_the_object_s_p = 0 of 7`, and the corpus corrects its own count of closed mechanisms from seven to five (`P_COINCIDENCE:209-210`). **What the precedent demonstrates is that this program can force a *threshold*. It has not yet forced a *value* against one.** All three deduping agents converged on this independently and I verified it.

### BOUNDED NEGATIVE (searches named, stated narrowly)

- **No sealed artifact assigns kappa_record a numeric value, bound, range or magnitude.** Searched `kappa_record` filtered on `must|forced|bound|magnitude|range|>=|<=|positiv|nonzero` across `*.md`; the corpus's own bounded search over `.md`/`.json`/`.py` reports the same.
- **No sealed text asserts kappa_record is rational.** Corpus-wide grep for `exact-rational|exact rational` returns 20 files; **every occurrence bearing on kappa_record qualifies the ENCLOSURE ENDPOINTS**, not the number — the result schema reads `"kappa_record_enclosure": {"lower": "exact rational string", "upper": "exact rational string"}` (`battery:425`; identical at `AUTHORITY_FABLE_V002:396`). An enclosure with rational endpoints contains irrationals. **This bears directly on the standing UNCHECKED observation in project memory (`project_exact_rational_observation.md`): the corpus does not supply the rationality that observation would need.** §N and §E1' item 7 permit rationals, pi, sqrt(2), zeta values and e, all with certified outward enclosures.
- **No set of sealed constraints narrows `c_R` beyond a positive half-line** (§4A/4B; searches: `finite_c_F2_deformation_excluded`, `c_R`, `field-history weight`, `absolute stiffness`).
- **No prior overdetermination measurement exists in the corpus** — `grep -i overdetermin` hits only the vendored `.proof_deps/sympy` tree.
- **Not searched:** the thirteen `CURRENT_AUTHORITY_LEDGER_V0xx.json` files; `review_packets/`, `results/`, `provenance/`, `tests/`; the ~2,000 `.py` files beyond three audit scripts. Both columns are lower bounds.

### UNRESOLVED (and belonging to the principal, not to a lane)

1. **Whether Stage 8 emits kappa_record at all.** Two sealed authorities give opposite terminal conditions — *"THESE CANNOT BOTH HOLD"* (`X_TO_KAPPA:199`). **If the source-scalarization no-go's architecture wins, Axis 1's shortfall is not a Stage-8 shortfall at all: it moves wholesale into complete Q_spec, and the honest Stage-8 count becomes Axis 2 alone — which closes, conditional on P1.** That single ruling changes the reported answer more than any other open item.
2. **Whether the p→alpha seam is an obligation or a second unknown** (my ruling R-2 is defensible, not certain).
3. **Whether the `N_4` fork is live or merely stale in the battery spec** (D-3's equivalence is skeleton-local; the open direction is where it fails).
4. **The transport rule, the atom-or-diamond unit, the carrier typing, and the `C_ref`/`D3` quantifier range** — four principal acts, none of which a lane may take.
5. **Whether `unique_flux_lift_proved = false` (V011:2213) or `T10.json` PASS is authoritative** — a status conflict neither deduping agent reconciled.

---

**THE COSTLY ANSWER, STATED PLAINLY BECAUSE IT IS TRUE.** The count cannot close, and the reason is not that it is short by a countable number of premises. It is that the corpus's constraints and its unknowns live on five axes of different type, the best-constrained axis (the tensor) is conditional on a gate nobody has run, and the axis that actually decides alpha carries **nine free objects and zero constraints**. The one genuine piece of good news is structural rather than numerical: **`c_R` — the mutation class every route has terminated at — is not a separate wall. Its exclusion mechanism is already written down in three sealed places, it is overdetermination rather than a new theorem, and it requires exactly the two things everything else requires: a computed value and a derived bridge.** That reprices the bridge from "a conversion step" to "the act that retires the mutation class," and it is the only place in this measurement where work on one item closes two.