# Principal Disposition — Gamma Scoping Accepted; Kill-Test + Parity Lemmas + Beta
Brian, 2026-07-25 (night). Recorded verbatim in operative content.

1. RUN THE KILL-TEST FIRST (cheap, decisive): exact symbolic Xi_A - Xi_B on
   the sealed three-site regression fixture, Route-1 comparator cross-check,
   all six Section-4 controls. Frozen prediction NONZERO stands, not
   revisable. Verdict rule: nonzero => GAMMA DEAD, BETA; exact zero =>
   mechanism-identification lane BEFORE any derivation spec. Rationale:
   converts "dead by argument" into "dead by computation"; calibration data
   on a frozen prediction either way. Do not skip.
2. SEAL THE TWO PARITY LEMMAS as standalone results, independent of the
   fork (unconditional yield: harden the executed gate; V002 stencil proven
   exactly odd-order-free; ~half the stencil error budget deleted in every
   successor).
3. BETA ADOPTED contingent on nonzero: ER-A as DISCLOSED PREMISE;
   conditionality clause on every headline downstream of kappa_record;
   recorded as a SECOND disclosed premise alongside complex-vs-real with
   the cumulative conditionality in ONE place. Scoping: any future closure
   is "ER fork closed FOR kappa_record", never globally; finite-a consumers
   (Q_spec, EM order steps) reopen the fork there.
4. ADOPTION-WATCH ANSWERS:
   (5.1) Reparametrization/equal-action/covariance principle — REFUSED;
         would re-adjudicate the demoted intrinsic-measure selector; if
         ever needed, standalone adoption decision to Brian, never inside
         a gamma/fork artifact.
   (5.2) ER-B battery obligations — DO NOT ADD; leave "ER-B not used and
         not silently excluded" as is; beta artifact must record ER-B as
         unexcluded but battery-undefined.
   (5.3) L1 (per-cell-to-K multi-cell lemma) — acknowledged as NEW NAMED
         OBLIGATION; add to the named blocker list; do NOT commission for
         gamma; assess at T7(iii) spec time whether the linked-cluster
         density needs it independently, and say plainly.
   (5.4) L2 (completed-vs-exhaustive typing of the intensive Hessian) —
         FREEZE NOW AT SPEC LEVEL, HIGH PRIORITY, upstream of remaining T7
         work; the Duhamel memo's H_K = g_{D,K} anchor (exhaustive) vs
         kappa_record's Gamma (completed) mismatch must not survive into
         T7(iv); substitution forbidden, decision must be explicit.
5. CRITICAL PATH UNCHANGED: v004 -> reviewer re-audit -> typed
   authorization -> Phase-A production. Report when v004 ready (it is —
   package with reviewer).
6. DIRECTION NOTE (not an instruction; no action without a spec): the toy
   result that kappa depends on the full cumulative profile sigma(.) — the
   record retains HOW action accumulated, not merely how much — is worth
   recording as an observation of record in the beta artifact. It reframes
   the fork as physical (how action accumulates on a causal diamond), and
   the eventual discharge route as DERIVING the write-rate from the record
   principle — a Brian-level scoping question for later, explicitly not
   opened now (same seductive shape as gamma, which died).
