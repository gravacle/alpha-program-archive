## PASTE 695 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / TRANCHE] V008 ENVELOPE CHECK + INDEPENDENT INTEGRATION: ONE WHOLE DELTA

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. THE FIRST-TIME-RIGHT RULES apply: pins generated never transcribed; interfaces are schemas; no unexecuted path ships; state changes ship whole. Builder A's V008 package is mirrored to archive: spec V008 = 3b24fc39…, A's report c597153e…, package inventory 1e89b685….)

CONTEXT: Q-611's three spec statements are now V008 text. A emits the seven-field carrier your contract enforces; A's parent validates the extended manifest schema and passes `--subject-manifest/--subject-manifest-sha256/--evidence-manifest/--evidence-manifest-sha256`. A constructed only a NON-AUTHORITATIVE test-fixture manifest instance; the real instance is yours.

TASK:
I1. ENVELOPE-CHECK V008 (your V006/V007 role): confirm the delta is the three Q-611 statements and mechanical carriage ONLY — 66 descriptor rows byte-identical, no criterion drift, closed schemas not prose, `PRECONDITION_NOT_REPLAYABLE` distinct from criterion-FAIL and naming the missing carrier. Anything beyond the three statements is a finding, not a thing you accommodate.
I2. INTEGRATE INDEPENDENTLY: author the real `rd22.verifier-manifest.v001.json` instance with the V008 `argv` and `input_roots` inventories; implement R9's P0 computation from the two supplied manifests (all six §2.1 conjuncts; a producer-emitted P0 object remains a fault; unevaluable conjunct → `PRECONDITION_NOT_REPLAYABLE` naming the carrier, never FAIL); re-pin your verifier_root and seal the instance.
I3. Dry-run every new path (FIRST-TIME-RIGHT rule 3 — run it, don't read it; your own 2.4 precedent). PIN CHECK grep value AND name for the superseded root fd59672a… and V007 references in your custody.
OUTPUT: updated verifier package + one sealed artifact
`STAGE8_TASK6_V008_INTEGRATION_DARIO_V001.md`
with final lines: `ENVELOPE = three statements only / (+findings)`, `ROWS_CHANGED_CONFIRMED = 0`, `P0_REPLAY = implemented, executed`, `MANIFEST_INSTANCE = authored, sealed`, `VERIFIER_ROOT = <new hex>`, `PIN_CLOSURE = N hits, all resolved`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors and invokes run 031). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
