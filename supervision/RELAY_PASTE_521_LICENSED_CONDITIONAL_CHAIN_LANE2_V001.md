## PASTE 521 — CODEX LANE 2 (HIGH EFFORT) — [TASK 5] THE LICENSED CONDITIONAL CHAIN: EVERYTHING DoR-020 PERMITS, TAGGED [EQ6]

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-445. DoR-020's ratification EXPRESSLY LICENSES downstream conditional derivation with every result condition-tagged; its prohibitions (no member binding, no fixed-point execution, no end test) STAND ABSOLUTELY — this chain computes structure conditionally, never executes the protected steps.
- Authorities (verify hashes): the conditional Banach theorem `88854f08…` and its premise ledger `feb84216…`; the carrier build `aebe708d…`; the threshold theorem `b569a89e…` (contraction iff |χ_K| < A_loop⁻¹); the return-certificate interface `d61a550a…` (the excision cert; the certificate family); the reader family `bae34116…`; DoR-020 + A1; Q-401–Q-445. This runs in parallel with 520 (the section dig in Codex 1), blind to it.
- All symbolic; target-blind; EVERY line tagged [EQ6].

TASK — the chain, conditionally:
L1. **The return certificate, conditionally inhabited:** under [EQ6] (the certificate family + the built structures as the conditional inhabitant), execute the conditional theorem's discharge — composite boundedness and closure as [EQ6]-tagged theorems.
L2. **A_loop's exact symbolic form** on the built carrier with the conditional inhabitant — the threshold inequality |χ_K| < A_loop⁻¹ made fully explicit in the program's symbols (p, ν, the reader coordinates), still no values.
L3. **The conditional fixed-point structure:** existence/uniqueness under [EQ6] + the threshold, as one clean conditional theorem; the sensitivity equations (the parameter-derivative system for the three freedoms) SET UP symbolically — not solved where solving requires execution, but reduced to their final computable form.
L4. **The witness-to-number map:** the exact ordered list of what remains between a certified [EQ6] witness and the licensed computation — each step, its consumer, its authorization gate (A32, the evaluation DoR) — the endgame's final checklist.
L5. Falsifiers on everything computable; anti-tuning; the tag ledger complete.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_LICENSED_CONDITIONAL_CHAIN_LANE2_V001.md`
with a lead-result block (`BOUNDEDNESS/CLOSURE = [EQ6]-THEOREMS / STOPPED`; `A_LOOP = <symbolic form>`; `CONDITIONAL_FIXED_POINT = STATED`; `WITNESS_TO_NUMBER = <n steps listed>`). Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
