## PASTE 770 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / Q-126 ROUTE] THE FLUX CORRESPONDENCE, BUILT TO THE STATED SCHEMA

PICKUP-ACK + LANE GUARD in force (DARIO). (Execute AFTER 768 — strict sequence: 766 -> 768 -> 770.)
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL standing laws apply. GOVERNING: Q-672 — the sealed V003 premise; the identity bar with its unproved-transport discipline; the 757 correspondence pattern; the VOID condition.)

TASK — build the S2_flux/Q_Sigma typed correspondence, or find it underivable, to the schema 769 displayed:
AS1. Type and direction; domain and codomain; THE DEMONSTRATED OVERLAP derived (what sealed structure the two objects provably share) AND the complement displayed (what they provably do not); licensed quantifiers; orientation and normalization; UNIQUENESS proved or its failure displayed; the correspondence's OWN falsifiers stated; and THE EXACT FLUX-TRANSPORT CONSEQUENCE — what V003's zero-or-one sector forces about deg L and |q| THROUGH the correspondence, derived not asserted.
AS2. Every step from sealed bytes (block-covered; meaning-probed); the identity never asserted; no property transported unproved; if any schema element is underivable from sealed stock, the package stops there with the element named — the principal sees the stop, not a stretch.
AS3. If the package derives whole: state the bundle-class consequence and the corrected free-data ledger (5 -> 4, with the falsifier's antecedent status re-examined). VOID clean throughout.
OUTPUT: one sealed artifact `STAGE8_7A_FLUX_CORRESPONDENCE_DARIO_V001.md`
Final lines: `CORRESPONDENCE = BUILT (schema complete; overlap + complement displayed) / STOPPED (element named)`, `UNIQUENESS = proved / failure displayed`, `FLUX_CONSEQUENCE = derived (|q| forced) / n-a`, `LEDGER = updated / unchanged`, `VOID = clean`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 770_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants. No common cell formed; no junction map evaluated; no smooth data derived.
