PASTE 356 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] BUILD P4 — THE PhysicalLogGerm ON P2'S CALCULUS (CONSTRUCTION)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-273)

```text
DOES THE OBJECT EXIST?   SPECIFIED as P4 in the Q-254 spec (241bf43f..., section :250-269):
                         the PhysicalLogGerm. NOW BUILDABLE: DoR-014's germ + Q-273's P2
                         (dense finite core, Frechet calculus, differentiation commuting
                         with restriction, Tail_src = {0}).
IS THE VERSION CURRENT?  YES through Q-273. Marks: TYPE-P | premises: DoR-008/009/013/014.
                         Rank discipline binds (no specific (r_0, r_ch) value, ever).
ARE ITS INPUTS PRESENT?  YES: the ratified germ's Z_inc with its exported derivatives, P2's
                         topology/calculus, the Q-245 finite cylindrical arrow (A_fin^p and
                         its exact finite derivatives), Q-243's finite Keldysh structure.
```

## 1. THE TASK — THE LOG GERM, BUILT NOT ASSUMED

```text
1  READ P4's contract from the Q-254 spec: what the PhysicalLogGerm must provide to P5
   (the raw correlator / Hessian chain). Quote the consumer signature.
2  CONSTRUCT the log germ: Gamma_phys = log Z_inc on P2's domain -- existence and
   branch handling FIRST (Z_inc's zero set in the physical domain: locate it or prove
   it empty on the domain of definition; a log without its branch analysis is not a
   construction), then the derivative tower via P2's calculus, verifying the exported
   finite derivatives (dGamma|0 = -i p_A form, the d/d Hessian) are RECOVERED as
   restrictions -- the DoR-008 falsifier's bite at this package.
3  CERTIFY: Q-243's finite Keldysh rotation embeds (the finite mixed retarded block is
   ZERO and p_ch-free -- the physical log germ must reproduce this on restriction,
   exactly); differentiation-commutes-with-restriction carries through the log; the
   symbolic p_A dependence (the rank-ratio form) flows through UNEVALUATED.
4  KILL-PASSES: no branch chosen where the analysis leaves freedom (type it); no domain
   quietly restricted to dodge a zero of Z_inc (report the zero set honestly); no rank
   selection; no measure/contour (still P3's job).
DELIVERABLE: P4 as a certified construction -- domain, branch analysis, derivative tower,
restriction certificates, consumer-readiness for P5. TYPE-P positives expected.
```

## 2. LEADS

```text
IF P4 BUILDS CLEAN -- lead with consumer-readiness: what P5 can consume immediately, and
whether the P5 chain (raw G -> H_R -> response) now has ALL its analytic inputs.
IF Z_inc HAS ZEROS IN THE PHYSICAL DOMAIN -- lead with the zero set: where the log fails
is physics (record-degeneracy loci), not an inconvenience; characterize it.
IF A BRANCH FREEDOM SURVIVES -- lead with it typed; it goes on the record before P5
depends on it.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
