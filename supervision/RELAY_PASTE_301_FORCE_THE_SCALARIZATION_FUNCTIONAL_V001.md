PASTE 301 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2d] RUN THE FORCING PROTOCOL ON THE SCALARIZATION FUNCTIONAL

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-218)

```text
DOES THE OBJECT EXIST?   AS AN INSTANTIATED FAMILY: the second kill-pass (Q-211) found "AT LEAST
                         TWO INEQUIVALENT DISTINGUISHED" positive B -> C functionals on the
                         ratified module. Q-218: the U2 state port needs exactly one.
IS THE VERSION CURRENT?  YES -- this is the direct continuation of 300's stop.
ARE ITS INPUTS PRESENT?  The ratified module (DoR 008) . U1_008's conventions . the declared
                         rho_pre (positive, trace-class, Tr = 1) waiting to be placed.
```

## 1. THE PROTOCOL

```text
STEP 1  DECLARE THE FAMILY: every positive B -> C functional on the ratified module satisfying
        the DATA conditions (positivity, compatibility with the module structure). START from the
        kill-pass's two distinguished candidates -- NAME THEM EXPLICITLY -- and state whether the
        family is exactly those two, a larger discrete set, or a continuum. *** "AT LEAST TWO"
        WAS A LOWER BOUND, NOT A CENSUS. THE CENSUS IS STEP 1. ***
STEP 2  EQUIVALENCE: functionals equal up to the sealed conventions (U1's reality involution,
        normalization) are one.
STEP 3  FAILURE-CAPABLE CONSTRAINTS, each with a named potential victim:
        (i)   rho_pre PLACEMENT: the functional must realize the DECLARED pre-state -- positive,
              trace-class, Tr = 1 -- on the joint carrier;
        (ii)  ONE-CELL RESTRICTION: the induced state on a single record cell must reproduce the
              sealed one-cell structure (the finite authority -- Q-213's passing check extends);
        (iii) BRANCH/CTP CONSISTENCY: forward/backward compatibility under U1's involution;
        (iv)  the inclusive-identity normalization (the module identity built in 300).
STEP 4  COVERAGE of the declared family.
STEP 5  SURVIVORS. 1 -> *** THE STATE PORT OPENS, DERIVED-GIVEN-DoR-008; place rho_pre and say
        what U2 still lacks. *** 2+ -> the residual, sized and parametrized -- the principal gets
        an honest ask. 0 -> the constraints conflict with the declared rho_pre; THAT would bear
        on v004's own declaration and must lead.
```

*** THE Q-183 RULE STANDS. AND DO NOT SCALARIZE ANYTHING BEYOND PLACING THE DECLARED STATE -- no
expectation values, no spectra, nothing evaluated. ***

## 2. LEADS

```text
IF ONE SURVIVES -- lead with the derived placement; the state port opens and U2 resumes.
IF THE FAMILY IS A CONTINUUM -- *** LEAD WITH THAT: the "two distinguished" framing was
misleading and the ask is not discrete. ***
IF ZERO SURVIVE -- lead with the conflicting pair; v004's rho_pre declaration itself is then
under pressure.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
