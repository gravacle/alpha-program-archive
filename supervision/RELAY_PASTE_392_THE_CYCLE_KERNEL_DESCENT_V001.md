PASTE 392 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THE CYCLE-KERNEL RESPONSE AND 2PI DESCENT — CONNECT THE THREE CARRIERS BY DERIVED MAPS (THE VERDICT'S LAST NAMED OBJECT)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-309)

```text
DOES THE OBJECT EXIST?   NO -- and it is the LAST named object before the verdict (Q-309):
                         the derived dynamics-bearing maps connecting the three cycle
                         carriers -- sequential relative-history Z_N, the composition-loop
                         phase Phi_c (V005), and the bilocal probe R -- so that the finite
                         support separation (all kernel blocks ZERO, p ABSENT, TYPE-P)
                         becomes or fails to become PHYSICAL cancellation.
IS THE VERSION CURRENT?  YES through Q-309. Marks: TYPE-P | premises: the full stack.
ARE ITS INPUTS PRESENT?  THE BRIDGEHEADS ARE SEALED: (a) dPhi_c = u_c -- V005's own
                         differential law for the cycle phase (verified twice); (b) Z_N =
                         prod z-bar z -- the influence functional's phase IS relative-
                         history cycle content on the doubled contour (the CTP doubling
                         makes a forward/backward pair a CLOSED loop -- check whether
                         Z_N's carrier is the CTP-closed cycle, which would make Z_N a
                         Phi_c INSTANCE, not a different carrier); (c) Q-279's dephasing
                         weight -- the law's response to the bilocal probe on equal
                         histories; (d) T_cyl's colimit maps; (e) the kernel subspace
                         built at Q-309 (closed, dense finite core).
```

## 1. THE TASK — THREE BRIDGES, THEN THE DESCENT, THEN THE VERDICT

```text
1  BRIDGE Z_N <-> Phi_c: test the CTP-closure identification -- the doubled contour's
   forward/backward pair closes into a loop; is Z_N literally the composition-loop phase
   of that CTP cycle under V005's own definitions? PROVE or refute with the sealed
   definitions (no new maps if the identification is definitional -- the best outcome).
2  BRIDGE (Z_N, Phi_c) <-> R: the bilocal probe's cycle-sector action -- from Q-279's
   exact probe-on forms, derive R's action ON the kernel subspace (the finite mixing was
   ZERO -- derive WHY from the carriers: is R's coupling structurally orthogonal to
   cycle content, and does that orthogonality LIFT through the completed kernel
   subspace's density?).
3  THE 2PI DESCENT: with the carriers connected, descend the law's second variation to
   the cycle-kernel sector at the physical level: the kernel-sector RetHess candidate,
   its mixing blocks against the q(1-q) complement RE-DERIVED at the physical level
   (the finite zeros must LIFT or fail-to-lift by proof, not assumption -- the density
   of the finite core in the completed kernel is the lifting engine; Q-247's separation
   discipline names the topology).
4  *** THE VERDICT: if the mixing zeros LIFT, then p is confined to the complement at
   the PHYSICAL level and the support separation IS the cancellation mechanism --
   CANCELLATION, with the mechanism: the physical response lives on cycle content, and
   p-weight has no support there, finitely or physically. If a mixing survives the
   lift, its exact form is p's entry. If a bridge fails, the exact failed bridge. ***
5  RESTRICTION checks throughout; six-account rows; door flags; no deletion of
   record-visible content; no selection; no evaluation.
DELIVERABLE: the three bridges (proved/refuted), the 2PI descent, and THE p-VERDICT with
mechanism -- or the exact remaining object.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
