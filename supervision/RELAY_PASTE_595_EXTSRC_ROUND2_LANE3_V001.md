## PASTE 595 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5 / STEP 4] EXTSRC — ROUND 2 OF THE TIMEBOX

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-524.
- Your round-1 artifact: `alpha-program-archive/workspace/STAGE8_TASK5_EXTSRC_BUILD_LANE3_V001.md`, SHA-256 `075a2b16e333ae0baad38f38d7a458290e5efacebf4f9b53470ef4177b8b0d97` — execute YOUR OWN Section-5 round-2 plan (the quotient-domain/current-landing, relation, topology, addressed-factor, and generator-base attacks), each attack its own displayed section, display-or-stop.
- PLUS ONE REGISTRAR-SUGGESTED ATTACK (verify its lawfulness yourself; a suggestion is not law): the A_G section gap — DoR-019's ratified metric supplies the canonical minimal-norm/orthogonal section of an onto map between metric carriers (the orthogonal complement of ker A_N is derived structure; the section is metric-canonical and isometry-covariant; NO selection — the metric selects). Attempt the A_G bridge via this section: b_N/e_N built from the metric's orthogonal structure, the covariance displayed, the cross-sector unit declared where crossed. If it fails, the exact reason (e.g., the section not landing in the lawful domain; covariance failing on some arrow class).
- Standing rules: the cross-sector-unit row; surface anchor; dependency re-audit; F_PLDEC; honest stop = success.
- Custody: you build; the timebox checkpoint follows round 2 if stopped again (2 of 4 rounds then spent — the principal sees the state).

TASK: execute the plan + the suggested attack; consequence board; battery.
Final lines: `EXTSRC = INHABITED / STOPPED (+per-attack gaps, +round-3 plan or checkpoint recommendation)`, `METRIC_SECTION_ATTACK = FORMED / FAILED (+reason)`, `CROSS_SECTOR_UNIT = declared / not_applicable`, `VERB_AUDIT_SELF = CLEAN / (+items)`.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EXTSRC_BUILD_LANE3_V002.md`
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
