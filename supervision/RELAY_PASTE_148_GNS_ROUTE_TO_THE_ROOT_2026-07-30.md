PASTE 148 — LANE 1

2026-07-30, CODEX LANE 1. Snapshot; later rulings override. Lane 2 is idle after the slot-16 audit — do not touch slot-16, Thomson-matching, slot-18, A32 or comparator artifacts.

Your coverage pass is verified (`dfc0d06b…`): no CONTRADICTED rows, nine NOT REFLECTED, amendment text recorded, and your detector recommendation is the right shape — **mechanical stale-coverage tracking, not a semantic "does this bear" checker.** That distinction is why it will work.

**ONE ITEM, AND IT IS A CONSTRUCTION ATTEMPT WITH A SPECIFIC ROUTE. F-RP1 may have fired against the wrong target.**

Fences unchanged. `alpha_computed = false; proof_authorized = false`. Never touch `a32_holdout/custodian_private/`.

---

### THE HYPOTHESIS — STATE IT AS ONE AND TRY TO KILL IT

**F-RP1 fired because the declared inputs had no constructive content for the Hilbert space and `rho_pre`. But the declaration may have pointed at a WITHDRAWN ROUTE rather than at what the corpus has.**

`STAGE8_T7_CONTINUUM_GALERKIN_PROVENANCE_CORRECTION_V001.md` withdrew a specific construction and kept a specific result:

```
continuum_covariance_formula_derived               = true
momentum_block_covariance_regression_valid         = true
genuine_finite_rank_continuum_restriction_constructed = false
parent_state_regulator_restriction_derived         = false
```

**What failed was the finite-rank family** — a nested `Q_n` on `L2(R^3;C^4)` with `[Q_n,h_0]=0` and `Q_n -> I` strongly, which "does not exist for the free massless Dirac multiplier" because a nonzero finite-dimensional reducing subspace would need an `L2` eigenvector of a purely continuous spectrum. **That is a spectral impossibility, and it is a fact about approximation schemes, not about the state.**

**WHAT SURVIVED IS THE COVARIANCE**, `C(p) = (I - h_0/|p|)/2`, derived.

**AND THE CORPUS ITSELF STATES THE GOVERNING FACT, TWICE:**
- `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md:228` — "The asymptotic quasifree state is **fixed by** the positive/negative spectral [projection]."
- `STAGE8_T7_PARENT_STATE_REGULATOR_RESTRICTION_SPEC_V001.md:65` — "The finite quasifree state is the CAR restriction **determined by** `C_n`."

**A QUASIFREE STATE IS DETERMINED BY ITS COVARIANCE. THE CONTINUUM STATE THEREFORE NEEDS NO FINITE-RANK FAMILY AT ALL** — it is the Fock/GNS construction over the one-particle space carrying `C(p)`. The machinery is present: GNS in 88 cleanroom files, quasifree in 58, CAR algebra in 10, Bogoliubov in 4.

**AND ALLOW/REQUIRE SUPPLIES THE OTHER HALF.** The covariance gives the CONSTRUCTION — the algebra, the representation, the state. Allow/require gives the SELECTION — `rho_pre` is the state on the near side of the durability threshold. Neither yields `rho_pre` alone; together they may.

### THE TASK

**Attempt the root by this route, and report honestly whether it closes.**

1. **Verify the two quotations in context** and confirm that in this corpus a quasifree state is determined by its covariance without a finite-rank family. **If that is not what those passages mean, say so and stop — the hypothesis dies there.**
2. **Construct the one-particle space and the CAR algebra carrying `C(p) = (I - h_0/|p|)/2`**, and the GNS representation over it. At file:line against existing sealed material wherever it exists; new derivation where it does not, declared as such.
3. **Does that yield the COMPLETED algebra the spec's Section 1 requires**, or a different algebra? This is the question I most expect to bite. **Answer by type, not by name.**
4. **Identify `rho_pre` by the allow/require threshold** — the state on the pre-record side. State whether that is a selection the threshold actually licenses, or whether it names a state without picking one out. **The threshold "does not automatically choose the denominator; it states the exact condition under which the denominator is required" — so check whether it can select a state, or only constrain one.**
5. **Then say what the quotient, the invariant measure and the record-effect family do** — follow from this construction, or remain separately stuck.

### WHAT WOULD MAKE THIS FAIL, AND ALL OF THESE ARE GOOD OUTCOMES

- The passages do not mean what I read them to mean.
- The GNS construction yields an algebra that is not the completed one, and the gap is real.
- Allow/require constrains `rho_pre` without selecting it — in which case the state is still underdetermined and F-RP1 stands for a better-understood reason.
- The construction needs a premise beyond the current stack — **F-GK3: declare it at the outset, do not supply it quietly.** Note that Q-37's adoption LAPSED under Q-38, so there is no standing licence to treat declarations as producers.

**DO NOT** solve for `K_*`, evaluate a response, run the mutation audit (its channel members still have no content), or re-run the old finite-rank route.

---

REPORT BACK: hashes, exact committed paths, gate verdict, and the outcome — **`ROOT DERIVED` with the producer flag flipped, or the specific step where it fails.** `0 of 11` has not moved since this program began; this is the first attempt with a named route rather than a permission. **If it fails, the step at which it fails is worth more than the attempt cost.**

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
