## PASTE 651 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / BUILD] CONFORM B'S ADAPTER TO THE SEALED CONTRACT

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. Builder A's code remains OFF LIMITS.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-589.
- The sealed addendum (your own draft, now law): `STAGE8_TASK6_SPEC_V005_INTEGRATION_ADDENDUM_DARIO_V001.md` = `d17c5e79986bea431dec0b572019096f9c059bcc43876fda9134abc96ce0f260`.
- Your package: archive `evaluator_build_B/` per your manifest `f79b50ac…`.

TASK:
F1. **Conform the verifier** to `rd22.verifier-manifest.v001` and the addendum's inventories — including the TWO CHANGES YOU OWE from Q-588 (ledger objects -> digests where the child row wants digests; the fixture expectation as a named record, not a hash) and the isolated-child launch discipline (manifest fields, stdout verdict-only, exit 1 vs exit 2 semantics). Every change a disclosed delta.
F2. **Re-run the self-check** (syntax/schema/live-parse of the spec expectations); update the file inventory with hashes.
F3. **The conformance artifact** (sealed in the archive workspace): the delta table, updated inventory, self-check transcript, PIN CHECK, VERDICT-LINE SCOPE.

OUTPUT: updated code under archive `evaluator_build_B/` + one sealed artifact
`STAGE8_TASK6_BUILD_B_CONFORMANCE_DARIO_V001.md`
with final lines: `CONFORMED = contract + inventories`, `OWED_CHANGES = 2/2 made`, `DELTAS = N disclosed (+table)`, `SELF_CHECK = passed (+transcript)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
