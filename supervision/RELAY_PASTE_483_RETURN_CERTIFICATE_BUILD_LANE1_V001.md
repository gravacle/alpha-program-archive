## PASTE 483 — CODEX LANE 1 (HIGH EFFORT) — [TASK 5] BUILD THE STATIONARY-RETURN/REGULARITY CERTIFICATE; CLOSE BOUNDEDNESS AND CLOSURE

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-404.
- Authorities (verify hashes): the certificate typing `02786457…` (X3 — follow its constructibility route exactly), your carrier build `aebe708d…`, the conditional theorem `88854f08…`, metric V005 `2a379098…`, DoR-008, Q-401–404.
- Custody: construction, derivation-first; authored steps at the choice table; cross-review follows. p, ν symbolic; no values.

TASK:
Y1. **Build the global stationary-return/regularity certificate** per the typed route: what it asserts, proven on the built scalar carrier, with every discipline certificate (covariance, reality, unit seam, DoR-008 restriction).
Y2. **On it: prove composite B_ind boundedness and closure** — the two obstructed premises discharged, or the exact residual obstruction stated.
Y3. **Restate the theorem's premise state exactly:** which of the four premises now hold unconditionally, which conditionally, and the precise final form of the existence/uniqueness statement — including the q < 1 status: state the contraction condition as the explicit inequality on |p_loc[R_K]| (the threshold, symbolically) so the fiber's consequence is a stated theorem, not a vibe: "the fixed point exists and is unique iff/if |p_loc[R_K]| < <threshold expression>" or whatever the mathematics actually gives. Also state what a non-contractive route (monotonicity/compactness on the built carrier) would need, without building it.
Y4. **Falsifiers:** finite restrictions; standing regressions; anti-tuning ledger.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_RETURN_CERTIFICATE_BUILD_LANE1_V001.md`
with a lead-result block (`CERTIFICATE = BUILT/BLOCKED`; `BOUNDEDNESS = PROVEN/…`; `CLOSURE = PROVEN/…`; `EXISTENCE_STATEMENT = <the exact conditional form with the symbolic threshold>`), the register-sweep list, and the Y1–Y4 chain. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No numeric evaluation; no registered verdict; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
