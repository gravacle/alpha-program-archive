## PASTE 488 — CODEX LANE 2 (HIGH EFFORT) — [TASK 5 / R4+R12] BUILD J_ref AND THE LOCAL-KERNEL ESTIMATES; CLOSE THE CONDITIONAL THEOREMS

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-409.
- Artifact under review + build source: `alpha-program-archive/workspace/STAGE8_TASK5_MAXWELL_SYMBOL_CERTIFICATES_LANE1_V001.md`
  SHA-256 `2aad6d8379777b8651a7e8dda78e9b085a69aa0203f455209eeb5e6e7da3adeb`. Verify before reading — the conditional theorems' exact premises are the build contract.
- Authorities: your kernel build `ccb94dfa…`, the ledger `c35ef846…` (the R12 route: the J_ref realization/refinement and response-naturality/common-refinement package), DoR-007 (derive-the-limit), metric V005 `2a379098…`, DoR-008, Q-406–409.
- Custody: verify then build; derivation-first; authored steps disclosed; cross-review follows. All symbolic; no member selected.

TASK:
D1. **Cross-verify the conditional construction theorems** (C1/C2) and the C3 partial — recompute the chains; verify the obstructions are exactly the local-kernel estimates and J_ref.
D2. **BUILD J_ref:** the reference realization/refinement package per the R12 route — the object DoR-007's derive-the-limit ruling always pointed at. Response-naturality and common-refinement as far as derivation carries; disclosed authored steps only where forced; the DoR-008 falsifier binds.
D3. **BUILD the local-kernel estimates** on the built carrier (the uniform estimates the conditional theorems consume).
D4. **Discharge:** feed D2/D3 into the conditional theorems — state exactly which of C1/C2/C3 now close, the completed Maxwell symbol's resulting status, and the faithfulness question's remaining content.
D5. **Falsifiers:** finite restrictions; standing regressions; anti-tuning ledger.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_JREF_AND_LOCAL_KERNEL_ESTIMATES_LANE2_V001.md`
with a lead-result block (`JREF = BUILT/BUILT_WITH_AUTHORED (+items)/OBSTRUCTED (+exact)`; `ESTIMATES = …`; `C1/C2/C3 = CLOSED/CONDITIONAL/…`; `COMPLETED_SYMBOL = …`), the register-sweep list, and the D1–D5 chain. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No numeric evaluation; no registered verdict; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
