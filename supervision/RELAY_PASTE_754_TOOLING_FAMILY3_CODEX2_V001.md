## PASTE 754 — CODEX 2 LANE (5.6 SOL EXTRA HIGH) — [7A / STEP 11] TOOLING FAMILY 3: MUTATION/OBSERVATION GENERATION

PICKUP-ACK + LANE GUARD in force (CODEX 2).
(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. ALL standing laws apply. FIRST-TIME-RIGHT rules 1 and 3 govern the build.)

CONTEXT (Q-626 proposal; Q-658): family 3 generates the mutation instances and expected rejections the rows' criteria demand (the negative-control layer), from sealed descriptors and instances — deterministic, never hand-authored.
TASK:
AF1. BUILD family 3: for each descriptor-demanded mutation class over the completed instances (744/745/747/748 + the SP1-07/SP2-05 proof bundles), generate the mutation instance and its expected rejection from the sealed criterion text — content-addressed, fail-closed, negative controls on the tool displayed.
AF2. RUN it; census: mutations generated / expected rejections bound / rows advanced; anything the sealed criterion underdetermines named, never invented.
AF3. Admission stays barred; state it.
OUTPUT: the tool + outputs + one sealed report `STAGE8_7A_TOOLING_FAMILY3_CODEX2_V001.md`
Final lines: `TOOL = built (controls displayed)`, `PRODUCED = census`, `UNDERDETERMINED = none/named`, `ROWS_ADVANCED = list`, `ADMISSION = barred, stated`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, write 754_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants.
