PASTE 319 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2d] THE p_ch-DEPENDENCE OF THE RESPONSE STRUCTURE — UNDER DoR 010

*** READ `alpha_supervision/LOCKED_PROCESS.md` AND `DECISION_OF_RECORD_010_...` -- the scope of
this computation is EXACTLY DoR 010's. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-236 + DoR 010)

```text
DOES THE OBJECT EXIST?   YES: the sealed exact family A_N^(p) = (1-p) + p Z_N (Q-236),
                         Z_N = prod_j conj(z_-,j) z_+,j, p in (0,1) bracketed by record
                         existence.
IS THE VERSION CURRENT?  YES. DoR 010 authorizes THIS computation and nothing more.
ARE ITS INPUTS PRESENT?  YES -- one closed-form family, symbolic p.
```

## 1. THE COMPUTATION — SYMBOLIC, FINITE, EXACT

```text
1  Gamma_N^(p) = -log A_N^(p). Compute the FIRST and SECOND variations in the holonomy data
   (the relative phases theta_j), exactly, symbolic in p: at coincidence (all theta_j = 0) and
   to the orders that carry the connected bilinear.
2  EXTRACT THE p-FACTOR: the exact function of p multiplying the phase bilinear in the
   connected second variation. SHOW THE DERIVATION at N = 1 and general N.
3  THE RATIO QUESTION -- THE POINT OF THE RUN: the root condition is a PHASE condition
   (Delta Phi[K; X_K] = pi -- C_record's phase form, structurally). Trace WHERE the overall
   p-factor of the kernel enters that condition's structure: does an overall multiplicative
   factor of the response kernel SURVIVE into the phase residual, or does the phase condition's
   ratio/normalization structure CANCEL overall kernel scale? *** ANSWER FROM THE SEALED
   STRUCTURE OF THE RESIDUAL, with paths and lines -- NOT from what would be convenient. ***
4  VERDICT, one of three, typed:
   P-INDEPENDENT (the factor cancels structurally in the residual)
   P-DEPENDENT VIA [named factor] (the factor survives to the root condition)
   UNDECIDABLE-YET (the residual's structure is not written to the needed level -- say what
   would decide)
5  IF P-DEPENDENT: state soberly what that means -- p_ch becomes a physical input to K_*, and
   alpha's universality would demand a canonical p -- WITHOUT proposing any value or selector.
```

*** DoR 010's BARS ARE ABSOLUTE: no numerical kernel value, no K/K_*/B_ind value, no alpha, no
scale, no measured comparison. SYMBOLIC p, FINITE N, STRUCTURE ONLY. ***

## 2. LEADS

```text
IF THE FACTOR CANCELS -- *** LEAD WITH THE CANCELLATION MECHANISM: alpha would need NO further
physics input, and the state-port continuum becomes fully gauge-like FOR THE TARGET. THE
LARGEST POSSIBLE RESULT. ATTACK IT HARDEST YOURSELF. ***
IF IT SURVIVES -- lead with the named factor and the exact place it enters the residual. That
is the program's final physical input, identified.
IF UNDECIDABLE-YET -- lead with the exact missing level of the residual's specification.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
