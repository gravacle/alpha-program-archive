# Route Memo — Connected Linked-Cluster Majorant (theory scoping lane return)
Claude lane · 2026-07-25 (late evening) · fresh-context scoping lane, read-only.
Full memo preserved in the session record; operative summary below.

## Target statement (typed)

Uniform connected-cumulant majorant for -Log Z_K on the amended Route-2
objects: per-cell activities Phi_C with sum over anchored connected clusters
bounded by eta(epsilon_star)^n, uniform over (i) the relayed causal
exhaustion, (ii) the declared cellulation families (hypercubic fixture,
cubical bisection A, simplicial/barycentric B), (iii) common refinements,
(iv) the complexified per-cell history polydisc |a| <= epsilon_star, (v)
per-cell independent source assignments — with every cell at FULL
tau_R = pi/sqrt(2) (no small-time hypothesis; the expansion parameter is the
connection difference, never the record coupling — R_all(a,a)=I makes the
diagonal exact, so item 2's baseline problem is discharged BY TYPE on the
exhaustive object). Item 4 consequences (intensive limit, subextensive
boundary, first two derivative convergences) must be corollaries of the same
theorem. REFINEMENT-STABILITY TRAP identified: activities must take
action-density form (cell 4-volume x field-strength density) or common
refinement breaks uniformity.

Shared prerequisite for all routes — LEMMA 0 (new obligation): exact relayed
multi-cell composition identity for Z_K (record-side relay isometries cancel
between CTP branches; tensor-form ready-root supply gives no cross-cell
record-color correlation). Must be the first numbered lemma of any spec.

## Routes

- Route Q (sea-compressed determinant + colored Kotecky-Preiss): termwise
  det over ran C per lambda-color (each term IS a single second quantization
  — determinant-shortcut fence satisfied termwise; CAR-lane fence does not
  bar the relocated complete-Q_spec obligation — cite both). Lemmas M0-M4.
  Failure mode: IR marginality of the massless sea kernel |x|^-3 under
  refinement — the same infrared structure already named T7's true bottom.
- Route T (anchored transfer-operator / certified spectral gap): generalize
  the sealed periodic machinery (the corpus's own named successor step);
  certified isolation -> exponential clustering -> majorant in a certified
  disk. THE CLIFF: numeric certificates cannot quantify over infinitely many
  refinements; needs an analytic refinement-intertwiner lemma that may not
  exist.
- Route D (Duhamel differential inequality): KILLED — reproduces exactly the
  extensive bound the sealed closure result proved insufficient; cannot fail
  the GHZ control. Retain its calculus only as the contact-term engine in
  item 5.
- RECOMMENDED: Route H = T then Q (T for certified decay on the pinned
  skeleton; Q's cellulation-blind constants — functionals of (||b_D||,
  tau_R, sea kernel, p_lambda) only — to carry the quantifier).

Graceful-block ranking: T degrades best (every failure is a named failed
certificate). Q must preregister the two-cell connected cumulant at tau_R as
its sharpening witness so failure falsifies rather than merely inconclusive.

## Mandatory negative controls (for the spec)

GHZ cos(N tau_R A) — pipeline must REFUSE by naming the violated hypothesis
(independent record colors; GHZ = perfectly-correlated-color limit);
correlated-color variant (shared color in Lemma 0 — must detectably fail);
t^-3 insufficiency control; periodic positive regression vs the certified
|z| <= 1/500 results; detuned-relay (record-erasing advance) control;
refinement-stability control (executable form of the trap); no-target fence.

## Dependency verdict

NO hard Phase-A/B production dependency — the majorant is a theorem about
sealed definitions and can start now. Interface caveat: a representation-
level Phase-A BLOCK would demote it to a statement about the abstract sealed
form. epsilon_star may never be tuned by any output.

## SCOPE DECISIONS FOR BRIAN (flagged loud by the lane, relayed verbatim)

(a) Any use of BUFFERED_EXHAUSTION_CLUSTERING or any clustering principle is
    an ADOPTION and goes to Brian — no lane may adopt it to rescue Route Q.
(b) If Route T's refinement-intertwiner does not exist and the cellulation
    family gets pinned to a finite list, that is a BLOCKING SCOPE RESTRICTION
    (Axis-3 style) changing what T7(iii)/T13 can ever claim — Brian must see
    it framed as a scope decision, not slipped in as a lemma.
(c) Spec-header scoping to settle up front: constants uniform in the
    Hermite carrier index n (recommend: constants as functionals of
    (||b_D||, tau_R, sea-kernel decay, p_lambda) only); ER-fork robustness
    (state constants over the envelope-profile CLASS so the theorem
    transports across ER-A/ER-B without re-execution).
