PASTE 132

Written 2026-07-30, for **CODEX LANE 2 — a new lane. Read this whole paste before writing anything.**
A paste is a snapshot; any ruling later than this overrides it without notice.

You are the program's SECOND construction lane. Lane 1 is working `Gamma_K` (paste 131). **Your subject
is slot 18 and it does not overlap lane 1's.** Item 4 is the lane protocol and it is not optional —
there are three writers on one working tree.

Fences, absolute: read-only outside your own writes; **compute no alpha, kappa_record, kappa_Thomson,
coupling, radius, scale, root, or eigenvalue**; **no comparison to any measured constant**; file:line +
verbatim quote for every claim; bounded negatives state roots and exclusions; report refutations, never
repair them. `alpha_computed = false; proof_authorized = false` on everything you write.
**NEVER touch `a32_holdout/custodian_private/` — not to read, not to list, not to stage.**

Namespace collisions live in `STAGE8_NAMESPACE_REGISTER_DRAFT_V004.md`: `C_R`, `K_H`, `Gamma_K`. Check it
before using any symbol.

---

### ITEM 1 — WHY SLOT 18 IS NOW A PRIMARY WORKSTREAM, WHICH IT WAS NOT YESTERDAY

Slot 18 is the last of eighteen sealed `Q_spec` obligations
(`STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md:119-138`): **"one unused structure-sensitive
prediction."** Until today it was treated as terminal bookkeeping. Two rulings changed that.

**Q-13 (2026-07-30):** slot 18 is one of TWELVE slots that **cannot fix a normalization under any
circumstances.** It is an invariance/protocol obligation. It has no bearing on alpha's VALUE and never
will.

**Q-18 (2026-07-30):** and yet it is load-bearing for alpha's **STANDING**. The chain, and this is the
reason your lane exists:

- The floor boundary value `Gamma_BR,k_R = 0` is the content of the ADOPTED induced-only axiom — not a
  derived spectral fact, not a subtraction convention
  (`RESULT_FLOOR_BOUNDARY_VALUE_SETTLED_2026-07-30.md`).
- Therefore **alpha's conditionality equals that axiom's status, exactly and no more.**
- The parameter accounting: "with the whole bare basis allowed there are five unknowns; with induced-only
  adopted there are two. The postulate supplies precisely the missing constraints."
- And the escape clause, from the corpus's own slot-9 attack: *"Predicting a dimensionless number from a
  postulate that contains exactly one free dimensionless number per predicted coefficient is a
  reparametrization, not a prediction — **unless the postulate is independently earned by predicting
  something else first.**"*

*** THAT CLAUSE IS SLOT 18. Your work is what would convert alpha from a conditional consequence of an
adopted axiom into a result. *** `FINAL-CLAIM` already rests on slot 18 rather than on alpha, which is a
design choice the program made before anyone knew why it was right.

**AND IT IS THE ONLY WORKSTREAM THAT SURVIVES IF `Gamma_K` PROVES UNBUILDABLE.** Lane 1 may return HARD
PROOF. Your lane does not depend on lane 1 at all.

### ITEM 2 — WHAT SLOT 18 REQUIRES, AND THE ORDERING FACT THAT MAKES IT AVAILABLE NOW

**THE DELIVERABLE:** one derived, structure-sensitive, **NON-alpha** observable on which this theory
differs from a published same-alpha comparator, at a stated significance of **D >= 5**, together with the
derivation that produces it.

**IT IS NOT BLOCKED BEHIND THE OTHER SEVENTEEN SLOTS.** This was believed and is struck
(`RESULT_FENCE_INVENTORY_AND_WHAT_IS_TESTABLE_TODAY_2026-07-29.md` §5, and R-43): the eighteen-slot
enumeration carries NO ordering language, the corpus's only frozen order is a NINE-item electromagnetic
sequence that does not contain slot 18, and the machine-readable form encodes the slots as an unordered
dictionary. **Verify this yourself before relying on it.**

**THE ONE GENUINE ORDERING CONSTRAINT, which is narrower:** A32 custodian unmasking sits behind
`ALPHA-RESULT-SEAL` (`V011:1816-1819`), so an A32-grade holdout cannot **COMPLETE** before alpha exists.
**Generating the prediction needs only a sealed theory output record.** So: derive now, complete later.

**AND A TIMING FACT THAT MATTERS MORE THAN IT LOOKS:** a holdout **committed late is VOID, not merely
weaker.** The commit half must happen early. This is the reason slot 18 cannot simply wait for lane 1.

### ITEM 3 — YOUR WORK: SCOPE FIRST. DO NOT ATTEMPT THE PREDICTION IN THIS TURN.

The program has learned this twice today, both times expensively avoided. A bounded scoping session on
Departure 2 found its entire four-channel framing rested on a formula superseded three minutes after it
was written. And lane 1's O-SC1 step zero correctly returned BLOCKED rather than attempting a
construction that could not start — which is what produced today's charter. **Scoping has outperformed
building two for two.**

**DELIVERABLE THIS TURN — a bounded inventory, with roots and exclusions stated:**

1. **What exists.** A32 is ratified; a holdout universe of **355 candidates** was collected and masked.
   Locate that universe, its selection rule, and its masking discipline. Report what is actually in it
   and by what criteria — without unmasking anything and without entering
   `a32_holdout/custodian_private/`.
2. **What "structure-sensitive" and "unused" mean in sealed text.** Both are conditions on the
   prediction. Quote the definitions at file:line. If the corpus does not define them, that is a
   finding — say so, because an undefined admissibility condition cannot be met.
3. **What `D >= 5` is computed against.** A comparator is required. Identify whether the corpus names
   one, what "same-alpha comparator" means operationally, and whether the significance measure is
   defined anywhere.
4. **The beacon rule.** `FINAL-CLAIM` requires a beacon-selected observable, and task 39 in the reviewer
   task list "fails closed on an empty eligible set at `D >= 5`." Find the beacon rule, and determine
   whether the eligible set can be shown non-empty **before** a prediction exists.
5. **Candidate observables the theory could speak to at all.** Not a prediction — a typed list of where
   this theory makes structure-sensitive statements that are not alpha. Say for each what would have to be
   derived.

**RETURN VERDICT: MISSING SPECIFICATION / HARD PROOF / DERIVABLE NOW**, same three as lane 1.

**FORBIDDEN IN THIS TURN:** deriving or asserting any prediction; unmasking or inferring any masked
candidate; entering the custodian directory; comparing anything to a measured value; computing any
coupling, scale, or significance figure. **If the inventory tempts you toward a number, stop and report
the temptation** — that is a finding about the fence, not a failure of the task.

### ITEM 4 — LANE PROTOCOL. THREE WRITERS, ONE WORKING TREE.

The last construction commit absorbed eight supervision files belonging to another lane. No harm that
time; with two construction lanes it is a live hazard, and a concurrent re-baseline has already nearly
frozen a double-count in this program once.

1. **Commit ONLY files you wrote, by explicit path.** No `git add -A`, no `git add <directory>`. Report
   the exact paths committed.
2. **`STAGE8_LANE_STATUS.md` is shared with lane 1.** Re-read it immediately before appending, append in
   the same step, and if it changed since your last read, re-read and re-append. Never rewrite the table.
3. **Your write scope is slot-18 / A32 / holdout-universe / prediction-map artifacts.** `Gamma_K`,
   joint-selector, O-SC1, measure and namespace artifacts belong to lane 1. If you need something in lane
   1's scope, say so and stop.
4. **Prefix your artifacts `STAGE8_SLOT18_*`** so ownership is legible from the filename.

**METHOD NOTE, and it is a real defect the reviewer lane just committed:** do not compute a bounded
negative by piping a path list into a second search. Both program roots contain spaces
(`New project`, `MB Work`), `xargs` splits on whitespace, and the pipeline returns **zero while exiting
successfully** — it fails silently toward "absent," which is the direction that manufactures false
bounded negatives. Use:

```
grep -rlZ <opts> -e A ROOT1 ROOT2 | tr '\0' '\n' | sort -u > A.txt
grep -rlZ <opts> -e B ROOT1 ROOT2 | tr '\0' '\n' | sort -u > B.txt
comm -12 A.txt B.txt        # and report the FILE LIST, not only the count
```

---

REPORT BACK: hashes for every artifact written, the exact committed paths, `corpus_check` gate verdict,
deploy state, and the verdict type with file:line evidence. **If item 2 finds that "structure-sensitive"
or "unused" is undefined in sealed text, lead with that** — it would mean the program's terminal claim
rests on an admissibility condition nobody has written down.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
