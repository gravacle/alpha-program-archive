## PASTE 630 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / SUBGATE] BOUNDED CHECK: THE EIGHT RE-RENDERED EVALUATOR CRITERIA

(Same Dario session rules.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-565 (your scoping review, registered).
- Under review: `alpha-program-archive/workspace/STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE3_V002.md`, SHA-256 `25e89ed24933d66fcb6991b83bb971775be834070f8570a8b89cd0248f22edb7`, vs base V001 `eb2073eb…`. Verify both before reading.
- Its claims (Q-564): 8 rows re-rendered — your four (SP1-07, SP2-03, SP2-05, SP2-06) plus four newly found by the 66/66 sweep (V010-03, MR-04, SP1-06, SP2-07); 58 CLEAN; all criteria opcode-reducible; 36-hunk carriage table.
- Custody: Codex 3 re-rendered per your two reviews; you check the re-renders. BOUNDED — the 8 rows, the sweep's soundness, the carriage. Not a fresh full-spec review.
- Your own standard from Q-561 applies: does each re-render restore the blocker's POSITIVE half as executable content (KERNEL/ENUM-class instruments), with every criterion term opcode-reducible, at the sealed byte span? Quotation discipline per Q-565: watch for paraphrase-as-quotation, especially any weakened-direction rendering.

TASK — bounded, verdict per item:
R1. **The eight re-renders**, each against its blocker at the sealed pin: positive half restored, not just present-by-name; the new PASS criterion exact and Boolean; class assignment unchanged and correct; no weakened-direction drift between the blocker text and the executable row.
R2. **The sweep's soundness:** spot-check at least 8 of the 58 CLEAN rows across families — is CLEAN earned (every criterion term names an opcode, schema field, content-addressed input, or defined term)? Any missed defective row is a finding.
R3. **Carriage:** the 36-hunk table against your own re-diff; uncovered hunks classified harmless-editorial or content-bearing; the runtime-pin and aggregate sections untouched as claimed (their pinned block hashes verify).
R4. **Battery + verdict:** F_PLDEC; M-2 (all four false-negative modes); self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_SPEC_V002_RERENDER_CHECK_DARIO_V001.md`
with final lines: `RE_RENDERS = 8/8 CONFIRMED / (+items)`, `SWEEP = SOUND / (+missed rows)`, `CARRIAGE = FULLY_ACCOUNTED / (+items)`, `SPEC_V002 = STANDS / NEEDS_V003 (+reason)`, `BUILD_READY = yes/no (readiness of the SPEC only; RD-22, builder custody, and the runtime pin remain principal acts)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
