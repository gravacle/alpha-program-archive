## PASTE 721 — CODEX 2 LANE (5.6 SOL EXTRA HIGH) — [7A / STEP 11] THE BOX BATCH: THE 18 S11-SPEC STATEMENTS, CONSOLIDATED

PICKUP-ACK + LANE GUARD in force (CODEX 2).
(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. ALL standing laws apply. Strategy of record per Q-626: boxes -> tooling -> batch generation.)

CONTEXT: your split names 18 S11-SPEC derivational elements — schema classes and spec statements that do not exist (the boxes). The generators need them sealed first.
TASK:
FF1. CONSOLIDATE the 18 into the SMALLEST lawful number of finite spec deltas (target: one, if the statements cohere as one schema family; justify the count either way). Each statement as CLOSED SCHEMA, never prose; descriptor rows unchanged (any row change is a finding to surface, not perform); every schema grounded on the sealed material it must carry (source + span, block-covered, object-names probed).
FF2. Where a box's shape is genuinely underdetermined by sealed sources — that element moves to the U class with what is missing named; no schema invented to fill a gap the corpus does not determine.
FF3. State per-box which generator family consumes it and which of the 198 C elements it unblocks (the dependency map the tooling build will follow).
OUTPUT: the spec delta document(s) + one sealed report `STAGE8_7A_BOX_BATCH_CODEX2_V001.md`
Final lines: `BOXES = 18 -> N sealed schemas in K deltas (+moved-to-U named)`, `ROWS_CHANGED = 0 / (findings surfaced)`, `DEPENDENCY_MAP = per-box (family, C-elements unblocked)`, `PIN_CLOSURE = N hits, all resolved`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, write 721_DONE.md, STOP. No register, plan, tracker, git action. (B's envelope check of the deltas follows; nothing is admitted here.)

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
