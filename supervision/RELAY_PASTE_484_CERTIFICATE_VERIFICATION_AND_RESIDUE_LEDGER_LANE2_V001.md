## PASTE 484 — CODEX LANE 2 (HIGH EFFORT) — [TASK 5] CROSS-VERIFY THE THRESHOLD THEOREM; TYPE THE INHABITANT MAPS; THE CONSOLIDATED RESIDUE LEDGER

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-405.
- Artifact under review (Lane 1's build): `alpha-program-archive/workspace/STAGE8_TASK5_RETURN_CERTIFICATE_BUILD_LANE1_V001.md`
  SHA-256 `b569a89e661ad92b744213bfc7cd65985908bc509b8dd9de77bcae3a2bdb4bad`. Verify before reading.
- Authorities: your typing artifact `02786457…`, the carrier build `aebe708d…`, the conditional theorem `88854f08…`, Q-391–Q-405 (the full seam sequence).
- Custody: adversarial verification + the consolidation. p, ν symbolic; no values.

TASK — PASS / KILL per item:
Z1. **The certificate interface and conditional theorem** — recompute; verify boundedness/closure genuinely follow on any inhabitant.
Z2. **THE THRESHOLD THEOREM** — recompute the iff: strict contraction ⟺ |p_loc[R̂_K]| < A_loop⁻¹ for 0 < A_loop < ∞; verify A_loop's definition on the built carrier and its (p,ν) dependence; verify no authority proves the inequality (sweep).
Z3. **The two inhabitant maps, typed:** the scalar-K-to-action/full-residual map and the completed stationary pullback/solution map — for each: DERIVABLE (chain) / CONSTRUCTIBLE (the route, what it consumes, which certificates it must pass) / FIBER-GRADE (a genuine freedom, typed for ratification). Sweep the ratified stack honestly.
Z4. **THE CONSOLIDATED RESIDUE LEDGER — the deliverable for the principal:** ONE table of every remaining free or unbuilt object between the current state and the sealed number, each row: the object, its type (freedom-for-ratification / constructible / derivable-open), what it gates, and its prepared fiber or route. Include: the completion fiber (Q-396), the reading rule (Q-397), p_loc[R_K] (Q-398), the covariant local-symbol map (Q-400), the two inhabitant maps (Q-405), the ν scale (A32/evaluation), the integer pair (the declared input), and anything else the sweep finds. Complete and honest — this ledger is what the principal will steer by.
Z5. **Falsifiers + one fresh attack.**

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_CERTIFICATE_VERIFICATION_AND_RESIDUE_LEDGER_LANE2_V001.md`
with the verdict table Z1–Z5, the residue ledger as its centerpiece, and final lines:
`THRESHOLD_THEOREM = CONFIRMED / KILLED (+item)` and `RESIDUE_COUNT = <n> objects (<k> ratification-grade, <m> constructible, <j> derivable-open)`.
Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No numeric evaluation; no registered verdict; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
