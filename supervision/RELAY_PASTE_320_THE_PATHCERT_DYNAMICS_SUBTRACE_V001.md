PASTE 320 — CODEX LANE 2 (HIGH EFFORT) — [TASK 2d] CONSTRUCT THE ENVELOPE'S FINITE PathCert DYNAMICS SUBTRACE

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-237)

```text
DOES THE OBJECT EXIST?   NAMED as preflight-clean work by the handoff receipts (Q-235): "construct
                         the envelope's finite PathCert dynamics subtrace." Its ingredients exist:
                         the envelope spec with its precisely-specified-but-uninstantiated
                         path-provenance witness (Q-226, bda00e99...) . the RATIFIED law's finite
                         edge U_N[a] (DoR 009) . the clean lower-row edge receipt (Q-235).
IS THE VERSION CURRENT?  YES.
ARE ITS INPUTS PRESENT?  YES, as instances.
```

## 1. THE BUILD

```text
1  INSTANTIATE THE FINITE PATH: rho_ready (the SEALED ready-cell product state) --U_N[a]-->
   finite completed state --(the outgoing record-only GNS limit's finite stage)--> outgoing.
   The nodes exist; U_N[a] is the ratified edge; WIRE THE PATH EXPLICITLY at N = 1, 2.
2  THE SUBTRACE: the dynamics component of the path-provenance witness -- the frozen trace
   showing every node-to-node step descends from THE ONE ratified law + the sealed ready state.
   *** THIS IS THE COMMON-ORIGIN WITNESS AT THE FINITE LEVEL: one origin (the law + ready
   state), every path object descending from it, certified step by step. ***
3  WHAT REMAINS OPEN, typed honestly: the p_ch weighting enters the path at the ready-state
   node (Q-236) -- the PathCert carries it as the path's ONE FREE DATUM, marked, not hidden.
   The joint/field-CTP upper row stays TYPE-U (the known completion territory).
4  CHECKS: the path's one-cell case vs sealed structure (the falsifier arm) . equal-history
   collapse . the N <= M zero-extension along the whole path.
5  EMIT: PathCert_N -- the finite certified path object -- and what the ENVELOPE's forcing
   protocol now looks like with a real certified path in hand: is step 1's family DECLARABLE
   over paths where it was not declarable over states (Q-225's refusal)?
```

*** MARKS: TYPE-P | premises: DoR-008, DoR-009. NO second variation, kernel, B_ind, or transport
map -- Task 4's territory stays Task 4's. ***

## 2. LEADS

```text
IF PathCert_N CONSTRUCTS -- *** LEAD WITH IT: the transition envelope gains its first CERTIFIED
PATH, the common-origin witness exists at the finite level, and the envelope's protocol may
become runnable over paths -- the stratum OBS-15 predicted forcing would live on. ***
IF THE PATH BREAKS AT A NODE -- lead with which node and why; that is a defect in a ratified or
sealed piece and must be typed precisely.
IF THE ENVELOPE'S FAMILY BECOMES DECLARABLE -- say so explicitly with the declaration; the
forcing run over paths is then tomorrow's natural relay.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
