## PASTE 603 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 6 / STEP 1] THE M5a SUBGATE STATUS COLUMN

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-537. The A32 instrument is RATIFIED (read `alpha-program-archive/supervision/DECISION_A32_INSTRUMENT_RATIFIED_2026-08-06.md`); nothing attained; this relay builds the subgate's tracking instrument.
- Your V002 (`c5d1090b…`) carries (M5a-V002)'s ~23 conjuncts; the 602 confirm (`6b40532e…`) demands exactly this column.

TASK:
S1. **The status column:** every (M5a-V002) conjunct, one row each — TRUE (already satisfied; the sealed source cited) / FALSE-SUPPLIED-BY (which Task 6 board step or named build supplies it) / FALSE-LONG-POLE (no current supplier; the exact missing object named — the J-II-class items flagged as such); the sealing-sequence dependencies displayed so the order of completion is readable.
S2. **The long-pole board for the principal:** the false-long-pole set summarized with each item's history citation (nothing new authored).
S3. **Battery:** surface anchor; verb discipline (TRUE only where the source proves it); self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_M5A_STATUS_LANE3_V001.md`
with final lines: `CONJUNCTS = n`, `TRUE = n / SUPPLIED_BY = n / LONG_POLE = n (+named)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
