PASTE 307 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2d] REBUILD THE FAMILY AS THE MULTIAXIAL ENVELOPE AND RE-RUN THE PROTOCOL

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-224)

```text
DOES THE OBJECT EXIST?   YOUR SIBLING LANE NAMED IT: the MULTIAXIAL_STATE_CLASS_ENVELOPE --
                         factorization, cumulant closure, provenance, temporal role as
                         INDEPENDENT AXES (8295786..., 816 lines; read it in full).
IS THE VERSION CURRENT?  YES. Two theorems bind: (1) JOINT MULTIPLICATIVE IS IMPOSSIBLE ON
                         C0_008 (no character of M_3(C)) -- one axis endpoint is DEAD BY
                         STRUCTURE; (2) record existence does NOT exclude product pre-states --
                         the correlation requirement bites the COMPLETED state via dynamics.
ARE ITS INPUTS PRESENT?  The envelope's axes are named; their VALUES per axis must be declared.
```

## 1. THE TASK

```text
STEP 1  DECLARE THE ENVELOPE PROPERLY: for each axis (factorization . cumulant closure .
        provenance . temporal role), the exhaustive set of values WITH SEALED OR STRUCTURAL
        DEFINITIONS. A value with no definition is reported TYPE-S, not invented (the
        finitely-correlated lesson). *** THE FAMILY = the product of axis values, minus the
        structurally impossible cells -- START by marking every cell containing joint-
        multiplicative DEAD (Theorem 1). ***
STEP 2  EQUIVALENCE across cells under sealed conventions.
STEP 3  CONSTRAINTS, corrected: Parent-State Covariance . stationarity . U1 compatibility .
        the one-cell finite authority . *** the COMPLETED-STATE correlation requirement,
        routed THROUGH DYNAMICS: which pre-state cells can the sealed dynamics carry to
        correlated completed records? A cell from which no sealed dynamics reaches a correlated
        completed record is killed BY RECORD EXISTENCE, correctly aimed this time. ***
STEP 4  COVERAGE: now provable BY CONSTRUCTION if step 1 is done right -- the product of
        declared axis values is exhaustive under its own definitions. SAY SO OR SAY WHY NOT.
STEP 5  SURVIVING CELLS, counted. 1 -> the class is REQUIRED; the ask becomes the covariance.
        N -> the sized authorship list for D6. Many/unbounded -> the stratum does not force;
        say so plainly.
```

## 2. LEADS

```text
IF ONE CELL SURVIVES -- *** LEAD WITH THE REQUIRE, and state what single object then remains
(the covariance-class datum on the carrier). D6 dissolves without authorship. ***
IF A SMALL FAMILY -- lead with the cells and what distinguishes them; the principal authors
from a finite honest list.
IF AXES CANNOT BE GIVEN EXHAUSTIVE VALUES -- lead with which axis fails; that axis's value set
is then the missing object, precisely named.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
