# RATIO ROUTE — CROSS-CITATION MAP, FIVE STRATA (2026-07-28)

Compiled from workflow wf_316aa116 (12 agents, decisive claims adversarially verified: all
CONFIRMED, two PARTLY on precision only). Verbatim agent returns:
BOHM_TWO_STEPS_WORKFLOW_RESULTS_2026-07-28.md. Read-only; nothing computed; no value compared.

## THE GRAPH IN ONE PARAGRAPH

Five strata carry the EM-GR ratio idea. ZERO artifact-level cross-citations exist between any
pair, in either direction (bounded token greps both ways for every pair). Each stratum hangs
on a DIFFERENT authority mechanism. The three scale variables that would have to be selected
(x, rho, T_R) are THREE INDEPENDENT OBLIGATIONS in three different theories — one saddle
cannot discharge them together. The only files anywhere that mention objects from more than
one stratum are this supervision layer's own 2026-07-28 notes.

## NODES

S1/S2 — BR heat-trace route (parent tree; one artifact cluster).
  Core: reports/alpha_br_common_induced_coefficients_v001.md + scripts/derive_..._v001.py.
  Content: induced C_V(x), C_R(x) (inverse-Newton), K_Q(x), K_H(x), K_QH(x) from the exact
  Lambda^even(C5) chiral-16 inventory on one normalized BR operator. Forced ratios
  K_H = (3/2) K_Q, K_QH = K_Q. PASS string COMPUTED at producer (inventory, recurrence,
  positivity, monotonicity checks) — not hardcoded. In this route the 4D Planck scale is an
  OUTPUT (k_R^2 C_R(x)) and alpha would be a normalization of K_Q(x).
  Authority: strict route ledger row 260, CLOSED_BUT_INSUFFICIENT ("no value of x is
  selected"); historical parent chain.
  Open condition: select depth x. In-tree constraints on that selection: the local vacuum
  coefficient has NO finite interior stationary point (alpha_br_local_induced_depth_no_go_v001);
  the exact compact matter saddle is MONOTONE in depth
  (NO_GO_EXACT_COMPACT_MATTER_DEPTH_MONOTONE_PUBLIC_LOOPS_REQUIRED) — and the route names its
  own next admissible step: "the public boson/odd/ghost Gaussian superdeterminant."
  UNEXECUTED.

S3 — Record-bundle modulus gate (cleanroom).
  Core: COUPLED_RECORD_BUNDLE_MODULUS_GATE_V002.md. alpha_tree = 4 ell_P^2 / R^2 from a
  GRANTED (not derived) 5D circle-bundle ansatz; "coupled_record_bundle_physically_derived
  = false"; the radius is a dynamical radion with no selecting potential.
  Authority: CURRENT_AUTHORITY_LEDGER_V010.json, bound in BOTH current_derived_results and
  conditional_or_diagnostic_only; status TREE_RELATION_ONLY_RADIUS_AND_ACTION_CLASS_OPEN;
  untouched by V013. Seal recomputed, matches.
  Open condition: parameter-free saddle selecting rho = R_*/ell_P; derived bundle; stabilized
  radion. Explicit fence at :99-100: "Declaring R equal to a record interval by dimensional
  analogy does not pass. A derived metric-gluing or stabilization law is required." — the
  gate itself forbids the cheap S3-S4 bridge.

S4 — BID scale gates (cleanroom).
  Core: BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md (T_R/t_P = sqrt[2s/(xi chi_*)];
  marginal closure C_R = 1 gives T_R = 2 sqrt(pi) t_P, CONDITIONAL);
  BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md (the sqrt(2) blocker: Misner-Sharp
  E_MS -> 2 sqrt(pi) t_P vs Brown-York E_BY -> sqrt(2 pi) t_P; result JSON computes
  interval_ratio = 1.4142...; BLOCK_STAGE3_HAMILTON_JACOBI_ENERGY_NOT_SELECTED);
  BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md (demands F(T_R/t_P,...) = 0).
  Authority: in NO CURRENT_AUTHORITY_LEDGER_V001-V013; hash-bound ONLY by the embedded
  Authority ledger of the sidecar-sealed COMPLETE_QSPEC_ABSOLUTE_SCALE_AND_CONTINUUM_
  PREREQUISITE_AUDIT_V001.md (2026-07-25, POST-dating V013) — a third seal-attachment
  mechanism, outside the ledger chain. All hashes match today.
  Open condition: the energy selection. The gate names EXACTLY ONE resolution path — a
  seven-item derivation from "one target-independent complete source-record-gravity action" —
  and FORECLOSES adoption ("cannot be supplied by a convention chosen after the record phase
  is known"). The only artifact ever arguing which energy enters a first-record condition
  (ADM, parent EDM lane) is RETIRED_SUPERSEDED. Classification: PART-PROVABLE, chained
  behind the complete parent action (= obligation O1).

S5 — Joint-handle character Hessian (parent tree; found by this sweep, in no prior list).
  Core: primitive_joint_handle_character_hessian_v001.md (+ reports, audit script, result
  JSON, test; all Jul 19). One character action producing matter, EM, source/proper-time and
  all mixed curvature blocks: "No independent coefficient is assigned to any block."
  Fixed-handle EM stiffness 3/16; Schur-relaxed K_Q|M,G = 1/8.
  Authority: strict route ledger row 498, CONDITIONAL
  (one_character_fixes_local_matter_EM_source_blocks_physical_reduction_ensemble_open).
  Open condition: physical reduction ensemble. Token joint_handle appears in NO supervision
  note before today.

ORPHAN — COUPLED_GRAVITY_CHARGED_FIXED_POINT_SELECTOR_GATE_V001.md (cleanroom, Jul 23).
  Proposes alpha_* = f_grav/b_ch. Unsealed, in no authority ledger, referenced only by its
  own audit script (which validates against the EARLY current_authority_ledger_v002.json and
  certifies by substring + hardcoded False literals). Its lineage was DROPPED between
  stiffness ledger V001 (names it) and sealed V003 (no f_grav/b_ch token). Status: dropped
  thread, not refuted — but carrying the full trap signature.

## EDGES

NONE. Bounded negatives (token greps, both directions, every pair — full scopes in the
verbatim archive): S1/S2 contains no T_R, t_P, ell_P, modulus, alpha_tree, BID_, COUPLED_
token; the cleanroom contains no chiral-16, K_QH, alpha_br_common token; the parent tree
outside the cleanroom never cites any S3/S4 filename; S3 and S4 never cite each other's
objects; S5 cites and is cited by none. The only cross-strata co-occurrences anywhere are
BOHM_* supervision notes dated 2026-07-28 (this layer).

## THE THREE SCALE VARIABLES ARE THREE OBLIGATIONS (adversarial verdict: INDEPENDENT)

- x = r^2/k_R^2 (S1/S2): ratio of two SPECTRAL (mass) scales of one operator — r^2 = ||h||^2
  (16-fold superconnection mass eigenvalue), k_R the first-record spectral scale fixed
  conditionally by an intrinsic capacity condition (N_BR(k_R) = 1). h -> c h invariance
  leaves r/k_R unfixed. NOT a radius.
- rho = R_*/ell_P (S3): ratio of two LENGTHS in a granted 5D bundle; R is a granted fiber
  radius / unstabilized radion. The gate itself shows R = beta c Delta tau leaves rho free
  for every beta > 0 — so fixing a record interval does NOT fix rho.
- T_R/t_P (S4): a duration in Planck units; its only construction is blocked by the sqrt(2)
  energy selection, whose only exit is the complete parent action (O1).
No bridge object between any two exists anywhere in the three roots (bounded). One saddle
cannot discharge them together. REVISED OBLIGATION STRUCTURE: O1 (generator/action form) —
which ALSO unlocks T_R; x-selection — next named step is the unexecuted public
boson/odd/ghost superdeterminant; rho-selection — needs derived bundle + radion
stabilization (S3's own reopen). The morning's "one missing require" is now: one upstream
action obligation plus two route-internal selections, each with its next action named
in-tree.

## AUTHORITY SPREAD (why nobody saw the whole)

Five strata, five mechanisms: historical parent ledger row (S1/S2, row 260) / cleanroom V010
ledger (S3) / post-V013 embedded hash audit (S4) / parent ledger row 498 (S5) / none
(orphan). No single authority artifact anywhere lists more than one stratum. The map above
is, as of today, the only document in the program that names all five.
