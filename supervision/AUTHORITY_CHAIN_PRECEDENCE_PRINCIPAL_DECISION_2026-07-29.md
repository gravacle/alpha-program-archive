# AUTHORITY CHAIN PRECEDENCE — PRINCIPAL DECISION (2026-07-29)

PROVENANCE: A PRINCIPAL ACT, ruled 2026-07-29, session e268883f, on V006's P0. THE ACT IS
AUTHORITY AND IS DISCLOSED AS AUTHORITY: neither chain contains a rule for adjudicating
between them, so this is a principal-issued precedence ruling, not a reading of sealed text.

## THE SITUATION RULED ON

Two authority chains have run in parallel and cite each other NOWHERE at the authority
level (verified: `alpha_post_cleanroom_current_authority_spec_v001.json` contains zero
occurrences of `CURRENT_AUTHORITY_LEDGER`; no `CURRENT_AUTHORITY_LEDGER_V0*.json` contains
`post_cleanroom`). They CONFLICT, not merely diverge: the parent chain's active route
instructs "set `alpha_micro = 1/(4 pi K_*)`; then derive the Thomson/RG matching map" —
emitting an alpha-shaped number BEFORE Thomson matching, in the bare unsubscripted form
that rule R-9 forbids — while `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` voids any
result in which alpha is evaluated before THOMSON-RESULT-SEAL.

## THE RULING

1. THE CLEANROOM CHAIN GOVERNS ALL PROCEDURE. `CURRENT_AUTHORITY_LEDGER` (V013 lineage,
   parent_authority V010), V011, the seal ladder, the A32 freeze as ratified, rule R-9, and
   the F' fence block are the program's procedural authority without exception.
2. PARENT-TREE MATERIAL IS ADMISSIBLE AS EVIDENCE AND AS INPUT — not as procedure. It
   enters under the cleanroom's existing external-citation practice (the cleanroom already
   cites `ROUTE_MEMO_*` artifacts that live outside it, in `/Users/bgm/MB Work/
   alpha_supervision`, and its ledgers V006-V010 cite three parent-tree research memos by
   `../` path). Any parent-tree artifact used this way must be cited by path AND hash on
   the face of the citing artifact.
3. PARENT-CHAIN ORDERING INSTRUCTIONS DO NOT GOVERN AND ARE VOID WHERE THEY CONFLICT.
   Specifically VOID: the instruction to produce `alpha_micro` before Thomson matching, and
   any bare unsubscripted "alpha_micro"/"kappa" emission (R-9). The governing order remains
   V011's seal DAG, terminating in FINAL-CLAIM behind a PASSED structure-sensitive A32.
4. NOTHING IN THE PARENT TREE IS RETIRED, REFUTED, OR DISCARDED BY THIS RULING. The BR
   induced-coefficient functions, the exact public coupled super-Hessian, S5's joint-handle
   character Hessian, the parent Step-5 identifiability results, and the two Jul-23 research
   memos all remain live evidence. This ruling changes WHICH RULES APPLY TO THEM, not
   whether they may be used. Their own honest status flags travel with them unchanged.
5. DISCLOSED CONSEQUENCE, ACCEPTED: adopting a parent-tree artifact as evidence does NOT
   import its premises. Anything the parent chain declared "derived" under its own ordering
   must satisfy the cleanroom's fences before it can be load-bearing here — in particular
   F'-1 (no assumed clustering/decay), F'-5 (carrier-indexed constants), and R-9.

## SCOPE — WHAT THIS RULING DOES NOT DO

Does NOT merge the chains (that would be construction; deliberately not commissioned).
Does NOT revive the parent chain's dormant active route as a route. Does NOT rule on
whether any parent artifact is correct. Does NOT touch O-1/F'-5, C-L2, or any open
obligation. Authorizes no computation of C-L3, kappa_record, kappa_Thomson, or alpha.

## EXECUTION

Construction lane: record this precedence in the cleanroom authority chain (a successor
CURRENT_AUTHORITY_LEDGER entry or an equivalent binding artifact naming this decision by
path and hash), and index the ruling in `STAGE8_LANE_STATUS.md` in the same act, per the
established pattern. Definition of done: SEALED, MIRRORED, COMMITTED, AND PUSHED, with
deploy_status output reported.

alpha_computed = false; proof_authorized = false.
