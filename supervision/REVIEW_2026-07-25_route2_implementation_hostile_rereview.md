# Hostile Implementation Re-Review — Inherited Route-2 Phase-A Scripts
Claude construction/supervision lane · 2026-07-25 · fresh-context subagent return (verbatim below)
Scope: the 8 inherited in-flight scripts (controller, launcher, primary/independent
derive lanes, comparator, 3 test files) vs custody-note Section-5 blockers 1-5,
spec A5/A6 conformance, and the Route-2-to-O6 refactor tautology question.
This return DISCHARGES custody-note Section-6 item 3 (hostile review of the
refactored Route-2-to-O6 production compression): O6_REFACTOR_VERDICT = TAUTOLOGY_FREE,
with its coverage caveats independently closed (see verdict text).
Companion spec-review return: REVIEW_2026-07-25_route2_phaseA_spec_hostile_preexecution.md.

---

VERDICT: NOT_READY

The workspace state matches the custody note exactly (all 8 inherited script hashes match Section 3; spec seal 789338ad... verified; canonical Route-1 result hash 6dbda44... intact; no actual-parent regulated-CAR production outputs, receipts, isolated-rerun snapshot, or implementation manifest exist; GPG key 18488605D44F65A9B57B610AA5F3A86512A04D61 is present and sign-capable). NOT_READY follows from blocker 4 being open by fact (manifest absent), blocker 2 being unclosed in-band, and the controller being untested — plus the conditions below. The numerical implementation itself is in strong shape: I found no physics-encoding errors, no forbidden constants, and no silently weakened thresholds.

BLOCKER_STATUS:

Blocker 1 (fabricated bundle / caller-selected inputs) — PARTIAL / CLOSABLE_AS_DESIGNED with disclosed scope. What now exists: the comparator (compare_*.py) re-verifies every supplied hash against file bytes (validate_file, lines 306-317), requires adjacent seals (347-354), re-hashes all four inputs after consumption (1601-1604), binds bundle payloads to the implementation manifest hash, the canonical executor row hashes, and the launcher row hash (1579-1595), forbids primary bundles from carrying an overlap matrix (505-509), pins identifier inventories exactly, and embeds input_hashes in its sealed output. The controller pins canonical paths and self-computes the hashes it passes (run_lane / main, lines 210-343). Residual: nothing binds comparator arguments to canonical workspace paths inside the comparator; it runs standalone and will seal an authoritative-looking PASS COMPARISON_JSON at any caller-chosen --output from any four files — including pre-consuming the canonical comparison path. All provenance fields it checks (executor_sha256, implementation_manifest_sha256, runtime_attestation.launcher_sha256) are copyable strings; the comparator's own test fixture (test_compare_*, write_bundle, lines 378-453 + test_matching_transported_bundles_pass) is literally a working recipe: synthetic bundles with copied provenance strings and S-transported single-source arrays obtain a sealed PASS. Receipts are locally minted, unsigned JSON — forgeable. The design honestly discloses this ("local_execution_receipt_not_remote_attestation": true). Closure is real only under an evaluator-side rule that authority requires the canonical paths + receipt chain + GPG-verified manifest, plus the repairs in Findings 1, 2, 8.

Blocker 2 (generator-to-propagator lineage) — STILL_OPEN. Exactly what exists: (a) unitarity/isometry checks on propagators and Stinespring (primary 971-981; independent 877-891; comparator recompute 921-928); (b) algebraic reconstruction chains (cross = u†u, aggregates from cross, direct responses from Kraus members — comparator 944-1037); (c) diagnostic h0 and midpoint-connection Hermiticity (comparator 903-915); (d) cross-lane 3e-4 agreement under a fixed public permutation-phase S. What is missing: no code path anywhere verifies that any stored u solves dU/dt = -i h(t) U for the stored/declared h(t) — no Duhamel/evolution residual, no independent re-integration spot-check, and the full time-dependent h(t) samples are not even stored in the bundles, so the comparator could not do it post hoc. Two-lane agreement is not a substitute: S is exactly known (comparator pins it to the predeclared label/phase matrix, 597-606, 680-686), so single-source fabrication transports trivially. Lineage currently rests entirely on execution custody (manifest + launcher + controller), i.e., on blockers 1/4 machinery.

Blocker 3 (Route-2-to-O6 reduction tautology) — CLOSABLE_AS_DESIGNED; see O6_REFACTOR_VERDICT below. My independent rederivation confirms the closed forms and that no expected value flows from the tested code path.

Blocker 4 (implementation manifest / signature / anchor) — STILL_OPEN by fact (files absent, verified). The designed verify_implementation logic (controller 149-196; lanes' verify_implementation_manifest) is mostly sound: adjacent seal + GPG --status-fd VALIDSIG fingerprint check + per-row hash verification + self-binding for controller, both executors, and comparator. Gaps: no consumer requires the manifest to contain the complete required row set up front (Finding 4); test files are required by no code path, so a manifest omitting them would still pass everything; receipts themselves are outside the signature.

Blocker 5 (runtime provenance scope) — CLOSABLE_AS_DESIGNED as a disclosed limitation. The launcher content-addresses exactly what it claims (python executable + NumPy package/dist files, 92-179), pins the NumPy BLAS config hash, and the comparator now hard-requires the honesty flags including malicious_interpreter_or_kernel_resistance_claimed == False (448-461). Stdlib/Accelerate/interpreter/kernel remain disclosed trusted boundaries; nothing pretends otherwise.

FINDINGS:

1. CONDITION — compare_stage8_t7_actual_parent_regulated_car_operator_response_v001.py, lines 411-415, 512-519, 1601-1604. TOCTOU is narrowed, not closed: validate_file hashes the files, then strict_json and np.load re-read them in separate reads; the final re-hash detects a persistent swap but not an A/B/A swap-and-restore around the parse window. Exploit: race the comparator to feed unverified bytes while the receipt pins the original hashes. Append-only repair: read each input once into memory, hash the buffer, parse JSON from the buffer and np.load from BytesIO; drop the post-hoc re-hash.

2. CONDITION — run_stage8_t7_actual_parent_car_pipeline_v001.py, lines 269, 171-174, 234-240, and launch_stage8_t7_content_addressed_runtime_v001.py, lines 202-225. The receipt's target_sha256 is copied from the manifest row; the file is hashed only at controller start (verify_implementation), and the launcher never hashes target content (resolve_target checks path allowlist only; runpy runs whatever bytes are there). The independent lane runs for a long interval before the primary lane's file is spawned. Exploit: swap a lane script after verify_implementation and before its subprocess; the receipt attributes sealed provenance to unexamined code (the swapped script simply omits its self-check). Append-only repair: run_lane re-hashes the target immediately before and after the subprocess and records both digests; additionally the launcher should hash the resolved target and embed target_sha256 in the attestation so lanes can bind it into their bundles.

3. CONDITION — launch_stage8_t7_content_addressed_runtime_v001.py, line 218 plus each lane's require_runtime_attestation. The sys-attribute attestation binds nothing against an adversary: any system python can setattr the marker with copied strings (schema, manifest hash, launcher_sha256, flags) and exec a derive script directly, producing sealed-looking outputs the comparator accepts (the comparator checks only the recorded strings, 1591-1595). It is a good accident fence, not an attack barrier. No in-process fix exists; repair is architectural: sign receipts with the disclosure key the way the manifest is signed, and record that the attestation is only launcher-cooperative.

4. CONDITION — run_stage8_t7_actual_parent_car_pipeline_v001.py, lines 163-174 and 269-275. verify_implementation checks only rows that are present (plus controller self-row); presence of the launcher, comparator, and both executor rows is never asserted up front. A manifest missing the launcher row passes verification, runs the full independent lane, then KeyErrors while building the receipt — production outputs sealed, no receipt, append-only paths wedged. Append-only repair: assert the complete required row set (controller, launcher, 3 lane scripts, 3 test files) before running lane 1.

5. CONDITION — derive_..._independent_v001.py, lines 871-939 (computed, never gated) versus primary lines 1428-1445 (gated). The independent lane's maximum_adjoint_exchange_residual and per-member spectral-vs-direct kraus_residuals are emitted but not enforced anywhere: the comparator's recompute_bundle_internal_gates (805-1059) never rebuilds K_x = Σ_λ ⟨x|P_λ|ready⟩ u_λ from stored propagators+projectors to compare against stored direct_kraus_members, and has no adjoint key. Given the enforced identities (cross reconstruction, aggregates, unitarity) these are nearly mathematically implied, but two spec-table thresholds ("Kraus-sum/direct-compression residual", "adjoint-exchange residual", both ≤3e-9) are not independently enforced for the independent lane. Append-only repair: add the per-member spectral-Kraus recompute (both lanes) to the comparator at 3e-9.

6. NOTE — comparator lines 1245-1257. E_conn's fourth term is computed untransported (||R_i384 − R_i192||) where the spec writes ||S(·)S†||. S is unitary to 2e-11, so the operator 2-norm is invariant up to an immaterial correction. Deviation recorded; no repair required beyond a comment.

7. NOTE — comparator lines 1506-1508. The PASS output sets actual_parent_regulated_CAR_operator_response_derived: True and actual_parent_same_carrier_one_source_restriction_derived: True while its verdict string is only COMPARISON_PASSED. Under the custody note the sealed evaluator is sole verdict authority; these A7-level flags in a comparator artifact invite downstream over-reading. Append-only repair: rename to comparison_supports_* in a successor, or have the evaluator ignore them by rule.

8. NOTE — comparator output (1477-1526) records input hashes but not resolved input paths, and nothing marks whether it ran under the controller. Combined with Finding 3 this is the standing fabrication surface; also a standalone run can pre-consume the canonical comparison output path (append-only denial). Repair: record resolved input/output paths in the sealed result; controller cross-checks them.

9. NOTE — controller line 193: the VALIDSIG check requires the first fingerprint field to equal the primary-key fingerprint. Fine for primary-key signing (key verified on this machine, [SC] capability); signing with a subkey would fail closed.

10. NOTE — numerics hygiene is clean: no randomness, no time/env dependence, dict iteration insertion-ordered, all arrays complex128 (labels int64), eigh used on strictly non-degenerate record spectrum (gap √2) and degeneracy-safe for exponentials; the 2e-15 diagnostic-binding comparisons are between byte-identical saved arrays; BLAS thread count is uncontrolled (Accelerate) but every enforced budget is ≥3e-9, orders above matmul rounding at dimension 96. The independent's multiplication cache (round(t,15)) cannot collide distinct RK4 stage times at N≤384 and never substitutes across stage times, satisfying the no-midpoint-cache clause.

11. NOTE — primary isolated_route1_rerun (1158-1297) is viable (the Route-1 executor performs its own runtime-manifest verification and sys.path setup; verified in its source) but untested end-to-end, and a crash after snapshot creation permanently blocks the snapshot path (append-only by design; worth knowing).

O6_REFACTOR_VERDICT: TAUTOLOGY_FREE (with coverage caveats). I rederived the Route-1 closed forms by hand: with ready = completed = (1,1)/√2 and U(θ) = diag(1, e^{iθ}), K_completed(θ) = (1+e^{iθ})/2, K_other(θ) = (1−e^{iθ})/2, completed response = conj(K_c(θ−))·K_c(θ+), exhaustive = (1+e^{i(θ+−θ−)})/2 — exactly the typed expected values in both lanes (primary 1114-1119, independent 1316-1321). Expected values are typed closed forms and flow through no tested code path; the computed values run through the genuine production functions (kraus_blocks/direct_responses in primary; kraus_from_direct/aggregate_responses in independent). My execution: both lanes reproduce the closed forms to 4.5e-16 at both frozen pairs; a wrong outcome index fails at O(1) (~1.0), so outcome 0 = completed is fixed by the closed form, not by choice. The comparator additionally revalidates with a third construction (plain inner products, 1341-1404) and pins the stored ready vector, both projectors, and all four unitaries to the declared O6 objects at 2e-11 — which is what excludes basis games: I verified that swapping completed/other leaves the scalar responses invariant (a symmetry of the O6 problem: ⟨v|diag(1,e^{iθ})|v⟩ = (1+e^{iθ})/2 for any balanced v), so the array pinning, not the scalar check, carries that load. Caveats: with source dimension 1 the reduction is provably blind to the Kraus reshape-interleave convention (I measured discrepancy 0 at source_dim=1 vs 3.05 at source_dim=2 for a record-major variant) and it exercises selected_outcome=0 while production uses index 1 — both holes are closed elsewhere: primary's maximum_kraus_component gates kraus_blocks against the spectral construction at 32-dim/3-outcome (936-961, gated 3e-9), the independent's kraus_from_spectral-vs-kraus_from_direct residuals cover its lane, and the comparator pins expected_direct_pointer = minus_kraus[1]†·plus_kraus[1] against the w-weighted spectral pointer kernel at 3e-9 (1007-1037), independently enforcing the index-1 selection.

CONFORMANCE_TABLE (spec constant/threshold -> in code):
- n=2: yes (primary 96; independent 63)
- ell {1, √2}: yes (primary 97; independent 68; comparator pins 2e-12, 889-897)
- histories {0, 7/100, −11/100, 13/100, 4/100}: yes (primary 98; independent 69; comparator 889-897)
- pairs {(0,0),(7/100,−11/100),(13/100,4/100)}: yes (primary 99; independent 70; comparator labels 67-75)
- record eigenvalues {−√2, 0, +√2}: yes (primary 100; independent 71)
- primary steps {12,24,48}: yes (primary 101; comparator diag keys 621-629)
- independent RK4 {192,384}: yes (independent 72; comparator 631-635)
- primary quadrature (10,10,20): yes (primary 102)
- independent/secondary quadrature (12,12,24): yes (primary 103; independent 73)
- complex128/binary64: yes throughout
- hermiticity/projector ≤2e-11: yes (primary 105; comparator 45)
- record spectral completeness ≤2e-12: yes (primary 106; comparator 46)
- internal identities ≤3e-9 (Gaussian/direct, Kraus, same-history, adjoint, contraction): yes primary (107, 1428-1445); comparator recompute (47, 1096-1098); independent-lane in-lane gating absent (Finding 5)
- convergence ratio ≥3.2, per nontrivial case, no averaging: yes (primary 108, 1379-1398; comparator 48, 1136-1162)
- unresolved-tail guard 1e-15: yes (primary 1387; comparator 1144-1147)
- scalar/projector/basis-overlap ≤2e-11: yes (comparator 40-42; independent 1384)
- transported matrix ≤3e-4, every component: yes (comparator 43, 737-779; categories 53-61)
- independent tail ≤ quarter tolerance (7.5e-5), every case: yes (comparator 44, 1186-1202)
- ||J|| > 1e-6: yes (primary 1522-1524 over all t_k; comparator midpoint witness 1204-1219)
- S_conn > 20·E_conn per ell, exact E_conn max-definition: yes (comparator 49-50, 1220-1262; fourth term untransported — Finding 6, immaterial)
- Strang F·A·G·A·F at midpoints, Hermitian-eig exponentials: yes (primary 542-603, 467-474)
- classical RK4 tableau, fresh stage evaluations: yes (independent 528-546; cache is per-exact-time only)
- basis order + phases + prepropagation S from sealed metadata only: yes (independent 267-315; comparator pins exact expected S, 597-606, 680-686)
- Route-1 rerun, 1e-10, canonical hash 6dbda44... before/after: yes (primary 1158-1297)
- blocked verdict string ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_BLOCKED, every failure path blocks: yes (primary 253; independent 781; comparator 38; no warning-and-continue found in any of the 8 files)
- forbidden constants (137.03…, 0.00729…, CODATA, transforms): none found in any of the 8 scripts (grep clean)

TEST_ASSESSMENT: All three test files pass under the pinned launcher runtime (primary PASS; independent 10/10; comparator 17/17 — matches custody claims). Fences exercised: comparator — every matrix category perturbation blocks, scalar categories block, basis unitarity and unprovenanced-but-unitary overlap block, executor/attestation/manifest-self-hash/external-hash/npz-inventory/path-alias/dimension/label/non-finite/firewall-flag fences all fire, convergence/tail/sensitivity/local-identity diagnostics block individually; independent — read-allowlist rejection fires, no-primary-import verified by AST, RK4 tableau verified by 4th-order convergence control (a wrong tableau fails the 14x ratio), bump support fences, atomic overwrite refusal fires. Not exercised: every failure fence of the primary derive lane (its test is entirely happy-path: authority drift, immutable-output refusal, attestation absence, blocked-write path are untested); the independent lane's FUTURE_OUTPUTS existence fence and blocked-write path; the launcher entirely (no test of target-allowlist escape, symlink handling, runtime drift, or attestation content); the controller entirely (untested per custody, confirmed — including the isolated Route-1 rerun subprocess). Also note test_matching_transported_bundles_pass doubles as a live demonstration that copied provenance strings plus S-transported single-source arrays suffice for a sealed PASS — the exact residual of blockers 1/2. A test that "passes on a broken implementation": none found in what the tests cover; the deficiency is coverage absence, not vacuous assertions.
