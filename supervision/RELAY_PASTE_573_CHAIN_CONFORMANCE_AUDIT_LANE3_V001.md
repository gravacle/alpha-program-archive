## PASTE 573 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5] THE RECORD-CONFORMANCE AUDIT OF THE COMPUTATIONAL CHAIN

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-498.
- THE COMMISSION (from the principal's question): the computational core — the response formula, the Schur reduction, the threshold theorem, the twelve-step map — was DERIVED BEFORE the record-level lessons existed. Its typing has been repaired (V004); its MATHEMATICAL CONTENT has never been audited against the record picture. This audit does that, BEFORE the picture freezes at A2.
- The chain of record: `STAGE8_TASK5_LICENSED_CONDITIONAL_CHAIN_LANE2_V004.md`, SHA-256 `1da746c3956c6b23e17ce10c8bb5ff8e902f7afcf97c3d4eb8d717d7fd7c541a` (+ its cross-check `0fabbe5e…`).
- THE RECORD PICTURE it must be audited against (the lesson ledger, all sealed): (1) couplings are OUTPUTS of self-consistency, not inputs (the founding principle; Q-494/Q-498's END_TEST_STRUCTURAL seed); (2) laws carry addresses/supports (A3/A6 and the support-scoping episodes); (3) no selection — families, never members (A3; the axiom's output typing); (4) contravariance on cycle creation, no upward lifts (FC4; the J15 mate); (5) Maxwell content on a loop is GLOBAL — a period/holonomy, not a local density (Q-468's lawful spreading; Q-496/Q-497's theorem-with-rider: the F² symbol's local reading cannot carry the period; the harmonic pairing is the loop's charge); (6) conservation-not-source laws (Ward homogeneity, Q-494).
- Custody: audit/determination — derive nothing new beyond what the audit licenses; Dario reviews. Tag PROVABLE / PART-PROVABLE / YOURS. SYMBOLIC ONLY; no evaluation.

TASK — walk the chain's mathematical content, step by step (0 through 12):
A1. **Per step: the conformance verdict.** For each step's actual mathematics (not its gates): ALREADY_CONFORMAL (show which lesson it embodies or why none applies) / RE-DERIVABLE_RECORD_FIRST (the record-native form stated and derived if bounded — especially: does the step consume the reader or the local symbol on a scope the amendments have since restricted?) / CONTINUUM_RESIDUAL (the exact residue named, its risk to the number typed).
A2. **THE THRESHOLD QUESTION — the named suspect:** Steps 8–10 run through chi_K = p_loc[R_K], a LOCAL reading, while lesson (5) says the loop's Maxwell content is a PERIOD. Determine: is q_loop = |chi_K|·A_loop the record-native contraction functional on the actual branch, or the local shadow of a period-based quantity? If the record-native form differs, state it exactly (the period-based threshold candidate), what re-derivation it requires, and whether the fixed point K = B_ind(K) changes carrier. Do not assume either answer.
A3. **The consequence board:** which steps (if any) need record-first re-derivation BEFORE the witness certification; which are conformal as they stand; what the A2 axiom text should freeze. If everything is conformal: say so plainly — a clean audit is a valid result.
A4. **Battery:** anti-tuning (no step's verdict chosen from any desired numeric consequence); F_PLDEC where the reader appears; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_CHAIN_CONFORMANCE_AUDIT_LANE3_V001.md`
with final lines: `STEPS_CONFORMAL = list`, `STEPS_REDERIVABLE = list (+forms)`, `STEPS_RESIDUAL = list (+risks)`, `THRESHOLD = RECORD_NATIVE / LOCAL_SHADOW (+period candidate)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
