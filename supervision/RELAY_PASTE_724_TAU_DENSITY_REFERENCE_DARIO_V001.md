## PASTE 724 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / STEP 8] THE REFERENCE DETERMINATION: WHAT DOES "TAU_DENSITY" NAME?

PICKUP-ACK + LANE GUARD in force (DARIO).
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL standing laws apply, incl. the void condition: no downstream number consulted.)

CONTEXT (Q-629, GAP 2): the sealed density topology lives on a density space (holomorphic polydisc); tau_norm/tau_cyl live on the ratified completion. Whether "choose one of three" is well-posed turns on REFERENCE: when V011 (and 4d) say "tau_density — density convergence over C_ref", does that phrase NAME (a) the sealed polydisc density topology (H2-class, a different carrier), or (b) a topology on the ratified completion characterized by density convergence (same carrier as the other two)? This is a reading of sealed text, not a construction.
TASK:
II1. Assemble every sealed occurrence of the operational phrase and its aliases (object-names probed: tau_density, density convergence, over C_ref, intensive log, gamma_K, over-N_4, and the completion-class vocabulary), each block-covered. Include V011's defining uses, 4d's would-build package, the Duhamel schema, and any consumer that TYPES the object it expects.
II2. DETERMINE REFERENCE from those bytes: does any sealed use bind tau_density to the completion carrier (e.g., by applying it to completion elements, by class membership claims, by composition with completion-class maps)? Does any bind it to the density space? Display every binding with its span. The verdict: REFERS-TO-DENSITY-SPACE / REFERS-TO-COMPLETION / AMBIVALENT (uses split — display the split) / SILENT (no use types it).
II3. STATE THE CONSEQUENCE per verdict for the matrix question: which cells become N/A, whether "one of three" survives, and what the well-posed successor question is. No typing is ADOPTED here; AMBIVALENT/SILENT routes to the principal with the display.
OUTPUT: one sealed artifact `STAGE8_7A_TAU_DENSITY_REFERENCE_DARIO_V001.md`
Final lines: `OCCURRENCES = N assembled, block-covered`, `VERDICT = <one of four>`, `BINDINGS = displayed (spans)`, `CONSEQUENCE = stated`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 724_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. No common cell formed; no junction map evaluated.
