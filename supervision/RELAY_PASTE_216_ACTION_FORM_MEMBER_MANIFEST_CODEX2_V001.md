PASTE 216 — CODEX LANE 2 — THE MEMBER MANIFEST: NAME THE OPEN ACTION-FORM CLASSES

ROAD: **UNBLOCKS STEP 3, AND STEP 3 IS SEQUENCING-FORCED TO GO FIRST.** *** DOES NOT WAIT ON `B0`. ***
This is step 3.1, the one blocker that is a naming-and-provenance task rather than a conceptual one.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.

*** THIS RELAY IS SELF-CONTAINED. ASSUME NO MEMORY OF ANY PRIOR RELAY. ***

---

## 1. WHY THIS, AND WHY NOW

`alpha = 1/(4 pi K_*)`, `K_*` the root of `C_record(K) = 0`. **§5.3 of
`STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md` is the uniqueness gate** that would establish a computed
value was **forced rather than fitted**. It requires **no inequivalent admitted positive root** and
**forbids narrowing the admitted family after the root is seen** (`:581`).

**Two prior audits established the state:**

```text
STAGE8_SECTION53_ADMITTED_FAMILY_CENSUS_REACHABILITY_AUDIT_V001.md  (4411b91a)
  Section_5_3_reachable_in_principle = true | TYPE-C   condition: a FROZEN PRE-ROOT census
  "open" means NOT YET ENUMERATED, not non-enumerable

STAGE8_OPEN_ACTION_FORM_ADMISSION_TO_SECTION53_AUDIT_V001.md  (fd688838)
  S_0 and S_1 ARE admitted mutation candidates under 5.3's action-partition criterion
  the other open classes are NOT INDIVIDUALLY CLASSIFIABLE -- *** THEY LACK CONCRETE MEMBER
  IDENTITIES AND PROVENANCE ***
  open_action_form_complete_cardinality = NO_VERDICT | TYPE-U
```

*** THE CRITERION WORKS — IT WAS APPLIED SUCCESSFULLY TWICE. WHAT IS MISSING IS THE MEMBERS
THEMSELVES. THAT IS WHAT THIS RELAY BUILDS. ***

**And the sequencing is not negotiable:** the census audit's §2.3 says *"freeze it **before any root is
seen**."* **A census assembled after the number exists cannot demonstrate the family was not narrowed
to fit it** — not because anyone cheated, but because of the order of operations. *** THE PROGRAM HAS
BEEN TREATING §5.3 AS END-OF-ROAD WORK. IT IS NOT. ***

---

## 2. THE TASK — BUILD THE MEMBER MANIFEST

**2.1 ENUMERATE THE MEMBERS.** For every open action-form class, produce **concrete member
identities**. Where the corpus names a member, cite it. **Where a class is specified by a schema
rather than a list, say so explicitly and give the schema plus what would instantiate it** — that is
a real answer and must not be disguised as an enumeration.

**2.2 FOR EACH MEMBER, THE LEDGER FIELDS.** Fill what sealed text supports and **mark the rest
MISSING rather than inferring**:

```text
canonical object identity
exact domain / codomain
action form and branch choices
source / record embeddings
provenance and adoption time
*** TARGET-AWARENESS DECLARATION -- target-value-free? target-blind? historically target-aware? ***
mutation relation (how it acts as a response-changing mutation)
```

*** THE TARGET-AWARENESS FIELD IS THE ONE THAT MATTERS MOST AND THE ONE MOST LIKELY TO BE ABSENT.
A KNOWN EXEMPLAR OF WHAT IT LOOKS LIKE WHEN PRESENT:
`BID_GLOBAL_BOUNDARY_DESCENT_QUASI_FREE_COMPLETENESS_V001.md:22-25` — "target-value-free but
historically target-aware." MOST MEMBERS WILL SAY NOTHING. RECORD THE SILENCE AS SILENCE. ***

**2.3 CLASSIFY EACH** against §5.3's action-partition criterion — admitted mutation candidate, not
admitted, or **undecidable on present evidence with the reason named.** `S_0` and `S_1` are already
classified as admitted; **re-derive them independently as a control on your own method.**

**2.4 STATE THE COVERAGE POSITION.** Whether the manifest is complete, and **what a coverage proof
over all envelope axes would additionally require.** *** DO NOT CLAIM COMPLETENESS. An honest
"complete for classes X and Y, open for Z" is the useful answer; a claimed-complete manifest that is
not would be worse than none, because §5.3 would then certify against a false family. ***

---

## 3. WHAT THIS RELAY IS NOT

*** DO NOT FREEZE THE CENSUS. *** Freezing is a separate act with its own custody, and it requires the
coverage proof and an adversarial omitted-member countermodel that are not in scope here. **Build the
manifest; report its completeness honestly; stop.**

**Do not compute or approach a root.** The manifest must be assembled **target-blind**, which is the
entire point of doing it now.

---

## 4. SYMMETRIC LEADS

```text
IF THE MANIFEST CLOSES -- if the open classes turn out to be finitely and concretely enumerable --
*** LEAD WITH IT. That would make 5.3 executable after one more step and it is the single most
consequential positive available to this program right now. ***

IF A CLASS IS IRREDUCIBLY SCHEMATIC -- lead with THAT, and state exactly what instantiating it would
require. A schema is not a failure; an unmarked schema pretending to be a list IS.
```

---

## 5. RULES

- **Q-54 typing on every negative:** TYPE-R refuted · TYPE-U unbuilt · TYPE-S scope-empty · TYPE-C
  constraint-blocked. `NO_VERDICT` is legal. **Only TYPE-R is physical content.**
- **Q-92:** specifying and naming what is missing is a complete answer, not a lesser one.
- **Q-69:** never identify a flag with the object that discharges it.
- **No unproved-identity transport.** A seal reaches what it names and no further.
- **Word-boundaried matching, CASE-INSENSITIVE on identifiers.**
- *** MARK HYPOTHETICAL FLAGS INLINE. If you write a `= true` under a heading like "what does not
  exist" or "possible future verdicts", say so on the same line. Three artifacts in one day carried
  hypothetical `= true` lines that a flag scan reads as asserted results. ***
- **Report search scope**: roots, exclusions, and the queries actually run.

## 6. FENCES — VERBATIM, NON-NEGOTIABLE

- **Never touch `a32_holdout/custodian_private/`.**
- **Do not compute** alpha, `kappa_record`, `kappa_Thomson`, a coupling, a scale, a root, an
  eigenvalue, a beta function, `E_R`, `T_R`, `k_R`, or any absolute interval.
- **No comparison to any measured constant.**
- **Do not resolve the Misner-Sharp / Brown-York fork by choosing — it must be DERIVED.**
- **Report refutations; never repair them.**

## 7. CUSTODY (Q-91)

*** NO LANE RUNS ANY GIT COMMAND. *** Write the artifact in the cleanroom, compute its
`.seal.sha256` sidecar and **verify it matches**, mirror artifact + sidecar to
`alpha-program-archive/workspace/`, report hashes and paths, and **stop**. You may run
`corpus_check.py --report` on your own artifact; `--gate` is not your concern and **a red gate is
never a reason to stop working.** The reviewer verifies, baselines, commits and pushes.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
