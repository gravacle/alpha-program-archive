PASTE 407 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THE COMPLEMENT-SCOPED VERDICT DETERMINATION — DOES B_ind's CHAIN NEED THE CYCLE SECTOR AT ALL? IF NOT, COMPUTE (THE VERDICT, AT LAST)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-324 + C40)

```text
DOES THE OBJECT EXIST?   THE QUESTION does, and everything it needs EXISTS: under C40's
                         complement-scoped spec, B_ind's chain is p_loc (BACKGROUND-
                         AGNOSTIC, Q-314) composed with Pi_R,ind -- and the complement's
                         structure is COMPLETE AND DERIVED: P_src and Leg_W (unique,
                         Q-318), G^007 (closed form), the two-sector layout, Map 1,
                         the squares (Q-315), the domain machinery, the finite
                         references. The cycle sector is bounded/open (Q-324) -- the
                         question is whether B_ind's chain TOUCHES it.
IS THE VERSION CURRENT?  YES through Q-324 + C40. The void clause binds (no cycle-
                         direction inversion). R_comp's cycle-need is KNOWN and is NOT
                         this chain -- do not conflate.
ARE ITS INPUTS PRESENT?  YES -- listed above, all built/derived/verified.
```

## 1. THE DETERMINATION, THEN THE COMPUTATION

```text
1  TRACE B_ind's chain under the amended spec, object by object: p_loc's argument =
   Pi_R,ind[G_K]; Pi_R,ind's construction on the COMPLEMENT (the retarded extraction
   through Map 1 and the derived Legendre structure; the stationary evaluation AT the
   complement's own critical structure -- the derived P_src/Leg_W supply it; Q-314's
   stationarity requirement read against the complement scope). ENUMERATE every
   consumed object; verify each EXISTS in the built stack; flag ANY step that touches
   cycle-action content or an unbuilt object.
2  IF THE CHAIN IS COMPLETE AND CYCLE-FREE: *** EXECUTE IT. Assemble Pi_R,ind on the
   complement; apply the stationary evaluation the derived structure supplies; TRACE p
   SYMBOLICALLY through the assembled response. THE QUESTION OF RECORD: DOES p APPEAR
   IN B_ind's ARGUMENT -- THE PHYSICAL RESPONSE ALPHA CONSUMES -- OR DOES IT CANCEL?
   State the verdict with the COMPLETE premise chain: every DoR (008/009/013/014-as-
   amended/015, C40), every derivation (P_src, Leg_W, N, the pairing), every theorem
   (the zeros, the separations, the squares), every restriction check (Q-243/Q-279
   EXACT). This is the sentence. ***
3  IF THE CHAIN TOUCHES THE CYCLE SECTOR: name the exact step and what it consumes --
   the identification problem is then on the critical path, stated precisely.
4  KILL-PASSES: no cycle-direction inversion (the void clause); no unbuilt object
   silently consumed; no selection; restriction executed at every stage; six-account
   rows; door flags; NO EVALUATION of any number -- the verdict is symbolic.
DELIVERABLE: the chain trace with per-object existence, and EITHER the executed verdict
with its full premise chain OR the exact cycle-touching step.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
