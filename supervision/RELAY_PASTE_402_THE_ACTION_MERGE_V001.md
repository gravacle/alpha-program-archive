PASTE 402 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] LEGENDRE_ACTION_PHYS V002 — THE MERGE: DERIVED SOURCE-SIDE IN, SIX AUTHORED FIELDS RETAINED, SEAMS PROVEN (DoR-016 RESERVED)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-319)

```text
DOES THE OBJECT EXIST?   BOTH ARMS DO: YOUR derivation (d220a4dc...: P_src and Leg_W
                         UNIQUE; the one-dimensional-image theorem; the refusal) and the
                         authored V001 (2e31b8ff..., the other lane: the six residue
                         fields, R2 self-flagged weakest). THE MERGE does not.
IS THE VERSION CURRENT?  YES through Q-319. The signature-race merge discipline binds
                         (Q-291's precedent): subtract what derives, retain what must be
                         authored, PROVE every seam.
ARE ITS INPUTS PRESENT?  YES: both artifacts, the Q-254/Q-288 batteries, the proven
                         normalizations, REC_GEOM_DIAG_399.
```

## 1. THE TASK

```text
1  MERGE: V002 = the authored V001 with the derived source-side structure (P_src, Leg_W)
   installed as PREMISE-MARKED derived components (cite your proofs; remove any authored
   duplication of them); the six authored fields retained with their choice tables.
2  PROVE THE SEAMS: where the derived source-side meets each authored field -- especially
   (a) R2's constrained Legendre rule against the one-dimensional-image theorem (the rule
   must handle EXACTLY the kernel the theorem identifies, preserving the record-visible
   cycle current -- the refusal discipline is the seam's own test); (b) the measure/delta
   pairing against P_src (no double-pairing); (c) the contour against E_post and the
   Keldysh structure; (d) the domain against Q-315's built squares.
3  RE-RUN the class-formation door audit on the merged object; the battery table fresh.
4  CONSUMER STATEMENT: with V002 as premise, how the Q-316 locus program runs (steps 3-4),
   what the stationary blocks consume, and where THE VERDICT computes.
5  KILL-PASSES: removal complete (nothing authored that derived); nothing derived
   weakened; field/residue reconciliation exact; no selection; door flags; no evaluation.
DELIVERABLE: V002 with per-field provenance (DERIVED with proof / AUTHORED with table),
seam proofs, door audit, consumer statements. DoR-016 reserved; the cross-review follows.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
