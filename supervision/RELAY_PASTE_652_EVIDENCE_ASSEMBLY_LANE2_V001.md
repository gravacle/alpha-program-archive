## PASTE 652 — CODEX LANE 2 = BUILDER A (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / BUILD] THE EVIDENCE PAYLOAD ASSEMBLY (D1)

(Same Lane 2 session rules: cleanroom writes; the registrar mirrors.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-590.
- Your evidence manifest names every absence: `evaluator_build_A/inputs/structural_evidence_manifest.json` (56 checks + 3 structural fixture observations). The governing spec V005 (`f8d1a7dc…`) and your check_map.json define what each descriptor demands.
- THE LAW OF THIS RELAY: evidence is CITED, never authored. A payload is existing sealed corpus bytes (whole files or exact byte spans) referenced by path + SHA-256 + span, drawn from the sealed packet and the sealed workspace artifacts of record. If a descriptor demands an object the corpus does not contain, that entry is marked `ABSENT_OF_RECORD` with the bounded search displayed (M-2, four modes) — and the check will honestly FAIL(INPUT_INTEGRITY) at run time. No absence is papered over; no evidence is written fresh. The 24 PASS rows of the executed matrix (`bc6c3e49…`, Q-553/Q-558) are your map: their displays cite exactly the sealed spans most checks consume.
TASK:
G1. **Populate** `evaluator_build_A/inputs/evidence/` with the payload files (copies of sealed bytes, content-addressed filenames) and complete `structural_evidence_manifest.json`: per check — the payload entries {source path, source SHA-256, span, payload SHA-256} or ABSENT_OF_RECORD (+search display). The three structural fixture observations likewise (their §10 expected records are spec-fixed; the observations cite the sealed fixture subjects).
G2. **Re-run the static self-check** including the evidence-manifest validation; update inventories/hashes.
G3. **The assembly artifact** (sealed): the per-check evidence table (56 rows + 3), the ABSENT_OF_RECORD list with searches, the updated inventory, the self-check transcript, PIN CHECK, VERDICT-LINE SCOPE.

OUTPUT: evidence under `evaluator_build_A/inputs/evidence/` + one sealed artifact
`STAGE8_TASK6_EVIDENCE_ASSEMBLY_LANE2_V001.md`
with final lines: `EVIDENCE = N/56 populated + M ABSENT_OF_RECORD (+list)`, `FIXTURE_OBS = N/3`, `AUTHORED_BYTES = zero`, `SELF_CHECK = passed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal in the cleanroom, report hashes, STOP (the registrar mirrors). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
