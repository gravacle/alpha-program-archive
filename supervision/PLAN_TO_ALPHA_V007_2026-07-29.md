# PLAN TO ALPHA — V007 (2026-07-29)

Supersedes V006 (same day) and V005. Built by working BACKWARD from the corpus's own terminal
claim, FORWARD from an exhaustive open-obligation inventory, then through a COMPLETENESS
CRITIC that read the MACHINE LAYER neither prior pass touched — the evaluator scripts, the
preregistration internals, `predictions.json`, the frozen charters, and the Stage-6 Q_spec
ledger — and RAN the battery evaluator read-only. Verbatim basis:
`BOHM_V007_BACKWARD_INVENTORY_CRITIC_RESULTS_2026-07-29.md`.

V006 WAS WRONG IN THREE PLACES AND THIS PLAN SAYS SO: (1) it called the A32 second prediction
"NEW, no precedent row" — it is the 18th and LAST of eighteen sealed Open Q_spec slots;
(2) it treated the gravity material as a side route — "full gravitational action and
gravitational quantum measure" is the 2nd of those same slots, i.e. REQUIRED, not optional;
(3) it put P2 at the front of the physics queue without knowing P2's certifying precondition
is failing right now.

================================================================================
## ONE SCREEN
================================================================================
THE FINDING THAT REORDERS EVERYTHING
  *** THE VERIFICATION APPARATUS CANNOT CURRENTLY PRODUCE A SINGLE VALID REPORT. ***
  Even if alpha were computed tomorrow it could not be sealed. Three independent reasons:
   - The ladder evaluator demands a report field `subject_sha256`; the sealed spec, the
     immutable preregistration and the frozen charters all name it `bundle_sha256`. Key-sets
     are compared EXACTLY. All 27 required role-reports are rejectable.
   - The evaluator's `--subjects` argument is required=True and NO SUBJECTS FILE EXISTS in
     any root. The ladder evaluator is not merely unimplemented; it is UNINVOKABLE.
   - The script the corpus calls the sole authority for the Stage-8 verdict is unsealed,
     uses Python `assert` for its selftest, and prints SELFTEST PASS unchanged under
     `python -O` — where those asserts are compiled out and certify nothing. The corpus's own
     permanent regression obligation 7 forbids exactly this.
  ALL 35 A-matrix rows are PENDING (mechanically verified, zero discharged), and SPEC-SEAL —
  the ROOT of the ladder — requires 30 of them passed. The ladder stands at level zero.

WHAT ELSE THE MACHINE LAYER SHOWED
  - `predictions.json` is PREREGISTRATION THEATRE: sealed pass-conditions for every test that
    the evaluator NEVER READS. That is how T01/T02/T11's voided `pass:true` survive — they
    are correctly hashed WRONG ANSWERS, and content-addressing gives no protection at all.
  - The immutable preregistration carries TWO stage encodings that CONTRADICT each other in
    six places. Freezing it freezes a self-contradiction; reconciling it is a principal act.
  - The anti-numerology fence (T15) greps 52 files against a 3937-file cleanroom — 1.3%.
  - Battery evaluator, run read-only: verdict GATE5_CORE_BLOCKED, 12 distinct blockers.

AND THE PHYSICS PRECONDITION IS FAILING SILENTLY
  P2's certifying fingerprint records a HARDCODED PASS while 8 of 58 tracked hashes actually
  MISMATCH. `operator_fingerprint_current` is FALSE right now. The corpus's own
  hardcoded-literal trap, live, in the artifact that certifies the object P2 consumes.

THE ONE CHEAP FALSIFIER NOBODY IS USING
  A sealed artifact requires every dimensionless alpha-facing output to be INVARIANT under
  T_R -> lambda T_R, and says that if it is not, the route "remains blocked by the
  absolute-scale gate." It costs nothing, it falsifies rather than confirms, and it applies
  to every candidate route including the BR/ratio one. USE IT FIRST ON ANY NEW ROUTE.

================================================================================
## THE PATH — REORDERED, WITH THE APPARATUS FIRST
================================================================================

### PHASE 0 — RECORDING DEBT (LANE-EXECUTABLE, OWED SINCE TODAY)
Three principal acts issued 2026-07-29 are cited by ZERO cleanroom files: the authority-chain
precedence ruling, the C1-C5 ratification, and admission of the gravity evidence register.
The register's own same-act maintenance rule is being violated in real time. COVERED BY THE
LOADED RELAY (paste 122, items 1-3). Cost of delay: every hour, the governing chain is
provably behind the rulings that govern it.

### PHASE 1 — REPAIR THE APPARATUS  [was nowhere in any prior plan; now first]
  1a. Reconcile `subject_sha256` vs `bundle_sha256`. One field name blocks all 27 reports.
      PRINCIPAL ACT if it touches the immutable preregistration; otherwise lane.
  1b. Author the `--subjects` file mapping all 11 stages to their immutable subject hashes.
      LANE. Without it the ladder evaluator cannot be invoked at all.
  1c. Reconcile the preregistration's two contradictory stage encodings (six specific
      disagreements). PRINCIPAL ACT — it is meant to be immutable.
  1d. Seal and repair the battery evaluator: sidecar it, replace `assert` with unconditional
      checks that survive `-O`, and remove the selftest tautology (it constructs the leak it
      then detects). LANE, then re-run.
  1e. Make `predictions.json` ENFORCED: the evaluator must compare each report against its
      preregistered condition. Until then every test's pass condition is decorative. LANE.
  1f. Widen the T15 numerology fence beyond the 52-line manifest. LANE.
  *** NOTHING DOWNSTREAM CAN BE SEALED UNTIL PHASE 1 LANDS. This is not bureaucracy: it is
  the difference between a result and an unverifiable assertion. ***

### PHASE 2 — THE Q_SPEC SLOTS ARE THE REAL BACKBONE  [reframed]
Eighteen sealed Open Q_spec slots exist in the latest Stage-6 ledger and NOTHING CITES THEM.
Slot 2 is the full gravitational action and gravitational quantum measure — which makes the
gravity evidence register REQUIRED material, not a side route. Slot 18 is the A32
second non-alpha structure-sensitive prediction — and being LAST, it is reachable only after
the other seventeen, not in parallel. ACT: enumerate all eighteen, map each to its current
status and owner, and make them the plan's spine. LANE-EXECUTABLE and high value.

### PHASE 3 — THE PHYSICS, WITH ITS PRECONDITION FIXED FIRST
  3a. REFRESH THE OPERATOR ARTIFACT FINGERPRINT and report the delta (8 of 58 mismatch,
      including modules the super-Hessian imports). Do not silently overwrite: if a changed
      file alters a recorded result, that is a finding. LANE. IN THE LOADED RELAY.
  3b. Then P2 — the public boson/odd/ghost Gaussian superdeterminant, the named act never
      executed; its gate hardwires three deliverables that do not exist. SIGN DISCIPLINE
      BINDING: the 54 all-negative eigenvalues are a CUT-1 artifact of an INDUCED-ONLY
      object; Morse index is 48 at cuts 2 and 3 and every depth-screen row has a positive
      maximum. No sign verdict from the induced-only fixed window. IN THE LOADED RELAY.
  3c. Apply the T_R -> lambda T_R invariance filter to every live route. LANE, cheap, first.
  3d. O1 (generator/action form) and O3 (UV boundary) remain the standing obligations.
      O2 (record scale) is RELOCATED not dissolved by the BR route — see below.

### PHASE 4 — TERMINAL GATES (unchanged in content, now correctly ordered behind Phase 1)
SPEC-SEAL -> CORE-RESULT -> QSPEC-SPEC -> THOMSON-RESULT -> PARENT-COMPARISON ->
HOLDOUT-UNIVERSE -> PREDICTION-MAP -> ALPHA-RESULT -> HOLDOUT-RESULT -> END-TO-END ->
FINAL-CLAIM. Eleven nodes, fail-closed: any failed dependency makes every descendant false.

================================================================================
## CORRECTIONS THIS PLAN CARRIES (all from checks run today, all adversarially verified)
================================================================================
- x (BR depth) and c_R (additive counterterm) are INDEPENDENT obligations — an executed
  discriminator exists in each direction, and the BR route's OWN gate certifies it excludes
  c_R "only as an adopted postulate". THE RATIO ROUTE INHERITS BOTH.
- The BR route does NOT invert the Planck problem. C_R(x) is genuinely generated, but no
  artifact draws the G consequence, the one BR Einstein-Hilbert artifact treats G as a FREE
  INPUT with witness value 1.0, and k_R has zero substantive occurrences in the cleanroom.
  The obligation is RELOCATED to the SAME SHAPE: one dimensionless selector + one absolute
  scale, both unfixed, capacity condition ADOPTED not derived.
- Every bounded negative from 2026-07-25..28 is being re-scoped (in flight); none may be
  cited as corpus-wide until that returns.
- FINAL-CLAIM does not turn on alpha. A32 excludes the alpha datum; the terminal claim rests
  on a beacon-selected NON-alpha observable where BID and a same-alpha comparator differ at
  D >= 5. Computing alpha is necessary and NOT sufficient.

================================================================================
## WHAT WOULD FALSIFY THIS PLAN
================================================================================
If Phase 1 cannot be repaired without amending artifacts declared immutable, the program's
verification design has a structural defect that must be ruled on before any result is
produced — and that ruling is the principal's. If the T_R -> lambda T_R filter fails on the
BR/ratio route, that route is blocked by the absolute-scale gate regardless of P2's outcome.
If P2 returns no isolated positive branch on the COMPLETE coupled Hessian, the ratio class
loses its most concrete leg.

alpha_computed = false; proof_authorized = false. This plan orders work and names owners; it
derives nothing and authorizes no computation.


## ERRATUM POINTER (appended 2026-07-29, append-only; artifact not otherwise edited)

The sentence at line 86-87 — that slot 18, "being LAST, it is reachable only after the other
seventeen, not in parallel" — IS UNSUPPORTED AND IS STRUCK. Basis:
`RESULT_FENCE_INVENTORY_AND_WHAT_IS_TESTABLE_TODAY_2026-07-29.md` section 5. The eighteen-slot
enumeration carries no ordering language; the corpus's only frozen slot order is a nine-item
electromagnetic sequence that excludes slot 18; the JSON encodes the slots unordered. The real
constraint is that A32 UNMASKING sits behind ALPHA-RESULT-SEAL, not that the prediction cannot be
derived first.
