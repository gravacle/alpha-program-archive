PASTE 217 — EINSTEIN — DESCRIBE THE SURFACE: THE RECORD CELL'S INTERNAL/EXTERNAL CONVERSION

ROAD: **ADVANCES STEP 2, AND IT IS THE GEOMETRIC ROUTE'S ONLY BLOCKER.** *** DOES NOT WAIT ON `B0`.
DOES NOT NEED A BRIDGE. *** This is the object the corpus names as the crux **twice, independently,
and records as never attempted.**

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.

*** THIS RELAY IS SELF-CONTAINED. ASSUME NO MEMORY OF ANY PRIOR RELAY. ***

---

## 1. THE OBJECT

**A record cell has two geometries and nothing converts between them.**

```text
INSIDE    projective state space -- dimensionless angles, Fubini-Study
OUTSIDE   the Lorentzian causal diamond -- lengths
MISSING   *** HOW MUCH EXTERNAL LENGTH CORRESPONDS TO ONE UNIT OF INTERNAL ANGLE ***
```

**The modulus gate names the gap in its own words:** Fubini-Study *"does not fix their dimensional
conversion relative to the spacetime metric or `ell_P`."* **The Stage-10 handoff names the same
object independently as the crux and records it unattempted:** *"the causal diamond is present but
unconsulted … the cell's Lorentzian geometry never entered."*

**Call the conversion `beta`.** The geometric route expresses the coupling as a ratio of the Planck
length to a bundle fiber radius, and the modulus gate contains the **proof** that `beta` is the whole
of what blocks it:

> every `R = beta c Delta tau`, `beta > 0`, preserves the base interval, phase periodicity, topology,
> gauge covariance, and unit character **while changing `K_KK` by `beta^2`.**

*** SO A FULLY DERIVED RECORD INTERVAL STILL LEAVES A ONE-PARAMETER FAMILY FREE. THIS IS NOT
"UNESTABLISHED" — IT IS "CANNOT BE, BECAUSE `beta` SURVIVES." DERIVING `T_R` DOES NOT REOPEN IT. ***

---

## 2. WHY THIS TARGET IS UNLIKE THE ONES THAT HAVE BEEN FAILING

*** IT NEEDS NO BRIDGE. *** Six cross-construction identifications were refuted on 2026-07-29, and
six routes to `B0` closed on 2026-07-31, **every one of them for want of a map between two
constructions.** Here there is nothing to connect: **ONE RECORD CELL ALREADY CARRIES BOTH
STRUCTURES** — a causal diamond bearing a primitive projective record degree — **and both
constructions already agree that object exists.**

---

## 3. THE TASK — DESCRIBE THE SURFACE

**3.1 STATE BOTH GEOMETRIES ON ONE OBJECT.** For a single record cell, write down what is sealed
about its **internal** projective structure and its **external** causal-diamond structure, **on the
same object, with paths and lines.** *** THE CORPUS SAYS THE DIAMOND IS "PRESENT BUT UNCONSULTED."
CONSULT IT. ***

**3.2 IS THERE A CONDITION THAT FIXES `beta`?** Look for a requirement that **closes** — something
the cell must satisfy that relates internal angle to external length. Candidate directions, offered
as prompts and not as answers: closure or consistency of the cell's own boundary; matching of an
internal periodicity to an external interval; a covariance requirement that acts on both structures
at once; any condition already sealed that mentions both.

**3.3 IF A CANDIDATE APPEARS, TEST WHETHER IT CLOSES.** *** TOUCHING BOTH GEOMETRIES IS NOT
SUFFICIENT. *** The precedent to cite: `boundary_access_closure_threshold_principle_v001.md` was
typed `CELL_CONSTRAINT_ONLY` and **"RETIRED AS A STANDALONE K_R SELECTOR"** for exactly this failure
— it touched both and did not close.

*** THE BAR, AND IT IS ONE GRADE LOWER THAN OLDER PROGRAM NOTES STATE: a candidate does NOT need to
be OVERDETERMINED. Q-65 established that forcing takes three shapes and that **a require whose
EXISTENCE AND UNIQUENESS ARE BOTH DERIVED forces a number on its own.** Overdetermination is not
required, is not pursued, and was struck from S9-A (Q-76). **Permissions still never force — that
half stands.** A prior memo recording the stricter bar may be part of why this target went
unattempted; it should not deter one now. ***

---

## 4. WHAT IS SETTLED AND MUST NOT BE RE-ATTEMPTED

*** THESE ARE CLOSED BY PROOF, NOT BY EXHAUSTION. RE-DERIVING THEM IS WASTE. ***

```text
THE FIBER RADIUS IS NOT THE CELL RADIUS.

THE BUNDLE CAN BE DERIVED FROM THE RECORD CELL (LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md does it)
BUT CANNOT YIELD A RADIUS: connections on a fixed line bundle form an AFFINE SPACE, so a bundle
from record combinatorics is topological and holonomic and has NO FIBER METRIC -- while a radius
is a METRIC DATUM.

NO CURVATURE TERM WAS EVER PROJECTED for the modulus radius. C_R = 1 is an adopted Level-1 rule
with the threshold passed in as a script function argument, and the only horizon radii the program
computed are exclusion checks whose result key is named "no_horizon".
```

**Rows indexed by question at Q-07, Q-08, Q-09 in `QUESTIONS_SETTLED_REGISTER_V001.md`; the open one
is Q-10.** **Grep that register before starting.**

---

## 5. SYMMETRIC LEADS

```text
IF A CONDITION CLOSES -- *** LEAD WITH IT. THAT WOULD BE THE FIRST OBJECT THIS PROGRAM HAS DERIVED
THAT FORCES A NUMBER, AND IT WOULD UNBLOCK THE GEOMETRIC ROUTE ENTIRELY. *** State the derivation,
then attack it yourself before reporting.

IF NOTHING CLOSES -- lead with that, and say precisely WHAT KIND of condition would have to exist.
"beta is a free modulus and here is the shape of what would fix it" is a complete answer under Q-92
and is more useful than a strained candidate.
```

*** DO NOT MANUFACTURE A CLOSURE. A candidate that touches both geometries without closing is the
exact failure that retired the boundary-access principle, and reporting one as a success would cost
more than reporting nothing. ***

---

## 6. RULES

- **Q-54 typing on every negative:** TYPE-R refuted · TYPE-U unbuilt · TYPE-S scope-empty · TYPE-C
  constraint-blocked. `NO_VERDICT` is legal. **Only TYPE-R is physical content.**
- **Q-92:** specifying and naming what is missing is a complete answer, not a lesser one.
- **Q-69:** never identify a flag with the object that discharges it.
- **No unproved-identity transport.** A seal reaches what it names and no further.
- **Word-boundaried matching, CASE-INSENSITIVE on identifiers.**
- **Mark hypothetical flags inline** — if a `= true` sits under "what does not exist" or "possible
  future verdicts", say so on the same line.
- **Report search scope**: roots, exclusions, queries run.

## 7. FENCES — VERBATIM, NON-NEGOTIABLE

- **Never touch `a32_holdout/custodian_private/`.**
- **Do not compute** alpha, `kappa_record`, `kappa_Thomson`, a coupling, a scale, a root, an
  eigenvalue, a beta function, `E_R`, `T_R`, `k_R`, or any absolute interval. *** THIS INCLUDES A
  NUMERICAL VALUE FOR `beta` — DERIVE THE CONDITION THAT FIXES IT AND STOP THERE. ***
- **No comparison to any measured constant.**
- **Do not resolve the Misner-Sharp / Brown-York fork by choosing — it must be DERIVED.**
- **Report refutations; never repair them.**

## 8. CUSTODY (Q-91)

*** NO LANE RUNS ANY GIT COMMAND. *** Write the artifact in the cleanroom, compute its
`.seal.sha256` sidecar and **verify it matches**, mirror artifact + sidecar to
`alpha-program-archive/workspace/`, report hashes and paths, and **stop**. You may run
`corpus_check.py --report` on your own artifact; `--gate` is not your concern and **a red gate is
never a reason to stop working.** The reviewer verifies, baselines, commits and pushes.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
