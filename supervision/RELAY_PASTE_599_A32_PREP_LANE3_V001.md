## PASTE 599 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 6 / STEP 1] THE A32 PREPARATION

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-532. TASK 5 IS CLOSED; THE ESCROW IS OPENED (read `alpha-program-archive/supervision/DECISION_ESCROW_OPENED_2026-08-06.md` and `FABLE_WITNESS_CERTIFICATION_TASK5_2026-08-06.md` — the certification's §III is what Task 6 inherits). The fences remain: alpha_computed = false; proof_authorized = false; kappa_record_computed = false — each lifts ONLY by its own Task 6 gate in sequence; THIS relay lifts nothing.
- THE TARGET: A32 — the ratification gate any computed number must pass. Its definition and obligations live in the sealed corpus (pre-Stage-8-endgame era). REGISTER-SWEEP FIRST: grep the questions-settled register and the sealed workspace for A32 by name and by content (ratification/authorization/evaluation-gate language); the corpus-audit caution applies (seals attach three ways; the corpus is not self-contained — verify every A32-citing source by hash before consuming it).
- Custody: you consolidate and draft; Dario reviews; the principal ratifies. Tag PROVABLE / PART-PROVABLE / YOURS. Surface anchor + dependency re-audit + verb discipline mandated.

TASK:
B1. **A32 CONSOLIDATED:** every sealed source defining or constraining A32 — located, hash-verified, quoted at the load-bearing lines; A32's exact obligations stated (what it demands of a number, of the evaluation that produces it, and of the authorization chain); any ambiguity or version drift across sources displayed, not harmonized silently.
B2. **A32 APPLIED TO THE INHERITED STATE:** each A32 obligation mapped against the certification's §III — the local-shadow computation's plan (the chain at V004, the typed product, the one-sided rider, the carried branches), item by item: SATISFIABLE-AS-INHERITED / NEEDS-A-TASK-6-STEP (name which board step supplies it) / IN-TENSION (the exact conflict displayed — e.g., if any A32 clause presumes an unconditional guard or a two-sided trial, the tension with the shadow typing stated for the principal).
B3. **THE RATIFICATION INSTRUMENT DRAFTED:** the exact text the principal would ratify — A32's gate conditions bound to this computation's typed reality, the shadow typing and the one-sided rider carried INTO the gate's text (no number may pass the gate stripped of its type), the falsifier ladder cited, nothing authored beyond binding.
B4. **Battery:** F_PLDEC; anti-tuning (no gate condition chosen from any desired numeric consequence); the cross-sector-unit row; surface anchor; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_A32_PREP_LANE3_V001.md`
with final lines: `A32_SOURCES = n located (+hashes)`, `OBLIGATIONS = consolidated (+count)`, `MAPPING = (counts per category)`, `INSTRUMENT = DRAFTED`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
