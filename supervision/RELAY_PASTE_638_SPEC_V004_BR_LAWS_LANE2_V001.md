## PASTE 638 — CODEX LANE 2 (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / SUBGATE] SPEC V004: INSTALL BR-1/BR-2 AS LAW + REPAIR THE CLOSED LIST

(Same Lane 2 session rules: write and seal in the CLEANROOM only; the registrar mirrors. PRE-SEAL PIN CHECK, VERDICT-LINE SCOPE RULE, and DECLARED-CONVENTIONS RULE are process law.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-574.
- Base: cleanroom `STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE2_V003.md` = `f910815c84fd29d3dc6ca55f43ebe5d4c7015f457ddcde7bd7a69afdf81658dc`. Verify before reading.
- THE CLOSED WORK LIST (implement exactly, do not relitigate): `STAGE8_TASK6_SPEC_V003_CLASS_CHECK_CORRECTION_DARIO_V001.md` = `1f588534257ef274db43f3015ee3c587c76c114467a77acc38c62f354d248a5a` — its consolidated V004 scope table (the confirmed findings with per-item repairs and verification criteria) plus its BR-1/BR-2 statements. Also its parent check `01adfa83…` for the four residual one-liners (H12->H13; display the seven shared-assignment hunks; the two reflow claims re-classified; the seven/eight count) and the FINAL_INSERTIONS = 261 correction.
- Census source for any blocker text you touch: `BID_FULL_STACK_REVIEW_LEDGER_V003.md` = `c09f2c24…`.

TASK:
P1. **Install BR-1 and BR-2 as spec LAW** in §2 (the executable-check language): the rule texts as Dario stated them, their rationale line ("hash-pinning prevents substitution, not fabrication"), and their MECHANICAL conformance tests as named procedures any reviewer can run over the file.
P2. **Repair the closed list**: every confirmed finding (V009-04's restored directional predicate with a content-forced receiver; SP1-05's ordering atom given a real receiver; the six confirmed rows incl. MR-07's paired SYMBOLIC) exactly per the correction artifact's per-item repair column. No row outside the list changes except as BR-conformance requires — and if BR conformance requires touching a row outside the list, that is a FINDING to display, not a silent edit.
P3. **Run both conformance tests over the entire final V004** and display the results: BR-1 zero failures, BR-2 zero failures, with the test transcript in the artifact.
P4. **The certificate**, built the converged way: byte-copy + named replacements; delta table derived from the diff (declared conventions stated); counts computed by a method that survives blank lines; paired blocks diffed; PIN CHECK displayed.
P5. **Battery:** F_PLDEC; anti-tuning; M-2 (four modes); protected sections (runtime pin, firewall, aggregate) untouched with pinned hashes verifying; self verb audit under the VERDICT-LINE SCOPE RULE.

OUTPUT: one artifact, lane-tagged filename, written and sealed IN THE CLEANROOM
`STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE2_V004.md`
with final lines: `BR_LAWS = installed (+conformance tests named)`, `CLOSED_LIST = N/N repaired`, `BR1_SWEEP = 0 failures / (+rows)`, `BR2_SWEEP = 0 failures / (+rows)`, `PROTECTED_SECTIONS = unchanged`, `CARRIAGE = complete_finite_delta (+hunks, conventions declared)`, `AUTHORIZATION = not_claimed`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in either location, STOP and report — do not overwrite. Seal in the cleanroom, report both hashes, STOP (the registrar mirrors). No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
