## PASTE 556 — CODEX LANE 1 (HIGH EFFORT) — [TASK 5] C_ret: THE STATIONARY-RETURN CERTIFICATE CONSTRUCTED

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-481.
- The chain stands CONFIRMED at V004: `alpha-program-archive/workspace/STAGE8_TASK5_LICENSED_CONDITIONAL_CHAIN_LANE2_V004.md`
  SHA-256 `1da746c3956c6b23e17ce10c8bb5ff8e902f7afcf97c3d4eb8d717d7fd7c541a`. Verify before reading — its Steps 3–4 and the certificate field list (return, stationarity, branch scope, topology/restriction, D_w CLOSEDNESS in the ambient carrier, the d_w/coordinate MODULUS COMPATIBILITY) are your build specification.
- Authorities: the chain V004 + its cross-check (`0fabbe5e…`), the sealed stationary-response stock (the exact Response_K decomposition and the return-map B_w's symbolic form), Q-448 (the disconnected-domain counterexample — the permanent regression), Q-457/Q-461 (the two certificate items added by cross-checks).
- Custody: you build; Lane 2 reviews. C_ret is INDEPENDENT of EQ6 (Q-448/450) — no equalizer object enters. SYMBOLIC ONLY.
- ESCROW REMINDER: this constructs and certifies a CERTIFICATE. No fixed-point execution, no iteration, no end test, no numeric evaluation, no member binding. Steps 5+ of the chain remain untouched.

TASK:
C1. **Name the actual branch:** the stationary-return branch of the sealed stock — D_w, B_w, and the scalar domain identified symbolically from the sealed decomposition; every identification premise-marked.
C2. **The certificate, field by field per chain V004's spec:** (i) return: B_w(D_w) ⊆ D_w proven; (ii) stationarity on the branch; (iii) branch scope: interval-convexity or the absolute-continuity certificate — state which holds and prove it; (iv) topology/restriction compatibility; (v) D_w CLOSED in the complete ambient carrier (or complete in the induced d_w) — the Q-457 item, proven, not assumed; (vi) the d_w/coordinate-derivative MODULUS COMPATIBILITY — the Q-461 item, its exact condition verified on the actual branch.
C3. **The regressions:** Q-448's disconnected domain excluded HERE (this is its home per A2/M16 — show the actual branch is not it); the alternate-complete-metric attack rerun on the actual d_w.
C4. **Honesty clause:** any field that cannot be proven on the actual branch is reported exactly (which field, which obstruction) — a C_ret failure is first-order (the return route to the threshold theorem closes) and outranks a completed certificate.
C5. **Battery:** anti-tuning ledger (no field chosen from any response/threshold/fixed-point/end-test consequence); the certificate's own falsifiable void conditions stated.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_CRET_CERTIFICATE_LANE1_V001.md`
with final lines: `C_RET = CONSTRUCTED / BLOCKED (+field, +obstruction)`, `BRANCH_SCOPE = interval_convex / absolute_continuity`, `D_W_CLOSED = PROVEN / FAILED`, `MODULUS_COMPAT = PROVEN / FAILED`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
