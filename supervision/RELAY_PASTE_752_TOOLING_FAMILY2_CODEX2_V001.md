## PASTE 752 — CODEX 2 LANE (5.6 SOL EXTRA HIGH) — [7A / STEP 11] TOOLING FAMILY 2 + THE CORPUS BINDING

PICKUP-ACK + LANE GUARD in force (CODEX 2).
(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. ALL standing laws apply. FIRST-TIME-RIGHT rules 1 and 3 govern the build.)

CONTEXT: the evidence layer now has four complete/near-complete rows and sealed instances accumulating. Family 2 (finite-family/expected-ledger generation) is next per your own proposal (Q-626), and Q-653's integration binding (the sealed V009-08 corpus definition -> its CD target) belongs to generated tooling, never hand-editing.
TASK:
AD1. BUILD family 2: deterministic generation of finite families and expected ledgers from sealed instances (content-addressed inputs; fail-closed on digest/schema/span failures; negative controls displayed).
AD2. RUN it over the ready components (the complete instances from 744/745/747/748 and the batch); include the CORPUS BINDING: the generator consumes the sealed corpus definition (2584d894…) and emits the CD:C-B-V009-08 binding as generated data with its own record — no hand edit.
AD3. Census: components produced / rows advanced / remaining owners. Admission stays barred; state it.
OUTPUT: the tool + outputs + one sealed report `STAGE8_7A_TOOLING_FAMILY2_CODEX2_V001.md`
Final lines: `TOOL = built (negative controls displayed)`, `PRODUCED = N (census)`, `BINDING = generated (record digest)`, `ROWS_ADVANCED = list`, `ADMISSION = barred, stated`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, write 752_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants.
