PASTE 325 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THE TRANSPORT MAP — DOES p_ch CANCEL OR SURVIVE? (DoR-011: CONSTRUCTION ONLY)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-241)

```text
DOES THE OBJECT EXIST?   AUTHORIZED, NOT YET BUILT. DoR-011 (sealed) authorizes CONSTRUCTION
                         ONLY of the transport map: finite Hessian --> physical response
                         operator --> Delta_Phi[K;X_K]. THE DECIDING QUESTION rides on it.
IS THE VERSION CURRENT?  YES: Q-239 (skeleton: Q_N = U(1)^N; F_N, A_N^(p) descend to r_j),
                         Q-240 (no common origin; p_ch NOT the complete state), Q-241
                         (K_square verdicts choice-invariant; X_K still unfixed -- do NOT
                         supply X_K here, and do NOT define it by pulling B_K backward).
ARE ITS INPUTS PRESENT?  THE FINITE SIDE, EXACT: dGamma|0 = -i*p_ch (coherent);
                         d2Gamma|0 = p_ch(1-p_ch)*(phase bilinears), DIFFERENCE/DIFFERENCE,
                         NOISE-TYPED (artifact 8a71b6cd). Both visible quotients equal p_ch
                         alone. THE PHYSICAL SIDE, PARTIAL: Q-239's five TYPE-U fields
                         (measure, contour/i-epsilon, boundary data, unbounded domains,
                         provenance) ENTER ONLY AS NAMED TYPED PREMISES -- DoR-011 pattern.
```

## 1. THE TASK — BUILD THE MAP, TRACE p_ch, STATE THE STRUCTURAL VERDICT

```text
1  DEFINE the transport frame precisely: what object does Delta_Phi[K;X_K] CONSUME (quote its
   sealed signature), and what does the finite CTP data PROVIDE? The known typed mismatch IS
   the content: the finite Hessian is DIFFERENCE/DIFFERENCE (noise/Keldysh-symmetric) while
   the response consumes a MIXED RETARDED derivative. State the conversion the CTP grammar
   itself licenses (the standard Keldysh rotation on the ratified doubling) and CERTIFY which
   of its steps are finite-exact vs premise-dependent.
2  CONSTRUCT the map at premise level, stage by stage, each stage TYPE-P | DoR-008, DoR-009,
   DoR-011, with every use of a TYPE-U field NAMED at the point of use.
3  *** TRACE p_ch SYMBOLICALLY THROUGH EVERY STAGE. THE DELIVERABLE IS ONE STRUCTURAL
   SENTENCE: p_ch CANCELS (state the mechanism -- e.g., a ratio in which p_ch(1-p_ch)
   divides out) OR p_ch SURVIVES (state the exact functional form in which it enters
   Delta_Phi's input). PARTIAL IS LEGAL: "cancels in the noise block, survives in the
   coherent block" is a complete answer if proven. ***
4  KILL-PASSES, MANDATORY:
   (a) NO TAUTOLOGICAL NORMALIZATION -- any step dividing by a p_ch-carrying quantity must
       be FORCED by the sealed signature of Delta_Phi, not chosen to make p_ch vanish;
   (b) the retarded conversion must be DERIVED from the ratified branch grammar, not assumed;
   (c) if the conversion NEEDS one of the five TYPE-U fields in an essential way (not merely
       as a domain formality), SAY WHICH -- that names the exact continuum object the
       deciding question waits on.
5  FENCES (DoR-011): NO evaluation, NO roots, NO K_*, NO numbers, no comparison to any
   measured constant. Construction and symbolic trace only.
```

## 2. LEADS

```text
IF p_ch CANCELS -- *** LEAD WITH THE MECHANISM. It means alpha waits on NO further physical
input beyond structure already built or named. Attack it hardest: is the cancellation an
artifact of a chosen normalization? ***
IF p_ch SURVIVES -- *** LEAD WITH THE FORM. p_ch becomes THE physical input of the program,
and its determination is the final physics question. State what kind of object would fix it. ***
IF THE MAP CANNOT BE BUILT EVEN AT PREMISE LEVEL -- name the exact missing conversion; if it
is one of Q-239's five fields, that field is promoted to the program's critical path.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
