PASTE 291 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2b] REDRAFT THE ADOPTION: REPAIR FIELDS 1 AND 7

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-207)

```text
DOES THE OBJECT EXIST?   Your own proposal (9ed92995...) and the kill determination against it
                         (bc06fa98...). Read the kill artifact IN FULL before touching anything.
IS THE VERSION CURRENT?  YES.
ARE ITS INPUTS PRESENT?  Q-201's frozen tuple, unchanged.
```

## 1. THE TWO BLOCKERS — REPAIR EXACTLY THESE, PRESERVE EVERYTHING THAT PASSED

```text
BLOCKER 1  Field 1's direct-limit labels assume an UNINSTANTIATED refinement-index/path functor.
   REPAIR OPTIONS (choose and justify in the choice table, or present both):
   (a) INSTANTIATE the index functor as an object with certificate -- from Q-201's frozen finite
       complexes if they suffice; if they do not, say what is missing;
   (b) RE-LABEL: replace direct-limit labels with a construction that needs no path functor
       (e.g. labels on the FROZEN finite family with explicit inclusions, completion taken in a
       stated norm);
   (c) DISCLOSE: keep Field 1 and declare the functor as the EIGHTH adoption, openly counted.
   *** WHATEVER YOU CHOOSE, THE DISCLOSED CHOICE COUNT MUST EQUAL THE TRUE CHOICE COUNT. ***

BLOCKER 2  Field 7's left-regular representation canonically supplies a TRACE and HAAR/GNS MEASURE
   -- violating C0's exclusion of state/measure.
   REPAIR OPTIONS:
   (a) a representation class WITHOUT canonical trace/measure structure, certified as such;
   (b) keep left-regular but PROVE the canonical trace/measure is unused and inert -- with a
       certificate that nothing downstream can consume it (hard; the killer will test this);
   (c) restructure: move representation choice OUT of C0's scope entirely if the interface allows.
   *** "WE WON'T USE IT" IS NOT A REPAIR. THE STRUCTURE MUST NOT BE SUPPLIED, OR MUST BE PROVEN
   INERT. ***
```

## 2. PRESERVE WHAT SURVIVED

The four mathematical consequences (conditional) . the even spatial join . both identity traps .
the clean TYPE-S list (no rho_pre, dynamics, quotient, effects, Ward, U1, no Task 4-6 preformation).
*** DO NOT REOPEN PASSED FIELDS. A redraft that churns what passed invites new defects. ***

## 3. DELIVERABLE

The full revised proposal, V002: eleven+ fields instantiated with certificates, the corrected
choice table, the TRUE adoption count stated as a number, every page-flag
`PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION`. *** IT RETURNS THROUGH THE SAME GATE:
another adversarial pass by the other lane BEFORE DoR 008. ***

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
