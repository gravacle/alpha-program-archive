## PASTE 470 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4b] CROSS-VERIFICATION OF THE p-VERDICT COMPUTATION + THE (P3-8) DECIDABILITY SWEEP

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-391.
- Artifact under review (Lane 1's computation): `alpha-program-archive/workspace/STAGE8_TASK4B_P_VERDICT_COMPUTATION_LANE1_V001.md`
  SHA-256 `27790d53b018a84f5f02e97f68e885de0ebb332735307ae0fa433322a6053189`. Verify before reading.
- Authorities: DoR-016/017/018/019 decision files; germ V003 `231bbd1d…`; metric V005 `2a379098…`; square V004 `abf6d366…`; the response computation `be570c18…`; Q-368 through Q-391.
- Custody: the registered p-verdict happens only after YOUR verification and the registrar's entry. This is the most consequential check of the program to date.

TASK — PASS / KILL per item:

K1. **The substitution (P1).** Recompute: Shape K's D_C-jets ≡ 0; the complement/mixed blocks base-only; D_K²φ_K correct on the ratified norms; the completed response restated correctly.
K2. **The split.** Verify Response_K = Response_base(p;k) + ν·Response_flat(k) is EXACT (no cross-terms hiding anywhere — the Schur algebra with zero C-jets worked independently).
K3. **The classification (P3-5 through P3-9).** Recompute the p-derivative through the Schur complement; verify the (P3-8) criterion is exactly right; verify CONDITIONALLY_DEPENDENT is the honest maximal statement of the ratified chain (nothing already forces it either way — sweep).
K4. **The rank-port audit (P3-11).** Verify each line: no operation in the chain carries (r₀,r_ch) except through p; DoR-014's gauge theorem correctly cited.
K5. **The ν statement.** Affine, p-free coefficient = RetExtract[T_K]; verify; note the coefficient's own vanishing question as typed.
K6. **The falsifier suite.** The finite shadows, the zero theorem with jets in, reality/batching/restriction, all regressions — rerun.
K7. **THE DECIDABILITY SWEEP — the forward item.** (P3-8) is member-free and lives on the completed stationary family, and DoR-019's R5 completed-carrier identification NOW EXISTS ratified. Determine: is (P3-8) computable on the ratified completed carrier — exactly, or on the minimal stages, or via a decidable criterion (e.g. the retarded extraction's kernel structure against the base Schur derivative's image)? State what the deciding computation would consume and whether every ingredient is ratified. Do NOT run the decision — type it.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK4B_P_VERDICT_CROSS_VERIFICATION_LANE2_V001.md`
with the verdict table K1–K7 and final lines:
`COMPUTATION = CONFIRMED / KILLED (+item)` and `P38_DECISION = COMPUTABLE_NOW (+what it consumes) / NEEDS (+the exact missing object)`.
Seal (.seal.sha256 sidecar), mirror both files byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, then STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No evaluation of alpha, K_*, roots, or any numeric response value; no registered verdict; no comparison to measured constants. Exact symbolic structural mathematics is the task; if a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
