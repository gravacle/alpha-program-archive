## PASTE 691 — CODEX LANE 2 = BUILDER A (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / TRANCHE] THE INVOCATION SHAPE: PACK THE LINKAGE

(Same Lane 2 session rules: cleanroom writes; the registrar mirrors.)

CONTEXT: run 028 — `checks[16].invocation[0]: undeclared=['source_sha256','span','span_sha256']`. Your invocation records carry the span linkage as three explicit fields; B's sealed 690 contract types the exact 4-field shape (result_name, opcode, args, instance_id) with the linkage PACKED as `instance_id = <symbol>@<source_sha256>:[start,end)`. B's is the later sealed typed contract; conform to it: emit the 4-field shape, the linkage packed into instance_id (the span_sha256 content is carried by the payload digests in observed evidence — nothing is lost, state that in the artifact).

TASK: conform the invocation emission; static self-check; disclosed delta; PIN CHECK.

OUTPUT: updated producer + one sealed artifact
`STAGE8_TASK6_INVOCATION_SHAPE_LANE2_V001.md`
with final lines: `INVOCATION = 4-field, linkage packed (+nothing-lost display)`, `SELF_CHECK = passed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal in the cleanroom, report hashes, STOP (the registrar mirrors and re-invokes). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
