PASTE 359 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] P3 SECOND ATTACK — BUILD THE FOUR FIELDS FROM THE LAW SIDE (THE COMMON-ORIGIN TRACE)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-275)

```text
DOES THE OBJECT EXIST?   THE INPUTS DO, AND THEY ARE NEW SINCE Q-239: your own P3 stop
                         (f97ee43e...) names the would-builds as COMMON-ORIGIN-GENERATED
                         objects "on the discharged common-origin trace" -- and the trace
                         IS discharged now (DoR-013/014). The four fields are DYNAMICS-side;
                         Q-275 directs this second attack from the LAW side.
IS THE VERSION CURRENT?  YES through Q-275. Marks: TYPE-P | premises: DoR-008/009/013/014.
                         Rank discipline binds. The Haar kill (Q-239) binds.
ARE ITS INPUTS PRESENT?  YES: the ratified law with E_post (DoR-009); F_N = P_0 + Z_N P_ch
                         (PURE PHASE) and A_N^(p); PathCert_N with its GNS_out (Q-238);
                         the sequential N<=M zero-extension; P2's projective/contractive
                         structure; the germ's finite operational certificates.
```

## 1. THE FOUR BUILDS — EACH FROM ITS NATURAL RATIFIED GENERATOR

```text
1  THE MEASURE from the law's amplitudes: the finite stages carry the law's own weights
   (the germ's finite operational certificates + the A_N family). Construct the branch-
   joint prequotient measure as the PROJECTIVE SYSTEM of finite-stage weights over P2's
   contractive restrictions (cylinder-set route); prove Kolmogorov-style consistency
   FROM the sequential zero-extension certificate (C7 of the law) and the DoR-008
   restriction discipline; then DESCEND through the Q-239 quotient (the descended
   nu_phys, q_* nu_phys of your own would-build list). PURE-PHASE HONESTY: F_N is unitary
   -- if the "measure" is complex/oscillatory (a CTP weight, not a probability measure),
   SAY SO AND BUILD THAT OBJECT; do not force positivity the dynamics does not have.
2  THE CONTOUR from E_post: the i-epsilon prescription is the analytic shadow of TIME
   ORIENTATION -- and the program RATIFIED a time orientation (E_post, DoR-009; the
   exchanged root REFUTED, Q-264). Construct the interacting contour/operator family
   with its i-epsilon limits taken in P2's topology, DERIVED from E_post's orientation
   of the endpoint charges (which branch is forward is not a choice here -- it is the
   ratified physics). Q-252's three-zeros discipline binds throughout.
3  THE BOUNDARY/GLUE package: take the exact finite sequential glue (Q-239 skeleton) to
   its P2-limit (dense core + contractivity); certify what survives the limit and what
   the preparation ends carry (the law's ready-side normalization from the germ).
4  THE DOMAINS from PathCert's GNS: the named unbounded operators act on GNS_out
   (Q-238); construct the common invariant closure domain from the finite PathCert
   vectors (dense by P2), prove essential self-adjointness where the pure-phase
   structure gives it; type honestly where it fails.
5  ASSEMBLE: if all four build -- U3_008_INSTANCE_EXISTS flips TRUE and
   P5_COMPLETE_INPUT_READY flips TRUE; state both with certificates. Re-certify against
   DoR-008 restriction reproduction per field.
6  KILL-PASSES: no imported measure/contour (each object's generator must be a CITED
   ratified structure); no positivity forced on oscillatory weights; every limit names
   its topology; no rank selection; the moving-tail check per field.
DELIVERABLE: P3 second attack as a certified construction or a NARROWED stop (per field:
built, or the exact ratified datum still missing). TYPE-P positives expected.
```

## 2. LEADS

```text
IF ALL FOUR BUILD -- *** LEAD WITH THE FLIP: U3's instance EXISTS and P5 is fully fed.
The wall that has defined the program's continuum boundary since Q-239 would be DOWN. ***
IF THE MEASURE IS OSCILLATORY-NOT-POSITIVE -- lead with that finding; a CTP weight is
the honest object and downstream consumers must know its type.
IF A FIELD STILL RESISTS -- lead with the exact missing ratified datum; after a law-side
AND a kinematics-side attack, what remains missing is sharply characterized.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
