PASTE 364 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THE BACKGROUND MAP FROM THE ANCHORED ORIGIN — IS THE PHYSICAL BACKGROUND THE FORCED STATE? (Q-280's ITEM (b))

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-280)

```text
DOES THE OBJECT EXIST?   THE QUESTION does. Q-252 left the physical common background
                         (Abar_*, G_*) uninstantiated with its p-dependence NO_VERDICT --
                         BEFORE the origin existed. NOW: DoR-013's anchored family FORCES
                         the invariant state (the normalized carrier identity, all three
                         anchor classes convergent, independently verified Q-266). THE
                         CANDIDATE: the physical response background IS the anchored
                         origin's forced invariant state, and its p-content is therefore
                         DETERMINED (the forced sector form), not free.
IS THE VERSION CURRENT?  YES through Q-280. Family discipline binds (no anchor member
                         selection -- the invariant state is family-uniform, which is
                         exactly why this candidate is well-posed). Rank discipline binds
                         (symbolic (r_0, r_ch) only).
ARE ITS INPUTS PRESENT?  YES: DoR-013/014, the V003/V004 artifacts (the forced-state
                         proofs), Q-252's determination (what a background must DO -- the
                         evaluation surface's partly-fixed structure), Q-279's reference
                         (what the background must restrict to at finite stages).
```

## 1. THE TASK — INSTANTIATE, THEN TEST THE IDENTIFICATION

```text
1  STATE what the background map must provide, from Q-252's own text: the common background
   Abar_* and stationary propagator G_* that the physical response is evaluated around,
   and the pullback structure (PULLBACK_STATIONARY_SET_EQUALS_CRITICAL_SET_OF_THETA was
   proved TYPE-P there -- use it).
2  INSTANTIATE the candidate: background := the anchored origin's forced invariant state
   (the normalized carrier identity on the (r_0, r_ch) sectors), transported to the
   evaluation surface through the germ's descent (d_state at the reference configuration)
   and P4's anchored branch. Construct Abar_* and G_* from it EXPLICITLY on the built
   subpackage (bounded/cylinder level).
3  TEST THE IDENTIFICATION, three ways: (a) CONSISTENCY -- does the candidate satisfy
   every property Q-252 established the background must have (the partly-fixed surface,
   the three-zeros discipline)? (b) RESTRICTION -- does it restrict at every finite stage
   to states compatible with Q-279's reference (the equal-history weights)? (c) FORCING --
   is the candidate THE UNIQUE background admissible under the anchored family, or is
   there residual background freedom BEYOND the forced state (if so, name it -- that
   freedom would be Q-252's residue surviving the origin, and the principal must see it)?
4  THE p-VERDICT FOR THE BACKGROUND: if the identification holds, derive the background's
   p-content SYMBOLICALLY: the normalized identity's charge weighting is the forced
   sector form -- state exactly how it enters G_* and whether the RETARDED extraction
   at this background inherits any p (the Q-243/Q-279 zero-blocks were AT zero source;
   the anchored background is the zero-source invariant state -- are they the SAME
   evaluation point? PROVE the relation).
5  KILL-PASSES: no anchor-member selection; no background chosen FOR its p-consequence
   (the identification must be argued from Q-252's requirements + the origin's forcing,
   never from what it does to the verdict); no continuum objects beyond the built
   subpackage (certificate level only); type every gap.
DELIVERABLE: the background map instantiated-or-refuted -- the construction, the three
tests, the background p-verdict, residual freedom named if any. Typed throughout.
```

## 2. LEADS

```text
IF THE IDENTIFICATION HOLDS AND THE EVALUATION POINT COINCIDES WITH THE ZERO-SOURCE
POINT -- *** LEAD WITH IT: item (b) of the verdict chain closes, the background is
FORCED and its retarded extraction is the already-proven p-free block. Combined with
(a) and (c), the verdict would stand one step from executable. ***
IF RESIDUAL BACKGROUND FREEDOM SURVIVES THE ORIGIN -- lead with it named; that is the
true remaining freedom of the program and outranks everything else in this relay.
IF THE CANDIDATE FAILS A Q-252 REQUIREMENT -- lead with which; the background then needs
genuine new structure and the verdict chain says so honestly.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
