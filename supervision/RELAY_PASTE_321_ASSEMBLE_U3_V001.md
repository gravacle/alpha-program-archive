PASTE 321 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2e] ASSEMBLE `U3` — QUOTIENT, MEASURE, BOUNDARY, DOMAINS, CONTOUR

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-238)

```text
DOES THE OBJECT EXIST?   SPECIFIED: U3 = quotient . descended measure . boundary/edge/gluing .
                         endpoint operator domains . predeclared contour prescription, ON C0
                         (the B0 stop spec's row). Q-194's corrections BIND: Log_0 is downstream
                         D1, NOT a U3 output; Ward compatibility is downstream D4 -- U3 supplies
                         ONLY its endpoint domains.
IS THE VERSION CURRENT?  YES. C0_008 + U1_008 built; the LAW in force; U2 partial with its known
                         open ports.
ARE ITS INPUTS PRESENT?  PARTLY -- that is the build. The gauge quotient's raw material exists
                         (the derived U(1), the sealed conventions); the contour is DECLARED in
                         sealed CTP text; the measure must DESCEND, not be imported.
```

## 1. THE BUILD — PREMISE-LEVEL, THE U1/U2 PATTERN

```text
1  INSTANTIATE ON C0_008, with certificates, marked TYPE-P | DoR-008, DoR-009:
   (a) THE QUOTIENT: the gauge quotient by the sealed vertex/branch conventions -- state the
       equivalence exactly and execute it on the finite objects (F_N, A_N are gauge-covariant;
       show their descent to the quotient);
   (b) THE CONTOUR: the predeclared CTP contour prescription, cited from sealed text and placed
       on the ratified doubling;
   (c) BOUNDARY/EDGE/GLUING data at the finite level -- what the sequential N <= M system
       already carries, certified;
   (d) ENDPOINT OPERATOR DOMAINS for the built unbounded generators.
2  THE MEASURE: *** DESCENDED, NOT IMPORTED. If a descended measure cannot be constructed from
   the ratified/sealed data, TYPE THE GAP -- do not borrow one. (The kill-passes exist for
   exactly this.) ***
3  d_U3^P premise-level, honestly labeled; residual provenance reported.
4  SCOPE: nothing from downstream (Log_0, Ward identities, response objects).
```

## 2. LEADS

```text
IF U3 ASSEMBLES -- the package's FOURTH component exists; state what 2f's certification still
needs (the comparison maps) and whether the correlator-to-Hessian map's inputs (quotient +
measure + endpoint domains, per Q-190) are now MATERIALLY PRESENT at the finite level.
IF THE MEASURE CANNOT DESCEND -- lead with the exact missing piece; that is a named gap, not a
license to import.
IF A COMPONENT BELONGS DOWNSTREAM -- boundary honesty first, as with U1.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
