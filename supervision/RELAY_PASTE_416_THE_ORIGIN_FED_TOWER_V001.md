PASTE 416 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE ORIGIN-FED TOWER — TIERS FED BY THE RATIFIED ORIGIN'S OWN GENERATED SOURCES (THE FINITE DEPTH PROGRAM, COMPUTATION FOUR)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-332)

```text
DOES THE OBJECT EXIST?   NO -- and it is the SYNTHESIS of Q-330 + Q-332: the declared
                         tower deepens but is source-conditioned; the record-only tower
                         is trivial (records export only p); BUT the theory's sources
                         are NOT external -- THE RATIFIED ORIGIN GENERATES THEM
                         (DoR-013's d_state; V007's ports consume them). The canonical
                         theory-intrinsic tower: tiers fed by Gen_Omega's OWN generated
                         sources. No declared data; no authored channel; the ratified
                         generative structure CONSUMED AS BUILT.
IS THE VERSION CURRENT?  YES through Q-332. Lane-tagged filename. Family discipline
                         binds (the origin consumed at family level; the p-neutrality
                         certificate; no anchor selection).
ARE ITS INPUTS PRESENT?  YES, ALL RATIFIED: Gen_Omega V003-as-ratified (d_state's exact
                         output on the primitives -- the generated source, in closed
                         form), germ V007 (the ports), the verified declared-probe
                         tower (the machinery: substitute the generated source for the
                         declared probe), the closed forms.
```

## 1. THE COMPUTATION

```text
1  EXTRACT the origin's generated source exactly: what d_state emits per tier under
   the ratified family-level descent (closed form, symbolic (r_0, r_ch) and p). If the
   origin's output is tier-independent (one generation, reused), say so -- the tower is
   then fed by a CONSTANT generated source and its k-dependence follows; if the
   generative structure supports tier-refreshed generation (each tier's primitives =
   the prior state -- CHECK what DoR-013's certificates permit WITHOUT new authorship),
   compute that variant too, clearly separated.
2  COMPUTE the origin-fed tower exactly: tier-k weights, amplitude, Gamma_k, D_k -- as
   functions of (k, p, ranks) alone. THE THEORY-INTRINSIC k-DEPENDENCE: growing?
   Form? (Linear echoes the declared tower; LOG lands the thread; trivial means the
   theory's own sources also cannot deepen -- a boundary-of-theory theorem.)
3  DISCIPLINE: tier-1 sealed; the declared tower as the general case (cite); identity
   behavior; restriction/reality/quotient/rank; no selection; no evaluation; door
   flags; lane-tagged filename.
DELIVERABLE: the generated source exact, the origin-fed tower in closed form, THE
THEORY-INTRINSIC k-DEPENDENCE -- or the boundary theorem if trivial.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
