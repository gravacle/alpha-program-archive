## PASTE 534 — CODEX LANE 2 (HIGH EFFORT) — [TASK 5] CHAIN V003: THE ENTRANCE GATE AND THE D_w COMPLETENESS REPAIR

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-457.
- Your chain V002 (`1069e4f4…`) was killed on exactly two items by the cross-check of record. Read it first:
  `alpha-program-archive/workspace/STAGE8_TASK5_CHAIN_V002_CROSS_CHECK_LANE1_V001.md`
  SHA-256 `9a8008b98ed48e61886e7e2d1e380dc72fe3d3a0b48480f2274b238ac05e0965`. Verify before reading — especially §2.2 (the missing entrance) and the K3 domain-completeness computation.
- Custody: builder repairs; V003 returns to Lane 1. Bounded delta only.

TASK — produce chain V003:
R1. **Install the entrance gate as Step 0/Step 1's opening premise:** the joint finite package certificate (`F_ACTUAL_JOINT_FINITE_PACKAGE_CERT`, per Q-455/Q-456 — under construction, not presupposed built), an ADOPTED completed-existence axiom (A2 — proposed, not adopted; the chain types the dependency, it does not adopt), and the inhabited actual antecedent (the membership theorem). Step 1's discharge is typed conditional on all three. Nothing downstream weakens.
R2. **The D_w completeness repair:** add to C_ret's certificate list the domain item — `D_w` closed in the complete ambient carrier (or complete in the induced metric), stated as a separate falsifiable certificate with its own witness form; propagate through Steps 4–7 (Step 4's domain row, Step 5's boundedness premise, Step 6's return-map domain, Step 7's Banach-branch completeness now CITES this item instead of ambient completeness + scope). State the failure mode it forecloses (a nonempty non-closed D_w where the iteration exits the domain).
R3. **Delta table vs V002 bounded to R1–R2; everything else verbatim.** Rerun: the disconnected-domain regression, the hidden-uniformity attack, the gate-bypass attack (a route to Step 8 without C_RET_SCOPE_w must not exist), plus one fresh attack of your choosing on the repaired entrance.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_LICENSED_CONDITIONAL_CHAIN_LANE2_V003.md`
with final lines:
`CHAIN_V003 = READY_FOR_CROSS_CHECK`, `ENTRANCE_GATE = INSTALLED`, `D_W_COMPLETENESS = CERTIFICATE_ADDED`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
