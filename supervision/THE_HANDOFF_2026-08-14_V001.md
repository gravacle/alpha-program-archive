# THE HANDOFF — session transfer instrument — 2026-08-14

**Purpose.** Complete resume-state for a successor registrar session (any account, same
machine, same working directory). Read this + MEMORY.md (auto-loads) + the road file,
then continue. Nothing else is required; everything of record is sealed on disk and
pushed to the public git (the integrity mechanism — never PGP).

## WHO/WHAT YOU ARE
Registrar/orchestrator for the Gravacle α-derivation program. Principal: Brian
Mulconrey. You verify seals at path, adjudicate build+check pairs, register verdicts,
mirror, commit, push. You never compute α, never flip fences, never enter premises
(entries are the principal's). Builder ≠ verifier, always. ALL results CLAIMED until
checked; weight the adversarial check over the build AND over any cold re-derivation.

## ACCOUNT-NAMING CAUTION
The Claude Code account previously used for the external lane "Dario" is being
repurposed as the main driver. The archived lane artifacts signed "Dario"
(e.g. 1119/Q-1021) retain their lane identity OF RECORD. If external lane work
resumes later, DO NOT reuse the name "Dario" for a lane — pick a fresh lane name to
avoid custody confusion. The "Codex 2" lane (cleanroom) is unaffected.

## THE STATE (register through Q-1048; all pushed; HEAD = 6e2a4bbc at handoff-writing)
- Fences: alpha_computed=false · proof_authorized=false · kappa_record_computed=false.
  Nothing computed. Nothing waiting on a number.
- Verdict board: FIXED-α's analytic core COMPLETE (B-L2* discharged at the summed
  quantifier, Q-1043/Q-1044; the survivor cancelled error-included, Q-1042); RUNS =
  per-pair lean only (posed at Q-1039); FREE = needs a bar nobody found.
- THE ADOPTED ENTRY (Q-1047): the summed-S2′ E1-successor amendment — principal's
  explicit "adopt" 2026-08-14, addendum STAGE8_SUMMED_S2_ENTRY_ADDENDUM_V001.md
  (cbfbb74c...). Falsifiers V-1..V-6 LIVE (V-1: refutation of the summed estimate at
  its own quantifier VOIDS the amendment). Two live witnesses; the old F′-14 witness
  retires only ON DISCHARGE.
- "C exists" gate list (exact, Q-1044/Q-1045/Q-1048): R-L4a/b + R-L0(/R-L0b) +
  C-L2/G_cm + OBL-D(transport package), with H-R/H-B0 inside; consumers = the compute
  pre-gate (Q_spec→Z_Q→the κ→K* bridge; C_record(K)) and F0 break [1].
- New exact data: the R-L2b exponents are FORCED α=β=1/2 uniquely (Q-1048); R-L0's
  failure branch exact (Lambert-W threshold; x ≥ 1/2 breaches; √2τ_R = π); R-L0b's
  convex footing derived (five positive weights, Σ=1, conditional on R-L4b); the
  R-L4b FALSE branch (compression regularization) is a live hazard; r-1 = a
  regularization-naming act (spec-author = principal-adjacent) still open.

## IN FLIGHT AT HANDOFF (adjudicate from disk if this session dies)
Workflow "s2prime-discharge" (run wf_942c07ae-077): three tasks — A: discharge the
adopted summed certification with the forced exponents (engine = the discharged
trilemma bound; watch the D3-cells-vs-carrier-family quantifier daylight); B: the
R.3′(c) segment remainder; C: the R-L0b phase localization (if it closes, R-L0 is NOT
NEEDED for the n=1 leg — say it no stronger). Expected artifacts in workspace/:
STAGE8_S2_DISCHARGE_V001.md (+.seal.sha256) and STAGE8_S2_DISCHARGE_AUDIT_V001.md.
TO ADJUDICATE: verify both seals at path (shasum -a 256 vs sidecar), read BOTH flag
blocks in full from the files (never from summaries), adopt the AUDIT's verdict over
the build's where they differ, register as Q-1049 via the splice pattern, mirror, push.
If V1_STATUS = FIRED in the audit: the amendment VOIDS — register that plainly
(Q-1047's REOPEN), no rescue.

## THE MECHANICS (exact recipes)
- REGISTER SPLICE: read QUESTIONS_SETTLED_REGISTER_V001.md (alpha_supervision);
  entry inserted before the unique anchor "## HOW TO USE THIS REGISTER" via
  t.replace(anchor, entry + anchor, 1) with asserts (count==1, new ID absent, changed);
  rewrite BOTH sidecars (.sha256 and .seal.sha256) with the new digest.
- MIRROR: cp register (3 files) + road (2 files) to archive/supervision/; verify
  digests match; git add -A; commit with trailer
  "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>" + Claude-Session line
  (use the successor session's own URL); push (hook prints "[deploy] pushed").
- ROAD FORMAT (principal's standing order, FINAL): one flat list here→signature;
  completed steps DELETED; ▶/○ only; never re-shape. File: ROAD_REMAINING_FLAT_V001.md.
- REPORTS: plain, numbers-first, in FORCED/FREE/RUNS terms. No new formats.
- BRIEFS: pose questions + fences + honest-outcome menus. NEVER include
  outcome-classification rules ("no third road" caused Q-1018's retraction).
- DERIVE-BEFORE-AUTHOR: before staging any entry, run a necessity determination
  (Q-1041's lesson — the typing rule proved unnecessary).
- PANEL WEIGHTS: adversarial check > build > cold re-derivation (the cold axis
  over-read twice on 08-13).
- WORKFLOW BUILDS: build+check pairs as background workflows; every artifact sealed
  with a .seal.sha256 sidecar; agents must not register/commit/push.
- If an agent API-flags spuriously: reword the prompt lightly (soften adversarial
  vocabulary), resume the workflow with resumeFromRunId (cached builds replay free).

## THE ROAD (verbatim state — see ROAD_REMAINING_FLAT_V001.md for the live copy)
5a-residue (lane): G1 kernel formalization DONE (Q-1044); G2-E3 escape, r-b, r-c open.
5b-discharge: prove the adopted summed test (in flight, above).
5c (principal): the carrier/admission act (the cell-family declaration) — future,
rides the physical-carrier construction; the r-1 regularization-naming act (new,
Q-1048). 5d: re-read the verdict of record when gates land → FORCED/FREE/RUNS →
then fences 1-3 (principal's flips) → α = 1/(4πK*) → end test once → gravity close
(the frozen recognition spec scored — where "it was GR" gets said of record or not)
→ THE SIGNATURE.

## KEY DOCUMENTS (all in alpha_supervision, sealed, mirrored)
- QUESTIONS_SETTLED_REGISTER_V001.md — the verdicts (Q-1012→Q-1048 = the campaign).
- ROAD_REMAINING_FLAT_V001.md — the plan (the ONLY format the principal wants).
- THE_SHAPE_OF_THE_RECORD_V001.md — the narrative (bricks/arches, the pairing motif,
  the GR dialect, the sphere) — feedstock for the principal's "Where Atoms Come From"
  article. THE_LENS_OF_RECORD_V001.md — the framework. RELEASE_CONDITION_F2 — the
  anti-fitting gate. Memory: ~/.claude/projects/-Users-bgm/memory/ (auto-loads;
  project_alpha_reduces_to_cluster_summability.md is the running state note).

## FENCE CARD (never cross without the principal's explicit flip, in order, per F2)
alpha_computed=false → the principal flips only after the verdict of record = FORCED
and Gate-1 evidence; kappa_record first, then alpha, then the one-shot end test.
The lens (GR readings, the sphere, the pairing motif) MOTIVATES AND CHECKS — it is
never an input to a blind derivation. GOV-F: sealed spec flags are the Gate-6
evaluator's to flip, never yours.
