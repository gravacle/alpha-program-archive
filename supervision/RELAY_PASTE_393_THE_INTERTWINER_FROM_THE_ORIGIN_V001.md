PASTE 393 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE ORIENTATION-BEARING INTERTWINER FROM THE COMMON ORIGIN — BOTH CARRIERS SHARE ONE GENERATOR (THE VERDICT'S LAST OBJECT)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-310)

```text
DOES THE OBJECT EXIST?   NO -- and it is the verdict's LAST named object (Q-310): the
                         CTP_RELATIVE_HISTORY_TO_INCIDENCE_CYCLE_DYNAMICS_INTERTWINER,
                         orientation-bearing (the boundary computation shows CTP closure
                         != incidence closure -- the map must CARRY orientation, not
                         assume it).
IS THE VERSION CURRENT?  YES through Q-310. THE STRUCTURAL FACT THIS BUILD RUNS ON: the
                         ratified origin (DoR-013) GENERATES BOTH CARRIERS from ONE
                         primitive stratum -- d_state/the preparation channel feeds the
                         relative-history side (Z_law); the rooted SIGNED-incidence
                         realization (d_ready, which SURVIVED the selection-trap attack)
                         IS the incidence side, WITH signs = orientations and E_post
                         fixing the time-arrow. The common-input and covariance
                         certificates (Q-242 spec, DoR-013 ratified) are the
                         compatibility material. THE INTERTWINER SHOULD BE THE ORIGIN'S
                         OWN DESCENT SQUARE -- derived, not authored.
ARE ITS INPUTS PRESENT?  YES: DoR-013/V003-origin (the two primitives and their
                         certificates), germ V007, V005's cycle apparatus (B_N, ker B^T,
                         dPhi_c = u_c), the Q-310 boundary computation (the exact
                         orientation data the map must carry), the lifted zero beta_N
                         (extends in P2 norm), E_post.
```

## 1. THE TASK — DERIVE THE SQUARE, THEN DESCEND, THEN THE VERDICT

```text
1  CONSTRUCT the intertwiner as the origin's descent-compatibility square: both carriers
   generated from Omega_prim -- write the two generation maps explicitly, then the
   induced map between their images (relative-history phases <-> signed-incidence
   cycles), PROVING it carries orientation (the signs of the rooted realization + E_post
   must map the CTP forward/backward distinction onto incidence orientation -- the exact
   Q-310 boundary data is the test case: the map must send the CTP-closed pair to the
   CORRECTLY ORIENTED incidence cycle, reproducing (0,0,0,0) where incidence closure
   holds and (-2,0,0,2)-type boundaries where it does not).
2  CERTIFY: the intertwiner's naturality (restriction, quotient, adjoints), its
   provenance (every clause cites DoR-013 certificates -- if ANY clause needs content
   beyond the origin's ratified data, name it typed; do not author silently), and the
   FOUR-carrier accounting (which pairs the square connects directly, which compose).
3  THE 2PI DESCENT through the intertwiner: transport the law's second variation to the
   incidence-cycle sector; the lifted zero beta_N = 0 (P2-norm-extended) is the
   bridgehead -- does the descent PRESERVE it (the finite zeros become physical zeros on
   the cycle sector) or generate a nonzero physical mixing?
4  *** THE VERDICT: with the intertwiner derived and the descent executed -- does p
   appear in the physical cycle-sector response? All finite evidence is one-sided
   (Q-243/279/309, the lifted beta_N); if the descent preserves the zeros, CANCELLATION
   IS THE VERDICT with the full mechanism: the response lives on cycle content; p-weight
   has no support there, and the support separation SURVIVES completion. State it with
   the complete premise chain. If not, the exact nonzero. ***
5  RESTRICTION checks; six-account rows; door flags; no selection; no evaluation; the
   anti-relabeling standard on the square (it must be the origin's map, not a chosen
   identification wearing its clothes).
DELIVERABLE: the derived intertwiner with certificates, the 2PI descent, and THE
p-VERDICT with mechanism -- or the exact typed residue.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
