PASTE 384 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] EXECUTE DoR-014 AMENDMENT 1 — THE DERIVED PAIRING b := i hbar L(x)L, THEN RE-RUN DOOR D

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-301 + DoR-014 AMENDMENT 1 / C36)

```text
DOES THE OBJECT EXIST?   THE RULING does: Choice C-B is VOID (its own condition fired,
                         Q-300/Q-301); the principal ruled the pairing is a REQUIRE --
                         the replacement is the DERIVED pairing b := i hbar L(x)L, with
                         the tensor precertificate as the derivation's certificate.
IS THE VERSION CURRENT?  YES. The DoR-015 freeze stands until Door D re-runs and PASSES.
                         Door D's specification is UNCHANGED (H-B refuted -- the check
                         was right). Marks: TYPE-P | premises: DoR-008/009/013/
                         014-as-amended/015.
ARE ITS INPUTS PRESENT?  YES: the germ V004 (d4cdbb66...) with C-B's slot, the linear
                         source realization L (SURVIVING and untouched), the diagnosis
                         (b105a2c9... -- the finite identity's exact form), the Door-D
                         execution artifact (bffd7ef2... -- the failing test to re-run),
                         the ratified field layer (V005).
```

## 1. THE TASK — DERIVE, CERTIFY, RE-EXECUTE

```text
1  EXECUTE THE DERIVATION: construct the bilocal pairing AS b := i hbar L(x)L from the
   germ's linear source realization -- definition by the require, zero new choices.
   Build the EXPLICIT TENSOR PRECERTIFICATE: the certificate that the pairing IS the
   tensor square (well-defined on the physical carrier, quotient-compatible, restriction-
   natural, adjoint-compatible per the W3 precision).
2  AMEND THE GERM: issue SOURCE_GERM_PHYS V005 = V004 with C-B's slot filled by the
   derived pairing, everything else untouched (minimal diff discipline -- list the exact
   changed clauses). Re-run the germ's 14-row battery (no inherited credit on the rows
   C-B touched; cite inheritance on the rest).
3  *** RE-RUN DOOR D EXACTLY AS SPECIFIED: the raw-G rerun (the bffd7ef2 test) on the
   amended germ + ratified signature. The same-correlator identity D_J A-bar = (i/hbar)G
   on the same carrier -- cycles AND open paths (the diagnosis showed both failed under
   C-B; both must now PASS). PASS: the freeze lifts and the chain resumes. FAIL: the
   amendment is wrong and the principal hears it first. ***
4  IF DOOR D PASSES: run the lifted chain as far as it goes (the 382 task's steps 3-5:
   lifted G, sector structure, p-content per block, connected subtraction, accounting
   rows, the p-trace and updated remaining-objects list).
5  KILL-PASSES: no new choices anywhere (the derivation must cite L only); minimal diff
   on the germ; no selection; no unflagged completion; restriction checks (Q-243/Q-279)
   executed at every stage.
DELIVERABLE: the derived pairing + precertificate, germ V005 (minimal diff), the Door-D
re-execution verdict, and (on PASS) the lifted chain with accounting and p-trace.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
