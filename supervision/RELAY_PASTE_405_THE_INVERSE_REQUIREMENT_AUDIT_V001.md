PASTE 405 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] WHAT REQUIRES THE FULL-QUOTIENT INVERSE? — THE AUDIT THAT DECIDES THE TRILEMMA (PRINCIPAL'S RULING)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-322)

```text
DOES THE OBJECT EXIST?   THE QUESTION does (Q-322, the principal's ruling): the trilemma
                         turns on ONE demand -- the response contract's two-sided inverse
                         on the FULL physical quotient, INCLUDING the cycle/loop sector.
                         READING B says that demand is an imported convention: the loop
                         sector is TOPOLOGICAL (zero stiffness by finite authority --
                         flat germs have no record shadow), and invertibility was only
                         ever required on the propagating complement. THE AUDIT DECIDES.
IS THE VERSION CURRENT?  YES through Q-322. The Q-270/Q-314 audit discipline binds:
                         sealed citations only; check what each consumer's own machinery
                         uses, not what its words evoke.
ARE ITS INPUTS PRESENT?  YES: the sealed response-contract texts (the H_R/inverse
                         requirements wherever they are stated -- v004's B_ind and
                         on-shell Delta_Phi clauses per Q-314's citations, the Thomson
                         texts, the Q-313 precedent's own scope), the built structures
                         (the complement's response: invertible, p-clean, DONE; the
                         kernel: all-zero finite data), Q-322's trilemma artifact.
```

## 1. THE AUDIT

```text
1  FOR EACH sealed consumer of the inverse (B_ind's Pi_R,ind, on-shell Delta_Phi,
   kappa_Thomson, any response quotient): quote the clause that demands inversion and
   determine its DOMAIN as written -- the full physical quotient, or the sector the
   consumer actually probes? Check the consumer's own machinery: does ANY sealed
   output ever apply the inverse to a pure-cycle direction, or do all applications
   factor through the propagating complement (where the inverse EXISTS and is built)?
2  THE Q-313 PRECEDENT'S SCOPE: it refused SUBSTITUTING a zero correlator block by a
   zero action Hessian -- does it also MANDATE a nonzero Hessian on the cycle kernel,
   or only forbid the substitution trick? Read it at source; the difference decides
   whether Reading B violates it or respects it.
3  FINITE AUTHORITY ON FLAT GERMS: state the theorem cleanly -- a flat germ's every
   finite restriction is zero; the ratified basis (DoR-008's falsifier + the finite-
   authority battery item) then classifies nonzero flat transverse content as record-
   invisible. Is there ANY sealed clause granting such content physical standing?
   (The tail lineage says no three times -- Tail_src, Tail_raw, the door discipline --
   but CHECK the function-space case explicitly.)
4  THE VERDICT: COMPLEMENT-ONLY (the sealed contract demands inversion only where the
   consumers probe -- Reading B by citation: the loop sector is topological, the
   transverse action is ZERO on the program's own quotient, the trilemma DISSOLVES,
   and state exactly what the verdict chain then computes and on what) / FULL-QUOTIENT
   (quote the clause -- Reading A's non-perturbative route becomes the program's next
   physics, stated) / SPLIT (per consumer).
5  KILL-PASSES: citations only; no dismissing a clause by taste; the PROVABLE/YOURS
   tags; no evaluation.
DELIVERABLE: the per-consumer inverse-domain table with quotes, the Q-313 scope reading,
the flat-germ standing determination, and the trilemma verdict.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
