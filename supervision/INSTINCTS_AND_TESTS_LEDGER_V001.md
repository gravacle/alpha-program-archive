# INSTINCTS & TESTS LEDGER — V001

**Purpose.** Capture the **instincts** (bare-surface hypotheses — often the principal's) as first-class, tracked objects, AND the results of *testing* them — with an explicit **MACHINERY-REVERSION status** on every test. The lanes default to reasoning *inside* the accumulated apparatus and pull truth-seeking questions back into it, so a result is not surface truth unless it stayed on bedrock. An instinct is never settled by a machinery-reverted test. This ledger outranks the register for surface-truth questions: the register records what the machinery produced; this records what the *bare surface* actually says.

## THE MACHINERY-REVERSION PROBLEM (the meta-finding, 2026-08-13)
The lanes (Fable agents, Codex, Dario) reason within the accumulated apparatus — the carrier, K_KK, the metric, "α rides a scale," the census framings — because that is what the corpus and their context supply. A bare-surface question gets answered in machinery terms; its result is contaminated, not surface truth. **GUARD:** every bare-surface test brief must (a) NAME and FORBID the machinery imports, (b) work on bedrock only, (c) output `MACHINERY_INVOKED = <no | yes: what>`; every checker must verify whether the test reverted to machinery. A machinery-reverted result is logged INCONCLUSIVE (reverted), never as settling an instinct.

## FRAMING — THE ETHER PRECEDENT (principal, 2026-08-13)
The scale-at-the-origin may be this program's **ether**: an entity the reigning machinery insists must exist ("couplings need a scale," as classical physics held "waves need a medium"), whose *absence* keeps showing up as null/UNDETERMINED results, and whose accommodation drives ever-more machinery (the imported KK scale and circular gravity = our Lorentz contortions: coherent math, mis-located ontology). The resolution precedent is Einstein's: **do not measure the ether better — deny it exists at that level**, and the "problems" dissolve as artifacts. Discipline: the ether went out because the scale-free theory (SR) was *confirmed*. So this framing MOTIVATES the test; it is settled only by the bare surface actually determining α with no scale. If the bare surface genuinely fails to fix α, the analogy breaks and we record that.

## BEDROCK (genuinely surface-derived, machinery-free — the ground tests stand on; itself falsifiable by a bare-surface test)
- The record is **CONNECTION-ONLY** — no metric.
- **Phase-rich / amplitude-poor.**
- Natively **DIMENSIONLESS.**
- **Forces all dimensionless content.**

## MACHINERY (imposed apparatus — NOT truth; candidates to throw out)
- The Λ^even(C⁵) carrier.
- K_KK, the fiber radius R, the metric, ℓ_P (all length/scale objects).
- **"α rides an absolute scale that doesn't cancel" (Q-01)** — derived *through* the carrier/KK; suspected artifact.
- The imported KK gravitational action (Q-1093, circular).
- The **Finish-A / Finish-B framing** itself — it presupposes a scale question.

## THE INSTINCTS (first-class; each: statement · why held · test · result · reversion)
| # | instinct | why held | test | result | machinery-reversion |
|---|---|---|---|---|---|
| I-1 | The origin is **pre-metric** (connection-only) so it structurally has **no scale**; expecting one at the beginning is a category error — length needs a metric, and there is none. | **PROVEN-grounded, not conjecture:** connection-only/no-metric (1101/1102, both-lane); dimensionless + forces only dimensionless content; `absolute_SI_record_duration_derived=false` (identifiability gate); scale objects (fiber radius, K_KK, metric) are ADOPTED/constructed; Q-1001 seals the record's content as NON-IDENTIFIABLE as a length | CONFIRMED (bedrock) | n/a | clean |
| I-6 | **"α rides a scale" is a POST-CONSTRUCTION relationship mis-projected to the origin.** A scale is CONSTRUCTED after the origin; before it is built there is nothing to ride, so "rides a scale" is not a well-formed statement about the bare record. | follows from I-1 (proven no-scale-at-origin) + the temporal ordering: build the record → then (maybe) construct a scale → only then can anything ride it | CONFIRMED as the correct ordering; "α rides a scale" (Q-01) re-typed as a post-construction/machinery statement, not origin truth | n/a | clean |
| I-7 (CORRECTED 2026-08-13, principal) | **Faithfulness/full source-distinguishability has UNDETERMINED status — it must be DETERMINED, not barred.** Weak distinguishability (kills n=0) is genuine bedrock; the UPGRADE to full faithfulness (resolving which winding → pins \|n\|) is either FORCED (a genuine structural feature of the record's dynamics, or required by the emergent boundary → \|n\| forced → Finish A) or IMPOSED (an epistemic demand the dynamics does not need → \|n\| free → Finish B). **METHOD CORRECTION:** the fence is "don't ACCEPT machinery blindly" = determine status before letting it be load-bearing — NOT "bar anything suspect." Barring faithfulness blindly (as the Z_Q build did to reach DOES-NOT-FORCE) is the same unexamined error as accepting it blindly (as the ether builder did to reach \|n\|=1). An entity is stripped as machinery only AFTER PROVEN machinery (as the scale was: metric needed, none exists). Faithfulness is NOT proven machinery — its status is open. | the principal's correction: we said we would not accept machinery blindly; we did not say we would bar it if it had value | TO DETERMINE (this is the founding-thesis question in correct form: does the record's dynamics / the emergent boundary FORCE full faithfulness?) | — | must be run without assuming OR barring |
| I-2 | The **bare surface fixes α's dimensionless value** from its own dimensionless content (α is a pure number, not riding anything). | bedrock ("forces all dimensionless content" + α is dimensionless) | DESIGNED | PENDING | guard to be enforced |
| I-3 | **"α rides an absolute scale" is a machinery artifact**, not surface truth. | it was derived through the carrier/KK, never on the bare surface | rides on I-2 (if the bare surface fixes α with no scale ⇒ I-3 confirmed) | PENDING | — |
| I-4 | **Scale (length/metric) emerges in the derived geometric world**, not present at the origin; α (a pure number) does not ride it. | structural (metric is emergent) | downstream of I-2 | — | — |
| I-5 | (principal, long-held) **The divergence IS the coupling** — the coupling is an *output* of the surface's self-consistency, not a stock ingredient. | [[project_divergence_is_the_coupling]]; flagged highest fitting risk | — | — | — |

## PROVENANCE-CLEAN REQUIREMENT (the hard gate on any accepted proof — principal, 2026-08-13)
Any proof/result we PICK from this analysis must pass a **full dependency audit**, separate from and stronger than the reversion flag: trace every ingredient of the claim down to surface **bedrock**, tag each node surface-native or imported, and confirm **ZERO imported-machinery-value nodes anywhere in the chain** — no imported GR length, no ℓ_P, no fiber radius R, no metric datum, no adopted/undetermined value that the surface does not itself determine. A *single* imported-value dependency **REJECTS** the proof, however clean the top-level argument looks (the reversion flag catches machinery *reasoning*; this catches a machinery *value buried in the chain* — the S16/S13/F6b failure mode, where a "surface-native" result silently stood on the imported GR scale). **The audit is run by the EXTERNAL lanes, not by the builder or the registrar.** A proof that determines α must be shown to NOT route through the imported GR scale (or any import) at any depth. Result of every audit appends here: claim → dependency chain → `IMPORTED_VALUE_NODES = none | <list>` → accept/reject.

## THE REVIEW PANEL (for a machinery-free result — two axes, three reviewers; principal, 2026-08-13)
A bare-surface result that is *also the hoped-for answer* is exposed to BOTH lineage failure modes and needs both axes covered:
- **Rigor / provenance axis — EXTERNAL, cross-lineage (Codex + Dario):** over-claims, imported values, the full provenance audit. Failure mode: reverts to machinery → fenced with the CHECKER MACHINERY-REVERSION FENCE (machinery-as-suspect allowed, machinery-as-authority forbidden; a refutation must stand on bedrock).
- **Bedrock-fidelity axis — SAME-lineage adversary (a fresh Fable, briefed to break it):** attacks the residual and the over-closure on bedrock. Failure mode: over-closure/confirms → briefed adversarially, and *a bedrock hole from the sympathetic lineage is near-decisive*.
- **DECISION RULE:** accept only if (a) no reviewer finds a BEDROCK hole (machinery-only objections do not refute), (b) provenance is clean (zero imported-value nodes, no unverifiable premise node), and (c) no reviewer reverted to machinery. Any bedrock hole from any of the three refutes.

## DEFEAT-PROVENANCE REQUIREMENT (the bare surface arbitrates in BOTH directions — principal, 2026-08-13)
Symmetric to the confirmation gate: a **refutation** (a claimed bedrock hole) counts ONLY if the hole is itself provenance-clean — a genuine surface-native defect that does **not** rest on any imported machinery concept, value, or premise the bare surface may not recognize. Just as machinery may not DEFEND the result (machinery-as-authority forbidden for defense), it may not DEFEAT it (forbidden for attack). Every claimed hole is routed to a **defeat-provenance audit**, cross-checked by the OTHER reviewers (not the registrar): does the defeat depend on something the surface does not carry? A machinery-dependent "hole" is a **FALSE defeat** — logged, but it does not sink the result. Both the confirmation axis AND the refutation axis owe provenance. A result stands unless it is defeated on bedrock the surface actually recognizes; it is accepted only if confirmed on that same bedrock.

## CONFIRMATION AXIS (attack in the sense of confirm — principal, 2026-08-13)
Refutation reviewers can only report "we could not break it" (negative evidence). A complete panel adds an independent **COLD RE-DERIVATION** — a cross-lineage party given only the bedrock and the *question*, never the subject's answer — that derives its own verdict. Independently reaching the same result (ideally the same surface-native invariant) is **positive overdetermination**, far stronger than absence-of-hole; a cold **divergence** is a red flag even if no hole was found. Accept requires: refutation axis finds no *surface-native* hole (defeat-provenance clean) AND the confirmation axis independently lands on the same result.

## PROOF-OBJECT REQUIREMENT (attach the adversarial — principal, 2026-08-13)
At a proof juncture a result is a "proof" ONLY as an **attached dossier**, never as the bare claim: the result artifact + EVERY adversarial review (all axes — external rigor + external provenance + same-lineage bedrock-fidelity), each **sealed by digest with its .seal.sha256 sidecar**, each showing what it attacked and its per-gate verdict. The adversarial reviews are first-class attached evidence, so any later audit can read the attacks and outcomes directly, not trust the registrar's summary. Reviews run as my own agents (e.g. the Fable adversary) get the SAME custody as fired-relay checks — seal, sidecar, attachment — no exceptions. The dossier is what gets registered/pushed; a claim without its attached surviving-attack record is not a proof.

## HOW TO USE (the protocol)
1. A bare-surface test names the instinct(s) it tests, **strips the machinery** (names the forbidden imports), works on bedrock, and returns `MACHINERY_INVOKED`.
2. The external checker verifies (i) the finding and (ii) **whether the test reverted to machinery** (`REVERTED_TO_MACHINERY = no | yes`).
3. Append the result here: instinct → test artifact (digest) → result → reversion-status.
4. An instinct is **CONFIRMED** only by a test that stayed on bedrock (reversion = no, checked) AND an external lane verified. A machinery-reverted result is **INCONCLUSIVE (reverted)** — it does not move the instinct.
5. The instinct can also **overturn bedrock or machinery**: if a clean bare-surface test contradicts a booked (machinery) finding, the booked finding is flagged as artifact; if it contradicts stated bedrock, bedrock is re-examined.
