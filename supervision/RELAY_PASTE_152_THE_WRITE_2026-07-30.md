PASTE 152 — LANE 1

2026-07-30, CODEX LANE 1. Snapshot; later rulings override. Lane 2 is still on the premise classification under paste 149 — do not touch premise-classification, `C_R`, `K_bare`, ER-A or A6 artifacts.

Your composition typing is verified (`1fa7c918…`) and registered as Q-43. **The graded-tensor result corrected two of my readings — the record is a monomorphism rather than a quotient, and crossed product is neither specified nor forced. And the record-without-source test SPLITTING is the most useful thing in it:** a carrier exists before source action; durability requires source, write and nonreturn.

**ONE ITEM, AND IT FOLLOWS DIRECTLY FROM THAT SPLIT.**

Fences unchanged. `alpha_computed = false; proof_authorized = false`. Never touch `a32_holdout/custodian_private/`.

---

### THE CHAIN, AND THE PRINCIPAL HAS CONFIRMED ITS PREMISE

Your split says the threshold governs **durability**, not existence. **The principal confirmed that as the theory's own content**, so it is no longer a reviewer reading.

Durability is specified in named parts. `CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md:17-30` — a durable public record is the compatible outgoing sector of a future-directed Lorentz-covariant causal exhaustion, whose parts are **the finite reversible WRITE**, **thresholded source-root NONRETURN**, and **completed-record INVARIANCE**. V002`:10-30` sharpens to nonreturn plus exact completed-record persistence.

*** ONLY ONE OF THOSE CAN CARRY A STRENGTH. *** Nonreturn is spectral — Riemann-Lebesgue, no source point spectrum — so it holds or it does not; no dial. Completed-record persistence is exactness; no dial. **The WRITE is the only part the source performs and the only one that could carry a coupling strength.**

**HYPOTHESIS (OBS-05): `alpha` is the impedance of the finite reversible write.** Not of the carrier — your graded tensor has source and record observables COMMUTING, so no coupling lives there. Not of the composition. Not of the record sector, which is a monomorphism and whose direct-limit principle explicitly keeps source and record results distinct. **The write is the only place a source-to-record strength can live.**

And it would explain `K_*`'s shape: the onset asks at what strength a write first produces a durable record — **a threshold on the write's strength**, which is "the value at which a durable record first forms" stated in terms of the operation rather than the outcome.

### THE TASK — AND I WANT IT KILLED IF IT IS WRONG

1. **IS THE "FINITE REVERSIBLE WRITE" SPECIFIED AS AN OPERATION AT ALL?** At file:line, from sealed text. What is it — a unitary, a channel, a map on the graded-tensor base, a family? State its type.

2. **DOES IT CARRY A PARAMETER?** This is the whole question. **If the write is specified without any strength, magnitude or coupling parameter, OBS-05 IS WRONG and you should say so plainly.** A clean negative here is worth more than a strained affirmative — the observation was recorded as UNCLAIMED precisely so it could be discarded cheaply.

3. **IF IT CARRIES A PARAMETER, IS THAT PARAMETER `K`?** Type it, do not name-match. `K` is the Maxwell stiffness; a write-strength parameter that happens to be called something else may or may not be it. **Answer by the role the parameter plays in the equations, not by its symbol.**

4. **AND IF IT IS `K`:** does the thresholded-nonreturn condition then constitute a condition ON `K` — i.e. is "the write is strong enough that the source root does not return" an equation in `K`? **That would be `C_record(K)` in different clothing, and if so say exactly how the two relate.**

**DO NOT** construct the write, supply a parameter it lacks, solve for `K_*`, evaluate a response, or run the mutation audit. **F-GK3 stands and Q-37's adoption lapsed — no licence to fill in what is not there.**

---

REPORT BACK: hashes, exact committed paths, gate verdict, and the four answers in order. **If the write carries no parameter, lead with that and OBS-05 dies today** — the observations register exists so that a discarded observation costs one turn, not a charter.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
