PASTE 141 — LANE 1

2026-07-30, CODEX LANE 1. Snapshot; later rulings override. Lane 2 is on the observable-bridge recovery
task under paste 138 — do not touch slot-18, A32, impedance or comparator artifacts.

**THIS IS THE CONSTRUCTION. Everything before it has been specification, typing and elimination. Section 1
of your own spec names eleven missing pieces and none has been attempted.**

Fences unchanged. `alpha_computed = false; proof_authorized = false`. Never touch
`a32_holdout/custodian_private/`.

---

### THE ELEVEN, FROM YOUR OWN SPEC

Complete `S_CTP` · complete source-record-field Hilbert space and `rho_pre` · nonzero differentiable
`Log_0` neighborhood · gauge-fixed physical quotient · invariant spacetime/contour measure · CTP branch
metric, reality condition and index ordering · physical Dyson kernel from the raw contour correlator ·
microcausal support proof and global Dirac boundary-form vanishing · induced boundary displacement ·
fixed-total-charge symplectic reduction · boundary gauge orbit and edge variables.

### ITEM 1 — ORDER THEM, AND SAY WHICH ARE RECOVERABLE RATHER THAN NEW

Two questions, both cheap, both structural:

**(a) DEPENDENCY ORDER.** Which of the eleven presuppose which? I expect `S_CTP` and the Hilbert
space/`rho_pre` near the root and the boundary/edge items late, but I am guessing and you should not.
Produce the order, with the dependency stated for each edge at file:line where the corpus fixes it, and
mark any edge you are inferring rather than reading.

**(b) RECOVERABLE OR NEW?** For each of the eleven: does sealed material already supply it, partially
supply it, or not address it? **State roots and exclusions; report FILE LISTS, not counts alone.**

**Why (b) is not optional: five times today the missing object turned out to already exist, misdescribed.**
The rescaling exclusion had a supersession note in the next version. F-FL1's answer was rank 1 of an
existing ranked table. The stitching rule decomposed into two objects already named in a sealed artifact.
O7 turned out to be a different map. **Search before deriving is now the most-cited discipline in the
register, and it has never once been wasted.**

### ITEM 2 — THEN DERIVE THE FIRST PIECE THAT HAS NO UPSTREAM DEPENDENCY

**In the same turn, if item 1 leaves time.** Not all eleven. One — whichever the order puts first with
nothing above it.

Constraints, all already frozen and none new:
- **F-GK3:** any premise the derivation needs beyond the current stack is declared AT THE OUTSET, in the
  artifact, not discovered at the end. An undeclared premise found late voids the result.
- **The rank-6 conditionality stands** (Q-22, spec Section 0): this construction is conditional on the
  induced-only axiom and `K_*` will be determinate-but-conditional, not absolute. Do not re-litigate it and
  do not let a derivation quietly assume otherwise.
- **F-GK5:** do not solve for `K_*`, evaluate any response, or run the mutation audit. Those come after
  the gates, and the gates are not fully frozen — tolerances are still deferred.
- **F-GK7:** nothing may be defined so as to make a later condition pass.
- Append-only. A new artifact citing the spec and its amendment; V001's bytes unchanged.

**IF THE FIRST PIECE TURNS OUT TO NEED SOMETHING UNDECLARED, STOP AND REPORT THAT INSTEAD.** A named
missing input is a result. A derivation that quietly supplies its own premise is worse than no derivation,
and F-GK3 exists precisely to catch it.

### A NOTE ON WHY THIS IS WORTH DOING NOW

Lane 1 has spent the session on specification and typing checks, and every one of them earned its turn —
the spec, the amendment, the modular refutation, the O7 typing. **But none of them produces a number, and
neither does anything lane 2 is doing.** Both lanes have been working gates on whether a number would
COUNT. The eleven pieces are the only work on the board that would produce one.

If paste 140 returns `WITNESS CONSTRUCTIBLE`, the finite-cell architecture is obstructed and this relay is
superseded before you start it — **check that verdict first and say so if it supersedes this.** If it
returns `PROVABLY INSULATED` or `UNDETERMINED`, proceed.

---

REPORT BACK: hashes, exact committed paths, gate verdict, the dependency order with inferred edges marked,
the recoverable/new classification per piece with file lists, and — if reached — the first piece with its
declared conditions. **If item 1(b) finds that several of the eleven already exist in sealed material, lead
with that**: it would mean Section 1 is smaller than it looks, and that has been the shape of five findings
today.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
