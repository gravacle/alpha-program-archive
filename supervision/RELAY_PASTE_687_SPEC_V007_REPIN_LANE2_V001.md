## PASTE 687 — CODEX LANE 2 = BUILDER A (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / TRANCHE] SPEC V007 + THE COORDINATED RE-PIN

(Same Lane 2 session rules: cleanroom writes; the registrar mirrors.)

PREFLIGHT: register head Q-610. Base spec V006 = `1b8b03e4…` (V005 = `f8d1a7dc…` remains the currently pinned chain spec). The convictions: Q-608's four V006 findings; Q-610's V009-06 criterion fault; 683's fixture-span drift (+2921) and P0/SPEC-INCOMPLETE precedence indeterminacy; 686's byte-span-linkage field for Builder A to emit.

TASK — one coordinated move:
E1. **Spec V007** (byte-copy from V006 + named replacements): (a) the four Q-608 findings — the insertion count corrected; H03's silently re-scoped no-optimizer fence RESTORED to all 14 opcodes; the corpus law wired into the closed opcode table (`SEALED_CORPUS_DEFINITION` schema-defined); the law's implementation-obligation clause restored in full; (b) V009-06's criterion re-rendered EXPLICIT: the single-authority form — DAG.success (parse/typing/acyclicity/parent-closure over the one principal-ruled encoding) + the byte-grounding digest check; state in the row that graph and required_parents are one sealed object BY PRINCIPAL RULING and the comparison clause is discharged by that identity — never a synthesized compare(X,X); (c) the three fixture spans corrected to V007 byte offsets (computed, shown); (d) one precedence clause: P0 integrity failures precede SPEC-INCOMPLETE classification; (e) the corpus-membership fifth false-negative surface registered in §12.4.
E2. **The coordinated re-pin:** parent SPEC_SHA256 -> V007; check_map regenerated (exactly one row digest changes — display it); the evidence manifest's spec references updated; your emitted evidence rows gain the byte-span-linkage field per 686's specification. Note for the registrar's relay to B: B's spec const re-pins in parallel.
E3. Carriage complete finite delta (both the V006->V007 spec delta and the package delta); static self-check; PIN CHECK.

OUTPUT: spec V007 + updated package + one sealed artifact
`STAGE8_TASK6_SPEC_V007_REPIN_LANE2_V001.md`
with final lines: `V007 = 5 repairs (+delta)`, `REPIN = parent+check_map+manifest (+the one changed row digest)`, `SPAN_LINKAGE = emitted per B's spec`, `SELF_CHECK = passed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal in the cleanroom, report hashes, STOP (the registrar mirrors; B re-pins; then run 026). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
