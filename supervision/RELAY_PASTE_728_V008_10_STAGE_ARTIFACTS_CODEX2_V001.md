## PASTE 728 — CODEX 2 LANE (5.6 SOL EXTRA HIGH) — [7A / STEP 11] V008-10: THE STAGE ARTIFACTS LOCATED AND BOUND

PICKUP-ACK + LANE GUARD in force (CODEX 2).
(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. ALL standing laws apply — searched spaces, object-names probe, span coverage, writer exclusion.)

CONTEXT (Q-633): the CS:C-B-V008-10:seal-stage-graph instance is PARTIAL — the name-parent adjacency is grounded; missing are the CONTENT-ADDRESSED STAGE ARTIFACTS, their DIGEST PARENTS, and the PARENT-MAP ROOT. These are locatable: the sealed provenance chain names the stage documents; binding them is location + hashing + root computation, not derivation.
TASK:
MM1. For each stage node in the grounded adjacency: LOCATE the sealed artifact realizing it (search the program space; object-names probed; searched spaces stated). Bind path + full-file digest + seal-attachment mode (sidecar / packet-manifest / sealed-inventory). A stage no sealed artifact realizes is ABSENT-OF-RECORD, named — never approximated by a near-match.
MM2. Compute digest parents per the adjacency and the PARENT-MAP ROOT by the sealed root formula (cite it; if no sealed formula exists for this root, that is a named gap — do not invent one).
MM3. Re-run your compiler on the completed (or honestly still-partial) instance; report the conformance outcome either way.
OUTPUT: updated instance + one sealed report `STAGE8_7A_V008_10_STAGE_BINDING_CODEX2_V001.md`
Final lines: `STAGES = N located / M absent-of-record (named)`, `ROOT = computed per <cited formula> / gap named`, `COMPILE = bound / SCHEMA_CONFORMANCE (remaining fields named)`, `ADMISSION = barred, stated`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, write 728_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
