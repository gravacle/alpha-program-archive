PASTE 408 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE COMPLEMENT-SCOPED STATIONARY RESPONSE PACKAGE — φ CARRIED SYMBOLIC, BOTH ROUTES COMPUTED, THE FAMILY-WIDE VERDICT IF FLATNESS KILLS THEM

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-325)

```text
DOES THE OBJECT EXIST?   NO: the COMPLEMENT_SCOPED_PHYSICAL_STATIONARY_RESPONSE_PACKAGE
                         (Q-325) -- the LAST object before the verdict. Its two open
                         cycle-dependence routes both consume the cycle action, and every
                         admissible cycle action is ALL-ORDERS-FLAT ON THE SECTION.
IS THE VERSION CURRENT?  YES through Q-325 + C40 (no cycle inversion -- the void clause).
ARE ITS INPUTS PRESENT?  YES: the derived Legendre pair (P_src, Leg_W -- UNIQUE, Q-318),
                         G^007, the two-sector layout, Map 1, the squares (Q-315), the
                         domain machinery, the finite references, the flat-family
                         characterization (Q-322/324: all-orders-flat on the active
                         section; exactly-constant source cosets).
```

## 1. THE BUILD — φ SYMBOLIC, EVERYTHING ELSE EXACT

```text
1  CARRY the cycle action as a SYMBOLIC UNKNOWN phi, constrained only by its PROVEN
   properties: smooth; all-orders-flat on the active section; reality-covariant;
   quotient-compatible. NO member selected, NO form assumed -- phi is the whole
   admissible family at once.
2  CONSTRUCT the complement-scoped stationary response: the stationary structure the
   DERIVED Legendre pair supplies on the complement (its critical/duality relations are
   Leg_W's own first-order structure -- Q-318's uniqueness is the license); the
   stationary 2PI blocks on the two-sector layout with phi in place.
3  *** ROUTE (a): compute the cycle-to-complement MIXING blocks exactly, as functionals
   of phi. The finite mixing is ZERO at every stage (Q-309, exact) and the source-level
   descent preserves it (Q-313). Determine: does the STATIONARY evaluation's mixing
   depend on phi ONLY through phi's on-section jets/derivatives? IF YES: flatness kills
   it for EVERY member -- the mixing is ZERO family-wide, PROVEN. If any phi-dependence
   survives off-jet (a genuinely nonlocal consumption), name its exact form. ***
4  *** ROUTE (b): the stationary solution G_K -- solve the complement's stationarity
   with phi carried. Does G_K depend on phi? Same test: if only through on-section
   derivatives, G_K is phi-INDEPENDENT family-wide. ***
5  *** IF BOTH ROUTES DIE BY FLATNESS: assemble Pi_R,ind[G_K] on the complement --
   COMPLETE, family-wide over the open cycle sector -- and TRACE p. THE VERDICT, with
   the premise chain AND the uniformity theorem (the verdict holds for EVERY admissible
   cycle action -- the open physics cannot change it). If a route survives, the exact
   phi-consumption is the finding. ***
6  KILL-PASSES: no cycle inversion; phi's constraints cited not invented; restriction
   (Q-243/Q-279) executed; six-account rows; door flags; no selection; no evaluation.
DELIVERABLE: the package, both routes' phi-dependence computed exactly, and EITHER the
family-wide p-verdict with premises + uniformity OR the surviving consumption named.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
