PASTE 234 — CODEX LANE 1 — IS THE HELD-FIXED BRANCH DERIVABLE, OR IS IT AN AXIOM?

ROAD: **ADVANCES STEP 2.** *** EVERY RESULT ON THE GEOMETRIC ROUTE IS CURRENTLY CONDITIONAL ON THIS
UNDERIVED CHOICE. IT MUST BE SETTLED EITHER WAY — DERIVED, OR NAMED AS AN AXIOM AND PRICED. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.

*** THIS RELAY IS SELF-CONTAINED. ASSUME NO MEMORY OF ANY PRIOR RELAY. ***

---

## 1. THE FORK, AND WHAT DEPENDS ON IT

The geometric route computes `alpha` as a ratio on `ds_5^2 = g_mu-nu dx^mu dx^nu +
R^2 (d-theta + A_mu dx^mu)^2`. A parent construction supplied the normalization and the definition
(`STAGE8_PARENT_KK_ACTION_ELL_P_DEFINITION_AND_REDUCTION_V001.md`, `01c4c0db…`):

```text
S_5 = (1/2 kappa_5^2) integral sqrt(-G) R_5        ell_P^2 := kappa_5^2 / (2 pi R_0)
*** THE FIBER FACTOR 2 pi R_0 ENTERS THE 4D EINSTEIN-HILBERT COEFFICIENT. IT DOES NOT DISAPPEAR. ***
```

**And a determination established the fork and that nothing settles it**
(`STAGE8_KAPPA5_ELL_P_FUNDAMENTAL_FORK_DETERMINATION_V001.md`, `71569dc8…`):

```text
kappa_5 HELD FIXED   the 4D coefficient scales with fiber volume; ell_P is NOT constant.
                     *** K_KK -> beta^3 K_KK, AND THE SEALED "changing K_KK by beta^2" IS
                     BRANCH-CONDITIONAL AND **FALSE ON THIS BRANCH**. ***
                     This is the STANDARD KK reading -- but recorded as "an explicit IMPORTED
                     PARENT MODEL, not a derived record-cell law."

ell_P HELD FIXED     preserves the beta^2 statement, but the parent normalization must scale with
                     the fiber extent. *** NO SEALED PARENT-NORMALIZATION/FRAME LAW SUPPLIES THAT. ***
                     *** AND THIS IS THE BRANCH THE LIVE REDUCED ROUTE IS SILENTLY USING. ***

held_fixed_branch_derived = false | TYPE-U | would-build: independent parent-normalization/frame law
```

*** WHAT INHERITS THE CHOICE: `beta^2`, `phi = ln(R/ell_P)`, `rho = R_*/ell_P`, THE RADION SCREENS —
**AND THE IDENTIFICATION THAT THE `beta`-DEGENERACY IS A MASSLESS RADION'S SHIFT SYMMETRY.** ALL
BRANCH-CONDITIONAL. ***

---

## 2. THE TASK

**2.1 CAN THE PARENT-NORMALIZATION / FRAME LAW BE DERIVED?** The would-build names it; **nothing
supplies it.** Establish, from sealed text, whether anything **could**:

```text
does the record-cell construction constrain what the parent normalization may depend on?
is kappa_5 a constant of the theory by construction, or a function of cell data?
does any sealed result require the 4D gravitational coefficient to be R-independent -- and if so,
  IS THAT REQUIREMENT ITSELF DERIVED OR ADOPTED?  *** CHECK THIS ONE CAREFULLY: a downstream
  requirement quietly forcing an upstream branch is exactly how the current silent adoption
  happened. ***
does diffeomorphism invariance, or any sealed covariance statement, bear?
is there a physical argument specific to the fiber being the RECORD CELL'S INTERNAL DIRECTION
  rather than a generic extra dimension?
```

**2.2 IF IT IS DERIVABLE, DERIVE IT.** State the derivation and which branch it forces.

**2.3 IF IT IS NOT, TYPE IT AND PRICE IT.** *** Q-82: PATHLESSNESS IS DIAGNOSTIC. A NODE WITH NO
DISCHARGE ROUTE IS TERMINAL, AND TERMINAL IS WHAT AN AXIOM LOOKS LIKE. *** For each branch state: what
adopting it costs, what it makes true, **what it makes FALSE**, and what downstream results would have
to be re-typed as branch-conditional.

**2.4 STATE THE DISCLOSURE.** If this is an axiom, it joins the adopted set. **Say what must travel
with any number computed on the geometric route.**

---

## 3. THE TRAP — AND IT IS THE SAME ONE AS LAST TIME

*** DO NOT PICK THE BRANCH THAT PRESERVES `beta^2`, AND DO NOT PICK THE BRANCH THE LIVE ROUTE IS
ALREADY USING. *** Both are **downstream consequences**, not evidence about which parameter is
fundamental. **Choosing to preserve a prior result is fitting; choosing to preserve current practice is
inertia.** *** IF THE EVIDENCE FAVOURS THE BRANCH THAT BREAKS `beta^2` AND CONTRADICTS THE LIVE ROUTE,
REPORT THAT — IT WOULD BE THE MOST VALUABLE RETURN AVAILABLE. ***

**A prior lane on this exact question did not take the preserving branch. Hold that standard.**

**Settled, do not re-litigate:** `beta` **is frame-invariant** — `R` is a proper radius in the fiber
block and `c Delta tau` a proper interval in the base block **of the same metric**. **No frame
declaration is required.** The frame ambiguity is real only once `R` is dynamical.

---

## 4. SYMMETRIC LEADS

```text
IF THE BRANCH IS DERIVABLE -- *** LEAD WITH IT. It sits beneath beta^2, both sides of the radion
potential, rho, and every weight downstream. Nothing else on this route unblocks as much. ***

IF IT IS AN AXIOM -- *** LEAD WITH THAT, PRICED BOTH WAYS. An honest "this is terminal, and here is
what each side costs" is complete under Q-92, and the program needs it stated rather than inherited
silently -- which is how it has been carried until today. ***
```

---

## 5. RULES

- **Q-54 typing on every negative:** TYPE-R refuted · TYPE-U unbuilt · TYPE-S scope-empty · TYPE-C
  constraint-blocked. `NO_VERDICT` is legal. **Only TYPE-R is physical content.**
- **Q-92:** specifying and naming what is missing is a complete answer, not a lesser one.
- **THE BAR (Q-65/Q-76, `PRINCIPAL_AMENDMENT_002`):** a REQUIRE whose **existence and uniqueness are
  both derived**. **Overdetermination is NOT required.** *** THE LIVE CROSS-SECTOR SPEC STILL EMBEDS
  THE STRUCK BAR AT `:265-267` AND `:292` — DO NOT APPLY F3 OR MINIMAL-PASS ITEM 3. ***
- **Q-69:** never identify a flag with the object that discharges it.
- **No unproved-identity transport.** A seal reaches what it names and no further.
- **Word-boundaried, CASE-INSENSITIVE on identifiers. Mark hypothetical flags inline.**
- *** CHECK SCOPE BEFORE ASSERTING A NEGATIVE. `alpha-program-archive/cleanroom_output/` holds 87 files
  OUTSIDE the `workspace/` subtree; sidecars are `NN_NAME_V001.seal.sha256`, no `.md` before
  `.seal`. ***

## 6. FENCES — VERBATIM, NON-NEGOTIABLE

- **Never touch `a32_holdout/custodian_private/`.**
- **Do not compute** alpha, `kappa_record`, `kappa_Thomson`, a coupling, a scale, a root, an
  eigenvalue, a beta function, `E_R`, `T_R`, `k_R`, or any absolute interval. *** NO NUMERICAL VALUE
  FOR `kappa_5`, `ell_P`, `beta`, `rho` OR `phi`. STRUCTURAL AND SYMBOLIC ONLY. ***
- **No comparison to any measured constant.**
- **Do not resolve the Misner-Sharp / Brown-York fork by choosing — it must be DERIVED.**
- **Report refutations; never repair them.**

## 7. CUSTODY (Q-91)

*** NO LANE RUNS ANY GIT COMMAND. *** Write the artifact in the cleanroom, compute its
`.seal.sha256` sidecar and **verify it matches**, mirror artifact + sidecar to
`alpha-program-archive/workspace/`, report hashes and paths, and **stop**. **A red gate is never a
reason to stop working.** The reviewer verifies, baselines, commits and pushes.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
