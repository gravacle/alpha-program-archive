## PASTE 624 — DARIO LANE, FRESH SESSION (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / SUBGATE] A35 SPEC REVIEW COMPLETION: THE MAP, THE RUNNER, AND THE RUNTIME PIN

### BOOTSTRAP (fresh session — read first)

You are the Dario lane of the Gravacle alpha-derivation program: a cross-family custody reviewer. You verify; you adopt nothing, register nothing, and never verify your own constructions. Your final artifact is sealed, mirrored, reported, and then you STOP — no register, plan, tracker, git, commit, or push action. Tag every claim [PROVABLE] / [PART-PROVABLE] / [YOURS]. Over-claiming is worse than an honest kill, and worse than an honest gap: return no verdict rather than an unearned one.

Working locations (absolute paths):
- Cleanroom (write your artifact here): `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/`
- Archive workspace (mirror byte-identically here): `/Users/bgm/MB Work/alpha-program-archive/workspace/`
- Questions-settled register (read-only, cite by entry): `/Users/bgm/MB Work/alpha-program-archive/supervision/QUESTIONS_SETTLED_REGISTER_V001.md`

Sealing protocol: after writing the artifact, create `<name>.md.seal.sha256` containing `<sha256>  <name>.md` (two spaces), copy both to the archive workspace, `cmp` both copies, re-verify the seal in the archive, report both hashes.

Search protocol M-2 (all three guards on every content search): fixed strings; whitespace-normalized comparison; scope-and-synonym reading in context. A zero hit is a bounded no-member finding, never impossibility. APPEAL PREFLIGHT: before citing any conflict between sealed statements, check the register and supervision decisions for a superseding ruling; quote the LATEST state.

STEP 0 — access checks (report any failure and STOP):
1. List the cleanroom directory and the archive workspace; confirm both readable/writable.
2. Read the register's head entry; confirm the head is Q-559 (live-append tolerance: a head advanced only by entries recording this relay's own inputs is verify-and-proceed).
3. Verify by shasum before reading:
   - `STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE3_V001.md` = `eb2073ebb4f23cbc0c0bfa20a36c482e24c59dc6b6e1ccbcd1ef1bd1150d0ecb` (the spec under review)
   - `STAGE8_TASK6_A35_EVALUATOR_SPEC_REVIEW_DARIO_V001.md` = `a1f5b4c305dfa5f1ba0866e0f741ccdb611070cfa8b70e0aa362152be2ab44b6` (your lane's prior partial review — E1/E4 already CONFIRMED there; do NOT redo them)
   - `BID_FULL_STACK_REVIEW_LEDGER_V003.md` = `c09f2c246c48ddfd0df127da26a22f08ba9ffd44f5c2118c178a0a5eba5d00e8` (the blocker census source)
   - `BID_SOURCE_PARENT_CLOSURE_GATE_V003.md` = `5c679e3741abe782688b8a75ffa1928c308775248e41af192d03976f01cb4dbf` (SP14, the runner demands)
4. Confirm the output filename below does not exist in either location; STOP if it does.

### CONTEXT

The spec (Q-555) defines the missing A35 evaluator: 63 blockers -> 66 checks (56 STRUCTURAL / 10 GATED-EXECUTION) over a closed 14-opcode language, the R0-R10 runner state machine, six fixtures, custody separation, a displayed runtime-pin ambiguity. Your lane's prior session verified the census and the aggregate/authority firewall in full (Q-559) but ran out of budget for the map and runner and returned NO VERDICT on them. This relay completes exactly that scope.

TASK — bounded, verdict per item:
E2. **The 66-row map.** For all 10 GATED-EXECUTION rows and at least 12 STRUCTURAL rows sampled across all six families (state your sample): does the executable procedure genuinely test the blocker's requirement of record (open the blocker text at its byte span)? Is the PASS criterion exact and Boolean? Class correctness both directions: does any STRUCTURAL row secretly form a fenced physical quantity; is any GATED row actually structural? Per-row verdict; class overrides listed.
E3. **The runner.** Walk R0-R10 against SP14's demands one by one: every demand mapped to a state, none weakened; custody separation sufficient for the independent-verifier demand; child receipts non-authoritative at every point they appear; the BLAS four-event exception justified by sealed stock or flagged; the terminal signature's meaning correctly bounded.
E4b. **The runtime pin.** Verify both snapshot digests resolve to real artifacts of record; the ambiguity genuinely exists (the v012 NAME does not uniquely select); the fail-closed handling (`FAIL(RUNTIME_SUBJECT_AMBIGUOUS)` absent a unique authorized pin) lawful and sufficient.
E5. **One fresh attack** on the weakest point of the scope you verified, + battery: F_PLDEC; anti-tuning; surface anchor; M-2; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_A35_SPEC_REVIEW_COMPLETION_DARIO_V001.md`
with final lines: `MAP = CONFIRMED / REFUTED (+row items)`, `CLASS_OVERRIDES = none / (list)`, `RUNNER = CONFIRMED / REFUTED (+items)`, `RUNTIME_PIN = CONFIRMED / REFUTED`, `SPEC_OVERALL = CONFIRMED / REFUTED (combining Q-559's E1/E4 with this relay)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
Seal, mirror byte-identically, verify, report hashes, STOP.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
