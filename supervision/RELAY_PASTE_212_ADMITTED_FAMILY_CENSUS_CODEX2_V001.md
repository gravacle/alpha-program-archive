PASTE 212 — CODEX LANE 2 — THE CENSUS: IS §5.3 REACHABLE AT ALL?

ROAD: **UNBLOCKS STEP 3.** *** AND IT DOES NOT WAIT ON `B0`. *** For days every route funnelled
into step 1's foundation. This does not. It is the first step-3 work in the program that runs
independently.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.

*** THIS RELAY IS SELF-CONTAINED. ASSUME NO MEMORY OF ANY PRIOR RELAY. Everything you need is
below. ***

---

## 1. WHY THIS IS URGENT NOW

`alpha = 1/(4 pi K_*)`, where `K_*` is the root of `C_record(K) = 0`. **§5.3 of
`STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md` is the uniqueness gate that would establish a computed
value was FORCED rather than FITTED.** It passes only on one simple positive root, no inequivalent
admitted positive root, no continuous modulus, the root solved once and independently reproduced —
**and it FAILS if uniqueness is obtained by narrowing the admitted family after seeing the root.**

**A prior audit (`STAGE8_TARGET_INDEPENDENCE_SEAM_AUDIT_V001.md`, `834e5041…`) established that the
anti-narrowing clause is DIRECTIONALLY VALID BUT NOT EXECUTABLE, because the admitted-family census
and provenance are UNFROZEN.**

*** SO THE PROGRAM CANNOT CERTIFY A NUMBER EVEN IF IT HAD ONE. THAT IS A MISSING CAPABILITY AT THE
END OF THE ROAD, NOT A DELAY. ***

**And there is a second, older concern that has been NAMED AND NEVER TESTED.** A previous Codex
finding characterized what the theory admits as a `MIXED_TYPE_ALLOWANCE_ENVELOPE` containing an
**OPEN ACTION-FORM CLASS**. *** YOU CANNOT EXHAUSTIVELY AUDIT AN OPEN CLASS. *** If that reaches
§5.3, step 3 is unreachable as written and steps 1 and 2 are building toward a gate that cannot be
passed.

---

## 2. THE TASK — TWO QUESTIONS, BOTH UNTESTED

**2.1 IS §5.3's "ADMITTED FAMILY" THE SAME OBJECT AS THE `MIXED_TYPE_ALLOWANCE_ENVELOPE`?**

They have been assumed identical. **Nobody has checked.** Establish, from sealed text: what §5.3
quantifies over, what the allowance envelope contains, and whether they are the same object, nested,
overlapping, or unrelated. **Quote paths and lines.**

*** IF THEY ARE DIFFERENT OBJECTS, THE OPEN ACTION-FORM CLASS MAY NEVER HAVE TOUCHED THE GATE. THAT
IS THE CHEAPEST GOOD OUTCOME AVAILABLE AND IT WOULD BE A CLEAN POSITIVE. ***

**2.2 DOES "OPEN" MEAN *NOT YET ENUMERATED* OR *NOT ENUMERABLE*?**

```text
NOT YET ENUMERATED   nobody has written the census. Step 3 is WORK. Freeze it and 5.3 executes.
NOT ENUMERABLE       no census can exist -- the class is genuinely unbounded or its membership
                     is undecidable. *** STEP 3 IS UNREACHABLE AS WRITTEN. ***
```

**Determine which, from what the corpus actually says** — not from what "open" suggests. If the
corpus does not settle it, say so and state precisely what would.

**2.3 If a census IS possible, state what freezing it requires**: which artifacts would have to be
enumerated, what provenance each member needs, and what would make the frozen family auditable. **Do
not build it. Specify it.**

---

## 3. THE EXEMPLAR YOUR PRIOR SWEEP DID NOT SURFACE — SUPPLIED DELIBERATELY

The seam audit did not find `BID_GLOBAL_BOUNDARY_DESCENT_QUASI_FREE_COMPLETENESS_V001.md`. Its
`:22-25` reads:

> *"This is an additional microscopic Gravacle premise. It does not follow from CAR, locality, or
> disjoint monoidality alone. It is **target-value-free but historically target-aware**, and is
> forward-adopted before further evaluation under this branch."*

**That is the corpus stating the inputs/specifications distinction about itself, in one sentence, on
a load-bearing microscopic premise.** Status flags: `global_boundary_descent_principle_adopted =
true`, `historically_target_blind = false`, `target_value_used_in_construction = false`.

*** IT WAS WITHHELD AS A THOROUGHNESS CHECK AND THE CHECK FIRED. THE PRIOR SWEEP MUST NOT BE TREATED
AS A CORPUS-WIDE NEGATIVE. *** **This is not a reprimand — it is a scope correction, and the finding
it belongs to (no definition exists) is STRENGTHENED by it, not weakened.**

**Use it as a calibration case:** any method you use for 2.1/2.2 should find this artifact. **If your
search does not reach it, widen the search before reporting.**

---

## 4. SYMMETRIC LEADS

```text
IF "NOT ENUMERABLE" -- LEAD WITH IT. It is the most consequential negative available and it would
stop the program from spending further effort on a road with no end. *** REPORTING IT IS THE
HIGHEST-VALUE THING THIS RELAY COULD DO. ***

IF "NOT YET ENUMERATED", OR IF 2.1 SEPARATES THE TWO OBJECTS -- LEAD WITH THAT. Step 3 becomes
reachable, and this program has very few clean positives.
```

**Do not frame the work to find a defect.** Two of the last four structural questions returned
positives. **A question framed to find a problem will find one.**

---

## 5. RULES

- **Q-54 typing on every negative:** TYPE-R refuted · TYPE-U unbuilt · TYPE-S scope-empty · TYPE-C
  constraint-blocked. `NO_VERDICT` is legal. **Only TYPE-R is physical content.**
- **Q-92:** specifying and naming what is missing is a complete answer, not a lesser one.
- **Q-69:** never identify a flag with the object that discharges it.
- **Word-boundaried matching only** — substring matching has produced false positives in the corpus,
  in the audit script, in the audit of the audit, and in the register.
- **Report search scope**: roots, exclusions, and the queries actually run.
- **No repair.** If one is obvious, name it in one line at the end and stop.

## 6. FENCES — VERBATIM, NON-NEGOTIABLE

- **Never touch `a32_holdout/custodian_private/`.**
- **Do not compute** alpha, `kappa_record`, `kappa_Thomson`, a coupling, a scale, a root, an
  eigenvalue, a beta function, `E_R`, `T_R`, `k_R`, or any absolute interval.
- **No comparison to any measured constant.**
- **Do not resolve the Misner-Sharp / Brown-York fork by choosing — it must be DERIVED.**
- **Report refutations; never repair them.**

## 7. CUSTODY (Q-91)

*** NO LANE RUNS ANY GIT COMMAND. *** Write the artifact in the cleanroom, compute its
`.seal.sha256` sidecar and **verify it matches**, mirror artifact + sidecar to
`alpha-program-archive/workspace/`, report hashes and paths, and **stop**. You may run
`corpus_check.py --report` on your own artifact; `--gate` is not your concern and **a red gate is
never a reason to stop working.** The reviewer verifies, baselines, commits and pushes.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
