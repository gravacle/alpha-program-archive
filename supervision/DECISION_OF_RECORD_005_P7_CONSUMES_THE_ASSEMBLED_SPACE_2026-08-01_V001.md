DECISION OF RECORD 005 — P7 CONSUMES THE ASSEMBLED SPACE, NOT ITSELF

2026-08-01. Ruled by the principal. Register head Q-198. Plan Task 1, step a (D1).
alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

## THE DECISION

P7's build clause — "solve delta Gamma_2PI/delta G = 0 on the physical package" — is DECLARED to
consume ONLY THE ASSEMBLED SPACE THAT P1-P6 AND P8 PRODUCE. It does not consume the whole
CTP_PHYS_INPUT_PACKAGE (whose build list includes P7 itself).

## WHY THIS IS A DECLARATION AND NOT A CHOICE OF PHYSICS

The identity is absent from the prose by construction: "the physical package" is prose, not the
exact identifier, and no artifact states what P7 was meant to consume
(cycle_resolvable_by_document_search = false | TYPE-R, Q-196). This ruling disambiguates a
self-reference in a specification. It selects no physical fork; the Misner-Sharp / Brown-York rule
is untouched. The natural reading is ratified: an equation is solved ON a space after the space
exists.

## CONSEQUENCE

The latent two-node cycle (counterfactual Tarjan, Q-195/Q-196) does NOT form. The front-line build
is ACYCLIC unconditionally within the audited scope. Task 2 proceeds in order:
P1-P6, P8 assemble the space -> P7 solves on it.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
