## PASTE 707 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / 7A-HANDOFF] THE OBS-22 RECOGNITION PREREGISTRATION — DRAFT FOR PRINCIPAL REVIEW

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. THE FIRST-TIME-RIGHT RULES apply. Parallel to Codex 2's lineage-V2 rerun; if a cross-confirm relay arrives, finish this first — strict sequence within the lane.)

CONTEXT (principal directive of record, sealed note 6e144dcc…): 7A opens with the junction arc — the OBS-22 test. The principal's requirement: the machinery must recognize the RECORD face of gravity without pretending the tests are the classical tests. Record-level gravity is necessarily pre-metric; the classical face is 7B's derived corollary (DoR-007). Two scars ground this: the log divergence typed as failure when it IS the coupling, and the singlet-ratio hunt (a classically-shaped expectation, proven empty on the carrier).

TASK — draft the preregistration the junction arc will be FROZEN under (draft: the principal reviews before it seals):
T1. RECORD-NATIVE RECOGNITION CRITERIA for "the maps land geometric": stated properties of the junction objects themselves — the Ward-symbol map (J-II) and the length normalization — checkable at the record level. Every criterion must name its object, its test, and its evidence form (what bytes/certificate would discharge it). FORBIDDEN as criteria: "metric-like", "reproduces Newton", any comparison to classical gravity's numerical or structural signatures. Ground each criterion in the sealed corpus (cite source + span); a criterion you cannot ground is listed as UNGROUNDED with what's missing.
T2. THE THREE VERDICTS, closed: CONFIRMED / INERT / NOT-RECOGNIZABLE-AS-STATED — the third naming the missing carrier, never collapsing into the second (your own 693/695 lineage: three outcomes because there are three).
T3. THE CORRESPONDENCE LEDGER channel: schema for logging likenesses to familiar classical structures as observations with ZERO verdict weight, feeding the discoveries digest only.
T4. THE FREEZE CONDITION: what seals when, and the rule that any post-freeze criterion change is a revision of record, surfaced to the principal.
Tag every criterion PROVABLE / PART-PROVABLE / YOURS. No junction computation, no map evaluation — this is preregistration only.
OUTPUT: one sealed artifact `STAGE8_7A_OBS22_RECOGNITION_PREREG_DARIO_V001.md`
with final lines: `CRITERIA = N record-native (G grounded / U ungrounded, each named)`, `VERDICTS = 3 closed + ledger channel`, `FREEZE = stated`, `CLASSICAL_TESTS = 0 (audited)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors; the principal reviews before the freeze). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
