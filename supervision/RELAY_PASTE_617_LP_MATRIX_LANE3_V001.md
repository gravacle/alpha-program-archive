## PASTE 617 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 6 / SUBGATE] LP-MATRIX: THE THIRTY-ROW PASS LEDGER

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-552.
- THE TARGET (the status column `bcb8cced…` row 6 / LP-MATRIX): one sealed INDEPENDENT PASS ledger for the thirty demanded rows — A01–A29 plus A35 — executed on ONE IMMUTABLE V011 lineage. The matrix is a protocol; this relay EXECUTES it. Root unblock for SPEC-SEAL and every descendant.
- Authorities: the A32 prep V002 (`c5d1090b…` — the rows' definitions and the lineage identification), the sixteen sources as consolidated there, the ratified instrument, the M-2 search protocol (three guards).
- Custody: you execute and ledger; Dario reviews the ledger. Per-row discipline: each row's requirement quoted from its sealed source; the execution displayed; PASS only where the display proves it — a row that cannot pass is reported FAIL/BLOCKED with its exact reason (a failed row is a finding, not a defeat). THE LEDGER IS THE ARTIFACT.
- NOTE the boundary: executing compliance rows is not numeric evaluation of any physical quantity; if any row's execution would require evaluating a fenced quantity, STOP that row as FENCE-ADJACENT and report — do not execute it.

TASK:
R1. **The lineage frozen:** the immutable V011 lineage identified, hashed, and declared — the single subject of all thirty rows.
R2. **The thirty rows executed** in order, one section each: the requirement quoted; the execution displayed; the verdict (PASS / FAIL+reason / BLOCKED+missing object / FENCE-ADJACENT).
R3. **The ledger board:** the thirty verdicts in one table with the counts; any FAIL/BLOCKED rows' consequences for the seal rail stated per the dependency structure.
R4. **Battery:** F_PLDEC; anti-tuning; surface anchor; self verb audit; the pre-seal re-diff not applicable (a fresh artifact) but the M-2 guards applied to all searches.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_LP_MATRIX_LEDGER_LANE3_V001.md`
with final lines: `LINEAGE = frozen (+hash)`, `ROWS = 30 executed (+counts by verdict)`, `SEAL_RAIL_CONSEQUENCE = stated`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal (protocol .seal.sha256), mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
