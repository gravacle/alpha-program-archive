## PASTE 697 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / TRANCHE] V009 ENVELOPE CHECK + INDEPENDENT CONFIRMATION/RE-PIN

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. THE FIRST-TIME-RIGHT RULES apply. A's V009 package is mirrored to archive: spec V009 = 900a240d…, Q-612 disposition 493d0b40…, package inventory 43 files 37d7202b….)

CONTEXT: A shipped the four Q-612 fixes as one delta with one finite V009. Registrar pre-verified: seals; J1's three span rows regenerated with the corrected (+1) lengths at the new document offsets; J4 landed as a NEW `verifier_root_members` schema field in the verifier-manifest inventory (member list out of the parent constant, per-row digest+byte-length binding, generated at inputs/verifier_root_members.generated.json); J3 binds every subject row to a content-addressed evidence payload — the addendum rides the EXISTING evidence carrier, no argv change claimed; the V008-hash hits in the manifests are retained supersession-witness evidence copies, not live pins.

TASK:
K1. ENVELOPE-CHECK V009 (your role): the delta must be the four Q-612 dispositions and mechanical carriage ONLY — 66 rows byte-identical, no criterion drift, schemas not prose. Confirm from bytes: the regenerated span table row-exact against the source; the recomputed carriage certificate (and that the stated cause of the old +5/+5 is a fact, not a story); the subject-row→evidence-payload binding closed over ALL SIX subject files including the addendum d17c5e79…; the `verifier_root_members` schema. Anything beyond the four dispositions is a finding.
K2. CONFIRM/RE-PIN INDEPENDENTLY: re-author your manifest instance under the V009 schema (verifier_root_members generated from YOUR sealed bytes — 13 rows), confirm argv/input_roots against sealed V009 text (not memory of it); verify with real inputs that every P0 conjunct is now replayable — the 695 counterfactual path becoming the real path — and that a chain-side refusal would no longer fire on the addendum. Update your verifier only where V009 requires; re-pin your root iff your bytes change.
K3. Dry-run every touched path; PIN CLOSURE grep value AND name (V008 900a-predecessor 3b24fc39… live references, superseded roots, stale instance fields).
OUTPUT: updated package + one sealed artifact
`STAGE8_TASK6_V009_CONFIRMATION_DARIO_V001.md`
with final lines: `ENVELOPE = four dispositions only / (+findings)`, `ROWS_CHANGED_CONFIRMED = 0`, `P0_ALL_SIX_REPLAYABLE = confirmed on real inputs / (blocker named)`, `MANIFEST_INSTANCE = re-authored, sealed`, `VERIFIER_ROOT = <hex> (changed/unchanged)`, `PIN_CLOSURE = N hits, all resolved`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors and invokes RUN 031 — the confirmation run). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
