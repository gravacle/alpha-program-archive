## PASTE 434 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] CROSS-REVIEW: THE PREFIX-TO-CYCLE DESCENT (Q-352)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-352.
- Artifact under review (Lane 2's build): `alpha-program-archive/workspace/STAGE8_TASK4A_FAMILY_NATURAL_PREFIX_TO_CYCLE_DESCENT_LANE2_V001.md`
  SHA-256 `5c5d2c828a62e302920c827e95678c9e9e00b2fdc1a2415c553383fcbbfe3a84`. Verify before reading.
- Seam authorities: your extension V002 `eb3675d5…` (the descent cites your repair as PENDING at three comparison points — audit those seams against your now-completed repair), your V001 obstruction (Q-347, confirmed terminal-only), Q-313 `ea8f4e9a…`, DoR-015/V005 `7ecf04e9…`, DoR-016/V004 `69f4d93b…`, the sufficiency theorem `d9a507fc…` (Q-348/349 confirmed).
- Custody: adversarial cross-review. The action-comparison square will be built ON this descent — a false PASS propagates into the p-verdict's foundation. Attack hard.

TASK — PASS / KILL per item:

Z1. **The formula and well-definedness.** Recompute D: r_j = Z_{j−1}⁻¹Z_j, h_{ε(j)} = r_j^{s_j}, D(Z) = q_G(h); verify well-definedness under the no-selection relation (simultaneous signed relabeling) and the basis-free holonomy form.
Z2. **The kernel theorem.** Verify ker(D_G) = Φ⁻¹(Γ_G) exactly — vertex rephasing and NOTHING else (a larger kernel deletes physics; a smaller one smuggles gauge into the physical quotient). Verify surjectivity and D* injective on C_G. Recompute the S8-A c₃ survival explicitly.
Z3. **Your own obstruction, re-fired.** Run the Y3 edge-exchange automorphism against D at the S8-A stage: it must act COVARIANTLY (not trivially) on the descent's output and delete nothing. Any invariance-where-covariance-is-required = KILL.
Z4. **The certificates.** Restriction (general signed cellular), batching, reality, identity zero-extension, family-automorphism covariance, and the rank-0/1 agreement with Q-313 — recompute each. For the three PENDING comparison points, substitute your completed V002 repair (`eb3675d5…`) and verify each seam actually closes with the corrected j_NM^Q square.
Z5. **The boundary honesty.** Verify the edge-resolved/signed-chain-map boundary is real (not an artifact), correctly typed TYPE-U, and that the residual ι-family statement (a member with kernel on C_G deletes that cycle) is exact.
Z6. **Selection scan.** No realization/edge/orientation/filtration/cycle-basis/chain-map member selected; p symbolic; the domain quotient (modulo simultaneous signed relabeling) does not hide a selection.
Z7. **One fresh attack** of your own devising on the descent.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK4A_DESCENT_CROSS_REVIEW_LANE1_V001.md`
with the verdict table Z1–Z7, computations, and final lines:
`DESCENT = CONFIRMED / KILLED (+item)` and `V002_SEAMS_CLOSED = yes/no (+which)`.
Seal (.seal.sha256 sidecar), mirror both files byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, then STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No evaluation of alpha, K_*, or roots; no comparison to measured constants. Structural mathematics of declared objects is permitted; if a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
