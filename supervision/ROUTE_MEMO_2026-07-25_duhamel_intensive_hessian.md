# Route Memo — Duhamel / Intensive-Hessian Equality (theory scoping lane return)
Claude lane · 2026-07-25 (late evening) · fresh-context scoping lane, read-only.
Recorded verbatim from the lane return. Derives nothing; seals nothing.

## Headline actionable structure

The item-5 obligation decomposes into three pieces with distinct dependency
status:

1. FINITE-K DUHAMEL IDENTITY H_K = g_{D,K} — provable NOW, unconditionally,
   on the abstract amended objects (needs only the sealed A2 closure, unitality
   Z[A,A]=1, and the already-sealed D5 operator Duhamel tangent). Hypotheses to
   state as named lemmas: C1 (branch generators affine in the branch tangent at
   baseline — TRUE for the Phase-A generator; NOT automatic for V011 link
   holonomies, a genuine falsification channel for the V011 wording) and C2
   (history-independence of ready injections, PVM, and relay isometries along
   the relayed exhaustion).
2. CONDITIONAL INTERCHANGE THEOREM (Route A, Vitali–Cauchy) — provable NOW as
   a theorem schema: given (H1) = T7(ii) volume-uniform zero-free polydisc and
   (H2) = T7(iii) uniform intensive bound + density convergence, the intensive
   Hessian equals the thermodynamic Duhamel covariance, with the G_L
   identification supplied by Route B's cluster resummation under the item-3
   majorant. NO principle adoption anywhere; the hypotheses must arrive as
   DERIVED outputs of program items 1–4 (assuming clustering would be a
   forbidden adoption per the sealed closure result).
3. INSTANTIATION on the actual parent + numerics — hard-requires Phase-A
   bundle + Phase-B scalar + relay items T7(-1)/(0)/(i) including the residual
   F(S,chi,beta,sigma,I) fork resolution. Unreachable until then; matches the
   amendment's A5 no-promotion list.

Ordering pinned by the memo: mixed-CTP (+/− Wightman) covariance is the
primary form; the T9/FS attenuation form is a corollary; ordering must be
pinned explicitly in any spec. Z_K[A,A] = 1 anchors the log branch; all
second-order content is in the mixed branch derivative.

Negative controls a spec MUST include: cos(N tau_R A) non-clustering witness
(must BLOCK); V010 zero-stiffness stays failed; a predeclared contact-term-
omission / wrong-ordering variant that must produce a certified mismatch
(proves the equality check has teeth); Route-1 falsifier re-execution;
per-state quantifier discipline; intersection-of-enclosures as the only pass
criterion.

Sealed anchors available: COMPLETE_QSPEC_FINITE_HOLONOMY_DUHAMEL_CROSSCHECK_
PROTOCOL_V001 (g_D = <eta|eta> − |<psi|eta>|^2, enclosure radius, positivity,
mandatory intersection), the INDEPENDENT_VERIFIER protocol (bit-mask CAR,
unsplit RK4), D5 exact derivation + its epsilon-halving numeric anchor
(8.19e-8/6.69e-8), the lattice tangent map at 1.58e-9, and the finite
three-way H_CTP/g_FS/g_Duhamel agreement on the three-site regulator.

## Full memo (verbatim)

[The complete lane return is preserved in the session record of 2026-07-25;
key files list and both routes (A: Vitali–Cauchy interchange; B: Kotecký–
Preiss cumulant route with the contact-term ledger) as returned. Route A
recommended for the interchange, B-L5 for the G_L identification; both funnel
through the item-3 majorant; neither bypasses items 1–4.]

## Disposition

Next artifact for this thread: a sealed derivation spec covering memo pieces
1+2 only (finite identity with C1/C2 as stated hypotheses; conditional
interchange schema with (H1)/(H2) as named underived inputs), with the
mandated negative controls; hostile pre-execution review; fresh-context
execution as an exact/symbolic derivation gate. Piece 3 stays behind Phase-A/B
and the relay chain. Spec authoring waits for the majorant route memo (in
flight) so the two specs name the same item-3 interface.
