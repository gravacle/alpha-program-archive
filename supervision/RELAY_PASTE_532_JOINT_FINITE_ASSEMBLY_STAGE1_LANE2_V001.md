## PASTE 532 — CODEX LANE 2 (HIGH EFFORT) — [TASK 5 / EQ6] THE JOINT FINITE ASSEMBLY, STAGE 1: THE PACKAGE FUNCTOR AND THE FULL FINITE BOTTOM

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-455.
- Your exhibition (`a681c784…`) failed by ABSENCE on FC2/FC3/FC10/FC11 and named the missing object: `F_ACTUAL_JOINT_FINITE_PACKAGE_CERT`. This relay builds its first two components. Re-read your own B2.2–B2.3 (the exact missing squares) before starting.
- Register-sweep FIRST: grep the questions-settled register for prior joint-package constructions (Q-313's maps, the pass-1 subpackages `452f1bb8…`, the bedrock dependency graph `288a3d41…`) — build nothing that exists.
- Custody: builder builds; stage 1 returns to Lane 1. This is FINITE constructive work — no completed object, no axiom invocation anywhere.

TASK — construct, on the full live tower I_F (no scope-shrink):
C1. **The package functor.** One functor `I_F -> FinPackage_020` carrying ALL F_actual components simultaneously: extend `Adm_fin`, C2, and C3 with their finite maps on every generator of I_F (the pass-1 hunt names the missing square — build exactly it and its composites); prove identity and composition laws on the actual arrows; every extension premise-marked to the sealed finite result that licenses it.
C2. **The full finite bottom.** One package-wide bottom square covering EVERY sealed finite consumer at once (the Q-451 P3 absence): each active restriction reproduces its DoR-008 shadow through the ONE functor of C1, including the Gate 1–4, Q-243, Q-279, Q-309 shadows where defined. Exactness, not approximation.
C3. **The J12/J15 repair check:** with C1+C2 in hand, verify whether J12 and J15's package-wide gaps close as implied; state exactly what remains for J2 (physical construction) and J4 (de-scoping) for stage 2.
C4. **Honesty clause (unchanged):** if any required square CANNOT commute — a genuine clash between existing componentwise objects — report the clash exactly with its witness and STOP the build there. A contradiction outranks a completed assembly.
C5. **Battery:** the nine geometric regressions rerun on the assembled functor; the anti-tuning ledger (no clause chosen from any downstream consequence); delta vs the componentwise stock.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md`
with final lines:
`PACKAGE_FUNCTOR = BUILT / BLOCKED (+witness)`, `FULL_FINITE_BOTTOM = BUILT / BLOCKED (+witness)`, `J12_J15 = CLOSED / OPEN (+remainder)`, `CLASH_FOUND = none / witness`.
Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
