PASTE 298 — CODEX LANE 2 (HIGH EFFORT) — [TASK 2c] ASSEMBLE `U1` — THE CONVENTION LAYER ON `C0_008`

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-214)

```text
DOES THE OBJECT EXIST?   SPECIFIED: U1 = physical branch/source orientation, branch metric,
                         reality involution, compound-index order, source symmetry, embeddings ON
                         C0 (B0 stop spec :1005-1006). The formal convention layer partly PREDATES
                         the field home; its PHYSICAL INSTANTIATION on the assembled carrier does
                         not exist.
IS THE VERSION CURRENT?  YES. *** C0_008 EXISTS (Q-213) -- U1 is the next component in order. ***
ARE ITS INPUTS PRESENT?  C0_008 + the ratified presentation + the pre-existing formal conventions.
```

## 1. THE BUILD

```text
1  INSTANTIATE EACH U1 COMPONENT ON C0_008, as instances with certificates:
   branch metric . reality involution . compound-index order . source symmetry . physical
   branch/source orientation . the embeddings. *** WHERE A FORMAL CONVENTION ALREADY EXISTS IN
   SEALED TEXT, CITE AND INSTANTIATE IT -- DO NOT INVENT A RIVAL. WHERE NONE EXISTS, THE GAP IS
   THE FINDING. ***
2  d_U1^P : (P_008, C0_008) -> U1_008 -- premise-level, honestly labeled, per the Q-213 pattern.
   What independent provenance would still require: reported, not papered.
3  SCOPE FENCE: U1 is CONVENTIONS. *** If any component turns out to require state, dynamics,
   quotient, measure, or effects to instantiate -- STOP THAT COMPONENT and report it; it belongs
   to U2/U3, and pulling it forward would repeat V001's smuggling defect. ***
4  MARKS: every claim TYPE-P | premises: DoR-008.
```

## 2. LEADS

```text
IF U1 ASSEMBLES -- lead with the instance list and the second package component exists. Note which
components were INHERITED formal conventions vs NEWLY instantiated.
IF A COMPONENT NEEDS AN UNLICENSED CHOICE -- name it as a candidate adoption with alternatives;
the principal rules.
IF A COMPONENT BELONGS DOWNSTREAM -- lead with the boundary correction; scope honesty beats
completeness.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
