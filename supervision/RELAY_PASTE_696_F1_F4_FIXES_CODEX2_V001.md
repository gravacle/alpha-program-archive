## PASTE 696 — CODEX 2 LANE = BUILDER A (5.6 SOL EXTRA HIGH) — [TASK 6 / TRANCHE] FOUR Q-612 FIXES: ONE WHOLE DELTA

(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. THE FIRST-TIME-RIGHT RULES apply. Builder B's V008 integration is mirrored to the cleanroom: verifier root now 43cff85d… over THIRTEEN members (verifier/preconditions.py added, disclosed), manifest instance argv 22 / input_roots 7, B report 4b93a9ac….)

CONTEXT (Q-612, registrar-verified): your V008 envelope is clean where it matters — three statements, 66/66 rows byte-identical. Four items block run 031; all four ship here or the delta does not ship:

J1 — SPEC SPAN TABLE (yours, transcription): V008 lines ~1623–1625 end FX-A35-03/04/05 at 133684/133872/134125; the generated manifest and the source bytes say 133685/133873/134126. The registrar reproduced all six fixture_spec_sha256 values: the manifest is span-exact, the three spec rows are one byte low. Correct the spec text FROM the generated values (rule 1: no retranscription — generate the table rows).
J2 — CARRIAGE CERTIFICATE (yours): the V008 certificate declares 160/26; four independent recomputations and the edit ledger give 155/21 (+5/+5 canceling). Recompute the certificate FROM BYTES and state what produced the +5/+5.
J3 — THE UNSUPPLIED SUBJECT FILE: V008-R9-2 claims all six P0 conjuncts replayable, but the integration addendum (d17c5e79…) reaches R9 through no carrier — real inputs return PRECONDITION_NOT_REPLAYABLE naming it (the refusal working as specified). Supply it by the SMALLEST LAWFUL MEANS; if the closed argv/input_roots inventories need a slot, that is a finite spec delta stated as schema, not prose.
J4 — VERIFIER ROOT CENSUS: your parent hard-codes a 12-member census and fails closed on B's sealed 13-member root. Re-pin to the 13 members and root 43cff85d… — GENERATED from B's sealed package bytes, never typed; if you conclude the member list belongs in a sealed schema'd carrier rather than a parent constant, say so and ship it that way.
If J3/J4 force spec text, issue ONE finite V009 carrying both (rows unchanged, schemas only); do not issue two deltas. Dry-run every touched path; full pin closure grep value AND name (V008 3b24fc39… references that must move, superseded roots).

OUTPUT: updated package (+ V009 iff required) + one sealed artifact
`STAGE8_TASK6_Q612_FIXES_CODEX2_V001.md`
with final lines: `J1 = corrected from generated values`, `J2 = recomputed from bytes, cause of +5/+5 stated`, `J3 = carrier named / V009 slot`, `J4 = 13-member census, generated`, `SPEC_ISSUED = none / V009 (rows changed 0)`, `PIN_CLOSURE = N hits, all resolved`, `DRY_RUN = executed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, STOP (the registrar mirrors; B confirms boundary terms iff V009 issues; then run 031). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
