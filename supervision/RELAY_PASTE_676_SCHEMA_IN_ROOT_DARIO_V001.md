## PASTE 676 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / BUILD] BRING THE SCHEMA INTO THE ROOT

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors.)

CONTEXT: your own 674 observation, now commissioned: `contracts/verifier_verdict.schema.json` is sidecar-pinned but sits OUTSIDE `verifier_root_sha256` — the same shape as the launcher question at 667, resolved there by root inclusion.

TASK: recompute the root to include the schema (state the member list and old->new values); reissue the instance + sidecar; dry-run confirmation; disclosed delta; PIN CHECK. (Note: Builder A is being fixed in parallel to validate your verdict against this schema as a pinned input — your root inclusion makes its pin part of one covered whole.)

OUTPUT: updated instance/sidecar + one sealed artifact
`STAGE8_TASK6_SCHEMA_IN_ROOT_DARIO_V001.md`
with final lines: `ROOT = covers schema (+members, old->new)`, `DRY_RUN = passes`, `INSTANCE = canonical, sidecar-pinned`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors and re-invokes). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
