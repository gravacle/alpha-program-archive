## PASTE 700 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / TRANCHE] V010 ENVELOPE CHECK + THE R9 GROUND-ATOM RESOLVER

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. THE FIRST-TIME-RIGHT RULES apply. A's V010 is mirrored: spec V010 = 31ccee9c…, report bf6a132d…, inventory b4c103e6…. Registrar pre-verified: parent.py and producer.py BYTE-UNCHANGED against the pre-relay mirror; the ground-atom rule is an iff against the closed schema rd22.r9-ground-atom.v001.)

CONTEXT (Q-614): run 031 executed end-to-end; the single substantive fault is your replay's `evidence has no result 'r_ground'`. V010 now states the resolution of record: a qualifying ground atom is reconstructed ONLY by R9, from your own P0-verified evidence table plus sealed spec constants — no producer carrier exists for it, and a producer-emitted result object for such an atom is a fault.

TASK:
N1. ENVELOPE-CHECK V010 (your role): one statement and mechanical carriage only; 66/66 rows byte-identical; the qualifying rule closed (iff against the schema, no admission by example); confirm A's zero-code-change claim from bytes. Anything more is a finding.
N2. IMPLEMENT THE RESOLVER: R9 resolves qualifying ground atoms from your own sources — for C-B-V009-06's r_ground, YOUR independently computed member-payload digest and the sealed constant — never from a producer record; a producer-emitted result object for a qualifying atom is a fault; a named refusal if an atom is neither carried nor qualifying. Negative controls: a perturbed operand must flip the atom; a smuggled producer r_ground object must be refused.
N3. Dry-run every new path (run it, don't read it); re-pin your root; PIN CLOSURE grep value AND name (superseded root 10622f17… iff your bytes change, V009 900a240d… live references).
OUTPUT: updated package + one sealed artifact `STAGE8_TASK6_GROUND_ATOM_RESOLVER_DARIO_V001.md`
with final lines: `ENVELOPE = one statement only / (+findings)`, `ROWS_CHANGED_CONFIRMED = 0`, `RESOLVER = implemented, executed (+negative controls)`, `VERIFIER_ROOT = <hex> (changed/unchanged)`, `PIN_CLOSURE = N hits, all resolved`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors and invokes RUN 032 — the confirmation run). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
