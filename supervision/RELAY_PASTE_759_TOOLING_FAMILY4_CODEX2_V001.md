## PASTE 759 — CODEX 2 LANE (5.6 SOL EXTRA HIGH) — [7A / STEP 11] TOOLING FAMILY 4: CORPUS/PIN/ENVELOPE GENERATION

PICKUP-ACK + LANE GUARD in force (CODEX 2).
(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. ALL standing laws apply. FIRST-TIME-RIGHT rules 1 and 3 govern the build.)

CONTEXT (Q-626 proposal): family 4 — deterministic generation of corpus definitions, pin manifests, and envelope shells from sealed instances and the box schemas, per the sealed corpus rule (V001+V002+V003: claim/provenance/REGISTRY typing respected; registrar sealing and dual verification remain the custody for corpus INSTANCES — the tool generates candidates and packaging, never seals a corpus).
TASK:
AK1. BUILD family 4 with that custody boundary explicit in its contract; run it over the completed instances and the sealed corpus material (the V009-08 binding as the worked example); emit envelope shells for the rows whose components now exist (from families 1-3).
AK2. Census: shells emitted / rows now ENVELOPE-READY (all components present, admission barred) / remaining owners per row.
AK3. Admission barred; state it.
OUTPUT: the tool + outputs + one sealed report `STAGE8_7A_TOOLING_FAMILY4_CODEX2_V001.md`
Final lines: `TOOL = built (controls displayed; custody boundary in contract)`, `PRODUCED = census`, `ENVELOPE_READY = rows listed`, `REMAINING_OWNERS = per row`, `ADMISSION = barred, stated`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If a name exists, STOP. Seal, report hashes, write 759_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants.
