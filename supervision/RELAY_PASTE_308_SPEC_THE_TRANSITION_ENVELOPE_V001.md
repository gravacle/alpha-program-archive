PASTE 308 — CODEX LANE 2 (HIGH EFFORT) — [TASK 2d] SPECIFY THE STATE-TRANSITION ENVELOPE: NODES, EDGES, PROVENANCE

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-225)

```text
DOES THE OBJECT EXIST?   NAMED by 307: the MULTIAXIAL_STATE_TRANSITION_ENVELOPE -- state-class
                         NODES . DYNAMICS EDGES between temporal roles (rho_pre -> finite
                         completed -> outgoing) . COMMON-ORIGIN PROVENANCE for the whole path.
                         Read 1421bfc7... in full first.
IS THE VERSION CURRENT?  YES. Binding: the split factorization index; the undefined cumulant
                         axis; DynPort_U2_008 TYPE-U; both of the reviewer's correlation cutters
                         DEAD (Q-224 Theorem 2, Q-225).
ARE ITS INPUTS PRESENT?  PARTLY -- that is the audit.
```

## 1. THE TASK — SPECIFICATION, NOT FORCING. THE PROTOCOL RUNS ONLY WHEN THE OBJECT IS REAL.

```text
1  NODES: for each temporal role (rho_pre . finite completed . outgoing), what does sealed text
   supply as the node's state description, on which algebra? Where split-indexing applies, say
   which coordinate is classified and which is TYPE-S.
2  EDGES: the dynamics maps between roles. The FINITE edge exists (the exact finite dynamics
   that 307 exercised -- cite it). The JOINT edge is DynPort_U2_008 = TYPE-U -- state exactly
   what it must carry, interface-level, per the U2 skeleton.
3  PROVENANCE: the common-origin condition ON THE PATH (Parent-State Covariance's requirement,
   re-aimed at the transition). What would a path-level provenance witness BE?
4  THE CUMULANT AXIS: give the completed-carrier quasifree PREDICATE a candidate definition
   from sealed material, or type it TYPE-S honestly. *** THIS AXIS IS WHERE THE TWO-POINT
   REDUCTION LIVES OR DIES -- do not improvise it. ***
5  EMIT: the envelope's spec sheet -- what exists, what is TYPE-U with interfaces, what is
   TYPE-S -- and WHETHER THE FORCING PROTOCOL IS RUNNABLE ON IT, i.e. can step 1's family be
   declared with instances. IF NOT, name the minimal build that makes it runnable.
```

## 2. LEADS

```text
IF THE ENVELOPE IS SPECIFIABLE AND THE PROTOCOL RUNNABLE -- lead with the family declaration;
the next relay runs it.
IF THE JOINT DYNAMICS EDGE IS THE BLOCKER -- *** LEAD WITH ITS INTERFACE: that is the influence-
functional instance AGAIN (one gap, now FOUR consumers), and the program should hear that the
transition envelope and the dynamics port are one build. ***
IF PROVENANCE CANNOT BE STATED AT PATH LEVEL -- lead with what Parent-State Covariance actually
licenses; do not stretch it.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
