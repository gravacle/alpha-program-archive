## PASTE 566 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5 / EQ6] THE TWO LEMMAS: THE SEED THEOREM AND LOC-NATURALITY

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-492.
- The membership route now waits on exactly two lemmas, both named by the cross-family review (`STAGE8_TASK5_EQ6_J7_REVIEW_AND_ADJUDICATION_DARIO_V001.md` — verify via its sealed sidecar in the workspace; read its F1(a)/F1(b) and O1 sections first).
- Authorities: your own face/display artifact (`9c74ed3d…` — the m_T construction T3–T5 and the (F1)/(F3) premises), the projector cert (`0bef9a00…` — what IS proven: d/delta naturality), the sealed Q-408 reciprocal-loop stock (the actual cycle-carrying stage with its Ward/flux data), the census (`676863b5…` — the O1 branches).
- SYMBOLIC ONLY. Nonvanishing (zero vs nonzero) is a structural fact and is permitted; no magnitudes, no numeric evaluation.

TASK:
L1. **THE SEED THEOREM:** prove m_T,N ≠ 0 at an actual named stage — the reciprocal-loop stage is the natural candidate (it carries the program's oldest nonzero Ward/flux data). Compute m_T there from its definition (the normalized harmonic Maxwell symbol w.r.t. the ratified metric) symbolically and establish nonvanishing structurally. If it CANNOT be established — report exactly why (which quantity fails to be provably nonzero) and STOP on L1: a genuine seed failure means the finite surface carries no established Maxwell coefficient anywhere, which is first-order physics outranking everything downstream.
L2. **THE LOC-NATURALITY LEMMA:** prove or refute `Loc_M ∘ eta_f = j_f^C ∘ Loc_N` on rank-preserving arrows (and its (F1) companion) from the sealed constructions of Loc and the transport — the premise the J2 display consumes. If it holds only on a sub-scope, state the exact boundary. If refuted: the witness, exactly.
L3. **The consequence board:** with L1+L2 — A_RP⁺'s inhabitance status; the J2 display's premise status; what the J7 amendment's inhabitance clause may now cite; what remains for FC11 (do not attempt FC11 — it is an axiom-text question).
L4. **Battery:** F_PLDEC (the seed computation must not touch the reader); anti-tuning; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_TWO_LEMMAS_LANE3_V001.md`
with final lines: `SEED_THEOREM = PROVEN (+stage) / FAILED (+quantity)`, `LOC_NATURALITY = PROVEN / SUBSCOPE (+boundary) / REFUTED (+witness)`, `A_RP_PLUS = INHABITED / EMPTY_OF_RECORD`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
