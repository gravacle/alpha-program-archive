PASTE 246 — CODEX LANE 1 — ROUTE 4: THE NATIVE FINITE-ALGEBRA THEOREM

ROAD: **UNBLOCKS STEP 3.** *** ROUTE 1 IS NOW CLOSED. ROUTE 4 IS THE CHEAPEST REMAINING CANDIDATE, AND
IT MUST NOW STAND ON ITS OWN. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.

*** THIS RELAY IS SELF-CONTAINED. ASSUME NO MEMORY OF ANY PRIOR RELAY. ***

---

## 1. WHERE THIS STANDS

**A pre-root equivalence theorem was proved** (`STAGE8_PRE_ROOT_HIGHER_DERIVATIVE_EQUIVALENCE_THEOREM_V001.md`,
`c6ad988a…`): EOM-exact and fixed-boundary-exact operators are removable by local field redefinitions
preserving the physical quotient, measure, contacts, Ward domains, counting metric, unit incidence
magnitude and `U(1)` holonomy. **The basis is finite at each derivative order and INFINITE across all
orders.** The Pauli mutation survives (`TYPE-R`).

**Four routes would close the all-orders union. Their standing as of now:**

```text
1  a finite cutoff                     *** CLOSED. ***
   internal_finite_record_algebra_terminates_tower = false | TYPE-R
   causal_diamond_shape_supplies_route1_cutoff     = false | TYPE-R
   *** THE TOWER IS "EXTERNAL_WITH_INTERNAL_INCIDENCE_CONSTRAINTS": the finite record incidence
   algebra constrains the INTERNAL sector, but the tower lives on CONTINUUM SOURCE/FIELD VARIABLES
   PLUS RECORD VARIABLES. THE FINITE ALGEBRA DOES NOT TERMINATE IT. ***
2  convergence / resummation           untouched
3  a microscopic generator             the required property is a RESPONSE-COMPLETE FINITE
                                       PRESENTATION and is WEAKER than full origin determination
                                       (TYPE-R twice); certificate still TYPE-U
4  *** A STRONGER NATIVE FINITE-ALGEBRA THEOREM UPGRADING THE INCIDENCE FILTRATION -- THIS RELAY ***
```

*** ROUTE 1's CLOSURE MATTERS FOR HOW YOU APPROACH THIS: ROUTES 1 AND 4 WOULD HAVE MERGED IF THE
OPERATORS WERE INTERNAL. THEY ARE NOT. **ROUTE 4 CANNOT LEAN ON THE COMPLEX BEING FINITE. IT MUST
UPGRADE THE FILTRATION ITSELF.** ***

---

## 2. WHAT ROUTE 4 MUST DELIVER

**A prior determination named the property precisely** — a **response-complete finite presentation**:

```text
finite generators, or an exhaustive generation grammar
decidable response-equivalence relations
exact descent of every descendant coefficient from finite primitive data
*** proof that NO admitted response-changing class lies OUTSIDE the presentation ***
```

**And it identified a candidate skeleton already in the corpus:** the **boundary-superconnection
filtration** and the **quasi-free / single-operator completeness rules.** *** BUT THOSE ARE ADOPTED,
HISTORICALLY TARGET-AWARE, LIMITED TO FINITE STATIONARY ONE-COMPLEXES, AND LACK COEFFICIENT DESCENT AND
RESPONSE COMPLETENESS. ***

---

## 3. THE TASK

**3.1 CAN THE INCIDENCE FILTRATION BE UPGRADED TO A FINITE-ALGEBRA THEOREM?** State what the
filtration currently gives, what an upgrade would have to add, and whether the corpus supports it.
**Quote paths and lines.**

**3.2 DOES THE UPGRADE REACH THE EXTERNAL SECTOR?** *** THIS IS THE DECIDING QUESTION AND ROUTE 1's
REFUTATION IS EXACTLY WHY. THE TOWER LIVES ON CONTINUUM VARIABLES **PLUS** RECORD VARIABLES. A
FINITE-ALGEBRA THEOREM THAT BOUNDS ONLY THE RECORD SECTOR REPEATS ROUTE 1's FAILURE IN DIFFERENT
NOTATION. **STATE EXPLICITLY WHAT YOUR THEOREM WOULD BOUND AND WHETHER THE CONTINUUM SIDE IS INSIDE
IT.** ***

**3.3 THE ADOPTION PROBLEM.** The candidate skeleton is adopted and target-aware. *** A ROUTE-4
THEOREM RESTING ON IT WOULD **MOVE** THE ADOPTION, NOT DISCHARGE IT. Say whether the upgrade can be
made from derived material, or whether it inherits the adoption — and if it inherits, say so plainly
rather than presenting a conditional theorem as a bound. ***

**3.4 IF ROUTE 4 CANNOT CLOSE, TYPE IT AND SAY WHAT REMAINS.** *** THREE OF FOUR ROUTES WOULD THEN BE
CLOSED OR BLOCKED, LEAVING ROUTE 2. THE PROGRAM NEEDS THAT STATED PLAINLY IF IT IS TRUE. ***

---

## 4. SYMMETRIC LEADS

```text
IF THE FILTRATION UPGRADES AND REACHES THE EXTERNAL SECTOR -- *** LEAD WITH IT. It closes the
all-orders tower, unblocks §5.3's quantification, and removes the leading-order caveat from any future
number. Then ATTACK IT YOURSELF -- state every hypothesis and check whether any is adopted. ***

IF IT UPGRADES BUT BOUNDS ONLY THE RECORD SECTOR -- lead with that precisely. It is a real theorem and
NOT a closure of the tower, and conflating those would be the same error route 1 just made concrete.

IF IT CANNOT UPGRADE -- lead with that, typed. Route 4 closes and only route 2 remains untouched.
```

*** DO NOT MANUFACTURE A BOUND. A theorem resting on an unstated hypothesis, or bounding only the
sector it can reach, would let §5.3 certify uniqueness over a family that is not bounded — and that
certification would look like a pass. **THE WORST OUTCOME AVAILABLE IS NOT A WRONG NUMBER; IT IS A
WRONG NUMBER WEARING A CERTIFICATION.** ***

---

## 5. RULES

- **Q-54 typing on every negative:** TYPE-R refuted · TYPE-U unbuilt · TYPE-S scope-empty · TYPE-C
  constraint-blocked. `NO_VERDICT` is legal. **Only TYPE-R is physical content.**
- **Q-92:** specifying and naming what is missing is a complete answer, not a lesser one.
- *** `TYPE-U` MEANS UNBUILT; `TYPE-R` MEANS REFUTED. DO NOT REPORT "I COULD NOT BUILD IT" AS "IT
  CANNOT BE BUILT." ***
- **Q-69:** never identify a flag with the object that discharges it.
- **No unproved-identity transport.** A seal reaches what it names and no further.
- *** SIX LIVE SYMBOL COLLISIONS. MOST DANGEROUSLY: `g_N` IN `cleanroom_output` IS **NOT NEWTON'S
  CONSTANT**. ***
- **Word-boundaried, CASE-INSENSITIVE. Mark hypothetical flags inline.**
- *** REPORT ALL ROOTS ENTERED. `cleanroom_output/` holds 87 files OUTSIDE `workspace/` (sidecars
  `NN_NAME_V001.seal.sha256`, no `.md`); the gravity arm is ~937 files in the PARENT of the cleanroom
  root. ***

## 6. FENCES — VERBATIM, NON-NEGOTIABLE

- **Never touch `a32_holdout/custodian_private/`.**
- **Do not compute** alpha, `kappa_record`, `kappa_Thomson`, a coupling, a scale, a root, an
  eigenvalue, a beta function, `E_R`, `T_R`, `k_R`, or any absolute interval. *** BUILD OR REFUTE A
  BOUND STRUCTURALLY. STAY PRE-ROOT. ***
- **No comparison to any measured constant.**
- **Do not resolve the Misner-Sharp / Brown-York fork by choosing — it must be DERIVED.**
- **Report refutations; never repair them.**

## 7. CUSTODY (Q-91)

*** NO LANE RUNS ANY GIT COMMAND. *** Write the artifact in the cleanroom, compute its
`.seal.sha256` sidecar and **verify it matches**, mirror artifact + sidecar to
`alpha-program-archive/workspace/`, report hashes and paths, and **stop**. **A red gate is never a
reason to stop working.** The reviewer verifies, baselines, commits and pushes.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
