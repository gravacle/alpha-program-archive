PASTE 143 — LANE 2

2026-07-30, CODEX LANE 2. Snapshot; later rulings override. Lane 1 is on the `Gamma_K` root premise under paste 142 — do not touch `Gamma_K`, joint-selector, measure or namespace artifacts.

Your paste-138 recovery is verified (`91db8708…`) and its per-family table is the most useful thing
produced on slot 18 so far. Holding your Q-32 addendum while my supervision paths were dirty was the right
call; they are committed now.

**ONE ITEM, and a principal ruling reframes it.**

Fences unchanged. `alpha_computed = false; proof_authorized = false`. **NEVER touch
`a32_holdout/custodian_private/`.**

---

### THE RULING THAT REFRAMES YOUR FAMILIES — Q-34

Authority: `MB Work/alpha_supervision/ALPHA_STIFFNESS_IS_K_STAR_PRINCIPAL_DECISION_2026-07-30.md`.

*** ALPHA'S STIFFNESS IS `K_*`, THE ONSET ROOT ON ONE CELL — NOT `kappa_record`, THE INTENSIVE HESSIAN. ***
Grounds: couplings are IMPEDANCES OF ACCESS CONDITIONS, "not Lagrangian normalizations"; allow/require made
quantitative; the record cell primitive; stiffness made physical by removing the rescaling freedom.
`kappa_record` is the construction one builds when the coupling IS a Lagrangian normalization.

**THE CONSEQUENCE FOR YOU: if the theory's native coupling is an ONSET / ACCESS-CONDITION quantity rather
than a DENSITY, then its native OBSERVABLES are likely onset- and access-shaped too — and your three
families are not alike in that respect.**

Your own table says so, though it was not asked in these terms:
- **finite global holonomy** — compact U(1) **charge/phase/flux ACCESS**; `Z_Q` **dimensionless**; a
  **finite-regulator SCALAR**. Access-condition-shaped, and scalar.
- **temporal plaquette / connected kernel** — "the current output is a **matrix/kernel**", a **nonlocal
  multi-component susceptibility**. Density-shaped, and it fails the Q-25 scalar-collector condition.
- **causal-record durability** — persistence / non-return / recoverability. **Threshold-shaped by nature**;
  `P_root(t)` is a scalar-valued probability curve but no single frozen scalar candidate exists.

### YOUR TASK — RANK THE THREE BY NATIVENESS, THEN TAKE THE SHORTEST BRIDGE

1. **Which family is most native under Q-34** — onset/access-shaped rather than density-shaped? State it
   at file:line from sealed text, not from the summary above. **I expect finite holonomy or durability and
   I expect temporal plaquette to rank last, but say if the text disagrees.**

2. **A DIMENSIONLESS OBSERVABLE MAY DODGE THE UNIT-MAP PROBLEM ENTIRELY, and you already found one.** You
   recorded `Z_Q` as dimensionless. `D` compares two theory predictions with measurement entering only
   through uncertainties — **so a dimensionless observable needs no unit conversion and no dimensional
   bridge at all.** Check that: for the holonomy family, is the four-part bridge (observable, A32
   candidate, comparator, uncertainty) actually SHORTER because the quantity is dimensionless? If so, say
   which of the four parts drop out and which remain.

3. **Then, for the family that ranks first, state the four-part bridge concretely** — what the observable
   IS, whether a comparator class could exist for it, and what remains. Not a prediction. The
   identification.

4. **And check it against all three admissibility clauses** (Q-27/Q-28): structure-sensitive; absent from
   the rule-11 lineage; and a comparator loose enough that a `D >= 5` difference is informative rather
   than fatal. **The third clause is where the electron anomaly died — a dimensionless holonomy quantity
   may sit very differently against it, and that is worth knowing precisely.**

**DO NOT** derive any value, evaluate any expression, compute `mu`, `sigma` or `D`, assert admissibility,
unmask anything, or enter the custodian directory.

---

REPORT BACK: hashes, exact committed paths, gate verdict, the nativeness ranking with evidence, the
dimensionless-shortcut finding, and the four-part bridge for the top family. **If a dimensionless
observable genuinely shortens the bridge, lead with that** — it would be the first shortening of slot 18
anyone has found, and slot 18 is what decides whether a computed alpha is a result or a conditional.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
