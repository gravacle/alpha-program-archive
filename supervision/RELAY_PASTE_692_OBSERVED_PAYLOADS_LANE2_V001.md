## PASTE 692 — CODEX LANE 2 = BUILDER A (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / TRANCHE] OBSERVED EVIDENCE = THE PAYLOADS, NOT THE TRACE

(Same Lane 2 session rules: cleanroom writes; the registrar mirrors.)

CONTEXT: run 029 — V009-06's observed evidence is now a single payload that is the EXECUTION TRACE (a JSON list of opcode results); the consumable `graph` argument appears inline in the invocation record but is reproduced by NO digest-verified payload; B faults exactly that ("invocation argument 'graph' is not reproduced by any digest-verified payload"). The trace is testimony about execution — receipt material; the EVIDENCE must be the bytes the check consumed.

TASK: the row's observed evidence returns to the payload set — (a) the canonical serialization of the graph object (the single-authority encoding, one object serving both DAG arguments per V007's stated identity), digest-verified, reproducing the invocation's consumable argument byte-for-byte; (b) the raw member span payload (47e7c329…, 932B) as byte-grounding. The execution trace moves to the receipt/child custody path (or is dropped if redundant with the ledger) — never counted as evidence. Static self-check incl. a line asserting every consumable invocation argument is reproduced by an observed digest-verified payload; disclosed delta; PIN CHECK.

OUTPUT: updated producer + one sealed artifact
`STAGE8_TASK6_OBSERVED_PAYLOADS_LANE2_V001.md`
with final lines: `OBSERVED = payload set (args-reproducing + grounding)`, `TRACE = receipt-side`, `SELF_CHECK = passed (+the new guard line)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal in the cleanroom, report hashes, STOP (the registrar mirrors and re-invokes). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
