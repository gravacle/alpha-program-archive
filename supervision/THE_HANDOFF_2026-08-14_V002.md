# THE HANDOFF — session transfer instrument — 2026-08-14 — V002

**Supersedes V001 (bc52eaba).** Complete resume-state for a successor registrar
session (any account, same machine, same working directory). Read this + the road
file; everything of record is sealed on disk and pushed to the public git (the
integrity mechanism — never PGP). The predecessor session (this one) ran on the
Dario account, Fable-5-served, session f8e6551f-4dac-4ce6-b653-173732f1a424.

## WHO/WHAT YOU ARE
Registrar/orchestrator for the Gravacle α-derivation program. Principal: Brian
Mulconrey. You verify seals at path, adjudicate build+check pairs (adversarial check
> build > cold re-derivation), register verdicts via the splice pattern, mirror,
commit, push. You never compute α, never flip fences, never enter premises. Builder ≠
verifier always. ALL results CLAIMED until checked. Builds run as in-session
background workflows (build + default-refute audit, both sealed with .seal.sha256
sidecars in alpha-program-archive/workspace/; agents never register/commit/push and
never read register/road/ledger/lens/plan/tracker).

## SINGLE-WRITER RULE
Exactly ONE active registrar. Until the principal switches, the prior session is the
writer. On switch (the principal simply stops prompting the old session), you are the
sole writer.

## THE STATE (register through Q-1058; all pushed; HEAD = 792fca2a at writing)
- Fences: alpha_computed=false · proof_authorized=false · kappa_record_computed=false.
- Register rows today: Q-1050 (wall sharpened) → Q-1051 (P-1 dissolved) → Q-1052
  (r-b/r-c resolved) → Q-1053 (E3 decided; 5a complete; the coupling's only door is
  the principal's authored Rule-2) → Q-1054 (total second-order content: PARTIAL-
  CANCELS; the unconditional slice identity; wall = Re e₂(K_n)) → Q-1055 (the
  channels at sealed max; wall = four definite-signed quantities; the of-record
  divergence detached) → Q-1056 (bare-surface I-2: ILL-POSED, reversion-clean; the
  vacancy = a magnitude-valued response junction; sole supply route named) → Q-1057
  (chain choice audit CLEAN: 58 choices, 0 OPEN; the 51-item premise set = the
  assembly's obligatory list) → Q-1058 (chain relevance audit: 1 surface-native /
  0 surface-derived / 25 apparatus-only as STATUS / 2 trace-incomplete; the
  pushback fork open on both horns with deciders named).
- THE GOVERNING INSTRUMENT (new today): ACTUAL_SURFACE_GUARD_V001.md (2baa4c31 at
  last reseal) — criterion, choice ledgers, TOY_SEPARATION, robustness promotion,
  final-run CHAIN CHOICE + CHAIN RELEVANCE audits before fence 2, and the principal's
  four calibrations: RELEVANCE BURDEN (machinery proves surface relevance, never the
  reverse; determination not construction), CALIBRATION (apparatus-only is a STATUS;
  earning the trace is the primary exit; phase-content landings count), PURPOSE
  (audits are hole-finding, never prohibition; internal soundness from survived
  adversarial work is NOT in question), PUSHBACK FORK (surface pushback poses
  machinery-fit vs surface-description-fit; state both horns; collapse neither).
- LEDGER states: I-2 = ILL-POSED at the referent gate, untestable until the referent
  exists (Q-1056). I-8 = PARTIAL (Q-1054). I-9 (survivor-is-a-feature) = PENDING,
  first test FAILS-AT with the known divergence detached (favorable, uncertified).
- HELD (principal's caution, reopens only on his explicit direction): the
  amplitude-junction bare-surface CONSTRUCTION design (5fa46838) — construction
  toward a named shape is machinery-chasing risk; determinations only.
- STAGED for the principal: FIVE_C_PRINCIPAL_ACTS_STAGING_V001.md (172df331) — five
  decision sheets (r-1; carrier/admission; Attach; DoR-013/014 falsifier; Rule-2).
  DoR-013/014 are RATIFIED — the 5c item is their falsifier, not their issuance.

## IN FLIGHT AT WRITING (adjudicate from disk if this session dies — recipe below)
Four pairs, each = build + default-refute audit, artifacts land sealed in workspace/:
1. r1-naming-candidate (wf_1f02da8f-c29): necessity determination FIRST (derive-
   before-author; the act may be UNNECESSARY — that outcome authors nothing), then
   the candidate PROPOSED-NOT-ADOPTED. Artifacts: STAGE8_R1_NAMING_CANDIDATE_V001.md
   + ..._AUDIT_V001.md. Register on return (audit governs).
2. pushback-resolution-sweep (wf_244389d1-1b2): every pushback event in the recent
   phase classified DETERMINED/NAMED-OPEN/ASSUMED + the catch ledger. Artifacts:
   PUSHBACK_RESOLUTION_SWEEP_V001.md + audit.
3. gravity-close-scoring (wf_4f6835c2-2ef): the FROZEN OBS-22 recognition spec
   (9f0d12b4, bytes govern; GOV-F flags untouched; scoring never declaration; the
   declaration gate is the principal's) scored against the sealed corpus. Artifacts:
   STAGE8_GRAVITY_CLOSE_SCORING_V001.md + audit.
4. kstar-typing (wf_edbcd14a-dee): does the onset root K* (definition of record =
   ALPHA_STIFFNESS_IS_K_STAR, 75450619: C_record(K*)=0, α_micro=1/(4πK*)) tie by
   booked derivation to the surface's §II.9 onset-bound lock; K*'s TYPE only, never
   its value; referent identity at S16 checked at bytes. Artifacts:
   KSTAR_TYPING_DETERMINATION_V001.md + audit.
TO ADJUDICATE ANY RETURN: verify both seals at path (shasum -a 256 vs sidecar), read
BOTH flag blocks in full FROM THE FILES, adopt the audit's verdict over the build's
where they differ, register via the splice, mirror, push. Expected-audit-note: agents
cannot read the register, so "Q-105x unlocatable" findings in audits are expected and
harmless — the evidence artifacts verify at path.

## THE QUEUE (after the four land, in order)
1. THE CONSOLIDATION REFRESH (horn-b decider; REGISTRAR work by precedent — the
   surface doc 20ee87c0 is registrar consolidation, lane fidelity-checked): author
   the refresh delta — append Q-1056's vacancy + corrected inventory (C-A: δ a chart
   variable; C-B: H_R·T_R/ℏ the free role-less invariant; C-C: the transition law in
   the inventory) per the doc's own REOPEN clause; SOURCE §I.5 (the refinement law
   has NO register row under its own vocabulary — candidate booked home = the
   subdivision/transport chain Q-1025/Q-1026; compare to D3's frozen reading
   9a0c2045 = STAGE8_T7_D3_QUANTIFIER_READING_FREEZE..., "all common refinements");
   then a fidelity-check pair; then register.
2. EARN-THE-TRACE (horn-a decider) — AFTER the refresh, targeting the corrected
   surface description: can the response spine land on the surface's phase-rich
   content (the dressed objects are unit-modulus-built)? Determination, never
   construction.
3. 5c acts as the principal directs off the staging sheet; Rule-2 and Attach held
   until the gravity close lands (his stated sequencing).
4. 5d re-read when 5a–5c land. Then the gated sequence (road 6–12).

## THE MECHANICS (exact recipes — unchanged from V001 except as noted)
- REGISTER SPLICE: read QUESTIONS_SETTLED_REGISTER_V001.md (alpha_supervision);
  entry inserted before the unique anchor "## HOW TO USE THIS REGISTER" via
  t.replace(anchor, entry + anchor, 1) with asserts (anchor count==1, new ID absent,
  hash changed); rewrite BOTH sidecars (.sha256 and .seal.sha256), format
  "<digest>  <filename>\n". Always python with utf-8 header; set -e; verify the
  pre-hash prefix before editing (the current register post-hash is 8b9fd2e9...).
- MIRROR: cp register (3 files) + road (2 files) + any touched supervision
  instrument to archive/supervision/; cmp byte-identical; git add -A; commit; the
  post-commit hook auto-pushes (banner "[deploy] pushed"). Trailer: Co-Authored-By
  follows the SERVING model of record (check the session transcript's per-turn
  model field, ~/.claude/projects/<project>/<session-id>.jsonl); Claude-Session:
  the successor session's own id.
- ROAD (ROAD_REMAINING_FLAT_V001.md, current post-hash 30a4fa34...): one flat list,
  completed steps DELETED, ▶/○ only, never re-shape; folds allowed, history lives in
  the register.
- BRIEFS: pose questions + fences + honest-outcome menus; NEVER outcome-
  classification rules. Every brief carries the guard's CHOICE LEDGER +
  TOY_SEPARATION requirements. DERIVE-BEFORE-AUTHOR for any entry.
- If an agent dies on an API error: resume the workflow with resumeFromRunId
  (cached builds replay free). If it API-flags on content: soften adversarial
  vocabulary, resume.
- Memory: this account's auto-memory is ~/.claude/projects/-Users-bgm-MB-Work/memory/
  (role note: project_alpha_relay_lane.md; the guard: feedback_actual_surface_guard).
  The predecessor registrar corpus (read on start): ~/.claude/projects/-Users-bgm/memory/.

## FENCE CARD (never cross without the principal's explicit flip, in order, per F2)
alpha_computed=false → flips only after the verdict of record = FORCED and Gate-1
evidence; kappa_record first, then alpha, then the one-shot end test. GOV-F: sealed
spec flags are the Gate-6 evaluator's to flip, never yours. The lens MOTIVATES AND
CHECKS — never an input. The guard's fence-2 preconditions: the CHAIN CHOICE AUDIT
(done, Q-1057 — keep it current per-brief) AND the CHAIN RELEVANCE AUDIT (Q-1058 —
the hole inventory must be cleared by earned traces / named premises / supply, in
the guard's own order, before any assembly).
