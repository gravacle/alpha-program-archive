PASTE 388 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE N-UNIQUENESS THEOREM — IS THE SOURCE→OUTPUT NORMALIZATION FORCED BY THE TWO REQUIRES? (THEN INSTALL AND RE-RUN)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-305 + C38)

```text
DOES THE OBJECT EXIST?   THE QUESTION does, forced by both lanes jointly: the direct
                         identification is provably incompatible with the same-correlator
                         identity AND U1-reality together (Q-305, TEST-CLEAN); the
                         residual is exact (Q-304: hbar q (1-i) L(x)L); W's i is spent.
                         The missing object: the source->output normalization map N at
                         the boundary between the Theta-even source slot and the
                         Theta-odd raw output.
IS THE VERSION CURRENT?  YES through Q-305 + C38. Marks: TYPE-P | premises: DoR-008/009/
                         013/014-as-amended/015. The freeze stands until full Door D
                         passes.
ARE ITS INPUTS PRESENT?  YES: the audit's factor derivation (0fcf42e8...: D_J A-bar -
                         (i/hbar)G_b = q[b - i hbar L(x)L]), the parity table (Q-303),
                         the sealed involution law, the even pairing b = hbar L(x)L
                         (Amendment 2), the identity's sealed coefficient (i/hbar).
```

## 1. THE THEOREM, THEN THE INSTALL

```text
1  STATE the admissible class for N exactly: maps from the even source image to the raw
   output consumed by the identity, constrained by (i) the same-correlator identity with
   its SEALED coefficient, (ii) U1-reality under the sealed involution law (N's own
   transformation included -- derive how an even->odd map MUST transform), (iii) the
   coefficient-neutral crossings (Q-305: no other structure supplies phase), (iv)
   naturality with restriction/adjoints (W3) and the quotient.
2  *** PROVE OR REFUTE UNIQUENESS: is N forced to be multiplication by the unit factor i
   (up to nothing)? The expected proof shape: the identity fixes N's modulus and phase on
   the open-path directions where the residual lives; the involution law fixes N's
   conjugation behavior; show any unit phase e^{i theta} other than theta = pi/2 fails
   one of the two requires. HANDLE THE DEGENERATE DIRECTIONS (cycles, where L = 0)
   explicitly -- uniqueness need only hold on the directions the identity constrains;
   state what happens off them (naturality should propagate; prove it). ***
3  IF UNIQUE: install N as DERIVED -- germ V007 = V006 + N with its uniqueness
   certificate (the two requires as its provenance; the OBS-16 pattern recorded); THEN
   RUN THE FULL FOUR-SUBTEST DOOR D. Clean PASS: the freeze lifts; run the lifted chain
   (the 382/386 continuation: lifted G, sectors, p-content, subtraction, accounting,
   p-trace, remaining-objects list).
4  IF A FAMILY SURVIVES: enumerate it exactly (the surviving phases/maps), DO NOT
   choose; the authored fallback returns to the principal with the family on the table.
5  KILL-PASSES: N must not touch the source slot's parity (the even pairing stands); no
   double-billing of any i (the ownership table: identity's i sealed, W's i spent, N's i
   -- if unique -- its own); minimal diff on the germ; no selection; restriction checks
   at every stage.
DELIVERABLE: the uniqueness theorem (PROVED with the install + full Door D verdict + on
PASS the lifted chain / FAMILY with its enumeration / obstruction named). Typed.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
