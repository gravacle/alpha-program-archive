PASTE 378 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] CROSS-VERIFY THE LAW-SIDE SUBPACKAGE — THE UNVERIFIED FLOOR UNDER P5

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-294)

```text
DOES THE OBJECT EXIST?   YES: the law-side subpackage (8e9a09c1..., built by the OTHER
                         lane, Q-278): the exact cylinder-amplitude system, the E_post
                         orientation structure, the law-side glue pair, bounded domains,
                         on the discharged common origin. IT HAS NEVER BEEN CROSS-VERIFIED
                         -- and the P5 chain (Q-284, verified) SITS ON IT. The P2
                         verification caught a propagating sign (Q-276); the same
                         standard applies here.
IS THE VERSION CURRENT?  YES through Q-294.
ARE ITS INPUTS PRESENT?  The subpackage artifact, DoR-009/013/014, F_N/A_N, PathCert
                         (Q-238), P2 V002, the finite references (Q-243, Q-279).
```

## 1. THE CHECKS — INDEPENDENT RECOMPUTATION

```text
Y1 THE CYLINDER-AMPLITUDE SYSTEM: recompute the amplitudes on your own from F_N/A_N and
   the germ's certificates; verify the zero-section pullback consistency claim (C7's
   actual content, per Q-278's own careful distinction from marginal consistency).
Y2 THE ORIENTATION STRUCTURE: E_post's encoding verified against DoR-009's certificates
   and the Q-264 exchanged-root refutation (the single-root discipline carried?).
Y3 THE GLUE PAIR: P2's source completion + PathCert's record-only GNS limit -- re-prove
   the pair's exactness; check the boundary honesty (Q-278 said it is NOT a joint
   boundary package -- confirm nothing downstream treats it as one).
Y4 BOUNDED DOMAINS: the bounded-domain claims re-derived; confirm no unbounded operator
   is used anywhere downstream without its TYPE-U mark.
Y5 CONVENTION AUDIT: the W-convention (Gamma = -Log Z), the Q-276 sign, and the
   restriction checks to Q-243/Q-279 -- consistent across the subpackage and its
   consumers (P5 chain, the seed)?
Y6 Tail/door audit: no unflagged class formation inside the subpackage (the Door F
   lesson from Q-294 -- projective/limit steps flagged?).
VERDICT: CONFIRMED / DEFECT (location + inheritance list) / CONFIRMED-WITH-NOTES.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
