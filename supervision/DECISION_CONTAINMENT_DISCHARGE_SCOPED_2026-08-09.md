# DECISION OF RECORD — CONTAINMENT DISCHARGES PATH B, SCOPED (2026-08-09)

Registrar determination, routed by relay 815 ("if the registrar so rules"), taken as an
argument-soundness (logic/process) ruling after RULING-LAST's three passes; the principal
told in-session.

**Ruling:** the sealed containment D ⊆ diag(candidates) — assembled from V011's sealed
spans (D ⊆ F_phys × F_phys; the finite totally-ordered ≤400-mode candidate enumeration;
diagonal-only criterion consumption) — DISCHARGES materiality Path B's demand ("an
enumeration of the pairs the sealed criteria consume") FOR RESTRICTION-TYPED CONSUMERS
ONLY. Ground (the provable part, displayed): an over-approximation of an evaluation
domain is sound for any argument that only RESTRICTS over that domain; soundness fails
for value-summing consumers, where exact-D changes the result.

**Scope guard:** any future consumer that sums or integrates over exact-D does NOT
inherit this discharge and re-opens Path B for itself. The discharge is tagged
PART-PROVABLE: the soundness implication is proved; the classification of each future
consumer as restriction-typed is checked per consumer at consumption time.

**Effect:** materiality GAP 2 (the pair set D) closes in scoped form on the already-
sealed spans. GAP 1 (the Hessian's form) is unaffected and waits on (H1), (H2), I3.
