PASTE 317 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2d] WIRE THE FOUR HANDOFFS OF F_N

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-234)

```text
DOES THE OBJECT EXIST?   YES: F_N = P_0 + Z_N P_ch, Z_N = prod_j conj(z_-,j) z_+,j
                         (2cd1ffce..., all checks + the one-cell falsifier PASS).
IS THE VERSION CURRENT?  YES -- DoR 008 + 009 in force; marks TYPE-P on both.
ARE ITS INPUTS PRESENT?  YES. This relay CONSUMES the handoffs 316 documented.
```

## 1. THE TASK — FOUR RECEIPTS, EACH CERTIFIED, EACH WITH ITS NEXT STEP NAMED

**For each consumer: certify receipt of F_N at its interface (what it needed, what F_N supplies,
what remains), and emit the consumer's NEXT CONCRETE RELAY-SIZED STEP. Four short sections, one
artifact.**

```text
1  U2's DYNAMICS PORT: wire F_N as the dynamics component of the U2 skeleton (Q-218's typed
   slot). State what U2 still lacks afterward (the state port -- operator-valued sandwich, the
   scalarization TYPE-R noted in Q-234 -- and the effects port). NEXT STEP: named.
2  TASK 3a's INSTANCE: F_N vs the sealed doubled-CTP influence FORM (Q-210). Does F_N INSTANCE
   that form at finite N within its scope (external-parent exclusions per DoR 009 row 3)?
   Certify or type the residue. NEXT STEP: named.
3  TASK 3c's FAMILY: the multiplier protocol stopped at step 1 for want of an instantiated
   PHYSICAL family (Q-202). Does F_N + the law now instantiate the physical multiplier family
   at finite N? If yes, step 1 REOPENS -- say exactly with what family. NEXT STEP: named.
4  THE ENVELOPE'S EDGES: the transition envelope lacked dynamics edges (Q-226). F_N's U_N[a]
   IS the finite edge, source-coupled. Wire it: which envelope nodes connect, what the path
   -level provenance witness now looks like with a REAL edge. NEXT STEP: named.
```

*** MARKS THROUGHOUT: TYPE-P | premises: DoR-008, DoR-009. NO second variation, NO kernel, NO
B_ind. EACH "NEXT STEP" MUST BE RELAY-SIZED AND PREFLIGHT-CLEAN -- they become tomorrow's queue. ***

## 2. LEADS

```text
IF ALL FOUR WIRE CLEAN -- lead with the four next steps as a ready queue.
IF A CONSUMER'S INTERFACE DOESN'T ACCEPT F_N -- lead with the mismatch, exactly; that is a
defect in either the handoff or the consumer's spec, and it must be typed before anything
builds on it.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
