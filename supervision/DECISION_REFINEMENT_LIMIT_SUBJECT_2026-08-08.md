# DECISION OF RECORD — THE REFINEMENT LIMIT'S SUBJECT IS THE INTENSIVE COEFFICIENT (2026-08-08, principal ruling)

Q-629/Q-630 context: the corpus is SILENT on what the refinement limit is taken
of (Dario, 724: nine occurrences, zero typing uses; three sealed texts naming
three different subjects). The principal rules: THE SUBJECT IS THE INTENSIVE
QUADRATIC COEFFICIENT — V011's own invariance subject, computed from the
compatible response (the response acknowledged as source, NOT identified with
the coefficient; the completion is not the subject — fixing the limit on the
one candidate whose realization (J_ref) is missing would be the unbuilt-object
trap).

Consequences of record:
1. The sealed Duhamel/Montel density topology (intensive logs over N_4,
   locally uniform on the polydisc) is a topology ON THE RULED SUBJECT'S KIND.
   The "choose one of three topologies" question is DISSOLVED as posed (Q-629:
   the entries were never typed); the limit's sense for the coefficient is the
   sealed density instance, PENDING ONLY ITS INDEX.
2. The index gap stands unchanged: the sealed instance is exhaustion-indexed;
   T_ref needs refinement-indexed — supplying that index IS RA27-2's
   same-region relation (Q-629 GAP 1). Nothing here forms it.
3. tau_cyl's elimination (Q-627) survives — it ran through property statements
   independent of carrier assignment. tau_norm remains what it was: a
   completion-class topology, untyped as the limit's sense, available to
   J_ref-class needs if and when J_ref exists.
4. The S5.3 burden carries: an instantiated coefficient limit must PROVE its
   defining convergence ((H1)/(H2) not discharged, not assumed).
5. The lambda/beta question remains open and independent (Q-627).
