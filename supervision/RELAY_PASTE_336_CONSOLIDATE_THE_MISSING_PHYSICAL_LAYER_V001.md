PASTE 336 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] CONSOLIDATE THE MISSING PHYSICAL LAYER — ONE SPEC, DEDUPLICATED, WITH ITS CONSTRAINT BATTERY

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-252)

```text
DOES THE OBJECT EXIST?   THE MATERIAL does, scattered: Q-239 (five U3 fields), Q-245
                         (restriction maps + separation-to-physical), Q-249 (the
                         PHYSICAL_RETHESS_CLASS_AND_RESTRICTION_PACKAGE, seven parts),
                         Q-250 (tail-quotient determinacy), Q-251 (six consumption
                         specifications), Q-252 (the background pair Abar_*, G_*, the
                         physical pullback map, the complete Thomson functional).
                         THE CONSOLIDATED SPEC does not exist. Both halves of the deciding
                         question bottom out in it.
IS THE VERSION CURRENT?  YES, through Q-252.
ARE ITS INPUTS PRESENT?  Register entries + their artifacts (hashes in the register), plus
                         the standing theorems that CONSTRAIN the layer.
```

## 1. THE TASK — THE COMMISSIONING BASIS, THE PRE-LAW PATTERN (Q-229 PRECEDENT)

```text
1  ENUMERATE every named missing physical-layer object across Q-239, Q-245, Q-249, Q-250,
   Q-251, Q-252 (and any the artifacts name that the register compressed away -- read the
   artifacts, not just the register). DEDUPLICATE: where two entries name the same object in
   different vocabulary, prove the identification (cite both texts) -- do not merely assert.
2  DEPENDENCY-ORDER the deduplicated list: which objects, if built, make which others
   buildable or moot; which are INDEPENDENT axes. Mark each object: THEOREM-SHAPED (derivable
   from ratified data -- name the candidate route), CONSTRUCTION-SHAPED (buildable but
   needing declared inputs -- name them), or AUTHORED-PHYSICS-SHAPED (new physics the corpus
   does not contain -- the D6/law pattern; ratification territory).
3  ASSEMBLE THE CONSTRAINT BATTERY the layer must survive, from standing theorems: DoR-008's
   falsifier (finite restriction reproduction, sealed results); Q-243 (finite retarded block
   zero and p_ch-free); Q-245 (finite restrictions forced p_ch-free; no naive continuous
   extension); Q-247 (separation on ratified classes; tail structure explicit); Q-250
   (modulo-tail determinacy); Q-251 (visible quotients finite-domain); Q-252 (no finite
   stationary point for p_ch in (0,1); C1 not an evaluation rule; the three zeros distinct);
   the FINITE-AUTHORITY PRINCIPLE (an authored consumption that SEES the tail smuggles
   non-finite physics -- battery item, principal's standing basis). Every battery item cited.
4  STATE, for the deciding question specifically: the MINIMAL SUBSET of the layer sufficient
   to decide p_ch cancellation vs survival -- not everything the layer contains, the minimal
   deciding subset, with its dependency chain. If the minimal subset is smaller than the
   full package, THAT is the commissioning target and the spec must say so prominently.
5  KILL-PASSES: no inventing new objects (consolidation only); no silent dedup (each
   identification proven); no promoting AUTHORED to THEOREM anywhere.
6  FENCES: reading and organization only. No evaluation, no roots, no numbers.
```

## 2. LEADS

```text
LEAD WITH THE MINIMAL DECIDING SUBSET AND ITS SHAPE-BREAKDOWN: how much is theorem-shaped
vs construction-shaped vs authored. If the p_ch question is decidable with LESS than the
full layer, that is the headline. If any single authored object gates everything, name it --
that is the next commissioning decision for the principal.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
