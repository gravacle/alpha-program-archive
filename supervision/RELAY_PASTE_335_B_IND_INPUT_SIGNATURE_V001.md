PASTE 335 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] B_ind'S OWN INPUT SIGNATURE — IS THE FIXED POINT TAIL-BLIND BY DOMAIN? (DoR-011)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-251)

```text
DOES THE OBJECT EXIST?   THE QUESTION does (Q-251): every alpha-facing consumption is
                         unspecified -- EXCEPT possibly B_ind's, whose sealed v004 DEFINITION
                         exists and whose possible tail term is exactly p_loc(t_ind).
                         Q-251's own reading names the one remaining derivation: if B_ind's
                         inputs are ALL finite-stage objects, tail-blindness FOLLOWS from
                         Q-247's separation -- no consumption ruling needed.
IS THE VERSION CURRENT?  YES: Q-247 (separation on ratified classes; tail killed by every
                         finite restriction), Q-250 (response determined modulo tail),
                         Q-251 (six consumptions unspecified; the visible quotients are
                         finite-domain and tail-independent BY DOMAIN -- the precedent for
                         exactly this argument shape).
ARE ITS INPUTS PRESENT?  The sealed v004 texts defining B_ind and p_loc (the zero-bare
                         architecture: K = B_ind(K), alpha = 1/(4 pi K) as normalization
                         identity), and the sealed definitions of every object B_ind's
                         definition mentions.
```

## 1. THE TASK — READ B_ind'S DOMAIN, NOT ITS INTENT

```text
1  QUOTE B_ind's complete sealed definition (v004) and enumerate EVERY input object it
   consumes: arguments, parameters, implicit context objects, and the domain p_loc is
   applied to inside it. For EACH input, determine from sealed text: FINITE-STAGE
   (a record-cell / finite-N / restriction-level object) or COMPLETED (an object of the
   completed response/correlator layer) or AMBIGUOUS (the text underdetermines it -- quote
   the ambiguous phrase EXACTLY).
2  *** THE THEOREM SOUGHT (the Q-251 visible-quotient move, one level up): IF every input in
   (1) is finite-stage, THEN the tail -- killed by every finite restriction, Q-247 -- cannot
   enter B_ind through ANY input, so B_IND_FIXED_POINT_TAIL_BLIND = true BY DOMAIN, and the
   possible term p_loc(t_ind) is vacuous because t_ind never reaches p_loc's argument.
   PROVE IT AS A DOMAIN STATEMENT, input by input. ***
3  IF ANY INPUT IS COMPLETED-LAYER: name it, and state whether p_loc(t_ind) is its exact
   tail exposure (connecting to Q-251's term) -- that would make B_ind's tail question
   equivalent to that ONE input's consumption specification, a strict narrowing.
4  IF AMBIGUOUS: the ambiguity is the finding. State BOTH readings, what each implies for
   tail-blindness, and WHICH reading the surrounding sealed context supports -- do not pick;
   an ambiguous domain is a ruling for the principal with the two readings on the table.
5  KILL-PASSES: no resolving ambiguity by convenience; no treating "K is a local surrogate"
   (Q-186/Q-188) as if the surrogate's FINITENESS were sealed -- check what the text
   actually types K's carrier as; the corpus-audit traps bind (substring presence is not
   definition; B_ind may be defined in more than one place -- report ALL definitions and
   any version differences, repair nothing).
6  FENCES (DoR-011): reading and proof only. No evaluation of K, no roots, no numbers, no
   comparison to measured constants.
```

## 2. LEADS

```text
IF ALL INPUTS FINITE-STAGE -- *** LEAD WITH THE DOMAIN THEOREM: THE FIXED POINT IS TAIL-BLIND;
no tail content can move K, and the tail route to p_ch-in-alpha DIES AT THE FIXED POINT
regardless of the other five outputs. State it exactly that strongly if proved. ***
IF ONE INPUT IS COMPLETED-LAYER -- lead with the input and the narrowed equivalence.
IF AMBIGUOUS -- lead with the two readings and their consequences, side by side.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
