PASTE 377 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] FIELD_SIGNATURE_PHYS V004 — THE FRAME-TANGENT ANSWER, DOOR F, AND THE BOUNDED RERUNS (DoR-015 RESERVED)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-294)

```text
DOES THE OBJECT EXIST?   V003 (a5a8420d..., YOUR draft) is REPAIR_THEN_READY (Q-294, review
                         6e191e56...): the frame-tangent refutation (dH != 0 at fixed A
                         while every u_e = 0), the unflagged {X_N} -> X_inf projective
                         class-formation, and the named bounded reruns. Most of the object
                         SURVIVES -- repair, do not rebuild.
IS THE VERSION CURRENT?  YES through Q-294.
ARE ITS INPUTS PRESENT?  V003, the review (READ ITS COUNTEREXAMPLE AND RERUN LIST EXACTLY),
                         the Q-288 ledger, the record-visibility machinery (the quotient
                         apparatus you built in V003).
```

## 1. THE REPAIRS

```text
R-1 THE FRAME-TANGENT QUESTION, ANSWERED NOT DODGED: are endpoint-frame directions
    (a) RECORD-VISIBLE physical content -- then the current family must be EXTENDED with
    frame-tangent currents (construct them; prove the extended family separates the
    framed carrier; state their physical meaning in record terms -- what record data
    detects a frame change at fixed A?); or
    (b) BOUNDARY GAUGE -- then the visibility quotient must ABSORB frame directions
    (prove the frame-quotiented carrier is what every consumer actually consumes, and
    that dH's frame-dependence is bookkeeping that cancels in all family-wide outputs).
    *** DECIDE BY THE PROGRAM'S OWN CRITERION: does ANY finite realized record
    distinguish two frame choices at fixed A? If yes -> (a); if provably no -> (b).
    The review's counterexample says dH != 0 -- determine whether H itself is
    record-visible or only frame-covariant. THE ANSWER IS PHYSICS; derive it. ***
R-2 DOOR F: flag the {X_N} -> X_inf projective class-formation explicitly -- its
    topology, kernel/image, sector transfers, restriction square, Tail_R action (the
    Q-288 row it never had). If the projective limit is tail-safe, prove it; if it
    could create tail content, that is a Q-285-class door and gets the full flag.
R-3 THE BOUNDED RERUNS: execute every rerun the review names, on the repaired object.
R-4 REGRESSION: seams, doors (D0 zero-tail re-check), the six-count, family-wideness of
    the torsor solution, quotient separation on the REPAIRED carrier.
KILL-PASSES: no both-ways answer on R-1 (one determination, derived); no unflagged
class-formation anywhere (sweep your own draft); no forbidden completions; no selection.
DELIVERABLE: V004 with the frame-tangent determination (extended currents OR absorbed
quotient, with the record-level proof), Door F flagged, reruns executed, regressions.
DoR-015 reserved; final review follows.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
