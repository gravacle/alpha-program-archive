## PASTE 592 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5] STEP 3: THE J-II REALIZATION FAMILY — THE LAST CONSTRUCTION

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-519. DoR-020-A9 is law (Ξ_N licensed; read the decision file). STEP 3 of the closing board: the J-II realization family — the ONE genuine unbuilt construction both Γ routes share, at which three artifact chains terminate.
- The gap of record (from your own Γ^H build `f2317e41…` J-II and the gap audit `61d41a3e…`): the physical Loc / symbol-to-cochain REALIZATION has no sealed MEMBER — three sealed artifacts DECLARE it (the gap audit's correction: the declarations exist; the built member does not). The F3 remainder (`5de94e16…`) and the ExtSrc/G4-D/G5 gates of A9 all wait on the same family.
- Authorities: the three declaring artifacts (locate them by the gap audit's citations and verify), the finite sigma^fin stock (`0bef9a00…`), the confirmed assembly chain, the seams V002 (`2525096b…` — the addressed orbit family it must respect), DoR-019 (the metric), A6/A9 (what consumes the realization on each route).
- MANDATED SECTIONS: the SURFACE ANCHOR section per LOCKED_PROCESS (every PASS on named actual objects; the geometry/rails split for anything new; the R9-lesson quantification check) AND the dependency re-audit for any clause you state.
- Custody: you build; the review pairs cross-family. Tag PROVABLE / PART-PROVABLE / YOURS. Honesty clause: an obstruction outranks a build — if the realization family cannot be inhabited from sealed stock, the exact obstruction per declaring artifact, displayed; that outcome routes the program through its own typed alternatives, and finding it now is the win.

TASK:
H1. **The specification consolidated:** what the realization member must BE, assembled from the three declaring artifacts — domain, codomain, the symbol-to-cochain action, the properties each downstream consumer actually uses (walk: the Γ^H joints, the F3 base squares, the S22–S25 instantiation, A9's ExtSrc gate) — the minimal common specification, displayed.
H2. **The inhabitance attempt, assembly-first:** every piece cited with type; the construction attempted joint by joint (per the 578 method that worked); each joint DISPLAYED or STOPPED with its exact gap.
H3. **If inhabited:** the immediate consequence board — which of the four Γ^H joints now close, the F3 base squares provable, the S22–S25 instantiation, which A9 gates inhabit; the identification falsifier's first commonly-formed cells if any.
H4. **If obstructed:** the per-declaration obstruction display + the typed alternatives (what the guard/axiom text would carry instead — analysis, not adoption).
H5. **Battery:** F_PLDEC; anti-tuning; the SURFACE ANCHOR section; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md`
with final lines: `SPEC = CONSOLIDATED`, `REALIZATION = INHABITED (+joints closed) / OBSTRUCTED (+per-declaration display)`, `CONSEQUENCES = stated`, `SURFACE_ANCHOR = present`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
