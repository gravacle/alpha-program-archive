PASTE 299 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2f] RUN THE FORCING PROTOCOL ON THE REALIZATION FUNCTOR

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-215)

```text
DOES THE OBJECT EXIST?   As a FAMILY: candidate maps sequential-labels -> oriented chain carriers
                         (your own 297 artifact defines the class). Not canonical BY INSPECTION --
                         whether it is canonical BY CONSTRAINT is exactly this relay.
IS THE VERSION CURRENT?  YES.
ARE ITS INPUTS PRESENT?  Both ends sealed/ratified; the one-cell restriction ALREADY PASSES
                         (Q-213) -- that is the anchor constraint.
```

## 1. THE PROTOCOL, ON MAPS INSTEAD OF OBJECTS

```text
STEP 1  DECLARE THE CANDIDATE FAMILY before any outcome: all maps from the sequential label
        carrier to finite oriented chain carriers satisfying the DATA conditions your 297
        artifact states (typed domain/codomain, well-defined on the ratified structure).
        *** INSTANTIATED family -- if it cannot be written down as a parametrized class, say so. ***
STEP 2  EQUIVALENCE: maps equal up to the sealed vertex-gauge/orientation conventions are one.
STEP 3  FAILURE-CAPABLE CONSTRAINTS, each with a named potential victim:
        (i)   BASE CASE: restrict to the single record cell -- must reproduce Q-213's PASSING
              restriction exactly;
        (ii)  NATURALITY: commute with the sequential inclusions N <= M;
        (iii) ORIENTATION/BOUNDARY compatibility with the sealed incidence differential;
        (iv)  finite support / unit-weight compatibility (Gate 4's derived normalization).
STEP 4  COVERAGE: show the declared family exhausts the data-condition-satisfying maps.
STEP 5  COUNT SURVIVORS. 1 -> *** THE FUNCTOR IS DERIVED, no adoption needed -- then RUN THE
        K_SQUARE FALSIFIER CHECK WITH IT. *** N -> the residual family, sized: the honest
        adoption ask. 0 -> the constraints conflict; name the pair -- that would bear on DoR 008.
```

*** THE Q-183 RULE: a constraint all candidates satisfy cuts nothing. THE Q-205 PRECEDENT: this
exact protocol turned an infinite family into a proved four -- and once into a proved ONE. ***

## 2. LEADS

```text
IF UNIQUE -- *** LEAD WITH THE DERIVED FUNCTOR AND THE EXECUTED K_SQUARE CHECK. The falsifier arms
with no new adoption. If the check FAILS, THE FALSIFIER FIRED -- lead with that above all. ***
IF A RESIDUAL FAMILY -- lead with its size and parametrization; the principal decides on a sized,
honest ask.
IF THE CONSTRAINTS CONFLICT -- lead with the pair; a base-case/naturality conflict would be
evidence against the ratified label scheme itself.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
