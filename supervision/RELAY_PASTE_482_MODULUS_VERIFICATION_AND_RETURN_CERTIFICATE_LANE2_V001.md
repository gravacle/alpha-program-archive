## PASTE 482 — CODEX LANE 2 (HIGH EFFORT) — [TASK 5] CROSS-VERIFY THE CARRIER BUILD AND THE MODULUS FACTORIZATION; TYPE THE RETURN CERTIFICATE

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-403.
- Artifact under review (Lane 1's build): `alpha-program-archive/workspace/STAGE8_TASK5_SCALAR_CARRIER_BUILD_AND_MODULUS_LANE1_V001.md`
  SHA-256 `aebe708d2e7ba4b67e828976bd01eae2d5eec04afbb6a28f3f77f9dc8003fc97`. Verify before reading.
- Authorities: your premise sweep `feb84216…`, the conditional theorem `88854f08…`, the coefficient result `70dde1c0…`, metric V005 `2a379098…`, Q-401/402/403.
- Custody: adversarial verification + the forward typing. p, ν symbolic; no values.

TASK — PASS / KILL per item:
X1. **The completeness construction** (the ambient scalar Maxwell line) — recompute; the memberwise reader boundedness — verify per member and the family discipline.
X2. **THE FACTORIZATION** q_loop = |p_loc[R_K]| · sup_K|a_loop(K,p,ν,I)| — recompute independently on the reciprocal loop; verify no hidden term breaks the clean factor; verify the S8-A statement (the additional covariant directions) and what they add to the modulus there.
X3. **The obstruction** — verify composite boundedness/closure genuinely need the global stationary-return/regularity certificate; TYPE that certificate exactly (what it must assert, on what carrier, what it consumes, derivable-or-constructible-or-underdetermined).
X4. **The structural sweep on the modulus factor:** does any ratified clause constrain |p_loc[R_K]| STRUCTURALLY (a normalization class, a bound, a covariance restriction) — NOT its value? A ratified bound |p_loc[R_K]| ≤ B would combine with the sup factor toward q < 1 or against it. Sweep honestly; the fiber discipline holds — no value, no selection.
X5. **The sup factor:** verify sup_K|a_loop| is well-defined on the built carrier and work its (p, ν) dependence symbolically — the modulus's freedom-map completed as far as the build allows.
X6. **Falsifiers + one fresh attack.**

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_MODULUS_VERIFICATION_AND_RETURN_CERTIFICATE_LANE2_V001.md`
with the verdict table X1–X6, the certificate typing, and final lines:
`BUILD = CONFIRMED / KILLED (+item)`, `FACTORIZATION = CONFIRMED / KILLED`, `RETURN_CERTIFICATE = DERIVABLE / CONSTRUCTIBLE / UNDERDETERMINED (+exact)`.
Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No numeric evaluation; no registered verdict; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
