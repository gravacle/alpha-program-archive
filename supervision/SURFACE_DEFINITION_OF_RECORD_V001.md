# SURFACE DEFINITION OF RECORD — V001

**Purpose.** One consolidated, verified definition of the record surface, assembled from
booked results, so that findings check against a *defined object* rather than a re-derived
fragment — and so the one genuinely open edge is named, not rediscovered. This is
registrar consolidation of already-sealed, already-verified entries; it authors no new
physics. Every clause carries its booked source. Goes for a lane fidelity-check
(does it faithfully consolidate the register?), not a physics re-derivation.

Fences live: alpha_computed=false, proof_authorized=false, kappa_record_computed=false,
JOINT_ANCHOR_DERIVED=false. Every scale symbolic.

---

## I. THE GROUND — what the surface is made of

1. **Connection-only.** The record's dynamics is indexed by connection / holonomy histories
   alone; there is **no native metric argument** anywhere in the ratified finite object, and it
   takes no logarithm. (Q-1005 / Q-1006; 1101 built, 1102 checked, both-lane.)
2. **Carrier.** Continuous fibers over a discrete base; base-smoothness exists only in the
   fibers. (OBS-12.)
3. **Holonomy characters.** z_j^(n) = χ_n(h_j[a_j]), **unit modulus**, n ∈ {+1,−1}; transition
   functions z_j = g_ij z_i, g_ij = exp(iθ_ij) ∈ U(1).
4. **Affine connection space.** "Connections on a fixed line bundle form an affine space;
   bundle geometry does not choose one." No fiber metric; a radius is a metric datum, not a
   surface datum. (Register :224.)
5. **Refinement law.** The exact derived refinement/coarsening structure of the cell.

## II. DEFINED PROPERTIES — what the surface forces (all booked)

6. **Phase-rich, amplitude-poor.** The write structure carries unit-modulus (phase) weights,
   **not** amplitude/magnitude. "Whatever converts internal to external is NOT a magnitude on
   record." (Register :6093.) Consequence, booked TYPE-R (refuted, exact mechanism): the
   ratified write-signature route to closing β **fails** — unit-modulus structure cannot produce
   a bounded trace-class→T_cyl lift; the physical signature needs weight/amplitude structure the
   records provably do not carry. (Q-290, relay 371.)
7. **The scale orbit is free.** First-opening kinematics fix the *dimensionless* interval
   τ_R = π/√2 but **not** the absolute scale: (T_R → λT_R, H_R → H_R/λ) leaves all first-opening
   probabilities, orthogonality, endpoint transfer, and projective lengths identical.
   `absolute_SI_record_duration_derived = false`. (BID Absolute Record-Scale Identifiability
   Gate, 8f9bdd93.)
8. **Every derived junction is scale-free (β-invariant).** Of the **thirteen sealed interface
   quantities** on the record cell, every junction that is *derived* is β-invariant — a fixed
   point of the scale action. (Register :5187; sealed β-closure hunt 04c10c0d, **four
   adversarial verifiers confirmed**.)
9. **The four derived locks** are fixed points of the scale action: |ΔS_record| = πℏ (an action);
   m_* T_R = π; the balanced-geodesic identities; the onset bound.
10. **Net:** the surface forces the entire **dimensionless** structure and leaves the **absolute
    scale free**.

## III. THE RAIL SIDE — what is NOT a surface object (booked)

11. **The internal/external metric conversion is a rail import**, not a surface object: it is a
    chart on the free presentation orbit; its selection is a modulus of an imported fiber, not of
    the surface. (Re-derived tonight off 04c10c0d; consistent with :6093 and Q-290.)
12. **The three β-sensitive junctions — NONE derived**, each off the surface (04c10c0d, four
    verifiers): the fiber proper radius R in K_KK (**ADOPTED** ansatz), the complete normalized
    D_BR spectrum (**UNBUILT**), the skeleton-to-cell embedding where the 4π-class factors enter
    (**GAP**, "has never been attempted").

## IV. THE ONE OPEN EDGE — named precisely

13. **S16 — threshold-conditioned Thomson matching, the bridge to α (K_* → κ_Thomson).**
    Underived ("named, underived; this is the bridge to alpha"). **Whether a *derived* S16 would
    be β-sensitive is UNDETERMINED of record** — nothing booked on it — *with a stated reason to
    expect YES:* Thomson matching lands on a spacetime cross-section, and β **is** the
    internal-to-length conversion. Every Thomson link is currently TYPE-U. Slot 16 must transport
    a cell quantity to a continuum coupling (principal act, K_* the onset root; the relocated hard
    problem). **This is the ONLY junction that could be both DERIVED and SCALE-SENSITIVE — the
    sole surviving Finish-A lever, and where a derived amplitude/weight structure could enter.**

## V. THE SURFACE'S VERDICT ON α

14. As it stands, on the defined surface: **the scale is free ⇒ α is determined up to one free
    scale ⇒ Finish B** (honest conditional), TYPE-U. **Finish A survives iff S16 (item 13) is
    derived AND turns out β-sensitive** — currently undetermined, reason-to-expect-yes booked.
    This is not a re-derivation to be re-checked; it is the *consequence* of I–IV.

## VI. ROAD MAPPING — each item vs the defined surface

| Road item | Status against the defined surface |
|---|---|
| 1 — branch-state measure | rides on the absolute normalization = the free scale; the **allow** side; not independent of the scale question |
| 2 — transport fields (connection/support/current-density) | connection is **native** (surface); support & current-density are **free** inputs (allow) |
| 3 — gluing obstruction | a **derived** junction ⇒ β-invariant, scale-free |
| 4 — metric conversion (β) | **RAIL — not a surface object. STRUCK** (§III) |
| 5 — β-invariance → common cell | β-invariance is the **defined property** (§II.8); the common cell is the free-orbit presentation |
| 6 — junction | the derived junction inventory (13 interface quantities), all β-invariant |
| 7 — charged Q-spec | feeds **S16** — **the open edge (§IV).** This is where the live question actually is |

**Consequence for the road:** items 2,3,5,6 are defined surface objects (scale-free); item 4 is
a rail (struck); item 1 is the free scale (allow); **item 7 → S16 is the one open edge.** The
top of the road collapses to a single live question — is the *derived* Thomson bridge (S16)
β-sensitive? — with everything else defined and settled.

---

**REOPEN / UPDATE:** this definition updates only by (a) a new booked surface result, or (b)
resolution of S16's β-sensitivity. It does not get re-shaped; entries append with their source.
