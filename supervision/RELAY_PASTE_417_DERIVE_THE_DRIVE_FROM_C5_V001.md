PASTE 417 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] DERIVE THE DRIVE FROM C5 — THE LAW'S OWN EXTERNAL-HISTORY DEPENDENCE READ AS THE MUTUAL-SOURCING MAP (THE DRIVE RACE, DERIVATION ARM)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-334 + C44)

```text
DOES THE OBJECT EXIST?   THE DEPENDENCE does, ratified: C5 (DoR-009's charge/flux access
                         certificate) states HOW an external history affects the charged
                         write -- the RECEIVING end of mutual sourcing, sealed. THE MAP
                         does not: what a neighbor system's history FEEDS (d_J, d_R --
                         V007's port types). THIS ARM DERIVES; the other authors,
                         uncoordinated. Lane-tagged filename.
IS THE VERSION CURRENT?  YES through Q-334 + C44. The four-theorem depth conclusion
                         binds: whatever this derives must inject genuine drive (the
                         identity slice is the null case).
ARE ITS INPUTS PRESENT?  YES: C5's exact certificate text (READ it as a mathematical
                         dependence: which functional of the external history enters
                         the write, in what slot, with what covariance); V007's port
                         types (J in l1+ (+) l1-, symmetric trace-class R); the write
                         dynamics (the law); the tower machinery (the drive's test bed).
```

## 1. THE DERIVATION ATTEMPT

```text
1  READ C5 AS A MAP: extract the exact functional dependence -- external history h ->
   the write's modification. C5 is a certificate ABOUT the law; invert the reading:
   the class of external influences the law RESPONDS to is exactly the class a
   neighbor must EMIT. Type that class against V007's ports: does the responded-to
   influence have J/R structure (the l1 pair and the trace-class bilocal)? If C5's
   dependence, read as the emission requirement, TYPES as the ports -- the mutual-
   sourcing map is DERIVED up to normalization: d_J/d_R = the port-typed content of
   a neighbor's history that C5 says the write hears.
2  THE RECIPROCITY QUESTION: a system both hears (C5) and is heard (its history is a
   neighbor's external history). Derive the consistency condition: system A's emission
   = what system B's C5 hears, symmetrically -- the network's reciprocity law. If the
   ratified structure forces symmetric mutual sourcing (or reveals an orientation --
   E_post's arrow at network level), state it exactly.
3  THE TWO-SYSTEM TOWER, exact: the minimal network -- two record systems mutually
   sourcing under the derived map, computed exactly (the tower machinery with each
   system's derived emission feeding the other's tiers). THE NETWORK k-DEPENDENCE:
   does mutual sourcing generate genuine drive from the identity start (bootstrap:
   both start at ready; do the mutual feeds ignite?), and with WHAT FORM? *** If two
   mutually-listening record systems generate growing depth from nothing but each
   other, THE NETWORK IS THE DRIVE and the coupling's home is the pair interaction --
   compute its exact form, symbolic p and ranks. ***
4  KILL-PASSES: derivation cites C5's text only (the emission class must be READ, not
   designed); typing gaps typed not filled; no post-output supplementation (each
   emission feeds NEXT-tier inputs -- the tower's lawful pattern); no selection; no
   evaluation; door flags; lane-tagged filename.
DELIVERABLE: C5-as-map with the typing verdict, the reciprocity condition, the exact
two-system tower with its network k-dependence -- or the exact typing/derivation gap.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
