PASTE 227 — CODEX LANE 1 — THE OTHER SIDE: THE SURFACE/SPECTRUM TERM

ROAD: **ADVANCES STEP 2 / ROUTE A.** *** THE RADION COMPETITION NEEDS TWO TERMS WITH OPPOSITE
`R`-SCALING. THE FLUX SIDE IS BLOCKED BEHIND AN UNSETTLED QUESTION. THIS SIDE IS NOT. BUILD IT. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.

*** THIS RELAY IS SELF-CONTAINED. ASSUME NO MEMORY OF ANY PRIOR RELAY. ***

---

## 1. WHERE THIS SITS

A record cell has an **inside** (projective, dimensionless angles) and an **outside** (Lorentzian
causal diamond, lengths). **`beta` is the missing conversion, and it is the whole of what blocks the
geometric route to alpha.**

**Two prior results establish the shape of the problem:**

> *** WITH `phi = ln(R/ell_P)`, THE SEALED MAP IS EXACTLY THE SHIFT `phi -> phi + ln beta`. THE
> `beta`-DEGENERACY IS THE SHIFT SYMMETRY OF A **MASSLESS RADION**. *** — the joint saddle system
> poses and does not close (`STAGE8_JOINT_SADDLE_SYSTEM_POSING_EINSTEIN_V001.md`, `e5e0e43f…`).

```text
THE CANDIDATE POTENTIAL, ALREADY WRITTEN
  (STAGE8_RADION_POTENTIAL_MECHANISM_SCREEN_AND_CANDIDATE_V001.md, d9d1d4f5...)

  *** V(phi) = A_- exp(-p phi) + A_+ exp(q phi) + V_0 ***

  sealed_radion_lifting_mechanism_exists = false | TYPE-S   -- NOTHING SEALED LIFTS IT
  seat_occupancy_lifts_radion            = false | TYPE-R   -- executed refutation
```

*** THE STRUCTURAL RULE: A ONE-SIDED TERM IS MONOTONE OR `beta`-BLIND AND CANNOT CLOSE `beta`. A TERM
SCALING THE SAME WAY AS WHAT IS ALREADY PRESENT **SHIFTS** THE FLAT DIRECTION RATHER THAN LIFTING IT.
CLOSURE NEEDS **TWO TERMS OF OPPOSITE `R`-SCALING** AND AN **INTERIOR STATIONARY POINT.** ***

**Why this side and not the other.** The negative-power side would come from a fixed flux / charged
current — **and that side is blocked**: the corpus has a **fixed integer CHARACTER, not a flux**
(`:142-147`; corroborated at `…JOINT_SADDLE…:161-170`), and converting character to geometric flux
requires a **held-fixed rule under `R -> beta R` that is currently unstated at parent level.** *** THAT
QUESTION IS UNDER SEPARATE INVESTIGATION. DO NOT DUPLICATE IT. BUILD THE POSITIVE-POWER SIDE, WHICH
DOES NOT DEPEND ON IT. ***

---

## 2. THE TASK — BUILD THE `A_+ exp(q phi)` SIDE

**2.1 WHAT SURFACE OR SPECTRUM TERM SCALES AS A POSITIVE POWER OF `R`?** Candidates to test against
sealed text, **each with its `R`-scaling stated explicitly**:

```text
a surface/area term at the record cell boundary
the spectrum of an operator on the compact direction -- level spacing, a lowest eigenvalue,
  a counted mode number
a curvature term of the fiber itself
the derived unique counting metric (INTERNAL NORMALIZATION IS ALREADY FULLY PINNED --
  the whole conversion freedom sits at the JUNCTION, not inside)
thresholded durability (cycle 7 forces it for incident many-cell work)
the causal diamond's sealed volumes -- causal_diamond_shape_derived = true; THE SHAPE IS DERIVED
```

*** STATE THE `R`-SCALING OF EVERY CANDIDATE EXPLICITLY, EVEN WHEN IT IS ZERO. A `beta`-BLIND TERM IS
A REAL FINDING AND MUST BE REPORTED AS ONE RATHER THAN QUIETLY DROPPED. ***

**2.2 WRITE THE LEADING CANDIDATE SYMBOLICALLY.** Its form, its exponent `q`, what it is derived from,
and **whether it is sealed, adopted, or absent.**

**2.3 PAIR IT.** Given a `q`, state what `p` would have to be for `V(phi)` to have an **interior
stationary point**, and whether anything in the corpus could supply that `p`. *** A COMPETITION WITH
NO INTERIOR STATIONARY POINT LIFTS NOTHING — CHECK IT, DO NOT ASSUME IT. ***

---

## 3. THE BAR AND THE FAILURE MODE

```text
THE BAR (Q-65/Q-76, PRINCIPAL_AMENDMENT_002)
  a REQUIRE whose EXISTENCE AND UNIQUENESS ARE BOTH DERIVED.
  *** OVERDETERMINATION IS NOT REQUIRED AND IS NOT AN ADMISSION CONDITION. The live cross-sector spec
  STILL embeds the struck bar at :265-267 and :292 -- DO NOT APPLY F3 OR MINIMAL-PASS ITEM 3. ***

THE FAILURE MODE, ALREADY COMMITTED ONCE
  *** TOUCHING BOTH GEOMETRIES IS NOT ENOUGH -- THE CONDITION MUST CLOSE. ***
  boundary_access_closure_threshold_principle_v001.md touched both and was retired as
  CELL_CONSTRAINT_ONLY; the joint saddle system REPRODUCED that failure and said so.
  *** A TERM THAT LEAVES A ONE-PARAMETER FAMILY AFTER STATIONARITY HAS NOT CLOSED. ***

AND: a candidate that exists only under an ADOPTED premise MOVES the adoption -- it does not
discharge it. Say so explicitly wherever it happens.
```

---

## 4. SYMMETRIC LEADS

```text
IF A DERIVED POSITIVE-POWER TERM EXISTS -- *** LEAD WITH IT. Half a competition that is DERIVED rather
than adopted is the most progress the geometric route has had, and it would make the flux side the
single remaining obstacle. ***

IF EVERY CANDIDATE IS beta-BLIND OR ABSENT -- LEAD WITH THAT, with the R-scaling table. "Nothing on
the positive-power side scales with R, and here is why" is a complete answer under Q-92 and would
mean the competition cannot be built from present material at all.
```

*** DO NOT MANUFACTURE A TERM. A potential that appears to close `beta` but rests on an adopted
premise, or has no interior stationary point, would be scored as progress and inherited downstream.
ATTACK YOUR OWN CANDIDATE BEFORE REPORTING IT. ***

---

## 5. RULES

- **Q-54 typing on every negative:** TYPE-R refuted · TYPE-U unbuilt · TYPE-S scope-empty · TYPE-C
  constraint-blocked. `NO_VERDICT` is legal. **Only TYPE-R is physical content.**
- **Q-92:** specifying and naming what is missing is a complete answer, not a lesser one.
- **Q-69:** never identify a flag with the object that discharges it.
- **No unproved-identity transport.** A seal reaches what it names and no further.
- **Word-boundaried, CASE-INSENSITIVE on identifiers. Mark hypothetical flags inline.**
- *** SEARCH THE WHOLE PROGRAM. `alpha-program-archive/cleanroom_output/` HOLDS 87 FILES **OUTSIDE**
  THE `workspace/` SUBTREE (sidecars: `NN_NAME_V001.seal.sha256`, no `.md` before `.seal`). ***

## 6. FENCES — VERBATIM, NON-NEGOTIABLE

- **Never touch `a32_holdout/custodian_private/`.**
- **Do not compute** alpha, `kappa_record`, `kappa_Thomson`, a coupling, a scale, a root, an
  eigenvalue, a beta function, `E_R`, `T_R`, `k_R`, or any absolute interval. *** NO NUMERICAL VALUE
  FOR `beta`, `rho` OR `phi`. WRITE THE TERM; DO NOT MINIMIZE THE POTENTIAL NUMERICALLY. ***
- **No comparison to any measured constant.**
- **Do not resolve the Misner-Sharp / Brown-York fork by choosing — it must be DERIVED.** *** ANY
  QUASILOCAL-ENERGY-CONSUMING CANDIDATE IS A SILENT FORK CHOICE AND MUST BE DECLARED. ***
- **Report refutations; never repair them.**

## 7. CUSTODY (Q-91)

*** NO LANE RUNS ANY GIT COMMAND. *** Write the artifact in the cleanroom, compute its
`.seal.sha256` sidecar and **verify it matches**, mirror artifact + sidecar to
`alpha-program-archive/workspace/`, report hashes and paths, and **stop**. **A red gate is never a
reason to stop working.** The reviewer verifies, baselines, commits and pushes.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
