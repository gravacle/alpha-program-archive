PASTE 300 — CODEX LANE 2 (HIGH EFFORT) — [TASK 2d] ASSEMBLE `U2` AT PREMISE LEVEL — DYNAMICS, STATE, EFFECTS ON `C0_008`

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. TYPING PER DoR 006. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-216)

```text
DOES THE OBJECT EXIST?   SPECIFIED: U2 = microscopic action/evolution . positive normalized
                         pre-state . inclusive identity . admitted record effects . contacts .
                         domains, ON C0. Pieces exist SEPARATELY: rho_pre declared in v004
                         (positive trace-class, Tr = 1); the inclusive identity effect
                         E_r = C_r^dagger C_r; record effects typed in the P5 census.
IS THE VERSION CURRENT?  YES -- C0_008 and U1_008 exist; U2 is next in order.
ARE ITS INPUTS PRESENT?  PARTLY, AND THE GAPS ARE SEALED FACTS: the record term's INSTANCE is
                         TYPE-U (Q-210 -- the doubled CTP influence functional's common-origin
                         physical instance); the P5 census holds ROWS, NOT PACKAGES (Q-200).
```

## 1. THE BUILD — ASSEMBLE WHAT EXISTS, TYPE WHAT DOESN'T

```text
1  INSTANTIATE ON C0_008, with certificates, marked TYPE-P | premises: DoR-008:
   (a) rho_pre as declared -- the positive normalized pre-state, placed on the joint carrier;
   (b) the inclusive identity effect;
   (c) the admitted record effects THAT ARE INSTANCES (Q-200 rule: rows that are schemas are
       reported, not instantiated by fiat);
   (d) contacts and domains to the extent the ratified data licenses them.
2  THE DYNAMICS COMPONENT: *** THE HONEST CENTER. The evolution/action piece needs the influence-
   functional INSTANCE, which is TYPE-U (Q-210). ASSEMBLE THE SLOT AND ITS INTERFACE -- what the
   dynamics must supply and to what -- AND TYPE THE INSTANCE GAP. DO NOT FILL IT WITH A TRIAL
   FORM: both trial potentials are sealed-rejected for durability (Q-209). ***
3  d_U2^P premise-level, honestly labeled; what common-origin provenance still lacks: reported.
4  SCOPE: nothing from U3 (quotient, measure, boundary/edge, contour) pulled forward.
```

## 2. LEADS

```text
IF U2 ASSEMBLES MINUS THE DYNAMICS INSTANCE -- *** THE EXPECTED BEST CASE: lead with the instance
list, the dynamics INTERFACE fully specified, and the single named gap. The package would then be
blocked on exactly ONE physical object -- the influence-functional instance -- which is ALSO what
Tasks 3a and 3c converge on. ONE GAP, THREE CONSUMERS: name it as such. ***
IF rho_pre CANNOT BE PLACED ON THE JOINT CARRIER -- lead with why; that would be a defect in the
ratified join and bears on DoR 008.
IF AN EFFECT ROW TURNS OUT INSTANTIABLE THAT Q-200 COUNTED AS SCHEMA -- note it; the census
correction is worth having.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
