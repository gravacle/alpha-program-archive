## PASTE 615 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 6 / STEP 2] THE DoR V006: THE ADDENDUM AND THE LINE

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-550. THE C-V5 INSTRUMENT IS RATIFIED as standing — WITH the ordered enumeration executing anyway (the substitution regularized, per the register). The whitespace-normalization sweep protocol is law.
- The sweep: `alpha-program-archive/workspace/STAGE8_TASK6_DOR_V005_SWEEP_DARIO_V001.md`, SHA-256 `970e4f4d3a218ef99a6f102e04fab8eba5fd6ad321403a33a6b07387fc5a257d` — its two standing items are this relay's whole scope.
- Custody: two bounded edits; the pre-seal re-diff displayed per C-V5.

TASK — produce DoR V006:
G1. **The X1–X9 enumeration addendum:** the 612-ordered edits enumerated of record, each located in the body and stated (under the ratified instrument's finite-equality form — a named diff against V003 for exactly those regions).
G2. **The clause-8 line restored verbatim:** "no conversion silently set to one" back in the cross-sector-unit row, alongside the sealed provenance it gained.
G3. The pre-seal re-diff (V005→V006) displayed: exactly the two regions, nothing else; self verb audit.
Final lines: `DOR_V006 = DRAFTED (+addendum, +line restored, +re-diff clean)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_EVALUATION_DOR_LANE3_V006.md`
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal (protocol .seal.sha256 sidecar), mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
