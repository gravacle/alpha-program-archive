## PASTE 655 — CODEX LANE 2 (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / SCOPING] THE EVIDENCE-ENVELOPE FORMALIZATION MAP

(Same Lane 2 session rules: cleanroom writes; the registrar mirrors. Register head Q-593 — via the cleanroom snapshot mechanism if needed: verify `REGISTER_HEAD_SNAPSHOT_Q592_2026-08-07.md` and treat Q-593 as a lawful live-append recording your own 653 return.)

CONTEXT (Q-591): the machine-evidence layer has never been built — 0/56 envelopes exist. Before anyone decides whether Task 6 builds it or hands it forward, the principal needs it SIZED. SCOPING ONLY — no envelope is authored here.

TASK:
L1. **Classify all 56 structural checks + 3 fixture observations by envelope type**, from your check_map.json and the sealed spec: (a) SCHEMA-INSTANCE (the envelope is a canonical serialization of structure already displayed in sealed prose — e.g. carrier graphs, orderings, inventories); (b) ENUM-CERTIFICATE (a finite enumeration + completeness certificate whose content the sealed prose already exhibits); (c) KERNEL-PROOF (a replayable proof object that must be FORMALIZED from a sealed prose derivation — real formalization work); (d) MIXED (+which parts). Per check: the type, the sealed prose source(s) that would ground it (path + span), and an effort class (S/M/L) with one-line justification.
L2. **The totals and the shape:** counts by type and effort; the ten cheapest and the ten most expensive named; any check whose envelope would require NEW mathematics (not formalization of existing sealed derivations) flagged separately — that is a different kind of gap and must not hide inside an effort class.
L3. **Three delivery options sized for the principal:** (i) full formalization inside Task 6; (ii) a minimal-first tranche (the S-class envelopes) to light up part of the board and prove the pipeline, remainder to 7A; (iii) park formalization entirely at 7A. Costs in relays, honestly.
L4. Battery: F_PLDEC; M-2; PIN CHECK; VERDICT-LINE SCOPE.

OUTPUT: one sealed artifact
`STAGE8_TASK6_ENVELOPE_FORMALIZATION_MAP_LANE2_V001.md`
with final lines: `CLASSIFIED = 56+3 (+type counts)`, `EFFORT = S:n M:n L:n`, `NEW_MATH_FLAGS = none / (list)`, `OPTIONS = 3 sized`, `AUTHORED_ENVELOPES = zero`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal in the cleanroom, report hashes, STOP (the registrar mirrors). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
