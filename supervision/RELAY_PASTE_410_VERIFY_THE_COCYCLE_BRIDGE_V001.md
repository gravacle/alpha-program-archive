PASTE 410 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] ADVERSARIAL VERIFICATION OF THE REVIEWER'S COCYCLE-BRIDGE PROPOSAL — KILL IT IF YOU CAN (K1-K8 MANDATORY)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-327)

```text
DOES THE OBJECT EXIST?   A REVIEWER (BOHM) PROPOSAL exists and requires your adversarial
                         verification before ANY registration:
                         BOHM_DEPTH_DEDUP_AND_COCYCLE_BRIDGE_PROPOSAL_V001.md (mirrored
                         in archive workspace/; verify its seal). ITS CLAIMS: (I) the
                         four depth notions are pairwise distinct as sealed (dedup does
                         NOT collapse); (II) object-level accumulation is dead as a class
                         (the 6.1 square) and lawful accumulation lives on ARROWS; the
                         log of the sealed dephasing weight is an additive arrow-cocycle
                         with zero identity-cell increments; (III) THE BRIDGE = cocycle
                         holonomy: phi_div(c) = sum over c's cells of log w(cell) — a
                         scalar functional on K_cycle, restriction-compatible
                         automatically, from sealed data only, with p-content manifest.
IS THE VERSION CURRENT?  YES through Q-327. THE REVIEWER'S SCORECARD IS ~14/10 — kill
                         this if it can be killed; the proposal REQUESTS the fire and
                         lists its own kill-tests K1-K8 (execute ALL, add your own).
ARE ITS INPUTS PRESENT?  The proposal, the 409 determination (ed49624b — its Section 6
                         countermodels and Section 7 bridge-package checklist), the
                         sealed closed forms (c7624d88), the quotient apparatus (V005),
                         the involution law, Q-408's exact forms.
```

## 1. THE TASK — K1 THROUGH K8, THEN YOUR OWN ATTACKS, THEN THE VERDICT

```text
Execute the proposal's kill-list K1-K8 exactly as written, on sealed sources. Then
attack beyond it: the 6.1-square evasion claim (is cocycle holonomy REALLY exempt, or
does some restriction constraint reach arrows?); the overloaded-Q_N hazard (the
proposal's w(cell) must cite the RIGHT sealed object); hidden selection (the cycle's
cell-traversal — is it orientation/representative-dependent beyond the settled
quotients?). VERDICT: PROPOSAL SURVIVES (the bridge exists from sealed data — state
what re-running the divergence draft with phi_div constructible now yields, including
the Q-408 consequences and, if the chain closes, THE p-TRACE) / KILLED (the exact
attack) / WOUNDED (the repair, by-removal preferred).
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
