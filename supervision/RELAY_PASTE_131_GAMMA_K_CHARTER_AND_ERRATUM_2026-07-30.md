PASTE 131

Written 2026-07-30, for CODEX LANE 1. **A PASTE IS A SNAPSHOT.** Any ruling later than this paste
overrides it without notice. Your paste-130 return was verified: all five hashes match, and your O-SC1
step zero is the finding that produced item 2 below. Registering Q-20 unprompted when it landed before
seal was the right call.

**A SECOND CODEX LANE IS NOW RUNNING** on slot 18, via paste 132. Read item 4 before you commit anything.

Four items. Item 1 is a correction I owe YOU. Item 2 is a principal charter. **Item 3 is your work and it
is the program's only construction target.** Item 4 is the lane protocol.

Fences unchanged: read-only outside your own writes; compute no alpha, kappa, coupling, radius, scale,
root or eigenvalue; no comparison to any measured constant; file:line + verbatim quote for every claim;
bounded negatives state roots and exclusions; report refutations, never repair them.
`alpha_computed = false; proof_authorized = false` on everything you write. Never touch
`a32_holdout/custodian_private/`.

Collisions: `C_R` (compactness ratio vs induced Einstein coefficient), `K_H` (Maxwell stiffness vs
Hermitised Dirac), `Gamma_K` (coupling-indexed CTP functional vs BID cell-set amplitude — **this one is
on your target object**, namespace draft v004 has it).

---

### ITEM 1 — A CORRECTION I OWE YOU, AND A DETECTOR THAT SHOULD EXIST

Source: `MB Work/alpha_supervision/ERRATUM_002_BOUNDED_NEGATIVE_METHOD_DEFECT_2026-07-30.md`.

**I RETRACTED A BOUNDED NEGATIVE IN Q-18.** I reported "lower-endpoint files 12, `lambda_0` files 16,
BOTH ZERO — the two objects have never appeared in the same file." Correct figures: **16 / 19 / BOTH 7**,
and the seven include YOUR
`STAGE8_OPERATOR_FLOOR_BOUNDARY_CHAIN_CANDIDATE_DERIVATION_V001.md`, plus relay paste 127 and the 07-29
operator-chain test.

**GOOD NEWS AND IT IS TO YOUR CREDIT: you did not propagate the figures.** Your registration record
carries the type-mismatch argument, the axiom quote, the conditionality finding and F-FL1 — not the
counts. Verified by direct search: zero occurrences of the retracted figures anywhere in the cleanroom.
**No erratum is owed in the governing chain for this.** Register the retraction only so the record is
complete.

The ruling is unaffected — it rests on the opposite-ends mathematics, not the count. And the correction
makes it sharper: **the type mismatch sits inside an actively-cited chain that two lanes have worked,
not in unexamined ground.**

**THE CAUSE, AND IT IS A DETECTOR-CLASS DEFECT.** I piped a file list through `xargs` into a second
`grep`. Both program roots contain spaces — `/Users/bgm/Documents/New project/...` and
`/Users/bgm/MB Work/...`. `xargs` splits on whitespace, so every path fragmented, every inner grep
matched nothing, and **the pipeline reported zero while exiting successfully.**

*** IT FAILS SILENTLY AND IT FAILS TOWARD "ABSENT", WHICH IS THE DIRECTION THAT MANUFACTURES BOUNDED
NEGATIVES. *** Same class as substring certification and cannot-fail checks: a check that cannot fail
loudly.

**REQUESTED — a `corpus_check` detector:** flag any script or recorded command that pipes a path list
into a second search without null delimiting (`xargs` without `-0`, or `$(grep -l ...)` word splitting)
when the roots contain spaces. And the standing pattern for any lane computing an intersection:

```
grep -rlZ <opts> -e A ROOT1 ROOT2 | tr '\0' '\n' | sort -u > A.txt
grep -rlZ <opts> -e B ROOT1 ROOT2 | tr '\0' '\n' | sort -u > B.txt
comm -12 A.txt B.txt        # report the FILE LIST, not only the count
```

**AND REPORT LISTS, NOT COUNTS ALONE.** "BOTH: 0, list empty" next to a 12 and a 16 would have looked
wrong immediately. Counts hide what lists expose. I would like this to become a convention.

### ITEM 2 — REGISTER THE PRINCIPAL CHARTER: `Gamma_K` IS THE SOLE CONSTRUCTION TARGET

Authority: `MB Work/alpha_supervision/GAMMA_K_SOLE_CONSTRUCTION_TARGET_CHARTER_2026-07-30.md`
sha256 `42f287b01d2de208c56ea1f45f307fada482a0f20a5b1fb710e2d470ce93957f`. Register row Q-21.

**FORCED BY FOUR CONVERGENCES, AND ONE OF THEM IS YOURS.** F-scale directly (Q-14); **your O-SC1 step
zero returning `BLOCKED_BY_ORDERING` behind this exact object, unprompted**; slot 1's closure items 1-2
requiring "one target-independent complete source-record-gravity action"; and S9-A's derived
overdetermined determination of the total, which a `C_record` root would be.

**IT IS THE ROOT OF THE DEPENDENCY GRAPH WITH NOTHING UPSTREAM.** Slot 3 is blocked on the action-form
class, which IS this object; slots 4-8 sit behind slot 3; slot 6 behind both. The readiness record lists
its inputs as already available. Everything waits on it; it waits on nothing.

**IT IS EXPLICITLY NOT A PRIMARY-ROUTE DECLARATION, and register that distinction as load-bearing.**
R-30 declared primacy on 07-29 and F2 fired on 07-30. Declaring primacy on an unattempted construction
hours later would repeat the error with a different noun. A construction target says what is being built
and what waits on it; a primary route says which path is privileged and citable. This does the first and
refuses the second.

Frozen: F-GK1 (scoping returns HARD PROOF behind another object -> `Gamma_K` is not the root, the
charter's premise is false, re-derive the graph); F-GK2 (no simple positive root, or a continuous family
-> route fails, report don't repair); F-GK3 (a premise beyond the current stack must be declared AT THE
OUTSET, not found at the end); F-GK4 (mutation audit passing only on a pinned cellulation/skeleton does
not count — quantifier pinning, F-2 forbids it); F-GK5 (`K_*` solved before the mutation and uniqueness
gates pass -> void by the preregistration's own evaluation order, whatever the value).

### ITEM 3 — YOUR WORK: THE MANDATORY SCOPING STEP. CONSTRUCTION DOES NOT START UNTIL THIS LANDS.

**DELIVERABLE: a bounded inventory of what the corpus actually holds toward one complete
target-independent `Gamma_K` — the microscopic CTP functional AND its measure — plus the BR closure
operator whose joint stationary problem outputs `Delta_tau(K)` and a scalar `C_record(K)`.**

State roots and exclusions. Use the null-safe pattern from item 1. Report file lists.

Known starting inventory, from `results/primitive_record_cell_joint_selector_readiness_v001.json`
`derived_before_joint_construction` — verify each rather than trusting the list: continuous compact U(1)
comparison connection; primitive unit character; post-C4 unit-character charged Dirac source; transverse
current-response tensor and subtracted running shape; CTP response/noise split and first orthogonal
action-phase record map; antiperiodic fermionic domain on the ordinary record-trace closure cycle;
causal-diamond domain, cutwise unit linking flux, and fixed-charge boundary ensemble.

Known missing, same source `not_supplied_by_the_word_unique`: the complete `Gamma_K` functional and
measure; the stationary Lorentz-invariant proper interval; the BR closure spectrum; the scalar residual
`C_record(K)`; the unique simple positive root.

**RETURN ONE OF THREE VERDICTS, ALL ACCEPTABLE, ALL INFORMATIVE:**
- **MISSING SPECIFICATION** — the pieces exist and nobody assembled them. State the assembly path.
  Four obligations in two days turned out to be this.
- **HARD PROOF** — a named mathematical obstruction at file:line. **If that obstruction is itself blocked
  behind another object, F-GK1 FIRES and you must say so** — that would mean `Gamma_K` is not the root.
- **DERIVABLE NOW** — with the path stated.

**DO NOT** construct, solve for `K_*`, evaluate any response, or run the mutation audit (it has no object
until `C_record(K)` exists). **DO NOT** reuse `L_open` in any renamed form. **DO NOT** work slot 18 —
lane 2 has it.

WORTH CHECKING WHILE YOU ARE IN THE MEASURE ARTIFACTS, but do not let it displace item 3: F-FL1 asks
whether any condition fixes the SMALL-`s` end of the proper-time integral. A bottom-of-spectrum condition
provably cannot; a statement about the operator DOMAIN, the MEASURE, or the ADMISSIBLE MODE CONTENT
might. **I am working that question myself** — if you happen to cross a candidate, name it and move on
rather than pursuing it.

### ITEM 4 — LANE PROTOCOL, BECAUSE THERE ARE NOW THREE WRITERS ON ONE WORKING TREE

Your commit `996591e` absorbed eight of my supervision files into your commit. No harm done this time,
but with a second construction lane it becomes a real hazard — two lanes can swallow each other's staged
work, and a concurrent re-baseline has already nearly frozen a double-count in this program once.

**THREE RULES, effective now:**
1. **Commit ONLY files you wrote, by explicit path.** No `git add -A`, no `git add <directory>`. Report
   the exact paths you committed.
2. **`STAGE8_LANE_STATUS.md` is shared.** Re-read it immediately before appending, append in the same
   step, and if it changed since your last read, re-read and re-append. Never rewrite the table.
3. **Your write scope is `Gamma_K` / joint-selector / O-SC1 / namespace / erratum artifacts.** Slot 18
   and A32 artifacts belong to lane 2. If you need something in lane 2's scope, say so and stop.

---

REPORT BACK: hashes for every artifact written, the exact committed paths, `corpus_check` gate verdict,
deploy state, and for item 3 the verdict type with file:line evidence. **If F-GK1 fires, lead with
that** — it is the most consequential thing you could return.

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
