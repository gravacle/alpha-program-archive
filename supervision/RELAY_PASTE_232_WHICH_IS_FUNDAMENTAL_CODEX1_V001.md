PASTE 232 — CODEX LANE 1 — WHICH IS FUNDAMENTAL: `kappa_5` OR `ell_P`?

ROAD: **ADVANCES STEP 2.** *** THE GEOMETRIC ROUTE'S SINGLE BOTTLENECK HAS RESOLVED INTO ONE BINARY,
AND THE SEALED `beta^2` — THE STATEMENT THAT DIAGNOSED `beta` AS THE WHOLE GAP — DEPENDS ON WHICH WAY
IT GOES. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.

*** THIS RELAY IS SELF-CONTAINED. ASSUME NO MEMORY OF ANY PRIOR RELAY. ***

---

## 1. WHAT WAS JUST BUILT, AND THE FORK IT EXPOSED

The geometric route computes `alpha` as a ratio of `ell_P` to a bundle fiber radius on

```text
ds_5^2 = g_mu-nu dx^mu dx^nu + R^2 (d-theta + A_mu dx^mu)^2
```

**Until today this corpus had NO name for its 5d gravitational normalization and `ell_P` was NEVER
DEFINED IN ANY ROOT.** A parent construction has now supplied both
(`STAGE8_PARENT_KK_ACTION_ELL_P_DEFINITION_AND_REDUCTION_V001.md`, `01c4c0db…`):

```text
PARENT ACTION    S_5 = (1/2 kappa_5^2) integral sqrt(-G) R_5
NORMALIZATION    kappa_5^2, the inverse 5D Einstein-Hilbert coefficient
DEFINITION       ell_P^2 := kappa_5^2 / (2 pi R_0)
REDUCTION        *** THE FIBER FACTOR 2 pi R_0 ENTERS THE 4D EINSTEIN-HILBERT COEFFICIENT.
                 IT DOES NOT DISAPPEAR. ***  (derived without circularity: the definition
                 precedes the scaling test, :70)
```

*** AND THE FORK IS NOW EXPLICIT (`:66`): THE 4D COEFFICIENT **SCALES WITH THE FIBER VOLUME IF
`kappa_5` IS HELD FIXED**, AND IS **CONSTANT ONLY IF THE PARENT NORMALIZATION SCALES.** YOU CANNOT
HAVE BOTH. *** `ell_P_held_fixed_under_R_scaling = NO_VERDICT | TYPE-U`, would-build: **an independent
parent-normalization/frame law.**

**Why this decides more than it looks like it does.** A separate determination established that the
sealed conclusion *"changing `K_KK` by `beta^2`"* **holds ONLY IF `ell_P` is held fixed across the map**
(`STAGE8_HELD_FIXED_AND_FRAME_DETERMINATION_EINSTEIN_V001.md`, `5199a3db…`). *** `beta^2` IS THE
STATEMENT THAT DIAGNOSED `beta` AS THE ENTIRE BLOCKER ON THIS ROUTE. IF `ell_P` IS **NOT** FIXED, THAT
DIAGNOSIS RESTS ON THE OTHER BRANCH AND MUST BE RE-EXAMINED. ***

---

## 2. THE TASK

**2.1 WHAT DOES THE CORPUS'S OWN STRUCTURE REQUIRE?** *** THIS IS THE PRIMARY QUESTION AND IT IS
ANSWERABLE FROM SEALED TEXT. *** Does anything in the corpus — the record-cell construction, the
bundle, the fiber, the way `R` enters any sealed result — **force** one of `kappa_5`, `ell_P` to be the
fundamental datum? Quote paths and lines. **If nothing forces it, say so plainly.**

**2.2 STATE THE STANDARD READING AND WHETHER IT APPLIES.** *** IN TEXTBOOK KALUZA-KLEIN, `kappa_5` IS
THE FUNDAMENTAL PARAMETER AND THE 4D GRAVITATIONAL CONSTANT IS **DERIVED** FROM IT PLUS THE
COMPACTIFICATION VOLUME. *** Establish whether that reading applies **here** — this fiber is not a
generic extra dimension, it is the record cell's internal direction. **Does that change which is
fundamental? Argue it; do not assert it either way.**

**2.3 PRICE BOTH BRANCHES.**

```text
BRANCH kappa_5 FIXED   ell_P is NOT constant under R -> beta R.
                       *** THEN THE SEALED beta^2 CONCLUSION DOES NOT HOLD AS WRITTEN. State
                       exactly what it becomes, and what else downstream inherits the change. ***
BRANCH ell_P FIXED     the parent normalization must scale with the fiber extent.
                       State what would have to supply that scaling, and whether anything does.
```

**2.4 IS THE FORK DERIVABLE, OR IS IT AN AXIOM?** *** Q-82: PATHLESSNESS IS DIAGNOSTIC — A NODE WITH NO
DISCHARGE ROUTE IS TERMINAL, AND TERMINAL IS WHAT AN AXIOM LOOKS LIKE. *** If nothing derives it, say
so and state the cost of adopting each side. **That is a complete result.**

---

## 3. THE TRAP

*** DO NOT PICK THE BRANCH THAT PRESERVES `beta^2`. *** The `beta^2` conclusion is a **downstream
consequence**, not evidence about which parameter is fundamental. **Choosing the branch that keeps a
prior result intact is fitting — the same failure the program's whole anti-fit apparatus exists to
prevent, applied one level deeper than usual.** *** IF THE EVIDENCE POINTS AT THE BRANCH THAT BREAKS
`beta^2`, REPORT THAT. IT WOULD BE THE MOST VALUABLE THING THIS RELAY COULD RETURN. ***

**Settled, do not re-litigate:** `beta` **is frame-invariant** — `R` is a proper radius in the fiber
block and `c Delta tau` a proper interval in the base block **of the same metric**, so `beta` is a
dimensionless invariant of one five-geometry. **No frame declaration is required.** The frame ambiguity
is real only once `R` is dynamical.

---

## 4. SYMMETRIC LEADS

```text
IF SEALED STRUCTURE FORCES ONE BRANCH -- *** LEAD WITH IT. It would settle the held-fixed rule, which
sits beneath beta^2, both sides of the radion potential, and every weight downstream. ***

IF THE EVIDENCE FAVOURS kappa_5 FIXED AND THEREFORE BREAKS beta^2 -- *** LEAD WITH THAT ABOVE
EVERYTHING. A diagnosis resting on the wrong branch is worth knowing before more is built on it. ***

IF NOTHING DERIVES IT -- lead with that, priced. An honest "this is an axiom and here is what each
side costs" is complete under Q-92 and better than a preference dressed as a derivation.
```

---

## 5. RULES

- **Q-54 typing on every negative:** TYPE-R refuted · TYPE-U unbuilt · TYPE-S scope-empty · TYPE-C
  constraint-blocked. `NO_VERDICT` is legal. **Only TYPE-R is physical content.**
- **Q-92:** specifying and naming what is missing is a complete answer, not a lesser one.
- **THE BAR (Q-65/Q-76, `PRINCIPAL_AMENDMENT_002`):** a REQUIRE whose **existence and uniqueness are
  both derived**. **Overdetermination is NOT required.** *** THE LIVE CROSS-SECTOR SPEC STILL EMBEDS
  THE STRUCK BAR AT `:265-267` AND `:292` — DO NOT APPLY F3 OR MINIMAL-PASS ITEM 3. ***
- **Q-69:** never identify a flag with the object that discharges it.
- **No unproved-identity transport.** A seal reaches what it names and no further.
- **Word-boundaried, CASE-INSENSITIVE on identifiers. Mark hypothetical flags inline.**
- *** CHECK SCOPE BEFORE ASSERTING A NEGATIVE. A recent "zero hits corpus-wide" had been taken over
  two files. `alpha-program-archive/cleanroom_output/` holds 87 files OUTSIDE the `workspace/`
  subtree; sidecars are `NN_NAME_V001.seal.sha256`, no `.md` before `.seal`. ***

## 6. FENCES — VERBATIM, NON-NEGOTIABLE

- **Never touch `a32_holdout/custodian_private/`.**
- **Do not compute** alpha, `kappa_record`, `kappa_Thomson`, a coupling, a scale, a root, an
  eigenvalue, a beta function, `E_R`, `T_R`, `k_R`, or any absolute interval. *** NO NUMERICAL VALUE
  FOR `kappa_5`, `ell_P`, `beta`, `rho` OR `phi`. STRUCTURAL AND SYMBOLIC ONLY. ***
- **No comparison to any measured constant.**
- **Do not resolve the Misner-Sharp / Brown-York fork by choosing — it must be DERIVED.**
- **Report refutations; never repair them.**

## 7. CUSTODY (Q-91)

*** NO LANE RUNS ANY GIT COMMAND. *** Write the artifact in the cleanroom, compute its
`.seal.sha256` sidecar and **verify it matches**, mirror artifact + sidecar to
`alpha-program-archive/workspace/`, report hashes and paths, and **stop**. **A red gate is never a
reason to stop working.** The reviewer verifies, baselines, commits and pushes.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
