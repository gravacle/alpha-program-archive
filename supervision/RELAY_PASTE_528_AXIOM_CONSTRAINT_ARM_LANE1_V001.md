## PASTE 528 — CODEX LANE 1 (HIGH EFFORT) — [TASK 5 / EQ6] THE AXIOM'S CONSTRAINT SYSTEM: WHAT ANY COMPLETED-EXISTENCE PRINCIPLE MUST AND MUST NOT DO

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-452. Codex 2 authors the completed-existence axiom in parallel (527), blind to you. Your arm derives the CONSTRAINTS any such axiom must satisfy — the adjudication standard, pre-derived (the 490 pattern).
- Authorities: DoR-020 + A1; DoR-008; the nine regressions and every excluded counterexample (the disconnected domain, the sector mixer, the abstract kernels, the P=id witness's ADMISSION requirement — note it must be admitted, not excluded); the built finite layers; the twelve-step map `1069e4f4…`; Q-410–Q-452.
- Derivation only; all symbolic.

TASK:
S1. **The MUST list:** every property the ratified stack forces on a completed-existence axiom — DoR-008 conservativity over finite shadows; the joint (not stagewise) form (the equalizer regression); compatibility with each adopted clause; the C_ret separation (the axiom must not absorb C_ret — Q-448); admission of the P=id witness; covariance/no-selection at axiom scale.
S2. **The MUST-NOT list:** what the axiom cannot do without voiding — inhabit the excluded counterexamples (each, with the exclusion condition derived); prove finite content not already proven (non-conservativity = kill); collapse the member fibers (the Z/N, reading-rule, and χ_K freedoms must survive — the axiom supplies existence, never selection).
S3. **The independence check:** verify the axiom-shaped principle is genuinely independent of the ratified stack (not derivable — the 15 rounds as the evidence base, consolidated into one non-derivability statement) AND not refutable by it (consistency — the direct-sum and flip models as the finite evidence).
S4. **The consumption map:** which of the twelve steps an adopted axiom discharges, exactly; what remains after it (C_ret, the member fibers, A32, the evaluation).
S5. Falsifiers; the constraint system as the adjudication standard for 527's draft.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_EQ6_AXIOM_CONSTRAINT_ARM_LANE1_V001.md`
with a lead-result block (`MUST = <n items>`; `MUST_NOT = <m items>`; `INDEPENDENCE = <status>`; `CONSUMPTION = <steps>`). Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
