PASTE 167 — CODEX 1 — OVERNIGHT QUEUE: THE BRIDGE CRITICAL PATH

2026-07-30. Snapshot; later rulings override. **REGISTER HEAD AT ISSUE: Q-64.**

*** THE PRINCIPAL IS ASLEEP. THIS IS A QUEUE, NOT A SINGLE ITEM. WORK IT IN ORDER AND DO NOT STOP FOR
INSTRUCTIONS. ***

```text
CODEX 1 (you) = the bridge critical path, items 0-4      [relay 167]
CODEX 2       = the CTP_PHYS_INPUT_PACKAGE, resumed      [relay 168]
EINSTEIN      = forcing boundary, then registry, then graph completion   [relay 166]
```

### STANDING OVERNIGHT RULES

1. **Seal each item's artifact before starting the next.** One artifact per item. **See the write protocol below — no commits tonight.**
2. *** IF AN ITEM IS BLOCKED, MARK IT `NO_VERDICT` WITH THE BLOCKER TYPED, AND MOVE TO THE NEXT. DO NOT
   STALL WAITING FOR A RULING. *** No ruling is coming until morning.
3. **Gate and deploy state are not your concern tonight.** Leave every artifact sealed on disk.
4. **Never merge an alias without sealed evidence.** Word-boundaried matching only.
5. **Report refutations; never repair them.** **Q-54:** every negative typed.
6. **Do not open Codex 2's or Einstein's items.** Q-64 proved these chains share zero nodes.

Fences: never touch `a32_holdout/custodian_private/`. **Do not compute** alpha, kappa_record,
kappa_Thomson, a coupling, a radius, a scale, a root, an eigenvalue, a beta function, `E_R`, `T_R`, or any
absolute interval. **No comparison to any measured constant.** **Q-52:** you may specify a missing object
to test it — declared, `derived = false`, never reported as derived.


### WRITE PROTOCOL TONIGHT — SEAL, DO NOT COMMIT

*** THREE LANES SHARE ONE GIT WORKING TREE AND NOBODY IS AWAKE TO ARBITRATE A COLLISION. TONIGHT YOU DO
NOT COMMIT. *** Today that shared index blocked three commits and one lane nearly swept another's
in-flight work into its own.

```text
DO      write your artifact
DO      compute its .seal.sha256 sidecar and VERIFY it matches
DO      leave both on disk and move to your next item
DO NOT  git add / git commit / git push / git pull / git reset / git stash / git restore
DO NOT  touch corpus_check_baseline_v001.json
DO NOT  run deploy_status.sh or treat any deploy state as your problem
```

**You MAY run `corpus_check.py --report` on your own artifact for its own sake. `--gate` is not your
concern tonight and a RED gate is NOT a reason to stop working.**

- **Nothing is lost.** A sealed artifact with a verified sidecar is fully recoverable. **The custodian
  commits everything in one reviewed pass in the morning.**
- **Other lanes' files will appear in the tree. Leave them alone entirely.**
- *** IF AN ITEM IS BLOCKED, MARK IT `NO_VERDICT` WITH THE BLOCKER TYPED AND MOVE TO THE NEXT. DO NOT STALL
  WAITING FOR A RULING — none is coming until morning. ***
- **One artifact per item**, named for the item, so the morning pass can attribute each cleanly.

---

### WHY THIS LANE, WHY NOW

Q-64's census found **no cycle** and mapped **two critical paths sharing ZERO nodes.** Codex 2 holds one
head. *** THE BRIDGE IS THE OTHER, IT IS UNSTAFFED, AND NOTHING ON STATED TEXT FORCES IT TO WAIT. ***

The bridge is what lets `C_R = 1` actually select — Q-59 found it the sole absolute scale-orbit breaker but
only a **conditional** selector, with `absolute_record_interval_derived_in_declared_branch = false`. **Six
objects stand between it and a selected scale:**

```text
1  complete Lorentzian CTP action / boundary / time-flow data
2  constant Hamilton-Jacobi record energy on the stationary cell
3  proof that branch-energy difference equals gravitating closure energy
4  reference-subtraction / no-spectator theorem
5  derived marginal first durable-public closure
6  one isolated stable positive interval solution
```

---

## ITEM 0 — THE ALIAS PRECONDITION

Q-64 carried four alias candidates unmerged. **One sits in your chain:**
`C_R_marginal_selector_derived_rather_than_adopted` versus
`marginal_closure_condition_derived_rather_than_assumed` — **same file, both TYPE-C, no sealed equation.**
One of three literally-required pathless identifiers is a child of `C_R_equals_1_truth_status`'s
`blocked_by`.

*** IF THE ALIAS RESOLVES, THAT NODE HAS A RELEASE. IF NOT, YOUR CHAIN HAS A GENUINE HOLE RATHER THAN A
NAMING ARTEFACT. *** **Adjudicate from sealed text. If text cannot settle it, say so — that escalates to
the principal and is a legitimate answer, not a failure.**

## ITEM 1 — TRIAGE THE SIX

For each: exists / partially exists / absent. **At file:line where it exists; TYPE-S with stated scope
where not.** *** CORRESPONDENCE CHECK BEFORE SPECIFYING ANYTHING, WORD-BOUNDARIED. Einstein's census
reported 43 false shared nodes from a regex letting `C_R` substring-match `C_record`. Substring matching
has now produced false positives in the corpus, in an audit script, and in the audit of the audit. ***
**Then rank: which must exist before the others can be specified.**

## ITEM 2 — SPECIFY THE LOAD-BEARING ONE

Under Q-52: declared, `derived = false`, premises stated at the outset, **stopping before any object it in
turn requires.** **Attach failure-capable tests.** *** CHECK THE WIRING — Codex 2 caught a defect today
where a test reported into a producer verdict while actually testing an external interface. A test wired to
the wrong verdict is the cannot-fail failure mode in a new costume. Verify each of yours reports into the
verdict it tests. ***

## ITEM 3 — SPECIFY THE SECOND LOAD-BEARING ONE

Same discipline. **If item 2 revealed that the ranking was wrong, say so and re-rank rather than following
a ranking you no longer believe.**

## ITEM 4 — DEPTH REPORT

For the six: **how deep does each chain run below it, and does any terminate in something derived?** Q-64
found the graph's **entire boundary unbuilt** — *** IF YOU FIND ANY CHAIN REACHING DERIVED GROUND, LEAD
WITH IT. That would be the first floor the program has found. ***

---

### CONSTRAINTS THAT DO NOT RELAX OVERNIGHT

- *** DO NOT RESOLVE THE `sqrt(2)` BY CHOOSING. *** Misner-Sharp versus reference-subtracted Brown-York is
  a real physical fork, currently TYPE-U. **A pick makes it a second scale-bearing adoption and moves the
  program from conditional derivation toward fit (Q-58, Q-59). It must be DERIVED, never chosen.**
- **Do not derive the absolute interval.** Specify; do not build the scale.
- **Do not merge aliases to make the graph tidier.**

---

REPORT BACK, with **CODEX 1** on your first line: per item — hashes, exact committed paths, gate verdict,
answers, **every negative typed.** **Lead the whole report with whichever of these fired: an object already
existing under another name; a chain reaching derived ground; or the alias being unsettleable from sealed
text.**

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`
