PASTE 412 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a] THE FINITE DEPTH PROGRAM, COMPUTATION ONE — THE REFINEMENT TOWER, EXACT (C43)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-329 + C43)

```text
DOES THE OBJECT EXIST?   NO -- and its ABSENCE is the proven cause of three bridge
                         failures (Q-329's composed fact: the sealed finite layer is
                         depth-poor; one-cell + invariant extension contains no depth).
                         C43 commissions its CREATION: the first exact multi-cell
                         REFINEMENT TOWER computation -- depth-bearing finite data,
                         F_N-style, sealed under the falsifier.
IS THE VERSION CURRENT?  YES through Q-329 + C43.
ARE ITS INPUTS PRESENT?  YES: the O7 TIER WITNESS (the tensor-power record-tier
                         structure -- the corpus's one sealed depth-adjacent object,
                         never transferred; locate it and READ its own transfer-failure
                         notes so this computation does not repeat that error), the
                         ratified law (DoR-009 -- the write dynamics that REFINEMENT
                         iterates), F_N/A_N and the one-cell sealed structure (the
                         tower's ground floor), the germ V007 (the source structure per
                         tier), the sealed closed forms (Q-279).
```

## 1. THE COMPUTATION — REFINEMENT, NOT EXTENSION

```text
1  DEFINE the tower precisely, and distinguish it from the DEAD direction: sequential
   zero-extension APPENDS IDENTITY cells (proven depth-free). REFINEMENT is different:
   each tier RE-RECORDS the same fact -- the record of a record: tier k+1's cells
   register tier k's completed record (the O7 tensor-power structure). Construct the
   tower from the RATIFIED LAW's own write dynamics applied to its own output:
   rho_ready -> record -> (the record AS the next tier's source) -> record-of-record
   -> ... k tiers. EVERYTHING EXACT: the tier-k state, the tier-k weights, the tier-k
   amplitude, in closed form as functions of (k, p, the probe data).
2  THE DEPTH QUESTION, computed not assumed: do the tier-k closed forms GROW with k?
   The candidates to extract exactly: the tier-k branch distinguishability (does
   re-recording deepen the commitment where extension could not?); the tier-k dephasing
   exponent; the tier-k amplitude's log-structure. IF a k-growing exact form appears,
   THE PROGRAM HAS ITS FIRST DEPTH-BEARING SEALED DATUM -- state its exact k-dependence
   (logarithmic? linear? the FORM is the physics -- the principal's log-divergence
   thread predicts LOG).
3  THE FALSIFIER DISCIPLINE: tier-1 must reproduce the sealed one-cell structure
   EXACTLY; the tower's restriction behavior stated (what maps tiers to tiers; the
   analogue of zero-extension for tiers and what is invariant under it); reality,
   quotient, rank-symbolic discipline throughout.
4  THE O7 TRANSFER, done right: consume the O7 witness's structure WITH its own
   transfer-failure notes respected -- what O7 said could not transfer (the response
   pullback) is NOT claimed here; this computation creates FINITE data only.
5  KILL-PASSES: no continuum objects; no bridge claims (the bridge comes AFTER the
   data exists); no target-tuning (the k-dependence is COMPUTED, never chosen); no
   evaluation; door flags; six-account rows for any new operation class.
DELIVERABLE: the refinement tower exact in closed form; the k-dependence of every
extracted quantity; the tier-restriction structure; the first depth-bearing sealed
datum if it exists -- or the exact proof that re-recording also fails to deepen,
which would itself be a profound finite theorem.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
