PASTE 323 — CODEX LANE 1 (HIGH EFFORT) — [TASK 2f] THE COMPARISON MAPS: DOES THE FALSIFIER'S K_SQUARE ARM HAVE CHOICE-INVARIANT CONTENT?

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-239)

```text
DOES THE OBJECT EXIST?   THE OBLIGATION does (DoR-008 falsifier: the completion must reproduce
                         EVERY sealed finite result on restriction, or the adoption VOIDS).
                         The one-cell arm PASSES. The K_square arm needs a comparison map from
                         the ratified sequential labels (Lambda_N = Z^N, disclosed zero-
                         extension) to the sealed K_square incidence presentation -- and the
                         realization functor is UNFORCED at 1,088 filtrations (Q-217).
IS THE VERSION CURRENT?  YES -- Q-239 adds the exact finite skeleton (Q_N = U(1)^N; F_N and
                         A_N^(p) descend to relative phases r_j).
ARE ITS INPUTS PRESENT?  The sequential side fully; the incidence side is sealed corpus data.
```

## 1. THE TASK — DO NOT RE-RUN THE FORCING; TEST WHAT THE FALSIFIER ACTUALLY NEEDS

```text
1  STATE the falsifier's K_square restriction test as a bare requirement: WHICH sealed
   K_square results must the completion reproduce, and WHAT must a comparison map preserve
   for that test to even be posed? (List the sealed results with paths/hashes.)
2  *** THE CENTRAL QUESTION: DOES THE RESTRICTION TEST FACTOR THROUGH THE CHOICE? For each
   sealed K_square result in (1), determine whether its comparison under ANY admissible
   filtration gives the SAME verdict -- i.e., is the falsifier's verdict CHOICE-INVARIANT
   across the 1,088? If yes, the arm runs WITHOUT forcing the functor, and Q-217's blockage
   is moot for the falsifier. If no, name the smallest sealed result whose verdict differs
   under two named filtrations -- that is the sharpest possible statement of what remains. ***
3  RUN the invariant subset concretely where finite: gauge-fixed content descends to relative
   phases (Q-239) -- check whether the sealed K_square results in (1) are themselves
   relative-phase (orbit-invariant) statements; orbit-invariant sealed content compared
   through orbit-invariant maps is the natural choice-free candidate.
4  TYPE every gap honestly. Four forcing attempts failed; a fifth is not authorized here.
```

## 2. LEADS

```text
IF CHOICE-INVARIANT -- *** LEAD WITH IT: the K_square arm executes, the falsifier gets its
second arm, and Task 2f closes WITHOUT the functor. Then RUN the arm on the strongest sealed
result and report PASS/FAIL. A FAIL VOIDS DoR-008 AND MUST BE REPORTED AS LOUDLY AS A PASS. ***
IF CHOICE-DEPENDENT -- lead with the minimal differing pair; that converts "functor unforced"
into a named, finite disagreement.
IF THE SEALED K_SQUARE RESULTS AREN'T ORBIT-INVARIANT -- say which, and what that means for
the arm.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
