PASTE 387 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] AUDIT DOOR D's OWN TRANSPORT — JACOBIANS, CTP METRIC, AND EVERY i/hbar, THROUGH THE GERM'S SOURCE MAPS (PRINCIPAL'S CHECK)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-303)

```text
DOES THE OBJECT EXIST?   THE QUESTION does, and it is THE PRINCIPAL'S: both Door-D firings
                         (Q-300 the identity, Q-302 the U1 sign) were BOOKKEEPING-DEPTH
                         defects. The decisive unchecked possibility: DOOR D's TEST ITSELF
                         is computed by transporting objects through the germ's linear and
                         bilocal source maps -- and that transport carries JACOBIANS, the
                         CTP METRIC contraction, and FACTORS OF i/hbar. If any of those
                         is wrong IN THE TEST'S OWN PLUMBING, the door can fire falsely
                         or mask a real defect. AUDIT THE TEST, not the tested.
IS THE VERSION CURRENT?  YES through Q-303. The OTHER lane is re-running Door D on
                         Amendment 2 (relay 386) -- DO NOT COORDINATE; your audit is of
                         the test's specification and transport as executed in the PRIOR
                         runs (bffd7ef2..., 365db191...), so the two results can be
                         compared independently when both land.
ARE ITS INPUTS PRESENT?  YES: both Door-D execution artifacts, the germ's source-map
                         definitions (V004/V005-attempt: the linear L and bilocal ports,
                         their Jacobian structure), the sealed CTP metric/contraction
                         (the Keldysh rotation texts, Q-243), the W-convention chain
                         (Gamma = -Log Z; W = -i hbar Log Z; the Q-276 sign), the parity
                         table (Q-303).
```

## 1. THE AUDIT — THE TEST'S FULL TRANSPORT, FACTOR BY FACTOR

```text
1  WRITE OUT Door D's test as a TRANSPORT DIAGRAM: every object it compares (D_J A-bar
   on one side, (i/hbar) G on the other; the U1-reality transform), from its DEFINITION
   site to its COMPARISON site, naming every map crossed: the germ's linear source map
   (its Jacobian), the bilocal source map (its Jacobian), the CTP doubling/metric
   contraction (which index pairing, with which signs), the quotient maps, the
   restriction/adjoint structure (the W3 precision), and EVERY factor of i and hbar
   with its provenance (W-convention? identity? involution?).
2  VERIFY each crossing against the sealed conventions: (a) are the Jacobians of the
   source maps applied (and applied ONCE) where the test changes variables? (b) is the
   CTP metric contraction the sealed one (the Keldysh [[1,-1],[-1,1]] structure and its
   rotated form) -- no silent index raising or branch-sign slip? (c) is every i/hbar
   accounted with one owner each -- no double-supply between the identity's i and the
   W-convention's i (the Q-302 lesson generalized to the TEST side)?
3  RE-DERIVE the two prior firings through the audited transport: does the Q-300
   identity failure and the Q-302 sign failure SURVIVE the corrected accounting, or
   was either an artifact of a transport defect in the test itself? (The diagnoses
   localized them to C-B and the parity -- but both diagnoses TRUSTED the test's
   transport; this audit is the missing leg.)
4  THE VERDICT: TEST-CLEAN (Door D's transport is correct as specified -- state the
   verified diagram; the firings stand as physical) / TEST-DEFECT (name the crossing,
   the factor, and which firing(s) it revises -- the principal must see this before
   anything else) / MIXED.
5  KILL-PASSES: audit only (no repair, no re-specification); the diagram complete (an
   unlisted crossing is a defect of the audit); sealed citations for every convention;
   no coordination with the 386 run.
DELIVERABLE: the transport diagram with per-crossing verification, the re-derivation of
both firings, the TEST-CLEAN / TEST-DEFECT verdict.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
