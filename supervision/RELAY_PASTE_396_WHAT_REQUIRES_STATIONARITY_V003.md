PASTE 396 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] WHAT ACTUALLY REQUIRES A STATIONARY BACKGROUND? — THE Q-270 AUDIT ON THE LAST WALL (PRINCIPAL'S CHECK) [V003 — ADDS THE SYNONYM AUDIT; SUPERSEDES V001/V002]

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-313)

```text
DOES THE OBJECT EXIST?   THE QUESTION does, and it is THE PRINCIPAL'S: the last four
                         unbuilt objects all hang off the word STATIONARY -- and the
                         ledger holds evidence the stationarity requirement is an IMPORT:
                         Q-252 proved NO finite stationary point exists for any p in
                         (0,1); the evaluation point is UNTYPED by sealed text; C1 is a
                         reduction certificate, not an evaluation rule; no sealed CTP
                         text selects a stationary history; the dynamics is PURE PHASE
                         (no damping -- no stationary point is the PHYSICS). Before the
                         frontal build: WHAT SEALED REQUIRE DEMANDS STATIONARY
                         EVALUATION AT ALL?
IS THE VERSION CURRENT?  YES through Q-313. This supersedes the 396 V001 build relay --
                         AUDIT FIRST (the Q-270 precedent: the dependency audit that
                         converted four failed builds into a moot road).
ARE ITS INPUTS PRESENT?  YES: the sealed defining texts of every alpha-facing output
                         (B_ind and p_loc's v004 lineage, kappa_Thomson's defining ratio,
                         Delta_Phi, the response quotients), Q-252's determination
                         (READ ITS EVIDENCE TABLES), the built reference-point structure
                         (C1's zero-source reduction; the forced state; the exact
                         response objects AT the reference: Q-243/279, G^007, the
                         two-sector structure, Map 1).
```

## 1. THE AUDIT — PER OUTPUT, THE Q-270 DISCIPLINE

```text
1  FOR EACH alpha-facing output (B_ind, p_loc, kappa_Thomson, Delta_Phi, the quotients):
   read the SEALED defining text and determine what evaluation structure it ACTUALLY
   requires: (a) STATIONARY/ON-SHELL evaluation specifically (quote the words); (b) only
   evaluation AT A DECLARED REFERENCE (and is the C1 zero-source reference + the forced
   state an admissible instance? -- check the definition's own conditions); (c) evaluation-
   structure UNSPECIFIED (name the freedom). DO NOT infer stationarity from the words
   "response", "Hessian", or "2PI" -- check what each definition's own machinery uses
   (the Q-270 clause verbatim).
2  THE 2PI QUESTION SPECIFICALLY: the "stationary 2PI blocks" -- does any SEALED text
   define the program's 2PI structure as stationary-evaluated, or did the 2PI language
   enter through the Q-249/Q-254 spec-writing (program-internal, revisable) rather than
   sealed physics? Trace the provenance of the word "stationary" in the deciding
   question's chain -- WHERE did it first enter, and on whose authority?
3  IF NOTHING SEALED REQUIRES STATIONARITY: state the collapse exactly -- the background
   = the C1 reference + forced state (EXISTS); the blocks at the reference (largely
   BUILT: Q-243/279, G^007, the two-sector structure, Map 1); the remaining genuine
   residue (the domain and the squares? -- list it, bounded). State what the verdict
   then needs, and whether it is computable from BUILT material.
4  IF SOMETHING SEALED DOES REQUIRE IT: quote it exactly; the four-object build resumes
   with its purpose nailed down.
5  *** THE SYNONYM AUDIT (the principal's second check -- the Q-254 dedup discipline):
   for EACH of the four named objects, determine whether it IS an already-built/proved
   object under a NEW NAME -- identifications PROVEN (cite both texts), not asserted:
   (a) "restriction/Tail squares" vs P7's PROVED commuting-square certificate (Q-257,
       6/6, class-transport lemma, discharges by composition) and the Q-288 ledger's
       restriction-square columns;
   (b) "stationary 2PI blocks" vs the BUILT two-sector structure (Q-309/391: kernel,
       complement, mixing) and Q-243's retarded blocks;
   (c) "physical background realization" vs Q-281's lift fiber vs DoR-015's ADOPTED
       external-background field (three names -- one object? and the adopted field has
       NEVER been consumed);
   (d) "common completed domain" vs Q-278's bounded domains + P2's closures + Q-239's
       field (4).
   A proven rename SHRINKS the gap by its proof; a proven distinction sharpens it. ***
6  KILL-PASSES: sealed citations only; no convention import in EITHER direction (do not
   dismiss stationarity by taste, and do not retain it by habit); identifications proven
   never asserted; the PROVABLE/YOURS tags on every conclusion.
DELIVERABLE: the per-output evaluation-requirement table with citations, the provenance
trace of "stationary", THE SYNONYM TABLE for the four objects (rename-proven /
distinct-proven per object), and the verdict-path statement: COLLAPSES-TO-REFERENCE
(with the remaining bounded list, post-dedup) or STATIONARITY-REQUIRED (with the quote).
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
