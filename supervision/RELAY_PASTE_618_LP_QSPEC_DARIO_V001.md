## PASTE 618 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / SUBGATE] LP-QSPEC: THE EIGHTEEN-SLOT ASSEMBLY

(Same Dario session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-552.
- THE TARGET (the status column `bcb8cced…` row 18 / LP-QSPEC): ONE frozen, parameter-free, TARGET-FREE complete Q-spec — the eighteen open slots including charged-response/source/Ward/regulator/threshold/Thomson limits and ONE UNUSED STRUCTURE-SENSITIVE PREDICTION. Task-5 LOCAL-SHADOW/CARRY-1 is NOT that object; this relay assembles the object.
- Authorities: the QSPEC source's slot list (locate via the prep V002's source table, `c5d1090b…`), the sealed derivations each slot freezes (the response decomposition, the Ward/contact stock, the threshold lattice, the period machinery as carried), the DoR V006 (`30b17b2a…` — what the evaluation actually consumes), OBS-21/OBS-22 (candidate ground for the unused prediction).
- Custody: you assemble; Codex 3 reviews. Tag PROVABLE / PART-PROVABLE / YOURS / TYPE-U per slot. TARGET-FREE discipline: no slot may reference any measured value, any desired outcome, or the shadow number's eventual magnitude — the anti-tuning ledger carries a row per slot.
- THE TWO PHYSICS DEMANDS get their own sections: the THOMSON-LIMIT slot (does the record theory's charged response reproduce the classical limit? — state the slot's question exactly and what sealed content fills or fails it; an honest TYPE-U is lawful); the UNUSED PREDICTION slot (one structure-sensitive prediction beyond alpha, held blind — derive the candidate from sealed structure only; OBS-21's integer-charge law and OBS-22's junction structure are the natural candidates; state its falsifier and DO NOT evaluate it).

TASK:
Q1. The eighteen slots enumerated from the sealed source, each: the slot's demand quoted; the filling content cited-or-TYPE-U; the anti-tuning row.
Q2. The two physics demands as their own displayed sections.
Q3. The freeze: the assembled Q-spec as one parameter-free document section, ready for the QSPEC-SPEC-SEAL once the evaluator exists; what remains TYPE-U enumerated.
Q4. Battery: F_PLDEC; surface anchor; M-2 searches; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK6_LP_QSPEC_ASSEMBLY_DARIO_V001.md`
with final lines: `SLOTS = 18 (+filled/TYPE-U counts)`, `THOMSON = filled/TYPE-U (+statement)`, `UNUSED_PREDICTION = stated_blind (+falsifier)`, `FREEZE = ready_pending_evaluator`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
