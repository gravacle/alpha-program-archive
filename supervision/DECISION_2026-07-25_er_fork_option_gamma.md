# Principal Decision — ER Fork Option γ (2026-07-25, night)

Brian's decision, superseding the α/β follow-up fork recorded in the sealed
ER insensitivity result: pursue γ — DERIVE record-level insensitivity as a
theorem. Strictly better than β (carrying ER-A as a disclosed premise): it
discharges a named blocker (the open ER fork / envelope-realization
selector) instead of carrying it as conditionality on every downstream
headline. It is also better-grounded than α (a battery-grade numeric gate):
the executed insensitivity gate produced a MECHANISM CANDIDATE — both
independent lanes found the completed-record three-history sum cancels
connection curvature to ~1e-13 while individual histories move at ~1e-10,
and the pointer weights w = (1/2, -1/4, -1/4) sum to ZERO exactly
(Σ_λ w_λ = <pointer|ready> = 0), so any history-independent connection
response cancels identically in the pointer sum.

The γ theorem, if it holds, dissolves the fork at the record level: the ER
choice enters only through the record-write envelope; if the record-level
(pointer/completed) response is provably insensitive to that envelope at
the order that feeds kappa_record, the fork never has to be selected —
the four-axis "exact identities" route, now with a numerically-discovered
mechanism.

Process per discipline: theory scoping lane (typed statement + mechanism
derivation + proof routes + negative controls + exact quantifier scope
needed to discharge ER_fork_closed) → sealed derivation spec → hostile
pre-execution review → fresh-context execution → blind verification.
The scoping lane is chartered NOT to assume the cancellation is exact —
proving it is leading-order-only with an envelope-suppressed residual
would be an honest partial discharge and must be reported as such.

## Brian's full directive (received after the note above; operative constraints)

1. ORDER OF WORK: (1) answer the proxy-to-kappa_record link question first —
   short, honest, no spec; (2) only if gamma viable given (1): seal the
   OPERATOR-LEVEL identity spec with refutation conditions (1)-(4) frozen as
   predictions; (3) if gamma dies: beta, recorded append-only with a
   CUMULATIVE-CONDITIONALITY note (second disclosed premise alongside the
   complex-vs-real selection, visible in one place); (4) alpha may NOT be
   commissioned without a positive answer to (1).
2. MANDATORY DISCIPLINE: gamma admissible ONLY as an exact algebraic
   identity at the operator level (the completed-record functional
   annihilates the ER-A/ER-B difference operator). A numerical cancellation
   at ~1e-13 is NOT the theorem and may not be labeled one. Numerical-only
   -> report honestly -> beta.
3. REFUTATION CONDITIONS (each must be explicitly checked and reported):
   (1) carrier accident; (2) a=0-only, degrading at physical coupling
   (a <= 0.13 leverage); (3) fails the thermodynamic/continuum limit;
   (4) annihilated quantity is the proxy, not what reaches kappa_record.
4. The physics reading if gamma holds: two envelope realizations producing
   identical records are one physics under two descriptions — the fork is
   gauge-like redundancy, not underdetermination. The NOT_RESOLVED verdict
   is then the expected signature of exact structural cancellation.
5. v004/production remains the critical path; gamma runs parallel.

## Construction lane's answer to the link question (order-of-work item 1)

Derivable, conditionally; underived today. kappa_record IS the intensive
limit over the relayed exhaustion of the per-cell connection Hessian of
-log(completed-record amplitude) (battery T7/T14; Duhamel route memo
typing) — the same functional the proxy finite-differences at one carrier.
The missing pieces are EXACTLY analytic-program items 3-5 (majorant,
linked-cluster density, Duhamel/Hessian interchange), already on the
critical path — no new obligation. Additional honest gaps: the proxy sits
on the finite comparison lane's amplitude, not the actual parent at
complete Q_spec; completed-vs-exhaustive must be respected. CONSEQUENCES:
alpha not commissioned (precision about a quantity of unproven relevance);
gamma formulated against the per-cell completed-record Hessian on the
amended objects with uniformity along the exhaustion.

OPERATOR-LEVEL TARGET identified from sealed record data: pointer moments
m0 = sum w_lambda = <pointer|ready> = 0 and m1 = <pointer|c|ready> = 0
EXACTLY (c[1][0] = 0), m2 = <pointer|c^2|ready> = -1. The pointer sum
identically annihilates all lambda-independent and lambda-linear Hessian
components; the theorem's entire content is the lambda-EVEN
(record-coupling-dependent) part — equivalently: the per-cell connection
Hessian is independent of the record-coupling magnitude at a=0. The
envelope enters only as that magnitude's time profile, so this identity IS
"the record cannot see the envelope." Scoping lane launched under the
mandatory discipline.
