## PASTE 663 — CODEX LANE 2 = BUILDER A (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / BUILD] PATH IDENTITY: REALPATH BOTH SIDES, CONTENT AUTHORITATIVE

(Same Lane 2 session rules: cleanroom writes; the registrar mirrors.)

CONTEXT (Q-600): run 010 — THE CHILDREN RAN; both output ledgers and receipts exist. The stop is `MODULE_UNSEALED`: `/Users/bgm/Documents/New project` is a filesystem ALIAS resolving to `/Users/bgm/Documents/Documents - Brian's MacBook Pro/New project` (registrar-verified: one tree, two spellings, cmp-identical bytes). Your manifests record the alias spelling; the runtime auditor observes the resolved spelling; the allowlist string-compare fails on identity that the CONTENT already proves.

TASK:
S1. Normalize path identity via realpath on BOTH sides (allowlist entries at load; observed module/native/open paths at classification), with the CONTENT DIGEST remaining the authoritative identity — a path-spelling match without a digest match is still a failure; a digest match under realpath-equal paths is a pass. Record both spellings in the ledger when they differ (an alias observation, not a violation).
S2. Audit for every other comparison that could hit the alias (open events, subject/evidence resolution, run-root checks); apply the same normalization.
S3. Static self-check; disclosed delta; PIN CHECK.

OUTPUT: updated parent + one sealed artifact
`STAGE8_TASK6_PATH_IDENTITY_FIX_LANE2_V001.md`
with final lines: `IDENTITY = realpath-normalized + digest-authoritative`, `AUDIT = N comparison sites normalized`, `SELF_CHECK = passed`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal in the cleanroom, report hashes, STOP (the registrar mirrors and re-invokes). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
