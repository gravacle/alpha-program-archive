PASTE 394 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THE ORIENTATION: SEALED FACT, OR VERDICT-INVARIANT? — BOTH ROUTES, NO AUTHORSHIP (THE LAST DATUM)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-311)

```text
DOES THE OBJECT EXIST?   THE GAP is one global datum (Q-311): the alternating edge
                         orientation the intertwiner must carry. Local signs + E_post do
                         not force it. TWO no-authorship routes, run BOTH:
IS THE VERSION CURRENT?  YES through Q-311. No selection, no authored assignment -- if
                         both routes fail, the authored option returns to the principal;
                         it is NOT taken here.
ARE ITS INPUTS PRESENT?  (a) the SEALED K_square artifacts: B_K's definite matrix elements
                         with their signs, the sealed charpoly/trace results, the sealed
                         incidence data -- the orientation THEY were computed with is in
                         their bytes; (b) the orientation FAMILY on the complex (finite:
                         enumerate it), the completed kernel machinery, the lifted zeros
                         (beta_N, Q-310), V005's cycle apparatus.
```

## 1. ROUTE (a) — THE SEALED-FACT READING

```text
1  EXTRACT the edge-orientation convention the SEALED B_K embodies: from the sealed
   matrix elements' signs, reconstruct the cell/edge orientation assignment they encode
   (the assignment is determined by the sealed bytes if the sealed operator is
   orientation-dependent -- PROVE which). If the sealed convention IS the alternating
   assignment (or any definite one), the intertwiner's orientation is a CITATION:
   install it with the sealed provenance and proceed to the descent.
2  HONESTY: if the sealed results are orientation-INDEPENDENT (the charpoly and traces
   might be -- squares of B), then route (a) yields nothing and says so; that itself
   feeds route (b).
```

## 2. ROUTE (b) — THE INVARIANCE THEOREM (THE Q-241 MOVE)

```text
3  ENUMERATE the orientation family (finite, on the finite complex). For the VERDICT
   COMPUTATION (the 2PI descent's p-trace): prove or refute that the p-presence/absence
   outcome is INVARIANT across the family -- the candidate mechanism: orientation flips
   act on cycle phases as conjugation/sign flips, and the p-content of blocks is carried
   by |·|-type weights (q(1-q), the dephasing forms) that are orientation-BLIND; the
   ZERO blocks stay zero under any sign assignment. If invariance proves, execute the
   2PI descent AT FAMILY LEVEL (no member selected) and STATE THE VERDICT family-wide.
4  IF EITHER ROUTE LANDS: the descent executes; TRACE p; THE VERDICT with mechanism and
   the complete premise chain -- the one-sided evidence becomes the theorem or fails to.
5  KILL-PASSES: no authored assignment; no selection (family-wide or sealed-cited only);
   restriction checks; six-account rows; door flags; no evaluation.
DELIVERABLE: route (a)'s sealed reading, route (b)'s invariance theorem, and -- if
either closes -- THE p-VERDICT. If both fail, the exact reason each failed, for the
principal's authored-orientation decision.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
