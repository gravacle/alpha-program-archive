PASTE 334 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THE ALPHA-FACING OUTPUT TAIL ANNIHILATION THEOREM — OUTPUT BY OUTPUT (DoR-011)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-250)

```text
DOES THE OBJECT EXIST?   NAMED BY Q-250, NOT PROVED: the ALPHA_FACING_OUTPUT_TAIL_ANNIHILATION
                         THEOREM. Q-250 proved the physical response is DETERMINED MODULO the
                         tail z_tail B** -- the tail is the response's ENTIRE remaining
                         freedom. If every alpha-facing output annihilates it, the tail is
                         physically inert and the p_ch tail route is DEAD.
IS THE VERSION CURRENT?  YES: Q-247 (tail = M_cyl(Y)^perp = z_tail B**, weak-* structure
                         explicit), Q-249 (chain untyped; finite s_J/s_R norm-class),
                         Q-250 (modulo-tail determinacy, TAIL_QUOTIENT_DETERMINACY = true).
ARE ITS INPUTS PRESENT?  The sealed DEFINITIONS of the alpha-facing outputs: Delta_Phi[K;X_K],
                         p_loc, B_ind, C_EM, R_comp, kappa_Thomson's defining ratio, and the
                         response quotients. The theorem is checked against DEFINITIONS, not
                         against constructions that do not exist yet.
```

## 1. THE TASK — ONE OUTPUT AT A TIME, NO AGGREGATE HAND-WAVING

```text
1  FOR EACH output -- Delta_Phi, p_loc, B_ind, C_EM, R_comp, kappa_Thomson, the
   response-visible and outgoing-record-visible quotients -- read the sealed defining text
   and determine HOW the output consumes the response: through finite restrictions? through
   norm-continuous functionals? through a weak-* functional (which COULD see the tail)?
   through an evaluation the text leaves unspecified? QUOTE the defining text per output.
2  *** PER OUTPUT, ONE OF FOUR VERDICTS:
   ANNIHILATES (proof: the consumption factors through finite restrictions or
     norm-continuous functionals -- Q-247's separation then kills the tail term; exhibit
     the factorization);
   SEES THE TAIL (the definition applies a weak-* functional or bidual pairing to the
     response -- exhibit the term and give the tail contribution's exact form);
   CONSUMPTION UNSPECIFIED (the sealed text does not say how the output touches the
     response -- an UNSPECIFIED consumption is a FREEDOM, name it; do not resolve it);
   OUTPUT NOT DEFINED ON THE RESPONSE AT ALL (scope finding, TYPE-S). ***
3  *** B_ind GETS THE DEEPEST TREATMENT: it defines the fixed point K = B_ind(K) under
   alpha = 1/(4 pi K). If B_ind ANNIHILATES the tail, no tail content moves the fixed point
   and the tail route to p_ch-in-alpha is dead EVEN IF other outputs see the tail. If B_ind
   SEES the tail, say exactly through which term. ***
4  ASSEMBLE the theorem from the rows: FULL (all annihilate), PARTIAL (name the split), or
   REFUTED (a named output sees the tail). PARTIAL with B_ind on the annihilating side is a
   major result -- do not bury it.
5  KILL-PASSES: no "physical outputs are continuous" arguments (Q-249 discipline); a
   factorization must be EXHIBITED, not asserted; unspecified consumption must not be
   silently upgraded to annihilation -- that would be typing by convenience, the exact
   move this program exists to refuse.
6  FENCES (DoR-011): proof/reading only. No evaluation, no roots, no numbers, no comparison
   to measured constants.
```

## 2. LEADS

```text
IF FULL ANNIHILATION -- *** LEAD WITH IT: the tail is physically inert; the p_ch tail route
is DEAD by theorem; the deciding question stands on the background channel alone (332's
terrain). ***
IF B_ind ANNIHILATES BUT OTHERS DON'T -- *** LEAD WITH B_ind: the fixed point is
tail-blind, and name which outputs still see it and what that affects. ***
IF ANY OUTPUT SEES THE TAIL -- *** LEAD WITH THE TERM: that is p_ch's live route into that
output, stated exactly. ***
IF CONSUMPTIONS ARE UNSPECIFIED -- lead with the list: those unspecified consumptions are
rulings for the principal, enumerated and typed.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
