PASTE 415 — CODEX LANE 1 (HIGH EFFORT) — [TASK 4a] THE TOWER AT FULL SOURCE GENERALITY — D_k EXTENDED FROM TIER-LOCAL PROBES TO COMPLETED BILOCAL SOURCES (THE FINITE DEPTH PROGRAM, COMPUTATION THREE)

*** READ `alpha_supervision/LOCKED_PROCESS.md`. SEAL, MIRROR, REPORT, STOP. ***

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

---

## PREFLIGHT (current through register head Q-331)

```text
DOES THE OBJECT EXIST?   PARTIALLY: the tower is doubly confirmed for DECLARED TIER-LOCAL
                         probes (Q-330/Q-331, the scope note explicit). The bridge will
                         consume the WIDEST sealed depth data; this computation extends
                         the exact tower to ARBITRARY completed bilocal sources (the P2
                         class), independent of and parallel to computation two (the
                         intrinsic tower, other lane -- DO NOT depend on its outcome).
IS THE VERSION CURRENT?  YES through Q-331. FILE DISCIPLINE (the collision lesson):
                         your artifact name carries the LANE TAG -- use
                         ..._COMPUTATION_THREE_LANE1_V001.md.
ARE ITS INPUTS PRESENT?  YES: the verified tower (both confirmations), P2 V002's source
                         class (the l1+trace-class completed bilocals), the closed
                         forms, the tier construction.
```

## 1. THE COMPUTATION

```text
1  EXTEND the tower exactly: tier inputs drawn from the FULL P2 completed bilocal
   source class (not tier-local declared probes) -- the tier-k state, weights,
   amplitude, and accumulated quantities in closed form as functionals of the source
   sequence. The declared-probe tower recovered as the special case (cite; falsifier
   check).
2  THE GENERALIZED D_k: what replaces kQ/2 at full generality -- the exact functional
   of the source sequence; its growth classes characterized (which source sequences
   give bounded / linear / super-linear accumulation; the SUPREMUM behavior over the
   admitted class); the weights' invariance re-proven at full generality (or its
   failure exhibited -- if completed sources can move the (1-p,p) weights, THAT is a
   major finding: the require side would be depth-sensitive at full generality).
3  DISCIPLINE: tier-1 sealed structure; identity behavior; restriction/reality/
   quotient/rank; no continuum response objects; no bridge claims; no evaluation.
DELIVERABLE: the full-generality tower in closed form, the generalized accumulation
functional with its growth classification, the weight-invariance verdict at full
generality. Lane-tagged filename.
```

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
