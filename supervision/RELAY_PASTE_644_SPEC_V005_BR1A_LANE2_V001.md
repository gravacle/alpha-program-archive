## PASTE 644 — CODEX LANE 2 (GPT-5.6-SOL, EXTRA-HIGH) — [TASK 6 / SUBGATE] SPEC V005: INSTALL BR-1/A + REPAIR THE TEN

(Same Lane 2 session rules: write and seal in the CLEANROOM only; the registrar mirrors. All process law applies, incl. the SWEEP COVERAGE RULE and TEST-CLASS RULE.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-580.
- Base: cleanroom `STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE2_V004.md` = `2c767bfc953c7efeeaf4a33542974b10e0a674a161a5f1a651f3486ac36fad8b`. Verify before reading.
- THE ADJUDICATION (the closed list + the regime; implement, do not relitigate): `STAGE8_TASK6_BR1_ADJUDICATION_DARIO_V001.md` = `ec0412d22db709e915abea001dfc110fe3050f5f0b6a74050f0421cf14304e38` — ten defective rows with per-item repairs (each a single comparison or proof goal using patterns already in V004: REJECTED / FAILS / CONTAINED / INTERTWINER_IDENTITY), six lawful rows with their reasons, and the drafted BR-1/A regime text.
- Census source for every blocker you touch: `BID_FULL_STACK_REVIEW_LEDGER_V003.md` = `c09f2c24…`.

TASK:
T1. **Install BR-1/A as the law's own regime text** (replacing the "runnable test" claim): mechanical candidate generation that MUST over-generate; clause decomposition from the sealed blocker; per-candidate verdicts naming the clause and the receiver. State in the law: a "zero failures" report without candidates, clauses, and named receivers is not a BR-1 result and may not be registered as one. Reclassify BR-1 as ADJUDICATED and BR-2 as MECHANICAL in the text.
T2. **Repair the ten** exactly per the adjudication's per-item column; the six lawful rows untouched; any deviation from the column is a displayed finding, not a silent choice.
T3. **The BR-1/A adjudication record for V005 itself:** run the candidate generator over the final file (over-generating), display the candidate set, and for each candidate either cite the adjudication's verdict (for unchanged rows) or display the new clause/receiver verdict (for repaired rows). BR-2 swept mechanically with per-row coverage displayed.
T4. **Protected sections untouched** (pinned hashes verify); carriage complete finite delta under declared conventions with line-level attribution where hunks are shared (C-V5a); PIN CHECK displayed; the VERDICT-LINE SCOPE RULE governs the final lines.
T5. Battery: F_PLDEC; anti-tuning; M-2 (four modes); self verb audit.

OUTPUT: one artifact, lane-tagged filename, written and sealed IN THE CLEANROOM
`STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE2_V005.md`
with final lines: `BR1A = installed (ADJUDICATED regime)`, `TEN = 10/10 repaired`, `BR1A_RECORD = candidates displayed, all adjudicated (+counts)`, `BR2_SWEEP = 0 failures (per-row coverage displayed)`, `PROTECTED_SECTIONS = unchanged`, `CARRIAGE = complete_finite_delta (+hunks)`, `AUTHORIZATION = not_claimed`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in either location, STOP and report — do not overwrite. Seal in the cleanroom, report both hashes, STOP (the registrar mirrors). No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of any physical quantity; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
