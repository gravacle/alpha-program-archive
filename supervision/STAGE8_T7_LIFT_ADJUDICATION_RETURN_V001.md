# Stage-8 T7 Primitive Connected-Lift Adjudication Return v001
Fable lane → Codex (via Brian) · 2026-07-24 (late) · serving STAGE8_T7_PRIMITIVE_CONNECTED_LIFT_BLOCKER_RETURN_V001.md
Fresh contexts; read-only; both seal bundles verified (shasum -c OK); isolated verifier PASS.

## Primary verdict

```
CONFIRM_PRIMITIVE_CONNECTED_LIFT_BLOCKED
```

All eight claims adjudicated independently and hold:

1. D1–D4 closure uniqueness: recomputed; FS triple exactly (1/4, 1/2, 0);
   D3 is a one-line theorem on the complex line; every competitor fails a
   stated axiom.
2. Exact zero mode: the 4-torus incidence rebuilt in INTEGER arithmetic
   with an independent vertex indexing at L=3,5,7 (dims 405/3125/12005
   match); residual exactly 0. Mechanism: flat path-independent transport
   telescopes around a closed complex with no boundary vertex to break it.
   Counterfactual: one nontrivial edge phase raises the residual to 0.686 —
   flatness is load-bearing. NOTE FOR THE RECORD: the sealed derive/verify
   scripts encode this check tautologically (residuals constructed as
   zeros); this lane's independent rebuild is the actual numerical
   confirmation — recommend the verifier be amended to rebuild from the
   incidence data.
3. Zero baseline for every completed endpoint: verified (24 random + 2
   deterministic endpoints, max 2.5e-16); mixed rays return exactly their
   root component.
4. Root survival inadmissible: semantic exclusion predates the block
   (pinned amplitude derivation text + V011 flag + battery NC5); accepting
   it now would be a post-failure repair.
5. POVM/instrument escape excluded: positivity argument re-derived, gap
   probed (200/200 Hermitian operators with ⟨r|E|r⟩=0 and Er≠0 are
   indefinite — non-positive "effects" are not effects); the load-bearing
   premise is the sealed W_00 = 0 no-output rule, hash-verified.
6. One-handle control: exact (B³=2B; U(τ_R) = I − B² maps r→p with
   amplitude exactly 1).
7. Duhamel tangent identity: verified as a full matrix identity on random
   carriers (2.5e-10) and on a re-laid-out 405-dim rebuild (1.50e-9,
   matching the sealed 1.58e-9 at the finite-difference floor).
8. Dependency logic: T7(ii)–(iv) all quantify over Z = a(A)/a(0); claims
   2–5 give a(0) = 0 for every admissible boundary. Confirmed.

No refuting theorem exists in the corpus.

## Closure-condition assessment: the competitor family is REAL but the
## unresolved choice is narrower than it appears — and it hides a dependency

The sealed selectors collapse almost everything (each exclusion carries an
exact witness, scripts preserved in the supervision scratchpad):

- OPEN-THEN-LOOP: excluded — Z(A) ≡ 1 exactly, violating derived holonomy
  sensitivity.
- JOINT-over-stage (all incidences in one exponential): excluded — a
  holonomy-sensitive stage needs ≥2 cells, and simultaneous multi-cell
  activity is the spacetime-concurrent configuration outside the proven
  causally-sequential class; a global stage interval has no sealed meaning.
- SINGLE-GLOBAL-HANDLE / recordless bulk cells: excluded — the Stage-7
  class exhausts BY primitive diamonds and CISP demands new record factors
  per stage; handle-per-cell with local root attachment is FORCED.
- Mixed per-cell generators (bulk edge + handle in one factor): excluded at
  the pinned interval — exact witness: K_{1,2} at τ_R has root survival
  0.1740 and transfer 0.5870 (vs sealed 0 and 1); its survival
  1/3 + (2/3)cos(√3·t) never fully closes; the joint 3-handle star also
  fails (survival 0.0503). The sealed exact transfer lives ONLY on the
  branch-conditioned 3-dim block.
- Tensor-product over SHARED-boundary cells: not licensed — the sealed
  monoidal clause covers disjoint components only.

What survives is the family F(S, χ, β, σ, I): bulk-transport assignment to
cell factors (load-bearing: same complex, swapped assignment gives
γ''(0) = 1.2829 vs 0.2439), handle placement, designated endpoint among
multiple future handles, per-branch vs branch-summed conditioning, and the
interval rule for any mixed block. Members provably differ; nothing sealed
selects among them.

**THE DEEPEST FACT (changes what the fork is):** the only per-cell factor
consistent with the pinned τ_R and exact one-handle reduction is the pure
branch-conditioned block — which transfers the ENTIRE root line into the
completed endpoint at each closure. The front therefore cannot advance to
the next cell without a closure-face-to-new-root RELAY ISOMETRY — and that
relay is precisely the durability/redundant-encoding map the sealed
interval derivation explicitly assigns to Q_spec ("must not rename
orthogonality as durability before that map is derived"). The connected
baseline can be nonzero ONLY through an object the primitive record
complex does not contain. The apparent theory choice may thus be a
DEPENDENCY INVERSION: primitive T7 requires (part of) the durability
derivation, not a new principle.

## Recommended next move (no principle adoption; two sealed resources unexhausted)

Forward-seal `STAGE8_T7_OPEN_EXHAUSTION_ATTACHMENT_AND_RELAY_NECESSITY_SPEC_V001`:

- O1 — Branch-conditioned exactness dichotomy: a per-cell generator meets
  the sealed exact closure at τ_R iff it is the pinned 3-dim block. Seal
  the K_{1,2} (0.1740/0.5870) and joint-star (0.0503) witnesses as negative
  controls.
- O2 — DEPENDENCY-INVERSION THEOREM: pure-block sequencing cannot advance
  the front without a closure-face-to-root isometry, and that isometry is
  exactly the Q_spec durability/redundant-encoding map. If proven, the
  correct disposition is a sealed stage-ordering amendment (pull the relay
  derivation forward, or report Gate 5's T7 as conditional on it) — an
  architecture bookkeeping act, not a physics choice.
- O3 — Mixed-branch interval adjudication under the sealed Durable-Record
  Interval Principle per carrier: prove least-closure
  existence/uniqueness or its failure (expected: incommensurate spectra
  fail closure — consistent with the sealed cycle-7 result).
- O4 — Seal the S/β load-bearing witnesses so the residual family's reality
  is on the record with exact numbers.

Only if O2 FAILS (the relay is not the Q_spec map, or the family survives
even with the relay derived) does a genuine adoption packet go to Brian;
its content would be the F-family and the one-sentence choice, per the
sealed decision discipline.

## Constraints honored
No kappa_record, coupling, or alpha computed; no protected flag promoted;
no construction artifact edited; no target value accessed.
