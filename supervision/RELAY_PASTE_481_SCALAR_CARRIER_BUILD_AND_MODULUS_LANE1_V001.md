## PASTE 481 — CODEX LANE 1 (HIGH EFFORT) — [TASK 5] BUILD THE SCALAR CARRIER; COMPUTE CLOSURE AND THE MODULUS ON IT

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-402.
- Authorities (verify hashes): the premise sweep `feb84216…` (the constructibility routes for completeness and boundedness — follow them), your conditional theorem `88854f08…`, metric V005 `2a379098…`, the consumption chain artifacts, DoR-008, Q-399–Q-402.
- Custody: construction (derivation-first; authored steps disclosed at a choice table); cross-review follows. p, ν symbolic.

TASK:
W1. **Build the scalar carrier:** execute the two CONSTRUCTIBLE premises — the complete scalar carrier for K (the space, its metric/topology, completeness proven) and B_ind's boundedness on it — per the sweep's routes; every certificate (covariance, reality, the R4-only unit seam, DoR-008 restriction) proven on the built object.
W2. **Closure, on the built carrier:** does B_ind map the candidate set into itself? Derive or give the exact obstruction.
W3. **The modulus q, symbolically:** compute the contraction modulus on the minimal stages (the reciprocal loop; S8-A) as a symbolic expression — and READ OFF its dependence on (i) p, (ii) ν, (iii) the completion fiber, (iv) the reading rule, (v) p_loc[R_K]. This is the first sensitivity data: a freedom-independent q < 1 opens the forgetting theorem; a freedom-dependent q is the parametric answer arriving. Report exactly; no numeric evaluation; q < 1 claimed only if symbolically provable.
W4. **Falsifiers:** finite restrictions reproduced; all standing regressions; the anti-tuning ledger.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_SCALAR_CARRIER_BUILD_AND_MODULUS_LANE1_V001.md`
with a lead-result block (`CARRIER = BUILT/BLOCKED`; `CLOSURE = PROVEN/OBSTRUCTED (+what)`; `q = <symbolic form> | freedom-dependence per direction`), the register-sweep list, and the W1–W4 chain. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No numeric evaluation of alpha, K_*, roots, or q; no registered verdict; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
