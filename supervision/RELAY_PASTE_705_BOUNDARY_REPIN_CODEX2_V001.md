## PASTE 705 — CODEX 2 LANE = BUILDER A (5.6 SOL EXTRA HIGH) — [TASK 6 / TRANCHE] TWO CROSS-BOUNDARY RE-PINS — BOUNDED, GENERATED

(Same Codex 2 session rules. CLEANROOM-ONLY writes; the registrar mirrors. THE FIRST-TIME-RIGHT RULES apply. B's V012 confirmation is mirrored: root 2294dfe5… over 14 members; report 2a2194211ff7….)

CONTEXT: B confirmed your V012 amendment linkage-only with a structural argument (the constant and span are byte-identical V007→V012; the amendment names where the bytes already were), and r_ground now RESOLVES on real inputs — the full criterion reduces to a value. The registrar invoked run 032; your parent FAILED CLOSED at the boundary firewall, correctly: registrar sweep of all 248 A-side pins found exactly TWO stale, both pins of B bytes superseded by B's later re-pins:
1. verifier_verdict.schema.json: pinned 757943f8… ; B's current sealed bytes 1674aada096dba33c7026d70fb5df8705429224b2d3e81b2911e508851bfe9e8
2. rd22.verifier-manifest.v001.json: pinned d4219a53… ; B's current sealed bytes b43912455db3… (verify the full digest yourself from the mirrored bytes)

TASK (bounded): regenerate the two pins FROM B'S CURRENT SEALED BYTES via your pin tooling (never typed); regenerate manifests/pins/inventory; dry-run the parent's verifier-manifest and schema validation paths against B's real current instance. Nothing else moves — no spec, no code, no row.
OUTPUT: updated package + one sealed artifact `STAGE8_TASK6_BOUNDARY_REPIN_CODEX2_V001.md`
with final lines: `REPINNED = 2 (schema, instance), generated`, `PIN_CLOSURE = N hits, all resolved (old values return zero)`, `DRY_RUN = executed against B's current instance`, `CODE_CHANGED = none`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors, runs the cross-boundary preflight — now standing law before every invocation — and invokes RUN 032). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
