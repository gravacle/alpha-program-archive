## PASTE 661 — DARIO LANE = BUILDER B (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / BUILD] ONE STRING: THE PARENT'S ACTUAL OUTPUT NAME

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors.)

CONTEXT (Q-599): the registrar's 659 relay authored the name `verifier.verdict.json` from the producer pattern instead of citing the parent's actual constant — which the registrar has now read at source: `verifier.output.json` (run-root flat). The receipt name `verifier.receipt.json` was correct. The registrar's error, disclosed of record; your instance carries the consequence.

TASK: update `output_path` to `verifier.output.json`; sidecar; canonical tight form; delta disclosed (one string + sidecar). Everything else untouched.

OUTPUT: updated instance + sidecar + one sealed artifact
`STAGE8_TASK6_OUTPUT_NAME_FIX_DARIO_V001.md`
with final lines: `OUTPUT_PATH = verifier.output.json`, `DELTA = 1 string (+sidecar)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors and re-invokes). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
