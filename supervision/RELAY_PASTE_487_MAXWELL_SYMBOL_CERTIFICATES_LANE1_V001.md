## PASTE 487 — CODEX LANE 1 (HIGH EFFORT) — [TASK 5 / R4+R12] THE THREE MAXWELL-SYMBOL CERTIFICATES, INCLUDING THE STITCHING LEVER

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-408.
- Authorities (verify hashes): the kernel build `ccb94dfa…` (the three obstructions exactly as typed), your partial build `bae34116…`, the ledger `c35ef846…` (the R12 row — the stitching/cellulation-independence package, DoR-007's derive-the-limit ruling), metric V005 `2a379098…`, DoR-008, Q-406/407/408.
- The weight note: the geometric refinement/locality certificate IS the program's oldest absence (no derived stitching/continuum rule exists; the corpus's finite-cell routes were capped in advance; O-D3 was named the lever). Treat it as the primary object, not a checkbox.
- Custody: build or type; derivation-first; authored steps disclosed; cross-review follows. All symbols symbolic; no member selected.

TASK — for each certificate: BUILD it, or TYPE it exactly (constructible route / fiber-grade / obstructed-with-proof):
C1. **Completion-continuity:** the finite kernel calculus's continuity to the completed carrier (the DoR-019 completion convention is the given; DoR-008 binds the finite shadows).
C2. **THE GEOMETRIC REFINEMENT/LOCALITY CERTIFICATE (the stitching lever):** refinement-naturality and locality of the kernel calculus across cellulations — the derived discrete-to-continuum seam. Consume DoR-007's derive-the-limit ruling and the retained refinement discipline (all 1,088 filtrations; the thin N≤M category; the W3 adjoints). Build as far as derivation carries; a proven boundary with the exact residue is a full success; note explicitly what this does to R12's Task-6 obligation.
C3. **Ward/contact/Hodge:** the split certificates on the completed carrier (the finite Ward split is built — extend or prove the obstruction).
C4. **On whatever holds:** state the completed Maxwell symbol's status (BUILT / PARTIAL with the exact remainder) and recompute the fiber-constraint question on the completed symbol (does completion add a relation on (χ_K,T)? — the finite level added none).
C5. **Falsifiers:** finite restrictions; standing regressions; anti-tuning ledger.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_MAXWELL_SYMBOL_CERTIFICATES_LANE1_V001.md`
with a lead-result block (per certificate: BUILT/TYPED/OBSTRUCTED; `COMPLETED_SYMBOL = …`; `FIBER_CONSTRAINT_COMPLETED = …`; `R12_STATUS = <what the stitching work discharged or left>`), the register-sweep list, and the C1–C5 chain. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No numeric evaluation; no registered verdict; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
