## PASTE 582 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5] THE COMMON SEAMS: BUILT ONCE, FOR BOTH ROUTES

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-508.
- The gap audit (`STAGE8_TASK5_GAMMA_H_REVIEW_DARIO_V001.md`, SHA-256 `61d41a3ed13039b8db1c149215763f97e0c4f2a5376ae717fd400f1962eae712` — verify) rewrote the comparison: the seams are largely COMMON to both A8 routes. This relay builds the common seams once. Read its J-I, J-III, and J-IV sections first, plus your own Γ^H boards (`f2317e41…`).
- THE RECORD-FIRST RULE and the law-with-address principle apply throughout: the orientation finding says addressless structure is PROVABLY zero — the build must carry addresses, not fight them.
- Custody: you build; Dario reviews. Tag PROVABLE / PART-PROVABLE / YOURS. Display standard.

TASK — three seams, each its own section, display-or-stop:
S1. **THE ORIENTATION-ADDRESS STRUCTURE (the J-III corrected gap):** construct the addressed orientation/unit family mu(a) with the covariance law mu(a^-) = −mu(a) carried per address — from the sealed orientation signs and metric positivity (the W3 stock that already proved the loop pairing's sign structure at Q-493's (W3)). The unit half is DISSOLVED (dimensionless by the ratified rule — cite Dario's derivation); only the addressed sign family is to build. If a genuine choice remains (which address gets +), type it — plausibly the no-selection principle applies (an orientation ORBIT carried, not a representative).
S2. **THE J-I CORESTRICTION (the halved gap):** formalize the automatic descent — Q_N as corestriction, the canonical arrow O_R5 → O^resp displayed — and state exactly what half remains (the source-extension package's inhabitance) with its type.
S3. **THE F3 NONIDENTITY PROOFS (the J-IV debt):** prove the (F3) Loc base squares on the actual nonidentity generators where the sealed stock reaches — the reciprocal and refinement arrows first; where it does not reach, the exact blocking statement per arrow class. Dario's correction of record: the statement appears in ≥6 files; what is needed is PROOF, not declaration — display or stop.
S4. **The consequence board:** with S1–S3, each route's REMAINING route-specific debt restated exactly (H: the J-II realization member; HOL: the corrected affine-relative Ξ_N + U^Hol); what the identification falsifier can already bite on.
S5. **Battery:** F_PLDEC; anti-tuning; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_COMMON_SEAMS_LANE3_V001.md`
with final lines: `ORIENTATION_ADDRESSES = BUILT / STOPPED (+gap)`, `CORESTRICTION = DISPLAYED`, `F3_NONIDENTITY = PROVEN (+arrows) / PARTIAL (+blocks)`, `ROUTE_DEBTS = restated`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
