## PASTE 578 — CODEX LANE 3 (SOL, HIGH EFFORT) — [TASK 5] THE Γ BUILD, H ROUTE: ASSEMBLY FIRST, THE FOUR JOINTS ONE AT A TIME

(Same Lane 3 session rules; if fresh, re-run the STEP 0 access checks from your bootstrap.)

PREFLIGHT (verify before starting; report any failure and STOP):
- Register head: Q-504. DoR-020-A8 IS LAW (read `alpha-program-archive/supervision/DOR_020_A8_GAMMA_BOTH_ROUTES_2026-08-05.md`): you build the H route; the HOL route drafts in parallel elsewhere; the identification falsifier binds both; both A7 branches ride.
- Authorities: your audit V002 (`44202c06…` — the H-route lattice and its (M1)–(M11) definitions), its review (`af955f81…` — the two conservative defects, REPAIR THEM HERE: restore (M9)'s collinearity condition ALONGSIDE the metric identity; correct Q-mode's booking to vacuous-under-(M2)), the Loc build artifacts (`b53d9e93…`, `c4826773…`, `258680c4…`), the typing repair (the scalar/bottom type-distinction), the projector cert (`0bef9a00…`), the confirmed assembly chain.
- METHOD IS MANDATED — ASSEMBLY FIRST: Section 1 lists EVERY piece with its sealed hash and exact type (domain, codomain, carrier). Then the four joints, EACH in its own section, each either DISPLAYED (the equalities written out) or STOPPED (the exact gap named, the section ended). A joint that stops does not block the others — attempt all four.
- THE FOUR JOINTS: (J-i) the kernel-side domain assembly — how L_T enters the composite (the Kernbar scoping split resolved as a displayed map, not a summand juxtaposition); (J-ii) the landing on the Hodge carrier — the physical Loc step proper, from the finite sigma^fin stock to the carrier P_H acts on; (J-iii) the codomain reconciliation — the scalar/bottom seam (the ev/U^H interface displayed as a lawful map, no sealed-arrow inventions); (J-iv) naturality beyond identities — the (L3)/(L8) base cases on the actual reciprocal and refinement generators.
- Custody: you build; Dario reviews. Tag PROVABLE / PART-PROVABLE / YOURS. F_PLDEC: the reader and the false anchor touch nothing; the E_C exposure rides explicitly per branch.

TASK:
E1. **The piece ledger** (Section 1 as mandated).
E2. **The four joints** (Sections 2–5, one each, display-or-stop).
E3. **If all four close: Γ^H assembled** — the map stated end to end with types; the period of the reciprocal loop formed as an OBJECT (not evaluated); chi^per's subject term displayed per branch; the naturality board updated.
E4. **The consequence board:** under each outcome (all-close / which-stopped) — what R4–R8 can now run, the seed condition's well-posedness status, membership's J2 premise board.
E5. **Battery:** the two audit repairs executed and displayed; F_PLDEC; anti-tuning; self verb audit.

OUTPUT: one artifact, lane-tagged filename
`STAGE8_TASK5_GAMMA_H_ROUTE_LANE3_V001.md`
with final lines: `PIECES = listed (+count)`, `JOINT_I = DISPLAYED / STOPPED (+gap)`, `JOINT_II = …`, `JOINT_III = …`, `JOINT_IV = …`, `GAMMA_H = ASSEMBLED / PARTIAL (+joints)`, `AUDIT_REPAIRS = DONE`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output filename already exists in the workspace, STOP and report — do not overwrite. Seal, mirror byte-identically to `alpha-program-archive/workspace/`, verify, report hashes, STOP. No register, plan, tracker, git, commit, or push action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. If a fence blocks a structural result, flag MACHINERY-APPEAL and continue.
