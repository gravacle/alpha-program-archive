## PASTE 719 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / STEP 8] THE TOPOLOGY REQUIRE-HUNT: FORCED, OR GENUINELY FREE?

PICKUP-ACK + LANE GUARD in force (DARIO).
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL standing laws apply, incl. THE OBJECT-NAMES PROBE installed from your own 716 disclosure. Governing: the frozen prereg 9f0d12b4…, the carrier 5f4979d5…, your 716 result 5234141f….)

CONTEXT (principal reframe of Q-624's fork, allow/require law): "no authority SELECTS a topology" is an allow-shaped fact. The require-shaped question is unasked: DOES ANY SEALED REQUIREMENT FORCE ONE? Permissions never force; a unique require does — including BY ELIMINATION. Your own cost list may be three disqualifications in disguise.

TASK — the require-hunt, exhaustive over consumers of the limit's sense:
DD1. ENUMERATE every sealed REQUIRE that touches the sense of the refinement limit: DoR-007's theorem content (cellulation-independence as the theorem), J2's invariance demand, T_ref's consumers, V011's density statement, the record action's convergence obligations, the (D2-4) countermodel's exclusion conditions — each with source + span (block-covered), each tagged REQUIRE vs ALLOW vs WANT by its own verbatim modality. Searched spaces stated; object-names probed (T_ref/C_ref/Ref AND tau_norm/tau_cyl/tau_density AND their aliases).
DD2. TEST each of the three topologies against each require: SATISFIES / VIOLATES (elimination certificate: the require's text + the property's sealed statement) / UNDETERMINED (what's missing). "Lacks geometric realization" and "admits a bidual tail" are candidate violations — find whether any sealed require actually demands realization or excludes the tail, or whether those are only wants.
DD3. VERDICT, one of three: FORCED (exactly one survives — display the elimination/forcing chain; the topology enters as DERIVED); UNDERDETERMINED (two+ survive every require — the adoption returns to the principal as genuinely free, with the surviving set); OVER-CONSTRAINED (none survives — the conflict displayed; a require must yield, and that is the principal's). 
DD4. NOTE what the hunt shows about lambda: does any forcing require also constrain the (D2-4) scale freedom? Observation only, zero adoption.
OUTPUT: one sealed artifact `STAGE8_7A_TOPOLOGY_REQUIRE_HUNT_DARIO_V001.md`
Final lines: `REQUIRES = N enumerated (R/A/W tagged)`, `MATRIX = 3 x N displayed`, `VERDICT = FORCED(<tau>) / UNDERDETERMINED({...}) / OVER-CONSTRAINED`, `LAMBDA_NOTE = stated`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 719_DONE.md, STOP. No register, plan, tracker, git action. NOTHING IS ADOPTED IN THIS RELAY UNDER ANY VERDICT.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. No common cell formed; no junction map evaluated.
