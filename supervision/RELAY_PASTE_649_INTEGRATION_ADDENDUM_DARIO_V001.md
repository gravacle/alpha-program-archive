## PASTE 649 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [TASK 6 / BUILD] THE INTEGRATION ADDENDUM DRAFT

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. YOU STILL MAY NOT READ BUILDER A'S CODE — this addendum is drafted from the spec's own patterns and your G1/G2 report; that is the point.)

PREFLIGHT (verify before starting; report any failure and STOP; live-append tolerance applies):
- Register head: Q-587 (Builder A delivered; its manifest is now a SEALED input you may lawfully read — the MANIFEST, `STAGE8_TASK6_EVALUATOR_BUILD_A_MANIFEST_LANE2_V001.md` = `767586af…`, in the archive workspace — but not the code files it inventories).
- The governing spec V005 = `f8d1a7dc…`; your verifier package's contracts (yours to read); your G1/G2/G3 report (Q-586).

TASK — one addendum document, three contracts, each derived from the spec's existing patterns (the check-row and child-row inventories) and NOT from either implementation:
A1. **The event-ledger fields (G1):** extend the child-row inventory with exact carriers for process, network, and mutation events, patterned on the module/native/open trio; exact field inventory, closed.
A2. **The fixture row inventory (G2):** the exact field inventory for fixture rows, patterned on the check-row inventory; closed.
A3. **The verifier-child contract (`rd22.verifier-manifest.v001`):** the R9 isolated-child launch contract for Builder B's verifier — manifest fields, invocation form, output/receipt paths, stdout discipline, optimization declaration — patterned on the producer child manifests; closed. State plainly: both builders CONFORM to this; no implementation choice is ratified by it.
A4. Battery: PIN CHECK; VERDICT-LINE SCOPE; self verb audit. State what you did NOT consult (A's code; your own adapter's convenience).

OUTPUT: one artifact, sealed IN THE ARCHIVE WORKSPACE
`STAGE8_TASK6_SPEC_V005_INTEGRATION_ADDENDUM_DARIO_V001.md`
with final lines: `EVENT_FIELDS = closed inventory (+count)`, `FIXTURE_FIELDS = closed inventory (+count)`, `VERIFIER_CONTRACT = closed (+fields)`, `SOURCES = spec patterns only (attested)`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, STOP (the registrar mirrors). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants.
