## PASTE 1031 — CODEX 2 LANE — [PLAN:DESC-25] B03 V002 CHECK — THE DISSOLUTION UNDER ATTACK, AND THE ONE IDENTITY THAT DECIDES THE TARGET

PICKUP-ACK + LANE GUARD in force (CODEX 2). CLEANROOM-SIDE writes only; the registrar mirrors. Full paths; digests-only certificates; the fixed-point closure.
SUBJECTS, verify seals BEFORE reading (cleanroom root): `STAGE8_DESC_B03_DARIO_V002.md` (4ffe99b2e8160a53db03dea69f229c62992d28def6ab2e11c9487e10157fcbb4, 36,519 B) and its generator `build_b03_v002_pins_v001.py` (5d6ef64f105a54cf8cd7322c74b3773e7b603c1d67110782b7c1104bb9bd8d1f — Q-920 standard); the record files it cites at its spans; V001 (581ec505441b…) read-only for carriage.
THE CLAIMS UNDER CHECK, in order of consequence:
(1) THE IDENTITY THAT DECIDES EVERYTHING DOWNSTREAM — Dario's own unattacked step-to-press: are A_C0 and A_SRF_CTP THE SAME completed object? The artifact treats them as one receiver; the record uses both names. Settle it at bytes: same object under two names (cite the identification's span), or two objects needing a transport that does not exist (in which case the dissolved-as-selector half falls back to OPEN and say so plainly). This single ruling sets the derivation's target.
(2) THE RESTRICTION ARGUMENT: res_B exact — positivity via star-homomorphism, normalization via i_B(1_B)=1 — so an L3 state determines its B-restriction. Re-derive both properties at their spans; attack the exactness (a non-unital embedding, a graded subtlety, anything).
(3) THE SIX-LEG DEMAND RESTATEMENT: confirm each leg is read from the older receivers (P1, P5, v004) at the cited spans and that the StatePort packaging exclusion is applied consistently — no leg quietly imported from the removed frame.
(4) THE 11-CONSUMER RE-DISPOSITION: zero-requires-choice under the reframe, K4 decisive. Re-derive; one consumer that still needs an independent choice = the dissolution is refuted.
(5) L4 AND L5, THE QUICK DISPOSITIONS: Dario dispositioned NOT SUPPLIED and ABSENT without attack because L3 had decided the outcome. Attack both now — if either is actually satisfiable from sealed grounds, the gap is smaller than reported.
(6) THE L3 GAP ITSELF: confirm the record's own absence statements at their spans (P1, P5, v004), and confirm the survival claim — that P5 sits in Arm B's own residue list, so the obstruction predates the frame.
(7) CARRIAGE AND FOLDS: V001 byte-untouched; the four Q-938 narrowings folded as ordered; the custody change (opening the 1024 check) declared at both ends.
(8) RESIDUE (Q-921): fresh patterns; Dario self-reports one hit after a self-caught miscount.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..17 pointer-only. ω_phys untouched; no candidate; no freeze; no numeric evaluation; no member bound. The derivation itself is NOT this relay's task — nothing here constructs the completed object.
OUTPUT: one sealed artifact `STAGE8_DESC_B03_V002_CHECK_CODEX2_V001.md` (cleanroom root).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `IDENTITY = SAME-OBJECT (span) / TWO-OBJECTS (transport absent; dissolution falls to OPEN)`, `RESTRICTION = EXACT-CONFIRMED / BROKEN (where)`, `LEGS = 6/6-RECORD-READ / <imports>`, `CONSUMERS = ZERO-CHOICE-CONFIRMED / <requires>`, `L4_L5 = STAND / SATISFIABLE (which, span)`, `L3_GAP = CONFIRMED-AT-SPANS / WEAKER`, `CARRIAGE = CLEAN / <items>`, `RESIDUE_GREP = <result> (own patterns)`, `VERDICT = CONFIRMS / CORRECTED / REFUTES`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1031_DONE.md. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
