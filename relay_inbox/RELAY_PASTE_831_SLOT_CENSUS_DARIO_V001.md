## PASTE 831 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:D2-0] THE SLOT CENSUS: ALL EIGHTEEN, TYPED

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. GOVERNING: the principal's challenge — the assumption that the Q_spec's other fifteen slots are separate free-content volume is UNAUDITED; slots 10-12 proved to be derivation obligations (O1/O2/O3, the node's faces). Sources you located at 828: STAGE8_QSPEC_SLOT_STATUS_MAP_V001.md (c26daa7e9cde29b7) and STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md (7995f6fda75e7879, the slot block at lines 119-139 / byte 3472). Law 9 in force: this census must display its own completeness — the slot list is sealed and finite, so completeness is exact.)

TASK: extract all EIGHTEEN slots verbatim (names, types, status flags as sealed); then type each of the fifteen non-O slots by the standard: FILLED (already discharged of record — cite) / NODE-FACE (consumes a named build discharge — say which: the action itself, a descendant class, a Q_spec content item the skeleton forces) / DERIVABLE-FROM-ACTION (fills by derivation once the built action exists — display the dependence) / FREE-CONTENT (genuinely open realization — the two-sketch standard) / UNDECIDABLE (missing sealed object named). VERDICT: the D2 pricing under the census — if the fifteen are predominantly node-faces/derivable, D2 collapses into the build's discharge ledger and the plan's second long pole falls; if predominantly free, D2 stands as adoption volume and the principal's future decision surface is exactly those slots; state the split plainly. FREEDOMS-CONSUMED block; FLATTENING CHECK. All headline items CLAIMED. PE-1..11 pointer-only.
OUTPUT: one sealed artifact `STAGE8_D2_SLOT_CENSUS_DARIO_V001.md`
Final lines: `SLOTS = 18 extracted verbatim (completeness exact)`, `CENSUS = <n> FILLED / <n> NODE-FACE (which discharges) / <n> DERIVABLE-FROM-ACTION / <n> FREE-CONTENT / <n> UNDECIDABLE`, `D2_PRICING = collapses into the build / stands as adoption volume / split (stated)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 831_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. Census only; nothing filled or adopted; no smooth import; no EM identification.
