## PASTE 840 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:AXN-BUILD-A7] PACKAGE V3 (THE COMPLETE CENSUS) — THEN THE CLOSURE ROUND

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. GOVERNING: Codex's 838 (mirrored: STAGE8_AXN_BUILD_SKELETON_V2_CROSSCHECK_CODEX2_V001.md 24001c12… — verify seal): T10/re-typings/T01-resolution CONFIRMED; the six local hole shapes CONFIRMED; but LAW 9 DEFEATS the package — five missing interfaces named: ENVIRONMENT_SECTOR_CLOSURE (the note-level treatment overruled), the GRAVITATIONAL_QUANTUM_MEASURE boundary (Q_spec slot 2's second half), ACTION_P5_NO_OUTSIDE, O1_SOURCE_TAIL, O3_CROSS_REGULATOR; plus U2/U3/D-1/E4c-O2 present-unresolved. Your 839 stop stands vindicated. STEP 1 THEN STEP 2, one artifact each or one combined — your judgment, custody-clean either way.)

STEP 1 — PACKAGE V3: add the five missing interfaces as TYPED ROWS (each with its shape, receiver, and supplier-status per the standard — nothing resolved, everything enumerated); fold the unresolved set explicitly (U2, U3, D-1, E4c/O2 as interface rows with their known statuses); the census then COMPLETE under law 9 by construction against 838's list. All hole shapes carried from the confirmed V2.
STEP 2 — THE CLOSURE ROUND on the complete census (the 839 task, now lawful): the binding predicate applied to the four exhibited completions (the stratum DERIVED per member, with computations); then the predicate + composed falsifier interface pressed against EVERY census row — the six local holes AND the five interfaces: CLOSED-FORCED (derive, display) / NARROWED (new exact shape) / UNMOVED (why) / RULING-SHAPED (the freedom displayed; RULING-LAST then runs). BOX_gravity: the KK re-import barred; display what the pressure forces or leaves. FREEDOMS-CONSUMED block; FLATTENING CHECK. All headline items CLAIMED. PE-1..11 pointer-only.
OUTPUT: `STAGE8_AXN_BUILD_PACKAGE_V3_DARIO_V001.md` and/or `STAGE8_AXN_BUILD_CLOSURE_ROUND1_DARIO_V001.md` (your split; both sealed if both).
Final lines: `V3 = census complete (rows: <n> holes + <n> interfaces)`, `STRATUM = predicate-selected: <members>`, `ROUND = <n> CLOSED-FORCED / <n> NARROWED / <n> UNMOVED / <n> RULING-SHAPED`, `BOX_GRAVITY = <verdict>`, `ROUND2 = <needs>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output names exist, STOP. Seal, report hashes, write 840_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. DERIVE-NEVER-SELECT absolute; charter fences live; no smooth import; no EM identification.
