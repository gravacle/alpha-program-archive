## PASTE 1081 — DARIO LANE — [PLAN:DESC-38] THE SURFACE-NECESSITY HUNT — DOES THE READ-OFF NEED SM-6 AT ALL, OR IS THE DESCENT MACHINERY?

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs; the measure mandate (b9716661d1a0…) verified live. This is a SURFACE-NECESSITY HUNT: it AUTHORS NOTHING, PROPOSES NOTHING, ADOPTS NOTHING, binds no member. It GATES the descent question rather than presupposing it. An honest UNDETERMINED (naming the exact missing sealed ground) is a full result. No numeric evaluation — symbolic only.
WHY THIS HUNT, AND WHY NOW (the principal's challenge): the measure question has cycled six relays under an SM-6 (common-origin, from-the-frozen-trace) demand on the descent. But the surface terminus (Q-960/961) found the physics composes a joint object and STOPS at a measure that is REQUIRED (Q-984) and UNDETERMINED (Q-975/1075). The program has repeatedly noticed SM-6-conformance ⟺ the measure is DERIVED ⟺ the mandate is UNNECESSARY. That raises a real risk that SM-6-on-a-SUPPLIED-measure is MACHINERY — a from-trace determination re-imposed on an object the surface deliberately left undetermined. Settle whether the descent is a surface requirement at all BEFORE any further descent work.
SUBJECTS, verify seals BEFORE reading (archive workspace / supervision):
- The necessity probe `STAGE8_DESC_NECESSITY_PROBE_DARIO_V001.md` (cc3ce8ca29736ede2b5d47e76bca34955c2da31c86147e8cd279fde4a0530577) — READ exactly what the state PORT consumes via GNS at its spans (Q-984: "consumes the functional, not an invariant").
- The surface-first terminus results (Q-960/961) — the composed JOINT source-record object, both marginals forced (source = ω_tr fixed point; record = two-term mixture, weight κ_ch), stopping at a measure.
- Member 09 SM-6 six-conjunct definition `[17865,20008)` (common primitive provenance) and its product-extension distinction.
- Codex 1080 `STAGE8_MEASURE_V002_DESCENT_CHECK_CODEX2_V001.md` (6a3297513f9a…) — the exact positivity condition (∫ f·Z_N dν positive iff Z_N=1 ν-a.e.) and ν_Δ; the port pairs a FIXED functional Z_N with a measure.
- The mandate member 06, its open slot :47-49 (future_derivation_can_select_the_measure = NO_VERDICT).
THE HUNT — settle at bytes, author nothing:
(1) THE DECIDING SURFACE QUESTION. Does the read-off (the port / GNS construction that yields the number) consume (a) ONLY A STATE ON Y — i.e. the Y-marginal measure μ paired with the surface-fixed functional Z_N, the pairing ∫ Z_N dμ — or (b) THE FULL JOINT SOURCE-RECORD STATE, so the joint correlation itself is an input? Rule (a) or (b) from what the port actually consumes at its sealed spans — NOT from what a descent story would prefer. If genuinely undecidable on sealed ground, say UNDETERMINED and name the span that would decide it.
(2) THE MACHINERY VERDICT (follows from (1)). If (a) Y-MARGINAL: SM-6/common-origin descent is NOT a surface requirement on a supplied measure — the descent apparatus (the 1078 SM-6 demand and everything after) is MACHINERY; the surface need is simply a lawful state on Y. If (b) JOINT-STATE: SM-6 (joint legitimacy, ruling out the arbitrary product) IS a surface requirement and the descent question is real.
(3) THE SELF-CONSISTENCY TELL. Is "SUPPLY a measure (because the surface left it UNDETERMINED) that ALSO satisfies SM-6 (from-the-trace DETERMINED)" a contradiction — undetermined-by-record and determined-by-trace at once? Rule CONFIRMED (the two demands cannot both hold, so the SM-6 route and the mandate are mutually exclusive faces of one fork) or REFUTED (name how a from-trace measure can still be record-undetermined).
(4) THE IMPLICATION FOR THE PRINCIPAL (report, do NOT decide). If SM-6 is MACHINERY: the measure question CLOSES to the mandate decision — the principal SUPPLIES a lawful state on Y (no SM-6), or a future target-independent derivation produces it; the six descent relays resolve to "the surface needs a lawful measure, nothing more." If SM-6 is SURFACE: the next step is the from-trace feasibility hunt (does the frozen trace admit a coherent history state carrying nontrivial phase). State which implication the ruling forces.
FENCES: HUNT/NECESSITY ONLY. Nothing entered/adopted/proposed/bound; ω_phys is the SUBJECT, not entered or evaluated; BLIND HELD, every scale symbolic; NO numeric evaluation of a physical quantity; NO output inspection; JOINT_ANCHOR_DERIVED = false; no member bound; no fixed point; no end test. Mandated refusal paths for whatever the artifact seals (closure declared-first; every digest AT ITS PATH; fence depth-walk; no-numeric-path scan; residue scan; generic extractor with NO ALIAS SET if any symbol block appears).
OUTPUT: one sealed artifact `STAGE8_DESC_SURFACE_NECESSITY_HUNT_DARIO_V001.md` (archive workspace). All CLAIMED until the Codex 2 check.
Final lines: `READOFF_CONSUMES = Y-MARGINAL-STATE / JOINT-SOURCE-RECORD-STATE / UNDETERMINED (<span>)`, `SM6_IS = MACHINERY (not a surface requirement on a supplied measure) / SURFACE (required) / UNDETERMINED`, `SUPPLY_AND_SM6 = CONTRADICTION-CONFIRMED / REFUTED (<how>)`, `IMPLICATION = CLOSE-TO-MANDATE-DECISION / RUN-FROM-TRACE-FEASIBILITY-HUNT / UNDETERMINED`, `NECESSITY_PROBE_READ = <what the port consumes, at span>`, `CLOSURE = declared-first (byte, scan)`, `PROSE_DIGESTS = <n>/<n> STRICT==STABLE`, `NO_NUMERIC_PATH = CLEAN / VIOLATION`, `RESIDUE_GREP = <result> (own patterns)`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `VERDICT = NECESSITY-MAPPED (implication stated) / UNDETERMINED (ground named)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN`.
If the output name exists, STOP. Seal, report hashes, write 1081_DONE.md. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
