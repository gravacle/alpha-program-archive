## PASTE 789 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:A1-CORRECTIVE-3] THE BETA SECTORS WITH w_Phi SYMBOLIC: ONE RESIDUAL, THEN THE LAYER BANKS

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL alignment laws apply, including the new 2a: SCALING WEIGHTS of carried operator freedoms are themselves freedoms and appear in the block. GOVERNING: Codex's 787 convergence test (artifact 513132c1…, mirrored — verify before reading): FIVE of seven verdicts CONFIRMED (radius derivation; no implicit unit radius; tier-2 drop; tier-3 conditions; member bounds; plus the beta-invariant ratios and the S27 reading independently confirmed). TWO residuals, one defect: BETA_SECTORS refuted — your table wrote the cross term beta^-1, silently substituting w_Phi = 0; sealed 768 leaves Phi's scaling UNSELECTED. With Phi -> beta^(w_Phi) Phi symbolic: cross term beta^(w_Phi - 1), Phi^dagger Phi beta^(2 w_Phi). FREEDOMS_BLOCK therefore FALSE at the Phi row.)

TASK — close the residual; small and exact:
AS1. RESTATE THE BETA-SECTOR TABLE with w_Phi symbolic throughout: three geometric sectors beta^-2; cross term beta^(w_Phi - 1); C2_parent beta^0; Phi^dagger Phi beta^(2 w_Phi). Verify against the sealed 768 spans (re-derive, cite). State what each specialization of w_Phi would mean and that NONE is selected (w_Phi = 0 displayed only as one unselected member).
AS2. Re-examine every downstream statement of the v2 rebuild that touched the beta grading (U8's sharpening; the two beta-invariant ratios claim; FC-e) for w_Phi sensitivity: which survive for ALL w_Phi (say so and why), which become w_Phi-conditional (display the condition). Codex confirmed the two ratios independently — verify their w_Phi-independence explicitly rather than inheriting it.
AS3. FREEDOMS-CONSUMED BLOCK corrected and complete under law 2a: w_Phi CARRIED-AS-PARAMETER with its own row; every other row re-checked item-by-item against the formulas (the enforcement standard). FLATTENING CHECK against DECLINE_REGISTER_V002.
AS4. SALVAGE: one-line dispositions of the v2 items — the five confirmed verdicts CITED (Q-695), not re-derived. All headline items CLAIMED. PE-1..7: pointer-known, zero weight, do not consult.
OUTPUT: one sealed artifact `STAGE8_7A_DBR_BETA_SECTORS_V3_DARIO_V001.md`
Final lines: `SECTORS = restated symbolic (table displayed; w_Phi unselected)`, `DOWNSTREAM = <n> statements checked: <n> all-w_Phi / <n> conditional (displayed)`, `RATIOS = w_Phi-independence verified / conditional (displayed)`, `FREEDOMS_CONSUMED = <block incl. w_Phi row>`, `FLATTENING_CHECK = clean / stopped at S<n>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 789_DONE.md, then chained pickup (smallest relay number > 789 addressed to DARIO; if absent, STOP cleanly). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants. No determinant evaluated; no member evaluated alone. No bundle class adopted or eliminated. No w_Phi value selected.
