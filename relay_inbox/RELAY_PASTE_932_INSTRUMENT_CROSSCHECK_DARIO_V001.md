## PASTE 932 — DARIO LANE — [PLAN:AXN-BUILD-D31] CROSS-CHECK THE ENTRY-COMPLETION INSTRUMENT (929)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BUILDER-NEVER-VERIFIES: the subject is the opposite lane's. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit.)
SUBJECT, in workspace/, verify seal BEFORE reading: STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V001.md (c650c578c8625ffba7e1e33713e65a10623adb41444a5bb95f76d76c4ced5046). This is the document the principal's second entry round will run on — the standard is the one you set at 918/921: READY only if actually ready; gaps fixed is ready unless something else is wrong; state the standard BEFORE the findings.
TASK — PRESS IT THE WAY YOU PRESSED V001/V002 OF THE ANCHOR INSTRUMENT: (1) the eight exact receivers — recompute the span digests; do the quoted demands match the instance's actual stopped fields? (2) OPTION-SPACE COMPLETENESS BOTH WAYS: is any LAWFUL option missing (your own 927 survey is the reference — anything it located that the grammar omits?), and is any listed option UNLAWFUL (an option the record actually bars)? (3) the per-option reverse-A2 price drafts — do they price everything authored, and does each price name the pairing it prices? (4) ENUM PROPAGATION — verify the forcing claims: HAAR/DIRAC forced by their certificates; NEITHER surviving only the certified non-control branch; no enum without a pairing+certificate. This decides whether the principal's existing V001 disclosure survives; press it. (5) the dependency/regeneration cascade — can a source-seed digest leak through where the entered joint digest belongs? (6) BLIND ROUTES: can every blind field truly be entered from the opaque pointer records without opening anything rank-shaped? (7) the law-9b empty-space claims — verify each authority table. (8) ASSESS, AS A FINDING, whether the two NEW decision-shaped fields (the stage rule, forbidden-identification; the superselection extension, where-charge-lives — with 930's labelling facts now sealed at STAGE8_AXN_RECORD_LABELLING_DARIO_V001.md) require a SUPPLEMENT to this instrument before the principal's round, or can be presented alongside it — report which, change nothing. ADVOCACY scan the whole document independently.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..13 pointer-only.
OUTPUT: one sealed report `STAGE8_AXN_ENTRY_INSTRUMENT_CROSSCHECK_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `RECEIVERS = 8/8 CONFIRMED / (which fails)`, `OPTION_SPACES = COMPLETE / MISSING (what) / UNLAWFUL-INCLUDED (what)`, `PRICES = CONFIRMED / (which fails)`, `ENUM_PROPAGATION = CONFIRMED / CORRECTED`, `CASCADE = LEAK-PROOF / LEAK (where)`, `BLIND_ROUTES = CONFIRMED / (which fails)`, `LAW9B = CONFIRMED / (which table fails)`, `NEW_FIELDS = SUPPLEMENT-NEEDED / PRESENTABLE-ALONGSIDE`, `VERDICT = READY-FOR-THE-PRINCIPAL / NOT-READY (why)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 932_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
