## PASTE 844 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:AXN-BUILD-A11] EXIT-B: THE CLASS-WIDE VARIATION SCHEME, BUILT AS FAR AS THE RECORD FORCES

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. CLAIM-STATUS GATE: verify `STAGE8_AXN_BUILD_EXITA_HUNT_CODEX2_V001.md` EXISTS mirrored archive-side with CIRCULARITY = CONFIRMED (or apply its corrections); standard wait-once-then-stop. GOVERNING: your own 842 V2 — EXIT-B: promote U1 to a variation scheme DEFINED ON THE ADMISSIBLE CLASS of record-sector integrands, after which the pullback exists and C2..C6 become genuine constraints. This is the VARY half of the sealed derive-and-vary instruction, formalized — build machinery, not an action substitute; T5 untouched. STOP-ON-FREEDOM absolute within it. If 843 lands EXIT-A INHABITED, note it: the scheme then serves as the TESTING instrument for the candidate — build it anyway, scoped to that use.)

TASK: construct the class-wide variation scheme from sealed ingredients — the admissible class as the corrected H1 bindings define it (the cross-check's bindings, not 840's); the proved variation machinery (the two Euler derivatives, sign-and-factor-checked twice; the identity carrier; the 36+882 exact identities); CIS's primitive/effective split carried as stated. Define the scheme's action on a GENERAL class member; derive the pullback of the six corrected constraints through it; display what the pulled-back system IS (the well-posed version of 842's task). Wherever the scheme's definition reaches a genuine freedom the sealed stock does not close: STOP AND DISPLAY (the charter's law). FREEDOMS-CONSUMED block; FLATTENING CHECK. All headline items CLAIMED. PE-1..11 pointer-only.
OUTPUT: one sealed artifact `STAGE8_AXN_BUILD_EXITB_SCHEME_DARIO_V001.md`
Final lines: `SCHEME = defined on the class (displayed) / stopped at freedom (displayed)`, `PULLBACK = derived (the well-posed system displayed) / partial (where it stops)`, `H1_SYSTEM = now poseable / still gated (on what)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 844_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. DERIVE-NEVER-SELECT; stop-on-freedom; charter fences live; no smooth import; no EM identification.
