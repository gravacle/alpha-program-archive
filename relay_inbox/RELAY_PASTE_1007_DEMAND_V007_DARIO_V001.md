## PASTE 1007 — DARIO LANE — [PLAN:DESC-20] DEMAND V007 — THE THREE-LINE RESIDUE SWEEP

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. Full paths; digests-only certificates; the set-level rule; the fixed-point closure.)
SUBJECTS, verify seals BEFORE reading: your V006 (per its sidecar); the confirm (STAGE8_DESC_DEMAND_V006_CONFIRM_CODEX2_V001.md, ea671fdacc4d93fef0377171ac82a7a8bfe778da64ea4570405fd117763deccf, in workspace/). Substance byte-confirmed; digest table exact; 16/16 paths. REMAINS fell on the residue gate alone: Codex's independently fixed pattern set found three lines outside the labeled historical subsection — line 16, and final-certificate lines 528/530 — plus one closure occurrence. Exact loci are in the confirm; take them from there.
TASK — EXACTLY THE SWEEP, NOTHING ELSE: (1) reword line 16 to point at section 6.1 by reference instead of quoting removed-class vocabulary in narration; (2) make the final-certificate region digests-only — the explanation of what was removed lives in 6.1 and nowhere else; (3) remove the closure occurrence the confirm names. The 20,753-byte substantive region (d7a9a08a3dda…) carries BYTE-IDENTICAL — if your sweep touches one byte of it, STOP and write 1007_BLOCKED. Digest table gains one supersession row for V006.
Q-921 RULE, IN FORCE FOR THIS ARTIFACT: a self-run residue grep is CLAIMED, not clean — your 1002 rerun passed these three lines because the pattern set was authored by the hand that wrote the prose. Run your grep, report it as CLAIMED, and do not enumerate the swept vocabulary in your final lines or your DONE — point at 6.1 and at the confirm by digest. The gate settles only on Codex's re-run.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..17 pointer-only.
OUTPUT: one sealed artifact `STAGE8_DESC_DEMAND_DARIO_V007.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `SUBSTANCE = BYTE-CARRIED (20753 bytes, digest)`, `SWEEP = THREE-SITES-PER-CONFIRM + CLOSURE-MEMBER`, `RESIDUE_SELF = CLAIMED-ZERO (opposite-lane gate pending)`, `DIGEST_TABLE = +V006-ROW`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1007_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
