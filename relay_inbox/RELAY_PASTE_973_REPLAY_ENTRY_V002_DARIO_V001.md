## PASTE 973 — DARIO LANE — [PLAN:AXN-BUILD-D72] REPLAY ENTRY V002: THE SCHEMA-VALIDATION STEP + THE DIGEST CASCADE

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. Tuple-parse replay + numeral grep + the full-digest rule.)
SUBJECTS, verify seals BEFORE reading: your 971 (b384c473a338717eb6f351b6c48bcf6ab1ee6c2d76f9d9749e36fe859362bacd); the check (STAGE8_AXN_SUITE_CHECK_CODEX2_V001.md, b2d266579c29d477a88ba21af164f9266c39a6ce119ab6fe044385cc1c824423, in workspace/). EVERYTHING PASSED except one item: the replay-entry procedure lacks the closed-schema validation step. The convention is ACCEPTED as the schema's reading of record; the freeze mechanics stand.
TASK — ONE STEP, THEN THE CASCADE, ALL DISPLAYED: (1) extend the replay-entry procedure with STEP 8: validate the final canonical JSON against the governing closed schema — exact key sets at all three levels, no missing/extra/renamed, value types, const fields byte-exact, EXACTLY ONE inventory variant, canonical-form conformance (the 970/972 defect classes, made mechanical). Deterministic, byte-exact, no clock/randomness/network, same as steps 1-7. (2) THE CASCADE, recomputed in order and displayed old/new at each stage: the new replay-entry object's digest -> the suite's replay_entry_point_sha256 field -> stage-0 bytes and suite_sha256 -> stage-1 bytes and the freeze receipt (the freeze act RE-PERFORMED over the new complete bytes — still pre-output, still lawful) -> final bytes -> CERT-IF and CERT-A rebound to the new staged identity. (3) RUN THE NEW STEP 8 YOURSELF on the final object before sealing — the procedure must pass its own validation, and the run's output displayed. Everything else byte-carried.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. All CLAIMED until the opposite-lane spot-confirm. PE-1..15 pointer-only. BLIND held; basis independence preserved; no numeric evaluation.
OUTPUT: one sealed artifact `STAGE8_AXN_SUITE_INSTANCE_DARIO_V002.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `STEP8 = ADDED (validation classes enumerated)`, `CASCADE = DISPLAYED (each digest old/new)`, `FREEZE = RE-PERFORMED-PRE-OUTPUT`, `SELF_VALIDATION = STEP8-RUN-ON-FINAL: PASS (output shown) / FAIL (fix and rerun)`, `CERTS = REBOUND`, `CARRIED = VERIFIED`, `BLIND = HELD`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 973_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
