## PASTE 995 — DARIO LANE — [PLAN:DESC-11] DEMAND V004 — THE FOUR CUSTODY ITEMS, ONE PASS, THE CHECK'S NUMBERS VERBATIM

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. Full digests; SCOPED grep; fixed-point closure. NOTE THE COST: this is the demand's FOURTH version over documentary items; fold ALL FOUR exactly, verify each against the check's own numbers BEFORE sealing, and leave nothing for a fifth pass.)
SUBJECTS, verify seals BEFORE reading: your V003 (fbf76d210bfc…); the check (STAGE8_DESC_DEMAND_V003_CHECK_CODEX2_V001.md, ba67264055f9191e864e2757a5380a5bdbfe3d5e5104ebb77d8f2f4b047429a1, in workspace/). U12 is CONFIRMED; the register decline is RULED RIGHT; four custody items remain.
TASK — EXACTLY THE FOUR: (1) THE DIFF TRIPLES corrected to the replayed values (V001->V002 = 2/372/347; V002->V003 = 10/158/38) — and RE-REPLAY them yourself against the sealed bytes before writing, so the certificate is of the files, not of memory; the V003->V004 triple computed as a fixed point. (2) THE METHOD NOTE made REPRODUCIBLE: the exact construction command/script displayed (the named-replacement invocation), such that a third party can re-run it on sealed V002 and reproduce V003's non-U12 bytes. (3) THE SET CLAIM fixed: V002->V003 = one row REPLACED (the 989 assignment -> the 993 assignment), stated exactly; "nothing removed" withdrawn in favor of the true set delta. (4) MEMBER 01's PATH: supply the exact existing path (or correct the declared one) so the closure member rehashes — verify by rehashing it. Substance untouched: the 36 rows, grounds, folds, and the U12 full-span carry byte-identical (programmatically, with the invocation displayed per item 2).
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..16 pointer-only.
OUTPUT: one sealed artifact `STAGE8_DESC_DEMAND_DARIO_V004.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `DIFFS = REPLAYED-AND-CORRECT (three triples)`, `METHOD = REPRODUCIBLE (invocation shown)`, `SET_CLAIM = EXACT (one replacement named)`, `MEMBER01 = REHASHES`, `SUBSTANCE = BYTE-CARRIED (invocation-verified)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 995_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
