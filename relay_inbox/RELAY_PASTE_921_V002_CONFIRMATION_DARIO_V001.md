## PASTE 921 — DARIO LANE — [PLAN:AXN-BUILD-C19] THE V002 CONFIRMATION: THE LAST PASS BEFORE THE PRINCIPAL

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. BUILDER-NEVER-VERIFIES. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. SUBJECT: Codex 2's V002 — STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V002.md (58b966ed…), mirrored to workspace/; verify seal BEFORE reading. This is the LAST pass: a READY verdict sends the envelope to the principal.
TASK: (1) THE FOUR REVISIONS AT BYTES: certificate fourteen (the exact replay format, complete per V003's A3 pattern); the blind A0 rank-fiber entry with the rank-ratio guard (your own 918 item 2 — closed in both directions?); the supersession-equivalence certificate (replayed against the adopted act, never silent); the pairing-trace/G6 connection with the control classes named in JAC-03. (2) THE DENOMINATOR CONFLICT, the pass's center: V002 claims 5-core/4-dependent verified at the Q-814/817/818 spans — your own 920 searched the register and found NO 5+4 enumeration anywhere, and the registrar's routed decision (Q-826) is that the ledger cite the VERIFIED NUMERATORS at the Q817-NUM span (S1 direct; S2/S3/S4 unblocked). Resolve at bytes: if V002's cited spans genuinely carry the lists, display them and withdraw 920's finding; if not, the ledger converts to the numerator form — a one-hunk revision you may specify exactly for a V003-of-the-instrument, or rule the numerator conversion applicable at signing time by registrar annotation if the defect is confined to the ledger's denominators and touches no certificate, obligation, or gauntlet content. (3) THE CARRY-FORWARDS: the two strengthenings + limit-square byte-verbatim. (4) Entries still zero; advocacy still zero; the diff's 74 hunks all assigned.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_V002_CONFIRMATION_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `REVISIONS = CONFIRMED-4/4 / CORRECTED (which)`, `DENOMINATORS = SPANS-CARRY-THE-LISTS (displayed; 920 withdrawn) / NUMERATOR-CONVERSION (form specified)`, `CARRY = VERBATIM-CONFIRMED / CORRECTED`, `ENTRIES = 0-CONFIRMED`, `VERDICT = READY-FOR-THE-PRINCIPAL / RETURNED (list)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 921_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
