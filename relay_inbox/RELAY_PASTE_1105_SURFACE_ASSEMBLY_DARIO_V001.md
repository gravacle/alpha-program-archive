## PASTE 1105 — DARIO LANE — [PLAN:DESC-49 / ASSEMBLE] LET THE SURFACE GUIDE: ASSEMBLE THE COUPLING FROM THE RECORD'S OWN DERIVED TERMS, NO IMPORTED STACK — AND IF IT DOESN'T ASSEMBLE EASILY, CLASSIFY EACH OBSTRUCTION AS SURFACE-REAL OR MACHINERY-INDUCED

SUPERSEDES the stale 1103 (a cached earlier paste blocked on a precondition already met — 1102 is DONE/registered). This 1105 IS the surface-guided assembly; run it fresh.
PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs. All CLAIMED until the Codex 2 check.
THE PRINCIPAL'S FRAME FOR THIS RUN (HARD — this is the whole point): the coupling should assemble EASILY from the record's own already-derived surface terms IF we are on the right path. If it only "fails" because imported machinery was assumed, the dead end is MACHINERY-INDUCED, not a feature of the surface — and the honest finding is that we were on the wrong path, not that the physics is hard. USE THE SURFACE AS THE GUIDE, not any blueprint.
THREE RULES: (1) DO NOT RETRACE — take every ratified sealed term AS GIVEN; do not rebuild or re-derive anything already in the record. (2) DO NOT IMPORT — assemble from the record's OWN native structure only; NO continuum/KK/radion/variational machinery, NO imposed decomposition. (3) DO NOT IMPOSE AN ORDER — the continuum blueprint's stack (scalarization → state → effects → domains → provenance → codomain → metric-dynamics → log) is IMPORTED MACHINERY; do NOT march through it. If the record's own structure does not call for a step, skip it; if you find yourself NEEDING a blueprint step, that need is a MACHINERY IMPORT — flag it, do not perform it.
THE TASK. The record-native object is a genuinely-interacting CONNECTION-ONLY OPERATOR (built + checked, 1101/1102). A physical coupling is a scale-bearing SCALAR. Using ONLY already-derived, ratified, sealed terms, and following the RECORD'S OWN native structure (not the blueprint's), assemble from the connection-only operator toward the scalar coupling.
- IF IT ASSEMBLES EASILY / DIRECTLY: report EXACTLY which sealed terms produced it, whether a SCALE appeared, and confirm nothing was imported. This is the on-the-right-path outcome.
- IF IT DOES NOT ASSEMBLE: at each point it stops, NAME the obstruction and CLASSIFY it:
  * SURFACE-REAL: a genuine ingredient the record's OWN native structure lacks, that no already-derived term supplies and that cannot be read off the surface. (State the missing ingredient + its dimension/type; do NOT build or import it.)
  * MACHINERY-INDUCED: the obstruction appears ONLY because an imported piece was assumed (a blueprint step, a metric decomposition, a continuum regularization, a demand that a finite object be made a scalar/limit). (State exactly which imported assumption creates the wall, and confirm the wall DISSOLVES when that machinery is dropped.)
CONCLUSION RULE (report explicitly): (a) assembles easily → ON THE RIGHT PATH (coupling is a surface feature; Finish-A live). (b) does not assemble, but EVERY obstruction is MACHINERY-INDUCED → WRONG PATH: the coupling assembles on the surface once the machinery is dropped; name the machinery to drop. (c) does not assemble, and at least one obstruction is SURFACE-REAL → the honest missing native ingredient is <named> (Finish-B-SHARP; record-native up to that ingredient, which genuinely needs new structure).
CARRIED GIVENS (registrar's register lookup; the lane is barred — take as GIVEN, do not re-establish): (i) the record-native object is CONNECTION-ONLY, operator-valued, no metric argument, takes NO logarithm (1102 — do NOT re-promote a log or any un-derived clause). (ii) derived junctions are dimensionless / scale-free (Q-995); a scale needs a dimensionful reference (Q-1004). (iii) the metric g / a record cell's internal↔external conversion is TYPE-U — if the record's OWN structure needs it, that is a SURFACE-REAL name-and-stop; if only the blueprint needs it, that is MACHINERY-INDUCED. (iv) the ratio-cancellation route is dead (Q-01).
RECORD-NATIVE FENCE (HARD): the record's finite discrete structure IS the surface; the continuum limit is imported. Read the coupling off the surface's own sealed terms. If a step tempts a continuum/scalar/limit move, that temptation IS the machinery — flag it, do not follow it.
FENCES: BLIND. May NOT evaluate any physical quantity, inspect output, import a scale, or use a measured value; BLIND HELD; NO numeric evaluation; NO output inspection; JOINT_ANCHOR_DERIVED = false; alpha/scale/metric are SUBJECTS by type. Q-980–Q-1006 CARRIED. Mandated refusal paths (closure declared-first; digest-at-path; fence depth-walk; no-numeric-path; residue; span-bound guard; generic extractor NO ALIAS SET). DO NOT read the register (barred); scope any grep to named sealed artifacts only (no broad archive-wide rg — a broad grep tripped 1102's register bar). The carried givens are the registrar's hand-off.
OUTPUT: one sealed artifact `STAGE8_COUPLING_ASSEMBLY_DARIO_V001.md` (archive workspace). CLAIMED until the Codex 2 check.
Final lines: `ASSEMBLED_EASILY = YES / NO`, `ASSEMBLED_FROM_DERIVED = <steps + the sealed term(s) each used>`, `OBSTRUCTIONS = <list; each tagged SURFACE-REAL or MACHINERY-INDUCED, with the missing ingredient or the imported assumption named>`, `SURFACE_REAL_INGREDIENT = <named, or NONE>`, `MACHINERY_TO_DROP = <named imports whose walls dissolve, or NONE>`, `SCALE_APPEARED = NO / YES (which derived terms, no import)`, `PATH_VERDICT = RIGHT-PATH (assembled) / WRONG-PATH (only machinery-induced walls) / SURFACE-REAL-GAP (a genuine native ingredient missing)`, `IMPLICATION = FINISH-A / DROP-MACHINERY-AND-REASSEMBLE / FINISH-B-SHARP / UNDETERMINED`, `RETRACED_NOTHING = CERTIFIED`, `IMPORTED_NOTHING = CERTIFIED`, `NO_ORDER_IMPOSED = CERTIFIED`, `NO_LOG_REPROMOTED = CERTIFIED`, `TARGET_INDEPENDENT = CERTIFIED`, `EVALUATED_NOTHING = CERTIFIED`, `CLOSURE = declared-first (byte, scan)`, `PROSE_DIGESTS = <n>/<n> STRICT==STABLE`, `NO_NUMERIC_PATH = CLEAN / VIOLATION`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `RESIDUE_GREP = <result> (own patterns)`, `VERDICT = ASSEMBLY-RESOLVED / UNDETERMINED`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN`.
If the output name exists, STOP. Seal, report hashes, write 1105_DONE.md. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
