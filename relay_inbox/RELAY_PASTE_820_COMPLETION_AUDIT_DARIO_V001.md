## PASTE 820 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:AXN-CONSTRUCT-0] THE COMPLETION AUDIT: HOW MUCH OF THE CONTINUUM COMPLETION ALREADY STANDS?

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. GOVERNING: the principal's challenge — "haven't we already mostly done this?" — against the registrar's unaudited sizing of the parent's continuum completion. The false-boundary standard applies TO THE SIZING ITSELF. R3's sealed verdict: FINITE_CAUSAL_PARENT_DERIVED_CONTINUUM_COMPLETION_OPEN. The anti-substitution fences (T1/T5) remain absolute — this relay SIZES, it does not construct.)

TASK — the audit that replaces the assumption:
AS1. THE DEMAND, ENUMERATED OF RECORD: what "continuum completion of the parent" actually requires — from R3's own artifact, ARCH's sections, and the sealed FALSE flags that name completion-parts (global_source_inclusive_state_limit_derived; the Moller/intertwiner; curved_nonstationary_extension_derived; complete_parent_action_derived; any others the stock names). Each demand span-cited, typed (dynamics / states / algebra / geometry / assembly) — AND, per the principal's classical-costume challenge, typed on a second axis: does the sealed sentence demand a CLASSICAL-SMOOTH continuum completion, or a completion in the RECORD'S OWN LIMIT SENSE (colimit/direct-limit — the E4a/CDL notion)? Read the sentence's words; S26's bar on the smooth constituent and the operator-native parent verdict are context. A demand that types RECORD-NATIVE and is met by the proved colimit stock is SCORED MET, not open — the classical detour ("pretend a classical continuum, translate back") is not a lawful reading unless a sealed sentence explicitly demands it.
AS2. SCORE EACH DEMAND against the proved stock as of today: E4a (PROVED — the dynamics limit); E4b (narrowed — the state limit's promoted branch); ERR/GNS/RNG (the algebra limit to the monomorphism); the spine's proved transport (incidence half Q-702; the exact measure transport; the conservation law; the mapped 1887 family; the bridge's E1-E3); the CDL architecture whole. Verdict per demand: STANDS PROVED / STANDS-ASSEMBLY (proved components need a compatibility statement — name it exactly) / GENUINELY OPEN (the missing derivation named) / FENCED (T1/T5's own content — mark, do not touch).
AS3. THE TRUE SIZE: the completion's remainder as a list — how many items are assembly, how many are open derivations, which of the five map-objects each would discharge, and the honest relay-order-of-magnitude for each item (grounded in comparable completed campaigns from this arc, cited). If the remainder is small, SAY SO PLAINLY; if the registrar's 20-40 sizing was right after all, say that with the items that make it so.
AS4. FREEDOMS-CONSUMED block; FLATTENING CHECK (the anti-substitution rows live). All headline items CLAIMED. PE-1..11 pointer-only.
OUTPUT: one sealed artifact `STAGE8_AXN_COMPLETION_AUDIT_DARIO_V001.md`
Final lines: `DEMANDS = <n> enumerated (typed, spans)`, `SCORE = <n> PROVED / <n> ASSEMBLY (named) / <n> OPEN (named) / <n> FENCED`, `TRUE_SIZE = <remainder list with per-item order-of-magnitude>`, `MAP_DISCHARGE = <which of the five objects each remainder item would discharge>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 820_DONE.md, then chained pickup (smallest relay number > 820 addressed to DARIO; if absent, STOP cleanly). No register, plan, tracker, git action.

GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. SIZING ONLY — nothing constructed, no completion attempted, the T1/T5 fences untouched; no smooth import; no EM identification.
