## PASTE 986 — DARIO LANE — [PLAN:DESC-2] THE DEMAND AT BYTES — WHAT THE REQUIRE ACTUALLY REQUIRES

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. Full digests; numeral grep; fixed-point closure byte; the Q-898 reading rule.)
THE MANDATE: the descent section is open (Q-901). While the opposite lane audits the SUPPLY side, you read the DEMAND side: the exact acceptance shape a descent object must meet — so that when construction starts, it builds against the real receiver and not a paraphrase.
TASK — THE DEMAND AT BYTES, RIGHT-CLOSURE BOTH WAYS (your own 946 discipline): (1) P5's common-origin descent requirement at its span (the ceiling the port cites at STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md:152-180 — read the WHOLE document's demand structure, not the cited window alone); (2) the port's two conditions exactly (choose-or-derive omega_phys WITHOUT OUTPUT INSPECTION — what does the no-inspection clause bar, at bytes; and CERTIFY COMMON ORIGIN — what certificate shape); (3) K7's clause in the governing V004 (descent from the frozen joint common-origin trace — what the clause accepts, what it rejects, the no-provenance-credit boundary); (4) the four-field stop's demand signatures (what each would-build says the built object must BE); (5) the B0-role bar (the frozen trace + four descent maps + certificates + no post-output supplementation — at its sealed bytes); (6) ASSEMBLE: the unified acceptance shape — every condition a descent object must satisfy, each at bytes, with any OVER-demand (a condition no consumer requires) and any UNDER-demand (a consumer condition the list would miss) named; law 9b where a demand's ground is absent. NO construction; the demand only.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..15 pointer-only.
OUTPUT: one sealed report `STAGE8_DESC_DEMAND_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `P5 = quoted (the requirement)`, `PORT_CONDITIONS = 2 quoted`, `K7 = accepts/rejects quoted`, `FOURFIELD_DEMANDS = 4 quoted`, `B0_BAR = quoted`, `ACCEPTANCE_SHAPE = assembled (n conditions, each at bytes)`, `OVER/UNDER = none / (named)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 986_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
