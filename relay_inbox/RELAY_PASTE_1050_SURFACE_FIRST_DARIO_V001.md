## PASTE 1050 — DARIO LANE — [PLAN:DESC-32] SURFACE-FIRST — BUILD THE SOURCE FROM THE PHYSICS; LET IT EXHIBIT ITS OWN SIGNATURE

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs absolutely — in particular: the form and all Q-92(c) machinery are OFF-LIMITS AS SOURCES, and this is the FINAL DERIVATION DEPTH by standing rule.
SUBJECTS — THE SURFACE, verify seals BEFORE reading: the record action on causal cells at its sealed law-side files (resolve from the state brief's canonical roots and TASK2D's own closure — the action, the cell structure, the write geometry); TASK2D in full (the sector derivations — the program's one sustained source of forced results); Gen_Omega WITH overlay V002 and DoR-013 (the channel family {BI,DB,SYM}, A0, and the executable d_state/d_ready/d_law maps); the sealed producers P1–P7 ONLY for their surface content (what physics they describe), never their schema fields; the U2/P5 older receivers for the target's physical description ("a positive normalized rho_pre … all supplied by the same microscopic source"). EXCLUDED AS SOURCES, consulted only in Step 3: PhysicalSig_0's nine fields, SignatureFreezeInputs_0, the port-contract schemas, every freeze/inventory artifact.
STEP 1 — BUILD. Construct, symbolically and upstream-only, the joint microscopic source object THE SURFACE ITSELF PRODUCES: compose what is sealed — the record action's generator structure on the cell complex; Gen_Omega's channel family acting on the source carrier; the sector structure TASK2D forces (source | record | history = B ≅ C(Y) by construction); the descent maps' joint action. The question is not "what fills a form" but "WHAT OBJECT DOES THIS PHYSICS ALREADY DETERMINE when its sealed pieces are composed?" Every composition step: GROUNDED (each ingredient and each composition rule at bytes) or STOP (the missing SURFACE object named, typed, located — never a schema, always a physical ingredient).
STEP 2 — WHAT THE OBJECT IS. If Step 1 yields an object (whole or partial): state its structure as forced facts — its carrier and sectors, its generator/dynamics, its stationary structure (d_state's fixed point and what it extends to), its common-origin content (what "same microscopic source" is now MADE TRUE BY, at bytes). Partial objects are results: state exactly which pieces composed and which did not, and why, at the failing span.
STEP 3 — THE EXHIBITION. Only now open the nine-field form, as a checklist: for each field, does the constructed object EXHIBIT a forced entry — a fact about the built object that the construction determines, read off rather than chosen? Tally per field: EXHIBITED (the fact, its construction trace) / NOT-DETERMINED-BY-THE-BUILD (what about the object is still open) / N/A (the build stopped upstream of it). No entry is written by anything but exhibition.
STEP 4 — THE TERMINAL DISPLAY, whichever branch: SURFACE-WRITES (the object built; k/9 exhibited; what remains, if anything, and its character) / SAME-WALL (the surface's own sealed description is ALSO lower-bound-shaped at the decisive point — display the exact span where the physics itself goes permissive) / MISSING-SURFACE-OBJECT (named, typed, located). This display is terminal for the section's open question by standing rule; write it to be stood on.
HONESTY: no softening; no form-consultation before Step 3 (declare compliance); the PE ledger stays shut; an honest Step-1 stop is a full result. Output-inspection temptation: the strongest yet — you know the nine fields exist; forget them until Step 3, and say in the artifact how you kept them out.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD — every scale symbolic; CASCADE-TERRITORY stop form live. PE-1..17 pointer-only. No member bound (the channel FAMILY acts; no member is selected — DoR-013's clause and the fence agree); no fixed-point execution (d_state's characterization citable); no end test; no measured-constant comparison; no freeze; ω_phys untouched.
OUTPUT: one sealed artifact `STAGE8_DESC_SURFACE_FIRST_DARIO_V001.md` (+ sealed generator, declared inputs, ambiguity-refusing anchors, prose-digest audit strict==stable).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `BUILD = OBJECT-YIELDED (structure) / PARTIAL (composed vs failed, spans) / STOPPED (missing surface object: name, type, locus)`, `COMMON_ORIGIN = MADE-TRUE-BY (bytes) / NOT-REACHED`, `EXHIBITION = <e>/<n>/<x> of 9 (exhibited/not-determined/N-A, each with trace or gap)`, `TERMINAL = SURFACE-WRITES / SAME-WALL (span) / MISSING-SURFACE-OBJECT`, `FORM_QUARANTINE = HELD-UNTIL-STEP-3 (method stated)`, `CASCADE_STOPS = NONE / <steps>`, `PROSE_DIGESTS = <n>/<n>, STRICT==STABLE`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1050_DONE.md, then chained pickup. No register, plan, tracker, git action (read-only included).
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
