## PASTE 1091 — DARIO LANE — [PLAN:DESC-43 / ROUTE-a] THE LEGITIMACY GATE — IS THE CONTINUUM PACKAGE TARGET-INDEPENDENT, OR DOES IT CARRY AN ALPHA-KNOB?

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs. HUNT / ALLOW-REQUIRE AUDIT: AUTHORS NOTHING, ADOPTS NOTHING, binds no member, EVALUATES NOTHING. It audits the FREEDOM STRUCTURE of the continuum package — does it FORCE each ingredient, or ALLOW freedom that reaches alpha. No numeric evaluation. Honest UNDETERMINED (with the exact undecidable ingredient) is a full result.
WHY THIS GATE: the principal has selected ROUTE (a) — convene the continuum ratification — CONDITIONAL on this gate (CONTINUUM_RATIFICATION_ROUTE_DECISION_OF_RECORD_V001, 7b594d3e535b84fc…, verify at path). Route (a) declares a continuum package and computes alpha CONDITIONAL on it, with the blind test as the sole falsifier. That is a DERIVATION only if the package is TARGET-INDEPENDENT — every ingredient fixed by principle, with NO knob that could set or shift alpha. The metric conversion is exactly where the coupling value is NOT forced, so declaring the continuum is one step from declaring the answer; this gate is what separates them.
CARRIED PREMISES (the registrar's register lookup; the lane is barred from the register — these are GIVENS, verify against sealed artifacts you can access): (i) the finite PROVABLY does not force the internal/external metric conversion (the beta gap / refinement bridge / J_ref / O-D3) — an exact NO-GO exists (no linear coframe law transports the measure through refinement); (ii) the free datum in the refinement bridge is located EXACTLY and it is the CONNECTION, not the flux; (iii) the continuum completion stands as a sealed PRINCIPLE, remainder priced. Use (i)-(iii) as context; do not re-derive them.
THE ALLOW/REQUIRE FRAME (Q-958 doctrine, carried): a permission never forces a number; a require-as-lower-bound narrows but does not pin; only a UNIQUE-REQUIRE (a universal property / minimality with fixed input) forces. The package is target-independent iff every alpha-reaching ingredient is a UNIQUE-REQUIRE; any ingredient that is merely ALLOWED (free choice / adopted ansatz / unpinned datum) AND reaches alpha is an ALPHA-KNOB and is disqualifying.
SUBJECTS, verify seals BEFORE reading (archive workspace / supervision): the DoR (7b594d3e…); the GAMMA_K extraction spec (2d63dfad…) — how the package feeds C_record/the root; the 13-item interface split (the member Dario read in 1089 as member 07) and the refinement/response members (the T_ref carriers: physical Ref/J_ref refinement arrows carrying incidence/coframe/connection/volume/density/support; the common-refinement realization; the eta_resp response-naturality square; tau_ker + boundary/contact; the boundary-to-four-volume subextensivity certificate; the coefficient-invariance theorem); T_cyl (430f0971…, if present).
THE GATE — audit the freedom structure at bytes, evaluate nothing:
(1) ENUMERATE THE PACKAGE. List the continuum package's ingredients (the completion absences that route (a) would declare): the J_ref/Ref refinement arrows and their carried data (incidence, coframe, CONNECTION, volume, density, support), the common-refinement realization, eta_resp response-naturality, tau_ker/boundary-contact, the subextensivity certificate, the coefficient-invariance theorem, and the metric conversion itself.
(2) ALLOW vs REQUIRE, PER INGREDIENT. For each, rule: is it a UNIQUE-REQUIRE (fixed by a principle / universal property with fixed input — target-independent), or is it ALLOWED (a free choice, adopted ansatz, or unpinned datum)? Cite the sealed basis.
(3) DOES IT REACH ALPHA. For each ALLOWED ingredient, determine (by type/structure, NOT evaluation) whether its free data reaches the alpha-bearing quantity (C_record / the root / kappa_record). The DANGER FOCUS: the CONNECTION (premise (ii), the located free datum) and the metric conversion — do they reach alpha, and can they be fixed by a target-independent principle, or are they genuine free physics that would have to be declared (and if declared, is the declaration target-independent)?
(4) THE VERDICT. TARGET-INDEPENDENT (every alpha-reaching ingredient is a unique-require; no alpha-knob → route (a) legitimate) / HAS-ALPHA-KNOB (name each allowed ingredient whose free data reaches alpha → route (a) VOID AS A DERIVATION) / MIXED (some knobs fixable by a named principle, some not) / UNDETERMINED (name the undecidable ingredient).
FENCES: HUNT / AUDIT ONLY. Nothing entered/adopted/bound/evaluated; alpha / kappa_record / C_record are SUBJECTS named by type, never evaluated; BLIND HELD, every scale symbolic; NO numeric evaluation of a physical quantity; NO output inspection; NO comparison to any measured value; JOINT_ANCHOR_DERIVED = false. The 1080 void, the 1082/1084/1086 corrections, the 1087/1088 reframe, and the 1089/1090 stitching map are CARRIED. Mandated refusal paths for whatever the artifact seals (closure declared-first; digest-at-path; fence depth-walk; no-numeric-path scan; residue scan; generic extractor with NO ALIAS SET if any symbol block appears). DO NOT read the register (barred); the carried premises above are the registrar's hand-off.
OUTPUT: one sealed artifact `STAGE8_CONTINUUM_TARGET_INDEPENDENCE_GATE_DARIO_V001.md` (archive workspace). All CLAIMED until the Codex 2 check.
Final lines: `PACKAGE_ENUMERATED = <n ingredients, at spans>`, `ALLOW_REQUIRE_TABLE = <per-ingredient require/allow>`, `ALPHA_REACHING_KNOBS = NONE / <named ingredients>`, `CONNECTION_DATUM = UNIQUE-REQUIRE (principle: <…>) / ALLOWED-REACHES-ALPHA / ALLOWED-DOES-NOT-REACH / UNDETERMINED`, `VERDICT = TARGET-INDEPENDENT (route a legitimate) / HAS-ALPHA-KNOB (route a void as derivation: <knobs>) / MIXED (<split>) / UNDETERMINED (<ingredient>)`, `EVALUATED_NOTHING = CERTIFIED`, `CLOSURE = declared-first (byte, scan)`, `PROSE_DIGESTS = <n>/<n> STRICT==STABLE`, `NO_NUMERIC_PATH = CLEAN / VIOLATION`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `RESIDUE_GREP = <result> (own patterns)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN`.
If the output name exists, STOP. Seal, report hashes, write 1091_DONE.md. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
