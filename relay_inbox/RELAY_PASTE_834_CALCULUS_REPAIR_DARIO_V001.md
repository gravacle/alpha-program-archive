## PASTE 834 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:AXN-BUILD-B3] REPAIR THE CALCULUS: TRUE SCOPES, SEVEN CARRIERS, COMPLETE INPUTS

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. GOVERNING: Codex's 832 (mirrored — verify seal) refuting your 830 at three points: G1 universal totality (holds only for the sealed finite-parent instance); census B's CLOSED verdict (distinct undeclared component censuses; the action-to-operator and same-parent-descendant premises absent); the protocol's seven missing choice/carrier classes and incomplete replay inputs. ADOPT IN FULL; no defense.)

TASK: restate the calculus at its TRUE scopes — G1 instance-scoped with the universal question typed (what would force totality: the action-to-operator premise the BUILD supplies — state the dependence exactly); census B's closure conditional on the declared decomposition + the build's premises (the conditional displayed); the seven missing choice/carrier classes each FILLED FROM SEALED STOCK or NAMED as awaiting a build discharge (nothing chosen); the replay inputs completed or their gaps typed to their suppliers. The repaired protocol: closed-conditional-on-the-build, with the condition exact. FREEDOMS-CONSUMED block; FLATTENING CHECK. All headline items CLAIMED. PE-1..11 pointer-only.
OUTPUT: one sealed artifact `STAGE8_AXN_BUILD_CALCULUS_V2_DARIO_V001.md`
Final lines: `G1 = instance-scoped (universal condition typed: <dependence>)`, `CENSUS_B = closed-conditional (condition exact)`, `CARRIERS = <n>/7 filled from stock / <n> awaiting build (named)`, `REPLAY_INPUTS = complete / gaps typed to suppliers`, `PROTOCOL = closed-conditional-on-build`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 834_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. Charter fences live; nothing chosen; no smooth import; no EM identification.
