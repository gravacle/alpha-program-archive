## PASTE 866 — DARIO LANE — [PLAN:AXN-BUILD-A33] THE GENERIC-U1 CENSUS: ROUND 3'S PRIORITY OBJECT

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. CONTEXT OF RECORD: the instance canonicity law is settled (Q-767: even/odd two-block census canonical at the sealed finite parent); Codex's 863 (STAGE8_AXN_BUILD_U2SQ_CONSUMER_IMPACT_CODEX2_V001.md, 40c830a8…, mirrored to workspace/ — verify seal BEFORE reading) adjudicated the seven U2_sq consumers: three eat the instance law now, FOUR are gated on exactly one object — REOPEN ITEM 6, THE GENERIC-U1 SCOPE/PROMOTION PROOF — which has the highest consumer impact in the family (four direct unlocks, seven-site reach). The even-sector splitter unlocks nothing current. This relay censuses the priority object.
TASK A — THE DEMAND AT BYTES. From 859's reopen-condition item 6 (STAGE8_AXN_BUILD_U2_CENSUS_CODEX2_V001.md, 981f1954… — its six-item display) and each of the four universal-gated consumers' own demand shapes (G1/STEP 2, Census B, D_N(t)/G6, the build-wide D-1 decision — 863's byte citations): display precisely WHAT a generic-U1 scope/promotion proof must state — over which parent class, with which uniformity, carrying the grading/projector structure how, and with what equivalence clause. Distinguish sharply: is the demand ONE proof or a conjunction (scope + promotion + equivalence)?
TASK B — MATERIAL CENSUS (law 9 AND law 9b — enumerate every relevant bundle's pinned authority table; state compliance in each absence line). What sealed stock bears on generic U1 under any name: the U-ledger's U1 build-supplied entry and its actual content; the finite parent's grading authority (2215f79c…) and whether its construction is parent-generic or instance-bound; the COMMON-ON-PAIR stratum result (family-level FORCED=0); the transport/order-independence identities ([B_j,B_k]=0); the exact energy transport results; anything typed "for all parents/cells/members" in the build's scope. Classify per component: MATERIAL-PRESENT / PARTIAL / ABSENT.
TASK C — VERDICT. Is a bounded attack startable (the 860/862 pattern: frozen inputs, typed stops, promotion barred beyond what the proof itself licenses)? If YES: display the contract (subjects with hashes, output, stops). If GATED: on what, at bytes. If NO-MATERIAL: the enumeration. Do NOT build; do not promote; a census verdict is not a license.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..12 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_BUILD_GENERIC_U1_CENSUS_DARIO_V001.md`
Final lines: `DEMAND = displayed (one proof / conjunction, split shown)`, `MATERIAL = <per-component classification>`, `GRADING_GENERICITY = PARENT-GENERIC / INSTANCE-BOUND (bytes)`, `VERDICT = ATTACKABLE (contract displayed) / GATED (on what) / NO-MATERIAL`, `LAW9B = complied (tables: n)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 866_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. Charter fences live; nothing selected; no smooth import; no EM identification; no member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants.
