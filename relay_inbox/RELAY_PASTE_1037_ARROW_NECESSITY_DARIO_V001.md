## PASTE 1037 — DARIO LANE — [PLAN:DESC-27] IS THE ARROW A RECORD REQUIRE? — THE IDENTIFICATION'S PROVENANCE AND THE SRF-NATIVE ROUTE

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. Full paths; digests-only certificates; the 1035 citation rule (canonical path + full source digest + half-open span + full span digest) for every span published. The symbolic line governs: structure yes, numbers never.
THE PRINCIPAL'S QUESTION, verbatim of intent: why do we assume a field-factor arrow is needed at the record level at all? Two determinate sub-questions follow, and this relay answers both.
SUBJECTS, verify seals BEFORE reading (workspace/): STAGE8_DESC_B03_DARIO_V003.md (2b3a99bc1c58…); STAGE8_DESC_B03_V003_CHECK_CODEX2_V001.md (2b251a781180…); STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md at [10322,10545) and its wider context; the P0–P7 producers; the six-leg demand spans (P1, P5, v004); the i_src and i_rec loci per the check's census; member 12 only with overlay V002 and DoR-013.
QUESTION 1 — THE PROVENANCE OF THE IDENTIFICATION REQUIREMENT. The clause "author the required carrier identification and typed embedding, or author a new typed embedding" lives in an INSTRUMENT. Run the same provenance test that dissolved the StatePort: is the requirement that A_C0 be identified/embedded into A_SRF_CTP RECORD-DERIVED (trace it to a law-side span — which demand leg, which producer, which theorem forces the identification) or INSTRUMENT-AUTHORED (the instrument wrote it; the record's own demand never mentions A_C0)? Answer with the trace, both directions: what cites the identification, and what each citing span's own ground is.
QUESTION 2 — THE SRF-NATIVE ROUTE. Can the record's actual demand be discharged ENTIRELY on A_SRF_CTP, never touching A_C0? Structurally, at bytes: (a) the six legs — for each, does its receiver live on A_SRF_CTP alone? (b) the pre-state — can a positive normalized rho_pre be built on A_SRF_CTP through its own SEALED port i_src (the census's one live sealed arrow), with the common-origin certificate running through that port? State the construction's shape or its missing ingredient. (c) K4's ω_hist — the record needs a restriction to B; does B reach A_SRF_CTP through i_rec (currently PROPOSED-ONLY)? If yes, the remaining gap is SEALING i_rec — a proposed port signature becoming sealed — not authoring a new arrow; state exactly what sealing i_rec requires under the record's own port rules and whether that act is derivation, ratification of an existing proposal, or new authorship. (d) if the SRF-native route fails somewhere, name the failing span — that failure would be the true record-level require.
THE STAKES, so the reading is honest in both directions: if Q1 = INSTRUMENT-AUTHORED and Q2's route closes, the field-factor arrow dissolves as the sixth frame artifact and the remaining act shrinks to i_rec's disposition. If Q1 = RECORD-DERIVED or Q2 fails at a span, the arrow is a true require and the Q-948 conversation resumes unchanged — say so plainly.
NO AUTHORING, NO CANDIDATE, NO OUTPUT INSPECTION. Nothing sealed, adopted, or identified here; i_rec is examined, not promoted.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..17 pointer-only. ω_phys untouched; no member bound; no freeze; no numeric evaluation.
OUTPUT: one sealed artifact `STAGE8_DESC_ARROW_NECESSITY_DARIO_V001.md` (+ sealed generator with declared inputs).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `IDENTIFICATION = RECORD-DERIVED (trace) / INSTRUMENT-AUTHORED (trace)`, `LEGS_ON_SRF = 6/6 / <exceptions>`, `PRESTATE_VIA_ISRC = SHAPE-STATED / MISSING (ingredient, span)`, `OMEGA_HIST_VIA_IREC = REACHES / FAILS (span)`, `IREC_SEALING = DERIVATION / RATIFICATION-OF-PROPOSAL / NEW-AUTHORSHIP / N/A`, `ARROW = DISSOLVES-AS-FRAME / TRUE-REQUIRE (span)`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1037_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
