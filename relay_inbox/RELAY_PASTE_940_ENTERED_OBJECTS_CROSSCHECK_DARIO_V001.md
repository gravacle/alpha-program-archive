## PASTE 940 — DARIO LANE — [PLAN:AXN-BUILD-D39] CROSS-CHECK THE ENTERED-OBJECTS BUILD (939)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BUILDER-NEVER-VERIFIES: the subject is the opposite lane's build of the PRINCIPAL'S entries — your check is what converts CLAIMED to booked.)
SUBJECT, in workspace/, verify seal BEFORE reading: STAGE8_AXN_ENTERED_OBJECTS_BUILD_CODEX2_V001.md (89aae8a7113098b6b3a6bf282e3df38c2eeae53dca16f51b55aaf7c710344a3b). References: the round-2 decision (bca010f10771…), the READY instrument V002 (af26ab035442…), your 930/932/936.
TASK — PRESS EVERY OBJECT AGAINST BOTH THE ENTRY AND THE MATHEMATICS: (1) THE PAIRING (payload aaa3b217d945…): re-derive the P-HS extension independently — seed at its sealed span, record trace, product-Haar history state, tensor extension law, normalization; reproduce the canonical payload BYTE-IDENTICALLY under the declared canonicalization; verify FAITHFULNESS on the pinned Delta_0^joint — press it: is the extended pairing actually faithful there (the record trace and Haar state both faithful, tensor product faithful — verify, do not assume)? (2) E_JOINT: replay all four certificates; press the charge-diagonal fixed-space one hardest (it is where the substantive-labelling risk would bite); confirm the Q-838 disclosure binds every extensible consumer. (3) THE HAAR EQUIVALENCE: re-derive the history restriction of the built pairing and replay the equivalence certificate — this is the certificate that supersedes the principal's round-1 enum; it must be exact, not plausible. Verify the FrozenEnumDisposition instance leaves both round-1 artifacts byte-untouched (rehash them). (4) THE POINTERS: all four records against their sealed spans; the S-TENSOR domain/codomain proof re-derived; blind custody audited by endpoints. (5) THE PRICE: regenerate the eight-draft concatenation from the instrument independently; byte-compare. (6) THE MANIFEST: 18/18 re-parse and reproduce. STOP-grade findings only where a defect would corrupt a principal act or a certificate's warrant; packaging nits are findings, not blocks — your own 936 standard.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD: no rank, dimension, ratio, or fiber comparison. PE-1..13 pointer-only.
OUTPUT: one sealed report `STAGE8_AXN_ENTERED_OBJECTS_CROSSCHECK_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `PAIRING = CONFIRMED-FAITHFUL / CORRECTED (what)`, `PAYLOAD = REPRODUCED / DIVERGES`, `EJOINT_CERTS = 4/4 REPLAYED / (which fails)`, `HAAR_CERT = EXACT-CONFIRMED / FAILS (why)`, `ROUND1 = BYTE-UNTOUCHED-VERIFIED`, `POINTERS = 4/4 / (which fails)`, `PRICE = BYTE-MATCHED / DIVERGES`, `MANIFEST = 18/18`, `VERDICT = OBJECTS-BOOK / REMAINS (what)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 940_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
