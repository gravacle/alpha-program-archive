## PASTE 1073 — CODEX 2 LANE — [PLAN:DESC-24] THE ARM-B CLOSURE POINTER — THE THIRD SUPERSESSION, EXECUTED

PICKUP-ACK + LANE GUARD in force (CODEX 2). CHAINED PICKUP applies (1068 remains queued behind this, guarded on the delivery mirror). CLEANROOM-SIDE writes only; the registrar mirrors. CLOSURE_MEMBER_CITATION_RULE_V001; prose-digest audit strict+stable; citation-resolution discipline (the Q-978 standard); no git of any kind.
SUBJECTS: your consumer re-runs artifact (STAGE8_DESC_CONSUMER_RERUNS_CODEX2_V001.md, 43b1c0cb07fc…) — supersession (3)'s specification; your Diagnosis Arm B check (STAGE8_DESC_DIAG_B_CHECK_DARIO_V001.md — resolve at its sealed location, verify sidecar; NOTE: that artifact is Dario's; the closure POINTER defect lives in YOUR Arm B artifact's consumption chain per the specification — read the spec's exact wording and act on the artifact it names); audit V002 (078d6d54a259…) and V003 (c8afae3333de…) read-only.
TASK — EXECUTE EXACTLY SUPERSESSION (3) as your 1067 specified it: an append-only V002 of the affected artifact repointing its closure from the defective V002 decline to the V003 discharge — the pointed-to rows quoted by digest under the citation rule; everything else carried byte-identically by span; a citation-resolution pass over the result (all member references resolve, none rebound — the V007 lesson applied). If on re-reading the specification the repair properly belongs to the other lane's artifact, write that finding and STOP rather than crossing the builder line — a mis-assigned repair is a finding, not a task.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..17 pointer-only. ω_phys untouched; the Attach thread not consumed; no numeric evaluation; no member bound.
OUTPUT: one sealed artifact (V002 of the affected artifact, named per its predecessor + `_V002`).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `POINTER = REPOINTED (old → new, by digest) / MIS-ASSIGNED (finding, stopped)`, `CITATIONS = ALL-RESOLVE`, `CARRIAGE = SPANNED`, `PROSE_DIGESTS = <n>/<n>`, `RESIDUE_GREP = <result> (own patterns)`, `VERDICT = SUPERSESSION-EXECUTED / FINDING-STOPPED`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1073_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
