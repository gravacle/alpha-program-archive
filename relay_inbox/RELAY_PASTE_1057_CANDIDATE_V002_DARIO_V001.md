## PASTE 1057 — DARIO LANE — [PLAN:DESC-33] CANDIDATE V002 — SIX REPAIRS; THE CIRCUIT IS THE SUBSTANTIVE ONE; DERIVE OR DISPLAY

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs (symbolic line; citation rule; prose-digest audit strict+stable; ambiguous-anchor refusal; no git of any kind). The mandate (ad9fc14e1f07…) remains this thread's authority — verify it live; the generator refuses without it.
SUBJECTS, verify seals BEFORE reading: your candidate V001 (workspace/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V001.md, 8c257818b55c…) and its check (workspace/STAGE8_ATTACH_CANDIDATE_CHECK_CODEX2_V001.md, 617e0144c0ad… — the six entry-blocking deltas at their spans); the acceptance registry (85514b04912f…); the cell-complex sealed sources the check cites on the K₁,₃ finding; member 20 :159–165 (the slot identity, now both-lane).
TASK — V002, append-only, V001 byte-untouched, EXACTLY THE SIX:
(1) THE CIRCUIT — the substantive repair, and it is derive-or-display, never choose: the check finds the first-opening cell is K₁,₃ (a tree) with later loops admissible but unselected. Determine at bytes whether the record's sealed cell-growth/write dynamics FORCE the existence of a loop-bearing cell in every admissible history (a derivation: the circuit exists wherever the candidate applies — exhibit it), or admit a lawful DOMAIN RESTRICTION stated from the record's own structure (the candidate applies on the loop-bearing sector, with the tree sector's disposition stated — a restriction is lawful if the record types the sectors; a SELECTOR is not yours to freeze). If neither: display the gap precisely — that display becomes an entry-relevant disclosure, not a silent hole.
(2) THE REPRESENTATION BRIDGE — supply it from sealed grounds or add it to the AUTHORED table with justification (the ledger grows honestly rather than the claim overreaching).
(3) THE COMPARATOR EMBEDDING — make it exact or table it as authored, same rule.
(4) THE INTRA-CELL TIME-ORDERING CARRIER — the physical carrier the check found absent: forced, authored, or displayed as open with its consequence stated.
(5) THE CLOSED CLAIM REPAIRED — the five escaped symbols (J, g, the representation bridge, the comparator embedding, physical time-ordering) enter exactly one table each; then restate the closed claim and re-invite the test.
(6) THE RECOUNT AND THE ANCHOR — the authored-core count restated per the post-repair tables (whatever it now is, said plainly — if the core grew from one, the headline changes and says so); the O4 tail anchor recited correctly.
CARRY: the registry's 2 FAIL items addressed within the above (they overlap the bridge and time-ordering); the 2 NOT-YET items stated as awaiting the completed object; AC6 stays open and honest. NO OUTPUT INSPECTION — unchanged, absolute; the circuit question especially must be answered from cell dynamics, never from what downstream wants.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD — every scale symbolic. PE-1..17 pointer-only. No member bound; no fixed-point execution; no end test; no numeric evaluation; no measured-constant comparison; no freeze; ω_phys untouched. Header verbatim: `PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL ENTRY (ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001, ad9fc14e1f07…)`.
OUTPUT: one sealed artifact `STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V002.md` (+ sealed generator; V001 superseded append-only).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `CIRCUIT = FORCED-EXISTS (derivation) / DOMAIN-RESTRICTED (record-typed sectors) / GAP-DISPLAYED`, `BRIDGE = FORCED / AUTHORED / OPEN`, `EMBEDDING = EXACT / AUTHORED`, `TIME_ORDERING = FORCED / AUTHORED / OPEN (consequence)`, `CLOSED_CLAIM = RESTATED-ALL-SYMBOLS-TABLED`, `CORE_COUNT = <n>-AUTHORED (headline updated if changed)`, `O4_ANCHOR = RECITED`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `PROSE_DIGESTS = <n>/<n>`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1057_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
