## PASTE 839 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:AXN-BUILD-A6] THE CLOSURE ROUND: THE BINDING PREDICATE AS THE SOLE FORCING INSTRUMENT

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. GOVERNING: your V2 package (STAGE8_AXN_BUILD_SKELETON_V2_DARIO_V001.md — verify seal) AND ITS CROSS-CHECK GATE: verify that `STAGE8_AXN_BUILD_SKELETON_V2_CROSSCHECK_CODEX2_V001.md` EXISTS mirrored archive-side with PACKAGE = complete (or corrections you must first apply); if absent at pickup, note it in the ACK, pause, re-check once, and STOP CLEANLY if still absent — no consumption before upgrade. Also governing: 836's resolution — T01 is NARROWED to the displayed pair; FORCED = 0 at family level; "derive the packet-binding stratum first." THE RECORD HAS CLOSED EVERY SHORTCUT: the BINDING PREDICATE (the parent-reproduction condition, 833-verified) is the build's sole forcing instrument.)

TASK — the first closure round:
AS1. THE STRATUM, BY BINDING: apply the binding predicate to the four exhibited completions (S_0, S_1, S_rot, S_BF): which satisfy the parent-reproduction clauses on the sealed packet parent? The PHYSICAL stratum is whatever the predicate selects — derived, not intersected. Display per-member verdicts with the computation.
AS2. THE HOLES, UNDER PRESSURE: for each of the six holes (T02's record functional; the update slot; BOX_gravity; the remaining three per the package): apply the binding predicate + the composed falsifier interface as constraints — what shape does each hole's content HAVE TO have for the variation to reproduce the sealed parent and survive F1-F7? VERDICT PER HOLE: CLOSED-FORCED (the pressure pins it — derive and display) / NARROWED (the shape shrinks — new exact shape displayed) / UNMOVED (the predicate does not reach it — why, exactly) / RULING-SHAPED (a genuine freedom survives all pressure — display it; RULING-LAST's three passes then run). For BOX_gravity specifically: the disqualification span bars the KK re-import; what the binding pressure says the gravitational functional must be — if the pressure forces it toward the conversion/obstruction structure, DISPLAY THAT DERIVATION (PE-9 pointer-known, zero weight, not consulted — the derivation stands on the spans alone).
AS3. THE ROUND'S LEDGER: census after pressure (forced/narrowed/unmoved/ruling-shaped); what U1 inherits (the action-to-operator map's status); what round two needs. FREEDOMS-CONSUMED block; FLATTENING CHECK. All headline items CLAIMED (Codex checks next). PE-1..11 pointer-only.
OUTPUT: one sealed artifact `STAGE8_AXN_BUILD_CLOSURE_ROUND1_DARIO_V001.md`
Final lines: `STRATUM = predicate-selected: <members> (per-member verdicts displayed)`, `HOLES = <n> CLOSED-FORCED / <n> NARROWED / <n> UNMOVED / <n> RULING-SHAPED`, `BOX_GRAVITY = <verdict + what the pressure says>`, `U1 = <status>`, `ROUND2 = <needs>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 839_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. DERIVE-NEVER-SELECT absolute; the KK re-import barred; charter fences live; no smooth import; no EM identification.
