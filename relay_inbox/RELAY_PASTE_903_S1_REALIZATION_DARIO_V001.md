## PASTE 903 — DARIO LANE — [PLAN:AXN-BUILD-C1] THE S1 REALIZATION: BUILD THE BASE FAMILY AGAINST ITS SETTLED SYSTEM

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position self-audit. PHASE OF RECORD: the basis campaign (Q-805/807/808) — the stop rule governs; this is BASIS WORK, the productive kind. The acceptance system is SETTLED BOTH WAYS (Q-792/794): seven constraints K1-K7; the exact conditional quotient with its affine lift fiber; four named input interfaces — the prequotient measurable carrier/extension class; the measure-normalization functional; the covariance/boundary/contour/domain actions with common-origin descent; the amplitude-to-event reproduction theorem. The Haar corollary and the Dirac satisfiability control stand as calibration. SUBJECTS, seal-verify BEFORE reading: the S1 pair (e857302b…, 3c803cda…); the OscCyl amplitude sources at their pins; the C7 source; the wave-0 freeze documents (in supervision/ — lambda_spin and K_R are FROZEN; consume as sealed).
TASK — CONSTRUCT, AGAINST THE TEST, DERIVING EVERY STEP: attempt the four interfaces IN ORDER: (1) THE CARRIER/EXTENSION CLASS — derive it from the amplitudes' own measurable structure (the cylinder sets the OscCyl data generate; the extension the C7 sections force); nothing imported, nothing chosen: if the amplitudes' structure determines the class, display the determination; if a choice intrudes, STOP there and name it. (2) THE NORMALIZATION FUNCTIONAL — from K6's type discipline (pointwise vs measure normalization kept distinct) and the amplitudes' own normalization data. (3) THE COVARIANCE/DESCENT ACTIONS — from the sealed relabeling/covariance stock (the 15-authority table's content; the common-origin trace's discharged port). (4) THE REPRODUCTION THEOREM — the statement that the constructed family reproduces the amplitudes; prove it on the finite anchor first (the Dirac control's scope), then at the family's declared scope. AT EVERY STAGE: check against K1-K7; any conflict is a STOP with the clause named. If all four land: display the family, run the full acceptance test (all seven clauses + the Haar corollary as the negative control), and state the residual quotient's fate (canonical representative derived, or the multiplicity displayed — WHICH WOULD CREATE A SELECTOR per Q-807's B1 caution: if so, say so plainly for the principal). VERDICT (typed): REALIZED-AND-ACCEPTED / PARTIAL (interfaces landed + the stop) / BLOCKED (the underivable input named).
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_S1_REALIZATION_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `INTERFACES = <n of 4 landed, per-interface>`, `ACCEPTANCE = PASSED (7/7 + control) / PARTIAL / NOT-REACHED`, `QUOTIENT_FATE = CANONICAL-DERIVED / MULTIPLICITY (selector flagged) / N/A`, `VERDICT = <typed>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 903_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live; no smooth import; no numeric evaluation of physical quantities.
