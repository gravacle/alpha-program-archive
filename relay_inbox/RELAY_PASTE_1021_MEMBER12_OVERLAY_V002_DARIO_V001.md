## PASTE 1021 — DARIO LANE — [PLAN:DESC-21] OVERLAY V002 — THE SIXTEEN SURFACES THE TOKEN MISSED

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. Full paths; digests-only certificates; declared conventions; the fixed-point closure.
SUBJECTS, verify seals BEFORE reading: your overlay V001 (workspace/MEMBER12_HEADER_OVERLAY_RECORD_V001.md, per its sidecar); the sweep (workspace/MEMBER12_SEMANTIC_SWEEP_CODEX2_V001.md, 9a9eb5a5226143936e7ac3be22f07b7f9e399a04fda23c4240b8de4a4ab3b1af); member 12 READ-ONLY, must rehash 1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0 (37,157 B) — if not, STOP with 1021_BLOCKED.
THE FINDING: your step-to-press was right and then some — the property sweep found 26 stale status surfaces; your 10 token pins cover 10; SIXTEEN token-free or differently worded surfaces lie outside pinned lines. The sweep displays every span, digest, line, and disposition. Codex also ruled your read rule EXACT and your one fenced residue hit lawful (its own scan: 1 fenced lexical, 0 operative).
TASK — OVERLAY V002, EXTENSION ONLY: take the sixteen spans FROM THE SWEEP BY DIGEST (extract each span from member 12 and match the sweep's span digest before pinning — the same eight-bytes-not-a-description discipline as 1013); add them to the pinned set under the same stated pinning convention (or a declared variant where a surface is not line-shaped — declare, don't improvise); carry the ten V001 pins and the read rule unchanged; V001 superseded append-only, byte-untouched; member 12 byte-untouched, digest before and after. The sweep's exclusion rulings (historically accurate proposal-phase labels) carry as its rulings, cited, not re-adjudicated.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..17 pointer-only. ω_phys untouched.
OUTPUT: one sealed artifact `MEMBER12_HEADER_OVERLAY_RECORD_V002.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `MEMBER12 = BYTE-UNTOUCHED (digest)`, `PINS = 26/26 (10-CARRIED + 16-ADDED-BY-DIGEST)`, `READ_RULE = CARRIED-UNCHANGED`, `V001 = SUPERSEDED-APPEND-ONLY`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1021_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live. No member bound.
