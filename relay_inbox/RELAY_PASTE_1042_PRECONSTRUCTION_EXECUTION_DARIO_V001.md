## PASTE 1042 — DARIO LANE — [PLAN:DESC-29] THE SIX PRECONSTRUCTION ROWS — COMPLETES-ZERO IS NOT SPECIFIES-ZERO; EXECUTE OR NAME THE HOLE

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1039 bootstrap's symbolic line, citation rule, and self-citation bar govern. GUARD: verify that the decision-of-record on the demand reading exists in workspace/ (the registrar seals it as DEMAND_READING_DECISION_OF_RECORD_V001.md after the principal rules) — verify its sidecar and read it BEFORE task work; if absent, write 1042_BLOCKED and STOP.
SUBJECTS, verify seals BEFORE reading: your P0 execution (STAGE8_DESC_P0_EXECUTION_DARIO_V001.md, c554c481d64f…) and its check (per the registrar's mirror when sealed); the SIX Q-92(c) preconstruction rows at their sealed source, resolved from your execution's Step-1 locus citation; member 06's provenance ceiling [18039,18385); DoR-008 at its spans; Gen_Omega only WITH overlay V002 and DoR-013; member 04's Core_0 typing [44072,44269).
THE TASK: the same question that opened the pipeline, one level down. Obj_0 — the joint microscopic law/operator/dynamics object — is typed at the six preconstruction rows, of which DoR-008 completes ZERO. But completes-zero is not specifies-zero. For EACH of the six rows, in the record's own order: read the row's would-build at bytes; determine whether it is EXECUTABLE from sealed grounds (execute it — construction trace, every ingredient cited), DERIVABLE-BY-COMPOSITION from already-executed rows plus sealed objects (exhibit the composition), or HOLE (the missing datum named, typed, located — the PE-17 display standard). Rows may consume earlier rows' outputs; respect the record's own dependency order and DECLARE it as read.
THEN, AGAINST WHATEVER EXECUTED: (a) does the executed subset assemble Obj_0 in whole or in part — state the assembly's sector reach and what Core_0 it yields (member 04's warning in force: Core_0 is not automatically P1's completed carrier — do not conflate); (b) does it ground Θ_joint : (common-origin data) → State(A_C0), in whole or in part — if a state-valued output appears, its certificate shape per the demand's conditions; (c) the provenance ceiling — if the executed subset supports a common-origin claim the premises declined, say EXACTLY what changed hands (the ceiling was a disclaimer of what DoR-008's premises claim, not a bar on what a construction proves — read it again at bytes and rule whether construction can lawfully exceed it).
HONESTY CONSTRAINTS unchanged: no softening, no prose bridges, no expectation-shaped dispositions (the PE ledger stays unopened); an honest one-row result is a full result; the output-inspection temptation at the closing step is named in advance — decide each row at its own grounds.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD — every scale symbolic. PE-1..17 pointer-only. No member bound; no fixed-point execution; no end test; no numeric evaluation; no measured-constant comparison; no freeze; ω_phys untouched.
OUTPUT: one sealed artifact `STAGE8_DESC_Q92C_EXECUTION_DARIO_V001.md` (+ sealed generator with declared inputs; refusal paths declared).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `ROWS = <e>/<d>/<h> (executed/derived/holed, in order, each with locus)`, `OBJ0 = ASSEMBLED / PARTIAL (sector reach) / NOT-REACHED`, `THETA_JOINT = GROUNDED (certificate shape) / PARTIAL / NOT-REACHED`, `CEILING = EXCEEDED-LAWFULLY (what changed hands) / BINDING (why) / N/A`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1042_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
