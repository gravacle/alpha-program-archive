## PASTE 871 — DARIO LANE — [PLAN:AXN-BUILD-A38] PROVE THE UNIFORMITY THEOREM (CANONICITY OVER THE RECORD'S OWN CLASS)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. BUILDER-NEVER-VERIFIES. GOVERNING SUBJECT: Codex 2's 870 — STAGE8_AXN_BUILD_GATED_DEMAND_RETYPING_CODEX2_V001.md (e681754f…), mirrored to workspace/; verify seal BEFORE reading. It rules: 4 of 6 gated consumers UNIFORMITY-SATISFIABLE, 0 purely instance-demanding; 866's C1 gate RETYPED UNIFORMITY ("proof over generic U1", "every admitted U1 output" — no declared A_c demanded); and IT STATES THE REQUIRED THEOREM — over the predicate-defined record class cut by C1/SC3 support, typed membership where supplied, the carrier-neutral D1/D2/D3, typed operator/domain, the ADOPTED-BRANCH GRADING ANTECEDENT, and the admitted equivalence action — with 866's conditional C2+C3 observation BECOMING-THE-THEOREM once the class is the record's own quantifier domain. Q-774 REMAINS BINDING: the branch hypotheses are ANTECEDENTS, never derived facts; outside-branch competitors stay live and named.
TASK — PROVE IT, EXACTLY AS STATED, NOTHING WIDER: for every member of the predicate-defined class (the quantifier domain as 870 states it, each cutting constraint cited at its sealed bytes), WITHIN THE ADOPTED BRANCH AS ANTECEDENT: (a) the even/odd projector census EXISTS (the grading extends per the sealed tensoring rule; the projectors are functions of the member's operator and the sealed grading); (b) it is UNIQUE (no member of the class admits a second census satisfying the same sealed conditions — or display the residual multiplicity precisely); (c) it is COVARIANT under the admitted equivalence action (the Z2 behaviour of Q-767 generalized, or its failure located). DERIVE-NEVER-SELECT; stop-on-freedom absolute — if any step needs an object the class predicate does not supply, STOP there and display it. The finite parent (Q-767) is your anchor instance: the proof must reduce to it on that member. State the theorem's consumers on success: the four uniformity-satisfiable sites by name; the two MIXED sites' theorem-shaped conjuncts (their instance conjuncts expressly NOT touched); scope cap: nothing outside the adopted branch, no promotion of the branch itself.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK (law 9b closure-first for any absence). All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_BUILD_UNIFORMITY_THEOREM_DARIO_V001.md`
Final lines: `THEOREM = PROVED (displayed, anchor-reduction shown) / PARTIAL (which clause; residual displayed) / STOPPED (at what; bytes)`, `EXISTENCE = proved / stopped`, `UNIQUENESS = proved / residual multiplicity displayed`, `COVARIANCE = proved / failure located`, `CONSUMERS_CLEARED = named / N/A`, `BRANCH = ANTECEDENT-HELD (no promotion)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 871_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. Charter fences live; nothing selected; no smooth import; no EM identification; no member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants.
