## PASTE 1001 — DARIO LANE — [PLAN:DESC-17] B01 V002 — THE THIRTY-TWO-ROW INVENTORY

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. Full paths; minimal-true certificates per the Q-913 standard; the set-delta rule; generator sealed and run pre-seal.)
SUBJECTS, verify seals BEFORE reading: your 997 (6558834ac75f…); the check (STAGE8_DESC_B01_CHECK_CODEX2_V001.md, 8c3e241bb06c8874fce23c3d30c136a75b4c624904df0c41d47f0c581057be1c, in workspace/). The check CONFIRMS your six rows, the boundary, the stop, and the forward obligation — and RULES the spine ADMIT-WITH-STATUS: A02's "frozen" governs membership/timing, not tested-status.
TASK — B01 V002: (1) add the 26 spine-class rows from their sealed source — each carrying the five A02 fields PLUS its status AS THE SOURCE STATES IT (untested stays untested; nothing promoted); the generator extended and sealed, run pre-seal, its table byte-identical to the artifact's; (2) the six original rows byte-carried; the boundary statement updated to the 32-row membership with the same exclusions otherwise; (3) the exhaustiveness stop and the B02 forward obligation carried UNCHANGED (the ruling did not touch them); (4) minimal-true certificate: digests + the set delta (6 retained / 26 added / 0 dropped — computed before written). BLIND held; every scale symbolic.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK (26 untested rows must read as untested everywhere — no status may flatten). All CLAIMED until the opposite-lane spot-confirm. PE-1..17 pointer-only.
OUTPUT: one sealed artifact `STAGE8_DESC_B01_DARIO_V002.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `ROWS = 32 (6 carried + 26 added, statuses as-sourced)`, `GENERATOR = SEALED-AND-RUN (byte-identical)`, `BOUNDARY = UPDATED`, `STOP+OBLIGATION = CARRIED-UNCHANGED`, `SET_DELTA = 6/26/0`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1001_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
