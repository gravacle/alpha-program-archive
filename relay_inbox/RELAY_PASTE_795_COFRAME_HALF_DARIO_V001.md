## PASTE 795 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:B1a-5] THE COFRAME HALF AND O1: FINISH THE CARRIER'S NAMED REMAINDER

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL alignment laws apply incl. 8 (probe all three sealing modes). GOVERNING: your own 788 (artifact 97f073c1…) — the incidence half is delivered; NOT delivered and named: (1) the coframe half — O3's declared F'/F square, where V011's only "coframe" spans are S26-BARRED smooth-side, and the vacuous-by-construction F' := D_(e')^{-1} xi' was rightly DECLINED; (2) O1's L_id = id and per-generator existence clauses, with A0 exercised only by type. The 794 cross-check of the incidence half runs lane-opposite; do not re-derive what it will judge.)

TASK — the carrier's remaining half, from sealed record-side stock only:
AS1. THE COFRAME HALF: the sealed record-side coframe stock is YOURS (SIMPLICIAL_COFRAME 753, the forced-denotation addendum, support-and-forcing 755, the coframe-closure lineage). Determine what the F'/F square can lawfully be: (a) DERIVED — a record-side refinement law for the closed coframe denotation exists or is forced by the sealed closure (display it and instantiate the square on A0/A1/A2); (b) FREE — the record admits inequivalent F'/F relations (two complete candidates displayed, neither adopted); (c) ABSENT — the record-side stock cannot pose the square without the S26-barred smooth constituent (name exactly what is missing and STOP — a lawful stop; the vacuous construction remains declined).
AS2. O1's CLAUSES: L_id = id and per-generator existence — prove them on the delivered incidence half (constructing A0 concretely, not by type), or display the failing clause.
AS3. If AS1 lands DERIVED: state whether the completed carrier (incidence + coframe halves) changes the 788 §3.3 min-norm lead's standing — does the coframe half add a constraint that bears on naturality? (Statement only; the naturality TEST is 794's, lane-opposite.)
AS4. FREEDOMS-CONSUMED block (item-by-item true; law 2a); FLATTENING CHECK against DECLINE_REGISTER_V002 (S26 live). All headline items CLAIMED. PE-1..7: pointer-known, zero weight, do not consult.
OUTPUT: one sealed artifact `STAGE8_B1A_COFRAME_HALF_DARIO_V001.md`
Final lines: `COFRAME_SQUARE = DERIVED (instantiated A0/A1/A2) / FREE (two candidates displayed) / ABSENT (missing element named; stopped)`, `O1 = proved (A0 constructed) / failing clause displayed`, `MINNORM_BEARING = statement / n-a`, `FREEDOMS_CONSUMED = <block>`, `FLATTENING_CHECK = clean / stopped at S<n>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 795_DONE.md, then chained pickup (smallest relay number > 795 addressed to DARIO; if absent, STOP cleanly). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants. No smooth import (S26); no EM identification (S08); no metric adopted; nothing selected by outcome. No common cell formed; no junction map evaluated.
