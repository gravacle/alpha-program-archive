## PASTE 873 — DARIO LANE — [PLAN:AXN-BUILD-B1] THE ASSEMBLY: COMPOSE THE CANDIDATE RECORD-SECTOR ACTION ON THE PROVED SUBCLASS

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. PHASE OF RECORD: the hunt/census phase is CLOSED at the determinacy boundary; THE ASSEMBLY PHASE is OPEN by principal decision (Q-778; plan change log updated). This is the phase's first relay.
THE CHARTER: on the PROVED finite-incidence subclass (Q-777's existence domain), COMPOSE the candidate record-sector action from DERIVED PIECES ONLY. The pieces, each seal-verified BEFORE consumption, each carried at its sealed scope: the carrier-neutral stabilizer triple D1/D3 with D2's conditional redundancy (Q-753/755 artifacts); the graded superconnection form D_BR = i gamma^mu nabla_mu (x) I_3 + gamma^5 (x) c_partial WITH ITS SEVEN BRANCH HYPOTHESES AS ANTECEDENTS (Q-774 artifact — never derived, competitors live); the gauge-family-invariant even/odd census with its projectors (Q-767/777 artifacts); the boundary TARGET specification (the CTP vanishing form, carried as TARGET, wrong generator/scale disclosed — Q-750/752); SC3 support typing; C1 membership; chi_P = 0 (the decided hole); the exact energy transport identities and [B_j,B_k]=0 order-independence as sealed.
TASK — COMPOSE AND DISPLAY: (1) THE SKELETON: the candidate action as a sum over the finite-incidence member's cells, EVERY TERM carrying a provenance tag — DERIVED (with its register/artifact citation) / ANTECEDENT (branch hypothesis, named) / TARGET (boundary form, not installed) — and EVERY UNFORCED WEIGHT SYMBOLIC (law 2a: no scaling weight chosen, each displayed as a named freedom). (2) CONSISTENCY: what conditions does COMPOSITION itself impose — cross-term constraints, parity bookkeeping between the census blocks and the triple's clauses, support/overlap conditions from D1/D3 meeting the operator's cross terms (the corrected 849/851 algebra), boundary-term interactions with the target spec? Each displayed as NEW-FORCING (derived from the composition) or NEW-FREEDOM (exposed by it). (3) THE VARIATION, POSED: with the assembly in hand, restate the H1 system on this subclass — what the stationarity condition reads as, term by term, and precisely which pieces block its execution (the instance conjunct; the physical halves) at their named receivers. (4) LACKS: the itemized list of what the assembly still cannot say, each at its receiver. DERIVE-NEVER-SELECT; STOP-ON-FREEDOM ABSOLUTE — the assembly composes what exists; it authors nothing; if composition demands an object of record that does not exist, STOP there and display it.
FREEDOMS-CONSUMED block (law 2a — expect it to be the artifact's largest block; that is honest); FLATTENING CHECK (law 9b closure-first). All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_ASSEMBLY_FINITE_DARIO_V001.md`
Final lines: `ASSEMBLY = COMPOSED (skeleton displayed, provenance per term) / BLOCKED (at what)`, `TERMS = n (n DERIVED / n ANTECEDENT / n TARGET)`, `NEW_FORCINGS = displayed / NONE`, `NEW_FREEDOMS = displayed / NONE`, `H1_POSED = restated (blockers at receivers) / NOT-POSEABLE (why)`, `LACKS = itemized`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 873_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. Charter fences live; nothing selected; no smooth import; no EM identification; no member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants.
