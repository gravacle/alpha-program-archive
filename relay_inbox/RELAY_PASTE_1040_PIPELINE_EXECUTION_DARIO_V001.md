## PASTE 1040 — DARIO LANE — [PLAN:DESC-28] EXECUTE THE RECORD'S OWN PIPELINE — UNBUILT IS NOT UNSPECIFIED; BUILD OR NAME THE HOLE

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1039 bootstrap's symbolic line, citation rule, and self-citation bar govern every step.
SUBJECTS, verify seals BEFORE reading (workspace/ and per closure resolution): the P0–P7 producer specs at their sealed files; DoR-008 and its completion supply; the sealed S1 receiver (Θ_hist's typing, per the 1038 check's citation); the port-order span (P2 after P5, c5.rho_pre); Gen_Omega only WITH overlay V002 and DoR-013; STAGE8_DESC_B03_DARIO_V003.md (2b3a99bc1c58…), STAGE8_DESC_ARROW_NECESSITY_DARIO_V001.md (473f849833d5…), and their checks (2b251a781180…, a193f696b5f7…) — for pins and rulings, NEVER as record witnesses; the six-leg demand spans (P1, P5, v004).
THE TASK: attempt the SYMBOLIC EXECUTION of the record's own assembly, in the record's own order, treating the producer would-builds as instructions. Each step ends GROUNDED-AND-EXECUTED (the construction trace at bytes, every ingredient cited) or HOLE (the missing datum named, typed, and located — the PE-17 display standard) or CASCADE-TERRITORY (a step that needs a number). Steps, in dependency order:
STEP 1 — THE JOINT CARRIER. Assemble the joint P0 object from DoR-008's supplied pre-algebra, completion, and common domain, carried AS RATIFIED PREMISES (TYPE-P, falsifier-attached, per Q-943's census row — the Q-212 void trigger carried in the artifact). State exactly what the assembly consumes, what it produces, and its sector structure — including whether the history sector is or contains C(Y)/B by construction.
STEP 2 — Θ_hist. Per the sealed S1 receiver's own typing — Θ_hist : (common-origin / outgoing-record state data) → State(C(Y)) — determine whether the executed Step-1 object plus the sealed descent maps (d_state, d_ready, d_law from Gen_Omega; the d_C0/d_U1 choice-free prefix from Arm A) COMPOSE to a Θ_hist realization: the delivery of the history-sector state from common-origin data, direction-bearing, with its certificate shape. If the composition exists, exhibit it at bytes. If a component is missing, that component is the hole — name it.
STEP 3 — THE PRE-STATE. Execute P5's production of rho_pre on the Step-1 object as far as its spec grounds it — the P5 leg's own words ("a positive normalized rho_pre on the completed object … all supplied by the same microscopic source") — then state P2's test shape (what i_src would verify, per its sealed signature). Production grounded, or hole named.
STEP 4 — THE SIX LEGS, RE-DISPOSITIONED against whatever Steps 1–3 produced: which legs close, which remain, each at its span. If all six close: the B03 port closes RECORD-NATIVELY and say what disposes of the selector question permanently. If any remain: the remainder is the section's TRUE residual demand — display it.
HONESTY CONSTRAINTS, verbatim-of-intent: do not soften a hole into a "nearly"; do not bridge a gap with prose; do not let the wish that PE-shaped expectations be satisfied touch a single disposition (you are blind to the expectations ledger; keep it that way). If Step 1 itself cannot execute from sealed grounds, STOP THERE and display — an honest one-step result is a full result.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD — every scale symbolic. PE-1..17 pointer-only. No member bound (DoR-013's clause and the fence agree); no fixed-point execution (characterizations citable); no end test; no numeric evaluation; no measured-constant comparison; no freeze; ω_phys as such untouched — if Θ_hist and the pre-state both ground, say what follows for it WITHOUT instantiating anything numeric.
OUTPUT: one sealed artifact `STAGE8_DESC_P0_EXECUTION_DARIO_V001.md` (+ sealed generator with declared inputs; refusal paths declared and, where exercised, reported).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `STEP1 = EXECUTED (sector structure) / HOLE (name, type, locus) / CASCADE-TERRITORY (step)`, `STEP2 = COMPOSED (trace) / HOLE (component, locus)`, `STEP3 = PRODUCED-IN-SHAPE / HOLE (ingredient, locus)`, `LEGS = <n>/6-CLOSED (remainder at spans)`, `PORT = CLOSES-RECORD-NATIVELY / RESIDUAL-DEMAND-DISPLAYED`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1040_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
