## PASTE 894 — DARIO LANE — [PLAN:AXN-BUILD-B22] THE QUASI-LOCAL STATE INVERSE: CONSTRAIN THE LOAD-BEARING OBLIGATION

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. PROCESS NOTE, BINDING: your lane's law-9b closure order has been found DEFICIENT THREE TIMES (Q-789/798/800) — in THIS artifact, the FIRST substantive content after the preflight block MUST be the closure declaration with an EXACT PINNED MEMBER LIST (names + digests), and NO absence-shaped sentence may precede it anywhere, headlines included. The registrar will audit by byte position.
CONTEXT OF RECORD: the QUASI-LOCAL OUTGOING RECORD STATE — item 3 of the gate's must-prove list — is now LOAD-BEARING TWICE: item 14's representation requirement (Q-791/793: making strong convergence a closure operation needs a representation, which needs the state) and the I2 census (Q-783: the compatible quasi-local state is PRESENT as a state at its own scope — the question is its COMPLETION status against the gate's demand). SUBJECTS, seal-verify BEFORE reading: the gate's items at the quasi-local span (0f94b343… in the R3_4 SPEC); the sealed state material 876 censused; the affiliation pair (19aaf7fc…, its check f194da93…, mirrored); the GNS/record-limit sources of the S3 completions.
TASK — THE INVERSE DERIVATION: derive every NECESSARY condition the complete quasi-local outgoing record state must satisfy from its sealed receivers — the gate's own item-3 wording; what the representation must do for item 14 (GNS of the state making strong limits closure); compatibility with the record-only completion (BdGlue_law_rec consumes state structure); the grading/census compatibility (the state's behaviour under alpha_infinity — must the state be even?); normalization and positivity as the record types them. Each at bytes. The PRESENT state material: classify what it already satisfies vs what the complete demand adds. CONTROL, TYPED per Q-797: state which kind (eliminating/explaining/transcription) with an auditable source index. VERDICT (typed): STATE-FORCED / STATE-REFUTED / RESIDUAL-FREEDOM (formal shape only if no canonical map — the 893 lesson) / CONSTRAINT-GAP (missing inputs named). DERIVE-NEVER-SELECT; stop-on-freedom; nothing installed.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_QUASILOCAL_STATE_INVERSE_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position stated)`, `CONSTRAINTS = derived (n, at bytes)`, `PRESENT_STATE = <satisfies m of n; the delta displayed>`, `VERDICT = <typed>`, `CONTROL = <type + result + independence>`, `STATE_OBLIGATION = <status>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 894_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
