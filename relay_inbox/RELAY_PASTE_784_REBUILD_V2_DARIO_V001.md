## PASTE 784 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:A1-CORRECTIVE-2] RADII CARRIED, TIERS CONDITIONED TRUE: THE REBUILD CONVERGES

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL seven alignment laws apply. GOVERNING: relay 783's cross-check of your 781 rebuild — CONFIRMED: the unconditional functional trace, and lambda >= C2_parent tight (NOW PROVED of record, Q-690); D1 and dead items consumed nowhere. REFUTED: (i) the (F)-alone 16·Tr_base tier — (F) does not make the unselected internal map Phi SCALAR on E_parent; (ii) the (F)+(S) printed sum and the literal member bounds SILENTLY SET ALL THREE RADII TO 1 — the radii are free data (decline-register D9/S28), so the formulas hold only under a unit-radius condition never stated; (iii) the freedoms-consumed block omitted the analytic input f, the radius specialization, and the (F) attachment at member-bound uses. Law 7: 781 is the named consumer; this relay is the propagation.)

TASK — converge the rebuild; the deltas are conditioning, not structure:
AS1. RADII EXPLICIT EVERYWHERE: restate the spectrum and every downstream formula with R_T, R_flux, R_Q carried — p_t^2/R_T^2 + p_q^2/R_Q^2 + ell(ell+2|q|)/R_flux^2 + [Phi-sector] + C2_parent (or the corrected sealed form; derive, don't pattern-match). State the unit-radius specialization as a named CONDITION (U) where prior formulas used it. CONSEQUENCE TO DISPLAY: with radii explicit, the beta mechanism (Q-674's non-uniform scaling — geometric towers carry 1/R^2, the internal Casimir is radius-free) appears IN the formulas rather than in prose; connect this to U8 explicitly.
AS2. TIERS RESTATED TRUE: tier 1 unconditional functional (confirmed — keep); tier 2 stated with its TRUE requirement ((F) AND a scalar-Phi-carrier condition, named and quoted, or the tier dropped if no sealed ground names one); tier 3 under (F)+(S)+(U) with all conditions displayed.
AS3. MEMBER BOUNDS CONDITIONED TRUE: the bounds with radii explicit — the "+1" separation term becomes 1/R_flux^2; state exactly which conditions ((F), (U), (S)) each bound consumes. The PROVED unconditional bound lambda >= C2_parent stands; cite Q-690, do not re-derive.
AS4. FREEDOMS-CONSUMED BLOCK, COMPLETE THIS TIME: include the analytic input f (carried as unselected test function), all three radii, the (F)/(S)/(U) conditions at every point of consumption. FLATTENING CHECK against DECLINE_REGISTER_V002 (S01–S37; S27/S28 are the live ones).
AS5. Salvage ledger from 781: every item dispositioned (stand / re-conditioned / dead), nothing carried silently. All headline items CLAIMED (cross-check follows). PE-1..6: pointer-known, zero weight, do not consult.
OUTPUT: one sealed artifact `STAGE8_7A_DBR_LAYER_REBUILD_V2_DARIO_V001.md`
Final lines: `RADII = explicit everywhere; condition (U) named at <n> sites`, `BETA_MECHANISM = displayed in-formula (U8 connected)`, `TIER2 = restated ((F)+scalar-carrier, quoted) / dropped (no sealed ground)`, `TIER3 = (F)+(S)+(U) displayed`, `MEMBER_BOUNDS = conditioned true (per-bound conditions listed)`, `FREEDOMS_CONSUMED = <complete block incl. f, radii>`, `FLATTENING_CHECK = clean / stopped at S<n>`, `SALVAGE = <dispositions>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 784_DONE.md, then chained pickup (smallest relay number > 784 addressed to DARIO; if absent, STOP cleanly). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants. No determinant evaluated; no member evaluated alone (FAMILY-BEFORE-DETERMINANT). No bundle class adopted or eliminated. No common cell formed; no junction map evaluated; no smooth data derived.
