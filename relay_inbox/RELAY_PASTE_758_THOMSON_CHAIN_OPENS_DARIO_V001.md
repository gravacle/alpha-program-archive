## PASTE 758 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / STEP 7] THE THOMSON CHAIN OPENS: THE SCOPING

PICKUP-ACK + LANE GUARD in force (DARIO).
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL standing laws apply. GOVERNING: the handoff package a0a7e1d1…; the frozen prereg; Q-661's correction — the junction test needs a derived, beta-sensitive junction, and step 7 is its expected feeder.)

CONTEXT: road step 7 — the Thomson chain through the bridge (S16, S13 conditioning per the road's own line). The scoping, not the derivations:
TASK:
AJ1. Assemble the chain's demand structure from sealed sources: what the Thomson chain IS of record (the sealed chain from kappa_Thomson's role through the bridge), each link's sealed status (derived / partial / absent), S16 and S13's exact conditioning demands, and what the A28 firewall requires of every link (only kappa_Thomson may enter the alpha map).
AJ2. THE BETA-SENSITIVITY QUESTION front and center: which link(s), if derived, produce a junction object that is BOTH derived AND beta-sensitive (Q-126's conjunction) — the exact objects, their sealed grounds, and what deriving each would take.
AJ3. Dependency order and first target chosen with justification; effort classes; searched spaces stated; the jurisdiction check on every rule cited.
OUTPUT: one sealed artifact `STAGE8_7A_THOMSON_SCOPING_DARIO_V001.md`
Final lines: `CHAIN = mapped (links + statuses)`, `S16_S13 = demands displayed`, `BETA_SENSITIVITY = candidate links named (grounds displayed)`, `FIRST_TARGET = chosen (justified)`, `VOID = clean`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 758_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants. No common cell formed; no junction map evaluated; no smooth data derived.
