## PASTE 892 — DARIO LANE — [PLAN:AXN-BUILD-B20] THE JOINT-COMPLETION INVERSE ATTACK: DERIVE S3'S CONSTRAINT SYSTEM

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. CONTEXT OF RECORD: S3 — the boundary-package stop, root D's RICHEST field (Q-769/772): TWO exact honest completions exist (BdGlue_law_src, the P2 norm source completion; BdGlue_law_rec, the PathCert outgoing record-only GNS direct limit), gated on the JOINT PHYSICAL COMPLETION — "no new finite glue is needed": no amount of finite work advances it. THE METHOD, now validated three times (Q-792/794/795/796/797): derive the missing object's COMPLETE CONSTRAINT SYSTEM from its sealed receivers — WITH THE TYPED-CONTROL DISCIPLINE of Q-797: state whether your control ELIMINATES, EXPLAINS, or merely TRANSCRIBES, and source-index it so a round trip cannot masquerade as convergence. SUBJECTS, seal-verify BEFORE reading: the four-field census (d3668b90…) + check (c9e5b5d5…) at their S3 sections; both completions at their sources; the boundary TARGET specification (Q-750/752 lineage — the CTP vanishing form, wrong generator/scale disclosed); the C4 attack + check (93467eb4…, 145eae2f…) for the boundary carrier roles.
TASK — THE INVERSE DERIVATION, BOUNDED AND FAILURE-CAPABLE. (1) From the sealed receivers (the S3 requirement's "complete preparation/gluing and boundary package"; the two completions' own structures; the target specification; the four-role boundary schematic): derive every NECESSARY condition the joint physical completion must satisfy — the joint carrier's relation to BOTH exact completions (the intertwiners' demands); the preparation/gluing variation's conditions; the boundary gauge orbit and edge reductions; microcausal support; the vanishing-form condition AS THE TARGET SPEC STATES IT (wrong generator/scale carried, not repaired); the descent relation to the per-cell receivers. Each at bytes. (2) The conditional residual: if a joint completion exists, the exact remaining freedom — or, if the quotient's defining map presupposes the object (the S2 situation), say so and display nothing. (3) CONTROL, TYPED: does the system reproduce "no new finite glue is needed" as a corollary of its own structure (an EXPLAINING control — why finite work cannot advance it), with the source index displayed so its independence is auditable? (4) VERDICT (typed): COMPLETION-FORCED / COMPLETION-REFUTED / RESIDUAL-FREEDOM (quotient) / CONSTRAINT-GAP (missing inputs named). DERIVE-NEVER-SELECT; stop-on-freedom; law 9b closure declared BEFORE any absence; nothing installed.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_JOINT_COMPLETION_INVERSE_DARIO_V001.md`
Final lines: `CONSTRAINTS = derived (n, at bytes)`, `VERDICT = <typed>`, `CONTROL = <type + result + source-index status>`, `S3 = <status>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 892_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live; no smooth import; no numeric evaluation of physical quantities.
