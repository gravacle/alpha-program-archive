## PASTE 1025 — DARIO LANE — [PLAN:DESC-24] DIAGNOSIS ARM A — IS THE FREEDOM GAUGE, AND WHO AUTHORED THE BAR?

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. Full paths; digests-only certificates; declared conventions; the fixed-point closure.
THIS IS A PE-17 DIAGNOSIS RELAY. The B03 stop (Q-937) is CLAIMED, under attack at 1024 by the opposite lane. This relay does not defend the stop, re-run it, or act on it. It answers two structural questions that matter under EVERY outcome. The working prior is ERROR BEFORE NEW PHYSICS. Nothing here selects, ranks, names, or approaches a candidate state; no freeze is proposed; the choose branch remains Brian's and untouched.
SUBJECTS, verify seals BEFORE reading (workspace/): STAGE8_DESC_B03_DARIO_V001.md (581ec505441b88b8a4d33d875ddc2d1d29ef8291fd140c4901a07a5513ac2506) — read for its pins, not to re-argue it; STAGE8_DESC_AUDIT_CODEX2_V002.md (078d6d54a259…); STAGE8_DESC_DEMAND_DARIO_V008.md (968aad4fb387…); STAGE8_DESC_B02_DARIO_V001.md (80fc23ed9687…); the record files their closures name, resolved and rehashed as you go; member 12 only with overlay V002 (e85f444cbbd3…) and DoR-013.
QUESTION 1 — THE GAUGE QUESTION. Member 17's theorem (B03 pin): every ω_B leaves the record marginal identical — the record cannot see the choice. Determine, structurally and at bytes, WHAT DOWNSTREAM OF THE SELECTOR CAN: build a dependency ledger over every consumer of the StatePort in the section's two maps — every basis row B04–B15, every demand condition that names the joint state, K7's base-family requirements, B10's aggregation on the B02 trace. For each consumer, one disposition: CONSUMES-STATE (needs ω_phys itself — cite the span that shows the dependence), CONSUMES-MARGINAL-ONLY (touches only what member 17 proves choice-invariant), or CONSUMES-COMPOSED (needs only Ω_C0 ∘ i_B or another derived object — say which). THE STAKES, stated so the ledger is read right: if every consumer is MARGINAL-ONLY or COMPOSED-from-invariants, the freedom is GAUGE for this program's outputs and there may be nothing to choose at all; if any consumer is CONSUMES-STATE, its span is exactly where a choice would become load-bearing. Report the ledger; draw the one-line conclusion; decide nothing.
QUESTION 2 — THE PROVENANCE OF THE BAR. B03's nearest miss (C18/C19): the record forces a unique state at State(A_src), and member 19 types its transport WRONG CODOMAIN, "PASS ONLY AT SOURCE-SECTOR SCOPE." Determine WHO AUTHORED THAT TYPING: is the codomain bar RECORD-FORCED (derived from the transition structure — cite the derivation's span) or INSTRUMENT-AUTHORED (a typing decision of an instrument, Q-900's class)? If instrument-authored, state precisely what a lawful revisit would require under the program's own supersession rules — SPECIFY, DO NOT EXECUTE; if record-forced, the near-miss closes and say so. Do not argue the bar should fall; establish only its provenance.
FENCES, unchanged and live: no numeric evaluation of any physical quantity; no comparison to measured constants; no member binding; no fixed-point execution; no end test; BLIND held, every scale symbolic. Reading downstream STRUCTURE (which object consumes which) is this relay's task; evaluating any downstream VALUE is not, and the line between them is the line this relay must never cross.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. PE-1..17 pointer-only.
OUTPUT: one sealed artifact `STAGE8_DESC_DIAG_A_DARIO_V001.md` (+ sealed generator with declared inputs if any computation runs).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `LEDGER = <n>-CONSUMERS-DISPOSITIONED`, `CONSUMES_STATE = <count> (spans)`, `GAUGE_VERDICT = GAUGE-FOR-OUTPUTS / LOAD-BEARING-AT (spans) / MIXED`, `BAR_PROVENANCE = RECORD-FORCED (span) / INSTRUMENT-AUTHORED (instrument, span)`, `REVISIT_SHAPE = SPECIFIED / N/A`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1025_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live. No member bound.
