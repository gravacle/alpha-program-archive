## PASTE 804 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:B1a-8] VERIFY THE NO-GO CERTIFICATE: THE RULING'S EVIDENCE

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. BUILDER-NEVER-VERIFIES: the subject is the other lane's UNSATISFIABLE verdict (Q-710), on which the first surviving principal ruling is parked. Your verification is the last gate before that ruling. SUBJECT (mirrored, verify seal): STAGE8_B1A_FULL_SPACE_SOLVE_CODEX2_V001.md (520a3fa2…). Use ONLY the packet-sealed V011 copy aa7c6d49….)

TASK — adversarial verification of the no-go, independently:
AS1. SYSTEM AUDIT: verify the assembled system IS the sealed constraint set — every equation traced to its span (the seven atomic constraints; nothing added, nothing dropped, no constraint strengthened beyond its sealed wording). The certificate is only as good as the system it certifies; a mistranscribed constraint would void everything. Special attention: the intrinsic-quadratic's exact sealed form (the killer), and the gauge quotient's dimension (300).
AS2. CERTIFICATE CHECK: independently verify the infeasibility — rebuild the linear block (confirm the 264-dimensional curvature-nonzero affine space, your own basis), then the exact Gram completion: reproduce (or refute) the minimum refined quadratic 352886/122871 against the required 2 with the certificate direction F_* = (1,-1,0,0,0,0). Exact rational arithmetic; your own implementation, not a re-run of theirs.
AS3. SCOPE AUDIT: verify the no-go's stated scope — the DECLARED FULL LINEAR LAW CLASS on these generators — is exactly right: neither narrower (does the argument actually cover all 864 law parameters?) nor broader (does anything in it constrain NONLINEAR laws? If any sealed constraint already excludes or restricts nonlinear law classes, SAY SO — it bears directly on the parked ruling's option (ii)).
AS4. THE OBSTRUCTION READING (zero-weight check, stated as structure only): is the positive excess 107144/122871 stable under the generator choice (A1? barycentric A2?), or generator-specific? A stable excess is obstruction-shaped; a generator-specific one is not. Compute both if lawful from the rebuilt receivers. Verdicts per claim: CONFIRMED / REFUTED (exhibit) / SCOPE-CORRECTED (displayed). FREEDOMS-CONSUMED block; FLATTENING CHECK. All headline items CLAIMED->your verdicts. PE-1..7 pointer-only.
OUTPUT: one sealed artifact `STAGE8_B1A_CERTIFICATE_VERIFY_DARIO_V001.md`
Final lines: `SYSTEM_AUDIT = faithful (all spans traced) / DEFECT (displayed)`, `CERTIFICATE = CONFIRMED (excess reproduced exactly) / REFUTED (exhibit)`, `SCOPE = exact / corrected (displayed)`, `NONLINEAR_BEARING = none sealed / FOUND (displayed)`, `EXCESS_STABILITY = stable across generators (values) / generator-specific (values) / not lawfully computable`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 804_DONE.md, then chained pickup (smallest relay number > 804 addressed to DARIO; if absent, STOP cleanly). No register, plan, tracker, git action.

GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. Exact solution-set geometry on displayed finite systems is the lawful class; no physical quantity evaluated; no law/metric/member adopted; no smooth import; no EM identification.
