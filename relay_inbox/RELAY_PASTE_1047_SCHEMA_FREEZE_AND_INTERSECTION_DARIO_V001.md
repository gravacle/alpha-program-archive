## PASTE 1047 — DARIO LANE — [PLAN:DESC-31] FREEZE THE SCHEMA, THEN INTERSECT — THE RE-ATTEMPT THROUGH THE FULL SURFACE

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1044 bootstrap's rules govern (symbolic line; citation rule; prose-digest audit; selected-by-derivation lawful only at exactly-one; a freeze of anything unforced is not a lane's act — WITH the one exception this paste defines below, which is itself derivation-shaped).
SUBJECTS, verify seals BEFORE reading: your selection (STAGE8_DESC_PHYSICALSIG_SELECTION_DARIO_V001.md, a844c9eac0c4…) and its check (STAGE8_DESC_SELECTION_CHECK_CODEX2_V001.md, 00ac4928a247… — the hinge ruling of record: the seven port-contract schemas are Phase-A peers, admissible; actual candidate outputs stay inadmissible); member 03's SignatureFreezeInputs_0 tuple and the twelve-conjunct PRIMITIVE_INTERFACE_REQUIREMENT at their spans; FrozenPortContractSchemas_1_to_7 (the sibling with real content — its structure is the pattern); Q94's structure requirement; SIG-T11; the record files these cite.
STEP 1 — FREEZE FrozenPrimitiveBindingSchema_0, DERIVATION-SHAPED OR NOT AT ALL: the schema is a TYPE object. Determine whether its typed shape is ENTAILED by what is sealed — the twelve conjuncts (which constrain I_prim), the sibling's structural pattern, the port contracts' demands on primitive bindings, Q94's consumption requirements. If the sealed constraints admit EXACTLY ONE typed shape (up to the record's own notational conventions, declared), EXHIBIT IT — that is a derivation, lawful for this lane, and the schema freezes by entailment. If more than one typed shape survives, this is THE GENERICITY QUESTION reached at last: display the shape-space, what pins it partially, and STOP THE STEP — a lane does not choose a schema any more than a value.
STEP 2 — THE INTERSECTION RE-ATTEMPT, per field, on whatever Step 1 yields (run it even if Step 1 stops, against the constraint surface WITHOUT the schema — say so if you do): for each of the nine fields, intersect ALL admissible Class-A constraints — the port-contract demands, the exclusions, the conditionals, the internal dependencies (respecting the head order: 1–3 first) — and re-disposition: SELECTED-BY-INTERSECTION (exactly one survivor — grounds at bytes, every constraint in the intersection cited), CONSTRAINED (the narrowed set's structure, and the finishing property), or UNCONSTRAINED (only if the check's constraint assignment fails at bytes — justify against Q-957). The exactly-one standard is unchanged; the surface is now the full one.
STEP 3 — THE TALLY AND THE FORK, stated for the record: if selections appear, what the remainder is; if none appear even at the full intersection, state — at bytes, as the display of record — WHY the intersection cannot pin (which branches survive every constraint), because that display is what any terminal scoring of the section's open question will rest on.
HONESTY: no output inspection (candidate outputs inadmissible per the check — the intersection uses constraints, never candidates); no softening; the PE ledger stays shut; an honest stop at Step 1 with the shape-space displayed is a full result.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD — every scale symbolic; CASCADE-TERRITORY stop form live (the known numeric boundary: finite dimension / sector-dimension ratios on the Gen_Omega side — not approached). PE-1..17 pointer-only. No member bound; no fixed-point execution; no end test; no measured-constant comparison; no freeze beyond Step 1's entailment case; ω_phys untouched.
OUTPUT: one sealed artifact `STAGE8_DESC_SCHEMA_AND_INTERSECTION_DARIO_V001.md` (+ sealed generator, declared inputs, refusal paths; prose-digest audit run and counted).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `SCHEMA = FROZEN-BY-ENTAILMENT (shape exhibited) / SHAPE-SPACE-DISPLAYED (genericity reached; cardinality/structure)`, `FIELDS = <s>/<c>/<u> of 9 (intersection surface; each with locus)`, `TALLY_DELTA = <from 0/9/0>`, `WHY_NOT_PINNED = N/A / DISPLAYED (surviving branches per field)`, `CASCADE_STOPS = NONE / <fields>`, `PROSE_DIGESTS = <n>/<n>-ACCOUNTED`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1047_DONE.md, then chained pickup. No register, plan, tracker, git action (and no read-only git either — the 1045 disclosure sets the letter).
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
