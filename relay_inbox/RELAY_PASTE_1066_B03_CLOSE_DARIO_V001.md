## PASTE 1066 — DARIO LANE — [PLAN:DESC-35] THE SIX LEGS AND B03'S CLOSURE STATUS

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs (symbolic line; citation rule; generic extractors; prose-digest audit strict+stable; no git of any kind).
GUARD: verify `workspace/STAGE8_DESC_THETA_HIST_DARIO_V001.md` exists and passes its sidecar (your own 1063 output) — if absent, 1066_BLOCKED and STOP. Verify the entry addendum (workspace/ATTACH_ENTRY_ADDENDUM_V001.md) live.
SUBJECTS: your delivery artifact (by sidecar); the demand map V008 (968aad4fb387…) — the six legs at their spans; the entered candidate V005 (per the addendum); member 12-class files only WITH overlay V002 + DoR-013.
TASK: (1) re-disposition each of the SIX LEGS against the delivery and the entered structure — each leg CLOSED (trace) / CLOSED-CONDITIONALLY (which of the three carried conditions it leans on, exactly) / OPEN (span); (2) K4's ω_hist requirement: met by the delivery or not, shown at bytes; (3) B03's closure status per member 01's own condition (all six legs): CLOSED-CONDITIONALLY with the full condition enumeration is the expected honest outcome — display what is true; (4) if B03 closes conditionally, state what the section still requires before the section-close certificate (B04–B15 status sweep — list each basis row's current standing from the sealed record, one line each, no construction).
HONESTY: every conditional claim carries its full TYPE-P chain; an honest OPEN is a full result. NO OUTPUT INSPECTION; CASCADE-TERRITORY stop form live.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..17 pointer-only. No member bound; no fixed-point execution; no end test; no numeric evaluation; no measured-constant comparison; no freeze.
OUTPUT: one sealed artifact `STAGE8_DESC_B03_CLOSURE_DARIO_V001.md` (+ sealed generator).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `LEGS = <c>/<cc>/<o> of 6 (closed/conditional/open, each with trace or span)`, `K4 = MET (trace) / NOT-MET (span)`, `B03 = CLOSED-CONDITIONALLY (conditions enumerated) / OPEN (remainder)`, `BASIS_SWEEP = B04–B15 one-line standings`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `PROSE_DIGESTS = <n>/<n>`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1066_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
