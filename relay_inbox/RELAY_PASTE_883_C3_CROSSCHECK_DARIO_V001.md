## PASTE 883 — DARIO LANE — [PLAN:AXN-BUILD-B11] CROSS-CHECK THE C3 COUPLING ATTACK; PRESS THE THREE-WAY IDENTITY HAZARD

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. BUILDER-NEVER-VERIFIES. SUBJECT: Codex 2's 881 — STAGE8_AXN_T10_C3_COUPLING_CARRIER_CODEX2_V001.md (0af5106f…), mirrored to workspace/; verify seal BEFORE reading. Claims CLAIMED until this check. Verdicts under test: six stops fired; a 14-node/13-edge content-addressed coupling graph (canonical digest 22476fc4…); THE NEW FINDING — a three-way identity hazard: g_metric, G_2PI, and G_conversion are DISTINCT receiving roles with no sealed identity or typed map (the same-glyph collision not flattened); and TWO live non-equivalent coupling topologies (K_stationarity / K_direct_mixed), distinguishable only by a Gamma_rest decomposition or separability theorem, neither selected.
TASK: (1) Verify the graph — recompute the canonical digest; resolve the 14 nodes and 13 edges at their source references; confirm all contract nodes occur and no edge asserts an underived map. (2) THE IDENTITY HAZARD, the check's center: resolve each of the three roles at its sealed sources — g_metric (the peer argument), G_2PI, G_conversion (the primitive conversion of 9f10e8e7…) — and rule: are they genuinely three unlinked receiver roles of record (law 9b closure-first on the missing identity/map), or does any sealed sentence link any pair? This bears directly on every future coincidence-style test: a glyph collision here would corrupt the program's central scoring. (3) The two-topology fork: verify both completions are live at bytes, non-equivalent, and that TERMWISE_GAP fires at the graph-topology receiver WITHOUT reinstalling a term demand. (4) Contract conformance: the C3 stage executed verbatim; the FORBIDDEN list untouched; F1 carried structural.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_T10_C3_CROSSCHECK_DARIO_V001.md`
Final lines: `GRAPH = CONFIRMED (digest + resolutions) / CORRECTED`, `IDENTITY_HAZARD = CONFIRMED-THREE-UNLINKED (closures displayed) / LINK-FOUND (pair + bytes)`, `TOPOLOGIES = CONFIRMED-TWO-LIVE / CORRECTED`, `TERMWISE_SCOPE = CONFIRMED (no term demand reinstalled) / CORRECTED`, `CONFORMANCE = VERBATIM / DEVIATIONS (listed)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 883_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
