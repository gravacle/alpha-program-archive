## PASTE 898 — DARIO LANE — [PLAN:AXN-BUILD-B26] THE BOUNDARY-RESOLVED GENERATOR INVERSE (SC5-B)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. BINDING: closure declaration FIRST with exact pinned members and a zero-hit pre-declaration token scan, self-audited by byte position — the 894/897 standard. CONTEXT: SC5-B — the complete Boundary-Resolved generator — is the closer SHARED by SC5-B and SC5-E (Q-757's two-share-a-closer note), carries the boundary TARGET's condition (the CTP vanishing form lives ON this generator, at global scale), and is ABSENT of record. The method: derive its complete constraint/acceptance system from its sealed receivers. SUBJECTS, seal-verify BEFORE reading: the generator's naming sources (the C4 attack + check pair, 93467eb4… / 145eae2f…, and their cited spans at ec848d29… [31771,32311) and [32029,32569)); the target specification lineage (Q-750/752); the S3 derivation + check (af1470e5…, f3ce18d9…, mirrored) for J6's conditional structure; the B0 acceptance law (ae93720f…, mirrored) for the correspondence side's typing.
TASK — THE INVERSE DERIVATION: derive every NECESSARY condition the complete Boundary-Resolved generator must satisfy — what "complete" means at its own sources (which sectors/faces it must resolve); the vanishing-form condition AS ITS CARRIER (the target's shape as a constraint ON the generator, wrong-scale disclosed); its relation to B0 (the correspondence is SEPARATELY underived — constrain what the generator must exhibit FOR a future correspondence without assuming one); its consumers' demands (S3's J6; SC5-E's descent source; the C4 diagram's node). Fold the sealed negatives as tests. CONTROL, TYPED with independence displayed. VERDICT (typed): GENERATOR-FORCED / REFUTED / RESIDUAL (formal shape only if no canonical map) / CONSTRAINT-GAP (missing inputs named). Nothing installed.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_GENERATOR_INVERSE_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan result)`, `CONSTRAINTS = derived (n, at bytes)`, `VERDICT = <typed>`, `CONTROL = <type + result + independence>`, `SC5B = <status>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 898_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
