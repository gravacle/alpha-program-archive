## PASTE 961 — DARIO LANE — [PLAN:AXN-BUILD-D60] BUILD THE EIGHT for_class REPLAY FAMILIES (THE LAST ABSENT OBJECTS)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. Numeral grep before sealing.)
THE STATE: run 3 (Q-872, fff…195ef2153148…) FAILED at G0 the NEW way — the repaired schema is SATISFIABLE, the coverage core PASSED, and the first missing key is JointAnchorCertificateSuite.v001.finite_stage_inventory.BOUNDED_CLASS.for_class.delta0_basis_family: the eight total replay families are simply ABSENT and are not consequences of the stage rule. They are the last unbuilt objects between the anchor and acceptance. SUBJECTS, verify seals: the governing V003 (79f0c3516120…) at its for_class schema bytes; the completed instance (089af246cbc0…); the booked build V002 (fd2625a079c7… — the pairing, E_joint, the four certificates); the stage material (payload 42b6850c1642…; receipts root 9451020e12f7…); run 3 (195ef2153148…, at the cleanroom root — its exact missing-key list is your work order).
TASK — CONSTRUCT THE EIGHT for_class TOTAL REPLAY FAMILIES over the bounded class (the F_cyl tower with the entered/booked objects), in V003's exact schema and JAC-14's replay format, IN SCHEMA ORDER: (1) delta0_basis_family — the Delta_0^joint basis family per class: derive it canonically from the stage structure and the booked E_joint/Tr_joint IF a canonical construction exists with zero freedom (the pinching structure and stage grammar may force one — display the forcing); IF a genuine basis choice is free, STOP THAT FAMILY and route it as a selector — do not choose; (2) phi_restriction_matrix family — the booked pairing's restriction matrices over the class; (3) factorization/inverse; (4) CPTP; (5) charge; (6) superselection (the booked E_joint's class family — its four certificates are the per-object ground); (7) fixed-space/mixing; (8) the BI anchor certificate family (the adopted tag class is {BI}; the BI equations are the entered content — the family certifies them per class). EACH family total over the class, exact, replayable, content-addressed; each grounded ONLY in booked objects and sealed spans; ANY genuine freedom STOPS that family with the freedom named (an honest partial with stops beats a complete set with one quiet choice — the standing law). BLIND: no rank, no ratio, no fiber comparison; the fiber stays an opaque pointer.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. All families CLAIMED until the opposite-lane check. PE-1..14 pointer-only. NO numeric evaluation of physical quantities.
OUTPUT: one sealed artifact `STAGE8_AXN_FOR_CLASS_FAMILIES_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `FAMILIES = n/8 BUILT (list) / STOPPED (which, freedom named)`, `BASIS = CANONICAL-FORCED (displayed) / SELECTOR-ROUTED`, `GROUNDS = BOOKED-ONLY-VERIFIED`, `FORMAT = JAC-14-REPLAY (digests listed)`, `NEW_CONTENT = NONE-BEYOND-DERIVATION`, `BLIND = HELD`, `NUMERAL_GREP = RUN`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 961_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
