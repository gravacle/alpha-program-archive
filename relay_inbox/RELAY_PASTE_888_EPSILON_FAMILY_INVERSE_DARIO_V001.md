## PASTE 888 — DARIO LANE — [PLAN:AXN-BUILD-B16] THE EPSILON-FAMILY INVERSE ATTACK: DERIVE S2'S CONSTRAINT SYSTEM

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. CONTEXT OF RECORD: S2 — the contour stop, root D's weakest field (Q-769/772: "an INVERTED structure — a receiver with nothing to receive"; the EpostEndpointOrientation receipt is genuine compatibility material; no epsilon-indexed analytic family exists anywhere). THE METHOD, proven at S1 (Q-792): derive the COMPLETE CONSTRAINT SYSTEM the missing object must satisfy from its sealed receivers — the require-half first; the inhabitant question after. SUBJECTS, seal-verify BEFORE reading: the four-field census (d3668b90…) + its check (c9e5b5d5…) at their S2 sections; the EpostEndpointOrientation receipt at its source; the I_EPSILON_LIMIT_IN_TOPOLOGY_SRC NO_VERDICT source; the S2 stop requirement at f97ee43e… [1907,2702).
TASK — THE INVERSE DERIVATION, BOUNDED AND FAILURE-CAPABLE. (1) From the receiver's own sealed demands (the endpoint-orientation receipt's compatibility conditions; the S2 requirement's "interacting contour/analytic/i-epsilon prescription"; the insertion rule and limit-topology demands of record): derive every NECESSARY condition an epsilon-indexed analytic operator family must satisfy — the epsilon-domain, the insertion rule's shape, the analytic half-domain and boundary-value map, the common invariant operator domain (note its relation to S4's domain conjunct — same family, distinct receivers discipline per Q-784/793), the limit topology. Each at bytes. (2) The conditional residual: if a family satisfying the system exists, what freedom remains (the exact quotient, per the S1 pattern)? (3) CONSISTENCY CONTROL: does the constraint system reproduce the recorded NO_VERDICT (I_EPSILON_LIMIT_IN_TOPOLOGY: prerequisite absent) as a corollary — the analogue of S1's Haar check? (4) VERDICT (typed): FAMILY-FORCED / FAMILY-REFUTED / RESIDUAL-FREEDOM (quotient displayed) / CONSTRAINT-GAP (missing inputs named). DERIVE-NEVER-SELECT; stop-on-freedom; law 9b closure-first (declare the exact closure BEFORE any absence line); nothing installed.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_EPSILON_FAMILY_INVERSE_DARIO_V001.md`
Final lines: `CONSTRAINTS = derived (n, at bytes)`, `VERDICT = <typed>`, `CONTROL = COROLLARY-CONFIRMED / TENSION (displayed)`, `S2 = <status>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 888_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live; no smooth import; no numeric evaluation of physical quantities.
