## PASTE 916 — DARIO LANE — [PLAN:AXN-BUILD-C14] THE TWO SPEC ACTS AS DERIVATIONS: BOOK LINE 6'S SOURCE THIRD

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. GROUND: 914 named line 6's two source-side spec acts (Q-820), both attemptable as DERIVATIONS before any authored supplement: (i) THE SEALED FINITE GALERKIN CARRIER AND ITS EMBEDDINGS — bind the finite stages' carrier/embedding structure at the declared receiver (the finite-stage membership 911 proved automatic needs its carrier chain sealed AS the receiver's content); (ii) THE EXACT dGamma_R RECEIVER/SCOPE FOR D_K AND ITS DESCENDANTS — the level bridge 911 named (the record supplies dGamma_R for h_K only; derive its receiver and lawful scope for D_K, or the exact obstruction). The 914 correction governs: the fermionic bounded case (rank-one gives a*(e)a(e)) is real — the scope derivation must respect it. SUBJECTS, seal-verify BEFORE reading: the 911/914 pair (5f5d3a73…, and the check in workspace/); the dGamma_R sources at their pins; the Galerkin-stage sources (CAR(K_n) = M_(2^n)(C) lineage); the declared-algebra sources.
TASK: (1) SPEC ACT (i): derive the finite carrier chain and embeddings from the sealed CAR/Galerkin structure — display the chain, its compatibility with the record-side inclusions (911's zero-residual homomorphism), and bind it at the declared receiver. If any step is authorship rather than derivation, STOP there, named — it becomes a producing-lane spec supplement, typed for the ledger. (2) SPEC ACT (ii): derive dGamma_R's receiver/scope for D_K — from its sealed definition for h_K, the algebra's own structure, and the 914-corrected boundedness facts; the honest output may be a SCOPED receiver (bounded parts in, lifted differential parts affiliation-typed) — derive the split exactly. (3) BOOKING: if both land, state line 6's status precisely: record third BOOKED (Q-820), source third BOOKED (here), field third core-gated — and what the two bookings feed downstream (item 14's finite component; the S4 family side). VERDICT (typed): BOTH-DERIVED (line 6 = 2/3 booked) / ONE-DERIVED (which; the other's stop) / BLOCKED (both stops named).
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_SPEC_ACTS_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `ACT_I = DERIVED / STOPPED (named)`, `ACT_II = DERIVED (scope displayed) / STOPPED (named)`, `LINE6 = <n/3 booked>`, `DOWNSTREAM = displayed`, `VERDICT = <typed>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 916_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
