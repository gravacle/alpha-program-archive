## PASTE 945 — DARIO LANE — [PLAN:AXN-BUILD-D44] THE COMMON-DOMAIN/AFFILIATION PROOF ATTEMPT (LINE 6'S THIRD REMAINDER)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. THIS TASK TOUCHES NO ANCHOR OBJECT, NO GAUNTLET CLAUSE, AND NO GALERKIN SELECTOR — it is the one line-6 remainder that is pure analysis on settled ground.)
GROUND, verify seals BEFORE reading: your 924 (c29fe710…) and the 926 check (107aa750…), which settled the obligation's exact shape: at a common scope both summands of the D_K split fall on the same membership side (M_c infinite-rank, not trace-class), the genuine difference sits at the ONE-PARTICLE BOUNDEDNESS line in the affiliated operator's DOMAIN — and 926's precise gap: "bounded non-trace-class one-particle input still has an unbounded second quantization, and no sealed relative-domain theorem makes its domain subordinate to the massless differential lift." The settled trace-class criterion (Q-829: dGamma(T) bounded iff T trace-class; ||dGamma(T)|| = max(Sum lam+, Sum lam-)) is your sealed tool.
TASK — ATTEMPT THE RELATIVE-DOMAIN THEOREM: for the write term's one-particle operator M_c (bounded, infinite-rank, NOT trace-class) and the massless differential lift dGamma(h_0), prove or refute: (1) DOMAIN SUBORDINATION — is Dom(dGamma(h_0)) (or a sealed common core) contained in Dom(dGamma(M_c)) with a relative bound ||dGamma(M_c) psi|| <= a ||dGamma(h_0) psi|| + b ||psi|| on the relevant sector (the number operator route: on the n-particle sector ||dGamma(M_c)|| <= n ||M_c||, so dGamma(M_c) is N-bounded — display whether N is h_0-bounded ON THE SEALED SECTOR with its infrared structure, at bytes; a massless h_0 has no gap, so N <= c h_0 FAILS naively — show exactly where, and whether the sealed sector's structure rescues or kills it); (2) if subordination fails in general, the HONEST CHARACTERIZATION: the exact condition on the sector (a gap, an infrared cutoff, a charge restriction) under which it holds — each condition tested against what the record actually seals, law 9b where absent; (3) the consequence either way for the recombined domain and the source third's booking conditions — display, do not book. STOP where an estimate genuinely resists, with the failing step shown — an honest stop with the obstruction displayed is a full result (941's standard). NO numeric evaluation of physical quantities; operator norms and abstract bounds only. BLIND: no rank, no ratio, no fiber comparison.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. All results CLAIMED until the opposite-lane check. PE-1..13 pointer-only.
OUTPUT: one sealed artifact `STAGE8_AXN_COMMON_DOMAIN_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `SUBORDINATION = PROVED (bound shown) / FAILS (exact failing step) / CONDITIONAL (condition named, record status law-9b)`, `N_ROUTE = displayed (where the massless gap bites)`, `CHARACTERIZATION = complete / partial (what remains)`, `CONSEQUENCE = displayed (booking conditions, unbooked)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 945_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
