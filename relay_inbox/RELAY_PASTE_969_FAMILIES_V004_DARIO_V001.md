## PASTE 969 — DARIO LANE — [PLAN:AXN-BUILD-D68] FAMILIES V004 — THE FIVE PACKAGING FIXES AND THE REAL SUITE WRAPPER

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. Numeral grep AND a tuple-parse replay on every closed tuple before sealing — the field-count check is now part of your own seal discipline.)
SUBJECTS, verify seals BEFORE reading: your 966 (25845e9223e6…); the check (STAGE8_AXN_FAMILIES_V003_CROSSCHECK_CODEX2_V001.md, ff587bada82e…, in workspace/). The check confirms the MATHEMATICS (Lemmas clean; joint quantifiers; basis independence 10/10) and fails the PACKAGING on five named items. The governing instrument is now V004 (a195edb03b36…, approved Q-881).
TASK — EXACTLY THE FIVE, mathematics byte-carried: (1) THE TUPLE ESCAPE: FC-03/FC-08/CERT-IF carry literal | bytes inside ker(Phi|Delta_0,N) breaking the 8-field parse — fix by the declared serialization's own escape (or an equivalent notation like ker(Phi restricted-to Delta_0,N)); EVERY tuple re-parsed to its declared field count before sealing. (2) THE SUITE WRAPPER: build the actual JointAnchorCertificateSuite.v001 INSTANCE per V004's closed schema — the outer fields (carrier_sha256 from the booked carrier; a0_rank_fiber_sha256 as the blind digest copy; the BOUNDED_CLASS inventory content per the sealed instantiation; restriction_and_limit_square_receipts; canonical_serialization; replay_entry_point_sha256; suite_sha256; frozen_pre_output_receipt_sha256) wrapping the for_class families AT THE CORRECT SCHEMA PATH (finite_stage_inventory.BOUNDED_CLASS.for_class — no invented path); Cert_IF_joint and Cert_A_joint each instantiating THIS suite per JAC-14's aligned row. (3) THE TWO EDGES: FC-05's derivative ground corrected to a certificate that actually proves what FC-05 consumes (or the consumption restated to what the booked certificate proves); FC-07's certificate conclusion SENT to a procedure (received, not listed). (4) FC-08/CERT-A BI BINDING: cite the entered {BI} tag and BI-content bytes at their spans IN JOINT_ANCHOR_DECISION_INSTANCE_V003.md (089af246cbc0…) — the object where the principal's entries live — not the blank instrument slot. (5) FC-07's RECEIVER: fill fixed_space_and_mixing_certificate_family with the honestly-scoped content stated AS the receiver's content (existence + stage compatibility, the uniqueness exclusion displayed); if the receiver's schema semantics demand strictly more than the grammar can give, STOP that receiver with the bytes shown — do not inflate. BLIND: the fiber digest copies without opening anything.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. All CLAIMED until the opposite-lane check. PE-1..15 pointer-only. Basis independence preserved; no numeric evaluation.
OUTPUT: one sealed artifact `STAGE8_AXN_FOR_CLASS_FAMILIES_DARIO_V004.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `TUPLES = ALL-REPARSED-AT-COUNT (list)`, `SUITE = INSTANTIATED (suite_sha256 shown; correct path) / STOPPED (what)`, `CERTS = INSTANTIATE-THE-SUITE / STOPPED`, `EDGES = FC-05+FC-07 CORRECTED`, `BI = BOUND-AT-INSTANCE-BYTES (spans)`, `FC07_RECEIVER = FILLED-AS-SCOPED / STOPPED (bytes)`, `MATH = BYTE-CARRIED-VERIFIED`, `BASIS_INDEPENDENCE = PRESERVED`, `BLIND = HELD`, `NUMERAL_GREP = RUN`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 969_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
