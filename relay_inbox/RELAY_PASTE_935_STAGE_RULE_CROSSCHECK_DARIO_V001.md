## PASTE 935 — DARIO LANE — [PLAN:AXN-BUILD-D34] CROSS-CHECK THE STAGE-RULE CONSTRUCTION AND REPLAY THE SEVEN RECEIPTS (933)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BUILDER-NEVER-VERIFIES: the subject is the opposite lane's. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit.)
SUBJECT, in workspace/, verify seal BEFORE reading: STAGE8_AXN_STAGE_RULE_CONSTRUCTION_CODEX2_V001.md (438e6c42bf876678eb0b3c016e231228f807e4f29469c5e221597d716642f0e6). Its claims are CLAIMED until this check. NOTE: the finder-builds custody shape — the same lane FOUND the F_cyl route (931) and BUILT from it (933); your check is the only independent eyes this construction gets. Press accordingly.
TASK: (1) STAGE SYSTEM — re-derive independently from the sealed spans (member [7090,8455) F_cyl(C0_008); [5412,5787) connecting maps): the index set, per-stage carrier, record map iota_NM = (·) tensor I_(M-N), label zero-extension, the two CTP branches beta_NM, the joint map J_NM, the composition laws J_ML∘J_NM = J_NL and J_NN = id, the limit target landing in the sealed A_C0. Verify the payload hash REPRODUCES (986cc0585d7513aed30151db4bd5aeb774104ab27bbcd4b640f1a8e1615800d4) — byte-identical reconstruction or the exact divergence shown. (2) DERIVATION PURITY — zero freedom claimed: hunt for any step that smuggles a choice (an ordering, a convention, a branch) not fixed by the sealed spans. (3) THE SEVEN RECEIPTS — REPLAY each RL-01..RL-07 per JAC-14's replay format (that is the format's claim; test it), verify each digest and the ordered-list root (20f12ff18ce3…). (4) THE BOUNDARY — verify the honesty claims: zero complete suites claimed; the infinite-family-vs-finite-list inventory tension accurately stated (does the schema truly demand a finite list, and would a cutoff truly consume a new freedom?); the fiber-gated remainder EXACTLY one digest copy (FGR-01) with all non-fiber gates separately named; the source-Galerkin irrelevance display (A_src fixed, id connecting map). (5) the Q-246/Q-839 register span pins — rehash. BLIND: no rank, no ratio, no fiber comparison.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. PE-1..13 pointer-only.
OUTPUT: one sealed report `STAGE8_AXN_STAGE_RULE_CROSSCHECK_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `STAGE_SYSTEM = CONFIRMED-BOOKABLE / CORRECTED (what)`, `PAYLOAD = REPRODUCED / DIVERGES (bytes)`, `PURITY = ZERO-FREEDOM / FREEDOM-FOUND (which step)`, `RECEIPTS = 7/7-REPLAYED / (which fails)`, `ROOT = CONFIRMED / FAILS`, `BOUNDARY = HONEST / OVERCLAIMED (what)`, `PINS = CONFIRMED`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 935_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
