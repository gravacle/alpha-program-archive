## PASTE 1054 — DARIO LANE — [PLAN:DESC-32] SURFACE-FIRST V002 — THE QUARANTINE ACCOUNTING, REPAIRED HONESTLY

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies (queues behind 1052). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs.
SUBJECTS, verify seals BEFORE reading: your surface-first V001 (953aa85b3aa7…), the preform (73ef21bdbfef…), and the check (912c708e328a…) — whose QUARANTINE = BROKEN finding this relay repairs: the sealed preform was NOT carried byte-identically into V001's Steps 1–2 (substantive baseline, same-projector/A0, and TYPE-P changes made after the form was opened), and the exclusion-list chronology lacks a pre-reading sealed receipt.
TASK — DOCUMENTARY V002, append-only, V001 byte-untouched: (1) an honest quarantine account — what the preform contained, what changed after the form was opened, WHEN each change entered, and which of V001's claims were touched by post-form edits versus which stand on pre-form content; the diff published as spans WITH digests on both sides (Q-930 standard — the manifest pattern, not prose). (2) Carry the check's corrections in kind: κ_ch is ONE COMPUTATION (a consistency check — restate the claim in its lawful form); the residue is WIDER than the Attach map alone (the six open conditions listed as the check lists them). (3) No substantive re-derivation: the check independently re-derived the load-bearing content and it stands; this relay repairs the ACCOUNTING. If any V001 claim cannot be honestly re-grounded once the quarantine account is written, say so plainly — that is a finding, not a failure.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..17 pointer-only. All standard fences; ω_phys untouched; no member bound; no numeric evaluation.
OUTPUT: one sealed artifact `STAGE8_DESC_SURFACE_FIRST_DARIO_V002.md` (+ generator if computation runs).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `QUARANTINE_ACCOUNT = SPANNED-WITH-DIGESTS (<n> post-form changes itemized)`, `CLAIMS = <p>-PRE-FORM / <q>-POST-FORM-TOUCHED (dispositions)`, `KCH = RESTATED-AS-CONSISTENCY`, `RESIDUE = SIX-CONDITIONS-CARRIED`, `V001 = SUPERSEDED-APPEND-ONLY (digest)`, `PROSE_DIGESTS = <n>/<n>`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1054_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
