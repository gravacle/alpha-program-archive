## PASTE 988 — DARIO LANE — [PLAN:DESC-4] CROSS-CHECK THE DESCENT AUDIT (OVERNIGHT-CHAINED)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. OVERNIGHT CUSTODY: the subject's digest is NOT pinned here — verify the subject against ITS OWN SIDECAR before reading, RECORD the digest in your report; the registrar re-pins at morning processing. BLOCKED GUARD: if the subject file does not exist in workspace/, write relay_outbox/988_BLOCKED.md naming the missing subject and STOP.)
SUBJECT: STAGE8_DESC_AUDIT_CODEX2_V001.md in workspace/ (the opposite lane's law-10 audit; mirrored by the registrar on its DONE). BUILDER-NEVER-VERIFIES holds. Your 900-section experience is the reference: this audit plays the role relay 900's audit played for the anchor section — a wrong classification here misprices the whole section.
TASK: (1) THE ENUMERATION — re-run your own register-first, four-mode hunt for sealed descent-bearing objects; anything the audit missed, anything it over-included; (2) THE STATUSES at bytes — the frozen trace; the four maps; the four-field signatures; the placement square — each re-read at its span (the Q-898 DERIVED-HERE rule applied to every provenance call, both directions); (3) THE CLASSIFICATION pressed — every SELECTOR call tested (is it genuinely unforced? the F_cyl lesson BOTH ways), every FORCED call traced past its proximate source; (4) THE STOP RULE and THE BASIS LIST — right order? minimal? anything downstream smuggled upstream? VERDICT: AUDIT-CONFIRMED (the basis list stands; selectors to the principal) / CORRECTED (what moves).
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..15 pointer-only.
OUTPUT: one sealed report `STAGE8_DESC_AUDIT_CHECK_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `SUBJECT_DIGEST = (as verified)`, `ENUMERATION = COMPLETE / MISSED (what) / OVER (what)`, `STATUSES = CONFIRMED / CORRECTED (which)`, `CLASSIFICATION = CONFIRMED / MOVES (which, to what)`, `STOP_RULE = RIGHT / CORRECTED`, `BASIS = STANDS (n items) / AMENDED`, `VERDICT = AUDIT-CONFIRMED / CORRECTED`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 988_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
