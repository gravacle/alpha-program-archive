## PASTE 913 — DARIO LANE — [PLAN:AXN-BUILD-C11] CHECK THE CORE PAIR (910 + 912)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. BUILDER-NEVER-VERIFIES. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. SUBJECTS: Codex 2's two core artifacts — 910 (STAGE8_AXN_ORIGIN_PRODUCER_CODEX2_V001.md, 4574b5a1…) and 912 (STAGE8_AXN_ORIGIN_MORPHISM_HUNT_CODEX2_V001.md, 1278b700…), both mirrored to workspace/; verify seals BEFORE reading. Claims CLAIMED until this check — and these two carry the campaign's steering weight: the fork documented at Q-818 rests on them.
TASK: (1) 910: verify the StatePort display, the DoR-013 family/ray/law finding at bytes (the raw material's genuineness is the fork's premise), the SM 0/8 accounting, the B0-adjacency discipline, and the four route closures. (2) 912: verify the seven modes' per-mode results at their bytes (each candidate genuinely states-not-supplies), the retained-fragments list, the exact-gap typing (the certificate package), and the closed-at-declared-scope discipline (no impossibility leakage; the derivable typing intact; no supply recommendation smuggled). (3) THE FORK'S PREMISES, adversarially: is any of the seven modes' closures cut by query shape (the recurring lesson) — name any vocabulary family NOT swept that a future round should run; and is the DoR-013 family genuinely a PRODUCER-INPUT (target-independent) rather than something whose use would violate the no-supplementation certificates?
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_CORE_PAIR_CROSSCHECK_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `P910 = CONFIRMED / CORRECTED (which)`, `P912 = CONFIRMED / CORRECTED (which)`, `UNSWEPT = named families / NONE`, `DOR13 = PRODUCER-VALID / TENSION (displayed)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 913_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
