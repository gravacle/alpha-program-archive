## PASTE 952 — DARIO LANE — [PLAN:AXN-BUILD-D51] CROSS-CHECK THE COMPLETED INSTANCE V003 (THE 18 TRANSCRIPTIONS AND THE SUITE READING)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BUILDER-NEVER-VERIFIES: the subject is the opposite lane's; you authored the V002 instance whose 19 stops it fills — your standing is exactly right for this check.)
SUBJECT, in workspace/, verify seal BEFORE reading: JOINT_ANCHOR_DECISION_INSTANCE_V003.md (089af246cbc0d66e6ce70971dbb14d355a78ee0f5e294706a1acaeacd0d4236d). References: your V002 instance (72191e0115d6…); the booked build V002 (fd2625a079c7…); the round-2 DoR (bca010f10771…); the governing instrument (58b966ed…).
TASK: (1) THE 20 CARRIED FIELDS — byte-verbatim from your V002 instance (verify against YOUR OWN artifact). (2) THE 18 TRANSCRIPTIONS — each filled digest matches its booked source object exactly; the two mechanical serializations (the P_0 pointer; the A0RankFiber and history-control objects) replay canonically from their sealed schemas; FGR-01 is a pure digest copy; BLIND audited by endpoints (no rank-shaped anything). (3) THE STAGE PIN update displayed correctly (old/new). (4) THE SUITE STOP — THE RULING THAT MATTERS: read the suite schema at its exact bytes [22341,23332) and rule the READING QUESTION: does finite_stage_inventory quantify over ALL stages of the infinite grammar (then the stop is real and the field cannot complete without a new freedom), or over the certificates/stages actually PRODUCED (then the seven receipts + root ARE a lawful finite inventory and the field completes with zero new freedom)? Read it as the schema's own text forces, not as convenience prefers — display the deciding bytes. If the stop is REAL, enumerate the lawful exits for the principal: (a) a cutoff freeze (name exactly what would be frozen and its price), (b) an instrument supersession adding the bounded-class escape (name what that changes about the ADOPTED act's governing terms and whether it is lawful post-adoption), (c) run the gauntlet fail-closed on the field (name what a rejection there does and does not mean). NO recommendation — exits displayed with consequences. (5) G0 audit: 5-addressed verified; the candidate grammar span pin rehashed. VERDICT: INSTANCE-BOOKS-AS-SCOPED (38/39 with the routed stop) or what fails.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..14 pointer-only.
OUTPUT: one sealed report `STAGE8_AXN_INSTANCE_V003_CROSSCHECK_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `CARRIED = 20/20-VERBATIM / (deviations)`, `TRANSCRIPTIONS = 18/18-EXACT / (which fails)`, `SERIALIZATIONS = REPLAYED / (which fails)`, `STAGE_PIN = CONFIRMED`, `SUITE_READING = PRODUCED-QUANTIFIED (completes, bytes) / ALL-STAGES (stop real, bytes)`, `EXITS = displayed (a/b/c consequences) / N-A`, `G0 = CONFIRMED (5+1)`, `BLIND = ENDPOINTS-CLEAN`, `VERDICT = INSTANCE-BOOKS-AS-SCOPED / FAILS (what)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 952_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
