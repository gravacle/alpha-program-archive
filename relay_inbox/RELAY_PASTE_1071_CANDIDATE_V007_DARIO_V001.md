## PASTE 1071 — DARIO LANE — [PLAN:DESC-33] CANDIDATE V007 — RESTORE THE CLOSURE BINDINGS; THE NUMBERING TRAP'S TENTH FIRING, REPAIRED

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs; the mandate (ad9fc14e1f07…) verified live; V001–V006 all byte-untouched, superseded append-only. Header verbatim: `PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL ENTRY (ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001, ad9fc14e1f07…)`.
SUBJECTS, verify seals BEFORE reading: V006 (workspace/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V006.md, 0701f49e4183…) and its check (workspace/STAGE8_ATTACH_CANDIDATE_V006_CHECK_CODEX2_V001.md, f9081a222707… — the binding finding: 34 body citations, 19 undefined / 13 rebound / 2 intact under the 6-row table); V005 (96ec8bf4e270…, read-only, the source of the 27-member table).
TASK — THE BINDING RESTORATION, per the check's specification: (1) the closure table carries V005's member IDs 01–27 UNCHANGED (same IDs, same files, same digests — extracted from sealed V005, digest-verified); (2) the three V006-specific objects (the 1069 assignment, V005 itself, the V005 check) APPEND as new IDs 28–30; (3) closure and certificate counts updated; the generator's expectation updated and it VERIFIES EVERY BODY CITATION RESOLVES — a new refusal path: any member-number citation in the body that is undefined or rebound relative to the table refuses the seal (the structural check the trap has earned ten firings over); (4) everything else byte-identical to V006 by span (the blocks, the fences, the map, the operative content — all confirmed perfect at 1070, all carried).
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..17 pointer-only. No member bound; no numeric evaluation; no freeze; ω_phys untouched. NO OUTPUT INSPECTION.
OUTPUT: one sealed artifact `STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md` (+ sealed generator with the citation-resolution refusal path).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `TABLE = 27-CARRIED + 3-APPENDED (IDs stable)`, `CITATIONS = <n>/<n>-RESOLVE (generator-verified)`, `CARRIAGE = SPANNED (all else byte-identical to V006)`, `MAP = <computed>`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `PROSE_DIGESTS = <n>/<n>`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1071_DONE.md, then chained pickup (1066's BLOCKED remains expected). No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
