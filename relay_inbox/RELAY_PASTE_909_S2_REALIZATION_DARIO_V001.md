## PASTE 909 — DARIO LANE — [PLAN:AXN-BUILD-C7] THE S2 REALIZATION: THE EPSILON FAMILY ON THE ALGEBRAIC ROUTE

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. PHASE: basis line 4. THE ACCEPTANCE SYSTEM IS SETTLED (Q-796/798): five receiver groups — E1 the family itself; E2 rebound (the sign NOT derivable from the arrow; the epsilon-domain restored); E3 equivariance bound to the complete prescription; E4 the common domain (S4's kept distinct); E5 an operator topology on the family's codomain (topology_src adjacent wrong-type). THE NEW INSTRUMENT: the 904 algebraic route (the cylinder algebras' C(Y) identification; states-as-measures) is the record's native idiom — and an epsilon-indexed ANALYTIC OPERATOR FAMILY may be constructible the same way: resolvent families and analytic semigroup elements live naturally in the algebra's own function calculus. The 903/905 discipline governs: derive what's determined; STOP at the first genuine choice, named; routes-closed vs unsearched distinguished. SUBJECTS, seal-verify BEFORE reading: the S2 pair (30cf4e87…, c0dfb78d…); the 904 route artifact (aaffb7be…, mirrored); the endpoint-orientation receipt; the frozen selectors.
TASK — CONSTRUCT AGAINST THE FIVE: (1) E1 VIA THE ALGEBRA: does the sealed function calculus on the branch algebra (or the realized free-tail operator's resolvent family — strong-resolvent convergence IS an epsilon-indexed family in embryo) DETERMINE an epsilon-indexed analytic operator family at the S2 receiver? Derive it or stop named. (2) E2: the epsilon-domain and insertion rule as the sources determine them (the sign carried UNDERIVED per Q-798). (3) E3: the equivariance against the sealed involution. (4) E4: the family's common domain (from the free domain if the family is resolvent-born). (5) E5: the topology the family's own convergence carries (strong-resolvent as the candidate — derived, not imported). Acceptance clauses run as they become runnable. VERDICT (typed): FAMILY-DERIVED (clauses passed) / PARTIAL (stages + stop) / BLOCKED (named; routes-closed vs unsearched).
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_S2_REALIZATION_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `E1 = <result>`, `E2_E5 = <per-receiver>`, `ACCEPTANCE = <runnable clauses passed>`, `VERDICT = <typed>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 909_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live; no smooth import; no numeric evaluation of physical quantities.
