## PASTE 890 — DARIO LANE — [PLAN:AXN-BUILD-B18] CROSS-CHECK THE S4 WRITE-DEFECT DERIVATION

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. BUILDER-NEVER-VERIFIES. SUBJECT: Codex 2's 889 — STAGE8_AXN_WRITE_DEFECT_INVERSE_CODEX2_V001.md (412cd22c…), mirrored to workspace/; verify seal BEFORE reading. Claims CLAIMED until this check. Verdicts under test: 24 necessary conditions for S4's physical half; CONSTRAINT-GAP with named missing inputs (joining-law provenance; carrier/root maps; admissible prepared root; complete defect spectrum; named family/common domain; closure/durability certificates); the conditional residual R_889 displayed uninhabited; the Layer-P 4/8 split reproduced EXACTLY as a corollary; item 14 carried distinct.
TASK: (1) Re-derive or verify each weight-bearing constraint at its cited bytes — especially the moment separation (finite first moment vs generator-domain squared moment for the prepared root; your own 869 arithmetic bears here) and the covariance-equation conditionality on the transported write formula (the after-the-fact selection bar). (2) THE COROLLARY CONTROL: verify the 4/8 reproduction is exact — the four realized conditions FOLLOWING from the system at free-tail scope, the four open landing precisely on derived constraints (this is the method's self-test; press it). (3) The residual quotient's typing and the void-condition press (nothing selected by the verdict). (4) Law 9b: the closure declared before every absence — audit the declaration order. (5) The item-14 discipline: same-family-distinct-receivers carried without merger.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_WRITE_DEFECT_CROSSCHECK_DARIO_V001.md`
Final lines: `CONSTRAINTS = CONFIRMED (n) / CORRECTED (which)`, `COROLLARY = CONFIRMED-EXACT / CORRECTED`, `RESIDUAL = CONFIRMED / CORRECTED`, `CLOSURE_ORDER = COMPLIANT / DEFICIENT`, `ITEM14 = carried-distinct-confirmed / CORRECTED`, `S4_PHYSICAL = <status per check>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 890_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
