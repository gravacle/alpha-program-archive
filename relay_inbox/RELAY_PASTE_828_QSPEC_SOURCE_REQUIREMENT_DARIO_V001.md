## PASTE 828 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:F0-3/D2-PRE] WHAT DOES THE COMPLETE Q_SPEC REQUIRE OF THE SOURCE SECTOR?

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. GOVERNING: your 827's residual-1 — "a statement of what the COMPLETE Q_SPEC requires of the source sector" is the missing sealed sentence; with it, the last source-question address closes completely. Also residual-2: whether kappa_Thomson's "regulator independence" is the completion's continuum_regulator_independence (O3) — no sealed sentence merges or separates them. Both hunts, one relay. NOTE the self-favor guard: 827 settled a Dario ambiguity in Dario's favor with disclosed checks; THIS relay's findings will get the opposite lane's audit before consumption — write for that audience.)

TASK: (i) extract the complete Q_spec's sealed specification wherever it lives (R3's status block context; the D2 nine-slot spine; V011's Q_spec spans; the charged-spec stock) — what it IS, what freezing it requires, and EVERY sentence touching its relation to the source sector (does its construction consume a source-inclusive object, finite source data only, or is it silent — the tripartite verdict, span-cited); (ii) hunt the regulator-independence relation: any sealed sentence connecting Thomson's "regulator independence" need to the completion's O3, either way; (iii) VERDICT on 827's residuals: RESOLVED (the sentences found — the source question's closure is then complete and unqualified) / the missing sentences confirmed absent (the residual stands as a named open, with what would author it lawfully). FREEDOMS-CONSUMED block; FLATTENING CHECK. All headline items CLAIMED. PE-1..11 pointer-only.
OUTPUT: one sealed artifact `STAGE8_F0_QSPEC_SOURCE_REQUIREMENT_DARIO_V001.md`
Final lines: `QSPEC = extracted (spans)`, `SOURCE_RELATION = consumes-infinite (span) / finite-only (span) / SILENT (confirmed absent)`, `REGULATOR_RELATION = merged (span) / separated (span) / silent`, `RESIDUALS = resolved / standing (named)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 828_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. Hunt only; no identity assumed either way; no smooth import; no EM identification.
