## PASTE 798 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:B1b-2] THE INDEX BRIDGE: EXHAUSTION TO REFINEMENT

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL alignment laws apply incl. 8. GOVERNING: your own 792 (Q-701) — B1b's real obstacle is the INDEX MISMATCH: the support law is indexed by the causal EXHAUSTION ("Growing volume, not subdivision"), Ref_a by SUBDIVISION; no sealed object carries one to the other; of record since IDX 3.3. The two unblock paths, verbatim: (a) a sealed object carrying the exhaustion index to the refinement index; (b) a proof that the support correspondence is simultaneously cofinal-invariant and Ref_a-natural. The sealed CAUSAL_DIRECT_LIMIT_* family (packet-manifest verified, scope flags false) is the architecture to build against.)

TASK — settle the bridge, hunt before build, build before rule:
AS1. PATH (a), THE HUNT: sweep the sealed stock for ANY object relating the two index categories — a functor, a cofinal embedding, a two-index diagram, a compatibility law between exhaustion stages and subdivision stages (token forms + content synonyms; the CDL family, IDX 3.3's own context, the tau-density stock, the conservation law's four-volume carrier, 755's exhaustion spans). Meaning-probe every hit; law 8 on every seal.
AS2. PATH (b), THE POSING: state precisely what "simultaneously cofinal-invariant and Ref_a-natural" requires of the support correspondence, as typed equations against the CDL architecture and the (now PROVED) carrier incidence half — the same generator-level machinery. Determine which parts are provable from stock (prove them), which fail (display), which are unposeable (name the missing object — the 786 standard).
AS3. CLASSIFY THE BRIDGE ITSELF: FORCED (a unique bridge exists or the two-property proof closes — display), FREE (inequivalent bridges — two complete displays), UNDECIDABLE (missing object named). STATE what B1b and C1 each inherit under the verdict: if (b) proves, the common cell's cofinality-invariance requirement gets its carrier; say exactly what remains for C1 either way.
AS4. FREEDOMS-CONSUMED block (item-by-item true; law 2a); FLATTENING CHECK against DECLINE_REGISTER_V002. All headline items CLAIMED. PE-1..7: pointer-known, zero weight, do not consult.
OUTPUT: one sealed artifact `STAGE8_B1B_INDEX_BRIDGE_DARIO_V001.md`
Final lines: `HUNT = <n> hits, <n> survivors (listed) / NONE`, `PATH_B = posed (<n> equations); proved: <list> / failed: <list> / unposeable: <named>`, `BRIDGE = FORCED (displayed) / FREE (two displays) / UNDECIDABLE (object named)`, `B1B_STATUS = <stated>`, `C1_INHERITS = <stated>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 798_DONE.md, then chained pickup (smallest relay number > 798 addressed to DARIO; if absent, STOP cleanly). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants. No complex selected; no smooth import (S26); no EM identification (S08). No common cell formed; no junction map evaluated.
