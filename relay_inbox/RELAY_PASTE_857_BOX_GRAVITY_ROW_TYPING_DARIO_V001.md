## PASTE 857 — DARIO LANE — [PLAN:AXN-BUILD-A24] TYPE THE BOX_GRAVITY ROW ITSELF: FORCED HOLE, OR IMPORTED EXPECTATION?

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. SUBJECT BASIS: the build census rows for BOX_gravity/T10 (STAGE8_AXN_BUILD_PACKAGE_V3 lineage + both closure rounds, as sealed) and the sealed sentences that put gravity into the build. THE QUESTION: the BOX_gravity row is typed as a HOLE — content expected in the action's term table, none derivable, no instrument reaching it through two closure rounds. IS THAT TYPING FORCED OF RECORD? Two competing types, NEITHER to be preferred in advance: (T) TERM-SHAPED — some sealed sentence requires gravity to appear AS A TERM/content row of the action; (S) STRUCTURE-SHAPED — gravity of record enters as structure of the action's lawfulness (variation admissibility, the allow/require conversion, boundary/descent structure), and the row imported a classical expectation (the barred KK shape wearing census clothing).
TASK A — THE GENEALOGY AT BYTES. Where did the row COME FROM: display the sealed sentences that created BOX_gravity/T10 in the build. For each, classify: does it FORCE a term (what receiver consumes a gravity TERM, byte-cited), or does it record an expectation/placeholder? Law 9: enumerate every sealed consumer of "gravity" content in the build's scope — what SHAPE does each consumer actually demand (a term? an equation form at the junction? a recognition verdict? a conversion mechanism?).
TASK B — THE UNREACHABILITY DIAGNOSTIC. Two closure rounds show NO instrument receives the row. Type this signature against the program's own precedents of record (the function-space demand Q-751; prior dissolved debts): is unreachability-by-every-instrument evidence the row is (T)-typed-but-deep, or (S)-mis-typed? Do NOT decide by pattern-resemblance — display what a (T) verdict would require of record (a named consumer demanding a term) and what an (S) verdict would require (the row's creating sentences shown expectation-only PLUS a sealed structural carrier for gravity's role). If neither is forced, say NEITHER-FORCED and display what object would decide it. Stop-on-freedom: a typing verdict is not a license to delete or fill the row.
TASK C — SMALL, SEPARATE, PROMISED: spot-check the frontier map's counting convention (STAGE8_AXN_BUILD_ROUND2_FRONTIER_MAP_CODEX2_V001.md, 5e163d85…, opposite-lane product): are OBLIGATIONS=11 (2 FREEDOM / 8 ABSENCE / 1 conditional) and the no-double-count claim correct against the sealed contracts? Report CONFIRMED / CORRECTED with the arithmetic displayed.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..12 pointer-only — in particular do NOT open or consume PE-9/PE-12; this relay's question must be answered from sealed stock alone.
OUTPUT: one sealed report `workspace/STAGE8_AXN_BUILD_BOX_GRAVITY_ROW_TYPING_DARIO_V001.md`
Final lines: `ROW_ORIGIN = displayed (bytes)`, `CONSUMER_CENSUS = displayed (each consumer's demanded shape)`, `ROW_TYPE = TERM-FORCED (consumer displayed) / STRUCTURE-INDICATED (carrier displayed) / NEITHER-FORCED (deciding object named)`, `UNREACHABILITY = DIAGNOSED (which reading it evidences, and why at bytes)`, `MAP_COUNT = CONFIRMED / CORRECTED`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 857_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. Charter fences live; nothing selected; no smooth import; no EM identification; no member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants.
