## PASTE 997 — DARIO LANE — [PLAN:DESC-13] CONSTRUCTION OPENS: BUILD B01

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. Full digests; SCOPED grep; fixed-point closure; the set-delta rule; the tuple-parse rule; reproducible builds where programmatic.)
THE STATE: the supply map is closed (audit V002, 078d6d54a259…, SUPPLY-CLOSES at Q-909: buildable through B02; B03 the gate); the demand stands at V004 (bed63c314a77…, final confirm running in the opposite lane — its outcome does not alter B01's row). THE DESCENT STOP RULE IS LIVE: no downstream replay before its inputs; no selector consumed; forced-downstream never means favorable.
TASK — BUILD B01, EXACTLY AS THE SUPPLY MAP DEFINES IT: (1) read basis row B01 at its bytes in STAGE8_DESC_AUDIT_CODEX2_V002.md (workspace/) — the row's own object, grounds, and acceptance are your entire scope; build THAT item and nothing beyond it (B02 is the next relay, not this one); (2) construct against the demand V004's applicable conditions (read which of the 36 bind B01's object — display the applicable subset at bytes); (3) every ingredient from sealed ground per the row's own pins; ANY freedom encountered STOPS with the freedom named (law 10 — the audit classified B01's ingredients; if the build surfaces one the audit missed, that is a finding, not a license); (4) the output sealed with the full current discipline (programmatic construction with the invocation sealed where applicable; set-deltas computed before claimed; diffs by the named convention). BLIND held; every scale symbolic; no numeric evaluation of physical quantities.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. All CLAIMED until the opposite-lane check. PE-1..16 pointer-only.
OUTPUT: one sealed artifact `STAGE8_DESC_B01_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `B01_ROW = quoted (bytes)`, `APPLICABLE_CONDITIONS = listed (which of the 36)`, `B01 = BUILT (acceptance shown) / STOPPED (freedom named) / PARTIAL (what stands, what stops)`, `GROUNDS = SEALED-ONLY-VERIFIED`, `BLIND = HELD`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 997_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
