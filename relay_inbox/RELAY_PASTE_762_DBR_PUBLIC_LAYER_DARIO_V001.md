## PASTE 762 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / Q-126 ROUTE] THE PRIMARIES READ; THE PUBLIC LAYER SCOPED

PICKUP-ACK + LANE GUARD in force (DARIO).
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL standing laws apply. NEW IN YOUR REACH: workspace/mirrored_sources/ now holds the superconnection principle (ae1d0492…), the BR closure-operator structural spectral-gap determination, and the two-cutoff continuum theorem — the primaries 760 recorded absent.)

TASK:
AM1. READ THE PRIMARIES: verify their seals/status blocks; extract what the defining principle fixes about D_BR that the geometry quotation did not; and verify-or-refute 760's raw-tower form against the report said to hold "an exact discrete product-spectrum family for the untwisted adopted skeleton" (if the family is there, display the exact comparison — agreement or divergence, term by term).
AM2. SCOPE THE PUBLIC LAYER: the derived public quotient + linear Tr_BR + null/private-mode rule + branch selection + bottom positivity/simplicity — for each: its sealed demand text, the four TYPE-R-refuted shortcuts displayed (so nobody re-walks them), what a lawful derivation requires, and whether sealed stock determines it or a named element is free (no adoption; forks displayed for the principal).
AM3. THE NORMALIZATION'S DEMAND: the transfer to the L_BR proper-time consumer — its sealed statement, what the beta-carrying step must preserve, and where the S5.3-class burden lands.
OUTPUT: one sealed artifact `STAGE8_7A_DBR_PUBLIC_LAYER_DARIO_V001.md`
Final lines: `PRIMARIES = read (seals verified; principle's fixings displayed)`, `TOWER_CHECK = agrees / diverges (term-by-term)`, `PUBLIC_LAYER = scoped (per-element: determined/free; refuted shortcuts displayed)`, `NORMALIZATION = demand displayed (burden located)`, `VOID = clean`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 762_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants. No common cell formed; no junction map evaluated; no smooth data derived.
