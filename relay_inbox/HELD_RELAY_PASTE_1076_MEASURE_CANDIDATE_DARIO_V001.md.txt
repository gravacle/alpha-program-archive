## PASTE 1076 — DARIO LANE — [PLAN:DESC-37] THE MEASURE CANDIDATE — AUTHORED PHYSICS UNDER THE MEASURE MANDATE

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs, PLUS the mandate's process discipline as mandatory generator refusal paths FROM V001: generic ingredient extractor (no aliases); two-table closed claim machine-checked before seal; fence depth-walk; citation-resolution gate; every pinned digest computed at the path; carriage-with-structural-edge-check. No git of any kind.
MANDATE GUARD: verify and read `workspace/MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` (b9716661d1a03a16ec7fd06c836de2f74dbae0acafa0564c9241f96e791b420c) BEFORE task work — this relay's authority; if absent or failing its sidecar, 1076_BLOCKED and STOP. This is AUTHORIZED AUTHORING of ω_phys, lawful ONLY here and ONLY in the form the mandate types.
SUBJECTS, verify seals BEFORE reading: the measure hunt (STAGE8_DESC_MEASURE_HUNT_DARIO_V001.md, 03dcaa17c789… — the STILL-CONTINUUM proof, the five DOES-NOT-NARROW rulings, the U1-reality row, ω(Z_N) attainability); the delivery (47f7c1248bcb… — Z_N, q_N, the survivor table); the entered Attach addendum and V007; the S1 receiver typing; the survivor table and Prob(Y)/State(B) structure at their spans; Gen_Omega WITH overlay V002 + DoR-013.
THE TASK — AUTHOR A CANDIDATE ω_phys: a U1-invariant probability measure μ on Y, WITH its descent map and its provenance certificate shape (per the demand map's conditions, stated as a certificate). Construct it consuming everything the record forces (U1-invariance; Prob(Y) positivity/normalization; the descent structure Gen_Omega supplies) so the AUTHORED CORE IS MINIMAL and its exact extent is the artifact's central display.
THE INGREDIENT LEDGER — the mandate's load-bearing clause: EVERY ingredient FORCED (span) or AUTHORED (minimal core, justified by internal coherence only, never downstream fitness); authored-as-forced VOIDS the candidate; when in doubt label AUTHORED.
FALSIFIER HOOKS in the artifact: (a) structural void conditions (which sealed forced results μ must never contradict); (b) blind-test exposure named as POINTERS ONLY, never opened — κ_record, alpha, the battery (μ's exposure is MAXIMAL; naming is not opening); (c) the descent/K4 coherence — how μ, once entered, would meet K4's ω_hist requirement (as a shape, nothing computed).
IF THE CANDIDATE IS THE DIRAC / A SPECIFIC SIMPLE FORM: it MUST be authored as a selection with the selection DISCLOSED — the mandate forbids presenting it as forced and does not favor it. Prefer the WEAKEST μ that types (the least authored structure that yields a lawful U1-invariant probability measure with descent + certificate), so the authored core is as small as honesty permits.
HEADER verbatim: `PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL ENTRY (MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001, b9716661d1a0…)`. Nothing consumes it until the entry addendum exists; say so in the closing lines.
NO OUTPUT INSPECTION — at maximal strength, this being the object most tunable: μ shaped by the record's typed requirements and internal coherence ONLY; κ_record, alpha, the battery never consulted, imagined, or optimized toward. Authoring toward the number is the one unforgivable act.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD — every scale symbolic; CASCADE-TERRITORY stop form live. PE-1..17 pointer-only. No member bound; no fixed-point execution; no end test; no numeric evaluation; no measured-constant comparison; no freeze beyond the authored measure itself, disclosed.
OUTPUT: one sealed artifact `STAGE8_MEASURE_CANDIDATE_PROPOSAL_DARIO_V001.md` (+ sealed generator with all mandated refusal paths).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `MEASURE = CONSTRUCTED (μ typed: U1-invariant prob. measure on Y) / PARTIAL (what)`, `LEDGER = <f>-FORCED / <a>-AUTHORED (core itemized)`, `DESCENT = MAP-STATED / GAP`, `CERT_SHAPE = STATED / GAP (slot)`, `SELECTION_DISCLOSED = YES (what was chosen) / N/A (weakest-typing μ)`, `VOID_CONDITIONS = NAMED (<n>)`, `K4_COHERENCE = SHAPE-STATED / GAP`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `CLOSED_CLAIM = <computed>`, `PROSE_DIGESTS = <n>/<n>`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1076_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
