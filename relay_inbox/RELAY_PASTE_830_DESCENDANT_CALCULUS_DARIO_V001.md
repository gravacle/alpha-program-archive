## PASTE 830 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:AXN-BUILD-B1] THE DESCENDANT CALCULUS: WHAT A CANDIDATE ACTION GENERATES, AND WHAT MEETS IT

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. GOVERNING: AXN_BUILD_CHARTER_V001 (its law binds whole). LEG B, step 1: the replay step of the minimal closure needs a GENERATION CALCULUS — this relay builds it, selection-free, from sealed stock.)

TASK:
AS1. THE GENERATION RULES OF RECORD: from the grammar's licensed moves (the 34+6 span base, corrections applied per 818), the parent's descendant structure (the packet's "generated descendants" content), and Fork-8's admitted exhaustion class: assemble the calculus — given a candidate action presentation, what descendant objects it generates (writes, sectors, updates), by which licensed moves, in what order structure. Every rule span-grounded; nothing licensed that a span does not license (law 9 on the move census: display why the move list is complete OR mark it open-of-record with the gap named).
AS2. THE FALSIFIER INTERFACE: the outgoing-sector falsifiers the replay must run against — extract them verbatim (the durability/threshold conditions; the outgoing-sector falsifier set MF and the charter reference); type each falsifier's input (what descendant data it consumes) so the replay is mechanical once a candidate exists.
AS3. THE REPLAY PROTOCOL, DRAFTED: candidate in, descendants generated, falsifiers evaluated, verdict out — as a closed procedure with no free choices (the 777-rule-draft standard: name any hole that cannot be eliminated). FREEDOMS-CONSUMED block; FLATTENING CHECK. All headline items CLAIMED. PE-1..11 pointer-only.
OUTPUT: one sealed artifact `STAGE8_AXN_BUILD_DESCENDANT_CALCULUS_DARIO_V001.md`
Final lines: `RULES = <n> assembled (spans; census complete / open-of-record: <gap>)`, `FALSIFIERS = <n> extracted verbatim (inputs typed)`, `PROTOCOL = closed (no holes) / holes named (<list>)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 830_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. Selection-free throughout; every charter fence live; no smooth import; no EM identification.
