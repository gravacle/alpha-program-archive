## PASTE 709 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A-HANDOFF] THE FREEZE: RECOGNITION PREREGISTRATION V002 — FINAL

### STANDING QUEUE RULE (installs now, applies from the NEXT relay on)
From the next relay onward, the principal will send you a BARE NUMBER N instead of pasting the relay text. On receiving a number: read the single file matching `/Users/bgm/MB Work/alpha-program-archive/relay_inbox/RELAY_PASTE_N_*.md`, verify its `.seal.sha256` sidecar, execute it under ALL standing session rules exactly as if pasted, seal your outputs as usual, ADDITIONALLY write your full completion report to `/Users/bgm/MB Work/alpha-program-archive/relay_outbox/N_DONE.md`, then STOP. If the inbox file is missing or its seal fails, report that and STOP. For THIS relay (709), also write `709_DONE.md` to the outbox on completion.

(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. THE FIRST-TIME-RIGHT RULES and THE JURISDICTION CHECK apply.)

CONTEXT — the principal has ruled (DECISION_F1_SPLIT_K2_SLOT_2026-08-08.md, sealed, in supervision/): (1) F1 is SPLIT: successor F1' — the weak-rule reading still kills; the scale-dependence reading routes to measurement under K-1/K-3 and can lose by them, never by mere family-hood; original F1 stays sealed, superseded of record. (2) K-2 is a NAMED-OPEN SLOT — kill set freezes as {K-1, K-3}; K-2 fillable only by a sealed grounding, as a surfaced revision. (3) The summary review is complete; the principal approved the freeze as summarized (15 criteria, 3 verdicts + ledger, 2 UNGROUNDED items listed not manufactured).

TASK (bounded):
V1. AUTHOR THE FROZEN PREREGISTRATION `STAGE8_7A_OBS22_RECOGNITION_PREREG_V002_FROZEN.md`: your V001 draft with exactly these deltas — F1' authored per the ruling (display F1 and F1' side by side); K-2 written as the named-open slot; the freeze condition §5 executed (state: FROZEN as of this seal; any later criterion change is a surfaced revision of record). NOTHING ELSE MOVES — the 15 criteria, guards, verdicts, ledger, and the two UNGROUNDED declarations carry byte-faithfully.
V2. PIN CHECK on every citation carried; dry-run any span you re-derive.
OUTPUT: the frozen artifact + updated V001→V002 delta note inside it, sealed.
Final lines: `FROZEN = true`, `DELTAS = F1' + K-2 slot + freeze execution only`, `CRITERIA = 15 unchanged`, `UNGROUNDED = 2 carried`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 709_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. No junction map is computed in this relay.
