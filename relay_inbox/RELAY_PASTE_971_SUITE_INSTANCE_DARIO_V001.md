## PASTE 971 — DARIO LANE — [PLAN:AXN-BUILD-D70] THE SUITE INSTANCE, EXACT-SCHEMA — THE LAST OBJECT

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. Tuple-parse replay + numeral grep + A FIELD-BY-FIELD SCHEMA DIFF against the closed schema before sealing — the wrapper failed on exactly that.)
SUBJECTS, verify seals BEFORE reading: your 969 (28bc43a0a8e8… — the families are DONE; only the wrapper failed); the check (STAGE8_AXN_FAMILIES_V004_CROSSCHECK_CODEX2_V001.md, 3cb349ea0d94…, in workspace/ — its finding 3 is the exact defect list); the governing V004 instrument (a195edb03b36…) at the 5.1 schema bytes.
TASK — BUILD JointAnchorCertificateSuite.v001 AS THE SCHEMA WRITES IT, character for character: (1) every field name VERBATIM from the closed schema (the eight for_class keys by their exact names, not fc_01..; the universal receipt-list field by its schema name); (2) both list receivers as LISTS (the seven receipts enumerated content-addressed, twice where the schema puts them: the BOUNDED_CLASS universal list AND the outer restriction_and_limit_square_receipts, bound by the outer_receipts_identity); (3) bounded_class_definition and explicit_reopening_trigger pointed at the STAGE-RULE ground and the sealed reopening predicate (the V004 instantiation block is the template — not the candidate grammar); (4) NO undeclared fields — the two certificates live OUTSIDE the suite, instantiating it by reference per JAC-14's row; (5) replay_entry_point_sha256 BOUND to a real sealed entry point (the replay procedure object — if none exists sealed, build it as part of this act: the deterministic procedure that re-parses and re-verifies the suite's members); (6) THEN, over the COMPLETE suite bytes: suite_sha256 computed and inserted per the schema's own self-reference convention (read it at bytes — if the schema means the digest of the suite-sans-digest-field, follow exactly that; display the convention reading); (7) frozen_pre_output_receipt_sha256 as the PRE-OUTPUT FREEZE ACT performed now — the receipt sealed over the completed suite bytes before any gauntlet output exists (gate 3 satisfied the only lawful way: by completing the object first). CERT-IF and CERT-A updated to instantiate THIS suite by its final digest. Everything else byte-carried from 969.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. All CLAIMED until the opposite-lane check. PE-1..15 pointer-only. BLIND (the fiber a digest copy); basis independence preserved; no numeric evaluation.
OUTPUT: one sealed artifact `STAGE8_AXN_SUITE_INSTANCE_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `SCHEMA_DIFF = FIELD-EXACT (0 missing / 0 extra / 0 renamed)`, `LISTS = BOTH-ENUMERATED (identity bound)`, `POINTERS = STAGE-GROUND+PREDICATE (not the grammar)`, `REPLAY_ENTRY = BOUND (object sealed)`, `SUITE_SHA = COMPUTED-OVER-COMPLETE (convention shown)`, `FREEZE_RECEIPT = SEALED-PRE-OUTPUT`, `CERTS = REBOUND-TO-FINAL-DIGEST`, `CARRIED = VERIFIED`, `BLIND = HELD`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 971_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
