## PASTE 927 — DARIO LANE — [PLAN:AXN-BUILD-D25] THE NINETEEN-FIELD CANDIDATE SURVEY (WHAT CAN LAWFULLY FILL EACH STOP)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit.)
SUBJECT: your own 925 instance (JOINT_ANCHOR_DECISION_INSTANCE_V002.md, 72191e0115d6f36d2327236e7a6d16e21f953422ba3fb2188b75e3db009cea99) — the NINETEEN stopped fields plus the candidate grammar. This task PREPARES the principal's decision; it makes none. Listing candidates is not choosing; you already proved you know the difference.
TASK — FOR EACH OF THE NINETEEN FIELDS, A CANDIDATE ROW: (a) every sealed corpus object that could LAWFULLY fill the field — path, digest, and the bytes where the object is declared (your 925 already named i_B in the state-algebra map, i_src and E_joint elsewhere: pin those); search the WHOLE PROGRAM corpus, not the cleanroom alone; grep the questions-settled register FIRST — old records exist that name these objects; (b) where NO sealed object fits: ABSENT-OF-RECORD with the law-9b pinned authority table; (c) classify the field's resolution shape: POINTER-ENTRY (the principal points at one sealed object), CONSTRUCTION-NEEDED (a lane must build and seal the object first), or GENUINE-DECISION (the principal must author content — the honestly-adopted species); (d) tag each PROVABLE / PART-PROVABLE / PRINCIPAL'S per the standing law. Where multiple candidates exist, list ALL with the discriminating facts — NO recommendation, NO ranking. PRESS THE TWO YOUR 925 FLAGGED: scalar_pairing_or_trace (what pairings exist sealed, if any — HistoryPairingControl classifies THIS object) and physical_price (what the reverse-A2 guard's lawful content would even be — quote the instrument's own clause at bytes). The A0RankFiber sub-digests: open the sealed DoR-013 source and report whether its components (I_A, P_0, P_ch, the structural rank record) are separably addressable at bytes — if yes those six are likely POINTER-ENTRIES; show the bytes. The candidate grammar: quote what the instrument requires it to be and report whether its content is instrument-shaped (the author lane supplies it) or entry-shaped (the principal does) — BY THE INSTRUMENT'S OWN TEXT, not your preference.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD: no rank, no ratio, no fiber comparison — pin the fiber components by digest without evaluating them. PE-1..13 pointer-only.
OUTPUT: one sealed report `STAGE8_AXN_ENTRY_CANDIDATE_SURVEY_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `FIELDS_SURVEYED = 19/19 + grammar`, `POINTER_ENTRIES = n (list)`, `CONSTRUCTION_NEEDED = n (list)`, `GENUINE_DECISIONS = n (list)`, `ABSENT_OF_RECORD = n (list)`, `GRAMMAR = INSTRUMENT-SHAPED / ENTRY-SHAPED (bytes)`, `BLIND = HELD`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 927_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
