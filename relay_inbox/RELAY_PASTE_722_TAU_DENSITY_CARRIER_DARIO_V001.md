## PASTE 722 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / STEP 8] TAU_DENSITY DEFINED, THEN THE MATRIX RE-RUN — THE DECISIVE TEST

PICKUP-ACK + LANE GUARD in force (DARIO).
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL standing laws apply — note especially the R4-kernel VOID CONDITION your own hunt surfaced: a topology may not be chosen from desired coefficient; the DEFINITION below must be built from V011's operational content alone, never from what any candidate outcome would do to any downstream number.)

CONTEXT (Q-627): the free set is {tau_norm, tau_density}; tau_density has never been defined as a complete topological carrier, so it has never been tested. Your own artifact names the decisive step and calls it cheaper than anything left in the RA27 chain.
TASK:
GG1. DEFINE tau_density as a complete topological carrier, derived from V011's operational statements and any sealed density-class material (object-names probed: density, tau_density, and the objects' own vocabulary). Every element of the definition grounded (source + span, block-covered); any element the sealed sources underdetermine is NAMED — and if the definition cannot be completed without an undetermined element, the deliverable is the partial definition plus the named gap, not an invention.
GG2. RE-RUN THE 3x8 MATRIX with the defined carrier in tau_density's column (tau_cyl stays eliminated; tau_norm's row unchanged unless the definition work surfaces new requires — if it does, test BOTH survivors against them).
GG3. VERDICT: FORCED(tau_norm) / FORCED(tau_density) / UNDERDETERMINED({both}) / DEFINITION-INCOMPLETE (gap named; untestable stands). Nothing is adopted under any verdict; the principal rules on the result.
GG4. LEDGER anything familiar (density topologies rhyme with distribution-theory carriers — zero verdict weight).
OUTPUT: one sealed artifact `STAGE8_7A_TAU_DENSITY_CARRIER_DARIO_V001.md`
Final lines: `DEFINITION = complete / partial (gaps named)`, `MATRIX = re-run, displayed`, `VERDICT = <one of four>`, `NEW_REQUIRES = none / tested against both`, `LEDGER_ENTRIES = k`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 722_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. No common cell formed; no junction map evaluated.
