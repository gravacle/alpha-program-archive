## PASTE 842 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:AXN-BUILD-A9] H1'S INTEGRAND: THE CHOKEPOINT, ON THE NARROWED SHAPE

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. CLAIM-STATUS GATE: verify `STAGE8_AXN_BUILD_ROUND1_CROSSCHECK_CODEX2_V001.md` EXISTS mirrored archive-side with the H1 constraints confirmed (or apply its corrections first); the standard wait-once-then-stop if absent. GOVERNING: your round 1 — H1 BOX_record narrowed to: a sum over primitive incidences of per-cell terms, support in Omega_c, one-use on new record factors, carrying w(s) = 32 min(s,1-s)^3 with no fitted parameter, first-order finite restriction supplying D_rec/inc, square-generating with no independent coefficient, exact completed-record persistence. Domain/coupling/inventory FIXED; THE INTEGRAND and the durability map open. H1 gates the stratum, U1, and leg A whole.)

TASK: derive the integrand on the narrowed shape — what per-cell term satisfies ALL six constraints simultaneously? The solution-set discipline (the 802/807 instrument): pose the constraint system on the per-cell term space; solve exactly; UNIQUE (the integrand derived — display it, and the stratum/U1 consequences) / FAMILY (dimension + parametrization; what further sealed pressure exists) / EMPTY (certificate — a route-level finding) / UNDECIDABLE (the missing object named). The durability map likewise if reachable. DERIVE-NEVER-SELECT absolute. FREEDOMS-CONSUMED block; FLATTENING CHECK. All headline items CLAIMED. PE-1..11 pointer-only.
OUTPUT: one sealed artifact `STAGE8_AXN_BUILD_H1_INTEGRAND_DARIO_V001.md`
Final lines: `SYSTEM = posed (constraints, spans)`, `INTEGRAND = UNIQUE (displayed; consequences) / FAMILY (dim, parametrization) / EMPTY (certificate) / UNDECIDABLE (object named)`, `DURABILITY_MAP = <status>`, `STRATUM/U1 = <what follows>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 842_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. DERIVE-NEVER-SELECT; charter fences live; no smooth import; no EM identification.
