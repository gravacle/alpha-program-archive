## PASTE 777 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / Q-126 ROUTE] THE TWO-MEMBER FAMILY, BUILT; THE DISCRIMINATION RULE, DRAFTED

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL standing laws apply. GOVERNING: DECISION_CARRY_BOTH_KINDS_2026-08-09 (sealed 27c15262…) — the principal adopted NEITHER kind; the route carries both; FAMILY-BEFORE-DETERMINANT is now the route's law: no member evaluated alone, evaluation family-wise only, discrimination rule frozen before any evaluation, members eliminated only by the frozen rule. Also governing: Q-682 (the public-layer core U1–U9 and family C1–C5) and its artifact 083e86e3….)

TASK — formalize the carried family and draft the frozen rule:
AS1. FAMILY BUILD: member A (q = 0) and member B (|q| >= 1/2, |q| carried as the affine parameter per U4). For each member, instantiate the 775 public layer AS TYPED STRUCTURE: its branch set, its bottom expression, its trace form specialization — SYMBOLIC ONLY, no f chosen, no sum evaluated, no numeric spectral value computed. Display the two members SIDE BY SIDE with the uniform core U1–U9 factored out once (shared stratum) and only C1–C5 instantiated per member. The artifact must make visually inspectable exactly WHAT the two kinds share and WHERE they part.
AS2. THE DISCRIMINATION RULE, DRAFTED NOT FROZEN: starting from the route's own falsifier clause (inequivalent completions yielding different determinants), draft the rule as a closed schema: the comparison quantity named symbolically; the family-wise evaluation protocol; the identity/difference criterion; the elimination consequence; the NEUTRAL outcome (comparison never lawfully evaluable). The draft must be executable-in-principle by a future relay WITHOUT further choices — every free choice in the rule is a fitting surface; eliminate them all or name the ones that cannot be eliminated. FREEZING IS REGISTRAR/PRINCIPAL BUSINESS — mark the draft DRAFT-FOR-SEAL.
AS3. CONSISTENCY GATE: verify the family build changes NOTHING in the uniform core — U1–U9 must be derivable identically from both members. If any core item silently picks up member-dependence during instantiation, STOP and display it (that would be a Q-682 correction, principal-facing).
AS4. If any element is underivable from sealed stock, stop with the element named. PE-6 exists, is sealed, and carries zero verdict weight: RECORD, DO NOT CONSULT — no step may reference the expected agreement of the kinds.
OUTPUT: one sealed artifact `STAGE8_7A_DBR_FAMILY_BUILD_DARIO_V001.md`
Final lines: `FAMILY = built (A, B side by side; core factored) / stopped (element named)`, `CORE_INVARIANCE = verified (U1-U9 identical from both) / VIOLATED (displayed)`, `RULE_DRAFT = complete (free choices: none / named list) / stopped`, `NOTHING_EVALUATED = true`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 777_DONE.md, then chained pickup (smallest relay number > 777 addressed to DARIO; if absent, STOP cleanly). No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants. No determinant evaluated; no member evaluated alone (FAMILY-BEFORE-DETERMINANT). No bundle class adopted or eliminated. No common cell formed; no junction map evaluated; no smooth data derived.
