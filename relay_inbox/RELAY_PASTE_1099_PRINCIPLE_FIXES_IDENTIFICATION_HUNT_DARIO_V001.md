## PASTE 1099 — DARIO LANE — [PLAN:DESC-47 / REQUIRE-SIDE] DOES A TARGET-INDEPENDENT PRINCIPLE FIX THE ACTION-TO-LENGTH IDENTIFICATION? — THE B1/B2 FORK (LEGITIMATE RATIFICATION vs FREE CHOICE)

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs. HUNT / FORCING-STRUCTURE AUDIT: AUTHORS NOTHING, ADOPTS NOTHING, binds no member, EVALUATES NOTHING. It audits whether a TARGET-INDEPENDENT PRINCIPLE forces the one-parameter scale freedom (the action↔length identification), or whether that scale is a genuine free normalization. NO numeric evaluation; the scale / interval / action / G / alpha are SUBJECTS named by TYPE.
**CONTINGENCY:** valid only if the derived-scale check (1098) upholds NEEDS-NEW-STRUCTURE. Verify `STAGE8_DERIVED_GRAV_SCALE_CHECK_CODEX2_V001.md` exists and reads HUNT-SOUND / needs-new-structure upheld; if it CLEARED (a length is constructible after all), write 1099_BLOCKED and STOP.
WHY THIS HUNT (Q-1001, and the principal's reframe): defining structure IS the project — a new dimensionful primitive is legitimate IF it is defined TARGET-INDEPENDENTLY (by a principle, blind to alpha, frozen before the answer), exactly as every other record primitive was; it is a FIT only if it can be fixed only by choosing the value that yields 1/137. The remaining toll is ONE dimensionful identification (Q-1001). This hunt asks the single decisive question: is there an answer-blind PRINCIPLE that FORCES it?
CARRIED PREMISES (registrar's register lookup; the lane is barred — GIVENS; verify against sealed artifacts you can access; do NOT re-derive): (i) even a FULLY DERIVED record interval leaves a ONE-PARAMETER FAMILY free: R = βcΔτ, β>0, preserves the base interval, phase periodicity, etc. So the scale = this β; a principle must PIN β. (ii) the existence form is E_R·T_R = s·ħ (action = energy×time in units of ħ); τ_R = π/√2 is a DERIVED DIMENSIONLESS interval; τ = R·tanh(s/2) on the central worldline (modular, with a derived conversion). (iii) TWO NAMED CANDIDATE PRINCIPLES to test, plus any you find: (P-STAT) the COUPLED GRAVITY-RECORD STATIONARITY — 'the bridge would DERIVE the identification as part of the stationary cell problem'; BUT `coupled_gravity_record_stationarity_equation_derived = false` and `stationary_record_cell_derived = false`. (P-SG) a SELF-GRAVITATION condition on the action quantum — 'it works because G supplies a scale' — RISK: circular, since G IS the imported gravitational normalization (the scale it would fix). (iv) selecting would fix 'a unique isolated stable Planck-unit record interval'.
THE HUNT — audit the forcing structure at bytes, evaluate nothing:
(1) THE ALLOW/REQUIRE TEST ON THE IDENTIFICATION. Is the action↔length identification (equivalently, β) a UNIQUE-REQUIRE (forced by a principle / universal property, TARGET-INDEPENDENT) or an ALLOW (a free normalization choice)? Cite the sealed basis.
(2) P-STAT — THE STATIONARITY PRINCIPLE. Does the coupled gravity-record stationarity FORCE β / derive the identification, TARGET-INDEPENDENTLY (blind to alpha) and NON-CIRCULARLY? Is the stationarity equation derivable, or is it itself unbuilt (`= false`)? Rule: FORCES (name it, show target-independence + non-circularity) / DOES-NOT / UNBUILT-BUT-CANDIDATE (what it would take).
(3) P-SG — THE SELF-GRAVITATION PRINCIPLE. Does it fix β via G? CHECK CIRCULARITY HARD: does it use the imported gravitational normalization G (the scale it is meant to fix)? If it does, it is CIRCULAR (relocates the import, not a target-independent principle). Rule: TARGET-INDEPENDENT / CIRCULAR (uses imported G) / UNDETERMINED.
(4) ANY OTHER PRINCIPLE. Search for a minimality / uniqueness / universal-property / symmetry clause that pins β answer-blind. Name it or report none found (a search result, not a proof).
(5) THE VERDICT (B1 vs B2). PRINCIPLE-FORCES (B1 — a target-independent, non-circular principle forces the identification → LEGITIMATE ratification; alpha derivable conditional on a principled primitive) / NO-PRINCIPLE (B2 — β is a genuine free normalization; ratifying a value is tuning → the HONEST CONDITIONAL result) / CIRCULAR-ONLY (every candidate uses the scale it would fix) / UNDETERMINED (name the deciding ground).
SELF-FAVOUR GUARD (held HARD): PRINCIPLE-FORCES is the answer that keeps the founding thesis as a DERIVATION and is the one everyone wants. Argue AGAINST it FIRST and HARDEST; a principle counts ONLY if it is target-independent (fixes β WITHOUT reference to any output / measured value) AND non-circular (does not use the imported G / the scale it fixes). Default to NO-PRINCIPLE / CIRCULAR-ONLY / UNDETERMINED on any doubt. Do NOT flatten 'a stationarity equation exists that could fix β' into 'β is fixed'.
FENCES: HUNT / FORCING-STRUCTURE ONLY. Nothing entered/adopted/bound/evaluated; scale/β/interval/action/G/alpha are SUBJECTS by type, never evaluated; BLIND HELD; NO numeric evaluation; NO output inspection; NO comparison to any measured value (no G value, no ħ value, no 137, no Planck-length value); JOINT_ANCHOR_DERIVED = false; no member bound. Q-980–Q-1001 CARRIED. Mandated refusal paths for whatever the artifact seals (closure declared-first; digest-at-path; fence depth-walk; no-numeric-path scan; residue scan; generic extractor NO ALIAS SET if a symbol block appears; span-bound guard). DO NOT read the register (barred); the carried premises are the registrar's hand-off.
OUTPUT: one sealed artifact `STAGE8_PRINCIPLE_FIXES_IDENTIFICATION_HUNT_DARIO_V001.md` (archive workspace). All CLAIMED until the Codex 2 check.
Final lines: `IDENTIFICATION = UNIQUE-REQUIRE (principle: <…>) / ALLOW (free β) / UNDETERMINED`, `P_STAT = FORCES-TARGET-INDEPENDENT / DOES-NOT / UNBUILT-CANDIDATE (<what it would take>) / CIRCULAR`, `P_SG = TARGET-INDEPENDENT / CIRCULAR (uses imported G) / UNDETERMINED`, `OTHER_PRINCIPLE = NAMED (<…>) / NONE-FOUND (search result)`, `BETA_PINNED = YES (principle: <…>, answer-blind) / NO (free normalization) / UNDETERMINED`, `VERDICT = PRINCIPLE-FORCES-B1 / NO-PRINCIPLE-B2 / CIRCULAR-ONLY / UNDETERMINED`, `IMPLICATION = LEGITIMATE-RATIFICATION (derive-conditional) / HONEST-CONDITIONAL / UNDETERMINED`, `SELF_FAVOUR_GUARD = CLEAN`, `EVALUATED_NOTHING = CERTIFIED`, `CLOSURE = declared-first (byte, scan)`, `PROSE_DIGESTS = <n>/<n> STRICT==STABLE`, `NO_NUMERIC_PATH = CLEAN / VIOLATION`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `RESIDUE_GREP = <result> (own patterns)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN`.
If the output name exists, STOP. Seal, report hashes, write 1099_DONE.md. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
