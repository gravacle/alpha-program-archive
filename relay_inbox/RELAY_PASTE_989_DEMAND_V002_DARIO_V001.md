## PASTE 989 — DARIO LANE — [PLAN:DESC-5] DEMAND V002 — THE WHOLE-SECTION CLOSURE (FOLD THE SIX GROUPS AND THE THREE CORRECTIONS)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. Full digests; numeral grep; fixed-point closure byte; the Q-898 reading rule.)
SUBJECTS, verify seals BEFORE reading: your 986 (da32dc9dfff3…); the check (STAGE8_DESC_DEMAND_CHECK_CODEX2_V001.md, f3704df1bc4d7b2f45833fb12a40352117751e8f1b036ba0ab7de9fd4cfa1414, in workspace/). The check CONFIRMS your 18+1 as a correct U2/P5 subassembly and rules it not right-closed for the section.
TASK — AUTHOR DEMAND V002, the whole-section acceptance shape: (1) fold the SIX under-demand groups at their consumers' bytes (U7 B0 A01-A05 structural origin; U8 the complete C0/U1/U3 descendants; U9 the right-domain d_C0/d_U1/d_U3 signatures; U10 B0 A14-A17; U11 MD-3; U12 the StatePort placement binding) — each condition quoted, right-closure both ways at each; (2) the THREE corrections: the no-inspection split (temporal bar from the words; the compositional bar from common-origin provenance/C6, regardless of timing); the PathCert(Omega) field schema at [19909,21513) folded as the certificate's PARTIALLY-SPECIFIED form (canonical serialization and digest convention still open — stated as the remaining open specification, not re-invented); the two quote attributions fixed (Q20 to the instrument's G6 bytes; Q21 to the entry-instrument V002 span). (3) keep the verdict-relative column and the O1-O4 over-demands; keep the four-field stack SEPARATE as before; (4) the unified count restated (n conditions, each at bytes) with the numeral grep run over both tables SCOPED (your own 986 lesson). Everything confirmed carries byte-verbatim; hunk-accounted.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..15 pointer-only. NO construction.
OUTPUT: one sealed artifact `STAGE8_DESC_DEMAND_DARIO_V002.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `GROUPS = U7-U12 FOLDED (bytes each)`, `CORRECTIONS = 3 APPLIED`, `PATHCERT = FOLDED-AS-PARTIAL (open remainder stated)`, `COUNT = n conditions + verdict-relative`, `CARRIED = VERBATIM`, `DIFF = hunk-accounted`, `NUMERAL_GREP = SCOPED-RUN`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 989_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
