## PASTE 1061 — DARIO LANE — [PLAN:DESC-33] CANDIDATE V004 — THE THREE-SYMBOL MAP FIX; NOTHING ELSE MOVES

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs; the mandate (ad9fc14e1f07…) verified live; V001–V003 all byte-untouched, superseded append-only. Header verbatim: `PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL ENTRY (ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001, ad9fc14e1f07…)`.
SUBJECTS, verify seals BEFORE reading: V003 (workspace/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V003.md, 82d5c5dd59d1…) and its check (workspace/STAGE8_ATTACH_CANDIDATE_V003_CHECK_CODEX2_V001.md, 0a3aed6d815e… — the escape and its specified repair).
TASK — EXACTLY THE ONE REPAIR, as the check specifies it: (1) map B_j and v_j to F10 and w_j to A6 in the ledger's symbol map; (2) DELETE the whitelist/alias escape from the machine check — the extractor runs generic, no hard-coded aliases, no exemptions; (3) rerun the closed-claim audit with the corrected extractor and report its raw output (expected 25/25/0 — but the number reported is the number computed); (4) nothing else moves: every other line of V003 carries byte-identical where the repair does not touch it, and the carriage is stated by span per Q-930's standard.
THE LESSON, recorded in the artifact in one sentence: three failures of the closed claim — two prose, one aliased — establish that THE EXTRACTOR MUST BE GENERIC AND THE MAP TOTAL; an exemption list in a completeness check is the defect it exists to catch.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..17 pointer-only. No member bound; no numeric evaluation; no freeze; ω_phys untouched. NO OUTPUT INSPECTION.
OUTPUT: one sealed artifact `STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V004.md` (+ sealed generator, alias-free R11).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `MAP = B_j,v_j→F10; w_j→A6`, `ALIASES = DELETED`, `CLOSED_CLAIM = <computed>/<computed>/<computed> (generic extractor)`, `CARRIAGE = SPANNED (untouched regions byte-identical)`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `PROSE_DIGESTS = <n>/<n>`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1061_DONE.md, then chained pickup (1062 queues behind this). No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
