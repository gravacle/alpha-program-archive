## PASTE 1030 — CODEX 2 LANE — [PLAN:DESC-24] THE OTHER DECLINED CANDIDATE — WAS THE CENSUS WRONG TWICE?

PICKUP-ACK + LANE GUARD in force (CODEX 2). CLEANROOM-SIDE writes only; the registrar mirrors. Full paths; digests-only certificates; the fixed-point closure.
CONTEXT OF RECORD: 1027 (Dario, cf9b577fdefd…) found that your supply map (audit V002, 078d6d54a259…) declined the Q-223 source artifact on a misread block — the decline cited a bare-fences terminal block while the artifact's real verdict block [25126,25677) carries a narrowing of the section's open object; the decline fails the audit's own consequential standard. The audit declined TWO candidates. Dario opened only the one its finding forced and said so. This relay opens the other.
SUBJECTS, verify seals BEFORE reading (cleanroom root): your audit V002 (078d6d54a2590f1caed4cea0245508981bd98e07adcef7f5b5df62d8419f838d) — locate the declined-candidate list and BOTH decline entries with their stated reasons; Dario's 1027 check (cf9b577fdefd8704ab10801eef85819b3f24a12257092b95830bb54434951229) — for the standard the first decline failed; the second declined artifact itself, resolved from the audit's own citation, rehashed.
THE CHECK:
(1) THE SECOND DECLINE: read the declined artifact IN FULL. Does its content, at bytes, carry any changed consequence for the section — a narrowing, a foreclosure, a survivor shape, a demand delta — that the decline's stated reason missed? Apply the audit's OWN consequential standard, the same one the first decline failed. Verdict per candidate: DECLINE-STANDS (the artifact truly carries nothing consequential — say what its terminal AND verdict blocks actually hold) or DECLINE-FAILS (what it carries, at spans, and which consumers should have seen it).
(2) THE FIRST DECLINE, RE-CONFIRMED FROM YOUR SIDE: Dario's finding is CLAIMED; confirm at the two spans ([6387,6556); [25126,25677)) that the decline reason misreads the artifact — your own audit, checked by you, corrected on the record if wrong.
(3) THE REPAIR SHAPE, SPECIFIED NOT EXECUTED: if either decline fails, state the supply-map repair as a bounded supersession (which rows change, what enters the census, which closures downstream must re-run their consumption) — the registrar routes it after the 1029 pause resolves.
(4) CENSUS INTEGRITY, THE WIDER QUESTION: the audit's census is what every later lane reads as the section's ground truth. State plainly whether the decline mechanism itself (the block-selection rule that read a terminal block as the verdict) is a one-off misread or a RULE DEFECT that could have misjudged other entries — if a rule defect, name the rule's span and the entries it touched; do not re-audit the world, bound the blast radius.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..17 pointer-only. ω_phys untouched; no candidate; no freeze; no numeric evaluation; no member bound. This relay does not touch 1029's subject and does not wait on it.
OUTPUT: one sealed artifact `STAGE8_DESC_DECLINE_AUDIT_CODEX2_V001.md` (cleanroom root).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `DECLINE_1 = MISREAD-CONFIRMED / DEFENDED (spans)`, `DECLINE_2 = STANDS / FAILS (spans)`, `REPAIR = SPECIFIED (rows) / N/A`, `MECHANISM = ONE-OFF / RULE-DEFECT (span, blast radius)`, `VERDICT = CENSUS-SOUND-AFTER-ONE / TWO-REPAIRS-ORDERED / RULE-REPAIR-ORDERED`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1030_DONE.md. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
