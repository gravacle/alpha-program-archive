## PASTE 716 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / STEP 8] RA27-2: THE SAME-REGION RELATION — DERIVE OR SURFACE ADOPTION

PICKUP-ACK + LANE GUARD in force (DARIO).
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. THE FIRST-TIME-RIGHT RULES, THE JURISDICTION CHECK, THE SEARCHED-SPACE CLAUSE, and THE SPAN-COVERAGE RULE apply. Governing law: the frozen prereg 9f0d12b4…, the sealed R9-JII carrier 5f4979d5…, your scoping b1a834e7….)

CONTEXT (Q-622): A27's root and U1's J2 are blocked on one object — the relation saying when two complexes represent the same physical region (Ref_a's ground). Three sealed sources agree it is the gap; nothing supplies it. D012 warns it "may require adoption if stock does not force it"; JREF warns a formal category can be authored but does not derive the missing geometric data.

TASK — the root derivation, honest about its fork:
AA1. THE GENERATOR SET: assemble from sealed stock every operation the corpus ALREADY licenses that relates two complexes (refinements, subdivisions, admissible re-presentations, the O2 moves, anything Ref_a-adjacent) — each with source + span (block-covered). This is the candidate basis for "same region".
AA2. THE FORCING QUESTION: does sealed stock FORCE a unique closure of that generator set into an equivalence (or groupoid) with the properties RA27-2's demand names? Derive what IS forced: reflexivity/symmetry/composition where licensed, invariance obligations inherited from F'-5 and J2's re-presentation family. Prove or refute uniqueness of the closure. A completeness/reachability certificate over the generator set is the deliverable either way.
AA3. THE FORK, surfaced not decided: if stock forces the relation — display it, typed, with the certificate. If stock does NOT force it — state exactly the free choice remaining (what a lawful adoption must fix, the smallest adoption that closes it, and what each candidate choice would commit downstream, incl. the effect on U1's J2 quantifier). ADOPTION IS THE PRINCIPAL'S: STOP at the fork with the request stated; do not adopt.
AA4. Correspondence-ledger anything familiar (a same-region groupoid will rhyme with atlas/gluing structures — zero verdict weight). Jurisdiction check on every rule invoked.
OUTPUT: one sealed artifact `STAGE8_7A_RA27_2_SAME_REGION_DARIO_V001.md`
Final lines: `GENERATORS = N, all block-covered`, `FORCED = relation displayed / NOT FORCED (fork stated, adoption request displayed)`, `CERTIFICATE = completeness/reachability over the generator set`, `U1_J2_EFFECT = stated`, `LEDGER_ENTRIES = k`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 716_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation; no comparison to measured constants. No common cell formed; no junction map evaluated.
