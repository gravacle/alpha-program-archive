## PASTE 901 — DARIO LANE — [PLAN:AXN-BUILD-B29] CROSS-CHECK THE FORCING-ORDER AUDIT — THE CAMPAIGN'S STEERING INSTRUMENT

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. BUILDER-NEVER-VERIFIES. BINDING: closure declaration FIRST, exact pinned members, zero-hit scan, byte-position self-audit. SUBJECT: Codex 2's 900 — STAGE8_AXN_FORCING_ORDER_AUDIT_CODEX2_V001.md (49c7bb3d…), mirrored to workspace/; verify seal BEFORE reading. Claims CLAIMED until this check — and this artifact will STEER the whole remaining campaign, so its classifications carry more downstream weight than any single result: check it accordingly. Verdicts under test: 16 objects = 7 INPUT / 3 FORCED / 2 SELECTOR / 4 MIXED; an ELEVEN-element atomic basis, ALL typed DERIVABLE-WITH-MORE-WORK, ZERO inherently supply-shaped; the selectors (lambda_spin, K_R) prospective-only; a SIX-WAVE cascade opening with the selector freeze; the productive stop rule.
TASK: (1) Per-object: verify each classification at its cited bytes — especially every FORCED and MIXED ruling (a wrong FORCED wastes nothing; a wrong INPUT wastes a campaign — but a MISSED forcing chain hides a freebie: press both directions). (2) THE BASIS'S ATOMICITY: is any of the eleven actually forced by a combination the audit missed (law 9b on the forcing-instrument closure)? Is any FORCED object actually independent (the chain consuming something not of record)? (3) THE DERIVABILITY TYPING: for each basis element, verify the "derivable-with-more-work" evidence — this is the claim that keeps the program supply-free at the basis; if any element's evidence is thin, say so with what would decide it. (4) THE CASCADE ORDER: verify the six waves' dependencies, especially wave 1 (the selector freeze) — confirm the selectors are genuinely prospective (freezable now without reverse-fitting risk) and what precisely each freeze unblocks. (5) The stop rule's scope: confirm it bars only downstream-before-basis runs, not basis work itself.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..13 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_FORCING_AUDIT_CROSSCHECK_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan result)`, `CLASSIFICATIONS = CONFIRMED (16) / CORRECTED (which, with chains)`, `BASIS = CONFIRMED-11 / AMENDED (n; changes displayed)`, `DERIVABILITY = CONFIRMED / THIN (elements + deciders)`, `WAVES = CONFIRMED / REORDERED`, `SELECTORS = PROSPECTIVE-CONFIRMED (unblocks displayed) / CORRECTED`, `STOP_RULE = CONFIRMED / CORRECTED`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 901_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
