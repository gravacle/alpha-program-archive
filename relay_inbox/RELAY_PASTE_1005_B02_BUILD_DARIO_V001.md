## PASTE 1005 — DARIO LANE — [PLAN:DESC-21] BUILD B02 — THE CONSTRUCTOR, THE COMPLETE SIGNATURE, THE FROZEN JOINT TRACE

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit. Full paths; digests-only certificates per the final standard; generators sealed and run pre-seal where programmatic; the set-level rule; the fixed-point closure.)
THE STATE: B01 stands at V002 (32 rows, statuses as-sourced; its spot-confirm runs in the opposite lane — its outcome refines B01's rows, not B02's mandate). The descent stop rule is live. THIS IS THE SECTION'S CENTRAL CONSTRUCTION.
TASK — BUILD B02 EXACTLY AS THE SUPPLY MAP DEFINES IT: (1) read basis row B02 at its bytes in STAGE8_DESC_AUDIT_CODEX2_V002.md (workspace/) — its own words govern: the TARGET-INDEPENDENT CONSTRUCTOR producing ONE INHABITED B0 CANDIDATE with COMPLETE SIGNATURE, carrier, core, provenance, and the FROZEN JOINT CONSTRUCTION TRACE; (2) the applicable demand conditions from V006 (workspace/) displayed at bytes before building — the U7 group (conditions 19-23: inhabited candidate; frozen inventory linkage; target-independent rule PLUS the frozen trace; a FILLED signature; provenance/domain certificate) binds directly; identify the rest honestly; (3) THE TRACE: assemble the frozen joint construction trace from the sealed partial subtraces the audit located (real source/family; finite/premise) — assembly with every element pinned, no gap papered: where the subtraces genuinely do not join, the gap is DISPLAYED at bytes, not bridged by prose; (4) THE SIGNATURE: complete — every field the record's Obj_0/B0 signature schema names, filled from sealed ground or STOPPED with the missing field named (a partial signature with named stops beats a filled one with a quiet guess); (5) TARGET-FREEDOM shown, not claimed: no output, root value, or physical target inspected; the constructor's every choice traced to sealed ground or stopped as a freedom (law 10 — and remember the audit typed B02's ingredients; a surfaced selector is a finding, not a license). BLIND held; every scale symbolic; no numeric evaluation of physical quantities.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. All CLAIMED until the opposite-lane check — which will ALSO run B01's exhaustiveness comparison against your signature (the Q-914 forward obligation; your signature is what makes that check possible). PE-1..17 pointer-only.
OUTPUT: one sealed artifact `STAGE8_DESC_B02_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `B02_ROW = quoted (bytes)`, `CONDITIONS = listed (which bind)`, `CANDIDATE = INHABITED (shown) / STOPPED (what)`, `SIGNATURE = COMPLETE (fields listed) / PARTIAL (stops named)`, `TRACE = FROZEN (elements pinned) / GAPS-DISPLAYED (where)`, `TARGET_FREEDOM = SHOWN`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1005_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
