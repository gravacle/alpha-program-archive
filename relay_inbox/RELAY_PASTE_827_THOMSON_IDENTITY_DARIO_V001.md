## PASTE 827 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:F0-2] THE THOMSON IDENTITY QUESTION: WHO HOLDS THE SOURCE QUESTION ON ALPHA'S PATH?

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. GOVERNING: your own 824's edge (c) — the retarded-response -> Thomson-limit chain lists "source-inclusive continuum/projective completion" among its needs; plan V004 types the R-L2b/linked-cluster campaign OFF alpha's value path, "but it types THAT campaign, not the Thomson chain, and identifies neither with the other. If they are the same, the last consumer is off the value path; if not, THAT CHAIN IS THE TRUE HOLDER OF THE SOURCE QUESTION." No sealed sentence settles it; you declined to supply one — correctly. This relay SETTLES IT FROM THE SEALED STOCK OR DISPLAYS THAT IT CANNOT BE SETTLED.)

TASK: (i) extract the retarded-response -> Thomson-limit chain as the record defines it (its artifacts, its needs-list verbatim, its position in K_*'s F0-pinned defining chain from Q-696); (ii) extract the R-L2b/linked-cluster campaign's sealed definition (the four obligations, arm structure, Q-691's map); (iii) compare BY CONTENT: same chain of record / distinct chains (display the separating spans) / overlapping (the shared and disjoint parts typed); (iv) VERDICT: where the source-inclusive completion need actually sits relative to alpha's value path — off it (the identity holds or the need falls in the off-path part) / ON IT (the distinct Thomson chain consumes it — name what that does to F0's chain and the F-phase pricing) / unsettleable (the missing sealed sentence named). FREEDOMS-CONSUMED block; FLATTENING CHECK. All headline items CLAIMED. PE-1..11 pointer-only.
OUTPUT: one sealed artifact `STAGE8_F0_THOMSON_IDENTITY_DARIO_V001.md`
Final lines: `THOMSON_CHAIN = extracted (needs verbatim)`, `RL2B = extracted`, `COMPARISON = same / distinct (spans) / overlapping (typed)`, `SOURCE_QUESTION_HOLDER = off alpha's path / ON IT (consequences stated) / unsettleable (sentence named)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 827_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. Comparison by content, no identity assumed (S13's lesson); no smooth import; no EM identification.
