## PASTE 1093 — DARIO LANE — [PLAN:DESC-44 / REQUIRE-SIDE] DOES DERIVED GRAVITY FORCE THE COUPLING? — THE FOUNDING-THESIS HUNT (SUFFICIENCY), AND IS THE RATIO ESCAPE CLOSED?

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs. HUNT / FORCING-STRUCTURE AUDIT: AUTHORS NOTHING, ADOPTS NOTHING, binds no member, EVALUATES NOTHING. It audits the ALLOW/REQUIRE forcing structure of the gravity side — does it FORCE the two alpha-knobs, or leave residual freedom. NO numeric evaluation of any physical quantity; the metric conversion, connection, K_*, kappa_record, and alpha are SUBJECTS named by TYPE, never computed. Honest UNDETERMINED (with the exact undecidable) is a full result.
WHY THIS HUNT (both-lane confirmed, Q-996/Q-997): the geometric/descent route CANNOT force alpha — it lands the value on TWO ALLOWED data, the METRIC CONVERSION and the CONNECTION, and nothing sealed forces them (the finite provably cannot force the metric conversion — no-go verified at bytes). By the allow/require doctrine (Q-61) only a UNIQUE-REQUIRE or an OVERDETERMINATION forces a number; the descent never worked the REQUIRE side. AND the two knobs ARE the gravity-facing content: the metric conversion is the internal/external geometry of a record cell — the gauge/gravity junction (Q-10: 'the first point where the gravity-facing and gauge-facing structures must be related'; Q-540: 'the correspondence boundary as gravity-side content'). So forcing alpha ⟺ fixing the external (spacetime) geometry ⟺ deriving gravity. This hunt tests whether that forcing actually holds.
CARRIED PREMISES (the registrar's register lookup; the lane is barred from the register — GIVENS, verify against sealed artifacts you can access; do NOT re-derive): (i) alpha_micro = 1/(4π·K_*) STRUCTURALLY; kappa_record is the primitive record-side intensive stiffness (Q-34/Q-62); a quantity cannot reach a continuum coupling without an intervening density. (ii) THE RATIO ESCAPE IS PROVEN DEAD IN ITS CLEAN FORM: no DEPTH-FREE gravity/gauge ratio exists on the Λ^even(C^5) carrier — the singlet has dimension 1 but index 0 (Q-01, PROVED). (iii) the gravity side PARTIALLY EXISTS: a named 5d gravitational normalization + a defined ell_P; the 4D Newton constant is derived from it plus the fundamental parameter. (iv) the two alpha-knobs are the metric conversion + the connection (Q-997).
THE HUNT — audit the forcing structure at bytes, evaluate nothing:
(1) THE GRAVITY FORK — SUFFICIENCY (necessity is already established: gravity is the only way to fix the conversion). Does the DERIVED gravitational side (the emergent external metric / the 5d normalization / ell_P / the Einstein-limit content the plan calls Task 7B) FORCE the metric conversion (make the coefficient absolute) AND pin the CONNECTION — hence force alpha = 1/(4π·K_*) — as a UNIQUE-REQUIRE with a FIXED (target-independent) trigger and PROVABLE INDEPENDENCE from the two knobs? Or does the gravity side ITSELF carry a residual freedom (its own unpinned datum) that re-opens the knob — i.e. gravity is NECESSARY but NOT SUFFICIENT? Rule: GRAVITY-FORCES (name the unique-require and its fixed trigger) / GRAVITY-INSUFFICIENT (name the residual freedom gravity leaves) / UNDETERMINED (name the undecidable).
(2) THE RATIO FORK — IS THE ESCAPE CLOSED? Q-01 killed the depth-FREE ratio. Confirm that no OTHER stratum of the ratio/overdetermination route forces alpha WITHOUT deriving gravity (a ratio in which the metric conversion cancels), or NAME the surviving stratum and whether it needs depth-selection (itself a knob). Rule: RATIO-ESCAPE-CLOSED / RATIO-STRATUM-SURVIVES (<named, and does it still need a knob?>).
(3) INDEPENDENCE / NON-CIRCULARITY. If the gravity fork forces alpha, verify the gravity side does NOT secretly run BACK through the same metric conversion (else it is the same freedom seen twice, not a forcing — the [O1] premise-DAG disjointness / Q-61 'complementary coordinates ≠ overdetermination' trap). A forcing must be genuinely independent of the knob it fixes.
(4) THE IMPLICATION (report, do NOT decide). GRAVITY-FORCES → alpha is forced by derived gravity: the founding thesis holds; next is the gravity derivation proper (Task 7B). GRAVITY-INSUFFICIENT → even gravity leaves alpha free; name what remains. RATIO-STRATUM-SURVIVES → a non-gravity forcing exists; name it. UNDETERMINED → name the deciding ground.
SELF-FAVOUR GUARD (held HARD): 'GRAVITY-FORCES' is the FOUNDING THESIS and the answer everyone wants; it is the maximum-pressure fit configuration. Argue AGAINST it FIRST and HARDEST; a forcing counts ONLY with a named unique-require, a fixed target-independent trigger, and proven independence from the two knobs. Default to GRAVITY-INSUFFICIENT / UNDETERMINED on any doubt; do NOT let the thesis's importance stand in for a proof.
FENCES: HUNT / FORCING-STRUCTURE AUDIT ONLY. Nothing entered/adopted/bound/evaluated; alpha / kappa_record / K_* / the metric / gravity are SUBJECTS named by type, never evaluated; BLIND HELD, every scale symbolic; NO numeric evaluation of a physical quantity; NO output inspection; NO comparison to any measured value (no G_N value, no 137); JOINT_ANCHOR_DERIVED = false; no member bound. Everything from Q-980 through Q-997 is CARRIED, not reopened. Mandated refusal paths for whatever the artifact seals (closure declared-first; digest-at-path; fence depth-walk; no-numeric-path scan; residue scan; generic extractor NO ALIAS SET if a symbol block appears). DO NOT read the register (barred); the carried premises are the registrar's hand-off.
OUTPUT: one sealed artifact `STAGE8_GRAVITY_FORCES_COUPLING_HUNT_DARIO_V001.md` (archive workspace). All CLAIMED until the Codex 2 check.
Final lines: `GRAVITY_FORK = GRAVITY-FORCES (unique-require: <…>; fixed trigger: <…>) / GRAVITY-INSUFFICIENT (residual freedom: <…>) / UNDETERMINED (<ground>)`, `NECESSITY = ESTABLISHED (only gravity fixes the conversion) / <challenge>`, `SUFFICIENCY = <the ruling>`, `RATIO_FORK = ESCAPE-CLOSED / STRATUM-SURVIVES (<name>; needs-knob: <y/n>)`, `INDEPENDENCE = PROVEN / CIRCULAR (runs back through the conversion) / N-A`, `IMPLICATION = DERIVE-GRAVITY-TASK7B / GRAVITY-INSUFFICIENT-<what remains> / NON-GRAVITY-FORCING-<name> / UNDETERMINED`, `SELF_FAVOUR_GUARD = CLEAN (thesis argued against first)`, `EVALUATED_NOTHING = CERTIFIED`, `CLOSURE = declared-first (byte, scan)`, `PROSE_DIGESTS = <n>/<n> STRICT==STABLE`, `NO_NUMERIC_PATH = CLEAN / VIOLATION`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `RESIDUE_GREP = <result> (own patterns)`, `VERDICT = FORCING-MAPPED (implication stated) / UNDETERMINED (ground named)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN`.
If the output name exists, STOP. Seal, report hashes, write 1093_DONE.md. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
