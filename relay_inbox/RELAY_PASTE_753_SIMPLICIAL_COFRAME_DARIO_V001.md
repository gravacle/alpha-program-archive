## PASTE 753 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / STEP 8] THE SIMPLICIAL COFRAME: DERIVED FROM THE SEALED CONSTRUCTION'S OWN COORDINATES

PICKUP-ACK + LANE GUARD in force (DARIO).
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL standing laws apply. GOVERNING: the booking 750; your frontier survey 751.)

CONTEXT (Q-657): the coframe field's A2 half is a spec gap — V011's tetrad display quantifies over orthogonal cells, and A2's children are order-simplices. BUT the sealed MAJ construction defines those 24 simplices CONCRETELY (the Freudenthal/order-simplex subdivision of the unit 4-cube, with the four-volume identity 24 x 1/24 = 1). Their edge vectors, volumes, and orientations are DETERMINED by the sealed coordinates.
TASK — derive-or-gap:
AE1. From the sealed construction's own coordinates: derive the canonical per-simplex frame data (the edge-vector matrix of each order-simplex; its determinant/volume — must reproduce the sealed 1/24; orientation consistent with the sealed orientation data). Nothing chosen: everything computed from the sealed simplex definitions. Display exactly (rational entries).
AE2. Derive whether this frame data SUPPLIES what V011's general clause needs — the general map is stated via wedge^2(e^-1) and |det e| for a GENERAL coframe: does the derived simplex frame plug into that clause (making the A2 tetrad transport DERIVED, closing the coframe field whole), or does the clause's own text bar non-orthogonal frames (then the one-line spec statement needed is named — not authored)?
AE3. If the coframe field closes: state RA27-3's updated board and what the R33 grounding gives on A2 (child-intrinsic evaluation for simplices). VOID clean throughout; anisotropic parent extents included via the sealed general-coframe clause if it applies.
OUTPUT: one sealed artifact `STAGE8_7A_SIMPLICIAL_COFRAME_DARIO_V001.md`
Final lines: `FRAME_DATA = derived (24 simplices, exact) / gap named`, `V011_CLAUSE = accepts (transport DERIVED; coframe field CLOSED) / bars (one-line need named)`, `BOARD = updated`, `VOID = clean`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 753_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities (exact-rational structural derivation is the task); no comparison to measured constants. No common cell formed; no junction map evaluated; no smooth data derived.
