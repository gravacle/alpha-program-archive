## PASTE 806 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [PLAN:B1c-3] INHABIT THE RECEIVER: TEST THE EXHIBIT AGAINST THE NINE PREDICATES

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL alignment laws apply. GOVERNING: your own 804 (Q-712) — the linear class is NON-EMPTY, the identity law a member; Q-698's "JOINT_A1_A2_FIELD_EXT_MEMBER impossible" was an inference from the refuted empty verdict and is WITHDRAWN. The B1c receiver (Codex's 793, mirrored: STAGE8_B1C_DENSITY_RECEIVER_BUILD_CODEX2_V001.md, 0c535497… — verify before reading) is typed and sealed with nonemptiness deliberately open; its named missing object was exactly a joint A1/A2 field-extension member. YOU NOW HOLD ONE CANDIDATE: the identity-law transport with its lift (your 804 exhibit). Test it. NOTE: 805 runs lane-opposite on the divergence; do not touch 802's L question.)

TASK — the inhabitance test:
AS1. ASSEMBLE THE CANDIDATE MEMBER from the exhibit: the identity-law coframe transport with its section/lift data on A0/A1/A2, in the receiver's own types (the dependent-equalizer interface from 793). State every conversion between your basis conventions and 793's, span-cited — the convention traps of this arc (L's normalization; the transpose-pivot bug) make the mapping itself a named step.
AS2. RUN THE NINE INTERFACE PREDICATES against the candidate, one by one: receiver acceptance, Ref_a totality, coframe-dual/Vol_4 density type, functoriality, conserved-current transport, incidence intertwining, coframe duality, exact-energy compatibility (the conservation law), coherence. Exact arithmetic; each predicate's verdict with its computation displayed or its unposeable element named.
AS3. VERDICT: INHABITED (the receiver has a member; display it in receiver types; B1c's inhabitance closes and the forcing question becomes poseable on a nonempty space) / NOT-THIS-CANDIDATE (which predicate rejects, exactly why — the identity law may fail density-side predicates even while solving the coframe system; that would type the residual gap sharply) / UNPOSEABLE (element named). State B2's runnability status under the verdict.
AS4. FREEDOMS-CONSUMED block (item-by-item true; law 2a — the exhibit is TESTED, not adopted); FLATTENING CHECK. All headline items CLAIMED. PE-1..7 pointer-only.
OUTPUT: one sealed artifact `STAGE8_B1C_RECEIVER_INHABIT_DARIO_V001.md`
Final lines: `CANDIDATE = assembled (conversions displayed)`, `PREDICATES = <n>/9 pass / failures displayed / unposeables named`, `VERDICT = INHABITED / NOT-THIS-CANDIDATE (rejecting predicate + reason) / UNPOSEABLE (element)`, `B2_RUNNABLE = <status>`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 806_DONE.md, then chained pickup (smallest relay number > 806 addressed to DARIO; if absent, STOP cleanly). No register, plan, tracker, git action.

GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. The exhibit tested, never adopted; no law/metric/member adopted; no smooth import (S26); no EM identification (S08); no common cell formed; no junction map evaluated.
