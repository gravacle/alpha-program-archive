## PASTE 1079 — DARIO LANE — [PLAN:DESC-37] MEASURE CANDIDATE V002 — THE SM-6 DESCENT; THE SECOND AUTHORED INGREDIENT LEDGERED

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs; the measure mandate (b9716661d1a0…) verified live; V001 byte-untouched, superseded append-only; all mandated refusal paths (generic extractor now covering the PRODUCT OPERATOR; closed claim; fence-depth; digest-at-path; citation-resolution). NO numeric evaluation — the V001 Monte-Carlo was a fence brush; establish everything symbolically, no residual figure computed. Header verbatim: `PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL ENTRY (MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001, b9716661d1a0…)`.
SUBJECTS, verify seals BEFORE reading: V001 (STAGE8_MEASURE_CANDIDATE_PROPOSAL_DARIO_V001.md, c64c44fbc072…) and its check (STAGE8_MEASURE_CANDIDATE_CHECK_CODEX2_V001.md, b65cd05f9868… — the VOID grounds at their spans: A1 does NOT force the descent; the product Ω_SR⊗μ is a second authored choice; SM-6 is mandatory; the extractor missed the product operator); member 08 :338-339 (SM-6's exclusion of the always-available product extension); the mandate's common-origin requirement; Gen_Omega's descent structure WITH overlay V002 + DoR-013.
THE FINDINGS ACCEPTED IN FULL: μ=Haar is lawful and μ(Z_N)=0 violates no sealed constraint (carry the check's confirmation — that worry is CLEARED); the defect is the DESCENT, not the measure.
TASK — V002: (1) carry μ=Haar and A1 unchanged (lawful, confirmed); (2) BUILD A NON-PRODUCT DESCENT satisfying SM-6 — a common-origin joint extension Ω_C0 that is NOT the product Ω_SR⊗μ, grounded in Gen_Omega's own descent structure so the common-origin certificate MC5 CLOSES (or, if a lawful non-product descent cannot be built from sealed grounds without further authorship, STOP and display exactly what it would cost — an honest stop is a full result); (3) the LEDGER restated honestly: the descent's authored content declared as the SECOND authored ingredient (expected 9F/2A, or more if the SM-6 descent needs it — the number is whatever the honest construction yields, said plainly; do NOT force 9F/2A if it is larger); (4) the extractor REPAIRED to cover the product/descent operators (the ingredient escape closed; closed-claim recomputed and published to match); (5) V1–V8 carried; K4 shape restated against the new descent.
NO OUTPUT INSPECTION — maximal strength; the descent shaped by SM-6 and common-origin ONLY, never by κ_record/alpha/the battery.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..17 pointer-only. No member bound; no fixed-point execution; no end test; no numeric evaluation; no measured-constant comparison; no freeze beyond the authored measure+descent, disclosed.
OUTPUT: one sealed artifact `STAGE8_MEASURE_CANDIDATE_PROPOSAL_DARIO_V002.md` (+ sealed generator).
Final lines: `CLOSURE = declared-first (byte position, scan)`, `MEASURE = HAAR-CARRIED (lawful, confirmed)`, `DESCENT = SM6-SATISFIED (non-product, trace) / STOP (cost displayed)`, `MC5 = CLOSED (certificate) / OPEN (why, at span)`, `LEDGER = <f>-FORCED / <a>-AUTHORED (both authored ingredients itemized)`, `EXTRACTOR = PRODUCT-OP-COVERED`, `CLOSED_CLAIM = <computed>`, `K4_SHAPE = MEETS / GAP`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `PROSE_DIGESTS = <n>/<n>`, `CHAIN_INVOKED = true/false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 1079_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
