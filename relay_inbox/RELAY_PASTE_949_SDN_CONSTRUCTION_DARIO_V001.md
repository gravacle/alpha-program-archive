## PASTE 949 — DARIO LANE — [PLAN:AXN-BUILD-D48] CONSTRUCT THE SD-N SLICING INSTANCE (THE PRINCIPAL HAS ENTERED)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BINDING: closure declared FIRST, exact pinned members, zero-hit scan, byte-position audit.)
THE EVENT: the principal entered ROUTE SD-N and deferred the Galerkin freeze — LINE6_SITTING_DECISION_OF_RECORD_V001.md (00b77a0887bb84ea2a87384b78d52a775c7e882ee99f054dac075c56697018b3, in workspace/ and supervision/). Governing instrument: STAGE8_AXN_SLICING_DECISION_INSTRUMENT_CODEX2_V001.md (08a13df40f8f…), READY per your own 946. Verify BOTH seals before reading.
TASK — BUILD THE SlicingDatum.v001 INSTANCE ON ROUTE SD-N, every route field, zero deviation: the hypersurface family generated from one declared Sigma_0 (declare it from sealed parent geometry at bytes — if no sealed surface exists to declare from, STOP that field and route it); T = n; N = 1; beta = 0; the normal-parameter normalization; the transported frame and spin frame (construction displayed); the GLOBAL-OR-BOUNDED-DOMAIN STATEMENT (the route's price — state the exact scope on which normal flow is a valid foliation, with the no-caustic ground at sealed bytes or an explicitly bounded domain); and the COMPLETE SLICE IDENTITY WITH PROOF: Slice(D_K) = h_K derived on the entered scope, the differential and write identities displayed (SD-04 maps the write term THROUGH the Hermiticity-forced S_n — the record supplies S_n; do not re-author it), landing exactly on the receiver dGamma_R(Slice(D_K)) = dGamma_R(h_K). SD-08's spatial-section profile disclosure: state the sharp/smooth status honestly — the smoothing profile is DEFERRED as a principal selector; a sharp section inherits the S_2 boundary divergence and this instance must SAY so rather than smooth it silently. STOP DISCIPLINE: any field needing a datum beyond the entry and sealed spans stops with the freedom named. NO numeric evaluation of physical quantities.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. The instance is CLAIMED until the opposite-lane check. PE-1..13 pointer-only. BLIND: no rank, no ratio, no fiber comparison.
OUTPUT: one sealed artifact `STAGE8_AXN_SDN_SLICING_INSTANCE_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `SIGMA0 = DECLARED (sealed ground) / STOPPED (routed)`, `FIELDS = n/9 COMPLETE / (which stopped)`, `SCOPE = GLOBAL (no-caustic ground) / BOUNDED (domain stated)`, `SLICE_IDENTITY = PROVED (receiver landed) / STOPPED (step)`, `PROFILE = SHARP-DISCLOSED (S2 inherited, stated) / (status)`, `NEW_CONTENT = ONLY-THE-ENTERED-AUTHORSHIP`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 949_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
