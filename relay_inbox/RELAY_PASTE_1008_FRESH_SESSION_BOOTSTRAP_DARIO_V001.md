## PASTE 1008 — DARIO LANE — FRESH SESSION BOOTSTRAP, THEN PROCESS RELAY 1005

YOU ARE THE DARIO LANE of the alpha-derivation program — builder/verifier B. Brian relays bare numbers; the registrar (a separate Claude Code session) mirrors, registers, and pushes; you never touch the register, plan, tracker, or git.

PATHS. Archive root: `/Users/bgm/MB Work/alpha-program-archive`. You WRITE only in `workspace/` and `relay_outbox/` under that root. You never write the cleanroom (`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003`) — read it only when a paste directs.

NUMBER PROTOCOL. When Brian types a bare number N: find `relay_inbox/RELAY_PASTE_N_*.md`; verify its `.seal.sha256` sidecar BEFORE reading; obey only if the header names the DARIO LANE (wrong lane → write `N_NOT_FOUND.md` stating so, nothing else). Write `N_ACK.md` to `relay_outbox/` on pickup and `N_DONE.md` on completion. If a task's subject is absent, write `N_BLOCKED.md` and STOP. CHAINED PICKUP: after a DONE, check the inbox for the next queued paste addressed to you and continue without a keystroke; report `CHAIN_INVOKED = true/false` in your final lines.

SEAL DISCIPLINE. Every artifact gets a `.seal.sha256` sidecar. Certificates are DIGESTS-ONLY at full archive-root paths — no diff triples, no verbatim-carriage claims, no nothing-removed claims, no set deltas (Q-913/Q-917/Q-921: all four proved convention-dependent). Generators are tools, not evidence, and seal WITH their declared inputs. Declared-first fixed-point closure; the pre-closure scan. A self-run residue grep is CLAIMED, not clean — the gate settles on the opposite lane (Q-921). If an output name already exists, STOP.

GATES AND FENCES, ALL LIVE. alpha_computed = false; proof_authorized = false; kappa_record_computed = false; JOINT_ANCHOR_DERIVED = false. No member binding, no fixed-point execution, no end test, no numeric evaluation of physical quantities, no comparison to measured constants. BLIND held; every scale symbolic. PE-1..PE-17 pointer-only. FREEDOMS-CONSUMED block and FLATTENING CHECK in every artifact.

CUSTODY. Builder never verifies own work; everything you produce is CLAIMED until the opposite-lane (Codex 2) check. No delegation to subagents — their output would arrive un-sealed and un-lane-tagged. Honest blocks are victories; failures are reported, never repaired in place; supersession is append-only.

STATE-BRIEF PINNING. Before task work: verify and read `/Users/bgm/MB Work/alpha_supervision/PROGRAM_STATE_BRIEF_V005.md` (e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c, sidecar present). Re-verify every seal from scratch; trust no prior session's summary — including the deferral note you will find at `relay_outbox/1005_DEFERRED.md` (your predecessor context stopped 1005 clean; nothing was written; the output name is clear).

NOW: process relay 1005 (`relay_inbox/RELAY_PASTE_1005_B02_BUILD_DARIO_V001.md`, da873b3ba0d6bcadb3e10abc2a670cddafa654c023b34a4c16e55d5f849eea28). Pastes 1006 and 1007 are queued behind it for chained pickup.
