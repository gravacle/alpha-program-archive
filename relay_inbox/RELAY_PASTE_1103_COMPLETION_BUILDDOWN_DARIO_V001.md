## PASTE 1103 — DARIO LANE — [PLAN:DESC-49 / ASSEMBLE] ASSEMBLE THE COUPLING FROM ALREADY-DERIVED SEALED TERMS ONLY — REPORT THE FIRST INGREDIENT THAT IS NOT ALREADY DERIVED

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs. All CLAIMED until the Codex 2 check.
DISCIPLINE (the principal's two rules for this run — HARD): (1) DO NOT RETRACE — do not rebuild, re-derive, or re-establish anything already in the sealed record; take every ratified term AS GIVEN and only ASSEMBLE. (2) DO NOT IMPORT — if the assembly needs an ingredient that is NOT already in the sealed record, STOP and NAME it; do NOT construct it, bridge it, or borrow continuum/KK/variational machinery to supply it. Constructing the missing ingredient would BE the import. The whole result is the NAME of the first missing ingredient. Keep it lean; make it no harder than it is.
THE TASK. The record-native object is a genuinely-interacting CONNECTION-ONLY OPERATOR (built + checked, 1101/1102). A physical coupling is a scale-bearing SCALAR. Using ONLY already-derived, ratified, sealed terms — assemble from the connection-only operator toward that scalar coupling, in the settled order (scalarization → state → nontrivial effects → domains/contacts → common-origin provenance → codomain conversion → metric-dependent dynamics → scalar). For EACH step, ONE question only: is every ingredient it needs ALREADY DERIVED (present in the sealed record)?
- YES → the step assembles from derived terms; note ASSEMBLED (cite the sealed term(s) used) and go on. Do NOT re-derive or elaborate it.
- NO → STOP. NAME the first not-already-derived ingredient precisely; state what dimension/type it carries and why no already-derived (dimensionless, scale-free) term supplies it. DO NOT author, construct, or import it. That STOP is the answer.
Continue until either (a) the scalar coupling assembles entirely from already-derived terms and a SCALE appears (→ Finish A — report exactly which derived terms produced the scale and confirm none of them imported it), or (b) the first not-already-derived ingredient is reached (→ report the ASSEMBLED steps + the NAMED missing ingredient).
CARRIED GIVENS (registrar's register lookup; the lane is barred — take as GIVEN, do not re-establish): (i) the record-native object is CONNECTION-ONLY, operator-valued, no metric argument, takes NO logarithm (1102 — do NOT re-promote a log or any un-derived clause; the removed log clause is the cautionary case). (ii) every derived junction is dimensionless / scale-free (Q-995); a length/scale needs a dimensionful reference (Q-1004). (iii) the metric g / a record cell's internal↔external conversion is TYPE-U — unbuilt, not forbidden; if the assembly needs it, that IS a legitimate NAME-and-STOP, not something to build. (iv) the ratio-cancellation route is dead (Q-01); do not revive it.
RECORD-NATIVE FENCE (HARD): assemble from the record's own sealed discrete terms only; NO continuum machinery, NO smooth structure, NO imported metric/length, NO KK/radion, NO variational/additive notation (it smuggles linearity, Q-509/Q-727). If a step tempts an import, that temptation IS the wall — name it and stop.
FENCES: BLIND. May NOT evaluate any physical quantity, inspect output, import a scale, or use a measured value; BLIND HELD; NO numeric evaluation; NO output inspection; JOINT_ANCHOR_DERIVED = false; alpha/scale/metric are SUBJECTS by type. Q-980–Q-1006 CARRIED. Mandated refusal paths (closure declared-first; digest-at-path; fence depth-walk; no-numeric-path; residue; span-bound guard; generic extractor NO ALIAS SET). DO NOT read the register (barred); scope any grep to named sealed artifacts only (no broad archive-wide rg — a broad grep tripped 1102's register bar). The carried givens are the registrar's hand-off.
OUTPUT: one sealed artifact `STAGE8_COUPLING_ASSEMBLY_DARIO_V001.md` (archive workspace). CLAIMED until the Codex 2 check.
Final lines: `STEPS = <n>`, `ASSEMBLED_FROM_DERIVED = <list of steps + the sealed term(s) each used>`, `FIRST_MISSING_INGREDIENT = <precisely named, or NONE (coupling assembled)>`, `MISSING_DIMENSION_TYPE = <dimension/type it carries + why no derived scale-free term supplies it>`, `SCALE_APPEARED = NO (missing ingredient) / YES (which derived terms produced it, no import)`, `IMPLICATION = FINISH-A (scale assembled from derived terms) / FINISH-B-SHARP (record-native up to <FIRST_MISSING_INGREDIENT>; that ingredient needs new structure) / UNDETERMINED`, `RETRACED_NOTHING = CERTIFIED`, `IMPORTED_NOTHING = CERTIFIED`, `NO_LOG_REPROMOTED = CERTIFIED`, `TARGET_INDEPENDENT = CERTIFIED`, `EVALUATED_NOTHING = CERTIFIED`, `CLOSURE = declared-first (byte, scan)`, `PROSE_DIGESTS = <n>/<n> STRICT==STABLE`, `NO_NUMERIC_PATH = CLEAN / VIOLATION`, `OUTPUT_INSPECTION = NONE-CERTIFIED`, `RESIDUE_GREP = <result> (own patterns)`, `VERDICT = ASSEMBLY-COMPLETE (missing ingredient named OR coupling assembled) / UNDETERMINED`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN`.
If the output name exists, STOP. Seal, report hashes, write 1103_DONE.md. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
