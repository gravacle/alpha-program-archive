## PASTE 1109 — DARIO LANE — [CHECK] SECOND EXTERNAL ADVERSARIAL CHECK OF THE RAIL-DECLARED VERDICT (a Fable construction; refute it — you built the connection-only ground it stands on, so you are the right lane to test its use)

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE. This is a CHECK, not a build. The subject is a blind Fable-agent CONSTRUCTION, CLAIMED, zero testimonial weight (BR-1) — re-derive at the bytes from your OWN sealed members (you authored the connection-only ground, member `1d11f150…`; verify the subject uses it correctly, do not defer to it). Highest-stakes verdict in the arc AND it favors the registrar's hope — so REFUTE it; default to skepticism.
SUBJECT (in the workspace, sealed): `STAGE8_BETA_SURFACE_DERIVATION_ATTACK_FABLE_V001.md` sha256 `f70d9c313863018a72d430269341b84c21ae44fb0d4de968df12b3aafa217573`. Verify at path first; mismatch → STOP.
THE VERDICT UNDER ATTACK: RAIL-DECLARED — "the internal/external metric conversion is a declared rail import: the connection-only surface supplies both feet of the junction and every derived junction between them, all dimensionless and invariant under the scale action, so no surface construction can select the orbit point the conversion is." Load-bearing claim (from an Einstein member it cites): "every DERIVED junction is beta-invariant; every junction where beta could appear is ADOPTED, GAP, or UNBUILT; no junction is simultaneously derived and scale-sensitive."
CHECK (CONFIRM/REFUTE/UNDETERMINED each, with the span):
 K1 — THE REFUTATION LEVER: search your OWN members exhaustively for ANY derived junction/object of the record cell that is SCALE-SENSITIVE (not a fixed point of the scale action). One such derived object → RAIL REFUTED (the surface CAN select the orbit point). This is the highest-value find.
 K2 — Is the "no junction simultaneously derived and scale-sensitive" claim COMPLETE over the actual junction inventory, or did the subject read a PARTIAL inventory as total? Name any unclassified junction.
 K3 — Is "a fixed-point-only inventory cannot produce an orbit-point-selector" a genuine surface-native impossibility, or does it smuggle a traditional-formalism assumption (→ false RAIL → downgrade to UNDETERMINED)?
 K4 — Does the subject correctly use YOUR connection-only ground (member 1d11f150…)? Any misread of the connection-only finding?
 K5 — Did the subject import anything (numeric, length, metric, fixed point, member binding)?
RECORD-NATIVE FENCE (HARD): finite discrete record IS the surface; continuum is imported. Scope greps to the named subject + your own sealed members; NO broad archive-wide rg. DO NOT read the register; disclose+zero-weight anything incidental.
FENCES: BLIND; no numeric eval; no output inspection; alpha/scale/metric/beta SUBJECTS by type; alpha_computed=false, proof_authorized=false, kappa_record_computed=false. Mandated refusal paths.
OUTPUT: `STAGE8_BETA_SURFACE_DERIVATION_CHECK_DARIO_V001.md` (archive workspace). Final lines: `K1_SCALE_SENSITIVE_JUNCTION_FOUND = NO (RAIL holds) / YES (<named> — RAIL refuted)`, `K2_BETA_INVARIANCE_CLAIM = COMPLETE / OVER-READ (<gap>)`, `K3_RAIL_VS_UNDETERMINED = RAIL-SOUND / DOWNGRADE-TO-UNDETERMINED (<why>)`, `K4_GROUND_USED_CORRECTLY = YES / NO (<misread>)`, `K5_SUBJECT_IMPORTED_NOTHING = CONFIRMED/REFUTED`, `VERDICT = RAIL-CONFIRMED / RAIL-REFUTED (<what the surface actually supplies>) / UNDETERMINED`, `OWN_MEMBERS_USED = <n>/<n>`, `CLOSURE = declared-first`, `NO_NUMERIC_PATH = CLEAN`, `REGISTER_READ = NONE-CERTIFIED`, `CHAIN_INVOKED = false`.
If the output name exists, STOP. Seal, report hashes, write 1109_DONE.md. No register/plan/tracker/git action.
