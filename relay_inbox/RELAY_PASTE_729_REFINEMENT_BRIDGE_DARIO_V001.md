## PASTE 729 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / STEP 8] THE REFINEMENT BRIDGE: F UNDER SUBDIVISION, DERIVED FROM ITS OWN SEALED DEFINITION

PICKUP-ACK + LANE GUARD in force (DARIO).
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL standing laws apply. GOVERNING: the built index 66f078ba…, the decision correction DECISION_CORRECTION_FC1_TRANSPORT (mirrored), the frozen prereg, PE-1 of record at zero weight.)

CONTEXT (Q-634): everything in 7A now stands on one derivation the corpus names as its own gap — "T11's gap is FUNCTORIALITY OF A MEASURE: does the response-map pullback commute with refinement, and is the boundary term subextensive?" The one-line patch (assign F on sub-faces) was rightly refused; the lawful route is the opposite direction: F's SEALED DEFINITION must SAY what F is on a sub-face, or say nothing.
TASK — the derivation, honest under the jurisdiction check:
NN1. F's DEFINITION, assembled: every sealed statement defining or constraining F (the record action's own bytes; object-names probed — F, flux, bivector, response map, and the objects' own vocabulary), block-covered. What IS F on a cell: primitive datum, derived functional, or restriction of something global?
NN2. THE SUBDIVISION QUESTION, derived: from those bytes alone, what does F on the sub-cells of a licensed move equal? Three honest outcomes: (a) DETERMINED — the definition forces the sub-face values (display the forcing; then prove or refute the functoriality of V_cell·sum F^2 with the boundary term's subextensivity under the shape-regular class); (b) UNDERDETERMINED — the definition is silent on sub-faces (name exactly the free datum; state what smallest sealed addition would determine it; NOTHING adopted); (c) OBSTRUCTED — sealed text makes the sub-face assignment impossible or inconsistent (display the obstruction).
NN3. The K-1/K-3 lens at zero adoption: IF the derivation surfaces scale-dependent structure (the divergence class), it is MEASURED AND RECORDED per Requirement 5 — coefficient class, dependence, locus — never halted on, never promoted. Ledger anything familiar (subdivision-invariance failure rhymes with anomaly/running structures — zero weight).
NN4. VOID attestation: no downstream numeric consulted.
OUTPUT: one sealed artifact `STAGE8_7A_REFINEMENT_BRIDGE_DARIO_V001.md`
Final lines: `F_DEFINITION = assembled (N statements, block-covered)`, `SUBDIVISION = DETERMINED (functoriality proven/refuted, boundary term stated) / UNDERDETERMINED (free datum named) / OBSTRUCTED (displayed)`, `DIVERGENCE_NOTE = none / measured (K-axes recorded)`, `LEDGER_ENTRIES = k`, `VOID = clean`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 729_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants. No common cell formed; no junction map evaluated.
