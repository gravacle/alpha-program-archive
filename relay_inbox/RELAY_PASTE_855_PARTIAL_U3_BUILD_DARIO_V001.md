## PASTE 855 — DARIO LANE — [PLAN:AXN-BUILD-A22] EXECUTE THE PARTIAL-U3 CONTRACT (ROOT D, THE ONE STARTABLE MOVE)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Archive-side writes only; the registrar mirrors. SUBJECTS: your own 854 cross-check — STAGE8_AXN_BUILD_B0_CENSUS_CROSSCHECK_DARIO_V001.md (8625fbba3d8d…) — and its subject census (a614a69409b4…); verify both seals BEFORE reading. Executing a contract your own check displayed is lawful build work; the build's verification will be opposite-lane.
TASK — RUN THE DISPLAYED CONTRACT, EXACTLY AND NOTHING MORE: place the EXISTING law-side U3 subpackage into U3's slot; carry its scope UNCHANGED; DISPLAY the four remaining physical fields (measure, interacting contour, complete boundary/edge carrier, unbounded endpoint domains) as STOPS at their sealed false/TYPE-U statuses; INVENT NOTHING — 850's gate ("inventing a measure, contour, boundary carrier, or domain is barred") is the governing fence and the bar is on inventing, not on placement. OUTPUT: a partial U3 with ONE FILLED LEG and FOUR DISPLAYED STOPS — expressly NOT a complete U3 and NOT a discharge of root D. Then display: what the partial U3 makes newly reachable of record (which DESCEND_B0/d_U3 preconditions it satisfies, which it cannot), and what each of the four stops would need, byte-cited where sealed sources name it. Also carry from 854: the partial-substitute bar applies to B0 (root A), NOT to U3's package assembly — do not conflate; if any sealed sentence bars a partial U3 PACKAGE, STOP at it and display the bytes.
FREEDOMS-CONSUMED block (law 2a); FLATTENING CHECK. All headline items CLAIMED. PE-1..12 pointer-only.
OUTPUT: one sealed report `workspace/STAGE8_AXN_BUILD_PARTIAL_U3_DARIO_V001.md`
Final lines: `PARTIAL_U3 = BUILT (one leg filled, four stops displayed) / BLOCKED (barring bytes displayed)`, `NEWLY_REACHABLE = displayed / NONE`, `STOP_REQUIREMENTS = byte-cited list`, `ROOT_D = NARROWED / UNMOVED`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 855_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. Charter fences live; nothing selected beyond the contract's placement; no smooth import; no EM identification; no member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants.
