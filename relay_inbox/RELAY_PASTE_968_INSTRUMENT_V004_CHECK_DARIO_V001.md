## PASTE 968 — DARIO LANE — [PLAN:AXN-BUILD-D67] CROSS-CHECK INSTRUMENT V004 (THE ROW ALIGNMENT)

PICKUP-ACK + LANE GUARD in force (DARIO). CHAINED PICKUP applies. STATE-BRIEF PINNING: verify and read PROGRAM_STATE_BRIEF_V005.md (e26f0d16055f…) before task work.
(Same Dario session rules. ARCHIVE-SIDE writes only; the registrar mirrors. BUILDER-NEVER-VERIFIES. The 918/921 standard; the governing-act bar.)
SUBJECT, in workspace/, verify seal BEFORE reading: STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V004.md (a195edb03b36be44bf8ce1b71dbf1a01a3a0956c4afc65ddf17382cdca4ed0ab). References: V003 (79f0c3516120…); the Q-877 adjudication at its register span; your 963 and the 964/966/967 arc.
TASK: (1) THE ROW — verify the aligned JAC-14 description and rejection clause state BOTH variants verbatim-consistently with the section-5.1 types (your own 963 reading of those types is the reference — re-read them at bytes, not from memory); the rejection clause rejecting whichever selected form lacks its own closed content while retaining the common gates. (2) NOTHING ELSE MOVED — the 5.1 schema and the anti-flattening guard byte-identical to V003 (rehash); no decision entry, JPO/SM/gauntlet row, failure rule, or ledger consequence changed; the 22-hunk table replayed, H08 the sole operative hunk. (3) THE ROW GREP — replay it: the nine semantic variant rows checked; confirm JAC-14 was the sole stale one (run your own sweep — the grep's completeness is the claim). (4) THE FORM — blank approval field; V003 still governing; effect withheld; the repair-of-record extension plain (the V003 omission named; Q-877 cited by span). (5) independent advocacy scan. VERDICT: READY-FOR-THE-PRINCIPAL or NOT-READY with items.
FREEDOMS-CONSUMED block, carrying JOINT_ANCHOR_DERIVED = false. FLATTENING CHECK. BLIND HELD. PE-1..15 pointer-only.
OUTPUT: one sealed report `STAGE8_AXN_INSTRUMENT_V004_CHECK_DARIO_V001.md`
Final lines: `CLOSURE = declared-first (byte position, scan)`, `ROW = CONSISTENT-WITH-5.1 / DEVIATES (where)`, `UNMOVED = VERIFIED (schema+guard byte-identical) / (what moved)`, `HUNKS = 22-CONFIRMED (H08 sole operative)`, `ROW_GREP = REPLAYED-CONFIRMED / MISSED (which row)`, `FORM = LAWFUL / DEFECT`, `ADVOCACY = n hits`, `VERDICT = READY-FOR-THE-PRINCIPAL / NOT-READY (items)`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the output name exists, STOP. Seal, report hashes, write 968_DONE.md, then chained pickup. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
