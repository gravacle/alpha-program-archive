## PASTE 765 — DARIO LANE (OPUS 5 ULTRA, HIGH EFFORT) — [7A / Q-126 ROUTE] THE BUNDLE-CLASS SELECTOR HUNT

PICKUP-ACK + LANE GUARD in force (DARIO).
(Same Dario session rules. ARCHIVE-SIDE ONLY; the registrar mirrors. ALL standing laws apply. GOVERNING: the rebuilt tower 764; the route's own sealed falsifier; the VOID condition — no selector may be chosen from a desired coefficient.)

CONTEXT (Q-668): five free data; the bundle class q carries the one existing constraint (|q| >= 1/2) and controls the zero-mode multiplicity 2|q| that reproduces the index theorem. The hunt-before-adopt pattern, on the first selector:
TASK:
AO1. ENUMERATE every sealed require bearing on the bundle class (meaning-probed, object-names probed: flux quantization statements, the record's source/charge structure, the index/zero-mode demands, the one-spectrum-one-trace rule, the ordinary-charged-durable-source branch selection, anything the 16's structure imposes) — each tagged by verbatim modality.
AO2. TEST the candidate classes against each require: does any sealed require FORCE |q| (elimination or direct determination)? Display the matrix. Note especially whether the durable-source branch or the record's charge quantization fixes the flux through the S2_flux factor.
AO3. VERDICT: FORCED(q = …) with the chain displayed / UNDERDETERMINED (the surviving set displayed; what a lawful selector must fix — the principal's brief) / OVER-CONSTRAINED. Nothing adopted; the falsifier's demand restated against the verdict (if underdetermined, say exactly what the route needs before any determinant is computed).
OUTPUT: one sealed artifact `STAGE8_7A_BUNDLE_CLASS_HUNT_DARIO_V001.md`
Final lines: `REQUIRES = N (modality-tagged)`, `MATRIX = displayed`, `VERDICT = FORCED / UNDERDETERMINED / OVER-CONSTRAINED`, `FALSIFIER_STATUS = restated`, `VOID = clean`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN / (+items)`.
If the name exists, STOP. Seal, report hashes, write 765_DONE.md, STOP. No register, plan, tracker, git action.

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false. No member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities; no comparison to measured constants. No common cell formed; no junction map evaluated; no smooth data derived.
